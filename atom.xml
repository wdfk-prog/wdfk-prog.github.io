<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>wdfk-prog的个人博客</title>
  
  <subtitle>WORK-LIFE BALANCE.</subtitle>
  <link href="https://wdfk-prog.space/atom.xml" rel="self"/>
  
  <link href="https://wdfk-prog.space/"/>
  <updated>2026-07-30T06:34:49.937Z</updated>
  <id>https://wdfk-prog.space/</id>
  
  <author>
    <name>Liya Huang</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>Lely CANopen：RPDO&amp;TPDO 运行期收发、SYNC、事件与定时器机制原理和运行流程</title>
    <link href="https://wdfk-prog.space/posts/78c092c/"/>
    <id>https://wdfk-prog.space/posts/78c092c/</id>
    <published>2026-07-30T06:31:07.000Z</published>
    <updated>2026-07-30T06:34:49.937Z</updated>
    
    <content type="html"><![CDATA[<meta name="referrer" content="no-referrer" /><h1 id="Lely-CANopen：RPDO-TPDO-运行期收发、SYNC、事件与定时器机制原理和运行流程"><a href="#Lely-CANopen：RPDO-TPDO-运行期收发、SYNC、事件与定时器机制原理和运行流程" class="headerlink" title="Lely CANopen：RPDO&#x2F;TPDO 运行期收发、SYNC、事件与定时器机制原理和运行流程"></a>Lely CANopen：RPDO&#x2F;TPDO 运行期收发、SYNC、事件与定时器机制原理和运行流程</h1><p><img src="https://i-blog.csdnimg.cn/direct/19fe093c43414382aa54ff10ebf5fb51.png" alt="在这里插入图片描述"></p><p>@[toc]</p><blockquote><p>源码基线：<code>lely-industries/lely-core</code>，提交 <code>620d1858eb8520dbc3dc5e1a7314565becd54199</code>，查阅时间为 2026-07-30。</p><p>本文核心分析以下文件：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">include/lely/co/rpdo.h</span><br><span class="line">include/lely/co/rpdo.hpp</span><br><span class="line">src/co/rpdo.c</span><br><span class="line">include/lely/co/tpdo.h</span><br><span class="line">include/lely/co/tpdo.hpp</span><br><span class="line">src/co/tpdo.c</span><br></pre></td></tr></table></figure><p>前置文章已经说明 <code>co_pdo_dn()</code>、<code>co_pdo_up()</code>、PDO 映射校验、位级编解码以及对象字典 indication 桥接。本文不重复展开这些基础算法，而是集中分析 RPDO&#x2F;TPDO 服务对象如何注册 CAN 接收器、维护运行期参数、处理 SYNC、事件定时器、接收超时、同步窗口、抑制时间、RTR 和用户回调。</p><p>事实边界：函数、字段、条件分支和调用链来自上述源码；并发风险、生命周期约束、测试建议和部分设计评价属于基于源码行为的工程分析。CiA 301 语义参考 V4.2.0 第 7.2.2、7.5.2.35 至 7.5.2.38 节。</p></blockquote><h2 id="一句话结论"><a href="#一句话结论" class="headerlink" title="一句话结论"></a>一句话结论</h2><p><code>rpdo.c</code> 和 <code>tpdo.c</code> 是 Lely CANopen 中真正负责 PDO 运行期行为的服务层：</p><ul><li><code>rpdo.c</code> 注册数据帧接收器，按 transmission type 决定立即处理还是缓存到下一个 SYNC，并通过接收 deadline 和同步窗口定时器报告错误；</li><li><code>tpdo.c</code> 统一处理事件、周期定时器、SYNC、RTR、抑制时间和异步采样，再调用 <code>co_pdo_up()</code> 构造并发送 CAN 帧；</li><li>两者都把对象 <code>0x1400/0x1600</code> 或 <code>0x1800/0x1A00</code> 的运行值复制到服务内部缓存，并安装 download indication，使 SDO 重配置可以立即改变接收器、定时器和状态；</li><li><code>co_pdo_dn()</code> 与 <code>co_pdo_up()</code> 只负责对象字典和 payload 之间的转换，真正的 CAN 收发、触发条件和时间状态均由 RPDO&#x2F;TPDO 服务维护；</li><li>C++ 头文件没有重新实现协议，只通过 <code>incomplete_c_type</code>、类型 traits 和回调适配器包装 C 服务对象。</li></ul><p>最重要的运行模型是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><span class="line">RPDO CAN frame</span><br><span class="line">    ↓ CAN receiver</span><br><span class="line">co_rpdo_recv</span><br><span class="line">    ├─ event-driven → co_rpdo_read_frame immediately</span><br><span class="line">    └─ synchronous  → keep the last frame until next SYNC</span><br><span class="line">                           ↓</span><br><span class="line">                      co_rpdo_sync</span><br><span class="line">                           ↓</span><br><span class="line">                      co_pdo_dn</span><br><span class="line">                           ↓</span><br><span class="line">                    object dictionary</span><br><span class="line"></span><br><span class="line">TPDO trigger</span><br><span class="line">    ├─ application event</span><br><span class="line">    ├─ event timer</span><br><span class="line">    ├─ SYNC</span><br><span class="line">    └─ RTR</span><br><span class="line">         ↓</span><br><span class="line">    optional sampling callback</span><br><span class="line">         ↓</span><br><span class="line">    co_tpdo_sample_res</span><br><span class="line">         ↓</span><br><span class="line">    co_tpdo_init_frame</span><br><span class="line">         ↓</span><br><span class="line">    co_pdo_up</span><br><span class="line">         ↓</span><br><span class="line">    can_net_send</span><br></pre></td></tr></table></figure><hr><h2 id="1-六个文件的职责边界"><a href="#1-六个文件的职责边界" class="headerlink" title="1. 六个文件的职责边界"></a>1. 六个文件的职责边界</h2><h3 id="1-1-include-lely-co-rpdo-h"><a href="#1-1-include-lely-co-rpdo-h" class="headerlink" title="1.1 include/lely/co/rpdo.h"></a>1.1 <code>include/lely/co/rpdo.h</code></h3><p>该头文件公开 Receive-PDO 服务的 C API：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">create / destroy</span><br><span class="line">start / stop / is_stopped</span><br><span class="line">network, device and PDO number accessors</span><br><span class="line">cached communication and mapping parameter accessors</span><br><span class="line">receive indication callback</span><br><span class="line">error callback</span><br><span class="line">SYNC actuation entry</span><br><span class="line">RTR request entry</span><br></pre></td></tr></table></figure><p>两个回调承担不同职责：</p><ul><li><code>co_rpdo_ind_t</code>：某个 RPDO payload 被解释后调用，携带 SDO abort code 和原始接收字节；</li><li><code>co_rpdo_err_t</code>：接收 deadline、payload 过短、payload 过长或 MPDO 目标错误时调用，携带 EMCY error code 和 error register。</li></ul><h3 id="1-2-src-co-rpdo-c"><a href="#1-2-src-co-rpdo-c" class="headerlink" title="1.2 src/co/rpdo.c"></a>1.2 <code>src/co/rpdo.c</code></h3><p>该文件实现：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">RPDO service lifetime</span><br><span class="line">CAN data-frame receiver registration</span><br><span class="line">communication and mapping parameter interception</span><br><span class="line">synchronous RPDO buffering</span><br><span class="line">SYNC-window filtering</span><br><span class="line">receive-deadline monitoring</span><br><span class="line">payload decoding and OD update</span><br><span class="line">length and MPDO error reporting</span><br><span class="line">RTR transmission request</span><br></pre></td></tr></table></figure><h3 id="1-3-include-lely-co-tpdo-h"><a href="#1-3-include-lely-co-tpdo-h" class="headerlink" title="1.3 include/lely/co/tpdo.h"></a>1.3 <code>include/lely/co/tpdo.h</code></h3><p>该头文件公开 Transmit-PDO 服务的 C API：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">create / destroy</span><br><span class="line">start / stop / is_stopped</span><br><span class="line">network, device and PDO number accessors</span><br><span class="line">cached communication and mapping parameter accessors</span><br><span class="line">send indication callback</span><br><span class="line">sampling indication callback</span><br><span class="line">event trigger</span><br><span class="line">SYNC trigger</span><br><span class="line">sampling completion response</span><br><span class="line">next inhibit deadline query</span><br><span class="line">DAM-MPDO and SAM-MPDO event functions</span><br></pre></td></tr></table></figure><h3 id="1-4-src-co-tpdo-c"><a href="#1-4-src-co-tpdo-c" class="headerlink" title="1.4 src/co/tpdo.c"></a>1.4 <code>src/co/tpdo.c</code></h3><p>该文件实现：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">TPDO service lifetime</span><br><span class="line">RTR receiver registration</span><br><span class="line">event timer</span><br><span class="line">synchronous window timer</span><br><span class="line">SYNC start and cycle counting</span><br><span class="line">application event processing</span><br><span class="line">inhibit-time enforcement</span><br><span class="line">synchronous and RTR sampling</span><br><span class="line">CAN frame construction and transmission</span><br><span class="line">DAM/SAM-MPDO frame construction</span><br><span class="line">communication and mapping parameter interception</span><br></pre></td></tr></table></figure><h3 id="1-5-include-lely-co-rpdo-hpp"><a href="#1-5-include-lely-co-rpdo-hpp" class="headerlink" title="1.5 include/lely/co/rpdo.hpp"></a>1.5 <code>include/lely/co/rpdo.hpp</code></h3><p><code>CORPDO</code> 是 C++ 包装层，提供：</p><ul><li>C 对象的分配、初始化、终结和释放 traits；</li><li><code>start()</code>、<code>stop()</code>、<code>sync()</code>、<code>rtr()</code> 等薄包装；</li><li>普通函数对象和成员函数到 C callback 的适配；</li><li>communication&#x2F;mapping 参数的引用式访问。</li></ul><h3 id="1-6-include-lely-co-tpdo-hpp"><a href="#1-6-include-lely-co-tpdo-hpp" class="headerlink" title="1.6 include/lely/co/tpdo.hpp"></a>1.6 <code>include/lely/co/tpdo.hpp</code></h3><p><code>COTPDO</code> 同样是薄包装，但当前提交中暴露的接口少于 C API：</p><ul><li>有 <code>event()</code>、<code>sync()</code>、<code>getNext()</code> 和普通 indication；</li><li>没有直接包装 <code>co_tpdo_set_sample_ind()</code>、<code>co_tpdo_sample_res()</code>；</li><li>没有直接包装 DAM&#x2F;SAM-MPDO 事件函数；</li><li>没有直接包装 <code>co_tpdo_is_stopped()</code>。</li></ul><p>需要这些能力时，C++ 代码仍可直接调用对应 C API。</p><hr><h2 id="2-RPDO-TPDO-在-Lely-分层中的位置"><a href="#2-RPDO-TPDO-在-Lely-分层中的位置" class="headerlink" title="2. RPDO&#x2F;TPDO 在 Lely 分层中的位置"></a>2. RPDO&#x2F;TPDO 在 Lely 分层中的位置</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[&quot;CAN network and timers&quot;] --&gt; B[&quot;rpdo.c / tpdo.c runtime service&quot;]</span><br><span class="line">    B --&gt; C[&quot;pdo.c mapping and OD bridge&quot;]</span><br><span class="line">    C --&gt; D[&quot;co_sub_dn_ind / co_sub_up_ind&quot;]</span><br><span class="line">    D --&gt; E[&quot;object dictionary values and application callbacks&quot;]</span><br><span class="line"></span><br><span class="line">    F[&quot;SDO writes 0x1400/0x1600/0x1800/0x1A00&quot;] --&gt; G[&quot;parameter download indications&quot;]</span><br><span class="line">    G --&gt; B</span><br></pre></td></tr></table></figure><p>职责边界可以概括为：</p><table><thead><tr><th>层级</th><th>负责内容</th></tr></thead><tbody><tr><td><code>can_net_t</code>、<code>can_recv_t</code>、<code>can_timer_t</code></td><td>CAN 帧和时间事件</td></tr><tr><td><code>co_rpdo_t</code>、<code>co_tpdo_t</code></td><td>PDO 运行期状态、触发条件、接收和发送</td></tr><tr><td><code>co_pdo_dn()</code>、<code>co_pdo_up()</code></td><td>PDO payload 与对象字典之间的位级转换</td></tr><tr><td><code>co_sub_dn_ind()</code>、<code>co_sub_up_ind()</code></td><td>对象访问权限、值转换和应用副作用</td></tr></tbody></table><p>因此：</p><blockquote><p><code>rpdo.c</code>&#x2F;<code>tpdo.c</code> 决定“何时处理”；<code>pdo.c</code> 决定“如何映射”；对象字典 indication 决定“是否允许以及处理后产生什么应用行为”。</p></blockquote><hr><h2 id="3-CiA-301-transmission-type-与源码分支"><a href="#3-CiA-301-transmission-type-与源码分支" class="headerlink" title="3. CiA 301 transmission type 与源码分支"></a>3. CiA 301 transmission type 与源码分支</h2><h3 id="3-1-RPDO-transmission-type"><a href="#3-1-RPDO-transmission-type" class="headerlink" title="3.1 RPDO transmission type"></a>3.1 RPDO transmission type</h3><table><thead><tr><th align="right">值</th><th>CiA 301 语义</th><th><code>rpdo.c</code> 行为</th></tr></thead><tbody><tr><td align="right"><code>0x00..0xF0</code></td><td>同步 RPDO</td><td>收帧后缓存，下一次 <code>co_rpdo_sync()</code> 才写入对象字典</td></tr><tr><td align="right"><code>0xF1..0xFD</code></td><td>保留</td><td><code>co_1400_dn_ind()</code> 拒绝配置</td></tr><tr><td align="right"><code>0xFE</code></td><td>事件驱动，制造商定义</td><td>收帧后立即处理</td></tr><tr><td align="right"><code>0xFF</code></td><td>事件驱动，设备协议或应用协议定义</td><td>收帧后立即处理</td></tr></tbody></table><p>Lely 对同步 RPDO 的 <code>0x00..0xF0</code> 没有进一步区分倍率。消费者收到同步 RPDO 后，只在下一次 SYNC 到来时执行最后收到的数据。</p><h3 id="3-2-TPDO-transmission-type"><a href="#3-2-TPDO-transmission-type" class="headerlink" title="3.2 TPDO transmission type"></a>3.2 TPDO transmission type</h3><table><thead><tr><th align="right">值</th><th>CiA 301 语义</th><th><code>tpdo.c</code> 行为</th></tr></thead><tbody><tr><td align="right"><code>0x00</code></td><td>同步、非周期</td><td><code>co_tpdo_event()</code> 置位事件，下一次 SYNC 采样并发送</td></tr><tr><td align="right"><code>0x01..0xF0</code></td><td>同步、周期</td><td>每收到指定数量的 SYNC 后采样并发送</td></tr><tr><td align="right"><code>0xF1..0xFB</code></td><td>保留</td><td><code>co_1800_dn_ind()</code> 拒绝配置</td></tr><tr><td align="right"><code>0xFC</code></td><td>同步 RTR-only</td><td>SYNC 时采样并缓存，收到 RTR 后发送缓存帧</td></tr><tr><td align="right"><code>0xFD</code></td><td>事件驱动 RTR-only</td><td>收到 RTR 后启动采样并发送响应帧</td></tr><tr><td align="right"><code>0xFE</code></td><td>事件驱动，制造商定义</td><td>事件或 event timer 触发，受 inhibit time 限制</td></tr><tr><td align="right"><code>0xFF</code></td><td>事件驱动，设备协议或应用协议定义</td><td>与 <code>0xFE</code> 使用相同运行路径</td></tr></tbody></table><h3 id="3-3-为什么-0xFE-和-0xFF-在源码中几乎相同"><a href="#3-3-为什么-0xFE-和-0xFF-在源码中几乎相同" class="headerlink" title="3.3 为什么 0xFE 和 0xFF 在源码中几乎相同"></a>3.3 为什么 <code>0xFE</code> 和 <code>0xFF</code> 在源码中几乎相同</h3><p>二者的区别主要属于配置来源和协议语义：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">0xFE    manufacturer-specific event</span><br><span class="line">0xFF    device-profile/application-profile event</span><br></pre></td></tr></table></figure><p><code>tpdo.c</code> 和 <code>rpdo.c</code> 的运行路径不需要知道事件由哪种规范定义，因此将它们按相同的 event-driven 分支处理。</p><hr><h2 id="4-struct-co-rpdo：RPDO-运行期状态"><a href="#4-struct-co-rpdo：RPDO-运行期状态" class="headerlink" title="4. struct __co_rpdo：RPDO 运行期状态"></a>4. <code>struct __co_rpdo</code>：RPDO 运行期状态</h2><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> __<span class="title">co_rpdo</span> &#123;</span></span><br><span class="line">    <span class="type">can_net_t</span> *net;</span><br><span class="line">    <span class="type">co_dev_t</span> *dev;</span><br><span class="line">    <span class="type">co_unsigned16_t</span> num;</span><br><span class="line">    <span class="type">int</span> stopped;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">co_pdo_comm_par</span> <span class="title">comm</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">co_pdo_map_par</span> <span class="title">map</span>;</span></span><br><span class="line">    <span class="type">can_recv_t</span> *recv;</span><br><span class="line">    <span class="type">can_timer_t</span> *timer_event;</span><br><span class="line">    <span class="type">can_timer_t</span> *timer_swnd;</span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span> sync : <span class="number">1</span>;</span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span> swnd : <span class="number">1</span>;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">can_msg</span> <span class="title">msg</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">co_sdo_req</span> <span class="title">req</span>;</span></span><br><span class="line">    <span class="type">co_rpdo_ind_t</span> *ind;</span><br><span class="line">    <span class="type">void</span> *ind_data;</span><br><span class="line">    <span class="type">co_rpdo_err_t</span> *err;</span><br><span class="line">    <span class="type">void</span> *err_data;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><h3 id="4-1-字段分类"><a href="#4-1-字段分类" class="headerlink" title="4.1 字段分类"></a>4.1 字段分类</h3><table><thead><tr><th>分类</th><th>字段</th><th>作用</th></tr></thead><tbody><tr><td>外部依赖</td><td><code>net</code>、<code>dev</code>、<code>num</code></td><td>CAN 网络、对象字典和 PDO 编号</td></tr><tr><td>生命周期</td><td><code>stopped</code></td><td>服务是否停止</td></tr><tr><td>参数缓存</td><td><code>comm</code>、<code>map</code></td><td>运行期使用的通信参数和映射参数副本</td></tr><tr><td>CAN 资源</td><td><code>recv</code></td><td>RPDO 数据帧接收器</td></tr><tr><td>时间资源</td><td><code>timer_event</code></td><td>RPDO 接收 deadline</td></tr><tr><td>时间资源</td><td><code>timer_swnd</code></td><td>同步窗口结束时间</td></tr><tr><td>同步状态</td><td><code>sync</code></td><td>是否存在等待下一个 SYNC 的缓存帧</td></tr><tr><td>同步状态</td><td><code>swnd</code></td><td>当前同步窗口是否已经过期</td></tr><tr><td>数据缓存</td><td><code>msg</code></td><td>等待下一个 SYNC 的最后一帧 RPDO</td></tr><tr><td>OD 桥接</td><td><code>req</code></td><td><code>co_pdo_dn()</code> 写对象字典时复用的请求对象</td></tr><tr><td>用户接口</td><td><code>ind</code>、<code>err</code></td><td>数据处理结果和通信错误回调</td></tr></tbody></table><h3 id="4-2-一个同步-RPDO-只有一帧缓存"><a href="#4-2-一个同步-RPDO-只有一帧缓存" class="headerlink" title="4.2 一个同步 RPDO 只有一帧缓存"></a>4.2 一个同步 RPDO 只有一帧缓存</h3><p><code>msg</code> 不是队列。同步窗口内连续收到多帧相同 RPDO 时：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">frame 1 → pdo-&gt;msg = frame 1</span><br><span class="line">frame 2 → pdo-&gt;msg = frame 2</span><br><span class="line">frame 3 → pdo-&gt;msg = frame 3</span><br><span class="line">next SYNC → process frame 3</span><br></pre></td></tr></table></figure><p>所以同步 RPDO 的语义是“在下一个 SYNC 使用最后收到的有效值”，不是积累并逐帧执行。</p><hr><h2 id="5-struct-co-tpdo：TPDO-运行期状态"><a href="#5-struct-co-tpdo：TPDO-运行期状态" class="headerlink" title="5. struct __co_tpdo：TPDO 运行期状态"></a>5. <code>struct __co_tpdo</code>：TPDO 运行期状态</h2><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> __<span class="title">co_tpdo</span> &#123;</span></span><br><span class="line">    <span class="type">can_net_t</span> *net;</span><br><span class="line">    <span class="type">co_dev_t</span> *dev;</span><br><span class="line">    <span class="type">co_unsigned16_t</span> num;</span><br><span class="line">    <span class="type">int</span> stopped;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">co_pdo_comm_par</span> <span class="title">comm</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">co_pdo_map_par</span> <span class="title">map</span>;</span></span><br><span class="line">    <span class="type">can_recv_t</span> *recv;</span><br><span class="line">    <span class="type">can_timer_t</span> *timer_event;</span><br><span class="line">    <span class="type">can_timer_t</span> *timer_swnd;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">can_msg</span> <span class="title">msg</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">timespec</span> <span class="title">inhibit</span>;</span></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span> event : <span class="number">1</span>;</span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span> swnd : <span class="number">1</span>;</span><br><span class="line">    <span class="type">co_unsigned8_t</span> sync;</span><br><span class="line">    <span class="type">co_unsigned8_t</span> cnt;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">co_sdo_req</span> <span class="title">req</span>;</span></span><br><span class="line">    <span class="type">co_tpdo_ind_t</span> *ind;</span><br><span class="line">    <span class="type">void</span> *data;</span><br><span class="line">    <span class="type">co_tpdo_sample_ind_t</span> *sample_ind;</span><br><span class="line">    <span class="type">void</span> *sample_data;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><h3 id="5-1-字段分类"><a href="#5-1-字段分类" class="headerlink" title="5.1 字段分类"></a>5.1 字段分类</h3><table><thead><tr><th>分类</th><th>字段</th><th>作用</th></tr></thead><tbody><tr><td>参数缓存</td><td><code>comm</code>、<code>map</code></td><td>运行期通信和映射配置</td></tr><tr><td>RTR 接收</td><td><code>recv</code></td><td>只接收与本 TPDO COB-ID 匹配的 RTR</td></tr><tr><td>定时器</td><td><code>timer_event</code></td><td>event-driven TPDO 周期触发</td></tr><tr><td>定时器</td><td><code>timer_swnd</code></td><td>同步窗口结束时间</td></tr><tr><td>帧缓存</td><td><code>msg</code></td><td>待发送帧或 RTR-only 缓存帧</td></tr><tr><td>抑制状态</td><td><code>inhibit</code></td><td>下一次 event-driven TPDO 最早允许发送时间</td></tr><tr><td>事件状态</td><td><code>event</code></td><td><code>trans=0</code> 时等待下一个 SYNC 的事件标志</td></tr><tr><td>同步状态</td><td><code>swnd</code></td><td>同步窗口是否过期</td></tr><tr><td>同步状态</td><td><code>sync</code></td><td>尚未达到的 SYNC start value</td></tr><tr><td>同步状态</td><td><code>cnt</code></td><td><code>1..240</code> 周期倍率计数器</td></tr><tr><td>OD 桥接</td><td><code>req</code></td><td><code>co_pdo_up()</code> 采样对象时复用的请求对象</td></tr><tr><td>用户接口</td><td><code>ind</code></td><td>发送结果或错误回调</td></tr><tr><td>用户接口</td><td><code>sample_ind</code></td><td>SYNC&#x2F;RTR 触发后的采样入口</td></tr></tbody></table><h3 id="5-2-inhibit-保存的是绝对时间"><a href="#5-2-inhibit-保存的是绝对时间" class="headerlink" title="5.2 inhibit 保存的是绝对时间"></a>5.2 <code>inhibit</code> 保存的是绝对时间</h3><p><code>inhibit</code> 不是剩余微秒数，而是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">next earliest allowed event-driven transmission time</span><br></pre></td></tr></table></figure><p>发送成功后：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">inhibit = send_time + communication_parameter.inhibit * 100 us</span><br></pre></td></tr></table></figure><p><code>co_tpdo_get_next()</code> 直接返回这个绝对时间点。</p><hr><h2 id="6-服务对象生命周期"><a href="#6-服务对象生命周期" class="headerlink" title="6. 服务对象生命周期"></a>6. 服务对象生命周期</h2><h3 id="6-1-创建即启动"><a href="#6-1-创建即启动" class="headerlink" title="6.1 创建即启动"></a>6.1 创建即启动</h3><p><code>co_rpdo_create()</code> 和 <code>co_tpdo_create()</code> 的公共行为为：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[&quot;validate PDO number&quot;] --&gt; B[&quot;find communication object&quot;]</span><br><span class="line">    B --&gt; C[&quot;find mapping object&quot;]</span><br><span class="line">    C --&gt; D[&quot;allocate CAN receiver and timers&quot;]</span><br><span class="line">    D --&gt; E[&quot;initialize co_sdo_req&quot;]</span><br><span class="line">    E --&gt; F[&quot;start service&quot;]</span><br><span class="line">    F --&gt; G[&quot;return active service&quot;]</span><br></pre></td></tr></table></figure><p>必备对象：</p><table><thead><tr><th>服务</th><th>通信参数</th><th>映射参数</th></tr></thead><tbody><tr><td>RPDO <code>num</code></td><td><code>0x1400 + num - 1</code></td><td><code>0x1600 + num - 1</code></td></tr><tr><td>TPDO <code>num</code></td><td><code>0x1800 + num - 1</code></td><td><code>0x1A00 + num - 1</code></td></tr></tbody></table><p>若任一对象不存在，创建失败。</p><h3 id="6-2-start-做的事情"><a href="#6-2-start-做的事情" class="headerlink" title="6.2 start() 做的事情"></a>6.2 <code>start()</code> 做的事情</h3><p>RPDO 和 TPDO 的 <code>start()</code> 都会：</p><ol><li>从对象字典复制 communication parameter；</li><li>从对象字典复制 mapping parameter；</li><li>在参数对象上安装 object-level download indication；</li><li>重置服务内部状态；</li><li>根据有效位、CAN-ID 和 transmission type 注册接收器或启动定时器；</li><li>清除 <code>stopped</code>。</li></ol><h3 id="6-3-stop-做的事情"><a href="#6-3-stop-做的事情" class="headerlink" title="6.3 stop() 做的事情"></a>6.3 <code>stop()</code> 做的事情</h3><p>两类服务都会：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">stop synchronous-window timer</span><br><span class="line">stop event/deadline timer</span><br><span class="line">stop CAN receiver</span><br><span class="line">remove mapping-object download indication</span><br><span class="line">remove communication-object download indication</span><br><span class="line">set stopped = 1</span><br></pre></td></tr></table></figure><p><code>start()</code> 和 <code>stop()</code> 都是幂等式接口：</p><ul><li>已启动时再次 <code>start()</code> 返回成功；</li><li>已停止时再次 <code>stop()</code> 直接返回。</li></ul><h3 id="6-4-生命周期依赖"><a href="#6-4-生命周期依赖" class="headerlink" title="6.4 生命周期依赖"></a>6.4 生命周期依赖</h3><p>服务对象内部只保存 <code>net</code> 和 <code>dev</code> 指针，不拥有它们。因此必须保证：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">can_net_t lifetime &gt; co_rpdo_t / co_tpdo_t lifetime</span><br><span class="line">co_dev_t lifetime &gt; co_rpdo_t / co_tpdo_t lifetime</span><br></pre></td></tr></table></figure><h3 id="6-5-同一-PDO-编号不要创建两个服务实例"><a href="#6-5-同一-PDO-编号不要创建两个服务实例" class="headerlink" title="6.5 同一 PDO 编号不要创建两个服务实例"></a>6.5 同一 PDO 编号不要创建两个服务实例</h3><p>每个实例都会：</p><ul><li>安装同一参数对象的 object-level indication；</li><li>注册相同 CAN-ID 的 receiver；</li><li>独立维护定时器和参数缓存。</li></ul><p>两个实例会互相覆盖对象回调，并可能重复接收或发送。一个 PDO 编号应只存在一个运行期服务对象。</p><hr><h2 id="7-参数缓存与对象字典回调"><a href="#7-参数缓存与对象字典回调" class="headerlink" title="7. 参数缓存与对象字典回调"></a>7. 参数缓存与对象字典回调</h2><h3 id="7-1-为什么服务要复制参数"><a href="#7-1-为什么服务要复制参数" class="headerlink" title="7.1 为什么服务要复制参数"></a>7.1 为什么服务要复制参数</h3><p>每次收发都查询对象字典会增加查找和类型转换开销。Lely 在启动时将参数复制为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">pdo-&gt;comm</span><br><span class="line">pdo-&gt;map</span><br></pre></td></tr></table></figure><p>运行期快速路径直接读取缓存。</p><h3 id="7-2-参数更新不能只改对象值"><a href="#7-2-参数更新不能只改对象值" class="headerlink" title="7.2 参数更新不能只改对象值"></a>7.2 参数更新不能只改对象值</h3><p>启动后，参数对象上的 download indication 负责同时更新：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">object dictionary current value</span><br><span class="line">service cached parameter</span><br><span class="line">CAN receiver registration</span><br><span class="line">event timer</span><br><span class="line">synchronous-window state</span><br><span class="line">inhibit and SYNC counters</span><br></pre></td></tr></table></figure><p>因此外部配置应使用：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">remote SDO download</span><br><span class="line">co_sub_dn_ind()</span><br><span class="line">co_sub_dn_ind_val()</span><br><span class="line">co_dev_cfg_rpdo() / co_dev_cfg_tpdo()</span><br></pre></td></tr></table></figure><p>不要绕过 indication 直接修改 current value，否则对象字典与 <code>pdo-&gt;comm</code>、<code>pdo-&gt;map</code> 可能不一致。</p><h3 id="7-3-服务会占用整个参数对象的-download-indication"><a href="#7-3-服务会占用整个参数对象的-download-indication" class="headerlink" title="7.3 服务会占用整个参数对象的 download indication"></a>7.3 服务会占用整个参数对象的 download indication</h3><p>源码使用：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">co_obj_set_dn_ind(obj, callback, pdo);</span><br></pre></td></tr></table></figure><p>这不是只针对单个子索引，而是整个参数 RECORD。启动服务会覆盖该对象此前安装的 object-level download indication；停止时直接清空，不会自动恢复旧 callback。</p><p>如果应用需要叠加自定义逻辑，应在服务回调之外设计明确的转发层，不要让多个模块分别争用同一 object-level indication。</p><hr><h2 id="8-RPDO-启动后的-receiver-注册"><a href="#8-RPDO-启动后的-receiver-注册" class="headerlink" title="8. RPDO 启动后的 receiver 注册"></a>8. RPDO 启动后的 receiver 注册</h2><p><code>co_rpdo_init_recv()</code> 的规则很直接：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">COB-ID valid bit = 0</span><br><span class="line">    → extract 11-bit or 29-bit CAN-ID</span><br><span class="line">    → register data-frame receiver</span><br><span class="line"></span><br><span class="line">COB-ID valid bit = 1</span><br><span class="line">    → stop receiver</span><br></pre></td></tr></table></figure><p>接收器的服务契约为经典 CAN PDO：</p><ul><li>不接收 RTR frame；</li><li>不接收其他 CAN-ID；</li><li>debug 构建会断言 EDL 未置位，payload 处理仍以经典 CAN 的 8 字节上限为边界。</li></ul><p><code>rpdo.c</code> 本身不检查 NMT state。服务是否应在 Pre-operational、Operational 或 Stopped 状态运行，需要由更高层生命周期控制。</p><hr><h2 id="9-RPDO-完整收帧流程"><a href="#9-RPDO-完整收帧流程" class="headerlink" title="9. RPDO 完整收帧流程"></a>9. RPDO 完整收帧流程</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant Net as CAN Network</span><br><span class="line">    participant Recv as RPDO Receiver</span><br><span class="line">    participant Service as RPDO Service</span><br><span class="line">    participant Codec as PDO Decoder</span><br><span class="line">    participant OD as Object Dictionary</span><br><span class="line">    participant App as Application</span><br><span class="line"></span><br><span class="line">    Net-&gt;&gt;Recv: matching data frame</span><br><span class="line">    Recv-&gt;&gt;Service: co_rpdo_recv</span><br><span class="line">    Service-&gt;&gt;Service: reset receive deadline timer</span><br><span class="line">    alt synchronous type 0x00..0xF0</span><br><span class="line">        Service-&gt;&gt;Service: save last frame and set sync flag</span><br><span class="line">    else event-driven type 0xFE or 0xFF</span><br><span class="line">        Service-&gt;&gt;Codec: co_rpdo_read_frame</span><br><span class="line">        Codec-&gt;&gt;OD: co_pdo_dn</span><br><span class="line">        OD-&gt;&gt;App: mapped download indications</span><br><span class="line">        Service-&gt;&gt;App: receive indication and optional error callback</span><br><span class="line">    end</span><br></pre></td></tr></table></figure><h3 id="9-1-收帧第一步总是重置-deadline"><a href="#9-1-收帧第一步总是重置-deadline" class="headerlink" title="9.1 收帧第一步总是重置 deadline"></a>9.1 收帧第一步总是重置 deadline</h3><p><code>co_rpdo_recv()</code> 进入后首先调用：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">co_rpdo_init_timer_event(pdo);</span><br></pre></td></tr></table></figure><p>因此接收到匹配 CAN-ID 的数据帧就会刷新 deadline，即使该同步帧随后因为同步窗口已经关闭而不被缓存。</p><h3 id="9-2-event-driven-RPDO"><a href="#9-2-event-driven-RPDO" class="headerlink" title="9.2 event-driven RPDO"></a>9.2 event-driven RPDO</h3><p>当 <code>trans &gt;= 0xFE</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">receive frame</span><br><span class="line">    ↓</span><br><span class="line">co_rpdo_read_frame</span><br><span class="line">    ↓</span><br><span class="line">co_pdo_dn</span><br><span class="line">    ↓</span><br><span class="line">write mapped OD entries</span><br><span class="line">    ↓</span><br><span class="line">invoke co_rpdo_ind_t</span><br></pre></td></tr></table></figure><h3 id="9-3-synchronous-RPDO"><a href="#9-3-synchronous-RPDO" class="headerlink" title="9.3 synchronous RPDO"></a>9.3 synchronous RPDO</h3><p>当 <code>trans &lt;= 0xF0</code>：</p><ul><li>同步窗口未过期：复制整帧到 <code>pdo-&gt;msg</code>，设置 <code>pdo-&gt;sync=1</code>；</li><li>同步窗口已过期：不缓存，不写对象字典；</li><li>不论是否缓存，当前 receiver callback 返回时都没有立即执行应用对象。</li></ul><hr><h2 id="10-RPDO-与-SYNC-的执行顺序"><a href="#10-RPDO-与-SYNC-的执行顺序" class="headerlink" title="10. RPDO 与 SYNC 的执行顺序"></a>10. RPDO 与 SYNC 的执行顺序</h2><p><code>co_rpdo_sync()</code> 的流程为：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[&quot;validate SYNC counter&quot;] --&gt; B&#123;&quot;RPDO is synchronous&quot;&#125;</span><br><span class="line">    B --&gt;|no| C[&quot;return without action&quot;]</span><br><span class="line">    B --&gt;|yes| D[&quot;open a new synchronous window&quot;]</span><br><span class="line">    D --&gt; E[&quot;start 0x1007 window timer&quot;]</span><br><span class="line">    E --&gt; F&#123;&quot;buffered frame exists&quot;&#125;</span><br><span class="line">    F --&gt;|no| G[&quot;return&quot;]</span><br><span class="line">    F --&gt;|yes| H[&quot;clear pending flag&quot;]</span><br><span class="line">    H --&gt; I[&quot;decode and apply buffered frame&quot;]</span><br></pre></td></tr></table></figure><p>关键时序：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">SYNC k arrives</span><br><span class="line">    1. open window k</span><br><span class="line">    2. apply the last RPDO received in window k-1</span><br><span class="line"></span><br><span class="line">RPDO frames arrive during window k</span><br><span class="line">    keep only the latest frame</span><br><span class="line"></span><br><span class="line">SYNC k+1 arrives</span><br><span class="line">    apply the latest frame from window k</span><br></pre></td></tr></table></figure><p>这与 CiA 301 中“同步 RPDO 在下一次 SYNC 后生效”的消费者模型一致。</p><h3 id="10-1-RPDO-不使用-SYNC-counter-倍率"><a href="#10-1-RPDO-不使用-SYNC-counter-倍率" class="headerlink" title="10.1 RPDO 不使用 SYNC counter 倍率"></a>10.1 RPDO 不使用 SYNC counter 倍率</h3><p><code>co_rpdo_sync(pdo, cnt)</code> 只检查 <code>cnt &lt;= 240</code>，不会按 <code>comm.trans</code> 计算第几个 SYNC。同步 RPDO 的 <code>0..240</code> 在该实现中统一表示：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">last accepted frame becomes active on the next SYNC</span><br></pre></td></tr></table></figure><hr><h2 id="11-RPDO-同步窗口-0x1007"><a href="#11-RPDO-同步窗口-0x1007" class="headerlink" title="11. RPDO 同步窗口 0x1007"></a>11. RPDO 同步窗口 <code>0x1007</code></h2><h3 id="11-1-窗口启动"><a href="#11-1-窗口启动" class="headerlink" title="11.1 窗口启动"></a>11.1 窗口启动</h3><p>每次 <code>co_rpdo_sync()</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">swnd = 0</span><br><span class="line">read 0x1007:00</span><br><span class="line">if RPDO valid, synchronous and window != 0</span><br><span class="line">    deadline = now + window microseconds</span><br><span class="line">    start timer_swnd</span><br></pre></td></tr></table></figure><h3 id="11-2-窗口结束"><a href="#11-2-窗口结束" class="headerlink" title="11.2 窗口结束"></a>11.2 窗口结束</h3><p><code>co_rpdo_timer_swnd()</code> 只做一件事：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">pdo-&gt;swnd = <span class="number">1</span>;</span><br></pre></td></tr></table></figure><p>窗口结束后到下一次 SYNC 之前收到的同步 RPDO 不会进入缓存。</p><h3 id="11-3-0x1007-0-的语义"><a href="#11-3-0x1007-0-的语义" class="headerlink" title="11.3 0x1007 = 0 的语义"></a>11.3 <code>0x1007 = 0</code> 的语义</h3><p>源码不启动 window timer，因此 <code>swnd</code> 在下一次 SYNC 前不会自动变为 1，可理解为不设置有限同步窗口。</p><h3 id="11-4-首个-SYNC-之前"><a href="#11-4-首个-SYNC-之前" class="headerlink" title="11.4 首个 SYNC 之前"></a>11.4 首个 SYNC 之前</h3><p>服务启动时 RPDO 将 <code>swnd</code> 初始化为 0。若首个 SYNC 之前已经收到同步 RPDO，该帧可以被缓存，并在首个 <code>co_rpdo_sync()</code> 时处理。</p><hr><h2 id="12-RPDO-receive-deadline"><a href="#12-RPDO-receive-deadline" class="headerlink" title="12. RPDO receive deadline"></a>12. RPDO receive deadline</h2><h3 id="12-1-timer-event-在-RPDO-中不是发送事件定时器"><a href="#12-1-timer-event-在-RPDO-中不是发送事件定时器" class="headerlink" title="12.1 timer_event 在 RPDO 中不是发送事件定时器"></a>12.1 <code>timer_event</code> 在 RPDO 中不是发送事件定时器</h3><p>同名字段在 RPDO 与 TPDO 中含义不同：</p><table><thead><tr><th>服务</th><th><code>timer_event</code> 含义</th></tr></thead><tbody><tr><td>RPDO</td><td>接收 deadline：多长时间没有收到下一帧</td></tr><tr><td>TPDO</td><td>event timer：周期触发发送事件</td></tr></tbody></table><p>RPDO 的通信参数 sub-index 5 被用作接收 deadline，单位为 ms。</p><h3 id="12-2-何时启动"><a href="#12-2-何时启动" class="headerlink" title="12.2 何时启动"></a>12.2 何时启动</h3><p><code>co_rpdo_start()</code> 不启动 deadline timer。它只在收到一帧 RPDO 后调用：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">can_timer_timeout(timer_event, net, comm.event);</span><br></pre></td></tr></table></figure><p>因此：</p><blockquote><p>服务自启动以来从未收到过 RPDO 时，当前实现不会产生 deadline timeout；第一次收帧后才开始监控后续间隔。</p></blockquote><h3 id="12-3-超时结果"><a href="#12-3-超时结果" class="headerlink" title="12.3 超时结果"></a>12.3 超时结果</h3><p>定时器到期后调用：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">co_rpdo_err_t(pdo, 0x8250, 0x10, data)</span><br></pre></td></tr></table></figure><p>普通 <code>co_rpdo_ind_t</code> 不会因 deadline timeout 被调用。</p><h3 id="12-4-动态修改-deadline-的细节"><a href="#12-4-动态修改-deadline-的细节" class="headerlink" title="12.4 动态修改 deadline 的细节"></a>12.4 动态修改 deadline 的细节</h3><p><code>co_1400_dn_ind()</code> 修改 sub-index 5 时只更新 <code>pdo-&gt;comm.event</code>，不会立即重启当前正在运行的 timer。新值会在下一次 RPDO 收帧并重置 timer 时生效。</p><p>这与 TPDO 修改 event timer 后立即重启 timer 的行为不同。</p><hr><h2 id="13-co-rpdo-read-frame-：从-CAN-payload-到对象字典"><a href="#13-co-rpdo-read-frame-：从-CAN-payload-到对象字典" class="headerlink" title="13. co_rpdo_read_frame()：从 CAN payload 到对象字典"></a>13. <code>co_rpdo_read_frame()</code>：从 CAN payload 到对象字典</h2><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">co_unsigned32_t</span> ac = co_pdo_dn(</span><br><span class="line">        &amp;pdo-&gt;<span class="built_in">map</span>, pdo-&gt;dev, &amp;pdo-&gt;req, msg-&gt;data, n, <span class="number">1</span>);</span><br></pre></td></tr></table></figure><h3 id="13-1-返回值"><a href="#13-1-返回值" class="headerlink" title="13.1 返回值"></a>13.1 返回值</h3><p><code>ac</code> 是 SDO abort code，用于表达本地映射或对象访问错误，例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">CO_SDO_AC_NO_OBJ</span><br><span class="line">CO_SDO_AC_NO_SUB</span><br><span class="line">CO_SDO_AC_NO_WRITE</span><br><span class="line">CO_SDO_AC_NO_PDO</span><br><span class="line">CO_SDO_AC_PDO_LEN</span><br></pre></td></tr></table></figure><p>RPDO 没有网络确认帧，因此这些 abort code 只用于本地 indication、诊断和 EMCY 路径。</p><h3 id="13-2-indication-回调"><a href="#13-2-indication-回调" class="headerlink" title="13.2 indication 回调"></a>13.2 indication 回调</h3><p>无论 <code>co_pdo_dn()</code> 成功还是失败，只要进入 <code>co_rpdo_read_frame()</code>，都会调用：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ind(pdo, ac, msg-&gt;data, n, ind_data);</span><br></pre></td></tr></table></figure><p>应用可以同时观察：</p><ul><li>解析结果；</li><li>原始 payload；</li><li>payload 实际长度。</li></ul><h3 id="13-3-映射写入不是事务"><a href="#13-3-映射写入不是事务" class="headerlink" title="13.3 映射写入不是事务"></a>13.3 映射写入不是事务</h3><p><code>co_pdo_dn()</code> 按映射顺序逐项写对象字典。如果第 3 项失败，前两项可能已经更新。<code>co_rpdo_read_frame()</code> 不做回滚。</p><p>要求帧内对象原子一致时，应采用：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">RPDO writes shadow variables</span><br><span class="line">    ↓ all mapped entries succeed</span><br><span class="line">application commits shadow group atomically</span><br></pre></td></tr></table></figure><hr><h2 id="14-RPDO-长度和-EMCY-错误路径"><a href="#14-RPDO-长度和-EMCY-错误路径" class="headerlink" title="14. RPDO 长度和 EMCY 错误路径"></a>14. RPDO 长度和 EMCY 错误路径</h2><p><code>co_rpdo_err_t</code> 的实现调用条件为：</p><table><thead><tr><th>条件</th><th align="right">EMCY error code</th><th align="right">error register</th></tr></thead><tbody><tr><td>payload 比映射要求短</td><td align="right"><code>0x8210</code></td><td align="right"><code>0x10</code></td></tr><tr><td>payload 比映射长度长</td><td align="right"><code>0x8220</code></td><td align="right"><code>0x10</code></td></tr><tr><td>DAM-MPDO 目标对象不存在</td><td align="right"><code>0x8230</code></td><td align="right"><code>0x10</code></td></tr><tr><td>RPDO receive deadline timeout</td><td align="right"><code>0x8250</code></td><td align="right"><code>0x10</code></td></tr></tbody></table><h3 id="14-1-头文件与实现的细节差异"><a href="#14-1-头文件与实现的细节差异" class="headerlink" title="14.1 头文件与实现的细节差异"></a>14.1 头文件与实现的细节差异</h3><p><code>rpdo.h</code> 对 <code>co_rpdo_err_t</code> 的注释只列出 <code>0x8210</code>、<code>0x8220</code> 和 <code>0x8250</code>，但 <code>rpdo.c</code> 在启用 MPDO 时还可能报告 <code>0x8230</code>。</p><p>使用回调时不应把错误码枚举硬编码为头文件注释中的三个值。</p><h3 id="14-2-payload-过长不是-co-pdo-dn-的-abort"><a href="#14-2-payload-过长不是-co-pdo-dn-的-abort" class="headerlink" title="14.2 payload 过长不是 co_pdo_dn() 的 abort"></a>14.2 payload 过长不是 <code>co_pdo_dn()</code> 的 abort</h3><p><code>co_pdo_dn()</code> 能正常处理映射所需的前若干字节时可能返回成功。随后 <code>co_rpdo_read_frame()</code> 重新计算映射 bit 总数，如果：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">received bytes &gt; ceil(mapped bits / 8)</span><br></pre></td></tr></table></figure><p>再通过 <code>co_rpdo_err_t</code> 报告 <code>0x8220</code>。</p><hr><h2 id="15-co-rpdo-rtr-：主动请求远端-TPDO"><a href="#15-co-rpdo-rtr-：主动请求远端-TPDO" class="headerlink" title="15. co_rpdo_rtr()：主动请求远端 TPDO"></a>15. <code>co_rpdo_rtr()</code>：主动请求远端 TPDO</h2><p>该函数构造：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">CAN-ID = RPDO communication COB-ID 中的 CAN-ID</span><br><span class="line">flags  = RTR</span><br><span class="line">length = 0</span><br></pre></td></tr></table></figure><p>然后调用 <code>can_net_send()</code>。</p><p>这适用于本地 RPDO 消费者主动请求远端 TPDO 生产者。</p><p>源码只检查 PDO valid bit，没有根据 COB-ID bit 30 阻止发送 RTR。应用应根据对端 TPDO 的 RTR 能力和 transmission type 决定是否调用。</p><hr><h2 id="16-RPDO-communication-parameter-动态更新"><a href="#16-RPDO-communication-parameter-动态更新" class="headerlink" title="16. RPDO communication parameter 动态更新"></a>16. RPDO communication parameter 动态更新</h2><p><code>co_1400_dn_ind()</code> 对各子索引的处理：</p><table><thead><tr><th align="right">sub-index</th><th>参数</th><th>规则和副作用</th></tr></thead><tbody><tr><td align="right"><code>0</code></td><td>highest sub-index</td><td>不可写</td></tr><tr><td align="right"><code>1</code></td><td>COB-ID</td><td>有效状态下不能直接改变 CAN-ID；校验 11&#x2F;29-bit；重注册 receiver；清除同步缓存和窗口状态</td></tr><tr><td align="right"><code>2</code></td><td>transmission type</td><td>接受 <code>0..0xF0</code>、<code>0xFE</code>、<code>0xFF</code>；拒绝 <code>0xF1..0xFD</code></td></tr><tr><td align="right"><code>3</code></td><td>inhibit time</td><td>PDO 有效时不允许修改</td></tr><tr><td align="right"><code>5</code></td><td>event&#x2F;deadline time</td><td>更新缓存，不立即重启 deadline timer</td></tr><tr><td align="right">其他</td><td>—</td><td><code>CO_SDO_AC_NO_SUB</code></td></tr></tbody></table><h3 id="16-1-RPDO-inhibit-time-在该文件中没有接收限速作用"><a href="#16-1-RPDO-inhibit-time-在该文件中没有接收限速作用" class="headerlink" title="16.1 RPDO inhibit time 在该文件中没有接收限速作用"></a>16.1 RPDO inhibit time 在该文件中没有接收限速作用</h3><p><code>comm.inhibit</code> 会被加载、校验和保存，但 <code>co_rpdo_recv()</code>、<code>co_rpdo_sync()</code> 和 <code>co_rpdo_read_frame()</code> 都不读取它。</p><p>因此在这个实现中，RPDO inhibit time 不会丢弃高频接收帧，也不会限制对象更新速率。若设备协议赋予其制造商特定行为，需要在应用层另行实现。</p><hr><h2 id="17-RPDO-mapping-parameter-动态更新"><a href="#17-RPDO-mapping-parameter-动态更新" class="headerlink" title="17. RPDO mapping parameter 动态更新"></a>17. RPDO mapping parameter 动态更新</h2><p><code>co_1600_dn_ind()</code> 强制遵守以下顺序：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">1. set COB-ID invalid bit</span><br><span class="line">2. write sub-index 0 = 0</span><br><span class="line">3. modify mapping entries</span><br><span class="line">4. write sub-index 0 = mapping count</span><br><span class="line">5. clear COB-ID invalid bit</span><br></pre></td></tr></table></figure><h3 id="17-1-写-sub-index-0"><a href="#17-1-写-sub-index-0" class="headerlink" title="17.1 写 sub-index 0"></a>17.1 写 sub-index 0</h3><p>当写入映射数量时：</p><ul><li>PDO 必须无效；</li><li>普通 PDO 数量必须不大于 64；</li><li>总映射长度不得超过 64 bit；</li><li>每个对象必须存在、可写且允许 RPDO mapping；</li><li>启用 MPDO 时也接受 <code>0xFE</code> 或 <code>0xFF</code> 特殊映射类型。</li></ul><h3 id="17-2-写映射项"><a href="#17-2-写映射项" class="headerlink" title="17.2 写映射项"></a>17.2 写映射项</h3><p>写 <code>sub-index 1..64</code> 时：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">PDO must be invalid</span><br><span class="line">and</span><br><span class="line">cached mapping count must be 0</span><br></pre></td></tr></table></figure><p>非零 mapping entry 会立即调用 <code>co_dev_chk_rpdo()</code> 校验。</p><hr><h2 id="18-TPDO-启动后的资源配置"><a href="#18-TPDO-启动后的资源配置" class="headerlink" title="18. TPDO 启动后的资源配置"></a>18. TPDO 启动后的资源配置</h2><p><code>co_tpdo_start()</code> 在复制参数后初始化：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">inhibit = current CAN-network time</span><br><span class="line">event = 0</span><br><span class="line">swnd = 1</span><br><span class="line">sync = communication.sync</span><br><span class="line">cnt = 0</span><br></pre></td></tr></table></figure><p>然后：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">initialize RTR receiver</span><br><span class="line">initialize event timer</span><br></pre></td></tr></table></figure><h3 id="18-1-swnd-初值为什么为-1"><a href="#18-1-swnd-初值为什么为-1" class="headerlink" title="18.1 swnd 初值为什么为 1"></a>18.1 <code>swnd</code> 初值为什么为 1</h3><p>TPDO 的同步发送必须由 SYNC 打开同步窗口。因此启动后还未收到 SYNC 时：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">swnd = expired</span><br></pre></td></tr></table></figure><p>只有 <code>co_tpdo_sync()</code> 才会将其清零并启动窗口 timer。</p><h3 id="18-2-inhibit-初值"><a href="#18-2-inhibit-初值" class="headerlink" title="18.2 inhibit 初值"></a>18.2 <code>inhibit</code> 初值</h3><p>启动时设为当前时间，意味着 event-driven TPDO 在启动后可立即发送，不需要先等待一个 inhibit interval。</p><hr><h2 id="19-TPDO-RTR-receiver-注册"><a href="#19-TPDO-RTR-receiver-注册" class="headerlink" title="19. TPDO RTR receiver 注册"></a>19. TPDO RTR receiver 注册</h2><p><code>co_tpdo_init_recv()</code> 只有在以下条件同时满足时注册 receiver：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">PDO valid</span><br><span class="line">and</span><br><span class="line">CO_PDO_COBID_RTR bit is clear</span><br></pre></td></tr></table></figure><p>receiver 只匹配：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">configured CAN-ID</span><br><span class="line">CAN_FLAG_RTR</span><br><span class="line">optional CAN_FLAG_IDE</span><br></pre></td></tr></table></figure><p>因此 TPDO 的 <code>recv</code> 不是接收过程数据，而是接收消费者发来的 Remote Transmission Request。</p><hr><h2 id="20-co-tpdo-event-：应用事件入口"><a href="#20-co-tpdo-event-：应用事件入口" class="headerlink" title="20. co_tpdo_event()：应用事件入口"></a>20. <code>co_tpdo_event()</code>：应用事件入口</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[&quot;co_tpdo_event&quot;] --&gt; B&#123;&quot;PDO valid and normal PDO&quot;&#125;</span><br><span class="line">    B --&gt;|no| C[&quot;ignore and return success&quot;]</span><br><span class="line">    B --&gt;|yes| D&#123;&quot;transmission type&quot;&#125;</span><br><span class="line">    D --&gt;|0x00| E[&quot;set pending event for next SYNC&quot;]</span><br><span class="line">    D --&gt;|0xFD| F[&quot;refresh buffered frame without sending&quot;]</span><br><span class="line">    D --&gt;|0xFE or 0xFF| G[&quot;check inhibit deadline&quot;]</span><br><span class="line">    G --&gt; H[&quot;sample OD and build frame&quot;]</span><br><span class="line">    H --&gt; I[&quot;send immediately&quot;]</span><br><span class="line">    D --&gt;|other| J[&quot;ignore synchronous event&quot;]</span><br><span class="line">    E --&gt; K[&quot;reset event timer when applicable&quot;]</span><br><span class="line">    F --&gt; K</span><br><span class="line">    I --&gt; L[&quot;advance inhibit deadline&quot;]</span><br><span class="line">    L --&gt; K</span><br></pre></td></tr></table></figure><h3 id="20-1-trans-0x00"><a href="#20-1-trans-0x00" class="headerlink" title="20.1 trans=0x00"></a>20.1 <code>trans=0x00</code></h3><p>调用 <code>co_tpdo_event()</code> 只设置：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">pdo-&gt;event = <span class="number">1</span>;</span><br></pre></td></tr></table></figure><p>真正采样和发送发生在下一次 <code>co_tpdo_sync()</code>。</p><p>连续发生多个事件不会排队，只保留一个 pending bit。</p><h3 id="20-2-trans-0xFE-0xFF"><a href="#20-2-trans-0xFE-0xFF" class="headerlink" title="20.2 trans=0xFE/0xFF"></a>20.2 <code>trans=0xFE/0xFF</code></h3><p>处理顺序：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">check inhibit time</span><br><span class="line">    ↓</span><br><span class="line">co_tpdo_init_frame</span><br><span class="line">    ↓</span><br><span class="line">co_pdo_up</span><br><span class="line">    ↓</span><br><span class="line">can_net_send</span><br><span class="line">    ↓</span><br><span class="line">advance next inhibit deadline</span><br></pre></td></tr></table></figure><p>若抑制时间尚未结束：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">return -1</span><br><span class="line">set ERRNUM_AGAIN</span><br><span class="line">no send indication callback</span><br></pre></td></tr></table></figure><p>调用者必须检查返回值，而不能只依赖 <code>co_tpdo_ind_t</code>。</p><h3 id="20-3-trans-0xFD"><a href="#20-3-trans-0xFD" class="headerlink" title="20.3 trans=0xFD"></a>20.3 <code>trans=0xFD</code></h3><p>当前源码中 <code>co_tpdo_event()</code> 会调用 <code>co_tpdo_init_frame()</code> 更新 <code>pdo-&gt;msg</code>，但不主动发送。真正收到 RTR 时，<code>co_tpdo_recv()</code> 会再次启动 sampling path。</p><p>这是该提交的具体实现行为，不应把 <code>co_tpdo_event()</code> 理解为 <code>0xFD</code> 的立即发送接口。</p><hr><h2 id="21-TPDO-event-timer"><a href="#21-TPDO-event-timer" class="headerlink" title="21. TPDO event timer"></a>21. TPDO event timer</h2><h3 id="21-1-启动条件"><a href="#21-1-启动条件" class="headerlink" title="21.1 启动条件"></a>21.1 启动条件</h3><p><code>co_tpdo_init_timer_event()</code> 仅在以下条件满足时启动：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">PDO valid</span><br><span class="line">trans &gt;= 0xFE</span><br><span class="line">event timer != 0</span><br></pre></td></tr></table></figure><p>因此 event timer 只作用于普通 event-driven TPDO：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">0xFE</span><br><span class="line">0xFF</span><br></pre></td></tr></table></figure><p>不会用于：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">synchronous TPDO</span><br><span class="line">0xFC RTR-only</span><br><span class="line">0xFD RTR-only</span><br><span class="line">MPDO-specific event API</span><br></pre></td></tr></table></figure><h3 id="21-2-到期流程"><a href="#21-2-到期流程" class="headerlink" title="21.2 到期流程"></a>21.2 到期流程</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">timer_event expires</span><br><span class="line">    ↓</span><br><span class="line">co_tpdo_timer_event</span><br><span class="line">    ↓</span><br><span class="line">co_tpdo_event</span><br><span class="line">    ↓</span><br><span class="line">transmit or report error</span><br><span class="line">    ↓</span><br><span class="line">restart event timer</span><br></pre></td></tr></table></figure><p>如果本次发送失败，timer callback 仍会重新启动 event timer，并恢复进入 callback 前保存的 error code。</p><h3 id="21-3-event-timer-是周期起点，不是精确硬实时发送点"><a href="#21-3-event-timer-是周期起点，不是精确硬实时发送点" class="headerlink" title="21.3 event timer 是周期起点，不是精确硬实时发送点"></a>21.3 event timer 是周期起点，不是精确硬实时发送点</h3><p>下一周期 timer 在处理完成后重新安排。若采样、对象回调或 CAN send 阻塞，实际周期会包含这些执行时间。</p><p>这是基于实现方式的工程结论。需要严格等周期时，应评估 <code>can_timer_timeout()</code> 的具体调度模型和 callback 执行耗时。</p><hr><h2 id="22-TPDO-inhibit-time"><a href="#22-TPDO-inhibit-time" class="headerlink" title="22. TPDO inhibit time"></a>22. TPDO inhibit time</h2><h3 id="22-1-单位"><a href="#22-1-单位" class="headerlink" title="22.1 单位"></a>22.1 单位</h3><p>通信参数中的 inhibit time 单位是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">100 microseconds</span><br></pre></td></tr></table></figure><p>例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">inhibit = 10</span><br><span class="line">minimum interval = 1000 us = 1 ms</span><br></pre></td></tr></table></figure><h3 id="22-2-只约束-event-driven-发送"><a href="#22-2-只约束-event-driven-发送" class="headerlink" title="22.2 只约束 event-driven 发送"></a>22.2 只约束 event-driven 发送</h3><p>当前实现中 inhibit time 用于：</p><ul><li><code>co_tpdo_event()</code> 的 <code>0xFE/0xFF</code>；</li><li><code>co_dam_mpdo_event()</code>；</li><li><code>co_sam_mpdo_event()</code>。</li></ul><p>它不约束：</p><ul><li>synchronous <code>0..0xF0</code>；</li><li>RTR-only <code>0xFC/0xFD</code>。</li></ul><h3 id="22-3-只在发送成功后推进-deadline"><a href="#22-3-只在发送成功后推进-deadline" class="headerlink" title="22.3 只在发送成功后推进 deadline"></a>22.3 只在发送成功后推进 deadline</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">send failed</span><br><span class="line">    → inhibit deadline not advanced</span><br><span class="line"></span><br><span class="line">send succeeded</span><br><span class="line">    → inhibit = previous send reference + inhibit * 100 us</span><br></pre></td></tr></table></figure><h3 id="22-4-co-tpdo-get-next"><a href="#22-4-co-tpdo-get-next" class="headerlink" title="22.4 co_tpdo_get_next()"></a>22.4 <code>co_tpdo_get_next()</code></h3><p>应用可查询下一次允许 event-driven 发送的绝对时间，用于：</p><ul><li>调度延迟发送；</li><li>避免反复得到 <code>ERRNUM_AGAIN</code>；</li><li>在上层 event coalescing 中合并多次触发。</li></ul><hr><h2 id="23-co-tpdo-sync-：SYNC-入口"><a href="#23-co-tpdo-sync-：SYNC-入口" class="headerlink" title="23. co_tpdo_sync()：SYNC 入口"></a>23. <code>co_tpdo_sync()</code>：SYNC 入口</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[&quot;co_tpdo_sync&quot;] --&gt; B[&quot;validate counter&quot;]</span><br><span class="line">    B --&gt; C&#123;&quot;valid and SYNC-driven&quot;&#125;</span><br><span class="line">    C --&gt;|no| D[&quot;ignore&quot;]</span><br><span class="line">    C --&gt;|yes| E&#123;&quot;wait for SYNC start value&quot;&#125;</span><br><span class="line">    E --&gt;|not reached| D</span><br><span class="line">    E --&gt;|reached or unused| F[&quot;open synchronous window&quot;]</span><br><span class="line">    F --&gt; G&#123;&quot;transmission type&quot;&#125;</span><br><span class="line">    G --&gt;|0x00| H&#123;&quot;pending event exists&quot;&#125;</span><br><span class="line">    H --&gt;|no| D</span><br><span class="line">    H --&gt;|yes| I[&quot;clear event&quot;]</span><br><span class="line">    G --&gt;|1..240| J&#123;&quot;cycle count reached&quot;&#125;</span><br><span class="line">    J --&gt;|no| D</span><br><span class="line">    J --&gt;|yes| K[&quot;reset cycle count&quot;]</span><br><span class="line">    G --&gt;|0xFC| L[&quot;sample every eligible SYNC&quot;]</span><br><span class="line">    I --&gt; M[&quot;invoke sampling indication&quot;]</span><br><span class="line">    K --&gt; M</span><br><span class="line">    L --&gt; M</span><br></pre></td></tr></table></figure><h3 id="23-1-SYNC-start-value"><a href="#23-1-SYNC-start-value" class="headerlink" title="23.1 SYNC start value"></a>23.1 SYNC start value</h3><p>服务启动时：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">pdo-&gt;sync = pdo-&gt;comm.sync;</span><br></pre></td></tr></table></figure><p>如果 <code>pdo-&gt;sync != 0</code> 且传入的 SYNC counter <code>cnt != 0</code>：</p><ul><li>counter 不等于 start value：忽略本次 SYNC；</li><li>等于 start value：清除等待状态并从该点开始计数。</li></ul><p>若调用者传入 <code>cnt=0</code>，源码不会执行 start-value 比较，等价于不使用 SYNC counter gate。</p><h3 id="23-2-transmission-type-1-240"><a href="#23-2-transmission-type-1-240" class="headerlink" title="23.2 transmission type 1..240"></a>23.2 transmission type <code>1..240</code></h3><p>内部 <code>pdo-&gt;cnt</code> 独立于 SYNC 帧中的 counter：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">receive SYNC → ++pdo-&gt;cnt</span><br><span class="line">pdo-&gt;cnt &lt; trans → no send</span><br><span class="line">pdo-&gt;cnt == trans → reset to 0 and sample</span><br></pre></td></tr></table></figure><h3 id="23-3-transmission-type-0"><a href="#23-3-transmission-type-0" class="headerlink" title="23.3 transmission type 0"></a>23.3 transmission type <code>0</code></h3><p>只有此前调用过 <code>co_tpdo_event()</code> 并设置 pending event，下一次 SYNC 才会采样。</p><h3 id="23-4-transmission-type-0xFC"><a href="#23-4-transmission-type-0xFC" class="headerlink" title="23.4 transmission type 0xFC"></a>23.4 transmission type <code>0xFC</code></h3><p>每次符合 start gate 的 SYNC 都触发采样。采样完成后不立即发送，而是缓存帧等待 RTR。</p><hr><h2 id="24-TPDO-同步窗口和异步采样"><a href="#24-TPDO-同步窗口和异步采样" class="headerlink" title="24. TPDO 同步窗口和异步采样"></a>24. TPDO 同步窗口和异步采样</h2><h3 id="24-1-SYNC-打开窗口"><a href="#24-1-SYNC-打开窗口" class="headerlink" title="24.1 SYNC 打开窗口"></a>24.1 SYNC 打开窗口</h3><p><code>co_tpdo_sync()</code> 在开始采样前：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">swnd = 0</span><br><span class="line">start timer at now + 0x1007 microseconds</span><br></pre></td></tr></table></figure><h3 id="24-2-sampling-indication"><a href="#24-2-sampling-indication" class="headerlink" title="24.2 sampling indication"></a>24.2 sampling indication</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="type">int</span> <span class="title function_">co_tpdo_sample_ind_t</span><span class="params">(<span class="type">co_tpdo_t</span> *pdo, <span class="type">void</span> *data)</span>;</span><br></pre></td></tr></table></figure><p>回调必须在采样完成后调用一次：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">co_tpdo_sample_res(pdo, abort_code);</span><br></pre></td></tr></table></figure><p>默认 callback 立即调用：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">co_tpdo_sample_res(pdo, <span class="number">0</span>);</span><br></pre></td></tr></table></figure><p>因此默认路径是同步采样。</p><h3 id="24-3-异步采样的用途"><a href="#24-3-异步采样的用途" class="headerlink" title="24.3 异步采样的用途"></a>24.3 异步采样的用途</h3><p>自定义 callback 可以：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">trigger ADC conversion</span><br><span class="line">request another task to snapshot data</span><br><span class="line">wait for hardware latch</span><br><span class="line">copy process image</span><br></pre></td></tr></table></figure><p>完成后再调用 <code>co_tpdo_sample_res()</code>。</p><h3 id="24-4-窗口超时"><a href="#24-4-窗口超时" class="headerlink" title="24.4 窗口超时"></a>24.4 窗口超时</h3><p>如果异步采样完成时：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">swnd == 1</span><br><span class="line">and</span><br><span class="line">trans != 0xFD</span><br></pre></td></tr></table></figure><p><code>co_tpdo_sample_res()</code> 将结果改为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">CO_SDO_AC_TIMEOUT</span><br></pre></td></tr></table></figure><p>随后：</p><ul><li>不发送 TPDO；</li><li>调用 <code>co_tpdo_ind_t</code>，<code>ptr=NULL</code>、<code>n=0</code>；</li><li>返回 0，因为该 abort 已通过 indication 报告。</li></ul><h3 id="24-5-并发采样风险"><a href="#24-5-并发采样风险" class="headerlink" title="24.5 并发采样风险"></a>24.5 并发采样风险</h3><p>服务结构没有显式的“sampling in progress”标志。若上层允许新的 SYNC 或 RTR 在上一次异步采样完成前再次触发，多个采样完成事件可能共用同一个 <code>pdo-&gt;req</code> 和 <code>pdo-&gt;msg</code>。</p><p>工程上应保证：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">one TPDO service has at most one outstanding sampling operation</span><br><span class="line">sample_res is invoked exactly once for each accepted sample indication</span><br></pre></td></tr></table></figure><hr><h2 id="25-co-tpdo-sample-res-：采样完成后的统一出口"><a href="#25-co-tpdo-sample-res-：采样完成后的统一出口" class="headerlink" title="25. co_tpdo_sample_res()：采样完成后的统一出口"></a>25. <code>co_tpdo_sample_res()</code>：采样完成后的统一出口</h2><p>处理步骤：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">validate mode</span><br><span class="line">    ↓</span><br><span class="line">reject MPDO path</span><br><span class="line">    ↓</span><br><span class="line">check synchronous-window timeout</span><br><span class="line">    ↓</span><br><span class="line">if application abort code != 0</span><br><span class="line">    invoke indication and stop</span><br><span class="line">    ↓</span><br><span class="line">co_tpdo_init_frame</span><br><span class="line">    ↓</span><br><span class="line">if trans == 0xFC</span><br><span class="line">    keep frame buffered and wait for RTR</span><br><span class="line">else</span><br><span class="line">    send frame now</span><br></pre></td></tr></table></figure><h3 id="25-1-应用提供的-abort-code"><a href="#25-1-应用提供的-abort-code" class="headerlink" title="25.1 应用提供的 abort code"></a>25.1 应用提供的 abort code</h3><p>应用可调用：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">co_tpdo_sample_res(pdo, CO_SDO_AC_DATA);</span><br></pre></td></tr></table></figure><p>表示采样失败。服务会通过 <code>co_tpdo_ind_t</code> 上报，但不会发送帧。</p><h3 id="25-2-0xFC-的特殊处理"><a href="#25-2-0xFC-的特殊处理" class="headerlink" title="25.2 0xFC 的特殊处理"></a>25.2 <code>0xFC</code> 的特殊处理</h3><p><code>co_tpdo_init_frame()</code> 已经把当前对象值编码到 <code>pdo-&gt;msg</code>，但函数直接返回，不调用 <code>can_net_send()</code>。后续 RTR 到来时发送这份缓存快照。</p><h3 id="25-3-0xFD-的特殊处理"><a href="#25-3-0xFD-的特殊处理" class="headerlink" title="25.3 0xFD 的特殊处理"></a>25.3 <code>0xFD</code> 的特殊处理</h3><p><code>0xFD</code> 不受同步窗口超时检查，收到 RTR 后采样完成即发送。</p><hr><h2 id="26-TPDO-RTR-处理"><a href="#26-TPDO-RTR-处理" class="headerlink" title="26. TPDO RTR 处理"></a>26. TPDO RTR 处理</h2><p><code>co_tpdo_recv()</code> 只接收 RTR frame，并按 transmission type 分支。</p><h3 id="26-1-0xFC：发送同步采样缓存"><a href="#26-1-0xFC：发送同步采样缓存" class="headerlink" title="26.1 0xFC：发送同步采样缓存"></a>26.1 <code>0xFC</code>：发送同步采样缓存</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">SYNC arrives</span><br><span class="line">    ↓</span><br><span class="line">sample current mapped values</span><br><span class="line">    ↓</span><br><span class="line">store complete CAN frame in pdo-&gt;msg</span><br><span class="line">    ↓</span><br><span class="line">RTR arrives</span><br><span class="line">    ↓</span><br><span class="line">send buffered frame without re-sampling</span><br></pre></td></tr></table></figure><p>若还没有有效缓存帧，RTR 被忽略。</p><h3 id="26-2-0xFD：RTR-到来后采样"><a href="#26-2-0xFD：RTR-到来后采样" class="headerlink" title="26.2 0xFD：RTR 到来后采样"></a>26.2 <code>0xFD</code>：RTR 到来后采样</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">RTR arrives</span><br><span class="line">    ↓</span><br><span class="line">invoke sample_ind</span><br><span class="line">    ↓</span><br><span class="line">sample_res</span><br><span class="line">    ↓</span><br><span class="line">co_pdo_up</span><br><span class="line">    ↓</span><br><span class="line">send response TPDO</span><br></pre></td></tr></table></figure><p>消费者得到的是 RTR 时刻触发的新采样，而不是上一个 SYNC 时刻的缓存。</p><h3 id="26-3-其他-transmission-type"><a href="#26-3-其他-transmission-type" class="headerlink" title="26.3 其他 transmission type"></a>26.3 其他 transmission type</h3><p>RTR receiver callback 返回 0，不处理该帧。</p><hr><h2 id="27-co-tpdo-init-frame-：构造-TPDO-帧"><a href="#27-co-tpdo-init-frame-：构造-TPDO-帧" class="headerlink" title="27. co_tpdo_init_frame()：构造 TPDO 帧"></a>27. <code>co_tpdo_init_frame()</code>：构造 TPDO 帧</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[&quot;clear CAN message&quot;] --&gt; B[&quot;extract 11-bit or 29-bit CAN-ID&quot;]</span><br><span class="line">    B --&gt; C[&quot;set IDE when needed&quot;]</span><br><span class="line">    C --&gt; D[&quot;set capacity to 8 bytes&quot;]</span><br><span class="line">    D --&gt; E[&quot;co_pdo_up&quot;]</span><br><span class="line">    E --&gt; F&#123;&quot;mapping and OD upload success&quot;&#125;</span><br><span class="line">    F --&gt;|no| G[&quot;invoke indication with abort code&quot;]</span><br><span class="line">    F --&gt;|yes| H[&quot;set actual CAN length&quot;]</span><br></pre></td></tr></table></figure><h3 id="27-1-co-pdo-up-的作用"><a href="#27-1-co-pdo-up-的作用" class="headerlink" title="27.1 co_pdo_up() 的作用"></a>27.1 <code>co_pdo_up()</code> 的作用</h3><p>它依次：</p><ol><li>解析 mapping entry；</li><li>调用映射对象的 upload indication；</li><li>将对象值按 bit length 打包到 CAN payload；</li><li>返回实际 payload 长度。</li></ol><h3 id="27-2-失败时没有部分帧发送"><a href="#27-2-失败时没有部分帧发送" class="headerlink" title="27.2 失败时没有部分帧发送"></a>27.2 失败时没有部分帧发送</h3><p>即使前几个映射对象已经读取并写入临时 payload，只要后续对象失败，<code>co_tpdo_init_frame()</code> 返回 -1，整帧不会发送。</p><p>但前面对象的 upload callback 副作用不会回滚。</p><hr><h2 id="28-co-tpdo-send-frame-：发送和-indication"><a href="#28-co-tpdo-send-frame-：发送和-indication" class="headerlink" title="28. co_tpdo_send_frame()：发送和 indication"></a>28. <code>co_tpdo_send_frame()</code>：发送和 indication</h2><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> result = can_net_send(pdo-&gt;net, msg);</span><br></pre></td></tr></table></figure><p>结果映射：</p><table><thead><tr><th>结果</th><th align="right">indication abort code</th><th>数据指针</th></tr></thead><tbody><tr><td>发送成功</td><td align="right"><code>0</code></td><td>已发送 payload</td></tr><tr><td>CAN I&#x2F;O 失败</td><td align="right"><code>CO_SDO_AC_ERROR</code></td><td><code>NULL</code></td></tr></tbody></table><p>需要注意：</p><ul><li>inhibit 尚未到期时函数根本不会进入 send path，因此不会调用 indication；</li><li>mapping error 在 <code>co_tpdo_init_frame()</code> 内调用 indication；</li><li>async sampling abort 和同步窗口 timeout 在 <code>co_tpdo_sample_res()</code> 内调用 indication；</li><li>application 必须同时检查 API 返回值和 indication。</li></ul><hr><h2 id="29-TPDO-communication-parameter-动态更新"><a href="#29-TPDO-communication-parameter-动态更新" class="headerlink" title="29. TPDO communication parameter 动态更新"></a>29. TPDO communication parameter 动态更新</h2><p><code>co_1800_dn_ind()</code> 对各子索引的处理：</p><table><thead><tr><th align="right">sub-index</th><th>参数</th><th>规则和副作用</th></tr></thead><tbody><tr><td align="right"><code>0</code></td><td>highest sub-index</td><td>不可写</td></tr><tr><td align="right"><code>1</code></td><td>COB-ID</td><td>有效状态下不能直接改 CAN-ID；校验 frame bit；重置 event&#x2F;window；重注册 RTR receiver；重启 event timer</td></tr><tr><td align="right"><code>2</code></td><td>transmission type</td><td>拒绝 <code>0xF1..0xFB</code>；<code>0xFC/0xFD</code> 要求允许 RTR；重注册 RTR receiver</td></tr><tr><td align="right"><code>3</code></td><td>inhibit time</td><td>PDO 有效时不允许修改</td></tr><tr><td align="right"><code>5</code></td><td>event timer</td><td>更新后立即重启 timer</td></tr><tr><td align="right"><code>6</code></td><td>SYNC start value</td><td>PDO 有效时不允许修改</td></tr><tr><td align="right">其他</td><td>—</td><td><code>CO_SDO_AC_NO_SUB</code></td></tr></tbody></table><h3 id="29-1-invalid-→-valid-的额外状态重置"><a href="#29-1-invalid-→-valid-的额外状态重置" class="headerlink" title="29.1 invalid → valid 的额外状态重置"></a>29.1 invalid → valid 的额外状态重置</h3><p>PDO 从禁用变为启用时：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">inhibit = current time</span><br><span class="line">event = 0</span><br><span class="line">sync = configured SYNC start value</span><br><span class="line">cnt = 0</span><br></pre></td></tr></table></figure><p>这样旧服务状态不会泄漏到重新启用后的 PDO。</p><h3 id="29-2-为什么改-CAN-ID-前必须禁用"><a href="#29-2-为什么改-CAN-ID-前必须禁用" class="headerlink" title="29.2 为什么改 CAN-ID 前必须禁用"></a>29.2 为什么改 CAN-ID 前必须禁用</h3><p>源码禁止：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">old valid = true</span><br><span class="line">new valid = true</span><br><span class="line">CAN-ID changes</span><br></pre></td></tr></table></figure><p>合法方式是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">set invalid bit</span><br><span class="line">change CAN-ID and parameters</span><br><span class="line">clear invalid bit</span><br></pre></td></tr></table></figure><p>否则运行中的 receiver、定时器和外部消费者可能在半配置状态下工作。</p><hr><h2 id="30-TPDO-mapping-parameter-动态更新"><a href="#30-TPDO-mapping-parameter-动态更新" class="headerlink" title="30. TPDO mapping parameter 动态更新"></a>30. TPDO mapping parameter 动态更新</h2><p><code>co_1a00_dn_ind()</code> 与 RPDO mapping 更新顺序相同，但使用：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">co_dev_chk_tpdo(dev, idx, subidx);</span><br></pre></td></tr></table></figure><p>检查目标对象：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">exists</span><br><span class="line">readable</span><br><span class="line">TPDO mappable</span><br><span class="line">mapping length total &lt;= 64 bits</span><br></pre></td></tr></table></figure><p>TPDO 不接受 RPDO dummy mapping 的特殊路径。</p><p>普通 mapping 和 MPDO mode 只能在 PDO invalid 时切换。</p><hr><h2 id="31-MPDO-运行路径"><a href="#31-MPDO-运行路径" class="headerlink" title="31. MPDO 运行路径"></a>31. MPDO 运行路径</h2><p>本文重点是普通 PDO，但 <code>tpdo.c</code> 和 <code>rpdo.c</code> 也包含 MPDO 入口。</p><h3 id="31-1-DAM-MPDO-发送"><a href="#31-1-DAM-MPDO-发送" class="headerlink" title="31.1 DAM-MPDO 发送"></a>31.1 DAM-MPDO 发送</h3><p><code>co_dam_mpdo_event()</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">validate PDO and DAM mode</span><br><span class="line">check Node-ID</span><br><span class="line">check inhibit time</span><br><span class="line">build fixed 8-byte MPDO</span><br><span class="line">copy caller-provided 4-byte data</span><br><span class="line">send</span><br><span class="line">advance inhibit deadline</span><br></pre></td></tr></table></figure><h3 id="31-2-SAM-MPDO-发送"><a href="#31-2-SAM-MPDO-发送" class="headerlink" title="31.2 SAM-MPDO 发送"></a>31.2 SAM-MPDO 发送</h3><p><code>co_sam_mpdo_event()</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">validate PDO and SAM mode</span><br><span class="line">read local Node-ID</span><br><span class="line">check inhibit time</span><br><span class="line">build MPDO address fields</span><br><span class="line">co_sam_mpdo_up reads object value</span><br><span class="line">send</span><br><span class="line">advance inhibit deadline</span><br></pre></td></tr></table></figure><h3 id="31-3-MPDO-接收"><a href="#31-3-MPDO-接收" class="headerlink" title="31.3 MPDO 接收"></a>31.3 MPDO 接收</h3><p>RPDO 仍由 <code>co_rpdo_read_frame()</code> 调用 <code>co_pdo_dn()</code>，后者根据 <code>map.n</code> 的特殊值进入 DAM&#x2F;SAM-MPDO 解码。</p><p>目标对象不存在时，RPDO error callback 可收到 <code>0x8230</code>。</p><hr><h2 id="32-C-API-最小接线示例"><a href="#32-C-API-最小接线示例" class="headerlink" title="32. C API 最小接线示例"></a>32. C API 最小接线示例</h2><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;lely/co/rpdo.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;lely/co/tpdo.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">static</span> <span class="type">void</span></span><br><span class="line"><span class="title function_">on_rpdo</span><span class="params">(<span class="type">co_rpdo_t</span> *pdo, <span class="type">co_unsigned32_t</span> ac, <span class="type">const</span> <span class="type">void</span> *ptr, <span class="type">size_t</span> n,</span></span><br><span class="line"><span class="params">        <span class="type">void</span> *data)</span></span><br><span class="line">&#123;</span><br><span class="line">    (<span class="type">void</span>)pdo;</span><br><span class="line">    (<span class="type">void</span>)ptr;</span><br><span class="line">    (<span class="type">void</span>)n;</span><br><span class="line">    (<span class="type">void</span>)data;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (ac) &#123;</span><br><span class="line">        <span class="comment">/* Record mapping or object-access error. */</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">static</span> <span class="type">void</span></span><br><span class="line"><span class="title function_">on_rpdo_error</span><span class="params">(<span class="type">co_rpdo_t</span> *pdo, <span class="type">co_unsigned16_t</span> eec, <span class="type">co_unsigned8_t</span> er,</span></span><br><span class="line"><span class="params">              <span class="type">void</span> *data)</span></span><br><span class="line">&#123;</span><br><span class="line">    (<span class="type">void</span>)pdo;</span><br><span class="line">    (<span class="type">void</span>)eec;</span><br><span class="line">    (<span class="type">void</span>)er;</span><br><span class="line">    (<span class="type">void</span>)data;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Forward the PDO communication error to the EMCY/application layer. */</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">static</span> <span class="type">void</span></span><br><span class="line"><span class="title function_">on_tpdo</span><span class="params">(<span class="type">co_tpdo_t</span> *pdo, <span class="type">co_unsigned32_t</span> ac, <span class="type">const</span> <span class="type">void</span> *ptr, <span class="type">size_t</span> n,</span></span><br><span class="line"><span class="params">        <span class="type">void</span> *data)</span></span><br><span class="line">&#123;</span><br><span class="line">    (<span class="type">void</span>)pdo;</span><br><span class="line">    (<span class="type">void</span>)ptr;</span><br><span class="line">    (<span class="type">void</span>)n;</span><br><span class="line">    (<span class="type">void</span>)data;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (ac) &#123;</span><br><span class="line">        <span class="comment">/* Record mapping, timeout or CAN I/O error. */</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">static</span> <span class="type">int</span></span><br><span class="line"><span class="title function_">on_tpdo_sample</span><span class="params">(<span class="type">co_tpdo_t</span> *pdo, <span class="type">void</span> *data)</span></span><br><span class="line">&#123;</span><br><span class="line">    (<span class="type">void</span>)data;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Snapshot application variables before co_pdo_up() reads them. */</span></span><br><span class="line">    <span class="keyword">return</span> co_tpdo_sample_res(pdo, <span class="number">0</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span></span><br><span class="line"><span class="title function_">pdo_services_init</span><span class="params">(<span class="type">can_net_t</span> *net, <span class="type">co_dev_t</span> *dev,</span></span><br><span class="line"><span class="params">                  <span class="type">co_rpdo_t</span> **prpdo, <span class="type">co_tpdo_t</span> **ptpdo)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">co_rpdo_t</span> *rpdo = co_rpdo_create(net, dev, <span class="number">1</span>);</span><br><span class="line">    <span class="keyword">if</span> (!rpdo)</span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line"></span><br><span class="line">    <span class="type">co_tpdo_t</span> *tpdo = co_tpdo_create(net, dev, <span class="number">1</span>);</span><br><span class="line">    <span class="keyword">if</span> (!tpdo) &#123;</span><br><span class="line">        co_rpdo_destroy(rpdo);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    co_rpdo_set_ind(rpdo, &amp;on_rpdo, <span class="literal">NULL</span>);</span><br><span class="line">    co_rpdo_set_err(rpdo, &amp;on_rpdo_error, <span class="literal">NULL</span>);</span><br><span class="line">    co_tpdo_set_ind(tpdo, &amp;on_tpdo, <span class="literal">NULL</span>);</span><br><span class="line">    co_tpdo_set_sample_ind(tpdo, &amp;on_tpdo_sample, <span class="literal">NULL</span>);</span><br><span class="line"></span><br><span class="line">    *prpdo = rpdo;</span><br><span class="line">    *ptpdo = tpdo;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span></span><br><span class="line"><span class="title function_">pdo_services_on_sync</span><span class="params">(<span class="type">co_rpdo_t</span> *rpdo, <span class="type">co_tpdo_t</span> *tpdo,</span></span><br><span class="line"><span class="params">                     <span class="type">co_unsigned8_t</span> counter)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">/* Apply the previous synchronous RPDO before sampling the new TPDO. */</span></span><br><span class="line">    (<span class="type">void</span>)co_rpdo_sync(rpdo, counter);</span><br><span class="line">    (<span class="type">void</span>)co_tpdo_sync(tpdo, counter);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="32-1-示例中的顺序"><a href="#32-1-示例中的顺序" class="headerlink" title="32.1 示例中的顺序"></a>32.1 示例中的顺序</h3><p>先执行 RPDO，再执行 TPDO，可使：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">previous synchronous RPDO becomes active</span><br><span class="line">    ↓</span><br><span class="line">TPDO samples the updated application state</span><br></pre></td></tr></table></figure><p>实际工程中应与系统的 SYNC 分发策略保持一致。</p><h3 id="32-2-NMT-生命周期"><a href="#32-2-NMT-生命周期" class="headerlink" title="32.2 NMT 生命周期"></a>32.2 NMT 生命周期</h3><p>上述示例没有 NMT gating。集成到完整 CANopen 节点时，应由 NMT 状态转换决定何时 start&#x2F;stop 或是否向服务分发 SYNC 和应用事件。</p><hr><h2 id="33-C-包装层"><a href="#33-C-包装层" class="headerlink" title="33. C++ 包装层"></a>33. C++ 包装层</h2><h3 id="33-1-c-type-traits"><a href="#33-1-c-type-traits" class="headerlink" title="33.1 c_type_traits"></a>33.1 <code>c_type_traits</code></h3><p><code>CORPDO</code> 和 <code>COTPDO</code> 为不完整 C 类型提供：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">alloc</span><br><span class="line">free</span><br><span class="line">init</span><br><span class="line">fini</span><br></pre></td></tr></table></figure><p>底层仍调用：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">__co_rpdo_alloc / __co_rpdo_init / __co_rpdo_fini / __co_rpdo_free</span><br><span class="line">__co_tpdo_alloc / __co_tpdo_init / __co_tpdo_fini / __co_tpdo_free</span><br></pre></td></tr></table></figure><h3 id="33-2-callback-适配"><a href="#33-2-callback-适配" class="headerlink" title="33.2 callback 适配"></a>33.2 callback 适配</h3><p><code>CORPDO::setInd()</code>、<code>CORPDO::setErr()</code> 和 <code>COTPDO::setInd()</code> 支持：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">raw C callback + void *data</span><br><span class="line">function object pointer</span><br><span class="line">class object + compile-time member function</span><br></pre></td></tr></table></figure><p>适配器最终仍保存为 C 函数指针和 <code>void *</code>。</p><h3 id="33-3-protected-destructor"><a href="#33-3-protected-destructor" class="headerlink" title="33.3 protected destructor"></a>33.3 protected destructor</h3><p>两个包装类的析构函数为 protected，因此不能像普通类型一样直接创建栈对象或使用裸 <code>delete</code>。Lely 提供的典型所有权方式是 <code>make_unique_c&lt;T&gt;()</code>、<code>make_shared_c&lt;T&gt;()</code> 和对应的 <code>delete_c_type</code>；也可以在更高层派生封装中使用。</p><h3 id="33-4-COTPDO-接口不完整"><a href="#33-4-COTPDO-接口不完整" class="headerlink" title="33.4 COTPDO 接口不完整"></a>33.4 <code>COTPDO</code> 接口不完整</h3><p>当前提交的 C++ wrapper 没有包装异步 sampling 和 MPDO API。需要时可直接调用：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">co_tpdo_set_sample_ind</span>(tpdo, sample_ind, data);</span><br><span class="line"><span class="built_in">co_tpdo_sample_res</span>(tpdo, ac);</span><br><span class="line"><span class="built_in">co_dam_mpdo_event</span>(tpdo, id, idx, subidx, bytes);</span><br><span class="line"><span class="built_in">co_sam_mpdo_event</span>(tpdo, idx, subidx);</span><br></pre></td></tr></table></figure><p>不要因为 C++ 类没有成员函数就误认为底层服务不支持这些能力。</p><hr><h2 id="34-回调上下文、所有权和线程边界"><a href="#34-回调上下文、所有权和线程边界" class="headerlink" title="34. 回调上下文、所有权和线程边界"></a>34. 回调上下文、所有权和线程边界</h2><h3 id="34-1-回调通常位于-CAN-network-执行路径"><a href="#34-1-回调通常位于-CAN-network-执行路径" class="headerlink" title="34.1 回调通常位于 CAN network 执行路径"></a>34.1 回调通常位于 CAN network 执行路径</h3><p>以下回调均由接收器、timer 或发送路径同步触发：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">co_rpdo_ind_t</span><br><span class="line">co_rpdo_err_t</span><br><span class="line">co_tpdo_ind_t</span><br><span class="line">co_tpdo_sample_ind_t</span><br></pre></td></tr></table></figure><p>回调过长会直接增加 CAN event loop 延迟和 timer 抖动。</p><h3 id="34-2-payload-指针生命周期"><a href="#34-2-payload-指针生命周期" class="headerlink" title="34.2 payload 指针生命周期"></a>34.2 payload 指针生命周期</h3><p><code>co_rpdo_ind_t</code> 的 <code>ptr</code> 指向接收 CAN message 数据；<code>co_tpdo_ind_t</code> 成功时指向服务的缓存帧数据。</p><p>应用不应在 callback 返回后继续保存裸指针。需要异步处理时立即复制。</p><h3 id="34-3-co-sdo-req-不可并发复用"><a href="#34-3-co-sdo-req-不可并发复用" class="headerlink" title="34.3 co_sdo_req 不可并发复用"></a>34.3 <code>co_sdo_req</code> 不可并发复用</h3><p>每个服务只有一个：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">pdo-&gt;req</span><br></pre></td></tr></table></figure><p>同一个 RPDO&#x2F;TPDO 服务不能在无保护条件下并行执行两次 <code>co_pdo_dn()</code> 或 <code>co_pdo_up()</code>。</p><h3 id="34-4-callback-内销毁服务的风险"><a href="#34-4-callback-内销毁服务的风险" class="headerlink" title="34.4 callback 内销毁服务的风险"></a>34.4 callback 内销毁服务的风险</h3><p>接收器和 timer callback 正在使用 <code>pdo</code> 时调用 <code>co_rpdo_destroy()</code> 或 <code>co_tpdo_destroy()</code>，可能导致 callback 返回路径访问已释放资源。</p><p>更安全的方式是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">callback marks a stop/destroy request</span><br><span class="line">    ↓</span><br><span class="line">event-loop owner performs stop/destroy after current callback returns</span><br></pre></td></tr></table></figure><hr><h2 id="35-运行期状态与-NMT-的边界"><a href="#35-运行期状态与-NMT-的边界" class="headerlink" title="35. 运行期状态与 NMT 的边界"></a>35. 运行期状态与 NMT 的边界</h2><p>源码中的 RPDO&#x2F;TPDO service 不读取 NMT state，也不自行判断：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Pre-operational</span><br><span class="line">Operational</span><br><span class="line">Stopped</span><br></pre></td></tr></table></figure><p>只要服务 started 且 COB-ID valid：</p><ul><li>RPDO receiver 可以接收帧；</li><li>TPDO event timer 可以触发；</li><li><code>co_tpdo_event()</code> 可以发送；</li><li>SYNC 和 RTR 可以驱动处理。</li></ul><p>因此完整 CANopen 节点必须由上层 NMT 服务或应用集成层控制这些入口。不能把“PDO 只在 Operational 有效”假定为 <code>rpdo.c</code>&#x2F;<code>tpdo.c</code> 内部自动保证。</p><hr><h2 id="36-关键差异汇总"><a href="#36-关键差异汇总" class="headerlink" title="36. 关键差异汇总"></a>36. 关键差异汇总</h2><table><thead><tr><th>维度</th><th>RPDO</th><th>TPDO</th></tr></thead><tbody><tr><td>CAN receiver</td><td>接收数据帧</td><td>接收 RTR</td></tr><tr><td>同步数据</td><td>缓存最后一帧，下一 SYNC 应用</td><td>SYNC 触发采样和发送&#x2F;缓存</td></tr><tr><td>event timer</td><td>接收 deadline</td><td>周期发送触发</td></tr><tr><td>inhibit time</td><td>缓存但运行期未使用</td><td>限制 event-driven 发送</td></tr><tr><td>synchronous window</td><td>决定哪些帧可进入下一 SYNC 缓存</td><td>决定异步采样完成后是否仍允许发送</td></tr><tr><td>OD 桥接</td><td><code>co_pdo_dn()</code></td><td><code>co_pdo_up()</code></td></tr><tr><td>普通 indication</td><td>处理结果和原始接收数据</td><td>发送结果或本地错误</td></tr><tr><td>专用错误 callback</td><td>有</td><td>无，统一通过 indication</td></tr><tr><td>RTR API</td><td>主动发 RTR</td><td>被动接收 RTR 并响应</td></tr></tbody></table><hr><h2 id="41-完整机制总图"><a href="#41-完整机制总图" class="headerlink" title="41. 完整机制总图"></a>41. 完整机制总图</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[&quot;RPDO data frame&quot;] --&gt; B[&quot;co_rpdo_recv&quot;]</span><br><span class="line">    B --&gt; C&#123;&quot;transmission type&quot;&#125;</span><br><span class="line">    C --&gt;|synchronous| D[&quot;cache last frame&quot;]</span><br><span class="line">    D --&gt; E[&quot;next co_rpdo_sync&quot;]</span><br><span class="line">    C --&gt;|event-driven| F[&quot;co_rpdo_read_frame&quot;]</span><br><span class="line">    E --&gt; F</span><br><span class="line">    F --&gt; G[&quot;co_pdo_dn&quot;]</span><br><span class="line">    G --&gt; H[&quot;object dictionary writes&quot;]</span><br><span class="line">    F --&gt; I[&quot;RPDO indication and error callback&quot;]</span><br><span class="line"></span><br><span class="line">    J[&quot;application event&quot;] --&gt; K[&quot;co_tpdo_event&quot;]</span><br><span class="line">    L[&quot;event timer&quot;] --&gt; K</span><br><span class="line">    M[&quot;SYNC&quot;] --&gt; N[&quot;co_tpdo_sync&quot;]</span><br><span class="line">    O[&quot;RTR&quot;] --&gt; P[&quot;co_tpdo_recv&quot;]</span><br><span class="line">    N --&gt; Q[&quot;sampling indication&quot;]</span><br><span class="line">    P --&gt; Q</span><br><span class="line">    Q --&gt; R[&quot;co_tpdo_sample_res&quot;]</span><br><span class="line">    K --&gt; S[&quot;co_tpdo_init_frame&quot;]</span><br><span class="line">    R --&gt; S</span><br><span class="line">    S --&gt; T[&quot;co_pdo_up&quot;]</span><br><span class="line">    T --&gt; U[&quot;object dictionary reads&quot;]</span><br><span class="line">    S --&gt; V[&quot;co_tpdo_send_frame&quot;]</span><br><span class="line">    V --&gt; W[&quot;CAN network&quot;]</span><br><span class="line"></span><br><span class="line">    X[&quot;SDO writes PDO parameters&quot;] --&gt; Y[&quot;0x1400/0x1600/0x1800/0x1A00 indications&quot;]</span><br><span class="line">    Y --&gt; B</span><br><span class="line">    Y --&gt; K</span><br></pre></td></tr></table></figure><hr><h2 id="42-最终心智模型"><a href="#42-最终心智模型" class="headerlink" title="42. 最终心智模型"></a>42. 最终心智模型</h2><p>可以把 RPDO&#x2F;TPDO 服务压缩成两个对称但不完全相同的状态机：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">RPDO</span><br><span class="line">    owns a data-frame receiver</span><br><span class="line">    owns one synchronous frame cache</span><br><span class="line">    owns receive deadline and window timers</span><br><span class="line">    decides when received bytes become application values</span><br><span class="line"></span><br><span class="line">TPDO</span><br><span class="line">    owns an RTR receiver</span><br><span class="line">    owns one output frame cache</span><br><span class="line">    owns event and window timers</span><br><span class="line">    owns inhibit, event and SYNC counters</span><br><span class="line">    decides when application values are sampled and transmitted</span><br></pre></td></tr></table></figure><p>最重要的五点是：</p><ol><li><strong>参数对象不是静态配置表。</strong> 服务通过 download indication 把对象字典写入转换为 receiver、timer 和状态更新。</li><li><strong>同步 RPDO 是 last-frame-wins。</strong> 收到的数据在下一次 SYNC 才执行。</li><li><strong>TPDO 的 event timer、inhibit time 和 synchronous window 是三个独立时间机制。</strong></li><li><strong>sampling callback 是同步 TPDO 与应用快照之间的边界。</strong> 自定义异步采样必须保证单实例、单次完成和窗口约束。</li><li><strong>这些服务本身不检查 NMT state，也不提供通用线程同步。</strong> 正确的生命周期、串行化和状态 gating 由更高层集成负责。</li></ol><p>理解这两个服务后，PDO 数据路径就不再只是“0x181 发 8 字节、0x201 收 8 字节”，而是一组由对象字典配置驱动、由 CAN receiver 和 timer 执行、通过 SYNC&#x2F;event&#x2F;RTR 触发并最终桥接到应用对象的运行期服务。</p><hr><h2 id="参考源码与资料"><a href="#参考源码与资料" class="headerlink" title="参考源码与资料"></a>参考源码与资料</h2><h3 id="核心源码"><a href="#核心源码" class="headerlink" title="核心源码"></a>核心源码</h3><ul><li><a href="https://github.com/lely-industries/lely-core/blob/620d1858eb8520dbc3dc5e1a7314565becd54199/include/lely/co/rpdo.h"><code>include/lely/co/rpdo.h</code></a></li><li><a href="https://github.com/lely-industries/lely-core/blob/620d1858eb8520dbc3dc5e1a7314565becd54199/include/lely/co/rpdo.hpp"><code>include/lely/co/rpdo.hpp</code></a></li><li><a href="https://github.com/lely-industries/lely-core/blob/620d1858eb8520dbc3dc5e1a7314565becd54199/src/co/rpdo.c"><code>src/co/rpdo.c</code></a></li><li><a href="https://github.com/lely-industries/lely-core/blob/620d1858eb8520dbc3dc5e1a7314565becd54199/include/lely/co/tpdo.h"><code>include/lely/co/tpdo.h</code></a></li><li><a href="https://github.com/lely-industries/lely-core/blob/620d1858eb8520dbc3dc5e1a7314565becd54199/include/lely/co/tpdo.hpp"><code>include/lely/co/tpdo.hpp</code></a></li><li><a href="https://github.com/lely-industries/lely-core/blob/620d1858eb8520dbc3dc5e1a7314565becd54199/src/co/tpdo.c"><code>src/co/tpdo.c</code></a></li></ul><h3 id="前置源码"><a href="#前置源码" class="headerlink" title="前置源码"></a>前置源码</h3><ul><li><a href="https://github.com/lely-industries/lely-core/blob/620d1858eb8520dbc3dc5e1a7314565becd54199/include/lely/co/pdo.h"><code>include/lely/co/pdo.h</code></a></li><li><a href="https://github.com/lely-industries/lely-core/blob/620d1858eb8520dbc3dc5e1a7314565becd54199/src/co/pdo.c"><code>src/co/pdo.c</code></a></li><li><a href="https://github.com/lely-industries/lely-core/blob/620d1858eb8520dbc3dc5e1a7314565becd54199/include/lely/co/sdo.h"><code>include/lely/co/sdo.h</code></a></li><li><a href="https://github.com/lely-industries/lely-core/blob/620d1858eb8520dbc3dc5e1a7314565becd54199/src/co/sdo.c"><code>src/co/sdo.c</code></a></li></ul><h3 id="协议参考"><a href="#协议参考" class="headerlink" title="协议参考"></a>协议参考</h3><ul><li>CiA 301 V4.2.0：7.2.2 PDO 服务和协议；7.5.2.35 RPDO 通信参数；7.5.2.36 RPDO 映射参数；7.5.2.37 TPDO 通信参数；7.5.2.38 TPDO 映射参数。</li></ul><h3 id="前置学习笔记"><a href="#前置学习笔记" class="headerlink" title="前置学习笔记"></a>前置学习笔记</h3><ul><li><code>Lely_CANopen_co_obj_co_sub_对象字典节点_机制原理与运行流程.md</code></li><li><code>Lely_CANopen_co_dev_DCF_EDS解析_对象字典设备描述_机制原理与运行流程.md</code></li><li><code>Lely_CANopen_co_sdo_req_PDO映射编解码_对象字典访问桥接_机制原理与运行流程.md</code></li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;meta name=&quot;referrer&quot; content=&quot;no-referrer&quot; /&gt;

&lt;h1 id=&quot;Lely-CANopen：RPDO-TPDO-运行期收发、SYNC、事件与定时器机制原理和运行流程&quot;&gt;&lt;a href=&quot;#Lely-CANopen：RPDO-TPDO-</summary>
      
    
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/categories/canopen/"/>
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/tags/canopen/"/>
    
  </entry>
  
  <entry>
    <title>README</title>
    <link href="https://wdfk-prog.space/posts/77cd4175/"/>
    <id>https://wdfk-prog.space/posts/77cd4175/</id>
    <published>2026-07-30T06:31:07.000Z</published>
    <updated>2026-07-30T06:31:11.775Z</updated>
    
    <content type="html"><![CDATA[<h1 id="canopen-学习笔记"><a href="#canopen-学习笔记" class="headerlink" title="canopen 学习笔记"></a>canopen 学习笔记</h1><ul><li>[1. CANopen  TIM&amp;SYNC 运行流程.md](CANopen  TIM&amp;SYNC 运行流程.md)</li><li>[2. CANopen CiA 协议全景：核心内容、重点与组合关系.md](CANopen CiA 协议全景：核心内容、重点与组合关系.md)</li><li>[3. CANopen Emergency： 搞懂 EM 运行流程.md](CANopen Emergency： 搞懂 EM 运行流程.md)</li><li>[4. CANopen NMT：搞懂 NMT 运行流程.md](CANopen NMT：搞懂 NMT 运行流程.md)</li><li>[5. CANopen PDO 运行流程.md](CANopen PDO 运行流程.md)</li><li>[6. CANopen 搞懂 SDO client&amp;server 运行流程.md](CANopen 搞懂 SDO client&amp;server 运行流程.md)</li><li>[7. CANopen 网络拓扑限制.md](CANopen 网络拓扑限制.md)</li><li>[8. CANopene Heartbeat运行流程.md](CANopene Heartbeat运行流程.md)</li><li>[9. CANopenEditor 从新建工程到导出 EDS  OD.c  OD.h.md](CANopenEditor 从新建工程到导出 EDS  OD.c  OD.h.md)</li><li>[10. CANopenNode <code>CO_new()</code> 与 <code>CO_config.h</code> 宏机制详解.md](CANopenNode <code>CO_new()</code> 与 <code>CO_config.h</code> 宏机制详解.md)</li><li>[11. CANopenNode LSS运行流程.md](CANopenNode LSS运行流程.md)</li><li>[12. CiA 303_3 CANopenNode 如何计算 RUN ERROR 指示灯.md](CiA 303_3 CANopenNode 如何计算 RUN ERROR 指示灯.md)</li><li>[13. Lely <code>dcf-tools</code> 与 <code>master.yml</code> 配置教程：从 EDS 生成可用的主站 DCF.md](Lely <code>dcf-tools</code> 与 <code>master.yml</code> 配置教程：从 EDS 生成可用的主站 DCF.md)</li><li>[14. Lely CANopen ：IO 服务 io_ctx.md](Lely CANopen ：IO 服务 io_ctx.md)</li><li>[15. Lely CANopen <code>coapp Device</code> 本地 SDO PDO 读写、远程对象映射与运行机制.md](Lely CANopen <code>coapp Device</code> 本地 SDO PDO 读写、远程对象映射与运行机制.md)</li><li>[16. Lely CANopen <code>ev_loop</code> 详解.md](Lely CANopen <code>ev_loop</code> 详解.md)</li><li>[17. Lely CANopen <code>spscring</code> 单生产者单消费者环形队列完整机制学习笔记.md](Lely CANopen <code>spscring</code> 单生产者单消费者环形队列完整机制学习笔记.md)</li><li>[18. Lely CANopen CAN 完整机制学习笔记.md](Lely CANopen CAN 完整机制学习笔记.md)</li><li>[19. Lely CANopen configure 配置项与日志解读.md](Lely CANopen configure 配置项与日志解读.md)</li><li>[20. Lely CANopen Future Promise 机制原理与源码详解.md](Lely CANopen Future Promise 机制原理与源码详解.md)</li><li>[21. Lely CANopen Timer 的实现原理与运行流程.md](Lely CANopen Timer 的实现原理与运行流程.md)</li><li>[22. Lely CANopen 交叉构建说明.md](Lely CANopen 交叉构建说明.md)</li><li>[23. Lely CANopen 入门：从协议背景到 i.MX8P 交叉编译安装.md](Lely CANopen 入门：从协议背景到 i.MX8P 交叉编译安装.md)</li><li>[24. Lely CANopen 事件调度机制详解：从 <code>ev_task</code>、Executor、<code>std_exec</code> 到 <code>ev_loop</code>、Future 与 Poll.md](Lely CANopen 事件调度机制详解：从 <code>ev_task</code>、Executor、<code>std_exec</code> 到 <code>ev_loop</code>、Future 与 Poll.md)</li><li>[25. Lely CANopen：can_net、io_can_net设计原理.md](Lely CANopen：can_net、io_can_net设计原理.md)</li><li>[26. Lely CANopen：co_dev 设备对象字典与 DCF 和 PDO 事件机制.md](Lely CANopen：co_dev 设备对象字典与 DCF 和 PDO 事件机制.md)</li><li>[27. Lely CANopen：io_tqueue 与 pheap 的实现原理、运行流程.md](Lely CANopen：io_tqueue 与 pheap 的实现原理、运行流程.md)</li><li>[28. Lely canopen：用 EDS 和 <code>master.yml</code> 生成 CANopen 主站 DCF.md](Lely canopen：用 EDS 和 <code>master.yml</code> 生成 CANopen 主站 DCF.md)</li><li>[29. Lely canopen<code>io_poll</code> 机制原理与完整运行流程详解.md](Lely canopen<code>io_poll</code> 机制原理与完整运行流程详解.md)</li><li>[30. 从 <code>autoreconf -i</code> 看懂 Autoconf：以 Lely CANopen 交叉编译为例.md](从 <code>autoreconf -i</code> 看懂 Autoconf：以 Lely CANopen 交叉编译为例.md)</li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;canopen-学习笔记&quot;&gt;&lt;a href=&quot;#canopen-学习笔记&quot; class=&quot;headerlink&quot; title=&quot;canopen 学习笔记&quot;&gt;&lt;/a&gt;canopen 学习笔记&lt;/h1&gt;&lt;ul&gt;
&lt;li&gt;[1. CANopen  TIM&amp;amp;SYN</summary>
      
    
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/categories/canopen/"/>
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/tags/canopen/"/>
    
  </entry>
  
  <entry>
    <title>Lely CANopen `coapp Device` 本地 SDO PDO 读写、远程对象映射与运行机制</title>
    <link href="https://wdfk-prog.space/posts/178cc435/"/>
    <id>https://wdfk-prog.space/posts/178cc435/</id>
    <published>2026-07-30T02:34:06.000Z</published>
    <updated>2026-07-30T02:35:15.396Z</updated>
    
    <content type="html"><![CDATA[<h1 id="Lely-CANopen-coapp-Device：本地-SDO-PDO-读写、远程对象映射与运行机制"><a href="#Lely-CANopen-coapp-Device：本地-SDO-PDO-读写、远程对象映射与运行机制" class="headerlink" title="Lely CANopen coapp::Device：本地 SDO&#x2F;PDO 读写、远程对象映射与运行机制"></a>Lely CANopen <code>coapp::Device</code>：本地 SDO&#x2F;PDO 读写、远程对象映射与运行机制</h1><p><img src="https://i-blog.csdnimg.cn/direct/bde27bf400fc411a8323fad13dc5e19d.png" alt="在这里插入图片描述"></p><p>@[toc]</p><h2 id="1-先给出最关键的结论"><a href="#1-先给出最关键的结论" class="headerlink" title="1. 先给出最关键的结论"></a>1. 先给出最关键的结论</h2><p>理解 <code>Device</code> 时，必须先把“本地读写”“远程读写”“SDO”“PDO”分开：</p><ol><li><code>Device</code> 本质上是 <code>co_dev_t</code> 的 C++ 所有权与访问封装。</li><li><code>Device::Read()</code>、<code>Device::Write()</code> 是对本地对象字典执行一次“本地 SDO 请求”，不会发送 CAN 帧。</li><li><code>Device::Get()</code>、<code>Device::Set()</code> 是直接访问本地对象字典值，绕过 SDO 访问权限、范围检查和下载&#x2F;上传回调。</li><li><code>Device::RpdoRead()</code> 并不会通过 CAN 去读取远程节点，而是把“远程 TPDO 对象地址”转换为本地 RPDO 映射对象，再读取本地缓存值。</li><li><code>Device::TpdoWrite()</code> 也不会执行远程 SDO 下载，而是把“远程 RPDO 对象地址”转换为本地 TPDO 映射对象，再写入本地待发送值。</li><li>真正通过 CAN 总线访问远程对象字典的是 <code>co_csdo_t</code> &#x2F; <code>lely::canopen::Sdo</code>，不是 <code>Device</code>。</li><li>RPDO 接收和 TPDO 发送虽然是 PDO 通信，但它们访问对象字典时复用了本地 SDO indication 机制：<ul><li>RPDO：<code>co_pdo_dn()</code> → <code>co_sub_dn_ind()</code>；</li><li>TPDO：<code>co_pdo_up()</code> → <code>co_sub_up_ind()</code>。</li></ul></li><li><code>Device</code> 位于“对象字典模型”和“CANopen 服务”之间，但它本身不是 CAN 网络服务，不持有 CAN 通道，也不直接收发 CAN 帧。</li></ol><p>最容易混淆的点是：</p><blockquote><p><code>RpdoRead()</code> 和 <code>TpdoWrite()</code> 的参数看起来是远程节点的 <code>node-ID/index/sub-index</code>，但它们操作的仍然是本地 <code>co_dev_t</code> 中的代理对象。</p></blockquote><hr><h2 id="2-Device-在-Lely-CANopen-中处于什么位置"><a href="#2-Device-在-Lely-CANopen-中处于什么位置" class="headerlink" title="2. Device 在 Lely CANopen 中处于什么位置"></a>2. <code>Device</code> 在 Lely CANopen 中处于什么位置</h2><h3 id="2-1-总体结构"><a href="#2-1-总体结构" class="headerlink" title="2.1 总体结构"></a>2.1 总体结构</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    DCF[EDS / DCF / static device] --&gt; DEV[co_dev_t\n本地对象字典模型]</span><br><span class="line"></span><br><span class="line">    DEVICE[lely::canopen::Device\nC++ 封装] --&gt;|拥有并访问| DEV</span><br><span class="line"></span><br><span class="line">    NODE[lely::canopen::Node] --&gt;|继承| DEVICE</span><br><span class="line">    NODE --&gt; NET[CAN network / timer / executor]</span><br><span class="line"></span><br><span class="line">    CSDO[co_csdo_t / coapp::Sdo\n远程 SDO 客户端] --&gt; NET</span><br><span class="line">    CSDO --&gt;|可读取 SDO 参数| DEV</span><br><span class="line"></span><br><span class="line">    RPDO[co_rpdo_t\nRPDO 服务] --&gt; NET</span><br><span class="line">    RPDO --&gt;|co_pdo_dn| DEV</span><br><span class="line"></span><br><span class="line">    TPDO[co_tpdo_t\nTPDO 服务] --&gt; NET</span><br><span class="line">    TPDO --&gt;|co_pdo_up| DEV</span><br></pre></td></tr></table></figure><h3 id="2-2-各模块的职责"><a href="#2-2-各模块的职责" class="headerlink" title="2.2 各模块的职责"></a>2.2 各模块的职责</h3><table><thead><tr><th>模块</th><th>核心职责</th><th align="right">是否直接收发 CAN 帧</th></tr></thead><tbody><tr><td><code>co_dev_t</code></td><td>保存对象字典、节点 ID、对象、子对象、当前值及访问回调</td><td align="right">否</td></tr><tr><td><code>co_obj_t</code></td><td>表示一个对象字典条目，例如 <code>0x6040</code></td><td align="right">否</td></tr><tr><td><code>co_sub_t</code></td><td>表示一个子对象，例如 <code>0x6040:00</code>，保存类型、权限、PDO 属性和值</td><td align="right">否</td></tr><tr><td><code>Device</code></td><td>管理 <code>co_dev_t</code> 生命周期，提供类型安全访问、锁、回调和远程 PDO 地址映射</td><td align="right">否</td></tr><tr><td><code>co_sdo_req</code></td><td>描述一次本地上传&#x2F;下载请求及其数据缓冲</td><td align="right">否</td></tr><tr><td><code>co_csdo_t</code></td><td>Client-SDO 状态机，向远程 Server-SDO 发起网络请求</td><td align="right">是</td></tr><tr><td><code>co_rpdo_t</code></td><td>接收 PDO 帧并写入本地对象字典</td><td align="right">是</td></tr><tr><td><code>co_tpdo_t</code></td><td>从本地对象字典取值并发送 PDO 帧</td><td align="right">是</td></tr><tr><td><code>co_pdo_dn()</code> &#x2F; <code>co_pdo_up()</code></td><td>在 PDO 数据与对象字典之间搬运数据</td><td align="right">否，供 PDO 服务调用</td></tr></tbody></table><p>因此，<code>Device</code> 可以理解为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">Device = co_dev_t 的 C++ 所有权封装</span><br><span class="line">       + 本地对象字典访问接口</span><br><span class="line">       + 类型转换</span><br><span class="line">       + SDO 错误转换</span><br><span class="line">       + 线程锁</span><br><span class="line">       + 写入通知</span><br><span class="line">       + 远程 PDO 对象地址到本地代理对象的映射</span><br></pre></td></tr></table></figure><p>它不等于：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">Device != Client-SDO</span><br><span class="line">Device != Server-SDO</span><br><span class="line">Device != RPDO service</span><br><span class="line">Device != TPDO service</span><br><span class="line">Device != CAN channel</span><br></pre></td></tr></table></figure><hr><h2 id="3-Device-与-co-dev-t-的关系"><a href="#3-Device-与-co-dev-t-的关系" class="headerlink" title="3. Device 与 co_dev_t 的关系"></a>3. <code>Device</code> 与 <code>co_dev_t</code> 的关系</h2><h3 id="3-1-co-dev-t-是真正的数据载体"><a href="#3-1-co-dev-t-是真正的数据载体" class="headerlink" title="3.1 co_dev_t 是真正的数据载体"></a>3.1 <code>co_dev_t</code> 是真正的数据载体</h3><p><code>co_dev_t</code> 保存完整的本地设备描述，包括：</p><ul><li>network-ID；</li><li>node-ID；</li><li>对象字典中的 <code>co_obj_t</code>；</li><li>每个对象下的 <code>co_sub_t</code>；</li><li>子对象类型；</li><li>访问权限；</li><li>PDO mapping 属性；</li><li>当前值；</li><li>SDO upload&#x2F;download indication 回调；</li><li>TPDO event indication 回调。</li></ul><p><code>Device</code> 本身没有重新实现一套对象字典，而是始终通过 C API 操作内部 <code>co_dev_t</code>。</p><h3 id="3-2-所有权关系"><a href="#3-2-所有权关系" class="headerlink" title="3.2 所有权关系"></a>3.2 所有权关系</h3><p><code>Device::Impl_</code> 内部保存：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">::std::unique_ptr&lt;<span class="type">co_dev_t</span>, DeviceDeleter&gt; dev;</span><br></pre></td></tr></table></figure><p>自定义删除器最终调用：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">co_dev_destroy</span>(dev);</span><br></pre></td></tr></table></figure><p>这表示：</p><ul><li><code>Device</code> 拥有 <code>co_dev_t</code>；</li><li><code>Device</code> 析构时自动销毁对象字典；</li><li>不需要用户手工调用 <code>co_dev_destroy()</code>；</li><li><code>Device</code> 禁止复制，避免两个实例同时拥有同一个 <code>co_dev_t</code>。</li></ul><h3 id="3-3-创建来源"><a href="#3-3-创建来源" class="headerlink" title="3.3 创建来源"></a>3.3 创建来源</h3><p><code>Device</code> 支持三种输入：</p><ol><li>已存在的 <code>co_dev_t*</code>；</li><li>文本 EDS&#x2F;DCF，以及可选的 concise DCF；</li><li>静态设备描述 <code>co_sdev*</code>。</li></ol><p>文本 DCF 路径最终进入：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">co_dev_create_from_dcf_file</span>(dcf_txt.<span class="built_in">c_str</span>());</span><br></pre></td></tr></table></figure><p>若存在二进制 concise DCF，再调用：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">co_dev_read_dcf_file</span>(dev, <span class="literal">nullptr</span>, <span class="literal">nullptr</span>, dcf_bin.<span class="built_in">c_str</span>());</span><br></pre></td></tr></table></figure><p>这一步只负责创建和初始化本地对象字典。真正的 NMT、SDO、PDO 服务是在 <code>Node</code> 等更高层对象启动后，根据这个对象字典创建的。</p><hr><h2 id="4-四类对象字典访问必须分清"><a href="#4-四类对象字典访问必须分清" class="headerlink" title="4. 四类对象字典访问必须分清"></a>4. 四类对象字典访问必须分清</h2><table><thead><tr><th>接口</th><th>地址含义</th><th>实际访问对象</th><th align="right">权限&#x2F;范围检查</th><th align="right">回调</th><th align="right">是否发送 CAN 帧</th></tr></thead><tbody><tr><td><code>Read&lt;T&gt;()</code></td><td>本地 index&#x2F;sub-index</td><td>本地 <code>co_dev_t</code></td><td align="right">是</td><td align="right">是</td><td align="right">否</td></tr><tr><td><code>Write&lt;T&gt;()</code></td><td>本地 index&#x2F;sub-index</td><td>本地 <code>co_dev_t</code></td><td align="right">是</td><td align="right">是</td><td align="right">否</td></tr><tr><td><code>Get&lt;T&gt;()</code></td><td>本地 index&#x2F;sub-index</td><td>本地 <code>co_dev_t</code></td><td align="right">否</td><td align="right">否</td><td align="right">否</td></tr><tr><td><code>Set&lt;T&gt;()</code></td><td>本地 index&#x2F;sub-index</td><td>本地 <code>co_dev_t</code></td><td align="right">否</td><td align="right">否</td><td align="right">否</td></tr><tr><td><code>RpdoRead&lt;T&gt;()</code></td><td>远程 TPDO 对象地址</td><td>本地 RPDO 代理对象</td><td align="right">是</td><td align="right">是</td><td align="right">否</td></tr><tr><td><code>RpdoGet&lt;T&gt;()</code></td><td>远程 TPDO 对象地址</td><td>本地 RPDO 代理对象</td><td align="right">否</td><td align="right">否</td><td align="right">否</td></tr><tr><td><code>TpdoRead&lt;T&gt;()</code></td><td>远程 RPDO 对象地址</td><td>本地 TPDO 代理对象</td><td align="right">是</td><td align="right">是</td><td align="right">否</td></tr><tr><td><code>TpdoWrite&lt;T&gt;()</code></td><td>远程 RPDO 对象地址</td><td>本地 TPDO 代理对象</td><td align="right">是</td><td align="right">是</td><td align="right">否</td></tr><tr><td><code>TpdoGet&lt;T&gt;()</code></td><td>远程 RPDO 对象地址</td><td>本地 TPDO 代理对象</td><td align="right">否</td><td align="right">否</td><td align="right">否</td></tr><tr><td><code>TpdoSet&lt;T&gt;()</code></td><td>远程 RPDO 对象地址</td><td>本地 TPDO 代理对象</td><td align="right">否</td><td align="right">否</td><td align="right">否</td></tr><tr><td><code>Sdo::SubmitUpload()</code></td><td>远程 index&#x2F;sub-index</td><td>远程节点对象字典</td><td align="right">由远程 SDO server 执行</td><td align="right">远程回调&#x2F;完成通知</td><td align="right">是</td></tr><tr><td><code>Sdo::SubmitDownload()</code></td><td>远程 index&#x2F;sub-index</td><td>远程节点对象字典</td><td align="right">由远程 SDO server 执行</td><td align="right">远程回调&#x2F;完成通知</td><td align="right">是</td></tr></tbody></table><p>其中 <code>Get/Set/RpdoGet/TpdoGet/TpdoSet</code> 位于 <code>protected</code> 区域，主要供 <code>Node</code>、<code>Master</code> 或派生类内部使用。</p><hr><h2 id="5-本地-SDO-读取：Device-Read"><a href="#5-本地-SDO-读取：Device-Read" class="headerlink" title="5. 本地 SDO 读取：Device::Read()"></a>5. 本地 SDO 读取：<code>Device::Read()</code></h2><h3 id="5-1-为什么叫本地-SDO"><a href="#5-1-为什么叫本地-SDO" class="headerlink" title="5.1 为什么叫本地 SDO"></a>5.1 为什么叫本地 SDO</h3><p><code>co_dev_up_req()</code> 的注释明确指出：它向本地设备提交 upload request，等价于从对象字典读取值。</p><p>这里复用了 SDO 的访问语义，但没有：</p><ul><li>Client-SDO COB-ID；</li><li>Server-SDO COB-ID；</li><li>CAN 帧；</li><li>超时；</li><li>expedited&#x2F;segmented&#x2F;block 网络状态机。</li></ul><h3 id="5-2-调用链"><a href="#5-2-调用链" class="headerlink" title="5.2 调用链"></a>5.2 调用链</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant App as Application</span><br><span class="line">    participant Device as Device::Read&lt;T&gt;</span><br><span class="line">    participant DevReq as co_dev_up_req</span><br><span class="line">    participant Sub as co_sub_t upload indication</span><br><span class="line">    participant Conv as OnUpCon / canopen_traits</span><br><span class="line"></span><br><span class="line">    App-&gt;&gt;Device: Read&lt;T&gt;(idx, subidx)</span><br><span class="line">    Device-&gt;&gt;Device: 创建 error_code、结果变量和 tuple</span><br><span class="line">    Device-&gt;&gt;DevReq: co_dev_up_req(dev, idx, subidx, OnUpCon&lt;T&gt;, data)</span><br><span class="line">    DevReq-&gt;&gt;Sub: 查找对象和子对象并执行 upload indication</span><br><span class="line">    Sub-&gt;&gt;Sub: 检查可读权限并取得当前值</span><br><span class="line">    Sub--&gt;&gt;DevReq: 返回字节和 SDO abort code</span><br><span class="line">    DevReq-&gt;&gt;Conv: OnUpCon&lt;T&gt;(ac, ptr, n, data)</span><br><span class="line">    Conv-&gt;&gt;Conv: CANopen C 数据转换为 C++ 类型 T</span><br><span class="line">    Conv--&gt;&gt;Device: 写入结果和 abort code</span><br><span class="line">    Device--&gt;&gt;App: 返回 T 或抛出 SdoError</span><br></pre></td></tr></table></figure><h3 id="5-3-代码主线"><a href="#5-3-代码主线" class="headerlink" title="5.3 代码主线"></a>5.3 代码主线</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt;</span><br><span class="line">Device::<span class="built_in">Read</span>(<span class="type">uint16_t</span> idx, <span class="type">uint8_t</span> subidx, std::error_code&amp; ec) <span class="type">const</span> &#123;</span><br><span class="line">    <span class="type">uint32_t</span> ac = <span class="number">0</span>;</span><br><span class="line">    T value = <span class="built_in">T</span>();</span><br><span class="line">    <span class="keyword">auto</span> t = std::<span class="built_in">tie</span>(ac, value);</span><br><span class="line"></span><br><span class="line">    <span class="function">std::lock_guard&lt;Impl_&gt; <span class="title">lock</span><span class="params">(*impl_)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (!<span class="built_in">co_dev_up_req</span>(<span class="built_in">dev</span>(), idx, subidx, &amp;OnUpCon&lt;T&gt;, &amp;t)) &#123;</span><br><span class="line">        <span class="keyword">if</span> (ac)</span><br><span class="line">            ec = <span class="built_in">static_cast</span>&lt;SdoErrc&gt;(ac);</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">            ec.<span class="built_in">clear</span>();</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        ec = util::<span class="built_in">make_error_code</span>();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> value;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>关键点：</p><ol><li><code>co_dev_up_req()</code> 返回值表示函数调用本身是否失败；</li><li><code>ac</code> 表示 CANopen SDO abort code；</li><li><code>OnUpCon&lt;T&gt;</code> 将原始字节转换为目标 C++ 类型；</li><li><code>canopen_traits&lt;T&gt;</code> 决定 <code>T</code> 对应的 CANopen 类型编号和 C 表示；</li><li>无 <code>error_code</code> 参数的重载会把错误转换为 <code>SdoError</code> 异常。</li></ol><h3 id="5-4-Read-与-Get-的区别"><a href="#5-4-Read-与-Get-的区别" class="headerlink" title="5.4 Read() 与 Get() 的区别"></a>5.4 <code>Read()</code> 与 <code>Get()</code> 的区别</h3><p><code>Get()</code> 的主线是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">co_dev_find_obj()</span><br><span class="line">  -&gt; co_obj_find_sub()</span><br><span class="line">  -&gt; 检查 CANopen 类型是否匹配</span><br><span class="line">  -&gt; co_sub_get_val()</span><br><span class="line">  -&gt; 直接复制当前值</span><br></pre></td></tr></table></figure><p>它不会经过 <code>co_dev_up_req()</code> 和 <code>co_sub_up_ind()</code>，因此：</p><ul><li>不执行 SDO read access 检查；</li><li>不执行用户注册的 upload indication；</li><li>不适合模拟一次真实的 SDO 读取；</li><li>适合协议栈内部直接观察本地状态。</li></ul><hr><h2 id="6-本地-SDO-写入：Device-Write"><a href="#6-本地-SDO-写入：Device-Write" class="headerlink" title="6. 本地 SDO 写入：Device::Write()"></a>6. 本地 SDO 写入：<code>Device::Write()</code></h2><h3 id="6-1-调用链"><a href="#6-1-调用链" class="headerlink" title="6.1 调用链"></a>6.1 调用链</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant App as Application</span><br><span class="line">    participant Device as Device::Write&lt;T&gt;</span><br><span class="line">    participant Traits as canopen_traits&lt;T&gt;</span><br><span class="line">    participant DevReq as co_dev_dn_val_req</span><br><span class="line">    participant Sub as co_sub_t download indication</span><br><span class="line">    participant Notify as Device::Impl_::OnWrite</span><br><span class="line"></span><br><span class="line">    App-&gt;&gt;Device: Write&lt;T&gt;(idx, subidx, value)</span><br><span class="line">    Device-&gt;&gt;Traits: to_c_type(value)</span><br><span class="line">    Traits--&gt;&gt;Device: C 类型值和 CANopen 类型编号</span><br><span class="line">    Device-&gt;&gt;DevReq: co_dev_dn_val_req(...)</span><br><span class="line">    DevReq-&gt;&gt;Sub: 执行本地 download indication</span><br><span class="line">    Sub-&gt;&gt;Sub: 权限、类型、长度、范围和应用回调检查</span><br><span class="line">    Sub-&gt;&gt;Sub: 更新当前值</span><br><span class="line">    Sub-&gt;&gt;Notify: 写入成功后通知</span><br><span class="line">    Notify--&gt;&gt;Device: OnWrite / OnRpdoWrite</span><br><span class="line">    Device--&gt;&gt;App: 成功或 SdoError</span><br></pre></td></tr></table></figure><h3 id="6-2-主体实现"><a href="#6-2-主体实现" class="headerlink" title="6.2 主体实现"></a>6.2 主体实现</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt;</span><br><span class="line">Device::<span class="built_in">Write</span>(<span class="type">uint16_t</span> idx, <span class="type">uint8_t</span> subidx, <span class="type">const</span> T&amp; value,</span><br><span class="line">              std::error_code&amp; ec) &#123;</span><br><span class="line">    <span class="keyword">using</span> traits = canopen_traits&lt;T&gt;;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">auto</span> val = traits::<span class="built_in">to_c_type</span>(value, ec);</span><br><span class="line">    <span class="keyword">if</span> (ec)</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line"></span><br><span class="line">    <span class="type">uint32_t</span> ac = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="function">std::lock_guard&lt;Impl_&gt; <span class="title">lock</span><span class="params">(*impl_)</span></span>;</span><br><span class="line">        <span class="built_in">co_dev_dn_val_req</span>(<span class="built_in">dev</span>(), idx, subidx, traits::index, &amp;val,</span><br><span class="line">                          &amp;OnDnCon, &amp;ac);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    traits::<span class="built_in">destroy</span>(val);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="6-3-写入成功后的通知机制"><a href="#6-3-写入成功后的通知机制" class="headerlink" title="6.3 写入成功后的通知机制"></a>6.3 写入成功后的通知机制</h3><p><code>Device::Impl_</code> 构造时遍历对象字典中的对象，并对 <code>0x2000..0xBFFF</code> 区域安装 download indication：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">co_obj_set_dn_ind</span>(</span><br><span class="line">    obj,</span><br><span class="line">    [](<span class="type">co_sub_t</span>* sub, co_sdo_req* req, <span class="type">void</span>* data) -&gt; <span class="type">uint32_t</span> &#123;</span><br><span class="line">        <span class="type">uint32_t</span> ac = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">if</span> (<span class="built_in">co_sub_on_dn</span>(sub, req, &amp;ac) == <span class="number">-1</span> || ac)</span><br><span class="line">            <span class="keyword">return</span> ac;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">auto</span> impl = <span class="built_in">static_cast</span>&lt;Impl_*&gt;(data);</span><br><span class="line">        impl-&gt;<span class="built_in">OnWrite</span>(<span class="built_in">co_obj_get_idx</span>(<span class="built_in">co_sub_get_obj</span>(sub)),</span><br><span class="line">                      <span class="built_in">co_sub_get_subidx</span>(sub));</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="keyword">this</span>);</span><br></pre></td></tr></table></figure><p>这里的逻辑不是替换默认写入，而是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">先执行 co_sub_on_dn()</span><br><span class="line">    -&gt; 完成默认下载处理和实际写入</span><br><span class="line"></span><br><span class="line">只有写入完整且成功</span><br><span class="line">    -&gt; 再调用 Impl_::OnWrite()</span><br></pre></td></tr></table></figure><p>因此 <code>OnWrite()</code> 看到的是已经成功写入后的值。</p><h3 id="6-4-Write-与-Set-的区别"><a href="#6-4-Write-与-Set-的区别" class="headerlink" title="6.4 Write() 与 Set() 的区别"></a>6.4 <code>Write()</code> 与 <code>Set()</code> 的区别</h3><p><code>Set()</code> 直接执行：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">查找 obj/sub</span><br><span class="line">  -&gt; 检查模板类型与对象字典类型是否匹配</span><br><span class="line">  -&gt; co_sub_set_val()</span><br></pre></td></tr></table></figure><p>它绕过：</p><ul><li>SDO 写权限；</li><li>数值范围检查；</li><li>对象自定义 download indication；</li><li><code>Device::OnWrite()</code>；</li><li><code>OnRpdoWrite()</code>。</li></ul><p>因此：</p><ul><li>应用层需要按 CANopen 规则访问对象时使用 <code>Write()</code>；</li><li>协议栈内部维护影子值或初始化值时才使用 <code>Set()</code>；</li><li>不应因为 <code>Set()</code> 更直接就用它替代正常 SDO 写入。</li></ul><hr><h2 id="7-PDO-为什么也走本地-SDO-indication"><a href="#7-PDO-为什么也走本地-SDO-indication" class="headerlink" title="7. PDO 为什么也走本地 SDO indication"></a>7. PDO 为什么也走本地 SDO indication</h2><p>PDO 与 SDO 在总线上是不同协议，但写入或读取对象字典时，它们需要遵守同一套对象访问钩子。</p><p>Lely 的实现选择复用：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">co_sub_dn_ind()  本地下载入口</span><br><span class="line">co_sub_up_ind()  本地上传入口</span><br></pre></td></tr></table></figure><p>这样做的结果是：</p><ul><li>通过 SDO 写对象；</li><li>通过 RPDO 写对象；</li><li>协议栈内部执行本地 SDO 写请求；</li></ul><p>都能进入相同的子对象下载处理链。</p><p>这正是 <code>Device::OnWrite()</code> 能同时观察 SDO download 和 RPDO 写入的原因。</p><hr><h2 id="8-RPDO-接收后如何写入本地对象字典"><a href="#8-RPDO-接收后如何写入本地对象字典" class="headerlink" title="8. RPDO 接收后如何写入本地对象字典"></a>8. RPDO 接收后如何写入本地对象字典</h2><h3 id="8-1-服务创建关系"><a href="#8-1-服务创建关系" class="headerlink" title="8.1 服务创建关系"></a>8.1 服务创建关系</h3><p><code>co_rpdo_create()</code> 同时需要：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">can_net_t *net</span><br><span class="line">co_dev_t *dev</span><br><span class="line">PDO number</span><br></pre></td></tr></table></figure><ul><li><code>net</code> 用于接收 CAN 帧；</li><li><code>dev</code> 提供 <code>0x1400..0x15FF</code> 通信参数、<code>0x1600..0x17FF</code> 映射参数以及目标应用对象。</li></ul><h3 id="8-2-数据路径"><a href="#8-2-数据路径" class="headerlink" title="8.2 数据路径"></a>8.2 数据路径</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant CAN as CAN network</span><br><span class="line">    participant RPDO as co_rpdo_t</span><br><span class="line">    participant PdoDn as co_pdo_dn</span><br><span class="line">    participant Sub as co_sub_dn_ind</span><br><span class="line">    participant DevCb as Device::Impl_::OnWrite</span><br><span class="line">    participant AppCb as OnRpdoWrite</span><br><span class="line"></span><br><span class="line">    CAN-&gt;&gt;RPDO: 收到 RPDO CAN 帧</span><br><span class="line">    RPDO-&gt;&gt;RPDO: 根据 0x1400 通信参数确认 PDO</span><br><span class="line">    RPDO-&gt;&gt;PdoDn: 映射参数 0x1600 + payload</span><br><span class="line"></span><br><span class="line">    loop 每个映射项</span><br><span class="line">        PdoDn-&gt;&gt;PdoDn: 解析 index/sub-index/bit length</span><br><span class="line">        PdoDn-&gt;&gt;Sub: 构造本地 co_sdo_req 并执行 download indication</span><br><span class="line">        Sub-&gt;&gt;Sub: 检查 RPDO 可写性并更新值</span><br><span class="line">        Sub-&gt;&gt;DevCb: 写入成功通知</span><br><span class="line">    end</span><br><span class="line"></span><br><span class="line">    DevCb-&gt;&gt;DevCb: 查询本地对象的反向远程映射</span><br><span class="line">    DevCb-&gt;&gt;AppCb: OnRpdoWrite(remote id, remote idx, remote subidx)</span><br></pre></td></tr></table></figure><h3 id="8-3-co-pdo-dn-的核心动作"><a href="#8-3-co-pdo-dn-的核心动作" class="headerlink" title="8.3 co_pdo_dn() 的核心动作"></a>8.3 <code>co_pdo_dn()</code> 的核心动作</h3><p>每个 mapping entry 是一个 32 位值：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">bits 31..16 : object index</span><br><span class="line">bits 15..8  : sub-index</span><br><span class="line">bits 7..0   : mapping length in bits</span><br></pre></td></tr></table></figure><p><code>co_pdo_dn()</code> 对每个映射项执行：</p><ol><li>从 PDO payload 按位提取数据；</li><li>可选执行 <code>co_dev_chk_rpdo()</code>；</li><li>查找本地 <code>co_sub_t</code>；</li><li>构造临时 <code>co_sdo_req</code>；</li><li>调用 <code>co_sub_dn_ind()</code>；</li><li>由子对象完成权限、类型和应用处理；</li><li>写入成功后进入 <code>Device::OnWrite()</code> 通知链。</li></ol><p>所以 RPDO 不是直接 <code>memcpy</code> 到变量，而是通过对象字典访问入口写入。</p><h3 id="8-4-RPDO-检查内容"><a href="#8-4-RPDO-检查内容" class="headerlink" title="8.4 RPDO 检查内容"></a>8.4 RPDO 检查内容</h3><p><code>co_dev_chk_rpdo()</code> 主要检查：</p><ul><li>对象是否存在；</li><li>子索引是否存在；</li><li>是否具有写权限；</li><li>是否允许 PDO mapping；</li><li>access 属性是否包含 RPDO。</li></ul><p>错误使用标准 SDO abort code 表示，例如：</p><ul><li><code>NO_OBJ</code>；</li><li><code>NO_SUB</code>；</li><li><code>NO_WRITE</code>；</li><li><code>NO_PDO</code>；</li><li><code>PDO_LEN</code>。</li></ul><hr><h2 id="9-TPDO-发送前如何读取本地对象字典"><a href="#9-TPDO-发送前如何读取本地对象字典" class="headerlink" title="9. TPDO 发送前如何读取本地对象字典"></a>9. TPDO 发送前如何读取本地对象字典</h2><h3 id="9-1-服务创建关系"><a href="#9-1-服务创建关系" class="headerlink" title="9.1 服务创建关系"></a>9.1 服务创建关系</h3><p><code>co_tpdo_create()</code> 同样需要：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">can_net_t *net</span><br><span class="line">co_dev_t *dev</span><br><span class="line">PDO number</span><br></pre></td></tr></table></figure><p><code>co_dev_t</code> 中需要存在：</p><ul><li><code>0x1800..0x19FF</code> TPDO communication parameters；</li><li><code>0x1A00..0x1BFF</code> TPDO mapping parameters；</li><li>被映射的本地应用对象。</li></ul><h3 id="9-2-数据路径"><a href="#9-2-数据路径" class="headerlink" title="9.2 数据路径"></a>9.2 数据路径</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant Trigger as Event / SYNC / timer</span><br><span class="line">    participant TPDO as co_tpdo_t</span><br><span class="line">    participant PdoUp as co_pdo_up</span><br><span class="line">    participant Sub as co_sub_up_ind</span><br><span class="line">    participant CAN as CAN network</span><br><span class="line"></span><br><span class="line">    Trigger-&gt;&gt;TPDO: 触发 TPDO</span><br><span class="line">    TPDO-&gt;&gt;TPDO: 检查 transmission type、inhibit time 等</span><br><span class="line">    TPDO-&gt;&gt;PdoUp: 传入 0x1A00 mapping</span><br><span class="line"></span><br><span class="line">    loop 每个映射项</span><br><span class="line">        PdoUp-&gt;&gt;PdoUp: 解析 index/sub-index/bit length</span><br><span class="line">        PdoUp-&gt;&gt;Sub: 本地 upload indication</span><br><span class="line">        Sub--&gt;&gt;PdoUp: 当前对象值</span><br><span class="line">        PdoUp-&gt;&gt;PdoUp: 按位拼接到 PDO payload</span><br><span class="line">    end</span><br><span class="line"></span><br><span class="line">    PdoUp--&gt;&gt;TPDO: 完整 payload</span><br><span class="line">    TPDO-&gt;&gt;CAN: 发送 TPDO CAN 帧</span><br></pre></td></tr></table></figure><h3 id="9-3-co-pdo-up-的核心动作"><a href="#9-3-co-pdo-up-的核心动作" class="headerlink" title="9.3 co_pdo_up() 的核心动作"></a>9.3 <code>co_pdo_up()</code> 的核心动作</h3><p>对每个映射项：</p><ol><li>可选执行 <code>co_dev_chk_tpdo()</code>；</li><li>查找本地子对象；</li><li>清理并复用 <code>co_sdo_req</code>；</li><li>调用 <code>co_sub_up_ind()</code>；</li><li>确认值可在一次 PDO 采样中完整取得；</li><li>按 mapping length 写入 payload。</li></ol><p><code>co_dev_chk_tpdo()</code> 检查：</p><ul><li>对象和子对象是否存在；</li><li>是否可读；</li><li>是否允许 PDO mapping；</li><li>access 属性是否包含 TPDO。</li></ul><hr><h2 id="10-WriteEvent-和-SetEvent-到底做什么"><a href="#10-WriteEvent-和-SetEvent-到底做什么" class="headerlink" title="10. WriteEvent() 和 SetEvent() 到底做什么"></a>10. <code>WriteEvent()</code> 和 <code>SetEvent()</code> 到底做什么</h2><p><code>WriteEvent()</code> 和 <code>SetEvent()</code> 不写入对象值，只表示：</p><blockquote><p>某个对象发生了需要触发事件型 TPDO 的变化。</p></blockquote><p>底层调用：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">co_dev_tpdo_event</span>(<span class="built_in">dev</span>(), idx, subidx);</span><br></pre></td></tr></table></figure><p><code>co_dev_tpdo_event()</code> 会：</p><ol><li>检查该子对象是否被映射到 PDO；</li><li>找到所有包含该子对象的有效异步或事件驱动 TPDO；</li><li>调用注册在 <code>co_dev_t</code> 上的 TPDO event indication；</li><li>对应 <code>co_tpdo_t</code> 服务再尝试发送 PDO。</li></ol><p>因此，典型事件型 TPDO 操作分为两步：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">TpdoWrite</span>&lt;<span class="type">uint16_t</span>&gt;(remote_id, remote_idx, remote_subidx, value);</span><br><span class="line"><span class="built_in">TpdoWriteEvent</span>(remote_id, remote_idx, remote_subidx);</span><br></pre></td></tr></table></figure><p>第一步只更新本地 TPDO 代理对象；第二步才通知 TPDO 服务“这个对象发生了发送事件”。</p><p>若 TPDO 使用 SYNC、事件定时器或其他 transmission type，发送时机仍由 TPDO 服务规则决定。</p><hr><h2 id="11-Device-所谓的“远程-PDO-读写”是什么"><a href="#11-Device-所谓的“远程-PDO-读写”是什么" class="headerlink" title="11. Device 所谓的“远程 PDO 读写”是什么"></a>11. <code>Device</code> 所谓的“远程 PDO 读写”是什么</h2><h3 id="11-1-核心思想：远程地址，实际访问本地代理对象"><a href="#11-1-核心思想：远程地址，实际访问本地代理对象" class="headerlink" title="11.1 核心思想：远程地址，实际访问本地代理对象"></a>11.1 核心思想：远程地址，实际访问本地代理对象</h3><p>假设远程节点 5 的 TPDO 映射了：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">0x6064:00  Position actual value</span><br></pre></td></tr></table></figure><p>主站本地对象字典中会有一个 RPDO 映射目标，例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">0x4000:01</span><br></pre></td></tr></table></figure><p>收到远程 TPDO 后，真正更新的是主站本地：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">0x4000:01</span><br></pre></td></tr></table></figure><p>应用层不希望记忆每个本地代理地址，因此 <code>Device</code> 建立映射：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">remote (node 5, 0x6064:00)</span><br><span class="line">        -&gt; local (0x4000:01)</span><br></pre></td></tr></table></figure><p>应用调用：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">RpdoRead</span>&lt;<span class="type">int32_t</span>&gt;(<span class="number">5</span>, <span class="number">0x6064</span>, <span class="number">0</span>);</span><br></pre></td></tr></table></figure><p>内部实际执行：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">RpdoMapping(5, 0x6064, 0)</span><br><span class="line">    -&gt; 得到本地 0x4000:01</span><br><span class="line"></span><br><span class="line">Read&lt;int32_t&gt;(0x4000, 1)</span><br><span class="line">    -&gt; 本地 SDO upload</span><br></pre></td></tr></table></figure><p>这里没有向节点 5 发送任何请求。</p><h3 id="11-2-为什么仍然叫-remote-object"><a href="#11-2-为什么仍然叫-remote-object" class="headerlink" title="11.2 为什么仍然叫 remote object"></a>11.2 为什么仍然叫 remote object</h3><p>因为 API 向应用层暴露的是远程设备语义：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">节点 ID + 远程 index + 远程 sub-index</span><br></pre></td></tr></table></figure><p>但是协议实现采用本地代理对象保存 PDO 数据，因此 <code>Device</code> 需要完成地址翻译。</p><hr><h2 id="12-UpdateRpdoMapping-的机制"><a href="#12-UpdateRpdoMapping-的机制" class="headerlink" title="12. UpdateRpdoMapping() 的机制"></a>12. <code>UpdateRpdoMapping()</code> 的机制</h2><h3 id="12-1-映射方向"><a href="#12-1-映射方向" class="headerlink" title="12.1 映射方向"></a>12.1 映射方向</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">远程 TPDO 对象</span><br><span class="line">    -&gt; 主站本地 RPDO 对象</span><br></pre></td></tr></table></figure><p>它用于：</p><ul><li><code>RpdoRead()</code>；</li><li><code>RpdoGet()</code>；</li><li>从本地写入通知反查远程对象，然后触发 <code>OnRpdoWrite()</code>。</li></ul><h3 id="12-2-使用的对象字典区域"><a href="#12-2-使用的对象字典区域" class="headerlink" title="12.2 使用的对象字典区域"></a>12.2 使用的对象字典区域</h3><table><thead><tr><th>对象范围</th><th>含义</th></tr></thead><tbody><tr><td><code>0x1400..0x15FF</code></td><td>本地 RPDO communication parameter</td></tr><tr><td><code>0x1600..0x17FF</code></td><td>本地 RPDO mapping parameter</td></tr><tr><td><code>0x5800..0x59FF</code></td><td>Lely 扩展：远程 TPDO number 和 node-ID</td></tr><tr><td><code>0x5A00..0x5BFF</code></td><td>Lely 扩展：远程 TPDO mapping</td></tr></tbody></table><h3 id="12-3-构建步骤"><a href="#12-3-构建步骤" class="headerlink" title="12.3 构建步骤"></a>12.3 构建步骤</h3><p>对每个 RPDO：</p><ol><li>检查 <code>0x1400+i:01</code> 的 COB-ID 是否有效；</li><li>从 <code>0x5800+i</code> 获取远程 node-ID；</li><li>若不存在扩展对象，则尝试从预定义连接 COB-ID 推导 node-ID；</li><li>从 <code>0x1600+i</code> 读取本地 RPDO mapping；</li><li>从 <code>0x5A00+i</code> 读取远程 TPDO mapping；</li><li>检查本地和远程 mapping 项数一致；</li><li>检查每个映射项的位长度一致；</li><li>建立远程到本地映射；</li><li>同时建立本地到远程的反向映射。</li></ol><h3 id="12-4-映射键编码"><a href="#12-4-映射键编码" class="headerlink" title="12.4 映射键编码"></a>12.4 映射键编码</h3><p>远程对象键被编码到 32 位整数：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">bits 31..24 : remote node-ID</span><br><span class="line">bits 23..8  : remote index</span><br><span class="line">bits 7..0   : remote sub-index</span><br></pre></td></tr></table></figure><p>等价于：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">(remote_id &lt;&lt; <span class="number">24</span>) | (remote_idx &lt;&lt; <span class="number">8</span>) | remote_subidx</span><br></pre></td></tr></table></figure><p>本地对象键使用：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">(local_idx &lt;&lt; <span class="number">8</span>) | local_subidx</span><br></pre></td></tr></table></figure><p><code>rpdo_mapping</code> 同时保存：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">remote key -&gt; local key</span><br><span class="line">local key  -&gt; remote key</span><br></pre></td></tr></table></figure><p>反向映射使 <code>Impl_::OnWrite(local_idx, local_subidx)</code> 能恢复远程来源地址。</p><hr><h2 id="13-UpdateTpdoMapping-的机制"><a href="#13-UpdateTpdoMapping-的机制" class="headerlink" title="13. UpdateTpdoMapping() 的机制"></a>13. <code>UpdateTpdoMapping()</code> 的机制</h2><h3 id="13-1-映射方向"><a href="#13-1-映射方向" class="headerlink" title="13.1 映射方向"></a>13.1 映射方向</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">远程 RPDO 对象</span><br><span class="line">    -&gt; 主站本地 TPDO 对象</span><br></pre></td></tr></table></figure><p>它用于：</p><ul><li><code>TpdoRead()</code>；</li><li><code>TpdoWrite()</code>；</li><li><code>TpdoGet()</code>；</li><li><code>TpdoSet()</code>；</li><li><code>TpdoWriteEvent()</code>；</li><li><code>TpdoSetEvent()</code>。</li></ul><h3 id="13-2-使用的对象字典区域"><a href="#13-2-使用的对象字典区域" class="headerlink" title="13.2 使用的对象字典区域"></a>13.2 使用的对象字典区域</h3><table><thead><tr><th>对象范围</th><th>含义</th></tr></thead><tbody><tr><td><code>0x1800..0x19FF</code></td><td>本地 TPDO communication parameter</td></tr><tr><td><code>0x1A00..0x1BFF</code></td><td>本地 TPDO mapping parameter</td></tr><tr><td><code>0x5C00..0x5DFF</code></td><td>Lely 扩展：远程 RPDO number 和 node-ID</td></tr><tr><td><code>0x5E00..0x5FFF</code></td><td>Lely 扩展：远程 RPDO mapping</td></tr></tbody></table><h3 id="13-3-构建步骤"><a href="#13-3-构建步骤" class="headerlink" title="13.3 构建步骤"></a>13.3 构建步骤</h3><p>对每个 TPDO：</p><ol><li>检查 TPDO COB-ID 是否有效；</li><li>获取远程 node-ID；</li><li>读取本地 TPDO mapping；</li><li>读取远程 RPDO mapping；</li><li>检查映射项数和位长度；</li><li>建立：</li></ol><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">remote RPDO address -&gt; local TPDO proxy address</span><br></pre></td></tr></table></figure><h3 id="13-4-写远程-RPDO-的实际过程"><a href="#13-4-写远程-RPDO-的实际过程" class="headerlink" title="13.4 写远程 RPDO 的实际过程"></a>13.4 写远程 RPDO 的实际过程</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant App as Application</span><br><span class="line">    participant Device as Device</span><br><span class="line">    participant Map as tpdo_mapping</span><br><span class="line">    participant LocalOD as Local co_dev_t</span><br><span class="line">    participant TPDO as TPDO service</span><br><span class="line">    participant CAN as CAN network</span><br><span class="line"></span><br><span class="line">    App-&gt;&gt;Device: TpdoWrite(id, idx, subidx, value)</span><br><span class="line">    Device-&gt;&gt;Map: 查 remote RPDO -&gt;&gt; local TPDO proxy</span><br><span class="line">    Map--&gt;&gt;Device: local index/sub-index</span><br><span class="line">    Device-&gt;&gt;LocalOD: 本地 Write(local index/sub-index)</span><br><span class="line">    Note over Device,LocalOD: 此时没有 CAN 帧</span><br><span class="line"></span><br><span class="line">    App-&gt;&gt;Device: TpdoWriteEvent(id, idx, subidx)</span><br><span class="line">    Device-&gt;&gt;Map: 再次取得 local proxy</span><br><span class="line">    Device-&gt;&gt;LocalOD: co_dev_tpdo_event(local index/sub-index)</span><br><span class="line">    LocalOD-&gt;&gt;TPDO: TPDO event indication</span><br><span class="line">    TPDO-&gt;&gt;LocalOD: co_pdo_up() 采集映射值</span><br><span class="line">    TPDO-&gt;&gt;CAN: 发送 TPDO</span><br></pre></td></tr></table></figure><p>从远程节点视角看，这个 TPDO 是它的 RPDO，因此应用 API 使用远程 RPDO 地址描述目标。</p><hr><h2 id="14-RpdoRead-、TpdoRead-、TpdoWrite-的准确含义"><a href="#14-RpdoRead-、TpdoRead-、TpdoWrite-的准确含义" class="headerlink" title="14. RpdoRead()、TpdoRead()、TpdoWrite() 的准确含义"></a>14. <code>RpdoRead()</code>、<code>TpdoRead()</code>、<code>TpdoWrite()</code> 的准确含义</h2><h3 id="14-1-RpdoRead"><a href="#14-1-RpdoRead" class="headerlink" title="14.1 RpdoRead()"></a>14.1 <code>RpdoRead()</code></h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">输入：远程节点 TPDO 中的对象地址</span><br><span class="line">过程：远程地址 -&gt; 本地 RPDO proxy -&gt; Device::Read()</span><br><span class="line">结果：读取最后一次被 RPDO 更新到本地的缓存值</span><br></pre></td></tr></table></figure><p>它不保证：</p><ul><li>远程节点当前仍在线；</li><li>数据刚刚更新；</li><li>主站会主动请求新的 PDO；</li><li>读取动作会产生 CAN 帧。</li></ul><h3 id="14-2-TpdoRead"><a href="#14-2-TpdoRead" class="headerlink" title="14.2 TpdoRead()"></a>14.2 <code>TpdoRead()</code></h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">输入：远程节点 RPDO 中的对象地址</span><br><span class="line">过程：远程地址 -&gt; 本地 TPDO proxy -&gt; Device::Read()</span><br><span class="line">结果：读取主站当前准备通过 TPDO 发出的本地值</span><br></pre></td></tr></table></figure><p>它读取的不是远程节点已接收后的值。</p><h3 id="14-3-TpdoWrite"><a href="#14-3-TpdoWrite" class="headerlink" title="14.3 TpdoWrite()"></a>14.3 <code>TpdoWrite()</code></h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">输入：远程节点 RPDO 中的对象地址</span><br><span class="line">过程：远程地址 -&gt; 本地 TPDO proxy -&gt; Device::Write()</span><br><span class="line">结果：更新主站本地待发送值</span><br></pre></td></tr></table></figure><p>它不等价于：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">远程 SDO download</span><br></pre></td></tr></table></figure><p>也不一定立即发送 PDO。事件型 PDO 通常还需要 <code>TpdoWriteEvent()</code>。</p><hr><h2 id="15-OnWrite-与-OnRpdoWrite-的关系"><a href="#15-OnWrite-与-OnRpdoWrite-的关系" class="headerlink" title="15. OnWrite() 与 OnRpdoWrite() 的关系"></a>15. <code>OnWrite()</code> 与 <code>OnRpdoWrite()</code> 的关系</h2><h3 id="15-1-OnWrite"><a href="#15-1-OnWrite" class="headerlink" title="15.1 OnWrite()"></a>15.1 <code>OnWrite()</code></h3><p>对象 <code>0x2000..0xBFFF</code> 经本地 SDO download 或 RPDO 成功写入后，<code>Impl_::OnWrite()</code> 会：</p><ol><li>调用虚函数 <code>Device::OnWrite(idx, subidx)</code>；</li><li>调用通过 <code>Device::OnWrite(std::function&lt;...&gt;)</code> 注册的函数对象；</li><li>查询该本地对象是否属于 RPDO 远程映射；</li><li>若能反查到远程地址，则调用 <code>RpdoWrite()</code>。</li></ol><h3 id="15-2-RpdoWrite"><a href="#15-2-RpdoWrite" class="headerlink" title="15.2 RpdoWrite()"></a>15.2 <code>RpdoWrite()</code></h3><p><code>RpdoWrite()</code> 进一步调用：</p><ol><li>虚函数 <code>OnRpdoWrite(remote_id, remote_idx, remote_subidx)</code>；</li><li>通过 <code>OnRpdoWrite(std::function&lt;...&gt;)</code> 注册的函数对象。</li></ol><p>因此完整关系为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">本地对象成功写入</span><br><span class="line">    -&gt; OnWrite(local idx, local subidx)</span><br><span class="line"></span><br><span class="line">若该本地对象是 RPDO proxy</span><br><span class="line">    -&gt; 反向映射</span><br><span class="line">    -&gt; OnRpdoWrite(remote id, remote idx, remote subidx)</span><br></pre></td></tr></table></figure><h3 id="15-3-锁和回调"><a href="#15-3-锁和回调" class="headerlink" title="15.3 锁和回调"></a>15.3 锁和回调</h3><p><code>Device</code> 使用 <code>std::lock_guard&lt;Impl_&gt;</code> 自动加锁。</p><p>调用用户 <code>std::function</code> 前会：</p><ol><li>复制函数对象；</li><li>使用 <code>UnlockGuard&lt;Impl_&gt;</code> 临时解锁；</li><li>执行用户回调；</li><li>离开作用域后重新加锁。</li></ol><p>目的在于避免用户回调再次调用 <code>Device</code> 公共接口时死锁。</p><p>虚函数 <code>OnWrite()</code>、<code>OnRpdoWrite()</code> 与注册的 <code>std::function</code> 锁环境并不完全相同，派生类实现时必须遵守 <code>Node</code> 对锁状态的约定。</p><hr><h2 id="16-真正的远程-SDO-与-Device-有什么关系"><a href="#16-真正的远程-SDO-与-Device-有什么关系" class="headerlink" title="16. 真正的远程 SDO 与 Device 有什么关系"></a>16. 真正的远程 SDO 与 <code>Device</code> 有什么关系</h2><h3 id="16-1-真正的远程-SDO"><a href="#16-1-真正的远程-SDO" class="headerlink" title="16.1 真正的远程 SDO"></a>16.1 真正的远程 SDO</h3><p>真正通过 CAN 读写远程对象字典的底层接口是：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">co_csdo_up_req();</span><br><span class="line">co_csdo_dn_req();</span><br><span class="line">co_csdo_dn_val_req();</span><br></pre></td></tr></table></figure><p>C++ 应用层封装是：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">lely::canopen::Sdo</span><br></pre></td></tr></table></figure><p>其典型接口是：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Sdo::<span class="built_in">SubmitUpload</span>&lt;T&gt;(...);</span><br><span class="line">Sdo::<span class="built_in">SubmitDownload</span>&lt;T&gt;(...);</span><br></pre></td></tr></table></figure><p><code>Sdo</code> 内部管理 Client-SDO 请求队列和 <code>co_csdo_t</code> 状态机。</p><h3 id="16-2-网络调用链"><a href="#16-2-网络调用链" class="headerlink" title="16.2 网络调用链"></a>16.2 网络调用链</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant App as Application</span><br><span class="line">    participant Queue as coapp::Sdo</span><br><span class="line">    participant CSDO as co_csdo_t</span><br><span class="line">    participant CAN as CAN network</span><br><span class="line">    participant SSDO as Remote Server-SDO</span><br><span class="line">    participant RemoteOD as Remote object dictionary</span><br><span class="line"></span><br><span class="line">    App-&gt;&gt;Queue: SubmitUpload / SubmitDownload</span><br><span class="line">    Queue-&gt;&gt;CSDO: 排队并启动请求</span><br><span class="line">    CSDO-&gt;&gt;CAN: 发送 SDO request CAN frame</span><br><span class="line">    CAN-&gt;&gt;SSDO: 远程节点接收</span><br><span class="line">    SSDO-&gt;&gt;RemoteOD: upload/download indication</span><br><span class="line">    RemoteOD--&gt;&gt;SSDO: 值或 abort code</span><br><span class="line">    SSDO-&gt;&gt;CAN: SDO response CAN frame</span><br><span class="line">    CAN-&gt;&gt;CSDO: 响应</span><br><span class="line">    CSDO--&gt;&gt;Queue: completion</span><br><span class="line">    Queue--&gt;&gt;App: callback / future</span><br></pre></td></tr></table></figure><h3 id="16-3-Device-在远程-SDO-中的作用"><a href="#16-3-Device-在远程-SDO-中的作用" class="headerlink" title="16.3 Device 在远程 SDO 中的作用"></a>16.3 <code>Device</code> 在远程 SDO 中的作用</h3><p><code>Sdo</code> 构造时可以接收：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">Sdo</span>(can_net, co_dev, sdo_number)</span><br></pre></td></tr></table></figure><p>此时本地 <code>co_dev_t</code> 主要提供：</p><ul><li><code>0x1280..0x12FF</code> Client-SDO 参数；</li><li>COB-ID；</li><li>对端 node-ID 等配置。</li></ul><p>但实际网络传输和状态机仍属于 <code>co_csdo_t</code>，不是 <code>Device::Read/Write()</code>。</p><h3 id="16-4-最重要的区分表"><a href="#16-4-最重要的区分表" class="headerlink" title="16.4 最重要的区分表"></a>16.4 最重要的区分表</h3><table><thead><tr><th>需求</th><th>正确接口</th></tr></thead><tbody><tr><td>读取本节点对象，并执行 SDO 权限与回调</td><td><code>Device::Read()</code></td></tr><tr><td>写本节点对象，并执行 SDO 权限与回调</td><td><code>Device::Write()</code></td></tr><tr><td>协议栈内部直接读取本地值</td><td><code>Device::Get()</code></td></tr><tr><td>协议栈内部直接修改本地值</td><td><code>Device::Set()</code></td></tr><tr><td>读取最近一次远程 TPDO 映射到本地的值</td><td><code>Device::RpdoRead()</code></td></tr><tr><td>修改本地主站将通过 TPDO 发往远程 RPDO 的值</td><td><code>Device::TpdoWrite()</code></td></tr><tr><td>触发事件型 TPDO</td><td><code>Device::TpdoWriteEvent()</code></td></tr><tr><td>主动读取远程节点对象字典</td><td><code>Sdo::SubmitUpload()</code> &#x2F; <code>co_csdo_up_req()</code></td></tr><tr><td>主动写远程节点对象字典</td><td><code>Sdo::SubmitDownload()</code> &#x2F; <code>co_csdo_dn_req()</code></td></tr></tbody></table><hr><h2 id="17-从应用视角理解一组典型操作"><a href="#17-从应用视角理解一组典型操作" class="headerlink" title="17. 从应用视角理解一组典型操作"></a>17. 从应用视角理解一组典型操作</h2><h3 id="17-1-本地对象字典-SDO-语义读取"><a href="#17-1-本地对象字典-SDO-语义读取" class="headerlink" title="17.1 本地对象字典 SDO 语义读取"></a>17.1 本地对象字典 SDO 语义读取</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">uint32_t</span> value = <span class="built_in">Read</span>&lt;<span class="type">uint32_t</span>&gt;(<span class="number">0x2000</span>, <span class="number">0x00</span>);</span><br></pre></td></tr></table></figure><p>含义：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">对当前 Device 的本地对象字典执行一次本地 SDO upload。</span><br></pre></td></tr></table></figure><h3 id="17-2-本地对象字典-SDO-语义写入"><a href="#17-2-本地对象字典-SDO-语义写入" class="headerlink" title="17.2 本地对象字典 SDO 语义写入"></a>17.2 本地对象字典 SDO 语义写入</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">Write</span>&lt;<span class="type">uint16_t</span>&gt;(<span class="number">0x2001</span>, <span class="number">0x00</span>, <span class="number">100</span>);</span><br></pre></td></tr></table></figure><p>含义：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">对当前 Device 的本地对象字典执行一次本地 SDO download，</span><br><span class="line">执行权限检查、范围检查和写入回调。</span><br></pre></td></tr></table></figure><h3 id="17-3-读取远程-TPDO-的本地缓存"><a href="#17-3-读取远程-TPDO-的本地缓存" class="headerlink" title="17.3 读取远程 TPDO 的本地缓存"></a>17.3 读取远程 TPDO 的本地缓存</h3><p>在 <code>Device</code> 派生类中：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int32_t</span> position = <span class="built_in">RpdoRead</span>&lt;<span class="type">int32_t</span>&gt;(<span class="number">5</span>, <span class="number">0x6064</span>, <span class="number">0x00</span>);</span><br></pre></td></tr></table></figure><p>含义：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">读取节点 5 的 0x6064:00 所对应的本地 RPDO proxy 值。</span><br><span class="line">不产生 SDO 请求，也不主动请求远程 TPDO。</span><br></pre></td></tr></table></figure><h3 id="17-4-通过-TPDO-为远程-RPDO-准备数据"><a href="#17-4-通过-TPDO-为远程-RPDO-准备数据" class="headerlink" title="17.4 通过 TPDO 为远程 RPDO 准备数据"></a>17.4 通过 TPDO 为远程 RPDO 准备数据</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">TpdoWrite</span>&lt;<span class="type">uint16_t</span>&gt;(<span class="number">5</span>, <span class="number">0x6040</span>, <span class="number">0x00</span>, control_word);</span><br><span class="line"><span class="built_in">TpdoWriteEvent</span>(<span class="number">5</span>, <span class="number">0x6040</span>, <span class="number">0x00</span>);</span><br></pre></td></tr></table></figure><p>含义：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">先把 control_word 写入本地 TPDO proxy，</span><br><span class="line">再触发映射了该对象的事件型 TPDO。</span><br></pre></td></tr></table></figure><h3 id="17-5-通过远程-SDO-写节点-5"><a href="#17-5-通过远程-SDO-写节点-5" class="headerlink" title="17.5 通过远程 SDO 写节点 5"></a>17.5 通过远程 SDO 写节点 5</h3><p>这类操作不走 <code>Device::TpdoWrite()</code>，而应提交到节点 5 对应的 Client-SDO 队列：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Sdo::SubmitDownload(..., 0x6040, 0x00, control_word, ...)</span><br></pre></td></tr></table></figure><p>该操作会产生 SDO CAN 帧，并等待远程 Server-SDO 响应。</p><hr><h2 id="18-完整运行关系图"><a href="#18-完整运行关系图" class="headerlink" title="18. 完整运行关系图"></a>18. 完整运行关系图</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br></pre></td><td class="code"><pre><span class="line">flowchart TB</span><br><span class="line">    APP[Application]</span><br><span class="line"></span><br><span class="line">    subgraph CPP[C++ coapp layer]</span><br><span class="line">        DEVICE[Device]</span><br><span class="line">        SDOQ[Sdo queue]</span><br><span class="line">        NODE[Node / Master]</span><br><span class="line">    end</span><br><span class="line"></span><br><span class="line">    subgraph CCORE[C CANopen core]</span><br><span class="line">        DEV[co_dev_t]</span><br><span class="line">        OBJ[co_obj_t / co_sub_t]</span><br><span class="line">        LOCALSDO[local SDO request\nco_dev_up_req / co_dev_dn_req]</span><br><span class="line">        CSDO[co_csdo_t]</span><br><span class="line">        RPDO[co_rpdo_t]</span><br><span class="line">        TPDO[co_tpdo_t]</span><br><span class="line">        PDODN[co_pdo_dn]</span><br><span class="line">        PDOUP[co_pdo_up]</span><br><span class="line">    end</span><br><span class="line"></span><br><span class="line">    CAN[CAN network]</span><br><span class="line">    REMOTE[Remote CANopen node]</span><br><span class="line"></span><br><span class="line">    APP --&gt; DEVICE</span><br><span class="line">    APP --&gt; SDOQ</span><br><span class="line">    NODE --&gt; DEVICE</span><br><span class="line">    NODE --&gt; CAN</span><br><span class="line"></span><br><span class="line">    DEVICE --&gt; DEV</span><br><span class="line">    DEV --&gt; OBJ</span><br><span class="line"></span><br><span class="line">    DEVICE --&gt; LOCALSDO</span><br><span class="line">    LOCALSDO --&gt; OBJ</span><br><span class="line"></span><br><span class="line">    DEVICE --&gt;|Rpdo/Tpdo address mapping| DEV</span><br><span class="line"></span><br><span class="line">    SDOQ --&gt; CSDO</span><br><span class="line">    CSDO --&gt; CAN</span><br><span class="line">    CAN --&gt; REMOTE</span><br><span class="line"></span><br><span class="line">    CAN --&gt; RPDO</span><br><span class="line">    RPDO --&gt; PDODN</span><br><span class="line">    PDODN --&gt; OBJ</span><br><span class="line"></span><br><span class="line">    TPDO --&gt; PDOUP</span><br><span class="line">    PDOUP --&gt; OBJ</span><br><span class="line">    TPDO --&gt; CAN</span><br></pre></td></tr></table></figure><hr><h2 id="20-只保留与机制直接相关的-C-语法"><a href="#20-只保留与机制直接相关的-C-语法" class="headerlink" title="20. 只保留与机制直接相关的 C++ 语法"></a>20. 只保留与机制直接相关的 C++ 语法</h2><h3 id="20-1-PIMPL"><a href="#20-1-PIMPL" class="headerlink" title="20.1 PIMPL"></a>20.1 PIMPL</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">Impl_</span>;</span><br><span class="line">std::unique_ptr&lt;Impl_&gt; impl_;</span><br></pre></td></tr></table></figure><p>用途：隐藏实现细节，避免在头文件暴露 <code>map</code>、回调、锁和 C 结构。</p><h3 id="20-2-RAII-锁"><a href="#20-2-RAII-锁" class="headerlink" title="20.2 RAII 锁"></a>20.2 RAII 锁</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="function">std::lock_guard&lt;Impl_&gt; <span class="title">lock</span><span class="params">(*impl_)</span></span>;</span><br></pre></td></tr></table></figure><p>进入作用域加锁，离开作用域自动解锁。</p><h3 id="20-3-traits"><a href="#20-3-traits" class="headerlink" title="20.3 traits"></a>20.3 traits</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">using</span> traits = canopen_traits&lt;T&gt;;</span><br></pre></td></tr></table></figure><p>将 C++ 类型映射为：</p><ul><li>CANopen data type index；</li><li>C 类型；</li><li>地址和长度；</li><li>构造、转换和销毁方法。</li></ul><h3 id="20-4-SFINAE"><a href="#20-4-SFINAE" class="headerlink" title="20.4 SFINAE"></a>20.4 SFINAE</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">typename</span> std::enable_if&lt;is_canopen&lt;T&gt;::value, T&gt;::type</span><br></pre></td></tr></table></figure><p>表示只有 <code>T</code> 是 Lely 支持的 CANopen 类型时，该模板函数才参与编译。</p><h3 id="20-5-std-tie"><a href="#20-5-std-tie" class="headerlink" title="20.5 std::tie()"></a>20.5 <code>std::tie()</code></h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">std::<span class="built_in">tie</span>(idx, subidx) = impl_-&gt;<span class="built_in">TpdoMapping</span>(...);</span><br></pre></td></tr></table></figure><p>把函数返回的 tuple 分别写回多个变量。</p><h3 id="20-6-std-function"><a href="#20-6-std-function" class="headerlink" title="20.6 std::function"></a>20.6 <code>std::function</code></h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">std::function&lt;<span class="type">void</span>(<span class="type">uint16_t</span>, <span class="type">uint8_t</span>)&gt; on_write;</span><br></pre></td></tr></table></figure><p>保存普通函数、lambda 或成员函数适配器，作为统一的写入通知接口。</p><p>这些语法都是服务于对象生命周期、类型转换、锁和回调，并没有改变底层 CANopen 数据流。</p><hr>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;Lely-CANopen-coapp-Device：本地-SDO-PDO-读写、远程对象映射与运行机制&quot;&gt;&lt;a href=&quot;#Lely-CANopen-coapp-Device：本地-SDO-PDO-读写、远程对象映射与运行机制&quot; class=&quot;headerlin</summary>
      
    
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/categories/canopen/"/>
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/tags/canopen/"/>
    
  </entry>
  
  <entry>
    <title>Lely CANopen：co_dev 设备对象字典与 DCF 和 PDO 事件机制</title>
    <link href="https://wdfk-prog.space/posts/84d7829a/"/>
    <id>https://wdfk-prog.space/posts/84d7829a/</id>
    <published>2026-07-29T09:36:47.000Z</published>
    <updated>2026-07-29T09:37:37.543Z</updated>
    
    <content type="html"><![CDATA[<meta name="referrer" content="no-referrer" /><h1 id="Lely-CANopen：co-dev-设备对象字典与-EDS-DCF-解析、concise-DCF-和-PDO-事件机制"><a href="#Lely-CANopen：co-dev-设备对象字典与-EDS-DCF-解析、concise-DCF-和-PDO-事件机制" class="headerlink" title="Lely CANopen：co_dev 设备对象字典与 EDS&#x2F;DCF 解析、concise DCF 和 PDO 事件机制"></a>Lely CANopen：co_dev 设备对象字典与 EDS&#x2F;DCF 解析、concise DCF 和 PDO 事件机制</h1><p><img src="https://i-blog.csdnimg.cn/direct/eb13f8d9ad204bb2b4025fc61dc28feb.png" alt="在这里插入图片描述"></p><p>@[toc]</p><h2 id="一句话结论"><a href="#一句话结论" class="headerlink" title="一句话结论"></a>一句话结论</h2><p>Lely 的 <code>co_dev_t</code> 不是协议状态机，也不是简单的 EDS 文件句柄，而是 CANopen 设备描述在运行期的根对象：</p><ul><li>它保存 Node-ID、网络号、设备名称、身份信息、支持的位速率、LSS 和 dummy PDO 类型等设备级元数据；</li><li>它用一棵以 16 位索引为 key 的红黑树管理全部 <code>co_obj_t</code>，每个 <code>co_obj_t</code> 再管理自己的 <code>co_sub_t</code>；</li><li><code>dcf.c</code> 先把 EDS&#x2F;DCF 的 INI 文本解析为通用配置树，再逐层创建 <code>co_dev_t → co_obj_t → co_sub_t</code>；</li><li><code>$NODEID + n</code> 不只是文本替换，解析器会记录相应 flag，<code>co_dev_set_id()</code> 在 Node-ID 改变时遍历整棵对象字典并按差值修正相关值；</li><li>文本 EDS&#x2F;DCF 与 concise DCF 是两种完全不同的格式：前者描述设备结构和元数据，后者是用于参数配置的一组紧凑二进制对象写入记录；</li><li><code>co_dev_tpdo_event()</code> 和 SAM-MPDO 事件接口不会直接发送 CAN 帧，而是根据 PDO 通信参数与映射参数反查受影响的 PDO，并通知上层 PDO 服务；</li><li>在资源受限目标上，运行期解析 EDS&#x2F;DCF 依赖标准 I&#x2F;O 和动态内存，更适合改用 <code>dcf2c</code> 生成静态 <code>co_sdev</code> 描述。</li></ul><p>最重要的心智模型是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line">EDS/DCF text</span><br><span class="line">    ↓ parse INI sections and keys</span><br><span class="line">config_t</span><br><span class="line">    ↓ build device description</span><br><span class="line">co_dev_t</span><br><span class="line">    ↓ 16-bit index red-black tree</span><br><span class="line">co_obj_t</span><br><span class="line">    ↓ 8-bit sub-index red-black tree</span><br><span class="line">co_sub_t</span><br><span class="line">    ↓</span><br><span class="line">metadata + current value + access + mapping + callbacks</span><br><span class="line"></span><br><span class="line">concise DCF binary</span><br><span class="line">    ↓ replay object-value records</span><br><span class="line">co_dev_t object dictionary</span><br><span class="line"></span><br><span class="line">local value changed</span><br><span class="line">    ↓ resolve PDO mappings</span><br><span class="line">TPDO or SAM-MPDO event indication</span><br></pre></td></tr></table></figure><hr><h2 id="1-co-dev-t-在-CANopen-设备模型中的位置"><a href="#1-co-dev-t-在-CANopen-设备模型中的位置" class="headerlink" title="1. co_dev_t 在 CANopen 设备模型中的位置"></a>1. <code>co_dev_t</code> 在 CANopen 设备模型中的位置</h2><p>CiA 301 把 CANopen 设备概括为通信功能、对象字典和应用程序三部分，对象字典位于通信对象与应用对象之间。对象通过 16 位索引寻址，复合对象中的数据字段再通过 8 位子索引寻址。</p><p>Lely 将其直接映射为三级对象：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">co_dev_t</span><br><span class="line">    一个 CANopen 设备及其完整对象字典</span><br><span class="line"></span><br><span class="line">co_obj_t</span><br><span class="line">    一个 16-bit index 对象</span><br><span class="line"></span><br><span class="line">co_sub_t</span><br><span class="line">    一个 8-bit sub-index 条目</span><br></pre></td></tr></table></figure><p>前一篇文章解决的是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">一个 index 内部有哪些 sub-object</span><br><span class="line">每个 sub-object 的值放在哪里</span><br><span class="line">SDO/PDO 如何读写一个 sub-object</span><br></pre></td></tr></table></figure><p>本篇解决的是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">整个设备有哪些 index</span><br><span class="line">设备描述从哪里来</span><br><span class="line">Node-ID 改变时哪些值需要同步变化</span><br><span class="line">文本 DCF 和 concise DCF 如何进入对象字典</span><br><span class="line">一个本地值变化如何定位相关 TPDO</span><br></pre></td></tr></table></figure><h3 id="1-1-co-dev-t-不负责什么"><a href="#1-1-co-dev-t-不负责什么" class="headerlink" title="1.1 co_dev_t 不负责什么"></a>1.1 <code>co_dev_t</code> 不负责什么</h3><p><code>co_dev_t</code> 本身不执行：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">CAN 控制器收发</span><br><span class="line">SocketCAN I/O</span><br><span class="line">NMT 状态机调度</span><br><span class="line">SDO 分段或块传输</span><br><span class="line">PDO 定时器和 SYNC 调度</span><br><span class="line">EDS 编辑与生成</span><br><span class="line">线程同步</span><br></pre></td></tr></table></figure><p>它提供的是设备级描述、对象查找、值访问和事件解析基础。NMT、SDO、PDO、EMCY、LSS 等协议对象在这棵对象字典上读取配置并访问数据。</p><hr><h2 id="2-五个源文件分别负责什么"><a href="#2-五个源文件分别负责什么" class="headerlink" title="2. 五个源文件分别负责什么"></a>2. 五个源文件分别负责什么</h2><h3 id="2-1-include-lely-co-dev-h"><a href="#2-1-include-lely-co-dev-h" class="headerlink" title="2.1 include/lely/co/dev.h"></a>2.1 <code>include/lely/co/dev.h</code></h3><p>这是 <code>co_dev_t</code> 的公开 C API，主要提供：</p><ul><li>设备创建、销毁和 Node-ID 管理；</li><li>对象索引枚举、插入、移除和查找；</li><li>设备名称、供应商、产品、版本等元数据接口；</li><li>支持位速率、LSS、dummy PDO 类型等能力描述；</li><li>设备级 current value 读写接口和 typed getter&#x2F;setter；</li><li>concise DCF 的内存与文件读写接口；</li><li>TPDO 与 SAM-MPDO 事件 indication 注册和触发接口。</li></ul><p>应用通常只持有不透明指针：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">co_dev_t</span> *dev;</span><br></pre></td></tr></table></figure><h3 id="2-2-include-lely-co-detail-dev-h"><a href="#2-2-include-lely-co-detail-dev-h" class="headerlink" title="2.2 include/lely/co/detail/dev.h"></a>2.2 <code>include/lely/co/detail/dev.h</code></h3><p>这是内部头文件，公开 <code>struct __co_dev</code> 的真实布局。</p><p>理解以下问题时必须阅读它：</p><ul><li><code>co_dev_t</code> 实际保存哪些字段；</li><li>哪些字段会被条件编译移除；</li><li>对象字典树和设备元数据的所有权；</li><li>TPDO、MPDO 回调是否存在于当前 ABI。</li></ul><h3 id="2-3-src-co-dev-c"><a href="#2-3-src-co-dev-c" class="headerlink" title="2.3 src/co/dev.c"></a>2.3 <code>src/co/dev.c</code></h3><p>该文件实现六类机制：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">1. co_dev_t 生命周期</span><br><span class="line">2. 对象红黑树管理</span><br><span class="line">3. Node-ID 相对值重定位</span><br><span class="line">4. 设备级元数据和 current value 访问</span><br><span class="line">5. concise DCF 编解码</span><br><span class="line">6. TPDO 与 SAM-MPDO 映射事件解析</span><br></pre></td></tr></table></figure><h3 id="2-4-include-lely-co-dcf-h"><a href="#2-4-include-lely-co-dcf-h" class="headerlink" title="2.4 include/lely/co/dcf.h"></a>2.4 <code>include/lely/co/dcf.h</code></h3><p>公开接口非常小，核心职责是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">EDS/DCF file or text</span><br><span class="line">    → dynamically created co_dev_t</span><br></pre></td></tr></table></figure><p>即从文件或内存文本创建、初始化一个运行时设备描述。</p><h3 id="2-5-src-co-dcf-c"><a href="#2-5-src-co-dcf-c" class="headerlink" title="2.5 src/co/dcf.c"></a>2.5 <code>src/co/dcf.c</code></h3><p>这是运行期 EDS&#x2F;DCF 构造器，负责：</p><ul><li>调用通用 INI parser 读取 section&#x2F;key；</li><li>解析 <code>[DeviceInfo]</code>、对象列表和对象 section；</li><li>创建 <code>co_obj_t</code> 与 <code>co_sub_t</code>；</li><li>解析数据类型、访问权限、limits、default、ParameterValue 和 PDO mapping；</li><li>处理 <code>$NODEID</code> 表达式；</li><li>处理 CompactSubObj、CompactPDO、UploadFile 和 DownloadFile；</li><li>解析 <code>[DeviceComissioning]</code> 并设置实际 Node-ID、网络号和波特率；</li><li>在失败时回收已经构造的对象。</li></ul><hr><h2 id="3-总体架构和两条不同的数据入口"><a href="#3-总体架构和两条不同的数据入口" class="headerlink" title="3. 总体架构和两条不同的数据入口"></a>3. 总体架构和两条不同的数据入口</h2><p>Lely 中有两条容易混淆的 DCF 路径。</p><h3 id="3-1-文本-EDS-DCF：构建设备结构"><a href="#3-1-文本-EDS-DCF：构建设备结构" class="headerlink" title="3.1 文本 EDS&#x2F;DCF：构建设备结构"></a>3.1 文本 EDS&#x2F;DCF：构建设备结构</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[&quot;EDS or DCF file&quot;] --&gt; B[&quot;config_parse_ini_file&quot;]</span><br><span class="line">    C[&quot;EDS or DCF text&quot;] --&gt; D[&quot;config_parse_ini_text&quot;]</span><br><span class="line">    B --&gt; E[&quot;config_t section and key tree&quot;]</span><br><span class="line">    D --&gt; E</span><br><span class="line">    E --&gt; F[&quot;co_dev_parse_cfg&quot;]</span><br><span class="line">    F --&gt; G[&quot;parse DeviceInfo&quot;]</span><br><span class="line">    F --&gt; H[&quot;parse object lists&quot;]</span><br><span class="line">    H --&gt; I[&quot;build co_obj_t&quot;]</span><br><span class="line">    I --&gt; J[&quot;build co_sub_t&quot;]</span><br><span class="line">    F --&gt; K[&quot;parse CompactPDO&quot;]</span><br><span class="line">    F --&gt; L[&quot;parse DeviceComissioning&quot;]</span><br><span class="line">    G --&gt; M[&quot;runtime co_dev_t&quot;]</span><br><span class="line">    J --&gt; M</span><br><span class="line">    K --&gt; M</span><br><span class="line">    L --&gt; M</span><br></pre></td></tr></table></figure><p>它回答的是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">这个设备有哪些对象</span><br><span class="line">对象是什么类型</span><br><span class="line">子对象如何组织</span><br><span class="line">访问权限和映射属性是什么</span><br><span class="line">默认值和初始值是什么</span><br></pre></td></tr></table></figure><h3 id="3-2-concise-DCF：向已有对象字典回放参数"><a href="#3-2-concise-DCF：向已有对象字典回放参数" class="headerlink" title="3.2 concise DCF：向已有对象字典回放参数"></a>3.2 concise DCF：向已有对象字典回放参数</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    A[&quot;concise DCF binary&quot;] --&gt; B[&quot;co_dev_read_dcf&quot;]</span><br><span class="line">    B --&gt; C[&quot;decode index sub-index size data&quot;]</span><br><span class="line">    C --&gt; D[&quot;find existing co_sub_t&quot;]</span><br><span class="line">    D --&gt; E[&quot;replace current value&quot;]</span><br></pre></td></tr></table></figure><p>它不创建对象结构，只向已经存在的对象字典写入 current value。</p><p>因此：</p><blockquote><p>文本 EDS&#x2F;DCF 是设备描述；concise DCF 是参数写入清单。二者都叫 DCF，但用途、格式和调用链完全不同。</p></blockquote><hr><h2 id="4-struct-co-dev：设备根对象的真实布局"><a href="#4-struct-co-dev：设备根对象的真实布局" class="headerlink" title="4. struct __co_dev：设备根对象的真实布局"></a>4. <code>struct __co_dev</code>：设备根对象的真实布局</h2><p>内部字段可以按职责分为五组。</p><table><thead><tr><th>分类</th><th>典型字段</th><th>作用</th></tr></thead><tbody><tr><td>设备地址</td><td><code>netid</code>、<code>id</code></td><td>网络号和 Node-ID</td></tr><tr><td>对象字典</td><td><code>tree</code></td><td>以 16 位 index 为 key 的对象红黑树</td></tr><tr><td>描述字符串</td><td><code>name</code>、<code>vendor_name</code>、<code>product_name</code>、<code>order_code</code></td><td>EDS&#x2F;DCF 中的设备描述信息</td></tr><tr><td>身份与能力</td><td><code>vendor_id</code>、<code>product_code</code>、<code>revision</code>、<code>baud</code>、<code>rate</code>、<code>lss</code>、<code>dummy</code></td><td>设备身份、支持速率、LSS 和 dummy PDO 类型</td></tr><tr><td>事件接口</td><td>TPDO、SAM-MPDO indication 与用户数据</td><td>把对象变化通知给上层 PDO 服务</td></tr></tbody></table><h3 id="4-1-tree-是设备对象字典的核心"><a href="#4-1-tree-是设备对象字典的核心" class="headerlink" title="4.1 tree 是设备对象字典的核心"></a>4.1 <code>tree</code> 是设备对象字典的核心</h3><p>设备初始化时，<code>tree</code> 使用 16 位无符号整数比较函数：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">key = co_obj_t::idx</span><br><span class="line">order = ascending unsigned 16-bit index</span><br></pre></td></tr></table></figure><p>因此对象遍历天然按索引升序，以下操作不需要线性扫描数组：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">find index</span><br><span class="line">find next index</span><br><span class="line">find previous index</span><br><span class="line">get first object</span><br><span class="line">get last object</span><br><span class="line">list indices in a range</span><br></pre></td></tr></table></figure><h3 id="4-2-元数据与对象字典值不是同一份信息"><a href="#4-2-元数据与对象字典值不是同一份信息" class="headerlink" title="4.2 元数据与对象字典值不是同一份信息"></a>4.2 元数据与对象字典值不是同一份信息</h3><p>例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">dev-&gt;vendor_id</span><br></pre></td></tr></table></figure><p>是设备描述级元数据，而对象 <code>0x1018:01</code> 也是 Vendor-ID 的网络可访问表示。</p><p>两者在概念上有关，但 <code>co_dev_t</code> 不会自动保证所有元数据字段与任意对象值永远双向同步。解析器在构建设备时会分别填充它们；运行期修改某一侧时，应用仍应明确维护一致性。</p><h3 id="4-3-条件编译会改变结构体-ABI"><a href="#4-3-条件编译会改变结构体-ABI" class="headerlink" title="4.3 条件编译会改变结构体 ABI"></a>4.3 条件编译会改变结构体 ABI</h3><p>名称、TPDO、MPDO 等功能字段可能被宏裁剪。因此：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">library build macros</span><br><span class="line">must equal</span><br><span class="line">application build macros</span><br></pre></td></tr></table></figure><p>否则 <code>sizeof(struct __co_dev)</code> 和字段偏移可能不一致，不能把这些宏仅看成链接时可选功能。</p><hr><h2 id="5-设备生命周期与对象所有权"><a href="#5-设备生命周期与对象所有权" class="headerlink" title="5. 设备生命周期与对象所有权"></a>5. 设备生命周期与对象所有权</h2><h3 id="5-1-创建和初始化"><a href="#5-1-创建和初始化" class="headerlink" title="5.1 创建和初始化"></a>5.1 创建和初始化</h3><p>动态模式下：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">co_dev_create(id)</span><br><span class="line">    ↓</span><br><span class="line">allocate struct __co_dev</span><br><span class="line">    ↓</span><br><span class="line">__co_dev_init(dev, id)</span><br><span class="line">    ↓</span><br><span class="line">initialize metadata and object tree</span><br></pre></td></tr></table></figure><p>合法 Node-ID 为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">1 ... 127</span><br><span class="line">255 for unconfigured device</span><br></pre></td></tr></table></figure><p><code>255</code> 不会作为正常 CANopen 节点地址上总线，它表示设备描述当前尚未绑定实际 Node-ID。</p><h3 id="5-2-插入对象"><a href="#5-2-插入对象" class="headerlink" title="5.2 插入对象"></a>5.2 插入对象</h3><p><code>co_dev_insert_obj()</code> 的主要判断顺序为：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[&quot;co_dev_insert_obj&quot;] --&gt; B&#123;&quot;object belongs to another device&quot;&#125;</span><br><span class="line">    B --&gt;|yes| X[&quot;return failure&quot;]</span><br><span class="line">    B --&gt;|no| C&#123;&quot;object already belongs to this device&quot;&#125;</span><br><span class="line">    C --&gt;|yes| Y[&quot;return success&quot;]</span><br><span class="line">    C --&gt;|no| D&#123;&quot;same index already exists&quot;&#125;</span><br><span class="line">    D --&gt;|yes| X</span><br><span class="line">    D --&gt;|no| E[&quot;set obj device pointer&quot;]</span><br><span class="line">    E --&gt; F[&quot;insert rbnode into device tree&quot;]</span><br><span class="line">    F --&gt; Y</span><br></pre></td></tr></table></figure><p>它具有两个重要性质：</p><ol><li>同一对象重复插入同一设备是幂等的；</li><li>同一索引不能同时存在两个不同对象。</li></ol><h3 id="5-3-移除对象"><a href="#5-3-移除对象" class="headerlink" title="5.3 移除对象"></a>5.3 移除对象</h3><p><code>co_dev_remove_obj()</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">verify obj belongs to dev</span><br><span class="line">    ↓</span><br><span class="line">remove rbnode</span><br><span class="line">    ↓</span><br><span class="line">reinitialize node key</span><br><span class="line">    ↓</span><br><span class="line">clear obj-&gt;dev</span><br></pre></td></tr></table></figure><p>移除只解除所有权，不一定销毁对象。动态模式下若随后调用 <code>co_obj_destroy()</code>，对象和子对象才被释放。</p><h3 id="5-4-销毁设备"><a href="#5-4-销毁设备" class="headerlink" title="5.4 销毁设备"></a>5.4 销毁设备</h3><p>动态模式下，<code>co_dev_destroy()</code> 最终会：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">destroy every co_obj_t in tree</span><br><span class="line">    ↓</span><br><span class="line">each co_obj_t destroys its co_sub_t members</span><br><span class="line">    ↓</span><br><span class="line">free device strings</span><br><span class="line">    ↓</span><br><span class="line">free co_dev_t</span><br></pre></td></tr></table></figure><p>所有权关系为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">co_dev_t owns co_obj_t</span><br><span class="line">co_obj_t owns co_sub_t</span><br></pre></td></tr></table></figure><p>这也是 DCF 解析失败时只需清理根设备即可回收整棵对象字典的原因。</p><hr><h2 id="6-对象查找和设备级值访问"><a href="#6-对象查找和设备级值访问" class="headerlink" title="6. 对象查找和设备级值访问"></a>6. 对象查找和设备级值访问</h2><h3 id="6-1-查找路径"><a href="#6-1-查找路径" class="headerlink" title="6.1 查找路径"></a>6.1 查找路径</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">co_dev_find_sub(dev, idx, subidx)</span><br><span class="line">    ↓</span><br><span class="line">co_dev_find_obj(dev, idx)</span><br><span class="line">    ↓ red-black tree</span><br><span class="line">co_obj_find_sub(obj, subidx)</span><br><span class="line">    ↓ red-black tree</span><br><span class="line">co_sub_t</span><br></pre></td></tr></table></figure><p>时间复杂度可概括为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">O(log number_of_objects)</span><br><span class="line">+</span><br><span class="line">O(log number_of_subobjects_in_object)</span><br></pre></td></tr></table></figure><h3 id="6-2-co-dev-get-val-只是便捷封装"><a href="#6-2-co-dev-get-val-只是便捷封装" class="headerlink" title="6.2 co_dev_get_val() 只是便捷封装"></a>6.2 <code>co_dev_get_val()</code> 只是便捷封装</h3><p>设备级 current value API 本质上是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">find sub-object</span><br><span class="line">    ↓</span><br><span class="line">call corresponding co_sub API</span><br></pre></td></tr></table></figure><p>例如设备级 setter 不会因为入口在 <code>co_dev_t</code> 就增加协议语义。</p><p>它仍然：</p><ul><li>不检查 SDO 网络访问权限；</li><li>不调用 download indication；</li><li>不生成 SDO abort code；</li><li>不自动触发 TPDO；</li><li>不执行应用状态约束。</li></ul><p>所以应区分：</p><table><thead><tr><th>入口</th><th>语义</th></tr></thead><tbody><tr><td><code>co_dev_set_val()</code> &#x2F; typed setter</td><td>本地直接修改对象字典缓存</td></tr><tr><td><code>co_sub_dn_ind()</code></td><td>模拟或处理协议级写入，执行权限和回调</td></tr><tr><td><code>co_dev_tpdo_event()</code></td><td>通知映射到该条目的 TPDO 发生事件</td></tr></tbody></table><p>典型应用更新流程可能是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">application changes value</span><br><span class="line">    ↓</span><br><span class="line">co_dev_set_val_*()</span><br><span class="line">    ↓</span><br><span class="line">co_dev_tpdo_event(idx, subidx)</span><br></pre></td></tr></table></figure><p>直接 setter 与 PDO event 是两个独立动作。</p><hr><h2 id="7-Node-ID-与-NODEID-n-的重定位机制"><a href="#7-Node-ID-与-NODEID-n-的重定位机制" class="headerlink" title="7. Node-ID 与 $NODEID + n 的重定位机制"></a>7. Node-ID 与 <code>$NODEID + n</code> 的重定位机制</h2><p>这是 <code>co_dev.c</code> 与 <code>dcf.c</code> 之间最关键的协作之一。</p><h3 id="7-1-为什么解析器先使用-0xFF"><a href="#7-1-为什么解析器先使用-0xFF" class="headerlink" title="7.1 为什么解析器先使用 0xFF"></a>7.1 为什么解析器先使用 <code>0xFF</code></h3><p>从 EDS&#x2F;DCF 构建设备时，解析器先初始化：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">dev-&gt;id = 0xFF</span><br></pre></td></tr></table></figure><p>此时对象中的某些字段可能写成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">$NODEID + 0x180</span><br><span class="line">$NODEID + 0x600</span><br><span class="line">$NODEID + 0x700</span><br></pre></td></tr></table></figure><p>解析器在读取这些值时，不只计算一个数值，还会为对应值设置 Node-ID-relative flag。</p><p>可能涉及：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">minimum value</span><br><span class="line">maximum value</span><br><span class="line">default value</span><br><span class="line">current value</span><br></pre></td></tr></table></figure><h3 id="7-2-co-dev-set-id-不是简单赋值"><a href="#7-2-co-dev-set-id-不是简单赋值" class="headerlink" title="7.2 co_dev_set_id() 不是简单赋值"></a>7.2 <code>co_dev_set_id()</code> 不是简单赋值</h3><p>设置实际 Node-ID 时，函数遍历整个对象树：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[&quot;co_dev_set_id old to new&quot;] --&gt; B[&quot;iterate every co_obj_t&quot;]</span><br><span class="line">    B --&gt; C[&quot;iterate every co_sub_t&quot;]</span><br><span class="line">    C --&gt; D&#123;&quot;MIN_NODEID flag&quot;&#125;</span><br><span class="line">    D --&gt;|yes| E[&quot;adjust minimum by delta&quot;]</span><br><span class="line">    C --&gt; F&#123;&quot;MAX_NODEID flag&quot;&#125;</span><br><span class="line">    F --&gt;|yes| G[&quot;adjust maximum by delta&quot;]</span><br><span class="line">    C --&gt; H&#123;&quot;DEF_NODEID flag&quot;&#125;</span><br><span class="line">    H --&gt;|yes| I[&quot;adjust default by delta&quot;]</span><br><span class="line">    C --&gt; J&#123;&quot;VAL_NODEID flag&quot;&#125;</span><br><span class="line">    J --&gt;|yes| K[&quot;adjust current value by delta&quot;]</span><br><span class="line">    E --&gt; L[&quot;store new Node-ID&quot;]</span><br><span class="line">    G --&gt; L</span><br><span class="line">    I --&gt; L</span><br><span class="line">    K --&gt; L</span><br></pre></td></tr></table></figure><p>其中：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">delta = new_node_id - old_node_id</span><br></pre></td></tr></table></figure><p>因此 <code>$NODEID + n</code> 的运行时语义是可重定位值，而不是只在文件加载时展开一次。</p><h3 id="7-3-工程影响"><a href="#7-3-工程影响" class="headerlink" title="7.3 工程影响"></a>7.3 工程影响</h3><p>该机制使同一份 EDS 可以复用于多个节点：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">EDS describes base COB-ID expressions</span><br><span class="line">actual Node-ID selected later</span><br><span class="line">all marked values are rebased automatically</span><br></pre></td></tr></table></figure><p>但也带来约束：</p><ul><li>只有被正确设置 Node-ID flag 的值才会调整；</li><li>应用直接写入一个绝对数值不会自动获得 relative 属性；</li><li>清除或误设 flag 会造成 Node-ID 更改后的 COB-ID 错误；</li><li>运行期改变 Node-ID 时，应确保协议服务未同时使用旧配置。</li></ul><hr><h2 id="8-EDS-DCF-文本解析入口"><a href="#8-EDS-DCF-文本解析入口" class="headerlink" title="8. EDS&#x2F;DCF 文本解析入口"></a>8. EDS&#x2F;DCF 文本解析入口</h2><p><code>dcf.h</code> 暴露两类入口：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">file path</span><br><span class="line">in-memory text</span><br></pre></td></tr></table></figure><p>两者的内部流程相同：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[&quot;create device from file or text&quot;] --&gt; B[&quot;allocate co_dev_t&quot;]</span><br><span class="line">    B --&gt; C[&quot;initialize with Node-ID 255&quot;]</span><br><span class="line">    C --&gt; D[&quot;parse INI into config_t&quot;]</span><br><span class="line">    D --&gt; E[&quot;co_dev_parse_cfg&quot;]</span><br><span class="line">    E --&gt; F&#123;&quot;success&quot;&#125;</span><br><span class="line">    F --&gt;|yes| G[&quot;return populated device&quot;]</span><br><span class="line">    F --&gt;|no| H[&quot;finalize partial device&quot;]</span><br><span class="line">    H --&gt; I[&quot;return failure&quot;]</span><br></pre></td></tr></table></figure><h3 id="8-1-两阶段解析的意义"><a href="#8-1-两阶段解析的意义" class="headerlink" title="8.1 两阶段解析的意义"></a>8.1 两阶段解析的意义</h3><p><code>dcf.c</code> 没有一边逐字符读取文件一边直接操作对象树，而是先得到通用配置结构，再构建设备。</p><p>优点：</p><ul><li>文件和字符串入口共享后半段逻辑；</li><li>section&#x2F;key 查找与 CANopen 对象构建分离；</li><li>错误信息能关联到配置位置；</li><li>CompactSubObj、CompactPDO 等跨 section 机制更容易实现。</li></ul><p>限制：</p><ul><li>需要保存配置树和动态对象树；</li><li>峰值内存高于直接流式解析；</li><li>不适合极小 MCU 在运行期加载大型 EDS。</li></ul><hr><h2 id="9-co-dev-parse-cfg-的总解析顺序"><a href="#9-co-dev-parse-cfg-的总解析顺序" class="headerlink" title="9. co_dev_parse_cfg() 的总解析顺序"></a>9. <code>co_dev_parse_cfg()</code> 的总解析顺序</h2><p>核心顺序可以概括为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">[DeviceInfo]</span><br><span class="line">    ↓</span><br><span class="line">[DummyUsage]</span><br><span class="line">    ↓</span><br><span class="line">[MandatoryObjects]</span><br><span class="line">[OptionalObjects]</span><br><span class="line">[ManufacturerObjects]</span><br><span class="line">    ↓</span><br><span class="line">construct every explicit object</span><br><span class="line">    ↓</span><br><span class="line">[CompactPDO]</span><br><span class="line">    ↓</span><br><span class="line">[DeviceComissioning]</span><br></pre></td></tr></table></figure><p>这种顺序并非任意安排。</p><h3 id="9-1-先构造显式对象，再处理-CompactPDO"><a href="#9-1-先构造显式对象，再处理-CompactPDO" class="headerlink" title="9.1 先构造显式对象，再处理 CompactPDO"></a>9.1 先构造显式对象，再处理 CompactPDO</h3><p>CompactPDO 可以自动补建缺失的 PDO 通信参数和映射参数对象。</p><p>若先创建 CompactPDO，再读取显式对象：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">synthesized object</span><br><span class="line">may collide with</span><br><span class="line">explicit object</span><br></pre></td></tr></table></figure><p>所以源码先构建文件中明确列出的对象，再对缺失项进行补建，避免自动对象覆盖用户定义。</p><h3 id="9-2-最后读取-DeviceComissioning"><a href="#9-2-最后读取-DeviceComissioning" class="headerlink" title="9.2 最后读取 DeviceComissioning"></a>9.2 最后读取 DeviceComissioning</h3><p>对象解析阶段可以先在 Node-ID <code>255</code> 下展开 <code>$NODEID</code> 表达式并记录 relative flags。最后读取实际 Node-ID 时，<code>co_dev_set_id()</code> 统一重定位相关值。</p><p>这把：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">syntax parsing</span><br></pre></td></tr></table></figure><p>和：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">actual network address binding</span><br></pre></td></tr></table></figure><p>分成两个阶段。</p><hr><h2 id="10-DeviceInfo-如何映射到-co-dev-t"><a href="#10-DeviceInfo-如何映射到-co-dev-t" class="headerlink" title="10. [DeviceInfo] 如何映射到 co_dev_t"></a>10. <code>[DeviceInfo]</code> 如何映射到 <code>co_dev_t</code></h2><p>设备级 section 主要填充描述和能力信息。</p><table><thead><tr><th>EDS&#x2F;DCF 信息</th><th><code>co_dev_t</code> 中的用途</th></tr></thead><tbody><tr><td>VendorName</td><td>供应商名称</td></tr><tr><td>ProductName</td><td>产品名称</td></tr><tr><td>OrderCode</td><td>订货号</td></tr><tr><td>VendorNumber</td><td>Vendor-ID</td></tr><tr><td>ProductNumber</td><td>Product code</td></tr><tr><td>RevisionNumber</td><td>revision number</td></tr><tr><td>BaudRate_x</td><td>支持的位速率位图</td></tr><tr><td>LSS_Supported</td><td>LSS 能力标志</td></tr><tr><td>DynamicChannelsSupported 等能力项</td><td>对应设备能力或诊断信息</td></tr></tbody></table><p>对象字典中可能同时存在：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">0x1008 manufacturer device name</span><br><span class="line">0x1009 hardware version</span><br><span class="line">0x100A software version</span><br><span class="line">0x1018 identity object</span><br></pre></td></tr></table></figure><p>解析器不会把所有 DeviceInfo 字段简单视为对象 <code>0x1000</code> 区域的替代品。设备级元数据与网络对象各有用途：</p><ul><li>元数据便于工具在不逐项读取对象时识别设备；</li><li>对象字典条目供 SDO、PDO 和应用访问；</li><li>一致性由输入 DCF、生成工具和应用共同保证。</li></ul><h3 id="10-1-DummyUsage"><a href="#10-1-DummyUsage" class="headerlink" title="10.1 [DummyUsage]"></a>10.1 <code>[DummyUsage]</code></h3><p>DummyUsage 描述某些基本数据类型是否允许用作 PDO mapping 的填充项。</p><p><code>co_dev_t::dummy</code> 以位图保存这些能力，供 PDO 映射验证逻辑查询。它不是实际对象值，也不会自动生成一个制造商对象。</p><hr><h2 id="11-三类对象列表如何驱动对象构建"><a href="#11-三类对象列表如何驱动对象构建" class="headerlink" title="11. 三类对象列表如何驱动对象构建"></a>11. 三类对象列表如何驱动对象构建</h2><p>EDS&#x2F;DCF 通常通过以下 section 列出对象索引：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">[MandatoryObjects]</span><br><span class="line">[OptionalObjects]</span><br><span class="line">[ManufacturerObjects]</span><br></pre></td></tr></table></figure><p>解析流程为：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[&quot;read SupportedObjects count&quot;] --&gt; B[&quot;allocate index list&quot;]</span><br><span class="line">    B --&gt; C[&quot;parse entries 1 through N&quot;]</span><br><span class="line">    C --&gt; D[&quot;format object section name from index&quot;]</span><br><span class="line">    D --&gt; E[&quot;co_obj_build&quot;]</span><br><span class="line">    E --&gt; F[&quot;co_obj_parse_cfg&quot;]</span><br><span class="line">    F --&gt; G[&quot;insert object into device tree&quot;]</span><br></pre></td></tr></table></figure><h3 id="11-1-列表类别不改变红黑树存储方式"><a href="#11-1-列表类别不改变红黑树存储方式" class="headerlink" title="11.1 列表类别不改变红黑树存储方式"></a>11.1 列表类别不改变红黑树存储方式</h3><p>Mandatory、Optional 和 Manufacturer 主要是 DCF 描述类别。进入 <code>co_dev_t</code> 后，所有对象都按 index 进入同一棵红黑树。</p><p>也就是说运行期查找不会先问：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">is this mandatory or optional</span><br></pre></td></tr></table></figure><p>而是直接按索引查找。</p><h3 id="11-2-重复索引的结果"><a href="#11-2-重复索引的结果" class="headerlink" title="11.2 重复索引的结果"></a>11.2 重复索引的结果</h3><p>若不同列表或自动生成流程试图插入相同索引的另一个对象：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">co_dev_insert_obj()</span><br><span class="line">    → duplicate key</span><br><span class="line">    → failure</span><br></pre></td></tr></table></figure><p>因此对象列表不是可以无条件合并的普通数组。</p><h3 id="11-3-强制对象缺失的处理边界"><a href="#11-3-强制对象缺失的处理边界" class="headerlink" title="11.3 强制对象缺失的处理边界"></a>11.3 强制对象缺失的处理边界</h3><p>解析器会对典型强制对象缺失发出诊断，例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">0x1000 Device type</span><br><span class="line">0x1001 Error register</span><br><span class="line">0x1018 Identity object</span><br></pre></td></tr></table></figure><p>但 EDS&#x2F;DCF 解析器的职责并不是完整实现所有设备 profile 的一致性证明。某些问题是 warning，某些结构错误才会导致创建失败。</p><hr><h2 id="12-单个对象-section-的构建流程"><a href="#12-单个对象-section-的构建流程" class="headerlink" title="12. 单个对象 section 的构建流程"></a>12. 单个对象 section 的构建流程</h2><h3 id="12-1-先创建-co-obj-t"><a href="#12-1-先创建-co-obj-t" class="headerlink" title="12.1 先创建 co_obj_t"></a>12.1 先创建 <code>co_obj_t</code></h3><p><code>co_obj_build()</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">parse object index</span><br><span class="line">    ↓</span><br><span class="line">create co_obj_t</span><br><span class="line">    ↓</span><br><span class="line">insert into co_dev_t</span><br><span class="line">    ↓</span><br><span class="line">set name and object code</span><br><span class="line">    ↓</span><br><span class="line">parse sub-object structure</span><br></pre></td></tr></table></figure><p>若后续步骤失败，构造器会将对象从设备移除并销毁，避免留下半初始化对象。</p><h3 id="12-2-ObjectType-决定后续结构分支"><a href="#12-2-ObjectType-决定后续结构分支" class="headerlink" title="12.2 ObjectType 决定后续结构分支"></a>12.2 <code>ObjectType</code> 决定后续结构分支</h3><p>主要分为两类：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">simple object</span><br><span class="line">    VAR or DOMAIN</span><br><span class="line"></span><br><span class="line">structured object</span><br><span class="line">    ARRAY or RECORD or DEFSTRUCT</span><br></pre></td></tr></table></figure><h4 id="简单对象"><a href="#简单对象" class="headerlink" title="简单对象"></a>简单对象</h4><p>通常只有 <code>sub-index 0</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">object section</span><br><span class="line">    acts as</span><br><span class="line">sub-object 0 description</span><br></pre></td></tr></table></figure><p>DOMAIN 在部分场景下可省略显式 DataType，因为对象代码已经确定其数据语义。</p><h4 id="复合对象"><a href="#复合对象" class="headerlink" title="复合对象"></a>复合对象</h4><p>必须描述成员结构，并在以下两种形式中选择一种：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">SubNumber</span><br><span class="line">or</span><br><span class="line">CompactSubObj</span><br></pre></td></tr></table></figure><p>源码要求二者不能同时作为同一对象的结构来源。</p><hr><h2 id="13-显式子对象与-CompactSubObj"><a href="#13-显式子对象与-CompactSubObj" class="headerlink" title="13. 显式子对象与 CompactSubObj"></a>13. 显式子对象与 CompactSubObj</h2><h3 id="13-1-显式子对象"><a href="#13-1-显式子对象" class="headerlink" title="13.1 显式子对象"></a>13.1 显式子对象</h3><p>复合对象可通过独立 section 描述成员，例如概念上：</p><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">[1018]</span></span><br><span class="line"><span class="attr">ObjectType</span>=<span class="number">0</span>x09</span><br><span class="line"><span class="attr">SubNumber</span>=<span class="number">5</span></span><br><span class="line"></span><br><span class="line"><span class="section">[1018sub0]</span></span><br><span class="line"><span class="attr">ParameterName</span>=Number of entries</span><br><span class="line"><span class="attr">DataType</span>=<span class="number">0</span>x0005</span><br><span class="line"><span class="attr">AccessType</span>=ro</span><br><span class="line"></span><br><span class="line"><span class="section">[1018sub1]</span></span><br><span class="line"><span class="attr">ParameterName</span>=Vendor-ID</span><br><span class="line"><span class="attr">DataType</span>=<span class="number">0</span>x0007</span><br><span class="line"><span class="attr">AccessType</span>=ro</span><br></pre></td></tr></table></figure><p>每个子 section 都经过独立 <code>co_sub_build()</code> 和 <code>co_sub_parse_cfg()</code>。</p><p>这种方式适合 RECORD，因为每个字段可以具有不同：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">data type</span><br><span class="line">access type</span><br><span class="line">limits</span><br><span class="line">default value</span><br><span class="line">PDO mapping</span><br><span class="line">flags</span><br></pre></td></tr></table></figure><h3 id="13-2-CompactSubObj"><a href="#13-2-CompactSubObj" class="headerlink" title="13.2 CompactSubObj"></a>13.2 CompactSubObj</h3><p>CompactSubObj 用一个公共模板描述一组同类型成员。</p><p>解析器会：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">create sub-index 0 as UNSIGNED8</span><br><span class="line">    name = NrOfObjects</span><br><span class="line">    value = member count</span><br><span class="line"></span><br><span class="line">create sub-index 1 ... N</span><br><span class="line">    same data type</span><br><span class="line">    shared access and mapping properties</span><br></pre></td></tr></table></figure><p>它适合 ARRAY 或大量同构条目，避免重复书写 section。</p><h3 id="13-3-CompactName-与-CompactValue"><a href="#13-3-CompactName-与-CompactValue" class="headerlink" title="13.3 CompactName 与 CompactValue"></a>13.3 CompactName 与 CompactValue</h3><p>若存在对应的 Name&#x2F;Value section，解析器可以再为各成员填充：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">individual parameter names</span><br><span class="line">individual current values</span><br></pre></td></tr></table></figure><p>因此 CompactSubObj 并不等于所有子对象必须具有完全相同的名字和值，它主要压缩共同结构描述。</p><h3 id="13-4-子索引-0-的结构检查"><a href="#13-4-子索引-0-的结构检查" class="headerlink" title="13.4 子索引 0 的结构检查"></a>13.4 子索引 0 的结构检查</h3><p>对 ARRAY&#x2F;RECORD，解析完成后会检查：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">sub-index 0 exists</span><br><span class="line">sub-index 0 type is UNSIGNED8</span><br></pre></td></tr></table></figure><p>因为按照 CiA 301，复合对象的子索引 0 通常保存支持的最高子索引或条目数量。</p><p>需要注意：解析器能检查基础结构，但不会替代设备 profile 对每个字段含义的全部验证。</p><hr><h2 id="14-co-sub-parse-cfg-：一条-EDS-DCF-条目如何落到运行时字段"><a href="#14-co-sub-parse-cfg-：一条-EDS-DCF-条目如何落到运行时字段" class="headerlink" title="14. co_sub_parse_cfg()：一条 EDS&#x2F;DCF 条目如何落到运行时字段"></a>14. <code>co_sub_parse_cfg()</code>：一条 EDS&#x2F;DCF 条目如何落到运行时字段</h2><p>这是文本描述变成运行时对象语义的核心函数。</p><table><thead><tr><th>EDS&#x2F;DCF key</th><th>运行时结果</th></tr></thead><tbody><tr><td><code>ParameterName</code></td><td>子对象名称</td></tr><tr><td><code>Denotation</code></td><td>可选显示描述</td></tr><tr><td><code>DataType</code></td><td><code>co_sub_t::type</code></td></tr><tr><td><code>LowLimit</code></td><td>minimum value</td></tr><tr><td><code>HighLimit</code></td><td>maximum value</td></tr><tr><td><code>AccessType</code></td><td>RO、WO、RW、RWR、RWW 或 CONST</td></tr><tr><td><code>DefaultValue</code></td><td>default value</td></tr><tr><td><code>PDOMapping</code></td><td>PDO mappable flag</td></tr><tr><td><code>ObjFlags</code></td><td>对象行为 flags</td></tr><tr><td><code>ParameterValue</code></td><td>current value，并标记显式参数值</td></tr><tr><td><code>UploadFile</code></td><td>DOMAIN 上传文件模式和文件名</td></tr><tr><td><code>DownloadFile</code></td><td>DOMAIN 下载文件模式和文件名</td></tr></tbody></table><h3 id="14-1-AccessType-映射"><a href="#14-1-AccessType-映射" class="headerlink" title="14.1 AccessType 映射"></a>14.1 AccessType 映射</h3><p>解析器接受的主要访问类型为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">ro</span><br><span class="line">wo</span><br><span class="line">rw</span><br><span class="line">rwr</span><br><span class="line">rww</span><br><span class="line">const</span><br></pre></td></tr></table></figure><p>它们被转换为 Lely 的 <code>CO_ACCESS_*</code> 位组合。</p><p>需要继续区分：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">AccessType</span><br><span class="line">    controls network read/write and PDO direction capabilities</span><br><span class="line"></span><br><span class="line">PDOMapping</span><br><span class="line">    controls whether entry may appear in a PDO map</span><br><span class="line"></span><br><span class="line">ObjFlags</span><br><span class="line">    changes default storage or file behavior</span><br></pre></td></tr></table></figure><p>三者不是同一个概念。</p><h3 id="14-2-Limits-的处理"><a href="#14-2-Limits-的处理" class="headerlink" title="14.2 Limits 的处理"></a>14.2 Limits 的处理</h3><p>若启用对象 limits，LowLimit 和 HighLimit 会解析到 <code>co_sub_t</code> 的 min&#x2F;max。</p><p>以下问题在 <code>dcf.c</code> 中可能只产生 warning：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">LowLimit &gt; HighLimit</span><br><span class="line">DefaultValue outside limits</span><br><span class="line">ParameterValue outside limits</span><br></pre></td></tr></table></figure><p>这意味着：</p><blockquote><p>成功加载 DCF 不等于所有值都已经通过运行期 SDO 范围检查。</p></blockquote><p>后续默认 SDO download indication 仍会依据 min&#x2F;max 返回相应 abort code。</p><hr><h2 id="15-DefaultValue、ParameterValue-与-current-value-的优先级"><a href="#15-DefaultValue、ParameterValue-与-current-value-的优先级" class="headerlink" title="15. DefaultValue、ParameterValue 与 current value 的优先级"></a>15. DefaultValue、ParameterValue 与 current value 的优先级</h2><p>这一点直接决定设备启动后的对象值。</p><h3 id="15-1-存在-ParameterValue"><a href="#15-1-存在-ParameterValue" class="headerlink" title="15.1 存在 ParameterValue"></a>15.1 存在 ParameterValue</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">DefaultValue</span><br><span class="line">    → stored as default metadata</span><br><span class="line"></span><br><span class="line">ParameterValue</span><br><span class="line">    → stored as current value</span><br><span class="line">    → CO_OBJ_FLAGS_PARAMETER_VALUE set</span><br></pre></td></tr></table></figure><h3 id="15-2-不存在-ParameterValue"><a href="#15-2-不存在-ParameterValue" class="headerlink" title="15.2 不存在 ParameterValue"></a>15.2 不存在 ParameterValue</h3><p>解析器会把 default 复制到 current value：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">default value</span><br><span class="line">    ↓ copy</span><br><span class="line">current value</span><br></pre></td></tr></table></figure><p>因此运行期对象通常仍有初始 current value。</p><p>但概念上必须保留区别：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">default</span><br><span class="line">    factory or profile default metadata</span><br><span class="line"></span><br><span class="line">current</span><br><span class="line">    value currently used by protocol and application</span><br></pre></td></tr></table></figure><p>运行期调用 <code>co_sub_set_def()</code> 不会自动再次覆盖 current value。</p><h3 id="15-3-NODEID-flag-的传播"><a href="#15-3-NODEID-flag-的传播" class="headerlink" title="15.3 $NODEID flag 的传播"></a>15.3 <code>$NODEID</code> flag 的传播</h3><p>如果 DefaultValue 是 Node-ID-relative，而 ParameterValue 缺失，则从 default 复制 current value 时，相关 relative 属性也需要传播到 current value。</p><p>否则后续 <code>co_dev_set_id()</code> 只会调整 default，不会调整实际正在使用的 current value。</p><hr><h2 id="16-DOMAIN、UploadFile-与-DownloadFile"><a href="#16-DOMAIN、UploadFile-与-DownloadFile" class="headerlink" title="16. DOMAIN、UploadFile 与 DownloadFile"></a>16. DOMAIN、UploadFile 与 DownloadFile</h2><p>DOMAIN 用于表示任意长度数据。Lely 允许 DCF 把它连接到文件。</p><h3 id="16-1-UploadFile"><a href="#16-1-UploadFile" class="headerlink" title="16.1 UploadFile"></a>16.1 UploadFile</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">DOMAIN current value</span><br><span class="line">    stores filename</span><br><span class="line"></span><br><span class="line">SDO upload</span><br><span class="line">    reads file contents</span><br></pre></td></tr></table></figure><h3 id="16-2-DownloadFile"><a href="#16-2-DownloadFile" class="headerlink" title="16.2 DownloadFile"></a>16.2 DownloadFile</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">SDO download data</span><br><span class="line">    written to file</span><br><span class="line"></span><br><span class="line">DOMAIN current value</span><br><span class="line">    stores filename</span><br></pre></td></tr></table></figure><h3 id="16-3-为什么存的是文件名而不是文件内容"><a href="#16-3-为什么存的是文件名而不是文件内容" class="headerlink" title="16.3 为什么存的是文件名而不是文件内容"></a>16.3 为什么存的是文件名而不是文件内容</h3><p>对象字典只保存一个外部数据源引用：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">small dictionary entry</span><br><span class="line">    → potentially large file</span><br></pre></td></tr></table></figure><p>这样可避免在对象字典中常驻固件、配置包或大块二进制数据。</p><h3 id="16-4-访问权限修正"><a href="#16-4-访问权限修正" class="headerlink" title="16.4 访问权限修正"></a>16.4 访问权限修正</h3><p>解析器会根据 UploadFile&#x2F;DownloadFile 的方向检查或修正 access，使文件模式与网络访问方向一致。</p><p>但文件 I&#x2F;O 的错误仍可能在真正执行 SDO upload&#x2F;download 时才暴露，例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">file does not exist</span><br><span class="line">permission denied</span><br><span class="line">storage full</span><br><span class="line">path invalid</span><br></pre></td></tr></table></figure><hr><h2 id="17-co-val-lex-dcf-的类型解析策略"><a href="#17-co-val-lex-dcf-的类型解析策略" class="headerlink" title="17. co_val_lex_dcf() 的类型解析策略"></a>17. <code>co_val_lex_dcf()</code> 的类型解析策略</h2><p>DCF 文本中的 value 最终必须转换为 <code>union co_val</code>。</p><h3 id="17-1-基本数值类型"><a href="#17-1-基本数值类型" class="headerlink" title="17.1 基本数值类型"></a>17.1 基本数值类型</h3><p>整数、布尔、浮点、时间等大部分类型委托给通用 value lexer。</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">text</span><br><span class="line">    ↓ lexical conversion by CANopen type</span><br><span class="line">union co_val</span><br></pre></td></tr></table></figure><h3 id="17-2-OCTET-STRING-与-DOMAIN"><a href="#17-2-OCTET-STRING-与-DOMAIN" class="headerlink" title="17.2 OCTET_STRING 与 DOMAIN"></a>17.2 OCTET_STRING 与 DOMAIN</h3><p>这两类值按十六进制字节串解释，而不是普通 C 字符串。</p><p>例如概念上：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">010203A0</span><br><span class="line">    ↓</span><br><span class="line">01 02 03 A0</span><br></pre></td></tr></table></figure><h3 id="17-3-UNICODE-STRING"><a href="#17-3-UNICODE-STRING" class="headerlink" title="17.3 UNICODE_STRING"></a>17.3 UNICODE_STRING</h3><p>输入文本按 UTF-8 读取，再转换为 CANopen Unicode string 所使用的 UTF-16 code units。</p><p>因此：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">file encoding</span><br><span class="line">and</span><br><span class="line">CANopen value encoding</span><br></pre></td></tr></table></figure><p>是两个不同层次。</p><h3 id="17-4-解析成功不等于业务值有效"><a href="#17-4-解析成功不等于业务值有效" class="headerlink" title="17.4 解析成功不等于业务值有效"></a>17.4 解析成功不等于业务值有效</h3><p>lexer 负责：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">syntax and type conversion</span><br></pre></td></tr></table></figure><p>而以下约束可能位于其他层：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">min/max range</span><br><span class="line">profile-specific enumeration</span><br><span class="line">state-dependent write rules</span><br><span class="line">hardware capability</span><br></pre></td></tr></table></figure><hr><h2 id="18-CompactPDO：自动补建-PDO-参数对象"><a href="#18-CompactPDO：自动补建-PDO-参数对象" class="headerlink" title="18. CompactPDO：自动补建 PDO 参数对象"></a>18. CompactPDO：自动补建 PDO 参数对象</h2><p><code>[CompactPDO]</code> 用于压缩 PDO 参数描述。解析器会根据配置为缺失对象创建通信参数和映射参数。</p><h3 id="18-1-RPDO-对象对"><a href="#18-1-RPDO-对象对" class="headerlink" title="18.1 RPDO 对象对"></a>18.1 RPDO 对象对</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">0x1400 ... 0x15FF</span><br><span class="line">    RPDO communication parameters</span><br><span class="line"></span><br><span class="line">0x1600 ... 0x17FF</span><br><span class="line">    RPDO mapping parameters</span><br></pre></td></tr></table></figure><h3 id="18-2-TPDO-对象对"><a href="#18-2-TPDO-对象对" class="headerlink" title="18.2 TPDO 对象对"></a>18.2 TPDO 对象对</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">0x1800 ... 0x19FF</span><br><span class="line">    TPDO communication parameters</span><br><span class="line"></span><br><span class="line">0x1A00 ... 0x1BFF</span><br><span class="line">    TPDO mapping parameters</span><br></pre></td></tr></table></figure><h3 id="18-3-构建原则"><a href="#18-3-构建原则" class="headerlink" title="18.3 构建原则"></a>18.3 构建原则</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[&quot;parse CompactPDO entry&quot;] --&gt; B[&quot;compute communication index&quot;]</span><br><span class="line">    B --&gt; C[&quot;compute mapping index&quot;]</span><br><span class="line">    C --&gt; D&#123;&quot;explicit object already exists&quot;&#125;</span><br><span class="line">    D --&gt;|yes| E[&quot;keep explicit object&quot;]</span><br><span class="line">    D --&gt;|no| F[&quot;create standard parameter object&quot;]</span><br><span class="line">    F --&gt; G[&quot;create required sub-objects&quot;]</span><br><span class="line">    G --&gt; H[&quot;insert into device tree&quot;]</span><br></pre></td></tr></table></figure><h3 id="18-4-它解决什么问题"><a href="#18-4-它解决什么问题" class="headerlink" title="18.4 它解决什么问题"></a>18.4 它解决什么问题</h3><p>CompactPDO 解决的是描述重复：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">many PDOs</span><br><span class="line">×</span><br><span class="line">standard communication fields</span><br><span class="line">×</span><br><span class="line">up to 64 mapping entries</span><br></pre></td></tr></table></figure><p>它不会自动证明映射对象一定适合业务应用。映射目标的数据类型、长度、access 和 PDO mapping 属性仍需要 PDO 模块验证。</p><hr><h2 id="19-DeviceComissioning-：把通用描述绑定到具体网络"><a href="#19-DeviceComissioning-：把通用描述绑定到具体网络" class="headerlink" title="19. [DeviceComissioning]：把通用描述绑定到具体网络"></a>19. <code>[DeviceComissioning]</code>：把通用描述绑定到具体网络</h2><p>源码使用的 section 名为：</p><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">[DeviceComissioning]</span></span><br></pre></td></tr></table></figure><p>需要按该拼写理解现有实现和文件兼容性。</p><p>主要字段包括：</p><table><thead><tr><th>字段</th><th>作用</th></tr></thead><tbody><tr><td><code>NodeID</code></td><td>设置设备 Node-ID，并触发 relative value 重定位</td></tr><tr><td><code>NetNumber</code></td><td>设置网络号</td></tr><tr><td><code>NodeName</code></td><td>设置节点名称</td></tr><tr><td><code>Baudrate</code></td><td>设置实际使用的位速率</td></tr><tr><td><code>LSS_SerialNumber</code></td><td>设置 LSS identity 中使用的序列号信息</td></tr></tbody></table><h3 id="19-1-DeviceInfo-与-DeviceComissioning-的区别"><a href="#19-1-DeviceInfo-与-DeviceComissioning-的区别" class="headerlink" title="19.1 DeviceInfo 与 DeviceComissioning 的区别"></a>19.1 DeviceInfo 与 DeviceComissioning 的区别</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">DeviceInfo</span><br><span class="line">    what the device supports and what it is</span><br><span class="line"></span><br><span class="line">DeviceComissioning</span><br><span class="line">    how this device instance is assigned in a network</span><br></pre></td></tr></table></figure><p>例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">supported baud rates</span><br><span class="line">    belongs to DeviceInfo</span><br><span class="line"></span><br><span class="line">selected baud rate</span><br><span class="line">    belongs to DeviceComissioning</span><br></pre></td></tr></table></figure><hr><h2 id="20-concise-DCF-的二进制布局"><a href="#20-concise-DCF-的二进制布局" class="headerlink" title="20. concise DCF 的二进制布局"></a>20. concise DCF 的二进制布局</h2><p>Lely 的设备 API还支持 concise DCF 的内存和文件读写。</p><h3 id="20-1-总体格式"><a href="#20-1-总体格式" class="headerlink" title="20.1 总体格式"></a>20.1 总体格式</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">+-------------------------+</span><br><span class="line">| number of entries u32   |</span><br><span class="line">+-------------------------+</span><br><span class="line">| entry 1                 |</span><br><span class="line">+-------------------------+</span><br><span class="line">| entry 2                 |</span><br><span class="line">+-------------------------+</span><br><span class="line">| ...                     |</span><br><span class="line">+-------------------------+</span><br></pre></td></tr></table></figure><p>每条 entry：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">+-------------------------+</span><br><span class="line">| index u16               | 2 bytes</span><br><span class="line">+-------------------------+</span><br><span class="line">| sub-index u8            | 1 byte</span><br><span class="line">+-------------------------+</span><br><span class="line">| data size u32           | 4 bytes</span><br><span class="line">+-------------------------+</span><br><span class="line">| data                    | size bytes</span><br><span class="line">+-------------------------+</span><br></pre></td></tr></table></figure><p>固定头长度为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">2 + 1 + 4 = 7 bytes per entry</span><br></pre></td></tr></table></figure><p>多字节整数和 CANopen 基本数据按 little-endian 编码。</p><h3 id="20-2-读取流程"><a href="#20-2-读取流程" class="headerlink" title="20.2 读取流程"></a>20.2 读取流程</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[&quot;read u32 entry count&quot;] --&gt; B[&quot;read index&quot;]</span><br><span class="line">    B --&gt; C[&quot;read sub-index&quot;]</span><br><span class="line">    C --&gt; D[&quot;read byte size&quot;]</span><br><span class="line">    D --&gt; E&#123;&quot;sub-object exists&quot;&#125;</span><br><span class="line">    E --&gt;|yes| F[&quot;decode by target CANopen type&quot;]</span><br><span class="line">    F --&gt; G[&quot;replace current value&quot;]</span><br><span class="line">    E --&gt;|no| H[&quot;consume and discard payload&quot;]</span><br><span class="line">    G --&gt; I[&quot;next entry&quot;]</span><br><span class="line">    H --&gt; I</span><br></pre></td></tr></table></figure><p>未知对象记录被消费但不必导致整份 concise DCF 立即失败，这允许配置数据包含当前设备版本不识别的条目。</p><h3 id="20-3-写出流程"><a href="#20-3-写出流程" class="headerlink" title="20.3 写出流程"></a>20.3 写出流程</h3><p><code>co_dev_write_dcf()</code> 的实现分两遍：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">first pass</span><br><span class="line">    calculate count and total size</span><br><span class="line"></span><br><span class="line">allocate DOMAIN buffer</span><br><span class="line"></span><br><span class="line">second pass</span><br><span class="line">    serialize every selected sub-object</span><br></pre></td></tr></table></figure><p>索引范围可用于只导出对象字典的一部分。</p><h3 id="20-4-与-SDO-download-的区别"><a href="#20-4-与-SDO-download-的区别" class="headerlink" title="20.4 与 SDO download 的区别"></a>20.4 与 SDO download 的区别</h3><p>concise DCF restore 是本地对象字典操作，不等价于通过 CAN 网络逐条执行 SDO download。</p><p>它通常不会自动获得：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">network access check</span><br><span class="line">custom download indication</span><br><span class="line">SDO abort semantics</span><br><span class="line">application state validation</span><br><span class="line">hardware side effect</span><br></pre></td></tr></table></figure><p>因此若某个参数必须通过自定义 indication 才能真正写入硬件，仅恢复对象字典缓存可能不够，应用还需要执行相应同步步骤。</p><hr><h2 id="21-TPDO-event：从一个对象变化反查受影响的-PDO"><a href="#21-TPDO-event：从一个对象变化反查受影响的-PDO" class="headerlink" title="21. TPDO event：从一个对象变化反查受影响的 PDO"></a>21. TPDO event：从一个对象变化反查受影响的 PDO</h2><p><code>co_dev_tpdo_event(dev, idx, subidx)</code> 的核心问题是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">which TPDO maps this object entry</span><br></pre></td></tr></table></figure><h3 id="21-1-扫描范围"><a href="#21-1-扫描范围" class="headerlink" title="21.1 扫描范围"></a>21.1 扫描范围</h3><p>函数扫描 TPDO 通信参数对象：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">0x1800 ... 0x19FF</span><br></pre></td></tr></table></figure><p>并读取对应映射对象：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">0x1A00 ... 0x1BFF</span><br></pre></td></tr></table></figure><h3 id="21-2-过滤条件"><a href="#21-2-过滤条件" class="headerlink" title="21.2 过滤条件"></a>21.2 过滤条件</h3><p>候选 TPDO 需要满足相关条件，例如：</p><ul><li>通信对象和映射对象存在；</li><li>COB-ID 有效；</li><li>不是不适用的 MPDO 形式；</li><li>传输类型允许事件触发；</li><li>某个映射条目确实引用目标 index&#x2F;sub-index。</li></ul><h3 id="21-3-匹配后做什么"><a href="#21-3-匹配后做什么" class="headerlink" title="21.3 匹配后做什么"></a>21.3 匹配后做什么</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">find matching TPDO number</span><br><span class="line">    ↓</span><br><span class="line">invoke registered TPDO event indication</span><br></pre></td></tr></table></figure><p>它不在 <code>dev.c</code> 中直接：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">pack PDO bytes</span><br><span class="line">apply inhibit timer</span><br><span class="line">schedule event timer</span><br><span class="line">send CAN frame</span><br></pre></td></tr></table></figure><p>这些动作由上层 TPDO 服务对象处理。</p><h3 id="21-4-为什么不在每个-co-sub-t-保存反向映射表"><a href="#21-4-为什么不在每个-co-sub-t-保存反向映射表" class="headerlink" title="21.4 为什么不在每个 co_sub_t 保存反向映射表"></a>21.4 为什么不在每个 <code>co_sub_t</code> 保存反向映射表</h3><p>当前实现选择在事件触发时扫描 PDO 配置：</p><p>优点：</p><ul><li>不需要维护额外反向索引；</li><li>PDO 重映射后无需同步多份缓存；</li><li>对象字典始终是配置事实来源。</li></ul><p>代价：</p><ul><li>每次 event 需要遍历候选 TPDO 和映射项；</li><li>高频变量、超多 PDO 场景的成本更高；</li><li>上层应避免对未映射变量无意义地反复触发 event。</li></ul><hr><h2 id="22-SAM-MPDO-event"><a href="#22-SAM-MPDO-event" class="headerlink" title="22. SAM-MPDO event"></a>22. SAM-MPDO event</h2><p>SAM-MPDO 使用源地址模式，把生产者对象字典中的 index&#x2F;sub-index 携带在 MPDO 中。</p><p><code>co_dev</code> 的 SAM-MPDO event 逻辑会：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">locate configured SAM-MPDO producer</span><br><span class="line">    ↓</span><br><span class="line">verify event-driven communication parameters</span><br><span class="line">    ↓</span><br><span class="line">notify upper layer with source index and sub-index</span><br></pre></td></tr></table></figure><p>CiA 301 的模型限制一个设备只有一个此类 SAM-MPDO producer，因此它与普通 TPDO 的多实例扫描语义不同。</p><p>同样，设备层只做配置解析和事件 indication，不承担 CAN 帧传输状态机。</p><hr><h2 id="23-文本-DCF-解析的错误与回滚边界"><a href="#23-文本-DCF-解析的错误与回滚边界" class="headerlink" title="23. 文本 DCF 解析的错误与回滚边界"></a>23. 文本 DCF 解析的错误与回滚边界</h2><h3 id="23-1-根级失败"><a href="#23-1-根级失败" class="headerlink" title="23.1 根级失败"></a>23.1 根级失败</h3><p>若文件解析或设备构建失败：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">__co_dev_fini()</span><br><span class="line">    destroys all objects already attached to device</span><br></pre></td></tr></table></figure><p>因此返回给调用方的不是一棵需要手工逐项清理的半成品树。</p><h3 id="23-2-对象级失败"><a href="#23-2-对象级失败" class="headerlink" title="23.2 对象级失败"></a>23.2 对象级失败</h3><p><code>co_obj_build()</code> 创建对象后，若名称、对象代码或子对象解析失败，会移除并销毁当前对象。</p><h3 id="23-3-子对象级失败"><a href="#23-3-子对象级失败" class="headerlink" title="23.3 子对象级失败"></a>23.3 子对象级失败</h3><p><code>co_sub_build()</code> 创建并插入子对象后，后续属性解析失败时会销毁该子对象；动态模式下对象的连续值存储也会随拓扑变化更新。</p><h3 id="23-4-warning-与-fatal-error-必须区分"><a href="#23-4-warning-与-fatal-error-必须区分" class="headerlink" title="23.4 warning 与 fatal error 必须区分"></a>23.4 warning 与 fatal error 必须区分</h3><p>典型 fatal error：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">missing mandatory structural key</span><br><span class="line">duplicate index or sub-index</span><br><span class="line">invalid data type syntax</span><br><span class="line">invalid ObjectType</span><br><span class="line">both SubNumber and CompactSubObj selected</span><br><span class="line">allocation failure</span><br></pre></td></tr></table></figure><p>典型 warning：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">recommended mandatory object missing</span><br><span class="line">limit order suspicious</span><br><span class="line">default or current value outside limits</span><br><span class="line">some profile consistency issue</span><br></pre></td></tr></table></figure><p>因此工程上应同时检查：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">return value</span><br><span class="line">and</span><br><span class="line">diagnostic output</span><br></pre></td></tr></table></figure><p>只看函数成功返回可能遗漏配置质量问题。</p><hr><h2 id="24-条件编译和嵌入式部署策略"><a href="#24-条件编译和嵌入式部署策略" class="headerlink" title="24. 条件编译和嵌入式部署策略"></a>24. 条件编译和嵌入式部署策略</h2><h3 id="24-1-与本篇直接相关的宏"><a href="#24-1-与本篇直接相关的宏" class="headerlink" title="24.1 与本篇直接相关的宏"></a>24.1 与本篇直接相关的宏</h3><table><thead><tr><th>宏</th><th>影响</th></tr></thead><tbody><tr><td><code>LELY_NO_CO_DCF</code></td><td>移除 EDS&#x2F;DCF 运行期解析支持</td></tr><tr><td><code>LELY_NO_CO_DCF_RESTORE</code></td><td>移除 application parameter concise DCF 保存&#x2F;恢复相关功能</td></tr><tr><td><code>LELY_NO_CO_OBJ_NAME</code></td><td>不保存对象和子对象名称</td></tr><tr><td><code>LELY_NO_CO_OBJ_LIMITS</code></td><td>不保存 min&#x2F;max，也不执行对应范围检查</td></tr><tr><td><code>LELY_NO_CO_OBJ_DEFAULT</code></td><td>不保存 default metadata</td></tr><tr><td><code>LELY_NO_CO_OBJ_FILE</code></td><td>移除 UploadFile&#x2F;DownloadFile</td></tr><tr><td><code>LELY_NO_CO_TPDO</code></td><td>移除 TPDO 相关能力和设备事件字段</td></tr><tr><td><code>LELY_NO_CO_MPDO</code></td><td>移除 MPDO 支持</td></tr><tr><td><code>LELY_NO_CO_SDEV</code></td><td>移除静态设备描述支持</td></tr><tr><td><code>LELY_NO_MALLOC</code></td><td>移除大部分动态创建路径，且会连带关闭标准 I&#x2F;O</td></tr></tbody></table><h3 id="24-2-为什么-LELY-NO-MALLOC-下不适合运行期-DCF"><a href="#24-2-为什么-LELY-NO-MALLOC-下不适合运行期-DCF" class="headerlink" title="24.2 为什么 LELY_NO_MALLOC 下不适合运行期 DCF"></a>24.2 为什么 <code>LELY_NO_MALLOC</code> 下不适合运行期 DCF</h3><p>官方构建关系为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">no malloc</span><br><span class="line">    → standard I/O automatically disabled</span><br><span class="line"></span><br><span class="line">no standard I/O</span><br><span class="line">    → EDS/DCF support automatically disabled</span><br></pre></td></tr></table></figure><p>这与 <code>dcf.c</code> 的实现需求一致：</p><ul><li>配置树需要动态节点；</li><li>对象、子对象和值需要动态存储；</li><li>文件入口需要 stdio；</li><li>字符串与 DOMAIN 可能继续分配内存。</li></ul><h3 id="24-3-dcf2c-的定位"><a href="#24-3-dcf2c-的定位" class="headerlink" title="24.3 dcf2c 的定位"></a>24.3 <code>dcf2c</code> 的定位</h3><p>资源受限目标更适合：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">build host</span><br><span class="line">    read EDS/DCF</span><br><span class="line">    ↓</span><br><span class="line">dcf2c</span><br><span class="line">    ↓</span><br><span class="line">generate co_sdev C structure</span><br><span class="line">    ↓</span><br><span class="line">compile static device description into firmware</span><br></pre></td></tr></table></figure><p>这将复杂文本解析从目标机移动到构建阶段，换取：</p><ul><li>固定 RAM&#x2F;Flash 布局；</li><li>更确定的启动时间；</li><li>无文件系统依赖；</li><li>更小的运行期错误面。</li></ul><p>代价是修改 DCF 后需要重新生成并编译固件。</p><hr><h2 id="25-线程安全和运行阶段边界"><a href="#25-线程安全和运行阶段边界" class="headerlink" title="25. 线程安全和运行阶段边界"></a>25. 线程安全和运行阶段边界</h2><p><code>dev.c</code> 与 <code>dcf.c</code> 内部没有为对象树提供通用 mutex。</p><p>以下操作不应与 SDO&#x2F;PDO&#x2F;NMT 访问无保护并发：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">parse and insert objects</span><br><span class="line">remove or destroy objects</span><br><span class="line">change Node-ID and rebase relative values</span><br><span class="line">restore concise DCF</span><br><span class="line">replace strings or DOMAIN values</span><br><span class="line">change TPDO or MPDO indication pointers</span><br></pre></td></tr></table></figure><p>推荐生命周期：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">initialization phase</span><br><span class="line">    load or initialize device description</span><br><span class="line">    validate diagnostics</span><br><span class="line">    register indications</span><br><span class="line">    create protocol services</span><br><span class="line"></span><br><span class="line">runtime phase</span><br><span class="line">    keep object topology stable</span><br><span class="line">    perform controlled value access</span><br><span class="line">    trigger PDO events explicitly</span><br><span class="line"></span><br><span class="line">shutdown phase</span><br><span class="line">    stop protocol services</span><br><span class="line">    ensure callbacks are no longer running</span><br><span class="line">    destroy device description</span><br></pre></td></tr></table></figure><p>若必须运行期加载新 DCF，应在更高层提供全局设备配置锁，并重新评估所有保存的 <code>co_obj_t *</code>、<code>co_sub_t *</code> 和 value pointer 生命周期。</p><hr><h2 id="26-完整运行流程整理"><a href="#26-完整运行流程整理" class="headerlink" title="26. 完整运行流程整理"></a>26. 完整运行流程整理</h2><h3 id="26-1-从-EDS-DCF-到运行期设备"><a href="#26-1-从-EDS-DCF-到运行期设备" class="headerlink" title="26.1 从 EDS&#x2F;DCF 到运行期设备"></a>26.1 从 EDS&#x2F;DCF 到运行期设备</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[&quot;read EDS or DCF&quot;] --&gt; B[&quot;parse INI configuration&quot;]</span><br><span class="line">    B --&gt; C[&quot;initialize co_dev_t with Node-ID 255&quot;]</span><br><span class="line">    C --&gt; D[&quot;parse device metadata&quot;]</span><br><span class="line">    D --&gt; E[&quot;parse object index lists&quot;]</span><br><span class="line">    E --&gt; F[&quot;create co_obj_t for each index&quot;]</span><br><span class="line">    F --&gt; G[&quot;create co_sub_t entries&quot;]</span><br><span class="line">    G --&gt; H[&quot;parse type access limits default and value&quot;]</span><br><span class="line">    H --&gt; I[&quot;insert all nodes into red-black trees&quot;]</span><br><span class="line">    I --&gt; J[&quot;synthesize CompactPDO objects&quot;]</span><br><span class="line">    J --&gt; K[&quot;apply commissioning Node-ID and bitrate&quot;]</span><br><span class="line">    K --&gt; L[&quot;rebase NODEID-relative values&quot;]</span><br><span class="line">    L --&gt; M[&quot;runtime co_dev_t ready&quot;]</span><br></pre></td></tr></table></figure><h3 id="26-2-应用修改值并触发-TPDO"><a href="#26-2-应用修改值并触发-TPDO" class="headerlink" title="26.2 应用修改值并触发 TPDO"></a>26.2 应用修改值并触发 TPDO</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">application computes new process value</span><br><span class="line">    ↓</span><br><span class="line">co_dev_set_val_*()</span><br><span class="line">    ↓</span><br><span class="line">current value updated</span><br><span class="line">    ↓</span><br><span class="line">co_dev_tpdo_event(index, sub-index)</span><br><span class="line">    ↓</span><br><span class="line">scan 0x1800/0x1A00 PDO configuration</span><br><span class="line">    ↓</span><br><span class="line">invoke TPDO event indication</span><br><span class="line">    ↓</span><br><span class="line">TPDO service applies transmission and timing rules</span><br><span class="line">    ↓</span><br><span class="line">CAN frame is sent by lower layers</span><br></pre></td></tr></table></figure><h3 id="26-3-concise-DCF-恢复"><a href="#26-3-concise-DCF-恢复" class="headerlink" title="26.3 concise DCF 恢复"></a>26.3 concise DCF 恢复</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">existing co_dev_t</span><br><span class="line">    ↓</span><br><span class="line">read entry count</span><br><span class="line">    ↓</span><br><span class="line">for every record</span><br><span class="line">    index + sub-index + size + payload</span><br><span class="line">    ↓</span><br><span class="line">find existing sub-object</span><br><span class="line">    ↓</span><br><span class="line">decode according to its CANopen type</span><br><span class="line">    ↓</span><br><span class="line">replace current value</span><br></pre></td></tr></table></figure><h3 id="26-4-Node-ID-更改"><a href="#26-4-Node-ID-更改" class="headerlink" title="26.4 Node-ID 更改"></a>26.4 Node-ID 更改</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">old Node-ID</span><br><span class="line">    ↓</span><br><span class="line">co_dev_set_id(new)</span><br><span class="line">    ↓</span><br><span class="line">walk every object and sub-object</span><br><span class="line">    ↓</span><br><span class="line">adjust marked min max default current values by delta</span><br><span class="line">    ↓</span><br><span class="line">store new Node-ID</span><br></pre></td></tr></table></figure><hr><h2 id="27-关键风险与工程注意事项"><a href="#27-关键风险与工程注意事项" class="headerlink" title="27. 关键风险与工程注意事项"></a>27. 关键风险与工程注意事项</h2><h3 id="27-1-把文本-DCF-与-concise-DCF-混为一谈"><a href="#27-1-把文本-DCF-与-concise-DCF-混为一谈" class="headerlink" title="27.1 把文本 DCF 与 concise DCF 混为一谈"></a>27.1 把文本 DCF 与 concise DCF 混为一谈</h3><p>风险：错误地认为 <code>co_dev_read_dcf()</code> 能解析 INI 文本。</p><p>处理：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">co_dev_create_from_dcf_file/text</span><br><span class="line">    for EDS/DCF text</span><br><span class="line"></span><br><span class="line">co_dev_read_dcf</span><br><span class="line">    for concise binary records</span><br></pre></td></tr></table></figure><h3 id="27-2-直接-setter-绕过协议和硬件副作用"><a href="#27-2-直接-setter-绕过协议和硬件副作用" class="headerlink" title="27.2 直接 setter 绕过协议和硬件副作用"></a>27.2 直接 setter 绕过协议和硬件副作用</h3><p>风险：对象字典值更新了，但硬件寄存器、Flash 或应用状态没有更新。</p><p>处理：需要协议语义时走 indication；恢复 concise DCF 后执行应用同步流程。</p><h3 id="27-3-current-value-更新后没有触发-TPDO"><a href="#27-3-current-value-更新后没有触发-TPDO" class="headerlink" title="27.3 current value 更新后没有触发 TPDO"></a>27.3 current value 更新后没有触发 TPDO</h3><p>风险：应用认为变量变化就会自动发 PDO。</p><p>处理：根据应用模型显式调用 <code>co_dev_tpdo_event()</code>，或由更高层封装 setter + event。</p><h3 id="27-4-NODEID-flag-不正确"><a href="#27-4-NODEID-flag-不正确" class="headerlink" title="27.4 $NODEID flag 不正确"></a>27.4 <code>$NODEID</code> flag 不正确</h3><p>风险：Node-ID 改变后 COB-ID 没有变化，或绝对值被错误重定位。</p><p>处理：只对真正的 <code>$NODEID + n</code> 表达式设置相应 flag，修改配置后验证关键通信对象。</p><h3 id="27-5-成功解析被误认为完整合规"><a href="#27-5-成功解析被误认为完整合规" class="headerlink" title="27.5 成功解析被误认为完整合规"></a>27.5 成功解析被误认为完整合规</h3><p>风险：忽略 warning，带着越界默认值或 profile 不一致对象运行。</p><p>处理：将诊断 warning 纳入 CI；配合 <code>dcfchk</code> 和设备 profile 专项校验。</p><h3 id="27-6-运行期热加载对象字典"><a href="#27-6-运行期热加载对象字典" class="headerlink" title="27.6 运行期热加载对象字典"></a>27.6 运行期热加载对象字典</h3><p>风险：协议服务仍保存旧对象或值指针，造成悬空引用。</p><p>处理：优先在启动前一次性构建；热更新时停止服务并建立严格所有权边界。</p><h3 id="27-7-元数据与标准对象不一致"><a href="#27-7-元数据与标准对象不一致" class="headerlink" title="27.7 元数据与标准对象不一致"></a>27.7 元数据与标准对象不一致</h3><p>风险：工具读取 <code>DeviceInfo</code> 得到一组身份，SDO 读取 <code>0x1018</code> 得到另一组身份。</p><p>处理：生成 DCF 时从单一数据源产生两部分信息，并在测试中交叉验证。</p><h3 id="27-8-CompactPDO-被误认为完整-PDO-配置器"><a href="#27-8-CompactPDO-被误认为完整-PDO-配置器" class="headerlink" title="27.8 CompactPDO 被误认为完整 PDO 配置器"></a>27.8 CompactPDO 被误认为完整 PDO 配置器</h3><p>风险：自动建出了参数对象，但映射目标无效或不允许映射。</p><p>处理：继续执行 PDO mapping 验证，并在 NMT Pre-operational 阶段测试配置。</p><hr><h2 id="28-推荐源码阅读顺序"><a href="#28-推荐源码阅读顺序" class="headerlink" title="28. 推荐源码阅读顺序"></a>28. 推荐源码阅读顺序</h2><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br></pre></td><td class="code"><pre><span class="line">include/lely/co/dev.h</span><br><span class="line">    ↓ understand public device API</span><br><span class="line"></span><br><span class="line">include/lely/co/detail/dev.h</span><br><span class="line">    ↓ inspect real struct and feature macros</span><br><span class="line"></span><br><span class="line">src/co/dev.c: __co_dev_init and co_dev_create</span><br><span class="line">    ↓ understand default state</span><br><span class="line"></span><br><span class="line">src/co/dev.c: insert remove find object</span><br><span class="line">    ↓ understand object red-black tree</span><br><span class="line"></span><br><span class="line">src/co/dev.c: co_dev_set_id</span><br><span class="line">    ↓ understand NODEID-relative relocation</span><br><span class="line"></span><br><span class="line">include/lely/co/dcf.h</span><br><span class="line">    ↓ understand text DCF entry points</span><br><span class="line"></span><br><span class="line">src/co/dcf.c: create/init from file or text</span><br><span class="line">    ↓ understand config parser boundary</span><br><span class="line"></span><br><span class="line">src/co/dcf.c: co_dev_parse_cfg</span><br><span class="line">    ↓ understand global section order</span><br><span class="line"></span><br><span class="line">src/co/dcf.c: co_obj_parse_cfg</span><br><span class="line">    ↓ understand object structures</span><br><span class="line"></span><br><span class="line">src/co/dcf.c: co_sub_parse_cfg</span><br><span class="line">    ↓ understand value, access, flags and files</span><br><span class="line"></span><br><span class="line">src/co/dev.c: co_dev_read_dcf and write_dcf</span><br><span class="line">    ↓ understand concise DCF</span><br><span class="line"></span><br><span class="line">src/co/dev.c: co_dev_tpdo_event and SAM-MPDO event</span><br><span class="line">    ↓ understand reverse mapping events</span><br></pre></td></tr></table></figure><hr><h2 id="29-推荐调试断点和观察变量"><a href="#29-推荐调试断点和观察变量" class="headerlink" title="29. 推荐调试断点和观察变量"></a>29. 推荐调试断点和观察变量</h2><h3 id="29-1-文本入口"><a href="#29-1-文本入口" class="headerlink" title="29.1 文本入口"></a>29.1 文本入口</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">co_dev_create_from_dcf_file</span><br><span class="line">co_dev_create_from_dcf_text</span><br><span class="line">__co_dev_init_from_dcf_cfg</span><br><span class="line">co_dev_parse_cfg</span><br></pre></td></tr></table></figure><p>观察：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">dev-&gt;id</span><br><span class="line">config section name</span><br><span class="line">current parser error position</span><br><span class="line">number of object indices</span><br></pre></td></tr></table></figure><h3 id="29-2-对象构建"><a href="#29-2-对象构建" class="headerlink" title="29.2 对象构建"></a>29.2 对象构建</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">co_obj_build</span><br><span class="line">co_obj_parse_cfg</span><br><span class="line">co_sub_build</span><br><span class="line">co_sub_parse_cfg</span><br><span class="line">co_dev_insert_obj</span><br><span class="line">co_obj_insert_sub</span><br></pre></td></tr></table></figure><p>观察：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">index</span><br><span class="line">sub-index</span><br><span class="line">ObjectType</span><br><span class="line">DataType</span><br><span class="line">AccessType</span><br><span class="line">PDOMapping</span><br><span class="line">flags</span><br><span class="line">default value</span><br><span class="line">current value</span><br></pre></td></tr></table></figure><h3 id="29-3-Node-ID-重定位"><a href="#29-3-Node-ID-重定位" class="headerlink" title="29.3 Node-ID 重定位"></a>29.3 Node-ID 重定位</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">co_dev_set_id</span><br><span class="line">co_obj_set_id</span><br><span class="line">co_sub_set_id related helper</span><br><span class="line">co_val_set_id</span><br></pre></td></tr></table></figure><p>观察：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">old id</span><br><span class="line">new id</span><br><span class="line">delta</span><br><span class="line">NODEID flags</span><br><span class="line">0x1200 through 0x1BFF communication values</span><br></pre></td></tr></table></figure><h3 id="29-4-concise-DCF"><a href="#29-4-concise-DCF" class="headerlink" title="29.4 concise DCF"></a>29.4 concise DCF</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">co_dev_read_sub</span><br><span class="line">co_dev_read_dcf</span><br><span class="line">co_dev_write_sub</span><br><span class="line">co_dev_write_dcf</span><br></pre></td></tr></table></figure><p>观察：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">entry count</span><br><span class="line">index</span><br><span class="line">sub-index</span><br><span class="line">encoded size</span><br><span class="line">target type</span><br><span class="line">unknown-entry discard path</span><br></pre></td></tr></table></figure><h3 id="29-5-PDO-event"><a href="#29-5-PDO-event" class="headerlink" title="29.5 PDO event"></a>29.5 PDO event</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">co_dev_tpdo_event</span><br><span class="line">co_dev_sam_mpdo_event</span><br><span class="line">registered event indication</span><br></pre></td></tr></table></figure><p>观察：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">changed index and sub-index</span><br><span class="line">TPDO communication index</span><br><span class="line">mapping object index</span><br><span class="line">transmission type</span><br><span class="line">COB-ID</span><br><span class="line">mapping entries</span><br><span class="line">callback argument</span><br></pre></td></tr></table></figure><hr><h2 id="30-常见误区"><a href="#30-常见误区" class="headerlink" title="30. 常见误区"></a>30. 常见误区</h2><h3 id="30-1-误区：co-dev-t-就是一个-EDS-文件"><a href="#30-1-误区：co-dev-t-就是一个-EDS-文件" class="headerlink" title="30.1 误区：co_dev_t 就是一个 EDS 文件"></a>30.1 误区：<code>co_dev_t</code> 就是一个 EDS 文件</h3><p>错误。EDS&#x2F;DCF 只是构造来源之一，<code>co_dev_t</code> 是运行期设备对象和对象字典根节点。</p><h3 id="30-2-误区：DCF-只有一种格式"><a href="#30-2-误区：DCF-只有一种格式" class="headerlink" title="30.2 误区：DCF 只有一种格式"></a>30.2 误区：DCF 只有一种格式</h3><p>错误。文本 EDS&#x2F;DCF 用于描述设备，concise DCF 是二进制参数记录。</p><h3 id="30-3-误区：修改-dev-id-只影响一个字段"><a href="#30-3-误区：修改-dev-id-只影响一个字段" class="headerlink" title="30.3 误区：修改 dev-&gt;id 只影响一个字段"></a>30.3 误区：修改 <code>dev-&gt;id</code> 只影响一个字段</h3><p>错误。公开 <code>co_dev_set_id()</code> 会重定位所有标记为 <code>$NODEID</code> 相对表达式的值。</p><h3 id="30-4-误区：读取-DCF-成功就没有配置问题"><a href="#30-4-误区：读取-DCF-成功就没有配置问题" class="headerlink" title="30.4 误区：读取 DCF 成功就没有配置问题"></a>30.4 误区：读取 DCF 成功就没有配置问题</h3><p>错误。部分范围和一致性问题只产生 warning。</p><h3 id="30-5-误区：ParameterValue-与-DefaultValue-相同"><a href="#30-5-误区：ParameterValue-与-DefaultValue-相同" class="headerlink" title="30.5 误区：ParameterValue 与 DefaultValue 相同"></a>30.5 误区：<code>ParameterValue</code> 与 <code>DefaultValue</code> 相同</h3><p>错误。前者是配置后的 current value，后者是默认元数据；缺少 ParameterValue 时解析器才会用 default 初始化 current。</p><h3 id="30-6-误区：直接修改对象值会自动发送-TPDO"><a href="#30-6-误区：直接修改对象值会自动发送-TPDO" class="headerlink" title="30.6 误区：直接修改对象值会自动发送 TPDO"></a>30.6 误区：直接修改对象值会自动发送 TPDO</h3><p>错误。值修改和 TPDO event indication 是两个步骤。</p><h3 id="30-7-误区：co-dev-tpdo-event-直接发送-CAN-帧"><a href="#30-7-误区：co-dev-tpdo-event-直接发送-CAN-帧" class="headerlink" title="30.7 误区：co_dev_tpdo_event() 直接发送 CAN 帧"></a>30.7 误区：<code>co_dev_tpdo_event()</code> 直接发送 CAN 帧</h3><p>错误。它解析映射并通知上层 TPDO 服务。</p><h3 id="30-8-误区：CompactPDO-会验证整个应用映射"><a href="#30-8-误区：CompactPDO-会验证整个应用映射" class="headerlink" title="30.8 误区：CompactPDO 会验证整个应用映射"></a>30.8 误区：CompactPDO 会验证整个应用映射</h3><p>错误。它主要补建标准参数对象，最终合法性仍由 PDO 配置和映射检查决定。</p><h3 id="30-9-误区：co-dev-read-dcf-等价于远程-SDO-写"><a href="#30-9-误区：co-dev-read-dcf-等价于远程-SDO-写" class="headerlink" title="30.9 误区：co_dev_read_dcf() 等价于远程 SDO 写"></a>30.9 误区：<code>co_dev_read_dcf()</code> 等价于远程 SDO 写</h3><p>错误。它本地恢复 current values，不自动执行全部网络权限、indication 和硬件副作用。</p><h3 id="30-10-误区：MCU-上运行期解析-EDS-一定更灵活、更好"><a href="#30-10-误区：MCU-上运行期解析-EDS-一定更灵活、更好" class="headerlink" title="30.10 误区：MCU 上运行期解析 EDS 一定更灵活、更好"></a>30.10 误区：MCU 上运行期解析 EDS 一定更灵活、更好</h3><p>不一定。运行期解析增加动态内存、文件系统、启动时间和错误处理成本。静态 <code>co_sdev</code> 往往更适合资源受限固件。</p><hr><h2 id="31-最终心智模型"><a href="#31-最终心智模型" class="headerlink" title="31. 最终心智模型"></a>31. 最终心智模型</h2><p>可以把这部分实现记成六层：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">第 1 层：文件描述</span><br><span class="line">    EDS / DCF INI text</span><br><span class="line"></span><br><span class="line">第 2 层：通用配置树</span><br><span class="line">    config_t sections and keys</span><br><span class="line"></span><br><span class="line">第 3 层：设备根对象</span><br><span class="line">    co_dev_t metadata + object red-black tree</span><br><span class="line"></span><br><span class="line">第 4 层：对象节点</span><br><span class="line">    co_obj_t index + sub-object red-black tree</span><br><span class="line"></span><br><span class="line">第 5 层：数据入口</span><br><span class="line">    co_sub_t type + access + values + callbacks</span><br><span class="line"></span><br><span class="line">第 6 层：协议消费</span><br><span class="line">    SDO / PDO / NMT / EMCY / LSS / application</span><br></pre></td></tr></table></figure><p>再加两条旁路：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">concise DCF</span><br><span class="line">    directly restores current values into an existing tree</span><br><span class="line"></span><br><span class="line">TPDO event</span><br><span class="line">    scans PDO configuration and reports which producer is affected</span><br></pre></td></tr></table></figure><p>最终完整图：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[&quot;EDS or DCF text&quot;] --&gt; B[&quot;INI config parser&quot;]</span><br><span class="line">    B --&gt; C[&quot;co_dev_t&quot;]</span><br><span class="line">    C --&gt; D[&quot;object rbtree by 16-bit index&quot;]</span><br><span class="line">    D --&gt; E[&quot;co_obj_t&quot;]</span><br><span class="line">    E --&gt; F[&quot;sub-object rbtree by 8-bit sub-index&quot;]</span><br><span class="line">    F --&gt; G[&quot;co_sub_t&quot;]</span><br><span class="line"></span><br><span class="line">    H[&quot;DeviceComissioning&quot;] --&gt; I[&quot;Node-ID and network assignment&quot;]</span><br><span class="line">    I --&gt; C</span><br><span class="line">    I --&gt; J[&quot;rebase NODEID-relative values&quot;]</span><br><span class="line">    J --&gt; G</span><br><span class="line"></span><br><span class="line">    K[&quot;concise DCF binary&quot;] --&gt; L[&quot;restore current values&quot;]</span><br><span class="line">    L --&gt; G</span><br><span class="line"></span><br><span class="line">    G --&gt; M[&quot;SDO and RPDO write&quot;]</span><br><span class="line">    G --&gt; N[&quot;SDO and TPDO read&quot;]</span><br><span class="line">    G --&gt; O[&quot;local application access&quot;]</span><br><span class="line"></span><br><span class="line">    O --&gt; P[&quot;co_dev_tpdo_event&quot;]</span><br><span class="line">    P --&gt; Q[&quot;scan communication and mapping objects&quot;]</span><br><span class="line">    Q --&gt; R[&quot;TPDO service indication&quot;]</span><br></pre></td></tr></table></figure><p>最简洁的结论是：</p><blockquote><p><code>co_dev_t</code> 负责“一个设备是谁、有哪些对象，以及如何从 index 定位对象”；<code>dcf.c</code> 负责“把 EDS&#x2F;DCF 的描述语言翻译为这棵运行时对象树”；concise DCF 负责“向已有对象树回放参数值”；PDO event 负责“从一个对象值变化反查需要被通知的 PDO”。这四部分共同把静态设备描述、运行时数据和 CANopen 协议对象连接起来。</p></blockquote><hr><h2 id="参考源码与资料"><a href="#参考源码与资料" class="headerlink" title="参考源码与资料"></a>参考源码与资料</h2><h3 id="本文核心源码"><a href="#本文核心源码" class="headerlink" title="本文核心源码"></a>本文核心源码</h3><ul><li><a href="https://github.com/lely-industries/lely-core/blob/620d1858eb8520dbc3dc5e1a7314565becd54199/include/lely/co/dcf.h"><code>include/lely/co/dcf.h</code></a></li><li><a href="https://github.com/lely-industries/lely-core/blob/620d1858eb8520dbc3dc5e1a7314565becd54199/include/lely/co/dev.h"><code>include/lely/co/dev.h</code></a></li><li><a href="https://github.com/lely-industries/lely-core/blob/620d1858eb8520dbc3dc5e1a7314565becd54199/include/lely/co/detail/dev.h"><code>include/lely/co/detail/dev.h</code></a></li><li><a href="https://github.com/lely-industries/lely-core/blob/620d1858eb8520dbc3dc5e1a7314565becd54199/src/co/dev.c"><code>src/co/dev.c</code></a></li><li><a href="https://github.com/lely-industries/lely-core/blob/620d1858eb8520dbc3dc5e1a7314565becd54199/src/co/dcf.c"><code>src/co/dcf.c</code></a></li></ul><h3 id="关联源码"><a href="#关联源码" class="headerlink" title="关联源码"></a>关联源码</h3><ul><li><code>include/lely/co/obj.h</code></li><li><code>include/lely/co/detail/obj.h</code></li><li><code>src/co/obj.c</code></li><li><code>include/lely/co/val.h</code></li><li><code>src/co/val.c</code></li><li><code>include/lely/co/type.h</code></li><li><code>src/co/type.c</code></li><li><code>include/lely/util/config.h</code></li><li><code>src/util/config.c</code></li><li><code>include/lely/co/sdev.h</code></li><li><code>src/co/sdev.c</code></li><li><code>src/co/pdo.c</code></li></ul><h3 id="Lely-官方资料"><a href="#Lely-官方资料" class="headerlink" title="Lely 官方资料"></a>Lely 官方资料</h3><ul><li><a href="https://opensource.lely.com/canopen/docs/overview/">Library overview</a></li><li><a href="https://opensource.lely.com/canopen/docs/configuration/">Build configuration</a></li><li><a href="https://opensource.lely.com/canopen/docs/dcf2c/">EDS&#x2F;DCF-to-C conversion tool</a></li><li><a href="https://opensource.lely.com/canopen/docs/dcf-tools/">EDS&#x2F;DCF tools</a></li></ul><h3 id="协议参考"><a href="#协议参考" class="headerlink" title="协议参考"></a>协议参考</h3><ul><li>CiA 301 V4.2.0：CANopen 设备模型、对象字典、对象代码、索引&#x2F;子索引、PDO 参数对象和 SDO 访问模型。</li></ul><h3 id="前置学习笔记"><a href="#前置学习笔记" class="headerlink" title="前置学习笔记"></a>前置学习笔记</h3><ul><li><code>Lely_CANopen_co_obj_co_sub_对象字典节点_机制原理与运行流程.md</code></li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;meta name=&quot;referrer&quot; content=&quot;no-referrer&quot; /&gt;

&lt;h1 id=&quot;Lely-CANopen：co-dev-设备对象字典与-EDS-DCF-解析、concise-DCF-和-PDO-事件机制&quot;&gt;&lt;a href=&quot;#Lely-CANope</summary>
      
    
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/categories/canopen/"/>
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/tags/canopen/"/>
    
  </entry>
  
  <entry>
    <title>Lely CANopen：can_net、io_can_net设计原理</title>
    <link href="https://wdfk-prog.space/posts/1f644911/"/>
    <id>https://wdfk-prog.space/posts/1f644911/</id>
    <published>2026-07-29T06:18:52.000Z</published>
    <updated>2026-07-29T06:20:17.738Z</updated>
    
    <content type="html"><![CDATA[<meta name="referrer" content="no-referrer" /><h1 id="Lely-CANopen：can-net、io-can-net-与-CAN-channel-的分层关系，以及-TX-RX-Ring-的设计原理"><a href="#Lely-CANopen：can-net、io-can-net-与-CAN-channel-的分层关系，以及-TX-RX-Ring-的设计原理" class="headerlink" title="Lely CANopen：can_net、io_can_net 与 CAN channel 的分层关系，以及 TX&#x2F;RX Ring 的设计原理"></a>Lely CANopen：can_net、io_can_net 与 CAN channel 的分层关系，以及 TX&#x2F;RX Ring 的设计原理</h1><p>@[toc]</p><h2 id="一句话结论"><a href="#一句话结论" class="headerlink" title="一句话结论"></a>一句话结论</h2><p>Lely 中容易混淆的几个对象，实际处于不同层次：</p><ul><li><code>can_net_t</code> 是<strong>纯内存中的 CAN 逻辑调度核心</strong>，负责协议定时器、CAN-ID 接收分发和发送回调；它不知道 SocketCAN、文件描述符、<code>poll</code>、executor 或真实 CAN 控制器。</li><li><code>io_can_chan_t</code> 是<strong>抽象 CAN 通道接口</strong>；<code>src/io2/linux/can_chan.c</code> 是它在 Linux SocketCAN 上的具体后端，负责文件描述符、收发操作、I&#x2F;O 就绪事件和写确认。</li><li><code>io_can_net_t</code> 是<strong>桥接层</strong>，把 <code>can_net_t</code>、<code>io_timer_t</code>、<code>io_tqueue_t</code> 和 <code>io_can_chan_t</code> 连接起来，使同步的协议核心能够运行在异步 I&#x2F;O 框架上。</li><li><code>lely::io::CanNet</code> 只是 <code>io_can_net_t*</code> 的 C++ RAII 包装，不是另一套独立实现；核心机制仍然由 C 文件 <code>src/io2/can_net.c</code> 和 <code>src/can/net.c</code> 完成。</li></ul><p><code>tx_ring</code> 和 <code>rxring</code> 放在不同层，并不是不对称设计，而是由数据来源和所有权决定的：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line">协议对象产生发送帧</span><br><span class="line">    ↓</span><br><span class="line">can_net_send()</span><br><span class="line">    ↓</span><br><span class="line">io_can_net.tx_ring</span><br><span class="line">    ↓ 解决协议突发、保存帧副本、串行提交写操作</span><br><span class="line">io_can_chan_submit_write()</span><br><span class="line">    ↓</span><br><span class="line">Linux SocketCAN</span><br><span class="line"></span><br><span class="line">Linux SocketCAN 收到帧</span><br><span class="line">    ↓</span><br><span class="line">Linux can_chan 后端</span><br><span class="line">    ↓</span><br><span class="line">io_can_chan_impl.rxring</span><br><span class="line">    ↓ 解决 fd 就绪与异步 read 请求之间的速率差</span><br><span class="line">io_can_net 的 read completion</span><br><span class="line">    ↓</span><br><span class="line">can_net_recv()</span><br><span class="line">    ↓</span><br><span class="line">CANopen 协议接收器</span><br></pre></td></tr></table></figure><p>最关键的区别是：</p><blockquote><p><code>io_can_net.tx_ring</code> 缓存的是“等待发送的 CAN 帧副本”；Linux <code>can_chan</code> 内的 <code>write_queue</code> 缓存的是“等待执行的异步写操作”。两者不是同一种队列，也不能互相替代。</p></blockquote><hr><h2 id="1-先澄清：这些名称不是同一个-net"><a href="#1-先澄清：这些名称不是同一个-net" class="headerlink" title="1. 先澄清：这些名称不是同一个 net"></a>1. 先澄清：这些名称不是同一个 <code>net</code></h2><h3 id="1-1-名称与实际对象对照"><a href="#1-1-名称与实际对象对照" class="headerlink" title="1.1 名称与实际对象对照"></a>1.1 名称与实际对象对照</h3><table><thead><tr><th>名称</th><th>实际类型</th><th>所在层</th><th>核心职责</th></tr></thead><tbody><tr><td><code>can_net_t</code></td><td><code>struct __can_net</code></td><td><code>can</code> 逻辑核心层</td><td>定时器调度、CAN-ID 接收分发、发送回调</td></tr><tr><td><code>io_can_net_t</code></td><td><code>struct io_can_net</code></td><td><code>io2</code> 桥接层</td><td>把逻辑核心接到 Timer、executor 和 CAN channel</td></tr><tr><td><code>lely::io::CanNet</code></td><td>C++ 类</td><td>C++ 包装层</td><td>RAII 生命周期、虚函数错误回调、类型转换</td></tr><tr><td><code>io_can_chan_t</code></td><td>抽象接口，实际为 vtable 指针</td><td><code>io2</code> 抽象设备层</td><td>定义同步&#x2F;异步 CAN read、write 操作</td></tr><tr><td><code>io_can_chan_impl</code></td><td>Linux 后端私有结构体</td><td>Linux SocketCAN 后端</td><td><code>fd</code>、<code>poll</code>、<code>rxring</code>、操作队列、写确认</td></tr><tr><td><code>net</code></td><td>参数名或成员名</td><td>取决于上下文</td><td>仅是变量名称，必须看其声明类型</td></tr></tbody></table><p>例如，<code>src/io2/can_net.c</code> 中有两层 <code>net</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">函数参数 net</span><br><span class="line">    类型：io_can_net_t *</span><br><span class="line"></span><br><span class="line">net-&gt;net</span><br><span class="line">    类型：can_net_t *</span><br></pre></td></tr></table></figure><p>因此看到下面的表达式时：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">can_net_recv(net-&gt;net, &amp;net-&gt;read_msg)</span><br></pre></td></tr></table></figure><p>应当读成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">把 io_can_net 对象中保存的 read_msg</span><br><span class="line">交给其内部的 can_net_t 逻辑核心处理</span><br></pre></td></tr></table></figure><p>而不是“net 调用另一个同名 net”。</p><h3 id="1-2-CiA-301-与-Lely-实现边界"><a href="#1-2-CiA-301-与-Lely-实现边界" class="headerlink" title="1.2 CiA 301 与 Lely 实现边界"></a>1.2 CiA 301 与 Lely 实现边界</h3><p>CiA 301 定义 CANopen 应用层、通信对象、对象字典和网络管理协议，并规定这些协议通过 CAN 数据链路层交换数据。</p><p>但以下名称不是 CiA 301 的标准对象名称：</p><ul><li><code>can_net_t</code>；</li><li><code>io_can_net_t</code>；</li><li><code>io_can_chan_t</code>；</li><li><code>io_tqueue_t</code>；</li><li><code>spscring</code>。</li></ul><p>它们是 Lely 为了实现协议栈、异步 I&#x2F;O 和平台适配而设计的软件抽象。</p><hr><h2 id="2-相关文件分别负责什么"><a href="#2-相关文件分别负责什么" class="headerlink" title="2. 相关文件分别负责什么"></a>2. 相关文件分别负责什么</h2><p>用户列出的文件可以分成四组。</p><h3 id="2-1-CAN-逻辑核心"><a href="#2-1-CAN-逻辑核心" class="headerlink" title="2.1 CAN 逻辑核心"></a>2.1 CAN 逻辑核心</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">include/lely/can/net.h</span><br><span class="line">src/can/net.c</span><br></pre></td></tr></table></figure><p>这组文件定义并实现：</p><ul><li><code>can_net_t</code>；</li><li><code>can_timer_t</code>；</li><li><code>can_recv_t</code>；</li><li><code>can_net_set_time()</code>；</li><li><code>can_net_recv()</code>；</li><li><code>can_net_send()</code>；</li><li><code>can_net_set_next_func()</code>；</li><li><code>can_net_set_send_func()</code>。</li></ul><p>它只处理：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">时间</span><br><span class="line">CAN 消息</span><br><span class="line">回调函数</span><br><span class="line">内存数据结构</span><br></pre></td></tr></table></figure><p>它不直接执行：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">read(fd)</span><br><span class="line">write(fd)</span><br><span class="line">poll/epoll</span><br><span class="line">timerfd</span><br><span class="line">线程阻塞</span><br><span class="line">硬件驱动调用</span><br></pre></td></tr></table></figure><h3 id="2-2-抽象-CAN-通道"><a href="#2-2-抽象-CAN-通道" class="headerlink" title="2.2 抽象 CAN 通道"></a>2.2 抽象 CAN 通道</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">include/lely/io2/can.h</span><br></pre></td></tr></table></figure><p>该文件定义 <code>io_can_chan_t</code> 的统一接口，包括：</p><ul><li>同步 <code>read()</code> &#x2F; <code>write()</code>；</li><li>异步 <code>submit_read()</code> &#x2F; <code>submit_write()</code>；</li><li>取消和中止操作；</li><li><code>io_can_chan_read</code> 与 <code>io_can_chan_write</code> 操作对象；</li><li>每个操作完成后执行的 <code>ev_task</code>。</li></ul><p>其本质是一个 vtable 接口：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">io_can_chan_t</span><br><span class="line">    ↓</span><br><span class="line">struct io_can_chan_vtbl</span><br><span class="line">    ├── get_dev</span><br><span class="line">    ├── get_flags</span><br><span class="line">    ├── read</span><br><span class="line">    ├── submit_read</span><br><span class="line">    ├── write</span><br><span class="line">    └── submit_write</span><br></pre></td></tr></table></figure><h3 id="2-3-CAN-网络桥接层"><a href="#2-3-CAN-网络桥接层" class="headerlink" title="2.3 CAN 网络桥接层"></a>2.3 CAN 网络桥接层</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">include/lely/io2/can_net.h</span><br><span class="line">src/io2/can_net.c</span><br></pre></td></tr></table></figure><p>该层把下面四部分连接起来：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">can_net_t       协议逻辑核心</span><br><span class="line">io_can_chan_t   实际 CAN 通道</span><br><span class="line">io_timer_t      系统定时器</span><br><span class="line">executor        异步任务执行器</span><br></pre></td></tr></table></figure><p>其公开接口明确说明：发送帧会先进入用户态发送队列，再提交给底层 CAN channel。</p><h3 id="2-4-C-包装层"><a href="#2-4-C-包装层" class="headerlink" title="2.4 C++ 包装层"></a>2.4 C++ 包装层</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">include/lely/io2/can_net.hpp</span><br></pre></td></tr></table></figure><p><code>lely::io::CanNet</code> 的构造函数调用：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">io_can_net_create(...)</span><br></pre></td></tr></table></figure><p>析构函数调用：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">io_can_net_destroy(...)</span><br></pre></td></tr></table></figure><p>它还把 C 回调转换为可重写的 C++ 虚函数：</p><ul><li><code>on_read_error()</code>；</li><li><code>on_queue_error()</code>；</li><li><code>on_write_error()</code>；</li><li><code>on_can_state()</code>；</li><li><code>on_can_error()</code>。</li></ul><p>所以答案很明确：</p><blockquote><p>有完整的 C 实现。<code>can_net.hpp</code> 只是 C API 的封装层，不负责底层运行机制。</p></blockquote><hr><h2 id="3-总体对象关系"><a href="#3-总体对象关系" class="headerlink" title="3. 总体对象关系"></a>3. 总体对象关系</h2><h3 id="3-1-静态对象图"><a href="#3-1-静态对象图" class="headerlink" title="3.1 静态对象图"></a>3.1 静态对象图</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    CPP[&quot;lely::io::CanNet&lt;br/&gt;C++ RAII wrapper&quot;] --&gt; IONET[&quot;io_can_net_t&lt;br/&gt;I/O bridge&quot;]</span><br><span class="line"></span><br><span class="line">    IONET --&gt; CORE[&quot;can_net_t&lt;br/&gt;logical CAN core&quot;]</span><br><span class="line">    IONET --&gt; TQ[&quot;io_tqueue_t&lt;br/&gt;timer wait multiplexer&quot;]</span><br><span class="line">    IONET --&gt; CHAN[&quot;io_can_chan_t&lt;br/&gt;abstract CAN channel&quot;]</span><br><span class="line">    IONET --&gt; EXEC[&quot;ev_exec_t&lt;br/&gt;executor&quot;]</span><br><span class="line"></span><br><span class="line">    TQ --&gt; TIMER[&quot;io_timer_t&lt;br/&gt;system timer abstraction&quot;]</span><br><span class="line">    CHAN --&gt; LINUX[&quot;io_can_chan_impl&lt;br/&gt;Linux SocketCAN backend&quot;]</span><br><span class="line"></span><br><span class="line">    LINUX --&gt; FD[&quot;SocketCAN fd&quot;]</span><br><span class="line">    LINUX --&gt; POLL[&quot;io_poll watch&quot;]</span><br><span class="line">    LINUX --&gt; RXRING[&quot;rxring + rxbuf&quot;]</span><br><span class="line">    LINUX --&gt; OPQ[&quot;read/write/confirm operation queues&quot;]</span><br><span class="line"></span><br><span class="line">    CORE --&gt; THEAP[&quot;timer_heap&lt;br/&gt;pheap&quot;]</span><br><span class="line">    CORE --&gt; RTREE[&quot;recv_tree&lt;br/&gt;rbtree&quot;]</span><br><span class="line">    CORE --&gt; SENDFUNC[&quot;send_func callback&quot;]</span><br></pre></td></tr></table></figure><h3 id="3-2-io-can-net-t-是整个连接点"><a href="#3-2-io-can-net-t-是整个连接点" class="headerlink" title="3.2 io_can_net_t 是整个连接点"></a>3.2 <code>io_can_net_t</code> 是整个连接点</h3><p><code>struct io_can_net</code> 内部同时保存：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">io_timer_t *timer</span><br><span class="line">io_tqueue_t *tq</span><br><span class="line">io_can_chan_t *chan</span><br><span class="line">can_net_t *net</span><br></pre></td></tr></table></figure><p>并额外保存：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">read operation</span><br><span class="line">write operation</span><br><span class="line">tx_ring + tx_buf</span><br><span class="line">wait_next</span><br><span class="line">wait_confirm</span><br><span class="line">错误统计与回调</span><br><span class="line">运行状态标志</span><br></pre></td></tr></table></figure><p>因此 <code>io_can_net_t</code> 不是 CANopen 协议状态机本身，也不是 Linux CAN 驱动，而是：</p><blockquote><p>将“同步回调式逻辑核心”适配到“异步 Timer + CAN channel + executor”的运行时对象。</p></blockquote><hr><h2 id="4-can-net-t-到底是什么"><a href="#4-can-net-t-到底是什么" class="headerlink" title="4. can_net_t 到底是什么"></a>4. <code>can_net_t</code> 到底是什么</h2><h3 id="4-1-内部只有三类核心资源"><a href="#4-1-内部只有三类核心资源" class="headerlink" title="4.1 内部只有三类核心资源"></a>4.1 内部只有三类核心资源</h3><p><code>struct __can_net</code> 可以概括为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">1. timer_heap</span><br><span class="line">   管理所有 can_timer_t</span><br><span class="line"></span><br><span class="line">2. recv_tree</span><br><span class="line">   管理所有 can_recv_t</span><br><span class="line"></span><br><span class="line">3. send_func</span><br><span class="line">   把发送请求交给外部实现</span><br></pre></td></tr></table></figure><p>再加上：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">time</span><br><span class="line">next</span><br><span class="line">next_func</span><br></pre></td></tr></table></figure><p>用于维护逻辑时钟和最早定时器。</p><h3 id="4-2-它不是设备驱动"><a href="#4-2-它不是设备驱动" class="headerlink" title="4.2 它不是设备驱动"></a>4.2 它不是设备驱动</h3><p><code>can_net_t</code> 不知道一帧 CAN 消息来自：</p><ul><li>Linux SocketCAN；</li><li>Windows；</li><li>RTOS CAN 驱动；</li><li>裸机中断；</li><li>测试桩；</li><li>仿真总线。</li></ul><p>外部只要在收到消息时调用：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">can_net_recv(core, msg)</span><br></pre></td></tr></table></figure><p>并为发送注册：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">can_net_set_send_func(core, send_func, data)</span><br></pre></td></tr></table></figure><p>逻辑核心就可以工作。</p><h3 id="4-3-接收方向：recv-tree"><a href="#4-3-接收方向：recv-tree" class="headerlink" title="4.3 接收方向：recv_tree"></a>4.3 接收方向：<code>recv_tree</code></h3><p>CANopen 协议对象会创建 <code>can_recv_t</code>，并按 CAN-ID 和 flags 注册到 <code>can_net_t</code>。</p><p>收到帧后：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">can_net_recv()</span><br><span class="line">    ↓</span><br><span class="line">根据 CAN-ID + flags 生成 key</span><br><span class="line">    ↓</span><br><span class="line">rbtree_find()</span><br><span class="line">    ↓</span><br><span class="line">遍历相同 key 下的 receiver list</span><br><span class="line">    ↓</span><br><span class="line">调用对应 can_recv_func_t</span><br></pre></td></tr></table></figure><p>因此 <code>can_net_t</code> 在接收方向是一个：</p><blockquote><p>CAN-ID 到协议回调的同步分发器。</p></blockquote><h3 id="4-4-发送方向：只调用回调"><a href="#4-4-发送方向：只调用回调" class="headerlink" title="4.4 发送方向：只调用回调"></a>4.4 发送方向：只调用回调</h3><p><code>can_net_send()</code> 不直接发送到硬件。</p><p>它只做：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">检查 send_func 是否存在</span><br><span class="line">    ↓</span><br><span class="line">调用 send_func(msg, send_data)</span><br><span class="line">    ↓</span><br><span class="line">返回该回调的结果</span><br></pre></td></tr></table></figure><p>这意味着 <code>can_net_t</code> 对发送端的唯一要求是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">外部提供一个“接收发送请求”的函数</span><br></pre></td></tr></table></figure><p>在 <code>io_can_net_t</code> 中，这个函数就是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">io_can_net_send_func()</span><br></pre></td></tr></table></figure><h3 id="4-5-定时方向：timer-heap"><a href="#4-5-定时方向：timer-heap" class="headerlink" title="4.5 定时方向：timer_heap"></a>4.5 定时方向：<code>timer_heap</code></h3><p><code>can_timer_t</code> 通过 <code>pnode</code> 插入 <code>can_net.timer_heap</code>。</p><p><code>can_net_set_time()</code> 每次推进逻辑时间时：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">查看 timer_heap 的 root</span><br><span class="line">    ↓</span><br><span class="line">如果 timer.start &lt;= current time</span><br><span class="line">    ↓</span><br><span class="line">移除该 timer</span><br><span class="line">    ↓</span><br><span class="line">周期 timer 重新计算 start 并入堆</span><br><span class="line">    ↓</span><br><span class="line">调用 timer callback</span><br><span class="line">    ↓</span><br><span class="line">继续处理新的 root</span><br></pre></td></tr></table></figure><p>处理完成后，<code>can_net_set_next()</code> 取新的最早 timer，并通过 <code>next_func</code> 通知 <code>io_can_net_t</code>。</p><p>这正是前一篇 <code>io_tqueue + pheap</code> 学习笔记中“两级最早截止时间压缩”的第一层。</p><hr><h2 id="5-io-can-chan-t-与-Linux-can-chan-c"><a href="#5-io-can-chan-t-与-Linux-can-chan-c" class="headerlink" title="5. io_can_chan_t 与 Linux can_chan.c"></a>5. <code>io_can_chan_t</code> 与 Linux <code>can_chan.c</code></h2><h3 id="5-1-io-can-chan-t-是接口，不是完整对象布局"><a href="#5-1-io-can-chan-t-是接口，不是完整对象布局" class="headerlink" title="5.1 io_can_chan_t 是接口，不是完整对象布局"></a>5.1 <code>io_can_chan_t</code> 是接口，不是完整对象布局</h3><p><code>io_can_chan_t</code> 通过 vtable 调用后端实现。</p><p>调用：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">io_can_chan_submit_read(chan, read)</span><br></pre></td></tr></table></figure><p>最终会跳转到当前后端的：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">io_can_chan_impl_submit_read()</span><br></pre></td></tr></table></figure><p>调用：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">io_can_chan_submit_write(chan, write)</span><br></pre></td></tr></table></figure><p>最终会跳转到：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">io_can_chan_impl_submit_write()</span><br></pre></td></tr></table></figure><p>Linux、POSIX、Windows 或其他后端可以使用不同实现，但上层 <code>io_can_net_t</code> 不需要知道差异。</p><h3 id="5-2-Linux-后端维护的资源"><a href="#5-2-Linux-后端维护的资源" class="headerlink" title="5.2 Linux 后端维护的资源"></a>5.2 Linux 后端维护的资源</h3><p><code>src/io2/linux/can_chan.c</code> 中的 <code>io_can_chan_impl</code> 主要包含：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">SocketCAN fd</span><br><span class="line">io_poll_watch</span><br><span class="line">rxbuf_task</span><br><span class="line">read_task</span><br><span class="line">write_task</span><br><span class="line">rxring + rxbuf</span><br><span class="line">read_queue</span><br><span class="line">write_queue</span><br><span class="line">confirm_queue</span><br><span class="line">current_write</span><br><span class="line">flags / state / mutex</span><br></pre></td></tr></table></figure><p>其中：</p><ul><li><code>rxbuf_task</code>：从 SocketCAN 读取数据并填充接收环形缓冲区；</li><li><code>read_task</code>：用缓冲区中的帧完成上层 read operation；</li><li><code>write_task</code>：执行等待中的 write operation；</li><li><code>confirm_queue</code>：在启用 <code>MSG_CONFIRM</code> 时等待发送确认。</li></ul><hr><h2 id="6-io-can-net-t-初始化时做了什么"><a href="#6-io-can-net-t-初始化时做了什么" class="headerlink" title="6. io_can_net_t 初始化时做了什么"></a>6. <code>io_can_net_t</code> 初始化时做了什么</h2><p><code>io_can_net_init()</code> 的核心步骤可压缩为：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[&quot;保存 executor、timer、channel&quot;] --&gt; B[&quot;创建 io_tqueue&quot;]</span><br><span class="line">    B --&gt; C[&quot;初始化 wait_next 和 wait_confirm&quot;]</span><br><span class="line">    C --&gt; D[&quot;初始化异步 read/write operation&quot;]</span><br><span class="line">    D --&gt; E[&quot;初始化 tx_ring 并分配 tx_buf&quot;]</span><br><span class="line">    E --&gt; F[&quot;创建内部 can_net_t&quot;]</span><br><span class="line">    F --&gt; G[&quot;读取系统时间并调用 can_net_set_time&quot;]</span><br><span class="line">    G --&gt; H[&quot;注册 can_net next_func&quot;]</span><br><span class="line">    H --&gt; I[&quot;注册 can_net send_func&quot;]</span><br><span class="line">    I --&gt; J[&quot;注册到 io_ctx&quot;]</span><br></pre></td></tr></table></figure><p>两个关键注册动作是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">can_net_set_next_func(core, io_can_net_next_func, io_net)</span><br><span class="line">can_net_set_send_func(core, io_can_net_send_func, io_net)</span><br></pre></td></tr></table></figure><p>这两个回调分别完成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">协议 timer_heap → I/O Timer</span><br><span class="line">协议发送请求   → 异步 CAN channel</span><br></pre></td></tr></table></figure><h3 id="6-1-io-can-net-start"><a href="#6-1-io-can-net-start" class="headerlink" title="6.1 io_can_net_start()"></a>6.1 <code>io_can_net_start()</code></h3><p>启动后主要做两件事：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">1. 等待 tx_ring 中出现待发送帧</span><br><span class="line">2. 向 CAN channel 提交一个异步 read operation</span><br></pre></td></tr></table></figure><p>read operation 完成后会再次提交，因此形成持续接收循环。</p><hr><h2 id="7-完整接收流程"><a href="#7-完整接收流程" class="headerlink" title="7. 完整接收流程"></a>7. 完整接收流程</h2><h3 id="7-1-从-SocketCAN-到-CANopen-协议对象"><a href="#7-1-从-SocketCAN-到-CANopen-协议对象" class="headerlink" title="7.1 从 SocketCAN 到 CANopen 协议对象"></a>7.1 从 SocketCAN 到 CANopen 协议对象</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant K as SocketCAN</span><br><span class="line">    participant P as io_poll</span><br><span class="line">    participant RXB as rxbuf_task</span><br><span class="line">    participant R as rxring</span><br><span class="line">    participant RT as read_task</span><br><span class="line">    participant ION as io_can_net</span><br><span class="line">    participant CORE as can_net_t</span><br><span class="line">    participant CO as CANopen receiver</span><br><span class="line"></span><br><span class="line">    K-&gt;&gt;P: fd readable</span><br><span class="line">    P-&gt;&gt;RXB: post rxbuf_task</span><br><span class="line">    RXB-&gt;&gt;K: read CAN frame</span><br><span class="line">    RXB-&gt;&gt;R: store frame and timestamp</span><br><span class="line">    R-&gt;&gt;RT: signal frame available</span><br><span class="line">    RT-&gt;&gt;ION: complete io_can_chan_read</span><br><span class="line">    ION-&gt;&gt;ION: update logical time</span><br><span class="line">    ION-&gt;&gt;CORE: can_net_recv(msg)</span><br><span class="line">    CORE-&gt;&gt;CO: dispatch matching receiver callback</span><br><span class="line">    ION-&gt;&gt;RT: submit next read operation</span><br></pre></td></tr></table></figure><h3 id="7-2-Linux-后端为什么先放入-rxring"><a href="#7-2-Linux-后端为什么先放入-rxring" class="headerlink" title="7.2 Linux 后端为什么先放入 rxring"></a>7.2 Linux 后端为什么先放入 <code>rxring</code></h3><p>文件描述符“可读”与上层 read operation 的执行时机不是同一件事：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">SocketCAN 已经有数据</span><br><span class="line">    ≠</span><br><span class="line">executor 现在立刻执行上层 completion task</span><br></pre></td></tr></table></figure><p>Linux 后端需要把：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">fd readiness</span><br><span class="line">系统调用读取</span><br><span class="line">原始 CAN/CAN FD 帧</span><br><span class="line">接收时间戳</span><br><span class="line">MSG_CONFIRM 标志</span><br><span class="line">异步 read operation</span><br></pre></td></tr></table></figure><p>连接起来。</p><p>因此 <code>rxring</code> 是 I&#x2F;O 后端内部的数据缓冲区，而不是协议队列。</p><h3 id="7-3-rxring-中保存的不是单纯-can-msg"><a href="#7-3-rxring-中保存的不是单纯-can-msg" class="headerlink" title="7.3 rxring 中保存的不是单纯 can_msg"></a>7.3 <code>rxring</code> 中保存的不是单纯 <code>can_msg</code></h3><p>Linux 后端的接收缓冲元素还包含：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">SocketCAN can_frame 或 canfd_frame</span><br><span class="line">实际字节数 nbytes</span><br><span class="line">接收时间戳 timespec</span><br></pre></td></tr></table></figure><p>这些信息与 Linux SocketCAN 的读取过程直接相关，所以放在 Linux channel 后端最合理。</p><h3 id="7-4-写确认也必须在接收后端处理"><a href="#7-4-写确认也必须在接收后端处理" class="headerlink" title="7.4 写确认也必须在接收后端处理"></a>7.4 写确认也必须在接收后端处理</h3><p>启用 <code>txwait</code> 后，SocketCAN 的发送确认以接收路径中的 <code>MSG_CONFIRM</code> 返回。</p><p>后端读取帧后必须先判断：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">普通接收帧</span><br><span class="line">    → 进入 rxring</span><br><span class="line"></span><br><span class="line">MSG_CONFIRM</span><br><span class="line">    → 匹配 confirm_queue 中的 write operation</span><br></pre></td></tr></table></figure><p>如果把接收环移到 <code>io_can_net_t</code>，底层后端仍然必须先解析 <code>MSG_CONFIRM</code>，反而会造成职责拆分和重复缓存。</p><hr><h2 id="8-完整发送流程"><a href="#8-完整发送流程" class="headerlink" title="8. 完整发送流程"></a>8. 完整发送流程</h2><h3 id="8-1-从-CANopen-协议对象到-SocketCAN"><a href="#8-1-从-CANopen-协议对象到-SocketCAN" class="headerlink" title="8.1 从 CANopen 协议对象到 SocketCAN"></a>8.1 从 CANopen 协议对象到 SocketCAN</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant CO as CANopen protocol</span><br><span class="line">    participant CORE as can_net_t</span><br><span class="line">    participant TXR as io_can_net tx_ring</span><br><span class="line">    participant ION as io_can_net write operation</span><br><span class="line">    participant CH as io_can_chan</span><br><span class="line">    participant WQ as Linux write_queue</span><br><span class="line">    participant K as SocketCAN</span><br><span class="line"></span><br><span class="line">    CO-&gt;&gt;CORE: can_net_send(msg)</span><br><span class="line">    CORE-&gt;&gt;TXR: io_can_net_send_func copies msg</span><br><span class="line">    TXR-&gt;&gt;ION: consumer signal when frame becomes available</span><br><span class="line">    ION-&gt;&gt;TXR: fetch one frame into write_msg</span><br><span class="line">    ION-&gt;&gt;CH: submit_write</span><br><span class="line">    CH-&gt;&gt;WQ: queue write operation</span><br><span class="line">    WQ-&gt;&gt;K: write CAN frame</span><br><span class="line">    K--&gt;&gt;CH: completion or confirmation</span><br><span class="line">    CH--&gt;&gt;ION: write completion task</span><br><span class="line">    ION-&gt;&gt;TXR: commit one consumed frame</span><br><span class="line">    ION-&gt;&gt;TXR: fetch next frame if available</span><br></pre></td></tr></table></figure><h3 id="8-2-can-net-send-成功不等于总线发送成功"><a href="#8-2-can-net-send-成功不等于总线发送成功" class="headerlink" title="8.2 can_net_send() 成功不等于总线发送成功"></a>8.2 <code>can_net_send()</code> 成功不等于总线发送成功</h3><p>对 <code>io_can_net_t</code> 而言，<code>can_net_send()</code> 返回成功通常表示：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">该帧已经成功复制到 io_can_net.tx_ring</span><br></pre></td></tr></table></figure><p>它并不表示：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">SocketCAN write 已完成</span><br><span class="line">CAN 仲裁已成功</span><br><span class="line">对端已收到</span><br><span class="line">发送确认已返回</span><br></pre></td></tr></table></figure><p>真正的异步写错误通过 <code>on_write_error</code> 回调报告。</p><p>当 <code>tx_ring</code> 已满时：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">io_can_net_send_func()</span><br><span class="line">    ↓</span><br><span class="line">返回 ERRNUM_AGAIN</span><br><span class="line">    ↓</span><br><span class="line">增加 tx_errcnt</span><br><span class="line">    ↓</span><br><span class="line">触发 on_queue_error</span><br></pre></td></tr></table></figure><p>因此 <code>tx_ring</code> 同时定义了协议发送端的背压边界。</p><hr><h2 id="9-为什么-tx-ring-在-io-can-net-t-中"><a href="#9-为什么-tx-ring-在-io-can-net-t-中" class="headerlink" title="9. 为什么 tx_ring 在 io_can_net_t 中"></a>9. 为什么 <code>tx_ring</code> 在 <code>io_can_net_t</code> 中</h2><h3 id="9-1-原因一：它服务的是-can-net-send-的语义"><a href="#9-1-原因一：它服务的是-can-net-send-的语义" class="headerlink" title="9.1 原因一：它服务的是 can_net_send() 的语义"></a>9.1 原因一：它服务的是 <code>can_net_send()</code> 的语义</h3><p><code>can_net_t</code> 的发送接口是同步回调：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">调用 send_func</span><br><span class="line">立即得到成功或失败</span><br></pre></td></tr></table></figure><p>而底层 CAN channel 使用异步 operation：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">提交 write</span><br><span class="line">稍后由 executor 执行 completion task</span><br></pre></td></tr></table></figure><p>两者之间必须有一个适配层：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">同步产生帧</span><br><span class="line">    ↓</span><br><span class="line">保存帧副本</span><br><span class="line">    ↓</span><br><span class="line">异步逐帧提交</span><br></pre></td></tr></table></figure><p>这个适配层就是 <code>io_can_net_t</code>，因此发送 ring 属于它。</p><h3 id="9-2-原因二：协议层可能在一个回调中产生多帧"><a href="#9-2-原因二：协议层可能在一个回调中产生多帧" class="headerlink" title="9.2 原因二：协议层可能在一个回调中产生多帧"></a>9.2 原因二：协议层可能在一个回调中产生多帧</h3><p>例如某个时间推进或接收回调可能连续触发：</p><ul><li>多个 PDO；</li><li>SDO 应答；</li><li>心跳；</li><li>EMCY；</li><li>NMT 相关消息；</li><li>多个同时到期的协议 timer。</li></ul><p>这些调用都通过 <code>can_net_send()</code> 快速产生消息。</p><p>如果每次都要求底层写操作立即完成，协议状态机会被 I&#x2F;O 延迟直接阻塞。</p><p><code>tx_ring</code> 允许：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">协议逻辑快速入队</span><br><span class="line">I/O 层按实际通道速度串行发送</span><br></pre></td></tr></table></figure><h3 id="9-3-原因三：发送队列长度是-io-can-net-的公开策略"><a href="#9-3-原因三：发送队列长度是-io-can-net-的公开策略" class="headerlink" title="9.3 原因三：发送队列长度是 io_can_net 的公开策略"></a>9.3 原因三：发送队列长度是 <code>io_can_net</code> 的公开策略</h3><p><code>io_can_net_create()</code> 直接接收：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">txlen</span><br></pre></td></tr></table></figure><p>用于配置用户态发送队列长度。</p><p>这说明该队列是 CAN 网络桥接层对外暴露的行为策略，而不是 Linux 后端的私有实现细节。</p><p>不同后端都应共享同样的：</p><ul><li>入队语义；</li><li>队列满错误；</li><li>丢帧计数；</li><li><code>on_queue_error</code> 回调；</li><li>顺序发送行为。</li></ul><p>所以它应当放在平台无关的 <code>src/io2/can_net.c</code> 中。</p><h3 id="9-4-原因四：io-can-net-t-只复用一个-write-operation"><a href="#9-4-原因四：io-can-net-t-只复用一个-write-operation" class="headerlink" title="9.4 原因四：io_can_net_t 只复用一个 write operation"></a>9.4 原因四：<code>io_can_net_t</code> 只复用一个 write operation</h3><p><code>io_can_net_t</code> 内部只维护：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">一个 write_msg</span><br><span class="line">一个 io_can_chan_write write</span><br></pre></td></tr></table></figure><p>运行方式是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">从 tx_ring 取一帧</span><br><span class="line">    ↓</span><br><span class="line">复制到 write_msg</span><br><span class="line">    ↓</span><br><span class="line">提交同一个 write operation</span><br><span class="line">    ↓</span><br><span class="line">完成后释放 ring 中这一帧</span><br><span class="line">    ↓</span><br><span class="line">继续下一帧</span><br></pre></td></tr></table></figure><p>因此：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">tx_ring = 多帧数据队列</span><br><span class="line">write    = 当前唯一在途操作</span><br></pre></td></tr></table></figure><p>这使发送状态机非常清晰。</p><h3 id="9-5-原因五：写确认超时属于桥接策略"><a href="#9-5-原因五：写确认超时属于桥接策略" class="headerlink" title="9.5 原因五：写确认超时属于桥接策略"></a>9.5 原因五：写确认超时属于桥接策略</h3><p><code>io_can_net_t</code> 还维护：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">wait_confirm</span><br><span class="line">txtimeo</span><br></pre></td></tr></table></figure><p>每次提交底层 write 后，会通过 <code>io_tqueue</code> 注册确认超时。</p><p>收到完成或写错误后再终止该等待。</p><p>这说明 <code>io_can_net_t</code> 管理的是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">帧排队</span><br><span class="line">逐帧写入</span><br><span class="line">写确认等待</span><br><span class="line">确认超时</span><br><span class="line">错误统计</span><br></pre></td></tr></table></figure><p>这些是完整的“逻辑网络发送策略”，不应散落在某个特定平台的 CAN channel 后端中。</p><hr><h2 id="10-为什么-rxring-在-Linux-can-chan-底层"><a href="#10-为什么-rxring-在-Linux-can-chan-底层" class="headerlink" title="10. 为什么 rxring 在 Linux can_chan 底层"></a>10. 为什么 <code>rxring</code> 在 Linux <code>can_chan</code> 底层</h2><h3 id="10-1-原因一：接收入口由底层设备决定"><a href="#10-1-原因一：接收入口由底层设备决定" class="headerlink" title="10.1 原因一：接收入口由底层设备决定"></a>10.1 原因一：接收入口由底层设备决定</h3><p>发送帧的生产者是上层协议，<code>io_can_net_t</code> 可以控制是否接受新帧。</p><p>接收帧的生产者则是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">CAN 控制器 / SocketCAN 内核队列</span><br></pre></td></tr></table></figure><p>当文件描述符可读时，底层必须尽快处理 I&#x2F;O 就绪事件，避免：</p><ul><li>内核接收队列堆积；</li><li>丢帧；</li><li><code>poll</code> 事件反复触发；</li><li>executor 调度延迟扩大。</li></ul><p>因此最接近 fd 的 channel 后端需要拥有接收缓冲。</p><h3 id="10-2-原因二：它连接的是-I-O-readiness-与-read-operation"><a href="#10-2-原因二：它连接的是-I-O-readiness-与-read-operation" class="headerlink" title="10.2 原因二：它连接的是 I&#x2F;O readiness 与 read operation"></a>10.2 原因二：它连接的是 I&#x2F;O readiness 与 read operation</h3><p>Linux 后端有两类队列：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">rxring</span><br><span class="line">    保存已经收到的数据</span><br><span class="line"></span><br><span class="line">read_queue</span><br><span class="line">    保存等待数据的 read operation</span><br></pre></td></tr></table></figure><p>二者分别代表：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">数据先到，操作后执行</span><br><span class="line">操作先到，数据后到</span><br></pre></td></tr></table></figure><p><code>read_task</code> 的职责就是在二者之间配对。</p><h3 id="10-3-原因三：接收数据包含平台相关信息"><a href="#10-3-原因三：接收数据包含平台相关信息" class="headerlink" title="10.3 原因三：接收数据包含平台相关信息"></a>10.3 原因三：接收数据包含平台相关信息</h3><p>Linux 后端需要处理：</p><ul><li><code>struct can_frame</code>；</li><li><code>struct canfd_frame</code>；</li><li><code>CAN_MTU</code> &#x2F; <code>CANFD_MTU</code>；</li><li><code>MSG_CONFIRM</code>；</li><li>SocketCAN 接收时间戳；</li><li><code>EAGAIN</code> &#x2F; <code>EWOULDBLOCK</code>；</li><li><code>IO_EVENT_IN</code>；</li><li>非阻塞 <code>read()</code>。</li></ul><p>这些都属于 channel 后端，不属于 <code>io_can_net_t</code>。</p><h3 id="10-4-原因四：channel-可以脱离-io-can-net-t-独立使用"><a href="#10-4-原因四：channel-可以脱离-io-can-net-t-独立使用" class="headerlink" title="10.4 原因四：channel 可以脱离 io_can_net_t 独立使用"></a>10.4 原因四：channel 可以脱离 <code>io_can_net_t</code> 独立使用</h3><p><code>io_can_chan_t</code> 是通用 CAN I&#x2F;O 接口。</p><p>用户可以直接：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">submit_read()</span><br><span class="line">submit_write()</span><br></pre></td></tr></table></figure><p>而不创建 <code>io_can_net_t</code> 或 CANopen 协议对象。</p><p>如果接收缓冲只放在 <code>io_can_net_t</code> 中，那么独立使用 channel 时就失去异步接收所需的缓冲和调度能力。</p><p>因此接收 ring 必须属于 channel 实现。</p><h3 id="10-5-原因五：避免二级接收缓存"><a href="#10-5-原因五：避免二级接收缓存" class="headerlink" title="10.5 原因五：避免二级接收缓存"></a>10.5 原因五：避免二级接收缓存</h3><p><code>io_can_net_t</code> 始终保持一个异步 read operation：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">read 完成</span><br><span class="line">    ↓</span><br><span class="line">can_net_recv()</span><br><span class="line">    ↓</span><br><span class="line">再次 submit_read()</span><br></pre></td></tr></table></figure><p>底层 channel 已经用 <code>rxring</code> 解决了 I&#x2F;O 与 task 调度之间的解耦。</p><p>如果 <code>io_can_net_t</code> 再增加一个 RX ring，会形成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">SocketCAN</span><br><span class="line">    ↓</span><br><span class="line">channel.rxring</span><br><span class="line">    ↓</span><br><span class="line">io_can_net.rx_ring</span><br><span class="line">    ↓</span><br><span class="line">can_net_recv</span><br></pre></td></tr></table></figure><p>其结果通常只是：</p><ul><li>多一次内存复制；</li><li>多一层容量配置；</li><li>多一套溢出策略；</li><li>增加延迟；</li><li>更难定位丢帧发生在哪一层。</li></ul><p>所以 Lely 没有在 <code>io_can_net_t</code> 中重复实现 RX ring。</p><hr><h2 id="11-关键点：write-queue-不是-tx-ring"><a href="#11-关键点：write-queue-不是-tx-ring" class="headerlink" title="11. 关键点：write_queue 不是 tx_ring"></a>11. 关键点：<code>write_queue</code> 不是 <code>tx_ring</code></h2><p>这是理解当前设计时最容易混淆的地方。</p><h3 id="11-1-各队列保存的内容不同"><a href="#11-1-各队列保存的内容不同" class="headerlink" title="11.1 各队列保存的内容不同"></a>11.1 各队列保存的内容不同</h3><table><thead><tr><th>队列</th><th>所在对象</th><th>保存内容</th><th>作用</th></tr></thead><tbody><tr><td><code>tx_ring + tx_buf</code></td><td><code>io_can_net_t</code></td><td><code>struct can_msg</code> 的副本</td><td>缓存协议层待发送帧</td></tr><tr><td><code>write_queue</code></td><td>Linux <code>io_can_chan_impl</code></td><td><code>io_can_chan_write.task</code> 节点</td><td>缓存等待执行的异步写操作</td></tr><tr><td><code>confirm_queue</code></td><td>Linux <code>io_can_chan_impl</code></td><td>已写出但等待确认的 write operation</td><td>匹配 SocketCAN <code>MSG_CONFIRM</code></td></tr><tr><td><code>rxring + rxbuf</code></td><td>Linux <code>io_can_chan_impl</code></td><td>收到的原始 CAN&#x2F;CAN FD 帧与时间戳</td><td>缓存已到达的数据</td></tr><tr><td><code>read_queue</code></td><td>Linux <code>io_can_chan_impl</code></td><td><code>io_can_chan_read.task</code> 节点</td><td>缓存等待数据的异步读操作</td></tr></tbody></table><h3 id="11-2-数据队列与操作队列不能替代"><a href="#11-2-数据队列与操作队列不能替代" class="headerlink" title="11.2 数据队列与操作队列不能替代"></a>11.2 数据队列与操作队列不能替代</h3><p><code>tx_ring</code> 解决：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">有很多 CAN 帧要发</span><br></pre></td></tr></table></figure><p><code>write_queue</code> 解决：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">有很多异步 write 请求等待 channel 执行</span><br></pre></td></tr></table></figure><p>在 <code>io_can_net_t</code> 的用法中，虽然 <code>tx_ring</code> 可以有很多帧，但它通常只向 channel 提交一个在途 write operation。</p><p>也就是说：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">100 个待发送帧</span><br><span class="line">    可以位于 tx_ring</span><br><span class="line"></span><br><span class="line">底层 write_queue</span><br><span class="line">    可能只有 1 个 io_can_net.write operation</span><br></pre></td></tr></table></figure><p>这正是两级队列职责不同的证明。</p><h3 id="11-3-为什么不把所有帧都变成独立-write-operation"><a href="#11-3-为什么不把所有帧都变成独立-write-operation" class="headerlink" title="11.3 为什么不把所有帧都变成独立 write operation"></a>11.3 为什么不把所有帧都变成独立 write operation</h3><p>若每个协议发送帧都动态创建一个 <code>io_can_chan_write</code>：</p><ul><li>需要额外分配或管理 operation 对象；</li><li>每个对象都要保证 <code>msg</code> 指针生命周期；</li><li>取消和确认超时管理更复杂；</li><li>协议层突发发送会制造大量 task；</li><li>很难统一实现固定长度的丢帧策略。</li></ul><p>Lely 选择：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">固定长度帧 ring</span><br><span class="line">    +</span><br><span class="line">单个可复用 write operation</span><br></pre></td></tr></table></figure><p>从而减少对象数量并明确所有权。</p><hr><h2 id="12-TX-与-RX-的背压模型本来就不同"><a href="#12-TX-与-RX-的背压模型本来就不同" class="headerlink" title="12. TX 与 RX 的背压模型本来就不同"></a>12. TX 与 RX 的背压模型本来就不同</h2><h3 id="12-1-发送方向可以拒绝上层"><a href="#12-1-发送方向可以拒绝上层" class="headerlink" title="12.1 发送方向可以拒绝上层"></a>12.1 发送方向可以拒绝上层</h3><p>当 <code>tx_ring</code> 已满时，<code>io_can_net_send_func()</code> 可以：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">返回 ERRNUM_AGAIN</span><br><span class="line">记录 dropped frame</span><br><span class="line">通知 on_queue_error</span><br></pre></td></tr></table></figure><p>也就是说，发送方向能够把压力反馈给帧生产者。</p><h3 id="12-2-接收方向不能要求总线暂停"><a href="#12-2-接收方向不能要求总线暂停" class="headerlink" title="12.2 接收方向不能要求总线暂停"></a>12.2 接收方向不能要求总线暂停</h3><p>当远端节点已经在 CAN 总线上发送帧后，本机不能通过软件调用告诉它：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">请暂停，等我的 executor 空闲后再发</span><br></pre></td></tr></table></figure><p>接收侧只能：</p><ul><li>尽快从控制器或内核队列取走；</li><li>缓存；</li><li>或在容量不足时丢弃。</li></ul><p>因此 RX 缓冲必须靠近 I&#x2F;O 入口。</p><h3 id="12-3-Linux-内核本身已有发送队列"><a href="#12-3-Linux-内核本身已有发送队列" class="headerlink" title="12.3 Linux 内核本身已有发送队列"></a>12.3 Linux 内核本身已有发送队列</h3><p>SocketCAN 写入后，内核和 CAN 控制器还有自己的发送队列。</p><p>底层 channel 再增加一个通用“帧副本 TX ring”会造成多级不可见排队：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">io_can_net.tx_ring</span><br><span class="line">    ↓</span><br><span class="line">channel frame ring</span><br><span class="line">    ↓</span><br><span class="line">SocketCAN send queue</span><br><span class="line">    ↓</span><br><span class="line">CAN controller mailbox</span><br></pre></td></tr></table></figure><p>这样会使：</p><ul><li>实际排队深度难以计算；</li><li>发送延迟难以预测；</li><li>错误归属模糊；</li><li><code>txlen</code> 的意义失真。</li></ul><p>因此 channel 后端只管理写操作和系统 I&#x2F;O，不重复承担网络层发送缓存策略。</p><hr><h2 id="13-spscring-在两处的生产者和消费者"><a href="#13-spscring-在两处的生产者和消费者" class="headerlink" title="13. spscring 在两处的生产者和消费者"></a>13. <code>spscring</code> 在两处的生产者和消费者</h2><p><code>spscring</code> 是 single-producer &#x2F; single-consumer ring。</p><h3 id="13-1-io-can-net-tx-ring"><a href="#13-1-io-can-net-tx-ring" class="headerlink" title="13.1 io_can_net.tx_ring"></a>13.1 <code>io_can_net.tx_ring</code></h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">Producer：io_can_net_send_func()</span><br><span class="line">    p_alloc</span><br><span class="line">    写 tx_buf</span><br><span class="line">    p_commit</span><br><span class="line"></span><br><span class="line">Consumer：io_can_net_do_wait() / io_can_net_write_func()</span><br><span class="line">    c_alloc</span><br><span class="line">    复制到 write_msg</span><br><span class="line">    写完成后 c_commit</span><br></pre></td></tr></table></figure><p>当 ring 为空时，consumer 注册等待：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">spscring_c_submit_wait()</span><br></pre></td></tr></table></figure><p>producer 提交新帧后，ring 调用：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">io_can_net_c_wait_func()</span><br></pre></td></tr></table></figure><p>进而启动发送。</p><h3 id="13-2-Linux-can-chan-rxring"><a href="#13-2-Linux-can-chan-rxring" class="headerlink" title="13.2 Linux can_chan.rxring"></a>13.2 Linux <code>can_chan.rxring</code></h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">Producer：rxbuf_task</span><br><span class="line">    从 SocketCAN read</span><br><span class="line">    写 rxbuf</span><br><span class="line">    p_commit</span><br><span class="line"></span><br><span class="line">Consumer：read_task 或同步 read</span><br><span class="line">    c_alloc</span><br><span class="line">    转换为 can_msg / can_err</span><br><span class="line">    c_commit</span><br></pre></td></tr></table></figure><p>当 ring 为空时，consumer 注册等待；producer 放入帧后唤醒 <code>read_task</code>。</p><h3 id="13-3-相同数据结构，不同层次语义"><a href="#13-3-相同数据结构，不同层次语义" class="headerlink" title="13.3 相同数据结构，不同层次语义"></a>13.3 相同数据结构，不同层次语义</h3><table><thead><tr><th>Ring</th><th>Producer</th><th>Consumer</th><th>业务语义</th></tr></thead><tbody><tr><td><code>tx_ring</code></td><td>协议发送适配回调</td><td>异步 write 状态机</td><td>待发送逻辑 CAN 帧</td></tr><tr><td><code>rxring</code></td><td>SocketCAN 读取任务</td><td>异步 read 状态机</td><td>已接收底层帧</td></tr></tbody></table><p>所以不能仅因为两者都使用 <code>spscring</code>，就认为它们应位于同一结构体中。</p><hr><h2 id="14-接收任务之间如何协作"><a href="#14-接收任务之间如何协作" class="headerlink" title="14. 接收任务之间如何协作"></a>14. 接收任务之间如何协作</h2><p>Linux channel 有三个与接收相关的对象：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">watch</span><br><span class="line">rxbuf_task</span><br><span class="line">read_task</span><br></pre></td></tr></table></figure><h3 id="14-1-watch"><a href="#14-1-watch" class="headerlink" title="14.1 watch"></a>14.1 <code>watch</code></h3><p><code>io_poll_watch</code> 只负责通知：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">fd 当前可读或可写</span><br></pre></td></tr></table></figure><p>它不执行完整协议处理。</p><p>收到 <code>IO_EVENT_IN</code> 后，watch callback 通常只投递 <code>rxbuf_task</code>。</p><h3 id="14-2-rxbuf-task"><a href="#14-2-rxbuf-task" class="headerlink" title="14.2 rxbuf_task"></a>14.2 <code>rxbuf_task</code></h3><p>主要职责：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">从 fd 读取</span><br><span class="line">识别普通帧或 MSG_CONFIRM</span><br><span class="line">普通帧写入 rxring</span><br><span class="line">确认帧完成 confirm_queue 中的写操作</span><br><span class="line">遇到 EAGAIN 时重新监听 IO_EVENT_IN</span><br></pre></td></tr></table></figure><h3 id="14-3-read-task"><a href="#14-3-read-task" class="headerlink" title="14.3 read_task"></a>14.3 <code>read_task</code></h3><p>主要职责：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">检查 read_queue</span><br><span class="line">检查 rxring</span><br><span class="line">把一帧复制给一个 read operation</span><br><span class="line">完成并投递该 operation 的 completion task</span><br><span class="line">ring 为空时注册 consumer wait</span><br></pre></td></tr></table></figure><h3 id="14-4-为什么拆成两个-task"><a href="#14-4-为什么拆成两个-task" class="headerlink" title="14.4 为什么拆成两个 task"></a>14.4 为什么拆成两个 task</h3><p>如果把 fd 读取、ring 管理、read operation 完成和协议回调全部塞进 poll callback：</p><ul><li>poll callback 执行时间不可控；</li><li>容易在 I&#x2F;O 监视上下文中执行大量协议逻辑；</li><li>难以处理阻塞与非阻塞模式；</li><li>难以统一 executor 的任务生命周期；</li><li>锁边界更复杂。</li></ul><p>拆分后，poll callback 只负责唤醒，executor 负责任务执行。</p><hr><h2 id="15-发送任务之间如何协作"><a href="#15-发送任务之间如何协作" class="headerlink" title="15. 发送任务之间如何协作"></a>15. 发送任务之间如何协作</h2><p><code>io_can_net_t</code> 与 Linux channel 各自维护一段发送状态机。</p><h3 id="15-1-io-can-net-t-发送状态机"><a href="#15-1-io-can-net-t-发送状态机" class="headerlink" title="15.1 io_can_net_t 发送状态机"></a>15.1 <code>io_can_net_t</code> 发送状态机</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">tx_ring empty</span><br><span class="line">    ↓</span><br><span class="line">注册 consumer wait</span><br><span class="line">    ↓</span><br><span class="line">新帧入队后被唤醒</span><br><span class="line">    ↓</span><br><span class="line">取一帧到 write_msg</span><br><span class="line">    ↓</span><br><span class="line">submit_write</span><br><span class="line">    ↓</span><br><span class="line">等待 completion</span><br><span class="line">    ↓</span><br><span class="line">释放 tx_ring 当前帧</span><br><span class="line">    ↓</span><br><span class="line">继续下一帧</span><br></pre></td></tr></table></figure><h3 id="15-2-Linux-channel-发送状态机"><a href="#15-2-Linux-channel-发送状态机" class="headerlink" title="15.2 Linux channel 发送状态机"></a>15.2 Linux channel 发送状态机</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">submit_write</span><br><span class="line">    ↓</span><br><span class="line">operation 进入 write_queue</span><br><span class="line">    ↓</span><br><span class="line">write_task 调用 SocketCAN write</span><br><span class="line">    ↓</span><br><span class="line">立即错误</span><br><span class="line">    或</span><br><span class="line">成功且无需确认</span><br><span class="line">    或</span><br><span class="line">进入 confirm_queue 等待 MSG_CONFIRM</span><br><span class="line">    ↓</span><br><span class="line">完成 operation</span><br></pre></td></tr></table></figure><h3 id="15-3-两层分别解决的问题"><a href="#15-3-两层分别解决的问题" class="headerlink" title="15.3 两层分别解决的问题"></a>15.3 两层分别解决的问题</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">io_can_net_t</span><br><span class="line">    解决“多帧如何排队并逐帧提交”</span><br><span class="line"></span><br><span class="line">Linux channel</span><br><span class="line">    解决“一个异步 write operation 如何在 fd 上完成”</span><br></pre></td></tr></table></figure><p>这就是 TX ring 不应下沉到 Linux channel 的核心原因。</p><hr><h2 id="16-与-io-tqueue-pheap-的完整连接"><a href="#16-与-io-tqueue-pheap-的完整连接" class="headerlink" title="16. 与 io_tqueue + pheap 的完整连接"></a>16. 与 <code>io_tqueue + pheap</code> 的完整连接</h2><p>发送、接收和 Timer 三条链路最终在 <code>io_can_net_t</code> 汇合。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    CT[&quot;CANopen can_timer_t objects&quot;] --&gt; CHP[&quot;can_net.timer_heap&quot;]</span><br><span class="line">    CHP --&gt; NF[&quot;can_net_set_next&quot;]</span><br><span class="line">    NF --&gt; NCB[&quot;io_can_net_next_func&quot;]</span><br><span class="line">    NCB --&gt; WN[&quot;wait_next&quot;]</span><br><span class="line">    WN --&gt; TQ[&quot;io_tqueue.queue&quot;]</span><br><span class="line">    TQ --&gt; ST[&quot;system timer&quot;]</span><br><span class="line">    ST --&gt; WNF[&quot;io_can_net_wait_next_func&quot;]</span><br><span class="line">    WNF --&gt; CST[&quot;can_net_set_time&quot;]</span><br><span class="line"></span><br><span class="line">    CO[&quot;CANopen send request&quot;] --&gt; CS[&quot;can_net_send&quot;]</span><br><span class="line">    CS --&gt; TX[&quot;io_can_net.tx_ring&quot;]</span><br><span class="line">    TX --&gt; CW[&quot;io_can_chan write&quot;]</span><br><span class="line"></span><br><span class="line">    CR[&quot;SocketCAN receive&quot;] --&gt; RX[&quot;can_chan.rxring&quot;]</span><br><span class="line">    RX --&gt; RR[&quot;io_can_net read completion&quot;]</span><br><span class="line">    RR --&gt; CNR[&quot;can_net_recv&quot;]</span><br></pre></td></tr></table></figure><p>因此整个系统可以分成三种适配：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">协议时间 → 系统 Timer</span><br><span class="line">协议发送 → 异步 write</span><br><span class="line">底层接收 → 协议 receiver</span><br></pre></td></tr></table></figure><p>这三种适配都由 <code>io_can_net_t</code> 组织，但只有平台相关接收缓冲位于 channel 后端。</p><hr><h2 id="参考源码"><a href="#参考源码" class="headerlink" title="参考源码"></a>参考源码</h2><h3 id="CAN-逻辑核心"><a href="#CAN-逻辑核心" class="headerlink" title="CAN 逻辑核心"></a>CAN 逻辑核心</h3><ul><li><code>lely-core/include/lely/can/net.h</code></li><li><code>lely-core/src/can/net.c</code></li></ul><h3 id="I-O-桥接层"><a href="#I-O-桥接层" class="headerlink" title="I&#x2F;O 桥接层"></a>I&#x2F;O 桥接层</h3><ul><li><code>lely-core/include/lely/io2/can_net.h</code></li><li><code>lely-core/include/lely/io2/can_net.hpp</code></li><li><code>lely-core/src/io2/can_net.c</code></li></ul><h3 id="CAN-channel-抽象与-Linux-后端"><a href="#CAN-channel-抽象与-Linux-后端" class="headerlink" title="CAN channel 抽象与 Linux 后端"></a>CAN channel 抽象与 Linux 后端</h3><ul><li><code>lely-core/include/lely/io2/can.h</code></li><li><code>lely-core/src/io2/linux/can_chan.c</code></li></ul><h3 id="数据结构"><a href="#数据结构" class="headerlink" title="数据结构"></a>数据结构</h3><ul><li><code>lely-core/include/lely/util/spscring.h</code></li><li><code>lely-core/include/lely/util/pheap.h</code></li><li><code>lely-core/src/util/pheap.c</code></li></ul><h3 id="前置学习笔记"><a href="#前置学习笔记" class="headerlink" title="前置学习笔记"></a>前置学习笔记</h3><ul><li><code>Lely_CANopen_io_tqueue_pheap_机制原理与运行流程(1).md</code></li></ul><h3 id="协议参考"><a href="#协议参考" class="headerlink" title="协议参考"></a>协议参考</h3><ul><li>CiA 301 V4.2.0：CANopen 应用层和通信协议</li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;meta name=&quot;referrer&quot; content=&quot;no-referrer&quot; /&gt;

&lt;h1 id=&quot;Lely-CANopen：can-net、io-can-net-与-CAN-channel-的分层关系，以及-TX-RX-Ring-的设计原理&quot;&gt;&lt;a href=&quot;#L</summary>
      
    
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/categories/canopen/"/>
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/tags/canopen/"/>
    
  </entry>
  
  <entry>
    <title>Lely CANopen：io_tqueue 与 pheap 的实现原理、运行流程</title>
    <link href="https://wdfk-prog.space/posts/ddeb74ca/"/>
    <id>https://wdfk-prog.space/posts/ddeb74ca/</id>
    <published>2026-07-29T05:36:09.000Z</published>
    <updated>2026-07-29T05:37:04.979Z</updated>
    
    <content type="html"><![CDATA[<meta name="referrer" content="no-referrer" /><h1 id="Lely-CANopen：io-tqueue-与-pheap-的实现原理、运行流程及其与-Timer、can-net-的关系"><a href="#Lely-CANopen：io-tqueue-与-pheap-的实现原理、运行流程及其与-Timer、can-net-的关系" class="headerlink" title="Lely CANopen：io_tqueue 与 pheap 的实现原理、运行流程及其与 Timer、can_net 的关系"></a>Lely CANopen：io_tqueue 与 pheap 的实现原理、运行流程及其与 Timer、can_net 的关系</h1><p><img src="https://i-blog.csdnimg.cn/direct/0ac2b4931c3e496f8e777ba72aa4013d.png" alt="在这里插入图片描述"></p><p>@[toc]</p><h2 id="一句话结论"><a href="#一句话结论" class="headerlink" title="一句话结论"></a>一句话结论</h2><p><code>io_tqueue</code> 是一个“多路逻辑定时器复用器”：</p><ul><li>上层可以同时提交很多个不同绝对截止时间的 <code>io_tqueue_wait</code>；</li><li>中间用 <code>pheap</code> 维护“当前最早到期”的等待；</li><li>下层始终只占用 <strong>一个</strong> <code>io_timer_t</code>；</li><li>底层 Timer 到期时，再回到 <code>io_tqueue</code> 批量完成所有 <code>value &lt;= now</code> 的等待。</li></ul><p>它与 <code>can_net</code> 串起来之后，又形成“两级最早截止时间压缩”：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">很多 can_timer_t</span><br><span class="line">    ↓</span><br><span class="line">can_net.timer_heap</span><br><span class="line">    ↓ 选出协议层最早一个</span><br><span class="line">io_tqueue.queue</span><br><span class="line">    ↓ 再与其他 I/O 等待一起排序</span><br><span class="line">一个 io_timer_t</span><br><span class="line">    ↓</span><br><span class="line">Linux timerfd / poll / executor</span><br></pre></td></tr></table></figure><hr><h2 id="1-为什么需要-io-tqueue"><a href="#1-为什么需要-io-tqueue" class="headerlink" title="1. 为什么需要 io_tqueue"></a>1. 为什么需要 <code>io_tqueue</code></h2><p>如果没有 <code>io_tqueue</code>，最直观的设计是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">每一个逻辑等待</span><br><span class="line">    → 一个 io_timer_t</span><br><span class="line">    → 一个系统层定时器（如 Linux timerfd）</span><br><span class="line">    → 一个异步等待对象</span><br></pre></td></tr></table></figure><p>但 CANopen 运行时会同时出现很多不同类型的超时和周期事件，例如：</p><ul><li>心跳生产 &#x2F; 消费；</li><li>SDO 超时；</li><li>PDO event timer；</li><li>SYNC 周期；</li><li>CAN 帧写确认超时；</li><li>其他用户层异步等待。</li></ul><p>如果每个都单独占用一个系统定时器，成本会很高。</p><p><code>io_tqueue</code> 的核心思想是：</p><blockquote><p>只维护“谁最早到期”，并把这个最早截止时间映射到底层唯一的 <code>io_timer_t</code> 上。</p></blockquote><p>因此，系统里可能有很多逻辑等待，但底层只有一个真正被设置到内核的 Timer。</p><hr><h2 id="2-四层对象模型"><a href="#2-四层对象模型" class="headerlink" title="2. 四层对象模型"></a>2. 四层对象模型</h2><p>从上到下可以分成四层：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">第 1 层：用户逻辑等待</span><br><span class="line">    struct io_tqueue_wait</span><br><span class="line"></span><br><span class="line">第 2 层：逻辑等待队列</span><br><span class="line">    struct io_tqueue</span><br><span class="line">    └── struct pheap queue</span><br><span class="line"></span><br><span class="line">第 3 层：底层唯一 Timer 等待</span><br><span class="line">    struct io_timer_wait wait</span><br><span class="line"></span><br><span class="line">第 4 层：真正系统 Timer</span><br><span class="line">    io_timer_t</span><br><span class="line">    └── Linux 后端通常落到 timerfd</span><br></pre></td></tr></table></figure><p>也可以画成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">很多 io_tqueue_wait</span><br><span class="line">        ↓</span><br><span class="line">io_tqueue.queue（pheap）</span><br><span class="line">        ↓ 只取堆顶最早截止时间</span><br><span class="line">一个 io_timer_t</span><br><span class="line">        ↓</span><br><span class="line">到期后触发 io_tqueue_wait_func()</span><br><span class="line">        ↓</span><br><span class="line">批量完成所有已过期等待</span><br></pre></td></tr></table></figure><hr><h2 id="3-io-tqueue-wait：一个逻辑截止时间"><a href="#3-io-tqueue-wait：一个逻辑截止时间" class="headerlink" title="3. io_tqueue_wait：一个逻辑截止时间"></a>3. <code>io_tqueue_wait</code>：一个逻辑截止时间</h2><p><code>io_tqueue_wait</code> 里最关键的成员有三个：</p><ul><li><code>value</code>：绝对到期时间；</li><li><code>task</code>：完成时投递到 executor 的任务；</li><li><code>_node</code>：嵌入在等待对象里的 <code>pnode</code>，用于把这个等待挂入 <code>pheap</code>。</li></ul><p>可以把它理解为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">一个等待对象 = 截止时间 + 完成回调 + 堆节点</span><br></pre></td></tr></table></figure><h3 id="3-1-为什么-node-直接嵌入-io-tqueue-wait"><a href="#3-1-为什么-node-直接嵌入-io-tqueue-wait" class="headerlink" title="3.1 为什么 _node 直接嵌入 io_tqueue_wait"></a>3.1 为什么 <code>_node</code> 直接嵌入 <code>io_tqueue_wait</code></h3><p>这是典型的 <strong>侵入式数据结构</strong> 设计。</p><p>好处：</p><ul><li>不需要为堆节点单独再分配内存；</li><li><code>pheap</code> 只负责维护“节点关系”，业务数据仍然在外部对象里；</li><li>从 <code>pnode</code> 可以通过 <code>structof()</code> 很快回到 <code>io_tqueue_wait</code>。</li></ul><h3 id="3-2-value-为什么不能在入堆后修改"><a href="#3-2-value-为什么不能在入堆后修改" class="headerlink" title="3.2 value 为什么不能在入堆后修改"></a>3.2 <code>value</code> 为什么不能在入堆后修改</h3><p>因为 <code>pnode.key</code> 指向的就是 <code>wait-&gt;value</code>。</p><p>一旦节点已经在堆里，如果你直接改了 <code>value</code>，堆序就可能被破坏。</p><p>所以正确做法只能是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">先 remove</span><br><span class="line">再修改时间</span><br><span class="line">再 insert</span><br></pre></td></tr></table></figure><hr><h2 id="4-pheap：为什么适合这里"><a href="#4-pheap：为什么适合这里" class="headerlink" title="4. pheap：为什么适合这里"></a>4. <code>pheap</code>：为什么适合这里</h2><p>Lely 这里使用的是 <strong>pairing heap（配对堆）</strong>。</p><h3 id="4-1-它维护什么性质"><a href="#4-1-它维护什么性质" class="headerlink" title="4.1 它维护什么性质"></a>4.1 它维护什么性质</h3><p>对最小堆来说，<code>pheap</code> 只保证：</p><ul><li><code>root</code> 是全局最小值；</li><li>每个父节点不大于自己的子节点。</li></ul><p><strong>它并不保证兄弟链表有序，也不保证整棵树线性有序。</strong></p><p>这点非常重要。可以画成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">      root = 10</span><br><span class="line">     /    |    \</span><br><span class="line">   20     30    25</span><br><span class="line">  /  \           |</span><br><span class="line">21   40         26</span><br></pre></td></tr></table></figure><p>这里只能保证：</p><ul><li><code>10</code> 是全局最小；</li><li><code>20 &gt;= 10</code>，<code>30 &gt;= 10</code>，<code>25 &gt;= 10</code>；</li><li><code>21 &gt;= 20</code>，<code>40 &gt;= 20</code>，<code>26 &gt;= 25</code>。</li></ul><p>但是：</p><ul><li>兄弟 <code>20 / 30 / 25</code> 没有顺序含义；</li><li>不能通过兄弟链顺序直接推断第二小、第三小；</li><li>“下一小是谁”不是简单看 <code>root-&gt;next</code> 或第一个 child。</li></ul><h3 id="4-2-为什么-pheap-first-能是-O-1"><a href="#4-2-为什么-pheap-first-能是-O-1" class="headerlink" title="4.2 为什么 pheap_first() 能是 O(1)"></a>4.2 为什么 <code>pheap_first()</code> 能是 <code>O(1)</code></h3><p>因为最小值总是堆根：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">struct</span> pnode *<span class="title function_">pheap_first</span><span class="params">(<span class="type">const</span> <span class="keyword">struct</span> pheap *heap)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">return</span> heap-&gt;root;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>所以“取当前最早截止时间”非常便宜，这正适合 <code>io_tqueue</code> 高频做的操作。</p><h3 id="4-3-为什么插入成本也低"><a href="#4-3-为什么插入成本也低" class="headerlink" title="4.3 为什么插入成本也低"></a>4.3 为什么插入成本也低</h3><p>新节点插入时，只需要和当前根比较：</p><ul><li>如果新节点更小，新节点成为新根，旧根挂成它的子节点；</li><li>否则把新节点挂到根的子节点链表。</li></ul><p>因此插入是 <code>O(1)</code>。</p><hr><h2 id="5-你最关心的问题：删除-root-后，下一个最小节点怎么产生？"><a href="#5-你最关心的问题：删除-root-后，下一个最小节点怎么产生？" class="headerlink" title="5. 你最关心的问题：删除 root 后，下一个最小节点怎么产生？"></a>5. 你最关心的问题：删除 root 后，下一个最小节点怎么产生？</h2><p>这是理解配对堆最关键的一步。</p><p>你的判断完全对：</p><ul><li><code>root</code> 是全局最小值；</li><li>其他节点只满足“父不大于子”；</li><li>根的孩子链表 <strong>不是</strong> 一个排好序的线性序列；</li><li>所以 root 删除后，不能靠“顺着兄弟链找下一个”来得到最小值。</li></ul><p>那答案是什么？</p><blockquote><p>删除 root 之后，不是“找一个现成排好序的第二名”；而是把 root 的各个子树重新做一次 <strong>meld &#x2F; merge（融合）</strong>，融合完成后，新根自然就是剩余节点中的全局最小值。</p></blockquote><h3 id="5-1-先看-pheap-remove-删除-root-时做了什么"><a href="#5-1-先看-pheap-remove-删除-root-时做了什么" class="headerlink" title="5.1 先看 pheap_remove() 删除 root 时做了什么"></a>5.1 先看 <code>pheap_remove()</code> 删除 root 时做了什么</h3><p>对 root 节点来说，逻辑可以概括成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">1. 把 root 从堆中摘掉</span><br><span class="line">2. 取出 root 的 child 链表</span><br><span class="line">3. 对这些 child 子树做配对融合（pnode_merge_pairs）</span><br><span class="line">4. 融合出来的一棵树，作为新的 root</span><br></pre></td></tr></table></figure><p>源码语义可以简化为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">if 删除的是 root:</span><br><span class="line">    heap-&gt;root = NULL</span><br><span class="line"></span><br><span class="line">child = root-&gt;child</span><br><span class="line">root-&gt;child = NULL</span><br><span class="line"></span><br><span class="line">if child 不为空:</span><br><span class="line">    child = pnode_merge_pairs(child, cmp)</span><br><span class="line">    heap-&gt;root = pnode_merge(heap-&gt;root, child, cmp)</span><br><span class="line">    heap-&gt;root-&gt;parent = NULL</span><br></pre></td></tr></table></figure><p>要点是：</p><ul><li>root 被删掉后，原来挂在 root 下面的每一个 child，实际上都代表“一棵仍然满足堆序的子树”；</li><li>每棵子树的根，都是它自己这棵子树里的最小值；</li><li>把这些“子树根”不断两两融合，最终得到一棵新树；</li><li>新树的根，就是整个剩余集合里的最小值。</li></ul><h3 id="5-2-为什么这样融合后就能得到新的全局最小值"><a href="#5-2-为什么这样融合后就能得到新的全局最小值" class="headerlink" title="5.2 为什么这样融合后就能得到新的全局最小值"></a>5.2 为什么这样融合后就能得到新的全局最小值</h3><p>假设删掉 root 后，剩余子树根分别是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">20, 21, 22, 25</span><br></pre></td></tr></table></figure><p>它们各自还带着自己的子树，但不用先扫描全部后代，因为：</p><ul><li><code>20</code> 已经是自己那棵子树里的最小值；</li><li><code>21</code> 也是自己那棵子树里的最小值；</li><li><code>22</code> 也是；</li><li><code>25</code> 也是。</li></ul><p>于是只要不断比较这些“子树根”即可。</p><p>例如配对融合的直觉过程可以理解成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">(20, 21) 先融合  →  得到一棵以 20 为根的树</span><br><span class="line">(22, 25) 先融合  →  得到一棵以 22 为根的树</span><br><span class="line">再把这两棵树融合 →  得到一棵以 20 为根的树</span><br></pre></td></tr></table></figure><p>最终新根就是 <code>20</code>，它就是删除原 root 后的全局最小值。</p><p>注意：</p><ul><li>这里的“20 是下一个最小值”并不是因为它恰好排在 child 链表第一个；</li><li>而是因为在一轮轮 <code>merge</code> 之后，它在所有剩余子树根中最小。</li></ul><h3 id="5-3-这不是“寻找第二小”，而是“重建堆顶”"><a href="#5-3-这不是“寻找第二小”，而是“重建堆顶”" class="headerlink" title="5.3 这不是“寻找第二小”，而是“重建堆顶”"></a>5.3 这不是“寻找第二小”，而是“重建堆顶”</h3><p>这是很容易想岔的一点。</p><p>很多人会自然地想成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">删除最小值(root)</span><br><span class="line">    → 找第二小</span><br></pre></td></tr></table></figure><p>但配对堆不是有序数组，也不是有序链表。</p><p>更准确的表述应该是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">删除 root</span><br><span class="line">    → 把 root 的子树集合重新融合</span><br><span class="line">    → 融合后的新根，就是新的全局最小值</span><br></pre></td></tr></table></figure><p>也就是说：</p><ul><li>算法并不直接维护“第二名指针”；</li><li>它维护的是“堆序结构”；</li><li>删除 root 后，通过合并剩余子树恢复一个新的最小堆。</li></ul><p>这就是为什么 <code>pheap_remove()</code> 的代价要比 <code>pheap_first()</code> 和 <code>pheap_insert()</code> 高。</p><hr><h2 id="6-pheap-remove-root-运行流程图"><a href="#6-pheap-remove-root-运行流程图" class="headerlink" title="6. pheap_remove(root) 运行流程图"></a>6. <code>pheap_remove(root)</code> 运行流程图</h2><p>下面专门用流程图展示“root 到期后被删除，下一个最小节点如何上来”。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[&quot;当前堆的 root 是全局最小值&quot;] --&gt; B[&quot;删除 root&quot;]</span><br><span class="line">    B --&gt; C[&quot;取出 root 的所有 child 子树&quot;]</span><br><span class="line">    C --&gt; D&#123;&quot;child 是否为空&quot;&#125;</span><br><span class="line">    D -- 是 --&gt; E[&quot;heap.root 置空，堆变空&quot;]</span><br><span class="line">    D -- 否 --&gt; F[&quot;把这些 child 看作多个小堆&quot;]</span><br><span class="line">    F --&gt; G[&quot;调用 pnode_merge_pairs，沿 sibling 链逐步融合&quot;]</span><br><span class="line">    G --&gt; H[&quot;不断比较两棵子树的根&quot;]</span><br><span class="line">    H --&gt; I[&quot;较小根保留为父，较大根挂为 child&quot;]</span><br><span class="line">    I --&gt; J[&quot;逐步融合成一棵新树&quot;]</span><br><span class="line">    J --&gt; K[&quot;新树的根成为新的 heap.root&quot;]</span><br><span class="line">    K --&gt; L[&quot;新的 root 就是剩余节点中的全局最小值&quot;]</span><br></pre></td></tr></table></figure><p>再补一张“不是线性有序”的示意图：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    R[&quot;old root = 10&quot;] --&gt; C1[&quot;20&quot;]</span><br><span class="line">    R --&gt; C2[&quot;30&quot;]</span><br><span class="line">    R --&gt; C3[&quot;25&quot;]</span><br><span class="line">    C1 --&gt; C11[&quot;21&quot;]</span><br><span class="line">    C1 --&gt; C12[&quot;40&quot;]</span><br><span class="line">    C3 --&gt; C31[&quot;26&quot;]</span><br><span class="line"></span><br><span class="line">    X[&quot;删除 10 后&quot;] --&gt; Y[&quot;不能顺着兄弟顺序直接找第二小&quot;]</span><br><span class="line">    Y --&gt; Z[&quot;需要把 20、30、25 这些子树根重新融合&quot;]</span><br><span class="line">    Z --&gt; N[&quot;new root = min of &#123;20, 30, 25&#125;&quot;]</span><br></pre></td></tr></table></figure><h3 id="6-1-pnode-merge-pairs-的实际融合顺序"><a href="#6-1-pnode-merge-pairs-的实际融合顺序" class="headerlink" title="6.1 pnode_merge_pairs() 的实际融合顺序"></a>6.1 <code>pnode_merge_pairs()</code> 的实际融合顺序</h3><p>需要特别注意：当前 Lely 源码中的 <code>pnode_merge_pairs()</code> 并不是先把所有兄弟节点分成独立的两两分组，再执行第二轮反向合并。它沿着 sibling 链从左向右执行连续融合：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">while</span> (node-&gt;next) &#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">pnode</span> *<span class="title">next</span> =</span> node-&gt;next-&gt;next;</span><br><span class="line">    node-&gt;next-&gt;next = <span class="literal">NULL</span>;</span><br><span class="line">    node = pnode_merge(node, node-&gt;next, cmp);</span><br><span class="line">    node-&gt;next = next;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>假设删除旧 root 后得到的子树根链为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">20 -&gt; 30 -&gt; 25 -&gt; 40</span><br></pre></td></tr></table></figure><p>实际执行过程如下：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    A[&quot;初始 sibling 链&lt;br/&gt;20 → 30 → 25 → 40&quot;] --&gt; B[&quot;第 1 轮&lt;br/&gt;merge 20 与 30&lt;br/&gt;融合根为 20&quot;]</span><br><span class="line">    B --&gt; C[&quot;剩余处理链&lt;br/&gt;20 → 25 → 40&quot;]</span><br><span class="line">    C --&gt; D[&quot;第 2 轮&lt;br/&gt;merge 20 与 25&lt;br/&gt;融合根为 20&quot;]</span><br><span class="line">    D --&gt; E[&quot;剩余处理链&lt;br/&gt;20 → 40&quot;]</span><br><span class="line">    E --&gt; F[&quot;第 3 轮&lt;br/&gt;merge 20 与 40&lt;br/&gt;融合根为 20&quot;]</span><br><span class="line">    F --&gt; G[&quot;返回节点 20&lt;br/&gt;成为新的 heap.root&quot;]</span><br></pre></td></tr></table></figure><p>每轮 <code>pnode_merge()</code> 都会比较两棵子树的根：</p><ul><li>较小的根继续作为父节点；</li><li>较大的根挂到较小根的 child 链；</li><li>融合结果仍然是一棵满足最小堆序的子树。</li></ul><p>因此，在处理到第 <code>k</code> 个子树根后，变量 <code>node</code> 始终是“目前已经处理过的所有子树中的最小根”。当 sibling 链全部处理完成时，返回的 <code>node</code> 自然就是剩余全部节点中的全局最小值。</p><p>这个过程的关键不是维护一条完整排序链，而是持续维护下面这个不变量：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">每完成一轮 merge：</span><br><span class="line">当前融合结果的 root = 已处理子树根中的最小值</span><br></pre></td></tr></table></figure><hr><h2 id="7-io-tqueue-的内部状态"><a href="#7-io-tqueue-的内部状态" class="headerlink" title="7. io_tqueue 的内部状态"></a>7. <code>io_tqueue</code> 的内部状态</h2><p><code>io_tqueue</code> 的关键成员可以概括成：</p><ul><li><code>timer</code>：底层复用的唯一 <code>io_timer_t</code>；</li><li><code>exec</code>：默认 executor；</li><li><code>wait</code>：唯一的底层 <code>io_timer_wait</code>；</li><li><code>submitted</code>：当前是否已经向底层提交了这个唯一 wait；</li><li><code>next</code>：当前底层 Timer 被设置到的绝对截止时间；</li><li><code>queue</code>：存放所有逻辑等待的 <code>pheap</code>；</li><li><code>shutdown</code>：是否关闭。</li></ul><p>它的核心状态关系是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">queue 里可以有很多逻辑等待</span><br><span class="line">但底层 wait 永远最多只有一个 submitted</span><br><span class="line">next 永远代表“当前底层 Timer 目标截止时间”</span><br></pre></td></tr></table></figure><hr><h2 id="8-初始化流程：io-tqueue-init"><a href="#8-初始化流程：io-tqueue-init" class="headerlink" title="8. 初始化流程：io_tqueue_init()"></a>8. 初始化流程：<code>io_tqueue_init()</code></h2><p>初始化主要做了几件事：</p><ol><li>保存 <code>timer</code>；</li><li>选择默认 <code>exec</code>；</li><li>初始化唯一底层 <code>io_timer_wait</code>，回调设为 <code>io_tqueue_wait_func()</code>；</li><li>初始化互斥锁（若启用线程）；</li><li><code>submitted = 0</code>，<code>next = {0,0}</code>；</li><li><code>pheap_init(&amp;tq-&gt;queue, &amp;timespec_cmp)</code>；</li><li>把 <code>io_tqueue</code> 注册到 <code>io_ctx</code>。</li></ol><h3 id="8-1-与-Timer-的约束关系"><a href="#8-1-与-Timer-的约束关系" class="headerlink" title="8.1 与 Timer 的约束关系"></a>8.1 与 Timer 的约束关系</h3><p><code>io_tqueue.h</code> 明确要求：</p><blockquote><p><code>io_tqueue</code> 生命周期内，外部不要再直接对这个 <code>timer</code> 调用 <code>io_timer_settime()</code>。</p></blockquote><p>因为 <code>io_tqueue</code> 自己要独占控制这个 Timer 的截止时间。</p><p>否则就会破坏：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">堆顶截止时间  &lt;-&gt;  底层 Timer 当前设置值</span><br></pre></td></tr></table></figure><p>这个关键对应关系。</p><hr><h2 id="9-提交等待：io-tqueue-submit-wait"><a href="#9-提交等待：io-tqueue-submit-wait" class="headerlink" title="9. 提交等待：io_tqueue_submit_wait()"></a>9. 提交等待：<code>io_tqueue_submit_wait()</code></h2><p>这是整个机制最重要的入口。</p><h3 id="9-1-提交时做什么"><a href="#9-1-提交时做什么" class="headerlink" title="9.1 提交时做什么"></a>9.1 提交时做什么</h3><p>当一个新的 <code>io_tqueue_wait</code> 被提交时，流程大致是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">1. 若 wait.task.exec 为空，则使用 tq-&gt;exec</span><br><span class="line">2. 初始化 executor 对 task 的生命周期跟踪</span><br><span class="line">3. 若 tq 已 shutdown，则立即按 canceled 完成</span><br><span class="line">4. 否则把 wait-&gt;_node 插入 tq-&gt;queue</span><br><span class="line">5. 如果它比 tq-&gt;next 更早：重设底层 Timer</span><br><span class="line">6. 如果当前还没有 submitted 的底层 wait：提交 tq-&gt;wait</span><br></pre></td></tr></table></figure><p>可以压缩成一张图：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[&quot;提交一个 io_tqueue_wait&quot;] --&gt; B&#123;&quot;task.exec 是否为空&quot;&#125;</span><br><span class="line">    B -- 是 --&gt; C[&quot;使用 tq-&gt;exec&quot;]</span><br><span class="line">    B -- 否 --&gt; D[&quot;保留用户指定的 exec&quot;]</span><br><span class="line">    C --&gt; E</span><br><span class="line">    D --&gt; E</span><br><span class="line">    E&#123;&quot;tq 是否 shutdown&quot;&#125;</span><br><span class="line">    E -- 是 --&gt; F[&quot;直接按 canceled 完成&quot;]</span><br><span class="line">    E -- 否 --&gt; G[&quot;初始化 wait 节点，并把 key 指向 wait.value&quot;]</span><br><span class="line">    G --&gt; H[&quot;把 wait 插入 tq.queue&quot;]</span><br><span class="line">    H --&gt; I&#123;&quot;wait.value 是否早于 tq.next&lt;br/&gt;或 tq.next 还未设置&quot;&#125;</span><br><span class="line">    I -- 是 --&gt; J[&quot;把底层 Timer 重设到 wait.value&quot;]</span><br><span class="line">    I -- 否 --&gt; K[&quot;底层 Timer 保持不变&quot;]</span><br><span class="line">    J --&gt; L</span><br><span class="line">    K --&gt; L</span><br><span class="line">    L&#123;&quot;当前是否还没有提交底层 tq.wait&quot;&#125;</span><br><span class="line">    L -- 是 --&gt; M[&quot;提交唯一的底层 io_timer_wait&quot;]</span><br><span class="line">    L -- 否 --&gt; N[&quot;结束&quot;]</span><br><span class="line">    M --&gt; N</span><br></pre></td></tr></table></figure><h3 id="9-2-为什么不是每次提交都要重新设置-Timer"><a href="#9-2-为什么不是每次提交都要重新设置-Timer" class="headerlink" title="9.2 为什么不是每次提交都要重新设置 Timer"></a>9.2 为什么不是每次提交都要重新设置 Timer</h3><p>因为 <code>io_tqueue</code> 只关心 <strong>最早截止时间</strong>。</p><p>例如当前：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">tq.next = 100ms</span><br></pre></td></tr></table></figure><p>新来一个等待：</p><ul><li>若到期时间是 <code>150ms</code>：不会影响当前最早者，不必重设底层 Timer；</li><li>若到期时间是 <code>80ms</code>：它变成新堆顶，需要立即把底层 Timer 改到 <code>80ms</code>。</li></ul><p>所以条件是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">新等待是否早于当前 tq.next</span><br></pre></td></tr></table></figure><p>而不是“是否新插入了一个节点”。</p><hr><h2 id="10-Timer-到期：io-tqueue-wait-func"><a href="#10-Timer-到期：io-tqueue-wait-func" class="headerlink" title="10. Timer 到期：io_tqueue_wait_func()"></a>10. Timer 到期：<code>io_tqueue_wait_func()</code></h2><p>底层唯一 <code>io_timer_wait</code> 到期后，会执行 <code>io_tqueue_wait_func()</code>。</p><p>这个函数做的事可以概括为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">1. 读取当前时间 now</span><br><span class="line">2. 清掉 submitted 标记，并清空 tq-&gt;next</span><br><span class="line">3. 从堆顶开始，反复弹出所有 value &lt;= now 的等待</span><br><span class="line">4. 如果堆还不空，把底层 Timer 设到新的最早截止时间</span><br><span class="line">5. 如有需要，再次提交唯一底层 wait</span><br><span class="line">6. 把本轮到期等待逐个 post 给 executor</span><br></pre></td></tr></table></figure><h3 id="10-1-为什么一次会弹出多个等待"><a href="#10-1-为什么一次会弹出多个等待" class="headerlink" title="10.1 为什么一次会弹出多个等待"></a>10.1 为什么一次会弹出多个等待</h3><p>因为底层 Timer 唤醒时，可能已经有多个逻辑等待同时到期了。</p><p>例如堆里有：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">100ms, 100ms, 102ms, 120ms</span><br></pre></td></tr></table></figure><p>当前 <code>now = 105ms</code>，那本轮就要一次性完成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">100ms, 100ms, 102ms</span><br></pre></td></tr></table></figure><p>只剩下 <code>120ms</code> 重新编程到底层 Timer。</p><h3 id="10-2-io-tqueue-wait-func-主流程图"><a href="#10-2-io-tqueue-wait-func-主流程图" class="headerlink" title="10.2 io_tqueue_wait_func() 主流程图"></a>10.2 <code>io_tqueue_wait_func()</code> 主流程图</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[&quot;底层 io_timer_wait 到期&quot;] --&gt; B[&quot;读取当前时间 now&quot;]</span><br><span class="line">    B --&gt; C[&quot;submitted 清零；next 清零&quot;]</span><br><span class="line">    C --&gt; D[&quot;查看当前堆顶&quot;]</span><br><span class="line">    D --&gt; E&#123;&quot;堆顶 wait.value 是否小于等于 now&quot;&#125;</span><br><span class="line">    E -- 是 --&gt; F[&quot;删除堆顶节点&quot;]</span><br><span class="line">    F --&gt; G[&quot;把对应 wait 放入完成队列&quot;]</span><br><span class="line">    G --&gt; D</span><br><span class="line">    E -- 否 --&gt; H&#123;&quot;堆是否为空&quot;&#125;</span><br><span class="line">    H -- 是 --&gt; I[&quot;本轮不再设置 Timer&quot;]</span><br><span class="line">    H -- 否 --&gt; J[&quot;取新的堆顶作为 tq.next&quot;]</span><br><span class="line">    J --&gt; K[&quot;把底层 Timer 设到 tq.next&quot;]</span><br><span class="line">    K --&gt; L[&quot;重新提交唯一底层 io_timer_wait&quot;]</span><br><span class="line">    I --&gt; M[&quot;锁外 post 所有到期任务&quot;]</span><br><span class="line">    L --&gt; M</span><br><span class="line">    M --&gt; N[&quot;结束&quot;]</span><br></pre></td></tr></table></figure><h3 id="10-3-这时-pheap-remove-root-正好派上用场"><a href="#10-3-这时-pheap-remove-root-正好派上用场" class="headerlink" title="10.3 这时 pheap_remove(root) 正好派上用场"></a>10.3 这时 <code>pheap_remove(root)</code> 正好派上用场</h3><p>注意这里的弹出动作不是别的，正是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">取堆顶 → 删除 root → 新 root 自动成为剩余最小值</span><br></pre></td></tr></table></figure><p>因此 <code>io_tqueue_wait_func()</code> 之所以能写成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">while ((node = pheap_first(&amp;tq-&gt;queue))) &#123;</span><br><span class="line">    if (now &lt; wait-&gt;value) break;</span><br><span class="line">    pheap_remove(&amp;tq-&gt;queue, node);</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>就是因为每次删掉当前 root 之后，配对堆已经帮它把“下一个最小节点”重建成新的 root 了。</p><p>这正好呼应了你前面的问题。</p><hr><h2 id="11-取消、abort-与-shutdown"><a href="#11-取消、abort-与-shutdown" class="headerlink" title="11. 取消、abort 与 shutdown"></a>11. 取消、abort 与 shutdown</h2><h3 id="11-1-cancel"><a href="#11-1-cancel" class="headerlink" title="11.1 cancel"></a>11.1 cancel</h3><p><code>cancel</code> 的语义是：</p><ul><li>如果等待还在队列里，就把它从堆里摘掉；</li><li>然后以 <code>ERRNUM_CANCELED</code> 作为完成结果，正常投递它的 completion task。</li></ul><h3 id="11-2-abort"><a href="#11-2-abort" class="headerlink" title="11.2 abort"></a>11.2 abort</h3><p><code>abort</code> 的语义是：</p><ul><li>也会把等待从堆里摘掉；</li><li>但 <strong>不会</strong> 投递 completion task。</li></ul><h3 id="11-3-堆空时为何要取消底层-wait"><a href="#11-3-堆空时为何要取消底层-wait" class="headerlink" title="11.3 堆空时为何要取消底层 wait"></a>11.3 堆空时为何要取消底层 wait</h3><p>当从堆里移除节点后，如果发现：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">pheap_empty(&amp;tq-&gt;queue) &amp;&amp; tq-&gt;submitted</span><br></pre></td></tr></table></figure><p>那么说明：</p><ul><li>逻辑等待已经没有了；</li><li>但底层唯一 <code>io_timer_wait</code> 可能还在挂着。</li></ul><p>这时会调用：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">io_timer_cancel_wait(tq-&gt;timer, &amp;tq-&gt;wait)</span><br></pre></td></tr></table></figure><p>避免底层继续无意义等待。</p><h3 id="11-4-shutdown"><a href="#11-4-shutdown" class="headerlink" title="11.4 shutdown"></a>11.4 shutdown</h3><p>关闭时会：</p><ol><li>标记 <code>shutdown = 1</code>；</li><li>若底层 wait 已提交，则尝试 <code>io_timer_abort_wait()</code>；</li><li>然后把所有逻辑等待统一 cancel 掉。</li></ol><hr><h2 id="12-io-tqueue-与-io-timer-t-的关系"><a href="#12-io-tqueue-与-io-timer-t-的关系" class="headerlink" title="12. io_tqueue 与 io_timer_t 的关系"></a>12. <code>io_tqueue</code> 与 <code>io_timer_t</code> 的关系</h2><p>二者不是替代关系，而是上下层关系。</p><h3 id="12-1-io-timer-t-负责什么"><a href="#12-1-io-timer-t-负责什么" class="headerlink" title="12.1 io_timer_t 负责什么"></a>12.1 <code>io_timer_t</code> 负责什么</h3><p><code>io_timer_t</code> 负责：</p><ul><li>持有具体系统 Timer 资源；</li><li>提供 <code>io_timer_settime()</code>；</li><li>提供 <code>io_timer_submit_wait()</code> &#x2F; <code>cancel_wait()</code> &#x2F; <code>abort_wait()</code>；</li><li>在 Linux 后端通常进一步映射到 <code>timerfd</code> + <code>io_poll</code>。</li></ul><h3 id="12-2-io-tqueue-负责什么"><a href="#12-2-io-tqueue-负责什么" class="headerlink" title="12.2 io_tqueue 负责什么"></a>12.2 <code>io_tqueue</code> 负责什么</h3><p><code>io_tqueue</code> 负责：</p><ul><li>把很多逻辑等待统一排序；</li><li>始终找出当前最早截止时间；</li><li>控制底层 Timer 应该被设成什么绝对时间；</li><li>底层到期后批量回灌上层任务。</li></ul><p>所以职责边界可以写成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">io_tqueue = 逻辑调度器</span><br><span class="line">io_timer_t = 单个真实 Timer 的异步封装</span><br></pre></td></tr></table></figure><h3 id="12-3-为什么这里用绝对时间"><a href="#12-3-为什么这里用绝对时间" class="headerlink" title="12.3 为什么这里用绝对时间"></a>12.3 为什么这里用绝对时间</h3><p>Lely 这里普遍采用绝对截止时间，而不是“从现在开始延时 N ms”。</p><p>好处是：</p><ul><li>避免连续重设时的累计漂移；</li><li>重新编程时只需维护“最早绝对 deadline”；</li><li>和 <code>timespec_cmp()</code> 及堆排序天然配合。</li></ul><hr><h2 id="13-io-tqueue-与-can-net-的关系"><a href="#13-io-tqueue-与-can-net-的关系" class="headerlink" title="13. io_tqueue 与 can_net 的关系"></a>13. <code>io_tqueue</code> 与 <code>can_net</code> 的关系</h2><p>这里是 CANopen 学习里非常关键的一环。</p><h3 id="13-1-can-net-自己也有一个-pheap"><a href="#13-1-can-net-自己也有一个-pheap" class="headerlink" title="13.1 can_net 自己也有一个 pheap"></a>13.1 <code>can_net</code> 自己也有一个 <code>pheap</code></h3><p><code>can_net</code> 内部也维护一个协议层定时器堆：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">can_net.timer_heap</span><br></pre></td></tr></table></figure><p>其中放的是很多 <code>can_timer_t</code>，例如：</p><ul><li>协议超时；</li><li>周期发送；</li><li>其他网络状态机的时间事件。</li></ul><p>它的职责是：</p><blockquote><p>在“协议层很多定时器”中，选出最早的一个。</p></blockquote><h3 id="13-2-io-can-net-会创建-io-tqueue"><a href="#13-2-io-can-net-会创建-io-tqueue" class="headerlink" title="13.2 io_can_net 会创建 io_tqueue"></a>13.2 <code>io_can_net</code> 会创建 <code>io_tqueue</code></h3><p>I&#x2F;O 桥接层 <code>io_can_net</code> 会创建一个 <code>io_tqueue</code>，并至少使用它来管理：</p><ul><li><code>wait_next</code>：等待下一个协议层截止时间；</li><li><code>wait_confirm</code>：等待 CAN 帧写确认相关超时。</li></ul><p>于是关系变成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">协议层多个 can_timer_t</span><br><span class="line">    ↓</span><br><span class="line">can_net.timer_heap</span><br><span class="line">    ↓</span><br><span class="line">协议层最早截止时间</span><br><span class="line">    ↓</span><br><span class="line">io_tqueue.queue</span><br><span class="line">    ↓ 再与其他 I/O 等待合并排序</span><br><span class="line">一个 io_timer_t</span><br></pre></td></tr></table></figure><h3 id="13-3-can-net-next-怎样进入-io-tqueue"><a href="#13-3-can-net-next-怎样进入-io-tqueue" class="headerlink" title="13.3 can_net.next 怎样进入 io_tqueue"></a>13.3 <code>can_net.next</code> 怎样进入 <code>io_tqueue</code></h3><p><code>can_net</code> 会根据自己堆顶 <code>can_timer_t</code> 的开始时间，更新：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">net-&gt;next</span><br></pre></td></tr></table></figure><p>随后 <code>io_can_net</code> 这一桥接层会把这个时间包装进 <code>wait_next.value</code>，再提交到 <code>io_tqueue</code>。</p><p>因此：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">can_net 只负责决定“协议层下一次该何时唤醒”</span><br><span class="line">io_tqueue 负责把这个唤醒点和其他 I/O 定时等待一起复用到底层 Timer</span><br></pre></td></tr></table></figure><h3 id="13-4-到期后如何回灌给-can-net"><a href="#13-4-到期后如何回灌给-can-net" class="headerlink" title="13.4 到期后如何回灌给 can_net"></a>13.4 到期后如何回灌给 <code>can_net</code></h3><p>底层 Timer 到期以后：</p><ol><li><code>io_tqueue_wait_func()</code> 发现 <code>wait_next</code> 已到期；</li><li><code>wait_next.task</code> 被投递；</li><li>对应的回调里再触发 <code>can_net</code> 继续处理协议层时间推进；</li><li><code>can_net</code> 可能重新算出新的 <code>next</code>；</li><li>再次经由 <code>io_tqueue_submit_wait()</code> 进入队列。</li></ol><p>这就形成了一个闭环。</p><hr><h2 id="14-两级堆的准确含义"><a href="#14-两级堆的准确含义" class="headerlink" title="14. 两级堆的准确含义"></a>14. 两级堆的准确含义</h2><p>很容易把 <code>can_net.timer_heap</code> 和 <code>io_tqueue.queue</code> 混成一个东西，但其实它们分层非常明确。</p><h3 id="14-1-第一层：can-net-timer-heap"><a href="#14-1-第一层：can-net-timer-heap" class="headerlink" title="14.1 第一层：can_net.timer_heap"></a>14.1 第一层：<code>can_net.timer_heap</code></h3><p>作用：</p><ul><li>管协议层事件；</li><li>元素是 <code>can_timer_t</code>；</li><li>决定“CANopen 协议下一次什么时候要跑”。</li></ul><h3 id="14-2-第二层：io-tqueue-queue"><a href="#14-2-第二层：io-tqueue-queue" class="headerlink" title="14.2 第二层：io_tqueue.queue"></a>14.2 第二层：<code>io_tqueue.queue</code></h3><p>作用：</p><ul><li>管 I&#x2F;O 层逻辑等待；</li><li>元素是 <code>io_tqueue_wait</code>；</li><li>决定“底层唯一 Timer 下次什么时候真正要唤醒”。</li></ul><h3 id="14-3-为什么不能直接只有一层"><a href="#14-3-为什么不能直接只有一层" class="headerlink" title="14.3 为什么不能直接只有一层"></a>14.3 为什么不能直接只有一层</h3><p>因为这两层处理的是不同抽象：</p><ul><li><code>can_net.timer_heap</code> 关心的是协议状态机；</li><li><code>io_tqueue.queue</code> 关心的是异步 I&#x2F;O 等待复用。</li></ul><p>所以可以把它们理解成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">第一层：协议事件调度</span><br><span class="line">第二层：I/O 定时复用</span><br></pre></td></tr></table></figure><hr><h2 id="15-最终心智模型"><a href="#15-最终心智模型" class="headerlink" title="15. 最终心智模型"></a>15. 最终心智模型</h2><p>把整套机制压缩成一句最容易记忆的话：</p><blockquote><p><code>pheap</code> 负责让“当前最早截止时间”始终出现在 root；当 root 被删除时，通过重新融合 root 的各个子树，把新的全局最小值重建成新的 root；<code>io_tqueue</code> 就利用这个特性，把很多逻辑等待压缩到底层唯一的 <code>io_timer_t</code> 上；而 <code>can_net</code> 又在它之上先做了一层协议定时器筛选。</p></blockquote><p>最后给一个完整总图：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[很多 can_timer_t] --&gt; B[can_net.timer_heap]</span><br><span class="line">    B --&gt; C[选出协议层最早截止时间]</span><br><span class="line">    C --&gt; D[封装为 wait_next]</span><br><span class="line">    D --&gt; E[io_tqueue.queue]</span><br><span class="line">    F[wait_confirm / 其他逻辑等待] --&gt; E</span><br><span class="line">    E --&gt; G[pheap_first = 当前全局最早等待]</span><br><span class="line">    G --&gt; H[io_timer_settime 到 tq.next]</span><br><span class="line">    H --&gt; I[唯一 io_timer_wait 已提交]</span><br><span class="line">    I --&gt; J[底层 Timer 到期]</span><br><span class="line">    J --&gt; K[io_tqueue_wait_func]</span><br><span class="line">    K --&gt; L[循环取堆顶并删除所有已过期 root]</span><br><span class="line">    L --&gt; M[每删一次 root 都通过 merge 重建新 root]</span><br><span class="line">    M --&gt; N[剩余堆顶重新设置到底层 Timer]</span><br><span class="line">    N --&gt; O[到期任务投递到 executor]</span><br><span class="line">    O --&gt; P[协议层 / I/O 层继续运行]</span><br></pre></td></tr></table></figure><hr><h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><h3 id="Lely-官方源码"><a href="#Lely-官方源码" class="headerlink" title="Lely 官方源码"></a>Lely 官方源码</h3><ul><li><code>lely-core/include/lely/io2/tqueue.h</code></li><li><code>lely-core/src/io2/tqueue.c</code></li><li><code>lely-core/include/lely/util/pheap.h</code></li><li><code>lely-core/src/util/pheap.c</code></li><li><code>lely-core/src/io2/can_net.c</code></li><li><code>lely-core/src/can/net.c</code></li></ul><h3 id="前置学习笔记"><a href="#前置学习笔记" class="headerlink" title="前置学习笔记"></a>前置学习笔记</h3><ul><li><code>Lely CANopen Timer 的实现原理与运行流程.md</code></li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;meta name=&quot;referrer&quot; content=&quot;no-referrer&quot; /&gt;

&lt;h1 id=&quot;Lely-CANopen：io-tqueue-与-pheap-的实现原理、运行流程及其与-Timer、can-net-的关系&quot;&gt;&lt;a href=&quot;#Lely-CANop</summary>
      
    
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/categories/canopen/"/>
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/tags/canopen/"/>
    
  </entry>
  
  <entry>
    <title>Lely CANopen `spscring` 单生产者单消费者环形队列完整机制学习笔记</title>
    <link href="https://wdfk-prog.space/posts/1510f72a/"/>
    <id>https://wdfk-prog.space/posts/1510f72a/</id>
    <published>2026-07-29T02:09:09.000Z</published>
    <updated>2026-07-29T02:10:01.371Z</updated>
    
    <content type="html"><![CDATA[<meta name="referrer" content="no-referrer" /><h1 id="Lely-CANopen-spscring-单生产者单消费者环形队列完整机制学习笔记"><a href="#Lely-CANopen-spscring-单生产者单消费者环形队列完整机制学习笔记" class="headerlink" title="Lely CANopen spscring 单生产者单消费者环形队列完整机制学习笔记"></a>Lely CANopen <code>spscring</code> 单生产者单消费者环形队列完整机制学习笔记</h1><p><img src="https://i-blog.csdnimg.cn/direct/a5ca6b2293654a2b8d3e6a220ae84a2b.png" alt="在这里插入图片描述"></p><p>@[toc]</p><hr><h2 id="1-先明确边界：spscring-不是数据缓冲区"><a href="#1-先明确边界：spscring-不是数据缓冲区" class="headerlink" title="1. 先明确边界：spscring 不是数据缓冲区"></a>1. 先明确边界：<code>spscring</code> 不是数据缓冲区</h2><p><code>SPSC</code> 是 Single-Producer Single-Consumer，即单生产者、单消费者。</p><p>Lely 的 <code>spscring</code> 与常见“结构体内部直接包含数组”的环形队列不同。它只管理：</p><ul><li>哪些索引可以由生产者写入；</li><li>哪些索引可以由消费者读取；</li><li>生产者何时把写入结果发布给消费者；</li><li>消费者何时把已读槽位归还给生产者；</li><li>队列由空变为可读或由满变为可写时，是否需要触发回调。</li></ul><p>实际数据存储由调用者单独提供：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">spscring</span> <span class="title">ring</span>;</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">io_can_frame</span> *<span class="title">rxbuf</span>;</span></span><br></pre></td></tr></table></figure><p>两者的关系是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">spscring</span><br><span class="line">= 索引所有权与可见性控制器</span><br><span class="line"></span><br><span class="line">rxbuf</span><br><span class="line">= 真正保存 CAN 帧的内存数组</span><br></pre></td></tr></table></figure><p>因此，调用 <code>spscring_p_alloc()</code> 只会得到一个可写索引，不会返回数据指针，也不会复制任何数据。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    Prod[Producer] --&gt;|p_alloc obtains index| Ring[spscring index controller]</span><br><span class="line">    Prod --&gt;|writes payload| Buf[User data buffer]</span><br><span class="line">    Prod --&gt;|p_commit publishes index| Ring</span><br><span class="line">    Ring --&gt;|c_alloc obtains index| Cons[Consumer]</span><br><span class="line">    Buf --&gt;|reads payload| Cons</span><br><span class="line">    Cons --&gt;|c_commit releases index| Ring</span><br></pre></td></tr></table></figure><p>这是一种低层、通用的设计。相同的 <code>spscring</code> 可以控制：</p><ul><li>CAN 帧数组；</li><li>音频采样数组；</li><li>日志记录数组；</li><li>DMA 描述符数组；</li><li>共享内存块；</li><li>文件中的逻辑槽位。</li></ul><hr><h2 id="2-它在-Lely-Linux-CAN-接收链路中的位置"><a href="#2-它在-Lely-Linux-CAN-接收链路中的位置" class="headerlink" title="2. 它在 Lely Linux CAN 接收链路中的位置"></a>2. 它在 Lely Linux CAN 接收链路中的位置</h2><p>在 Linux <code>CanChannel</code> 中，<code>spscring</code> 只用于控制用户态接收队列：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">io_can_chan_impl</span><br><span class="line">├─ struct spscring rxring</span><br><span class="line">└─ struct io_can_frame *rxbuf</span><br></pre></td></tr></table></figure><p>初始化时：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">spscring_init(&amp;impl-&gt;rxring, rxlen);</span><br><span class="line">impl-&gt;rxbuf = <span class="built_in">calloc</span>(rxlen, <span class="keyword">sizeof</span>(<span class="keyword">struct</span> io_can_frame));</span><br></pre></td></tr></table></figure><p>实际职责划分如下：</p><table><thead><tr><th>组件</th><th>角色</th><th>主要操作</th></tr></thead><tbody><tr><td><code>rxbuf_task</code></td><td>逻辑生产者</td><td>从 SocketCAN <code>recvmsg()</code>，写 <code>rxbuf[i]</code>，执行 <code>p_commit()</code></td></tr><tr><td><code>read_task</code></td><td>逻辑消费者</td><td>从 <code>rxbuf[i]</code> 取帧，完成异步读请求，执行 <code>c_commit()</code></td></tr><tr><td>同步 <code>read()</code></td><td>逻辑消费者的另一个入口</td><td>同样从 <code>rxring</code> 取帧，但由 <code>c_mtx</code> 与异步消费者串行化</td></tr><tr><td><code>c_mtx</code></td><td>消费者侧串行化</td><td>把多个消费入口约束成“一个逻辑消费者”</td></tr><tr><td><code>io_can_chan_impl_c_signal()</code></td><td>可读通知桥梁</td><td>队列产生数据后，重新投递 <code>read_task</code></td></tr></tbody></table><p>因此它与上一篇 IO2 笔记的连接关系是：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">flowchart TB</span><br><span class="line">    Sock[SocketCAN fd] --&gt; RxTask[rxbuf_task]</span><br><span class="line">    RxTask --&gt;|p_alloc| Ring[rxring]</span><br><span class="line">    RxTask --&gt;|recvmsg writes| Buf[rxbuf array]</span><br><span class="line">    RxTask --&gt;|p_commit| Ring</span><br><span class="line">    Ring --&gt;|consumer signal| Sig[io_can_chan_impl_c_signal]</span><br><span class="line">    Sig --&gt;|post| ReadTask[read_task]</span><br><span class="line">    ReadTask --&gt;|c_alloc| Ring</span><br><span class="line">    Buf --&gt;|copy frame| ReadTask</span><br><span class="line">    ReadTask --&gt;|c_commit| Ring</span><br><span class="line">    ReadTask --&gt; Done[User read completion task]</span><br></pre></td></tr></table></figure><p><code>SocketCAN</code> 负责把帧交给进程，<code>spscring</code> 负责在 <code>rxbuf_task</code> 与读任务之间安全转移槽位所有权。</p><hr><h2 id="3-公共-API-按生产者和消费者严格分组"><a href="#3-公共-API-按生产者和消费者严格分组" class="headerlink" title="3. 公共 API 按生产者和消费者严格分组"></a>3. 公共 API 按生产者和消费者严格分组</h2><h3 id="3-1-通用接口"><a href="#3-1-通用接口" class="headerlink" title="3.1 通用接口"></a>3.1 通用接口</h3><table><thead><tr><th>API</th><th>作用</th></tr></thead><tbody><tr><td><code>spscring_init()</code></td><td>初始化环形队列的索引状态</td></tr><tr><td><code>spscring_size()</code></td><td>返回逻辑容量</td></tr></tbody></table><h3 id="3-2-生产者接口"><a href="#3-2-生产者接口" class="headerlink" title="3.2 生产者接口"></a>3.2 生产者接口</h3><table><thead><tr><th>API</th><th>作用</th></tr></thead><tbody><tr><td><code>spscring_p_capacity()</code></td><td>查询总可写槽位数，允许跨越数组末尾</td></tr><tr><td><code>spscring_p_capacity_no_wrap()</code></td><td>查询当前连续可写槽位数，不跨越数组末尾</td></tr><tr><td><code>spscring_p_alloc()</code></td><td>申请可写索引范围，允许逻辑环绕</td></tr><tr><td><code>spscring_p_alloc_no_wrap()</code></td><td>申请连续可写索引范围，不允许环绕</td></tr><tr><td><code>spscring_p_commit()</code></td><td>发布已写槽位，使消费者可见</td></tr><tr><td><code>spscring_p_submit_wait()</code></td><td>可写空间不足时注册通知</td></tr><tr><td><code>spscring_p_abort_wait()</code></td><td>取消生产者等待</td></tr></tbody></table><h3 id="3-3-消费者接口"><a href="#3-3-消费者接口" class="headerlink" title="3.3 消费者接口"></a>3.3 消费者接口</h3><table><thead><tr><th>API</th><th>作用</th></tr></thead><tbody><tr><td><code>spscring_c_capacity()</code></td><td>查询总可读槽位数，允许跨越数组末尾</td></tr><tr><td><code>spscring_c_capacity_no_wrap()</code></td><td>查询当前连续可读槽位数，不跨越数组末尾</td></tr><tr><td><code>spscring_c_alloc()</code></td><td>申请可读索引范围，允许逻辑环绕</td></tr><tr><td><code>spscring_c_alloc_no_wrap()</code></td><td>申请连续可读索引范围，不允许环绕</td></tr><tr><td><code>spscring_c_commit()</code></td><td>归还已读槽位，使生产者可重用</td></tr><tr><td><code>spscring_c_submit_wait()</code></td><td>可读数据不足时注册通知</td></tr><tr><td><code>spscring_c_abort_wait()</code></td><td>取消消费者等待</td></tr></tbody></table><p>最重要的使用约束是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">所有 p_* 接口只能由一个逻辑生产者调用；</span><br><span class="line">所有 c_* 接口只能由一个逻辑消费者调用。</span><br></pre></td></tr></table></figure><p>这里的“逻辑”允许外部使用互斥锁把多个入口串行化，但一旦没有串行化，算法就不再是安全的 SPSC。</p><hr><h2 id="4-struct-spscring-的内存布局"><a href="#4-struct-spscring-的内存布局" class="headerlink" title="4. struct spscring 的内存布局"></a>4. <code>struct spscring</code> 的内存布局</h2><p>核心结构可以概括为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">spscring</span><br><span class="line">├─ p: producer side</span><br><span class="line">│  ├─ ctx: producer local context</span><br><span class="line">│  ├─ pos: producer published position</span><br><span class="line">│  └─ sig: producer wait registration</span><br><span class="line">└─ c: consumer side</span><br><span class="line">   ├─ ctx: consumer local context</span><br><span class="line">   ├─ pos: consumer published position</span><br><span class="line">   └─ sig: consumer wait registration</span><br></pre></td></tr></table></figure><h3 id="4-1-本地上下文-spscring-ctx"><a href="#4-1-本地上下文-spscring-ctx" class="headerlink" title="4.1 本地上下文 spscring_ctx"></a>4.1 本地上下文 <code>spscring_ctx</code></h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">spscring_ctx</span> &#123;</span></span><br><span class="line">    <span class="type">size_t</span> size;</span><br><span class="line">    <span class="type">size_t</span> base;</span><br><span class="line">    <span class="type">size_t</span> pos;</span><br><span class="line">    <span class="type">size_t</span> end;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><table><thead><tr><th>字段</th><th>含义</th></tr></thead><tbody><tr><td><code>size</code></td><td>环形队列容量 <code>N</code></td></tr><tr><td><code>base</code></td><td>当前本地轮次对应的绝对基址</td></tr><tr><td><code>pos</code></td><td>当前物理索引，范围为 <code>[0, N-1]</code></td></tr><tr><td><code>end</code></td><td>本地缓存的逻辑可用区间终点，可能大于 <code>N</code></td></tr></tbody></table><p>生产者只修改 <code>p.ctx</code>，消费者只修改 <code>c.ctx</code>。这些字段不是原子的，因为不会被对端直接访问。</p><h3 id="4-2-发布位置-p-pos-与-c-pos"><a href="#4-2-发布位置-p-pos-与-c-pos" class="headerlink" title="4.2 发布位置 p.pos 与 c.pos"></a>4.2 发布位置 <code>p.pos</code> 与 <code>c.pos</code></h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">p.pos = 已经提交、可供消费者读取的生产者绝对位置</span><br><span class="line">c.pos = 已经提交、可供生产者重用的消费者绝对位置</span><br></pre></td></tr></table></figure><p>访问关系：</p><table><thead><tr><th>原子位置</th><th>谁写</th><th>谁读</th></tr></thead><tbody><tr><td><code>p.pos</code></td><td>生产者</td><td>消费者</td></tr><tr><td><code>c.pos</code></td><td>消费者</td><td>生产者</td></tr></tbody></table><p>这两个位置是生产者和消费者之间最核心的共享状态。</p><h3 id="4-3-等待记录-spscring-sig"><a href="#4-3-等待记录-spscring-sig" class="headerlink" title="4.3 等待记录 spscring_sig"></a>4.3 等待记录 <code>spscring_sig</code></h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">spscring_sig</span> &#123;</span></span><br><span class="line">    <span class="type">spscring_atomic_t</span> size;</span><br><span class="line">    <span class="type">void</span> (*func)(<span class="keyword">struct</span> spscring *ring, <span class="type">void</span> *arg);</span><br><span class="line">    <span class="type">void</span> *arg;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><table><thead><tr><th>等待记录</th><th>谁注册&#x2F;取消</th><th>谁检查并触发</th></tr></thead><tbody><tr><td><code>p.sig</code></td><td>生产者</td><td>消费者在 <code>c_commit()</code> 后检查</td></tr><tr><td><code>c.sig</code></td><td>消费者</td><td>生产者在 <code>p_commit()</code> 后检查</td></tr></tbody></table><p><code>p.sig</code> 的意思不是“生产者通知消费者”，而是“生产者正在等待可写空间”。</p><p><code>c.sig</code> 的意思不是“消费者通知生产者”，而是“消费者正在等待可读数据”。</p><hr><h2 id="5-为什么大量使用缓存行对齐与填充"><a href="#5-为什么大量使用缓存行对齐与填充" class="headerlink" title="5. 为什么大量使用缓存行对齐与填充"></a>5. 为什么大量使用缓存行对齐与填充</h2><p>结构体中的 <code>ctx</code>、<code>pos</code> 和 <code>sig</code> 都使用：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">_Alignas</span>(LEVEL1_DCACHE_LINESIZE)</span><br></pre></td></tr></table></figure><p>并通过 <code>_pad</code> 填充到一个一级数据缓存行。</p><p>其目的不是为了数据对齐美观，而是避免 false sharing，即伪共享。</p><h3 id="5-1-没有隔离时的问题"><a href="#5-1-没有隔离时的问题" class="headerlink" title="5.1 没有隔离时的问题"></a>5.1 没有隔离时的问题</h3><p>假设生产者频繁修改 <code>p.ctx.pos</code>，消费者频繁读取 <code>p.pos</code>。如果它们位于同一个缓存行，即使双方访问的是不同变量，该缓存行仍会在 CPU 核之间不断失效和迁移。</p><h3 id="5-2-当前布局的热点划分"><a href="#5-2-当前布局的热点划分" class="headerlink" title="5.2 当前布局的热点划分"></a>5.2 当前布局的热点划分</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">生产者高频私有写：p.ctx</span><br><span class="line">消费者高频私有写：c.ctx</span><br><span class="line">跨线程发布位置：p.pos / c.pos</span><br><span class="line">等待协议状态：p.sig / c.sig</span><br></pre></td></tr></table></figure><p>每类热点单独占据缓存行后：</p><ul><li>本地 <code>ctx</code> 更新通常不会触发对端缓存失效；</li><li>只有真正需要通信的 <code>pos</code> 或 <code>sig.size</code> 才产生共享缓存流量；</li><li>在 64 字节缓存行平台上，整个对象大约会占用六个缓存行，这是用空间换并发稳定性。</li></ul><p>因此 <code>spscring</code> 不适合作为海量小对象嵌入到每个消息中，它更适合作为长期存在的队列控制对象。</p><hr><h2 id="6-两套坐标：物理索引与绝对位置"><a href="#6-两套坐标：物理索引与绝对位置" class="headerlink" title="6. 两套坐标：物理索引与绝对位置"></a>6. 两套坐标：物理索引与绝对位置</h2><p>普通环形队列通常只保存：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">read_index</span><br><span class="line">write_index</span><br></pre></td></tr></table></figure><p>当两者相等时，必须额外区分“空”还是“满”，常见做法是浪费一个槽位。</p><p><code>spscring</code> 不浪费槽位。它同时维护：</p><ul><li>物理索引：<code>pos</code>，用于访问数组；</li><li>绝对位置：<code>base + pos</code>，用于区分不同轮次。</li></ul><p>假设容量 <code>N = 8</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">绝对位置 0  → 物理索引 0</span><br><span class="line">绝对位置 7  → 物理索引 7</span><br><span class="line">绝对位置 8  → 物理索引 0</span><br><span class="line">绝对位置 13 → 物理索引 5</span><br></pre></td></tr></table></figure><p><code>base</code> 保存当前物理轮次的起点：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">physical_index = absolute_position - base</span><br></pre></td></tr></table></figure><p>当 <code>pos</code> 前进到 <code>N</code> 时：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">base += N;</span><br><span class="line">pos -= N;</span><br><span class="line">end -= N;</span><br></pre></td></tr></table></figure><p>于是物理索引回到数组开头，而绝对位置仍继续前进。</p><h3 id="6-1-可用数据与可写空间公式"><a href="#6-1-可用数据与可写空间公式" class="headerlink" title="6.1 可用数据与可写空间公式"></a>6.1 可用数据与可写空间公式</h3><p>定义：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">P = producer absolute position</span><br><span class="line">C = consumer absolute position</span><br><span class="line">N = ring size</span><br></pre></td></tr></table></figure><p>则：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">可读数量 = P - C</span><br><span class="line">可写数量 = C + N - P</span><br></pre></td></tr></table></figure><p>并始终满足：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">可读数量 + 可写数量 = N</span><br></pre></td></tr></table></figure><p>这就是它可以使用全部 <code>N</code> 个槽位而不需要浪费一个槽位的原因。</p><hr><h2 id="7-ctx-end-是本地缓存，不是第三个共享游标"><a href="#7-ctx-end-是本地缓存，不是第三个共享游标" class="headerlink" title="7. ctx.end 是本地缓存，不是第三个共享游标"></a>7. <code>ctx.end</code> 是本地缓存，不是第三个共享游标</h2><p><code>ctx.end</code> 容易被误解成全局结束位置。实际上它只是本地线程对“当前可用范围终点”的缓存。</p><p>生产者查询容量时：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">size_t</span> cpos = <span class="type">atomic_load</span>(&amp;ring-&gt;c.pos) + ctx-&gt;size;</span><br><span class="line">cpos -= ctx-&gt;base;</span><br><span class="line">ctx-&gt;end = cpos;</span><br><span class="line"><span class="keyword">return</span> ctx-&gt;end - ctx-&gt;pos;</span><br></pre></td></tr></table></figure><p>消费者查询容量时：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">size_t</span> ppos = <span class="type">atomic_load</span>(&amp;ring-&gt;p.pos);</span><br><span class="line">ppos -= ctx-&gt;base;</span><br><span class="line">ctx-&gt;end = ppos;</span><br><span class="line"><span class="keyword">return</span> ctx-&gt;end - ctx-&gt;pos;</span><br></pre></td></tr></table></figure><h3 id="7-1-为什么允许-end-size"><a href="#7-1-为什么允许-end-size" class="headerlink" title="7.1 为什么允许 end &gt; size"></a>7.1 为什么允许 <code>end &gt; size</code></h3><p>假设容量为 8，生产者物理位置在 6，而消费者已前进到下一轮的物理位置 2。</p><p>生产者可写槽位是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">6, 7, 0, 1</span><br></pre></td></tr></table></figure><p>为了把这个跨界范围表示成一个线性区间，本地上下文可表示为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">pos = 6</span><br><span class="line">end = 10</span><br><span class="line">capacity = end - pos = 4</span><br></pre></td></tr></table></figure><p>其中逻辑索引 8、9 分别映射到物理索引 0、1。</p><h3 id="7-2-缓存的性能意义"><a href="#7-2-缓存的性能意义" class="headerlink" title="7.2 缓存的性能意义"></a>7.2 缓存的性能意义</h3><p><code>p_alloc()</code> 和 <code>c_alloc()</code> 会先使用：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ctx.end - ctx.pos</span><br></pre></td></tr></table></figure><p>只有请求数量超过本地缓存容量时，才重新原子读取对端发布位置。</p><p>因此，在批量操作期间，多次 <code>alloc()</code> 不一定每次都产生跨核原子读取。</p><hr><h2 id="8-alloc-与-commit-是一个两阶段事务"><a href="#8-alloc-与-commit-是一个两阶段事务" class="headerlink" title="8. alloc() 与 commit() 是一个两阶段事务"></a>8. <code>alloc()</code> 与 <code>commit()</code> 是一个两阶段事务</h2><h3 id="8-1-alloc-只授予本地访问权"><a href="#8-1-alloc-只授予本地访问权" class="headerlink" title="8.1 alloc() 只授予本地访问权"></a>8.1 <code>alloc()</code> 只授予本地访问权</h3><p>生产者：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">p_alloc</span><br><span class="line">  → 返回可写起始索引</span><br><span class="line">  → 调整请求数量到实际可用数量</span><br><span class="line">  → 不修改共享 p.pos</span><br></pre></td></tr></table></figure><p>消费者：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">c_alloc</span><br><span class="line">  → 返回可读起始索引</span><br><span class="line">  → 调整请求数量到实际可用数量</span><br><span class="line">  → 不修改共享 c.pos</span><br></pre></td></tr></table></figure><p>因此在没有 <code>commit()</code> 的情况下，重复调用相同的 <code>alloc()</code> 是幂等的，返回的仍是同一段尚未提交范围。</p><h3 id="8-2-commit-才转移所有权"><a href="#8-2-commit-才转移所有权" class="headerlink" title="8.2 commit() 才转移所有权"></a>8.2 <code>commit()</code> 才转移所有权</h3><p>生产者完成数据写入后调用：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">spscring_p_commit(ring, count);</span><br></pre></td></tr></table></figure><p>这一步会：</p><ol><li>推进生产者本地位置；</li><li>必要时处理物理环绕；</li><li>release-store 新的 <code>p.pos</code>；</li><li>检查消费者等待条件；</li><li>条件满足时同步调用消费者信号函数。</li></ol><p>消费者完成数据读取后调用：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">spscring_c_commit(ring, count);</span><br></pre></td></tr></table></figure><p>它会把槽位归还给生产者，并检查生产者是否正在等待可写空间。</p><h3 id="8-3-正确访问区间"><a href="#8-3-正确访问区间" class="headerlink" title="8.3 正确访问区间"></a>8.3 正确访问区间</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">生产者：p_alloc → 写用户缓冲区 → p_commit</span><br><span class="line">消费者：c_alloc → 读用户缓冲区 → c_commit</span><br></pre></td></tr></table></figure><p>禁止把数据访问放到事务边界之外：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">错误：先 p_commit，再写缓冲区</span><br><span class="line">错误：先 c_commit，再读取缓冲区</span><br></pre></td></tr></table></figure><p>提交后，槽位所有权已经转移，对端可能立即访问或覆盖该槽位。</p><hr><h2 id="9-生产者路径逐步解析"><a href="#9-生产者路径逐步解析" class="headerlink" title="9. 生产者路径逐步解析"></a>9. 生产者路径逐步解析</h2><h3 id="9-1-查询总可写容量"><a href="#9-1-查询总可写容量" class="headerlink" title="9.1 查询总可写容量"></a>9.1 查询总可写容量</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">size_t</span> capacity = spscring_p_capacity(ring);</span><br></pre></td></tr></table></figure><p>其逻辑等价于：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">free = consumer_position + ring_size - producer_position</span><br></pre></td></tr></table></figure><p>初始化时：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">P = 0</span><br><span class="line">C = 0</span><br><span class="line">free = 0 + N - 0 = N</span><br></pre></td></tr></table></figure><p>所以空队列的生产者容量是全部 <code>N</code> 个槽位。</p><h3 id="9-2-分配可写范围"><a href="#9-2-分配可写范围" class="headerlink" title="9.2 分配可写范围"></a>9.2 分配可写范围</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">size_t</span> n = requested;</span><br><span class="line"><span class="type">size_t</span> i = spscring_p_alloc(ring, &amp;n);</span><br></pre></td></tr></table></figure><p>行为：</p><ul><li>返回当前生产者物理索引；</li><li>若请求超过容量，把 <code>n</code> 截断到实际容量；</li><li>不推进生产者位置；</li><li>允许返回的逻辑范围跨越数组末尾。</li></ul><p>使用允许环绕的 API 时，调用者必须自行取模：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">for</span> (<span class="type">size_t</span> j = <span class="number">0</span>; j &lt; n; j++)</span><br><span class="line">    buffer[(i + j) % ring_size] = input[j];</span><br></pre></td></tr></table></figure><h3 id="9-3-发布已写数据"><a href="#9-3-发布已写数据" class="headerlink" title="9.3 发布已写数据"></a>9.3 发布已写数据</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">spscring_p_commit(ring, n);</span><br></pre></td></tr></table></figure><p>位置推进代码的核心含义是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">new local pos = old pos + n</span><br><span class="line"></span><br><span class="line">if new local pos reaches ring_size:</span><br><span class="line">    advance base by one ring</span><br><span class="line">    wrap local pos to beginning</span><br><span class="line">    normalize cached end</span><br></pre></td></tr></table></figure><p>然后发布：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">p.pos = p.ctx.base + p.ctx.pos</span><br></pre></td></tr></table></figure><p>返回值是提交后的下一个生产者物理索引。</p><hr><h2 id="10-消费者路径与生产者完全对称"><a href="#10-消费者路径与生产者完全对称" class="headerlink" title="10. 消费者路径与生产者完全对称"></a>10. 消费者路径与生产者完全对称</h2><p>消费者容量：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">readable = producer_position - consumer_position</span><br></pre></td></tr></table></figure><p>初始化时为 0。</p><p>当生产者提交 3 个槽位后：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">P = 3</span><br><span class="line">C = 0</span><br><span class="line">readable = 3</span><br></pre></td></tr></table></figure><p>消费者典型流程：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">size_t</span> n = requested;</span><br><span class="line"><span class="type">size_t</span> i = spscring_c_alloc(ring, &amp;n);</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> (<span class="type">size_t</span> j = <span class="number">0</span>; j &lt; n; j++)</span><br><span class="line">    output[j] = buffer[(i + j) % ring_size];</span><br><span class="line"></span><br><span class="line">spscring_c_commit(ring, n);</span><br></pre></td></tr></table></figure><p><code>c_commit()</code> 发布新的 <code>c.pos</code> 后，生产者才能重新覆盖这些槽位。</p><hr><h2 id="11-no-wrap-接口解决连续内存需求"><a href="#11-no-wrap-接口解决连续内存需求" class="headerlink" title="11. no_wrap 接口解决连续内存需求"></a>11. <code>no_wrap</code> 接口解决连续内存需求</h2><h3 id="11-1-总容量与连续容量不同"><a href="#11-1-总容量与连续容量不同" class="headerlink" title="11.1 总容量与连续容量不同"></a>11.1 总容量与连续容量不同</h3><p>假设：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">ring size = 8</span><br><span class="line">producer pos = 6</span><br><span class="line">总可写容量 = 4</span><br></pre></td></tr></table></figure><p>总可写槽位为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">6, 7, 0, 1</span><br></pre></td></tr></table></figure><p>但从索引 6 到数组末尾只有两个连续槽位。</p><p>因此：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">p_capacity()         = 4</span><br><span class="line">p_capacity_no_wrap() = 2</span><br></pre></td></tr></table></figure><p><code>no_wrap</code> 容量公式为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">min(end, size) - pos</span><br></pre></td></tr></table></figure><h3 id="11-2-什么时候应该使用-no-wrap"><a href="#11-2-什么时候应该使用-no-wrap" class="headerlink" title="11.2 什么时候应该使用 no_wrap"></a>11.2 什么时候应该使用 <code>no_wrap</code></h3><p>适合：</p><ul><li>单次 <code>memcpy()</code>；</li><li>DMA 连续描述符；</li><li>要求返回单一连续内存区间的 API；</li><li>不希望调用方做取模循环的批量数据处理。</li></ul><p>不适合：</p><ul><li>希望一次申请全部剩余总容量；</li><li>可以自然处理两段数据的场景。</li></ul><p>跨界批量访问通常要拆成两次：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">第一次：处理数组尾部连续段</span><br><span class="line">commit</span><br><span class="line">第二次：处理数组头部连续段</span><br><span class="line">commit</span><br></pre></td></tr></table></figure><hr><h2 id="12-数值运行示例：容量为-8"><a href="#12-数值运行示例：容量为-8" class="headerlink" title="12. 数值运行示例：容量为 8"></a>12. 数值运行示例：容量为 8</h2><h3 id="12-1-初始化"><a href="#12-1-初始化" class="headerlink" title="12.1 初始化"></a>12.1 初始化</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">P = 0, C = 0</span><br><span class="line">readable = 0</span><br><span class="line">writable = 8</span><br></pre></td></tr></table></figure><h3 id="12-2-生产者写入-3-个元素"><a href="#12-2-生产者写入-3-个元素" class="headerlink" title="12.2 生产者写入 3 个元素"></a>12.2 生产者写入 3 个元素</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">p_alloc(3)  → index 0, count 3</span><br><span class="line">write       → buffer[0..2]</span><br><span class="line">p_commit(3) → P = 3</span><br></pre></td></tr></table></figure><p>状态：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">readable = 3</span><br><span class="line">writable = 5</span><br></pre></td></tr></table></figure><h3 id="12-3-消费者读取-2-个元素"><a href="#12-3-消费者读取-2-个元素" class="headerlink" title="12.3 消费者读取 2 个元素"></a>12.3 消费者读取 2 个元素</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">c_alloc(2)  → index 0, count 2</span><br><span class="line">read        → buffer[0..1]</span><br><span class="line">c_commit(2) → C = 2</span><br></pre></td></tr></table></figure><p>状态：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">readable = 1</span><br><span class="line">writable = 7</span><br></pre></td></tr></table></figure><h3 id="12-4-生产者再写-5-个元素"><a href="#12-4-生产者再写-5-个元素" class="headerlink" title="12.4 生产者再写 5 个元素"></a>12.4 生产者再写 5 个元素</h3><p>当前生产者物理索引为 3，数组尾部正好有 5 个槽位：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">p_alloc_no_wrap(5) → index 3, count 5</span><br><span class="line">write               → buffer[3..7]</span><br><span class="line">p_commit(5)         → P = 8, physical pos wraps to 0</span><br></pre></td></tr></table></figure><p>状态：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">readable = P - C = 8 - 2 = 6</span><br><span class="line">writable = 2</span><br></pre></td></tr></table></figure><p>消费者可读物理顺序为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">2, 3, 4, 5, 6, 7</span><br></pre></td></tr></table></figure><h3 id="12-5-消费者全部读完"><a href="#12-5-消费者全部读完" class="headerlink" title="12.5 消费者全部读完"></a>12.5 消费者全部读完</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">c_alloc_no_wrap(6) → index 2, count 6</span><br><span class="line">c_commit(6)         → C = 8, physical pos wraps to 0</span><br></pre></td></tr></table></figure><p>最终：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">P = 8, C = 8</span><br><span class="line">readable = 0</span><br><span class="line">writable = 8</span><br></pre></td></tr></table></figure><p>虽然物理索引再次相等，但绝对位置表明这是“第二轮的空队列”，不会与初始状态或满队列混淆。</p><hr><h2 id="13-Acquire-Release-如何保证数据可见性"><a href="#13-Acquire-Release-如何保证数据可见性" class="headerlink" title="13. Acquire&#x2F;Release 如何保证数据可见性"></a>13. Acquire&#x2F;Release 如何保证数据可见性</h2><p>在正常原子构建中：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">atomic_load_explicit(..., memory_order_acquire);</span><br><span class="line">atomic_store_explicit(..., memory_order_release);</span><br></pre></td></tr></table></figure><h3 id="13-1-生产者到消费者"><a href="#13-1-生产者到消费者" class="headerlink" title="13.1 生产者到消费者"></a>13.1 生产者到消费者</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">Producer                              Consumer</span><br><span class="line">--------                              --------</span><br><span class="line">write buffer[i]</span><br><span class="line">write buffer[i + 1]</span><br><span class="line">release-store p.pos  -------------&gt;   acquire-load p.pos</span><br><span class="line">                                      read buffer[i]</span><br><span class="line">                                      read buffer[i + 1]</span><br></pre></td></tr></table></figure><p>当消费者通过 acquire-load 观察到新的 <code>p.pos</code> 时，生产者在 release-store 之前完成的缓冲区写入也对消费者可见。</p><h3 id="13-2-消费者到生产者"><a href="#13-2-消费者到生产者" class="headerlink" title="13.2 消费者到生产者"></a>13.2 消费者到生产者</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">Consumer                              Producer</span><br><span class="line">--------                              --------</span><br><span class="line">read buffer[i]</span><br><span class="line">release-store c.pos  -------------&gt;   acquire-load c.pos</span><br><span class="line">                                      overwrite buffer[i]</span><br></pre></td></tr></table></figure><p>生产者只有观察到消费者提交的新 <code>c.pos</code> 后，才会认为槽位可重用。</p><h3 id="13-3-环形队列不会替调用者同步其他无关数据"><a href="#13-3-环形队列不会替调用者同步其他无关数据" class="headerlink" title="13.3 环形队列不会替调用者同步其他无关数据"></a>13.3 环形队列不会替调用者同步其他无关数据</h3><p>保证只覆盖与槽位发布顺序相关的数据访问。若回调参数或其他共享对象还会被并发修改，仍需要它们自己的同步规则。</p><hr><h2 id="14-等待机制不是阻塞队列，而是条件通知注册"><a href="#14-等待机制不是阻塞队列，而是条件通知注册" class="headerlink" title="14. 等待机制不是阻塞队列，而是条件通知注册"></a>14. 等待机制不是阻塞队列，而是条件通知注册</h2><p><code>spscring</code> 不直接创建条件变量、信号量或线程等待对象。它提供的是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">如果当前条件不满足，登记一个回调；</span><br><span class="line">当对端 commit 使条件满足时，调用该回调。</span><br></pre></td></tr></table></figure><p>生产者可等待“至少有 <code>size</code> 个可写槽位”：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">spscring_p_submit_wait(ring, size, producer_signal, arg);</span><br></pre></td></tr></table></figure><p>消费者可等待“至少有 <code>size</code> 个可读槽位”：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">spscring_c_submit_wait(ring, size, consumer_signal, arg);</span><br></pre></td></tr></table></figure><p>返回值：</p><table><thead><tr><th>返回值</th><th>含义</th></tr></thead><tbody><tr><td><code>0</code></td><td>条件已经满足，没有注册等待，也不会调用回调</td></tr><tr><td><code>1</code></td><td>等待已经注册；稍后条件满足时由对端 <code>commit()</code> 调用回调，或等待可能被取消</td></tr></tbody></table><p>回调不是由独立线程自动执行，而是在使条件满足的对端 <code>commit()</code> 调用栈中同步执行。</p><p>这一点必须牢记：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">consumer wait callback</span><br><span class="line">由 producer 的 p_commit() 线程执行</span><br><span class="line"></span><br><span class="line">producer wait callback</span><br><span class="line">由 consumer 的 c_commit() 线程执行</span><br></pre></td></tr></table></figure><hr><h2 id="15-双重检查如何避免丢失唤醒"><a href="#15-双重检查如何避免丢失唤醒" class="headerlink" title="15. 双重检查如何避免丢失唤醒"></a>15. 双重检查如何避免丢失唤醒</h2><p>以消费者等待一个可读元素为例。</p><p>错误的简单实现可能是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">1. 检查队列为空</span><br><span class="line">2. 生产者提交数据并检查“无人等待”</span><br><span class="line">3. 消费者登记等待</span><br><span class="line">4. 数据已经存在，但之后再也没有提交，消费者永久沉睡</span><br></pre></td></tr></table></figure><p>Lely 使用“检查—发布等待—再次检查”的流程：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">1. abort previous wait</span><br><span class="line">2. 保存 func/arg</span><br><span class="line">3. 第一次检查容量</span><br><span class="line">4. 若已满足，直接返回 0</span><br><span class="line">5. 原子发布 sig.size = requested</span><br><span class="line">6. 第二次检查容量</span><br><span class="line">7. 若并发期间条件已满足，尝试把 sig.size 从 requested 改回 0</span><br><span class="line">8. CAS 成功：等待尚未被对端接管，返回 0</span><br><span class="line">9. CAS 失败：对端已开始或完成信号流程，返回 1</span><br></pre></td></tr></table></figure><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant Cons as Consumer</span><br><span class="line">    participant Ring as spscring</span><br><span class="line">    participant Prod as Producer</span><br><span class="line"></span><br><span class="line">    Cons-&gt;&gt;Ring: first capacity check</span><br><span class="line">    Ring--&gt;&gt;Cons: not enough data</span><br><span class="line">    Cons-&gt;&gt;Ring: publish c.sig.size</span><br><span class="line">    Prod-&gt;&gt;Ring: write payload and p_commit</span><br><span class="line">    Prod-&gt;&gt;Ring: check c.sig and available data</span><br><span class="line">    Cons-&gt;&gt;Ring: second capacity check</span><br><span class="line"></span><br><span class="line">    alt Consumer removes wait first</span><br><span class="line">        Cons-&gt;&gt;Ring: CAS requested to 0</span><br><span class="line">        Ring--&gt;&gt;Cons: condition already satisfied, return 0</span><br><span class="line">    else Producer claims signal first</span><br><span class="line">        Prod-&gt;&gt;Ring: CAS requested to SIZE_MAX</span><br><span class="line">        Prod-&gt;&gt;Ring: copy callback and clear size</span><br><span class="line">        Prod--&gt;&gt;Cons: invoke callback, submit_wait returns 1</span><br><span class="line">    end</span><br></pre></td></tr></table></figure><p>第二次检查与 CAS 竞争共同保证：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">条件不会在“检查”和“登记”等待之间悄悄变为满足而丢失通知。</span><br></pre></td></tr></table></figure><hr><h2 id="16-sig-size-实际是一个三态同步协议"><a href="#16-sig-size-实际是一个三态同步协议" class="headerlink" title="16. sig.size 实际是一个三态同步协议"></a>16. <code>sig.size</code> 实际是一个三态同步协议</h2><p><code>sig.size</code> 不只是“等待数量”，还承担状态机作用：</p><table><thead><tr><th>值</th><th>状态</th></tr></thead><tbody><tr><td><code>0</code></td><td>没有等待，或等待已经取消&#x2F;完成</td></tr><tr><td><code>1..N</code></td><td>已注册等待，数值是条件阈值</td></tr><tr><td><code>SIZE_MAX</code></td><td>对端已经接管通知，正在读取 <code>func</code> 和 <code>arg</code></td></tr></tbody></table><h3 id="16-1-为什么需要-SIZE-MAX"><a href="#16-1-为什么需要-SIZE-MAX" class="headerlink" title="16.1 为什么需要 SIZE_MAX"></a>16.1 为什么需要 <code>SIZE_MAX</code></h3><p><code>func</code> 与 <code>arg</code> 本身不是原子变量。</p><p>如果取消等待的一方在对端读取函数指针期间立即覆盖它们，会产生数据竞争。信号方先执行：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">CAS sig.size: requested → SIZE_MAX</span><br></pre></td></tr></table></figure><p>这相当于声明：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">我已经进入读取 callback 元数据的临界窗口。</span><br></pre></td></tr></table></figure><p>随后：</p><ol><li>复制 <code>func</code> 与 <code>arg</code> 到局部变量；</li><li>release-store <code>sig.size = 0</code>；</li><li>调用局部副本中的函数。</li></ol><p>等待方看到 <code>SIZE_MAX</code> 时不能直接取消，只能短暂自旋，直到信号方完成元数据读取。</p><h3 id="16-2-为什么请求数量不会与-SIZE-MAX-冲突"><a href="#16-2-为什么请求数量不会与-SIZE-MAX-冲突" class="headerlink" title="16.2 为什么请求数量不会与 SIZE_MAX 冲突"></a>16.2 为什么请求数量不会与 <code>SIZE_MAX</code> 冲突</h3><p>等待数量会被限制到环形队列容量：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (size &gt; ctx-&gt;size)</span><br><span class="line">    size = ctx-&gt;size;</span><br></pre></td></tr></table></figure><p>同时队列容量被限制为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">size &lt;= SIZE_MAX / 2 + 1</span><br></pre></td></tr></table></figure><p>因此正常请求阈值不会取到 <code>SIZE_MAX</code>，该值可以安全作为内部哨兵。</p><hr><h2 id="17-abort-wait-的真实语义"><a href="#17-abort-wait-的真实语义" class="headerlink" title="17. abort_wait() 的真实语义"></a>17. <code>abort_wait()</code> 的真实语义</h2><p>取消等待时：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">load sig.size</span><br><span class="line"></span><br><span class="line">while size != 0:</span><br><span class="line">    if size == SIZE_MAX:</span><br><span class="line">        processor pause</span><br><span class="line">        reload</span><br><span class="line">    else:</span><br><span class="line">        CAS size → 0</span><br></pre></td></tr></table></figure><p>返回值：</p><table><thead><tr><th>返回值</th><th>含义</th></tr></thead><tbody><tr><td><code>1</code></td><td>成功取消了一个尚未触发的等待</td></tr><tr><td><code>0</code></td><td>当前没有已注册等待</td></tr></tbody></table><h3 id="17-1-取消与回调可能存在竞争"><a href="#17-1-取消与回调可能存在竞争" class="headerlink" title="17.1 取消与回调可能存在竞争"></a>17.1 取消与回调可能存在竞争</h3><p>如果信号方已经把状态改成 <code>SIZE_MAX</code>，取消方不能阻止本次回调，因为信号方已经接管了函数指针和参数。</p><p>此时 <code>abort_wait()</code> 会等待信号方把状态恢复为 0，随后返回 0。</p><p>因此调用者不能假设：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">只要调用 abort_wait()，回调就绝对不会发生。</span><br></pre></td></tr></table></figure><p>更准确的语义是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">若等待仍处于可取消状态，则取消；</span><br><span class="line">若通知已经开始，则等待其完成接管。</span><br></pre></td></tr></table></figure><hr><h2 id="18-pc-signal-与-cp-signal-的命名和判断公式"><a href="#18-pc-signal-与-cp-signal-的命名和判断公式" class="headerlink" title="18. pc_signal() 与 cp_signal() 的命名和判断公式"></a>18. <code>pc_signal()</code> 与 <code>cp_signal()</code> 的命名和判断公式</h2><h3 id="18-1-spscring-pc-signal"><a href="#18-1-spscring-pc-signal" class="headerlink" title="18.1 spscring_pc_signal()"></a>18.1 <code>spscring_pc_signal()</code></h3><p>在生产者 <code>p_commit()</code> 后调用：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">producer commit → consumer signal</span><br></pre></td></tr></table></figure><p>它检查 <code>c.sig</code>，判断消费者请求的可读数量是否已经满足。</p><p>源码使用：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">readable = ring_size - producer_free_capacity</span><br></pre></td></tr></table></figure><p>若：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">requested_readable &lt;= readable</span><br></pre></td></tr></table></figure><p>则触发消费者回调。</p><h3 id="18-2-spscring-cp-signal"><a href="#18-2-spscring-cp-signal" class="headerlink" title="18.2 spscring_cp_signal()"></a>18.2 <code>spscring_cp_signal()</code></h3><p>在消费者 <code>c_commit()</code> 后调用：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">consumer commit → producer signal</span><br></pre></td></tr></table></figure><p>它检查 <code>p.sig</code>，判断生产者请求的可写数量是否已经满足。</p><p>源码使用：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">writable = ring_size - consumer_readable_capacity</span><br></pre></td></tr></table></figure><p>若：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">requested_writable &lt;= writable</span><br></pre></td></tr></table></figure><p>则触发生产者回调。</p><hr><h2 id="19-回调函数必须保持轻量且避免重入死锁"><a href="#19-回调函数必须保持轻量且避免重入死锁" class="headerlink" title="19. 回调函数必须保持轻量且避免重入死锁"></a>19. 回调函数必须保持轻量且避免重入死锁</h2><p>信号函数在 <code>commit()</code> 内同步执行。因此回调会延长提交操作的执行时间。</p><p>推荐回调只做：</p><ul><li>设置原子标志；</li><li>释放信号量；</li><li>向 Executor 投递任务；</li><li>写 eventfd；</li><li>触发轻量通知。</li></ul><p>不推荐：</p><ul><li>长时间阻塞；</li><li>执行复杂协议状态机；</li><li>进行大量内存分配；</li><li>获取可能已由提交方持有的同一把锁；</li><li>在不满足线程所有权的情况下直接调用对端专属 <code>p_*</code> 或 <code>c_*</code> API。</li></ul><p>Lely <code>CanChannel</code> 采用的是安全模式：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">p_commit()</span><br><span class="line">  → io_can_chan_impl_c_signal()</span><br><span class="line">      → 只检查状态并 post read_task</span><br></pre></td></tr></table></figure><p>而不是在生产者线程中直接消费 CAN 帧。</p><p>另一个关键细节是，<code>rxbuf_task</code> 在执行 <code>p_commit()</code> 时没有持有 <code>impl-&gt;mtx</code>，而回调 <code>io_can_chan_impl_c_signal()</code> 会获取该互斥锁。这样避免了提交线程在同步回调中重复获取同一把锁而死锁。</p><hr><h2 id="20-Lock-free-与-Wait-free-在这里分别意味着什么"><a href="#20-Lock-free-与-Wait-free-在这里分别意味着什么" class="headerlink" title="20. Lock-free 与 Wait-free 在这里分别意味着什么"></a>20. Lock-free 与 Wait-free 在这里分别意味着什么</h2><h3 id="20-1-Wait-free"><a href="#20-1-Wait-free" class="headerlink" title="20.1 Wait-free"></a>20.1 Wait-free</h3><p>当没有注册信号函数时，普通容量、分配和提交路径只包含有限数量的本地操作与原子读写，不需要等待另一个线程完成某个临界区。</p><p>这不表示：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">生产者一定能分配到空间；</span><br><span class="line">消费者一定能读到数据。</span><br></pre></td></tr></table></figure><p>队列满或空时，操作会立即返回容量 0。这里的 wait-free 描述的是算法步骤有界，而不是业务条件永远满足。</p><h3 id="20-2-Lock-free"><a href="#20-2-Lock-free" class="headerlink" title="20.2 Lock-free"></a>20.2 Lock-free</h3><p>等待注册、取消和通知使用 CAS 循环。单个线程可能短暂重试，但系统整体会持续取得进展。</p><p>当回调存在时，<code>commit()</code> 的进度还取决于回调：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">回调是 lock-free → 提交路径仍可视为 lock-free</span><br><span class="line">回调会阻塞       → 整体不再具备该性质</span><br></pre></td></tr></table></figure><h3 id="20-3-它不是多生产者多消费者队列"><a href="#20-3-它不是多生产者多消费者队列" class="headerlink" title="20.3 它不是多生产者多消费者队列"></a>20.3 它不是多生产者多消费者队列</h3><p>多个生产者同时修改 <code>p.ctx</code>，或多个消费者同时修改 <code>c.ctx</code>，不会被原子位置保护。</p><p>若业务确实存在多生产者或多消费者：</p><ul><li>使用外部互斥锁把同侧访问串行化；或</li><li>选择专门的 MPSC、SPMC、MPMC 队列；</li><li>不要仅因为 <code>p.pos/c.pos</code> 是原子的，就误认为整个结构支持 MPMC。</li></ul><hr><h2 id="21-spscring-yield-不是操作系统线程让步"><a href="#21-spscring-yield-不是操作系统线程让步" class="headerlink" title="21. spscring_yield() 不是操作系统线程让步"></a>21. <code>spscring_yield()</code> 不是操作系统线程让步</h2><p>等待协议中的自旋提示实现为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Windows   → YieldProcessor()</span><br><span class="line">x86/x64   → pause 指令</span><br><span class="line">其他平台  → 可能为空操作</span><br></pre></td></tr></table></figure><p>它主要用于降低自旋时的流水线和功耗代价，并不等价于：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sched_yield();</span><br></pre></td></tr></table></figure><p>因此，在信号方长期阻塞或被高优先级线程永久抢占时，取消方可能持续占用 CPU 自旋。</p><p>正常设计要求 <code>SIZE_MAX</code> 状态窗口极短，只覆盖复制函数指针与参数的几条指令。</p><hr><h2 id="22-LELY-NO-ATOMICS-分支应如何理解"><a href="#22-LELY-NO-ATOMICS-分支应如何理解" class="headerlink" title="22. LELY_NO_ATOMICS 分支应如何理解"></a>22. <code>LELY_NO_ATOMICS</code> 分支应如何理解</h2><p>头文件根据构建环境选择：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">C++                → std::atomic_size_t</span><br><span class="line">C11                 → atomic_size_t</span><br><span class="line">LELY_NO_ATOMICS     → size_t</span><br></pre></td></tr></table></figure><p>正常多线程环境依赖 C&#x2F;C++ 原子操作的 acquire&#x2F;release 与 CAS 语义。</p><p><code>LELY_NO_ATOMICS</code> 分支的普通位置加载和存储退化为直接内存访问，不提供标准 C11 跨线程内存序保证。Windows 的部分 CAS 路径使用 Interlocked，但这不能把所有普通访问自动变成完整、可移植的原子协议。</p><p>工程上应采用以下判断：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">单线程或明确受平台约束的特殊构建</span><br><span class="line">  → 可以评估 LELY_NO_ATOMICS</span><br><span class="line"></span><br><span class="line">正常 Linux/RTOS 多线程生产者-消费者</span><br><span class="line">  → 应启用编译器原子支持</span><br></pre></td></tr></table></figure><p>不要仅因为程序“在当前 CPU 上看起来能运行”就忽略数据竞争与内存可见性问题。</p><hr><h2 id="23-容量上限与-size-t-回绕"><a href="#23-容量上限与-size-t-回绕" class="headerlink" title="23. 容量上限与 size_t 回绕"></a>23. 容量上限与 <code>size_t</code> 回绕</h2><p>初始化断言：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">assert(size &lt;= SIZE_MAX / <span class="number">2</span> + <span class="number">1</span>);</span><br></pre></td></tr></table></figure><p>其作用包括：</p><ol><li>保证生产者与消费者之间的最大逻辑距离不超过半个无符号计数空间；</li><li>让无符号位置回绕后，差值仍能在环容量约束下被唯一解释；</li><li>避免等待数量与内部 <code>SIZE_MAX</code> 哨兵冲突。</li></ol><p>绝对位置不是永远不溢出的数学整数，而是按 <code>size_t</code> 模数回绕的逻辑计数器。算法依赖以下不变量：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">0 &lt;= P - C &lt;= N</span><br><span class="line">N &lt;= SIZE_MAX / 2 + 1</span><br></pre></td></tr></table></figure><p>在此约束下，即使 <code>P</code> 或 <code>C</code> 穿过 <code>SIZE_MAX</code>，无符号减法仍能得到正确的环内距离。</p><h3 id="23-1-容量不能为-0"><a href="#23-1-容量不能为-0" class="headerlink" title="23.1 容量不能为 0"></a>23.1 容量不能为 0</h3><p>当前 <code>spscring_init()</code> 没有显式断言 <code>size &gt; 0</code>，但后续所有操作都要求：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">assert(ctx-&gt;pos &lt; ctx-&gt;size);</span><br></pre></td></tr></table></figure><p>因此 <code>size = 0</code> 在实际使用中无效。工程接口应在上层明确保证：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ring size &gt;= 1</span><br></pre></td></tr></table></figure><hr><h2 id="24-Lely-CanChannel-接收流程的完整时序"><a href="#24-Lely-CanChannel-接收流程的完整时序" class="headerlink" title="24. Lely CanChannel 接收流程的完整时序"></a>24. Lely <code>CanChannel</code> 接收流程的完整时序</h2><h3 id="24-1-空队列上的异步读"><a href="#24-1-空队列上的异步读" class="headerlink" title="24.1 空队列上的异步读"></a>24.1 空队列上的异步读</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant App as Application</span><br><span class="line">    participant Rd as read_task</span><br><span class="line">    participant Ring as rxring</span><br><span class="line">    participant Rx as rxbuf_task</span><br><span class="line">    participant Sock as SocketCAN</span><br><span class="line">    participant Sig as c_signal</span><br><span class="line"></span><br><span class="line">    App-&gt;&gt;Rd: submit async read</span><br><span class="line">    Rd-&gt;&gt;Ring: c_alloc 1</span><br><span class="line">    Ring--&gt;&gt;Rd: count 0</span><br><span class="line">    Rd-&gt;&gt;Ring: c_submit_wait 1</span><br><span class="line">    Rd-&gt;&gt;Rx: post rxbuf_task</span><br><span class="line">    Rx-&gt;&gt;Ring: p_alloc 1</span><br><span class="line">    Ring--&gt;&gt;Rx: writable index</span><br><span class="line">    Rx-&gt;&gt;Sock: recvmsg</span><br><span class="line">    Sock--&gt;&gt;Rx: CAN frame</span><br><span class="line">    Rx-&gt;&gt;Rx: write rxbuf[index]</span><br><span class="line">    Rx-&gt;&gt;Ring: p_commit 1</span><br><span class="line">    Ring-&gt;&gt;Sig: invoke consumer signal</span><br><span class="line">    Sig-&gt;&gt;Rd: post read_task</span><br><span class="line">    Rd-&gt;&gt;Ring: c_alloc 1</span><br><span class="line">    Ring--&gt;&gt;Rd: readable index</span><br><span class="line">    Rd-&gt;&gt;Rd: copy and decode rxbuf[index]</span><br><span class="line">    Rd-&gt;&gt;Ring: c_commit 1</span><br><span class="line">    Rd--&gt;&gt;App: post completion task</span><br></pre></td></tr></table></figure><h3 id="24-2-为什么-c-mtx-仍然存在"><a href="#24-2-为什么-c-mtx-仍然存在" class="headerlink" title="24.2 为什么 c_mtx 仍然存在"></a>24.2 为什么 <code>c_mtx</code> 仍然存在</h3><p><code>spscring</code> 只允许一个消费者，但 <code>CanChannel</code> 具有两个可能的消费入口：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">同步 io_can_chan_impl_read()</span><br><span class="line">异步 io_can_chan_impl_do_read()</span><br></pre></td></tr></table></figure><p>两条路径都通过 <code>c_mtx</code> 保护 <code>c_alloc/c_commit</code>，把它们串行化为一个逻辑消费者。</p><p>因此：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">spscring 的无锁性质</span><br><span class="line">不等于整个 CanChannel 接收路径完全没有互斥锁</span><br></pre></td></tr></table></figure><p>环形队列解决的是生产者与消费者之间的槽位转移；上层仍可能需要互斥锁维护更复杂的 API 并发语义。</p><h3 id="24-3-MSG-CONFIRM-不进入普通接收环"><a href="#24-3-MSG-CONFIRM-不进入普通接收环" class="headerlink" title="24.3 MSG_CONFIRM 不进入普通接收环"></a>24.3 <code>MSG_CONFIRM</code> 不进入普通接收环</h3><p><code>rxbuf_task</code> 收到 SocketCAN 自发帧确认时：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">MSG_CONFIRM</span><br><span class="line">  → 转为 can_msg</span><br><span class="line">  → 匹配 confirm_queue</span><br><span class="line">  → 不提交到 rxring</span><br></pre></td></tr></table></figure><p>普通 CAN&#x2F;CAN FD 帧才会执行 <code>p_commit()</code> 并成为用户读请求的数据来源。</p><h3 id="24-4-环满与发送确认并存时的源码注意点"><a href="#24-4-环满与发送确认并存时的源码注意点" class="headerlink" title="24.4 环满与发送确认并存时的源码注意点"></a>24.4 环满与发送确认并存时的源码注意点</h3><p><code>rxbuf_task</code> 在存在写确认等待时，即使普通接收环没有空槽，也可能继续调用 <code>recvmsg()</code> 以寻找 <code>MSG_CONFIRM</code>。</p><p>此时：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">p_alloc() 可能返回 count = 0</span><br><span class="line">frame 指向栈上临时对象</span><br></pre></td></tr></table></figure><p>若读到的是普通帧而不是确认帧，该帧无法进入已经满的 <code>rxring</code>。调试高总线负载、启用 <code>txwait</code> 且消费端处理过慢的问题时，应把这一版本相关行为纳入丢帧分析。</p><p>根本治理方向仍然是：</p><ul><li>提高 <code>rxlen</code>；</li><li>避免读任务长期阻塞；</li><li>缩短完成回调；</li><li>监控应用层消费延迟；</li><li>根据发送确认需求评估是否必须启用 <code>txwait</code>。</li></ul><hr><h2 id="25-一个最小、正确的用户数据队列示例"><a href="#25-一个最小、正确的用户数据队列示例" class="headerlink" title="25. 一个最小、正确的用户数据队列示例"></a>25. 一个最小、正确的用户数据队列示例</h2><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;lely/util/spscring.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdbool.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stddef.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> SAMPLE_QUEUE_SIZE 8</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sample</span> &#123;</span></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span> id;</span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span> value;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sample_queue</span> &#123;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">spscring</span> <span class="title">ring</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sample</span> <span class="title">data</span>[<span class="title">SAMPLE_QUEUE_SIZE</span>];</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="type">static</span> <span class="type">void</span></span><br><span class="line"><span class="title function_">sample_queue_init</span><span class="params">(<span class="keyword">struct</span> sample_queue *<span class="built_in">queue</span>)</span></span><br><span class="line">&#123;</span><br><span class="line">    spscring_init(&amp;<span class="built_in">queue</span>-&gt;ring, SAMPLE_QUEUE_SIZE);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">static</span> <span class="type">bool</span></span><br><span class="line"><span class="title function_">sample_queue_push</span><span class="params">(<span class="keyword">struct</span> sample_queue *<span class="built_in">queue</span>, <span class="type">const</span> <span class="keyword">struct</span> sample *sample)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">size_t</span> n = <span class="number">1</span>;</span><br><span class="line">    <span class="type">size_t</span> index = spscring_p_alloc_no_wrap(&amp;<span class="built_in">queue</span>-&gt;ring, &amp;n);</span><br><span class="line">    <span class="keyword">if</span> (!n)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">queue</span>-&gt;data[index] = *sample;</span><br><span class="line">    spscring_p_commit(&amp;<span class="built_in">queue</span>-&gt;ring, <span class="number">1</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">static</span> <span class="type">bool</span></span><br><span class="line"><span class="title function_">sample_queue_pop</span><span class="params">(<span class="keyword">struct</span> sample_queue *<span class="built_in">queue</span>, <span class="keyword">struct</span> sample *sample)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">size_t</span> n = <span class="number">1</span>;</span><br><span class="line">    <span class="type">size_t</span> index = spscring_c_alloc_no_wrap(&amp;<span class="built_in">queue</span>-&gt;ring, &amp;n);</span><br><span class="line">    <span class="keyword">if</span> (!n)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line"></span><br><span class="line">    *sample = <span class="built_in">queue</span>-&gt;data[index];</span><br><span class="line">    spscring_c_commit(&amp;<span class="built_in">queue</span>-&gt;ring, <span class="number">1</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>成立条件：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">sample_queue_push() 只由一个生产者线程调用；</span><br><span class="line">sample_queue_pop() 只由一个消费者线程调用；</span><br><span class="line">对象初始化完成后再启动两个线程；</span><br><span class="line">队列销毁前两个线程已经停止。</span><br></pre></td></tr></table></figure><p>如果同侧有多个调用者，必须在该侧增加外部串行化。</p><hr><h2 id="26-批量写入的两种正确方式"><a href="#26-批量写入的两种正确方式" class="headerlink" title="26. 批量写入的两种正确方式"></a>26. 批量写入的两种正确方式</h2><h3 id="26-1-允许环绕，一次逻辑申请"><a href="#26-1-允许环绕，一次逻辑申请" class="headerlink" title="26.1 允许环绕，一次逻辑申请"></a>26.1 允许环绕，一次逻辑申请</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">size_t</span> n = requested;</span><br><span class="line"><span class="type">size_t</span> index = spscring_p_alloc(&amp;<span class="built_in">queue</span>-&gt;ring, &amp;n);</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> (<span class="type">size_t</span> i = <span class="number">0</span>; i &lt; n; i++)</span><br><span class="line">    <span class="built_in">queue</span>-&gt;data[(index + i) % SAMPLE_QUEUE_SIZE] = input[i];</span><br><span class="line"></span><br><span class="line">spscring_p_commit(&amp;<span class="built_in">queue</span>-&gt;ring, n);</span><br></pre></td></tr></table></figure><p>优点：一次取得总容量。<br>缺点：每个元素需要取模或拆分成两段。</p><h3 id="26-2-不允许环绕，分两次连续处理"><a href="#26-2-不允许环绕，分两次连续处理" class="headerlink" title="26.2 不允许环绕，分两次连续处理"></a>26.2 不允许环绕，分两次连续处理</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">size_t</span> remaining = requested;</span><br><span class="line"><span class="type">size_t</span> offset = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">while</span> (remaining) &#123;</span><br><span class="line">    <span class="type">size_t</span> n = remaining;</span><br><span class="line">    <span class="type">size_t</span> index = spscring_p_alloc_no_wrap(&amp;<span class="built_in">queue</span>-&gt;ring, &amp;n);</span><br><span class="line">    <span class="keyword">if</span> (!n)</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">memcpy</span>(&amp;<span class="built_in">queue</span>-&gt;data[index], &amp;input[offset], n * <span class="keyword">sizeof</span>(input[<span class="number">0</span>]));</span><br><span class="line">    spscring_p_commit(&amp;<span class="built_in">queue</span>-&gt;ring, n);</span><br><span class="line"></span><br><span class="line">    offset += n;</span><br><span class="line">    remaining -= n;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>优点：每段都是连续内存，适合 <code>memcpy()</code>。<br>缺点：跨界时需要两次申请与提交。</p><hr><h2 id="27-等待回调的推荐接法"><a href="#27-等待回调的推荐接法" class="headerlink" title="27. 等待回调的推荐接法"></a>27. 等待回调的推荐接法</h2><p>下面的回调只负责通知，不直接消费：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">consumer_context</span> &#123;</span></span><br><span class="line">    <span class="comment">/* executor, semaphore or event object */</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="type">static</span> <span class="type">void</span></span><br><span class="line"><span class="title function_">sample_queue_readable</span><span class="params">(<span class="keyword">struct</span> spscring *ring, <span class="type">void</span> *arg)</span></span><br><span class="line">&#123;</span><br><span class="line">    (<span class="type">void</span>)ring;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">consumer_context</span> *<span class="title">ctx</span> =</span> arg;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Post a task or release a semaphore. Keep this callback short. */</span></span><br><span class="line">    consumer_schedule(ctx);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">static</span> <span class="type">bool</span></span><br><span class="line"><span class="title function_">sample_queue_wait_readable</span><span class="params">(<span class="keyword">struct</span> sample_queue *<span class="built_in">queue</span>,</span></span><br><span class="line"><span class="params">        <span class="keyword">struct</span> consumer_context *ctx)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">return</span> spscring_c_submit_wait(</span><br><span class="line">            &amp;<span class="built_in">queue</span>-&gt;ring, <span class="number">1</span>, &amp;sample_queue_readable, ctx) != <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>调用方必须处理两种返回路径：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">返回 0：现在已经可读，调用方应直接继续处理，回调不会发生。</span><br><span class="line">返回 1：等待已经登记，稍后由生产者提交路径触发回调。</span><br></pre></td></tr></table></figure><p>错误模式是只等待回调而忽略返回 0，这会在条件已经满足时造成任务停滞。</p><hr><h2 id="28-调试时建议观察的状态与不变量"><a href="#28-调试时建议观察的状态与不变量" class="headerlink" title="28. 调试时建议观察的状态与不变量"></a>28. 调试时建议观察的状态与不变量</h2><h3 id="28-1-核心不变量"><a href="#28-1-核心不变量" class="headerlink" title="28.1 核心不变量"></a>28.1 核心不变量</h3><p>对生产者上下文：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">pos &lt; size</span><br><span class="line">pos &lt;= end</span><br><span class="line">end - pos &lt;= size</span><br></pre></td></tr></table></figure><p>消费者上下文相同。</p><p>全局逻辑关系：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">0 &lt;= producer_absolute - consumer_absolute &lt;= size</span><br></pre></td></tr></table></figure><h3 id="28-2-关键观测点"><a href="#28-2-关键观测点" class="headerlink" title="28.2 关键观测点"></a>28.2 关键观测点</h3><table><thead><tr><th>位置</th><th>建议记录</th></tr></thead><tbody><tr><td><code>p_alloc</code> 返回后</td><td>请求数量、实际数量、起始索引、<code>p.ctx.base/pos/end</code></td></tr><tr><td><code>p_commit</code> 前后</td><td>提交数量、新 <code>p.pos</code>、是否发生环绕</td></tr><tr><td><code>c_alloc</code> 返回后</td><td>请求数量、实际数量、起始索引、<code>c.ctx.base/pos/end</code></td></tr><tr><td><code>c_commit</code> 前后</td><td>提交数量、新 <code>c.pos</code>、是否发生环绕</td></tr><tr><td><code>submit_wait</code></td><td>阈值、第一次容量、第二次容量、返回值</td></tr><tr><td>signal</td><td><code>sig.size</code> 状态、CAS 是否成功、回调执行线程</td></tr><tr><td>abort</td><td>是否观察到 <code>SIZE_MAX</code>、自旋时间、最终返回值</td></tr></tbody></table><h3 id="28-3-常见故障定位"><a href="#28-3-常见故障定位" class="headerlink" title="28.3 常见故障定位"></a>28.3 常见故障定位</h3><table><thead><tr><th>现象</th><th>优先检查</th></tr></thead><tbody><tr><td>偶发读取旧数据</td><td>是否在 <code>p_commit()</code> 前完成数据写入；是否禁用了原子支持</td></tr><tr><td>偶发被覆盖</td><td>消费者是否在读取完成前调用了 <code>c_commit()</code></td></tr><tr><td>队列容量异常</td><td>是否同侧存在多个未串行化调用者；提交数量是否超过分配数量</td></tr><tr><td>永久不再唤醒</td><td>是否忽略 <code>submit_wait()</code> 返回 0；是否错误取消等待</td></tr><tr><td><code>abort_wait()</code> 长时间自旋</td><td>回调接管窗口是否被阻塞；提交线程是否被异常挂起</td></tr><tr><td>回调死锁</td><td><code>commit()</code> 时是否持有回调还会获取的锁</td></tr><tr><td>CAN 接收丢帧</td><td><code>rxring</code> 是否满；消费任务是否阻塞；发送确认路径是否迫使继续读取</td></tr></tbody></table><hr><h2 id="29-单元测试应该覆盖哪些边界"><a href="#29-单元测试应该覆盖哪些边界" class="headerlink" title="29. 单元测试应该覆盖哪些边界"></a>29. 单元测试应该覆盖哪些边界</h2><p>Lely 自带测试重点覆盖：</p><ul><li>空队列生产者容量等于 <code>size</code>；</li><li>提交一个、多个和全部槽位后的容量；</li><li>请求数量大于容量时被截断；</li><li><code>no_wrap</code> 在数组尾部的连续容量；</li><li>生产者提交触发消费者信号；</li><li>消费者提交触发生产者信号；</li><li>条件已满足时 <code>submit_wait()</code> 返回 0；</li><li>条件不满足时等待成功注册；</li><li><code>abort_wait()</code> 后回调不再执行。</li></ul><p>工程移植时还应增加：</p><ol><li>多轮绝对位置回绕测试；</li><li>批量跨物理边界读写测试；</li><li>高并发长时间顺序校验；</li><li>生产者和消费者不同 CPU 核绑定测试；</li><li>ThreadSanitizer 或等效数据竞争检测；</li><li>回调注册、触发和取消的竞争测试；</li><li>队列容量为 1 的边界测试；</li><li>上层线程停止与对象销毁竞争测试。</li></ol><hr><h2 id="30-设计优点与限制"><a href="#30-设计优点与限制" class="headerlink" title="30. 设计优点与限制"></a>30. 设计优点与限制</h2><h3 id="30-1-优点"><a href="#30-1-优点" class="headerlink" title="30.1 优点"></a>30.1 优点</h3><ul><li>使用完整 <code>N</code> 个槽位，不浪费一个空槽；</li><li>生产者与消费者各自维护本地上下文，减少共享原子访问；</li><li>缓存行隔离降低伪共享；</li><li>acquire&#x2F;release 正确发布用户数据；</li><li>支持总容量与连续容量两类查询；</li><li><code>alloc/commit</code> 允许批量和增量提交；</li><li>等待协议通过双重检查避免丢失唤醒；</li><li>不绑定线程、条件变量或操作系统，可嵌入 Executor、RTOS 与裸机适配层。</li></ul><h3 id="30-2-限制"><a href="#30-2-限制" class="headerlink" title="30.2 限制"></a>30.2 限制</h3><ul><li>只支持一个逻辑生产者和一个逻辑消费者；</li><li>只管理索引，不管理数据内存和对象生命周期；</li><li>回调在对端提交线程同步执行，使用不当会阻塞或死锁；</li><li>取消等待存在短暂忙等；</li><li>对象体积明显大于简单读写索引环；</li><li><code>LELY_NO_ATOMICS</code> 不应被当作通用多线程实现；</li><li>上层仍需负责 shutdown、线程退出和缓冲区生命周期。</li></ul><hr><h2 id="31-最终心智模型"><a href="#31-最终心智模型" class="headerlink" title="31. 最终心智模型"></a>31. 最终心智模型</h2><p>可以把 <code>spscring</code> 理解为一个只有两把“所有权印章”的仓库：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line">用户数据数组</span><br><span class="line">= 仓库货架</span><br><span class="line"></span><br><span class="line">p_alloc</span><br><span class="line">= 生产者取得空货架的临时写权限</span><br><span class="line"></span><br><span class="line">p_commit</span><br><span class="line">= 生产者盖章，宣布货物已上架，可供消费者领取</span><br><span class="line"></span><br><span class="line">c_alloc</span><br><span class="line">= 消费者取得已上架货物的临时读权限</span><br><span class="line"></span><br><span class="line">c_commit</span><br><span class="line">= 消费者盖章，宣布货架已清空，可重新使用</span><br><span class="line"></span><br><span class="line">p.pos / c.pos</span><br><span class="line">= 双方发布给对端的所有权边界</span><br><span class="line"></span><br><span class="line">p.ctx / c.ctx</span><br><span class="line">= 双方自己的本地工作账本</span><br><span class="line"></span><br><span class="line">p.sig / c.sig</span><br><span class="line">= 空间或数据不足时登记的通知条件</span><br></pre></td></tr></table></figure>]]></content>
    
    
      
      
    <summary type="html">&lt;meta name=&quot;referrer&quot; content=&quot;no-referrer&quot; /&gt;

&lt;h1 id=&quot;Lely-CANopen-spscring-单生产者单消费者环形队列完整机制学习笔记&quot;&gt;&lt;a href=&quot;#Lely-CANopen-spscring-单生产者单消费者</summary>
      
    
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/categories/canopen/"/>
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/tags/canopen/"/>
    
  </entry>
  
  <entry>
    <title>Lely CANopen CAN 完整机制学习笔记</title>
    <link href="https://wdfk-prog.space/posts/a04ed79b/"/>
    <id>https://wdfk-prog.space/posts/a04ed79b/</id>
    <published>2026-07-29T01:33:36.000Z</published>
    <updated>2026-07-29T01:33:58.679Z</updated>
    
    <content type="html"><![CDATA[<meta name="referrer" content="no-referrer" /><h1 id="Lely-CANopen-IO2-CAN-与-SocketCAN-完整机制学习笔记"><a href="#Lely-CANopen-IO2-CAN-与-SocketCAN-完整机制学习笔记" class="headerlink" title="Lely CANopen IO2 CAN 与 SocketCAN 完整机制学习笔记"></a>Lely CANopen IO2 CAN 与 SocketCAN 完整机制学习笔记</h1><p><img src="https://i-blog.csdnimg.cn/direct/8f76fefc104c4b04ac3745aca06d13d5.png" alt="在这里插入图片描述"></p><p>@[toc]</p><hr><h2 id="1-CANopen、liblely-co、io2-与-SocketCAN-的边界"><a href="#1-CANopen、liblely-co、io2-与-SocketCAN-的边界" class="headerlink" title="1. CANopen、liblely-co、io2 与 SocketCAN 的边界"></a>1. CANopen、liblely-co、io2 与 SocketCAN 的边界</h2><p>Lely CANopen 协议栈本身是被动状态机。它不直接拥有 Linux socket，也不自行创建线程。Linux 上的实际 I&#x2F;O 由 <code>liblely-io2</code> 提供，任务调度由 <code>liblely-ev</code> 提供。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">flowchart TB</span><br><span class="line">    APP[业务代码 / CANopen Master] --&gt; CO[liblely-co / coapp]</span><br><span class="line">    CO --&gt; NET[CAN network / protocol state machine]</span><br><span class="line">    NET --&gt; IO2[liblely-io2 abstract CAN API]</span><br><span class="line">    IO2 --&gt; CHAN[Linux CanChannel]</span><br><span class="line">    CHAN --&gt; SOCK[SocketCAN raw socket]</span><br><span class="line">    SOCK --&gt; KERNEL[Linux CAN networking]</span><br><span class="line">    IO2 --&gt; EXEC[Executor]</span><br><span class="line">    EXEC --&gt; EVL[ev_loop]</span><br><span class="line">    EVL --&gt; POLL[epoll/poll backend]</span><br></pre></td></tr></table></figure><p>可以用一句话区分：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">CANopen 层决定“这是什么协议报文、状态机如何变化”；</span><br><span class="line">io2 层决定“报文怎样异步进入或离开 Linux CAN socket”。</span><br></pre></td></tr></table></figure><hr><h2 id="2-CanController-与-CanChannel-必须严格区分"><a href="#2-CanController-与-CanChannel-必须严格区分" class="headerlink" title="2. CanController 与 CanChannel 必须严格区分"></a>2. <code>CanController</code> 与 <code>CanChannel</code> 必须严格区分</h2><h3 id="2-1-CanController：网络接口控制面"><a href="#2-1-CanController：网络接口控制面" class="headerlink" title="2.1 CanController：网络接口控制面"></a>2.1 <code>CanController</code>：网络接口控制面</h3><p><code>CanController</code> 代表一个 Linux CAN 网络接口，例如 <code>can0</code>、<code>can1</code> 或 <code>vcan0</code>。它主要完成：</p><ul><li>名称与接口索引转换；</li><li>读取 CAN 接口能力标志；</li><li>查询或设置标称位率、CAN FD 数据位率；</li><li>查询总线状态；</li><li>将接口置为 UP 或 DOWN；</li><li>尝试设置接口发送队列长度。</li></ul><p>它不保存 CAN 报文收发队列，也不负责执行异步读写。</p><h3 id="2-2-CanChannel：数据面"><a href="#2-2-CanChannel：数据面" class="headerlink" title="2.2 CanChannel：数据面"></a>2.2 <code>CanChannel</code>：数据面</h3><p><code>CanChannel</code> 代表一个可执行读写操作的 CAN 通道。Linux 实现内部拥有：</p><ul><li>SocketCAN 文件描述符 <code>fd</code>；</li><li><code>io_poll_watch</code>；</li><li>三个内部任务 <code>rxbuf_task/read_task/write_task</code>；</li><li>用户态单生产者单消费者环形缓冲区；</li><li>待处理读队列；</li><li>待发送队列；</li><li>等待发送确认队列；</li><li>shutdown 与任务已投递状态位。</li></ul><h3 id="2-3-两者的对象关系"><a href="#2-3-两者的对象关系" class="headerlink" title="2.3 两者的对象关系"></a>2.3 两者的对象关系</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">CanController(&quot;can1&quot;)</span><br><span class="line">    └─ 指向 Linux 接口 can1</span><br><span class="line"></span><br><span class="line">CanChannel(poll, exec, 256, true)</span><br><span class="line">    ├─ 绑定 Poll 与 Executor</span><br><span class="line">    ├─ 创建 256 槽用户态接收队列</span><br><span class="line">    └─ 配置发送完成是否等待 MSG_CONFIRM</span><br><span class="line"></span><br><span class="line">channel.open(controller)</span><br><span class="line">    └─ 创建 socket，并 bind 到 controller 对应的 ifindex</span><br></pre></td></tr></table></figure><hr><h2 id="3-抽象-C-接口：两个虚函数表"><a href="#3-抽象-C-接口：两个虚函数表" class="headerlink" title="3. 抽象 C 接口：两个虚函数表"></a>3. 抽象 C 接口：两个虚函数表</h2><p><code>include/lely/io2/can.h</code> 定义两个抽象类型：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">io_can_ctrl_t  → CAN 控制器接口</span><br><span class="line">io_can_chan_t  → CAN 通道接口</span><br></pre></td></tr></table></figure><p>它们和 <code>ev_exec_t</code> 一样，都是 C 语言虚函数表对象。</p><h3 id="3-1-Controller-虚函数表"><a href="#3-1-Controller-虚函数表" class="headerlink" title="3.1 Controller 虚函数表"></a>3.1 Controller 虚函数表</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">stop</span><br><span class="line">stopped</span><br><span class="line">restart</span><br><span class="line">get_bitrate</span><br><span class="line">set_bitrate</span><br><span class="line">get_state</span><br></pre></td></tr></table></figure><h3 id="3-2-Channel-虚函数表"><a href="#3-2-Channel-虚函数表" class="headerlink" title="3.2 Channel 虚函数表"></a>3.2 Channel 虚函数表</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">get_dev</span><br><span class="line">get_flags</span><br><span class="line">read</span><br><span class="line">submit_read</span><br><span class="line">write</span><br><span class="line">submit_write</span><br></pre></td></tr></table></figure><p>其中：</p><ul><li><code>read/write</code> 是同步阻塞接口；</li><li><code>submit_read/submit_write</code> 是异步请求提交接口；</li><li><code>io_dev_t</code> 提供统一的 <code>cancel/abort/context/executor</code> 能力。</li></ul><h3 id="3-3-请求对象为什么内嵌-ev-task"><a href="#3-3-请求对象为什么内嵌-ev-task" class="headerlink" title="3.3 请求对象为什么内嵌 ev_task"></a>3.3 请求对象为什么内嵌 <code>ev_task</code></h3><p>异步读请求的逻辑布局：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">io_can_chan_read</span><br><span class="line">├─ msg      输出普通 CAN 帧</span><br><span class="line">├─ err      输出 CAN 错误帧</span><br><span class="line">├─ tp       输出接收时间</span><br><span class="line">├─ task     完成后投递给 Executor</span><br><span class="line">└─ result   result + errc</span><br></pre></td></tr></table></figure><p>异步写请求：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">io_can_chan_write</span><br><span class="line">├─ msg      待发送帧指针</span><br><span class="line">├─ task     完成后投递给 Executor</span><br><span class="line">└─ errc     完成状态</span><br></pre></td></tr></table></figure><p>这说明“异步操作”和“完成回调”不是两个独立分配的对象，而是一个请求对象内部直接携带完成任务。</p><hr><h2 id="4-C-包装层：RAII、异常与回调对象"><a href="#4-C-包装层：RAII、异常与回调对象" class="headerlink" title="4. C++ 包装层：RAII、异常与回调对象"></a>4. C++ 包装层：RAII、异常与回调对象</h2><h3 id="4-1-include-lely-io2-linux-can-hpp"><a href="#4-1-include-lely-io2-linux-can-hpp" class="headerlink" title="4.1 include/lely/io2/linux/can.hpp"></a>4.1 <code>include/lely/io2/linux/can.hpp</code></h3><p>Linux 平台类负责资源所有权：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">CanController 构造 → io_can_ctrl_create_*</span><br><span class="line">CanController 析构 → io_can_ctrl_destroy</span><br><span class="line"></span><br><span class="line">CanChannel 构造 → io_can_chan_create</span><br><span class="line">CanChannel 析构 → io_can_chan_destroy</span><br></pre></td></tr></table></figure><p>创建失败时，构造函数读取 Lely 错误码并抛出异常。</p><h3 id="4-2-include-lely-io2-can-hpp"><a href="#4-2-include-lely-io2-can-hpp" class="headerlink" title="4.2 include/lely/io2/can.hpp"></a>4.2 <code>include/lely/io2/can.hpp</code></h3><p>平台无关 C++ 层提供三种使用方式：</p><table><thead><tr><th>方式</th><th>API</th><th>完成模型</th></tr></thead><tbody><tr><td>同步</td><td><code>read()</code> &#x2F; <code>write()</code></td><td>当前线程阻塞</td></tr><tr><td>回调</td><td><code>submit_read()</code> &#x2F; <code>submit_write()</code></td><td>完成任务调用 callable</td></tr><tr><td>Future</td><td><code>async_read()</code> &#x2F; <code>async_write()</code></td><td>Promise ready 后由调用方等待或续接</td></tr></tbody></table><p>回调包装器会在完成回调结束后删除自身；显式 <code>CanChannelRead/CanChannelWrite</code> 对象则由调用方管理生命周期。</p><hr><h2 id="5-CanController-can1-的真实调用链"><a href="#5-CanController-can1-的真实调用链" class="headerlink" title="5. CanController(&quot;can1&quot;) 的真实调用链"></a>5. <code>CanController(&quot;can1&quot;)</code> 的真实调用链</h2><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">CanController(name)</span><br><span class="line">  → io_can_ctrl_create_from_name(name, txlen)</span><br><span class="line">  → if_nametoindex(name)</span><br><span class="line">  → io_can_ctrl_create_from_index(index, txlen)</span><br><span class="line">  → io_can_ctrl_init()</span><br><span class="line">      ├─ 保存 ifindex 与接口名</span><br><span class="line">      ├─ 尝试读取 CAN netlink 属性</span><br><span class="line">      ├─ 保存 CAN/CAN FD 能力标志</span><br><span class="line">      └─ 设置接口发送队列长度</span><br></pre></td></tr></table></figure><h3 id="5-1-默认发送队列长度"><a href="#5-1-默认发送队列长度" class="headerlink" title="5.1 默认发送队列长度"></a>5.1 默认发送队列长度</h3><p>源码默认值是 128 帧。构造 Controller 时会尝试保证接口使用指定的发送队列长度。这是一个容易忽略的副作用：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">构造 Controller</span><br><span class="line">≠ 单纯只读对象</span><br></pre></td></tr></table></figure><p>普通用户进程可能因缺少网络管理权限而失败。官方教程也专门提示了 <code>Operation not permitted</code> 与 <code>txqueuelen</code> 的关系。</p><p>生产环境应在服务启动前完成接口配置，例如由 systemd-networkd、NetworkManager、启动脚本或具备权限的初始化服务执行，而不是无条件依赖业务进程修改网络接口。</p><h3 id="5-2-get-bitrate"><a href="#5-2-get-bitrate" class="headerlink" title="5.2 get_bitrate()"></a>5.2 <code>get_bitrate()</code></h3><p>Linux 实现从 CAN netlink 属性读取：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">nominal bitrate → 仲裁阶段位率</span><br><span class="line">data bitrate    → CAN FD 数据阶段位率；经典 CAN 为 0</span><br></pre></td></tr></table></figure><p>需要注意：初始化阶段对“不支持 CAN 属性”的部分接口进行了容忍，但 <code>get_bitrate()</code> 本身若无法取得属性仍会返回错误。因此，以下代码是严格校验策略，而不是适用于所有 SocketCAN 类型的通用策略：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">controller-&gt;<span class="built_in">get_bitrate</span>(&amp;nominal_bitrate, &amp;data_bitrate);</span><br></pre></td></tr></table></figure><p>对 <code>vcan</code>、部分 SLCAN 或定制虚拟接口，应根据本地内核驱动能力决定是否启用位率校验。</p><h3 id="5-3-get-state"><a href="#5-3-get-state" class="headerlink" title="5.3 get_state()"></a>5.3 <code>get_state()</code></h3><p>Linux 实现先读取接口 flags：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">接口没有 IFF_UP → CAN_STATE_STOPPED</span><br><span class="line">接口为 UP       → 再读取 CAN netlink state</span><br></pre></td></tr></table></figure><p>因此 <code>STOPPED</code> 首先反映网络接口未启用。其他状态依赖驱动提供的 CAN 状态属性。</p><hr><h2 id="6-CanChannel-256-true-的真实含义"><a href="#6-CanChannel-256-true-的真实含义" class="headerlink" title="6. CanChannel(..., 256, true) 的真实含义"></a>6. <code>CanChannel(..., 256, true)</code> 的真实含义</h2><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">impl_-&gt;channel.<span class="built_in">reset</span>(</span><br><span class="line">    <span class="keyword">new</span> lely::io::<span class="built_in">CanChannel</span>(*impl_-&gt;poll, *impl_-&gt;executor, <span class="number">256</span>, <span class="literal">true</span>));</span><br></pre></td></tr></table></figure><p>四个参数分别是：</p><table><thead><tr><th>参数</th><th>含义</th></tr></thead><tbody><tr><td><code>poll</code></td><td>监听 SocketCAN fd 的 I&#x2F;O readiness</td></tr><tr><td><code>executor</code></td><td>执行内部 I&#x2F;O 任务与完成任务</td></tr><tr><td><code>256</code></td><td>Lely 用户态接收环形队列的槽数</td></tr><tr><td><code>true</code></td><td>启用发送确认等待 <code>txwait</code></td></tr></tbody></table><h3 id="6-1-256-不是什么"><a href="#6-1-256-不是什么" class="headerlink" title="6.1 256 不是什么"></a>6.1 <code>256</code> 不是什么</h3><p>它不是：</p><ul><li>Linux <code>SO_RCVBUF</code> 字节数；</li><li>CAN 控制器硬件 FIFO 深度；</li><li>CANopen PDO 数量；</li><li>一次事件循环最多处理的报文数；</li><li>发送队列长度。</li></ul><p>它控制的是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">struct io_can_frame rxbuf[256]</span><br></pre></td></tr></table></figure><p>以及配套 <code>spscring</code> 的逻辑容量。</p><h3 id="6-2-txwait-true-的含义"><a href="#6-2-txwait-true-的含义" class="headerlink" title="6.2 txwait=true 的含义"></a>6.2 <code>txwait=true</code> 的含义</h3><p>开启后，Lely 会：</p><ol><li>启用 SocketCAN 本地回环；</li><li>启用接收本 socket 自己发送的帧；</li><li><code>sendmsg()</code> 成功后把写请求放入 <code>confirm_queue</code>；</li><li>等收到带 <code>MSG_CONFIRM</code> 的回送帧；</li><li>匹配成功后才完成异步写请求。</li></ol><p>因此：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">async_write Future ready</span><br><span class="line">≠ sendmsg() 已把帧交给内核</span><br><span class="line"></span><br><span class="line">async_write Future ready 且 txwait=true</span><br><span class="line">≈ SocketCAN 已回送该帧的发送确认</span><br></pre></td></tr></table></figure><p>若 <code>txwait=false</code>，成功 <code>sendmsg()</code> 后即可完成写请求，吞吐更高，但完成语义更弱。</p><hr><h2 id="7-CanChannel-open-：SocketCAN-socket-的创建过程"><a href="#7-CanChannel-open-：SocketCAN-socket-的创建过程" class="headerlink" title="7. CanChannel::open()：SocketCAN socket 的创建过程"></a>7. <code>CanChannel::open()</code>：SocketCAN socket 的创建过程</h2><p>调用链：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">CanChannel::open(controller)</span><br><span class="line">  → io_can_chan_open()</span><br><span class="line">      ├─ 校验请求 flags</span><br><span class="line">      ├─ socket(AF_CAN, SOCK_RAW | SOCK_CLOEXEC, CAN_RAW)</span><br><span class="line">      ├─ bind(sockaddr_can&#123;ifindex&#125;)</span><br><span class="line">      ├─ 可选启用错误帧过滤</span><br><span class="line">      ├─ 可选启用 CAN FD</span><br><span class="line">      ├─ 配置 loopback / own messages / send buffer</span><br><span class="line">      └─ io_can_chan_impl_set_fd()</span><br></pre></td></tr></table></figure><h3 id="7-1-默认打开-flags"><a href="#7-1-默认打开-flags" class="headerlink" title="7.1 默认打开 flags"></a>7.1 默认打开 flags</h3><p>用户代码调用：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">channel-&gt;<span class="built_in">open</span>(*controller);</span><br></pre></td></tr></table></figure><p>未显式传入 <code>CanBusFlag</code>，因此默认是 <code>NONE</code>。这意味着：</p><ul><li>不主动订阅 SocketCAN 错误帧；</li><li>不启用 CAN FD；</li><li>不启用 BRS。</li></ul><p><code>txwait</code> 与 <code>CanBusFlag</code> 是两组不同概念：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">CanBusFlag → 总线报文能力和错误帧接收</span><br><span class="line"></span><br><span class="line">txwait     → 写操作完成时是否等待本地发送确认</span><br></pre></td></tr></table></figure><h3 id="7-2-替换-fd-时的行为"><a href="#7-2-替换-fd-时的行为" class="headerlink" title="7.2 替换 fd 时的行为"></a>7.2 替换 fd 时的行为</h3><p><code>open()</code>、<code>assign()</code>、<code>release()</code> 最终都经过 <code>io_can_chan_impl_set_fd()</code>。替换 fd 时会：</p><ul><li>停止原 fd 的 Poll 监听；</li><li>中止环形队列等待；</li><li>清空用户态接收队列；</li><li>交换新旧 fd；</li><li>取消所有 pending read&#x2F;write&#x2F;confirmation 请求。</li></ul><p>所以 <code>open()</code> 不是无副作用的重复操作。对已经有 pending I&#x2F;O 的 channel 再次 open，会导致这些操作以取消状态完成。</p><hr><h2 id="8-io-can-chan-impl：Poll-watch、三个内部任务与私有队列"><a href="#8-io-can-chan-impl：Poll-watch、三个内部任务与私有队列" class="headerlink" title="8. io_can_chan_impl：Poll watch、三个内部任务与私有队列"></a>8. <code>io_can_chan_impl</code>：Poll watch、三个内部任务与私有队列</h2><p><code>io_can_chan_impl</code> 不是一个简单的 socket 包装器。它把 SocketCAN fd、Poll 监听对象、Executor 任务、接收环形缓冲区和三类请求队列组合成一个异步状态机。</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">io_can_chan_impl</span><br><span class="line">├─ poll / ctx / exec</span><br><span class="line">├─ txwait</span><br><span class="line">├─ watch</span><br><span class="line">├─ rxbuf_task</span><br><span class="line">├─ read_task</span><br><span class="line">├─ write_task</span><br><span class="line">├─ rxring + rxbuf</span><br><span class="line">├─ fd / flags / events</span><br><span class="line">├─ shutdown</span><br><span class="line">├─ rxbuf_posted / read_posted / write_posted</span><br><span class="line">├─ read_queue</span><br><span class="line">├─ write_queue</span><br><span class="line">├─ confirm_queue</span><br><span class="line">└─ current_write</span><br></pre></td></tr></table></figure><h3 id="8-1-初始化代码实际建立了什么"><a href="#8-1-初始化代码实际建立了什么" class="headerlink" title="8.1 初始化代码实际建立了什么"></a>8.1 初始化代码实际建立了什么</h3><p>源码初始化：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">impl-&gt;watch = (<span class="keyword">struct</span> io_poll_watch)IO_POLL_WATCH_INIT(</span><br><span class="line">        &amp;io_can_chan_impl_watch_func);</span><br><span class="line"></span><br><span class="line">impl-&gt;rxbuf_task = (<span class="keyword">struct</span> ev_task)EV_TASK_INIT(</span><br><span class="line">        impl-&gt;exec, &amp;io_can_chan_impl_rxbuf_task_func);</span><br><span class="line">impl-&gt;read_task = (<span class="keyword">struct</span> ev_task)EV_TASK_INIT(</span><br><span class="line">        impl-&gt;exec, &amp;io_can_chan_impl_read_task_func);</span><br><span class="line">impl-&gt;write_task = (<span class="keyword">struct</span> ev_task)EV_TASK_INIT(</span><br><span class="line">        impl-&gt;exec, &amp;io_can_chan_impl_write_task_func);</span><br></pre></td></tr></table></figure><p>这段代码只完成“对象与回调入口绑定”，不会立刻监听 fd，也不会立刻执行任务。</p><table><thead><tr><th>对象</th><th>类型</th><th>初始化后绑定的入口</th><th>谁触发它</th><th>主要职责</th></tr></thead><tbody><tr><td><code>watch</code></td><td><code>io_poll_watch</code></td><td><code>io_can_chan_impl_watch_func()</code></td><td>Poll 后端检测到 <code>IN/OUT/ERR</code></td><td>把 readiness 转换成内部任务投递</td></tr><tr><td><code>rxbuf_task</code></td><td><code>ev_task</code></td><td><code>io_can_chan_impl_rxbuf_task_func()</code></td><td>首次读、等待确认、Poll 可读事件</td><td><code>recvmsg()</code>、填充 <code>rxring</code>、处理 <code>MSG_CONFIRM</code></td></tr><tr><td><code>read_task</code></td><td><code>ev_task</code></td><td><code>io_can_chan_impl_read_task_func()</code></td><td><code>submit_read()</code> 或 <code>rxring</code> 产生数据</td><td>用接收帧满足 <code>read_queue</code></td></tr><tr><td><code>write_task</code></td><td><code>ev_task</code></td><td><code>io_can_chan_impl_write_task_func()</code></td><td><code>submit_write()</code> 或 Poll 可写事件</td><td><code>sendmsg()</code>、重试阻塞写、进入确认队列</td></tr></tbody></table><p><code>IO_POLL_WATCH_INIT()</code> 会初始化整个 watch 记录：写入回调函数，将内部 fd 置为 <code>-1</code>，初始化红黑树节点，并把内部事件掩码清零。真正把目标 fd、事件掩码和 <code>watch</code> 注册到 Poll 后端的是后续的：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">io_poll_watch(impl-&gt;poll, impl-&gt;fd, events, &amp;impl-&gt;watch);</span><br></pre></td></tr></table></figure><p><code>EV_TASK_INIT(exec, func)</code> 则把任务固定绑定到同一个 Executor，并设置执行函数。任务只有在调用 <code>ev_exec_post()</code> 后才进入 Executor&#x2F;event loop 的可执行队列。Poll 接口采用一次通知、再次注册的语义：一次注册在事件发生并回调后即被消费；若仍需接收后续事件，channel 必须再次调用 <code>io_poll_watch()</code> 注册。<code>watch_func()</code> 因此会从 <code>impl-&gt;events</code> 中移除本次已处理事件，并重新注册尚未处理的事件掩码。</p><h3 id="8-2-四个回调载体的连接关系"><a href="#8-2-四个回调载体的连接关系" class="headerlink" title="8.2 四个回调载体的连接关系"></a>8.2 四个回调载体的连接关系</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    Sock[&quot;SocketCAN fd&quot;] --&gt; Poller[&quot;io_poll_t / epoll backend&quot;]</span><br><span class="line">    Poller --&gt; Watch[&quot;watch.func&lt;br/&gt;io_can_chan_impl_watch_func&quot;]</span><br><span class="line">    Watch --&gt;|&quot;IO_EVENT_IN / ERR&quot;| RxTask[&quot;rxbuf_task&quot;]</span><br><span class="line">    Watch --&gt;|&quot;IO_EVENT_OUT / ERR&quot;| WrTask[&quot;write_task&quot;]</span><br><span class="line"></span><br><span class="line">    RdReq[&quot;read_queue&quot;] --&gt; RdTask[&quot;read_task&quot;]</span><br><span class="line">    RdTask --&gt;|&quot;rxring empty&quot;| RxTask</span><br><span class="line">    RxTask --&gt; Ring[&quot;rxring + rxbuf&quot;]</span><br><span class="line">    Ring --&gt;|&quot;consumer signal&quot;| RdTask</span><br><span class="line"></span><br><span class="line">    WrReq[&quot;write_queue&quot;] --&gt; WrTask</span><br><span class="line">    WrTask --&gt;|&quot;txwait = true&quot;| Confirm[&quot;confirm_queue&quot;]</span><br><span class="line">    Confirm --&gt; RxTask</span><br><span class="line"></span><br><span class="line">    RxTask --&gt; Exec[&quot;Executor&quot;]</span><br><span class="line">    RdTask --&gt; Exec</span><br><span class="line">    WrTask --&gt; Exec</span><br></pre></td></tr></table></figure><p>图中最重要的边界是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">watch 负责“事件转任务”；</span><br><span class="line">三个 ev_task 负责“在 Executor 上推进状态机”；</span><br><span class="line">用户请求 task 负责“完成通知与 Future/回调”。</span><br></pre></td></tr></table></figure><h3 id="8-3-三个内部任务不是三个线程"><a href="#8-3-三个内部任务不是三个线程" class="headerlink" title="8.3 三个内部任务不是三个线程"></a>8.3 三个内部任务不是三个线程</h3><p>三个 <code>ev_task</code> 都绑定到 <code>impl-&gt;exec</code>，它们只是可重复投递的工作对象：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">rxbuf_task.func = io_can_chan_impl_rxbuf_task_func</span><br><span class="line">read_task.func  = io_can_chan_impl_read_task_func</span><br><span class="line">write_task.func = io_can_chan_impl_write_task_func</span><br></pre></td></tr></table></figure><p>实际在哪个 OS 线程执行，取决于哪个线程正在驱动对应的 <code>ev_loop</code>。若多个 worker 同时运行同一个 loop，任务可以由不同 worker 取出；channel 通过 mutex、队列状态和 posted 标志保护内部状态。</p><h3 id="8-4-posted-标志保证内部任务不会重复入队"><a href="#8-4-posted-标志保证内部任务不会重复入队" class="headerlink" title="8.4 posted 标志保证内部任务不会重复入队"></a>8.4 posted 标志保证内部任务不会重复入队</h3><table><thead><tr><th>标志</th><th>对应任务</th><th>置 1 的典型位置</th><th>清零或更新位置</th></tr></thead><tbody><tr><td><code>rxbuf_posted</code></td><td><code>rxbuf_task</code></td><td>需要读取 socket 或等待确认时</td><td><code>rxbuf_task</code> 结束前根据后续工作更新</td></tr><tr><td><code>read_posted</code></td><td><code>read_task</code></td><td>首个 read 请求入队、ring consumer 被唤醒时</td><td><code>read_task</code> 结束前更新</td></tr><tr><td><code>write_posted</code></td><td><code>write_task</code></td><td>首个 write 请求入队、fd 可写时</td><td><code>write_task</code> 结束前更新</td></tr></tbody></table><p>同一个 <code>ev_task</code> 内嵌同一个侵入式链表节点，因此不能在尚未出队时再次 <code>post</code>。posted 标志就是每个内部任务的“单实例在途锁”。</p><h3 id="8-5-请求队列、内部任务和-Poll-的统一状态机"><a href="#8-5-请求队列、内部任务和-Poll-的统一状态机" class="headerlink" title="8.5 请求队列、内部任务和 Poll 的统一状态机"></a>8.5 请求队列、内部任务和 Poll 的统一状态机</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    Init[&quot;初始化 watch 与三个 ev_task&quot;] --&gt; Idle[&quot;等待业务请求&quot;]</span><br><span class="line"></span><br><span class="line">    Idle --&gt;|&quot;submit_read&quot;| ReadQ[&quot;请求进入 read_queue&quot;]</span><br><span class="line">    ReadQ --&gt; PostRead[&quot;post read_task&quot;]</span><br><span class="line">    PostRead --&gt; RunRead[&quot;read_task 检查 rxring&quot;]</span><br><span class="line">    RunRead --&gt;|&quot;有帧&quot;| CompleteRead[&quot;完成 read 请求&quot;]</span><br><span class="line">    RunRead --&gt;|&quot;无帧&quot;| NeedRx[&quot;post rxbuf_task&quot;]</span><br><span class="line"></span><br><span class="line">    NeedRx --&gt; RunRx[&quot;rxbuf_task 调用 recvmsg&quot;]</span><br><span class="line">    RunRx --&gt;|&quot;收到普通帧&quot;| CommitRing[&quot;提交到 rxring&quot;]</span><br><span class="line">    CommitRing --&gt; SignalRead[&quot;consumer signal&quot;]</span><br><span class="line">    SignalRead --&gt; PostRead</span><br><span class="line">    RunRx --&gt;|&quot;EAGAIN&quot;| WatchIn[&quot;注册 IO_EVENT_IN&quot;]</span><br><span class="line"></span><br><span class="line">    Idle --&gt;|&quot;submit_write&quot;| WriteQ[&quot;请求进入 write_queue&quot;]</span><br><span class="line">    WriteQ --&gt; PostWrite[&quot;post write_task&quot;]</span><br><span class="line">    PostWrite --&gt; RunWrite[&quot;write_task 调用 sendmsg&quot;]</span><br><span class="line">    RunWrite --&gt;|&quot;EAGAIN&quot;| WatchOut[&quot;注册 IO_EVENT_OUT&quot;]</span><br><span class="line">    RunWrite --&gt;|&quot;成功且 txwait=false&quot;| CompleteWrite[&quot;完成 write 请求&quot;]</span><br><span class="line">    RunWrite --&gt;|&quot;成功且 txwait=true&quot;| ConfirmQ[&quot;请求进入 confirm_queue&quot;]</span><br><span class="line">    ConfirmQ --&gt; NeedRx</span><br><span class="line">    RunRx --&gt;|&quot;MSG_CONFIRM&quot;| MatchConfirm[&quot;匹配 confirm_queue&quot;]</span><br><span class="line">    MatchConfirm --&gt; CompleteWrite</span><br><span class="line"></span><br><span class="line">    WatchIn --&gt; PollWait[&quot;event loop 进入 Poll 等待&quot;]</span><br><span class="line">    WatchOut --&gt; PollWait</span><br><span class="line">    PollWait --&gt; Ready[&quot;fd readiness&quot;]</span><br><span class="line">    Ready --&gt; WatchFunc[&quot;watch_func&quot;]</span><br><span class="line">    WatchFunc --&gt;|&quot;IN / ERR&quot;| NeedRx</span><br><span class="line">    WatchFunc --&gt;|&quot;OUT / ERR&quot;| PostWrite</span><br></pre></td></tr></table></figure><h3 id="8-6-为什么内部任务没有单独调用-on-task-init"><a href="#8-6-为什么内部任务没有单独调用-on-task-init" class="headerlink" title="8.6 为什么内部任务没有单独调用 on_task_init()"></a>8.6 为什么内部任务没有单独调用 <code>on_task_init()</code></h3><p><code>submit_read()</code> 和 <code>submit_write()</code> 会对“用户请求自身的 completion task”调用 <code>ev_exec_on_task_init()</code>。请求随后可能停留在 <code>read_queue</code>、<code>write_queue</code>、<code>confirm_queue</code> 或系统调用过程中，这个 outstanding-work 引用保证 event loop 在等待外部 CAN 事件时不会因全局任务队列暂时为空而退出。</p><p>三个内部任务是推进这些请求的执行工具；它们通过普通 <code>ev_exec_post()</code> 投递，不另行持有长期 work 引用。完成请求时，辅助函数遵守：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">先 post 用户 completion task</span><br><span class="line">后 on_task_fini 释放请求的 work 引用</span><br></pre></td></tr></table></figure><p>因此从提交到完成，请求始终满足至少一个条件：位于私有队列、正在执行、已经进入 Executor 队列，或由 outstanding-work 引用保活。</p><hr><h2 id="9-异步读完整机制"><a href="#9-异步读完整机制" class="headerlink" title="9. 异步读完整机制"></a>9. 异步读完整机制</h2><h3 id="9-1-提交阶段"><a href="#9-1-提交阶段" class="headerlink" title="9.1 提交阶段"></a>9.1 提交阶段</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">submit_read(request)</span><br><span class="line">  ├─ 若 request.task.exec 为空，绑定 channel 默认 executor</span><br><span class="line">  ├─ ev_exec_on_task_init(exec)</span><br><span class="line">  ├─ request 加入 read_queue</span><br><span class="line">  └─ 必要时 post 内部 read_task</span><br></pre></td></tr></table></figure><p>这里的 <code>on_task_init()</code> 非常关键：用户读请求已经进入 channel 私有队列，但尚未进入 event loop 的可执行队列。它必须用 outstanding work 引用保持 loop 存活。</p><h3 id="9-2-read-task-阶段"><a href="#9-2-read-task-阶段" class="headerlink" title="9.2 read_task 阶段"></a>9.2 <code>read_task</code> 阶段</h3><p><code>read_task</code> 尝试从 <code>rxring</code> 取帧：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">rxring 有帧</span><br><span class="line">  → 填充 read-&gt;msg / read-&gt;err / read-&gt;tp</span><br><span class="line">  → 设置 result</span><br><span class="line">  → 请求移到局部完成队列</span><br><span class="line"></span><br><span class="line">rxring 无帧</span><br><span class="line">  → 在 spscring 注册 consumer wait</span><br><span class="line">  → 确保 rxbuf_task 或 Poll 正在等待 fd 可读</span><br></pre></td></tr></table></figure><h3 id="9-3-Poll-与-rxbuf-task"><a href="#9-3-Poll-与-rxbuf-task" class="headerlink" title="9.3 Poll 与 rxbuf_task"></a>9.3 Poll 与 <code>rxbuf_task</code></h3><p>若 SocketCAN 非阻塞读取返回 <code>EAGAIN/EWOULDBLOCK</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">rxbuf_task 不持续忙轮询</span><br><span class="line">  → io_poll_watch(fd, IO_EVENT_IN)</span><br><span class="line">  → event loop 进入 Poll</span><br><span class="line">  → fd 可读时 watch_func 被调用</span><br><span class="line">  → watch_func post rxbuf_task</span><br></pre></td></tr></table></figure><p><code>watch_func</code> 本身只做轻量状态更新和任务投递，不在 Poll 回调中直接执行完整读循环。</p><h3 id="9-4-rxbuf-task-对两类帧的分流"><a href="#9-4-rxbuf-task-对两类帧的分流" class="headerlink" title="9.4 rxbuf_task 对两类帧的分流"></a>9.4 <code>rxbuf_task</code> 对两类帧的分流</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">普通接收帧</span><br><span class="line">  → 写入 rxring</span><br><span class="line">  → 后续由 read_task 交给用户读请求</span><br><span class="line"></span><br><span class="line">MSG_CONFIRM 帧</span><br><span class="line">  → 不进入普通接收 ring</span><br><span class="line">  → 用于完成 confirm_queue 中的写请求</span><br></pre></td></tr></table></figure><h3 id="9-5-完成阶段"><a href="#9-5-完成阶段" class="headerlink" title="9.5 完成阶段"></a>9.5 完成阶段</h3><p>完成的 read 请求先移入局部队列，解锁后统一执行：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">ev_task_queue_post(queue)</span><br><span class="line">  → ev_exec_post(request.task)</span><br><span class="line">  → ev_exec_on_task_fini(request.task.exec)</span><br></pre></td></tr></table></figure><p>这正是前一篇 Executor&#x2F;Future 笔记中的“先 post，后 fini”。</p><hr><h2 id="10-异步写完整机制"><a href="#10-异步写完整机制" class="headerlink" title="10. 异步写完整机制"></a>10. 异步写完整机制</h2><h3 id="10-1-提交阶段"><a href="#10-1-提交阶段" class="headerlink" title="10.1 提交阶段"></a>10.1 提交阶段</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">submit_write(request)</span><br><span class="line">  ├─ 校验 CAN FD/BRS flags 是否被 channel 支持</span><br><span class="line">  ├─ 绑定默认 executor</span><br><span class="line">  ├─ on_task_init()</span><br><span class="line">  ├─ 加入 write_queue</span><br><span class="line">  └─ 必要时 post write_task</span><br></pre></td></tr></table></figure><h3 id="10-2-write-task-发送阶段"><a href="#10-2-write-task-发送阶段" class="headerlink" title="10.2 write_task 发送阶段"></a>10.2 <code>write_task</code> 发送阶段</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">write_queue 首元素</span><br><span class="line">  → 转换 Lely can_msg 为 Linux can_frame/canfd_frame</span><br><span class="line">  → sendmsg()</span><br></pre></td></tr></table></figure><p>结果有三类：</p><table><thead><tr><th>结果</th><th>行为</th></tr></thead><tbody><tr><td>成功，<code>txwait=false</code></td><td>立即完成请求</td></tr><tr><td>成功，<code>txwait=true</code></td><td>请求进入 <code>confirm_queue</code></td></tr><tr><td><code>EAGAIN/EWOULDBLOCK</code></td><td>放回 write_queue 头部，监听 <code>IO_EVENT_OUT</code></td></tr><tr><td>其他错误</td><td>以对应 errno 完成请求</td></tr></tbody></table><h3 id="10-3-为什么设置最小发送缓冲区"><a href="#10-3-为什么设置最小发送缓冲区" class="headerlink" title="10.3 为什么设置最小发送缓冲区"></a>10.3 为什么设置最小发送缓冲区</h3><p>channel 初始化 socket 时会把 <code>SO_SNDBUF</code> 设为最小值。源码意图是让发送压力表现为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">阻塞或 EAGAIN</span><br></pre></td></tr></table></figure><p>而不是直接返回 <code>ENOBUFS</code>。这样异步写状态机可以把请求留在队列中，并等待 <code>IO_EVENT_OUT</code> 后重试。</p><h3 id="10-4-写确认匹配"><a href="#10-4-写确认匹配" class="headerlink" title="10.4 写确认匹配"></a>10.4 写确认匹配</h3><p>发送确认使用 <code>can_msg_cmp()</code> 比较：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">id</span><br><span class="line">flags</span><br><span class="line">length</span><br><span class="line">data bytes</span><br></pre></td></tr></table></figure><p>收到 <code>MSG_CONFIRM</code> 后，在 <code>confirm_queue</code> 中查找首个完全相等的写请求。</p><p>匹配到某个请求时：</p><ul><li>该请求标记成功；</li><li>它之前仍未确认的请求标记 <code>EIO</code>；</li><li>完成任务统一投递给 Executor。</li></ul><p>这一实现隐含了发送确认顺序假设。若调试中出现偶发 <code>EIO</code>，应同时检查：</p><ul><li>是否重复发送完全相同的帧；</li><li>SocketCAN own-message 的顺序；</li><li>channel 是否在发送期间被 reopen&#x2F;release；</li><li>是否关闭了 <code>txwait</code> 或改变了 socket 选项；</li><li>是否存在并发 cancel。</li></ul><hr><h2 id="11-CAN-帧转换层"><a href="#11-CAN-帧转换层" class="headerlink" title="11. CAN 帧转换层"></a>11. CAN 帧转换层</h2><p><code>src/io2/linux/can_msg.h</code> 在 Lely 与 Linux 结构之间转换。</p><h3 id="11-1-经典-CAN"><a href="#11-1-经典-CAN" class="headerlink" title="11.1 经典 CAN"></a>11.1 经典 CAN</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">Lely CAN_FLAG_IDE ↔ Linux CAN_EFF_FLAG</span><br><span class="line">Lely CAN_FLAG_RTR ↔ Linux CAN_RTR_FLAG</span><br><span class="line">11 位 ID          ↔ CAN_SFF_MASK</span><br><span class="line">29 位 ID          ↔ CAN_EFF_MASK</span><br><span class="line">长度上限          ↔ CAN_MAX_LEN</span><br></pre></td></tr></table></figure><p>RTR 帧只携带 DLC，不复制数据区。</p><h3 id="11-2-CAN-FD"><a href="#11-2-CAN-FD" class="headerlink" title="11.2 CAN FD"></a>11.2 CAN FD</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">CAN_FLAG_FDF ↔ canfd_frame</span><br><span class="line">CAN_FLAG_BRS ↔ CANFD_BRS</span><br><span class="line">CAN_FLAG_ESI ↔ CANFD_ESI</span><br><span class="line">长度上限     ↔ CANFD_MAX_LEN</span><br></pre></td></tr></table></figure><p>经典 CAN 转换函数会拒绝带 <code>FDF</code> 标志的报文；CAN FD 转换函数也会拒绝没有 <code>FDF</code> 标志的报文。</p><h3 id="11-3-错误帧独立处理"><a href="#11-3-错误帧独立处理" class="headerlink" title="11.3 错误帧独立处理"></a>11.3 错误帧独立处理</h3><p>Linux <code>CAN_ERR_FLAG</code> 报文不会被普通 <code>can_frame2can_msg()</code> 接受，而是走单独的 <code>can_frame2can_err()</code> 路径。是否能收到错误帧取决于 channel open 时是否启用 <code>CanBusFlag::ERR</code>。</p><hr><h2 id="12-Future-怎样接到-CAN-channel-与-event-loop"><a href="#12-Future-怎样接到-CAN-channel-与-event-loop" class="headerlink" title="12. Future 怎样接到 CAN channel 与 event loop"></a>12. Future 怎样接到 CAN channel 与 event loop</h2><p><code>src/io2/can.c</code> 使用 Promise 的内部数据区保存完整异步请求：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Promise allocation</span><br><span class="line">├─ promise pointer</span><br><span class="line">└─ io_can_chan_read 或 io_can_chan_write</span><br></pre></td></tr></table></figure><h3 id="12-1-async-read"><a href="#12-1-async-read" class="headerlink" title="12.1 async_read()"></a>12.1 <code>async_read()</code></h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">io_can_chan_async_read()</span><br><span class="line">  → ev_promise_create()</span><br><span class="line">  → 从 promise 数据区取得 async_read 对象</span><br><span class="line">  → IO_CAN_CHAN_READ_INIT 初始化 read request 与 completion task</span><br><span class="line">  → io_can_chan_submit_read()</span><br><span class="line">  → 返回 future</span><br></pre></td></tr></table></figure><p>读请求的 completion task 最终执行：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">io_can_chan_async_read_func(task)</span><br><span class="line">  → 从 task 反查 io_can_chan_read</span><br><span class="line">  → 从 read 反查 io_can_chan_async_read</span><br><span class="line">  → ev_promise_set(promise, &amp;read-&gt;r)</span><br><span class="line">  → ev_promise_release(promise)</span><br></pre></td></tr></table></figure><p>C++ 返回类型是：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ev::Future&lt;<span class="type">int</span>, <span class="type">int</span>&gt;</span><br></pre></td></tr></table></figure><p>结果含义：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">result = 1   收到普通 CAN 帧</span><br><span class="line">result = 0   收到 CAN 错误帧</span><br><span class="line">result = -1  失败或取消，错误号位于 errc</span><br></pre></td></tr></table></figure><h3 id="12-2-async-write"><a href="#12-2-async-write" class="headerlink" title="12.2 async_write()"></a>12.2 <code>async_write()</code></h3><p>写路径与读路径使用相同的 Promise&#x2F;Future 包装模式：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">io_can_chan_async_write()</span><br><span class="line">  → 创建 promise/future</span><br><span class="line">  → 初始化 io_can_chan_write 与 completion task</span><br><span class="line">  → io_can_chan_submit_write()</span><br><span class="line">  → 返回 future</span><br></pre></td></tr></table></figure><p>完成任务运行时：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">io_can_chan_async_write_func(task)</span><br><span class="line">  → 从 task 反查 io_can_chan_write</span><br><span class="line">  → ev_promise_set(promise, &amp;write-&gt;errc)</span><br><span class="line">  → ev_promise_release(promise)</span><br></pre></td></tr></table></figure><p>C++ 返回类型是：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ev::Future&lt;<span class="type">void</span>, <span class="type">int</span>&gt;</span><br></pre></td></tr></table></figure><p>当 <code>txwait=true</code> 时，Future 只有在匹配到 SocketCAN <code>MSG_CONFIRM</code> 后才 ready；当 <code>txwait=false</code> 时，<code>sendmsg()</code> 成功即可进入完成路径。</p><h3 id="12-3-与-event-loop-的完整连接"><a href="#12-3-与-event-loop-的完整连接" class="headerlink" title="12.3 与 event loop 的完整连接"></a>12.3 与 event loop 的完整连接</h3><p>下面的时序图使用 <code>Evl</code> 作为 Mermaid participant 标识，避免使用保留关键字 <code>loop</code> 导致解析错误。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant App as Application</span><br><span class="line">    participant Chan as CanChannel</span><br><span class="line">    participant Exec as Executor</span><br><span class="line">    participant Evl as ev_loop</span><br><span class="line">    participant Poller as io_poll</span><br><span class="line">    participant Sock as SocketCAN fd</span><br><span class="line">    participant Done as Completion task</span><br><span class="line">    participant Prm as Promise/Future</span><br><span class="line"></span><br><span class="line">    App-&gt;&gt;Chan: async_read() or async_write()</span><br><span class="line">    Chan-&gt;&gt;Exec: on_task_init(request task)</span><br><span class="line">    Chan-&gt;&gt;Chan: enqueue request in private queue</span><br><span class="line">    Chan-&gt;&gt;Exec: post internal task</span><br><span class="line">    Exec-&gt;&gt;Evl: enqueue internal task</span><br><span class="line">    Evl-&gt;&gt;Exec: run internal task</span><br><span class="line">    Exec-&gt;&gt;Sock: recvmsg() or sendmsg()</span><br><span class="line"></span><br><span class="line">    opt EAGAIN or EWOULDBLOCK</span><br><span class="line">        Exec-&gt;&gt;Poller: io_poll_watch(fd, IN or OUT, watch)</span><br><span class="line">        Exec--&gt;&gt;Evl: internal task returns</span><br><span class="line">        Evl-&gt;&gt;Poller: wait for I/O readiness</span><br><span class="line">        Sock--&gt;&gt;Poller: fd becomes ready</span><br><span class="line">        Poller-&gt;&gt;Chan: watch_func(watch, events)</span><br><span class="line">        Chan-&gt;&gt;Exec: post rxbuf_task or write_task</span><br><span class="line">        Exec-&gt;&gt;Evl: enqueue internal task again</span><br><span class="line">        Evl-&gt;&gt;Exec: run internal task again</span><br><span class="line">        Exec-&gt;&gt;Sock: retry recvmsg() or sendmsg()</span><br><span class="line">    end</span><br><span class="line"></span><br><span class="line">    Sock--&gt;&gt;Exec: frame, error, or confirmation</span><br><span class="line">    Exec-&gt;&gt;Chan: update ring and request queues</span><br><span class="line">    Chan-&gt;&gt;Exec: post completion task</span><br><span class="line">    Chan-&gt;&gt;Exec: on_task_fini(request task)</span><br><span class="line">    Exec-&gt;&gt;Evl: enqueue completion task</span><br><span class="line">    Evl-&gt;&gt;Done: run completion task</span><br><span class="line">    Done-&gt;&gt;Prm: ev_promise_set()</span><br><span class="line">    Prm--&gt;&gt;App: Future becomes ready</span><br></pre></td></tr></table></figure><p>这里需要把“事件循环”和“Poll”分开理解：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">Executor / ev_loop：运行 ev_task；</span><br><span class="line">Poll：等待 fd readiness 并调用 watch 回调；</span><br><span class="line">watch_func：只把 readiness 转换成新的内部 task；</span><br><span class="line">内部 task：执行 recvmsg/sendmsg 并推进队列状态；</span><br><span class="line">completion task：设置结果、回调用户代码或完成 Promise。</span><br></pre></td></tr></table></figure><h3 id="12-4-event-loop-为什么能在等待-CAN-帧时保持运行"><a href="#12-4-event-loop-为什么能在等待-CAN-帧时保持运行" class="headerlink" title="12.4 event loop 为什么能在等待 CAN 帧时保持运行"></a>12.4 event loop 为什么能在等待 CAN 帧时保持运行</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    Submit[&quot;提交异步请求&quot;] --&gt; Guard[&quot;on_task_init 增加 outstanding work&quot;]</span><br><span class="line">    Guard --&gt; PrivateQ[&quot;请求停留在 channel 私有队列&quot;]</span><br><span class="line">    PrivateQ --&gt; Internal[&quot;内部 task 尝试 I/O&quot;]</span><br><span class="line">    Internal --&gt;|&quot;EAGAIN&quot;| Register[&quot;注册 Poll watch&quot;]</span><br><span class="line">    Register --&gt; Empty[&quot;event loop 可执行队列暂时为空&quot;]</span><br><span class="line">    Empty --&gt; Decision&#123;&quot;outstanding work 是否为 0&quot;&#125;</span><br><span class="line">    Decision --&gt;|&quot;否&quot;| WaitIO[&quot;ev_loop 调用 Poll 等待 I/O&quot;]</span><br><span class="line">    WaitIO --&gt; Ready[&quot;fd ready&quot;]</span><br><span class="line">    Ready --&gt; WatchCb[&quot;watch_func post 内部 task&quot;]</span><br><span class="line">    WatchCb --&gt; Internal</span><br><span class="line">    Internal --&gt;|&quot;完成&quot;| PostDone[&quot;post completion task&quot;]</span><br><span class="line">    PostDone --&gt; Fini[&quot;on_task_fini 释放 work 引用&quot;]</span><br><span class="line">    Fini --&gt; RunDone[&quot;event loop 执行 completion task&quot;]</span><br></pre></td></tr></table></figure><p>关键点：Poll watch 本身不是 Future，也不是用户回调。event loop 之所以不会提前退出，根本原因是未完成请求仍持有 <code>on_task_init()</code> 建立的 outstanding-work 引用。</p><h3 id="12-5-读路径的任务与队列迁移"><a href="#12-5-读路径的任务与队列迁移" class="headerlink" title="12.5 读路径的任务与队列迁移"></a>12.5 读路径的任务与队列迁移</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    AR[&quot;async_read&quot;] --&gt; RQ[&quot;read_queue&quot;]</span><br><span class="line">    RQ --&gt; RT[&quot;read_task&quot;]</span><br><span class="line">    RT --&gt;|&quot;rxring 有数据&quot;| RC[&quot;read completion task&quot;]</span><br><span class="line">    RT --&gt;|&quot;rxring 为空&quot;| RX[&quot;rxbuf_task&quot;]</span><br><span class="line">    RX --&gt;|&quot;recvmsg EAGAIN&quot;| WI[&quot;watch IN&quot;]</span><br><span class="line">    WI --&gt; WF[&quot;watch_func&quot;]</span><br><span class="line">    WF --&gt; RX</span><br><span class="line">    RX --&gt;|&quot;普通 CAN 帧&quot;| Ring[&quot;rxring&quot;]</span><br><span class="line">    Ring --&gt; CS[&quot;consumer signal&quot;]</span><br><span class="line">    CS --&gt; RT</span><br><span class="line">    RC --&gt; PR[&quot;Promise set&quot;]</span><br></pre></td></tr></table></figure><h3 id="12-6-写路径的任务与队列迁移"><a href="#12-6-写路径的任务与队列迁移" class="headerlink" title="12.6 写路径的任务与队列迁移"></a>12.6 写路径的任务与队列迁移</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    AW[&quot;async_write&quot;] --&gt; WQ[&quot;write_queue&quot;]</span><br><span class="line">    WQ --&gt; WT[&quot;write_task&quot;]</span><br><span class="line">    WT --&gt;|&quot;sendmsg EAGAIN&quot;| WO[&quot;watch OUT&quot;]</span><br><span class="line">    WO --&gt; WF[&quot;watch_func&quot;]</span><br><span class="line">    WF --&gt; WT</span><br><span class="line">    WT --&gt;|&quot;success and txwait=false&quot;| WC[&quot;write completion task&quot;]</span><br><span class="line">    WT --&gt;|&quot;success and txwait=true&quot;| CQ[&quot;confirm_queue&quot;]</span><br><span class="line">    CQ --&gt; RX[&quot;rxbuf_task&quot;]</span><br><span class="line">    RX --&gt;|&quot;MSG_CONFIRM&quot;| Match[&quot;match queued message&quot;]</span><br><span class="line">    Match --&gt; WC</span><br><span class="line">    WC --&gt; PR[&quot;Promise set&quot;]</span><br></pre></td></tr></table></figure><h3 id="12-7-三类-task-不要混淆"><a href="#12-7-三类-task-不要混淆" class="headerlink" title="12.7 三类 task 不要混淆"></a>12.7 三类 task 不要混淆</h3><table><thead><tr><th>task 类型</th><th>典型对象</th><th>是否长期复用</th><th>作用</th></tr></thead><tbody><tr><td>channel 内部任务</td><td><code>rxbuf_task/read_task/write_task</code></td><td>是</td><td>推进 SocketCAN I&#x2F;O 状态机</td></tr><tr><td>请求 completion task</td><td><code>io_can_chan_read.task</code>、<code>io_can_chan_write.task</code></td><td>每个请求一个</td><td>发布结果，执行回调或设置 Promise</td></tr><tr><td>Future 等待任务</td><td>调用方通过 Future API 注册的 task</td><td>由调用方&#x2F;Future 管理</td><td>Future ready 后继续上层状态机</td></tr></tbody></table><p>Promise ready 不等于直接在 <code>recvmsg()</code> 或 <code>sendmsg()</code> 的调用栈中运行上层 CANopen 逻辑。请求 completion task 先进入 Executor 队列；如果 Future 上还有等待任务，Future 再按其自身规则把等待任务提交给相应 Executor。</p><hr>]]></content>
    
    
      
      
    <summary type="html">&lt;meta name=&quot;referrer&quot; content=&quot;no-referrer&quot; /&gt;


&lt;h1 id=&quot;Lely-CANopen-IO2-CAN-与-SocketCAN-完整机制学习笔记&quot;&gt;&lt;a href=&quot;#Lely-CANopen-IO2-CAN-与-SocketC</summary>
      
    
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/categories/canopen/"/>
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/tags/canopen/"/>
    
  </entry>
  
  <entry>
    <title>Lely CANopen 事件调度机制详解：从 `ev_task`、Executor、`std_exec` 到 `ev_loop`、Future 与 Poll</title>
    <link href="https://wdfk-prog.space/posts/873e530e/"/>
    <id>https://wdfk-prog.space/posts/873e530e/</id>
    <published>2026-07-28T08:13:12.000Z</published>
    <updated>2026-07-28T08:14:18.460Z</updated>
    
    <content type="html"><![CDATA[<meta name="referrer" content="no-referrer" /><h1 id="Lely-CANopen-事件调度机制详解：从-ev-task、Executor、std-exec-到-ev-loop、Future-与-Poll"><a href="#Lely-CANopen-事件调度机制详解：从-ev-task、Executor、std-exec-到-ev-loop、Future-与-Poll" class="headerlink" title="Lely CANopen 事件调度机制详解：从 ev_task、Executor、std_exec 到 ev_loop、Future 与 Poll"></a>Lely CANopen 事件调度机制详解：从 <code>ev_task</code>、Executor、<code>std_exec</code> 到 <code>ev_loop</code>、Future 与 Poll</h1><p><img src="https://i-blog.csdnimg.cn/direct/1cb5c8e23a0145c0a5dbe4d5b8579367.png" alt="在这里插入图片描述"></p><p>@[toc]</p><hr><h2 id="0-阅读前先记住六个结论"><a href="#0-阅读前先记住六个结论" class="headerlink" title="0. 阅读前先记住六个结论"></a>0. 阅读前先记住六个结论</h2><ol><li><strong><code>ev_task</code> 只是“可执行工作”的载体，不负责排队、线程切换或等待。</strong></li><li><strong><code>ev_exec_t</code> 是抽象 Executor 接口，本质是 C 语言虚函数表。</strong></li><li><strong><code>ev_std_exec</code> 不是另一个事件循环，而是一个适配层：只要求后端实现 <code>post/abort/on_task_init/on_task_fini</code>，它补齐 <code>dispatch/defer/run</code>。</strong></li><li><strong><code>ev_loop</code> 才拥有真正的全局任务队列、等待线程、Poll 线程和 outstanding work 状态。</strong></li><li><strong><code>ev_exec_run()</code> 的关键价值不是简单调用回调，而是建立“当前线程正在这个 Executor 内执行任务”的 TLS 上下文。</strong></li><li><strong>Future、I&#x2F;O、定时器最终都不会直接“执行用户逻辑”；它们把 <code>ev_task</code> 投递给 Executor，由 <code>ev_loop</code> 取出并运行。</strong></li></ol><p>可以先把整个系统压缩成下面这条链：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line">异步事件 / Future ready / 用户主动 post</span><br><span class="line">                    │</span><br><span class="line">                    ▼</span><br><span class="line">                ev_task</span><br><span class="line">                    │</span><br><span class="line">                    ▼</span><br><span class="line">      ev_exec_dispatch/post/defer</span><br><span class="line">                    │</span><br><span class="line">                    ▼</span><br><span class="line">            ev_std_exec 语义层</span><br><span class="line">                    │</span><br><span class="line">                    ▼</span><br><span class="line">        ev_loop 后端队列与唤醒逻辑</span><br><span class="line">                    │</span><br><span class="line">                    ▼</span><br><span class="line"> ev_loop_wait_one() 从队列取出 task</span><br><span class="line">                    │</span><br><span class="line">                    ▼</span><br><span class="line">             ev_exec_run(task)</span><br><span class="line">                    │</span><br><span class="line">                    ▼</span><br><span class="line">              task-&gt;func(task)</span><br></pre></td></tr></table></figure><hr><h2 id="1-源码范围与文件职责"><a href="#1-源码范围与文件职责" class="headerlink" title="1. 源码范围与文件职责"></a>1. 源码范围与文件职责</h2><table><thead><tr><th>文件</th><th>主要职责</th><th align="right">阅读优先级</th></tr></thead><tbody><tr><td><code>include/lely/ev/ev.h</code></td><td>前置声明 <code>ev_task</code>，定义抽象类型 <code>ev_exec_t</code></td><td align="right">最高</td></tr><tr><td><code>include/lely/ev/task.h</code></td><td>定义 C 任务对象 <code>ev_task</code> 与任务队列辅助函数</td><td align="right">最高</td></tr><tr><td><code>include/lely/ev/task.hpp</code></td><td>C++ <code>Task</code>、临时 <code>TaskWrapper</code></td><td align="right">中</td></tr><tr><td><code>src/ev/task.c</code></td><td><code>structof()</code> 反查、批量 post&#x2F;abort</td><td align="right">高</td></tr><tr><td><code>include/lely/ev/exec.h</code></td><td>Executor 虚函数表及 C API</td><td align="right">最高</td></tr><tr><td><code>src/ev/exec.c</code></td><td>为头文件 inline API 提供外部定义</td><td align="right">中</td></tr><tr><td><code>include/lely/ev/exec.hpp</code></td><td>C++ <code>Executor</code> 包装</td><td align="right">中</td></tr><tr><td><code>include/lely/ev/std_exec.h</code></td><td>标准 Executor 适配器及精简后端接口</td><td align="right">最高</td></tr><tr><td><code>src/ev/std_exec.c</code></td><td><code>dispatch/post/defer/run/abort</code> 的核心语义</td><td align="right">最高</td></tr><tr><td><code>include/lely/ev/loop.h</code></td><td>Event loop 的公开行为与 outstanding work 定义</td><td align="right">高</td></tr><tr><td><code>src/ev/loop.c</code></td><td>队列、线程、Poll、条件变量、Future wait 的落地实现</td><td align="right">最高</td></tr><tr><td><code>include/lely/ev/future.h</code></td><td>非阻塞 Future&#x2F;Promise 契约</td><td align="right">高</td></tr><tr><td><code>src/ev/future.c</code></td><td>Future 状态机、等待任务队列、计数闭环</td><td align="right">高</td></tr></tbody></table><hr><h2 id="2-整体分层：谁定义语义，谁保存状态，谁真正执行"><a href="#2-整体分层：谁定义语义，谁保存状态，谁真正执行" class="headerlink" title="2. 整体分层：谁定义语义，谁保存状态，谁真正执行"></a>2. 整体分层：谁定义语义，谁保存状态，谁真正执行</h2><p>Lely 的事件库不是把所有功能堆在 <code>ev_loop</code> 中，而是分成四层：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">flowchart TB</span><br><span class="line">    A[业务层 / CANopen / I/O / Future] --&gt; B[ev_task 工作对象]</span><br><span class="line">    B --&gt; C[ev_exec_t 抽象 Executor]</span><br><span class="line">    C --&gt; D[ev_std_exec 标准语义适配层]</span><br><span class="line">    D --&gt; E[ev_loop 精简后端实现]</span><br><span class="line">    E --&gt; F[全局 task queue]</span><br><span class="line">    E --&gt; G[Poll / condition variable]</span><br><span class="line">    F --&gt; H[ev_exec_run]</span><br><span class="line">    H --&gt; I[task-&gt;func]</span><br></pre></td></tr></table></figure><h3 id="2-1-四层职责不能混淆"><a href="#2-1-四层职责不能混淆" class="headerlink" title="2.1 四层职责不能混淆"></a>2.1 四层职责不能混淆</h3><table><thead><tr><th>层</th><th>解决的问题</th><th>不负责什么</th></tr></thead><tbody><tr><td><code>ev_task</code></td><td>“要执行哪个函数，属于哪个 Executor”</td><td>不保存线程，不主动入队</td></tr><tr><td><code>ev_exec_t</code></td><td>“用统一 API 提交、运行、取消任务”</td><td>不规定具体队列结构</td></tr><tr><td><code>ev_std_exec</code></td><td>“补齐 dispatch&#x2F;defer&#x2F;run 的标准行为”</td><td>不拥有全局事件循环队列</td></tr><tr><td><code>ev_loop</code></td><td>“任务排队、唤醒、Poll、停止、线程协调”</td><td>不理解 CANopen 协议业务语义</td></tr></tbody></table><p>因此，下面两句话都不准确：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">“Future 自己执行回调”</span><br><span class="line">“Poll 返回后直接执行 CANopen 用户回调”</span><br></pre></td></tr></table></figure><p>更准确的描述是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">Future 或 Poll 产生完成事件</span><br><span class="line">    → 形成或找到一个 ev_task</span><br><span class="line">    → 向 Executor 投递</span><br><span class="line">    → event loop 取出 task</span><br><span class="line">    → Executor 建立执行上下文</span><br><span class="line">    → task-&gt;func() 运行</span><br></pre></td></tr></table></figure><hr><h2 id="3-ev-exec-t：用-C-指针模拟对象和虚函数"><a href="#3-ev-exec-t：用-C-指针模拟对象和虚函数" class="headerlink" title="3. ev_exec_t：用 C 指针模拟对象和虚函数"></a>3. <code>ev_exec_t</code>：用 C 指针模拟对象和虚函数</h2><h3 id="3-1-看似简单的-typedef，实际有两层指针语义"><a href="#3-1-看似简单的-typedef，实际有两层指针语义" class="headerlink" title="3.1 看似简单的 typedef，实际有两层指针语义"></a>3.1 看似简单的 typedef，实际有两层指针语义</h3><p><code>ev.h</code> 中的核心定义是：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="type">const</span> <span class="class"><span class="keyword">struct</span> <span class="title">ev_exec_vtbl</span> *<span class="title">const</span> <span class="title">ev_exec_t</span>;</span></span><br></pre></td></tr></table></figure><p>这表示：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">ev_exec_t</span><br><span class="line">= 指向 const ev_exec_vtbl 的 const 指针类型</span><br></pre></td></tr></table></figure><p>公开 API 使用的通常是：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">ev_exec_t</span> *exec;</span><br></pre></td></tr></table></figure><p>所以 <code>exec</code> 并不是“直接指向 Executor 结构体”，而是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">指向 concrete object 内部 vptr 字段的地址</span><br></pre></td></tr></table></figure><p>调用方式：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">(*exec)-&gt;post(exec, task);</span><br></pre></td></tr></table></figure><p>可以把它类比为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">C++ this 指针 + 虚函数表</span><br></pre></td></tr></table></figure><h3 id="3-2-对象布局"><a href="#3-2-对象布局" class="headerlink" title="3.2 对象布局"></a>3.2 对象布局</h3><p>以 <code>ev_std_exec</code> 为例：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">ev_std_exec</span> &#123;</span></span><br><span class="line">    <span class="type">const</span> <span class="class"><span class="keyword">struct</span> <span class="title">ev_exec_vtbl</span> *<span class="title">exec_vptr</span>;</span></span><br><span class="line">    <span class="type">ev_std_exec_impl_t</span> *impl;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>调用方拿到的是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">&amp;std_exec-&gt;exec_vptr</span><br></pre></td></tr></table></figure><p>内存关系如下：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"> ev_exec_t *exec</span><br><span class="line">        │</span><br><span class="line">        ▼</span><br><span class="line">┌───────────────────────────────────────┐</span><br><span class="line">│ struct ev_std_exec                    │</span><br><span class="line">│  ┌─────────────────────────────────┐  │</span><br><span class="line">│  │ exec_vptr ───────────────┐      │  │</span><br><span class="line">│  └──────────────────────────│──────┘  │</span><br><span class="line">│  ┌──────────────────────────│──────┐  │</span><br><span class="line">│  │ impl                     │      │  │</span><br><span class="line">│  └──────────────────────────│──────┘  │</span><br><span class="line">└─────────────────────────────│─────────┘</span><br><span class="line">                              ▼</span><br><span class="line">                    struct ev_exec_vtbl</span><br></pre></td></tr></table></figure><p><code>std_exec.c</code> 通过：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">structof(exec, <span class="keyword">struct</span> ev_std_exec, exec_vptr)</span><br></pre></td></tr></table></figure><p>从 <code>exec_vptr</code> 字段地址反推出完整 <code>ev_std_exec</code> 对象地址。</p><h3 id="3-3-为什么不是普通函数指针"><a href="#3-3-为什么不是普通函数指针" class="headerlink" title="3.3 为什么不是普通函数指针"></a>3.3 为什么不是普通函数指针</h3><p>单个函数指针只能表示“如何执行”，而 Executor 需要一组统一操作：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">ev_exec_vtbl</span> &#123;</span></span><br><span class="line">    <span class="type">void</span>   (*on_task_init)(<span class="type">ev_exec_t</span> *exec);</span><br><span class="line">    <span class="type">void</span>   (*on_task_fini)(<span class="type">ev_exec_t</span> *exec);</span><br><span class="line">    <span class="type">int</span>    (*dispatch)(<span class="type">ev_exec_t</span> *exec, <span class="keyword">struct</span> ev_task *task);</span><br><span class="line">    <span class="type">void</span>   (*post)(<span class="type">ev_exec_t</span> *exec, <span class="keyword">struct</span> ev_task *task);</span><br><span class="line">    <span class="type">void</span>   (*defer)(<span class="type">ev_exec_t</span> *exec, <span class="keyword">struct</span> ev_task *task);</span><br><span class="line">    <span class="type">size_t</span> (*<span class="built_in">abort</span>)(<span class="type">ev_exec_t</span> *exec, <span class="keyword">struct</span> ev_task *task);</span><br><span class="line">    <span class="type">void</span>   (*run)(<span class="type">ev_exec_t</span> *exec, <span class="keyword">struct</span> ev_task *task);</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>这允许同一套上层代码连接不同 Executor：</p><ul><li>polling event loop；</li><li>fiber executor；</li><li>自定义线程池；</li><li>测试用同步 Executor；</li><li>其他平台后端。</li></ul><h3 id="3-4-exec-c-为什么只有几行"><a href="#3-4-exec-c-为什么只有几行" class="headerlink" title="3.4 exec.c 为什么只有几行"></a>3.4 <code>exec.c</code> 为什么只有几行</h3><p><code>exec.h</code> 中的 API 采用 <code>static inline</code>&#x2F;<code>inline</code> 转调虚函数表。<code>src/ev/exec.c</code> 先定义：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> LELY_EV_EXEC_INLINE extern inline</span></span><br></pre></td></tr></table></figure><p>再包含 <code>exec.h</code>，主要作用是为 C inline 函数提供可链接的外部定义。</p><p>因此：</p><blockquote><p><code>exec.c</code> 几乎没有业务逻辑，真正的 Executor 行为在虚函数表对应的具体实现中。</p></blockquote><hr><h2 id="4-ev-task：侵入式、零额外队列节点的任务对象"><a href="#4-ev-task：侵入式、零额外队列节点的任务对象" class="headerlink" title="4. ev_task：侵入式、零额外队列节点的任务对象"></a>4. <code>ev_task</code>：侵入式、零额外队列节点的任务对象</h2><h3 id="4-1-数据结构"><a href="#4-1-数据结构" class="headerlink" title="4.1 数据结构"></a>4.1 数据结构</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">ev_task</span> &#123;</span></span><br><span class="line">    <span class="type">ev_exec_t</span> *exec;</span><br><span class="line">    <span class="type">ev_task_func_t</span> *func;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">slnode</span> _<span class="title">node</span>;</span></span><br><span class="line">    <span class="type">void</span> *_data;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><table><thead><tr><th>字段</th><th>含义</th></tr></thead><tbody><tr><td><code>exec</code></td><td>该任务所属或将要提交到的 Executor</td></tr><tr><td><code>func</code></td><td>执行入口，签名为 <code>void func(struct ev_task *)</code></td></tr><tr><td><code>_node</code></td><td>侵入式单链表节点，可直接挂到队列</td></tr><tr><td><code>_data</code></td><td>内部处理时可保存附加信息，不应随意假设用途</td></tr></tbody></table><h3 id="4-2-为什么使用侵入式链表"><a href="#4-2-为什么使用侵入式链表" class="headerlink" title="4.2 为什么使用侵入式链表"></a>4.2 为什么使用侵入式链表</h3><p>普通任务队列通常需要单独分配节点：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">queue node → task object</span><br></pre></td></tr></table></figure><p>Lely 直接把 <code>_node</code> 嵌入任务：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">queue 直接保存 task._node</span><br></pre></td></tr></table></figure><p>优点：</p><ol><li>投递时不必额外 malloc；</li><li>队列操作开销固定；</li><li>适合实时性和嵌入式场景；</li><li>Future 队列、event loop 队列、defer 临时队列可以复用同一节点。</li></ol><p>代价也很明确：</p><blockquote><p>同一个 <code>ev_task</code> 在同一时刻只能安全地位于一个链表中。</p></blockquote><p>不要在任务仍在 Future 队列或 event loop 队列时再次提交同一个对象，否则 <code>_node</code> 链接关系会被破坏。</p><h3 id="4-3-通过嵌入实现“继承”"><a href="#4-3-通过嵌入实现“继承”" class="headerlink" title="4.3 通过嵌入实现“继承”"></a>4.3 通过嵌入实现“继承”</h3><p>典型 C 用法：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">read_task</span> &#123;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">ev_task</span> <span class="title">task</span>;</span></span><br><span class="line">    <span class="type">int</span> fd;</span><br><span class="line">    <span class="type">size_t</span> count;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="type">static</span> <span class="type">void</span></span><br><span class="line"><span class="title function_">read_task_func</span><span class="params">(<span class="keyword">struct</span> ev_task *task)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">read_task</span> *<span class="title">self</span> =</span></span><br><span class="line">            structof(task, <span class="keyword">struct</span> read_task, task);</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 使用 self-&gt;fd、self-&gt;count */</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这是 Lely 中非常常见的对象模型：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">基类对象嵌入派生结构</span><br><span class="line">回调只拿到基类指针</span><br><span class="line">通过 structof() 恢复派生对象</span><br></pre></td></tr></table></figure><h3 id="4-4-生命周期责任"><a href="#4-4-生命周期责任" class="headerlink" title="4.4 生命周期责任"></a>4.4 生命周期责任</h3><p><code>ev_task</code> 本身不带引用计数，也不会被 Executor 自动复制。</p><p>必须保证：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">从提交任务开始</span><br><span class="line">直到任务执行完成或被成功 abort</span><br><span class="line">任务对象一直有效</span><br></pre></td></tr></table></figure><p>栈上任务只有在调用方能保证任务在离开作用域前完成时才安全。异步提交通常应使用：</p><ul><li>长生命周期对象内嵌任务；</li><li>堆对象；</li><li>Promise&#x2F;Future 共享对象内嵌任务；</li><li>驱动对象或 I&#x2F;O 请求对象内嵌任务。</li></ul><hr><h2 id="5-C-Task-与-TaskWrapper"><a href="#5-C-Task-与-TaskWrapper" class="headerlink" title="5. C++ Task 与 TaskWrapper"></a>5. C++ <code>Task</code> 与 <code>TaskWrapper</code></h2><h3 id="5-1-Task"><a href="#5-1-Task" class="headerlink" title="5.1 Task"></a>5.1 <code>Task</code></h3><p><code>task.hpp</code> 的 <code>Task</code> 继承 <code>ev_task</code>，内部持有：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">std::function&lt;<span class="type">void</span>()&gt; func_;</span><br></pre></td></tr></table></figure><p>构造函数把 C 回调入口设置为一个无捕获 lambda：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">ev_task 回调</span><br><span class="line">    → static_cast&lt;Task *&gt;(task)</span><br><span class="line">    → 调用 func_()</span><br></pre></td></tr></table></figure><p><code>Task</code> 不自删除，因此其生命周期仍由调用方负责。</p><h3 id="5-2-TaskWrapper"><a href="#5-2-TaskWrapper" class="headerlink" title="5.2 TaskWrapper"></a>5.2 <code>TaskWrapper</code></h3><p><code>make_task_wrapper()</code> 使用 <code>new</code> 创建临时任务。任务运行时：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">调用 invoker_()</span><br><span class="line">然后 delete self</span><br></pre></td></tr></table></figure><p>这使 C++ API 可以直接写：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">exec.<span class="built_in">post</span>([] &#123;</span><br><span class="line">  <span class="comment">// task body</span></span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure><p>但要记住：</p><blockquote><p>临时 wrapper 是在“任务实际运行”时自删除；若在运行前被取消，必须确认当前 Lely 版本对该匿名任务的所有权约定，不能把“abort”自动等同于“delete”。</p></blockquote><h3 id="5-3-一个版本相关的源码阅读提示"><a href="#5-3-一个版本相关的源码阅读提示" class="headerlink" title="5.3 一个版本相关的源码阅读提示"></a>5.3 一个版本相关的源码阅读提示</h3><p>在官方 <code>v2.3.5</code> 与当前 <code>master</code> 的 <code>exec.hpp</code> 中，Callable 版本的 <code>Executor::dispatch()</code> 声明返回 <code>bool</code>，函数体调用 <code>dispatch(*task)</code> 后未显式 <code>return</code>。</p><p>这不影响理解 Executor 主机制，但如果本地代码使用了这个重载的返回值，应检查本地版本、编译告警和上游修复情况。直接提交已有 <code>ev_task</code> 的重载没有这一问题。</p><hr><h2 id="6-Executor-七个操作的准确语义"><a href="#6-Executor-七个操作的准确语义" class="headerlink" title="6. Executor 七个操作的准确语义"></a>6. Executor 七个操作的准确语义</h2><h3 id="6-1-总表"><a href="#6-1-总表" class="headerlink" title="6.1 总表"></a>6.1 总表</h3><table><thead><tr><th>API</th><th>是否允许同步完成</th><th>当前任务结束前是否允许开始</th><th>主要用途</th></tr></thead><tbody><tr><td><code>on_task_init</code></td><td>不适用</td><td>不适用</td><td>声明未来还有工作，防止 loop 提前退出</td></tr><tr><td><code>on_task_fini</code></td><td>不适用</td><td>不适用</td><td>释放先前声明的工作引用</td></tr><tr><td><code>dispatch</code></td><td>允许</td><td>允许，常在当前 Executor 上内联执行</td><td>“能立即执行就立即执行”</td></tr><tr><td><code>post</code></td><td>调用本身不等待完成</td><td>允许其他 worker 并发执行</td><td>普通异步投递</td></tr><tr><td><code>defer</code></td><td>调用本身不等待完成</td><td>若在当前任务内调用，禁止在当前任务结束前执行</td><td>continuation&#x2F;避免重入</td></tr><tr><td><code>abort</code></td><td>不执行任务</td><td>不适用</td><td>移除尚未开始的任务</td></tr><tr><td><code>run</code></td><td>当前调用中执行</td><td>立即</td><td>建立 Executor 执行上下文并调用任务</td></tr></tbody></table><h3 id="6-2-dispatch"><a href="#6-2-dispatch" class="headerlink" title="6.2 dispatch"></a>6.2 <code>dispatch</code></h3><p>抽象契约是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">如果当前就在该 Executor 的执行上下文中</span><br><span class="line">    可以直接运行任务并同步返回完成</span><br><span class="line">否则</span><br><span class="line">    退化为普通 post</span><br></pre></td></tr></table></figure><p>标准 Executor 的返回值：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">1：本次调用中任务已经完成</span><br><span class="line">0：仅提交，尚未由本次调用完成</span><br></pre></td></tr></table></figure><h3 id="6-3-post"><a href="#6-3-post" class="headerlink" title="6.3 post"></a>6.3 <code>post</code></h3><p><code>post</code> 保证调用者不等待任务完成，但不保证任务一定等到函数返回之后才开始。</p><p>在 Lely <code>ev_loop</code> 后端中，<code>post()</code> 自己只是加锁入队；但如果另一个 worker 正在运行同一 loop，该 worker 可能在投递线程返回前就取走并执行任务。</p><h3 id="6-4-defer"><a href="#6-4-defer" class="headerlink" title="6.4 defer"></a>6.4 <code>defer</code></h3><p><code>defer</code> 的核心语义不是“低优先级”，而是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">如果当前正在这个 Executor 的某个任务中</span><br><span class="line">    把新任务放到当前执行上下文的临时 defer 队列</span><br><span class="line">    当前任务返回后再转交后端</span><br><span class="line">否则</span><br><span class="line">    直接退化为 post</span><br></pre></td></tr></table></figure><p>它主要解决回调重入和 continuation 顺序问题。</p><h3 id="6-5-run"><a href="#6-5-run" class="headerlink" title="6.5 run"></a>6.5 <code>run</code></h3><p><code>run</code> 不是通常意义上的“启动事件循环”。它的对象是单个任务：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ev_exec_run(exec, task);</span><br></pre></td></tr></table></figure><p>其职责是：</p><ol><li>标记当前线程正在 <code>exec</code> 上执行；</li><li>为本次任务建立 defer 队列；</li><li>调用 <code>task-&gt;func(task)</code>；</li><li>任务结束后，把 defer 队列转交后端；</li><li>恢复上一层嵌套执行上下文。</li></ol><p>这是理解 <code>std_exec.c</code> 的核心。</p><hr><h2 id="7-ev-std-exec：为什么要在-Executor-与-event-loop-之间再加一层"><a href="#7-ev-std-exec：为什么要在-Executor-与-event-loop-之间再加一层" class="headerlink" title="7. ev_std_exec：为什么要在 Executor 与 event loop 之间再加一层"></a>7. <code>ev_std_exec</code>：为什么要在 Executor 与 event loop 之间再加一层</h2><h3 id="7-1-精简后端接口"><a href="#7-1-精简后端接口" class="headerlink" title="7.1 精简后端接口"></a>7.1 精简后端接口</h3><p><code>ev_std_exec_impl_vtbl</code> 只要求后端提供四个操作：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">ev_std_exec_impl_vtbl</span> &#123;</span></span><br><span class="line">    <span class="type">void</span>   (*on_task_init)(<span class="type">ev_std_exec_impl_t</span> *impl);</span><br><span class="line">    <span class="type">void</span>   (*on_task_fini)(<span class="type">ev_std_exec_impl_t</span> *impl);</span><br><span class="line">    <span class="type">void</span>   (*post)(<span class="type">ev_std_exec_impl_t</span> *impl, <span class="keyword">struct</span> ev_task *task);</span><br><span class="line">    <span class="type">size_t</span> (*<span class="built_in">abort</span>)(<span class="type">ev_std_exec_impl_t</span> *impl, <span class="keyword">struct</span> ev_task *task);</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>而对外的完整 Executor 有七个操作。</p><p>因此：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">ev_std_exec</span><br><span class="line">= 用 post + abort + work-count hooks</span><br><span class="line">  实现完整 Executor 语义的通用适配器</span><br></pre></td></tr></table></figure><p>这样 <code>ev_loop</code> 不必重复实现复杂的：</p><ul><li>当前线程是否在该 Executor 上执行；</li><li>dispatch 是否可以内联；</li><li>defer 应该挂在哪一层；</li><li>嵌套 run 的 defer 队列如何恢复。</li></ul><h3 id="7-2-两套虚函数表"><a href="#7-2-两套虚函数表" class="headerlink" title="7.2 两套虚函数表"></a>7.2 两套虚函数表</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    API[ev_exec_t API] --&gt; V1[ev_exec_vtbl 完整接口]</span><br><span class="line">    V1 --&gt; S[ev_std_exec]</span><br><span class="line">    S --&gt; V2[ev_std_exec_impl_vtbl 精简接口]</span><br><span class="line">    V2 --&gt; L[ev_loop backend]</span><br></pre></td></tr></table></figure><p>调用 <code>ev_exec_post()</code> 时的实际链路：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">ev_exec_post(exec, task)</span><br><span class="line">    ↓</span><br><span class="line">(*exec)-&gt;post(exec, task)</span><br><span class="line">    ↓</span><br><span class="line">ev_std_exec_post()</span><br><span class="line">    ↓</span><br><span class="line">(*exec-&gt;impl)-&gt;post(exec-&gt;impl, task)</span><br><span class="line">    ↓</span><br><span class="line">ev_loop_std_exec_impl_post()</span><br></pre></td></tr></table></figure><hr><h2 id="8-std-exec-c-最关键的数据结构：线程局部执行上下文"><a href="#8-std-exec-c-最关键的数据结构：线程局部执行上下文" class="headerlink" title="8. std_exec.c 最关键的数据结构：线程局部执行上下文"></a>8. <code>std_exec.c</code> 最关键的数据结构：线程局部执行上下文</h2><h3 id="8-1-TLS-根节点"><a href="#8-1-TLS-根节点" class="headerlink" title="8.1 TLS 根节点"></a>8.1 TLS 根节点</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">static</span> <span class="keyword">_Thread_local</span> <span class="class"><span class="keyword">struct</span> <span class="title">ev_exec_node</span> *<span class="title">ev_exec_list</span>;</span></span><br></pre></td></tr></table></figure><p>每个线程拥有独立的 <code>ev_exec_list</code>。</p><p>它回答一个问题：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">当前线程此刻正在执行哪些 Executor 的任务？</span><br></pre></td></tr></table></figure><h3 id="8-2-两级结构"><a href="#8-2-两级结构" class="headerlink" title="8.2 两级结构"></a>8.2 两级结构</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">ev_task_node</span> &#123;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">ev_task_node</span> *<span class="title">next</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sllist</span> *<span class="title">queue</span>;</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">ev_exec_node</span> &#123;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">ev_exec_node</span> *<span class="title">next</span>;</span></span><br><span class="line">    <span class="type">ev_exec_t</span> *exec;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">ev_task_node</span> *<span class="title">queue</span>;</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>关系如下：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">线程 TLS ev_exec_list</span><br><span class="line">    │</span><br><span class="line">    ├─ Executor A</span><br><span class="line">    │    ├─ 最内层 run 的 defer queue</span><br><span class="line">    │    ├─ 外一层 run 的 defer queue</span><br><span class="line">    │    └─ ...</span><br><span class="line">    │</span><br><span class="line">    ├─ Executor B</span><br><span class="line">    │    └─ 当前 run 的 defer queue</span><br><span class="line">    │</span><br><span class="line">    └─ ...</span><br></pre></td></tr></table></figure><h3 id="8-3-为什么一个-Executor-还需要多层-queue"><a href="#8-3-为什么一个-Executor-还需要多层-queue" class="headerlink" title="8.3 为什么一个 Executor 还需要多层 queue"></a>8.3 为什么一个 Executor 还需要多层 queue</h3><p>同一线程可能出现嵌套 <code>ev_exec_run()</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">run(task A)</span><br><span class="line">    └─ 任务 A 内部显式 run(task B)</span><br><span class="line">           └─ 任务 B defer(task C)</span><br></pre></td></tr></table></figure><p>任务 C 应属于 B 这一层的 continuation，而不是直接混到 A 的 defer 队列中。</p><p>所以同一个 <code>ev_exec_node</code> 下还有 <code>ev_task_node</code> 栈。</p><h3 id="8-4-不同-Executor-的嵌套"><a href="#8-4-不同-Executor-的嵌套" class="headerlink" title="8.4 不同 Executor 的嵌套"></a>8.4 不同 Executor 的嵌套</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Executor A 正在运行 task A</span><br><span class="line">    └─ task A 调用 Executor B.run(task B)</span><br></pre></td></tr></table></figure><p>线程 TLS 中会同时出现 A、B 两个 <code>ev_exec_node</code>。<code>ev_exec_find()</code> 按 Executor 地址查找对应节点。</p><hr><h2 id="9-ev-std-exec-run-：完整执行边界"><a href="#9-ev-std-exec-run-：完整执行边界" class="headerlink" title="9. ev_std_exec_run()：完整执行边界"></a>9. <code>ev_std_exec_run()</code>：完整执行边界</h2><p>其核心过程可简化为：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">run</span><span class="params">(exec, task)</span></span><br><span class="line">&#123;</span><br><span class="line">    建立本次 defer <span class="built_in">queue</span>;</span><br><span class="line">    把当前 exec 标记进 TLS;</span><br><span class="line"></span><br><span class="line">    task-&gt;func(task);</span><br><span class="line"></span><br><span class="line">    从 TLS 弹出本次执行上下文;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (defer <span class="built_in">queue</span> 非空) &#123;</span><br><span class="line">        impl-&gt;post(task);</span><br><span class="line">        on_task_fini();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="9-1-为什么-defer-入队时要-on-task-init"><a href="#9-1-为什么-defer-入队时要-on-task-init" class="headerlink" title="9.1 为什么 defer 入队时要 on_task_init()"></a>9.1 为什么 defer 入队时要 <code>on_task_init()</code></h3><p><code>defer</code> 任务暂时只存在于线程栈上的局部队列中，还没有进入 <code>ev_loop-&gt;queue</code>。</p><p>若不登记 outstanding work：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">其他 loop 线程看到全局队列为空</span><br><span class="line">并且 ntasks == 0</span><br><span class="line">    → 可能停止 loop</span><br></pre></td></tr></table></figure><p>因此 <code>ev_std_exec_defer()</code> 在加入局部队列前执行：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ev_std_exec_on_task_init(exec);</span><br></pre></td></tr></table></figure><h3 id="9-2-为什么-flush-时必须先-post，再-fini"><a href="#9-2-为什么-flush-时必须先-post，再-fini" class="headerlink" title="9.2 为什么 flush 时必须先 post，再 fini"></a>9.2 为什么 flush 时必须先 post，再 fini</h3><p>源码顺序是：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">impl-&gt;post(impl, task);</span><br><span class="line">ev_std_exec_on_task_fini(exec);</span><br></pre></td></tr></table></figure><p>不能随意交换。</p><p>正确的状态迁移：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">任务在 defer 临时队列</span><br><span class="line">    │ 由 on_task_init 保活</span><br><span class="line">    ▼</span><br><span class="line">先进入 event loop 全局队列</span><br><span class="line">    ▼</span><br><span class="line">再释放临时 work 引用</span><br></pre></td></tr></table></figure><p>若先 <code>fini</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">ntasks 可能先变 0</span><br><span class="line">全局队列此时仍为空</span><br><span class="line">loop 被 stop</span><br><span class="line">随后才 post</span><br></pre></td></tr></table></figure><p>这会形成提前停止窗口。</p><p>同样的“先 post，后 fini”原则也出现在：</p><ul><li><code>ev_task_queue_post()</code>；</li><li>Future ready 后转移等待任务；</li><li>Future cancel 后提交任务。</li></ul><hr><h2 id="10-dispatch、post、defer-在-std-exec-中怎样实现"><a href="#10-dispatch、post、defer-在-std-exec-中怎样实现" class="headerlink" title="10. dispatch、post、defer 在 std_exec 中怎样实现"></a>10. <code>dispatch</code>、<code>post</code>、<code>defer</code> 在 <code>std_exec</code> 中怎样实现</h2><h3 id="10-1-dispatch"><a href="#10-1-dispatch" class="headerlink" title="10.1 dispatch"></a>10.1 <code>dispatch</code></h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">查找当前线程 TLS 中是否存在该 exec</span><br><span class="line">    ├─ 不存在：调用 post，返回 0</span><br><span class="line">    └─ 存在：直接 task-&gt;func(task)，返回 1</span><br></pre></td></tr></table></figure><p>关键细节：</p><blockquote><p><code>dispatch()</code> 的内联分支直接调用 <code>task-&gt;func()</code>，不会再创建新的 <code>ev_exec_run()</code> defer 边界。</p></blockquote><p>因此，内联 dispatch 的任务与当前外层任务共享当前最内层 defer queue。</p><p>例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">outer task</span><br><span class="line">    dispatch(inner task)</span><br><span class="line">        inner task defer(D)</span><br><span class="line">    outer task 继续执行</span><br><span class="line">outer task 返回</span><br><span class="line">    才 flush D</span><br></pre></td></tr></table></figure><p>D 不是在 inner task 返回后立即 flush，而是在当前 <code>ev_exec_run()</code> 边界结束后 flush。</p><h3 id="10-2-post"><a href="#10-2-post" class="headerlink" title="10.2 post"></a>10.2 <code>post</code></h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">如果 task-&gt;exec 为空，绑定当前 exec</span><br><span class="line">调用后端 impl-&gt;post()</span><br></pre></td></tr></table></figure><p>对 <code>ev_loop</code> 后端而言就是进入全局 FIFO 单链表。</p><h3 id="10-3-defer"><a href="#10-3-defer" class="headerlink" title="10.3 defer"></a>10.3 <code>defer</code></h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">查找当前线程是否正在该 exec 上运行</span><br><span class="line">    ├─ 否：退化为 post</span><br><span class="line">    └─ 是：</span><br><span class="line">         绑定 task-&gt;exec</span><br><span class="line">         on_task_init()</span><br><span class="line">         加到当前最内层 defer queue 尾部</span><br></pre></td></tr></table></figure><h3 id="10-4-abort"><a href="#10-4-abort" class="headerlink" title="10.4 abort"></a>10.4 <code>abort</code></h3><p>标准 Executor 先检查当前线程该 Executor 的局部 defer queue：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">找到待取消任务</span><br><span class="line">    → 从 defer queue 删除</span><br><span class="line">    → on_task_fini()</span><br><span class="line">    → 不再调用后端 abort</span><br></pre></td></tr></table></figure><p>若未找到，或要求取消全部任务，则再调用后端 <code>impl-&gt;abort()</code>。</p><p>这体现了一个重要事实：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">尚未开始的任务可能位于两处：</span><br><span class="line">1. 当前线程的 defer 临时队列</span><br><span class="line">2. 后端 event loop 全局队列</span><br></pre></td></tr></table></figure><hr><h2 id="11-一个最直观的执行顺序示例"><a href="#11-一个最直观的执行顺序示例" class="headerlink" title="11. 一个最直观的执行顺序示例"></a>11. 一个最直观的执行顺序示例</h2><p>假设单线程 event loop 正在运行 <code>outer</code>：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">outer</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  exec.<span class="built_in">defer</span>(C);</span><br><span class="line">  exec.<span class="built_in">dispatch</span>(A);</span><br><span class="line">  exec.<span class="built_in">post</span>(B);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>执行过程：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">1. outer 开始</span><br><span class="line">2. defer(C)</span><br><span class="line">   C 进入 outer 的局部 defer queue</span><br><span class="line">3. dispatch(A)</span><br><span class="line">   A 立即内联执行</span><br><span class="line">4. post(B)</span><br><span class="line">   B 立即进入 event loop 全局 queue</span><br><span class="line">5. outer 返回</span><br><span class="line">6. ev_std_exec_run() flush C</span><br><span class="line">   C 被追加到 event loop 全局 queue</span><br><span class="line">7. event loop 后续依次执行 B、C</span><br></pre></td></tr></table></figure><p>单线程且没有其他已有任务时，顺序通常为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">outer → A → B → C</span><br></pre></td></tr></table></figure><p>多线程时，B 进入全局队列后可能被其他 worker 并发执行，甚至在 outer 返回前完成；C 仍不会在 outer 的当前 <code>run</code> 边界结束前被提交。</p><hr><h2 id="12-ev-loop-怎样接入-ev-std-exec"><a href="#12-ev-loop-怎样接入-ev-std-exec" class="headerlink" title="12. ev_loop 怎样接入 ev_std_exec"></a>12. <code>ev_loop</code> 怎样接入 <code>ev_std_exec</code></h2><h3 id="12-1-event-loop-内嵌标准-Executor"><a href="#12-1-event-loop-内嵌标准-Executor" class="headerlink" title="12.1 event loop 内嵌标准 Executor"></a>12.1 event loop 内嵌标准 Executor</h3><p><code>struct ev_loop</code> 中包含：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">const</span> <span class="class"><span class="keyword">struct</span> <span class="title">ev_std_exec_impl_vtbl</span> *<span class="title">impl_vptr</span>;</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">ev_std_exec</span> <span class="title">exec</span>;</span></span><br></pre></td></tr></table></figure><p>初始化时：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">loop-&gt;impl_vptr = &amp;ev_loop_std_exec_impl_vtbl</span><br><span class="line"></span><br><span class="line">loop-&gt;exec</span><br><span class="line">    通过 ev_std_exec_init()</span><br><span class="line">    连接到 &amp;loop-&gt;impl_vptr</span><br></pre></td></tr></table></figure><p>所以：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ev_loop_get_exec(loop)</span><br></pre></td></tr></table></figure><p>返回的是 event loop 内嵌 Executor 的接口地址，而不是新建对象。</p><h3 id="12-2-event-loop-只实现四个后端操作"><a href="#12-2-event-loop-只实现四个后端操作" class="headerlink" title="12.2 event loop 只实现四个后端操作"></a>12.2 event loop 只实现四个后端操作</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">static</span> <span class="type">const</span> <span class="class"><span class="keyword">struct</span> <span class="title">ev_std_exec_impl_vtbl</span></span></span><br><span class="line"><span class="class">        <span class="title">ev_loop_std_exec_impl_vtbl</span> =</span> &#123;</span><br><span class="line">    on_task_init,</span><br><span class="line">    on_task_fini,</span><br><span class="line">    post,</span><br><span class="line">    <span class="built_in">abort</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>其余 <code>dispatch/defer/run</code> 都由 <code>ev_std_exec</code> 提供。</p><hr><h2 id="13-event-loop-后端四个操作"><a href="#13-event-loop-后端四个操作" class="headerlink" title="13. event loop 后端四个操作"></a>13. event loop 后端四个操作</h2><h3 id="13-1-on-task-init：增加额外-work-引用"><a href="#13-1-on-task-init：增加额外-work-引用" class="headerlink" title="13.1 on_task_init：增加额外 work 引用"></a>13.1 <code>on_task_init</code>：增加额外 work 引用</h3><p>多线程正常构建中：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">atomic_fetch_add_explicit(&amp;loop-&gt;ntasks, <span class="number">1</span>,</span><br><span class="line">                          memory_order_relaxed);</span><br></pre></td></tr></table></figure><p>其语义不是“队列中新增一个任务”，而是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">还有一份尚未进入 loop queue 的异步工作</span><br><span class="line">请不要因队列暂时为空而退出</span><br></pre></td></tr></table></figure><h3 id="13-2-on-task-fini：释放-work-引用"><a href="#13-2-on-task-fini：释放-work-引用" class="headerlink" title="13.2 on_task_fini：释放 work 引用"></a>13.2 <code>on_task_fini</code>：释放 work 引用</h3><p>当最后一个引用从 1 变 0：</p><ol><li>建立 acquire 屏障；</li><li>获取 <code>loop-&gt;mtx</code>；</li><li>再检查真实任务队列；</li><li>队列为空时调用 <code>ev_loop_do_stop()</code>。</li></ol><p>这里采用典型的：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">原子快路径</span><br><span class="line">+ 最后一个引用进入加锁慢路径</span><br></pre></td></tr></table></figure><h3 id="13-3-post：加入全局队列并唤醒一个线程"><a href="#13-3-post：加入全局队列并唤醒一个线程" class="headerlink" title="13.3 post：加入全局队列并唤醒一个线程"></a>13.3 <code>post</code>：加入全局队列并唤醒一个线程</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">lock loop-&gt;mtx</span><br><span class="line">记录投递前是否为空</span><br><span class="line">push_back(loop-&gt;queue, task)</span><br><span class="line">若发生 空 → 非空</span><br><span class="line">    ev_loop_kill_any(loop, 1)</span><br><span class="line">unlock</span><br></pre></td></tr></table></figure><p>只在空队列首次获得任务时唤醒，可减少无效 signal&#x2F;kill。</p><h3 id="13-4-abort"><a href="#13-4-abort" class="headerlink" title="13.4 abort"></a>13.4 <code>abort</code></h3><p>后端 abort 从 <code>loop-&gt;queue</code> 移除指定任务或全部真实任务。</p><p>若启用了 <code>poll_task</code>，这个伪任务会被保留或重新放回队列，因为它是 event loop 的调度哨兵，不是普通用户任务。</p><hr><h2 id="14-poll-task：不是用户任务，而是避免-Poll-饥饿的调度哨兵"><a href="#14-poll-task：不是用户任务，而是避免-Poll-饥饿的调度哨兵" class="headerlink" title="14. poll_task：不是用户任务，而是避免 Poll 饥饿的调度哨兵"></a>14. <code>poll_task</code>：不是用户任务，而是避免 Poll 饥饿的调度哨兵</h2><p>当 <code>poll_task</code> 启用时，event loop 队列中会长期存在一个特殊 <code>loop-&gt;task</code>。</p><p>其目的：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">即使 CPU 任务持续不断</span><br><span class="line">也定期给 Poll 一次机会</span><br><span class="line">避免 I/O readiness 长期得不到处理</span><br></pre></td></tr></table></figure><p>关键规则：</p><ul><li><code>ev_loop_empty()</code> 认为“队列中仅有 poll_task”仍然等价于没有真实任务；</li><li>wait 循环弹出它后不调用 <code>task-&gt;func()</code>；</li><li>本轮结束时重新把它放回队列；</li><li>abort-all 时必须保留它。</li></ul><p>因此，不能把：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sllist_empty(loop-&gt;queue)</span><br></pre></td></tr></table></figure><p>简单等同于：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">没有业务工作</span><br></pre></td></tr></table></figure><p>源码专门通过 <code>ev_loop_empty()</code> 统一判断。</p><hr><h2 id="15-ev-loop-wait-one-：任务真正开始运行的桥"><a href="#15-ev-loop-wait-one-：任务真正开始运行的桥" class="headerlink" title="15. ev_loop_wait_one()：任务真正开始运行的桥"></a>15. <code>ev_loop_wait_one()</code>：任务真正开始运行的桥</h2><p>核心路径：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">持有 loop-&gt;mtx</span><br><span class="line">    ↓</span><br><span class="line">从 loop-&gt;queue 头部弹出一个真实 task</span><br><span class="line">    ↓</span><br><span class="line">释放 loop-&gt;mtx</span><br><span class="line">    ↓</span><br><span class="line">ev_exec_run(task-&gt;exec, task)</span><br><span class="line">    ↓</span><br><span class="line">ev_std_exec_run()</span><br><span class="line">    ↓</span><br><span class="line">建立 TLS 执行上下文</span><br><span class="line">    ↓</span><br><span class="line">task-&gt;func(task)</span><br><span class="line">    ↓</span><br><span class="line">flush defer queue</span><br><span class="line">    ↓</span><br><span class="line">重新取得 loop-&gt;mtx</span><br></pre></td></tr></table></figure><p>为什么执行任务前必须释放 <code>loop-&gt;mtx</code>：</p><ol><li>用户回调可能再次 post&#x2F;defer&#x2F;abort；</li><li>回调可能启动新的异步操作；</li><li>回调可能调用 Future、I&#x2F;O、CANopen API；</li><li>长时间持锁会阻塞其他 worker 和 I&#x2F;O 完成线程；</li><li>若回调内部再次访问 loop，会产生死锁风险。</li></ol><hr><h2 id="16-队列为空后：Poll-还是条件变量"><a href="#16-队列为空后：Poll-还是条件变量" class="headerlink" title="16. 队列为空后：Poll 还是条件变量"></a>16. 队列为空后：Poll 还是条件变量</h2><p>当没有真实任务可执行，但仍有 outstanding work 时：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">当前线程有 Poll 资格</span><br><span class="line">    → ev_poll_wait()</span><br><span class="line"></span><br><span class="line">当前线程没有 Poll 资格</span><br><span class="line">    → cnd_wait()</span><br></pre></td></tr></table></figure><h3 id="16-1-Poll-线程"><a href="#16-1-Poll-线程" class="headerlink" title="16.1 Poll 线程"></a>16.1 Poll 线程</h3><p>负责等待外部事件，例如：</p><ul><li>SocketCAN fd 可读&#x2F;可写；</li><li>timerfd；</li><li>signal fd&#x2F;pipe；</li><li>平台 I&#x2F;O completion。</li></ul><p>Poll 返回后，后端回调通常继续向 Executor <code>post()</code> 任务。</p><h3 id="16-2-非-Poll-worker"><a href="#16-2-非-Poll-worker" class="headerlink" title="16.2 非 Poll worker"></a>16.2 非 Poll worker</h3><p>在 <code>npoll</code> 已达到上限时，其他 worker 使用条件变量等待内部事件：</p><ul><li>新任务入队；</li><li>loop stop；</li><li><code>ev_loop_kill()</code>；</li><li>Future ready。</li></ul><h3 id="16-3-为什么优先唤醒-waiting-worker"><a href="#16-3-为什么优先唤醒-waiting-worker" class="headerlink" title="16.3 为什么优先唤醒 waiting worker"></a>16.3 为什么优先唤醒 waiting worker</h3><p><code>ev_loop_kill_any()</code> 先查 <code>waiting</code> 链表，再查 <code>polling</code> 链表。</p><p>这样可以：</p><ul><li>让条件变量 worker 处理新入队任务；</li><li>尽量保持 Poll 线程继续等待外部 I&#x2F;O；</li><li>减少不必要地打断 <code>epoll_pwait()</code>。</li></ul><hr><h2 id="17-outstanding-work：不能只看任务队列长度"><a href="#17-outstanding-work：不能只看任务队列长度" class="headerlink" title="17. outstanding work：不能只看任务队列长度"></a>17. outstanding work：不能只看任务队列长度</h2><p>公开文档将 outstanding work 定义为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">待执行任务</span><br><span class="line">+ 当前执行任务</span><br><span class="line">+ on_task_init 调用次数</span><br><span class="line">- on_task_fini 调用次数</span><br></pre></td></tr></table></figure><p>在 <code>loop.c</code> 的停止判断中，最关键的两个观测量是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">ev_loop_empty(loop)</span><br><span class="line">ev_loop_ntasks(loop)</span><br></pre></td></tr></table></figure><p>其中 <code>ntasks</code> 主要承担“尚未进入全局队列的工作引用”，例如：</p><ul><li>Future 等待队列中的任务；</li><li>当前任务的 defer 临时队列；</li><li>正在等待 I&#x2F;O 完成、未来才会提交回调的异步操作；</li><li>其他显式 work guard。</li></ul><h3 id="17-1-为什么计数必须严格成对"><a href="#17-1-为什么计数必须严格成对" class="headerlink" title="17.1 为什么计数必须严格成对"></a>17.1 为什么计数必须严格成对</h3><p>少调用一次 <code>fini</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">loop 永远认为还有工作</span><br><span class="line">run() 无法自然退出</span><br></pre></td></tr></table></figure><p>多调用一次 <code>fini</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">计数下溢或提前归零</span><br><span class="line">loop 可能提前 stop</span><br></pre></td></tr></table></figure><h3 id="17-2-计数迁移原则"><a href="#17-2-计数迁移原则" class="headerlink" title="17.2 计数迁移原则"></a>17.2 计数迁移原则</h3><p>任务在多个“未执行位置”间迁移时，应保持连续保活：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">临时队列 / Future queue</span><br><span class="line">    │ 由 on_task_init 保活</span><br><span class="line">    ▼</span><br><span class="line">先 post 到 Executor 后端</span><br><span class="line">    ▼</span><br><span class="line">再 on_task_fini</span><br></pre></td></tr></table></figure><p>这个顺序是整个系统正确性的关键不变量之一。</p><hr><h2 id="18-task-c-两个批处理函数为什么重要"><a href="#18-task-c-两个批处理函数为什么重要" class="headerlink" title="18. task.c 两个批处理函数为什么重要"></a>18. <code>task.c</code> 两个批处理函数为什么重要</h2><h3 id="18-1-ev-task-queue-post"><a href="#18-1-ev-task-queue-post" class="headerlink" title="18.1 ev_task_queue_post()"></a>18.1 <code>ev_task_queue_post()</code></h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">逐个从临时 queue 取任务</span><br><span class="line">    → ev_exec_post(task-&gt;exec, task)</span><br><span class="line">    → ev_exec_on_task_fini(task-&gt;exec)</span><br></pre></td></tr></table></figure><p>用途：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">把“由 work 引用保护的等待队列”</span><br><span class="line">安全迁移到真正 Executor 队列</span><br></pre></td></tr></table></figure><h3 id="18-2-ev-task-queue-abort"><a href="#18-2-ev-task-queue-abort" class="headerlink" title="18.2 ev_task_queue_abort()"></a>18.2 <code>ev_task_queue_abort()</code></h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">逐个丢弃任务</span><br><span class="line">    → 不调用 task-&gt;func</span><br><span class="line">    → 只调用 on_task_fini</span><br></pre></td></tr></table></figure><p>用途：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">任务不再执行</span><br><span class="line">但必须释放此前占用的 outstanding work</span><br></pre></td></tr></table></figure><h3 id="18-3-为什么-post-在-fini-前"><a href="#18-3-为什么-post-在-fini-前" class="headerlink" title="18.3 为什么 post 在 fini 前"></a>18.3 为什么 post 在 fini 前</h3><p>再次强调：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">ev_exec_post(exec, task);</span><br><span class="line">ev_exec_on_task_fini(exec);</span><br></pre></td></tr></table></figure><p>这是为了让任务在任何时刻至少满足一个条件：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">要么由额外 work 引用保活</span><br><span class="line">要么已经位于 Executor 的真实队列中</span><br></pre></td></tr></table></figure><hr><h2 id="19-Future-怎样连接-Executor"><a href="#19-Future-怎样连接-Executor" class="headerlink" title="19. Future 怎样连接 Executor"></a>19. Future 怎样连接 Executor</h2><p>Lely Future 是非阻塞 Future：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Future::get() 不负责等待</span><br><span class="line">Future ready 后提交预先注册的 task</span><br></pre></td></tr></table></figure><h3 id="19-1-ev-future-submit"><a href="#19-1-ev-future-submit" class="headerlink" title="19.1 ev_future_submit()"></a>19.1 <code>ev_future_submit()</code></h3><p>核心流程：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">取得 task-&gt;exec</span><br><span class="line">    ↓</span><br><span class="line">on_task_init(exec)</span><br><span class="line">    ↓</span><br><span class="line">lock future-&gt;mtx</span><br><span class="line">    ↓</span><br><span class="line">Future 已 ready？</span><br><span class="line">    ├─ 是：unlock → post(task) → on_task_fini()</span><br><span class="line">    └─ 否：task 加入 future-&gt;queue → unlock</span><br></pre></td></tr></table></figure><p>为什么注册 Future 等待时就要 <code>on_task_init()</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">任务此时不在 event loop queue</span><br><span class="line">但它未来一定可能被提交</span><br><span class="line">loop 必须继续存活</span><br></pre></td></tr></table></figure><h3 id="19-2-Promise-完成"><a href="#19-2-Promise-完成" class="headerlink" title="19.2 Promise 完成"></a>19.2 Promise 完成</h3><p><code>ev_promise_set_release()</code>：</p><ol><li>写入结果指针；</li><li>锁定 Future；</li><li>把等待任务整体转移到局部队列；</li><li>release-store 状态为 READY；</li><li>解锁；</li><li>调用 <code>ev_task_queue_post()</code>。</li></ol><p>因此每个等待任务完成如下迁移：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">Future queue</span><br><span class="line">  由 on_task_init 保活</span><br><span class="line">        ↓</span><br><span class="line">Executor queue</span><br><span class="line">        ↓</span><br><span class="line">on_task_fini 释放 Future 阶段引用</span><br></pre></td></tr></table></figure><h3 id="19-3-cancel-与-abort"><a href="#19-3-cancel-与-abort" class="headerlink" title="19.3 cancel 与 abort"></a>19.3 cancel 与 abort</h3><table><thead><tr><th>操作</th><th>从 Future queue 移除</th><th>是否继续执行任务</th><th>work 引用</th></tr></thead><tbody><tr><td><code>ev_future_cancel()</code></td><td>是</td><td>是，立即 post</td><td>释放</td></tr><tr><td><code>ev_future_abort()</code></td><td>是</td><td>否</td><td>释放</td></tr></tbody></table><p>所以：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">cancel ≠ 保证回调不执行</span><br><span class="line">abort 才是不再提交该等待任务</span><br></pre></td></tr></table></figure><hr><h2 id="20-ev-loop-wait-loop-future-并不是睡眠等待-Future"><a href="#20-ev-loop-wait-loop-future-并不是睡眠等待-Future" class="headerlink" title="20. ev_loop_wait(loop, future) 并不是睡眠等待 Future"></a>20. <code>ev_loop_wait(loop, future)</code> 并不是睡眠等待 Future</h2><p>当 loop 等待一个 Future 时，会创建 <code>ev_loop_ctx</code>，其中内嵌：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">ev_task</span> <span class="title">task</span>;</span></span><br></pre></td></tr></table></figure><p>该任务的 Executor 是：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ev_loop_get_exec(loop)</span><br></pre></td></tr></table></figure><p>然后：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ev_future_submit(<span class="built_in">future</span>, &amp;ctx-&gt;task);</span><br></pre></td></tr></table></figure><p>Future ready 后：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">Promise 完成</span><br><span class="line">    ↓</span><br><span class="line">Future 把 ctx-&gt;task post 到 loop Executor</span><br><span class="line">    ↓</span><br><span class="line">loop 取出 ctx-&gt;task</span><br><span class="line">    ↓</span><br><span class="line">ev_loop_ctx_task_func()</span><br><span class="line">    ↓</span><br><span class="line">ctx-&gt;ready = 1</span><br><span class="line">    ↓</span><br><span class="line">唤醒 Poll 或 cnd wait</span><br><span class="line">    ↓</span><br><span class="line">wait 主循环观察 ready 并返回</span><br></pre></td></tr></table></figure><p>因此 <code>ev_loop_wait()</code> 的真实含义是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">持续驱动同一个 event loop</span><br><span class="line">直到指定 Future ready 或其他退出条件成立</span><br></pre></td></tr></table></figure><p>它在等待期间仍会：</p><ul><li>运行其他任务；</li><li>处理 SocketCAN；</li><li>推进 SDO&#x2F;NMT 状态机；</li><li>处理定时器；</li><li>执行超时回调。</li></ul><hr><h2 id="21-从异步-SDO-到-Future-ready-的完整链路"><a href="#21-从异步-SDO-到-Future-ready-的完整链路" class="headerlink" title="21. 从异步 SDO 到 Future ready 的完整链路"></a>21. 从异步 SDO 到 Future ready 的完整链路</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant App as 应用程序</span><br><span class="line">    participant Co as CANopen SDO 客户端</span><br><span class="line">    participant Io as SocketCAN/I-O</span><br><span class="line">    participant Poller as ev_poll</span><br><span class="line">    participant EvLoop as ev_loop</span><br><span class="line">    participant Exec as ev_std_exec</span><br><span class="line">    participant Fut as Future/Promise</span><br><span class="line"></span><br><span class="line">    App-&gt;&gt;Co: 发起异步 SDO，取得 Future</span><br><span class="line">    Co-&gt;&gt;Io: 注册 CAN 接收/超时工作</span><br><span class="line">    Co-&gt;&gt;EvLoop: on_task_init 或异步工作保活</span><br><span class="line">    App-&gt;&gt;EvLoop: wait(future) / run()</span><br><span class="line">    EvLoop-&gt;&gt;Poller: ev_poll_wait()</span><br><span class="line">    Io--&gt;&gt;Poller: CAN fd ready</span><br><span class="line">    Poller-&gt;&gt;EvLoop: post 接收任务</span><br><span class="line">    EvLoop-&gt;&gt;Exec: ev_exec_run(task)</span><br><span class="line">    Exec-&gt;&gt;Co: 执行任务，推进 SDO 状态机</span><br><span class="line">    Co-&gt;&gt;Fut: Promise set success/error</span><br><span class="line">    Fut-&gt;&gt;EvLoop: post ctx.task</span><br><span class="line">    EvLoop-&gt;&gt;Exec: run ctx.task</span><br><span class="line">    Exec-&gt;&gt;EvLoop: ctx.ready = 1 并唤醒等待</span><br><span class="line">    EvLoop--&gt;&gt;App: wait 返回</span><br><span class="line">    App-&gt;&gt;Fut: 读取 Result</span><br></pre></td></tr></table></figure><blockquote><p>Mermaid 兼容性说明：参与者的内部标识使用 <code>EvLoop</code>，显示名称仍通过 <code>as ev_loop</code> 保持不变。不要将 <code>Loop</code>&#x2F;<code>loop</code> 用作 sequence diagram 的参与者标识，因为 <code>loop ... end</code> 是 Mermaid 的循环控制块语法，部分解析器会按关键字处理并产生解析错误。</p></blockquote><p>这条链路解释了一个常见问题：</p><blockquote><p>如果没有其他线程驱动 loop，当前线程也没有调用 <code>run()</code>&#x2F;<code>wait()</code>，SDO Future 不会凭空完成，因为 CAN I&#x2F;O 和超时任务无人执行。</p></blockquote><hr><h2 id="22-从-Executor-post-到任务执行的完整调用链"><a href="#22-从-Executor-post-到任务执行的完整调用链" class="headerlink" title="22. 从 Executor::post() 到任务执行的完整调用链"></a>22. 从 <code>Executor::post()</code> 到任务执行的完整调用链</h2><h3 id="22-1-C-入口"><a href="#22-1-C-入口" class="headerlink" title="22.1 C++ 入口"></a>22.1 C++ 入口</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">auto</span> exec = loop.<span class="built_in">get_executor</span>();</span><br><span class="line">exec.<span class="built_in">post</span>([] &#123;</span><br><span class="line">  <span class="comment">// user task</span></span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure><h3 id="22-2-调用链"><a href="#22-2-调用链" class="headerlink" title="22.2 调用链"></a>22.2 调用链</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><span class="line">Executor::post(callable)</span><br><span class="line">    ↓</span><br><span class="line">make_task_wrapper()</span><br><span class="line">    ↓</span><br><span class="line">Executor::post(ev_task&amp;)</span><br><span class="line">    ↓</span><br><span class="line">ev_exec_post()</span><br><span class="line">    ↓</span><br><span class="line">ev_std_exec_post()</span><br><span class="line">    ↓</span><br><span class="line">ev_loop_std_exec_impl_post()</span><br><span class="line">    ↓</span><br><span class="line">loop-&gt;queue.push_back(task)</span><br><span class="line">    ↓</span><br><span class="line">ev_loop_kill_any()</span><br><span class="line">    ↓</span><br><span class="line">某个 run/wait 线程醒来</span><br><span class="line">    ↓</span><br><span class="line">ev_loop_ctx_wait_one()</span><br><span class="line">    ↓</span><br><span class="line">ev_exec_run(task-&gt;exec, task)</span><br><span class="line">    ↓</span><br><span class="line">ev_std_exec_run()</span><br><span class="line">    ↓</span><br><span class="line">TaskWrapper invoker</span><br><span class="line">    ↓</span><br><span class="line">delete wrapper</span><br></pre></td></tr></table></figure><hr><h2 id="23-从-defer-到任务执行的完整调用链"><a href="#23-从-defer-到任务执行的完整调用链" class="headerlink" title="23. 从 defer() 到任务执行的完整调用链"></a>23. 从 <code>defer()</code> 到任务执行的完整调用链</h2><p>当正在 loop task 内调用 <code>defer()</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line">ev_exec_defer()</span><br><span class="line">    ↓</span><br><span class="line">ev_std_exec_defer()</span><br><span class="line">    ↓</span><br><span class="line">在 TLS 找到当前 exec_node</span><br><span class="line">    ↓</span><br><span class="line">on_task_init()</span><br><span class="line">    ↓</span><br><span class="line">加入本次 run 的局部 defer queue</span><br><span class="line">    ↓</span><br><span class="line">当前 task-&gt;func 返回</span><br><span class="line">    ↓</span><br><span class="line">ev_std_exec_run() drain defer queue</span><br><span class="line">    ↓</span><br><span class="line">impl-&gt;post(task)</span><br><span class="line">    ↓</span><br><span class="line">on_task_fini()</span><br><span class="line">    ↓</span><br><span class="line">任务进入 loop 全局队列</span><br></pre></td></tr></table></figure><p>当不在该 Executor 的执行上下文中调用：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">defer() → post()</span><br></pre></td></tr></table></figure><p>因此，不要把 <code>defer()</code> 理解为固定延迟或定时器 API。</p><hr><h2 id="24-从-dispatch-到任务执行的完整调用链"><a href="#24-从-dispatch-到任务执行的完整调用链" class="headerlink" title="24. 从 dispatch() 到任务执行的完整调用链"></a>24. 从 <code>dispatch()</code> 到任务执行的完整调用链</h2><h3 id="24-1-当前不在该-Executor-上"><a href="#24-1-当前不在该-Executor-上" class="headerlink" title="24.1 当前不在该 Executor 上"></a>24.1 当前不在该 Executor 上</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">dispatch(task)</span><br><span class="line">    ↓</span><br><span class="line">TLS 中找不到 exec</span><br><span class="line">    ↓</span><br><span class="line">post(task)</span><br><span class="line">    ↓</span><br><span class="line">返回 0</span><br></pre></td></tr></table></figure><h3 id="24-2-当前正在该-Executor-上"><a href="#24-2-当前正在该-Executor-上" class="headerlink" title="24.2 当前正在该 Executor 上"></a>24.2 当前正在该 Executor 上</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">dispatch(task)</span><br><span class="line">    ↓</span><br><span class="line">TLS 中找到 exec</span><br><span class="line">    ↓</span><br><span class="line">直接 task-&gt;func(task)</span><br><span class="line">    ↓</span><br><span class="line">返回 1</span><br></pre></td></tr></table></figure><p>这会产生同步重入。若不希望当前调用栈中立即进入下一个回调，应使用 <code>defer()</code> 或 <code>post()</code>。</p><hr><h2 id="25-多线程模型下的准确理解"><a href="#25-多线程模型下的准确理解" class="headerlink" title="25. 多线程模型下的准确理解"></a>25. 多线程模型下的准确理解</h2><p>假设：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">npoll = 1</span><br><span class="line">线程 A、B 都调用 loop.run()</span><br></pre></td></tr></table></figure><p>可能状态：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">线程 A：进入 ev_poll_wait()</span><br><span class="line">线程 B：进入 cnd_wait()</span><br></pre></td></tr></table></figure><p>线程 C <code>post(task)</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">loop queue 空 → 非空</span><br><span class="line">优先 signal waiting 线程 B</span><br><span class="line">B 醒来执行 task</span><br><span class="line">A 保持 Poll</span><br></pre></td></tr></table></figure><p>若没有 waiting worker：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">post(task)</span><br><span class="line">    → ev_poll_kill(A)</span><br><span class="line">    → A 的 Poll wait 被中断</span><br><span class="line">    → A 回到 loop 并执行 task</span><br></pre></td></tr></table></figure><h3 id="25-1-多线程中的-post-可并发执行"><a href="#25-1-多线程中的-post-可并发执行" class="headerlink" title="25.1 多线程中的 post 可并发执行"></a>25.1 多线程中的 <code>post</code> 可并发执行</h3><p>即使 <code>post()</code> 本身不内联调用任务，另一个 worker 也可以立即取走任务。</p><p>因此下面写法存在竞态：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">exec.<span class="built_in">post</span>([&amp;] &#123; <span class="built_in">use</span>(value); &#125;);</span><br><span class="line">value = new_value;  <span class="comment">// 不能假设一定先于回调</span></span><br></pre></td></tr></table></figure><p>需要通过锁、原子变量、消息所有权或把数据封装进任务对象来保证同步。</p><h3 id="25-2-defer-只约束当前执行边界"><a href="#25-2-defer-只约束当前执行边界" class="headerlink" title="25.2 defer 只约束当前执行边界"></a>25.2 <code>defer</code> 只约束当前执行边界</h3><p><code>defer()</code> 保证：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">不会在当前 task 返回前，通过当前 defer 路径开始执行</span><br></pre></td></tr></table></figure><p>但它被 flush 到全局队列后，仍可能由任意 worker 执行。</p><p>它不是线程亲和性机制。</p><hr><h2 id="26-C-语言最小任务示例"><a href="#26-C-语言最小任务示例" class="headerlink" title="26. C 语言最小任务示例"></a>26. C 语言最小任务示例</h2><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;lely/ev/exec.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;lely/ev/task.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;lely/util/util.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">app_task</span> &#123;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">ev_task</span> <span class="title">task</span>;</span></span><br><span class="line">    <span class="type">int</span> value;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="type">static</span> <span class="type">void</span></span><br><span class="line"><span class="title function_">app_task_func</span><span class="params">(<span class="keyword">struct</span> ev_task *task)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">app_task</span> *<span class="title">self</span> =</span></span><br><span class="line">            structof(task, <span class="keyword">struct</span> app_task, task);</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 处理 self-&gt;value。 */</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">static</span> <span class="type">void</span></span><br><span class="line"><span class="title function_">submit_app_task</span><span class="params">(<span class="type">ev_exec_t</span> *exec, <span class="keyword">struct</span> app_task *task, <span class="type">int</span> value)</span></span><br><span class="line">&#123;</span><br><span class="line">    task-&gt;value = value;</span><br><span class="line">    task-&gt;task = (<span class="keyword">struct</span> ev_task)EV_TASK_INIT(exec, app_task_func);</span><br><span class="line">    ev_exec_post(exec, &amp;task-&gt;task);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>注意：</p><ol><li><code>struct app_task</code> 必须保持有效直到任务完成；</li><li>未完成前不能再次提交同一个 <code>_node</code>；</li><li>若任务可能被 abort，调用方仍需负责对象生命周期；</li><li>回调内可通过 <code>task-&gt;exec</code> 获取所属 Executor。</li></ol><hr><h2 id="27-C-最小示例"><a href="#27-C-最小示例" class="headerlink" title="27. C++ 最小示例"></a>27. C++ 最小示例</h2><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;lely/ev/loop.hpp&gt;</span></span></span><br><span class="line"></span><br><span class="line">lely::ev::Loop loop;</span><br><span class="line"><span class="keyword">auto</span> exec = loop.<span class="built_in">get_executor</span>();</span><br><span class="line"></span><br><span class="line">exec.<span class="built_in">post</span>([] &#123;</span><br><span class="line">  <span class="comment">// 异步任务</span></span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line">loop.<span class="built_in">run</span>();</span><br></pre></td></tr></table></figure><p>若任务需要稳定对象生命周期，优先显式定义 <code>lely::ev::Task</code> 或把任务嵌入业务对象，而不是无条件依赖匿名临时 wrapper。</p><hr><h2 id="28-dispatch-post-defer-选择表"><a href="#28-dispatch-post-defer-选择表" class="headerlink" title="28. dispatch/post/defer 选择表"></a>28. <code>dispatch/post/defer</code> 选择表</h2><table><thead><tr><th>需求</th><th>建议接口</th><th>原因</th></tr></thead><tbody><tr><td>当前已在 Executor 上，允许立即同步调用</td><td><code>dispatch</code></td><td>减少入队和上下文切换</td></tr><tr><td>必须异步排队</td><td><code>post</code></td><td>不在当前调用栈内直接执行</td></tr><tr><td>必须等当前任务完整返回后再提交 continuation</td><td><code>defer</code></td><td>避免当前 task 内重入</td></tr><tr><td>Future ready 后运行回调</td><td><code>ev_future_submit</code></td><td>自动处理等待队列与 work 引用</td></tr><tr><td>只想直接调用函数，不需要 Executor 语义</td><td>直接函数调用</td><td>不应滥用 <code>ev_exec_run</code></td></tr></tbody></table><h3 id="28-1-常见误用"><a href="#28-1-常见误用" class="headerlink" title="28.1 常见误用"></a>28.1 常见误用</h3><h4 id="误用一：把-dispatch-当作普通异步-post"><a href="#误用一：把-dispatch-当作普通异步-post" class="headerlink" title="误用一：把 dispatch 当作普通异步 post"></a>误用一：把 <code>dispatch</code> 当作普通异步 post</h4><p>当前已在 Executor 上时，它会同步重入。</p><h4 id="误用二：把-defer-当作毫秒级延迟"><a href="#误用二：把-defer-当作毫秒级延迟" class="headerlink" title="误用二：把 defer 当作毫秒级延迟"></a>误用二：把 <code>defer</code> 当作毫秒级延迟</h4><p>它不使用时钟，不提供 deadline。</p><h4 id="误用三：手工调用-on-task-fini，但没有对应-init"><a href="#误用三：手工调用-on-task-fini，但没有对应-init" class="headerlink" title="误用三：手工调用 on_task_fini，但没有对应 init"></a>误用三：手工调用 <code>on_task_fini</code>，但没有对应 init</h4><p>会破坏 outstanding work 计数。</p><h4 id="误用四：Future-等待任务使用空-task-exec"><a href="#误用四：Future-等待任务使用空-task-exec" class="headerlink" title="误用四：Future 等待任务使用空 task-&gt;exec"></a>误用四：Future 等待任务使用空 <code>task-&gt;exec</code></h4><p><code>ev_future_submit()</code> 要求任务已绑定 Executor。</p><h4 id="误用五：重复提交同一个-task"><a href="#误用五：重复提交同一个-task" class="headerlink" title="误用五：重复提交同一个 task"></a>误用五：重复提交同一个 task</h4><p>侵入式 <code>_node</code> 不能同时挂在多个队列。</p><hr><h2 id="29-三类“队列”必须分清"><a href="#29-三类“队列”必须分清" class="headerlink" title="29. 三类“队列”必须分清"></a>29. 三类“队列”必须分清</h2><table><thead><tr><th>队列</th><th>所属对象</th><th>何时进入</th><th>如何离开</th></tr></thead><tbody><tr><td>event loop 全局 queue</td><td><code>ev_loop</code></td><td><code>post()</code></td><td>loop worker 弹出并 <code>run()</code></td></tr><tr><td>defer 临时 queue</td><td>TLS <code>ev_task_node</code></td><td>当前任务内 <code>defer()</code></td><td>当前 <code>ev_exec_run()</code> 返回时 flush</td></tr><tr><td>Future 等待 queue</td><td><code>ev_future</code></td><td><code>ev_future_submit()</code> 且尚未 ready</td><td>Promise ready、cancel 或 abort</td></tr></tbody></table><p>一个任务在不同队列间迁移时仍使用同一个 <code>_node</code>，所以迁移必须先从旧队列移除，再加入新队列。</p><hr><h2 id="30-三类状态也必须分清"><a href="#30-三类状态也必须分清" class="headerlink" title="30. 三类状态也必须分清"></a>30. 三类状态也必须分清</h2><table><thead><tr><th>状态</th><th>范围</th><th>含义</th></tr></thead><tbody><tr><td><code>loop-&gt;stopped</code></td><td>整个 event loop</td><td>后续 run&#x2F;wait 立即停止，需 restart</td></tr><tr><td><code>ev_loop_thrd.stopped</code></td><td>当前 OS 线程</td><td>中断该线程最内层一次 loop 调用</td></tr><tr><td><code>ctx-&gt;ready</code></td><td>一次 Future wait context</td><td>等待目标已 ready</td></tr></tbody></table><p>Executor 的 defer TLS 又是另一套状态：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">ev_exec_list</span><br><span class="line">= 当前线程正在执行哪些 Executor 及其嵌套 run 层级</span><br></pre></td></tr></table></figure><p>它与 <code>ev_loop_thrd</code> 的 context 栈职责不同：</p><table><thead><tr><th>TLS 状态</th><th>管什么</th></tr></thead><tbody><tr><td><code>ev_exec_list</code></td><td>Executor 执行上下文、dispatch&#x2F;defer&#x2F;run</td></tr><tr><td><code>ev_loop_thrd</code></td><td>loop wait&#x2F;run 嵌套、中断与 Future wait context</td></tr></tbody></table><hr><h2 id="31-一张对象关系总图"><a href="#31-一张对象关系总图" class="headerlink" title="31. 一张对象关系总图"></a>31. 一张对象关系总图</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br></pre></td><td class="code"><pre><span class="line">flowchart TB</span><br><span class="line">    subgraph LoopObject[ev_loop_t]</span><br><span class="line">        LV[impl_vptr]</span><br><span class="line">        SE[struct ev_std_exec exec]</span><br><span class="line">        Q[global task queue]</span><br><span class="line">        NT[ntasks]</span><br><span class="line">        W[waiting contexts]</span><br><span class="line">        P[polling contexts]</span><br><span class="line">    end</span><br><span class="line"></span><br><span class="line">    subgraph StdExec[ev_std_exec]</span><br><span class="line">        EV[exec_vptr]</span><br><span class="line">        IMPL[impl pointer]</span><br><span class="line">    end</span><br><span class="line"></span><br><span class="line">    subgraph ThreadTLS[每线程 TLS]</span><br><span class="line">        EL[ev_exec_list]</span><br><span class="line">        LT[ev_loop_thrd]</span><br><span class="line">        DQ[defer queue stack]</span><br><span class="line">        LC[loop ctx stack]</span><br><span class="line">    end</span><br><span class="line"></span><br><span class="line">    T[ev_task] --&gt;|exec| EV</span><br><span class="line">    EV --&gt;|full vtable| StdExec</span><br><span class="line">    IMPL --&gt;|reduced vtable| LV</span><br><span class="line">    LV --&gt; LoopObject</span><br><span class="line">    Q --&gt;|pop| T</span><br><span class="line">    EL --&gt; DQ</span><br><span class="line">    LT --&gt; LC</span><br><span class="line">    F[ev_future queue] --&gt;|ready: post then fini| Q</span><br><span class="line">    NT --&gt;|0 and queue empty| STOP[loop stop]</span><br><span class="line">    P --&gt; POLL[ev_poll_wait]</span><br><span class="line">    W --&gt; CND[cnd_wait]</span><br></pre></td></tr></table></figure><hr><h2 id="32-一张完整调用链总图"><a href="#32-一张完整调用链总图" class="headerlink" title="32. 一张完整调用链总图"></a>32. 一张完整调用链总图</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    S[事件发生] --&gt; K&#123;来源&#125;</span><br><span class="line">    K --&gt;|用户提交| POST[ev_exec_post]</span><br><span class="line">    K --&gt;|当前任务 continuation| DEF[ev_exec_defer]</span><br><span class="line">    K --&gt;|Future ready| FQ[ev_task_queue_post]</span><br><span class="line">    K --&gt;|I/O ready| IO[I/O watch callback]</span><br><span class="line"></span><br><span class="line">    DEF --&gt; TLSQ[TLS defer queue]</span><br><span class="line">    TLSQ --&gt;|current run returns| POST</span><br><span class="line">    FQ --&gt; POST</span><br><span class="line">    IO --&gt; POST</span><br><span class="line"></span><br><span class="line">    POST --&gt; STD[ev_std_exec_post]</span><br><span class="line">    STD --&gt; LPOST[ev_loop impl post]</span><br><span class="line">    LPOST --&gt; GQ[loop global queue]</span><br><span class="line">    GQ --&gt; WAKE[wake cnd waiter or poller]</span><br><span class="line">    WAKE --&gt; WAIT[ev_loop_wait_one]</span><br><span class="line">    WAIT --&gt; POP[pop task]</span><br><span class="line">    POP --&gt; RUN[ev_exec_run]</span><br><span class="line">    RUN --&gt; SRUN[ev_std_exec_run]</span><br><span class="line">    SRUN --&gt; FUNC[task func]</span><br><span class="line">    FUNC --&gt;|may dispatch| INLINE[inline task func]</span><br><span class="line">    FUNC --&gt;|may defer| TLSQ</span><br><span class="line">    FUNC --&gt;|may post| POST</span><br></pre></td></tr></table></figure><hr><h2 id="33-调试时建议重点观察的变量"><a href="#33-调试时建议重点观察的变量" class="headerlink" title="33. 调试时建议重点观察的变量"></a>33. 调试时建议重点观察的变量</h2><table><thead><tr><th>变量</th><th>观察目的</th></tr></thead><tbody><tr><td><code>task-&gt;exec</code></td><td>任务是否绑定到预期 Executor</td></tr><tr><td><code>task-&gt;_node.next</code></td><td>是否可能重复入队或链表损坏</td></tr><tr><td><code>loop-&gt;queue</code></td><td>当前真实待执行任务</td></tr><tr><td><code>loop-&gt;ntasks</code></td><td>是否存在未进入 queue 的保活工作</td></tr><tr><td><code>loop-&gt;stopped</code></td><td>run 不执行是否因为 loop 已停止</td></tr><tr><td><code>loop-&gt;npolling</code></td><td>Poll 线程数量是否达到上限</td></tr><tr><td><code>ev_exec_list</code></td><td>当前线程是否处于该 Executor 上</td></tr><tr><td><code>exec_node-&gt;queue</code></td><td>当前最内层 defer queue</td></tr><tr><td><code>future-&gt;queue</code></td><td>Future 是否仍持有等待任务</td></tr><tr><td><code>future-&gt;state</code></td><td>WAITING&#x2F;SETTING&#x2F;READY</td></tr><tr><td><code>ctx-&gt;ready</code></td><td><code>ev_loop_wait(future)</code> 是否收到 ready 任务</td></tr></tbody></table><h3 id="33-1-任务“提交了但不执行”排查顺序"><a href="#33-1-任务“提交了但不执行”排查顺序" class="headerlink" title="33.1 任务“提交了但不执行”排查顺序"></a>33.1 任务“提交了但不执行”排查顺序</h3><ol><li><code>task-&gt;exec</code> 是否为 NULL；</li><li>task 是否真的进入 <code>loop-&gt;queue</code>；</li><li>loop 是否已经 <code>stopped</code>；</li><li>是否有线程正在调用 <code>run/wait</code>；</li><li>是否误把任务留在 Future queue；</li><li><code>on_task_init/fini</code> 是否失衡；</li><li>是否重复使用同一 <code>_node</code>；</li><li>worker 是否全部阻塞在非预期锁中；</li><li>Poll&#x2F;condition variable 是否被正确唤醒；</li><li>回调是否被 <code>abort</code> 移除。</li></ol><hr><h2 id="34-与-CANopen-业务层的对应关系"><a href="#34-与-CANopen-业务层的对应关系" class="headerlink" title="34. 与 CANopen 业务层的对应关系"></a>34. 与 CANopen 业务层的对应关系</h2><p>Lely CANopen 上层异步接口通常可以映射为：</p><table><thead><tr><th>CANopen 行为</th><th>Event&#x2F;Future 层动作</th></tr></thead><tbody><tr><td>发起 SDO 请求</td><td>创建请求对象、Promise&#x2F;Future、注册 I&#x2F;O&#x2F;超时</td></tr><tr><td>收到 SDO 响应</td><td>Poll 回调 post 接收任务</td></tr><tr><td>推进协议状态机</td><td>loop 执行 task</td></tr><tr><td>SDO 成功或失败</td><td>Promise set，Future READY</td></tr><tr><td><code>AsyncWrite()</code> 返回</td><td>返回 Future 句柄，不阻塞</td></tr><tr><td><code>Wait(future)</code></td><td>coroutine 或 loop 持续驱动，直到 ready</td></tr><tr><td>NMT&#x2F;Heartbeat 定时事件</td><td>timer readiness → post task → 状态机回调</td></tr><tr><td>PDO 接收</td><td>CAN fd ready → receive task → OD&#x2F;driver callback</td></tr></tbody></table><p>CANopen 协议本身是“被动、异步”的，线程与 I&#x2F;O 调度由 liblely-ev&#x2F;liblely-io2 提供。理解 Executor 后，再读 SDO、NMT、PDO 代码会清晰很多。</p><hr><h2 id="35-推荐源码阅读顺序"><a href="#35-推荐源码阅读顺序" class="headerlink" title="35. 推荐源码阅读顺序"></a>35. 推荐源码阅读顺序</h2><p>不要直接从 1000 多行的 <code>loop.c</code> 从头看到尾。建议按下面顺序：</p><ol><li><code>ev.h</code>：看懂 <code>ev_exec_t</code>；</li><li><code>task.h</code>：看懂任务对象和侵入式节点；</li><li><code>exec.h</code>：掌握七个操作契约；</li><li><code>std_exec.h</code>：理解“完整接口&#x2F;精简后端”两层虚表；</li><li><code>std_exec.c</code>：重点读 <code>run/dispatch/defer</code> 与 TLS；</li><li><code>task.c</code>：理解 post-then-fini 迁移原则；</li><li><code>loop.c</code>：先看后端四函数，再看 wait_one；</li><li><code>future.c</code>：重点读 submit&#x2F;set_release&#x2F;cancel&#x2F;abort；</li><li><code>poll.h</code> 与 Linux poll 后端；</li><li>最后再追到 SocketCAN、Timer、SDO&#x2F;NMT。</li></ol><p>建议第一次阅读时只围绕三条调用链做笔记：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">post → queue → run</span><br><span class="line"></span><br><span class="line">defer → TLS queue → post → queue → run</span><br><span class="line"></span><br><span class="line">Future submit → Future queue → post → queue → run</span><br></pre></td></tr></table></figure><hr><h2 id="36-最终心智模型"><a href="#36-最终心智模型" class="headerlink" title="36. 最终心智模型"></a>36. 最终心智模型</h2><p>可以把 Lely 事件机制想成一个物流系统：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line">ev_task</span><br><span class="line">= 包裹</span><br><span class="line"></span><br><span class="line">ev_exec_t</span><br><span class="line">= 统一寄件接口</span><br><span class="line"></span><br><span class="line">ev_std_exec</span><br><span class="line">= 根据“立即送、普通送、当前任务后再送”处理寄件语义</span><br><span class="line"></span><br><span class="line">ev_loop-&gt;queue</span><br><span class="line">= 中央分拣队列</span><br><span class="line"></span><br><span class="line">ev_exec_run</span><br><span class="line">= 建立本次派送工作上下文</span><br><span class="line"></span><br><span class="line">Future queue</span><br><span class="line">= 等待某条件后才能转运的暂存区</span><br><span class="line"></span><br><span class="line">defer queue</span><br><span class="line">= 当前处理单结束后再转运的线程本地暂存区</span><br><span class="line"></span><br><span class="line">on_task_init/fini</span><br><span class="line">= 暂存区占用的未完成工作凭证</span><br><span class="line"></span><br><span class="line">Poll</span><br><span class="line">= 等外部世界产生新包裹</span><br><span class="line"></span><br><span class="line">condition variable</span><br><span class="line">= 等进程内部其他线程送来新包裹</span><br></pre></td></tr></table></figure><p>最重要的不变量是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">一份尚未完成的异步工作，必须满足至少一个条件：</span><br><span class="line"></span><br><span class="line">1. 已经位于可执行队列；</span><br><span class="line">2. 正在执行；</span><br><span class="line">3. 由 on_task_init/fini 的 outstanding work 引用保活。</span><br></pre></td></tr></table></figure><p>而任务从 Future&#x2F;defer 暂存区迁移到 Executor 队列时必须遵守：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">先 post</span><br><span class="line">后 fini</span><br></pre></td></tr></table></figure><p>一旦掌握这两个原则，Lely 中大部分“为什么 loop 没退出”“为什么任务没执行”“为什么要有 ntasks”“为什么还要 std_exec”的问题就能统一解释。</p><hr><h2 id="37-官方源码索引"><a href="#37-官方源码索引" class="headerlink" title="37. 官方源码索引"></a>37. 官方源码索引</h2><h3 id="37-1-稳定版本-v2-3-5"><a href="#37-1-稳定版本-v2-3-5" class="headerlink" title="37.1 稳定版本 v2.3.5"></a>37.1 稳定版本 v2.3.5</h3><ol><li><code>ev.h</code><br><a href="https://gitlab.com/lely_industries/lely-core/-/blob/v2.3.5/include/lely/ev/ev.h">https://gitlab.com/lely_industries/lely-core/-/blob/v2.3.5/include/lely/ev/ev.h</a></li><li><code>task.h</code><br><a href="https://gitlab.com/lely_industries/lely-core/-/blob/v2.3.5/include/lely/ev/task.h">https://gitlab.com/lely_industries/lely-core/-/blob/v2.3.5/include/lely/ev/task.h</a></li><li><code>task.hpp</code><br><a href="https://gitlab.com/lely_industries/lely-core/-/blob/v2.3.5/include/lely/ev/task.hpp">https://gitlab.com/lely_industries/lely-core/-/blob/v2.3.5/include/lely/ev/task.hpp</a></li><li><code>task.c</code><br><a href="https://gitlab.com/lely_industries/lely-core/-/blob/v2.3.5/src/ev/task.c">https://gitlab.com/lely_industries/lely-core/-/blob/v2.3.5/src/ev/task.c</a></li><li><code>exec.h</code><br><a href="https://gitlab.com/lely_industries/lely-core/-/blob/v2.3.5/include/lely/ev/exec.h">https://gitlab.com/lely_industries/lely-core/-/blob/v2.3.5/include/lely/ev/exec.h</a></li><li><code>exec.hpp</code><br><a href="https://gitlab.com/lely_industries/lely-core/-/blob/v2.3.5/include/lely/ev/exec.hpp">https://gitlab.com/lely_industries/lely-core/-/blob/v2.3.5/include/lely/ev/exec.hpp</a></li><li><code>exec.c</code><br><a href="https://gitlab.com/lely_industries/lely-core/-/blob/v2.3.5/src/ev/exec.c">https://gitlab.com/lely_industries/lely-core/-/blob/v2.3.5/src/ev/exec.c</a></li><li><code>std_exec.h</code><br><a href="https://gitlab.com/lely_industries/lely-core/-/blob/v2.3.5/include/lely/ev/std_exec.h">https://gitlab.com/lely_industries/lely-core/-/blob/v2.3.5/include/lely/ev/std_exec.h</a></li><li><code>std_exec.c</code><br><a href="https://gitlab.com/lely_industries/lely-core/-/blob/v2.3.5/src/ev/std_exec.c">https://gitlab.com/lely_industries/lely-core/-/blob/v2.3.5/src/ev/std_exec.c</a></li><li><code>loop.h</code><br><a href="https://gitlab.com/lely_industries/lely-core/-/blob/v2.3.5/include/lely/ev/loop.h">https://gitlab.com/lely_industries/lely-core/-/blob/v2.3.5/include/lely/ev/loop.h</a></li><li><code>loop.c</code><br><a href="https://gitlab.com/lely_industries/lely-core/-/blob/v2.3.5/src/ev/loop.c">https://gitlab.com/lely_industries/lely-core/-/blob/v2.3.5/src/ev/loop.c</a></li><li><code>future.h</code><br><a href="https://gitlab.com/lely_industries/lely-core/-/blob/v2.3.5/include/lely/ev/future.h">https://gitlab.com/lely_industries/lely-core/-/blob/v2.3.5/include/lely/ev/future.h</a></li><li><code>future.c</code><br><a href="https://gitlab.com/lely_industries/lely-core/-/blob/v2.3.5/src/ev/future.c">https://gitlab.com/lely_industries/lely-core/-/blob/v2.3.5/src/ev/future.c</a></li><li><code>future.hpp</code><br><a href="https://gitlab.com/lely_industries/lely-core/-/blob/v2.3.5/include/lely/ev/future.hpp">https://gitlab.com/lely_industries/lely-core/-/blob/v2.3.5/include/lely/ev/future.hpp</a></li></ol><h3 id="37-2-官方文档"><a href="#37-2-官方文档" class="headerlink" title="37.2 官方文档"></a>37.2 官方文档</h3><ol><li>Library overview<br><a href="https://opensource.lely.com/canopen/docs/overview/">https://opensource.lely.com/canopen/docs/overview/</a></li><li>Build configuration<br><a href="https://opensource.lely.com/canopen/docs/configuration/">https://opensource.lely.com/canopen/docs/configuration/</a></li><li>C++ tutorial<br><a href="https://opensource.lely.com/canopen/docs/cpp-tutorial/">https://opensource.lely.com/canopen/docs/cpp-tutorial/</a></li><li>Lely CANopen homepage<br><a href="https://opensource.lely.com/canopen/">https://opensource.lely.com/canopen/</a></li><li>Mermaid sequence diagram syntax<br><a href="https://mermaid.js.org/syntax/sequenceDiagram.html">https://mermaid.js.org/syntax/sequenceDiagram.html</a></li></ol><hr><h2 id="38-建议配套实验"><a href="#38-建议配套实验" class="headerlink" title="38. 建议配套实验"></a>38. 建议配套实验</h2><p>为了把源码机制真正学会，建议依次做四个小实验：</p><h3 id="实验一：验证-dispatch-post-defer-顺序"><a href="#实验一：验证-dispatch-post-defer-顺序" class="headerlink" title="实验一：验证 dispatch/post/defer 顺序"></a>实验一：验证 <code>dispatch/post/defer</code> 顺序</h3><p>在一个 loop task 内分别调用三种接口，打印：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">线程 ID</span><br><span class="line">任务名称</span><br><span class="line">进入时间</span><br><span class="line">离开时间</span><br></pre></td></tr></table></figure><p>先单线程，再双线程运行 loop。</p><h3 id="实验二：故意制造-work-引用不平衡"><a href="#实验二：故意制造-work-引用不平衡" class="headerlink" title="实验二：故意制造 work 引用不平衡"></a>实验二：故意制造 work 引用不平衡</h3><p>仅在测试程序中：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">调用 on_task_init 但不 fini</span><br></pre></td></tr></table></figure><p>观察 <code>run()</code> 是否无法自然退出。然后补上 <code>fini</code> 验证恢复。</p><h3 id="实验三：Future-ready-前后提交任务"><a href="#实验三：Future-ready-前后提交任务" class="headerlink" title="实验三：Future ready 前后提交任务"></a>实验三：Future ready 前后提交任务</h3><p>分别在：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Promise set 前 submit</span><br><span class="line">Promise set 后 submit</span><br></pre></td></tr></table></figure><p>观察两条路径都能进入 Executor，但前者经过 Future queue，后者直接 post。</p><h3 id="实验四：npoll-1-双-worker"><a href="#实验四：npoll-1-双-worker" class="headerlink" title="实验四：npoll = 1 双 worker"></a>实验四：<code>npoll = 1</code> 双 worker</h3><p>记录哪个线程进入 Poll，哪个线程进入条件变量，以及普通任务入队后优先唤醒哪个线程。</p><p>完成这四个实验后，再追踪 SocketCAN 接收、SDO Future 和定时器，理解会稳定得多。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;meta name=&quot;referrer&quot; content=&quot;no-referrer&quot; /&gt;

&lt;h1 id=&quot;Lely-CANopen-事件调度机制详解：从-ev-task、Executor、std-exec-到-ev-loop、Future-与-Poll&quot;&gt;&lt;a href=&quot;</summary>
      
    
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/categories/canopen/"/>
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/tags/canopen/"/>
    
  </entry>
  
  <entry>
    <title>Lely CANopen Future Promise 机制原理与源码详解</title>
    <link href="https://wdfk-prog.space/posts/ea3bda78/"/>
    <id>https://wdfk-prog.space/posts/ea3bda78/</id>
    <published>2026-07-28T07:23:11.000Z</published>
    <updated>2026-07-28T07:24:33.136Z</updated>
    
    <content type="html"><![CDATA[<meta name="referrer" content="no-referrer" /><h1 id="Lely-CANopen-Future-Promise-机制原理与源码详解"><a href="#Lely-CANopen-Future-Promise-机制原理与源码详解" class="headerlink" title="Lely CANopen Future&#x2F;Promise 机制原理与源码详解"></a>Lely CANopen Future&#x2F;Promise 机制原理与源码详解</h1><p><img src="https://i-blog.csdnimg.cn/direct/95cd41d396604b1b9779661c74a739cd.png" alt="在这里插入图片描述"></p><p>@[toc]</p><h2 id="1-先给结论：Lely-Future-到底是什么"><a href="#1-先给结论：Lely-Future-到底是什么" class="headerlink" title="1. 先给结论：Lely Future 到底是什么"></a>1. 先给结论：Lely Future 到底是什么</h2><p>Lely 的 Future&#x2F;Promise 不是一个“阻塞等待结果”的线程同步工具，而是一个和 executor、task、event loop 深度结合的<strong>一次性异步完成通知与结果共享机制</strong>。</p><p>可以先记住以下五句话：</p><ol><li><code>ev_promise_t</code> 是异步结果的生产端，负责把共享状态从未完成推进到完成；</li><li><code>ev_future_t</code> 是观察端，负责检查结果是否完成、读取结果、登记完成后的任务；</li><li>Future 本身不会执行 I&#x2F;O，也不会主动运行回调；真正执行回调的是 task 所属的 executor；</li><li><code>ev_future_submit()</code> 不阻塞，而是把 task 暂存在 Future 内部队列，Future ready 后再提交给 executor；</li><li>Promise 和 Future 不是两个独立对象，它们共享同一块内存、同一个引用计数和同一个生命周期。</li></ol><p>官方 C 接口头文件直接强调：与 C++11 的 Future&#x2F;Promise 不同，Lely 提供非阻塞语义；用户不是等待 Future，而是提交一个在 Future ready 后执行的 task。</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">异步操作发起</span><br><span class="line">    ↓</span><br><span class="line">返回 Future 给调用方</span><br><span class="line">    ↓</span><br><span class="line">操作完成方通过 Promise 写入结果</span><br><span class="line">    ↓</span><br><span class="line">Future 变为 ready</span><br><span class="line">    ↓</span><br><span class="line">Future 中登记的 task 被提交到 executor</span><br><span class="line">    ↓</span><br><span class="line">executor/event loop 执行 task</span><br><span class="line">    ↓</span><br><span class="line">调用方读取结果或继续异步链</span><br></pre></td></tr></table></figure><p>因此，理解 Lely Future 的关键不是“谁在等待”，而是：</p><blockquote><p>谁持有 Promise、谁持有 Future、结果存在哪里、Future ready 后哪个 task 被投递到哪个 executor。</p></blockquote><hr><h2 id="2-三个源码文件各自负责什么"><a href="#2-三个源码文件各自负责什么" class="headerlink" title="2. 三个源码文件各自负责什么"></a>2. 三个源码文件各自负责什么</h2><table><thead><tr><th>文件</th><th>层次</th><th>主要职责</th></tr></thead><tbody><tr><td><code>future.h</code></td><td>C 公共接口</td><td>定义 Promise&#x2F;Future 的外部语义、引用计数接口、结果发布、task 提交、取消、组合 Future。</td></tr><tr><td><code>future.c</code></td><td>C 底层实现</td><td>定义实际内存结构、状态机、原子同步、等待 task 队列、生命周期和 <code>when_all/when_any</code>。</td></tr><tr><td><code>future.hpp</code></td><td>C++ 类型封装</td><td>提供 RAII、模板化结果、异常捕获、continuation、implicit unwrapping 和辅助函数。</td></tr></tbody></table><p>推荐阅读顺序：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">future.h</span><br><span class="line">  ↓ 先理解对外契约</span><br><span class="line">future.c</span><br><span class="line">  ↓ 再理解状态、内存和并发实现</span><br><span class="line">future.hpp</span><br><span class="line">  ↓ 最后理解 C++ 如何组合这些底层机制</span><br></pre></td></tr></table></figure><p>不要一开始就从 <code>future.hpp</code> 的模板代码往下钻，否则很容易被 <code>AsyncTask</code>、类型萃取和 <code>Result&lt;T, E&gt;</code> 淹没，而没有建立共享状态的核心模型。</p><hr><h2 id="3-四个核心角色"><a href="#3-四个核心角色" class="headerlink" title="3. 四个核心角色"></a>3. 四个核心角色</h2><h3 id="3-1-Promise：结果生产端"><a href="#3-1-Promise：结果生产端" class="headerlink" title="3.1 Promise：结果生产端"></a>3.1 Promise：结果生产端</h3><p><code>ev_promise_t</code> 用于完成一次异步操作。它的典型持有者包括：</p><ul><li>异步 CAN 读写操作；</li><li>异步 SDO 请求对象；</li><li>定时器等待操作；</li><li>C++ <code>AsyncTask</code>；</li><li><code>when_all()</code>、<code>when_any()</code> 的内部聚合对象。</li></ul><p>Promise 的核心动作只有一次：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">ev_promise_set</span><span class="params">(<span class="type">ev_promise_t</span> *promise, <span class="type">void</span> *value)</span>;</span><br></pre></td></tr></table></figure><p>成功完成后，Promise 对应的 Future 永久进入 ready 状态，不能重新复位后再次使用。</p><p>所以 Promise 更接近：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">一次性完成开关 + 结果发布端</span><br></pre></td></tr></table></figure><p>而不是可重复发送消息的队列。</p><h3 id="3-2-Future：结果观察端"><a href="#3-2-Future：结果观察端" class="headerlink" title="3.2 Future：结果观察端"></a>3.2 Future：结果观察端</h3><p><code>ev_future_t</code> 主要提供三种能力：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">ev_future_is_ready</span><span class="params">(<span class="type">const</span> <span class="type">ev_future_t</span> *<span class="built_in">future</span>)</span>;</span><br><span class="line"><span class="type">void</span> *<span class="title function_">ev_future_get</span><span class="params">(<span class="type">const</span> <span class="type">ev_future_t</span> *<span class="built_in">future</span>)</span>;</span><br><span class="line"><span class="type">void</span> <span class="title function_">ev_future_submit</span><span class="params">(<span class="type">ev_future_t</span> *<span class="built_in">future</span>, <span class="keyword">struct</span> ev_task *task)</span>;</span><br></pre></td></tr></table></figure><p>它不负责完成结果，只负责观察状态和登记后续任务。</p><h3 id="3-3-Task：Future-ready-后要执行的动作"><a href="#3-3-Task：Future-ready-后要执行的动作" class="headerlink" title="3.3 Task：Future ready 后要执行的动作"></a>3.3 Task：Future ready 后要执行的动作</h3><p>Future 不直接保存 C 函数指针列表，而是保存 <code>ev_task</code> 队列。<code>ev_task</code> 至少包含：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">ev_task</span> &#123;</span></span><br><span class="line">    <span class="type">ev_exec_t</span> *exec;</span><br><span class="line">    <span class="type">ev_task_func_t</span> *func;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">slnode</span> _<span class="title">node</span>;</span></span><br><span class="line">    <span class="type">void</span> *_data;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>这意味着每一个等待 Future 的动作都明确指定：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Future ready 后</span><br><span class="line">    在哪个 executor 上</span><br><span class="line">    执行哪个 task function</span><br></pre></td></tr></table></figure><h3 id="3-4-Executor：真正执行-task-的对象"><a href="#3-4-Executor：真正执行-task-的对象" class="headerlink" title="3.4 Executor：真正执行 task 的对象"></a>3.4 Executor：真正执行 task 的对象</h3><p>Future 只决定 task 何时具备提交条件，executor 决定 task 在哪里、何时运行。</p><p>在典型 Lely CANopen 程序中，executor 往往来自 <code>ev_loop_t</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">Future</span><br><span class="line">  └─ 等待 task</span><br><span class="line">       └─ task.exec</span><br><span class="line">            └─ ev_loop 的 executor</span><br><span class="line">                 └─ event loop 线程执行 task.func</span><br></pre></td></tr></table></figure><p>因此，Future 与 event loop 的关系是：</p><blockquote><p>Future 负责完成依赖，event loop 负责调度执行。</p></blockquote><hr><h2 id="4-最重要的内存布局：Promise-内嵌-Future"><a href="#4-最重要的内存布局：Promise-内嵌-Future" class="headerlink" title="4. 最重要的内存布局：Promise 内嵌 Future"></a>4. 最重要的内存布局：Promise 内嵌 Future</h2><p><code>future.c</code> 中的两个核心结构如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">ev_future</span> &#123;</span></span><br><span class="line">    <span class="type">atomic_int</span> state;</span><br><span class="line">    <span class="type">void</span> *value;</span><br><span class="line">    <span class="type">mtx_t</span> mtx;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sllist</span> <span class="title">queue</span>;</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">ev_promise</span> &#123;</span></span><br><span class="line">    <span class="type">atomic_size_t</span> refcnt;</span><br><span class="line">    <span class="type">ev_promise_dtor_t</span> *dtor;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">ev_future</span> <span class="title">future</span>;</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>实际代码会根据单线程、Windows、C11 原子等构建条件选择不同字段类型，但逻辑结构不变。</p><p>对象关系不是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Promise ──指针──&gt; Future</span><br></pre></td></tr></table></figure><p>而是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">一整块分配内存</span><br><span class="line">┌────────────────────────────┐</span><br><span class="line">│ ev_promise_t               │</span><br><span class="line">│  ├─ refcnt                 │</span><br><span class="line">│  ├─ dtor                   │</span><br><span class="line">│  └─ ev_future_t future     │</span><br><span class="line">│      ├─ state              │</span><br><span class="line">│      ├─ value              │</span><br><span class="line">│      ├─ mtx                │</span><br><span class="line">│      └─ queue              │</span><br><span class="line">├────────────────────────────┤</span><br><span class="line">│ 用户共享数据区 size bytes  │</span><br><span class="line">└────────────────────────────┘</span><br></pre></td></tr></table></figure><p><code>ev_promise_create(size, dtor)</code> 实际分配：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">calloc</span>(<span class="number">1</span>, EV_PROMISE_SIZE + size);</span><br></pre></td></tr></table></figure><p>而 <code>ev_promise_data()</code> 返回 Promise 结构后方的尾随数据区：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">return</span> (<span class="type">char</span> *)promise + EV_PROMISE_SIZE;</span><br></pre></td></tr></table></figure><p>这套布局有三个直接效果。</p><h3 id="4-1-Promise-和-Future-共用生命周期"><a href="#4-1-Promise-和-Future-共用生命周期" class="headerlink" title="4.1 Promise 和 Future 共用生命周期"></a>4.1 Promise 和 Future 共用生命周期</h3><p><code>ev_future_acquire()</code> 并不是增加 Future 自己的引用计数，而是先通过 <code>structof()</code> 从内嵌 Future 找回外层 Promise，然后增加 Promise 的 <code>refcnt</code>。</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">ev_future_t *</span><br><span class="line">    ↓ structof</span><br><span class="line">对应的 ev_promise_t</span><br><span class="line">    ↓</span><br><span class="line">共享 refcnt</span><br></pre></td></tr></table></figure><p>因此 <code>refcnt</code> 的真实语义是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">所有 Promise 引用数量</span><br><span class="line">+</span><br><span class="line">所有 Future 引用数量</span><br></pre></td></tr></table></figure><h3 id="4-2-结果对象可以和控制块一次分配"><a href="#4-2-结果对象可以和控制块一次分配" class="headerlink" title="4.2 结果对象可以和控制块一次分配"></a>4.2 结果对象可以和控制块一次分配</h3><p>C 接口可用尾随数据区存放任意共享状态；C++ <code>Promise&lt;T, E&gt;</code> 则直接在该区域 placement-new 一个：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">util::Result&lt;T, E&gt;</span><br></pre></td></tr></table></figure><p>这样控制块、Future 状态和结果对象可以在一次分配中完成。</p><h3 id="4-3-Future-指针不是独立分配的对象"><a href="#4-3-Future-指针不是独立分配的对象" class="headerlink" title="4.3 Future 指针不是独立分配的对象"></a>4.3 Future 指针不是独立分配的对象</h3><p><code>ev_promise_get_future()</code> 返回的是：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">&amp;promise-&gt;<span class="built_in">future</span></span><br></pre></td></tr></table></figure><p>外加一次引用计数增加。</p><p>所以不能单独 <code>free(future)</code>，也不能假设 Future 的地址来自独立分配。</p><hr><h2 id="5-Future-状态机：WAITING、SETTING、READY"><a href="#5-Future-状态机：WAITING、SETTING、READY" class="headerlink" title="5. Future 状态机：WAITING、SETTING、READY"></a>5. Future 状态机：WAITING、SETTING、READY</h2><p><code>future.c</code> 定义了三个状态：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">enum</span> <span class="title">ev_future_state</span> &#123;</span></span><br><span class="line">    EV_FUTURE_WAITING,</span><br><span class="line">    EV_FUTURE_SETTING,</span><br><span class="line">    EV_FUTURE_READY</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>状态变化只能单向进行：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">stateDiagram-v2</span><br><span class="line">    [*] --&gt; WAITING</span><br><span class="line">    WAITING --&gt; SETTING: ev_promise_set_acquire() 成功</span><br><span class="line">    SETTING --&gt; READY: ev_promise_set_release()</span><br><span class="line">    READY --&gt; READY: 后续 set 请求失败</span><br></pre></td></tr></table></figure><h3 id="5-1-WAITING"><a href="#5-1-WAITING" class="headerlink" title="5.1 WAITING"></a>5.1 WAITING</h3><p>含义：</p><ul><li>Promise 尚未被任何完成者占有；</li><li>Future 尚未 ready；</li><li>task 可以继续通过 <code>ev_future_submit()</code> 加入等待队列。</li></ul><h3 id="5-2-SETTING"><a href="#5-2-SETTING" class="headerlink" title="5.2 SETTING"></a>5.2 SETTING</h3><p>含义：</p><ul><li>某一个线程已经获得完成权；</li><li>其他线程不能再完成同一个 Promise；</li><li>结果可能还在构造或写入中；</li><li>Future 对外仍不应被视为 ready。</li></ul><h3 id="5-3-READY"><a href="#5-3-READY" class="headerlink" title="5.3 READY"></a>5.3 READY</h3><p>含义：</p><ul><li><code>value</code> 已经写入；</li><li>结果数据已经发布；</li><li>原先等待的 task 已从 Future 队列移出并提交给 executor；</li><li>后续新提交的 task 不再排队，会直接提交给 executor。</li></ul><hr><h2 id="6-为什么完成-Promise-要拆成-acquire-和-release-两步"><a href="#6-为什么完成-Promise-要拆成-acquire-和-release-两步" class="headerlink" title="6. 为什么完成 Promise 要拆成 acquire 和 release 两步"></a>6. 为什么完成 Promise 要拆成 acquire 和 release 两步</h2><p>简单接口是：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">ev_promise_set</span><span class="params">(<span class="type">ev_promise_t</span> *promise, <span class="type">void</span> *value)</span>;</span><br></pre></td></tr></table></figure><p>内部等价于：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> result = ev_promise_set_acquire(promise);</span><br><span class="line"><span class="keyword">if</span> (result)</span><br><span class="line">    ev_promise_set_release(promise, value);</span><br><span class="line"><span class="keyword">return</span> result;</span><br></pre></td></tr></table></figure><p>两阶段设计不是多余封装，而是为了把“抢占完成权”和“构造结果”分开。</p><h3 id="6-1-第一步：抢占唯一完成权"><a href="#6-1-第一步：抢占唯一完成权" class="headerlink" title="6.1 第一步：抢占唯一完成权"></a>6.1 第一步：抢占唯一完成权</h3><p>多线程下，<code>ev_promise_set_acquire()</code> 使用原子 compare-exchange：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">期望状态：WAITING</span><br><span class="line">目标状态：SETTING</span><br></pre></td></tr></table></figure><p>只有一个线程能成功完成这个状态转换。</p><p>假设两个完成路径同时到达：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">线程 A：WAITING → SETTING，成功</span><br><span class="line">线程 B：发现状态已不是 WAITING，失败</span><br></pre></td></tr></table></figure><p>因此，一次性语义由状态机保证，而不是靠调用者约定。</p><h3 id="6-2-第二步：构造并发布结果"><a href="#6-2-第二步：构造并发布结果" class="headerlink" title="6.2 第二步：构造并发布结果"></a>6.2 第二步：构造并发布结果</h3><p>获得完成权后，调用者可以：</p><ul><li>填充尾随共享数据区；</li><li>构造 C++ <code>Result&lt;T, E&gt;</code>；</li><li>保存错误码；</li><li>保存异常；</li><li>决定最终传给 <code>ev_future_get()</code> 的地址。</li></ul><p>完成后再调用：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">ev_promise_set_release</span><span class="params">(<span class="type">ev_promise_t</span> *promise, <span class="type">void</span> *value)</span>;</span><br></pre></td></tr></table></figure><p>它执行以下关键步骤：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">1. future-&gt;value = value</span><br><span class="line">2. 获取 future-&gt;mtx</span><br><span class="line">3. 把 future-&gt;queue 整体转移到局部队列</span><br><span class="line">4. release-store state = READY</span><br><span class="line">5. 释放 future-&gt;mtx</span><br><span class="line">6. 将局部队列中的 task 提交到各自 executor</span><br></pre></td></tr></table></figure><h3 id="6-3-为什么不在持锁状态下执行-executor-post"><a href="#6-3-为什么不在持锁状态下执行-executor-post" class="headerlink" title="6.3 为什么不在持锁状态下执行 executor post"></a>6.3 为什么不在持锁状态下执行 executor post</h3><p>Future 内部互斥锁只用于保护：</p><ul><li>ready 检查与 task 入队之间的原子关系；</li><li>Future 等待队列的转移和移除。</li></ul><p>它不应该覆盖 executor 的调度过程。否则 executor 可能：</p><ul><li>立即运行 task；</li><li>再次访问同一个 Future；</li><li>引发锁嵌套或死锁；</li><li>造成不必要的长临界区。</li></ul><p>因此，源码先把等待队列整体转移到局部变量，解锁后再提交 task。</p><hr><h2 id="7-ev-future-submit-如何避免丢失完成通知"><a href="#7-ev-future-submit-如何避免丢失完成通知" class="headerlink" title="7. ev_future_submit() 如何避免丢失完成通知"></a>7. <code>ev_future_submit()</code> 如何避免丢失完成通知</h2><p>这是整套实现中最值得学习的并发细节之一。</p><p>表面上存在一个经典竞态：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">线程 A：检查 Future 还没 ready</span><br><span class="line">线程 B：把 Future 设为 ready，并唤醒当前等待者</span><br><span class="line">线程 A：此后才把 task 放入等待队列</span><br></pre></td></tr></table></figure><p>如果实现不正确，线程 A 的 task 将永久留在队列里，再也不会被提交。</p><p>Lely 用同一把 <code>future-&gt;mtx</code> 消除了这个窗口。</p><p><code>ev_future_submit()</code> 的核心逻辑是：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">ev_exec_on_task_init(exec);</span><br><span class="line">mtx_lock(&amp;<span class="built_in">future</span>-&gt;mtx);</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (ev_future_is_ready(<span class="built_in">future</span>)) &#123;</span><br><span class="line">    mtx_unlock(&amp;<span class="built_in">future</span>-&gt;mtx);</span><br><span class="line">    ev_exec_post(exec, task);</span><br><span class="line">    ev_exec_on_task_fini(exec);</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    sllist_push_back(&amp;<span class="built_in">future</span>-&gt;<span class="built_in">queue</span>, &amp;task-&gt;_node);</span><br><span class="line">    mtx_unlock(&amp;<span class="built_in">future</span>-&gt;mtx);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>而 <code>ev_promise_set_release()</code> 也在同一把锁下：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">转移全部等待 task</span><br><span class="line">设置 READY</span><br></pre></td></tr></table></figure><p>因此只会出现两种情况。</p><h3 id="情况-A：submit-先获得锁"><a href="#情况-A：submit-先获得锁" class="headerlink" title="情况 A：submit 先获得锁"></a>情况 A：submit 先获得锁</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">submit：看到未 ready</span><br><span class="line">submit：task 入队</span><br><span class="line">submit：解锁</span><br><span class="line">set_release：获得锁</span><br><span class="line">set_release：取走该 task</span><br><span class="line">set_release：设置 ready</span><br><span class="line">set_release：解锁并提交 task</span><br></pre></td></tr></table></figure><h3 id="情况-B：set-release-先获得锁"><a href="#情况-B：set-release-先获得锁" class="headerlink" title="情况 B：set_release 先获得锁"></a>情况 B：set_release 先获得锁</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">set_release：取走原等待队列</span><br><span class="line">set_release：设置 ready</span><br><span class="line">set_release：解锁</span><br><span class="line">submit：获得锁</span><br><span class="line">submit：看到 ready</span><br><span class="line">submit：直接提交 task</span><br></pre></td></tr></table></figure><p>不存在第三种“既没入队，也没直接提交”的状态。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant S as submit线程</span><br><span class="line">    participant F as Future</span><br><span class="line">    participant P as Promise完成线程</span><br><span class="line">    participant E as Executor</span><br><span class="line"></span><br><span class="line">    S-&gt;&gt;F: lock(mtx)</span><br><span class="line">    alt Future 尚未 ready</span><br><span class="line">        S-&gt;&gt;F: task 加入 queue</span><br><span class="line">        S-&gt;&gt;F: unlock(mtx)</span><br><span class="line">        P-&gt;&gt;F: lock(mtx)</span><br><span class="line">        P-&gt;&gt;F: 取走 queue + state=READY</span><br><span class="line">        P-&gt;&gt;F: unlock(mtx)</span><br><span class="line">        P-&gt;&gt;E: post(task)</span><br><span class="line">    else Future 已 ready</span><br><span class="line">        S-&gt;&gt;F: unlock(mtx)</span><br><span class="line">        S-&gt;&gt;E: post(task)</span><br><span class="line">    end</span><br></pre></td></tr></table></figure><hr><h2 id="8-Future-task-与-outstanding-work-的闭环"><a href="#8-Future-task-与-outstanding-work-的闭环" class="headerlink" title="8. Future task 与 outstanding work 的闭环"></a>8. Future task 与 outstanding work 的闭环</h2><p><code>ev_future_submit()</code> 在 task 真正进入 executor 队列之前，先调用：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ev_exec_on_task_init(exec);</span><br></pre></td></tr></table></figure><p>它的含义是：</p><blockquote><p>这份 task 现在还不能执行，但未来会提交，请不要让 executor&#x2F;event loop 因当前队列为空而提前退出。</p></blockquote><p>随后会在三个出口之一调用 <code>ev_exec_on_task_fini()</code>：</p><table><thead><tr><th>出口</th><th>处理</th></tr></thead><tbody><tr><td>Future ready，task 被 post</td><td><code>ev_exec_post()</code> 后 <code>on_task_fini()</code></td></tr><tr><td>Future cancel，task 仍被 post</td><td><code>ev_task_queue_post()</code> 内调用 <code>on_task_fini()</code></td></tr><tr><td>Future abort，task 不执行</td><td><code>ev_task_queue_abort()</code> 内调用 <code>on_task_fini()</code></td></tr></tbody></table><p>这构成严格闭环：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">submit Future waiter</span><br><span class="line">    ↓</span><br><span class="line">on_task_init：登记未来工作</span><br><span class="line">    ↓</span><br><span class="line">等待 Future 完成、取消或中止</span><br><span class="line">    ↓</span><br><span class="line">on_task_fini：撤销未来工作登记</span><br></pre></td></tr></table></figure><p>对 <code>ev_loop</code> 而言，这份等待 Future 的 task 会计入 <code>ntasks</code>，即使它尚未进入 event loop 的真实执行队列。</p><p>这正是为什么：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">loop queue 暂时为空</span><br></pre></td></tr></table></figure><p>不等于：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">系统已经没有异步工作</span><br></pre></td></tr></table></figure><hr><h2 id="9-ev-future-get-不会等待"><a href="#9-ev-future-get-不会等待" class="headerlink" title="9. ev_future_get() 不会等待"></a>9. <code>ev_future_get()</code> 不会等待</h2><p>C 接口：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">void</span> *<span class="title function_">ev_future_get</span><span class="params">(<span class="type">const</span> <span class="type">ev_future_t</span> *<span class="built_in">future</span>)</span>;</span><br></pre></td></tr></table></figure><p>它内部只做：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">assert(ev_future_is_ready(<span class="built_in">future</span>));</span><br><span class="line"><span class="keyword">return</span> <span class="built_in">future</span>-&gt;value;</span><br></pre></td></tr></table></figure><p>它没有条件变量，没有 sleep，没有 Poll，也不会帮调用者推进 event loop。</p><p>所以正确用法是：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (ev_future_is_ready(<span class="built_in">future</span>)) &#123;</span><br><span class="line">    <span class="type">void</span> *value = ev_future_get(<span class="built_in">future</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>或者先通过 <code>ev_future_submit()</code> 登记完成 task。</p><p>错误理解是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ev_future_get() 会一直阻塞，直到结果完成</span><br></pre></td></tr></table></figure><p>正确理解是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ev_future_get() 只是读取一个已经发布的结果</span><br></pre></td></tr></table></figure><p>C++ <code>Future&lt;T, E&gt;::get()</code> 做了更友好的检查：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (!*<span class="keyword">this</span> || !<span class="built_in">is_ready</span>())</span><br><span class="line">    <span class="keyword">throw</span> <span class="built_in">future_not_ready</span>(<span class="string">&quot;get&quot;</span>);</span><br></pre></td></tr></table></figure><p>但它同样不会阻塞。</p><hr><h2 id="10-release-acquire-内存序解决了什么"><a href="#10-release-acquire-内存序解决了什么" class="headerlink" title="10. release&#x2F;acquire 内存序解决了什么"></a>10. release&#x2F;acquire 内存序解决了什么</h2><p>多线程实现中，结果发布路径采用：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">写入 result/value</span><br><span class="line">    ↓</span><br><span class="line">release-store READY</span><br></pre></td></tr></table></figure><p>观察路径采用：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">acquire-load state</span><br><span class="line">    ↓</span><br><span class="line">确认 READY</span><br><span class="line">    ↓</span><br><span class="line">读取 value/result</span><br></pre></td></tr></table></figure><p>其目标是建立以下可见性关系：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Promise 完成线程在 READY 之前写入的结果</span><br><span class="line">happens-before</span><br><span class="line">Future 观察线程在 acquire 读取 READY 后访问结果</span><br></pre></td></tr></table></figure><p>因此，观察者只要通过 acquire 语义确认 Future 已 ready，就能看到完成者在发布 ready 之前写入的数据。</p><p>需要区分两类同步：</p><table><thead><tr><th>同步对象</th><th>保护内容</th></tr></thead><tbody><tr><td>原子 <code>state</code></td><td>完成状态和结果发布可见性。</td></tr><tr><td><code>future-&gt;mtx</code></td><td>等待 task 队列，以及 ready 检查与入队之间的竞态。</td></tr><tr><td>原子 <code>refcnt</code></td><td>Promise&#x2F;Future 共享控制块的生命周期。</td></tr></tbody></table><p>互斥锁不能替代引用计数；引用计数也不能替代等待队列锁。三者职责不同。</p><hr><h2 id="11-Promise-Future-引用计数和销毁顺序"><a href="#11-Promise-Future-引用计数和销毁顺序" class="headerlink" title="11. Promise&#x2F;Future 引用计数和销毁顺序"></a>11. Promise&#x2F;Future 引用计数和销毁顺序</h2><h3 id="11-1-初始引用"><a href="#11-1-初始引用" class="headerlink" title="11.1 初始引用"></a>11.1 初始引用</h3><p><code>ev_promise_create()</code> 初始化：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">refcnt = 1</span><br></pre></td></tr></table></figure><p>这一个引用属于返回给调用者的 Promise。</p><h3 id="11-2-获取-Future-会增加引用"><a href="#11-2-获取-Future-会增加引用" class="headerlink" title="11.2 获取 Future 会增加引用"></a>11.2 获取 Future 会增加引用</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">ev_future_t</span> *<span class="built_in">future</span> = ev_promise_get_future(promise);</span><br></pre></td></tr></table></figure><p>内部会增加同一个 <code>refcnt</code>。</p><p>典型状态：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Promise 引用：1</span><br><span class="line">Future 引用：1</span><br><span class="line">总 refcnt：2</span><br></pre></td></tr></table></figure><h3 id="11-3-Promise-和-Future-的-acquire-release-都操作同一计数"><a href="#11-3-Promise-和-Future-的-acquire-release-都操作同一计数" class="headerlink" title="11.3 Promise 和 Future 的 acquire&#x2F;release 都操作同一计数"></a>11.3 Promise 和 Future 的 acquire&#x2F;release 都操作同一计数</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">ev_promise_acquire/release</span><br><span class="line">    └─ 直接修改 promise-&gt;refcnt</span><br><span class="line"></span><br><span class="line">ev_future_acquire/release</span><br><span class="line">    └─ 先从 future 找到外层 promise</span><br><span class="line">       再修改 promise-&gt;refcnt</span><br></pre></td></tr></table></figure><h3 id="11-4-最后一个引用释放时的顺序"><a href="#11-4-最后一个引用释放时的顺序" class="headerlink" title="11.4 最后一个引用释放时的顺序"></a>11.4 最后一个引用释放时的顺序</h3><p>当 <code>refcnt</code> 从 1 变为 0：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">1. 调用用户注册的共享数据析构函数 dtor</span><br><span class="line">2. 调用 ev_future_fini()</span><br><span class="line">3. 销毁 future mutex</span><br><span class="line">4. 释放整个 Promise 控制块</span><br></pre></td></tr></table></figure><p>引用计数减一使用 release 语义，最后一个释放者再执行 acquire fence，这是典型的“最后引用负责销毁”模式。</p><hr><h2 id="12-一个容易忽略的设计：Future-被遗弃时仍会调度等待-task"><a href="#12-一个容易忽略的设计：Future-被遗弃时仍会调度等待-task" class="headerlink" title="12. 一个容易忽略的设计：Future 被遗弃时仍会调度等待 task"></a>12. 一个容易忽略的设计：Future 被遗弃时仍会调度等待 task</h2><p><code>ev_future_fini()</code> 的行为不是直接丢弃等待队列，而是：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ev_task_queue_post(&amp;<span class="built_in">future</span>-&gt;<span class="built_in">queue</span>);</span><br></pre></td></tr></table></figure><p>也就是说，如果 Promise&#x2F;Future 的最后一个引用被释放，而 Future 仍未 ready，之前通过 <code>ev_future_submit()</code> 登记的 task 仍会被提交到 executor。</p><p>这看起来反直觉，但它是为了让等待者能够观察到：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">我被唤醒了</span><br><span class="line">但 Future 并没有 ready</span><br></pre></td></tr></table></figure><p>从而识别共享状态已被遗弃。</p><p>因此，一个通用 Future 完成 task 不应无条件调用 <code>ev_future_get()</code>，而应先判断：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (ev_future_is_ready(<span class="built_in">future</span>)) &#123;</span><br><span class="line">    <span class="comment">// 正常完成</span></span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="comment">// Future 被取消、遗弃或以非 ready 路径唤醒</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这也是组合 Future 可以处理“输入 Future 在 ready 前被遗弃”的基础。</p><hr><h2 id="13-cancel-和-abort-的区别"><a href="#13-cancel-和-abort-的区别" class="headerlink" title="13. cancel() 和 abort() 的区别"></a>13. <code>cancel()</code> 和 <code>abort()</code> 的区别</h2><p>两者都会从 Future 的等待队列中移除尚未调度的 task，但后续处理不同。</p><table><thead><tr><th>接口</th><th>task 是否执行</th><th>outstanding work 是否结束</th></tr></thead><tbody><tr><td><code>ev_future_cancel()</code></td><td>是，仍提交到 task 的 executor</td><td>是</td></tr><tr><td><code>ev_future_abort()</code></td><td>否，直接终止</td><td>是</td></tr></tbody></table><h3 id="13-1-cancel"><a href="#13-1-cancel" class="headerlink" title="13.1 cancel"></a>13.1 cancel</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">从 Future queue 移除 task</span><br><span class="line">    ↓</span><br><span class="line">post 到 executor</span><br><span class="line">    ↓</span><br><span class="line">task function 仍会运行</span><br></pre></td></tr></table></figure><p>所以 cancel 的语义不是“保证回调不执行”，而是：</p><blockquote><p>不再等待 Future，尽快调度 task，由 task 自己处理取消状态。</p></blockquote><h3 id="13-2-abort"><a href="#13-2-abort" class="headerlink" title="13.2 abort"></a>13.2 abort</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">从 Future queue 移除 task</span><br><span class="line">    ↓</span><br><span class="line">不提交 executor</span><br><span class="line">    ↓</span><br><span class="line">只结束 outstanding work</span><br></pre></td></tr></table></figure><p>这才是“该 task 不再执行”。</p><h3 id="13-3-工程风险"><a href="#13-3-工程风险" class="headerlink" title="13.3 工程风险"></a>13.3 工程风险</h3><p>如果 task 内部持有需要在回调中释放的资源，则直接 abort 可能绕过该清理逻辑。资源所有权必须由外围对象或析构函数保证，不能默认依赖 task function 一定会运行。</p><p>官方还特别提示：<code>when_all()</code> 和 <code>when_any()</code> 内部提交的 task 不应由调用者 abort，否则可能导致资源泄漏。</p><hr><h2 id="14-when-all-的实现思路"><a href="#14-when-all-的实现思路" class="headerlink" title="14. when_all() 的实现思路"></a>14. <code>when_all()</code> 的实现思路</h2><p><code>ev_future_when_all_n()</code> 创建一个新的 Promise&#x2F;Future，用于表示多个输入 Future 的聚合完成状态。</p><p>内部共享数据大致为：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">ev_future_when_all</span> &#123;</span></span><br><span class="line">    <span class="type">size_t</span> idx;</span><br><span class="line">    <span class="type">ev_promise_t</span> *promise;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">ev_task</span> <span class="title">task</span>;</span></span><br><span class="line">    <span class="type">size_t</span> n;</span><br><span class="line">    <span class="type">ev_future_t</span> *futures[];</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>它没有为每个输入 Future 创建一个 task，而是使用同一个 task 按顺序等待：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">等待 futures[0]</span><br><span class="line">    ↓</span><br><span class="line">完成后复用同一个 task 等待 futures[1]</span><br><span class="line">    ↓</span><br><span class="line">完成后等待 futures[2]</span><br><span class="line">    ↓</span><br><span class="line">...</span><br></pre></td></tr></table></figure><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    F0[Future 0] --&gt; T[同一个 when_all task]</span><br><span class="line">    T --&gt; F1[Future 1]</span><br><span class="line">    F1 --&gt; T</span><br><span class="line">    T --&gt; F2[Future 2]</span><br><span class="line">    F2 --&gt; T</span><br><span class="line">    T --&gt; R[完成聚合 Promise]</span><br></pre></td></tr></table></figure><p>这种设计的优势是：</p><ul><li>只需要一个 <code>ev_task</code>；</li><li>组合逻辑简单；</li><li>不需要额外的原子完成计数器；</li><li>适合 event-loop 串行推进模型。</li></ul><p>其代价是：</p><ul><li>它按顺序观察输入 Future；</li><li>后面的 Future 即使已经 ready，也要等前面的检查流程推进到它；</li><li>精确行为还受到输入 Future 是否被遗弃以及引用唯一性检查影响。</li></ul><p>对外契约是：</p><ul><li>全部输入 ready 时，结果表示 ready 的数量；</li><li>某个输入在 ready 前被遗弃时，结果表示对应索引；</li><li>输入数量为 0 时，立即返回 ready Future，结果指针为空。</li></ul><p>工程上不要把 <code>when_all()</code> 的结果简单当作业务数据，它是一个聚合状态索引&#x2F;计数。</p><hr><h2 id="15-when-any-的实现思路"><a href="#15-when-any-的实现思路" class="headerlink" title="15. when_any() 的实现思路"></a>15. <code>when_any()</code> 的实现思路</h2><p><code>ev_future_when_any_n()</code> 为每一个输入 Future 创建一个独立等待项：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">ev_future_when_any</span> &#123;</span></span><br><span class="line">    <span class="type">size_t</span> idx;</span><br><span class="line">    <span class="type">ev_promise_t</span> *promise;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">ev_task</span> <span class="title">task</span>;</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Future 0 → waiter[0] ┐</span><br><span class="line">Future 1 → waiter[1] ├→ 竞争完成同一个聚合 Promise</span><br><span class="line">Future 2 → waiter[2] ┘</span><br></pre></td></tr></table></figure><p>每个 waiter 被唤醒后都会尝试：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ev_promise_set(when-&gt;promise, &amp;when-&gt;idx);</span><br></pre></td></tr></table></figure><p>由于 Promise 是一次性的，只有第一个执行 <code>WAITING → SETTING</code> 的 waiter 能成功。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant F0 as Future0 waiter</span><br><span class="line">    participant F1 as Future1 waiter</span><br><span class="line">    participant P as 聚合 Promise</span><br><span class="line"></span><br><span class="line">    F1-&gt;&gt;P: set(index=1)</span><br><span class="line">    P--&gt;&gt;F1: 成功，WAITING→SETTING→READY</span><br><span class="line">    F0-&gt;&gt;P: set(index=0)</span><br><span class="line">    P--&gt;&gt;F0: 失败，Promise 已完成</span><br></pre></td></tr></table></figure><p>因此，<code>when_any()</code> 的本质是：</p><blockquote><p>多个 task 竞争完成同一个 Promise，原子状态机决定唯一赢家。</p></blockquote><p>对外结果为第一个 ready 或被遗弃的输入 Future 索引。</p><p>剩余 waiter 即使后来运行，也无法再次改变聚合结果；它们只需释放自己持有的 Promise 引用。</p><hr><h2 id="16-C-Promise-做了什么"><a href="#16-C-Promise-做了什么" class="headerlink" title="16. C++ Promise&lt;T, E&gt; 做了什么"></a>16. C++ <code>Promise&lt;T, E&gt;</code> 做了什么</h2><p>C++ 层把 C 的无类型 <code>void *</code> 结果包装为：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">util::Result&lt;T, E&gt;</span><br></pre></td></tr></table></figure><h3 id="16-1-构造"><a href="#16-1-构造" class="headerlink" title="16.1 构造"></a>16.1 构造</h3><p><code>Promise&lt;T, E&gt;</code> 创建足够大的共享数据区：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">ev_promise_create</span>(<span class="built_in">sizeof</span>(result_type), dtor)</span><br></pre></td></tr></table></figure><p>然后在尾随数据区 placement-new：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">new</span> (<span class="built_in">ev_promise_data</span>(promise_)) <span class="built_in">result_type</span>();</span><br></pre></td></tr></table></figure><h3 id="16-2-析构"><a href="#16-2-析构" class="headerlink" title="16.2 析构"></a>16.2 析构</h3><p>C 层最后释放共享状态时调用注册的 <code>dtor</code>，显式析构 <code>Result&lt;T, E&gt;</code>。</p><h3 id="16-3-set"><a href="#16-3-set" class="headerlink" title="16.3 set"></a>16.3 set</h3><p>C++ <code>set()</code> 仍遵循底层两阶段完成协议：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">set_acquire</span><br><span class="line">    ↓</span><br><span class="line">给 Result&lt;T, E&gt; 赋值</span><br><span class="line">    ↓</span><br><span class="line">set_release(result 地址)</span><br></pre></td></tr></table></figure><p>如果结果赋值抛出异常，源码仍调用 <code>set_release(..., nullptr)</code> 结束 Promise，然后重新抛出异常。</p><p>这意味着完成权一旦被当前调用者取得，就不能把状态退回 WAITING。</p><h3 id="16-4-RAII"><a href="#16-4-RAII" class="headerlink" title="16.4 RAII"></a>16.4 RAII</h3><p>复制 Promise&#x2F;Future 会 acquire 引用；析构会 release；移动操作转移裸指针并清空源对象。</p><p>所以 C++ 层解决的是：</p><ul><li>引用计数自动管理；</li><li>结果类型安全；</li><li>错误值统一封装；</li><li>异常安全；</li><li>continuation 组合。</li></ul><p>它没有改变底层状态机。</p><hr><h2 id="17-C-Future-做了什么"><a href="#17-C-Future-做了什么" class="headerlink" title="17. C++ Future&lt;T, E&gt; 做了什么"></a>17. C++ <code>Future&lt;T, E&gt;</code> 做了什么</h2><p>C++ Future 主要是薄封装：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Future</span> &#123;</span><br><span class="line">    <span class="type">ev_future_t</span> *future_&#123;<span class="literal">nullptr</span>&#125;;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>对应关系如下：</p><table><thead><tr><th>C++ 接口</th><th>C 接口</th></tr></thead><tbody><tr><td><code>is_ready()</code></td><td><code>ev_future_is_ready()</code></td></tr><tr><td><code>get()</code></td><td><code>ev_future_get()</code></td></tr><tr><td><code>submit()</code></td><td><code>ev_future_submit()</code></td></tr><tr><td><code>cancel()</code></td><td><code>ev_future_cancel()</code></td></tr><tr><td><code>abort()</code></td><td><code>ev_future_abort()</code></td></tr><tr><td>拷贝构造</td><td><code>ev_future_acquire()</code></td></tr><tr><td>析构</td><td><code>ev_future_release()</code></td></tr></tbody></table><p><code>get()</code> 返回的是共享状态中 <code>Result&lt;T, E&gt;</code> 的引用，不是复制一份结果。</p><p>因此结果对象的有效期受共享状态生命周期约束：只要调用方仍在使用该引用，就必须保证至少有一个 Promise&#x2F;Future 引用继续持有共享状态。</p><hr><h2 id="18-AsyncTask：把函数执行结果转成-Future"><a href="#18-AsyncTask：把函数执行结果转成-Future" class="headerlink" title="18. AsyncTask：把函数执行结果转成 Future"></a>18. <code>AsyncTask</code>：把函数执行结果转成 Future</h2><p><code>make_async_task()</code> 会把以下内容放在 Promise 的尾随共享数据区：</p><ul><li>一个继承自 <code>ev_task</code> 的 <code>AsyncTask</code>；</li><li>Promise 包装对象；</li><li>callable 和参数；</li><li>最终结果；</li><li>必要时保存 inner Future。</li></ul><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">Promise 控制块</span><br><span class="line">┌───────────────────────────┐</span><br><span class="line">│ refcnt / dtor / Future    │</span><br><span class="line">├───────────────────────────┤</span><br><span class="line">│ AsyncTask                 │</span><br><span class="line">│  ├─ ev_task               │</span><br><span class="line">│  ├─ Promise               │</span><br><span class="line">│  ├─ Invoker               │</span><br><span class="line">│  ├─ inner Future（可选）   │</span><br><span class="line">│  └─ Result                │</span><br><span class="line">└───────────────────────────┘</span><br></pre></td></tr></table></figure><h3 id="18-1-callable-返回普通值"><a href="#18-1-callable-返回普通值" class="headerlink" title="18.1 callable 返回普通值"></a>18.1 callable 返回普通值</h3><p>执行 task 时：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">调用 callable</span><br><span class="line">    ↓</span><br><span class="line">成功：Result 保存返回值</span><br><span class="line">失败：Result 保存 exception_ptr</span><br><span class="line">    ↓</span><br><span class="line">完成 Promise</span><br></pre></td></tr></table></figure><p><code>catch_result()</code> 同时处理普通返回值和 <code>void</code> 返回值。</p><h3 id="18-2-callable-返回-Future"><a href="#18-2-callable-返回-Future" class="headerlink" title="18.2 callable 返回 Future"></a>18.2 callable 返回 Future</h3><p>这时使用另一套 <code>AsyncTask</code> 特化，实现 implicit unwrapping。</p><p>第一次运行 task：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">执行 callable</span><br><span class="line">    ↓</span><br><span class="line">得到 inner Future</span><br><span class="line">    ↓</span><br><span class="line">把同一个 task 重新 submit 到 inner Future</span><br></pre></td></tr></table></figure><p>第二次由 inner Future 唤醒 task：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">读取 inner Future 的 Result</span><br><span class="line">    ↓</span><br><span class="line">提取 value()</span><br><span class="line">    ↓</span><br><span class="line">完成外层 Promise</span><br></pre></td></tr></table></figure><p>所以：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Future&lt;A&gt;.<span class="built_in">then</span>(... -&gt; Future&lt;B&gt;)</span><br></pre></td></tr></table></figure><p>最终得到的是：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Future&lt;B&gt;</span><br></pre></td></tr></table></figure><p>而不是：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Future&lt;Future&lt;B&gt;&gt;</span><br></pre></td></tr></table></figure><p>这就是源码注释所称的 implicit unwrapping。</p><hr><h2 id="19-then-如何构造-continuation"><a href="#19-then-如何构造-continuation" class="headerlink" title="19. then() 如何构造 continuation"></a>19. <code>then()</code> 如何构造 continuation</h2><p><code>Future::then()</code> 的流程是：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">auto</span> task = <span class="built_in">make_async_task</span>(exec, continuation, *<span class="keyword">this</span>);</span><br><span class="line"><span class="keyword">auto</span> future = task-&gt;<span class="built_in">get_future</span>();</span><br><span class="line"><span class="built_in">submit</span>(*task);</span><br><span class="line"><span class="keyword">return</span> future;</span><br></pre></td></tr></table></figure><p>完整链路：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">原 Future A</span><br><span class="line">    ↓ submit continuation task</span><br><span class="line">A ready</span><br><span class="line">    ↓</span><br><span class="line">executor 执行 continuation(A)</span><br><span class="line">    ↓</span><br><span class="line">结果写入 Promise B</span><br><span class="line">    ↓</span><br><span class="line">Future B ready</span><br></pre></td></tr></table></figure><p>源码特意先取得返回 Future，再把 task 提交给原 Future：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">先 get_future()</span><br><span class="line">再 submit(task)</span><br></pre></td></tr></table></figure><p>原因是 executor 允许 task 在 <code>submit()</code> 返回前开始执行。如果先 submit，task 可能立即运行并释放其内部状态，随后调用者再取 Future 就可能发生竞态。</p><p>这是一条非常通用的异步编程经验：</p><blockquote><p>在提交可能立即执行的异步任务前，先取得调用方后续需要的所有引用和句柄。</p></blockquote><hr><h2 id="20-async-如何工作"><a href="#20-async-如何工作" class="headerlink" title="20. async() 如何工作"></a>20. <code>async()</code> 如何工作</h2><p><code>async(exec, f, args...)</code> 是 <code>make_async_task()</code> 加 executor submit 的便利封装：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">创建 AsyncTask 和 Promise</span><br><span class="line">    ↓</span><br><span class="line">先取得返回 Future</span><br><span class="line">    ↓</span><br><span class="line">把 AsyncTask 提交到 executor</span><br><span class="line">    ↓</span><br><span class="line">executor 执行 callable</span><br><span class="line">    ↓</span><br><span class="line">完成 Promise</span><br><span class="line">    ↓</span><br><span class="line">返回 Future ready</span><br></pre></td></tr></table></figure><p>它不是创建一个新线程。callable 在传入的 executor 上执行。</p><p>如果 executor 属于 <code>ev_loop</code>，那么 callable 通常就在运行该 loop 的线程中执行。</p><hr><h2 id="21-Future-与-ev-loop-wait-的关系"><a href="#21-Future-与-ev-loop-wait-的关系" class="headerlink" title="21. Future 与 ev_loop_wait() 的关系"></a>21. Future 与 <code>ev_loop_wait()</code> 的关系</h2><p>虽然 Lely Future 本身不阻塞，但 <code>ev_loop_wait(loop, future)</code> 可以把 Future 作为 event loop 本次运行的结束条件。</p><p>其内部不是阻塞在 Future 条件变量上，而是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">向 Future 提交一个内部唤醒 task</span><br><span class="line">    ↓</span><br><span class="line">继续运行 event loop</span><br><span class="line">    ├─ 执行普通 task</span><br><span class="line">    ├─ Poll I/O</span><br><span class="line">    └─ 等待内部唤醒</span><br><span class="line">    ↓</span><br><span class="line">异步操作完成 Promise</span><br><span class="line">    ↓</span><br><span class="line">Future 内部唤醒 task 被提交到 loop</span><br><span class="line">    ↓</span><br><span class="line">loop 执行该 task，标记等待 context ready</span><br><span class="line">    ↓</span><br><span class="line">ev_loop_wait() 返回</span><br></pre></td></tr></table></figure><p>这解决了 CANopen 异步操作中的关键问题：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Future 的完成本身依赖 event loop 继续处理 SocketCAN、定时器和协议状态机。</span><br></pre></td></tr></table></figure><p>如果只让线程同步睡眠，而没有其他线程运行 event loop，异步 SDO Future 可能永远不会 ready。</p><hr><h2 id="22-放到异步-CANopen-场景中理解"><a href="#22-放到异步-CANopen-场景中理解" class="headerlink" title="22. 放到异步 CANopen 场景中理解"></a>22. 放到异步 CANopen 场景中理解</h2><p>以异步 SDO 上传为例：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant App as 应用层</span><br><span class="line">    participant SDO as SDO异步操作</span><br><span class="line">    participant EvLoop as ev_loop</span><br><span class="line">    participant IO as SocketCAN/Poll</span><br><span class="line">    participant P as Promise</span><br><span class="line">    participant F as Future</span><br><span class="line"></span><br><span class="line">    App-&gt;&gt;SDO: 发起异步 SDO 上传</span><br><span class="line">    SDO-&gt;&gt;P: 创建并持有 Promise</span><br><span class="line">    SDO--&gt;&gt;App: 返回 Future</span><br><span class="line">    App-&gt;&gt;EvLoop: wait(Future) 或注册 then()</span><br><span class="line">    EvLoop-&gt;&gt;IO: 等待 CAN 响应或超时</span><br><span class="line">    IO--&gt;&gt;EvLoop: CAN 帧可读</span><br><span class="line">    EvLoop-&gt;&gt;SDO: 执行接收与状态机 task</span><br><span class="line">    SDO-&gt;&gt;P: set(result)</span><br><span class="line">    P-&gt;&gt;F: state = READY</span><br><span class="line">    F-&gt;&gt;EvLoop: 提交等待 task</span><br><span class="line">    EvLoop--&gt;&gt;App: wait 返回或执行 continuation</span><br></pre></td></tr></table></figure><p>角色不要混淆：</p><table><thead><tr><th>角色</th><th>在 SDO 场景中的对象</th></tr></thead><tbody><tr><td>异步操作生产端</td><td>SDO 客户端状态机及其 Promise。</td></tr><tr><td>结果观察端</td><td>应用层持有的 Future。</td></tr><tr><td>I&#x2F;O 驱动</td><td>SocketCAN + Poll。</td></tr><tr><td>调度器</td><td><code>ev_loop</code> executor。</td></tr><tr><td>结果内容</td><td>SDO 结果、错误码或相关数据。</td></tr></tbody></table><p>Future 不传输 CAN 帧，也不实现 SDO 协议。它只负责把“SDO 操作已经完成”这一事实及结果传给观察者。</p><hr><h2 id="23-常见错误理解"><a href="#23-常见错误理解" class="headerlink" title="23. 常见错误理解"></a>23. 常见错误理解</h2><h3 id="23-1-把-Future-当消息队列"><a href="#23-1-把-Future-当消息队列" class="headerlink" title="23.1 把 Future 当消息队列"></a>23.1 把 Future 当消息队列</h3><p>错误：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Promise 可以不断写结果，Future 可以不断取消息</span><br></pre></td></tr></table></figure><p>正确：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">一个 Promise 只能成功完成一次</span><br><span class="line">一个 Future 只观察这一次完成</span><br></pre></td></tr></table></figure><h3 id="23-2-认为-get-会阻塞"><a href="#23-2-认为-get-会阻塞" class="headerlink" title="23.2 认为 get() 会阻塞"></a>23.2 认为 <code>get()</code> 会阻塞</h3><p>错误：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">直接 get，没完成就等</span><br></pre></td></tr></table></figure><p>正确：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">C 版要求调用前已经 ready</span><br><span class="line">C++ 版未 ready 会抛 future_not_ready</span><br></pre></td></tr></table></figure><h3 id="23-3-认为-Promise-和-Future-有两个引用计数"><a href="#23-3-认为-Promise-和-Future-有两个引用计数" class="headerlink" title="23.3 认为 Promise 和 Future 有两个引用计数"></a>23.3 认为 Promise 和 Future 有两个引用计数</h3><p>错误：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Promise 管 Promise，Future 管 Future</span><br></pre></td></tr></table></figure><p>正确：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">二者共享外层 Promise 控制块中的一个 refcnt</span><br></pre></td></tr></table></figure><h3 id="23-4-认为-cancel-后-task-不会执行"><a href="#23-4-认为-cancel-后-task-不会执行" class="headerlink" title="23.4 认为 cancel 后 task 不会执行"></a>23.4 认为 cancel 后 task 不会执行</h3><p>错误：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">cancel == 丢弃 task</span><br></pre></td></tr></table></figure><p>正确：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">cancel 会把 task 提交到 executor</span><br><span class="line">abort 才不会执行 task</span><br></pre></td></tr></table></figure><h3 id="23-5-认为-Future-ready-后回调在完成线程直接执行"><a href="#23-5-认为-Future-ready-后回调在完成线程直接执行" class="headerlink" title="23.5 认为 Future ready 后回调在完成线程直接执行"></a>23.5 认为 Future ready 后回调在完成线程直接执行</h3><p>不一定。</p><p>Promise 完成线程只负责把 task 提交到 task 指定的 executor。task 实际在哪个线程运行，由 executor 的实现和当前运行环境决定。</p><h3 id="23-6-认为-async-一定创建线程"><a href="#23-6-认为-async-一定创建线程" class="headerlink" title="23.6 认为 async() 一定创建线程"></a>23.6 认为 <code>async()</code> 一定创建线程</h3><p>错误。</p><p>Lely <code>async()</code> 只是把 callable 包成 task 并提交给指定 executor。executor 可以是 event loop，也可以是其他实现。</p><hr><h2 id="24-调试-Future-问题时建议观察的变量"><a href="#24-调试-Future-问题时建议观察的变量" class="headerlink" title="24. 调试 Future 问题时建议观察的变量"></a>24. 调试 Future 问题时建议观察的变量</h2><h3 id="24-1-future-state"><a href="#24-1-future-state" class="headerlink" title="24.1 future-&gt;state"></a>24.1 <code>future-&gt;state</code></h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">WAITING：尚无人完成</span><br><span class="line">SETTING：某个线程正在构造/发布结果</span><br><span class="line">READY：结果已发布</span><br></pre></td></tr></table></figure><p>如果长期停在 SETTING，通常说明某条路径成功调用了 <code>set_acquire()</code>，却没有调用 <code>set_release()</code>。</p><h3 id="24-2-promise-refcnt"><a href="#24-2-promise-refcnt" class="headerlink" title="24.2 promise-&gt;refcnt"></a>24.2 <code>promise-&gt;refcnt</code></h3><p>用于定位：</p><ul><li>提前释放；</li><li>Future 永不销毁；</li><li>continuation 引用环；</li><li>聚合 Future 内部引用未释放。</li></ul><p>不要仅凭某一瞬间的 refcnt 在多线程环境中做严格逻辑判断，但可以用于断点和生命周期分析。</p><h3 id="24-3-future-queue"><a href="#24-3-future-queue" class="headerlink" title="24.3 future-&gt;queue"></a>24.3 <code>future-&gt;queue</code></h3><p>如果 Future 已 ready，但 queue 中仍残留 task，需要检查：</p><ul><li>实际是否执行到 <code>set_release()</code>；</li><li>是否使用了不同的 Future 对象；</li><li>task 是否被重复挂入链表；</li><li>是否有内存破坏。</li></ul><h3 id="24-4-future-value"><a href="#24-4-future-value" class="headerlink" title="24.4 future-&gt;value"></a>24.4 <code>future-&gt;value</code></h3><p>READY 时应指向有效结果，除非调用路径明确使用 <code>NULL</code> 表示空结果或结果构造失败。</p><h3 id="24-5-task-的-exec-和-func"><a href="#24-5-task-的-exec-和-func" class="headerlink" title="24.5 task 的 exec 和 func"></a>24.5 task 的 <code>exec</code> 和 <code>func</code></h3><p>Future 只负责提交。如果 task 不执行，还要继续检查：</p><ul><li>executor 是否仍在运行；</li><li>event loop 是否 stopped；</li><li>task 是否被 executor abort；</li><li><code>func</code> 是否为空或对象生命周期已结束。</li></ul><h3 id="24-6-event-loop-的-ntasks"><a href="#24-6-event-loop-的-ntasks" class="headerlink" title="24.6 event loop 的 ntasks"></a>24.6 event loop 的 <code>ntasks</code></h3><p>Future waiter 已调用 <code>on_task_init()</code> 后，即使尚未进入 loop queue，也应作为 outstanding work 存在。</p><p>如果 <code>ntasks</code> 提前归零，检查 Future task 的 init&#x2F;fini 是否配对。</p><hr><h2 id="25-建议的源码单步顺序"><a href="#25-建议的源码单步顺序" class="headerlink" title="25. 建议的源码单步顺序"></a>25. 建议的源码单步顺序</h2><p>第一次调试时，不建议直接从完整 CANopen 主站流程开始。可以按以下顺序建立断点。</p><h3 id="25-1-最小-Promise-Future"><a href="#25-1-最小-Promise-Future" class="headerlink" title="25.1 最小 Promise&#x2F;Future"></a>25.1 最小 Promise&#x2F;Future</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">ev_promise_create</span><br><span class="line">    ↓</span><br><span class="line">ev_promise_get_future</span><br><span class="line">    ↓</span><br><span class="line">ev_future_submit</span><br><span class="line">    ↓</span><br><span class="line">ev_promise_set_acquire</span><br><span class="line">    ↓</span><br><span class="line">ev_promise_set_release</span><br><span class="line">    ↓</span><br><span class="line">ev_task_queue_post</span><br><span class="line">    ↓</span><br><span class="line">task.func</span><br></pre></td></tr></table></figure><h3 id="25-2-引用计数"><a href="#25-2-引用计数" class="headerlink" title="25.2 引用计数"></a>25.2 引用计数</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">ev_promise_acquire/release</span><br><span class="line">ev_future_acquire/release</span><br><span class="line">ev_promise_fini</span><br><span class="line">ev_future_fini</span><br></pre></td></tr></table></figure><h3 id="25-3-组合-Future"><a href="#25-3-组合-Future" class="headerlink" title="25.3 组合 Future"></a>25.3 组合 Future</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">ev_future_when_all_n</span><br><span class="line">ev_future_when_all_func</span><br><span class="line"></span><br><span class="line">ev_future_when_any_n</span><br><span class="line">ev_future_when_any_func</span><br></pre></td></tr></table></figure><h3 id="25-4-C-continuation"><a href="#25-4-C-continuation" class="headerlink" title="25.4 C++ continuation"></a>25.4 C++ continuation</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">Promise&lt;T,E&gt;::set</span><br><span class="line">Future&lt;T,E&gt;::then</span><br><span class="line">make_async_task</span><br><span class="line">AsyncTask::task function</span><br><span class="line">Future&lt;T,E&gt;::get</span><br></pre></td></tr></table></figure><h3 id="25-5-接入-CANopen"><a href="#25-5-接入-CANopen" class="headerlink" title="25.5 接入 CANopen"></a>25.5 接入 CANopen</h3><p>最后再从具体异步 SDO&#x2F;PDO&#x2F;定时器 API 返回的 Future 开始，追踪是谁持有 Promise，以及在哪条完成或错误路径调用 set。</p><hr><h2 id="26-最小-C-接口示例"><a href="#26-最小-C-接口示例" class="headerlink" title="26. 最小 C 接口示例"></a>26. 最小 C 接口示例</h2><p>下面的示例只展示对象关系，executor 创建过程需按实际平台补充。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;lely/ev/future.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;lely/ev/task.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;lely/util/util.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">result_data</span> &#123;</span></span><br><span class="line">    <span class="type">int</span> value;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">wait_task</span> &#123;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">ev_task</span> <span class="title">task</span>;</span></span><br><span class="line">    <span class="type">ev_future_t</span> *<span class="built_in">future</span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="type">static</span> <span class="type">void</span></span><br><span class="line"><span class="title function_">on_ready</span><span class="params">(<span class="keyword">struct</span> ev_task *task)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">wait_task</span> *<span class="title">wait</span> =</span> structof(task, <span class="keyword">struct</span> wait_task, task);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (ev_future_is_ready(wait-&gt;<span class="built_in">future</span>)) &#123;</span><br><span class="line">        <span class="type">const</span> <span class="class"><span class="keyword">struct</span> <span class="title">result_data</span> *<span class="title">result</span> =</span> ev_future_get(wait-&gt;<span class="built_in">future</span>);</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;result = %d\n&quot;</span>, result-&gt;value);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="built_in">puts</span>(<span class="string">&quot;future was canceled or abandoned&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    ev_future_release(wait-&gt;<span class="built_in">future</span>);</span><br><span class="line">    <span class="built_in">free</span>(wait);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span></span><br><span class="line"><span class="title function_">start_example</span><span class="params">(<span class="type">ev_exec_t</span> *exec)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">ev_promise_t</span> *promise = ev_promise_create(<span class="keyword">sizeof</span>(<span class="keyword">struct</span> result_data), <span class="literal">NULL</span>);</span><br><span class="line">    <span class="keyword">if</span> (!promise)</span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">wait_task</span> *<span class="title">wait</span> =</span> <span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(*wait));</span><br><span class="line">    <span class="keyword">if</span> (!wait) &#123;</span><br><span class="line">        ev_promise_release(promise);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    wait-&gt;task = (<span class="keyword">struct</span> ev_task)EV_TASK_INIT(exec, &amp;on_ready);</span><br><span class="line">    wait-&gt;<span class="built_in">future</span> = ev_promise_get_future(promise);</span><br><span class="line">    ev_future_submit(wait-&gt;<span class="built_in">future</span>, &amp;wait-&gt;task);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (ev_promise_set_acquire(promise)) &#123;</span><br><span class="line">        <span class="class"><span class="keyword">struct</span> <span class="title">result_data</span> *<span class="title">result</span> =</span> ev_promise_data(promise);</span><br><span class="line">        result-&gt;value = <span class="number">42</span>;</span><br><span class="line">        ev_promise_set_release(promise, result);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    ev_promise_release(promise);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>示例把等待 task 放在堆上，并由 task function 在执行后释放，以保证对象生命周期覆盖 executor 的异步调度。生产代码还应结合具体异步操作处理分配失败、取消和 executor 关闭流程。</p><hr><h2 id="27-最小-C-接口示例"><a href="#27-最小-C-接口示例" class="headerlink" title="27. 最小 C++ 接口示例"></a>27. 最小 C++ 接口示例</h2><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;lely/ev/future.hpp&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">using</span> lely::ev::Future;</span><br><span class="line"><span class="keyword">using</span> lely::ev::Promise;</span><br><span class="line"></span><br><span class="line"><span class="function">Future&lt;<span class="type">int</span>&gt; <span class="title">start_operation</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Promise&lt;<span class="type">int</span>&gt; promise;</span><br><span class="line">    <span class="keyword">auto</span> future = promise.<span class="built_in">get_future</span>();</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 实际程序应把 promise 移交给异步操作对象。</span></span><br><span class="line">    promise.<span class="built_in">set</span>(lely::util::<span class="built_in">success</span>(<span class="number">42</span>));</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> future;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>使用 continuation：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">auto</span> future = <span class="built_in">start_operation</span>();</span><br><span class="line"></span><br><span class="line"><span class="keyword">auto</span> next = future.<span class="built_in">then</span>(exec, [](Future&lt;<span class="type">int</span>&gt; completed) &#123;</span><br><span class="line">    <span class="keyword">return</span> completed.<span class="built_in">get</span>().<span class="built_in">value</span>() * <span class="number">2</span>;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure><p>这里 <code>next</code> 的类型是保存 continuation 返回值的 Future；continuation 中的异常会被捕获为 <code>std::exception_ptr</code> 错误结果。</p><hr><h2 id="28-一张总图串起全部机制"><a href="#28-一张总图串起全部机制" class="headerlink" title="28. 一张总图串起全部机制"></a>28. 一张总图串起全部机制</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[异步操作创建 Promise] --&gt; B[Promise 内嵌 Future]</span><br><span class="line">    B --&gt; C[调用方取得 Future 引用]</span><br><span class="line">    C --&gt; D[ev_future_submit 登记 task]</span><br><span class="line">    D --&gt; E[on_task_init 增加 outstanding work]</span><br><span class="line">    E --&gt; F&#123;Promise 是否完成&#125;</span><br><span class="line">    F -- 否 --&gt; G[task 保存在 Future queue]</span><br><span class="line">    F -- 是 --&gt; H[task 直接 post 到 executor]</span><br><span class="line">    G --&gt; I[完成方 set_acquire 抢到完成权]</span><br><span class="line">    I --&gt; J[写共享结果]</span><br><span class="line">    J --&gt; K[set_release 发布 READY]</span><br><span class="line">    K --&gt; L[转移 Future queue]</span><br><span class="line">    L --&gt; H</span><br><span class="line">    H --&gt; M[on_task_fini 撤销 outstanding work]</span><br><span class="line">    M --&gt; N[executor/event loop 执行 task]</span><br><span class="line">    N --&gt; O[检查 ready 并读取 Result]</span><br></pre></td></tr></table></figure><hr><h2 id="29-最终理解框架"><a href="#29-最终理解框架" class="headerlink" title="29. 最终理解框架"></a>29. 最终理解框架</h2><p>学完这三个文件后，应该能回答以下问题：</p><ol><li>为什么 Lely Future 是非阻塞的？</li><li>Promise 和 Future 为什么共享同一个引用计数？</li><li><code>WAITING → SETTING → READY</code> 为什么需要三个状态，而不是布尔值？</li><li><code>ev_future_submit()</code> 如何避免 task 丢失？</li><li>为什么等待 Future 的 task 要先调用 <code>on_task_init()</code>？</li><li>Future 被遗弃时，为什么仍要提交等待 task？</li><li>cancel 和 abort 为什么语义不同？</li><li><code>when_any()</code> 如何用一次性 Promise 决定唯一赢家？</li><li>C++ <code>then()</code> 如何把 continuation 变成新的 Future？</li><li>continuation 返回 Future 时，为什么不会得到嵌套 Future？</li><li>Future、executor、event loop 和 SocketCAN 各自负责哪一部分？</li></ol><p>如果这些问题都能从对象关系、状态机和 task 调度路径解释清楚，就已经掌握了 Lely Future&#x2F;Promise 的核心设计。</p><hr><h2 id="30-官方源码与参考资料"><a href="#30-官方源码与参考资料" class="headerlink" title="30. 官方源码与参考资料"></a>30. 官方源码与参考资料</h2><h3 id="30-1-本文核心源码"><a href="#30-1-本文核心源码" class="headerlink" title="30.1 本文核心源码"></a>30.1 本文核心源码</h3><ol><li><a href="https://gitlab.com/lely_industries/lely-core/-/blob/master/src/ev/future.c">Lely <code>src/ev/future.c</code></a></li><li><a href="https://gitlab.com/lely_industries/lely-core/-/blob/master/include/lely/ev/future.h">Lely <code>include/lely/ev/future.h</code></a></li><li><a href="https://gitlab.com/lely_industries/lely-core/-/blob/master/include/lely/ev/future.hpp">Lely <code>include/lely/ev/future.hpp</code></a></li></ol><h3 id="30-2-关联源码"><a href="#30-2-关联源码" class="headerlink" title="30.2 关联源码"></a>30.2 关联源码</h3><ol><li><a href="https://gitlab.com/lely_industries/lely-core/-/blob/master/include/lely/ev/task.h">Lely <code>include/lely/ev/task.h</code></a></li><li><a href="https://gitlab.com/lely_industries/lely-core/-/blob/master/src/ev/task.c">Lely <code>src/ev/task.c</code></a></li><li><a href="https://gitlab.com/lely_industries/lely-core/-/blob/master/include/lely/ev/exec.h">Lely <code>include/lely/ev/exec.h</code></a></li><li><a href="https://gitlab.com/lely_industries/lely-core/-/blob/master/src/ev/loop.c">Lely <code>src/ev/loop.c</code></a></li></ol><h3 id="30-3-官方说明"><a href="#30-3-官方说明" class="headerlink" title="30.3 官方说明"></a>30.3 官方说明</h3><ol><li><a href="https://opensource.lely.com/canopen/docs/overview/">Lely CANopen Library overview</a></li><li><a href="https://opensource.lely.com/canopen/docs/configuration/">Lely CANopen Build configuration</a></li></ol>]]></content>
    
    
      
      
    <summary type="html">&lt;meta name=&quot;referrer&quot; content=&quot;no-referrer&quot; /&gt;

&lt;h1 id=&quot;Lely-CANopen-Future-Promise-机制原理与源码详解&quot;&gt;&lt;a href=&quot;#Lely-CANopen-Future-Promise-机制原理与源码</summary>
      
    
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/categories/canopen/"/>
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/tags/canopen/"/>
    
  </entry>
  
  <entry>
    <title>Lely CANopen `ev_loop` 详解</title>
    <link href="https://wdfk-prog.space/posts/3ecba0f7/"/>
    <id>https://wdfk-prog.space/posts/3ecba0f7/</id>
    <published>2026-07-28T06:13:10.000Z</published>
    <updated>2026-07-28T06:14:35.716Z</updated>
    
    <content type="html"><![CDATA[<meta name="referrer" content="no-referrer" /><h1 id="Lely-CANopen-ev-loop-详解"><a href="#Lely-CANopen-ev-loop-详解" class="headerlink" title="Lely CANopen ev_loop 详解"></a>Lely CANopen <code>ev_loop</code> 详解</h1><p><img src="https://i-blog.csdnimg.cn/direct/101c021ff3354fd49a88b48e1668cf15.png" alt="在这里插入图片描述"></p><p>@[toc]</p><h2 id="1-先区分三个容易混淆的“停止”状态"><a href="#1-先区分三个容易混淆的“停止”状态" class="headerlink" title="1. 先区分三个容易混淆的“停止”状态"></a>1. 先区分三个容易混淆的“停止”状态</h2><p><code>loop.c</code> 中至少存在三类停止或完成状态：</p><table><thead><tr><th>状态</th><th>所属范围</th><th>作用</th></tr></thead><tbody><tr><td><code>loop-&gt;stopped</code></td><td>每个 <code>ev_loop_t</code></td><td>整个事件循环是否处于 stopped 状态；<code>ev_loop_stop()</code> 会影响所有正在运行该 loop 的线程。</td></tr><tr><td><code>ev_loop_thrd.stopped</code></td><td>每个线程</td><td>中断该线程当前最内层的一次 <code>run/wait</code> 调用；返回后通常会清零，以便下次重新运行。</td></tr><tr><td><code>ctx-&gt;ready</code></td><td>每个等待 context</td><td>表示正在等待的 future 已经 ready，应结束本次等待。</td></tr></tbody></table><p>不能把它们当作同一个状态：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">loop-&gt;stopped</span><br><span class="line">= loop 级停止</span><br><span class="line"></span><br><span class="line">thread-local stopped</span><br><span class="line">= 线程级、单次运行中断</span><br><span class="line"></span><br><span class="line">ctx-&gt;ready</span><br><span class="line">= 本次等待目标已经完成</span><br></pre></td></tr></table></figure><p><code>ev_loop_ctx_wait_one()</code> 的主循环实际同时检查这些条件：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">loop 没有全局停止</span><br><span class="line">并且</span><br><span class="line">当前线程没有被 kill</span><br><span class="line">并且</span><br><span class="line">future 尚未 ready</span><br></pre></td></tr></table></figure><hr><h2 id="2-ntasks：outstanding-work-计数"><a href="#2-ntasks：outstanding-work-计数" class="headerlink" title="2. ntasks：outstanding work 计数"></a>2. <code>ntasks</code>：outstanding work 计数</h2><h3 id="2-1-统计边界：不等于任务链表长度"><a href="#2-1-统计边界：不等于任务链表长度" class="headerlink" title="2.1 统计边界：不等于任务链表长度"></a>2.1 统计边界：不等于任务链表长度</h3><p>源码注释把 <code>ntasks</code> 描述为 pending work 计数，其语义可以理解为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">事件循环已知但尚未完成的工作数量</span><br></pre></td></tr></table></figure><p>公开接口文档进一步定义 outstanding work 为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">待执行任务</span><br><span class="line">+ 当前执行中的任务</span><br><span class="line">+ ev_exec_on_task_init() 调用次数</span><br><span class="line">- ev_exec_on_task_fini() 调用次数</span><br></pre></td></tr></table></figure><p><code>ev_exec_on_task_init()</code> 的典型意义是：</p><blockquote><p>我现在还没有把任务放入队列，但未来会提交工作，事件循环不要因为暂时空队列而退出。</p></blockquote><p><code>ev_exec_on_task_fini()</code> 表示：</p><blockquote><p>先前声明的这份 outstanding work 已经结束，不再需要阻止事件循环退出。</p></blockquote><p>因此，<code>ntasks</code> 的关键价值是覆盖“任务尚未入队，但异步操作仍然存活”的窗口。</p><h3 id="2-2-队列为空不代表异步工作已经结束"><a href="#2-2-队列为空不代表异步工作已经结束" class="headerlink" title="2.2 队列为空不代表异步工作已经结束"></a>2.2 队列为空不代表异步工作已经结束</h3><p>假设一个异步 CAN 读取操作已经启动，但当前 SocketCAN 没有数据：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">任务队列暂时为空</span><br><span class="line">SocketCAN read 正等待 I/O</span><br><span class="line">未来收到 CAN 帧后才会投递完成任务</span><br></pre></td></tr></table></figure><p>如果 event loop 只看任务队列，就可能错误判断：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">queue 为空 → 没有工作 → stop</span><br></pre></td></tr></table></figure><p>而 <code>ntasks &gt; 0</code> 表示仍存在已登记的异步工作，因此 loop 应继续等待 Poll 或条件变量事件。</p><h3 id="2-3-ntasks-的状态变化"><a href="#2-3-ntasks-的状态变化" class="headerlink" title="2.3 ntasks 的状态变化"></a>2.3 <code>ntasks</code> 的状态变化</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">stateDiagram-v2</span><br><span class="line">    [*] --&gt; Zero: 初始化为 0</span><br><span class="line">    Zero --&gt; Active: on_task_init 0→1</span><br><span class="line">    Active --&gt; Active: on_task_init / on_task_fini 后仍大于 0</span><br><span class="line">    Active --&gt; LastDone: on_task_fini 1→0</span><br><span class="line">    LastDone --&gt; StopCheck: 加锁检查任务队列</span><br><span class="line">    StopCheck --&gt; Zero: 队列仍有任务</span><br><span class="line">    StopCheck --&gt; Stopped: 队列为空，调用 ev_loop_do_stop</span><br></pre></td></tr></table></figure><p>关键代码路径是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">on_task_init:</span><br><span class="line">    ntasks 原子加一</span><br><span class="line"></span><br><span class="line">on_task_fini:</span><br><span class="line">    ntasks 原子减一</span><br><span class="line">    若减前为 1，即减后为 0：</span><br><span class="line">        获取 loop-&gt;mtx</span><br><span class="line">        若任务队列为空：停止 loop</span><br></pre></td></tr></table></figure><hr><h2 id="3-ntasks-的线程安全与原子内存序"><a href="#3-ntasks-的线程安全与原子内存序" class="headerlink" title="3. ntasks 的线程安全与原子内存序"></a>3. <code>ntasks</code> 的线程安全与原子内存序</h2><h3 id="3-1-跨线程并发更新是原子化的根本原因"><a href="#3-1-跨线程并发更新是原子化的根本原因" class="headerlink" title="3.1 跨线程并发更新是原子化的根本原因"></a>3.1 跨线程并发更新是原子化的根本原因</h3><p><code>ev_exec_on_task_init()</code> 和 <code>ev_exec_on_task_fini()</code> 是 executor 的公开生命周期接口。调用它们的线程不一定是正在运行 <code>ev_loop_wait_one()</code> 的线程，也不保证已经持有 <code>loop-&gt;mtx</code>。</p><p>典型并发场景：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">线程 A：启动异步读，调用 on_task_init()</span><br><span class="line">线程 B：启动异步写，调用 on_task_init()</span><br><span class="line">线程 C：某个异步操作结束，调用 on_task_fini()</span><br><span class="line">线程 D：event loop 读取 ntasks 判断是否应停止</span><br></pre></td></tr></table></figure><p>如果使用普通 <code>size_t</code> 且没有统一互斥锁，会出现 C 语言层面的数据竞争，行为未定义。</p><h3 id="3-2-非原子操作会出现丢失更新"><a href="#3-2-非原子操作会出现丢失更新" class="headerlink" title="3.2 非原子操作会出现丢失更新"></a>3.2 非原子操作会出现丢失更新</h3><p>假设 <code>ntasks == 5</code>，两个线程同时执行普通的 <code>++</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">线程 A 读取 5</span><br><span class="line">线程 B 读取 5</span><br><span class="line">线程 A 写入 6</span><br><span class="line">线程 B 写入 6</span><br></pre></td></tr></table></figure><p>正确结果应为 7，实际可能为 6。</p><p>对于 <code>ntasks</code>，计数错误会带来两类严重后果：</p><table><thead><tr><th>错误方向</th><th>后果</th></tr></thead><tbody><tr><td>少计</td><td>loop 可能在异步操作尚未结束时提前 stop。</td></tr><tr><td>多计</td><td>loop 可能一直认为还有 outstanding work，无法自然退出。</td></tr></tbody></table><h3 id="3-3-未统一使用-loop-mtx-的原因"><a href="#3-3-未统一使用-loop-mtx-的原因" class="headerlink" title="3.3 未统一使用 loop-&gt;mtx 的原因"></a>3.3 未统一使用 <code>loop-&gt;mtx</code> 的原因</h3><p>理论上可以把所有 <code>ntasks</code> 读写都放进 <code>loop-&gt;mtx</code>，但当前实现选择原子计数，原因可以从代码行为推断为：</p><ol><li><code>on_task_init()</code> 是高频轻量路径，只做原子加一，不需要锁任务队列；</li><li><code>on_task_fini()</code> 在减后仍不为 0 时，也不需要进入互斥锁；</li><li>只有最后一个 outstanding work 消失时，才加锁检查队列并决定是否 stop；</li><li>避免每次工作引用变化都与任务队列竞争同一把锁。</li></ol><p>这是典型的“原子引用计数快路径 + 最后一个引用进入慢路径”的结构。</p><h3 id="3-4-加一-relaxed、减一-release-和归零-acquire-fence"><a href="#3-4-加一-relaxed、减一-release-和归零-acquire-fence" class="headerlink" title="3.4 加一 relaxed、减一 release 和归零 acquire fence"></a>3.4 加一 relaxed、减一 release 和归零 acquire fence</h3><p>Linux&#x2F;POSIX 原子实现中：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">加一：relaxed</span><br><span class="line">减一：release</span><br><span class="line">观察到 1→0：acquire fence</span><br></pre></td></tr></table></figure><p>这里最基本的要求是：</p><ul><li>所有线程必须对同一个计数进行不可分割的 read-modify-write；</li><li>单个变量的修改顺序必须一致；</li><li>最后一个完成者在执行“归零后的停止判断”前，需要建立更强的内存顺序边界。</li></ul><p><code>relaxed</code> 加一足以完成“占一个 outstanding work 名额”；它不负责发布其他数据。</p><p>减一使用 release，并在最后一个减一线程上执行 acquire fence，是常见的最后引用释放模式：只有真正从 1 变为 0 的线程承担最终收尾工作。</p><p>需要强调：</p><blockquote><p>原子操作主要解决的是 <code>ntasks</code> 自身的并发计数问题；任务队列、waiting&#x2F;polling 链表、<code>loop-&gt;stopped</code> 等结构仍由 <code>loop-&gt;mtx</code> 保护。</p></blockquote><h3 id="3-5-不同构建配置下的计数类型"><a href="#3-5-不同构建配置下的计数类型" class="headerlink" title="3.5 不同构建配置下的计数类型"></a>3.5 不同构建配置下的计数类型</h3><p>源码针对不同平台和裁剪配置提供多个实现：</p><table><thead><tr><th>配置</th><th>实现</th></tr></thead><tbody><tr><td>禁用线程</td><td>普通 <code>size_t</code>，因为不存在并发访问。</td></tr><tr><td>Windows MSVC</td><td><code>InterlockedIncrement/Decrement</code>。</td></tr><tr><td>支持 C11 原子</td><td><code>atomic_size_t</code>。</td></tr><tr><td>明确禁用原子且特定配置</td><td>普通整数，依赖该裁剪配置的使用约束。</td></tr></tbody></table><p>因此，“为什么要原子”准确地说是：</p><blockquote><p>在启用多线程且平台支持原子操作的正常构建中，<code>ntasks</code> 必须以原子方式维护；单线程构建不需要。</p></blockquote><hr><h2 id="4-线程局部-ev-loop-thrd-与-context-栈"><a href="#4-线程局部-ev-loop-thrd-与-context-栈" class="headerlink" title="4. 线程局部 ev_loop_thrd 与 context 栈"></a>4. 线程局部 <code>ev_loop_thrd</code> 与 context 栈</h2><h3 id="4-1-TLS-实例按线程独立"><a href="#4-1-TLS-实例按线程独立" class="headerlink" title="4.1 TLS 实例按线程独立"></a>4.1 TLS 实例按线程独立</h3><p>定义使用 <code>_Thread_local</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">static + _Thread_local + struct ev_loop_thrd</span><br></pre></td></tr></table></figure><p>三个关键词分别表示：</p><table><thead><tr><th>关键字</th><th>含义</th></tr></thead><tbody><tr><td><code>static</code></td><td>变量名只在当前源文件可见。</td></tr><tr><td><code>_Thread_local</code></td><td>每个线程拥有独立存储实例。</td></tr><tr><td>静态初始化 <code>{0, NULL}</code></td><td>每个线程第一次获得的实例初始为停止标志 0、context 指针 NULL。</td></tr></tbody></table><p>这里的“每个线程一份”是 C TLS 的存储语义，不应理解为进程启动时预先为未来所有线程集中创建对象。对实际存在并访问该符号的线程，运行库提供该线程自己的实例，并以 <code>{ 0, NULL }</code> 初始化。</p><p>因此，假设有三个线程调用同一个 <code>ev_loop_t</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">线程 A：自己的 ev_loop_thrd</span><br><span class="line">线程 B：自己的 ev_loop_thrd</span><br><span class="line">线程 C：自己的 ev_loop_thrd</span><br></pre></td></tr></table></figure><p>它们之间不会共享 <code>stopped</code> 和 <code>ctx</code> 字段。</p><h3 id="4-2-线程状态与-loop-状态相互独立"><a href="#4-2-线程状态与-loop-状态相互独立" class="headerlink" title="4.2 线程状态与 loop 状态相互独立"></a>4.2 线程状态与 loop 状态相互独立</h3><p>同一个线程可以先后运行不同的 loop，甚至在任务回调中嵌套运行另一个 loop。该线程仍只有一个 <code>ev_loop_thrd</code>。</p><p>这也是为什么结构体名称是 <code>ev_loop_thrd</code>，而不是 <code>ev_loop_thread_of_loop</code>。</p><h3 id="4-3-两个字段的职责"><a href="#4-3-两个字段的职责" class="headerlink" title="4.3 两个字段的职责"></a>4.3 两个字段的职责</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">stopped：中断当前线程正在执行的最内层 loop 调用</span><br><span class="line">ctx：指向当前线程最内层的 ev_loop_ctx</span><br></pre></td></tr></table></figure><p><code>ev_loop_self()</code> 返回的实际上就是当前线程这份 TLS 状态的地址。这个地址被当成线程令牌传给 <code>ev_loop_kill()</code>。</p><h3 id="4-4-ctx-构成嵌套调用栈"><a href="#4-4-ctx-构成嵌套调用栈" class="headerlink" title="4.4 ctx 构成嵌套调用栈"></a>4.4 <code>ctx</code> 构成嵌套调用栈</h3><p>创建 context 时：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">新 ctx.next = 当前 TLS ctx</span><br><span class="line">TLS ctx = 新 ctx</span><br></pre></td></tr></table></figure><p>销毁时：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">TLS ctx = 当前 ctx.next</span><br></pre></td></tr></table></figure><p>形成的结构是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">TLS ev_loop_thrd.ctx</span><br><span class="line">        ↓</span><br><span class="line">最内层 ctx</span><br><span class="line">        ↓ next</span><br><span class="line">外一层 ctx</span><br><span class="line">        ↓ next</span><br><span class="line">更外层 ctx</span><br></pre></td></tr></table></figure><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    T[线程局部 ev_loop_thrd] --&gt; C3[最内层 ctx]</span><br><span class="line">    C3 --&gt; C2[外层 ctx]</span><br><span class="line">    C2 --&gt; C1[最外层 ctx]</span><br><span class="line">    C1 --&gt; N[NULL]</span><br></pre></td></tr></table></figure><p>所以 <code>ev_loop_kill()</code> 的公开契约能够实现：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">存在嵌套 event loop 时，只中断最内层 loop。</span><br></pre></td></tr></table></figure><p>它通过 <code>thr-&gt;ctx</code> 直接找到栈顶 context，而不是遍历某个全局 loop 列表。</p><h3 id="4-5-ev-loop-ctx-不是-ev-loop-t"><a href="#4-5-ev-loop-ctx-不是-ev-loop-t" class="headerlink" title="4.5 ev_loop_ctx 不是 ev_loop_t"></a>4.5 <code>ev_loop_ctx</code> 不是 <code>ev_loop_t</code></h3><p>二者是“被管理对象”和“单次运行上下文”的关系：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ev_loop_ctx.loop ──指向──&gt; ev_loop_t</span><br></pre></td></tr></table></figure><p>一个 <code>ev_loop_t</code> 可以同时被多个线程运行，因此可以同时存在多个 context；同一线程发生嵌套 <code>wait/run</code> 时，也可以在 TLS 栈上挂载多个 context。每个 context 只记录本次调用需要的状态，例如：</p><ul><li>使用哪个 <code>loop</code>；</li><li>是否等待某个 <code>future</code>；</li><li>当前正在 Poll 还是条件变量等待；</li><li>Poll 后端线程令牌；</li><li>所属线程的 <code>pstopped</code>；</li><li>future 是否 ready。</li></ul><h3 id="4-6-context-是按需创建的"><a href="#4-6-context-是按需创建的" class="headerlink" title="4.6 context 是按需创建的"></a>4.6 context 是按需创建的</h3><p><code>ev_loop_ctx_wait_one()</code> 先尝试从任务队列取得真实任务。只有没有任务可立即执行，并且需要进入 Poll、条件变量等待或注册 future 完成通知时，才调用 <code>ev_loop_ctx_create()</code>。因此：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">调用 wait_one/run</span><br><span class="line">    ├─ 立即取到任务：可以不创建 context</span><br><span class="line">    └─ 需要等待或 Poll：创建 context 并压入线程 TLS 栈</span><br></pre></td></tr></table></figure><p>这也是 <code>pctx</code> 在第一次进入 <code>ev_loop_ctx_wait_one()</code> 时允许为 <code>NULL</code> 的原因。</p><hr><h2 id="5-pstopped：context-到线程中断标志的引用"><a href="#5-pstopped：context-到线程中断标志的引用" class="headerlink" title="5. pstopped：context 到线程中断标志的引用"></a>5. <code>pstopped</code>：context 到线程中断标志的引用</h2><h3 id="5-1-指向当前线程-TLS-中的-stopped"><a href="#5-1-指向当前线程-TLS-中的-stopped" class="headerlink" title="5.1 指向当前线程 TLS 中的 stopped"></a>5.1 指向当前线程 TLS 中的 <code>stopped</code></h3><p>context 创建时建立关系：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">ctx-&gt;pstopped</span><br><span class="line">      ↓</span><br><span class="line">当前线程的 ev_loop_thrd.stopped</span><br></pre></td></tr></table></figure><p>所以 <code>pstopped</code> 不是指向 <code>loop-&gt;stopped</code>，也不是 context 自己单独分配的停止标志。</p><h3 id="5-2-context-保存该指针的用途"><a href="#5-2-context-保存该指针的用途" class="headerlink" title="5.2 context 保存该指针的用途"></a>5.2 context 保存该指针的用途</h3><p><code>ev_loop_ctx</code> 可能被以下执行路径访问：</p><ul><li>当前运行线程的 wait 主循环；</li><li>future 完成后执行的 <code>ctx-&gt;task</code>；</li><li>其他线程调用 <code>ev_loop_kill()</code>；</li><li>loop stop 或新任务到来时的唤醒逻辑。</li></ul><p>这些路径需要操作“该 context 所属线程的中断标志”。其他线程不能通过普通变量名取得目标线程的 TLS 实例，但 context 在创建时已经把目标 TLS 地址保存为 <code>pstopped</code>，因此可以定向访问。</p><h3 id="5-3-线程级停止标志优于-context-私有标志"><a href="#5-3-线程级停止标志优于-context-私有标志" class="headerlink" title="5.3 线程级停止标志优于 context 私有标志"></a>5.3 线程级停止标志优于 context 私有标志</h3><p>从现有设计可以看出，停止语义刻意绑定到线程，而不是单个 context 对象：</p><ol><li><code>ev_loop_self()</code> 返回线程令牌；</li><li><code>ev_loop_kill()</code> 接收线程令牌；</li><li>TLS 中维护最内层 context；</li><li>中断后标志会在本次 wait 返回前清零，使下一次调用能够继续运行。</li></ol><p>如果每个 context 有独立停止字段，就需要额外处理：</p><ul><li>没有 context 时发生的 kill；</li><li>context 尚未创建但线程即将进入等待的窗口；</li><li>嵌套 context 的停止传播规则；</li><li><code>ev_loop_self()</code> 所代表的线程级语义。</li></ul><p>当前方案用一个线程级标志统一这些状态。</p><h3 id="5-4-pstopped-的同步保护"><a href="#5-4-pstopped-的同步保护" class="headerlink" title="5.4 pstopped 的同步保护"></a>5.4 <code>pstopped</code> 的同步保护</h3><p>虽然指向的是线程局部对象，但该指针可能被其他线程使用。源码通过 <code>loop-&gt;mtx</code> 对相关访问进行同步：</p><ul><li>wait 主循环在持有 <code>loop-&gt;mtx</code> 时读取；</li><li><code>ev_loop_kill()</code> 持有 <code>loop-&gt;mtx</code> 时设置；</li><li>future 回调持有 <code>loop-&gt;mtx</code> 时检查；</li><li>wait 退出时持有 <code>loop-&gt;mtx</code> 清零。</li></ul><p>因此，这个字段没有像 <code>ntasks</code> 一样采用原子类型。二者区别是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">ntasks：有无锁访问路径 → 必须原子化</span><br><span class="line">pstopped 指向的值：统一在 loop-&gt;mtx 下访问 → 普通 int 即可</span><br></pre></td></tr></table></figure><h3 id="5-5-单次中断结束后的自动清零"><a href="#5-5-单次中断结束后的自动清零" class="headerlink" title="5.5 单次中断结束后的自动清零"></a>5.5 单次中断结束后的自动清零</h3><p>线程级 stopped 的目标是中断“当前这一次”运行，而不是永久停止 loop。</p><p>若本次没有执行任务，并且检测到 thread-local stopped：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">清零 stopped</span><br><span class="line">返回调用者</span><br></pre></td></tr></table></figure><p>因此下次再调用 <code>run()</code> 或 <code>wait_one()</code> 时，可以重新进入事件循环。</p><p>永久或全局停止使用的是 <code>loop-&gt;stopped</code>，需要通过 <code>ev_loop_restart()</code> 重置。</p><h3 id="5-6-loop-级停止与线程定向中断"><a href="#5-6-loop-级停止与线程定向中断" class="headerlink" title="5.6 loop 级停止与线程定向中断"></a>5.6 loop 级停止与线程定向中断</h3><p>两个公开接口的作用范围不同：</p><table><thead><tr><th>接口</th><th>作用范围</th><th>内部状态</th><th>恢复方式</th></tr></thead><tbody><tr><td><code>ev_loop_stop(loop)</code></td><td>停止该 <code>loop</code> 上所有正在进行及后续的 run&#x2F;wait 调用</td><td>设置 <code>loop-&gt;stopped = 1</code>，并唤醒 waiting&#x2F;polling contexts</td><td><code>ev_loop_restart(loop)</code></td></tr><tr><td><code>ev_loop_kill(loop, thr)</code></td><td>中断目标线程当前最内层的一次 run&#x2F;wait 调用</td><td>通过栈顶 context 设置目标线程 TLS <code>stopped</code>，再 signal 条件变量或 kill Poll wait</td><td>本次调用返回前自动清零</td></tr></tbody></table><p>所以不能表述为“需要整个线程停止时直接使用 <code>stopped</code>”。<code>stopped</code> 是内部标志；对外应使用 <code>ev_loop_kill(loop, ev_loop_self() 返回的线程令牌)</code>。该接口不会终止操作系统线程，只会让目标线程当前最内层的 event-loop 调用返回。</p><hr><h2 id="6-cnd：非-Poll-线程的休眠与唤醒"><a href="#6-cnd：非-Poll-线程的休眠与唤醒" class="headerlink" title="6. cnd：非 Poll 线程的休眠与唤醒"></a>6. <code>cnd</code>：非 Poll 线程的休眠与唤醒</h2><h3 id="6-1-每个-context-独立持有条件变量"><a href="#6-1-每个-context-独立持有条件变量" class="headerlink" title="6.1 每个 context 独立持有条件变量"></a>6.1 每个 context 独立持有条件变量</h3><p><code>ev_loop_ctx</code> 中包含：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">cnd_t cond</span><br><span class="line">waiting 标志</span><br></pre></td></tr></table></figure><p>在 Linux pthread 兼容实现中：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">cnd_t              → pthread_cond_t</span><br><span class="line">cnd_wait()         → pthread_cond_wait()</span><br><span class="line">cnd_timedwait()    → pthread_cond_timedwait()</span><br><span class="line">cnd_signal()       → pthread_cond_signal()</span><br></pre></td></tr></table></figure><p>它不是 CANopen 条件，也不是 Poll 的事件对象，而是纯线程同步原语。</p><h3 id="6-2-进入条件变量等待的条件"><a href="#6-2-进入条件变量等待的条件" class="headerlink" title="6.2 进入条件变量等待的条件"></a>6.2 进入条件变量等待的条件</h3><p>只有同时满足以下条件时，线程才会进入 <code>cnd_wait()</code>：</p><ol><li>当前没有真实任务可执行；</li><li>context 已经创建；</li><li>当前线程不能进入 <code>ev_poll_wait()</code>；</li><li>任务队列仍为空；</li><li>loop 尚未停止、线程未被 kill、future 未 ready。</li></ol><p>“不能进入 Poll”通常是因为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">loop 配置了 npoll 限制</span><br><span class="line">并且</span><br><span class="line">已有足够数量的其他线程正在 ev_poll_wait()</span><br></pre></td></tr></table></figure><p>POSIX reactor 场景通常建议 <code>npoll == 1</code>。这意味着：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">一个线程负责 epoll_pwait</span><br><span class="line">其他 loop 线程没有任务时用条件变量睡眠</span><br></pre></td></tr></table></figure><h3 id="6-3-Poll-并发数量限制"><a href="#6-3-Poll-并发数量限制" class="headerlink" title="6.3 Poll 并发数量限制"></a>6.3 Poll 并发数量限制</h3><p>技术上 <code>npoll == 0</code> 可以允许无限数量线程同时 poll，但在 POSIX readiness reactor 模型中，多线程同时等待同一个事件源通常会增加：</p><ul><li>唤醒竞争；</li><li>锁竞争；</li><li>同一批 readiness 事件的调度复杂度；</li><li>不必要的上下文切换。</li></ul><p>Lely 的 C API 文档明确建议 POSIX reactor 使用单 polling thread。</p><p>因此条件变量承担“非 polling worker 的休眠机制”。</p><h3 id="6-4-cnd-wait-的原子解锁与重新加锁"><a href="#6-4-cnd-wait-的原子解锁与重新加锁" class="headerlink" title="6.4 cnd_wait() 的原子解锁与重新加锁"></a>6.4 <code>cnd_wait()</code> 的原子解锁与重新加锁</h3><p>调用前线程持有 <code>loop-&gt;mtx</code>。条件变量等待会完成一个原子组合动作：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">释放 loop-&gt;mtx</span><br><span class="line">并进入 cond 等待</span><br></pre></td></tr></table></figure><p>被唤醒后：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">重新获得 loop-&gt;mtx</span><br><span class="line">然后 cnd_wait 返回</span><br></pre></td></tr></table></figure><p>这里“释放锁并睡眠”必须是相对于 signal 过程原子的，否则会出现经典丢失唤醒：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">等待线程：检查无任务</span><br><span class="line">等待线程：准备睡眠但尚未真正睡眠</span><br><span class="line">投递线程：加入任务并 signal</span><br><span class="line">等待线程：此后才睡眠</span><br><span class="line">结果：任务已存在，但等待线程永久睡眠</span><br></pre></td></tr></table></figure><p>条件变量和互斥锁的组合就是为解决这个窗口。</p><h3 id="6-5-条件变量的唤醒来源"><a href="#6-5-条件变量的唤醒来源" class="headerlink" title="6.5 条件变量的唤醒来源"></a>6.5 条件变量的唤醒来源</h3><p><code>ev_loop_ctx_kill()</code> 根据 context 当前状态选择唤醒方式：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">ctx 正在 cnd 等待</span><br><span class="line">    → cnd_signal(&amp;ctx-&gt;cond)</span><br><span class="line"></span><br><span class="line">ctx 正在 Poll 等待</span><br><span class="line">    → ev_poll_kill(loop-&gt;poll, ctx-&gt;thr)</span><br><span class="line"></span><br><span class="line">ctx 两者都不是</span><br><span class="line">    → 只更新状态，后续循环自行观察</span><br></pre></td></tr></table></figure><p>触发 <code>ev_loop_ctx_kill()</code> 的主要来源包括：</p><ul><li>新任务从空队列变为非空；</li><li>future 变为 ready；</li><li><code>ev_loop_stop()</code>；</li><li>其他线程调用 <code>ev_loop_kill()</code>。</li></ul><h3 id="6-6-每个-context-独立条件变量的价值"><a href="#6-6-每个-context-独立条件变量的价值" class="headerlink" title="6.6 每个 context 独立条件变量的价值"></a>6.6 每个 context 独立条件变量的价值</h3><p>它可以做到定向唤醒：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">只唤醒被选中的 waiting context</span><br><span class="line">而不是 broadcast 唤醒所有 worker</span><br></pre></td></tr></table></figure><p><code>loop-&gt;waiting</code> 链表按 LIFO 方式组织。<code>ev_loop_kill_any()</code> 取一个 waiting context，然后 signal 它自己的 <code>cond</code>。</p><p>这减少了惊群效应。</p><h3 id="6-7-虚假唤醒与循环复核"><a href="#6-7-虚假唤醒与循环复核" class="headerlink" title="6.7 虚假唤醒与循环复核"></a>6.7 虚假唤醒与循环复核</h3><p>条件变量允许虚假唤醒，因此正确写法必须始终在循环中重新检查状态，而不能认为 <code>cnd_wait()</code> 返回就必然有任务。</p><p>Lely 的结构正是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">while 条件仍允许继续等待:</span><br><span class="line">    检查 stop / killed / future</span><br><span class="line">    检查任务队列</span><br><span class="line">    检查 poll 资格</span><br><span class="line">    必要时再次 wait</span><br></pre></td></tr></table></figure><p>所以虚假唤醒不会破坏逻辑，只会多执行一轮条件检查。</p><h3 id="6-8-条件变量与-Poll-的职责边界"><a href="#6-8-条件变量与-Poll-的职责边界" class="headerlink" title="6.8 条件变量与 Poll 的职责边界"></a>6.8 条件变量与 Poll 的职责边界</h3><table><thead><tr><th>等待方式</th><th>等待对象</th><th>唤醒机制</th></tr></thead><tbody><tr><td><code>ev_poll_wait()</code></td><td>外部 I&#x2F;O、timerfd、signal pipe 等 Poll 事件</td><td><code>ev_poll_kill()</code> 或底层 I&#x2F;O 事件</td></tr><tr><td><code>cnd_wait()</code></td><td>loop 内部任务或状态变化</td><td><code>cnd_signal()</code></td></tr></tbody></table><p>可以把它们理解为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Poll：等外部世界</span><br><span class="line">cnd：等同一进程内其他线程</span><br></pre></td></tr></table></figure><hr><h2 id="7-Future-Promise：一次性异步结果与-event-loop-的连接点"><a href="#7-Future-Promise：一次性异步结果与-event-loop-的连接点" class="headerlink" title="7. Future&#x2F;Promise：一次性异步结果与 event loop 的连接点"></a>7. Future&#x2F;Promise：一次性异步结果与 event loop 的连接点</h2><h3 id="7-1-Future-不是消息队列"><a href="#7-1-Future-不是消息队列" class="headerlink" title="7.1 Future 不是消息队列"></a>7.1 Future 不是消息队列</h3><p>Lely 的 Future&#x2F;Promise 可以宽泛地看成一种“异步完成通知机制”，但更准确的定义是：</p><blockquote><p>Future&#x2F;Promise 是围绕一份共享状态建立的一次性异步结果同步机制。</p></blockquote><p>它与消息队列的区别如下：</p><table><thead><tr><th>机制</th><th>主要语义</th></tr></thead><tbody><tr><td>消息队列</td><td>发送方可以连续写入多条消息，接收方逐条取出；通常是多次生产、多次消费。</td></tr><tr><td>Future&#x2F;Promise</td><td>Promise 最多成功完成一次；Future 观察同一个 ready 状态，并在 ready 后取得一次异步操作的结果。</td></tr><tr><td>Event-loop task queue</td><td>保存待执行的 <code>ev_task</code>，解决“在哪个 executor 上执行回调”；它不等同于 Future 的结果存储区。</td></tr></tbody></table><p>因此，不宜把 Future 理解为“发送方把请求消息放进 loop 队列，接收方执行后再沿队列返回结果”。Future 自身不负责传输请求，也不负责执行 I&#x2F;O；它只保存或关联异步操作的完成状态、结果值以及等待完成通知的任务。</p><h3 id="7-2-Promise、Future、异步操作和-Loop-的角色"><a href="#7-2-Promise、Future、异步操作和-Loop-的角色" class="headerlink" title="7.2 Promise、Future、异步操作和 Loop 的角色"></a>7.2 Promise、Future、异步操作和 Loop 的角色</h3><table><thead><tr><th>对象</th><th>准确角色</th></tr></thead><tbody><tr><td>异步操作发起方</td><td>调用 <code>AsyncRead()</code>、<code>AsyncWrite()</code>、异步 SDO 等接口，并取得 Future。</td></tr><tr><td><code>ev_promise_t</code></td><td>异步操作完成方持有的写端。操作成功、失败或取消后，通过 Promise 发布结果并将共享状态置为 ready。</td></tr><tr><td><code>ev_future_t</code></td><td>观察端。用于检查 ready、取得结果，或登记“Future ready 后应投递”的任务。</td></tr><tr><td><code>ev_loop_t</code></td><td>任务执行器和 I&#x2F;O 驱动器。它执行 executor 队列中的任务，并在队列暂时为空时进入 Poll 或条件变量等待。</td></tr><tr><td>Poll&#x2F;I&#x2F;O 组件</td><td>驱动真正的 SocketCAN、timerfd、信号或其他异步事件；事件完成后通常投递任务，任务再推进异步操作。</td></tr></tbody></table><p><code>ev_loop_t</code> 不是固定的“发送方”或“接收方”。同一个 loop 既可能执行发起异步操作的任务，也可能执行 I&#x2F;O 完成任务、Promise 完成后的通知任务以及其他应用任务。</p><h3 id="7-3-Promise-与-Future-共享一次性结果状态"><a href="#7-3-Promise-与-Future-共享一次性结果状态" class="headerlink" title="7.3 Promise 与 Future 共享一次性结果状态"></a>7.3 Promise 与 Future 共享一次性结果状态</h3><p>Promise 用于存入一个稍后由 Future 异步取得的值，其语义类似 single-shot event。Future 则提供对异步操作结果的访问。</p><p>典型状态变化为：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">stateDiagram-v2</span><br><span class="line">    [*] --&gt; Waiting</span><br><span class="line">    Waiting --&gt; Setting: promise acquires completion right</span><br><span class="line">    Setting --&gt; Ready: store result and publish ready</span><br><span class="line">    Ready --&gt; Ready: later completion attempts are rejected</span><br></pre></td></tr></table></figure><p>Promise 完成后，Future 可以：</p><ol><li>通过 <code>ev_future_is_ready()</code> 判断是否完成；</li><li>在 ready 后通过 <code>ev_future_get()</code> 取得 Promise 保存的结果；</li><li>通过 <code>ev_future_submit()</code> 登记一个任务，使其在 Future ready 后被投递到指定 executor。</li></ol><p>这里的结果值与 executor task 是两个不同概念：结果保存在 Future&#x2F;Promise 的共享状态中；task 只负责在完成时执行通知或后续处理。</p><h3 id="7-4-ev-loop-wait-loop-future-如何接入-Future"><a href="#7-4-ev-loop-wait-loop-future-如何接入-Future" class="headerlink" title="7.4 ev_loop_wait(loop, future) 如何接入 Future"></a>7.4 <code>ev_loop_wait(loop, future)</code> 如何接入 Future</h3><p>当 <code>ev_loop_ctx_create()</code> 收到非空 Future 时，它会：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">1. 获取 Future 引用并保存到 ctx-&gt;future</span><br><span class="line">2. 初始化 ctx-&gt;task，使其 executor 指向当前 loop</span><br><span class="line">3. 调用 ev_future_submit(future, &amp;ctx-&gt;task)</span><br></pre></td></tr></table></figure><p>这一步不是把“异步请求”提交给 Future，而是把一个“Future 完成后唤醒本次 wait”的内部任务登记到 Future。</p><p>当异步操作完成并满足 Promise 后：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">Promise 发布结果并置 Future ready</span><br><span class="line">    ↓</span><br><span class="line">Future 将 ctx-&gt;task 投递到 loop executor</span><br><span class="line">    ↓</span><br><span class="line">loop 从任务队列取出并执行 ctx-&gt;task</span><br><span class="line">    ↓</span><br><span class="line">ev_loop_ctx_task_func() 设置 ctx-&gt;ready = 1</span><br><span class="line">    ↓</span><br><span class="line">ev_loop_ctx_kill(ctx, 0) 唤醒 Poll 或条件变量等待</span><br><span class="line">    ↓</span><br><span class="line">ev_loop_wait() 看到 ctx-&gt;ready 后结束本次等待</span><br></pre></td></tr></table></figure><p><code>ctx-&gt;task</code> 的职责是结束等待，不是读取和返回业务结果。业务结果仍由调用方或上层包装在 Future ready 后通过 <code>ev_future_get()</code> 或相应的 C++ Future 接口取得。</p><h3 id="7-5-等待-Future-时仍要持续驱动-Loop"><a href="#7-5-等待-Future-时仍要持续驱动-Loop" class="headerlink" title="7.5 等待 Future 时仍要持续驱动 Loop"></a>7.5 等待 Future 时仍要持续驱动 Loop</h3><p><code>ev_loop_wait(loop, future)</code> 不是单纯阻塞在 Future 上。只要 Future 尚未 ready，它仍会继续推进 event loop：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">Future 尚未 ready</span><br><span class="line">    ├─ queue 有真实任务：执行一个任务</span><br><span class="line">    ├─ 当前 context 可以 Poll：调用 ev_poll_wait() 等待 I/O</span><br><span class="line">    └─ 当前 context 不可 Poll：调用 cnd_wait() 等待内部唤醒</span><br></pre></td></tr></table></figure><p>这正是异步 CANopen 操作需要的行为。例如，异步 SDO 请求的完成依赖：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">SocketCAN 收到响应或定时器到期</span><br><span class="line">    ↓</span><br><span class="line">Poll 回调投递 I/O 任务</span><br><span class="line">    ↓</span><br><span class="line">loop 执行任务并推进 SDO 状态机</span><br><span class="line">    ↓</span><br><span class="line">异步操作完成方满足 Promise</span><br><span class="line">    ↓</span><br><span class="line">Future ready，wait 返回</span><br></pre></td></tr></table></figure><p>如果线程只是同步睡眠而不再驱动同一个 loop，且没有其他线程负责执行该 loop，那么依赖 CAN I&#x2F;O 或超时任务的 Future 可能无法变为 ready。</p><h3 id="7-6-更准确的完整流程"><a href="#7-6-更准确的完整流程" class="headerlink" title="7.6 更准确的完整流程"></a>7.6 更准确的完整流程</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant Caller as AsyncCaller</span><br><span class="line">    participant Operation as AsyncOperation</span><br><span class="line">    participant EL as EventLoop</span><br><span class="line">    participant Poller as PollBackend</span><br><span class="line">    participant Device as SocketCAN</span><br><span class="line">    participant Promise as PromiseFuture</span><br><span class="line"></span><br><span class="line">    Caller-&gt;&gt;Operation: start async request</span><br><span class="line">    Operation--&gt;&gt;Caller: return Future</span><br><span class="line">    Caller-&gt;&gt;EL: wait on Future</span><br><span class="line">    EL-&gt;&gt;Promise: submit internal wake task</span><br><span class="line">    EL-&gt;&gt;Poller: wait for I/O</span><br><span class="line">    Device--&gt;&gt;Poller: response or readiness event</span><br><span class="line">    Poller--&gt;&gt;EL: post completion task</span><br><span class="line">    EL-&gt;&gt;Operation: run completion task</span><br><span class="line">    Operation-&gt;&gt;Promise: publish result and set ready</span><br><span class="line">    Promise--&gt;&gt;EL: post Future-ready task</span><br><span class="line">    EL-&gt;&gt;EL: set context ready and wake wait</span><br><span class="line">    EL--&gt;&gt;Caller: wait returns</span><br><span class="line">    Caller-&gt;&gt;Promise: obtain result</span><br></pre></td></tr></table></figure><p>因此，更准确的理解是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">异步操作发起方启动请求并取得 Future；</span><br><span class="line">Loop 持续执行任务和驱动 I/O；</span><br><span class="line">异步操作完成方通过 Promise 发布结果；</span><br><span class="line">Future 把完成通知任务投递到 Loop；</span><br><span class="line">Loop 执行该通知任务并结束 wait；</span><br><span class="line">调用方再从 Future 取得结果。</span><br></pre></td></tr></table></figure><h3 id="7-7-run-与-wait-future-的区别"><a href="#7-7-run-与-wait-future-的区别" class="headerlink" title="7.7 run() 与 wait(future) 的区别"></a>7.7 <code>run()</code> 与 <code>wait(future)</code> 的区别</h3><table><thead><tr><th>调用</th><th>内部 Future</th><th>主要返回条件</th></tr></thead><tbody><tr><td><code>ev_loop_run(loop)</code></td><td><code>NULL</code></td><td>loop stopped，或 outstanding work 归零触发 stop。</td></tr><tr><td><code>ev_loop_wait(loop, future)</code></td><td>指定 Future</td><td>Future ready、loop stopped、线程被中断或发生错误。</td></tr><tr><td><code>ev_loop_wait_until(loop, future, deadline)</code></td><td>指定 Future</td><td>在上述条件外增加绝对超时。</td></tr><tr><td><code>ev_loop_poll(loop)</code></td><td><code>NULL</code></td><td>非阻塞处理当前可立即执行的工作。</td></tr></tbody></table><p>Future 在 <code>wait()</code> 中是“本次 event-loop 运行的结束条件之一”，而不是 loop 队列中的普通业务任务。</p><hr><h2 id="8-任务投递后的-Poll-cnd-唤醒路径"><a href="#8-任务投递后的-Poll-cnd-唤醒路径" class="headerlink" title="8. 任务投递后的 Poll&#x2F;cnd 唤醒路径"></a>8. 任务投递后的 Poll&#x2F;cnd 唤醒路径</h2><h3 id="8-1-任务投递路径"><a href="#8-1-任务投递路径" class="headerlink" title="8.1 任务投递路径"></a>8.1 任务投递路径</h3><p>任务被投递到 event loop 时：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">获取 loop-&gt;mtx</span><br><span class="line">判断投递前队列是否为空</span><br><span class="line">把 task 放到 queue 尾部</span><br><span class="line">若此前为空，调用 ev_loop_kill_any(loop, 1)</span><br><span class="line">释放 loop-&gt;mtx</span><br></pre></td></tr></table></figure><p>只在“空 → 非空”转换时唤醒线程，避免每次追加任务都重复发唤醒。</p><h3 id="8-2-waiting-context-的优先级"><a href="#8-2-waiting-context-的优先级" class="headerlink" title="8.2 waiting context 的优先级"></a>8.2 waiting context 的优先级</h3><p><code>ev_loop_kill_any()</code> 的顺序是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">先找 waiting 链表中的 context</span><br><span class="line">若存在：通过 cnd_signal 唤醒</span><br><span class="line">否则在允许时找 polling context</span><br><span class="line">若存在：通过 ev_poll_kill 唤醒</span><br></pre></td></tr></table></figure><p>原因很直接：</p><ul><li>条件变量线程本来就在等内部任务；</li><li>唤醒它不需要打断正在处理外部 I&#x2F;O 的 poller；</li><li>可让 poller 继续服务 I&#x2F;O readiness。</li></ul><h3 id="8-3-完整时序"><a href="#8-3-完整时序" class="headerlink" title="8.3 完整时序"></a>8.3 完整时序</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant P as 投递线程</span><br><span class="line">    participant L as ev_loop</span><br><span class="line">    participant W as 条件变量等待线程</span><br><span class="line">    participant R as Poll 等待线程</span><br><span class="line"></span><br><span class="line">    P-&gt;&gt;L: ev_exec_post(task)</span><br><span class="line">    L-&gt;&gt;L: lock + queue push</span><br><span class="line">    alt 存在 waiting context</span><br><span class="line">        L-&gt;&gt;W: cnd_signal(ctx.cond)</span><br><span class="line">        W-&gt;&gt;W: 重新取得 loop.mtx</span><br><span class="line">        W-&gt;&gt;L: 从 queue 取任务</span><br><span class="line">    else 没有 waiting，但存在 polling context</span><br><span class="line">        L-&gt;&gt;R: ev_poll_kill()</span><br><span class="line">        R-&gt;&gt;R: 底层 Poll wait 被中断</span><br><span class="line">        R-&gt;&gt;L: 返回并从 queue 取任务</span><br><span class="line">    end</span><br></pre></td></tr></table></figure><hr><h2 id="9-ev-poll-wait-的虚函数分派链"><a href="#9-ev-poll-wait-的虚函数分派链" class="headerlink" title="9. ev_poll_wait() 的虚函数分派链"></a>9. <code>ev_poll_wait()</code> 的虚函数分派链</h2><h3 id="9-1-抽象接口分派"><a href="#9-1-抽象接口分派" class="headerlink" title="9.1 抽象接口分派"></a>9.1 抽象接口分派</h3><p><code>ev_poll_t</code> 不是一个固定的 Linux <code>io_poll</code> 类型，而是一个抽象 Poll 接口。它的虚函数表包含：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">self</span><br><span class="line">wait</span><br><span class="line">kill</span><br></pre></td></tr></table></figure><p><code>ev_poll_wait()</code> 做的事情等价于：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">调用当前 poll 对象虚函数表中的 wait 指针</span><br></pre></td></tr></table></figure><p>因此：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">ev_poll_wait</span><br><span class="line">不是具体后端函数</span><br><span class="line">而是统一分派入口</span><br></pre></td></tr></table></figure><h3 id="9-2-Linux-io-Poll-后端注册"><a href="#9-2-Linux-io-Poll-后端注册" class="headerlink" title="9.2 Linux io::Poll 后端注册"></a>9.2 Linux <code>io::Poll</code> 后端注册</h3><p>Linux <code>io_poll</code> 初始化时：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">poll_vptr = io_poll_poll_vtbl</span><br></pre></td></tr></table></figure><p>该虚函数表的 <code>wait</code> 成员指向：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">io_poll_poll_wait</span><br></pre></td></tr></table></figure><p><code>io_poll_get_poll()</code> 返回的不是新对象，而是 <code>io_poll</code> 内部 <code>poll_vptr</code> 字段的地址。<code>ev_loop</code> 保存这个抽象接口指针。</p><p>完整对象关系：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    A[ev_loop.poll] --&gt; B[io_poll.poll_vptr 的地址]</span><br><span class="line">    B --&gt; C[io_poll_poll_vtbl]</span><br><span class="line">    C --&gt; D[self = io_poll_poll_self]</span><br><span class="line">    C --&gt; E[wait = io_poll_poll_wait]</span><br><span class="line">    C --&gt; F[kill = io_poll_poll_kill]</span><br></pre></td></tr></table></figure><h3 id="9-3-完整调用链"><a href="#9-3-完整调用链" class="headerlink" title="9.3 完整调用链"></a>9.3 完整调用链</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">ev_loop_ctx_wait_one_until()</span><br><span class="line">  ↓</span><br><span class="line">ev_poll_wait(loop-&gt;poll, msec)</span><br><span class="line">  ↓</span><br><span class="line">(*loop-&gt;poll)-&gt;wait(loop-&gt;poll, msec)</span><br><span class="line">  ↓</span><br><span class="line">io_poll_poll_wait(..., msec)</span><br><span class="line">  ↓</span><br><span class="line">epoll_pwait(epfd, events, maxevents, msec, signal_mask)</span><br></pre></td></tr></table></figure><p>因此，在该对象组合下可以确定：</p><blockquote><p>如果 <code>loop-&gt;poll</code> 来自 Linux <code>io_poll_get_poll()</code>，执行的就是 <code>io_poll_poll_wait()</code>。</p></blockquote><p>但要加两个限定：</p><ol><li>它是通过函数指针间接调用，不是 <code>loop.c</code> 对 <code>io_poll_poll_wait()</code> 的直接静态调用；</li><li>换成 Windows Poll 或自定义 <code>ev_poll_t</code> 实现后，会调用对应后端的 <code>wait</code>。</li></ol><h3 id="9-4-与-POSIX-poll-2-的区别"><a href="#9-4-与-POSIX-poll-2-的区别" class="headerlink" title="9.4 与 POSIX poll(2) 的区别"></a>9.4 与 POSIX <code>poll(2)</code> 的区别</h3><p>名称容易产生误导：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">ev_poll_wait：Lely 抽象接口</span><br><span class="line">io_poll_poll_wait：Linux io2 后端函数</span><br><span class="line">epoll_pwait：实际 Linux 系统调用</span><br></pre></td></tr></table></figure><p>它没有调用传统 POSIX <code>poll()</code>。</p><h3 id="9-5-msec-在两个-wait-函数中的含义"><a href="#9-5-msec-在两个-wait-函数中的含义" class="headerlink" title="9.5 msec 在两个 wait 函数中的含义"></a>9.5 <code>msec</code> 在两个 wait 函数中的含义</h3><table><thead><tr><th>调用场景</th><th align="right"><code>msec</code></th></tr></thead><tbody><tr><td>无超时 wait，队列为空</td><td align="right"><code>-1</code>，无限阻塞</td></tr><tr><td>无超时 wait，但已有任务</td><td align="right"><code>0</code>，只检查一次 I&#x2F;O</td></tr><tr><td>until，有未来 deadline 且队列为空</td><td align="right">deadline 剩余毫秒</td></tr><tr><td>until，deadline 为 NULL</td><td align="right"><code>0</code>，完全非阻塞</td></tr><tr><td>until，队列已非空</td><td align="right"><code>0</code>，避免阻塞任务执行</td></tr></tbody></table><p>注意：<code>ev_poll_wait()</code> 的返回值表示 Poll 调用成功或失败，并不等于“执行了几个 loop task”。Poll 后端可能在内部处理多个 I&#x2F;O watch 回调，这些回调通常再把任务投递到 loop 队列；loop 下一轮才执行对应 task。</p><hr><h2 id="10-从-Loop-run-到-SocketCAN-事件的完整流程"><a href="#10-从-Loop-run-到-SocketCAN-事件的完整流程" class="headerlink" title="10. 从 Loop::run() 到 SocketCAN 事件的完整流程"></a>10. 从 <code>Loop::run()</code> 到 SocketCAN 事件的完整流程</h2><p>结合前一篇 <code>io_poll</code> 分析，可以把完整运行链路串起来：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">1. C++ 调用 loop.run()</span><br><span class="line">2. loop.hpp 转调 ev_loop_run()</span><br><span class="line">3. ev_loop_run() 转调 ev_loop_wait(loop, NULL)</span><br><span class="line">4. ev_loop_wait() 在外层循环中重复调用 ev_loop_ctx_wait_one()</span><br><span class="line">5. 每次 wait_one 最多主动执行一个真实队列任务；若当前没有任务但 ntasks &gt; 0，loop 不会 stop</span><br><span class="line">6. 若当前线程有 Poll 资格，调用 ev_poll_wait(..., -1)</span><br><span class="line">7. 虚函数分派到 io_poll_poll_wait()</span><br><span class="line">8. io_poll_poll_wait() 在 epoll_pwait() 阻塞</span><br><span class="line">9. SocketCAN fd 可读，epoll 返回</span><br><span class="line">10. io_poll 找到对应 io_poll_watch 并调用 watch 回调</span><br><span class="line">11. CanChannel watch 回调向 executor 投递接收任务</span><br><span class="line">12. 投递使 loop queue 从空变为非空，并中断 Poll wait 或唤醒 cnd waiter</span><br><span class="line">13. ev_loop_ctx_wait_one() 重新检查 queue</span><br><span class="line">14. 弹出并执行 CanChannel 接收任务</span><br><span class="line">15. 读取 CAN 帧，完成异步操作并进入 CanNet/CANopen 上层</span><br></pre></td></tr></table></figure><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant App as Application</span><br><span class="line">    participant Ev as EventLoop</span><br><span class="line">    participant Vtbl as PollVtable</span><br><span class="line">    participant Io as IoPoll</span><br><span class="line">    participant Kernel as EpollWait</span><br><span class="line">    participant Can as SocketCAN</span><br><span class="line">    participant Exec as ExecutorQueue</span><br><span class="line"></span><br><span class="line">    App-&gt;&gt;Ev: call run</span><br><span class="line">    Ev-&gt;&gt;Ev: queue empty and outstanding work exists</span><br><span class="line">    Ev-&gt;&gt;Vtbl: wait with infinite timeout</span><br><span class="line">    Vtbl-&gt;&gt;Io: call backend wait</span><br><span class="line">    Io-&gt;&gt;Kernel: enter epoll wait</span><br><span class="line">    Can--&gt;&gt;Kernel: file descriptor becomes readable</span><br><span class="line">    Kernel--&gt;&gt;Io: return input event</span><br><span class="line">    Io-&gt;&gt;Exec: callback posts receive task</span><br><span class="line">    Exec-&gt;&gt;Ev: queue changes from empty to nonempty</span><br><span class="line">    Ev-&gt;&gt;Exec: pop one task</span><br><span class="line">    Exec-&gt;&gt;Can: read CAN frame</span><br></pre></td></tr></table></figure><hr><h2 id="11-npoll-1-的典型多线程运行"><a href="#11-npoll-1-的典型多线程运行" class="headerlink" title="11. npoll = 1 的典型多线程运行"></a>11. <code>npoll = 1</code> 的典型多线程运行</h2><p>假设：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">npoll = 1</span><br><span class="line">线程 A、B 都调用 loop.run()</span><br></pre></td></tr></table></figure><p>可能出现：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">线程 A：取得 Poll 资格，进入 epoll_pwait()</span><br><span class="line">线程 B：无 Poll 资格，进入 cnd_wait()</span><br></pre></td></tr></table></figure><p>此时另一个线程 C 投递一个普通任务：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">线程 C：queue 空→非空</span><br><span class="line">线程 C：优先 signal 线程 B 的 cond</span><br><span class="line">线程 B：醒来并执行任务</span><br><span class="line">线程 A：继续等待 I/O</span><br></pre></td></tr></table></figure><p>如果没有 waiting 线程，才会中断 Poll 线程 A：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">线程 C：ev_poll_kill(A 的 poll thread token)</span><br><span class="line">线程 A：epoll_pwait 被信号打断</span><br><span class="line">线程 A：返回 loop，执行新任务</span><br></pre></td></tr></table></figure><p>这种设计让：</p><ul><li>Poll 线程尽量持续负责外部 I&#x2F;O；</li><li>非 Poll worker 优先处理内部任务；</li><li>必要时仍能中断 Poll，保证任务投递不会长期等待。</li></ul><hr><h2 id="12-关键对象关系"><a href="#12-关键对象关系" class="headerlink" title="12. 关键对象关系"></a>12. 关键对象关系</h2><table><thead><tr><th>对象&#x2F;字段</th><th>归属</th><th>是否共享</th><th>保护方式</th><th>主要职责</th></tr></thead><tbody><tr><td><code>ev_loop_t</code></td><td>每个 loop</td><td>多线程共享</td><td><code>loop-&gt;mtx</code> + 部分原子</td><td>任务队列、停止状态、poller&#x2F;waiter 管理</td></tr><tr><td><code>loop-&gt;queue</code></td><td>每个 loop</td><td>多线程共享</td><td><code>loop-&gt;mtx</code></td><td>待执行任务队列</td></tr><tr><td><code>loop-&gt;ntasks</code></td><td>每个 loop</td><td>多线程共享</td><td>原子操作</td><td>outstanding work 计数</td></tr><tr><td><code>loop-&gt;stopped</code></td><td>每个 loop</td><td>多线程共享</td><td><code>loop-&gt;mtx</code></td><td>loop 全局停止状态</td></tr><tr><td><code>ev_loop_thrd</code></td><td>每个线程</td><td>不在线程间共享实例</td><td>TLS；其字段跨线程访问时受 <code>loop-&gt;mtx</code> 保护</td><td>线程级中断和嵌套 context 栈顶</td></tr><tr><td><code>ev_loop_ctx</code></td><td>按需创建的一次 wait&#x2F;run context</td><td>可被 future&#x2F;kill 路径访问</td><td><code>loop-&gt;mtx</code> + refcnt</td><td>连接 loop、线程、Poll&#x2F;cond 和可选 future；不是 loop 对象</td></tr><tr><td><code>ctx-&gt;pstopped</code></td><td>context 指向线程 TLS</td><td>指向目标线程实例</td><td><code>loop-&gt;mtx</code></td><td>检查或设置该线程的单次中断标志</td></tr><tr><td><code>ctx-&gt;cond</code></td><td>每个 context</td><td>目标线程等待、其他线程 signal</td><td>条件变量 + <code>loop-&gt;mtx</code></td><td>非 Poll 线程休眠与唤醒</td></tr><tr><td><code>ctx-&gt;thr</code></td><td>每个 context</td><td>保存 Poll 后端线程令牌</td><td>后端定义</td><td>通过 <code>ev_poll_kill()</code> 中断具体 Poll wait</td></tr><tr><td><code>ctx-&gt;future</code></td><td>可选，每个 context</td><td>与 promise 共享异步完成状态</td><td>future mutex&#x2F;atomic state + refcount</td><td>future ready 时投递 <code>ctx-&gt;task</code> 并结束本次 wait</td></tr><tr><td><code>ev_promise_t</code></td><td>每个异步结果生产端</td><td>可跨线程共享</td><td>future mutex&#x2F;atomic state + refcount</td><td>一次性写入结果并发布 ready</td></tr><tr><td><code>loop-&gt;poll</code></td><td>每个 loop</td><td>可共享后端 Poll</td><td><code>ev_poll</code> 接口契约</td><td>对外部事件进行抽象等待</td></tr></tbody></table><hr><h2 id="13-参考资料"><a href="#13-参考资料" class="headerlink" title="13. 参考资料"></a>13. 参考资料</h2><h3 id="13-1-Lely-官方源码"><a href="#13-1-Lely-官方源码" class="headerlink" title="13.1 Lely 官方源码"></a>13.1 Lely 官方源码</h3><ol><li><a href="https://gitlab.com/lely_industries/lely-core/-/blob/master/src/ev/loop.c">Lely <code>src/ev/loop.c</code></a></li><li><a href="https://gitlab.com/lely_industries/lely-core/-/blob/master/include/lely/ev/loop.h">Lely <code>include/lely/ev/loop.h</code></a></li><li><a href="https://gitlab.com/lely_industries/lely-core/-/blob/master/include/lely/ev/loop.hpp">Lely <code>include/lely/ev/loop.hpp</code></a></li><li><a href="https://gitlab.com/lely_industries/lely-core/-/blob/master/include/lely/ev/poll.h">Lely <code>include/lely/ev/poll.h</code></a></li><li><a href="https://gitlab.com/lely_industries/lely-core/-/blob/master/src/io2/linux/poll.c">Lely Linux <code>src/io2/linux/poll.c</code></a></li><li><a href="https://gitlab.com/lely_industries/lely-core/-/blob/master/src/libc/threads-pthread.c">Lely pthread 兼容实现 <code>src/libc/threads-pthread.c</code></a></li><li><a href="https://gitlab.com/lely_industries/lely-core/-/blob/master/include/lely/ev/exec.h">Lely executor API <code>include/lely/ev/exec.h</code></a></li><li><a href="https://gitlab.com/lely_industries/lely-core/-/blob/master/src/ev/std_exec.c">Lely standard executor <code>src/ev/std_exec.c</code></a></li><li><a href="https://gitlab.com/lely_industries/lely-core/-/blob/master/include/lely/ev/future.h">Lely Future API <code>include/lely/ev/future.h</code></a></li><li><a href="https://gitlab.com/lely_industries/lely-core/-/blob/master/src/ev/future.c">Lely Future implementation <code>src/ev/future.c</code></a></li><li><a href="https://opensource.lely.com/canopen/docs/overview/">Lely library overview</a></li></ol><h3 id="13-2-线程与语言规范"><a href="#13-2-线程与语言规范" class="headerlink" title="13.2 线程与语言规范"></a>13.2 线程与语言规范</h3><ol><li><a href="https://pubs.opengroup.org/onlinepubs/9699919799/functions/pthread_cond_timedwait.html">The Open Group: <code>pthread_cond_wait()</code> &#x2F; <code>pthread_cond_timedwait()</code></a></li><li><a href="https://pubs.opengroup.org/onlinepubs/9699919799/functions/pthread_cond_broadcast.html">The Open Group: <code>pthread_cond_signal()</code></a></li><li><a href="https://www.open-std.org/jtc1/sc22/wg14/www/docs/n1570.pdf">WG14 N1570: C11 draft</a></li></ol><h3 id="13-3-关联文章"><a href="#13-3-关联文章" class="headerlink" title="13.3 关联文章"></a>13.3 关联文章</h3><p>本文的章节结构、结论先行、流程图、状态表和源码索引方式参考了已上传的《Lely canopen <code>io_poll</code> 机制原理与完整运行流程详解》。两篇文章可以连续阅读：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">ev_loop：决定何时执行 task、何时等待、怎样协调多个线程</span><br><span class="line">io_poll：决定怎样等待 Linux fd、怎样分发 epoll readiness</span><br></pre></td></tr></table></figure>]]></content>
    
    
      
      
    <summary type="html">&lt;meta name=&quot;referrer&quot; content=&quot;no-referrer&quot; /&gt;

&lt;h1 id=&quot;Lely-CANopen-ev-loop-详解&quot;&gt;&lt;a href=&quot;#Lely-CANopen-ev-loop-详解&quot; class=&quot;headerlink&quot; title</summary>
      
    
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/categories/canopen/"/>
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/tags/canopen/"/>
    
  </entry>
  
  <entry>
    <title>Lely canopen`io_poll` 机制原理与完整运行流程详解</title>
    <link href="https://wdfk-prog.space/posts/12cc1336/"/>
    <id>https://wdfk-prog.space/posts/12cc1336/</id>
    <published>2026-07-28T02:07:12.000Z</published>
    <updated>2026-07-29T01:33:31.806Z</updated>
    
    <content type="html"><![CDATA[<meta name="referrer" content="no-referrer" /><h1 id="Lely-canopenio-poll-机制原理与完整运行流程详解"><a href="#Lely-canopenio-poll-机制原理与完整运行流程详解" class="headerlink" title="Lely canopenio_poll 机制原理与完整运行流程详解"></a>Lely canopen<code>io_poll</code> 机制原理与完整运行流程详解</h1><p><img src="https://i-blog.csdnimg.cn/direct/dc46f17f56404d538717621954000b0a.png" alt="在这里插入图片描述"></p><p>@[toc]</p><h2 id="1-先给结论"><a href="#1-先给结论" class="headerlink" title="1. 先给结论"></a>1. 先给结论</h2><p>Lely 的 <code>io_poll</code> 是一个基于 Linux <code>epoll</code> 的 I&#x2F;O 事件分发器。它不解析 CANopen，也不主动读取 SocketCAN；它只负责：</p><ol><li>把需要关注的文件描述符 <code>fd</code> 登记到 Linux <code>epoll</code>；</li><li>阻塞等待这些 <code>fd</code> 出现可读、可写、异常或断开事件；</li><li>根据内核返回的 <code>fd</code>，在红黑树中找到对应的 <code>io_poll_watch</code>；</li><li>调用 <code>watch-&gt;func(watch, events)</code>；</li><li>因为所有登记都附加了 <code>EPOLLONESHOT</code>，每次通知后当前一次监听失效；</li><li>调用方若要继续接收后续事件，必须再次调用 <code>io_poll_watch()</code> 恢复下一次监听。</li></ol><p>最重要的完整周期是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line">创建 Poll</span><br><span class="line">  ↓</span><br><span class="line">初始化 io_poll_watch</span><br><span class="line">  ↓</span><br><span class="line">io_poll_watch(events != 0)</span><br><span class="line">  ↓</span><br><span class="line">首次登记：EPOLL_CTL_ADD</span><br><span class="line">  ↓</span><br><span class="line">ev_poll_wait() → epoll_pwait()</span><br><span class="line">  ↓</span><br><span class="line">内核返回 fd 和 EPOLL* 事件</span><br><span class="line">  ↓</span><br><span class="line">红黑树按 fd 找到 io_poll_watch</span><br><span class="line">  ↓</span><br><span class="line">将 watch-&gt;_events 清零，表示本次一次性监听已消费</span><br><span class="line">  ↓</span><br><span class="line">调用 watch-&gt;func()</span><br><span class="line">  ↓</span><br><span class="line">回调读取/写入 fd</span><br><span class="line">  ↓</span><br><span class="line">仍需继续监听？</span><br><span class="line">  ├─ 是：再次 io_poll_watch(events != 0) → EPOLL_CTL_MOD，恢复下一次监听</span><br><span class="line">  └─ 否：io_poll_watch(events = 0) → EPOLL_CTL_DEL，彻底取消登记</span><br></pre></td></tr></table></figure><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[创建 io_poll] --&gt; B[初始化 io_poll_watch]</span><br><span class="line">    B --&gt; C[io_poll_watch: events 非 0]</span><br><span class="line">    C --&gt; D&#123;红黑树中已有该 fd?&#125;</span><br><span class="line">    D -- 否 --&gt; E[EPOLL_CTL_ADD]</span><br><span class="line">    D -- 是且本次监听需更新/恢复 --&gt; F[EPOLL_CTL_MOD]</span><br><span class="line">    E --&gt; G[当前一次监听有效]</span><br><span class="line">    F --&gt; G</span><br><span class="line">    G --&gt; H[epoll_pwait 等待]</span><br><span class="line">    H --&gt; I[内核返回 fd 和 EPOLL 事件]</span><br><span class="line">    I --&gt; J[红黑树按 fd 找到 watch]</span><br><span class="line">    J --&gt; K[_events=0, nwatch--]</span><br><span class="line">    K --&gt; L[调用 watch-&gt;func]</span><br><span class="line">    L --&gt; M&#123;还要继续监听?&#125;</span><br><span class="line">    M -- 是 --&gt; C</span><br><span class="line">    M -- 否 --&gt; N[io_poll_watch: events=0]</span><br><span class="line">    N --&gt; O[EPOLL_CTL_DEL + 红黑树删除]</span><br><span class="line">    O --&gt; P[销毁 Poll]</span><br></pre></td></tr></table></figure><hr><h2 id="2-Poll-在-Lely-CANopen-全栈中的位置：哪些模块使用它、分别做什么"><a href="#2-Poll-在-Lely-CANopen-全栈中的位置：哪些模块使用它、分别做什么" class="headerlink" title="2. Poll 在 Lely CANopen 全栈中的位置：哪些模块使用它、分别做什么"></a>2. Poll 在 Lely CANopen 全栈中的位置：哪些模块使用它、分别做什么</h2><p>本章基于 Lely CANopen 源码中的实际调用关系回答两个问题：</p><ol><li>哪些模块直接调用 <code>io_poll_watch()</code>，把文件描述符交给 <code>Poll</code> 监听；</li><li>哪些 CANopen 组件虽然不直接调用 <code>Poll</code>，但其异步收发和定时机制最终依赖 <code>Poll</code>。</li></ol><p>这里分析的是新 I&#x2F;O 库 <code>liblely-io2</code> 的 Linux&#x2F;POSIX 实现，对应：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">include/lely/io2/...</span><br><span class="line">src/io2/...</span><br><span class="line">src/ev/...</span><br><span class="line">src/coapp/...</span><br></pre></td></tr></table></figure><p>不是旧 I&#x2F;O 库中的：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">include/lely/io/poll.h</span><br><span class="line">src/io/poll.c</span><br></pre></td></tr></table></figure><p>两套库中都存在名为 <code>poll</code> 的接口，但对象模型和调用方式不同。本文后续所说的 <code>Poll</code>，均指本文件正在分析的 <code>io2</code> 实现：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">include/lely/io2/posix/poll.h</span><br><span class="line">include/lely/io2/posix/poll.hpp</span><br><span class="line">src/io2/linux/poll.c</span><br></pre></td></tr></table></figure><h3 id="2-1-先给结论：直接把-fd-登记到-Poll-的核心组件只有三个"><a href="#2-1-先给结论：直接把-fd-登记到-Poll-的核心组件只有三个" class="headerlink" title="2.1 先给结论：直接把 fd 登记到 Poll 的核心组件只有三个"></a>2.1 先给结论：直接把 fd 登记到 Poll 的核心组件只有三个</h3><p>在 <code>liblely-io2</code> 的 Linux&#x2F;POSIX 后端中，搜索 <code>IO_POLL_WATCH_INIT</code> 和 <code>io_poll_watch()</code> 可以看到三个核心设备实现直接持有 <code>struct io_poll_watch</code>：</p><table><thead><tr><th>直接使用 Poll 的组件</th><th>源码文件</th><th>交给 Poll 的 fd</th><th>监听事件</th><th>主要作用</th></tr></thead><tbody><tr><td>Linux CAN 通道</td><td><code>src/io2/linux/can_chan.c</code></td><td>SocketCAN raw socket</td><td><code>IO_EVENT_IN</code>、<code>IO_EVENT_OUT</code></td><td>CAN 帧异步接收、发送阻塞后的重试、发送确认处理</td></tr><tr><td>Linux 系统定时器</td><td><code>src/io2/linux/timer.c</code></td><td><code>timerfd</code></td><td><code>IO_EVENT_IN</code></td><td>把定时器到期转换成异步 wait 完成事件</td></tr><tr><td>POSIX 信号集合</td><td><code>src/io2/posix/sigset.c</code></td><td>self-pipe 的读端</td><td><code>IO_EVENT_IN</code></td><td>把 POSIX signal 安全地转换成事件循环任务</td></tr></tbody></table><p>此外，还有一个非常关键但角色不同的组件：</p><table><thead><tr><th>组件</th><th>是否调用 <code>io_poll_watch()</code></th><th>它对 Poll 做什么</th></tr></thead><tbody><tr><td><code>ev::Loop</code> &#x2F; <code>ev_loop</code></td><td>否</td><td>调用 <code>ev_poll_wait()</code> 驱动 Poll 等待事件；需要唤醒时调用 <code>ev_poll_kill()</code></td></tr></tbody></table><p>再往上的组件都属于间接依赖：</p><table><thead><tr><th>上层组件</th><th>是否直接操作 Poll</th><th>实际依赖路径</th></tr></thead><tbody><tr><td><code>io::CanNet</code></td><td>否</td><td>使用 <code>CanChannel</code> 收发 CAN 帧，使用 <code>Timer</code> 驱动 CAN 网络时间和超时</td></tr><tr><td><code>canopen::Node</code></td><td>否</td><td>继承&#x2F;组合 <code>CanNet</code>，把收到的 CAN 帧交给 CANopen 协议对象</td></tr><tr><td><code>BasicMaster</code> &#x2F; <code>AsyncMaster</code></td><td>否</td><td>通过 <code>Node → CanNet → CanChannel/Timer → Poll</code> 间接使用</td></tr><tr><td><code>BasicSlave</code></td><td>否</td><td>同样通过 <code>Node</code> 和 I&#x2F;O 层间接使用</td></tr><tr><td><code>DriverBase</code> &#x2F; <code>BasicDriver</code> &#x2F; <code>LoopDriver</code></td><td>否</td><td>消费 Master 分发的 CANopen 事件，不负责监听 SocketCAN fd</td></tr></tbody></table><p>因此，不能把 <code>Poll</code> 理解成“CANopen 主站模块的一部分”。更准确的层次是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">Poll 是底层异步 I/O 事件源聚合器；</span><br><span class="line">CanChannel、Timer、SignalSet 是它的直接设备客户端；</span><br><span class="line">ev::Loop 是它的等待驱动者；</span><br><span class="line">CanNet、Node、Master、Slave 和 CANopen Driver 是间接消费者。</span><br></pre></td></tr></table></figure><h3 id="2-2-全栈依赖关系图"><a href="#2-2-全栈依赖关系图" class="headerlink" title="2.2 全栈依赖关系图"></a>2.2 全栈依赖关系图</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br></pre></td><td class="code"><pre><span class="line">flowchart TB</span><br><span class="line">    APP[用户应用程序] --&gt; CTX[io::Context]</span><br><span class="line">    APP --&gt; POLL[io::Poll]</span><br><span class="line">    APP --&gt; LOOP[ev::Loop]</span><br><span class="line">    APP --&gt; TIMER[io::Timer]</span><br><span class="line">    APP --&gt; CHAN[io::CanChannel]</span><br><span class="line">    APP --&gt; SIGSET[io::SignalSet]</span><br><span class="line"></span><br><span class="line">    CTX --&gt; POLL</span><br><span class="line">    POLL -- get_poll() --&gt; LOOP</span><br><span class="line">    LOOP -- ev_poll_wait() --&gt; POLL</span><br><span class="line">    LOOP -- 执行已投递任务 --&gt; EXEC[Executor task queue]</span><br><span class="line"></span><br><span class="line">    POLL -- IO_EVENT_IN / OUT --&gt; CHAN</span><br><span class="line">    CHAN --&gt; CANFD[SocketCAN raw socket fd]</span><br><span class="line"></span><br><span class="line">    POLL -- IO_EVENT_IN --&gt; TIMER</span><br><span class="line">    TIMER --&gt; TFD[timerfd]</span><br><span class="line"></span><br><span class="line">    POLL -- IO_EVENT_IN --&gt; SIGSET</span><br><span class="line">    SIGSET --&gt; PIPE[self-pipe read fd]</span><br><span class="line"></span><br><span class="line">    CHAN -- 异步读写完成任务 --&gt; EXEC</span><br><span class="line">    TIMER -- 定时等待完成任务 --&gt; EXEC</span><br><span class="line">    SIGSET -- 信号处理任务 --&gt; EXEC</span><br><span class="line"></span><br><span class="line">    CHAN --&gt; CANNET[io::CanNet]</span><br><span class="line">    TIMER --&gt; CANNET</span><br><span class="line">    CANNET --&gt; PASSIVE[can_net_t 被动 CAN 网络核心]</span><br><span class="line">    PASSIVE --&gt; NODE[canopen::Node]</span><br><span class="line">    NODE --&gt; MASTER[BasicMaster / AsyncMaster]</span><br><span class="line">    NODE --&gt; SLAVE[BasicSlave]</span><br><span class="line">    MASTER --&gt; DRIVER[DriverBase / BasicDriver / LoopDriver]</span><br></pre></td></tr></table></figure><p>图中最关键的边界是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">Linux fd 就绪通知</span><br><span class="line">        ↓</span><br><span class="line">Poll</span><br><span class="line">        ↓</span><br><span class="line">设备回调只负责投递 task</span><br><span class="line">        ↓</span><br><span class="line">ev::Loop / Executor 执行 task</span><br><span class="line">        ↓</span><br><span class="line">CanChannel、Timer、SignalSet 完成真正处理</span><br><span class="line">        ↓</span><br><span class="line">CanNet 把 I/O 结果送入被动 CAN/CANopen 协议核心</span><br></pre></td></tr></table></figure><p><code>Poll</code> 的回调通常不直接执行完整协议逻辑，而是尽快把后续工作投递给 executor。这样可以缩短 <code>epoll_pwait()</code> 返回后的临界处理时间，并避免在 Poll 内部互斥锁上下文中执行复杂业务。</p><h3 id="2-3-C-应用是怎样把这些组件连接起来的"><a href="#2-3-C-应用是怎样把这些组件连接起来的" class="headerlink" title="2.3 C++ 应用是怎样把这些组件连接起来的"></a>2.3 C++ 应用是怎样把这些组件连接起来的</h3><p>典型 C++ 初始化关系如下：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">io::Context ctx;</span><br><span class="line"><span class="function">io::Poll <span class="title">poll</span><span class="params">(ctx)</span></span>;</span><br><span class="line"><span class="function">ev::Loop <span class="title">loop</span><span class="params">(poll.get_poll())</span></span>;</span><br><span class="line"><span class="keyword">auto</span> exec = loop.<span class="built_in">get_executor</span>();</span><br><span class="line"></span><br><span class="line"><span class="function">io::Timer <span class="title">timer</span><span class="params">(poll, exec, CLOCK_MONOTONIC)</span></span>;</span><br><span class="line"><span class="function">io::CanController <span class="title">ctrl</span><span class="params">(<span class="string">&quot;can0&quot;</span>)</span></span>;</span><br><span class="line"><span class="function">io::CanChannel <span class="title">chan</span><span class="params">(poll, exec)</span></span>;</span><br><span class="line">chan.<span class="built_in">open</span>(ctrl);</span><br><span class="line"></span><br><span class="line"><span class="function">canopen::AsyncMaster <span class="title">master</span><span class="params">(timer, chan, <span class="comment">/* DCF 等参数 */</span>)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="function">io::SignalSet <span class="title">sigset</span><span class="params">(poll, exec)</span></span>;</span><br><span class="line">loop.<span class="built_in">run</span>();</span><br></pre></td></tr></table></figure><p>每个构造参数都对应一条真实依赖：</p><table><thead><tr><th>构造关系</th><th>含义</th></tr></thead><tbody><tr><td><code>Poll(ctx)</code></td><td>Poll 注册到 I&#x2F;O context，统一管理生命周期和 shutdown&#x2F;fork 通知</td></tr><tr><td><code>Loop(poll.get_poll())</code></td><td>Event loop 获得与平台无关的 <code>ev_poll_t</code> 接口，用它阻塞等待 I&#x2F;O</td></tr><tr><td><code>Timer(poll, exec, ...)</code></td><td>Timer 把 <code>timerfd</code> 登记到 Poll；到期后将 wait task 投递到 executor</td></tr><tr><td><code>CanChannel(poll, exec)</code></td><td>CAN channel 把 SocketCAN fd 的可读&#x2F;可写事件交给 Poll</td></tr><tr><td><code>AsyncMaster(timer, chan, ...)</code></td><td>Master 不直接拿 Poll；它通过 Timer 和 CanChannel 获得时间和 CAN I&#x2F;O</td></tr><tr><td><code>SignalSet(poll, exec)</code></td><td>SignalSet 把 self-pipe 读端登记到 Poll，统一处理退出信号</td></tr><tr><td><code>loop.run()</code></td><td>Event loop 开始执行任务，并在任务队列为空时进入 <code>ev_poll_wait()</code></td></tr></tbody></table><p>换句话说，<code>Poll</code> 不是由 <code>AsyncMaster</code> 创建或拥有的。通常由应用在最外层创建，然后把同一个 Poll 传给所有需要异步 fd 事件的 I&#x2F;O 组件。</p><h3 id="2-4-直接用户一：Linux-CAN-通道-can-chan-c"><a href="#2-4-直接用户一：Linux-CAN-通道-can-chan-c" class="headerlink" title="2.4 直接用户一：Linux CAN 通道 can_chan.c"></a>2.4 直接用户一：Linux CAN 通道 <code>can_chan.c</code></h3><h4 id="2-4-1-它持有哪些与-Poll-相关的成员"><a href="#2-4-1-它持有哪些与-Poll-相关的成员" class="headerlink" title="2.4.1 它持有哪些与 Poll 相关的成员"></a>2.4.1 它持有哪些与 Poll 相关的成员</h4><p><code>struct io_can_chan_impl</code> 中的关键成员是：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">io_can_chan_impl</span> &#123;</span></span><br><span class="line">    <span class="comment">/* ... */</span></span><br><span class="line">    <span class="type">io_poll_t</span> *poll;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">io_poll_watch</span> <span class="title">watch</span>;</span></span><br><span class="line">    <span class="comment">/* ... */</span></span><br><span class="line">    <span class="type">int</span> fd;</span><br><span class="line">    <span class="type">int</span> events;</span><br><span class="line">    <span class="comment">/* ... */</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>它们分别表示：</p><table><thead><tr><th>成员</th><th>含义</th></tr></thead><tbody><tr><td><code>poll</code></td><td>当前 CAN channel 使用的 Poll 实例</td></tr><tr><td><code>watch</code></td><td>该 SocketCAN fd 对应的回调和 Poll 内部索引节点</td></tr><tr><td><code>fd</code></td><td>Linux SocketCAN raw socket</td></tr><tr><td><code>events</code></td><td>CAN channel 当前希望继续关注的 Lely I&#x2F;O 事件位</td></tr></tbody></table><p>初始化时，CAN channel 保存 Poll，并给 watch 设置回调：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">impl-&gt;poll = poll;</span><br><span class="line"></span><br><span class="line">impl-&gt;watch = (<span class="keyword">struct</span> io_poll_watch)IO_POLL_WATCH_INIT(</span><br><span class="line">        &amp;io_can_chan_impl_watch_func);</span><br><span class="line"></span><br><span class="line">impl-&gt;fd = <span class="number">-1</span>;</span><br><span class="line">impl-&gt;events = <span class="number">0</span>;</span><br></pre></td></tr></table></figure><p>注意：创建 <code>CanChannel</code> 时并不一定立即登记 SocketCAN fd。只有 channel 打开&#x2F;绑定具体 CAN 控制器，并且异步操作遇到需要等待的条件后，才会根据需要登记 <code>IO_EVENT_IN</code> 或 <code>IO_EVENT_OUT</code>。</p><h4 id="2-4-2-读路径：只有非阻塞读取返回-EAGAIN-时才监听-IO-EVENT-IN"><a href="#2-4-2-读路径：只有非阻塞读取返回-EAGAIN-时才监听-IO-EVENT-IN" class="headerlink" title="2.4.2 读路径：只有非阻塞读取返回 EAGAIN 时才监听 IO_EVENT_IN"></a>2.4.2 读路径：只有非阻塞读取返回 EAGAIN 时才监听 <code>IO_EVENT_IN</code></h4><p>CAN channel 的接收任务会先直接尝试从 SocketCAN fd 读取帧。只要 socket 中仍有数据，就继续读取并填充用户态接收队列。</p><p>当 <code>read()</code> 返回：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">EAGAIN 或 EWOULDBLOCK</span><br></pre></td></tr></table></figure><p>表示当前没有更多 CAN 帧可读。如果仍存在待完成的读请求，代码才执行：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> events = impl-&gt;events | IO_EVENT_IN;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (!io_poll_watch(impl-&gt;poll, impl-&gt;fd, events, &amp;impl-&gt;watch)) &#123;</span><br><span class="line">    impl-&gt;events = events;</span><br><span class="line">    post_rxbuf = <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里的动作是：</p><ol><li>保留此前可能已经关注的事件位；</li><li>增加 <code>IO_EVENT_IN</code>；</li><li>把 SocketCAN fd 登记或重新登记到 Poll；</li><li>不再反复投递接收任务，等待内核通知“现在又可以读了”。</li></ol><p>所以 Poll 在读路径中的价值不是“替代 <code>read()</code>”，而是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">直接 read() 已经无法继续推进</span><br><span class="line">        ↓</span><br><span class="line">用 Poll 睡眠等待 SocketCAN 重新可读</span><br><span class="line">        ↓</span><br><span class="line">避免 CPU 忙轮询</span><br></pre></td></tr></table></figure><h4 id="2-4-3-写路径：非阻塞写返回-EAGAIN-时监听-IO-EVENT-OUT"><a href="#2-4-3-写路径：非阻塞写返回-EAGAIN-时监听-IO-EVENT-OUT" class="headerlink" title="2.4.3 写路径：非阻塞写返回 EAGAIN 时监听 IO_EVENT_OUT"></a>2.4.3 写路径：非阻塞写返回 EAGAIN 时监听 <code>IO_EVENT_OUT</code></h4><p>发送任务同样会先直接尝试写 SocketCAN fd：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> result = io_can_fd_write_msg(fd, write-&gt;msg,</span><br><span class="line">        impl-&gt;poll ? <span class="number">0</span> : LELY_IO_TX_TIMEOUT);</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> errc = !result ? <span class="number">0</span> : errno;</span><br><span class="line"><span class="type">int</span> wouldblock = errc == EAGAIN || errc == EWOULDBLOCK;</span><br></pre></td></tr></table></figure><p>使用 Poll 时，写操作采用非阻塞模式。若发送缓冲区暂时无法接收新帧，待发送操作被重新放回队列，然后登记 <code>IO_EVENT_OUT</code>：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> events = impl-&gt;events | IO_EVENT_OUT;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (!io_poll_watch(impl-&gt;poll, impl-&gt;fd, events, &amp;impl-&gt;watch)) &#123;</span><br><span class="line">    impl-&gt;events = events;</span><br><span class="line">    post_write = <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>等 Linux 报告 <code>EPOLLOUT</code> 后，Poll 转换为 <code>IO_EVENT_OUT</code>，再触发 CAN channel 的 watch 回调，由回调投递写任务重新尝试发送。</p><p>这里也不能把 <code>IO_EVENT_OUT</code> 理解成“这一帧必定已经发到 CAN 总线”。它只表示 socket 当前允许继续推进写入。若启用了 <code>txwait</code>，CAN channel 还会等待 SocketCAN 的发送确认消息。</p><h4 id="2-4-4-同一个-SocketCAN-fd-可以同时监听-IN-和-OUT"><a href="#2-4-4-同一个-SocketCAN-fd-可以同时监听-IN-和-OUT" class="headerlink" title="2.4.4 同一个 SocketCAN fd 可以同时监听 IN 和 OUT"></a>2.4.4 同一个 SocketCAN fd 可以同时监听 IN 和 OUT</h4><p><code>impl-&gt;events</code> 是位掩码，因此可能出现：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">IO_EVENT_IN | IO_EVENT_OUT</span><br></pre></td></tr></table></figure><p>典型场景是：</p><ul><li>接收队列正等待新 CAN 帧；</li><li>发送队列又因为 socket 暂时不可写而等待重试。</li></ul><p>此时同一个 fd 只需要一个 <code>io_poll_watch</code>，但关注两个方向的事件。</p><h4 id="2-4-5-CAN-channel-的-Poll-回调做什么"><a href="#2-4-5-CAN-channel-的-Poll-回调做什么" class="headerlink" title="2.4.5 CAN channel 的 Poll 回调做什么"></a>2.4.5 CAN channel 的 Poll 回调做什么</h4><p>回调入口：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">static</span> <span class="type">void</span></span><br><span class="line"><span class="title function_">io_can_chan_impl_watch_func</span><span class="params">(<span class="keyword">struct</span> io_poll_watch *watch, <span class="type">int</span> events)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">io_can_chan_impl</span> *<span class="title">impl</span> =</span></span><br><span class="line">            structof(watch, <span class="keyword">struct</span> io_can_chan_impl, watch);</span><br><span class="line">    <span class="comment">/* ... */</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>它首先处理 <code>EPOLLONESHOT</code> 带来的“本次通知已消费”问题：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> ((events &amp; IO_EVENT_ERR) || impl-&gt;fd == <span class="number">-1</span> || impl-&gt;shutdown) &#123;</span><br><span class="line">    impl-&gt;events = <span class="number">0</span>;</span><br><span class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span> ((impl-&gt;events &amp;= ~events) != <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="keyword">if</span> (io_poll_watch(impl-&gt;poll, impl-&gt;fd, impl-&gt;events,</span><br><span class="line">            &amp;impl-&gt;watch) == <span class="number">-1</span>) &#123;</span><br><span class="line">        impl-&gt;events = <span class="number">0</span>;</span><br><span class="line">        events |= IO_EVENT_ERR;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这段代码的含义是：</p><ol><li>如果发生错误、fd 已关闭或 channel 正在 shutdown，不再恢复监听；</li><li>否则，从 <code>impl-&gt;events</code> 中清除本次已经发生的事件位；</li><li>如果仍剩其他关注事件，立即重新调用 <code>io_poll_watch()</code>；</li><li>这样可以继续等待“尚未发生的另一个方向事件”。</li></ol><p>举例：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">原来关注：IN | OUT</span><br><span class="line">本次只返回：IN</span><br><span class="line">清除后剩余：OUT</span><br><span class="line">动作：立即重新登记 OUT</span><br></pre></td></tr></table></figure><p>随后回调不直接在 Poll 上下文中完成全部收发，而是投递任务：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> ((events &amp; (IO_EVENT_IN | IO_EVENT_ERR)) &amp;&amp; !impl-&gt;shutdown) &#123;</span><br><span class="line">    post_rxbuf = !impl-&gt;rxbuf_posted;</span><br><span class="line">    impl-&gt;rxbuf_posted = <span class="number">1</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> ((events &amp; (IO_EVENT_OUT | IO_EVENT_ERR))</span><br><span class="line">        &amp;&amp; !sllist_empty(&amp;impl-&gt;write_queue)</span><br><span class="line">        &amp;&amp; !impl-&gt;shutdown) &#123;</span><br><span class="line">    post_write = !impl-&gt;write_posted;</span><br><span class="line">    impl-&gt;write_posted = <span class="number">1</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (post_rxbuf)</span><br><span class="line">    ev_exec_post(impl-&gt;rxbuf_task.exec, &amp;impl-&gt;rxbuf_task);</span><br><span class="line"><span class="keyword">if</span> (post_write)</span><br><span class="line">    ev_exec_post(impl-&gt;write_task.exec, &amp;impl-&gt;write_task);</span><br></pre></td></tr></table></figure><p>因此它的职责可以概括为：</p><table><thead><tr><th>Poll 事件</th><th>CAN channel 回调动作</th><th>后续真正处理者</th></tr></thead><tbody><tr><td><code>IO_EVENT_IN</code></td><td>投递 <code>rxbuf_task</code></td><td>接收任务读取 SocketCAN、填充接收队列、完成异步读请求</td></tr><tr><td><code>IO_EVENT_OUT</code></td><td>投递 <code>write_task</code></td><td>写任务重试发送队列中的 CAN 帧</td></tr><tr><td><code>IO_EVENT_ERR</code></td><td>同时唤醒读写处理路径</td><td>读写任务读取错误状态并完成&#x2F;取消相关请求</td></tr></tbody></table><h4 id="2-4-6-CAN-接收的完整链路"><a href="#2-4-6-CAN-接收的完整链路" class="headerlink" title="2.4.6 CAN 接收的完整链路"></a>2.4.6 CAN 接收的完整链路</h4><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant K as Linux SocketCAN</span><br><span class="line">    participant P as io::Poll</span><br><span class="line">    participant C as CanChannel watch callback</span><br><span class="line">    participant E as Executor / ev::Loop</span><br><span class="line">    participant R as rxbuf_task/read_task</span><br><span class="line">    participant N as io::CanNet</span><br><span class="line">    participant CN as can_net_t / CANopen objects</span><br><span class="line"></span><br><span class="line">    R-&gt;&gt;K: 非阻塞 read()</span><br><span class="line">    K--&gt;&gt;R: EAGAIN</span><br><span class="line">    R-&gt;&gt;P: io_poll_watch(fd, IO_EVENT_IN)</span><br><span class="line">    P-&gt;&gt;K: epoll 监听 EPOLLIN|EPOLLONESHOT</span><br><span class="line">    K--&gt;&gt;P: fd 可读</span><br><span class="line">    P-&gt;&gt;C: watch-&gt;&gt;func(IO_EVENT_IN)</span><br><span class="line">    C-&gt;&gt;E: ev_exec_post(rxbuf_task)</span><br><span class="line">    E-&gt;&gt;R: 执行接收任务</span><br><span class="line">    R-&gt;&gt;K: 读取并排空当前 CAN 帧</span><br><span class="line">    R-&gt;&gt;N: 完成 io_can_chan_read</span><br><span class="line">    N-&gt;&gt;CN: can_net_recv(msg)</span><br><span class="line">    R-&gt;&gt;P: 若再次 EAGAIN 且仍需接收，重新监听 IN</span><br></pre></td></tr></table></figure><h4 id="2-4-7-CAN-发送的完整链路"><a href="#2-4-7-CAN-发送的完整链路" class="headerlink" title="2.4.7 CAN 发送的完整链路"></a>2.4.7 CAN 发送的完整链路</h4><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant CN as CANopen protocol</span><br><span class="line">    participant N as io::CanNet</span><br><span class="line">    participant W as CanChannel write_task</span><br><span class="line">    participant K as Linux SocketCAN</span><br><span class="line">    participant P as io::Poll</span><br><span class="line">    participant C as CanChannel watch callback</span><br><span class="line">    participant E as Executor / ev::Loop</span><br><span class="line"></span><br><span class="line">    CN-&gt;&gt;N: send callback(CAN frame)</span><br><span class="line">    N-&gt;&gt;W: io_can_chan_submit_write()</span><br><span class="line">    W-&gt;&gt;K: 非阻塞 write()</span><br><span class="line">    K--&gt;&gt;W: EAGAIN</span><br><span class="line">    W-&gt;&gt;P: io_poll_watch(fd, IO_EVENT_OUT)</span><br><span class="line">    K--&gt;&gt;P: fd 可写</span><br><span class="line">    P-&gt;&gt;C: watch-&gt;&gt;func(IO_EVENT_OUT)</span><br><span class="line">    C-&gt;&gt;E: ev_exec_post(write_task)</span><br><span class="line">    E-&gt;&gt;W: 重试发送</span><br><span class="line">    W-&gt;&gt;K: write(CAN frame)</span><br><span class="line">    K--&gt;&gt;W: 成功或发送确认</span><br><span class="line">    W-&gt;&gt;N: 完成 write operation</span><br></pre></td></tr></table></figure><h4 id="2-4-8-关闭时做什么"><a href="#2-4-8-关闭时做什么" class="headerlink" title="2.4.8 关闭时做什么"></a>2.4.8 关闭时做什么</h4><p>CAN channel shutdown 或关闭 socket 时会执行：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">impl-&gt;events = <span class="number">0</span>;</span><br><span class="line">io_poll_watch(impl-&gt;poll, impl-&gt;fd, <span class="number">0</span>, &amp;impl-&gt;watch);</span><br><span class="line">close(impl-&gt;fd);</span><br></pre></td></tr></table></figure><p>顺序很重要：先从 Poll 删除 fd，再关闭 fd，避免后续事件仍通过旧的 watch 进入回调。</p><h3 id="2-5-直接用户二：Linux-系统定时器-timer-c"><a href="#2-5-直接用户二：Linux-系统定时器-timer-c" class="headerlink" title="2.5 直接用户二：Linux 系统定时器 timer.c"></a>2.5 直接用户二：Linux 系统定时器 <code>timer.c</code></h3><h4 id="2-5-1-Timer-把-Linux-timerfd-当作普通可读-fd"><a href="#2-5-1-Timer-把-Linux-timerfd-当作普通可读-fd" class="headerlink" title="2.5.1 Timer 把 Linux timerfd 当作普通可读 fd"></a>2.5.1 Timer 把 Linux <code>timerfd</code> 当作普通可读 fd</h4><p>Linux timer 实现持有：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">io_timer_impl</span> &#123;</span></span><br><span class="line">    <span class="comment">/* ... */</span></span><br><span class="line">    <span class="type">io_poll_t</span> *poll;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">io_poll_watch</span> <span class="title">watch</span>;</span></span><br><span class="line">    <span class="type">int</span> tfd;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">ev_task</span> <span class="title">wait_task</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sllist</span> <span class="title">wait_queue</span>;</span></span><br><span class="line">    <span class="type">int</span> overrun;</span><br><span class="line">    <span class="comment">/* ... */</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>打开定时器时：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">impl-&gt;tfd = timerfd_create(</span><br><span class="line">        impl-&gt;clockid,</span><br><span class="line">        TFD_NONBLOCK | TFD_CLOEXEC);</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (io_poll_watch(impl-&gt;poll, impl-&gt;tfd,</span><br><span class="line">        IO_EVENT_IN, &amp;impl-&gt;watch) == <span class="number">-1</span>) &#123;</span><br><span class="line">    <span class="comment">/* error handling */</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>timerfd</code> 到期后会变成“可读”。因此 Timer 不需要特殊的 Poll 类型，只需监听 <code>IO_EVENT_IN</code>。</p><h4 id="2-5-2-Poll-回调只负责投递-wait-task"><a href="#2-5-2-Poll-回调只负责投递-wait-task" class="headerlink" title="2.5.2 Poll 回调只负责投递 wait_task"></a>2.5.2 Poll 回调只负责投递 <code>wait_task</code></h4><p>Timer 的 watch 回调很短：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">static</span> <span class="type">void</span></span><br><span class="line"><span class="title function_">io_timer_impl_watch_func</span><span class="params">(<span class="keyword">struct</span> io_poll_watch *watch, <span class="type">int</span> events)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">io_timer_impl</span> *<span class="title">impl</span> =</span></span><br><span class="line">            structof(watch, <span class="keyword">struct</span> io_timer_impl, watch);</span><br><span class="line">    (<span class="type">void</span>)events;</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> post_wait = !impl-&gt;wait_posted &amp;&amp; !impl-&gt;shutdown;</span><br><span class="line">    <span class="keyword">if</span> (post_wait)</span><br><span class="line">        impl-&gt;wait_posted = <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (post_wait)</span><br><span class="line">        ev_exec_post(impl-&gt;wait_task.exec, &amp;impl-&gt;wait_task);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>它没有在 Poll 回调中直接读取 <code>timerfd</code>，而是把任务交给 executor。</p><h4 id="2-5-3-wait-task-读取计数并恢复下一次监听"><a href="#2-5-3-wait-task-读取计数并恢复下一次监听" class="headerlink" title="2.5.3 wait_task 读取计数并恢复下一次监听"></a>2.5.3 <code>wait_task</code> 读取计数并恢复下一次监听</h4><p><code>timerfd</code> 的一次 <code>read()</code> 返回的是累计到期次数，而不是普通字节流。任务会循环读取并累加 <code>overrun</code>：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">uintmax_t</span> value = <span class="number">0</span>;</span><br><span class="line">result = read(impl-&gt;tfd, &amp;value, <span class="keyword">sizeof</span>(value));</span><br></pre></td></tr></table></figure><p>当数据被排空后，非阻塞 <code>read()</code> 返回 <code>EAGAIN</code>，代码设置：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">events |= IO_EVENT_IN;</span><br></pre></td></tr></table></figure><p>随后：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (events &amp;&amp; !impl-&gt;shutdown)</span><br><span class="line">    io_poll_watch(impl-&gt;poll, impl-&gt;tfd, events, &amp;impl-&gt;watch);</span><br></pre></td></tr></table></figure><p>这一步恢复下一次 <code>EPOLLONESHOT</code> 监听。</p><p>完整周期是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">timerfd_settime() 设置到期时间</span><br><span class="line">        ↓</span><br><span class="line">timerfd 到期，可读</span><br><span class="line">        ↓</span><br><span class="line">Poll 返回 IO_EVENT_IN</span><br><span class="line">        ↓</span><br><span class="line">Timer watch callback 投递 wait_task</span><br><span class="line">        ↓</span><br><span class="line">wait_task 读取到期计数</span><br><span class="line">        ↓</span><br><span class="line">完成 wait_queue 中的异步等待操作</span><br><span class="line">        ↓</span><br><span class="line">重新 io_poll_watch(IO_EVENT_IN)</span><br></pre></td></tr></table></figure><h4 id="2-5-4-Timer-与-CANopen-定时逻辑的关系"><a href="#2-5-4-Timer-与-CANopen-定时逻辑的关系" class="headerlink" title="2.5.4 Timer 与 CANopen 定时逻辑的关系"></a>2.5.4 Timer 与 CANopen 定时逻辑的关系</h4><p><code>io::Timer</code> 本身不知道 SDO、NMT、Heartbeat 或 PDO。它只提供通用异步时间等待。</p><p>真正把它接到 CANopen 时间系统的是 <code>io::CanNet</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">io::CanNet</span><br><span class="line">  ├─ 持有 io_timer_t *timer</span><br><span class="line">  ├─ 基于 timer 创建 io_tqueue</span><br><span class="line">  ├─ 等待 can_net_t 给出的“下一次到期时间”</span><br><span class="line">  └─ 到期时调用 can_net_set_time()</span><br></pre></td></tr></table></figure><p>因此应区分两层：</p><table><thead><tr><th>层</th><th>认识的概念</th></tr></thead><tbody><tr><td><code>src/io2/linux/timer.c</code></td><td><code>timerfd</code>、到期次数、异步 wait queue</td></tr><tr><td><code>io_can_net</code> &#x2F; <code>can_net_t</code></td><td>CAN 网络当前时间、下一个协议定时器、超时回调</td></tr></tbody></table><p>Poll 只负责让第一层在到期时被唤醒；CANopen 协议栈根据自己的 timer heap 决定具体执行哪个协议定时动作。</p><h3 id="2-6-直接用户三：POSIX-信号集合-sigset-c"><a href="#2-6-直接用户三：POSIX-信号集合-sigset-c" class="headerlink" title="2.6 直接用户三：POSIX 信号集合 sigset.c"></a>2.6 直接用户三：POSIX 信号集合 <code>sigset.c</code></h3><h4 id="2-6-1-Poll-并不是直接监听-POSIX-signal"><a href="#2-6-1-Poll-并不是直接监听-POSIX-signal" class="headerlink" title="2.6.1 Poll 并不是直接监听 POSIX signal"></a>2.6.1 Poll 并不是直接监听 POSIX signal</h4><p>普通 signal 不是一个 fd，不能直接加入 epoll。Lely 使用 self-pipe：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">signal handler</span><br><span class="line">    ↓ 写 1 字节</span><br><span class="line">非阻塞 pipe 写端</span><br><span class="line">    ↓</span><br><span class="line">非阻塞 pipe 读端变为可读</span><br><span class="line">    ↓</span><br><span class="line">Poll 返回 IO_EVENT_IN</span><br></pre></td></tr></table></figure><p>关键成员：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">io_sigset_impl</span> &#123;</span></span><br><span class="line">    <span class="comment">/* ... */</span></span><br><span class="line">    <span class="type">io_poll_t</span> *poll;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">io_poll_watch</span> <span class="title">watch</span>;</span></span><br><span class="line">    <span class="type">int</span> fds[<span class="number">2</span>];</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">ev_task</span> <span class="title">read_task</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">ev_task</span> <span class="title">wait_task</span>;</span></span><br><span class="line">    <span class="comment">/* ... */</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><h4 id="2-6-2-创建-self-pipe-并监听读端"><a href="#2-6-2-创建-self-pipe-并监听读端" class="headerlink" title="2.6.2 创建 self-pipe 并监听读端"></a>2.6.2 创建 self-pipe 并监听读端</h4><p>Linux 下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">pipe2(impl-&gt;fds, O_CLOEXEC | O_NONBLOCK);</span><br><span class="line"></span><br><span class="line">io_poll_watch(impl-&gt;poll, impl-&gt;fds[<span class="number">0</span>],</span><br><span class="line">        IO_EVENT_IN, &amp;impl-&gt;watch);</span><br></pre></td></tr></table></figure><p>其中：</p><table><thead><tr><th>fd</th><th>用途</th></tr></thead><tbody><tr><td><code>fds[0]</code></td><td>pipe 读端，交给 Poll 监听</td></tr><tr><td><code>fds[1]</code></td><td>pipe 写端，由 signal handler 写入</td></tr></tbody></table><h4 id="2-6-3-signal-handler-只做最小工作"><a href="#2-6-3-signal-handler-只做最小工作" class="headerlink" title="2.6.3 signal handler 只做最小工作"></a>2.6.3 signal handler 只做最小工作</h4><p>信号处理函数记录 pending 状态并向 pipe 写一个字节：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">static</span> <span class="type">void</span></span><br><span class="line"><span class="title function_">io_sigset_handler</span><span class="params">(<span class="type">int</span> signo)</span></span><br><span class="line">&#123;</span><br><span class="line">    io_sigset_shared.sig[signo - <span class="number">1</span>].pending = <span class="number">1</span>;</span><br><span class="line">    io_sigset_shared.pending = <span class="number">1</span>;</span><br><span class="line">    io_sigset_kill(signo);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>io_sigset_kill()</code> 的核心动作是：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">write(pipe_write_fd, <span class="string">&quot;&quot;</span>, <span class="number">1</span>);</span><br></pre></td></tr></table></figure><p>这样做的原因是 signal handler 上下文限制很多，不能安全执行复杂的 C&#x2F;C++ 逻辑、锁操作或用户回调。写 self-pipe 是一种把异步 signal 转换到普通事件循环上下文的方式。</p><h4 id="2-6-4-Poll-回调和后续任务"><a href="#2-6-4-Poll-回调和后续任务" class="headerlink" title="2.6.4 Poll 回调和后续任务"></a>2.6.4 Poll 回调和后续任务</h4><p>pipe 可读时，watch 回调只投递读取任务：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> post_read = !impl-&gt;read_posted;</span><br><span class="line">impl-&gt;read_posted = <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (post_read)</span><br><span class="line">    ev_exec_post(impl-&gt;read_task.exec, &amp;impl-&gt;read_task);</span><br></pre></td></tr></table></figure><p><code>read_task</code> 会：</p><ol><li>循环读空 pipe；</li><li>调用 <code>io_sigset_process_all()</code> 处理 pending signal；</li><li>把对应 signal 交给等待队列；</li><li>当读到 <code>EAGAIN</code> 后重新登记 <code>IO_EVENT_IN</code>。</li></ol><p>恢复监听代码：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (events &amp;&amp; !impl-&gt;shutdown)</span><br><span class="line">    io_poll_watch(impl-&gt;poll, impl-&gt;fds[<span class="number">0</span>],</span><br><span class="line">            events, &amp;impl-&gt;watch);</span><br></pre></td></tr></table></figure><p>完整链路：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant OS as POSIX signal</span><br><span class="line">    participant H as signal handler</span><br><span class="line">    participant W as self-pipe write fd</span><br><span class="line">    participant R as self-pipe read fd</span><br><span class="line">    participant P as io::Poll</span><br><span class="line">    participant E as Executor / ev::Loop</span><br><span class="line">    participant S as SignalSet tasks</span><br><span class="line">    participant A as Application shutdown logic</span><br><span class="line"></span><br><span class="line">    OS-&gt;&gt;H: SIGINT / SIGTERM</span><br><span class="line">    H-&gt;&gt;W: write 1 byte + set pending flag</span><br><span class="line">    W--&gt;&gt;R: pipe becomes readable</span><br><span class="line">    R--&gt;&gt;P: EPOLLIN</span><br><span class="line">    P-&gt;&gt;S: watch callback(IO_EVENT_IN)</span><br><span class="line">    S-&gt;&gt;E: post read_task</span><br><span class="line">    E-&gt;&gt;S: drain pipe and process pending signals</span><br><span class="line">    S-&gt;&gt;A: complete async signal wait</span><br><span class="line">    S-&gt;&gt;P: re-register IO_EVENT_IN</span><br></pre></td></tr></table></figure><h4 id="2-6-5-在-CANopen-程序中的作用"><a href="#2-6-5-在-CANopen-程序中的作用" class="headerlink" title="2.6.5 在 CANopen 程序中的作用"></a>2.6.5 在 CANopen 程序中的作用</h4><p>SignalSet 通常用于：</p><ul><li>捕获 <code>SIGINT</code>、<code>SIGTERM</code>；</li><li>请求 <code>Context</code> shutdown；</li><li>让 CAN channel、timer、CAN network 等注册在 context 中的服务依次停止；</li><li>最终让 event loop 退出。</li></ul><p>它不是 CANopen 协议通信对象，而是应用生命周期和进程退出控制组件。</p><h3 id="2-7-ev-Loop：它不登记设备-fd，但负责真正驱动-Poll"><a href="#2-7-ev-Loop：它不登记设备-fd，但负责真正驱动-Poll" class="headerlink" title="2.7 ev::Loop：它不登记设备 fd，但负责真正驱动 Poll"></a>2.7 <code>ev::Loop</code>：它不登记设备 fd，但负责真正驱动 Poll</h3><p><code>Poll</code> 只实现了 <code>ev_poll_t</code> 接口：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">self()</span><br><span class="line">wait(timeout)</span><br><span class="line">kill(thread_token)</span><br></pre></td></tr></table></figure><p><code>ev_loop</code> 保存这个接口：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">ev_loop</span> &#123;</span></span><br><span class="line">    <span class="type">ev_poll_t</span> *poll;</span><br><span class="line">    <span class="comment">/* task queue, executor, contexts... */</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>当任务队列中存在真实任务时，event loop 优先执行任务：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ev_exec_run(task-&gt;exec, task);</span><br></pre></td></tr></table></figure><p>当没有任务可执行、并且允许当前线程进入 Poll 时，才调用：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> result = ev_poll_wait(</span><br><span class="line">        loop-&gt;poll,</span><br><span class="line">        empty ? <span class="number">-1</span> : <span class="number">0</span>);</span><br></pre></td></tr></table></figure><p>含义是：</p><table><thead><tr><th>event loop 状态</th><th>Poll timeout</th></tr></thead><tbody><tr><td>任务队列为空</td><td><code>-1</code>，允许阻塞等待 I&#x2F;O</td></tr><tr><td>已经还有任务</td><td><code>0</code>，只做一次非阻塞 I&#x2F;O 检查</td></tr></tbody></table><p>当其他线程向 event loop 投递任务，需要把正在 <code>epoll_pwait()</code> 中睡眠的线程叫醒时，event loop 调用：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">return</span> ctx-&gt;polling</span><br><span class="line">        ? ev_poll_kill(ctx-&gt;loop-&gt;poll, ctx-&gt;thr)</span><br><span class="line">        : <span class="number">0</span>;</span><br></pre></td></tr></table></figure><p>所以二者分工为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">Poll：知道 Linux epoll 和 fd 事件；</span><br><span class="line">Loop：知道任务队列、executor、何时阻塞、何时唤醒；</span><br><span class="line">设备组件：知道 fd 就绪后应做什么；</span><br><span class="line">CANopen 上层：知道收到 CAN 帧或定时器到期后应执行哪个协议动作。</span><br></pre></td></tr></table></figure><h3 id="2-8-io-CanNet：Poll-驱动-I-O-与被动-CANopen-核心之间的桥梁"><a href="#2-8-io-CanNet：Poll-驱动-I-O-与被动-CANopen-核心之间的桥梁" class="headerlink" title="2.8 io::CanNet：Poll 驱动 I&#x2F;O 与被动 CANopen 核心之间的桥梁"></a>2.8 <code>io::CanNet</code>：Poll 驱动 I&#x2F;O 与被动 CANopen 核心之间的桥梁</h3><p><code>io_can_net</code> 不包含 <code>io_poll_t *</code>，也不调用 <code>io_poll_watch()</code>。它持有的是：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">io_timer_t</span> *timer;</span><br><span class="line"><span class="type">io_tqueue_t</span> *tq;</span><br><span class="line"><span class="type">io_can_chan_t</span> *chan;</span><br><span class="line"><span class="type">can_net_t</span> *net;</span><br></pre></td></tr></table></figure><p>它把三个不同世界连接起来：</p><table><thead><tr><th>下层</th><th>中间桥梁</th><th>上层</th></tr></thead><tbody><tr><td>Poll 驱动的 <code>CanChannel</code></td><td><code>io_can_net_read_func()</code> &#x2F; <code>write_func()</code></td><td>被动 <code>can_net_t</code></td></tr><tr><td>Poll 驱动的 <code>Timer</code></td><td><code>io_tqueue</code> &#x2F; <code>wait_next_func()</code></td><td><code>can_net_set_time()</code> 和 timer heap</td></tr><tr><td>CANopen 发送请求</td><td><code>io_can_net_send_func()</code></td><td><code>CanChannel</code> 异步写操作</td></tr></tbody></table><h4 id="2-8-1-接收链路"><a href="#2-8-1-接收链路" class="headerlink" title="2.8.1 接收链路"></a>2.8.1 接收链路</h4><p><code>io_can_net_read_func()</code> 在异步 CAN read 完成后执行：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (read-&gt;r.result == <span class="number">1</span>) &#123;</span><br><span class="line">    io_can_net_set_time(net);</span><br><span class="line">    can_net_recv(net-&gt;net, &amp;net-&gt;read_msg);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>随后它再次提交读请求：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">io_can_chan_submit_read(net-&gt;chan, &amp;net-&gt;read);</span><br></pre></td></tr></table></figure><p>完整链路为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">SocketCAN fd</span><br><span class="line">  → Poll</span><br><span class="line">  → CanChannel rx/read task</span><br><span class="line">  → io_can_net_read_func()</span><br><span class="line">  → can_net_recv()</span><br><span class="line">  → CANopen NMT/SDO/PDO/SYNC/EMCY 等接收处理器</span><br></pre></td></tr></table></figure><p>最后一层具体进入哪些通信对象，取决于 <code>can_net_t</code> 中注册的接收者和当前编译配置。</p><h4 id="2-8-2-定时链路"><a href="#2-8-2-定时链路" class="headerlink" title="2.8.2 定时链路"></a>2.8.2 定时链路</h4><p>初始化时，<code>io_can_net</code> 注册：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">can_net_set_next_func(net-&gt;net,</span><br><span class="line">        &amp;io_can_net_next_func, net);</span><br></pre></td></tr></table></figure><p>当 CAN 网络核心的“最近到期时间”发生变化时，<code>io_can_net_next_func()</code> 把该绝对时间提交到 <code>io_tqueue</code>。</p><p>Timer 到期后，<code>io_can_net_wait_next_func()</code> 执行：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">io_can_net_set_time(net);</span><br></pre></td></tr></table></figure><p>而 <code>io_can_net_set_time()</code> 最终调用：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">can_net_set_time(io_can_net_get_net(net), &amp;now);</span><br></pre></td></tr></table></figure><p>这会让被动 CAN 网络核心检查 timer heap，并执行所有已经到期的协议定时器。</p><p>完整链路：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">CANopen/CAN timer heap 更新最近到期时间</span><br><span class="line">  → io_can_net_next_func()</span><br><span class="line">  → io_tqueue_submit_wait()</span><br><span class="line">  → Timer 设置 timerfd</span><br><span class="line">  → Poll 等待 timerfd 可读</span><br><span class="line">  → Timer wait_task</span><br><span class="line">  → io_can_net_wait_next_func()</span><br><span class="line">  → can_net_set_time()</span><br><span class="line">  → 执行到期协议回调</span><br></pre></td></tr></table></figure><h4 id="2-8-3-发送链路"><a href="#2-8-3-发送链路" class="headerlink" title="2.8.3 发送链路"></a>2.8.3 发送链路</h4><p>CAN&#x2F;CANopen 核心需要发送帧时调用已注册的 send callback：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">can_net_set_send_func(net-&gt;net,</span><br><span class="line">        &amp;io_can_net_send_func, net);</span><br></pre></td></tr></table></figure><p><code>io_can_net_send_func()</code> 把 CAN 帧放入用户态发送环形队列，随后 <code>io_can_net_do_write()</code> 调用：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">io_can_chan_submit_write(net-&gt;chan, &amp;net-&gt;write);</span><br></pre></td></tr></table></figure><p>若 SocketCAN 暂时不可写，则回到前面所述的 CanChannel <code>IO_EVENT_OUT</code> + Poll 重试机制。</p><p>完整链路：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">CANopen 协议对象请求发送</span><br><span class="line">  → can_net_t send callback</span><br><span class="line">  → io_can_net_send_func()</span><br><span class="line">  → io_can_net 发送队列</span><br><span class="line">  → io_can_chan_submit_write()</span><br><span class="line">  → SocketCAN write()</span><br><span class="line">  → 若 EAGAIN：Poll 等待 IO_EVENT_OUT</span><br><span class="line">  → write_task 重试</span><br></pre></td></tr></table></figure><h3 id="2-9-Node、Master、Slave-和-CANopen-Driver-到底怎样依赖-Poll"><a href="#2-9-Node、Master、Slave-和-CANopen-Driver-到底怎样依赖-Poll" class="headerlink" title="2.9 Node、Master、Slave 和 CANopen Driver 到底怎样依赖 Poll"></a>2.9 <code>Node</code>、<code>Master</code>、<code>Slave</code> 和 CANopen Driver 到底怎样依赖 Poll</h3><h4 id="2-9-1-canopen-Node"><a href="#2-9-1-canopen-Node" class="headerlink" title="2.9.1 canopen::Node"></a>2.9.1 <code>canopen::Node</code></h4><p><code>Node</code> 构造函数首先构造 <code>io::CanNet</code>：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">Node::<span class="built_in">Node</span>(<span class="type">ev_exec_t</span>* exec,</span><br><span class="line">           io::TimerBase&amp; timer,</span><br><span class="line">           io::CanChannelBase&amp; chan,</span><br><span class="line">           __co_dev* dev,</span><br><span class="line">           <span class="type">uint8_t</span> id)</span><br><span class="line">    : io::<span class="built_in">CanNet</span>(exec, timer, chan, <span class="number">0</span>, <span class="number">0</span>),</span><br><span class="line">      <span class="built_in">Device</span>(dev, id, <span class="keyword">this</span>),</span><br><span class="line">      <span class="built_in">impl_</span>(<span class="keyword">new</span> <span class="built_in">Impl_</span>(<span class="keyword">this</span>, <span class="built_in">net</span>(), Device::<span class="built_in">dev</span>())) &#123;</span><br><span class="line">    <span class="built_in">start</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>因此 Node 的 I&#x2F;O 入口已经由 <code>CanNet</code> 提供。<code>start()</code> 后，CanNet 持续提交异步 CAN read；收到帧后进入 <code>can_net_recv()</code>，再由 Node 内部 NMT、PDO、SYNC、TIME、EMCY 等对象接收。</p><p>Node 本身不需要知道 SocketCAN fd，也不调用 <code>io_poll_watch()</code>。</p><h4 id="2-9-2-BasicMaster-AsyncMaster"><a href="#2-9-2-BasicMaster-AsyncMaster" class="headerlink" title="2.9.2 BasicMaster &#x2F; AsyncMaster"></a>2.9.2 <code>BasicMaster</code> &#x2F; <code>AsyncMaster</code></h4><p>Master 构造函数继续把 timer 和 channel 交给 Node：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">BasicMaster::<span class="built_in">BasicMaster</span>(<span class="type">ev_exec_t</span>* exec,</span><br><span class="line">        io::TimerBase&amp; timer,</span><br><span class="line">        io::CanChannelBase&amp; chan,</span><br><span class="line">        __co_dev* dev,</span><br><span class="line">        <span class="type">uint8_t</span> id)</span><br><span class="line">    : <span class="built_in">Node</span>(exec, timer, chan, dev, id) &#123;</span><br><span class="line">    <span class="comment">/* master-specific initialization */</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>因此 Master 对 Poll 的实际依赖链是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">BasicMaster / AsyncMaster</span><br><span class="line">  → Node</span><br><span class="line">  → CanNet</span><br><span class="line">  ├─ CanChannel → Poll → SocketCAN fd</span><br><span class="line">  └─ Timer      → Poll → timerfd</span><br></pre></td></tr></table></figure><p>SDO 请求、NMT boot、heartbeat 监控等上层行为并不是直接向 Poll 登记事件，而是创建 CAN 操作或协议 timer，最终通过上述链路落到底层 I&#x2F;O。</p><h4 id="2-9-3-BasicSlave"><a href="#2-9-3-BasicSlave" class="headerlink" title="2.9.3 BasicSlave"></a>2.9.3 <code>BasicSlave</code></h4><p>Slave 与 Master 一样建立在 Node 和 CanNet 上。它接收 SDO、NMT、RPDO 等帧时，最底层仍由同一个 <code>CanChannel → Poll</code> 接收链路驱动；协议定时行为仍由 <code>Timer → Poll</code> 驱动。</p><h4 id="2-9-4-CANopen-“Driver”-不是-Linux-fd-驱动"><a href="#2-9-4-CANopen-“Driver”-不是-Linux-fd-驱动" class="headerlink" title="2.9.4 CANopen “Driver” 不是 Linux fd 驱动"></a>2.9.4 CANopen “Driver” 不是 Linux fd 驱动</h4><p>Lely <code>coapp</code> 中的：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">DriverBase</span><br><span class="line">BasicDriver</span><br><span class="line">LoopDriver</span><br><span class="line">FiberDriver</span><br></pre></td></tr></table></figure><p>这里的 Driver 表示“远程 CANopen 节点的应用逻辑驱动&#x2F;处理器”，不是 Linux 字符设备驱动，也不是负责 SocketCAN fd 的 I&#x2F;O driver。</p><p>它们的主要职责是：</p><ul><li>接收 Master 分发的远程节点状态事件；</li><li>执行从站配置；</li><li>发起和等待 SDO 请求；</li><li>处理 RPDO&#x2F;TPDO、heartbeat、boot 等上层回调；</li><li>在需要时使用自己的 strand、fiber 或局部 event loop 串行化业务逻辑。</li></ul><p>它们不直接调用 <code>io_poll_watch()</code>。事件在到达 Driver 之前已经经过：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">Poll</span><br><span class="line">  → CanChannel / Timer task</span><br><span class="line">  → CanNet</span><br><span class="line">  → CANopen Node/Master</span><br><span class="line">  → Driver callback</span><br></pre></td></tr></table></figure><p><code>LoopDriver</code> 虽然内部有一个 event loop 和线程，但它的 loop 主要用于执行该远程节点 Driver 的异步任务。它不会重新监听 CAN fd；CAN fd 仍归 Master 共享的 CanChannel 和主 event loop 管理。</p><h3 id="2-10-三条最重要的完整运行流程"><a href="#2-10-三条最重要的完整运行流程" class="headerlink" title="2.10 三条最重要的完整运行流程"></a>2.10 三条最重要的完整运行流程</h3><h4 id="2-10-1-收到一帧-TPDO、SDO-响应或-heartbeat"><a href="#2-10-1-收到一帧-TPDO、SDO-响应或-heartbeat" class="headerlink" title="2.10.1 收到一帧 TPDO、SDO 响应或 heartbeat"></a>2.10.1 收到一帧 TPDO、SDO 响应或 heartbeat</h4><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">1. CAN 控制器收到帧，SocketCAN raw socket 变为可读</span><br><span class="line">2. epoll 返回 EPOLLIN</span><br><span class="line">3. io_poll 转换为 IO_EVENT_IN</span><br><span class="line">4. io_can_chan_impl_watch_func() 投递 rxbuf_task</span><br><span class="line">5. rxbuf_task 读取 SocketCAN 帧并填入接收队列</span><br><span class="line">6. CanChannel 完成 io_can_chan_read</span><br><span class="line">7. io_can_net_read_func() 更新时间并调用 can_net_recv()</span><br><span class="line">8. can_net_t 根据 CAN-ID 分发给已注册的 CAN/CANopen 接收对象</span><br><span class="line">9. Node/Master 触发 PDO、SDO、NMT、heartbeat 等对应回调</span><br><span class="line">10. coapp Driver 或用户回调在 executor 上运行</span><br><span class="line">11. CanChannel 再次等待下一帧；读到 EAGAIN 后重新登记 IO_EVENT_IN</span><br></pre></td></tr></table></figure><h4 id="2-10-2-CANopen-协议定时器到期"><a href="#2-10-2-CANopen-协议定时器到期" class="headerlink" title="2.10.2 CANopen 协议定时器到期"></a>2.10.2 CANopen 协议定时器到期</h4><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">1. CANopen 对象在 can_net_t timer heap 中更新下一到期时间</span><br><span class="line">2. io_can_net_next_func() 把时间提交给 io_tqueue</span><br><span class="line">3. Timer 设置 timerfd</span><br><span class="line">4. timerfd 到期并变为可读</span><br><span class="line">5. Poll 返回 IO_EVENT_IN</span><br><span class="line">6. Timer watch callback 投递 wait_task</span><br><span class="line">7. wait_task 读取 timerfd 到期计数</span><br><span class="line">8. io_tqueue 完成对应 wait</span><br><span class="line">9. io_can_net_wait_next_func() 调用 can_net_set_time()</span><br><span class="line">10. can_net_t 执行已到期 timer callback</span><br><span class="line">11. 若仍有下一到期时间，重新设置 timerfd 并恢复 Poll 监听</span><br></pre></td></tr></table></figure><h4 id="2-10-3-Ctrl-C-退出程序"><a href="#2-10-3-Ctrl-C-退出程序" class="headerlink" title="2.10.3 Ctrl+C 退出程序"></a>2.10.3 Ctrl+C 退出程序</h4><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">1. 进程收到 SIGINT</span><br><span class="line">2. SignalSet signal handler 设置 pending，并写 self-pipe</span><br><span class="line">3. self-pipe 读端变为可读</span><br><span class="line">4. Poll 返回 IO_EVENT_IN</span><br><span class="line">5. SignalSet watch callback 投递 read_task</span><br><span class="line">6. read_task 读空 pipe，确认具体 signal</span><br><span class="line">7. 异步 signal wait 完成</span><br><span class="line">8. 应用调用 Context shutdown / Loop stop</span><br><span class="line">9. CanChannel、Timer、SignalSet、CanNet 取消等待和登记</span><br><span class="line">10. ev_poll_kill() 唤醒仍在 epoll_pwait() 中的 event-loop 线程</span><br><span class="line">11. loop.run() 返回，应用进行析构</span><br></pre></td></tr></table></figure><h3 id="2-11-为什么-Poll-可以被多个组件共享"><a href="#2-11-为什么-Poll-可以被多个组件共享" class="headerlink" title="2.11 为什么 Poll 可以被多个组件共享"></a>2.11 为什么 Poll 可以被多个组件共享</h3><p>同一个 Poll 可以同时维护多个 fd：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">SocketCAN channel A fd</span><br><span class="line">SocketCAN channel B fd</span><br><span class="line">timerfd A</span><br><span class="line">timerfd B</span><br><span class="line">SignalSet self-pipe fd</span><br><span class="line">其他可轮询 I/O fd</span><br></pre></td></tr></table></figure><p>每个 fd 都有独立的 <code>io_poll_watch</code>，而 Poll 内部红黑树按 fd 建立映射。内核返回某个 fd 后，Poll 能找到其所属组件的 watch callback。</p><p>这也是红黑树存在的实际应用背景：不仅一个 CAN channel 会使用 Poll；一个应用可以共享同一个 Poll 给多个 CAN channel、多个 timer 和一个 SignalSet。随着节点或网络数量增加，Poll 中登记的 fd 数量也会增加。</p><p>但需要注意：</p><ul><li>一个具体 fd 在同一 Poll 中只能对应一个 watch；</li><li>每个 <code>CanChannel</code>、<code>Timer</code>、<code>SignalSet</code> 对象都持有自己的 watch；</li><li><code>EPOLLONESHOT</code> 使每个组件必须在自己的任务中明确恢复下一次监听；</li><li>多个 CANopen Node 可以共享 Poll、Loop 和 Controller，但通常需要各自独立的 Timer 和 CanChannel，以保持异步操作状态相互独立。</li></ul><h3 id="2-12-直接与间接依赖总表"><a href="#2-12-直接与间接依赖总表" class="headerlink" title="2.12 直接与间接依赖总表"></a>2.12 直接与间接依赖总表</h3><table><thead><tr><th>层次</th><th>模块&#x2F;组件</th><th>直接调用 <code>io_poll_watch()</code></th><th>被监听对象</th><th>Poll 事件后的动作</th></tr></thead><tbody><tr><td>平台 I&#x2F;O</td><td><code>io_can_chan_impl</code></td><td>是</td><td>SocketCAN fd</td><td>投递接收或写重试任务</td></tr><tr><td>平台 I&#x2F;O</td><td><code>io_timer_impl</code></td><td>是</td><td>timerfd</td><td>投递定时 wait 任务</td></tr><tr><td>平台 I&#x2F;O</td><td><code>io_sigset_impl</code></td><td>是</td><td>self-pipe read fd</td><td>投递 pipe 读取和 signal 分发任务</td></tr><tr><td>Event</td><td><code>ev_loop</code></td><td>否</td><td>不拥有具体设备 fd</td><td>调用 <code>ev_poll_wait()</code>，执行 executor task，必要时 <code>ev_poll_kill()</code></td></tr><tr><td>I&#x2F;O 适配</td><td><code>io_can_net</code></td><td>否</td><td>通过 Timer&#x2F;CanChannel 间接依赖</td><td>把 CAN 帧、发送请求和时间同步给 <code>can_net_t</code></td></tr><tr><td>CAN 核心</td><td><code>can_net_t</code></td><td>否</td><td>无系统 fd</td><td>被动接收帧、维护接收者和 timer heap、请求发送</td></tr><tr><td>CANopen 应用</td><td><code>canopen::Node</code></td><td>否</td><td>无系统 fd</td><td>构造协议对象并启动 CanNet 收发</td></tr><tr><td>CANopen 应用</td><td><code>BasicMaster</code> &#x2F; <code>AsyncMaster</code></td><td>否</td><td>无系统 fd</td><td>主站 NMT、SDO、节点管理等</td></tr><tr><td>CANopen 应用</td><td><code>BasicSlave</code></td><td>否</td><td>无系统 fd</td><td>从站协议处理</td></tr><tr><td>节点业务</td><td><code>DriverBase</code> &#x2F; <code>BasicDriver</code></td><td>否</td><td>无系统 fd</td><td>处理远程节点事件和业务逻辑</td></tr><tr><td>节点业务</td><td><code>LoopDriver</code> &#x2F; <code>FiberDriver</code></td><td>否</td><td>不重新监听 CAN fd</td><td>提供业务任务的同步&#x2F;串行执行环境</td></tr></tbody></table><h3 id="2-13-阅读这些模块时的推荐源码顺序"><a href="#2-13-阅读这些模块时的推荐源码顺序" class="headerlink" title="2.13 阅读这些模块时的推荐源码顺序"></a>2.13 阅读这些模块时的推荐源码顺序</h3><p>要从 Poll 一直读到 CANopen 主站，推荐按下面顺序：</p><ol><li><code>include/lely/io2/posix/poll.h</code>：理解 watch 的一次性回调契约；</li><li><code>src/io2/linux/poll.c</code>：理解 epoll、红黑树、wait 和 kill；</li><li><code>src/ev/loop.c</code>：理解谁调用 <code>ev_poll_wait()</code>，谁运行任务；</li><li><code>src/io2/linux/can_chan.c</code>：理解 SocketCAN 的异步读写如何登记 IN&#x2F;OUT；</li><li><code>src/io2/linux/timer.c</code>：理解 timerfd 如何产生异步定时完成；</li><li><code>src/io2/posix/sigset.c</code>：理解 self-pipe 信号转换；</li><li><code>src/io2/can_net.c</code>：理解 CanChannel&#x2F;Timer 如何接入被动 <code>can_net_t</code>；</li><li><code>src/coapp/node.cpp</code>：理解 Node 如何基于 CanNet 启动 CANopen 协议对象；</li><li><code>src/coapp/master.cpp</code>：理解 Master 如何在 Node 之上增加远程节点管理；</li><li><code>src/coapp/driver.cpp</code>、<code>loop_driver.cpp</code> 或对应头文件：理解远程节点业务 Driver 如何消费上层事件。</li></ol><p>按照这个顺序，不会把以下三个层次混在一起：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Linux fd 是否就绪</span><br><span class="line">CAN 帧如何异步收发</span><br><span class="line">CANopen 协议收到帧后做什么</span><br></pre></td></tr></table></figure><hr><h2 id="3-阅读源码前必须先懂的-Linux-epoll"><a href="#3-阅读源码前必须先懂的-Linux-epoll" class="headerlink" title="3. 阅读源码前必须先懂的 Linux epoll"></a>3. 阅读源码前必须先懂的 Linux <code>epoll</code></h2><h3 id="3-1-epoll-解决什么问题"><a href="#3-1-epoll-解决什么问题" class="headerlink" title="3.1 epoll 解决什么问题"></a>3.1 <code>epoll</code> 解决什么问题</h3><p>进程可能同时管理很多文件描述符，例如：</p><ul><li>SocketCAN socket；</li><li>TCP&#x2F;UDP socket；</li><li><code>timerfd</code>；</li><li><code>eventfd</code>；</li><li>pipe；</li><li>串口或其他可轮询设备。</li></ul><p>如果每个 <code>fd</code> 都用一个线程阻塞读取，线程数量和切换成本会很高。<code>epoll</code> 允许一个线程统一等待多个 <code>fd</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">fd 3: SocketCAN</span><br><span class="line">fd 4: timerfd</span><br><span class="line">fd 7: TCP socket</span><br><span class="line">fd 9: eventfd</span><br><span class="line">        ↓ 全部登记到同一个 epoll 实例</span><br><span class="line">      epoll_pwait()</span><br><span class="line">        ↓</span><br><span class="line">内核只返回当前已经就绪的 fd</span><br></pre></td></tr></table></figure><p>Linux <code>epoll</code> 的核心概念可以简化为两个集合：</p><table><thead><tr><th>内核集合</th><th>含义</th></tr></thead><tbody><tr><td>interest list</td><td>调用方已经登记、希望监控的 <code>fd</code> 集合</td></tr><tr><td>ready list</td><td>interest list 中当前已经发生事件、可以返回给调用方的 <code>fd</code> 集合</td></tr></tbody></table><h3 id="3-2-epfd-是什么"><a href="#3-2-epfd-是什么" class="headerlink" title="3.2 epfd 是什么"></a>3.2 <code>epfd</code> 是什么</h3><p>源码通过：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">poll-&gt;epfd = epoll_create1(EPOLL_CLOEXEC);</span><br></pre></td></tr></table></figure><p>创建一个 epoll 实例。返回值 <code>epfd</code> 本身也是一个文件描述符，但它代表的是“内核中的 epoll 管理对象”，不是被监控的 SocketCAN 或 timerfd。</p><p><code>EPOLL_CLOEXEC</code> 表示执行 <code>exec()</code> 装载新程序时自动关闭该 <code>epfd</code>，避免文件描述符泄漏到新程序。</p><h3 id="3-3-struct-epoll-event-的两个关键字段"><a href="#3-3-struct-epoll-event-的两个关键字段" class="headerlink" title="3.3 struct epoll_event 的两个关键字段"></a>3.3 <code>struct epoll_event</code> 的两个关键字段</h3><p>源码宏最终构造的是：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">epoll_event</span> <span class="title">event</span> =</span> &#123;</span><br><span class="line">    .events = <span class="comment">/* EPOLLIN、EPOLLOUT、EPOLLONESHOT 等位掩码 */</span>,</span><br><span class="line">    .data.fd = fd</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>含义：</p><table><thead><tr><th>字段</th><th>用途</th></tr></thead><tbody><tr><td><code>event.events</code></td><td>告诉内核要关注哪些事件，以及采用什么行为模式</td></tr><tr><td><code>event.data.fd</code></td><td>与该登记项绑定的用户数据；事件返回时内核原样带回来</td></tr></tbody></table><p>Lely 只把 <code>fd</code> 放入 <code>data.fd</code>，没有把 <code>watch*</code> 直接放入 <code>data.ptr</code>。因此内核返回事件后，Lely 还需要用 <code>fd</code> 去红黑树查找对应的 <code>io_poll_watch</code>。</p><h3 id="3-4-epoll-ctl-：管理-interest-list"><a href="#3-4-epoll-ctl-：管理-interest-list" class="headerlink" title="3.4 epoll_ctl()：管理 interest list"></a>3.4 <code>epoll_ctl()</code>：管理 interest list</h3><p>函数原型可理解为：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">epoll_ctl</span><span class="params">(<span class="type">int</span> epfd, <span class="type">int</span> op, <span class="type">int</span> fd, <span class="keyword">struct</span> epoll_event *event)</span>;</span><br></pre></td></tr></table></figure><p>三个操作是理解 <code>io_poll_watch()</code> 的基础：</p><table><thead><tr><th>操作</th><th>含义</th><th>Lely 中的使用场景</th></tr></thead><tbody><tr><td><code>EPOLL_CTL_ADD</code></td><td>把一个新 <code>fd</code> 加入 epoll interest list</td><td>第一次监听该 <code>fd</code></td></tr><tr><td><code>EPOLL_CTL_MOD</code></td><td>修改该 <code>fd</code> 的事件掩码；对于 <code>EPOLLONESHOT</code>，同时恢复下一次监听</td><td>修改关注类型，或一次通知后继续监听</td></tr><tr><td><code>EPOLL_CTL_DEL</code></td><td>从 interest list 删除该 <code>fd</code></td><td>不再监听该 <code>fd</code></td></tr></tbody></table><p>需要严格区分：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">ADD：此前内核中没有该登记项</span><br><span class="line">MOD：此前已有登记项，只更新或恢复</span><br><span class="line">DEL：彻底删除登记项</span><br></pre></td></tr></table></figure><h3 id="3-5-epoll-pwait-：等待事件"><a href="#3-5-epoll-pwait-：等待事件" class="headerlink" title="3.5 epoll_pwait()：等待事件"></a>3.5 <code>epoll_pwait()</code>：等待事件</h3><p>源码调用：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> nevents = epoll_pwait(</span><br><span class="line">        poll-&gt;epfd,</span><br><span class="line">        events,</span><br><span class="line">        LELY_IO_EPOLL_MAXEVENTS,</span><br><span class="line">        timeout,</span><br><span class="line">        &amp;<span class="built_in">set</span>);</span><br></pre></td></tr></table></figure><p>参数含义：</p><table><thead><tr><th>参数</th><th>含义</th></tr></thead><tbody><tr><td><code>poll-&gt;epfd</code></td><td>等待哪个 epoll 实例</td></tr><tr><td><code>events</code></td><td>输出数组，内核把已经发生的事件写入这里</td></tr><tr><td><code>LELY_IO_EPOLL_MAXEVENTS</code></td><td>本次数组最多容纳多少个事件</td></tr><tr><td><code>timeout</code></td><td>等待时间，单位为毫秒；<code>-1</code> 无限等待，<code>0</code> 立即返回</td></tr><tr><td><code>&amp;set</code></td><td>等待期间临时使用的信号掩码</td></tr></tbody></table><p>返回值：</p><table><thead><tr><th>返回值</th><th>含义</th></tr></thead><tbody><tr><td><code>&gt; 0</code></td><td>本次返回的事件数量</td></tr><tr><td><code>0</code></td><td>超时，没有事件</td></tr><tr><td><code>-1</code></td><td>系统调用失败，错误在 <code>errno</code> 中；被信号打断时通常为 <code>EINTR</code></td></tr></tbody></table><p><code>epoll_pwait()</code> 与 <code>epoll_wait()</code> 的主要区别是：它能在“进入等待”的同时原子地切换信号掩码，用于避免检查状态与进入阻塞之间的丢失唤醒竞态。</p><h3 id="3-6-本实现是“电平触发-一次性通知”"><a href="#3-6-本实现是“电平触发-一次性通知”" class="headerlink" title="3.6 本实现是“电平触发 + 一次性通知”"></a>3.6 本实现是“电平触发 + 一次性通知”</h3><p>宏没有加入 <code>EPOLLET</code>，所以 epoll 使用默认的 level-triggered，即电平触发模式；同时又加入 <code>EPOLLONESHOT</code>。</p><p>组合后的实际行为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">只要 fd 仍满足可读/可写条件，电平触发本来可以持续报告</span><br><span class="line">                    +</span><br><span class="line">EPOLLONESHOT 规定每次恢复监听后最多只报告一次</span><br><span class="line">                    =</span><br><span class="line">收到一次通知后暂停该 fd，调用方处理完后显式决定是否继续</span><br></pre></td></tr></table></figure><p>如果回调没有消除就绪条件，例如 socket 中仍有未读数据，那么再次 <code>EPOLL_CTL_MOD</code> 恢复监听后，可能立刻再次收到事件。这是正常的电平触发行为。</p><hr><h2 id="4-IO-EVENT-与-EPOLL-到底是什么关系"><a href="#4-IO-EVENT-与-EPOLL-到底是什么关系" class="headerlink" title="4. IO_EVENT_* 与 EPOLL* 到底是什么关系"></a>4. <code>IO_EVENT_*</code> 与 <code>EPOLL*</code> 到底是什么关系</h2><h3 id="4-1-两套事件名称属于不同层"><a href="#4-1-两套事件名称属于不同层" class="headerlink" title="4.1 两套事件名称属于不同层"></a>4.1 两套事件名称属于不同层</h3><table><thead><tr><th>名称前缀</th><th>所属层</th><th>用途</th></tr></thead><tbody><tr><td><code>IO_EVENT_*</code></td><td>Lely 跨平台抽象层</td><td>上层设备代码使用，不直接依赖 Linux</td></tr><tr><td><code>EPOLL*</code></td><td>Linux epoll API</td><td>Linux 内核能识别的事件和控制标志</td></tr></tbody></table><p>因此，<code>IO_EVENT_IN</code> 不是 <code>EPOLLIN</code> 的别名，而是 Lely 的通用“读侧事件”抽象。Linux 后端负责把它转换成 <code>EPOLLIN</code> 等内核标志。</p><h3 id="4-2-事件转换宏逐项解释"><a href="#4-2-事件转换宏逐项解释" class="headerlink" title="4.2 事件转换宏逐项解释"></a>4.2 事件转换宏逐项解释</h3><p>源码：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> EPOLL_EVENT_INIT(events, fd) \</span></span><br><span class="line"><span class="meta">    &#123; \</span></span><br><span class="line"><span class="meta">        (((events) &amp; IO_EVENT_IN) ? (EPOLLIN | EPOLLRDHUP) : 0) \</span></span><br><span class="line"><span class="meta">                | (((events) &amp; IO_EVENT_PRI) ? EPOLLPRI : 0) \</span></span><br><span class="line"><span class="meta">                | (((events) &amp; IO_EVENT_OUT) ? EPOLLOUT : 0) \</span></span><br><span class="line"><span class="meta">                | EPOLLONESHOT, \</span></span><br><span class="line"><span class="meta">        &#123; .fd = (fd) &#125; \</span></span><br><span class="line"><span class="meta">    &#125;</span></span><br></pre></td></tr></table></figure><p>它构造一个 <code>struct epoll_event</code>，第一部分是 <code>event.events</code>，第二部分是 <code>event.data.fd</code>。</p><h4 id="4-2-1-IO-EVENT-IN"><a href="#4-2-1-IO-EVENT-IN" class="headerlink" title="4.2.1 IO_EVENT_IN"></a>4.2.1 <code>IO_EVENT_IN</code></h4><p>Lely 含义：调用方关注“读侧有事件”。通常意味着应该尝试 <code>read()</code>、<code>recv()</code> 或设备对应的读取操作。</p><p>它在 Linux 后端被映射为：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">EPOLLIN | EPOLLRDHUP</span><br></pre></td></tr></table></figure><h4 id="4-2-2-EPOLLIN"><a href="#4-2-2-EPOLLIN" class="headerlink" title="4.2.2 EPOLLIN"></a>4.2.2 <code>EPOLLIN</code></h4><p>Linux 含义：关联对象当前可进行读取操作。</p><p>需要注意：</p><ul><li>它表示“应该尝试读取”，不是保证一定能读取到完整业务帧；</li><li>多线程或其他代码也可能先一步读走数据，因此非阻塞读取仍应处理 <code>EAGAIN</code>；</li><li>对 pipe 或 stream socket，读侧 EOF&#x2F;关闭也可能伴随读事件，最终应以 <code>read()</code> 返回值判断；</li><li>对 SocketCAN，收到 CAN 帧后通常表现为 <code>EPOLLIN</code>。</li></ul><h4 id="4-2-3-EPOLLRDHUP"><a href="#4-2-3-EPOLLRDHUP" class="headerlink" title="4.2.3 EPOLLRDHUP"></a>4.2.3 <code>EPOLLRDHUP</code></h4><p>Linux 含义：面向连接的流式 socket 对端关闭了写方向，或者关闭了连接。</p><p>Lely 把它也映射为 <code>IO_EVENT_IN</code>：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (events[i].events &amp; (EPOLLIN | EPOLLRDHUP))</span><br><span class="line">    revents |= IO_EVENT_IN;</span><br></pre></td></tr></table></figure><p>原因是读侧处理函数需要被唤醒，然后通过 <code>read()/recv()</code> 判断数据、EOF 或断开状态。</p><p>对于 SocketCAN 这类非 TCP 流式连接，<code>EPOLLRDHUP</code> 通常不是主要事件；但宏是通用 POSIX I&#x2F;O 后端，所以统一加入。</p><h4 id="4-2-4-IO-EVENT-PRI"><a href="#4-2-4-IO-EVENT-PRI" class="headerlink" title="4.2.4 IO_EVENT_PRI"></a>4.2.4 <code>IO_EVENT_PRI</code></h4><p>Lely 含义：调用方关注“高优先级或异常条件事件”。</p><p>它被映射为：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">EPOLLPRI</span><br></pre></td></tr></table></figure><h4 id="4-2-5-EPOLLPRI"><a href="#4-2-5-EPOLLPRI" class="headerlink" title="4.2.5 EPOLLPRI"></a>4.2.5 <code>EPOLLPRI</code></h4><p>Linux 含义：文件描述符出现 exceptional condition，即异常优先级条件。典型例子包括某些 socket 的带外数据或特定设备的紧急状态。</p><p>它不是普通输入数据：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">普通可读数据 → EPOLLIN</span><br><span class="line">异常/紧急条件 → EPOLLPRI</span><br></pre></td></tr></table></figure><p>是否使用 <code>IO_EVENT_PRI</code> 取决于具体 fd 类型。仅从 <code>poll.c</code> 无法断定某个 CANopen 设备模块会不会登记该事件。</p><h4 id="4-2-6-IO-EVENT-OUT"><a href="#4-2-6-IO-EVENT-OUT" class="headerlink" title="4.2.6 IO_EVENT_OUT"></a>4.2.6 <code>IO_EVENT_OUT</code></h4><p>Lely 含义：调用方关注“写侧可以继续推进”。通常用于非阻塞 socket 发送缓冲区有空间、异步连接完成等情况。</p><p>它被映射为：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">EPOLLOUT</span><br></pre></td></tr></table></figure><h4 id="4-2-7-EPOLLOUT"><a href="#4-2-7-EPOLLOUT" class="headerlink" title="4.2.7 EPOLLOUT"></a>4.2.7 <code>EPOLLOUT</code></h4><p>Linux 含义：关联对象当前可执行写操作。</p><p>它不保证一次 <code>write()</code> 就能写完全部数据，只说明至少可以尝试推进写操作。正确的非阻塞代码仍要处理：</p><ul><li>部分写入；</li><li><code>EAGAIN</code>；</li><li>连接错误；</li><li>待发送缓冲区剩余数据。</li></ul><h4 id="4-2-8-“所有登记强制附加-EPOLLONESHOT”"><a href="#4-2-8-“所有登记强制附加-EPOLLONESHOT”" class="headerlink" title="4.2.8 “所有登记强制附加 EPOLLONESHOT”"></a>4.2.8 “所有登记强制附加 <code>EPOLLONESHOT</code>”</h4><p>宏最后无条件执行：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">| EPOLLONESHOT</span><br></pre></td></tr></table></figure><p>因此，只要 <code>events != 0</code> 并进入 ADD 或 MOD，生成的 Linux 事件掩码一定包含 <code>EPOLLONESHOT</code>，无论调用方登记的是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">IO_EVENT_IN</span><br><span class="line">IO_EVENT_PRI</span><br><span class="line">IO_EVENT_OUT</span><br><span class="line">IO_EVENT_IN | IO_EVENT_OUT</span><br><span class="line">或其他受支持组合</span><br></pre></td></tr></table></figure><p><code>EPOLLONESHOT</code> 的准确含义是：</p><ol><li>内核返回该 <code>fd</code> 的一次事件；</li><li>该 <code>fd</code> 的 epoll 登记项仍存在，但暂时不再继续报告事件；</li><li>调用方必须执行 <code>epoll_ctl(..., EPOLL_CTL_MOD, ...)</code> 才能恢复下一次通知。</li></ol><p>本文把这个过程称为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">第一次 ADD/MOD 后：当前一次监听有效</span><br><span class="line">事件返回后：本次监听已消费</span><br><span class="line">再次 MOD 后：恢复下一次监听</span><br></pre></td></tr></table></figure><h4 id="4-2-9-为什么宏没显式加入-EPOLLERR-和-EPOLLHUP"><a href="#4-2-9-为什么宏没显式加入-EPOLLERR-和-EPOLLHUP" class="headerlink" title="4.2.9 为什么宏没显式加入 EPOLLERR 和 EPOLLHUP"></a>4.2.9 为什么宏没显式加入 <code>EPOLLERR</code> 和 <code>EPOLLHUP</code></h4><p>Linux 会始终报告 <code>EPOLLERR</code> 和 <code>EPOLLHUP</code>，即使调用 <code>epoll_ctl()</code> 时没有把它们写入事件掩码。因此源码只在事件返回阶段进行转换：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (events[i].events &amp; EPOLLERR)</span><br><span class="line">    revents |= IO_EVENT_ERR;</span><br><span class="line"><span class="keyword">if</span> (events[i].events &amp; EPOLLHUP)</span><br><span class="line">    revents |= IO_EVENT_HUP;</span><br></pre></td></tr></table></figure><hr><h2 id="5-核心对象以及本文使用的状态术语"><a href="#5-核心对象以及本文使用的状态术语" class="headerlink" title="5. 核心对象以及本文使用的状态术语"></a>5. 核心对象以及本文使用的状态术语</h2><h3 id="5-1-struct-io-poll"><a href="#5-1-struct-io-poll" class="headerlink" title="5.1 struct io_poll"></a>5.1 <code>struct io_poll</code></h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">io_poll</span> &#123;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">io_svc</span> <span class="title">svc</span>;</span></span><br><span class="line">    <span class="type">const</span> <span class="class"><span class="keyword">struct</span> <span class="title">ev_poll_vtbl</span> *<span class="title">poll_vptr</span>;</span></span><br><span class="line">    <span class="type">io_ctx_t</span> *ctx;</span><br><span class="line">    <span class="type">int</span> signo;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sigaction</span> <span class="title">oact</span>;</span></span><br><span class="line">    <span class="type">sigset_t</span> oset;</span><br><span class="line">    <span class="type">int</span> epfd;</span><br><span class="line">    <span class="type">pthread_mutex_t</span> mtx;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">rbtree</span> <span class="title">tree</span>;</span></span><br><span class="line">    <span class="type">size_t</span> nwatch;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><table><thead><tr><th>字段</th><th>含义</th></tr></thead><tbody><tr><td><code>svc</code></td><td>注册到 <code>io_ctx</code> 的服务对象，用于接收 fork 通知</td></tr><tr><td><code>poll_vptr</code></td><td><code>ev_poll_t</code> 虚函数表入口，包含 <code>self/wait/kill</code></td></tr><tr><td><code>ctx</code></td><td>所属 I&#x2F;O context</td></tr><tr><td><code>signo</code></td><td>跨线程唤醒 <code>epoll_pwait()</code> 使用的信号</td></tr><tr><td><code>oact</code></td><td>保存原信号处理函数</td></tr><tr><td><code>oset</code></td><td>保存原信号掩码</td></tr><tr><td><code>epfd</code></td><td>Linux epoll 实例文件描述符</td></tr><tr><td><code>mtx</code></td><td>保护内部红黑树、计数和停止状态的互斥锁</td></tr><tr><td><code>tree</code></td><td>以 <code>fd</code> 为键保存 <code>io_poll_watch</code> 的红黑树</td></tr><tr><td><code>nwatch</code></td><td>当前仍处于“一次监听有效”状态的 watch 数量</td></tr></tbody></table><h3 id="5-2-struct-io-poll-watch"><a href="#5-2-struct-io-poll-watch" class="headerlink" title="5.2 struct io_poll_watch"></a>5.2 <code>struct io_poll_watch</code></h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">io_poll_watch</span> &#123;</span></span><br><span class="line">    <span class="type">io_poll_watch_func_t</span> *func;</span><br><span class="line">    <span class="type">int</span> _fd;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">rbnode</span> _<span class="title">node</span>;</span></span><br><span class="line">    <span class="type">int</span> _events;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><table><thead><tr><th>字段</th><th>含义</th></tr></thead><tbody><tr><td><code>func</code></td><td>事件发生时调用的回调函数</td></tr><tr><td><code>_fd</code></td><td>该 watch 对应的文件描述符，同时作为红黑树键</td></tr><tr><td><code>_node</code></td><td>嵌入在 watch 内部的红黑树节点</td></tr><tr><td><code>_events</code></td><td>Lely 认为当前有效的事件掩码；<code>0</code> 表示当前没有有效的一次监听</td></tr></tbody></table><h3 id="5-3-watch-的三种监听状态"><a href="#5-3-watch-的三种监听状态" class="headerlink" title="5.3 watch 的三种监听状态"></a>5.3 watch 的三种监听状态</h3><table><thead><tr><th>状态</th><th>红黑树中是否存在</th><th align="right"><code>_events</code></th><th>内核状态</th><th>含义</th></tr></thead><tbody><tr><td>未登记</td><td>否</td><td align="right">通常为 0</td><td>epoll 中无该登记项</td><td><code>io_poll</code> 不再管理该 fd</td></tr><tr><td>已登记且当前一次监听有效</td><td>是</td><td align="right">非 0</td><td>ADD&#x2F;MOD 后可返回一次事件</td><td>正在等待下一次通知</td></tr><tr><td>已登记，但本次监听已消费</td><td>是</td><td align="right">0</td><td><code>EPOLLONESHOT</code> 已暂停该项</td><td>关联关系还在，但必须 MOD 才能继续收到通知</td></tr></tbody></table><p>第三种状态最容易误解：红黑树节点还在，不代表当前还能收到事件。它只是保留了 <code>fd → watch</code> 关联，便于后续通过 <code>EPOLL_CTL_MOD</code> 恢复监听，或者通过 <code>EPOLL_CTL_DEL</code> 彻底删除。</p><hr><h2 id="6-为什么需要红黑树"><a href="#6-为什么需要红黑树" class="headerlink" title="6. 为什么需要红黑树"></a>6. 为什么需要红黑树</h2><h3 id="6-1-先回答“是不是因为可能挂载很多事件”"><a href="#6-1-先回答“是不是因为可能挂载很多事件”" class="headerlink" title="6.1 先回答“是不是因为可能挂载很多事件”"></a>6.1 先回答“是不是因为可能挂载很多事件”</h3><p>是，但这只是原因之一。</p><p><code>epoll</code> 本身适合管理大量文件描述符，Lely 选择红黑树也使用户态的 <code>fd → watch</code> 查找在监控对象较多时仍保持稳定的 <code>O(log N)</code> 复杂度。不过，从这三个文件不能推断实际运行时一定会有很多 <code>fd</code>；这是面向通用 I&#x2F;O 库的可扩展设计。</p><h3 id="6-2-红黑树在本实现中承担四个具体职责"><a href="#6-2-红黑树在本实现中承担四个具体职责" class="headerlink" title="6.2 红黑树在本实现中承担四个具体职责"></a>6.2 红黑树在本实现中承担四个具体职责</h3><h4 id="6-2-1-由内核返回的-fd-找到回调对象"><a href="#6-2-1-由内核返回的-fd-找到回调对象" class="headerlink" title="6.2.1 由内核返回的 fd 找到回调对象"></a>6.2.1 由内核返回的 <code>fd</code> 找到回调对象</h4><p>内核只带回：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> fd = events[i].data.fd;</span><br></pre></td></tr></table></figure><p>Lely 随后执行：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">rbnode</span> *<span class="title">node</span> =</span> rbtree_find(&amp;poll-&gt;tree, &amp;fd);</span><br></pre></td></tr></table></figure><p>找到节点后，再通过：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">io_poll_watch_from_node(node)</span><br></pre></td></tr></table></figure><p>恢复到包含它的 <code>struct io_poll_watch</code>。</p><h4 id="6-2-2-保证同一-fd-只能绑定一个-watch-对象"><a href="#6-2-2-保证同一-fd-只能绑定一个-watch-对象" class="headerlink" title="6.2.2 保证同一 fd 只能绑定一个 watch 对象"></a>6.2.2 保证同一 <code>fd</code> 只能绑定一个 watch 对象</h4><p>源码：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (node &amp;&amp; node != &amp;watch-&gt;_node) &#123;</span><br><span class="line">    errsv = EEXIST;</span><br><span class="line">    <span class="keyword">goto</span> error;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>如果树中已经有这个 <code>fd</code>，但对应的节点不是当前传入的 <code>watch-&gt;_node</code>，说明调用方试图用另一个 watch 接管同一个 <code>fd</code>，函数返回 <code>EEXIST</code>。</p><h4 id="6-2-3-过滤已经取消登记的陈旧事件"><a href="#6-2-3-过滤已经取消登记的陈旧事件" class="headerlink" title="6.2.3 过滤已经取消登记的陈旧事件"></a>6.2.3 过滤已经取消登记的陈旧事件</h4><p>并发情况下，内核事件数组中可能已经包含某个 <code>fd</code>，但另一个线程随后取消了登记。事件分发时再次查树：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (node) &#123;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>如果节点已经不存在，就忽略该事件，不会直接解引用一个可能已经失效的 <code>watch*</code>。</p><p>这也是为什么实现选择 <code>event.data.fd + 红黑树</code>，而不是简单地把裸 <code>watch*</code> 放在 <code>event.data.ptr</code> 后直接调用。</p><h4 id="6-2-4-fork-后遍历并重建-epoll-登记项"><a href="#6-2-4-fork-后遍历并重建-epoll-登记项" class="headerlink" title="6.2.4 fork 后遍历并重建 epoll 登记项"></a>6.2.4 fork 后遍历并重建 epoll 登记项</h4><p>子进程收到 <code>IO_FORK_CHILD</code> 后，源码遍历红黑树，对仍有 <code>_events</code> 的 watch 重新执行 <code>EPOLL_CTL_ADD</code>。</p><h3 id="6-3-为什么不是数组、链表或哈希表"><a href="#6-3-为什么不是数组、链表或哈希表" class="headerlink" title="6.3 为什么不是数组、链表或哈希表"></a>6.3 为什么不是数组、链表或哈希表</h3><table><thead><tr><th>结构</th><th>优点</th><th>对本实现的限制</th></tr></thead><tbody><tr><td>数组按 fd 直接索引</td><td>查找 <code>O(1)</code></td><td>fd 数值可能稀疏且很大，浪费空间</td></tr><tr><td>链表</td><td>实现简单</td><td>每次按 fd 查找 <code>O(N)</code></td></tr><tr><td>哈希表</td><td>平均 <code>O(1)</code></td><td>需要容量、扩容和哈希策略，最坏情况不稳定</td></tr><tr><td>红黑树</td><td>最坏 <code>O(log N)</code>，支持有序遍历，适合稀疏整数键</td><td>实现比链表复杂</td></tr></tbody></table><p>上述“为什么选红黑树”属于基于数据结构和调用方式的设计分析；源码没有注释直接说明作者的完整选型理由。</p><hr><h2 id="7-io-fd-cmp-实际比较的是什么"><a href="#7-io-fd-cmp-实际比较的是什么" class="headerlink" title="7. io_fd_cmp() 实际比较的是什么"></a>7. <code>io_fd_cmp()</code> 实际比较的是什么</h2><p>源码：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">static</span> <span class="type">int</span></span><br><span class="line"><span class="title function_">io_fd_cmp</span><span class="params">(<span class="type">const</span> <span class="type">void</span> *p1, <span class="type">const</span> <span class="type">void</span> *p2)</span></span><br><span class="line">&#123;</span><br><span class="line">    assert(p1);</span><br><span class="line">    <span class="type">int</span> fd1 = *(<span class="type">const</span> <span class="type">int</span> *)p1;</span><br><span class="line">    assert(p2);</span><br><span class="line">    <span class="type">int</span> fd2 = *(<span class="type">const</span> <span class="type">int</span> *)p2;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> (fd2 &lt; fd1) - (fd1 &lt; fd2);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="7-1-p1-和-p2-都指向一个-int-fd"><a href="#7-1-p1-和-p2-都指向一个-int-fd" class="headerlink" title="7.1 p1 和 p2 都指向一个 int fd"></a>7.1 <code>p1</code> 和 <code>p2</code> 都指向一个 <code>int fd</code></h3><p>红黑树初始化时：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">rbtree_init(&amp;poll-&gt;tree, &amp;io_fd_cmp);</span><br></pre></td></tr></table></figure><p>插入节点时：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">watch-&gt;_fd = fd;</span><br><span class="line">rbnode_init(&amp;watch-&gt;_node, &amp;watch-&gt;_fd);</span><br></pre></td></tr></table></figure><p>所以节点的键是 <code>&amp;watch-&gt;_fd</code>。</p><p>查找时：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">rbtree_find(&amp;poll-&gt;tree, &amp;fd);</span><br></pre></td></tr></table></figure><p>传入的查找键也是一个 <code>int</code> 的地址。</p><p>因此，<code>io_fd_cmp()</code> 比较的不是：</p><ul><li><code>watch</code> 地址；</li><li><code>rbnode</code> 地址；</li><li>事件掩码；</li><li>epoll 实例 <code>epfd</code>。</li></ul><p>它只比较两个整数文件描述符 <code>fd1</code> 和 <code>fd2</code>。</p><h3 id="7-2-返回表达式如何工作"><a href="#7-2-返回表达式如何工作" class="headerlink" title="7.2 返回表达式如何工作"></a>7.2 返回表达式如何工作</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">return</span> (fd2 &lt; fd1) - (fd1 &lt; fd2);</span><br></pre></td></tr></table></figure><p>C 中比较表达式结果为 <code>0</code> 或 <code>1</code>：</p><table><thead><tr><th>条件</th><th align="right">第一项</th><th align="right">第二项</th><th align="right">返回值</th><th>比较结果</th></tr></thead><tbody><tr><td><code>fd1 &lt; fd2</code></td><td align="right">0</td><td align="right">1</td><td align="right">-1</td><td>第一个键更小</td></tr><tr><td><code>fd1 == fd2</code></td><td align="right">0</td><td align="right">0</td><td align="right">0</td><td>两个键相等</td></tr><tr><td><code>fd1 &gt; fd2</code></td><td align="right">1</td><td align="right">0</td><td align="right">1</td><td>第一个键更大</td></tr></tbody></table><p>例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">fd1=3, fd2=5 → (5&lt;3)-(3&lt;5) = 0-1 = -1</span><br><span class="line">fd1=5, fd2=3 → (3&lt;5)-(5&lt;3) = 1-0 = 1</span><br><span class="line">fd1=4, fd2=4 → 0-0 = 0</span><br></pre></td></tr></table></figure><p>这等价于普通升序比较，但避免写成：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">return</span> fd1 - fd2;</span><br></pre></td></tr></table></figure><p>后者对通用整数键可能存在减法溢出风险。</p><hr><h2 id="8-完整使用周期第-1-步：创建-io-poll"><a href="#8-完整使用周期第-1-步：创建-io-poll" class="headerlink" title="8. 完整使用周期第 1 步：创建 io_poll"></a>8. 完整使用周期第 1 步：创建 <code>io_poll</code></h2><h3 id="8-1-C-入口：lely-io-Poll-Poll"><a href="#8-1-C-入口：lely-io-Poll-Poll" class="headerlink" title="8.1 C++ 入口：lely::io::Poll::Poll()"></a>8.1 C++ 入口：<code>lely::io::Poll::Poll()</code></h3><p><code>poll.hpp</code>：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">Poll</span>(ContextBase&amp; ctx, <span class="type">int</span> signo = <span class="number">0</span>) : <span class="built_in">poll_</span>(<span class="built_in">io_poll_create</span>(ctx, signo)) &#123;</span><br><span class="line">  <span class="keyword">if</span> (!poll_) util::<span class="built_in">throw_errc</span>(<span class="string">&quot;Poll&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>C++ 类只是 RAII 包装：</p><ul><li>构造时调用 <code>io_poll_create()</code>；</li><li>析构时调用 <code>io_poll_destroy()</code>；</li><li><code>watch()</code> 转调 <code>io_poll_watch()</code>；</li><li><code>get_poll()</code> 返回通用 <code>ev::Poll</code> 接口。</li></ul><p>实际 epoll 算法全部在 <code>poll.c</code>。</p><h3 id="8-2-io-poll-create"><a href="#8-2-io-poll-create" class="headerlink" title="8.2 io_poll_create()"></a>8.2 <code>io_poll_create()</code></h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">io_poll_t</span> *poll = io_poll_alloc();</span><br><span class="line">...</span><br><span class="line"><span class="type">io_poll_t</span> *tmp = io_poll_init(poll, ctx, signo);</span><br></pre></td></tr></table></figure><p>职责：</p><ol><li><code>malloc(sizeof(io_poll_t))</code>；</li><li>调用 <code>io_poll_init()</code>；</li><li>初始化失败时释放内存并保留错误码。</li></ol><h3 id="8-3-io-poll-init"><a href="#8-3-io-poll-init" class="headerlink" title="8.3 io_poll_init()"></a>8.3 <code>io_poll_init()</code></h3><p>关键步骤按执行顺序如下：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line">设置 io_svc 虚函数表</span><br><span class="line">  ↓</span><br><span class="line">保存 ctx</span><br><span class="line">  ↓</span><br><span class="line">设置 ev_poll self/wait/kill 虚函数表</span><br><span class="line">  ↓</span><br><span class="line">选择唤醒信号，signo=0 时使用 SIGUSR1</span><br><span class="line">  ↓</span><br><span class="line">安装空信号处理函数 sig_ign</span><br><span class="line">  ↓</span><br><span class="line">正常执行期间阻塞该唤醒信号</span><br><span class="line">  ↓</span><br><span class="line">初始化 mutex</span><br><span class="line">  ↓</span><br><span class="line">初始化红黑树，比较函数为 io_fd_cmp</span><br><span class="line">  ↓</span><br><span class="line">nwatch=0</span><br><span class="line">  ↓</span><br><span class="line">epoll_create1(EPOLL_CLOEXEC)</span><br><span class="line">  ↓</span><br><span class="line">注册到 io_ctx</span><br></pre></td></tr></table></figure><p>初始化完成后的关键状态：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">poll-&gt;epfd   = 有效 epoll fd</span><br><span class="line">poll-&gt;tree   = 空</span><br><span class="line">poll-&gt;nwatch = 0</span><br></pre></td></tr></table></figure><hr><h2 id="9-完整使用周期第-2-步：初始化-io-poll-watch"><a href="#9-完整使用周期第-2-步：初始化-io-poll-watch" class="headerlink" title="9. 完整使用周期第 2 步：初始化 io_poll_watch"></a>9. 完整使用周期第 2 步：初始化 <code>io_poll_watch</code></h2><p>头文件提供：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> IO_POLL_WATCH_INIT(func) \</span></span><br><span class="line"><span class="meta">    &#123; \</span></span><br><span class="line"><span class="meta">        (func), -1, RBNODE_INIT, 0 \</span></span><br><span class="line"><span class="meta">    &#125;</span></span><br></pre></td></tr></table></figure><p>例如：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">static</span> <span class="type">void</span> <span class="title function_">on_event</span><span class="params">(<span class="keyword">struct</span> io_poll_watch *watch, <span class="type">int</span> events)</span>;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">io_poll_watch</span> <span class="title">watch</span> =</span> IO_POLL_WATCH_INIT(&amp;on_event);</span><br></pre></td></tr></table></figure><p>初始值：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">watch.func    = on_event</span><br><span class="line">watch._fd     = -1</span><br><span class="line">watch._node   = 未插入红黑树</span><br><span class="line">watch._events = 0</span><br></pre></td></tr></table></figure><p>调用方应该把 <code>io_poll_watch</code> 嵌入自己的设备对象中，以便在回调中通过 <code>structof()</code> 找回上层对象。该 watch 的内存必须一直存活到取消登记完成。</p><hr><h2 id="10-完整使用周期第-3-步：调用-io-poll-watch-登记-fd"><a href="#10-完整使用周期第-3-步：调用-io-poll-watch-登记-fd" class="headerlink" title="10. 完整使用周期第 3 步：调用 io_poll_watch() 登记 fd"></a>10. 完整使用周期第 3 步：调用 <code>io_poll_watch()</code> 登记 fd</h2><p>这是整个文件最关键的函数。</p><h3 id="10-1-函数入口和参数检查"><a href="#10-1-函数入口和参数检查" class="headerlink" title="10.1 函数入口和参数检查"></a>10.1 函数入口和参数检查</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span></span><br><span class="line"><span class="title function_">io_poll_watch</span><span class="params">(<span class="type">io_poll_t</span> *poll, <span class="type">int</span> fd, <span class="type">int</span> events,</span></span><br><span class="line"><span class="params">        <span class="keyword">struct</span> io_poll_watch *watch)</span></span><br><span class="line">&#123;</span><br><span class="line">    assert(poll);</span><br><span class="line">    assert(watch);</span><br><span class="line">    <span class="type">int</span> epfd = poll-&gt;epfd;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (fd == <span class="number">-1</span> || fd == epfd) &#123;</span><br><span class="line">        errno = EBADF;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (events &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        errno = EINVAL;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    events &amp;= IO_EVENT_MASK;</span><br></pre></td></tr></table></figure><p>逐项解释：</p><ol><li><code>fd == -1</code>：无效文件描述符；</li><li><code>fd == epfd</code>：不允许把当前 epoll 实例自身作为目标 fd；</li><li><code>events &lt; 0</code>：事件掩码不合法；</li><li><code>events &amp;= IO_EVENT_MASK</code>：只保留 Lely 支持的事件位，其他位被清除。<code>IO_EVENT_MASK</code> 的具体位值定义不在当前三个文件中。</li></ol><h3 id="10-2-加锁和红黑树查找"><a href="#10-2-加锁和红黑树查找" class="headerlink" title="10.2 加锁和红黑树查找"></a>10.2 加锁和红黑树查找</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">pthread_mutex_lock(&amp;poll-&gt;mtx);</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">rbnode</span> *<span class="title">node</span> =</span> rbtree_find(&amp;poll-&gt;tree, &amp;fd);</span><br><span class="line"><span class="keyword">if</span> (node &amp;&amp; node != &amp;watch-&gt;_node) &#123;</span><br><span class="line">    errsv = EEXIST;</span><br><span class="line">    <span class="keyword">goto</span> error;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>此时：</p><ul><li><code>node == NULL</code>：该 <code>fd</code> 从未登记，或者已经彻底取消登记；</li><li><code>node == &amp;watch-&gt;_node</code>：该 <code>fd</code> 已经由当前 watch 管理；</li><li><code>node != NULL &amp;&amp; node != &amp;watch-&gt;_node</code>：同一个 <code>fd</code> 已绑定另一个 watch，返回 <code>EEXIST</code>。</li></ul><h3 id="10-3-核心代码原文"><a href="#10-3-核心代码原文" class="headerlink" title="10.3 核心代码原文"></a>10.3 核心代码原文</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (events) &#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">epoll_event</span> <span class="title">event</span> =</span> EPOLL_EVENT_INIT(events, fd);</span><br><span class="line">    <span class="keyword">if</span> (node &amp;&amp; events != watch-&gt;_events) &#123;</span><br><span class="line">        <span class="keyword">if</span> (epoll_ctl(epfd, EPOLL_CTL_MOD, fd, &amp;event) == <span class="number">-1</span>) &#123;</span><br><span class="line">            errsv = errno;</span><br><span class="line">            epoll_ctl(epfd, EPOLL_CTL_DEL, fd, <span class="literal">NULL</span>);</span><br><span class="line">            rbtree_remove(&amp;poll-&gt;tree, node);</span><br><span class="line">            <span class="keyword">if</span> (watch-&gt;_events)</span><br><span class="line">                poll-&gt;nwatch--;</span><br><span class="line">            watch-&gt;_events = <span class="number">0</span>;</span><br><span class="line">            <span class="keyword">goto</span> error;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (!node) &#123;</span><br><span class="line">        <span class="keyword">if</span> (epoll_ctl(epfd, EPOLL_CTL_ADD, fd, &amp;event) == <span class="number">-1</span>) &#123;</span><br><span class="line">            errsv = errno;</span><br><span class="line">            <span class="keyword">goto</span> error;</span><br><span class="line">        &#125;</span><br><span class="line">        watch-&gt;_fd = fd;</span><br><span class="line">        rbnode_init(&amp;watch-&gt;_node, &amp;watch-&gt;_fd);</span><br><span class="line">        watch-&gt;_events = <span class="number">0</span>;</span><br><span class="line">        rbtree_insert(&amp;poll-&gt;tree, &amp;watch-&gt;_node);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (!watch-&gt;_events)</span><br><span class="line">        poll-&gt;nwatch++;</span><br><span class="line">    watch-&gt;_events = events;</span><br><span class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span> (node) &#123;</span><br><span class="line">    epoll_ctl(epfd, EPOLL_CTL_DEL, fd, <span class="literal">NULL</span>);</span><br><span class="line">    rbtree_remove(&amp;poll-&gt;tree, node);</span><br><span class="line">    <span class="keyword">if</span> (watch-&gt;_events)</span><br><span class="line">        poll-&gt;nwatch--;</span><br><span class="line">    watch-&gt;_events = <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="10-4-先看总决策树"><a href="#10-4-先看总决策树" class="headerlink" title="10.4 先看总决策树"></a>10.4 先看总决策树</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[进入 io_poll_watch] --&gt; B[校验 fd 和 events]</span><br><span class="line">    B --&gt; C[加锁并按 fd 查红黑树]</span><br><span class="line">    C --&gt; D&#123;同 fd 是否属于另一个 watch?&#125;</span><br><span class="line">    D -- 是 --&gt; X[返回 EEXIST]</span><br><span class="line">    D -- 否 --&gt; E&#123;events 是否非 0?&#125;</span><br><span class="line">    E -- 否 --&gt; F&#123;node 是否存在?&#125;</span><br><span class="line">    F -- 否 --&gt; G[什么也不做，返回成功]</span><br><span class="line">    F -- 是 --&gt; H[EPOLL_CTL_DEL]</span><br><span class="line">    H --&gt; I[红黑树删除]</span><br><span class="line">    I --&gt; J[必要时 nwatch--]</span><br><span class="line">    J --&gt; K[_events=0]</span><br><span class="line">    E -- 是 --&gt; L[构造 epoll_event]</span><br><span class="line">    L --&gt; M&#123;node 是否存在?&#125;</span><br><span class="line">    M -- 否 --&gt; N[EPOLL_CTL_ADD]</span><br><span class="line">    N --&gt; O[保存 fd并初始化 rbnode]</span><br><span class="line">    O --&gt; P[插入红黑树]</span><br><span class="line">    M -- 是 --&gt; Q&#123;events 与旧 _events 是否不同?&#125;</span><br><span class="line">    Q -- 是 --&gt; R[EPOLL_CTL_MOD]</span><br><span class="line">    Q -- 否 --&gt; S[内核配置不变]</span><br><span class="line">    R --&gt; T&#123;MOD 是否成功?&#125;</span><br><span class="line">    T -- 否 --&gt; U[DEL + 树删除 + 状态清理 + 返回错误]</span><br><span class="line">    T -- 是 --&gt; V[更新状态]</span><br><span class="line">    P --&gt; V</span><br><span class="line">    S --&gt; V</span><br><span class="line">    V --&gt; W&#123;旧 _events 是否为 0?&#125;</span><br><span class="line">    W -- 是 --&gt; Y[nwatch++]</span><br><span class="line">    W -- 否 --&gt; Z[nwatch 不变]</span><br><span class="line">    Y --&gt; AA[_events=events]</span><br><span class="line">    Z --&gt; AA</span><br></pre></td></tr></table></figure><hr><h2 id="11-io-poll-watch-分支一：首次登记，执行-EPOLL-CTL-ADD"><a href="#11-io-poll-watch-分支一：首次登记，执行-EPOLL-CTL-ADD" class="headerlink" title="11. io_poll_watch() 分支一：首次登记，执行 EPOLL_CTL_ADD"></a>11. <code>io_poll_watch()</code> 分支一：首次登记，执行 <code>EPOLL_CTL_ADD</code></h2><p>触发条件：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">events != 0</span><br><span class="line">node == NULL</span><br></pre></td></tr></table></figure><p>对应源码：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span> (!node) &#123;</span><br><span class="line">    <span class="keyword">if</span> (epoll_ctl(epfd, EPOLL_CTL_ADD, fd, &amp;event) == <span class="number">-1</span>) &#123;</span><br><span class="line">        errsv = errno;</span><br><span class="line">        <span class="keyword">goto</span> error;</span><br><span class="line">    &#125;</span><br><span class="line">    watch-&gt;_fd = fd;</span><br><span class="line">    rbnode_init(&amp;watch-&gt;_node, &amp;watch-&gt;_fd);</span><br><span class="line">    watch-&gt;_events = <span class="number">0</span>;</span><br><span class="line">    rbtree_insert(&amp;poll-&gt;tree, &amp;watch-&gt;_node);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">if</span> (!watch-&gt;_events)</span><br><span class="line">    poll-&gt;nwatch++;</span><br><span class="line">watch-&gt;_events = events;</span><br></pre></td></tr></table></figure><p>按时间顺序解释：</p><h3 id="11-1-构造内核事件描述"><a href="#11-1-构造内核事件描述" class="headerlink" title="11.1 构造内核事件描述"></a>11.1 构造内核事件描述</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">epoll_event</span> <span class="title">event</span> =</span> EPOLL_EVENT_INIT(events, fd);</span><br></pre></td></tr></table></figure><p>假设调用：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">io_poll_watch(poll, fd, IO_EVENT_IN, watch);</span><br></pre></td></tr></table></figure><p>则宏生成的事件近似为：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">event.events = EPOLLIN | EPOLLRDHUP | EPOLLONESHOT;</span><br><span class="line">event.data.fd = fd;</span><br></pre></td></tr></table></figure><h3 id="11-2-EPOLL-CTL-ADD"><a href="#11-2-EPOLL-CTL-ADD" class="headerlink" title="11.2 EPOLL_CTL_ADD"></a>11.2 <code>EPOLL_CTL_ADD</code></h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">epoll_ctl(epfd, EPOLL_CTL_ADD, fd, &amp;event);</span><br></pre></td></tr></table></figure><p>内核把该 <code>fd</code> 加入 <code>epfd</code> 的 interest list。</p><p>失败时：</p><ul><li>不修改 <code>watch-&gt;_fd</code>；</li><li>不插入红黑树；</li><li>不增加 <code>nwatch</code>；</li><li>返回 <code>-1</code>，<code>errno</code> 保留为 <code>epoll_ctl()</code> 的错误。</li></ul><h3 id="11-3-建立用户态关联"><a href="#11-3-建立用户态关联" class="headerlink" title="11.3 建立用户态关联"></a>11.3 建立用户态关联</h3><p>ADD 成功后：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">watch-&gt;_fd = fd;</span><br><span class="line">rbnode_init(&amp;watch-&gt;_node, &amp;watch-&gt;_fd);</span><br><span class="line">watch-&gt;_events = <span class="number">0</span>;</span><br><span class="line">rbtree_insert(&amp;poll-&gt;tree, &amp;watch-&gt;_node);</span><br></pre></td></tr></table></figure><p>作用：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">内核：epfd 已经管理 fd</span><br><span class="line">用户态：红黑树建立 fd → watch 映射</span><br></pre></td></tr></table></figure><h3 id="11-4-更新-nwatch-和-events"><a href="#11-4-更新-nwatch-和-events" class="headerlink" title="11.4 更新 nwatch 和 _events"></a>11.4 更新 <code>nwatch</code> 和 <code>_events</code></h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (!watch-&gt;_events)</span><br><span class="line">    poll-&gt;nwatch++;</span><br><span class="line">watch-&gt;_events = events;</span><br></pre></td></tr></table></figure><p>因为首次登记时 <code>_events</code> 被设为 0，所以 <code>nwatch++</code>。</p><p>完成后的状态：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">红黑树：有节点</span><br><span class="line">watch-&gt;_events：非 0</span><br><span class="line">nwatch：增加 1</span><br><span class="line">内核：当前一次监听有效，最多返回一次事件</span><br></pre></td></tr></table></figure><hr><h2 id="12-io-poll-watch-分支二：修改事件或恢复下一次监听，执行-EPOLL-CTL-MOD"><a href="#12-io-poll-watch-分支二：修改事件或恢复下一次监听，执行-EPOLL-CTL-MOD" class="headerlink" title="12. io_poll_watch() 分支二：修改事件或恢复下一次监听，执行 EPOLL_CTL_MOD"></a>12. <code>io_poll_watch()</code> 分支二：修改事件或恢复下一次监听，执行 <code>EPOLL_CTL_MOD</code></h2><p>触发条件：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">events != 0</span><br><span class="line">node != NULL</span><br><span class="line">events != watch-&gt;_events</span><br></pre></td></tr></table></figure><p>源码：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (node &amp;&amp; events != watch-&gt;_events) &#123;</span><br><span class="line">    <span class="keyword">if</span> (epoll_ctl(epfd, EPOLL_CTL_MOD, fd, &amp;event) == <span class="number">-1</span>) &#123;</span><br><span class="line">        ...</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这个分支包含两种不同场景。</p><h3 id="12-1-场景-A：真正修改关注事件"><a href="#12-1-场景-A：真正修改关注事件" class="headerlink" title="12.1 场景 A：真正修改关注事件"></a>12.1 场景 A：真正修改关注事件</h3><p>原来：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">watch-&gt;_events = IO_EVENT_IN</span><br></pre></td></tr></table></figure><p>现在调用：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">events = IO_EVENT_OUT</span><br></pre></td></tr></table></figure><p>因为不同，执行 MOD，把内核事件掩码从读侧关注改成写侧关注。</p><h3 id="12-2-场景-B：一次通知后继续监听"><a href="#12-2-场景-B：一次通知后继续监听" class="headerlink" title="12.2 场景 B：一次通知后继续监听"></a>12.2 场景 B：一次通知后继续监听</h3><p>事件处理前，<code>io_poll_process()</code> 会执行：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">watch-&gt;_events = <span class="number">0</span>;</span><br><span class="line">poll-&gt;nwatch--;</span><br></pre></td></tr></table></figure><p>回调若再次调用：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">io_poll_watch(poll, fd, IO_EVENT_IN, watch);</span><br></pre></td></tr></table></figure><p>此时：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">node 仍存在</span><br><span class="line">watch-&gt;_events == 0</span><br><span class="line">events == IO_EVENT_IN != 0</span><br></pre></td></tr></table></figure><p>所以必然进入 <code>EPOLL_CTL_MOD</code>。</p><p>这次 MOD 不只是“修改事件类型”，更重要的是让此前因 <code>EPOLLONESHOT</code> 暂停的内核登记项恢复下一次通知。</p><h3 id="12-3-MOD-成功后的计数"><a href="#12-3-MOD-成功后的计数" class="headerlink" title="12.3 MOD 成功后的计数"></a>12.3 MOD 成功后的计数</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (!watch-&gt;_events)</span><br><span class="line">    poll-&gt;nwatch++;</span><br><span class="line">watch-&gt;_events = events;</span><br></pre></td></tr></table></figure><ul><li>如果原 <code>_events == 0</code>：说明本次监听已消费，现在恢复监听，所以 <code>nwatch++</code>；</li><li>如果原 <code>_events != 0</code>：只是从一种有效事件配置改成另一种，数量没有变化。</li></ul><h3 id="12-4-MOD-失败时为什么做彻底清理"><a href="#12-4-MOD-失败时为什么做彻底清理" class="headerlink" title="12.4 MOD 失败时为什么做彻底清理"></a>12.4 MOD 失败时为什么做彻底清理</h3><p>源码：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (epoll_ctl(epfd, EPOLL_CTL_MOD, fd, &amp;event) == <span class="number">-1</span>) &#123;</span><br><span class="line">    errsv = errno;</span><br><span class="line">    epoll_ctl(epfd, EPOLL_CTL_DEL, fd, <span class="literal">NULL</span>);</span><br><span class="line">    rbtree_remove(&amp;poll-&gt;tree, node);</span><br><span class="line">    <span class="keyword">if</span> (watch-&gt;_events)</span><br><span class="line">        poll-&gt;nwatch--;</span><br><span class="line">    watch-&gt;_events = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">goto</span> error;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>执行顺序：</p><ol><li>保存 MOD 失败原因；</li><li>尝试从内核 epoll 中删除该 <code>fd</code>；</li><li>从红黑树删除关联；</li><li>若原来仍计入 <code>nwatch</code>，则减一；</li><li><code>_events = 0</code>；</li><li>返回失败。</li></ol><p>这是“失败后不保留半有效状态”的处理：MOD 失败后，代码不再假设内核状态仍可靠，而是把该 watch 从 <code>io_poll</code> 管理关系中移除。</p><p>需要注意：清理用的 <code>EPOLL_CTL_DEL</code> 返回值被忽略，最终 <code>errno</code> 仍报告最初的 MOD 失败原因。</p><hr><h2 id="13-io-poll-watch-分支三：重复传入相同事件"><a href="#13-io-poll-watch-分支三：重复传入相同事件" class="headerlink" title="13. io_poll_watch() 分支三：重复传入相同事件"></a>13. <code>io_poll_watch()</code> 分支三：重复传入相同事件</h2><p>触发条件：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">events != 0</span><br><span class="line">node != NULL</span><br><span class="line">events == watch-&gt;_events</span><br></pre></td></tr></table></figure><p>这时：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (node &amp;&amp; events != watch-&gt;_events) &#123;</span><br><span class="line">    ...</span><br><span class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span> (!node) &#123;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>两个分支都不执行，因此不会调用 <code>epoll_ctl()</code>。</p><p>随后：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (!watch-&gt;_events)</span><br><span class="line">    poll-&gt;nwatch++;</span><br><span class="line">watch-&gt;_events = events;</span><br></pre></td></tr></table></figure><p>由于 <code>_events</code> 原本与 <code>events</code> 相同且非 0，<code>nwatch</code> 不变。</p><p>实际效果：这是一个幂等调用，不重复修改内核登记。</p><p>注意：在“本次监听已消费”的状态下 <code>_events == 0</code>，而传入 <code>events != 0</code>，不可能进入此分支；它一定会进入 MOD 以恢复监听。</p><hr><h2 id="14-io-poll-watch-分支四：events-0，彻底取消登记"><a href="#14-io-poll-watch-分支四：events-0，彻底取消登记" class="headerlink" title="14. io_poll_watch() 分支四：events == 0，彻底取消登记"></a>14. <code>io_poll_watch()</code> 分支四：<code>events == 0</code>，彻底取消登记</h2><p>源码：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span> (node) &#123;</span><br><span class="line">    epoll_ctl(epfd, EPOLL_CTL_DEL, fd, <span class="literal">NULL</span>);</span><br><span class="line">    rbtree_remove(&amp;poll-&gt;tree, node);</span><br><span class="line">    <span class="keyword">if</span> (watch-&gt;_events)</span><br><span class="line">        poll-&gt;nwatch--;</span><br><span class="line">    watch-&gt;_events = <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>调用形式：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">io_poll_watch(poll, fd, <span class="number">0</span>, watch);</span><br></pre></td></tr></table></figure><h3 id="14-1-EPOLL-CTL-DEL"><a href="#14-1-EPOLL-CTL-DEL" class="headerlink" title="14.1 EPOLL_CTL_DEL"></a>14.1 <code>EPOLL_CTL_DEL</code></h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">epoll_ctl(epfd, EPOLL_CTL_DEL, fd, <span class="literal">NULL</span>);</span><br></pre></td></tr></table></figure><p>从内核 epoll interest list 中删除该 <code>fd</code>。</p><p>对 DEL 操作，<code>event</code> 参数不需要内容，因此传 <code>NULL</code>。</p><h3 id="14-2-从红黑树删除"><a href="#14-2-从红黑树删除" class="headerlink" title="14.2 从红黑树删除"></a>14.2 从红黑树删除</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">rbtree_remove(&amp;poll-&gt;tree, node);</span><br></pre></td></tr></table></figure><p>删除后，后续内核即使返回一个已经进入事件数组的陈旧 <code>fd</code>，分发阶段也无法在树中找到 watch，因此会忽略。</p><h3 id="14-3-nwatch-为什么有条件递减"><a href="#14-3-nwatch-为什么有条件递减" class="headerlink" title="14.3 nwatch 为什么有条件递减"></a>14.3 <code>nwatch</code> 为什么有条件递减</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (watch-&gt;_events)</span><br><span class="line">    poll-&gt;nwatch--;</span><br></pre></td></tr></table></figure><p>两种情况：</p><table><thead><tr><th>取消前状态</th><th align="right"><code>_events</code></th><th>是否已计入 <code>nwatch</code></th><th>是否递减</th></tr></thead><tbody><tr><td>当前一次监听仍有效</td><td align="right">非 0</td><td>是</td><td>是</td></tr><tr><td>本次一次性监听已经消费</td><td align="right">0</td><td>否，事件分发时已经减过</td><td>否</td></tr></tbody></table><h3 id="14-4-DEL-失败的源码行为"><a href="#14-4-DEL-失败的源码行为" class="headerlink" title="14.4 DEL 失败的源码行为"></a>14.4 DEL 失败的源码行为</h3><p>这里源码没有检查 <code>epoll_ctl(... DEL ...)</code> 的返回值，而是继续删除用户态红黑树节点，并最终返回成功。</p><p>这是源码客观行为。它意味着取消登记时以“清理用户态关联”为优先；但如果要诊断内核 DEL 失败，这个函数不会把该错误返回给调用方。</p><h3 id="14-5-events-0-node-NULL"><a href="#14-5-events-0-node-NULL" class="headerlink" title="14.5 events == 0 &amp;&amp; node == NULL"></a>14.5 <code>events == 0 &amp;&amp; node == NULL</code></h3><p>函数不做任何操作并返回成功，相当于允许重复取消一个已经不存在的登记。</p><hr><h2 id="15-io-poll-watch-的全部场景汇总"><a href="#15-io-poll-watch-的全部场景汇总" class="headerlink" title="15. io_poll_watch() 的全部场景汇总"></a>15. <code>io_poll_watch()</code> 的全部场景汇总</h2><table><thead><tr><th>红黑树 <code>node</code></th><th align="right">传入 <code>events</code></th><th>与旧 <code>_events</code> 关系</th><th>epoll 操作</th><th>结果</th></tr></thead><tbody><tr><td>无</td><td align="right">非 0</td><td>不适用</td><td>ADD</td><td>首次建立监听</td></tr><tr><td>有，属于当前 watch</td><td align="right">非 0</td><td>不同</td><td>MOD</td><td>修改事件或恢复下一次监听</td></tr><tr><td>有，属于当前 watch</td><td align="right">非 0</td><td>相同</td><td>无</td><td>幂等，保持现状</td></tr><tr><td>有，属于其他 watch</td><td align="right">任意</td><td>不适用</td><td>无</td><td><code>EEXIST</code></td></tr><tr><td>有</td><td align="right">0</td><td>不适用</td><td>DEL</td><td>彻底取消登记并删除树节点</td></tr><tr><td>无</td><td align="right">0</td><td>不适用</td><td>无</td><td>重复取消，返回成功</td></tr></tbody></table><p><code>errno</code> 处理还应注意：函数入口先保存原 <code>errno</code>，成功时会恢复原值。因此调用方只能在返回 <code>-1</code> 时读取 <code>errno</code>，不能因为成功后 <code>errno</code> 非 0 就判断失败。</p><hr><h2 id="16-完整使用周期第-4-步：io-poll-poll-wait-等待事件"><a href="#16-完整使用周期第-4-步：io-poll-poll-wait-等待事件" class="headerlink" title="16. 完整使用周期第 4 步：io_poll_poll_wait() 等待事件"></a>16. 完整使用周期第 4 步：<code>io_poll_poll_wait()</code> 等待事件</h2><p>上层通过 <code>ev_poll_wait()</code> 最终进入：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">static</span> <span class="type">int</span> <span class="title function_">io_poll_poll_wait</span><span class="params">(<span class="type">ev_poll_t</span> *poll_, <span class="type">int</span> timeout)</span></span><br></pre></td></tr></table></figure><h3 id="16-1-获取当前线程状态"><a href="#16-1-获取当前线程状态" class="headerlink" title="16.1 获取当前线程状态"></a>16.1 获取当前线程状态</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">void</span> *thr_ = io_poll_poll_self(poll_);</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">io_poll_thrd</span> *<span class="title">thr</span> =</span> (<span class="keyword">struct</span> io_poll_thrd *)thr_;</span><br></pre></td></tr></table></figure><p><code>io_poll_poll_self()</code> 为每个线程维护一个 <code>_Thread_local</code> 对象：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">io_poll_thrd</span> &#123;</span></span><br><span class="line">    <span class="type">int</span> stopped;</span><br><span class="line">    <span class="type">pthread_t</span> *thread;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>作用：</p><ul><li>保存实际 <code>pthread_t</code>，供其他线程定向发送唤醒信号；</li><li><code>stopped</code> 表示当前这次 <code>wait()</code> 已经被要求停止阻塞、进入非阻塞收尾。</li></ul><h3 id="16-2-调用-epoll-pwait-前释放互斥锁"><a href="#16-2-调用-epoll-pwait-前释放互斥锁" class="headerlink" title="16.2 调用 epoll_pwait() 前释放互斥锁"></a>16.2 调用 <code>epoll_pwait()</code> 前释放互斥锁</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">pthread_mutex_unlock(&amp;poll-&gt;mtx);</span><br><span class="line"><span class="type">int</span> nevents = epoll_pwait(...);</span><br></pre></td></tr></table></figure><p>不能持锁阻塞等待，否则其他线程无法：</p><ul><li>调用 <code>io_poll_watch()</code> ADD&#x2F;MOD&#x2F;DEL；</li><li>调用 <code>io_poll_poll_kill()</code> 修改停止状态；</li><li>更新内部元数据。</li></ul><h3 id="16-3-timeout-语义"><a href="#16-3-timeout-语义" class="headerlink" title="16.3 timeout 语义"></a>16.3 <code>timeout</code> 语义</h3><table><thead><tr><th align="right"><code>timeout</code></th><th>行为</th></tr></thead><tbody><tr><td align="right"><code>-1</code></td><td>无限等待，直到 I&#x2F;O 或信号发生</td></tr><tr><td align="right"><code>0</code></td><td>非阻塞查询，立即返回</td></tr><tr><td align="right"><code>&gt; 0</code></td><td>最多等待指定毫秒数</td></tr></tbody></table><p>源码在 <code>timeout == 0</code> 时先设置：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">thr-&gt;stopped = <span class="number">1</span>;</span><br></pre></td></tr></table></figure><p>从而本次只做非阻塞事件收集，不进入后续阻塞循环。</p><h3 id="16-4-EINTR-分支"><a href="#16-4-EINTR-分支" class="headerlink" title="16.4 EINTR 分支"></a>16.4 <code>EINTR</code> 分支</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (nevents == <span class="number">-1</span> &amp;&amp; errno == EINTR) &#123;</span><br><span class="line">    sigaddset(&amp;<span class="built_in">set</span>, poll-&gt;signo);</span><br><span class="line">    pthread_mutex_lock(&amp;poll-&gt;mtx);</span><br><span class="line">    thr-&gt;stopped = <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">continue</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>含义：</p><ol><li>等待被信号中断；</li><li>本次 <code>epoll_pwait()</code> 没有返回 I&#x2F;O 事件；</li><li>下一轮把唤醒信号加入临时阻塞集合；</li><li>设置 <code>stopped=1</code>，使下一轮 <code>timeout=0</code>；</li><li>再进行一次非阻塞 I&#x2F;O 检查。</li></ol><p>这样可以避免大量唤醒信号持续打断 wait，导致真正已经就绪的 I&#x2F;O 长期得不到处理。</p><hr><h2 id="17-完整使用周期第-5-步：把-Linux-事件转换回-Lely-事件"><a href="#17-完整使用周期第-5-步：把-Linux-事件转换回-Lely-事件" class="headerlink" title="17. 完整使用周期第 5 步：把 Linux 事件转换回 Lely 事件"></a>17. 完整使用周期第 5 步：把 Linux 事件转换回 Lely 事件</h2><p>内核返回 <code>struct epoll_event events[]</code> 后，源码逐项转换：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> revents = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">if</span> (events[i].events &amp; (EPOLLIN | EPOLLRDHUP))</span><br><span class="line">    revents |= IO_EVENT_IN;</span><br><span class="line"><span class="keyword">if</span> (events[i].events &amp; EPOLLPRI)</span><br><span class="line">    revents |= IO_EVENT_PRI;</span><br><span class="line"><span class="keyword">if</span> (events[i].events &amp; EPOLLOUT)</span><br><span class="line">    revents |= IO_EVENT_OUT;</span><br><span class="line"><span class="keyword">if</span> (events[i].events &amp; EPOLLERR)</span><br><span class="line">    revents |= IO_EVENT_ERR;</span><br><span class="line"><span class="keyword">if</span> (events[i].events &amp; EPOLLHUP)</span><br><span class="line">    revents |= IO_EVENT_HUP;</span><br></pre></td></tr></table></figure><p>映射表：</p><table><thead><tr><th>Linux 返回事件</th><th>Lely 回调事件</th></tr></thead><tbody><tr><td><code>EPOLLIN</code></td><td><code>IO_EVENT_IN</code></td></tr><tr><td><code>EPOLLRDHUP</code></td><td><code>IO_EVENT_IN</code></td></tr><tr><td><code>EPOLLPRI</code></td><td><code>IO_EVENT_PRI</code></td></tr><tr><td><code>EPOLLOUT</code></td><td><code>IO_EVENT_OUT</code></td></tr><tr><td><code>EPOLLERR</code></td><td><code>IO_EVENT_ERR</code></td></tr><tr><td><code>EPOLLHUP</code></td><td><code>IO_EVENT_HUP</code></td></tr></tbody></table><p>多个事件可以同时存在。例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">EPOLLIN | EPOLLHUP</span><br><span class="line">    ↓</span><br><span class="line">IO_EVENT_IN | IO_EVENT_HUP</span><br></pre></td></tr></table></figure><p>回调不能假设 <code>events</code> 只有一个位。</p><hr><h2 id="18-完整使用周期第-6-步：按-fd-查红黑树并找到-watch"><a href="#18-完整使用周期第-6-步：按-fd-查红黑树并找到-watch" class="headerlink" title="18. 完整使用周期第 6 步：按 fd 查红黑树并找到 watch"></a>18. 完整使用周期第 6 步：按 fd 查红黑树并找到 watch</h2><p>源码：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> fd = events[i].data.fd;</span><br><span class="line">pthread_mutex_lock(&amp;poll-&gt;mtx);</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">rbnode</span> *<span class="title">node</span> =</span> rbtree_find(&amp;poll-&gt;tree, &amp;fd);</span><br><span class="line"><span class="keyword">if</span> (node) &#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">io_poll_watch</span> *<span class="title">watch</span> =</span> io_poll_watch_from_node(node);</span><br><span class="line">    io_poll_process(poll, revents, watch);</span><br><span class="line">    n += n &lt; INT_MAX;</span><br><span class="line">&#125;</span><br><span class="line">pthread_mutex_unlock(&amp;poll-&gt;mtx);</span><br></pre></td></tr></table></figure><p>流程：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">内核返回 event.data.fd</span><br><span class="line">  ↓</span><br><span class="line">rbtree_find(tree, &amp;fd)</span><br><span class="line">  ↓</span><br><span class="line">取得 rbnode</span><br><span class="line">  ↓</span><br><span class="line">structof(node, io_poll_watch, _node)</span><br><span class="line">  ↓</span><br><span class="line">取得 watch</span><br></pre></td></tr></table></figure><p><code>io_poll_watch_from_node()</code>：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">return</span> structof(node, <span class="keyword">struct</span> io_poll_watch, _node);</span><br></pre></td></tr></table></figure><p>因为 <code>_node</code> 是嵌入在 <code>io_poll_watch</code> 内部的成员，<code>structof()</code> 根据成员地址反推出外层结构体地址。</p><p>如果 <code>node == NULL</code>，事件被忽略。常见原因是该 <code>fd</code> 在事件进入内核返回数组后，被另一个线程取消登记并从红黑树移除。</p><hr><h2 id="19-完整使用周期第-7-步：io-poll-process-调用回调"><a href="#19-完整使用周期第-7-步：io-poll-process-调用回调" class="headerlink" title="19. 完整使用周期第 7 步：io_poll_process() 调用回调"></a>19. 完整使用周期第 7 步：<code>io_poll_process()</code> 调用回调</h2><p>源码：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">static</span> <span class="type">void</span></span><br><span class="line"><span class="title function_">io_poll_process</span><span class="params">(<span class="type">io_poll_t</span> *poll, <span class="type">int</span> revents,</span></span><br><span class="line"><span class="params">        <span class="keyword">struct</span> io_poll_watch *watch)</span></span><br><span class="line">&#123;</span><br><span class="line">    assert(poll);</span><br><span class="line">    assert(poll-&gt;nwatch);</span><br><span class="line">    assert(watch);</span><br><span class="line">    assert(watch-&gt;_events);</span><br><span class="line"></span><br><span class="line">    watch-&gt;_events = <span class="number">0</span>;</span><br><span class="line">    poll-&gt;nwatch--;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (watch-&gt;func) &#123;</span><br><span class="line">        pthread_mutex_unlock(&amp;poll-&gt;mtx);</span><br><span class="line">        watch-&gt;func(watch, revents);</span><br><span class="line">        pthread_mutex_lock(&amp;poll-&gt;mtx);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="19-1-为什么先-events-0"><a href="#19-1-为什么先-events-0" class="headerlink" title="19.1 为什么先 _events = 0"></a>19.1 为什么先 <code>_events = 0</code></h3><p>这表示：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">该 fd 的本次 EPOLLONESHOT 通知已经被消费</span><br><span class="line">当前不再认为它能继续产生下一次回调</span><br></pre></td></tr></table></figure><p>它与内核行为一致：内核已经因 <code>EPOLLONESHOT</code> 暂停该登记项。</p><h3 id="19-2-为什么-nwatch"><a href="#19-2-为什么-nwatch" class="headerlink" title="19.2 为什么 nwatch--"></a>19.2 为什么 <code>nwatch--</code></h3><p><code>nwatch</code> 统计的是当前仍有一次有效监听的 watch 数量。事件已经返回并准备分发，本次一次性监听已经结束，所以减一。</p><h3 id="19-3-为什么回调前释放锁"><a href="#19-3-为什么回调前释放锁" class="headerlink" title="19.3 为什么回调前释放锁"></a>19.3 为什么回调前释放锁</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">pthread_mutex_unlock(&amp;poll-&gt;mtx);</span><br><span class="line">watch-&gt;func(watch, revents);</span><br><span class="line">pthread_mutex_lock(&amp;poll-&gt;mtx);</span><br></pre></td></tr></table></figure><p>回调可能需要：</p><ul><li>再次调用 <code>io_poll_watch()</code> 恢复监听；</li><li>调用 <code>io_poll_watch(..., 0, ...)</code> 取消登记；</li><li>登记其他 fd；</li><li>读取或写入设备；</li><li>投递任务到 executor。</li></ul><p>如果带锁调用回调，回调再次进入 <code>io_poll_watch()</code> 会尝试锁同一把 mutex，造成自锁；复杂回调还会长时间阻塞其他 I&#x2F;O 管理操作。</p><hr><h2 id="20-完整使用周期第-8-步：回调中处理-fd，并决定是否继续"><a href="#20-完整使用周期第-8-步：回调中处理-fd，并决定是否继续" class="headerlink" title="20. 完整使用周期第 8 步：回调中处理 fd，并决定是否继续"></a>20. 完整使用周期第 8 步：回调中处理 fd，并决定是否继续</h2><p>一个典型读事件回调的逻辑应是：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">static</span> <span class="type">void</span></span><br><span class="line"><span class="title function_">on_read</span><span class="params">(<span class="keyword">struct</span> io_poll_watch *watch, <span class="type">int</span> events)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">my_device</span> *<span class="title">dev</span> =</span> structof(watch, <span class="keyword">struct</span> my_device, watch);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (events &amp; IO_EVENT_IN) &#123;</span><br><span class="line">        <span class="comment">// 调用 read()/recv()，消费当前可读条件。</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (events &amp; (IO_EVENT_ERR | IO_EVENT_HUP)) &#123;</span><br><span class="line">        <span class="comment">// 查询具体错误，决定关闭还是继续。</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (dev-&gt;continue_reading) &#123;</span><br><span class="line">        io_poll_watch(dev-&gt;poll, dev-&gt;fd, IO_EVENT_IN, &amp;dev-&gt;watch);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        io_poll_watch(dev-&gt;poll, dev-&gt;fd, <span class="number">0</span>, &amp;dev-&gt;watch);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这段是示意代码，具体错误处理取决于设备类型。</p><h3 id="20-1-继续监听"><a href="#20-1-继续监听" class="headerlink" title="20.1 继续监听"></a>20.1 继续监听</h3><p>再次调用：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">io_poll_watch(poll, fd, IO_EVENT_IN, watch);</span><br></pre></td></tr></table></figure><p>此时红黑树节点仍在，但 <code>_events == 0</code>，因此进入 MOD 分支：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">EPOLL_CTL_MOD</span><br><span class="line">  ↓</span><br><span class="line">nwatch++</span><br><span class="line">  ↓</span><br><span class="line">watch-&gt;_events = IO_EVENT_IN</span><br><span class="line">  ↓</span><br><span class="line">当前下一次监听重新生效</span><br></pre></td></tr></table></figure><h3 id="20-2-不再监听"><a href="#20-2-不再监听" class="headerlink" title="20.2 不再监听"></a>20.2 不再监听</h3><p>调用：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">io_poll_watch(poll, fd, <span class="number">0</span>, watch);</span><br></pre></td></tr></table></figure><p>进入 DEL 分支，彻底删除内核登记和红黑树节点。</p><h3 id="20-3-不做任何调用"><a href="#20-3-不做任何调用" class="headerlink" title="20.3 不做任何调用"></a>20.3 不做任何调用</h3><p>如果回调返回前既没有 MOD，也没有 DEL：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">红黑树节点仍存在</span><br><span class="line">watch-&gt;_events == 0</span><br><span class="line">内核 EPOLLONESHOT 登记项暂停</span><br><span class="line">后续不会继续收到该 fd 的事件</span><br></pre></td></tr></table></figure><p>这通常是“只收到一次，后续再也没有”的直接原因。</p><hr><h2 id="21-一个-SocketCAN-可读事件的完整时序示例"><a href="#21-一个-SocketCAN-可读事件的完整时序示例" class="headerlink" title="21. 一个 SocketCAN 可读事件的完整时序示例"></a>21. 一个 SocketCAN 可读事件的完整时序示例</h2><p>以下只描述 Poll 层，不代表完整 CANopen 协议调用链：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant App as CAN 设备适配层</span><br><span class="line">    participant Poll as io_poll</span><br><span class="line">    participant Tree as 红黑树</span><br><span class="line">    participant Kernel as Linux epoll</span><br><span class="line">    participant CB as watch 回调</span><br><span class="line"></span><br><span class="line">    App-&gt;&gt;Poll: io_poll_watch(can_fd, IO_EVENT_IN, watch)</span><br><span class="line">    Poll-&gt;&gt;Kernel: EPOLL_CTL_ADD&lt;br/&gt;EPOLLIN|EPOLLRDHUP|EPOLLONESHOT</span><br><span class="line">    Poll-&gt;&gt;Tree: 插入 can_fd → watch</span><br><span class="line">    Poll-&gt;&gt;Poll: nwatch++, _events=IO_EVENT_IN</span><br><span class="line"></span><br><span class="line">    App-&gt;&gt;Poll: ev_poll_wait(timeout)</span><br><span class="line">    Poll-&gt;&gt;Kernel: epoll_pwait()</span><br><span class="line">    Kernel--&gt;&gt;Poll: data.fd=can_fd, events=EPOLLIN</span><br><span class="line">    Poll-&gt;&gt;Tree: rbtree_find(can_fd)</span><br><span class="line">    Tree--&gt;&gt;Poll: watch</span><br><span class="line">    Poll-&gt;&gt;Poll: _events=0, nwatch--</span><br><span class="line">    Poll-&gt;&gt;CB: watch-&gt;&gt;func(watch, IO_EVENT_IN)</span><br><span class="line">    CB-&gt;&gt;CB: recv/read struct can_frame</span><br><span class="line">    CB-&gt;&gt;Poll: io_poll_watch(can_fd, IO_EVENT_IN, watch)</span><br><span class="line">    Poll-&gt;&gt;Kernel: EPOLL_CTL_MOD&lt;br/&gt;恢复下一次通知</span><br><span class="line">    Poll-&gt;&gt;Poll: nwatch++, _events=IO_EVENT_IN</span><br></pre></td></tr></table></figure><p>Poll 层只知道 <code>can_fd</code> 可读。CAN 帧解析、COB-ID 分类、NMT&#x2F;SDO&#x2F;PDO&#x2F;Heartbeat 状态机均由更上层代码完成。</p><hr><h2 id="22-nwatch-的准确含义"><a href="#22-nwatch-的准确含义" class="headerlink" title="22. nwatch 的准确含义"></a>22. <code>nwatch</code> 的准确含义</h2><p><code>nwatch</code> 不是：</p><ul><li>红黑树节点数量；</li><li>已经处理过的事件数量；</li><li>当前 epoll ready list 数量；</li><li>所有历史登记数量。</li></ul><p>它表示：</p><blockquote><p>当前在 Lely 状态中仍有一次有效监听、理论上可以产生下一次通知的 watch 数量。</p></blockquote><p>状态变化：</p><table><thead><tr><th>操作</th><th><code>_events</code> 变化</th><th align="right"><code>nwatch</code> 变化</th></tr></thead><tbody><tr><td>首次 ADD</td><td><code>0 → 非0</code></td><td align="right"><code>+1</code></td></tr><tr><td>有效状态下修改事件</td><td><code>非0 → 非0</code></td><td align="right">0</td></tr><tr><td>事件分发</td><td><code>非0 → 0</code></td><td align="right"><code>-1</code></td></tr><tr><td>回调后 MOD 恢复监听</td><td><code>0 → 非0</code></td><td align="right"><code>+1</code></td></tr><tr><td>有效状态下 DEL</td><td><code>非0 → 0</code></td><td align="right"><code>-1</code></td></tr><tr><td>已消费状态下 DEL</td><td><code>0 → 0</code></td><td align="right">0</td></tr></tbody></table><p>因此可能出现：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">红黑树节点数 &gt; nwatch</span><br></pre></td></tr></table></figure><p>原因是树中可能保留“本次监听已消费但尚未恢复或删除”的 watch。</p><hr><h2 id="23-io-poll-poll-wait-为什么可能连续调用多次-epoll-pwait"><a href="#23-io-poll-poll-wait-为什么可能连续调用多次-epoll-pwait" class="headerlink" title="23. io_poll_poll_wait() 为什么可能连续调用多次 epoll_pwait()"></a>23. <code>io_poll_poll_wait()</code> 为什么可能连续调用多次 <code>epoll_pwait()</code></h2><p>每轮完成后：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">thr-&gt;stopped = <span class="number">1</span>;</span><br><span class="line">stopped = nevents != LELY_IO_EPOLL_MAXEVENTS;</span><br></pre></td></tr></table></figure><p>如果本次返回数量刚好等于数组容量：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">nevents == LELY_IO_EPOLL_MAXEVENTS</span><br></pre></td></tr></table></figure><p>说明内核 ready list 可能还有更多事件未能装入本次数组。代码会再次循环；因为 <code>thr-&gt;stopped == 1</code>，下一轮把 <code>timeout</code> 改为 0，只做非阻塞收集。</p><p>所以一次 <code>wait()</code> 的策略是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">最多阻塞一次</span><br><span class="line">  +</span><br><span class="line">尽量排空当时已经就绪的事件</span><br></pre></td></tr></table></figure><p>它不会每处理一批事件后再次按原始 timeout 阻塞。</p><hr><h2 id="24-跨线程唤醒：self-、kill-和信号"><a href="#24-跨线程唤醒：self-、kill-和信号" class="headerlink" title="24. 跨线程唤醒：self()、kill() 和信号"></a>24. 跨线程唤醒：<code>self()</code>、<code>kill()</code> 和信号</h2><p>这部分不是正常 I&#x2F;O 周期的主线，但解释了 executor 有新任务时如何让 poll 线程退出阻塞。</p><h3 id="24-1-io-poll-poll-self"><a href="#24-1-io-poll-poll-self" class="headerlink" title="24.1 io_poll_poll_self()"></a>24.1 <code>io_poll_poll_self()</code></h3><p>为当前线程返回一个线程局部令牌：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">static</span> <span class="keyword">_Thread_local</span> <span class="class"><span class="keyword">struct</span> <span class="title">io_poll_thrd</span> <span class="title">thr</span> =</span> &#123; <span class="number">0</span>, <span class="literal">NULL</span> &#125;;</span><br></pre></td></tr></table></figure><p>第一次调用时保存：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">thread = pthread_self();</span><br><span class="line">thr.thread = &amp;thread;</span><br></pre></td></tr></table></figure><h3 id="24-2-io-poll-poll-kill-不是杀死线程"><a href="#24-2-io-poll-poll-kill-不是杀死线程" class="headerlink" title="24.2 io_poll_poll_kill() 不是杀死线程"></a>24.2 <code>io_poll_poll_kill()</code> 不是杀死线程</h3><p>它的真实语义是：</p><blockquote><p>请求目标线程结束当前这一次阻塞等待，完成一次非阻塞收尾后从 <code>wait()</code> 返回。</p></blockquote><p>流程：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">检查目标是否为当前线程</span><br><span class="line">  ↓</span><br><span class="line">加锁读取 target-&gt;stopped</span><br><span class="line">  ↓</span><br><span class="line">若尚未停止，设置 stopped=1</span><br><span class="line">  ↓</span><br><span class="line">解锁</span><br><span class="line">  ↓</span><br><span class="line">pthread_kill(target, poll-&gt;signo)</span><br><span class="line">  ↓</span><br><span class="line">目标线程的 epoll_pwait() 返回 -1/EINTR</span><br><span class="line">  ↓</span><br><span class="line">目标线程再做一次 timeout=0 的 I/O 检查</span><br><span class="line">  ↓</span><br><span class="line">wait() 返回</span><br></pre></td></tr></table></figure><h3 id="24-3-为什么既要-stopped-又要信号"><a href="#24-3-为什么既要-stopped-又要信号" class="headerlink" title="24.3 为什么既要 stopped 又要信号"></a>24.3 为什么既要 <code>stopped</code> 又要信号</h3><table><thead><tr><th>机制</th><th>单独使用的问题</th></tr></thead><tbody><tr><td>只设置 <code>stopped</code></td><td>目标线程若已经阻塞在内核中，不能及时看到变量变化</td></tr><tr><td>只发送信号</td><td>线程醒来后没有持久状态表明应该停止，可能再次阻塞</td></tr><tr><td>两者组合</td><td>状态表达停止意图，信号负责打断阻塞</td></tr></tbody></table><hr><h2 id="25-fork-后恢复：io-poll-svc-notify-fork"><a href="#25-fork-后恢复：io-poll-svc-notify-fork" class="headerlink" title="25. fork 后恢复：io_poll_svc_notify_fork()"></a>25. fork 后恢复：<code>io_poll_svc_notify_fork()</code></h2><p>收到 <code>IO_FORK_CHILD</code> 时：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">关闭旧 epfd</span><br><span class="line">  ↓</span><br><span class="line">创建新 epfd</span><br><span class="line">  ↓</span><br><span class="line">遍历红黑树</span><br><span class="line">  ↓</span><br><span class="line">_events != 0：重新 EPOLL_CTL_ADD</span><br><span class="line">_events == 0：从树中移除</span><br><span class="line">ADD 失败：从树中移除并返回错误</span><br></pre></td></tr></table></figure><p>源码中的判断：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (events) &#123;</span><br><span class="line">    ... EPOLL_CTL_ADD ...</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    rbtree_remove(&amp;poll-&gt;tree, node);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这再次证明 <code>_events == 0</code> 的节点只是暂存关联，不能视为当前有效监听。</p><p>注意：当前源码在 fork 恢复时删除节点，但没有同步调整 <code>nwatch</code>。本文只陈述代码行为，不对更高层 fork 调用约束作额外假设。</p><hr><h2 id="26-完整使用周期第-9-步：销毁"><a href="#26-完整使用周期第-9-步：销毁" class="headerlink" title="26. 完整使用周期第 9 步：销毁"></a>26. 完整使用周期第 9 步：销毁</h2><h3 id="26-1-C-析构"><a href="#26-1-C-析构" class="headerlink" title="26.1 C++ 析构"></a>26.1 C++ 析构</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">~<span class="built_in">Poll</span>() &#123; <span class="built_in">io_poll_destroy</span>(*<span class="keyword">this</span>); &#125;</span><br></pre></td></tr></table></figure><h3 id="26-2-io-poll-destroy"><a href="#26-2-io-poll-destroy" class="headerlink" title="26.2 io_poll_destroy()"></a>26.2 <code>io_poll_destroy()</code></h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (poll) &#123;</span><br><span class="line">    io_poll_fini(poll);</span><br><span class="line">    io_poll_free(poll);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="26-3-io-poll-fini"><a href="#26-3-io-poll-fini" class="headerlink" title="26.3 io_poll_fini()"></a>26.3 <code>io_poll_fini()</code></h3><p>执行顺序：</p><ol><li>从 <code>io_ctx</code> 移除服务；</li><li>关闭 <code>epfd</code>；</li><li>销毁 mutex；</li><li>清除可能残留的已阻塞唤醒信号；</li><li>恢复原线程信号掩码；</li><li>恢复原信号处理函数。</li></ol><p>调用方应在销毁 Poll 前，确保没有其他线程仍在使用它，也没有回调正在访问其内部对象。</p><hr><h2 id="27-按函数理解整个文件"><a href="#27-按函数理解整个文件" class="headerlink" title="27. 按函数理解整个文件"></a>27. 按函数理解整个文件</h2><h3 id="27-1-生命周期函数"><a href="#27-1-生命周期函数" class="headerlink" title="27.1 生命周期函数"></a>27.1 生命周期函数</h3><table><thead><tr><th>函数</th><th>作用</th></tr></thead><tbody><tr><td><code>io_poll_alloc()</code></td><td>分配 <code>io_poll_t</code> 内存</td></tr><tr><td><code>io_poll_free()</code></td><td>释放内存</td></tr><tr><td><code>io_poll_init()</code></td><td>初始化信号、mutex、红黑树、epoll 和 context 注册</td></tr><tr><td><code>io_poll_fini()</code></td><td>逆向清理初始化资源</td></tr><tr><td><code>io_poll_create()</code></td><td>alloc + init</td></tr><tr><td><code>io_poll_destroy()</code></td><td>fini + free</td></tr><tr><td><code>io_poll_open()</code></td><td>关闭旧 epfd 后创建新 epoll 实例</td></tr><tr><td><code>io_poll_close()</code></td><td>关闭 epfd，并先将成员设为 <code>-1</code></td></tr></tbody></table><h3 id="27-2-对外访问函数"><a href="#27-2-对外访问函数" class="headerlink" title="27.2 对外访问函数"></a>27.2 对外访问函数</h3><table><thead><tr><th>函数</th><th>作用</th></tr></thead><tbody><tr><td><code>io_poll_get_ctx()</code></td><td>返回所属 <code>io_ctx_t*</code></td></tr><tr><td><code>io_poll_get_poll()</code></td><td>返回通用 <code>ev_poll_t*</code> 接口入口</td></tr><tr><td><code>io_poll_watch()</code></td><td>ADD&#x2F;MOD&#x2F;DEL 一个 fd，并维护红黑树和 <code>_events/nwatch</code></td></tr></tbody></table><h3 id="27-3-ev-poll-后端函数"><a href="#27-3-ev-poll-后端函数" class="headerlink" title="27.3 ev_poll 后端函数"></a>27.3 <code>ev_poll</code> 后端函数</h3><table><thead><tr><th>函数</th><th>作用</th></tr></thead><tbody><tr><td><code>io_poll_poll_self()</code></td><td>获取当前 poll 线程令牌</td></tr><tr><td><code>io_poll_poll_wait()</code></td><td><code>epoll_pwait()</code> 等待、转换并分发事件</td></tr><tr><td><code>io_poll_poll_kill()</code></td><td>用状态和线程信号结束目标线程当前一次等待</td></tr></tbody></table><h3 id="27-4-内部辅助函数"><a href="#27-4-内部辅助函数" class="headerlink" title="27.4 内部辅助函数"></a>27.4 内部辅助函数</h3><table><thead><tr><th>函数</th><th>作用</th></tr></thead><tbody><tr><td><code>io_poll_process()</code></td><td>标记本次监听已消费并调用回调</td></tr><tr><td><code>io_fd_cmp()</code></td><td>比较两个 <code>int fd</code>，供红黑树排序和查找</td></tr><tr><td><code>io_poll_watch_from_node()</code></td><td>由嵌入式 <code>rbnode</code> 找回 <code>io_poll_watch</code></td></tr><tr><td><code>io_poll_from_svc()</code></td><td>由 <code>io_svc</code> 成员找回 <code>io_poll_t</code></td></tr><tr><td><code>io_poll_from_poll()</code></td><td>由 <code>poll_vptr</code> 成员找回 <code>io_poll_t</code></td></tr><tr><td><code>io_poll_svc_notify_fork()</code></td><td>子进程中重建 epoll 登记</td></tr><tr><td><code>sig_ign()</code></td><td>空信号处理函数，只用于打断等待</td></tr></tbody></table><hr><h2 id="28-最容易出现的理解和使用错误"><a href="#28-最容易出现的理解和使用错误" class="headerlink" title="28. 最容易出现的理解和使用错误"></a>28. 最容易出现的理解和使用错误</h2><h3 id="28-1-把-events-0-理解为“已经彻底注销”"><a href="#28-1-把-events-0-理解为“已经彻底注销”" class="headerlink" title="28.1 把 _events == 0 理解为“已经彻底注销”"></a>28.1 把 <code>_events == 0</code> 理解为“已经彻底注销”</h3><p>错误。只有红黑树节点被删除并执行 DEL，才是彻底取消登记。</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">_events == 0 + 节点仍在树中</span><br><span class="line">= 本次一次性监听已消费，等待调用方恢复或删除</span><br></pre></td></tr></table></figure><h3 id="28-2-回调处理完后没有再次调用-io-poll-watch"><a href="#28-2-回调处理完后没有再次调用-io-poll-watch" class="headerlink" title="28.2 回调处理完后没有再次调用 io_poll_watch()"></a>28.2 回调处理完后没有再次调用 <code>io_poll_watch()</code></h3><p>结果：只收到第一次事件，后续永远没有通知。</p><h3 id="28-3-看到-EPOLLOUT-就假设所有数据都能一次写完"><a href="#28-3-看到-EPOLLOUT-就假设所有数据都能一次写完" class="headerlink" title="28.3 看到 EPOLLOUT 就假设所有数据都能一次写完"></a>28.3 看到 <code>EPOLLOUT</code> 就假设所有数据都能一次写完</h3><p>错误。仍需处理部分写和 <code>EAGAIN</code>。</p><h3 id="28-4-只处理-IO-EVENT-IN，忽略同时出现的-ERR-HUP"><a href="#28-4-只处理-IO-EVENT-IN，忽略同时出现的-ERR-HUP" class="headerlink" title="28.4 只处理 IO_EVENT_IN，忽略同时出现的 ERR&#x2F;HUP"></a>28.4 只处理 <code>IO_EVENT_IN</code>，忽略同时出现的 ERR&#x2F;HUP</h3><p><code>revents</code> 是位组合。回调应逐位判断，不应使用互斥的单值比较。</p><h3 id="28-5-在取消登记前释放-watch"><a href="#28-5-在取消登记前释放-watch" class="headerlink" title="28.5 在取消登记前释放 watch"></a>28.5 在取消登记前释放 watch</h3><p>红黑树节点嵌入在 <code>io_poll_watch</code> 对象中。对象释放后树中节点会变成悬空内存。</p><p>正确顺序：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">停止产生新操作</span><br><span class="line">  ↓</span><br><span class="line">io_poll_watch(..., 0, watch)</span><br><span class="line">  ↓</span><br><span class="line">确认相关回调/任务结束</span><br><span class="line">  ↓</span><br><span class="line">close(fd)</span><br><span class="line">  ↓</span><br><span class="line">释放包含 watch 的对象</span><br></pre></td></tr></table></figure><h3 id="28-6-把-ev-poll-kill-理解为线程终止"><a href="#28-6-把-ev-poll-kill-理解为线程终止" class="headerlink" title="28.6 把 ev_poll_kill() 理解为线程终止"></a>28.6 把 <code>ev_poll_kill()</code> 理解为线程终止</h3><p>错误。它只结束当前一次 wait，不销毁线程或 Poll。</p><hr><h2 id="29-推荐断点和观察变量"><a href="#29-推荐断点和观察变量" class="headerlink" title="29. 推荐断点和观察变量"></a>29. 推荐断点和观察变量</h2><table><thead><tr><th>断点位置</th><th>重点观察</th></tr></thead><tbody><tr><td><code>io_poll_watch():310</code></td><td><code>fd</code>、<code>node</code>、<code>watch</code> 是否匹配</td></tr><tr><td><code>io_poll_watch():317</code></td><td>宏转换后的 <code>event.events</code> 和 <code>event.data.fd</code></td></tr><tr><td><code>io_poll_watch():319</code></td><td>为什么进入 MOD：修改事件还是 <code>_events==0</code></td></tr><tr><td><code>io_poll_watch():329</code></td><td>首次 ADD 的返回值和 <code>errno</code></td></tr><tr><td><code>io_poll_watch():338</code></td><td><code>nwatch</code> 是否需要增加</td></tr><tr><td><code>io_poll_watch():341</code></td><td>DEL 前后的树和计数</td></tr><tr><td><code>io_poll_poll_wait():455</code></td><td><code>timeout</code>、<code>nevents</code>、<code>errno</code></td></tr><tr><td><code>io_poll_poll_wait():482</code></td><td>每个内核事件位和转换后的 <code>revents</code></td></tr><tr><td><code>io_poll_poll_wait():498</code></td><td>fd 是否还能在红黑树找到</td></tr><tr><td><code>io_poll_process():602</code></td><td>回调前 <code>_events</code> 从非 0 变为 0</td></tr><tr><td>具体 <code>watch-&gt;func</code></td><td>是否读取&#x2F;写入并再次调用 <code>io_poll_watch()</code></td></tr><tr><td><code>io_poll_poll_kill():540</code></td><td><code>stopped</code> 和 <code>pthread_kill()</code> 返回值</td></tr></tbody></table><p>推荐日志：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">thread_id, epfd, fd,</span><br><span class="line">requested_io_events, generated_epoll_events,</span><br><span class="line">returned_epoll_events, callback_io_events,</span><br><span class="line">watch_pointer, watch-&gt;_events,</span><br><span class="line">nwatch, node_pointer, timeout, errno</span><br></pre></td></tr></table></figure><hr><h2 id="30-用一句话记住每个关键概念"><a href="#30-用一句话记住每个关键概念" class="headerlink" title="30. 用一句话记住每个关键概念"></a>30. 用一句话记住每个关键概念</h2><table><thead><tr><th>概念</th><th>一句话</th></tr></thead><tbody><tr><td><code>IO_EVENT_IN</code></td><td>Lely 层请求读侧通知</td></tr><tr><td><code>EPOLLIN</code></td><td>Linux 表示当前可以尝试读取</td></tr><tr><td><code>IO_EVENT_PRI</code></td><td>Lely 层请求异常优先级通知</td></tr><tr><td><code>EPOLLPRI</code></td><td>Linux 表示出现 exceptional condition</td></tr><tr><td><code>IO_EVENT_OUT</code></td><td>Lely 层请求写侧通知</td></tr><tr><td><code>EPOLLOUT</code></td><td>Linux 表示当前可以尝试推进写入</td></tr><tr><td><code>EPOLLONESHOT</code></td><td>每次 ADD&#x2F;MOD 后最多通知一次，之后必须 MOD 才能继续</td></tr><tr><td><code>EPOLL_CTL_ADD</code></td><td>首次加入 epoll interest list</td></tr><tr><td><code>EPOLL_CTL_MOD</code></td><td>修改事件或恢复下一次一次性监听</td></tr><tr><td><code>EPOLL_CTL_DEL</code></td><td>从 epoll interest list 彻底删除</td></tr><tr><td>红黑树</td><td>用户态 <code>fd → watch</code> 索引、唯一性检查和陈旧事件过滤</td></tr><tr><td><code>io_fd_cmp()</code></td><td>比较两个整数 fd 的大小</td></tr><tr><td><code>_events != 0</code></td><td>当前有一次有效监听</td></tr><tr><td><code>_events == 0</code> 且节点仍在</td><td>本次监听已消费，需恢复或删除</td></tr><tr><td><code>nwatch</code></td><td>当前有效一次监听的数量，不是树节点数</td></tr></tbody></table><hr><h2 id="31-源码位置索引"><a href="#31-源码位置索引" class="headerlink" title="31. 源码位置索引"></a>31. 源码位置索引</h2><table><thead><tr><th>主题</th><th>位置</th></tr></thead><tbody><tr><td><code>EPOLL_EVENT_INIT</code> 事件映射</td><td><code>poll.c:44-52</code></td></tr><tr><td><code>io_poll</code> 数据结构</td><td><code>poll.c:88-101</code></td></tr><tr><td>初始化和 epoll 创建</td><td><code>poll.c:131-205</code>、<code>572-592</code></td></tr><tr><td><code>io_poll_watch()</code></td><td><code>poll.c:286-357</code></td></tr><tr><td>fork 恢复</td><td><code>poll.c:359-402</code></td></tr><tr><td>线程令牌 <code>self()</code></td><td><code>poll.c:404-420</code></td></tr><tr><td><code>wait()</code></td><td><code>poll.c:422-523</code></td></tr><tr><td><code>kill()</code></td><td><code>poll.c:525-554</code></td></tr><tr><td>事件回调前状态更新</td><td><code>poll.c:594-614</code></td></tr><tr><td><code>io_fd_cmp()</code></td><td><code>poll.c:616-625</code></td></tr><tr><td>watch 结构和 API 约定</td><td><code>poll.h:35-123</code></td></tr><tr><td>C++ RAII 包装</td><td><code>poll.hpp:36-103</code></td></tr><tr><td>Event loop 驱动 Poll</td><td><code>src/ev/loop.c</code>：<code>ev_loop_ctx_wait_one()</code>、<code>ev_loop_ctx_kill()</code></td></tr><tr><td>SocketCAN fd 直接使用 Poll</td><td><code>src/io2/linux/can_chan.c</code>：<code>io_can_chan_impl_watch_func()</code>、读写 task</td></tr><tr><td>timerfd 直接使用 Poll</td><td><code>src/io2/linux/timer.c</code>：<code>io_timer_impl_open()</code>、<code>io_timer_impl_wait_task_func()</code></td></tr><tr><td>self-pipe 直接使用 Poll</td><td><code>src/io2/posix/sigset.c</code>：<code>io_sigset_impl_open()</code>、<code>io_sigset_impl_read_task_func()</code></td></tr><tr><td>I&#x2F;O 到 CAN 网络核心的桥接</td><td><code>src/io2/can_net.c</code>：read、write、next-time callback</td></tr><tr><td>CANopen Node 间接依赖链</td><td><code>src/coapp/node.cpp</code>：<code>Node::Node()</code></td></tr><tr><td>CANopen Master 间接依赖链</td><td><code>src/coapp/master.cpp</code>：<code>BasicMaster::BasicMaster()</code></td></tr></tbody></table><hr><h2 id="32-参考资料"><a href="#32-参考资料" class="headerlink" title="32. 参考资料"></a>32. 参考资料</h2><h3 id="32-1-本次分析的-Poll-源码"><a href="#32-1-本次分析的-Poll-源码" class="headerlink" title="32.1 本次分析的 Poll 源码"></a>32.1 本次分析的 Poll 源码</h3><ul><li><code>poll.c</code>：Lely Linux epoll 后端实现。</li><li><code>poll.h</code>：<code>io_poll_watch</code>、回调语义及对外 C API。</li><li><code>poll.hpp</code>：C++ RAII 包装。</li></ul><h3 id="32-2-Lely-全栈模块源码依据"><a href="#32-2-Lely-全栈模块源码依据" class="headerlink" title="32.2 Lely 全栈模块源码依据"></a>32.2 Lely 全栈模块源码依据</h3><p>本章的组件调用关系按 Lely <code>liblely-io2</code> &#x2F; <code>liblely-coapp</code> 源码分析：</p><ul><li><code>src/ev/loop.c</code>：event loop 调用 <code>ev_poll_wait()</code> 和 <code>ev_poll_kill()</code>。</li><li><code>src/io2/linux/can_chan.c</code>：SocketCAN fd 的 <code>IO_EVENT_IN/OUT</code> 登记、收发任务和发送确认。</li><li><code>src/io2/linux/timer.c</code>：<code>timerfd</code> 创建、可读事件、到期计数和 wait queue。</li><li><code>src/io2/posix/sigset.c</code>：self-pipe、signal handler、pipe 可读事件和信号分发。</li><li><code>src/io2/can_net.c</code>：CanChannel&#x2F;Timer 与被动 <code>can_net_t</code> 之间的桥接。</li><li><code>src/coapp/node.cpp</code>：Node 基于 <code>io::CanNet</code> 启动 CANopen 收发。</li><li><code>src/coapp/master.cpp</code>：Master 在 Node 之上实现主站和远程节点管理。</li><li><code>src/coapp/loop_driver.cpp</code>：CANopen 远程节点 Driver 的独立业务 event loop；不直接监听 CAN fd。</li><li><code>include/lely/io2/posix/poll.hpp</code>、<code>linux/can.hpp</code>、<code>sys/timer.hpp</code>、<code>sys/sigset.hpp</code>：对应 C++ 包装及构造依赖。</li></ul><p>源码层级以 Lely 官方 <code>lely-core</code> 仓库为准；章节中的短代码片段使用同源公开镜像提交 <code>e2ca7cb9e719e3371b88c3441d7f66e54a1a408d</code> 校核路径和调用关系。</p><ul><li><a href="https://gitlab.com/lely_industries/lely-core">Lely Core 官方仓库</a></li><li><a href="https://opensource.lely.com/canopen/docs/overview/">Lely Core Library Overview</a></li><li><a href="https://opensource.lely.com/canopen/docs/cpp-tutorial/">C++ CANopen application tutorial</a></li></ul><h3 id="32-3-Linux-API-依据"><a href="#32-3-Linux-API-依据" class="headerlink" title="32.3 Linux API 依据"></a>32.3 Linux API 依据</h3><ul><li><a href="https://man7.org/linux/man-pages/man7/epoll.7.html">Linux man-pages: epoll(7)</a></li><li><a href="https://man7.org/linux/man-pages/man2/epoll_ctl.2.html">Linux man-pages: epoll_ctl(2)</a></li><li><a href="https://man7.org/linux/man-pages/man2/epoll_wait.2.html">Linux man-pages: epoll_wait(2) &#x2F; epoll_pwait(2)</a></li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;meta name=&quot;referrer&quot; content=&quot;no-referrer&quot; /&gt;


&lt;h1 id=&quot;Lely-canopenio-poll-机制原理与完整运行流程详解&quot;&gt;&lt;a href=&quot;#Lely-canopenio-poll-机制原理与完整运行流程详解&quot; cla</summary>
      
    
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/categories/canopen/"/>
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/tags/canopen/"/>
    
  </entry>
  
  <entry>
    <title>Lely CANopen ：IO 服务 io_ctx</title>
    <link href="https://wdfk-prog.space/posts/9a4ae0f9/"/>
    <id>https://wdfk-prog.space/posts/9a4ae0f9/</id>
    <published>2026-07-27T08:24:40.000Z</published>
    <updated>2026-07-27T08:25:33.063Z</updated>
    
    <content type="html"><![CDATA[<meta name="referrer" content="no-referrer" /><h1 id="Lely-CANopen-：I-O-服务-io-ctx"><a href="#Lely-CANopen-：I-O-服务-io-ctx" class="headerlink" title="Lely CANopen ：I&#x2F;O 服务 io_ctx"></a>Lely CANopen ：I&#x2F;O 服务 io_ctx</h1><p><img src="https://i-blog.csdnimg.cn/direct/6968b23a95ab4ccfac01e02949d10a1c.png" alt="在这里插入图片描述"></p><p>@[toc]</p><h2 id="摘要"><a href="#摘要" class="headerlink" title="摘要"></a>摘要</h2><p>在 Lely CANopen 应用中，<code>lely::io::Context</code> 往往是最先创建、最后销毁的对象之一。它看起来只是一个很薄的 C++ 包装类，但其底层 <code>io_ctx_t</code> 承担了一个关键职责：</p><blockquote><p>维护一组已注册的 Lely I&#x2F;O 服务，并在进程 <code>fork()</code> 或系统退出时，按确定顺序向这些服务分发维护事件。</p></blockquote><p><code>ctx.c</code> 本身并不执行 CANopen 协议，也不负责运行事件循环。它更像一个轻量级的“服务生命周期协调器”：</p><ul><li>通过双向链表登记 <code>Timer</code>、CAN 网络接口及其他底层 I&#x2F;O 服务；</li><li>通过服务虚表调用各组件自己的 <code>notify_fork</code> 和 <code>shutdown</code> 回调；</li><li>在 <code>fork()</code> 前后按不同方向遍历服务；</li><li>在 shutdown 时按注册顺序的逆序关闭服务；</li><li>通过服务内部的 <code>_shutdown</code> 标志保证每个服务最多关闭一次；</li><li>通过 C++ 的 move-only 包装实现单个底层 Context 的唯一所有权；</li><li>允许同一进程创建多个彼此独立的 Context，并非全局单例。</li></ul><p>本文以 Lely Core 官方 Doxygen 当前展示的 2.4.0 源码为基础，重点阅读：</p><ul><li><code>include/lely/io2/ctx.h</code></li><li><code>src/io2/ctx.c</code></li><li><code>include/lely/io2/ctx.hpp</code></li><li><code>src/io2/can_net.c</code></li></ul><hr><h2 id="1-先明确：io-ctx-属于哪一层"><a href="#1-先明确：io-ctx-属于哪一层" class="headerlink" title="1. 先明确：io_ctx 属于哪一层"></a>1. 先明确：<code>io_ctx</code> 属于哪一层</h2><p>Lely Core 并不是一个单一的 CANopen 协议库，而是由多个层次组成，其中包括：</p><ul><li><code>liblely-ev</code>：任务、执行器和事件循环；</li><li><code>liblely-io2</code>：异步 I&#x2F;O 抽象；</li><li><code>liblely-co</code>：CANopen 协议实现；</li><li><code>liblely-coapp</code>：面向 C++ 应用的 CANopen 封装。</li></ul><p><code>io_ctx</code> 位于 <code>liblely-io2</code>。因此它不是 CiA 301 规定的对象字典、PDO、SDO 或 NMT 状态机的一部分，而是 Lely 在操作系统和异步 I&#x2F;O 层提供的基础设施。</p><p>更准确的依赖关系是：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    APP[&quot;CANopen 应用&lt;br/&gt;AsyncMaster / BasicSlave / Driver&quot;]</span><br><span class="line">    CO[&quot;CANopen 协议层&lt;br/&gt;NMT / SDO / PDO / EMCY&quot;]</span><br><span class="line">    IO[&quot;Lely I/O 服务&lt;br/&gt;CAN Channel / CAN Net / Timer / Signal&quot;]</span><br><span class="line">    CTX[&quot;io_ctx&lt;br/&gt;服务登记与生命周期协调&quot;]</span><br><span class="line">    OS[&quot;操作系统资源&lt;br/&gt;fd / poll / timer / thread&quot;]</span><br><span class="line"></span><br><span class="line">    APP --&gt; CO</span><br><span class="line">    CO --&gt; IO</span><br><span class="line">    IO --&gt; CTX</span><br><span class="line">    IO --&gt; OS</span><br></pre></td></tr></table></figure><p>因此，<code>io_ctx_shutdown()</code> 不是直接逐个关闭 NMT、SDO 或 PDO 对象。它遍历的是注册到该 Context 的底层 <code>io_svc</code>。上层 CANopen 对象因为依赖这些 I&#x2F;O 服务而间接受到影响。</p><hr><h2 id="2-io-ctx-的内部结构为什么如此简单"><a href="#2-io-ctx-的内部结构为什么如此简单" class="headerlink" title="2. io_ctx 的内部结构为什么如此简单"></a>2. <code>io_ctx</code> 的内部结构为什么如此简单</h2><p>从 <code>ctx.c</code> 可以看到，<code>io_ctx</code> 的核心成员只有两类：</p><ol><li>可选的互斥锁；</li><li>一条双向链表。</li></ol><p>可将其抽象为：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">io_ctx</span> &#123;</span></span><br><span class="line">    mutex;</span><br><span class="line">    service_list;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>互斥锁用于保护服务链表的插入、删除和遍历。链表则保存所有注册到该 Context 的 <code>io_svc</code>。</p><p>这说明 <code>io_ctx</code> 本身不保存：</p><ul><li>CAN 帧；</li><li>定时器超时时间；</li><li>事件循环任务；</li><li>CANopen 节点状态；</li><li>文件描述符；</li><li>线程对象。</li></ul><p>这些状态都属于具体服务。Context 只掌握服务节点以及调用服务回调所需的统一接口。</p><p>从设计模式上看，它接近以下组合：</p><ul><li><strong>Registry</strong>：登记服务；</li><li><strong>Observer&#x2F;Event Dispatcher</strong>：广播 fork 事件；</li><li><strong>Lifecycle Coordinator</strong>：统一 shutdown；</li><li><strong>Intrusive List</strong>：链表节点嵌入服务对象自身。</li></ul><hr><h2 id="3-io-svc：所有可被-Context-管理的服务都要遵守的协议"><a href="#3-io-svc：所有可被-Context-管理的服务都要遵守的协议" class="headerlink" title="3. io_svc：所有可被 Context 管理的服务都要遵守的协议"></a>3. <code>io_svc</code>：所有可被 Context 管理的服务都要遵守的协议</h2><p><code>ctx.h</code> 定义了两个关键结构：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">io_svc</span><br><span class="line"> ├─ vptr</span><br><span class="line"> ├─ _shutdown</span><br><span class="line"> └─ _node</span><br><span class="line"></span><br><span class="line">io_svc_vtbl</span><br><span class="line"> ├─ notify_fork</span><br><span class="line"> └─ shutdown</span><br></pre></td></tr></table></figure><h3 id="3-1-io-svc"><a href="#3-1-io-svc" class="headerlink" title="3.1 io_svc"></a>3.1 <code>io_svc</code></h3><p>每个可注册服务都内嵌一个 <code>io_svc</code>：</p><ul><li><code>vptr</code> 指向服务虚表；</li><li><code>_shutdown</code> 记录该服务是否已被 Context 关闭；</li><li><code>_node</code> 是挂入 Context 双向链表的节点。</li></ul><p>这是一种典型的 C 语言“基类”写法。具体服务对象通常包含一个 <code>io_svc</code> 成员，再通过 <code>structof()</code> 或类似 container-of 机制由 <code>io_svc*</code> 反推出外层服务对象。</p><h3 id="3-2-io-svc-vtbl"><a href="#3-2-io-svc-vtbl" class="headerlink" title="3.2 io_svc_vtbl"></a>3.2 <code>io_svc_vtbl</code></h3><p>服务虚表只规定两种生命周期回调：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span>  <span class="title function_">notify_fork</span><span class="params">(io_svc *svc, io_fork_event event)</span>;</span><br><span class="line"><span class="type">void</span> <span class="title function_">shutdown</span><span class="params">(io_svc *svc)</span>;</span><br></pre></td></tr></table></figure><p>两个函数指针都允许为 <code>NULL</code>。这意味着：</p><ul><li>某个服务可能需要 shutdown，但不需要 fork 维护；</li><li>某个服务可能同时实现两者；</li><li>Context 不要求所有服务做完全相同的处理。</li></ul><p><code>io_ctx</code> 只负责调度，不理解任何驱动细节。真正的资源维护逻辑留在各服务内部。</p><p>这正是该设计最重要的解耦点：</p><blockquote><p>Context 知道“何时通知”，具体服务知道“如何处理”。</p></blockquote><hr><h2 id="4-服务如何加入和退出-Context"><a href="#4-服务如何加入和退出-Context" class="headerlink" title="4. 服务如何加入和退出 Context"></a>4. 服务如何加入和退出 Context</h2><h2 id="4-1-io-ctx-insert"><a href="#4-1-io-ctx-insert" class="headerlink" title="4.1 io_ctx_insert()"></a>4.1 <code>io_ctx_insert()</code></h2><p>服务初始化完成后，通过 <code>io_ctx_insert()</code> 注册到 Context。其核心步骤是：</p><ol><li>锁住 Context；</li><li>将服务节点追加到链表尾部；</li><li>解锁。</li></ol><p>因此，链表顺序就是服务注册顺序。</p><p>官方接口给出了一个重要前置条件：</p><blockquote><p>被插入的 <code>io_svc</code> 当前不得注册在任何 Context 中，包括目标 Context。</p></blockquote><p>原因很直接：<code>io_svc</code> 内部只有一个 <code>_node</code>。同一个侵入式链表节点不能同时属于两条链表。</p><p>所以：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">一个 io_svc 实例</span><br><span class="line">    └─ 同一时刻最多属于一个 io_ctx</span><br></pre></td></tr></table></figure><p>这不等于一个 Context 只能管理一个服务，也不等于一个进程只能创建一个 Context。</p><h2 id="4-2-io-ctx-remove"><a href="#4-2-io-ctx-remove" class="headerlink" title="4.2 io_ctx_remove()"></a>4.2 <code>io_ctx_remove()</code></h2><p>服务销毁前，通过 <code>io_ctx_remove()</code> 从链表中删除。</p><p>官方文档明确限制：</p><blockquote><p><code>io_ctx_notify_fork()</code> 或 <code>io_ctx_shutdown()</code> 正在运行时，不得注销服务。</p></blockquote><p>虽然 <code>ctx.c</code> 在调用具体服务回调前会暂时释放 Context 互斥锁，但遍历逻辑已经保存了前一个或后一个链表节点。如果另一个线程此时移除服务，迭代游标可能失效。因此，Context 的内部锁并不能替代上层生命周期同步。</p><p>换句话说，线程安全边界是：</p><ul><li>Context 负责保护链表的基本读写；</li><li>调用者负责保证整次 fork 通知或 shutdown 期间，服务集合保持稳定。</li></ul><hr><h2 id="5-io-ctx-notify-fork-的调度机制"><a href="#5-io-ctx-notify-fork-的调度机制" class="headerlink" title="5. io_ctx_notify_fork() 的调度机制"></a>5. <code>io_ctx_notify_fork()</code> 的调度机制</h2><h2 id="5-1-Context-不会替用户调用-fork"><a href="#5-1-Context-不会替用户调用-fork" class="headerlink" title="5.1 Context 不会替用户调用 fork()"></a>5.1 Context 不会替用户调用 <code>fork()</code></h2><p><code>io_ctx_notify_fork()</code> 只是事件广播函数。真正的 <code>fork()</code> 仍由应用调用。</p><p>标准流程是：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">ctx.<span class="built_in">notify_fork</span>(lely::io::ForkEvent::prepare);</span><br><span class="line"></span><br><span class="line"><span class="type">pid_t</span> pid = ::fork();</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (pid == <span class="number">0</span>) &#123;</span><br><span class="line">    ctx.<span class="built_in">notify_fork</span>(lely::io::ForkEvent::child);</span><br><span class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span> (pid &gt; <span class="number">0</span>) &#123;</span><br><span class="line">    ctx.<span class="built_in">notify_fork</span>(lely::io::ForkEvent::parent);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>如果程序从不调用进程级 <code>fork()</code>，就不需要调用该接口。创建 <code>std::thread</code> 与它无关。</p><h2 id="5-2-三种-fork-事件"><a href="#5-2-三种-fork-事件" class="headerlink" title="5.2 三种 fork 事件"></a>5.2 三种 fork 事件</h2><p><code>ctx.h</code> 定义三种事件：</p><table><thead><tr><th>事件</th><th>调用时机</th><th>作用</th></tr></thead><tbody><tr><td><code>IO_FORK_PREPARE</code></td><td><code>fork()</code> 前</td><td>让服务进入可安全复制进程状态</td></tr><tr><td><code>IO_FORK_PARENT</code></td><td><code>fork()</code> 后的父进程</td><td>恢复父进程服务状态</td></tr><tr><td><code>IO_FORK_CHILD</code></td><td><code>fork()</code> 后的子进程</td><td>修复或重建子进程继承的服务状态</td></tr></tbody></table><p>具体要暂停什么、重建什么、重新注册什么，完全由服务自己的 <code>notify_fork</code> 回调决定。</p><h2 id="5-3-为什么-PREPARE-是逆序，而-PARENT-CHILD-是正序"><a href="#5-3-为什么-PREPARE-是逆序，而-PARENT-CHILD-是正序" class="headerlink" title="5.3 为什么 PREPARE 是逆序，而 PARENT&#x2F;CHILD 是正序"></a>5.3 为什么 PREPARE 是逆序，而 PARENT&#x2F;CHILD 是正序</h2><p>源码中的遍历规则是：</p><ul><li><code>IO_FORK_PREPARE</code>：从链表尾部向头部遍历；</li><li><code>IO_FORK_PARENT</code>、<code>IO_FORK_CHILD</code>：从链表头部向尾部遍历。</li></ul><p>假设服务按以下顺序创建：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">A → B → C</span><br></pre></td></tr></table></figure><p>则通知顺序为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">PREPARE: C → B → A</span><br><span class="line">PARENT : A → B → C</span><br><span class="line">CHILD  : A → B → C</span><br></pre></td></tr></table></figure><p>这与常见的资源栈语义一致：</p><ul><li>准备阶段按依赖的逆序拆解或冻结；</li><li>fork 后按原始构建顺序恢复。</li></ul><p>该顺序并不自动证明所有服务都存在严格依赖，但它为“后创建者依赖先创建者”的常见场景提供了稳定行为。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant U as 用户代码</span><br><span class="line">    participant C as io_ctx</span><br><span class="line">    participant A as Service A</span><br><span class="line">    participant B as Service B</span><br><span class="line">    participant D as Service C</span><br><span class="line"></span><br><span class="line">    U-&gt;&gt;C: notify_fork(PREPARE)</span><br><span class="line">    C-&gt;&gt;D: PREPARE</span><br><span class="line">    C-&gt;&gt;B: PREPARE</span><br><span class="line">    C-&gt;&gt;A: PREPARE</span><br><span class="line"></span><br><span class="line">    U-&gt;&gt;U: fork()</span><br><span class="line"></span><br><span class="line">    alt 父进程</span><br><span class="line">        U-&gt;&gt;C: notify_fork(PARENT)</span><br><span class="line">        C-&gt;&gt;A: PARENT</span><br><span class="line">        C-&gt;&gt;B: PARENT</span><br><span class="line">        C-&gt;&gt;D: PARENT</span><br><span class="line">    else 子进程</span><br><span class="line">        U-&gt;&gt;C: notify_fork(CHILD)</span><br><span class="line">        C-&gt;&gt;A: CHILD</span><br><span class="line">        C-&gt;&gt;B: CHILD</span><br><span class="line">        C-&gt;&gt;D: CHILD</span><br><span class="line">    end</span><br></pre></td></tr></table></figure><h2 id="5-4-调用回调时为什么要释放-Context-锁"><a href="#5-4-调用回调时为什么要释放-Context-锁" class="headerlink" title="5.4 调用回调时为什么要释放 Context 锁"></a>5.4 调用回调时为什么要释放 Context 锁</h2><p><code>io_ctx_notify_fork()</code> 的循环逻辑可以抽象为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">锁住 Context</span><br><span class="line">取出当前服务和下一个遍历节点</span><br><span class="line">解锁</span><br><span class="line">调用服务 notify_fork()</span><br><span class="line">重新加锁</span><br><span class="line">继续遍历</span><br></pre></td></tr></table></figure><p>这是一个重要实现细节。Context 不会在执行任意服务代码时一直持有自己的互斥锁。</p><p>基于源码可以得到两个结论：</p><ol><li>服务回调不会被迫在 Context 互斥锁内执行；</li><li>服务集合稳定性必须由调用者额外保证。</li></ol><p>从工程角度分析，释放锁可降低服务回调内部发生锁顺序反转或长时间占用 Context 锁的风险，但代价是调用方必须严格遵守“通知期间不得移除服务”的约束。</p><h2 id="5-5-错误处理：记录第一个错误，但继续通知"><a href="#5-5-错误处理：记录第一个错误，但继续通知" class="headerlink" title="5.5 错误处理：记录第一个错误，但继续通知"></a>5.5 错误处理：记录第一个错误，但继续通知</h2><p>某个服务的 <code>notify_fork()</code> 返回 <code>-1</code> 时，Context 会：</p><ol><li>保存第一个失败服务产生的错误码；</li><li>将整体结果标记为失败；</li><li>继续通知后续服务；</li><li>最终恢复第一个错误码并返回 <code>-1</code>。</li></ol><p>这种策略比“遇到第一个错误立即停止”更适合生命周期广播。即使某个服务准备失败，其他服务仍有机会接收到同一阶段事件，避免系统停在更不一致的中间状态。</p><p>但需要注意：该函数只报告第一个错误，不提供所有失败服务的聚合列表。</p><hr><h2 id="6-io-ctx-shutdown-的调度机制"><a href="#6-io-ctx-shutdown-的调度机制" class="headerlink" title="6. io_ctx_shutdown() 的调度机制"></a>6. <code>io_ctx_shutdown()</code> 的调度机制</h2><h2 id="6-1-shutdown-按注册逆序执行"><a href="#6-1-shutdown-按注册逆序执行" class="headerlink" title="6.1 shutdown 按注册逆序执行"></a>6.1 shutdown 按注册逆序执行</h2><p><code>io_ctx_shutdown()</code> 从链表尾部开始，逐个调用服务的 <code>shutdown</code> 回调。</p><p>如果注册顺序为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Poll → Timer → CAN Channel → CAN Net</span><br></pre></td></tr></table></figure><p>那么 shutdown 顺序为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">CAN Net → CAN Channel → Timer → Poll</span><br></pre></td></tr></table></figure><p>这种顺序类似 C++ 局部对象的逆序析构，适用于“后创建的高层组件依赖先创建的底层组件”的情况。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    R[&quot;注册顺序&quot;] --&gt; A[&quot;Poll&quot;]</span><br><span class="line">    A --&gt; B[&quot;Timer&quot;]</span><br><span class="line">    B --&gt; C[&quot;CAN Channel&quot;]</span><br><span class="line">    C --&gt; D[&quot;CAN Net&quot;]</span><br><span class="line"></span><br><span class="line">    S[&quot;shutdown 顺序&quot;] --&gt; D2[&quot;CAN Net&quot;]</span><br><span class="line">    D2 --&gt; C2[&quot;CAN Channel&quot;]</span><br><span class="line">    C2 --&gt; B2[&quot;Timer&quot;]</span><br><span class="line">    B2 --&gt; A2[&quot;Poll&quot;]</span><br></pre></td></tr></table></figure><h2 id="6-2-shutdown-让操作具备幂等性"><a href="#6-2-shutdown-让操作具备幂等性" class="headerlink" title="6.2 _shutdown 让操作具备幂等性"></a>6.2 <code>_shutdown</code> 让操作具备幂等性</h2><p>每个 <code>io_svc</code> 内部都有 <code>_shutdown</code> 标志。</p><p>Context 在调用服务回调前会：</p><ol><li>检查 <code>_shutdown</code>；</li><li>已经关闭则跳过；</li><li>未关闭则先置为已关闭；</li><li>再调用服务的 shutdown 回调。</li></ol><p>因此同一个服务无论 Context shutdown 被调用多少次，都只会执行一次回调。</p><p>这里“先置位、再回调”也意味着，即使 shutdown 回调间接导致再次调用 Context shutdown，该服务也不会递归关闭第二次。</p><h2 id="6-3-shutdown-回调同样在-Context-锁之外执行"><a href="#6-3-shutdown-回调同样在-Context-锁之外执行" class="headerlink" title="6.3 shutdown 回调同样在 Context 锁之外执行"></a>6.3 shutdown 回调同样在 Context 锁之外执行</h2><p>其模式与 fork 通知相似：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">锁住 Context</span><br><span class="line">确定当前服务和前一个节点</span><br><span class="line">设置 _shutdown</span><br><span class="line">解锁</span><br><span class="line">调用服务 shutdown()</span><br><span class="line">重新加锁</span><br><span class="line">继续</span><br></pre></td></tr></table></figure><p>因此，Context 的互斥锁不保护服务内部 shutdown 过程。调用方仍必须保证：</p><ul><li>shutdown 期间不注销服务；</li><li>不再启动依赖这些服务的新异步操作；</li><li>工作线程和事件循环的停止顺序与服务依赖一致。</li></ul><h2 id="6-4-shutdown-不等于-destroy"><a href="#6-4-shutdown-不等于-destroy" class="headerlink" title="6.4 shutdown 不等于 destroy"></a>6.4 <code>shutdown</code> 不等于 <code>destroy</code></h2><p>这是最容易混淆的一点。</p><h3 id="io-ctx-shutdown"><a href="#io-ctx-shutdown" class="headerlink" title="io_ctx_shutdown()"></a><code>io_ctx_shutdown()</code></h3><p>作用是：</p><ul><li>遍历注册服务；</li><li>调用服务 shutdown 回调；</li><li>让底层 I&#x2F;O 进入终止状态。</li></ul><h3 id="io-ctx-destroy"><a href="#io-ctx-destroy" class="headerlink" title="io_ctx_destroy()"></a><code>io_ctx_destroy()</code></h3><p>作用是：</p><ul><li>销毁 Context 自身的互斥锁；</li><li>释放 Context 内存。</li></ul><p>从 <code>ctx.c</code> 可以看到，<code>io_ctx_destroy()</code> 不会自动调用 <code>io_ctx_shutdown()</code>。</p><p>因此不能将两者等同：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">shutdown = 终止注册服务</span><br><span class="line">destroy  = 释放 Context 容器本身</span><br></pre></td></tr></table></figure><p>更稳妥的程序退出顺序通常是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">停止产生新请求</span><br><span class="line">→ ctx.shutdown()</span><br><span class="line">→ 让事件循环排空或退出</span><br><span class="line">→ join 工作线程</span><br><span class="line">→ 销毁各具体 I/O 服务</span><br><span class="line">→ 最后销毁 Context</span><br></pre></td></tr></table></figure><p>具体项目仍需根据对象依赖关系调整。</p><hr><h2 id="7-具体服务如何接入：以-io-can-net-为例"><a href="#7-具体服务如何接入：以-io-can-net-为例" class="headerlink" title="7. 具体服务如何接入：以 io_can_net 为例"></a>7. 具体服务如何接入：以 <code>io_can_net</code> 为例</h2><p><code>src/io2/can_net.c</code> 展示了一个典型服务如何纳入 Context。</p><p>其服务虚表中：</p><ul><li><code>notify_fork</code> 为 <code>NULL</code>；</li><li><code>shutdown</code> 指向 CAN 网络服务自己的关闭函数。</li></ul><p>初始化完成后，<code>io_can_net</code> 调用 <code>io_ctx_insert()</code> 注册；销毁时先调用 <code>io_ctx_remove()</code> 注销，然后执行自身 shutdown 逻辑。</p><p>这说明：</p><ol><li>Context 并不要求每个服务都处理 fork；</li><li>CAN 网络服务可以只参与统一 shutdown；</li><li>服务自身仍负责取消未完成操作、释放内部资源和关闭设备；</li><li>Context 只提供统一入口和确定的调用顺序。</li></ol><p>可以把接入模式概括为：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">static</span> <span class="type">const</span> io_svc_vtbl service_vtbl = &#123;</span><br><span class="line">    .notify_fork = optional_fork_handler,</span><br><span class="line">    .shutdown = service_shutdown</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">service_init(...) &#123;</span><br><span class="line">    initialize_internal_state();</span><br><span class="line">    io_ctx_insert(ctx, &amp;service-&gt;svc);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">service_fini(...) &#123;</span><br><span class="line">    io_ctx_remove(ctx, &amp;service-&gt;svc);</span><br><span class="line">    service_shutdown(&amp;service-&gt;svc);</span><br><span class="line">    release_internal_state();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>上面是结构化伪代码，并非对原文件的逐字复制。</p><hr><h2 id="8-C-包装：为什么-Context-只能移动，不能复制"><a href="#8-C-包装：为什么-Context-只能移动，不能复制" class="headerlink" title="8. C++ 包装：为什么 Context 只能移动，不能复制"></a>8. C++ 包装：为什么 <code>Context</code> 只能移动，不能复制</h2><p><code>ctx.hpp</code> 将 C 接口分为两个层次。</p><h2 id="8-1-ContextBase：非拥有型引用"><a href="#8-1-ContextBase：非拥有型引用" class="headerlink" title="8.1 ContextBase：非拥有型引用"></a>8.1 <code>ContextBase</code>：非拥有型引用</h2><p><code>ContextBase</code> 只保存一个 <code>io_ctx_t*</code>，并提供：</p><ul><li><code>insert()</code>；</li><li><code>remove()</code>；</li><li><code>notify_fork()</code>；</li><li><code>shutdown()</code>；</li><li>到 <code>io_ctx_t*</code> 的转换。</li></ul><p>它只是对已有 Context 的引用包装，不负责释放底层对象。</p><p>可将其理解为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">ContextBase</span><br><span class="line">    = non-owning view</span><br><span class="line">    = 借用一个 io_ctx_t*</span><br></pre></td></tr></table></figure><h2 id="8-2-Context：拥有型-RAII-对象"><a href="#8-2-Context：拥有型-RAII-对象" class="headerlink" title="8.2 Context：拥有型 RAII 对象"></a>8.2 <code>Context</code>：拥有型 RAII 对象</h2><p><code>Context</code> 构造时调用 <code>io_ctx_create()</code>，析构时调用 <code>io_ctx_destroy()</code>。</p><p>它删除了：</p><ul><li>拷贝构造；</li><li>拷贝赋值。</li></ul><p>同时支持：</p><ul><li>移动构造；</li><li>移动赋值。</li></ul><p>原因是底层 <code>io_ctx_t*</code> 必须只有一个负责销毁它的 C++ 所有者。若允许默认复制，两个 <code>Context</code> 最终会对同一指针执行两次 <code>io_ctx_destroy()</code>。</p><p>因此，准确表述是：</p><blockquote><p>每个具体底层 <code>io_ctx_t</code> 采用唯一所有权，而不是整个程序只能存在一个 Context。</p></blockquote><h2 id="8-3-移动构造与移动赋值的细节"><a href="#8-3-移动构造与移动赋值的细节" class="headerlink" title="8.3 移动构造与移动赋值的细节"></a>8.3 移动构造与移动赋值的细节</h2><p>移动构造会把原对象指针交给新对象，并把原对象设为空。</p><p>移动赋值则使用 <code>swap()</code> 交换两个指针。因此移动赋值后：</p><ul><li>目标对象获得源对象原来的 Context；</li><li>源对象获得目标对象原来的 Context；</li><li>两个底层 Context 仍分别只有一个所有者。</li></ul><p>这与 <code>std::unique_ptr</code> 的常见移动赋值结果略有不同：这里被移动对象不一定变成空对象，因为实现采用的是交换。</p><h2 id="8-4-析构函数不会自动-shutdown"><a href="#8-4-析构函数不会自动-shutdown" class="headerlink" title="8.4 析构函数不会自动 shutdown"></a>8.4 析构函数不会自动 shutdown</h2><p><code>Context::~Context()</code> 只调用 <code>io_ctx_destroy()</code>，不会先调用 <code>shutdown()</code>。</p><p>这进一步说明：</p><ul><li>RAII 负责 Context 内存所有权；</li><li>I&#x2F;O 服务终止时机仍由应用生命周期决定；</li><li>用户不能假设 <code>Context</code> 离开作用域时，会自动统一关闭所有注册服务。</li></ul><hr><h2 id="9-Context-不是单例：程序可以创建多个实例"><a href="#9-Context-不是单例：程序可以创建多个实例" class="headerlink" title="9. Context 不是单例：程序可以创建多个实例"></a>9. Context 不是单例：程序可以创建多个实例</h2><p><code>io_ctx_create()</code> 每次都会独立分配并初始化一个新的 Context。源码中不存在全局唯一实例或单例检查。</p><p>下面的代码在语义上是允许的：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">lely::io::Context ctx_a;</span><br><span class="line">lely::io::Context ctx_b;</span><br></pre></td></tr></table></figure><p>它们形成两套独立注册表：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">ctx_a</span><br><span class="line"> ├─ service A1</span><br><span class="line"> └─ service A2</span><br><span class="line"></span><br><span class="line">ctx_b</span><br><span class="line"> ├─ service B1</span><br><span class="line"> └─ service B2</span><br></pre></td></tr></table></figure><p>但一个具体服务只能注册到其中一个 Context。</p><p>实际项目是否需要多个 Context，取决于隔离需求。例如：</p><ul><li>两套完全独立的 I&#x2F;O 子系统；</li><li>不同生命周期域；</li><li>测试环境中的隔离实例。</li></ul><p>对常规单套 Lely CANopen 主站或从站程序而言，一个 Context 通常已经足够，且统一 shutdown 更容易维护。</p><hr><h2 id="10-一套推荐的应用生命周期"><a href="#10-一套推荐的应用生命周期" class="headerlink" title="10. 一套推荐的应用生命周期"></a>10. 一套推荐的应用生命周期</h2><p>下面给出一个简化的 C++ 生命周期框架：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    lely::io::IoGuard io_guard;</span><br><span class="line">    lely::io::Context ctx;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 创建 Poll、Loop、Timer、CAN Channel、CANopen 节点等对象。</span></span><br><span class="line">    <span class="comment">// 各底层 I/O 服务在初始化期间注册到 ctx。</span></span><br><span class="line"></span><br><span class="line">    <span class="built_in">start_event_threads</span>();</span><br><span class="line"></span><br><span class="line">    <span class="built_in">run_application</span>();</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 先阻止新的业务请求进入。</span></span><br><span class="line">    <span class="built_in">request_application_stop</span>();</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 统一通知所有已注册 I/O 服务停止。</span></span><br><span class="line">    ctx.<span class="built_in">shutdown</span>();</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 让事件线程结束，并确保不再访问已 shutdown 的服务。</span></span><br><span class="line">    <span class="built_in">join_event_threads</span>();</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 其余对象按依赖关系析构。</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里要强调三个边界：</p><ol><li><code>ctx.shutdown()</code> 不是线程 join；</li><li><code>ctx.shutdown()</code> 不是 <code>io_ctx_destroy()</code>；</li><li><code>ctx.shutdown()</code> 之后不应继续把已关闭服务当作可恢复对象使用。</li></ol><hr><h2 id="11-fork-场景下的推荐流程"><a href="#11-fork-场景下的推荐流程" class="headerlink" title="11. fork() 场景下的推荐流程"></a>11. <code>fork()</code> 场景下的推荐流程</h2><p>仅当程序确实调用 POSIX <code>fork()</code>，并且父子进程需要维护继承的 Lely I&#x2F;O 服务时，才需要 fork 通知。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">ctx.<span class="built_in">notify_fork</span>(lely::io::ForkEvent::prepare);</span><br><span class="line"></span><br><span class="line"><span class="type">pid_t</span> pid = ::fork();</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (pid == <span class="number">-1</span>) &#123;</span><br><span class="line">    <span class="comment">// fork 失败时，需要根据应用策略恢复或终止。</span></span><br><span class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span> (pid == <span class="number">0</span>) &#123;</span><br><span class="line">    ctx.<span class="built_in">notify_fork</span>(lely::io::ForkEvent::child);</span><br><span class="line">    <span class="built_in">run_child</span>();</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    ctx.<span class="built_in">notify_fork</span>(lely::io::ForkEvent::parent);</span><br><span class="line">    <span class="built_in">run_parent</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>工程上还应注意：</p><ul><li>fork 通知期间禁止注销服务；</li><li>多线程进程调用 <code>fork()</code> 本身就有严格限制；</li><li>子进程若立即 <code>exec()</code> 且不再访问继承的 Lely 对象，可采用更简单的进程模型；</li><li>如果可以选择，先 fork、再分别创建各自的 Context 和 I&#x2F;O 服务，通常更容易推理。</li></ul><p>后两点属于通用 POSIX 工程建议，不是 <code>ctx.c</code> 自身强制的策略。</p><hr><h2 id="12-这套设计解决了什么问题"><a href="#12-这套设计解决了什么问题" class="headerlink" title="12. 这套设计解决了什么问题"></a>12. 这套设计解决了什么问题</h2><p>如果没有 Context，每个应用都需要手工记录所有 I&#x2F;O 组件，并在退出时逐个调用：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">shutdown CAN Net</span><br><span class="line">shutdown CAN Channel</span><br><span class="line">shutdown Timer</span><br><span class="line">shutdown Poll</span><br><span class="line">...</span><br></pre></td></tr></table></figure><p>同时，fork 前后还要分别通知不同组件。</p><p>这种写法存在几个问题：</p><ul><li>应用层需要了解所有驱动内部类型；</li><li>新增服务后容易遗漏退出处理；</li><li>服务关闭顺序分散在业务代码中；</li><li>fork 维护逻辑难以统一；</li><li>重复 shutdown 容易导致二次释放或重复取消。</li></ul><p><code>io_ctx</code> 将这些横切关注点收敛为统一协议：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">服务初始化</span><br><span class="line">    → 注册 io_svc</span><br><span class="line"></span><br><span class="line">fork/shutdown</span><br><span class="line">    → Context 遍历服务</span><br><span class="line">    → 调用服务虚表</span><br><span class="line"></span><br><span class="line">服务销毁</span><br><span class="line">    → 注销 io_svc</span><br></pre></td></tr></table></figure><p>它并没有消除各驱动的复杂性，而是将“何时调用各驱动的生命周期逻辑”标准化。</p><hr><h2 id="13-常见误解"><a href="#13-常见误解" class="headerlink" title="13. 常见误解"></a>13. 常见误解</h2><h3 id="误解一：Context-管理整个-CANopen-协议栈"><a href="#误解一：Context-管理整个-CANopen-协议栈" class="headerlink" title="误解一：Context 管理整个 CANopen 协议栈"></a>误解一：Context 管理整个 CANopen 协议栈</h3><p>不准确。</p><p>Context 直接管理的是注册到它的 <code>io_svc</code>。CANopen 上层对象通常通过底层 I&#x2F;O 服务间接受到影响。</p><h3 id="误解二：notify-fork-会调用-fork"><a href="#误解二：notify-fork-会调用-fork" class="headerlink" title="误解二：notify_fork() 会调用 fork()"></a>误解二：<code>notify_fork()</code> 会调用 <code>fork()</code></h3><p>错误。</p><p>它只广播 <code>PREPARE</code>、<code>PARENT</code> 和 <code>CHILD</code> 事件。</p><h3 id="误解三：销毁-Context-会自动-shutdown-所有服务"><a href="#误解三：销毁-Context-会自动-shutdown-所有服务" class="headerlink" title="误解三：销毁 Context 会自动 shutdown 所有服务"></a>误解三：销毁 Context 会自动 shutdown 所有服务</h3><p>错误。</p><p><code>io_ctx_destroy()</code> 只释放 Context 自身资源；C++ <code>Context</code> 析构也只调用 destroy。</p><h3 id="误解四：整个程序只能存在一个-Context"><a href="#误解四：整个程序只能存在一个-Context" class="headerlink" title="误解四：整个程序只能存在一个 Context"></a>误解四：整个程序只能存在一个 Context</h3><p>错误。</p><p>一个底层 Context 只有一个 C++ 所有者，但一个进程可以创建多个独立 Context。</p><h3 id="误解五：Context-有锁，所以-shutdown-期间可以任意删除服务"><a href="#误解五：Context-有锁，所以-shutdown-期间可以任意删除服务" class="headerlink" title="误解五：Context 有锁，所以 shutdown 期间可以任意删除服务"></a>误解五：Context 有锁，所以 shutdown 期间可以任意删除服务</h3><p>错误。</p><p>官方接口明确要求，fork 通知和 shutdown 运行期间不得注销服务。</p><h3 id="误解六：所有服务都必须实现-fork-处理"><a href="#误解六：所有服务都必须实现-fork-处理" class="headerlink" title="误解六：所有服务都必须实现 fork 处理"></a>误解六：所有服务都必须实现 fork 处理</h3><p>错误。</p><p><code>notify_fork</code> 和 <code>shutdown</code> 回调都可以为 <code>NULL</code>。具体服务按需求实现。</p><hr><h2 id="14-总结"><a href="#14-总结" class="headerlink" title="14. 总结"></a>14. 总结</h2><p><code>ctx.c</code> 的代码量不大，但其设计边界非常清晰：</p><blockquote><p><code>io_ctx</code> 是 Lely I&#x2F;O 层的服务注册表和生命周期协调器。</p></blockquote><p>它通过一条受互斥锁保护的双向链表维护 <code>io_svc</code>，并通过虚表将 fork 和 shutdown 事件转发给具体服务。</p><p>其关键机制可以归纳为：</p><ol><li><strong>显式注册</strong>：服务初始化后加入 Context，销毁前移除；</li><li><strong>接口统一</strong>：所有服务通过 <code>io_svc_vtbl</code> 暴露可选的 fork 和 shutdown 回调；</li><li><strong>顺序确定</strong>：<ul><li>fork prepare 逆序；</li><li>fork parent&#x2F;child 正序；</li><li>shutdown 逆序；</li></ul></li><li><strong>回调解锁</strong>：调用服务回调时不持有 Context 锁；</li><li><strong>调用方同步</strong>：通知和 shutdown 期间不得注销服务；</li><li><strong>错误保留</strong>：fork 通知继续遍历，并返回第一个失败服务的错误；</li><li><strong>关闭幂等</strong>：每个服务的 shutdown 最多执行一次；</li><li><strong>所有权明确</strong>：C++ <code>Context</code> 不可复制、可以移动；</li><li><strong>非单例设计</strong>：一个进程可以拥有多个独立 Context；</li><li><strong>shutdown 与 destroy 分离</strong>：服务终止和 Context 内存释放是两个不同阶段。</li></ol><p>因此，对这套机制最准确的一句话描述是：</p><blockquote><p><code>ctx.c</code> 维护一个 I&#x2F;O 上下文，通过链表统一登记 Lely 的底层 I&#x2F;O 服务；当用户主动执行进程 fork 协调或系统 shutdown 时，Context 按确定顺序遍历服务并调用各自的维护回调。每个具体 Context 实例采用唯一所有权，但程序并非只能创建一个 Context。</p></blockquote><hr>]]></content>
    
    
      
      
    <summary type="html">&lt;meta name=&quot;referrer&quot; content=&quot;no-referrer&quot; /&gt;

&lt;h1 id=&quot;Lely-CANopen-：I-O-服务-io-ctx&quot;&gt;&lt;a href=&quot;#Lely-CANopen-：I-O-服务-io-ctx&quot; class=&quot;headerlin</summary>
      
    
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/categories/canopen/"/>
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/tags/canopen/"/>
    
  </entry>
  
  <entry>
    <title>Lely CANopen Timer 的实现原理与运行流程</title>
    <link href="https://wdfk-prog.space/posts/cb8be560/"/>
    <id>https://wdfk-prog.space/posts/cb8be560/</id>
    <published>2026-07-27T06:50:36.000Z</published>
    <updated>2026-07-27T06:52:15.586Z</updated>
    
    <content type="html"><![CDATA[<meta name="referrer" content="no-referrer" /><h1 id="Lely-CANopen-Timer-的实现原理与运行流程"><a href="#Lely-CANopen-Timer-的实现原理与运行流程" class="headerlink" title="Lely CANopen: Timer 的实现原理与运行流程"></a>Lely CANopen: Timer 的实现原理与运行流程</h1><p><img src="https://i-blog.csdnimg.cn/direct/a588a14772624848b2c99235617bfe3f.png" alt="在这里插入图片描述"></p><p>@[toc]</p><h2 id="一句话结论"><a href="#一句话结论" class="headerlink" title="一句话结论"></a>一句话结论</h2><p>Lely 的 <code>lely::io::Timer</code> 不是一个独立线程，也不是一个直接执行回调的传统软件定时器。它本质上是一个异步事件适配器：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">Linux timerfd 负责计时</span><br><span class="line">        ↓</span><br><span class="line">io_poll 负责监听 timerfd 的可读事件</span><br><span class="line">        ↓</span><br><span class="line">内部 wait_task 负责读取到期计数</span><br><span class="line">        ↓</span><br><span class="line">ev_exec 负责调度完成任务</span><br><span class="line">        ↓</span><br><span class="line">io_timer_wait 对应的回调最终被执行</span><br></pre></td></tr></table></figure><p>创建 Timer、设置到期时间和提交等待操作是三个不同动作：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">创建 Timer：建立 timerfd 并注册到 io_poll</span><br><span class="line">设置时间：调用 timerfd_settime()，决定何时到期</span><br><span class="line">提交等待：把 io_timer_wait 放入 wait_queue，决定到期后通知谁</span><br></pre></td></tr></table></figure><p>从 CANopen 主站视角看，还要再增加两层调度：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">BasicMaster / Node</span><br><span class="line">        ↓ 传入专用 io::TimerBase&amp;</span><br><span class="line">io::CanNet / io_can_net</span><br><span class="line">        ↓ 创建 io_tqueue，并注册 can_net 的 next 回调</span><br><span class="line">io_tqueue</span><br><span class="line">        ↓ 用一个底层 io_timer_wait 复用同一个系统 Timer</span><br><span class="line">can_net.timer_heap</span><br><span class="line">        ↓ 管理 NMT、Heartbeat、SDO、PDO、SYNC 等 can_timer_t</span><br><span class="line">具体协议定时回调</span><br></pre></td></tr></table></figure><p>因此，Lely CANopen 的核心设计不是“每个协议功能创建一个 Linux timerfd”，而是：</p><blockquote><p>一个 CANopen Node 使用一个专用系统 Timer；<code>io_tqueue</code> 和 <code>can_net</code> 的两级最小堆把大量逻辑超时复用到这个 Timer 上。</p></blockquote><hr><h2 id="1-从-C-创建语句开始"><a href="#1-从-C-创建语句开始" class="headerlink" title="1. 从 C++ 创建语句开始"></a>1. 从 C++ 创建语句开始</h2><p>上层代码：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">impl_-&gt;timer.<span class="built_in">reset</span>(</span><br><span class="line">    <span class="keyword">new</span> lely::io::<span class="built_in">Timer</span>(</span><br><span class="line">        *impl_-&gt;poll,</span><br><span class="line">        *impl_-&gt;executor,</span><br><span class="line">        CLOCK_MONOTONIC));</span><br></pre></td></tr></table></figure><p>构造函数：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">Timer</span>(<span class="type">io_poll_t</span>* poll, <span class="type">ev_exec_t</span>* exec, <span class="type">clockid_t</span> clockid)</span><br><span class="line">    : <span class="built_in">TimerBase</span>(<span class="built_in">io_timer_create</span>(poll, exec, clockid)) &#123;</span><br><span class="line">  <span class="keyword">if</span> (!timer)</span><br><span class="line">    util::<span class="built_in">throw_errc</span>(<span class="string">&quot;Timer&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>可以确认的创建链路为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">lely::io::Timer::Timer()</span><br><span class="line">  └─ io_timer_create()</span><br><span class="line">      ├─ io_timer_alloc()</span><br><span class="line">      └─ io_timer_init()</span><br><span class="line">          ├─ 初始化 io_timer_impl</span><br><span class="line">          ├─ 初始化 watch 回调</span><br><span class="line">          ├─ 初始化内部 wait_task</span><br><span class="line">          ├─ io_timer_impl_open()</span><br><span class="line">          │   ├─ timerfd_create()</span><br><span class="line">          │   └─ io_poll_watch()</span><br><span class="line">          └─ io_ctx_insert()</span><br></pre></td></tr></table></figure><p>这里完成的是“创建 I&#x2F;O 定时器基础设施”，还没有指定具体的到期时刻。</p><h3 id="1-1-CLOCK-MONOTONIC-的作用"><a href="#1-1-CLOCK-MONOTONIC-的作用" class="headerlink" title="1.1 CLOCK_MONOTONIC 的作用"></a>1.1 <code>CLOCK_MONOTONIC</code> 的作用</h3><p>Timer 使用：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">CLOCK_MONOTONIC</span><br></pre></td></tr></table></figure><p>表示定时器基于单调时钟运行。它适合协议超时和周期调度，因为系统日历时间被人工修改时，单调时钟不会发生向前或向后的跳变。</p><p>对 CANopen 而言，心跳超时、SDO 超时、PDO 事件定时、SYNC 周期等都属于持续时间或截止时间问题，而不是日历时间问题，因此使用单调时钟符合这类任务的需求。</p><hr><h2 id="2-io-timer-impl：Timer-的真实内部对象"><a href="#2-io-timer-impl：Timer-的真实内部对象" class="headerlink" title="2. io_timer_impl：Timer 的真实内部对象"></a>2. <code>io_timer_impl</code>：Timer 的真实内部对象</h2><p>Linux 实现中的核心结构体为：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">io_timer_impl</span> &#123;</span></span><br><span class="line">    <span class="type">const</span> <span class="class"><span class="keyword">struct</span> <span class="title">io_dev_vtbl</span> *<span class="title">dev_vptr</span>;</span></span><br><span class="line">    <span class="type">const</span> <span class="class"><span class="keyword">struct</span> <span class="title">io_timer_vtbl</span> *<span class="title">timer_vptr</span>;</span></span><br><span class="line">    <span class="type">io_poll_t</span> *poll;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">io_svc</span> <span class="title">svc</span>;</span></span><br><span class="line">    <span class="type">io_ctx_t</span> *ctx;</span><br><span class="line">    <span class="type">ev_exec_t</span> *exec;</span><br><span class="line">    <span class="type">clockid_t</span> clockid;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">io_poll_watch</span> <span class="title">watch</span>;</span></span><br><span class="line">    <span class="type">int</span> tfd;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">ev_task</span> <span class="title">wait_task</span>;</span></span><br><span class="line"><span class="meta">#<span class="keyword">if</span> !LELY_NO_THREADS</span></span><br><span class="line">    <span class="type">pthread_mutex_t</span> mtx;</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line">    <span class="type">unsigned</span> shutdown : <span class="number">1</span>;</span><br><span class="line">    <span class="type">unsigned</span> wait_posted : <span class="number">1</span>;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sllist</span> <span class="title">wait_queue</span>;</span></span><br><span class="line">    <span class="type">int</span> overrun;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>关键字段如下：</p><table><thead><tr><th>字段</th><th>职责</th></tr></thead><tbody><tr><td><code>poll</code></td><td>监听 <code>timerfd</code> 是否出现可读事件</td></tr><tr><td><code>exec</code></td><td>执行内部任务和等待完成任务</td></tr><tr><td><code>clockid</code></td><td>指定 <code>CLOCK_MONOTONIC</code> 或 <code>CLOCK_REALTIME</code></td></tr><tr><td><code>tfd</code></td><td>Linux <code>timerfd</code> 文件描述符</td></tr><tr><td><code>watch</code></td><td>注册到 <code>io_poll</code> 的监听对象</td></tr><tr><td><code>wait_task</code></td><td><code>timerfd</code> 就绪后投递给 executor 的内部处理任务</td></tr><tr><td><code>wait_queue</code></td><td>保存等待下一次 Timer 完成的 <code>io_timer_wait</code></td></tr><tr><td><code>wait_posted</code></td><td>防止同一到期事件重复投递内部任务</td></tr><tr><td><code>shutdown</code></td><td>标记 Timer 已进入关闭流程</td></tr><tr><td><code>overrun</code></td><td>保存额外到期次数</td></tr><tr><td><code>mtx</code></td><td>保护等待队列和状态位</td></tr></tbody></table><h3 id="2-1-一个对象同时暴露多个接口"><a href="#2-1-一个对象同时暴露多个接口" class="headerlink" title="2.1 一个对象同时暴露多个接口"></a>2.1 一个对象同时暴露多个接口</h3><p><code>io_timer_impl</code> 同时包含：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">dev_vptr    → io_dev_t 接口</span><br><span class="line">timer_vptr  → io_timer_t 接口</span><br><span class="line">svc         → io_svc 服务接口</span><br></pre></td></tr></table></figure><p>Lely 使用类似 <code>container_of()</code> 的 <code>structof()</code> 机制，由成员地址反推出完整对象地址：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">static</span> <span class="keyword">inline</span> <span class="keyword">struct</span> io_timer_impl *</span><br><span class="line"><span class="title function_">io_timer_impl_from_timer</span><span class="params">(<span class="type">const</span> <span class="type">io_timer_t</span> *timer)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">return</span> structof(timer, <span class="keyword">struct</span> io_timer_impl, timer_vptr);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这也是为什么 <code>io_timer_alloc()</code> 返回的不是 <code>impl</code>，而是：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">return</span> &amp;impl-&gt;timer_vptr;</span><br></pre></td></tr></table></figure><hr><h2 id="3-打开-Timer：timerfd-create-与-io-poll-watch"><a href="#3-打开-Timer：timerfd-create-与-io-poll-watch" class="headerlink" title="3. 打开 Timer：timerfd_create() 与 io_poll_watch()"></a>3. 打开 Timer：<code>timerfd_create()</code> 与 <code>io_poll_watch()</code></h2><p>真正创建 Linux 定时器的代码位于：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">static</span> <span class="type">int</span></span><br><span class="line"><span class="title function_">io_timer_impl_open</span><span class="params">(<span class="keyword">struct</span> io_timer_impl *impl)</span></span><br><span class="line">&#123;</span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line">    impl-&gt;tfd = timerfd_create(</span><br><span class="line">            impl-&gt;clockid,</span><br><span class="line">            TFD_NONBLOCK | TFD_CLOEXEC);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (impl-&gt;tfd == <span class="number">-1</span>)</span><br><span class="line">        ...</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (io_poll_watch(</span><br><span class="line">            impl-&gt;poll,</span><br><span class="line">            impl-&gt;tfd,</span><br><span class="line">            IO_EVENT_IN,</span><br><span class="line">            &amp;impl-&gt;watch) == <span class="number">-1</span>)</span><br><span class="line">        ...</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="3-1-TFD-NONBLOCK"><a href="#3-1-TFD-NONBLOCK" class="headerlink" title="3.1 TFD_NONBLOCK"></a>3.1 <code>TFD_NONBLOCK</code></h3><p><code>TFD_NONBLOCK</code> 让：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">read(impl-&gt;tfd, ...)</span><br></pre></td></tr></table></figure><p>在当前没有到期计数可读时立即返回 <code>EAGAIN</code> 或 <code>EWOULDBLOCK</code>，而不是阻塞 executor 线程。</p><p>这是异步事件循环中的关键要求。若内部处理任务在 <code>read()</code> 上阻塞，使用同一个 executor 的其他 CANopen 任务也可能被阻塞。</p><h3 id="3-2-TFD-CLOEXEC"><a href="#3-2-TFD-CLOEXEC" class="headerlink" title="3.2 TFD_CLOEXEC"></a>3.2 <code>TFD_CLOEXEC</code></h3><p><code>TFD_CLOEXEC</code> 表示进程执行 <code>exec()</code> 加载新程序时，内核自动关闭该文件描述符，避免子程序意外继承 Timer 资源。</p><p>它只影响 <code>exec()</code>，不阻止 <code>fork()</code> 继承。<code>fork()</code> 的处理由后面的 <code>io_timer_impl_svc_notify_fork()</code> 完成。</p><h3 id="3-3-io-poll-watch-IO-EVENT-IN"><a href="#3-3-io-poll-watch-IO-EVENT-IN" class="headerlink" title="3.3 io_poll_watch(..., IO_EVENT_IN, ...)"></a>3.3 <code>io_poll_watch(..., IO_EVENT_IN, ...)</code></h3><p>该调用的语义是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">请 io_poll 监听 impl-&gt;tfd；</span><br><span class="line">当该 fd 可读时，通过 impl-&gt;watch 调用注册的 watch 回调。</span><br></pre></td></tr></table></figure><p><code>IO_EVENT_IN</code> 对应“可读事件”。对 timerfd 而言，可读意味着至少发生过一次定时到期，内核中存在可读取的 64 位到期计数。</p><p>这条 <code>if</code> 判断的是“注册监听是否失败”，不是判断 Timer 是否已经到期。</p><hr><h2 id="4-设置时间：io-timer-impl-settime"><a href="#4-设置时间：io-timer-impl-settime" class="headerlink" title="4. 设置时间：io_timer_impl_settime()"></a>4. 设置时间：<code>io_timer_impl_settime()</code></h2><p>Timer 的虚函数表包含：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">static</span> <span class="type">const</span> <span class="class"><span class="keyword">struct</span> <span class="title">io_timer_vtbl</span> <span class="title">io_timer_impl_vtbl</span> =</span> &#123;</span><br><span class="line">    &amp;io_timer_impl_get_dev,</span><br><span class="line">    &amp;io_timer_impl_get_clock,</span><br><span class="line">    &amp;io_timer_impl_getoverrun,</span><br><span class="line">    &amp;io_timer_impl_gettime,</span><br><span class="line">    &amp;io_timer_impl_settime,</span><br><span class="line">    &amp;io_timer_impl_submit_wait</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>其中设置时间的实现为：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">static</span> <span class="type">int</span></span><br><span class="line"><span class="title function_">io_timer_impl_settime</span><span class="params">(<span class="type">io_timer_t</span> *timer, <span class="type">int</span> flags,</span></span><br><span class="line"><span class="params">        <span class="type">const</span> <span class="keyword">struct</span> itimerspec *value, <span class="keyword">struct</span> itimerspec *ovalue)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">io_timer_impl</span> *<span class="title">impl</span> =</span> io_timer_impl_from_timer(timer);</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> flags_ = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">if</span> (flags &amp; TIMER_ABSTIME)</span><br><span class="line">        flags_ |= TFD_TIMER_ABSTIME;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> timerfd_settime(impl-&gt;tfd, flags_, value, ovalue);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="4-1-flags-TFD-TIMER-ABSTIME-是什么"><a href="#4-1-flags-TFD-TIMER-ABSTIME-是什么" class="headerlink" title="4.1 flags_ |= TFD_TIMER_ABSTIME 是什么"></a>4.1 <code>flags_ |= TFD_TIMER_ABSTIME</code> 是什么</h3><p>这句把 Lely 通用定时接口中的：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">TIMER_ABSTIME</span><br></pre></td></tr></table></figure><p>转换为 Linux timerfd 使用的：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">TFD_TIMER_ABSTIME</span><br></pre></td></tr></table></figure><p>两种模式的区别：</p><table><thead><tr><th>模式</th><th><code>it_value</code> 的含义</th></tr></thead><tbody><tr><td>未设置 <code>TFD_TIMER_ABSTIME</code></td><td>从现在开始再等待多长时间</td></tr><tr><td>设置 <code>TFD_TIMER_ABSTIME</code></td><td>当指定 clock 到达哪个绝对时间点时到期</td></tr></tbody></table><p>假设当前 <code>CLOCK_MONOTONIC</code> 为 100 秒，<code>it_value</code> 为 10 秒：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">相对模式：在 110 秒时到期</span><br><span class="line">绝对模式：目标时刻是 10 秒，已过期，因此会立即就绪</span><br></pre></td></tr></table></figure><p>绝对时间模式常用于避免周期调度的累计漂移。上层可以始终维护固定截止时间轴，而不是在每次任务执行完后再重新增加一个相对周期。</p><p>需要注意：<code>TFD_TIMER_ABSTIME</code> 主要改变首次到期值 <code>it_value</code> 的解释方式；周期字段 <code>it_interval</code> 仍表示每次到期后的相对间隔。</p><h3 id="4-2-TimerBase-的完整-C-API-已可确认"><a href="#4-2-TimerBase-的完整-C-API-已可确认" class="headerlink" title="4.2 TimerBase 的完整 C++ API 已可确认"></a>4.2 <code>TimerBase</code> 的完整 C++ API 已可确认</h3><p>官方 <a href="https://github.com/lely-industries/lely-core/blob/620d1858eb8520dbc3dc5e1a7314565becd54199/include/lely/io2/timer.hpp"><code>include/lely/io2/timer.hpp</code></a> 显示，<code>TimerBase</code> 不是只有一个模糊的“异步等待接口”，而是提供了完整的时间和等待操作封装：</p><table><thead><tr><th>C++ 接口</th><th>下层 C 接口</th><th>语义</th></tr></thead><tbody><tr><td><code>get_clock()</code></td><td><code>io_timer_get_clock()</code></td><td>获取 Timer 使用的时钟</td></tr><tr><td><code>getoverrun()</code></td><td><code>io_timer_getoverrun()</code></td><td>获取最近一次额外到期次数</td></tr><tr><td><code>gettime()</code></td><td><code>io_timer_gettime()</code></td><td>获取剩余时间和周期</td></tr><tr><td><code>settime(duration, period)</code></td><td><code>io_timer_settime(..., 0, ...)</code></td><td>按相对时间设置</td></tr><tr><td><code>settime(time_point, period)</code></td><td><code>io_timer_settime(..., TIMER_ABSTIME, ...)</code></td><td>按绝对时间设置</td></tr><tr><td><code>submit_wait(io_timer_wait&amp;)</code></td><td><code>io_timer_submit_wait()</code></td><td>提交已有等待对象</td></tr><tr><td><code>submit_wait(F)</code></td><td><code>make_timer_wait_wrapper()</code></td><td>包装回调并提交；完成后自动释放 wrapper</td></tr><tr><td><code>cancel_wait()</code></td><td><code>io_timer_cancel_wait()</code></td><td>取消并以错误结果完成</td></tr><tr><td><code>abort_wait()</code></td><td><code>io_timer_abort_wait()</code></td><td>移除任务，不走正常完成路径</td></tr><tr><td><code>async_wait()</code></td><td><code>io_timer_async_wait()</code></td><td>返回 <code>ev::Future&lt;int, int&gt;</code></td></tr></tbody></table><p>其中，回调包装器最终执行的函数签名为：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">void</span>(<span class="type">int</span> overrun, std::error_code ec)</span><br></pre></td></tr></table></figure><p><code>overrun == -1</code> 时，<code>ec</code> 由 <code>wait-&gt;r.errc</code> 构造；正常到期时，<code>overrun</code> 为额外到期次数。<code>make_timer_wait_wrapper()</code> 创建的对象在任务执行完成后会 <code>delete self</code>，因此提交以后调用方不能再手动释放它。</p><hr><h2 id="5-提交等待：io-timer-impl-submit-wait"><a href="#5-提交等待：io-timer-impl-submit-wait" class="headerlink" title="5. 提交等待：io_timer_impl_submit_wait()"></a>5. 提交等待：<code>io_timer_impl_submit_wait()</code></h2><p>实现如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">static</span> <span class="type">void</span></span><br><span class="line"><span class="title function_">io_timer_impl_submit_wait</span><span class="params">(<span class="type">io_timer_t</span> *timer, <span class="keyword">struct</span> io_timer_wait *wait)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">io_timer_impl</span> *<span class="title">impl</span> =</span> io_timer_impl_from_timer(timer);</span><br><span class="line">    assert(wait);</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">ev_task</span> *<span class="title">task</span> =</span> &amp;wait-&gt;task;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (!task-&gt;exec)</span><br><span class="line">        task-&gt;exec = impl-&gt;exec;</span><br><span class="line">    ev_exec_on_task_init(task-&gt;exec);</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">if</span> !LELY_NO_THREADS</span></span><br><span class="line">    pthread_mutex_lock(&amp;impl-&gt;mtx);</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line">    <span class="keyword">if</span> (impl-&gt;shutdown) &#123;</span><br><span class="line"><span class="meta">#<span class="keyword">if</span> !LELY_NO_THREADS</span></span><br><span class="line">        pthread_mutex_unlock(&amp;impl-&gt;mtx);</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line">        io_timer_wait_post(wait, <span class="number">-1</span>, ECANCELED);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        sllist_push_back(&amp;impl-&gt;wait_queue, &amp;task-&gt;_node);</span><br><span class="line"><span class="meta">#<span class="keyword">if</span> !LELY_NO_THREADS</span></span><br><span class="line">        pthread_mutex_unlock(&amp;impl-&gt;mtx);</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="5-1-它提交的不是“立即执行任务”"><a href="#5-1-它提交的不是“立即执行任务”" class="headerlink" title="5.1 它提交的不是“立即执行任务”"></a>5.1 它提交的不是“立即执行任务”</h3><p>更准确的语义是：</p><blockquote><p>将一个一次性的异步等待操作登记到 Timer；当 Timer 下一次到期、出错或被取消时，再把该等待对象中的任务投递给 executor。</p></blockquote><p>因此：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">io_timer_impl_submit_wait()</span><br><span class="line">    ≠ 启动 Timer</span><br><span class="line">    ≠ 设置 Timer 时间</span><br><span class="line">    ≠ 立即调用回调</span><br><span class="line">    = 把 io_timer_wait 加入 wait_queue</span><br></pre></td></tr></table></figure><h3 id="5-2-默认-executor"><a href="#5-2-默认-executor" class="headerlink" title="5.2 默认 executor"></a>5.2 默认 executor</h3><p>如果调用者没有为 <code>wait-&gt;task</code> 指定 executor：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (!task-&gt;exec)</span><br><span class="line">    task-&gt;exec = impl-&gt;exec;</span><br></pre></td></tr></table></figure><p>就使用创建 Timer 时传入的默认 executor。</p><h3 id="5-3-ev-exec-on-task-init"><a href="#5-3-ev-exec-on-task-init" class="headerlink" title="5.3 ev_exec_on_task_init()"></a>5.3 <code>ev_exec_on_task_init()</code></h3><p>这不是执行任务，而是通知 executor：存在一个尚未完成的异步任务。它与完成时的：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ev_exec_on_task_fini(exec);</span><br></pre></td></tr></table></figure><p>形成生命周期配对。</p><h3 id="5-4-shutdown-后提交"><a href="#5-4-shutdown-后提交" class="headerlink" title="5.4 shutdown 后提交"></a>5.4 shutdown 后提交</h3><p>如果 Timer 已关闭，新提交的 wait 不会进入队列，而是以：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">result = -1</span><br><span class="line">errc   = ECANCELED</span><br></pre></td></tr></table></figure><p>完成，并被投递给 executor。这样调用者不会得到一个永远无法结束的悬空异步操作。</p><h3 id="5-5-同一个-wait-不能重复挂入"><a href="#5-5-同一个-wait-不能重复挂入" class="headerlink" title="5.5 同一个 wait 不能重复挂入"></a>5.5 同一个 wait 不能重复挂入</h3><p>队列保存的是：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">&amp;task-&gt;_node</span><br></pre></td></tr></table></figure><p>同一个链表节点在完成或取消前不能重复加入同一个或其他队列，否则可能破坏链表结构。</p><hr><h2 id="6-Timer-到期后发生什么"><a href="#6-Timer-到期后发生什么" class="headerlink" title="6. Timer 到期后发生什么"></a>6. Timer 到期后发生什么</h2><p>核心流程分成两个阶段：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">poll 阶段：发现 fd 就绪，投递内部任务</span><br><span class="line">executor 阶段：读取 timerfd，完成所有 wait</span><br></pre></td></tr></table></figure><h3 id="6-1-第一阶段：io-timer-impl-watch-func"><a href="#6-1-第一阶段：io-timer-impl-watch-func" class="headerlink" title="6.1 第一阶段：io_timer_impl_watch_func()"></a>6.1 第一阶段：<code>io_timer_impl_watch_func()</code></h3><p>创建时已经绑定：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">impl-&gt;watch = IO_POLL_WATCH_INIT(</span><br><span class="line">        &amp;io_timer_impl_watch_func);</span><br></pre></td></tr></table></figure><p>因此 timerfd 可读时，<code>io_poll</code> 调用：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">static</span> <span class="type">void</span></span><br><span class="line"><span class="title function_">io_timer_impl_watch_func</span><span class="params">(<span class="keyword">struct</span> io_poll_watch *watch, <span class="type">int</span> events)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">io_timer_impl</span> *<span class="title">impl</span> =</span></span><br><span class="line">            structof(watch, <span class="keyword">struct</span> io_timer_impl, watch);</span><br><span class="line"></span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> post_wait = !impl-&gt;wait_posted &amp;&amp; !impl-&gt;shutdown;</span><br><span class="line">    <span class="keyword">if</span> (post_wait)</span><br><span class="line">        impl-&gt;wait_posted = <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (post_wait)</span><br><span class="line">        ev_exec_post(impl-&gt;wait_task.exec, &amp;impl-&gt;wait_task);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>它不读取 timerfd，也不直接执行用户回调，只做两件事：</p><ol><li>用 <code>wait_posted</code> 防止重复投递；</li><li>把内部 <code>wait_task</code> 投递给 executor。</li></ol><p>这样可避免在 I&#x2F;O poll 回调上下文中执行较重的处理逻辑。</p><h3 id="6-2-为什么需要-wait-posted"><a href="#6-2-为什么需要-wait-posted" class="headerlink" title="6.2 为什么需要 wait_posted"></a>6.2 为什么需要 <code>wait_posted</code></h3><p>如果 poll 在内部处理任务完成前多次报告同一 fd 可读，而每次都投递一个读取任务，就可能出现多个任务竞争读取同一个 timerfd。</p><p><code>wait_posted</code> 保证任意时刻最多有一个内部读取任务待执行：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">第一次事件：wait_posted 0 → 1，投递 wait_task</span><br><span class="line">后续重复事件：wait_posted 已为 1，不重复投递</span><br><span class="line">任务处理结束：wait_posted 重新置 0</span><br></pre></td></tr></table></figure><hr><h2 id="7-第二阶段：io-timer-impl-wait-task-func"><a href="#7-第二阶段：io-timer-impl-wait-task-func" class="headerlink" title="7. 第二阶段：io_timer_impl_wait_task_func()"></a>7. 第二阶段：<code>io_timer_impl_wait_task_func()</code></h2><p>内部任务由 executor 执行：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">static</span> <span class="type">void</span></span><br><span class="line"><span class="title function_">io_timer_impl_wait_task_func</span><span class="params">(<span class="keyword">struct</span> ev_task *task)</span></span><br></pre></td></tr></table></figure><p>该函数是整个 Timer 完成路径的核心。</p><h3 id="7-1-读取-timerfd-到期计数"><a href="#7-1-读取-timerfd-到期计数" class="headerlink" title="7.1 读取 timerfd 到期计数"></a>7.1 读取 timerfd 到期计数</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">uintmax_t</span> value = <span class="number">0</span>;</span><br><span class="line">result = read(impl-&gt;tfd, &amp;value, <span class="keyword">sizeof</span>(value));</span><br></pre></td></tr></table></figure><p>一次成功读取返回 8 字节计数，表示自上次成功读取后累计发生了多少次到期。</p><p>例如周期为 10 ms，但 executor 35 ms 后才处理，该值可能为 3。</p><p>代码循环读取，直到返回 <code>EAGAIN</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">持续读取所有当前积压的到期计数</span><br><span class="line">        ↓</span><br><span class="line">读空后得到 EAGAIN</span><br><span class="line">        ↓</span><br><span class="line">重新监听下一次 IO_EVENT_IN</span><br></pre></td></tr></table></figure><p>因为创建时设置了 <code>TFD_NONBLOCK</code>，读空时不会阻塞。</p><h3 id="7-2-overrun-的含义"><a href="#7-2-overrun-的含义" class="headerlink" title="7.2 overrun 的含义"></a>7.2 <code>overrun</code> 的含义</h3><p>变量初始为：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> overrun = <span class="number">-1</span>;</span><br></pre></td></tr></table></figure><p>每读取到 <code>value</code> 后执行累计。结果语义为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">overrun = 总到期次数 - 1</span><br></pre></td></tr></table></figure><p>示例：</p><table><thead><tr><th align="right">到期计数</th><th align="right"><code>overrun</code></th><th>含义</th></tr></thead><tbody><tr><td align="right">1</td><td align="right">0</td><td>正常到期一次</td></tr><tr><td align="right">2</td><td align="right">1</td><td>额外积压一次</td></tr><tr><td align="right">3</td><td align="right">2</td><td>额外积压两次</td></tr></tbody></table><p>代码使用饱和处理避免超过 <code>INT_MAX</code>。</p><h3 id="7-3-将内部-wait-队列整体转移到局部队列"><a href="#7-3-将内部-wait-队列整体转移到局部队列" class="headerlink" title="7.3 将内部 wait 队列整体转移到局部队列"></a>7.3 将内部 wait 队列整体转移到局部队列</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sllist</span> <span class="title">queue</span>;</span></span><br><span class="line">sllist_init(&amp;<span class="built_in">queue</span>);</span><br><span class="line"></span><br><span class="line">...</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (overrun &gt;= <span class="number">0</span> || errno)</span><br><span class="line">    sllist_append(&amp;<span class="built_in">queue</span>, &amp;impl-&gt;wait_queue);</span><br></pre></td></tr></table></figure><p>这里不是复制，而是将当前 <code>impl-&gt;wait_queue</code> 中的等待对象整体移入局部 <code>queue</code>。</p><p>效果可以理解为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">转移前：impl-&gt;wait_queue = [A, B, C]</span><br><span class="line">转移后：impl-&gt;wait_queue = []</span><br><span class="line">        local queue      = [A, B, C]</span><br></pre></td></tr></table></figure><p>在解锁后新提交的 wait 会进入新的 <code>impl-&gt;wait_queue</code>，不会被本次到期事件完成。</p><h3 id="7-4-Linux-后端确定使用-epoll-EPOLLONESHOT"><a href="#7-4-Linux-后端确定使用-epoll-EPOLLONESHOT" class="headerlink" title="7.4 Linux 后端确定使用 epoll + EPOLLONESHOT"></a>7.4 Linux 后端确定使用 <code>epoll + EPOLLONESHOT</code></h3><p>官方 <a href="https://github.com/lely-industries/lely-core/blob/620d1858eb8520dbc3dc5e1a7314565becd54199/src/io2/linux/poll.c"><code>src/io2/linux/poll.c</code></a> 已经直接证明 Linux 后端的行为：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> EPOLL_EVENT_INIT(events, fd) \</span></span><br><span class="line"><span class="meta">    &#123; mapped_events | EPOLLONESHOT, &#123; .fd = (fd) &#125; &#125;</span></span><br></pre></td></tr></table></figure><p>事件映射关系为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">IO_EVENT_IN  → EPOLLIN | EPOLLRDHUP</span><br><span class="line">IO_EVENT_PRI → EPOLLPRI</span><br><span class="line">IO_EVENT_OUT → EPOLLOUT</span><br><span class="line">并始终附加 EPOLLONESHOT</span><br></pre></td></tr></table></figure><p><code>io_poll_watch()</code> 使用 <code>epoll_ctl()</code> 完成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">第一次监听      → EPOLL_CTL_ADD</span><br><span class="line">修改/重新激活   → EPOLL_CTL_MOD</span><br><span class="line">events == 0     → EPOLL_CTL_DEL</span><br></pre></td></tr></table></figure><p>轮询线程通过 <code>epoll_pwait()</code> 获取事件，<code>io_poll_process()</code> 在调用 watch 回调之前先执行：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">watch-&gt;_events = <span class="number">0</span>;</span><br><span class="line">poll-&gt;nwatch--;</span><br></pre></td></tr></table></figure><p>这意味着一次事件发生后，该 watch 在 Lely 的内部状态中也被标记为未激活。Timer 在读空 <code>timerfd</code>、得到 <code>EAGAIN</code> 后执行：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">events |= IO_EVENT_IN;</span><br><span class="line">io_poll_watch(impl-&gt;poll, impl-&gt;tfd, events, &amp;impl-&gt;watch);</span><br></pre></td></tr></table></figure><p>不是保守性重复注册，而是对 <code>EPOLLONESHOT</code> 的必要 rearm。完整路径已经可以确定为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">epoll_pwait()</span><br><span class="line">  → io_poll_process()</span><br><span class="line">      → watch-&gt;_events = 0</span><br><span class="line">      → io_timer_impl_watch_func()</span><br><span class="line">          → executor 读取 timerfd</span><br><span class="line">          → io_poll_watch(..., IO_EVENT_IN, ...)</span><br><span class="line">              → epoll_ctl(EPOLL_CTL_MOD)</span><br></pre></td></tr></table></figure><hr><h2 id="8-io-timer-wait-queue-post-会调用所有回调吗"><a href="#8-io-timer-wait-queue-post-会调用所有回调吗" class="headerlink" title="8. io_timer_wait_queue_post() 会调用所有回调吗"></a>8. <code>io_timer_wait_queue_post()</code> 会调用所有回调吗</h2><p>内部队列完成函数：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">static</span> <span class="keyword">inline</span> <span class="type">size_t</span></span><br><span class="line"><span class="title function_">io_timer_wait_queue_post</span><span class="params">(<span class="keyword">struct</span> sllist *<span class="built_in">queue</span>, <span class="type">int</span> result, <span class="type">int</span> errc)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">size_t</span> n = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">slnode</span> *<span class="title">node</span>;</span></span><br><span class="line">    <span class="keyword">while</span> ((node = sllist_pop_front(<span class="built_in">queue</span>))) &#123;</span><br><span class="line">        <span class="class"><span class="keyword">struct</span> <span class="title">ev_task</span> *<span class="title">task</span> =</span> ev_task_from_node(node);</span><br><span class="line">        <span class="class"><span class="keyword">struct</span> <span class="title">io_timer_wait</span> *<span class="title">wait</span> =</span> io_timer_wait_from_task(task);</span><br><span class="line">        io_timer_wait_post(wait, result, errc);</span><br><span class="line">        n += n &lt; SIZE_MAX;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> n;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>答案是：</p><blockquote><p>它会处理传入局部 <code>queue</code> 中的所有 <code>io_timer_wait</code>，但不是在当前调用栈中直接同步调用所有用户函数，而是逐个把每个 wait 的 <code>ev_task</code> 投递到对应 executor。</p></blockquote><p><code>io_timer_wait_post()</code> 做的是：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">wait-&gt;r.result = result;</span><br><span class="line">wait-&gt;r.errc = errc;</span><br><span class="line"></span><br><span class="line"><span class="type">ev_exec_t</span> *exec = wait-&gt;task.exec;</span><br><span class="line">ev_exec_post(exec, &amp;wait-&gt;task);</span><br><span class="line">ev_exec_on_task_fini(exec);</span><br></pre></td></tr></table></figure><p>所以实际链路为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">io_timer_wait_queue_post()</span><br><span class="line">  └─ 遍历当前局部 queue</span><br><span class="line">      └─ io_timer_wait_post()</span><br><span class="line">          ├─ 写入 result / errc</span><br><span class="line">          ├─ ev_exec_post()</span><br><span class="line">          └─ ev_exec_on_task_fini()</span><br><span class="line">                  ↓</span><br><span class="line">          executor 之后执行 task 绑定的函数</span><br></pre></td></tr></table></figure><h3 id="8-1-“所有”的边界"><a href="#8-1-“所有”的边界" class="headerlink" title="8.1 “所有”的边界"></a>8.1 “所有”的边界</h3><p>这里的“所有”只包括：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">本次被转移到局部 queue 的等待对象</span><br></pre></td></tr></table></figure><p>不包括：</p><ul><li>其他 Timer 的 wait；</li><li>已完成、已取消或已 abort 的 wait；</li><li>队列转移后才提交的新 wait；</li><li>系统中所有历史注册函数；</li><li>未包装为 <code>io_timer_wait</code> 的普通函数。</li></ul><h3 id="8-2-wait-是一次性的"><a href="#8-2-wait-是一次性的" class="headerlink" title="8.2 wait 是一次性的"></a>8.2 wait 是一次性的</h3><p>队列使用：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sllist_pop_front(<span class="built_in">queue</span>)</span><br></pre></td></tr></table></figure><p>每个 wait 完成后都会离开等待队列。因此即使底层 timerfd 是周期性的，一次 <code>submit_wait()</code> 也只完成一次。若上层需要持续接收每个周期，必须重新提交 wait，或者由更高层封装自动重新提交。</p><hr><h2 id="9-正常到期的完整时序"><a href="#9-正常到期的完整时序" class="headerlink" title="9. 正常到期的完整时序"></a>9. 正常到期的完整时序</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant App as 上层/CANopen逻辑</span><br><span class="line">    participant Timer as lely::io::Timer</span><br><span class="line">    participant TFD as Linux timerfd</span><br><span class="line">    participant Poll as io_poll</span><br><span class="line">    participant Exec as ev_exec</span><br><span class="line">    participant Wait as io_timer_wait任务</span><br><span class="line"></span><br><span class="line">    App-&gt;&gt;Timer: 设置 itimerspec</span><br><span class="line">    Timer-&gt;&gt;TFD: timerfd_settime()</span><br><span class="line">    App-&gt;&gt;Timer: submit_wait(wait)</span><br><span class="line">    Timer-&gt;&gt;Timer: wait 加入 wait_queue</span><br><span class="line"></span><br><span class="line">    Note over TFD: 到达截止时间</span><br><span class="line">    TFD--&gt;&gt;Poll: fd 可读 / IO_EVENT_IN</span><br><span class="line">    Poll-&gt;&gt;Timer: io_timer_impl_watch_func()</span><br><span class="line">    Timer-&gt;&gt;Exec: ev_exec_post(wait_task)</span><br><span class="line">    Exec-&gt;&gt;Timer: io_timer_impl_wait_task_func()</span><br><span class="line">    Timer-&gt;&gt;TFD: read() 到期计数</span><br><span class="line">    Timer-&gt;&gt;Timer: 移走当前 wait_queue</span><br><span class="line">    Timer-&gt;&gt;Exec: 逐个 ev_exec_post(wait-&gt;&gt;task)</span><br><span class="line">    Exec-&gt;&gt;Wait: 执行完成任务</span><br><span class="line">    Wait--&gt;&gt;App: result / errc / overrun</span><br></pre></td></tr></table></figure><p>用一句话概括：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">内核产生到期事件，poll 发现事件，executor 读取事件，再由 executor 执行用户等待任务。</span><br></pre></td></tr></table></figure><hr><h2 id="10-取消、abort-与-shutdown"><a href="#10-取消、abort-与-shutdown" class="headerlink" title="10. 取消、abort 与 shutdown"></a>10. 取消、abort 与 shutdown</h2><h3 id="10-1-cancel"><a href="#10-1-cancel" class="headerlink" title="10.1 cancel"></a>10.1 cancel</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">io_timer_impl_dev_cancel()</span><br></pre></td></tr></table></figure><p>将指定 wait 或全部 wait 从队列中取出，然后：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">io_timer_wait_queue_post(&amp;<span class="built_in">queue</span>, <span class="number">-1</span>, ECANCELED);</span><br></pre></td></tr></table></figure><p>调用者仍会收到完成通知，只是结果为取消。</p><h3 id="10-2-abort"><a href="#10-2-abort" class="headerlink" title="10.2 abort"></a>10.2 abort</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">io_timer_impl_dev_abort()</span><br></pre></td></tr></table></figure><p>将任务移出队列并调用：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ev_task_queue_abort(&amp;<span class="built_in">queue</span>);</span><br></pre></td></tr></table></figure><p>语义上，abort 不走正常完成回调路径。</p><h3 id="10-3-shutdown"><a href="#10-3-shutdown" class="headerlink" title="10.3 shutdown"></a>10.3 shutdown</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">static</span> <span class="type">void</span></span><br><span class="line"><span class="title function_">io_timer_impl_svc_shutdown</span><span class="params">(<span class="keyword">struct</span> io_svc *svc)</span></span><br></pre></td></tr></table></figure><p>关闭流程：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">shutdown = 1</span><br><span class="line">    ↓</span><br><span class="line">停止监听 timerfd</span><br><span class="line">    ↓</span><br><span class="line">尝试 abort 已投递的内部 wait_task</span><br><span class="line">    ↓</span><br><span class="line">取消所有仍在 wait_queue 中的等待操作</span><br><span class="line">    ↓</span><br><span class="line">等待者以 ECANCELED 完成</span><br></pre></td></tr></table></figure><p>之后 <code>io_timer_fini()</code> 会关闭 timerfd 并销毁互斥锁。</p><hr><h2 id="11-io-timer-impl-svc-notify-fork-为什么存在"><a href="#11-io-timer-impl-svc-notify-fork-为什么存在" class="headerlink" title="11. io_timer_impl_svc_notify_fork() 为什么存在"></a>11. <code>io_timer_impl_svc_notify_fork()</code> 为什么存在</h2><p>实现：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">static</span> <span class="type">int</span></span><br><span class="line"><span class="title function_">io_timer_impl_svc_notify_fork</span><span class="params">(<span class="keyword">struct</span> io_svc *svc, <span class="keyword">enum</span> io_fork_event e)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">io_timer_impl</span> *<span class="title">impl</span> =</span> io_timer_impl_from_svc(svc);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (e != IO_FORK_CHILD || impl-&gt;shutdown)</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">itimerspec</span> <span class="title">value</span> =</span> &#123; &#123; <span class="number">0</span>, <span class="number">0</span> &#125;, &#123; <span class="number">0</span>, <span class="number">0</span> &#125; &#125;;</span><br><span class="line">    timerfd_gettime(impl-&gt;tfd, &amp;value);</span><br><span class="line">    io_timer_impl_close(impl);</span><br><span class="line">    io_timer_impl_open(impl);</span><br><span class="line">    timerfd_settime(impl-&gt;tfd, <span class="number">0</span>, &amp;value, <span class="literal">NULL</span>);</span><br><span class="line"></span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="11-1-Timer-不会主动-fork"><a href="#11-1-Timer-不会主动-fork" class="headerlink" title="11.1 Timer 不会主动 fork"></a>11.1 Timer 不会主动 fork</h3><p>这段函数并不意味着 Timer 会创建子进程。它只是作为 <code>io_svc</code> 回调注册到 <code>io_ctx</code>：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">impl-&gt;svc = IO_SVC_INIT(&amp;io_timer_impl_svc_vtbl);</span><br><span class="line">io_ctx_insert(impl-&gt;ctx, &amp;impl-&gt;svc);</span><br></pre></td></tr></table></figure><p>只有宿主程序或依赖库确实执行了 <code>fork()</code>，并且 <code>io_ctx</code> 收到相应通知时，该回调才会被调用。</p><h3 id="11-2-为什么子进程要重建-timerfd"><a href="#11-2-为什么子进程要重建-timerfd" class="headerlink" title="11.2 为什么子进程要重建 timerfd"></a>11.2 为什么子进程要重建 timerfd</h3><p><code>fork()</code> 后，子进程继承父进程的文件描述符，父子进程中的 fd 指向同一个底层 timerfd 对象。若双方都读取到期计数，可能互相消费事件。</p><p>因此子进程执行：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">timerfd_gettime()   保存剩余时间和周期</span><br><span class="line">io_timer_impl_close() 关闭继承的 timerfd</span><br><span class="line">io_timer_impl_open()  创建独立 timerfd 并重新注册 poll</span><br><span class="line">timerfd_settime()     按剩余时间恢复</span><br></pre></td></tr></table></figure><p>父进程继续使用原 timerfd，子进程使用新 timerfd，双方不再共享到期计数。</p><h3 id="11-3-与-TFD-CLOEXEC-的区别"><a href="#11-3-与-TFD-CLOEXEC-的区别" class="headerlink" title="11.3 与 TFD_CLOEXEC 的区别"></a>11.3 与 <code>TFD_CLOEXEC</code> 的区别</h3><table><thead><tr><th>场景</th><th>处理方式</th></tr></thead><tbody><tr><td><code>fork()</code> 后仍继续运行 Lely</td><td><code>io_timer_impl_svc_notify_fork()</code> 重建 timerfd</td></tr><tr><td>随后 <code>exec()</code> 加载新程序</td><td><code>TFD_CLOEXEC</code> 自动关闭 timerfd</td></tr></tbody></table><hr><h2 id="12-从-BasicMaster-到-Linux-Timer：官方源码中的真实所有权"><a href="#12-从-BasicMaster-到-Linux-Timer：官方源码中的真实所有权" class="headerlink" title="12. 从 BasicMaster 到 Linux Timer：官方源码中的真实所有权"></a>12. 从 <code>BasicMaster</code> 到 Linux Timer：官方源码中的真实所有权</h2><p>此前不能确认的“完整 CANopen 主站工程调用关系”，在官方 <code>coapp</code>、<code>io2</code> 和 <code>can</code> 源码中可以完整闭环。</p><h3 id="12-1-BasicMaster-不创建-Timer，而是接收外部-Timer"><a href="#12-1-BasicMaster-不创建-Timer，而是接收外部-Timer" class="headerlink" title="12.1 BasicMaster 不创建 Timer，而是接收外部 Timer"></a>12.1 <code>BasicMaster</code> 不创建 Timer，而是接收外部 Timer</h3><p><a href="https://github.com/lely-industries/lely-core/blob/620d1858eb8520dbc3dc5e1a7314565becd54199/src/coapp/master.cpp#L84-L105"><code>BasicMaster</code></a> 的构造函数接收：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">io::TimerBase&amp; timer</span><br></pre></td></tr></table></figure><p>然后直接传给父类 <code>Node</code>：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">BasicMaster::<span class="built_in">BasicMaster</span>(..., io::TimerBase&amp; timer, ...)</span><br><span class="line">    : <span class="built_in">Node</span>(exec, timer, chan, ...), ... &#123;&#125;</span><br></pre></td></tr></table></figure><p><a href="https://github.com/lely-industries/lely-core/blob/620d1858eb8520dbc3dc5e1a7314565becd54199/include/lely/coapp/node.hpp#L125-L151"><code>Node</code></a> 的接口文档明确要求：</p><blockquote><p>传入的 Timer 用于 CANopen 事件，且不得用于其他目的。</p></blockquote><p><code>Node</code> 构造时继续把 Timer 传给 <code>io::CanNet</code>：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Node::<span class="built_in">Node</span>(..., io::TimerBase&amp; timer, ...)</span><br><span class="line">    : io::<span class="built_in">CanNet</span>(exec, timer, chan, <span class="number">0</span>, <span class="number">0</span>), ... &#123;&#125;</span><br></pre></td></tr></table></figure><p>所以在你的工程中：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">impl_-&gt;timer.<span class="built_in">reset</span>(<span class="keyword">new</span> lely::io::<span class="built_in">Timer</span>(...));</span><br></pre></td></tr></table></figure><p><code>impl_-&gt;timer</code> 是 Timer 的所有者；Lely 的 <code>BasicMaster/Node</code> 只接收引用并使用它，不负责创建该系统 Timer。</p><h3 id="12-2-真正直接保存-io-timer-t-的是-io-can-net"><a href="#12-2-真正直接保存-io-timer-t-的是-io-can-net" class="headerlink" title="12.2 真正直接保存 io_timer_t* 的是 io_can_net"></a>12.2 真正直接保存 <code>io_timer_t*</code> 的是 <code>io_can_net</code></h3><p><a href="https://github.com/lely-industries/lely-core/blob/620d1858eb8520dbc3dc5e1a7314565becd54199/include/lely/io2/can_net.hpp#L39-L78"><code>io::CanNet</code></a> 将 Timer 传给：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">io_can_net_create(exec, timer, chan, ...)</span><br></pre></td></tr></table></figure><p><a href="https://github.com/lely-industries/lely-core/blob/620d1858eb8520dbc3dc5e1a7314565becd54199/src/io2/can_net.c#L69-L89"><code>struct io_can_net</code></a> 中直接保存：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">io_timer_t</span> *timer;</span><br><span class="line"><span class="type">io_tqueue_t</span> *tq;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">io_tqueue_wait</span> <span class="title">wait_next</span>;</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">io_tqueue_wait</span> <span class="title">wait_confirm</span>;</span></span><br></pre></td></tr></table></figure><p>初始化时：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">net-&gt;timer = timer;</span><br><span class="line">net-&gt;tq = io_tqueue_create(net-&gt;timer, <span class="literal">NULL</span>);</span><br><span class="line">can_net_set_next_func(net-&gt;net, &amp;io_can_net_next_func, net);</span><br></pre></td></tr></table></figure><p>因此系统 Timer 的直接使用者是 I&#x2F;O CAN 网络适配层 <code>io_can_net</code>，而不是 NMT、SDO、PDO 模块。</p><h3 id="12-3-io-tqueue-如何用一个系统-Timer-管理多个绝对截止时间"><a href="#12-3-io-tqueue-如何用一个系统-Timer-管理多个绝对截止时间" class="headerlink" title="12.3 io_tqueue 如何用一个系统 Timer 管理多个绝对截止时间"></a>12.3 <code>io_tqueue</code> 如何用一个系统 Timer 管理多个绝对截止时间</h3><p><a href="https://github.com/lely-industries/lely-core/blob/620d1858eb8520dbc3dc5e1a7314565becd54199/src/io2/tqueue.c#L66-L80"><code>struct io_tqueue</code></a> 包含：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">io_timer_t</span> *timer;          <span class="comment">// 唯一底层 Timer</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">io_timer_wait</span> <span class="title">wait</span>;</span>  <span class="comment">// 唯一底层 Timer wait</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">timespec</span> <span class="title">next</span>;</span>       <span class="comment">// 当前最早截止时间</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">pheap</span> <span class="title">queue</span>;</span>         <span class="comment">// 多个 io_tqueue_wait 的最小堆</span></span><br></pre></td></tr></table></figure><p>当上层提交一个 <code>io_tqueue_wait</code> 时，<code>io_tqueue_submit_wait()</code>：</p><ol><li><p>将等待对象按绝对时间插入 <code>pheap</code>；</p></li><li><p>如果它成为最早截止时间，调用：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">io_timer_settime(tq-&gt;timer, TIMER_ABSTIME, &amp;value, <span class="literal">NULL</span>);</span><br></pre></td></tr></table></figure></li><li><p>若底层尚未提交等待，则只提交一个：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">io_timer_submit_wait(tq-&gt;timer, &amp;tq-&gt;wait);</span><br></pre></td></tr></table></figure></li></ol><p>底层 Timer 到期后，<code>io_tqueue_wait_func()</code>：</p><ol><li>获取当前时刻；</li><li>从堆中弹出所有 <code>value &lt;= now</code> 的等待对象；</li><li>将底层 Timer 重新设置为下一个最早截止时间；</li><li>如果堆仍非空，再次提交同一个 <code>tq-&gt;wait</code>；</li><li>将所有已到期的 <code>io_tqueue_wait</code> 投递给各自 executor。</li></ol><p>这证明 <code>io_tqueue</code> 是第一层逻辑定时复用器。</p><h3 id="12-4-io-can-net-中-TimerQueue-的两个直接用途"><a href="#12-4-io-can-net-中-TimerQueue-的两个直接用途" class="headerlink" title="12.4 io_can_net 中 TimerQueue 的两个直接用途"></a>12.4 <code>io_can_net</code> 中 TimerQueue 的两个直接用途</h3><p><code>io_can_net</code> 在同一 <code>io_tqueue</code> 中至少维护两个等待操作：</p><table><thead><tr><th>等待对象</th><th>目的</th></tr></thead><tbody><tr><td><code>wait_next</code></td><td>等待下一个 CAN&#x2F;CANopen 逻辑定时器到期</td></tr><tr><td><code>wait_confirm</code></td><td>等待 CAN 帧发送确认超时</td></tr></tbody></table><p><code>io_can_net_next_func()</code> 收到 <code>can_net</code> 更新后的最早时间后，会设置：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">net-&gt;wait_next.value = *tp;</span><br><span class="line">io_tqueue_submit_wait(net-&gt;tq, &amp;net-&gt;wait_next);</span><br></pre></td></tr></table></figure><p>而发送 CAN 帧时，<code>io_can_net_do_write()</code> 会为写确认计算绝对截止时间并提交 <code>wait_confirm</code>。因此同一个系统 Timer 不仅承载 CANopen 协议定时器，也承载 CAN 通道发送确认超时。</p><hr><h2 id="13-CANopen-协议定时器如何映射到同一个-timerfd"><a href="#13-CANopen-协议定时器如何映射到同一个-timerfd" class="headerlink" title="13. CANopen 协议定时器如何映射到同一个 timerfd"></a>13. CANopen 协议定时器如何映射到同一个 <code>timerfd</code></h2><h3 id="13-1-can-net-是第二层逻辑定时复用器"><a href="#13-1-can-net-是第二层逻辑定时复用器" class="headerlink" title="13.1 can_net 是第二层逻辑定时复用器"></a>13.1 <code>can_net</code> 是第二层逻辑定时复用器</h3><p><a href="https://github.com/lely-industries/lely-core/blob/620d1858eb8520dbc3dc5e1a7314565becd54199/src/can/net.c#L38-L56"><code>struct __can_net</code></a> 保存：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">pheap</span> <span class="title">timer_heap</span>;</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">timespec</span> <span class="title">time</span>;</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">timespec</span> <span class="title">next</span>;</span></span><br><span class="line"><span class="type">can_timer_func_t</span> *next_func;</span><br></pre></td></tr></table></figure><p>每个 <a href="https://github.com/lely-industries/lely-core/blob/620d1858eb8520dbc3dc5e1a7314565becd54199/src/can/net.c#L64-L81"><code>can_timer_t</code></a> 保存：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">timespec</span> <span class="title">start</span>;</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">timespec</span> <span class="title">interval</span>;</span></span><br><span class="line"><span class="type">can_timer_func_t</span> *func;</span><br><span class="line"><span class="type">void</span> *data;</span><br></pre></td></tr></table></figure><p><code>can_timer_start()</code> 将逻辑 Timer 插入 <code>can_net.timer_heap</code>，再调用 <code>can_net_set_next()</code>。后者取堆顶，也就是最早截止时间，并调用注册的 <code>next_func</code>。在 <code>io_can_net</code> 中，这个回调就是 <code>io_can_net_next_func()</code>。</p><h3 id="13-2-一次协议-Timer-到期的完整、可验证调用链"><a href="#13-2-一次协议-Timer-到期的完整、可验证调用链" class="headerlink" title="13.2 一次协议 Timer 到期的完整、可验证调用链"></a>13.2 一次协议 Timer 到期的完整、可验证调用链</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br></pre></td><td class="code"><pre><span class="line">NMT / Heartbeat / SDO / PDO / SYNC 模块</span><br><span class="line">  │ can_timer_start()</span><br><span class="line">  ▼</span><br><span class="line">can_net.timer_heap</span><br><span class="line">  │ can_net_set_next()：取最早 start</span><br><span class="line">  ▼</span><br><span class="line">io_can_net_next_func()</span><br><span class="line">  │ 更新 net-&gt;next，提交 wait_next</span><br><span class="line">  ▼</span><br><span class="line">io_tqueue_submit_wait()</span><br><span class="line">  │ 插入 io_tqueue.pheap</span><br><span class="line">  │ io_timer_settime(TIMER_ABSTIME)</span><br><span class="line">  │ io_timer_submit_wait(&amp;tq-&gt;wait)</span><br><span class="line">  ▼</span><br><span class="line">io_timer_impl_settime()</span><br><span class="line">  │ timerfd_settime(TFD_TIMER_ABSTIME)</span><br><span class="line">  ▼</span><br><span class="line">Linux timerfd 到期</span><br><span class="line">  ▼</span><br><span class="line">epoll_pwait() + EPOLLONESHOT</span><br><span class="line">  ▼</span><br><span class="line">io_timer_impl_watch_func()</span><br><span class="line">  │ ev_exec_post(wait_task)</span><br><span class="line">  ▼</span><br><span class="line">io_timer_impl_wait_task_func()</span><br><span class="line">  │ read(timerfd)</span><br><span class="line">  │ io_timer_wait_queue_post()</span><br><span class="line">  ▼</span><br><span class="line">io_tqueue_wait_func()</span><br><span class="line">  │ 弹出全部已到期 io_tqueue_wait</span><br><span class="line">  │ 重新设置下一个绝对截止时间</span><br><span class="line">  ▼</span><br><span class="line">io_can_net_wait_next_func()</span><br><span class="line">  │ io_can_net_set_time()</span><br><span class="line">  ▼</span><br><span class="line">can_net_set_time()</span><br><span class="line">  │ 弹出所有 start &lt;= now 的 can_timer_t</span><br><span class="line">  │ 周期 Timer 先重入堆</span><br><span class="line">  │ 调用 timer-&gt;func(&amp;net-&gt;time, timer-&gt;data)</span><br><span class="line">  ▼</span><br><span class="line">具体协议回调</span><br></pre></td></tr></table></figure><p>这里有一个重要设计点：<code>can_net_set_time()</code> 在调用周期 Timer 的协议回调之前，先把该 Timer 的 <code>start</code> 增加 <code>interval</code> 并重新插回堆中。这可以降低回调内部修改或停止 Timer 时的状态复杂度。</p><h3 id="13-3-哪些-CANopen-模块持有逻辑-can-timer-t"><a href="#13-3-哪些-CANopen-模块持有逻辑-can-timer-t" class="headerlink" title="13.3 哪些 CANopen 模块持有逻辑 can_timer_t"></a>13.3 哪些 CANopen 模块持有逻辑 <code>can_timer_t</code></h3><p>协议模块不直接持有 <code>lely::io::Timer</code> 或 Linux <code>timerfd</code>，而是持有注册在 <code>can_net.timer_heap</code> 中的 <code>can_timer_t</code>。</p><table><thead><tr><th>模块</th><th>源码中的逻辑 Timer</th><th>用途</th></tr></thead><tbody><tr><td>NMT 主&#x2F;从服务</td><td><code>ec_timer</code></td><td>life guarding 或 heartbeat 生产</td></tr><tr><td>NMT Master</td><td><code>cs_timer</code></td><td>发送缓冲的 NMT 命令</td></tr><tr><td>每个 NMT slave 状态</td><td><code>timer</code></td><td>node guarding</td></tr><tr><td>Heartbeat consumer</td><td><code>timer</code></td><td>消费者心跳超时，每个 consumer 一个</td></tr><tr><td>Client-SDO</td><td><code>timer</code></td><td>SDO 请求&#x2F;分段&#x2F;块传输超时</td></tr><tr><td>TPDO</td><td><code>timer_event</code></td><td>event timer、事件驱动 TPDO</td></tr><tr><td>TPDO</td><td><code>timer_swnd</code></td><td>同步窗口截止时间</td></tr><tr><td>RPDO</td><td><code>timer_event</code></td><td>deadline monitoring</td></tr><tr><td>RPDO</td><td><code>timer_swnd</code></td><td>同步窗口截止时间</td></tr><tr><td>SYNC</td><td><code>timer</code></td><td>SYNC producer 周期</td></tr></tbody></table><p>对应官方源码：</p><ul><li>NMT：<a href="https://github.com/lely-industries/lely-core/blob/620d1858eb8520dbc3dc5e1a7314565becd54199/src/co/nmt.c#L84-L149"><code>src/co/nmt.c</code></a>、<a href="https://github.com/lely-industries/lely-core/blob/620d1858eb8520dbc3dc5e1a7314565becd54199/src/co/nmt.c#L152-L250"><code>src/co/nmt.c</code></a></li><li>Heartbeat consumer：<a href="https://github.com/lely-industries/lely-core/blob/620d1858eb8520dbc3dc5e1a7314565becd54199/src/co/nmt_hb.c#L34-L69"><code>src/co/nmt_hb.c</code></a></li><li>Client-SDO：<a href="https://github.com/lely-industries/lely-core/blob/620d1858eb8520dbc3dc5e1a7314565becd54199/src/co/csdo.c#L72-L89"><code>src/co/csdo.c</code></a></li><li>TPDO：<a href="https://github.com/lely-industries/lely-core/blob/620d1858eb8520dbc3dc5e1a7314565becd54199/src/co/tpdo.c#L54-L96"><code>src/co/tpdo.c</code></a></li><li>RPDO：<a href="https://github.com/lely-industries/lely-core/blob/620d1858eb8520dbc3dc5e1a7314565becd54199/src/co/rpdo.c#L45-L81"><code>src/co/rpdo.c</code></a></li><li>SYNC：<a href="https://github.com/lely-industries/lely-core/blob/620d1858eb8520dbc3dc5e1a7314565becd54199/src/co/sync.c#L41-L69"><code>src/co/sync.c</code></a></li></ul><h3 id="13-4-“哪个模块直接持有-Timer”的准确答案"><a href="#13-4-“哪个模块直接持有-Timer”的准确答案" class="headerlink" title="13.4 “哪个模块直接持有 Timer”的准确答案"></a>13.4 “哪个模块直接持有 Timer”的准确答案</h3><p>可以分三层回答：</p><table><thead><tr><th>层级</th><th>直接持有对象</th><th>说明</th></tr></thead><tbody><tr><td>应用&#x2F;运行时</td><td><code>std::unique_ptr&lt;lely::io::Timer&gt;</code></td><td>你的 <code>impl_-&gt;timer</code> 负责所有权</td></tr><tr><td>I&#x2F;O CAN 网络层</td><td><code>io_timer_t *timer</code>、<code>io_tqueue_t *tq</code></td><td><code>io_can_net</code> 直接使用系统 Timer</td></tr><tr><td>CANopen 协议层</td><td><code>can_timer_t *</code></td><td>NMT、Heartbeat、SDO、PDO、SYNC 等只持有逻辑 Timer</td></tr></tbody></table><p>因此不能说“SDO 直接调用 Linux timerfd”，也不能说“每个 NMT&#x2F;SDO&#x2F;PDO 都有独立 Timer”。准确关系是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">协议模块 can_timer_t</span><br><span class="line">  → can_net 最小堆</span><br><span class="line">  → io_can_net.wait_next</span><br><span class="line">  → io_tqueue 最小堆</span><br><span class="line">  → 单个 io::Timer</span><br><span class="line">  → 单个 Linux timerfd</span><br></pre></td></tr></table></figure><hr><h2 id="14-完整生命周期"><a href="#14-完整生命周期" class="headerlink" title="14. 完整生命周期"></a>14. 完整生命周期</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[C++ Timer 构造] --&gt; B[io_timer_create]</span><br><span class="line">    B --&gt; C[io_timer_init]</span><br><span class="line">    C --&gt; D[timerfd_create]</span><br><span class="line">    D --&gt; E[io_poll_watch IO_EVENT_IN]</span><br><span class="line">    E --&gt; F[Timer 已创建但未必已设置时间]</span><br><span class="line"></span><br><span class="line">    F --&gt; G[settime 设置截止时间]</span><br><span class="line">    F --&gt; H[submit_wait 登记等待对象]</span><br><span class="line">    G --&gt; I[Linux 内核计时]</span><br><span class="line">    H --&gt; J[wait_queue]</span><br><span class="line"></span><br><span class="line">    I --&gt; K[timerfd 可读]</span><br><span class="line">    K --&gt; L[watch_func]</span><br><span class="line">    L --&gt; M[executor 执行 wait_task]</span><br><span class="line">    M --&gt; N[read 到期计数]</span><br><span class="line">    N --&gt; O[转移当前 wait_queue]</span><br><span class="line">    O --&gt; P[逐个 post wait task]</span><br><span class="line">    P --&gt; Q[用户完成任务执行]</span><br><span class="line"></span><br><span class="line">    Q --&gt; R&#123;是否再次等待&#125;</span><br><span class="line">    R -- 是 --&gt; H</span><br><span class="line">    R -- 否 --&gt; S[等待结束]</span><br><span class="line"></span><br><span class="line">    F --&gt; T[shutdown/fini]</span><br><span class="line">    T --&gt; U[停止 poll 监听]</span><br><span class="line">    U --&gt; V[取消未完成 waits]</span><br><span class="line">    V --&gt; W[close timerfd]</span><br></pre></td></tr></table></figure><hr><h2 id="15-调试一次真实到期流程"><a href="#15-调试一次真实到期流程" class="headerlink" title="15. 调试一次真实到期流程"></a>15. 调试一次真实到期流程</h2><p>推荐断点顺序：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">lely::io::Timer::Timer</span><br><span class="line">io_timer_create</span><br><span class="line">io_timer_init</span><br><span class="line">io_timer_impl_open</span><br><span class="line">io_timer_impl_settime</span><br><span class="line">io_timer_impl_submit_wait</span><br><span class="line">io_timer_impl_watch_func</span><br><span class="line">io_timer_impl_wait_task_func</span><br><span class="line">io_timer_wait_queue_post</span><br><span class="line">io_timer_wait_post</span><br></pre></td></tr></table></figure><p>重点观察：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">impl-&gt;tfd</span><br><span class="line">impl-&gt;clockid</span><br><span class="line">impl-&gt;shutdown</span><br><span class="line">impl-&gt;wait_posted</span><br><span class="line">impl-&gt;wait_queue</span><br><span class="line">impl-&gt;overrun</span><br><span class="line">value</span><br><span class="line">result</span><br><span class="line">errno</span><br></pre></td></tr></table></figure><h3 id="15-1-查看-timerfd-状态"><a href="#15-1-查看-timerfd-状态" class="headerlink" title="15.1 查看 timerfd 状态"></a>15.1 查看 timerfd 状态</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">ls</span> -l /proc/&lt;pid&gt;/fd</span><br><span class="line"><span class="built_in">cat</span> /proc/&lt;pid&gt;/fdinfo/&lt;tfd&gt;</span><br></pre></td></tr></table></figure><h3 id="15-2-使用-strace"><a href="#15-2-使用-strace" class="headerlink" title="15.2 使用 strace"></a>15.2 使用 strace</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">strace -f -tt \</span><br><span class="line">  -e trace=timerfd_create,timerfd_settime,<span class="built_in">read</span>,close,epoll_ctl,epoll_wait \</span><br><span class="line">  ./your_program</span><br></pre></td></tr></table></figure><p>预期可观察到类似链路：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">timerfd_create(..., TFD_CLOEXEC|TFD_NONBLOCK) = 7</span><br><span class="line">timerfd_settime(7, TFD_TIMER_ABSTIME, ...)     = 0</span><br><span class="line">epoll_wait(...)                                = 1</span><br><span class="line">read(7, ..., 8)                                = 8</span><br></pre></td></tr></table></figure><h3 id="15-3-确认程序是否-fork"><a href="#15-3-确认程序是否-fork" class="headerlink" title="15.3 确认程序是否 fork"></a>15.3 确认程序是否 fork</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">strace -ff -e trace=process ./your_program</span><br></pre></td></tr></table></figure><p>GDB 可设置：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">break fork</span><br><span class="line">break __libc_fork</span><br><span class="line">break vfork</span><br><span class="line">break posix_spawn</span><br><span class="line">break io_timer_impl_svc_notify_fork</span><br></pre></td></tr></table></figure><p>若 <code>io_timer_impl_svc_notify_fork()</code> 从未命中，说明该兼容路径没有参与当前程序运行。</p><hr><h2 id="16-常见误区"><a href="#16-常见误区" class="headerlink" title="16. 常见误区"></a>16. 常见误区</h2><h3 id="误区-1：创建-Timer-就开始计时"><a href="#误区-1：创建-Timer-就开始计时" class="headerlink" title="误区 1：创建 Timer 就开始计时"></a>误区 1：创建 Timer 就开始计时</h3><p>错误。创建阶段只建立 timerfd 和 poll 监听，真正计时由 <code>timerfd_settime()</code> 启动或更新。</p><h3 id="误区-2：submit-wait-会立即执行任务"><a href="#误区-2：submit-wait-会立即执行任务" class="headerlink" title="误区 2：submit_wait() 会立即执行任务"></a>误区 2：<code>submit_wait()</code> 会立即执行任务</h3><p>错误。它只把 wait 放入 <code>wait_queue</code>。只有到期、错误或取消后，wait 才会被投递给 executor。</p><h3 id="误区-3：io-timer-wait-queue-post-直接调用所有用户函数"><a href="#误区-3：io-timer-wait-queue-post-直接调用所有用户函数" class="headerlink" title="误区 3：io_timer_wait_queue_post() 直接调用所有用户函数"></a>误区 3：<code>io_timer_wait_queue_post()</code> 直接调用所有用户函数</h3><p>不准确。它逐个调用 <code>ev_exec_post()</code>，由 executor 决定具体何时、在哪个执行上下文运行任务。</p><h3 id="误区-4：周期-timerfd-会自动反复执行同一个-wait"><a href="#误区-4：周期-timerfd-会自动反复执行同一个-wait" class="headerlink" title="误区 4：周期 timerfd 会自动反复执行同一个 wait"></a>误区 4：周期 timerfd 会自动反复执行同一个 wait</h3><p>错误。<code>io_timer_wait</code> 是一次性等待操作。完成后已从队列移除，下一周期需要重新提交。</p><h3 id="误区-5：Timer-内部会主动-fork"><a href="#误区-5：Timer-内部会主动-fork" class="headerlink" title="误区 5：Timer 内部会主动 fork"></a>误区 5：Timer 内部会主动 fork</h3><p>错误。Timer 只注册 fork 通知回调；是否 fork 由宿主程序或依赖库决定。</p><h3 id="误区-6：TFD-TIMER-ABSTIME-表示周期时间是绝对值"><a href="#误区-6：TFD-TIMER-ABSTIME-表示周期时间是绝对值" class="headerlink" title="误区 6：TFD_TIMER_ABSTIME 表示周期时间是绝对值"></a>误区 6：<code>TFD_TIMER_ABSTIME</code> 表示周期时间是绝对值</h3><p>错误。它改变首次到期值 <code>it_value</code> 的解释；<code>it_interval</code> 仍是相对周期。</p><hr><h2 id="17-最终心智模型"><a href="#17-最终心智模型" class="headerlink" title="17. 最终心智模型"></a>17. 最终心智模型</h2><p>阅读完整源码时，应把它拆成八个角色：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line">1. can_timer_t</span><br><span class="line">   NMT、Heartbeat、SDO、PDO、SYNC 的协议逻辑 Timer</span><br><span class="line"></span><br><span class="line">2. can_net.timer_heap</span><br><span class="line">   对全部协议 Timer 排序，只上报最早截止时间</span><br><span class="line"></span><br><span class="line">3. io_can_net</span><br><span class="line">   把 can_net 的最早截止时间转换为 io_tqueue_wait</span><br><span class="line"></span><br><span class="line">4. io_tqueue</span><br><span class="line">   复用一个系统 Timer，管理 wait_next、wait_confirm 和 Node 用户等待</span><br><span class="line"></span><br><span class="line">5. io::Timer / timerfd</span><br><span class="line">   设置唯一的内核截止时间并累计到期次数</span><br><span class="line"></span><br><span class="line">6. io_poll / epoll</span><br><span class="line">   使用 EPOLLONESHOT 发现 timerfd 可读，并要求显式 rearm</span><br><span class="line"></span><br><span class="line">7. wait_task / wait_queue</span><br><span class="line">   从 poll 上下文切到 executor，读取 timerfd 并分发完成任务</span><br><span class="line"></span><br><span class="line">8. executor</span><br><span class="line">   执行 tqueue、CAN 网络和最终协议回调</span><br></pre></td></tr></table></figure><p>完整运行公式：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">can_timer_start 决定“协议逻辑何时到期”</span><br><span class="line">can_net 决定“所有协议 Timer 中谁最早”</span><br><span class="line">io_tqueue 决定“所有 I/O 等待中谁最早”</span><br><span class="line">TimerBase/settime 决定“内核何时唤醒”</span><br><span class="line">epoll 决定“如何发现唤醒”</span><br><span class="line">executor 决定“在哪里推进状态机和执行回调”</span><br></pre></td></tr></table></figure><p>最终应形成的核心心智模型是：</p><blockquote><p>Lely CANopen 用两级最小堆将大量协议定时事件压缩为一个绝对截止时间，再由一个 <code>timerfd</code> 唤醒事件循环。到期后时间从底层向上回灌，最终由 <code>can_net_set_time()</code> 批量执行所有已到期的协议回调。</p></blockquote><hr><h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><h3 id="官方-Lely-源码基线"><a href="#官方-Lely-源码基线" class="headerlink" title="官方 Lely 源码基线"></a>官方 Lely 源码基线</h3><ul><li>仓库：<a href="https://github.com/lely-industries/lely-core"><code>lely-industries/lely-core</code></a></li><li>固定提交：<a href="https://github.com/lely-industries/lely-core/commit/620d1858eb8520dbc3dc5e1a7314565becd54199"><code>620d1858eb8520dbc3dc5e1a7314565becd54199</code></a></li><li><code>TimerBase</code> C++ API：<a href="https://github.com/lely-industries/lely-core/blob/620d1858eb8520dbc3dc5e1a7314565becd54199/include/lely/io2/timer.hpp"><code>include/lely/io2/timer.hpp</code></a></li><li>Linux Timer：<a href="https://github.com/lely-industries/lely-core/blob/620d1858eb8520dbc3dc5e1a7314565becd54199/src/io2/linux/timer.c"><code>src/io2/linux/timer.c</code></a></li><li>Linux epoll 后端：<a href="https://github.com/lely-industries/lely-core/blob/620d1858eb8520dbc3dc5e1a7314565becd54199/src/io2/linux/poll.c"><code>src/io2/linux/poll.c</code></a></li><li>TimerQueue：<a href="https://github.com/lely-industries/lely-core/blob/620d1858eb8520dbc3dc5e1a7314565becd54199/src/io2/tqueue.c"><code>src/io2/tqueue.c</code></a></li><li>I&#x2F;O CAN 网络适配：<a href="https://github.com/lely-industries/lely-core/blob/620d1858eb8520dbc3dc5e1a7314565becd54199/src/io2/can_net.c"><code>src/io2/can_net.c</code></a></li><li>CAN 网络逻辑 Timer：<a href="https://github.com/lely-industries/lely-core/blob/620d1858eb8520dbc3dc5e1a7314565becd54199/src/can/net.c"><code>src/can/net.c</code></a></li><li>CANopen Node：<a href="https://github.com/lely-industries/lely-core/blob/620d1858eb8520dbc3dc5e1a7314565becd54199/include/lely/coapp/node.hpp"><code>include/lely/coapp/node.hpp</code></a>、<a href="https://github.com/lely-industries/lely-core/blob/620d1858eb8520dbc3dc5e1a7314565becd54199/src/coapp/node.cpp"><code>src/coapp/node.cpp</code></a></li><li>CANopen Master：<a href="https://github.com/lely-industries/lely-core/blob/620d1858eb8520dbc3dc5e1a7314565becd54199/include/lely/coapp/master.hpp"><code>include/lely/coapp/master.hpp</code></a>、<a href="https://github.com/lely-industries/lely-core/blob/620d1858eb8520dbc3dc5e1a7314565becd54199/src/coapp/master.cpp"><code>src/coapp/master.cpp</code></a><br>制规定的实现方式。</li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;meta name=&quot;referrer&quot; content=&quot;no-referrer&quot; /&gt;

&lt;h1 id=&quot;Lely-CANopen-Timer-的实现原理与运行流程&quot;&gt;&lt;a href=&quot;#Lely-CANopen-Timer-的实现原理与运行流程&quot; class=&quot;heade</summary>
      
    
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/categories/canopen/"/>
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/tags/canopen/"/>
    
  </entry>
  
  <entry>
    <title>VS Code + GDB 远程调试中的系统库边界、跳过策略与反汇编排查</title>
    <link href="https://wdfk-prog.space/posts/f24244bf/"/>
    <id>https://wdfk-prog.space/posts/f24244bf/</id>
    <published>2026-07-25T08:41:12.000Z</published>
    <updated>2026-07-25T08:42:43.808Z</updated>
    
    <content type="html"><![CDATA[<meta name="referrer" content="no-referrer" /><h1 id="VS-Code-GDB-远程调试中的系统库边界、跳过策略与反汇编排查"><a href="#VS-Code-GDB-远程调试中的系统库边界、跳过策略与反汇编排查" class="headerlink" title="VS Code + GDB 远程调试中的系统库边界、跳过策略与反汇编排查"></a>VS Code + GDB 远程调试中的系统库边界、跳过策略与反汇编排查</h1><p>@[toc]<br><img src="https://i-blog.csdnimg.cn/direct/c083b6a09b4a4359bdf4268cf8afb4f2.png" alt="在这里插入图片描述"></p><p>在嵌入式 Linux 远程调试中，经常会出现一种容易误判的情况：</p><ul><li>业务程序已经使用 Debug 参数构建；</li><li>依赖的第三方动态库已经能够加载符号；</li><li><code>info sharedlibrary</code> 中目标库已经显示 <code>Syms Read = Yes</code>；</li><li>断点可以命中 <code>main()</code> 或业务入口；</li></ul><p>但继续调试时仍然可能遇到：</p><ul><li>在某一行按 F11 后，VS Code 直接变成“运行中”；</li><li><code>std::string</code>、容器、内存分配等语句无法稳定单步；</li><li>点击暂停后只看到一个地址和 <code>??</code>；</li><li><code>step</code>、<code>next</code>、<code>finish</code> 报 <code>Cannot find bounds of current function</code>；</li><li>明明已经配置了 <code>skip</code>，仍然会停进系统库；</li><li>无法同步目标板系统库到宿主机，不知道该继续调试还是直接绕过。</li></ul><p>这类问题说明：<strong>动态库符号加载成功，只解决了“某个库能否源码级调试”的问题，并不等于整个进程中的所有执行路径都具备源码、函数边界和行号信息。</strong></p><p>本文重点讨论以下场景：</p><ol><li>已确认业务程序和目标动态库可调试；</li><li>仍然无法稳定断点或源码级单步；</li><li>需要判断是否进入了系统库；</li><li>希望了解系统库如何完整调试；</li><li>无法调试系统库时，如何可靠跳过；</li><li>无法回到源码时，如何使用反汇编和指令级单步继续排查。</li></ol><hr><h2 id="1-先明确调试边界：Syms-Read-Yes-只代表当前库符号已加载"><a href="#1-先明确调试边界：Syms-Read-Yes-只代表当前库符号已加载" class="headerlink" title="1. 先明确调试边界：Syms Read = Yes 只代表当前库符号已加载"></a>1. 先明确调试边界：<code>Syms Read = Yes</code> 只代表当前库符号已加载</h2><p>执行：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">-exec info sharedlibrary</span><br></pre></td></tr></table></figure><p>可能看到：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">From                To                  Syms Read   Shared Object Library</span><br><span class="line">0x...                0x...               Yes         .../libthirdparty-core.so</span><br><span class="line">0x...                0x...               Yes         .../libthirdparty-io.so</span><br><span class="line">                                        No          /usr/lib/.../libstdc++.so.6</span><br><span class="line">                                        No          /lib/.../libc.so.6</span><br><span class="line">                                        No          /lib/.../libpthread.so.0</span><br></pre></td></tr></table></figure><p>这里应分成两组理解：</p><table><thead><tr><th>类型</th><th align="right"><code>Syms Read</code></th><th>含义</th></tr></thead><tbody><tr><td>业务程序和目标第三方库</td><td align="right"><code>Yes</code></td><td>GDB 已读取可用符号，可进行函数断点、调用栈和源码级调试</td></tr><tr><td>libc、libstdc++、libpthread 等系统库</td><td align="right"><code>No</code></td><td>程序可以正常调用，但 GDB 缺少完整函数、源码行和局部变量信息</td></tr></tbody></table><p>因此，第三方动态库全部变成 <code>Yes</code> 后，不需要因为系统库仍为 <code>No</code> 而反复重建第三方库。</p><p>真正需要继续排查的是：<strong>当前单步动作是否进入了没有符号或没有源码行信息的代码区域。</strong></p><h3 id="1-1-动态库已加载、符号已加载、源码可见是三件不同的事"><a href="#1-1-动态库已加载、符号已加载、源码可见是三件不同的事" class="headerlink" title="1.1 动态库已加载、符号已加载、源码可见是三件不同的事"></a>1.1 动态库已加载、符号已加载、源码可见是三件不同的事</h3><p>远程调试一个共享库通常涉及三层能力：</p><ol><li><strong>运行库已加载</strong>：目标动态加载器已经把 <code>.so</code> 映射进进程；</li><li><strong>符号已加载</strong>：GDB 能识别函数名、类型、变量或函数边界；</li><li><strong>源码可见</strong>：GDB 记录的源码路径能够映射到宿主机上的真实源码文件。</li></ol><p>三者关系如下：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    A[目标进程已映射共享库] --&gt; B&#123;宿主机有匹配 ELF 或 debug 文件?&#125;</span><br><span class="line">    B -- 否 --&gt; C[只能看到地址或少量动态符号]</span><br><span class="line">    B -- 是 --&gt; D[函数名和边界可识别]</span><br><span class="line">    D --&gt; E&#123;源码路径可访问或已映射?&#125;</span><br><span class="line">    E -- 否 --&gt; F[可看符号和汇编，但无法显示源码]</span><br><span class="line">    E -- 是 --&gt; G[完整源码级调试]</span><br></pre></td></tr></table></figure><p>所以，看到目标第三方库 <code>Yes</code>，只能证明该库自身已经满足第二层；当代码调用到系统库时，调试能力会自然退化。</p><hr><h2 id="2-为什么明明停在业务源码，按-F11-后却突然变成“运行中”"><a href="#2-为什么明明停在业务源码，按-F11-后却突然变成“运行中”" class="headerlink" title="2. 为什么明明停在业务源码，按 F11 后却突然变成“运行中”"></a>2. 为什么明明停在业务源码，按 F11 后却突然变成“运行中”</h2><p>下面这种代码很常见：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">CommandLineOptions</span> &#123;</span><br><span class="line">    std::string config_path&#123;kDefaultConfigPath&#125;;</span><br><span class="line">    <span class="type">bool</span> check_config&#123;<span class="literal">false</span>&#125;;</span><br><span class="line">    <span class="type">bool</span> show_help&#123;<span class="literal">false</span>&#125;;</span><br><span class="line">    <span class="type">bool</span> show_version&#123;<span class="literal">false</span>&#125;;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>或者：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">const</span> std::string argument = argv[index];</span><br></pre></td></tr></table></figure><p>在这些位置按 F11 后，调试器可能不再停在下一行业务代码，而是进入持续运行状态。</p><p>这通常不是断点失效，而是进入了编译器生成代码、C++ 标准库内联实现或无符号系统库。</p><h2 id="2-1-结构体声明不是运行语句"><a href="#2-1-结构体声明不是运行语句" class="headerlink" title="2.1 结构体声明不是运行语句"></a>2.1 结构体声明不是运行语句</h2><p>如果调试器停在：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">CommandLineOptions</span> &#123;</span><br></pre></td></tr></table></figure><p>不代表 CPU 正在执行“结构体定义”。</p><p>真正执行的代码通常来自：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">CommandLineOptions options;</span><br></pre></td></tr></table></figure><p>这条语句会调用编译器隐式生成的默认构造函数，而默认构造函数需要初始化：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">std::string config_path&#123;kDefaultConfigPath&#125;;</span><br></pre></td></tr></table></figure><p>编译器生成的构造函数没有独立源码定义，DWARF 行号可能被归属到结构体首行、成员定义行或对象定义行。因此，源码指示位置有时并不直观。</p><p>实际调用关系可能是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">ParseArguments()</span><br><span class="line">  -&gt; CommandLineOptions::CommandLineOptions()</span><br><span class="line">    -&gt; std::string::basic_string()</span><br><span class="line">      -&gt; strlen / allocation / copy</span><br></pre></td></tr></table></figure><h2 id="2-2-一条-std-string-语句背后可能跨越多个库"><a href="#2-2-一条-std-string-语句背后可能跨越多个库" class="headerlink" title="2.2 一条 std::string 语句背后可能跨越多个库"></a>2.2 一条 <code>std::string</code> 语句背后可能跨越多个库</h2><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">const</span> std::string argument = argv[index];</span><br></pre></td></tr></table></figure><p>底层可能涉及：</p><ul><li>C++ 标准库头文件中的内联模板；</li><li><code>std::string</code> 构造函数；</li><li><code>char_traits&lt;char&gt;::length()</code>；</li><li><code>strlen()</code>；</li><li><code>operator new()</code>；</li><li><code>malloc()</code>；</li><li><code>memcpy()</code>；</li><li>异常处理和栈展开辅助代码。</li></ul><p>这些代码可能分别位于：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">业务可执行文件</span><br><span class="line">libstdc++.so.6</span><br><span class="line">libgcc_s.so.1</span><br><span class="line">libc.so.6</span><br><span class="line">编译进业务程序的标准库内联代码</span><br></pre></td></tr></table></figure><p>如果系统库没有调试信息，GDB 在执行 <code>step</code> 时可能找不到下一条可用源码行，程序就会继续运行，直到：</p><ul><li>命中其他断点；</li><li>回到具有源码行信息的函数；</li><li>收到信号；</li><li>被手动暂停；</li><li>或程序结束。</li></ul><h2 id="2-3-F10、F11、si-和-ni-的边界"><a href="#2-3-F10、F11、si-和-ni-的边界" class="headerlink" title="2.3 F10、F11、si 和 ni 的边界"></a>2.3 F10、F11、<code>si</code> 和 <code>ni</code> 的边界</h2><table><thead><tr><th>操作</th><th>GDB 命令</th><th>行为</th></tr></thead><tbody><tr><td>单步跳过</td><td><code>next</code></td><td>执行当前源码行，但不主动进入当前行调用的函数</td></tr><tr><td>单步进入</td><td><code>step</code></td><td>尝试进入当前行调用的函数</td></tr><tr><td>指令单步进入</td><td><code>stepi</code> &#x2F; <code>si</code></td><td>执行一条机器指令，允许进入 <code>bl</code> 调用</td></tr><tr><td>指令单步跳过</td><td><code>nexti</code> &#x2F; <code>ni</code></td><td>执行一条机器指令；若为调用指令则运行到返回</td></tr></tbody></table><p>对于以下语句：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">const</span> std::string argument = argv[index];</span><br></pre></td></tr></table></figure><p>如果目标是调试业务逻辑，应优先使用 <strong>F10</strong>。</p><p>如果目标是分析 <code>std::string</code> 的实现，则需要先具备匹配的 libstdc++ 调试符号和源码，然后再使用 F11。</p><hr><h2 id="3-为什么配置了-skip，仍然会进入系统库"><a href="#3-为什么配置了-skip，仍然会进入系统库" class="headerlink" title="3. 为什么配置了 skip，仍然会进入系统库"></a>3. 为什么配置了 <code>skip</code>，仍然会进入系统库</h2><p>GDB 支持为源码级单步配置跳过规则：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">set step-mode off</span><br><span class="line"></span><br><span class="line">skip -rfunction ^std::</span><br><span class="line">skip -rfunction ^__gnu_cxx::</span><br><span class="line">skip -rfunction ^__cxxabiv1::</span><br><span class="line">skip -rfunction ^operator new</span><br><span class="line">skip -rfunction ^operator delete</span><br><span class="line"></span><br><span class="line">skip -gfile */include/c++/**/**</span><br><span class="line">skip -gfile */libstdc++-v3/**/**</span><br></pre></td></tr></table></figure><p>这些规则有效，但需要正确理解其作用范围。</p><h2 id="3-1-skip-只改变源码级-step-的停靠策略"><a href="#3-1-skip-只改变源码级-step-的停靠策略" class="headerlink" title="3.1 skip 只改变源码级 step 的停靠策略"></a>3.1 <code>skip</code> 只改变源码级 <code>step</code> 的停靠策略</h2><p><code>skip</code> 可以告诉 GDB：</p><blockquote><p>当执行 <code>step</code> 时，如果下一目标函数或源码文件匹配规则，就不要停在里面。</p></blockquote><p>它不能：</p><ul><li>阻止 CPU 实际执行系统库；</li><li>阻止业务代码调用 <code>malloc()</code>、<code>strlen()</code>、<code>pthread_*()</code>；</li><li>让“暂停”自动回到业务源码；</li><li>为缺失的 DWARF 信息补出函数边界；</li><li>匹配完全未知的 <code>??</code> 函数；</li><li>保证所有编译器内联代码都能被识别。</li></ul><h2 id="3-2-无符号的-无法按函数名跳过"><a href="#3-2-无符号的-无法按函数名跳过" class="headerlink" title="3.2 无符号的 ?? 无法按函数名跳过"></a>3.2 无符号的 <code>??</code> 无法按函数名跳过</h2><p>如果系统库没有符号，暂停后只显示：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">0x0000ffff... in ?? ()</span><br></pre></td></tr></table></figure><p>此时 GDB 不知道当前地址对应：</p><ul><li><code>std::string</code>；</li><li><code>malloc</code>；</li><li><code>strlen</code>；</li><li><code>memcpy</code>；</li><li><code>epoll_pwait</code>；</li><li>还是其他内部函数。</li></ul><p>因此：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">skip -rfunction ^std::</span><br></pre></td></tr></table></figure><p>无法匹配一个名称未知的函数。</p><p>同理，<code>skip -gfile</code> 依赖调试信息中的源文件名；没有源码行信息时，也无法匹配。</p><h2 id="3-3-验证-skip-是否实际加载"><a href="#3-3-验证-skip-是否实际加载" class="headerlink" title="3.3 验证 skip 是否实际加载"></a>3.3 验证 <code>skip</code> 是否实际加载</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">-exec info skip</span><br><span class="line">-exec show step-mode</span><br></pre></td></tr></table></figure><p>需要排查规则命中情况时，可临时开启：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">-exec set debug skip on</span><br></pre></td></tr></table></figure><p>使用完关闭：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">-exec set debug skip off</span><br></pre></td></tr></table></figure><p>如果 <code>info skip</code> 没有任何条目，说明跳过脚本根本没有加载，而不是规则本身失效。</p><hr><h2 id="4-比-skip-更可靠的处理：直接运行到下一行业务代码"><a href="#4-比-skip-更可靠的处理：直接运行到下一行业务代码" class="headerlink" title="4. 比 skip 更可靠的处理：直接运行到下一行业务代码"></a>4. 比 <code>skip</code> 更可靠的处理：直接运行到下一行业务代码</h2><p>当某一行包含 STL、内存分配或系统调用，而 F10&#x2F;F11 表现不稳定时，不必继续和系统库内部单步逻辑纠缠。</p><p>最可靠的策略是：<strong>在下一处确定的业务代码位置设置临时断点，然后继续运行。</strong></p><p>例如：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">-exec tbreak application.cpp:120</span><br><span class="line">-exec continue</span><br></pre></td></tr></table></figure><p>或者：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">-exec advance application.cpp:120</span><br></pre></td></tr></table></figure><p>两者的区别：</p><table><thead><tr><th>命令</th><th>特点</th></tr></thead><tbody><tr><td><code>tbreak file:line</code> + <code>continue</code></td><td>创建一次性断点，命中后自动删除，行为直观</td></tr><tr><td><code>advance file:line</code></td><td>在当前栈帧范围内运行到目标位置或当前函数返回</td></tr></tbody></table><p>这种方式不要求 GDB理解 <code>std::string</code>、<code>malloc()</code> 或 <code>memcpy()</code> 内部如何执行，只要求程序最终能回到指定业务源码位置。</p><p>对于第三方库调用，也可以使用函数断点：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">-exec tbreak third_party::Runtime::OnEvent</span><br><span class="line">-exec continue</span><br></pre></td></tr></table></figure><p>这是一种比连续按 F11 更确定、更适合远程调试的工作方式。</p><hr><h2 id="5-点击暂停后只看到地址和-，是不是又进入系统库了"><a href="#5-点击暂停后只看到地址和-，是不是又进入系统库了" class="headerlink" title="5. 点击暂停后只看到地址和 ??，是不是又进入系统库了"></a>5. 点击暂停后只看到地址和 <code>??</code>，是不是又进入系统库了</h2><p>通常是。</p><p>典型输出：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Thread 1 received signal SIGINT, Interrupt.</span><br><span class="line">0x0000fffff7ae4740 in ?? ()</span><br></pre></td></tr></table></figure><p>这并不说明程序异常，也不代表跳过规则失效。</p><h3 id="5-1-“暂停”是异步冻结，不是“停到我的代码”"><a href="#5-1-“暂停”是异步冻结，不是“停到我的代码”" class="headerlink" title="5.1 “暂停”是异步冻结，不是“停到我的代码”"></a>5.1 “暂停”是异步冻结，不是“停到我的代码”</h3><p>点击暂停的语义是：</p><blockquote><p>立即停止当前正在运行的线程。</p></blockquote><p>事件循环或多线程程序大部分时间可能处于：</p><ul><li><code>epoll_pwait()</code>；</li><li><code>poll()</code>；</li><li><code>read()</code>；</li><li><code>futex()</code>；</li><li><code>pthread_cond_wait()</code>；</li><li><code>nanosleep()</code>；</li><li>动态加载器；</li><li>系统调用返回路径。</li></ul><p>所以暂停时 CPU 在系统库，GDB 就会停在系统库。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[程序运行中] --&gt; B&#123;暂停瞬间 CPU 在哪里?&#125;</span><br><span class="line">    B --&gt;|业务代码| C[显示业务源码]</span><br><span class="line">    B --&gt;|第三方库且有符号| D[显示第三方库源码]</span><br><span class="line">    B --&gt;|系统库且无符号| E[显示地址和 ??]</span><br><span class="line">    E --&gt; F[查看调用栈、映射和反汇编]</span><br><span class="line">    F --&gt; G[设置业务断点后继续]</span><br></pre></td></tr></table></figure><h2 id="5-2-为什么此时-step-和-next-会报错"><a href="#5-2-为什么此时-step-和-next-会报错" class="headerlink" title="5.2 为什么此时 step 和 next 会报错"></a>5.2 为什么此时 <code>step</code> 和 <code>next</code> 会报错</h2><p>在 <code>??</code> 栈帧中，GDB 可能只知道当前 PC 地址，不知道：</p><ul><li>当前函数入口；</li><li>当前函数结束地址；</li><li>当前源码行范围；</li><li>下一源码行；</li><li>当前函数返回点。</li></ul><p>因此执行：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">-exec step</span><br><span class="line">-exec next</span><br><span class="line">-exec finish</span><br></pre></td></tr></table></figure><p>可能得到：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Cannot find bounds of current function</span><br></pre></td></tr></table></figure><p>这不是程序崩溃，而是当前栈帧没有足够的调试元数据。</p><h2 id="5-3-暂停后正确的检查顺序"><a href="#5-3-暂停后正确的检查顺序" class="headerlink" title="5.3 暂停后正确的检查顺序"></a>5.3 暂停后正确的检查顺序</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">-exec info threads</span><br><span class="line">-exec thread apply all bt</span><br><span class="line">-exec p/x $pc</span><br><span class="line">-exec info symbol $pc</span><br><span class="line">-exec info proc mappings</span><br><span class="line">-exec x/20i $pc-32</span><br></pre></td></tr></table></figure><table><thead><tr><th>命令</th><th>作用</th></tr></thead><tbody><tr><td><code>info threads</code></td><td>查看线程状态和当前线程</td></tr><tr><td><code>thread apply all bt</code></td><td>查看所有线程调用栈</td></tr><tr><td><code>p/x $pc</code></td><td>查看当前 AArch64 程序计数器</td></tr><tr><td><code>info symbol $pc</code></td><td>尝试识别当前地址对应符号</td></tr><tr><td><code>info proc mappings</code></td><td>判断当前地址属于哪个 ELF 或 <code>.so</code></td></tr><tr><td><code>x/20i $pc-32</code></td><td>查看当前地址前后的汇编指令</td></tr></tbody></table><p>如果调用栈上层存在业务代码或目标第三方库，可以在 VS Code 的“调用堆栈”中选择上层帧查看变量和源码。</p><p>但要注意：<strong>选择上层帧只改变观察上下文，不会把 CPU 的真实执行位置移动回该帧。</strong></p><p>要重新进入源码级调试，应设置业务断点后继续：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">-exec tbreak application.cpp:120</span><br><span class="line">-exec continue</span><br></pre></td></tr></table></figure><hr><h2 id="6-暂停控制与程序退出：正确处理-SIGINT-和库内部信号"><a href="#6-暂停控制与程序退出：正确处理-SIGINT-和库内部信号" class="headerlink" title="6. 暂停控制与程序退出：正确处理 SIGINT 和库内部信号"></a>6. 暂停控制与程序退出：正确处理 SIGINT 和库内部信号</h2><p>远程调试时，暂停动作通常与 <code>SIGINT</code> 有关。</p><p>如果应用本身把 <code>SIGINT</code> 当作正常退出请求，而 GDB 又配置为：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">handle SIGINT nostop noprint pass</span><br></pre></td></tr></table></figure><p>那么点击暂停可能变成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">VS Code pause</span><br><span class="line">  -&gt; GDB 产生中断</span><br><span class="line">  -&gt; SIGINT 传给应用</span><br><span class="line">  -&gt; 应用执行 shutdown</span><br><span class="line">  -&gt; 进程正常退出</span><br></pre></td></tr></table></figure><p>调试期间更合理的设置是：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">handle SIGINT stop print nopass</span><br></pre></td></tr></table></figure><p>即：</p><ul><li>GDB 因 <code>SIGINT</code> 停止；</li><li>控制台打印中断信息；</li><li>不把该信号继续传给应用。</li></ul><p>如果第三方库使用某个用户信号唤醒事件循环，例如 <code>SIGUSR1</code>，则应配置：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">handle SIGUSR1 nostop noprint pass</span><br></pre></td></tr></table></figure><p>信号处理原则是：</p><table><thead><tr><th>信号角色</th><th>GDB 建议策略</th></tr></thead><tbody><tr><td>调试器暂停信号</td><td><code>stop print nopass</code></td></tr><tr><td>第三方库内部唤醒信号</td><td><code>nostop noprint pass</code></td></tr><tr><td>应用正常终止信号</td><td>根据调试目标决定是否 <code>pass</code></td></tr></tbody></table><p>验证：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">-exec info signals SIGINT</span><br><span class="line">-exec info signals SIGUSR1</span><br><span class="line">-exec info signals SIGTERM</span><br></pre></td></tr></table></figure><hr><h2 id="7-系统库能不能完整调试"><a href="#7-系统库能不能完整调试" class="headerlink" title="7. 系统库能不能完整调试"></a>7. 系统库能不能完整调试</h2><p>可以。</p><p>GDB 并不限制进入 libc、libstdc++、libpthread 或动态加载器。要实现完整系统库源码调试，需要同时具备以下条件：</p><ol><li>与目标板运行库完全匹配的 ELF 文件；</li><li>对应版本的独立调试符号包；</li><li>对应版本的源码；</li><li>正确的 <code>sysroot</code>；</li><li>正确的 <code>debug-file-directory</code>；</li><li>正确的 <code>substitute-path</code>；</li><li>运行库与调试符号的 Build ID 一致。</li></ol><h2 id="7-1-为什么“同名库”不能代替“同一版本库”"><a href="#7-1-为什么“同名库”不能代替“同一版本库”" class="headerlink" title="7.1 为什么“同名库”不能代替“同一版本库”"></a>7.1 为什么“同名库”不能代替“同一版本库”</h2><p>远程目标可能运行 Debian、Ubuntu 或厂商定制 rootfs，而宿主机交叉 SDK 可能来自 Yocto。</p><p>即使两边都存在：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">libc.so.6</span><br><span class="line">libstdc++.so.6</span><br><span class="line">ld-linux-aarch64.so.1</span><br></pre></td></tr></table></figure><p>它们也可能不是同一版本、同一配置或同一次构建。</p><p>可通过 Build ID 检查：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">readelf -n &lt;library&gt; | grep <span class="string">&#x27;Build ID&#x27;</span></span><br></pre></td></tr></table></figure><p>只有名称相同而 Build ID 不同，不能视为可互换的调试符号来源。</p><p>使用错误系统库符号可能导致：</p><ul><li>函数边界判断错误；</li><li>源码行错位；</li><li>调用栈展开异常；</li><li>临时断点位置错误；</li><li>多线程调试行为异常。</li></ul><h2 id="7-2-完整系统库调试所需配置"><a href="#7-2-完整系统库调试所需配置" class="headerlink" title="7.2 完整系统库调试所需配置"></a>7.2 完整系统库调试所需配置</h2><p>典型 GDB 配置如下：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">set sysroot /path/to/matching-target-sysroot</span><br><span class="line">set debug-file-directory /path/to/matching-target-sysroot/usr/lib/debug</span><br><span class="line">set solib-search-path /path/to/matching-target-sysroot/lib:/path/to/matching-target-sysroot/usr/lib</span><br></pre></td></tr></table></figure><p>如果 DWARF 中记录的源码路径与宿主机源码路径不同，还需要：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">set substitute-path /build/glibc-source /path/to/local/glibc-source</span><br><span class="line">set substitute-path /build/gcc-source /path/to/local/gcc-source</span><br></pre></td></tr></table></figure><p>系统库调试准备完成后，原先的：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">0x... in ?? ()</span><br></pre></td></tr></table></figure><p>可能变成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">__GI___epoll_pwait(...)</span><br><span class="line">std::__cxx11::basic_string&lt;...&gt;::basic_string(...)</span><br><span class="line">__libc_malloc(...)</span><br></pre></td></tr></table></figure><p>并可继续进入对应源码。</p><hr><h2 id="8-无法同步目标板系统库时，应该怎么处理"><a href="#8-无法同步目标板系统库时，应该怎么处理" class="headerlink" title="8. 无法同步目标板系统库时，应该怎么处理"></a>8. 无法同步目标板系统库时，应该怎么处理</h2><p>如果无法把目标板运行库同步到宿主机，又不打算专门分析系统库内部实现，最合理的方式不是强行使用不匹配的 SDK 系统库，而是把系统库明确当作黑盒。</p><p>推荐调试边界：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">业务程序：完整源码级调试</span><br><span class="line">目标第三方库：完整源码级调试</span><br><span class="line">系统库：黑盒执行，不做源码级单步</span><br><span class="line">无符号位置：调用栈 + 地址 + 反汇编 + 指令级调试</span><br></pre></td></tr></table></figure><h2 id="8-1-关闭无关系统库的自动符号加载"><a href="#8-1-关闭无关系统库的自动符号加载" class="headerlink" title="8.1 关闭无关系统库的自动符号加载"></a>8.1 关闭无关系统库的自动符号加载</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">set auto-solib-add off</span><br></pre></td></tr></table></figure><p>然后只手动加载需要调试的第三方库：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sharedlibrary .*libthirdparty-.*</span><br></pre></td></tr></table></figure><p>这样可以避免 GDB 错误加载宿主机 SDK 中与目标不匹配的系统库符号。</p><h2 id="8-2-保留跳过规则，但不要把它当作绝对保证"><a href="#8-2-保留跳过规则，但不要把它当作绝对保证" class="headerlink" title="8.2 保留跳过规则，但不要把它当作绝对保证"></a>8.2 保留跳过规则，但不要把它当作绝对保证</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">set step-mode off</span><br><span class="line"></span><br><span class="line">skip -rfunction ^std::</span><br><span class="line">skip -rfunction ^__gnu_cxx::</span><br><span class="line">skip -rfunction ^__cxxabiv1::</span><br><span class="line">skip -rfunction ^operator new</span><br><span class="line">skip -rfunction ^operator delete</span><br><span class="line">skip -gfile */include/c++/**/**</span><br><span class="line">skip -gfile */libstdc++-v3/**/**</span><br></pre></td></tr></table></figure><p>这些规则用于提升日常 F11 体验，但遇到完全无符号的 <code>??</code> 时仍然可能失效。</p><h2 id="8-3-将“源码单步”切换为“断点驱动”"><a href="#8-3-将“源码单步”切换为“断点驱动”" class="headerlink" title="8.3 将“源码单步”切换为“断点驱动”"></a>8.3 将“源码单步”切换为“断点驱动”</h2><p>不要连续进入每一层函数，而是围绕业务边界设置断点：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">-exec break application.cpp:120</span><br><span class="line">-exec break runtime.cpp:260</span><br><span class="line">-exec break third_party::Runtime::OnEvent</span><br><span class="line">-exec continue</span><br></pre></td></tr></table></figure><p>遇到 STL 或系统调用时：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">-exec tbreak application.cpp:121</span><br><span class="line">-exec continue</span><br></pre></td></tr></table></figure><p>这比尝试跨越所有无符号调用更稳定。</p><hr><h2 id="9-无法源码级调试时，使用反汇编继续分析"><a href="#9-无法源码级调试时，使用反汇编继续分析" class="headerlink" title="9. 无法源码级调试时，使用反汇编继续分析"></a>9. 无法源码级调试时，使用反汇编继续分析</h2><p>即使没有系统库符号，机器码仍然存在，GDB 仍然可以进行 AArch64 反汇编和指令级调试。</p><p>这是无符号环境下最可靠的兜底手段。</p><h2 id="9-1-查看当前-PC-附近的汇编"><a href="#9-1-查看当前-PC-附近的汇编" class="headerlink" title="9.1 查看当前 PC 附近的汇编"></a>9.1 查看当前 PC 附近的汇编</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">-exec x/20i $pc</span><br></pre></td></tr></table></figure><p>查看当前指令前后：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">-exec x/40i $pc-64</span><br></pre></td></tr></table></figure><p>典型输出：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">0x0000ffff...:      mov     x0, x19</span><br><span class="line">0x0000ffff...:      bl      0x0000ffff...</span><br><span class="line">=&gt; 0x0000ffff...:   ldr     x1, [x0,#8]</span><br><span class="line">0x0000ffff...:      cbz     x1, 0x0000ffff...</span><br></pre></td></tr></table></figure><p><code>=&gt;</code> 表示当前 PC 所在指令。</p><h2 id="9-2-按地址范围反汇编"><a href="#9-2-按地址范围反汇编" class="headerlink" title="9.2 按地址范围反汇编"></a>9.2 按地址范围反汇编</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">-exec disassemble /r $pc-64,$pc+128</span><br></pre></td></tr></table></figure><p><code>/r</code> 会同时显示原始机器码和汇编指令，适合确认：</p><ul><li><code>bl</code> 调用了哪里；</li><li>条件跳转走了哪个分支；</li><li>当前寄存器参与了什么内存访问；</li><li>返回地址和栈帧是否合理。</li></ul><h2 id="9-3-反汇编已知函数"><a href="#9-3-反汇编已知函数" class="headerlink" title="9.3 反汇编已知函数"></a>9.3 反汇编已知函数</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">-exec disassemble application::Run</span><br></pre></td></tr></table></figure><p>源码与汇编混合：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">-exec disassemble /s application::Run</span><br></pre></td></tr></table></figure><p>带原始机器码：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">-exec disassemble /r application::Run</span><br></pre></td></tr></table></figure><h2 id="9-4-指令级单步"><a href="#9-4-指令级单步" class="headerlink" title="9.4 指令级单步"></a>9.4 指令级单步</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">-exec display/i $pc</span><br><span class="line">-exec si</span><br><span class="line">-exec ni</span><br></pre></td></tr></table></figure><p>含义：</p><ul><li><code>si</code>：执行一条指令，遇到 <code>bl</code> 会进入被调用函数；</li><li><code>ni</code>：执行一条指令，遇到调用时运行到返回；</li><li><code>display/i $pc</code>：每次停下自动显示当前指令。</li></ul><p>当源码级 <code>step</code> 报函数边界错误时，可以使用 <code>si</code> 或 <code>ni</code> 继续分析。</p><h2 id="9-5-查看-AArch64-关键寄存器"><a href="#9-5-查看-AArch64-关键寄存器" class="headerlink" title="9.5 查看 AArch64 关键寄存器"></a>9.5 查看 AArch64 关键寄存器</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">-exec info registers pc sp x0 x1 x2 x3 x4 x5 x6 x7 x29 x30</span><br></pre></td></tr></table></figure><table><thead><tr><th>寄存器</th><th>常见用途</th></tr></thead><tbody><tr><td><code>pc</code></td><td>当前指令地址</td></tr><tr><td><code>sp</code></td><td>栈指针</td></tr><tr><td><code>x0</code>～<code>x7</code></td><td>前八个整数或指针参数；<code>x0</code> 常兼作返回值</td></tr><tr><td><code>x29</code></td><td>帧指针 FP</td></tr><tr><td><code>x30</code></td><td>链接寄存器 LR，通常保存返回地址</td></tr></tbody></table><p>查看返回地址附近：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">-exec x/12i $x30-16</span><br></pre></td></tr></table></figure><h2 id="9-6-VS-Code-中打开反汇编视图"><a href="#9-6-VS-Code-中打开反汇编视图" class="headerlink" title="9.6 VS Code 中打开反汇编视图"></a>9.6 VS Code 中打开反汇编视图</h2><p>程序暂停后，可通过命令面板执行：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Debug: Open Disassembly View</span><br></pre></td></tr></table></figure><p>也可以在调用堆栈中右键当前栈帧打开反汇编视图。</p><p>如果当前帧为 <code>??</code>，VS Code 可能无法推断函数范围，此时命令行方式更可靠：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">-exec x/40i $pc-64</span><br><span class="line">-exec disassemble /r $pc-64,$pc+128</span><br></pre></td></tr></table></figure><hr><h2 id="10-推荐配置：只调试业务程序和目标第三方库"><a href="#10-推荐配置：只调试业务程序和目标第三方库" class="headerlink" title="10. 推荐配置：只调试业务程序和目标第三方库"></a>10. 推荐配置：只调试业务程序和目标第三方库</h2><p>下面是一套适用于“系统库不做源码调试”的配置思路。</p><h3 id="10-1-launch-json-关键配置"><a href="#10-1-launch-json-关键配置" class="headerlink" title="10.1 launch.json 关键配置"></a>10.1 <code>launch.json</code> 关键配置</h3><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Remote GDB Debug&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;cppdbg&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;request&quot;</span><span class="punctuation">:</span> <span class="string">&quot;launch&quot;</span><span class="punctuation">,</span></span><br><span class="line"></span><br><span class="line">    <span class="attr">&quot;program&quot;</span><span class="punctuation">:</span> <span class="string">&quot;$&#123;workspaceFolder&#125;/build/application&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;cwd&quot;</span><span class="punctuation">:</span> <span class="string">&quot;$&#123;workspaceFolder&#125;&quot;</span><span class="punctuation">,</span></span><br><span class="line"></span><br><span class="line">    <span class="attr">&quot;MIMode&quot;</span><span class="punctuation">:</span> <span class="string">&quot;gdb&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;miDebuggerPath&quot;</span><span class="punctuation">:</span> <span class="string">&quot;/path/to/aarch64-cross-gdb&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;miDebuggerServerAddress&quot;</span><span class="punctuation">:</span> <span class="string">&quot;&lt;target-host&gt;:&lt;gdbserver-port&gt;&quot;</span><span class="punctuation">,</span></span><br><span class="line"></span><br><span class="line">    <span class="attr">&quot;setupCommands&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">        <span class="punctuation">&#123;</span></span><br><span class="line">            <span class="attr">&quot;description&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Disable unrelated shared-library symbol loading&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="attr">&quot;text&quot;</span><span class="punctuation">:</span> <span class="string">&quot;set auto-solib-add off&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="attr">&quot;ignoreFailures&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">false</span></span></span><br><span class="line">        <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="punctuation">&#123;</span></span><br><span class="line">            <span class="attr">&quot;description&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Locate local third-party Debug libraries&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="attr">&quot;text&quot;</span><span class="punctuation">:</span> <span class="string">&quot;set solib-search-path $&#123;workspaceFolder&#125;/third_party/stage/usr/lib&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="attr">&quot;ignoreFailures&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">false</span></span></span><br><span class="line">        <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="punctuation">&#123;</span></span><br><span class="line">            <span class="attr">&quot;description&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Keep debugger interrupt inside GDB&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="attr">&quot;text&quot;</span><span class="punctuation">:</span> <span class="string">&quot;handle SIGINT stop print nopass&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="attr">&quot;ignoreFailures&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">false</span></span></span><br><span class="line">        <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="punctuation">&#123;</span></span><br><span class="line">            <span class="attr">&quot;description&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Pass library wake-up signal&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="attr">&quot;text&quot;</span><span class="punctuation">:</span> <span class="string">&quot;handle SIGUSR1 nostop noprint pass&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="attr">&quot;ignoreFailures&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">false</span></span></span><br><span class="line">        <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="punctuation">&#123;</span></span><br><span class="line">            <span class="attr">&quot;description&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Step over functions without source line information&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="attr">&quot;text&quot;</span><span class="punctuation">:</span> <span class="string">&quot;set step-mode off&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="attr">&quot;ignoreFailures&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">false</span></span></span><br><span class="line">        <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="punctuation">&#123;</span></span><br><span class="line">            <span class="attr">&quot;description&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Demangle C++ symbols in assembly&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="attr">&quot;text&quot;</span><span class="punctuation">:</span> <span class="string">&quot;set print asm-demangle on&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="attr">&quot;ignoreFailures&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">true</span></span></span><br><span class="line">        <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="punctuation">&#123;</span></span><br><span class="line">            <span class="attr">&quot;description&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Disable pagination&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="attr">&quot;text&quot;</span><span class="punctuation">:</span> <span class="string">&quot;set pagination off&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="attr">&quot;ignoreFailures&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">true</span></span></span><br><span class="line">        <span class="punctuation">&#125;</span></span><br><span class="line">    <span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line"></span><br><span class="line">    <span class="attr">&quot;postRemoteConnectCommands&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">        <span class="punctuation">&#123;</span></span><br><span class="line">            <span class="attr">&quot;description&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Load target third-party symbols&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="attr">&quot;text&quot;</span><span class="punctuation">:</span> <span class="string">&quot;source $&#123;workspaceFolder&#125;/.vscode/load-third-party-symbols.gdb&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="attr">&quot;ignoreFailures&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">false</span></span></span><br><span class="line">        <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="punctuation">&#123;</span></span><br><span class="line">            <span class="attr">&quot;description&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Install system-library stepping filters&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="attr">&quot;text&quot;</span><span class="punctuation">:</span> <span class="string">&quot;source $&#123;workspaceFolder&#125;/.vscode/skip-system-libs.gdb&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="attr">&quot;ignoreFailures&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">false</span></span></span><br><span class="line">        <span class="punctuation">&#125;</span></span><br><span class="line">    <span class="punctuation">]</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><h3 id="10-2-load-third-party-symbols-gdb"><a href="#10-2-load-third-party-symbols-gdb" class="headerlink" title="10.2 load-third-party-symbols.gdb"></a>10.2 <code>load-third-party-symbols.gdb</code></h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">set breakpoint pending on</span><br><span class="line"></span><br><span class="line">tbreak main</span><br><span class="line">commands</span><br><span class="line">    silent</span><br><span class="line">    sharedlibrary .*libthirdparty-.*</span><br><span class="line">    echo Third-party shared-library symbols loaded.\n</span><br><span class="line">    info sharedlibrary</span><br><span class="line">end</span><br></pre></td></tr></table></figure><h3 id="10-3-skip-system-libs-gdb"><a href="#10-3-skip-system-libs-gdb" class="headerlink" title="10.3 skip-system-libs.gdb"></a>10.3 <code>skip-system-libs.gdb</code></h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">set step-mode off</span><br><span class="line"></span><br><span class="line">skip -rfunction ^std::</span><br><span class="line">skip -rfunction ^__gnu_cxx::</span><br><span class="line">skip -rfunction ^__cxxabiv1::</span><br><span class="line">skip -rfunction ^operator new</span><br><span class="line">skip -rfunction ^operator delete</span><br><span class="line"></span><br><span class="line">skip -gfile */include/c++/**/**</span><br><span class="line">skip -gfile */libstdc++-v3/**/**</span><br><span class="line"></span><br><span class="line">echo System-library stepping filters installed.\n</span><br></pre></td></tr></table></figure><p>配置完成后验证：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">-exec info sharedlibrary</span><br><span class="line">-exec info skip</span><br><span class="line">-exec show step-mode</span><br><span class="line">-exec info signals SIGINT</span><br><span class="line">-exec info signals SIGUSR1</span><br></pre></td></tr></table></figure><hr><h2 id="11-一套可重复使用的诊断流程"><a href="#11-一套可重复使用的诊断流程" class="headerlink" title="11. 一套可重复使用的诊断流程"></a>11. 一套可重复使用的诊断流程</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[确认业务程序和目标动态库 Syms Read = Yes] --&gt; B[在业务代码设置断点]</span><br><span class="line">    B --&gt; C&#123;单步是否正常?&#125;</span><br><span class="line">    C -- 是 --&gt; D[继续源码级调试]</span><br><span class="line">    C -- 否 --&gt; E&#123;当前行是否含 STL/系统调用/隐式构造?&#125;</span><br><span class="line">    E -- 是 --&gt; F[优先 F10]</span><br><span class="line">    F --&gt; G&#123;仍持续运行?&#125;</span><br><span class="line">    G -- 是 --&gt; H[tbreak 下一行业务代码 + continue]</span><br><span class="line">    G -- 否 --&gt; D</span><br><span class="line">    E -- 否 --&gt; I[检查优化级别、DWARF 行表和源码映射]</span><br><span class="line"></span><br><span class="line">    H --&gt; J&#123;暂停后是否为 ??&#125;</span><br><span class="line">    J -- 否 --&gt; D</span><br><span class="line">    J -- 是 --&gt; K[bt + mappings + info symbol + x/i]</span><br><span class="line">    K --&gt; L&#123;需要系统库内部实现?&#125;</span><br><span class="line">    L -- 是 --&gt; M[准备匹配 sysroot、debug symbols 和源码]</span><br><span class="line">    L -- 否 --&gt; N[系统库黑盒处理]</span><br><span class="line">    N --&gt; O[设置业务/第三方库断点后 continue]</span><br><span class="line">    N --&gt; P[必要时 si/ni 指令级调试]</span><br></pre></td></tr></table></figure><h3 id="11-1-动态库前置确认"><a href="#11-1-动态库前置确认" class="headerlink" title="11.1 动态库前置确认"></a>11.1 动态库前置确认</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">-exec info sharedlibrary</span><br><span class="line">-exec sharedlibrary .*libthirdparty-.*</span><br></pre></td></tr></table></figure><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">file &lt;library&gt;</span><br><span class="line">readelf -n &lt;library&gt; | grep <span class="string">&#x27;Build ID&#x27;</span></span><br><span class="line">readelf -SW &lt;library&gt; | grep -E <span class="string">&#x27;\.debug_(info|line)&#x27;</span></span><br></pre></td></tr></table></figure><h3 id="11-2-单步策略检查"><a href="#11-2-单步策略检查" class="headerlink" title="11.2 单步策略检查"></a>11.2 单步策略检查</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">-exec info skip</span><br><span class="line">-exec show step-mode</span><br><span class="line">-exec show range-stepping</span><br></pre></td></tr></table></figure><h3 id="11-3-暂停位置检查"><a href="#11-3-暂停位置检查" class="headerlink" title="11.3 暂停位置检查"></a>11.3 暂停位置检查</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">-exec thread apply all bt</span><br><span class="line">-exec p/x $pc</span><br><span class="line">-exec info symbol $pc</span><br><span class="line">-exec info proc mappings</span><br><span class="line">-exec x/20i $pc-32</span><br></pre></td></tr></table></figure><h3 id="11-4-确定性返回业务代码"><a href="#11-4-确定性返回业务代码" class="headerlink" title="11.4 确定性返回业务代码"></a>11.4 确定性返回业务代码</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">-exec tbreak &lt;file&gt;:&lt;next-line&gt;</span><br><span class="line">-exec continue</span><br></pre></td></tr></table></figure><p>或者：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">-exec advance &lt;file&gt;:&lt;next-line&gt;</span><br></pre></td></tr></table></figure><h3 id="11-5-指令级分析"><a href="#11-5-指令级分析" class="headerlink" title="11.5 指令级分析"></a>11.5 指令级分析</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">-exec display/i $pc</span><br><span class="line">-exec disassemble /r $pc-64,$pc+128</span><br><span class="line">-exec info registers pc sp x0 x1 x2 x3 x29 x30</span><br><span class="line">-exec ni</span><br></pre></td></tr></table></figure><hr><h2 id="12-常见现象速查"><a href="#12-常见现象速查" class="headerlink" title="12. 常见现象速查"></a>12. 常见现象速查</h2><table><thead><tr><th>现象</th><th>实际原因</th><th>建议处理</th></tr></thead><tbody><tr><td>目标第三方库 <code>Syms Read = Yes</code></td><td>该库符号已加载</td><td>不需要继续重建该库</td></tr><tr><td>系统库 <code>Syms Read = No</code></td><td>系统库没有对应调试符号</td><td>不调试系统库时可以接受</td></tr><tr><td>停在结构体声明</td><td>隐式构造函数的 DWARF 行号归属</td><td>到对象定义处调试，成员构造优先 F10</td></tr><tr><td><code>std::string</code> 行 F11 后持续运行</td><td>进入 STL、libstdc++、libc 或内联实现</td><td>F10；或临时断点到下一行业务代码</td></tr><tr><td>已配置 <code>skip</code> 仍进入 <code>??</code></td><td>当前函数无名称或无源文件信息</td><td><code>skip</code> 无法匹配完全未知函数</td></tr><tr><td>点击暂停后显示地址和 <code>??</code></td><td>暂停瞬间位于无符号系统库</td><td>查看调用栈、映射和反汇编</td></tr><tr><td><code>Cannot find bounds of current function</code></td><td>当前帧缺少函数边界信息</td><td>使用 <code>si/ni</code>，或设置源码断点后继续</td></tr><tr><td>点击暂停后程序退出</td><td><code>SIGINT</code> 被传给应用退出处理器</td><td><code>handle SIGINT stop print nopass</code></td></tr><tr><td>第三方事件库反复触发 <code>SIGUSR1</code></td><td>库内部唤醒机制</td><td><code>handle SIGUSR1 nostop noprint pass</code></td></tr><tr><td>系统库能否源码调试</td><td>可以，但必须精确匹配运行库、符号和源码</td><td>准备 sysroot、debug 文件、源码映射</td></tr><tr><td>无法同步目标系统库</td><td>无法可靠做系统库源码调试</td><td>系统库黑盒化，断点驱动 + 反汇编兜底</td></tr></tbody></table><hr><h2 id="13-核心认识"><a href="#13-核心认识" class="headerlink" title="13. 核心认识"></a>13. 核心认识</h2><h3 id="13-1-动态库可调试，不代表整个进程都可源码级单步"><a href="#13-1-动态库可调试，不代表整个进程都可源码级单步" class="headerlink" title="13.1 动态库可调试，不代表整个进程都可源码级单步"></a>13.1 动态库可调试，不代表整个进程都可源码级单步</h3><p>业务程序和目标第三方库可以具备完整 DWARF，而系统库仍然没有。执行流跨越符号边界时，调试能力会随之变化。</p><h3 id="13-2-F11-不是“执行下一行”，而是“尝试进入当前调用”"><a href="#13-2-F11-不是“执行下一行”，而是“尝试进入当前调用”" class="headerlink" title="13.2 F11 不是“执行下一行”，而是“尝试进入当前调用”"></a>13.2 F11 不是“执行下一行”，而是“尝试进入当前调用”</h3><p>一行业务代码可能包含多个函数调用。F11 进入哪一层，取决于编译器生成代码、内联、行号表和符号可用性。</p><h3 id="13-3-skip-是策略，不是隔离机制"><a href="#13-3-skip-是策略，不是隔离机制" class="headerlink" title="13.3 skip 是策略，不是隔离机制"></a>13.3 <code>skip</code> 是策略，不是隔离机制</h3><p>它只在 GDB 能识别函数或源文件时影响 <code>step</code>。它不能阻止系统库执行，也不能控制异步暂停位置。</p><h3 id="13-4-异步暂停落在系统库是事件驱动程序的正常状态"><a href="#13-4-异步暂停落在系统库是事件驱动程序的正常状态" class="headerlink" title="13.4 异步暂停落在系统库是事件驱动程序的正常状态"></a>13.4 异步暂停落在系统库是事件驱动程序的正常状态</h3><p>事件循环程序大部分时间都在等待系统调用。暂停看到 <code>epoll</code>、<code>futex</code> 或 <code>??</code>，不等于业务程序异常。</p><h3 id="13-5-远程调试应从“连续单步”转向“断点驱动”"><a href="#13-5-远程调试应从“连续单步”转向“断点驱动”" class="headerlink" title="13.5 远程调试应从“连续单步”转向“断点驱动”"></a>13.5 远程调试应从“连续单步”转向“断点驱动”</h3><p>在复杂 C++、多线程、事件循环和第三方动态库场景中，临时断点、函数断点和回调断点通常比逐层 F11 更稳定。</p><h3 id="13-6-反汇编不是最后无奈，而是调试能力的一部分"><a href="#13-6-反汇编不是最后无奈，而是调试能力的一部分" class="headerlink" title="13.6 反汇编不是最后无奈，而是调试能力的一部分"></a>13.6 反汇编不是最后无奈，而是调试能力的一部分</h3><p>当函数边界、源码和 DWARF 不可用时，PC、寄存器、栈和机器指令仍然是可靠事实。能够在源码级和指令级之间切换，是嵌入式 Linux 远程调试的基本能力。</p><hr><h2 id="14-最终建议"><a href="#14-最终建议" class="headerlink" title="14. 最终建议"></a>14. 最终建议</h2><p>对于已经确认业务程序和目标第三方动态库可调试，但系统库无法准备或不值得准备的场景，推荐采用以下策略：</p><ol><li>业务程序使用 <code>-O0 -g3 -fno-omit-frame-pointer</code> 构建；</li><li>目标第三方库使用同一次构建产生的未 strip Debug 文件；</li><li>只手动加载需要调试的第三方库符号；</li><li>关闭无关系统库的自动符号加载；</li><li>为 <code>std::</code>、C++ ABI 和标准库头文件配置 <code>skip</code>；</li><li>STL 构造、内存分配和系统调用语句优先使用 F10；</li><li>F10&#x2F;F11 不稳定时使用 <code>tbreak</code> 或 <code>advance</code>；</li><li>暂停后落入 <code>??</code> 时先看调用栈和内存映射；</li><li>不在无符号帧上继续执行源码级 <code>step/next</code>；</li><li>必要时使用 <code>disassemble</code>、<code>si</code>、<code>ni</code> 和寄存器视图；</li><li>通过业务回调、状态变化和错误处理路径设置断点，而不是依赖连续单步；</li><li>只有确实需要分析 libc 或 libstdc++ 内部问题时，才投入成本准备匹配的系统库调试环境。</li></ol><p>一句话总结：</p><blockquote><p>当动态库已经具备调试符号，但断点和单步仍然异常时，应把问题从“库有没有符号”转向“当前执行流跨到了哪里、该位置是否有函数边界和源码行信息、是否应该源码级进入，以及无法进入时如何通过断点和反汇编继续推进”。</p></blockquote><h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ol><li><a href="https://sourceware.org/gdb/current/onlinedocs/gdb.html/Continuing-and-Stepping.html">GDB Manual: Continuing and Stepping</a></li><li><a href="https://sourceware.org/gdb/current/onlinedocs/gdb.html/Skipping-Over-Functions-and-Files.html">GDB Manual: Skipping Over Functions and Files</a></li><li><a href="https://sourceware.org/gdb/current/onlinedocs/gdb.html/Signals.html">GDB Manual: Signals</a></li><li><a href="https://sourceware.org/gdb/current/onlinedocs/gdb.html/Files.html">GDB Manual: Files, sysroot and solib-search-path</a></li><li><a href="https://sourceware.org/gdb/current/onlinedocs/gdb.html/Memory.html">GDB Manual: Examining Memory</a></li><li><a href="https://sourceware.org/gdb/current/onlinedocs/gdb.html/Machine-Code.html">GDB Manual: Machine Code</a></li><li><a href="https://code.visualstudio.com/docs/cpp/cpp-debug">VS Code: Debug C++</a></li><li><a href="https://code.visualstudio.com/docs/cpp/launch-json-reference">VS Code: Configure C&#x2F;C++ debugging</a></li></ol>]]></content>
    
    
      
      
    <summary type="html">&lt;meta name=&quot;referrer&quot; content=&quot;no-referrer&quot; /&gt;


&lt;h1 id=&quot;VS-Code-GDB-远程调试中的系统库边界、跳过策略与反汇编排查&quot;&gt;&lt;a href=&quot;#VS-Code-GDB-远程调试中的系统库边界、跳过策略与反汇编排查&quot; c</summary>
      
    
    
    
    <category term="杂谈" scheme="https://wdfk-prog.space/categories/%E6%9D%82%E8%B0%88/"/>
    
    
    <category term="杂谈" scheme="https://wdfk-prog.space/tags/%E6%9D%82%E8%B0%88/"/>
    
  </entry>
  
  <entry>
    <title>VS Code Remote GDB 调试动态库源码断点灰色：问题分析与解决方案</title>
    <link href="https://wdfk-prog.space/posts/95e2e7d2/"/>
    <id>https://wdfk-prog.space/posts/95e2e7d2/</id>
    <published>2026-07-25T01:55:32.000Z</published>
    <updated>2026-07-25T08:42:41.213Z</updated>
    
    <content type="html"><![CDATA[<meta name="referrer" content="no-referrer" /><h1 id="VS-Code-Remote-GDB-调试动态库源码断点灰色：问题分析与解决方案"><a href="#VS-Code-Remote-GDB-调试动态库源码断点灰色：问题分析与解决方案" class="headerlink" title="VS Code Remote GDB 调试动态库源码断点灰色：问题分析与解决方案"></a>VS Code Remote GDB 调试动态库源码断点灰色：问题分析与解决方案</h1><p><img src="https://i-blog.csdnimg.cn/direct/2331e2f8b34f4b88a59a8b6e4e637c9c.png" alt="在这里插入图片描述"></p><p>@[toc]</p><h2 id="1-问题概述"><a href="#1-问题概述" class="headerlink" title="1. 问题概述"></a>1. 问题概述</h2><p>在嵌入式 Linux 交叉调试中，主程序源码断点能够正常命中，但第三方动态库源码中的断点一直显示为灰色空心状态。GDB 中对应断点显示为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">&lt;PENDING&gt;</span><br></pre></td></tr></table></figure><p>这表示 GDB 已接受断点请求，但尚未将源码行解析为实际运行地址。</p><p>动态库源码断点同时依赖以下条件：</p><ol><li>动态库包含 DWARF 调试信息；</li><li>动态库未被剥离；</li><li>构建输出、GDB sysroot 和目标设备中的动态库完全一致；</li><li>GDB 能找到正确的本地动态库副本；</li><li>GDB 已读取该动态库的完整调试符号；</li><li>DWARF 中记录的源码路径能够映射到当前工作区；</li><li>调试会话建立时机与共享库加载时机匹配。</li></ol><hr><h2 id="2-调试链路"><a href="#2-调试链路" class="headerlink" title="2. 调试链路"></a>2. 调试链路</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    A[源码目录] --&gt; B[构建输出动态库]</span><br><span class="line">    B --&gt; C[GDB sysroot 动态库]</span><br><span class="line">    B --&gt; D[目标设备动态库]</span><br><span class="line">    C --&gt; E[交叉 GDB 读取 DWARF]</span><br><span class="line">    D --&gt; F[目标进程实际执行]</span><br><span class="line">    E --&gt; G[源码行与运行地址映射]</span><br><span class="line">    F --&gt; G</span><br></pre></td></tr></table></figure><p>三份动态库用途不同：</p><ul><li>构建输出目录保存当前编译结果；</li><li>GDB sysroot 供宿主机上的交叉 GDB 读取符号；</li><li>目标设备动态库供程序运行时加载。</li></ul><p>三者必须来自同一次构建。</p><hr><h2 id="3-检查动态库是否具备调试条件"><a href="#3-检查动态库是否具备调试条件" class="headerlink" title="3. 检查动态库是否具备调试条件"></a>3. 检查动态库是否具备调试条件</h2><p>检查真正的版本化动态库：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">file <span class="string">&quot;<span class="variable">$&#123;STAGE_LIB&#125;</span>/liblely-io2.so.2.x.x&quot;</span></span><br></pre></td></tr></table></figure><p>正常结果应包含：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">ELF 64-bit LSB shared object</span><br><span class="line">ARM aarch64</span><br><span class="line">with debug_info</span><br><span class="line">not stripped</span><br></pre></td></tr></table></figure><p>进一步检查 DWARF 段：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">LC_ALL=C readelf -SW <span class="string">&quot;<span class="variable">$&#123;STAGE_LIB&#125;</span>/liblely-io2.so.2.x.x&quot;</span> |</span><br><span class="line">    grep -E <span class="string">&#x27;\.debug_info|\.debug_line&#x27;</span></span><br></pre></td></tr></table></figure><p>至少应存在：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">.debug_info</span><br><span class="line">.debug_line</span><br></pre></td></tr></table></figure><p>如果动态库显示为 <code>stripped</code>，或者没有 <code>.debug_line</code>，源码行断点通常无法使用。</p><h3 id="注意命令本地化"><a href="#注意命令本地化" class="headerlink" title="注意命令本地化"></a>注意命令本地化</h3><p>非英文系统中的 <code>readelf -h</code> 可能输出本地化字段。自动化脚本如果只匹配英文 <code>Machine:</code>，可能把合法的 AArch64 动态库误判为未知架构。</p><p>脚本解析 <code>readelf</code> 输出时应固定语言环境：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">LC_ALL=C readelf -h library.so</span><br><span class="line">LC_ALL=C readelf -n library.so</span><br><span class="line">LC_ALL=C readelf -SW library.so</span><br></pre></td></tr></table></figure><hr><h2 id="4-离线验证-DWARF-行表"><a href="#4-离线验证-DWARF-行表" class="headerlink" title="4. 离线验证 DWARF 行表"></a>4. 离线验证 DWARF 行表</h2><p>在连接目标设备之前，直接使用交叉 GDB 检查动态库：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">GDB=<span class="string">&quot;<span class="variable">$&#123;SDK_ROOT&#125;</span>/usr/bin/aarch64-linux-gdb&quot;</span></span><br><span class="line">LIB=<span class="string">&quot;<span class="variable">$&#123;STAGE_LIB&#125;</span>/liblely-io2.so.2.x.x&quot;</span></span><br><span class="line">SRC=<span class="string">&quot;<span class="variable">$&#123;WORKSPACE&#125;</span>/lely-core/src/io2/ctx.c&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="string">&quot;<span class="variable">$&#123;GDB&#125;</span>&quot;</span> -q -nx -batch     -ex <span class="string">&quot;set pagination off&quot;</span>     -ex <span class="string">&quot;file <span class="variable">$&#123;LIB&#125;</span>&quot;</span>     -ex <span class="string">&quot;info address io_ctx_create&quot;</span>     -ex <span class="string">&quot;info line io_ctx_create&quot;</span>     -ex <span class="string">&quot;info line <span class="variable">$&#123;SRC&#125;</span>:94&quot;</span></span><br></pre></td></tr></table></figure><p>正常结果类似：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Symbol &quot;io_ctx_create&quot; is a function at address 0x...</span><br><span class="line">Line 94 of &quot;../../../src/io2/ctx.c&quot; starts at address 0x...</span><br></pre></td></tr></table></figure><p>这一步成功说明：</p><ul><li>函数符号存在；</li><li>动态库包含有效 DWARF；</li><li>指定源码行有对应机器指令；</li><li>当前问题不在源码编译阶段。</li></ul><p>因此，不应仅因为断点灰色就立即改成 <code>-O0</code> 重新编译。</p><hr><h2 id="5-比较构建输出、sysroot-和目标设备动态库"><a href="#5-比较构建输出、sysroot-和目标设备动态库" class="headerlink" title="5. 比较构建输出、sysroot 和目标设备动态库"></a>5. 比较构建输出、sysroot 和目标设备动态库</h2><h3 id="Build-ID"><a href="#Build-ID" class="headerlink" title="Build ID"></a>Build ID</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">LC_ALL=C readelf -n <span class="string">&quot;<span class="variable">$&#123;STAGE_LIB&#125;</span>/liblely-io2.so.2.x.x&quot;</span> |</span><br><span class="line">    grep <span class="string">&quot;Build ID&quot;</span></span><br><span class="line"></span><br><span class="line">LC_ALL=C readelf -n <span class="string">&quot;<span class="variable">$&#123;SYSROOT&#125;</span>/usr/local/lib/liblely-io2.so.2.x.x&quot;</span> |</span><br><span class="line">    grep <span class="string">&quot;Build ID&quot;</span></span><br><span class="line"></span><br><span class="line">ssh <span class="string">&quot;<span class="variable">$&#123;TARGET&#125;</span>&quot;</span>     <span class="string">&quot;LC_ALL=C readelf -n /usr/local/lib/liblely-io2.so.2.x.x |</span></span><br><span class="line"><span class="string">     grep &#x27;Build ID&#x27;&quot;</span></span><br></pre></td></tr></table></figure><p>三者应完全一致。</p><h3 id="SHA-256"><a href="#SHA-256" class="headerlink" title="SHA-256"></a>SHA-256</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">sha256sum</span> <span class="string">&quot;<span class="variable">$&#123;STAGE_LIB&#125;</span>/liblely-io2.so.2.x.x&quot;</span></span><br><span class="line"><span class="built_in">sha256sum</span> <span class="string">&quot;<span class="variable">$&#123;SYSROOT&#125;</span>/usr/local/lib/liblely-io2.so.2.x.x&quot;</span></span><br><span class="line"></span><br><span class="line">ssh <span class="string">&quot;<span class="variable">$&#123;TARGET&#125;</span>&quot;</span>     <span class="string">&quot;sha256sum /usr/local/lib/liblely-io2.so.2.x.x&quot;</span></span><br></pre></td></tr></table></figure><p>如果目标设备运行的库与 GDB 读取的本地库不一致，可能出现：</p><ul><li>断点无法解析；</li><li>断点落在错误源码行；</li><li>单步执行跳行；</li><li>局部变量异常；</li><li>调用栈与源码不匹配。</li></ul><hr><h2 id="6-部署后必须重启目标进程"><a href="#6-部署后必须重启目标进程" class="headerlink" title="6. 部署后必须重启目标进程"></a>6. 部署后必须重启目标进程</h2><p>替换磁盘上的动态库不会更新已经运行进程中的内存映射。</p><p>部署完成后可能出现：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">磁盘文件：新版本</span><br><span class="line">运行进程：仍映射旧版本</span><br></pre></td></tr></table></figure><p>因此应停止旧的目标程序和 gdbserver，然后重新启动：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">pkill gdbserver</span><br><span class="line">pkill -f application_name</span><br><span class="line"></span><br><span class="line">gdbserver :<span class="variable">$&#123;GDB_PORT&#125;</span> ./application</span><br></pre></td></tr></table></figure><p>VS Code 原调试会话也必须完全结束并重新建立。</p><hr><h2 id="7-运行时检查共享库符号状态"><a href="#7-运行时检查共享库符号状态" class="headerlink" title="7. 运行时检查共享库符号状态"></a>7. 运行时检查共享库符号状态</h2><p>程序停在 <code>main()</code> 后，在 VS Code Debug Console 中执行：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">-exec show architecture</span><br><span class="line">-exec show sysroot</span><br><span class="line">-exec show solib-search-path</span><br><span class="line">-exec show auto-solib-add</span><br><span class="line">-exec info sharedlibrary liblely-io2</span><br></pre></td></tr></table></figure><p>架构应显示为 AArch64：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">The target architecture is set to &quot;auto&quot; (currently &quot;aarch64&quot;).</span><br></pre></td></tr></table></figure><p>最关键的是 <code>Syms Read</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">From                To                  Syms Read   Shared Object Library</span><br><span class="line">0x...               0x...               No          .../liblely-io2.so.2</span><br></pre></td></tr></table></figure><p><code>Syms Read No</code> 表示：</p><blockquote><p>GDB 已发现目标进程加载了该动态库，也找到了本地文件，但尚未读取其完整调试符号。</p></blockquote><p>这与“找不到动态库文件”不是同一个问题。</p><hr><h2 id="8-关键诊断：显式加载动态库符号"><a href="#8-关键诊断：显式加载动态库符号" class="headerlink" title="8. 关键诊断：显式加载动态库符号"></a>8. 关键诊断：显式加载动态库符号</h2><p>执行：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">-exec sharedlibrary liblely-io2</span><br></pre></td></tr></table></figure><p>再次检查：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">-exec info sharedlibrary liblely-io2</span><br></pre></td></tr></table></figure><p>如果变成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Syms Read   Yes</span><br></pre></td></tr></table></figure><p>继续验证：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">-exec info address io_ctx_create</span><br><span class="line">-exec info line io_ctx_create</span><br></pre></td></tr></table></figure><p>然后设置源码行断点：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">-exec break /workspace/lely-core/src/io2/ctx.c:94</span><br><span class="line">-exec info break</span><br></pre></td></tr></table></figure><p>成功时会显示实际运行地址：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Breakpoint ... at 0x...:</span><br><span class="line">file ../../../src/io2/ctx.c, line 94.</span><br></pre></td></tr></table></figure><p>原来的 <code>&lt;PENDING&gt;</code> 会变为有效地址。</p><p>由此可以确认直接原因：</p><blockquote><p>目标进程已经加载动态库，但 GDB 没有自动读取该动态库的完整调试符号。</p></blockquote><hr><h2 id="9-为什么-auto-solib-add-on-仍可能没有加载符号"><a href="#9-为什么-auto-solib-add-on-仍可能没有加载符号" class="headerlink" title="9. 为什么 auto-solib-add on 仍可能没有加载符号"></a>9. 为什么 <code>auto-solib-add on</code> 仍可能没有加载符号</h2><p>远程调试通常按以下顺序启动：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant VS as VS Code</span><br><span class="line">    participant GDB as Cross GDB</span><br><span class="line">    participant GS as gdbserver</span><br><span class="line">    participant LD as Dynamic Loader</span><br><span class="line">    participant APP as Application</span><br><span class="line"></span><br><span class="line">    VS-&gt;&gt;GDB: 启动调试器</span><br><span class="line">    GDB-&gt;&gt;GS: 连接远程目标</span><br><span class="line">    GS--&gt;&gt;GDB: 停在动态加载器入口</span><br><span class="line">    GDB-&gt;&gt;LD: 继续运行</span><br><span class="line">    LD-&gt;&gt;LD: 加载依赖动态库</span><br><span class="line">    LD-&gt;&gt;APP: 进入 main</span><br><span class="line">    APP--&gt;&gt;GDB: 命中 main 断点</span><br></pre></td></tr></table></figure><p>在部分 <code>cppdbg + gdbserver + 交叉 GDB</code> 组合中，可能同时出现：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">auto-solib-add = on</span><br><span class="line">动态库已经出现在共享库列表中</span><br><span class="line">Syms Read = No</span><br></pre></td></tr></table></figure><p>这表明自动符号加载链路没有完成。</p><p>仅凭该现象，不能直接断言一定是 VS Code 扩展缺陷，也可能与调试适配器的符号过滤、GDB&#x2F;MI 命令时序或动态加载器事件有关。</p><p>确定性的解决方法是在动态库已经加载后执行：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sharedlibrary liblely-io2</span><br></pre></td></tr></table></figure><hr><h2 id="10-VS-Code-自动化解决方案"><a href="#10-VS-Code-自动化解决方案" class="headerlink" title="10. VS Code 自动化解决方案"></a>10. VS Code 自动化解决方案</h2><h3 id="10-1-创建-GDB-命令文件"><a href="#10-1-创建-GDB-命令文件" class="headerlink" title="10.1 创建 GDB 命令文件"></a>10.1 创建 GDB 命令文件</h3><p>创建：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">.vscode/load-lely-symbols.gdb</span><br></pre></td></tr></table></figure><p>内容：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">tbreak main</span><br><span class="line">commands</span><br><span class="line">  silent</span><br><span class="line">  sharedlibrary liblely-io2</span><br><span class="line">end</span><br></pre></td></tr></table></figure><p>作用：</p><ol><li>设置一次性 <code>main</code> 断点；</li><li>程序到达 <code>main()</code> 时执行命令；</li><li>此时动态链接器已经完成依赖库加载；</li><li>GDB 显式读取 <code>liblely-io2</code> 调试符号；</li><li>pending 断点自动重新解析。</li></ol><h3 id="10-2-launch-json-关键配置"><a href="#10-2-launch-json-关键配置" class="headerlink" title="10.2 launch.json 关键配置"></a>10.2 <code>launch.json</code> 关键配置</h3><figure class="highlight jsonc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Remote AArch64 GDB Debug&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;cppdbg&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;request&quot;</span><span class="punctuation">:</span> <span class="string">&quot;launch&quot;</span><span class="punctuation">,</span></span><br><span class="line"></span><br><span class="line">    <span class="attr">&quot;program&quot;</span><span class="punctuation">:</span> <span class="string">&quot;$&#123;workspaceFolder&#125;/application/build/application&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;cwd&quot;</span><span class="punctuation">:</span> <span class="string">&quot;$&#123;workspaceFolder&#125;/application&quot;</span><span class="punctuation">,</span></span><br><span class="line"></span><br><span class="line">    <span class="attr">&quot;MIMode&quot;</span><span class="punctuation">:</span> <span class="string">&quot;gdb&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;miDebuggerServerAddress&quot;</span><span class="punctuation">:</span> <span class="string">&quot;$&#123;env:TARGET_HOST&#125;:$&#123;env:GDB_PORT&#125;&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;miDebuggerPath&quot;</span><span class="punctuation">:</span> <span class="string">&quot;$&#123;env:SDK_ROOT&#125;/usr/bin/aarch64-linux-gdb&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;targetArchitecture&quot;</span><span class="punctuation">:</span> <span class="string">&quot;arm64&quot;</span><span class="punctuation">,</span></span><br><span class="line"></span><br><span class="line">    <span class="attr">&quot;additionalSOLibSearchPath&quot;</span><span class="punctuation">:</span> <span class="string">&quot;$&#123;env:TARGET_SYSROOT&#125;/usr/local/lib&quot;</span><span class="punctuation">,</span></span><br><span class="line"></span><br><span class="line">    <span class="attr">&quot;symbolLoadInfo&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;loadAll&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">false</span></span></span><br><span class="line">    <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line"></span><br><span class="line">    <span class="attr">&quot;setupCommands&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">        <span class="punctuation">&#123;</span></span><br><span class="line">            <span class="attr">&quot;description&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Enable pending breakpoints&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="attr">&quot;text&quot;</span><span class="punctuation">:</span> <span class="string">&quot;set breakpoint pending on&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="attr">&quot;ignoreFailures&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">false</span></span></span><br><span class="line">        <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="punctuation">&#123;</span></span><br><span class="line">            <span class="attr">&quot;description&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Set target sysroot&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="attr">&quot;text&quot;</span><span class="punctuation">:</span> <span class="string">&quot;set sysroot $&#123;env:TARGET_SYSROOT&#125;&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="attr">&quot;ignoreFailures&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">false</span></span></span><br><span class="line">        <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="punctuation">&#123;</span></span><br><span class="line">            <span class="attr">&quot;description&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Set shared library search path&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="attr">&quot;text&quot;</span><span class="punctuation">:</span> <span class="string">&quot;set solib-search-path $&#123;env:TARGET_SYSROOT&#125;/usr/local/lib&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="attr">&quot;ignoreFailures&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">false</span></span></span><br><span class="line">        <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="punctuation">&#123;</span></span><br><span class="line">            <span class="attr">&quot;description&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Enable automatic shared library loading&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="attr">&quot;text&quot;</span><span class="punctuation">:</span> <span class="string">&quot;set auto-solib-add on&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="attr">&quot;ignoreFailures&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">false</span></span></span><br><span class="line">        <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="punctuation">&#123;</span></span><br><span class="line">            <span class="attr">&quot;description&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Disable pagination&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="attr">&quot;text&quot;</span><span class="punctuation">:</span> <span class="string">&quot;set pagination off&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="attr">&quot;ignoreFailures&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">true</span></span></span><br><span class="line">        <span class="punctuation">&#125;</span></span><br><span class="line">    <span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line"></span><br><span class="line">    <span class="attr">&quot;postRemoteConnectCommands&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">        <span class="punctuation">&#123;</span></span><br><span class="line">            <span class="attr">&quot;description&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Install delayed library symbol loader&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="attr">&quot;text&quot;</span><span class="punctuation">:</span> <span class="string">&quot;source $&#123;workspaceFolder&#125;/.vscode/load-lely-symbols.gdb&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="attr">&quot;ignoreFailures&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">false</span></span></span><br><span class="line">        <span class="punctuation">&#125;</span></span><br><span class="line">    <span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line"></span><br><span class="line">    <span class="attr">&quot;sourceFileMap&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;/build/source/application&quot;</span><span class="punctuation">:</span> <span class="string">&quot;$&#123;workspaceFolder&#125;/application&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;/build/source/lely-core&quot;</span><span class="punctuation">:</span> <span class="string">&quot;$&#123;workspaceFolder&#125;/lely-core&quot;</span></span><br><span class="line">    <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p>这里应尽量只保留一个经过校验的动态库搜索目录，避免 GDB 在多个不同构建结果之间选择错误文件。</p><hr><h2 id="11-验收方法"><a href="#11-验收方法" class="headerlink" title="11. 验收方法"></a>11. 验收方法</h2><p>重新启动目标程序、gdbserver 和 VS Code 调试会话。</p><p>程序停在 <code>main()</code> 后执行：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">-exec info sharedlibrary liblely-io2</span><br></pre></td></tr></table></figure><p>预期：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Syms Read   Yes</span><br></pre></td></tr></table></figure><p>检查断点：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">-exec info break</span><br></pre></td></tr></table></figure><p>预期断点具有实际地址，而不是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">&lt;PENDING&gt;</span><br></pre></td></tr></table></figure><p>再检查源码行：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">-exec info line /workspace/lely-core/src/io2/ctx.c:94</span><br></pre></td></tr></table></figure><p>最后继续运行：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">-exec continue</span><br></pre></td></tr></table></figure><p>当程序再次调用目标函数时，断点应实际命中。</p><hr><h2 id="12-VS-Code-图标与-GDB-状态不一致"><a href="#12-VS-Code-图标与-GDB-状态不一致" class="headerlink" title="12. VS Code 图标与 GDB 状态不一致"></a>12. VS Code 图标与 GDB 状态不一致</h2><p>判断断点是否真正有效，应以 GDB 输出为准：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">-exec info break</span><br></pre></td></tr></table></figure><p>如果显示：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">breakpoint keep y 0x...</span><br><span class="line">in io_ctx_create at .../ctx.c:94</span><br></pre></td></tr></table></figure><p>说明断点已经绑定成功。</p><p>即使 VS Code 图标短暂保持空心，也可能只是调试适配器的 UI 状态未及时刷新。删除后重新创建断点，或者直接继续运行验证即可。</p><p>如果 GDB 中仍显示 <code>&lt;PENDING&gt;</code>，才说明断点确实尚未解析。</p><hr><h2 id="13-常见误区"><a href="#13-常见误区" class="headerlink" title="13. 常见误区"></a>13. 常见误区</h2><h3 id="看到灰色断点就立即改为-O0"><a href="#看到灰色断点就立即改为-O0" class="headerlink" title="看到灰色断点就立即改为 -O0"></a>看到灰色断点就立即改为 <code>-O0</code></h3><p>先用交叉 GDB 离线执行：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">info line source.c:line</span><br></pre></td></tr></table></figure><p>如果源码行已经有地址，优化不是首要原因。</p><h3 id="只更新目标设备动态库"><a href="#只更新目标设备动态库" class="headerlink" title="只更新目标设备动态库"></a>只更新目标设备动态库</h3><p>GDB 从本地 sysroot 读取符号，因此目标设备和 sysroot 必须同步。</p><h3 id="auto-solib-add-on-等于符号一定加载成功"><a href="#auto-solib-add-on-等于符号一定加载成功" class="headerlink" title="auto-solib-add on 等于符号一定加载成功"></a><code>auto-solib-add on</code> 等于符号一定加载成功</h3><p>必须通过以下命令确认：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">info sharedlibrary</span><br></pre></td></tr></table></figure><p>以 <code>Syms Read Yes/No</code> 为准。</p><h3 id="sourceFileMap-可以解决所有-pending-断点"><a href="#sourceFileMap-可以解决所有-pending-断点" class="headerlink" title="sourceFileMap 可以解决所有 pending 断点"></a><code>sourceFileMap</code> 可以解决所有 pending 断点</h3><p><code>sourceFileMap</code> 只解决源码路径映射，不能替代动态库符号加载。</p><h3 id="替换动态库后不重启目标进程"><a href="#替换动态库后不重启目标进程" class="headerlink" title="替换动态库后不重启目标进程"></a>替换动态库后不重启目标进程</h3><p>运行中的进程仍映射旧库，必须重启程序和 gdbserver。</p><h3 id="同时配置多个动态库搜索目录更保险"><a href="#同时配置多个动态库搜索目录更保险" class="headerlink" title="同时配置多个动态库搜索目录更保险"></a>同时配置多个动态库搜索目录更保险</h3><p>多个目录中可能存在不同构建版本，反而增加误加载风险。应只保留一份经过校验的 sysroot。</p><hr><h2 id="14-推荐排查流程"><a href="#14-推荐排查流程" class="headerlink" title="14. 推荐排查流程"></a>14. 推荐排查流程</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[动态库源码断点灰色] --&gt; B&#123;动态库包含 debug_info 且未 strip&#125;</span><br><span class="line">    B -- 否 --&gt; C[重新构建并保留 DWARF]</span><br><span class="line">    B -- 是 --&gt; D&#123;交叉 GDB 离线 info line 成功&#125;</span><br><span class="line">    D -- 否 --&gt; E[检查行号、优化和 DWARF 路径]</span><br><span class="line">    D -- 是 --&gt; F&#123;构建输出、sysroot、目标库一致&#125;</span><br><span class="line">    F -- 否 --&gt; G[同步部署并重启进程]</span><br><span class="line">    F -- 是 --&gt; H&#123;Syms Read 是否为 Yes&#125;</span><br><span class="line">    H -- 否 --&gt; I[执行 sharedlibrary 库名]</span><br><span class="line">    I --&gt; J&#123;pending 是否解析&#125;</span><br><span class="line">    J -- 是 --&gt; K[配置延迟自动加载]</span><br><span class="line">    J -- 否 --&gt; L[检查库路径和源码映射]</span><br><span class="line">    H -- 是 --&gt; M&#123;info break 是否有实际地址&#125;</span><br><span class="line">    M -- 是 --&gt; N[断点有效]</span><br><span class="line">    M -- 否 --&gt; O[检查 sourceFileMap]</span><br></pre></td></tr></table></figure><hr><h2 id="15-最终结论"><a href="#15-最终结论" class="headerlink" title="15. 最终结论"></a>15. 最终结论</h2><p>完整证据链如下：</p><ol><li>动态库架构正确；</li><li>动态库包含 DWARF，且未被剥离；</li><li>交叉 GDB 离线能够解析目标函数和源码行；</li><li>构建输出、GDB sysroot 和目标设备动态库一致；</li><li>运行时 GDB 已发现目标动态库；</li><li><code>info sharedlibrary</code> 显示 <code>Syms Read No</code>；</li><li>执行 <code>sharedlibrary liblely-io2</code> 后变为 <code>Syms Read Yes</code>；</li><li>原 pending 断点立即绑定到实际运行地址。</li></ol><p>因此，直接原因是：</p><blockquote><p>在当前远程调试启动序列中，GDB 没有自动读取目标动态库的完整调试符号。</p></blockquote><p>最终解决方案：</p><ul><li>保证构建输出、sysroot 和目标设备动态库完全一致；</li><li>部署后重启目标应用和 gdbserver；</li><li>在动态库已经加载后显式执行 <code>sharedlibrary</code>；</li><li>通过 GDB 命令文件和 <code>postRemoteConnectCommands</code> 将动作自动化；</li><li>使用 <code>info sharedlibrary</code> 和 <code>info break</code> 作为最终验收依据。</li></ul><hr><h2 id="16-快速检查清单"><a href="#16-快速检查清单" class="headerlink" title="16. 快速检查清单"></a>16. 快速检查清单</h2><ul><li><input disabled="" type="checkbox"> 动态库架构为 AArch64；</li><li><input disabled="" type="checkbox"> 动态库包含 <code>.debug_info</code> 和 <code>.debug_line</code>；</li><li><input disabled="" type="checkbox"> 动态库未被 <code>strip</code>；</li><li><input disabled="" type="checkbox"> 离线 <code>info line</code> 能解析目标源码行；</li><li><input disabled="" type="checkbox"> 构建输出、sysroot 和目标设备 Build ID 一致；</li><li><input disabled="" type="checkbox"> 部署后已重启目标进程；</li><li><input disabled="" type="checkbox"> <code>show architecture</code> 显示 AArch64；</li><li><input disabled="" type="checkbox"> <code>show sysroot</code> 指向正确目录；</li><li><input disabled="" type="checkbox"> <code>info sharedlibrary</code> 能找到目标动态库；</li><li><input disabled="" type="checkbox"> 目标动态库显示 <code>Syms Read Yes</code>；</li><li><input disabled="" type="checkbox"> <code>info break</code> 中断点具有实际地址；</li><li><input disabled="" type="checkbox"> 源码路径映射与 DWARF 记录一致。</li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;meta name=&quot;referrer&quot; content=&quot;no-referrer&quot; /&gt;


&lt;h1 id=&quot;VS-Code-Remote-GDB-调试动态库源码断点灰色：问题分析与解决方案&quot;&gt;&lt;a href=&quot;#VS-Code-Remote-GDB-调试动态库源码断点灰色：</summary>
      
    
    
    
    <category term="杂谈" scheme="https://wdfk-prog.space/categories/%E6%9D%82%E8%B0%88/"/>
    
    
    <category term="杂谈" scheme="https://wdfk-prog.space/tags/%E6%9D%82%E8%B0%88/"/>
    
  </entry>
  
  <entry>
    <title>资源受限系统中的分层低功耗与低延迟唤醒架构设计</title>
    <link href="https://wdfk-prog.space/posts/25a410b8/"/>
    <id>https://wdfk-prog.space/posts/25a410b8/</id>
    <published>2026-07-24T03:35:32.000Z</published>
    <updated>2026-07-24T03:36:00.426Z</updated>
    
    <content type="html"><![CDATA[<meta name="referrer" content="no-referrer" /><h1 id="嵌入式面试真题第-17-题：资源受限系统中的分层低功耗与低延迟唤醒架构设计"><a href="#嵌入式面试真题第-17-题：资源受限系统中的分层低功耗与低延迟唤醒架构设计" class="headerlink" title="嵌入式面试真题第 17 题：资源受限系统中的分层低功耗与低延迟唤醒架构设计"></a>嵌入式面试真题第 17 题：资源受限系统中的分层低功耗与低延迟唤醒架构设计</h1><p><img src="https://i-blog.csdnimg.cn/direct/13c05a022a0b49fb93e1501aeb8f309e.png" alt="在这里插入图片描述"></p><h2 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h2><p>在电池供电、能量采集供电或热设计受限的嵌入式设备中，系统通常同时存在多级 CPU&#x2F;SoC 低功耗状态、多个可独立关断的电源域，以及时钟、存储、传感器、显示、音频、无线通信和外部协处理器等设备。不同休眠状态的静态功耗、进入时间、退出时间、上下文保留能力、可用唤醒源和恢复复杂度差异很大：浅睡眠退出快但待机功耗较高，深睡眠功耗低但可能关闭 PLL、主 SRAM、总线、外设或整个数字域，唤醒后甚至需要从复位入口重新启动。</p><p>业务侧又可能同时提出多种约束：按键、触摸、旋钮、门磁、佩戴检测等人机事件要求快速反馈；采样、控制、告警和协议时隙要求确定性响应；网络、音频、显示和传感器只需在需要时恢复；后台日志、统计、同步和自检可以延后。系统还可能频繁出现短空闲、突发交互、定时任务、通信重传、传感器中断和误触发。如果每次空闲都进入最深休眠，恢复成本、唤醒抖动和设备状态错乱可能抵消节能收益；如果长期停留在浅睡眠，又无法满足续航目标。</p><p>你会如何设计一套通用的低功耗与唤醒管理框架，在不绑定某一款 MCU、某一种外设或某一个业务场景的前提下，综合处理以下问题：</p><ol><li>如何根据预计空闲时间、唤醒延迟预算、能量收益、设备依赖和唤醒源能力选择休眠深度？</li><li>如何把唤醒拆分为快速响应路径和完整恢复路径，使关键事件先被可靠捕获并得到及时反馈，而不是等待全部设备串行初始化？</li><li>如何保存和校验上下文，协调 PLL、总线、存储、I2C&#x2F;SPI 设备、显示、音频、无线和后台任务的恢复顺序？</li><li>如何处理多模块并发请求、禁止休眠区间、超时、误唤醒、重复事件、进入休眠与中断同时发生等竞态？</li><li>如何量化能耗与响应延迟，设计可测试、可观测、可回退的策略，并借鉴 Linux、Zephyr、FreeRTOS、RT-Thread、Apache NuttX、ESP-IDF 等开源实现？</li></ol><h2 id="回答"><a href="#回答" class="headerlink" title="回答"></a>回答</h2><p>结论：低功耗设计的核心不是“系统一空闲就进入最深睡眠”，而是建立一套<strong>受延迟约束、受驻留时间约束、按电源域分层、支持分阶段恢复、由业务意图驱动并可在线度量的电源管理框架</strong>。</p><p>系统应把休眠与唤醒拆成四个相互解耦的层次：</p><ol><li><strong>策略层</strong>根据预计空闲时间、当前延迟约束、设备使用计数、唤醒源、能量模型和历史行为选择最深但仍满足约束的状态。</li><li><strong>事务层</strong>把一次休眠视为可提交、可拒绝、可中止、可回滚的系统事务，统一处理模块投票、设备依赖、上下文保存和竞态。</li><li><strong>快速唤醒层</strong>只完成事件锁存、时钟最小恢复、关键状态机推进和首个可感知反馈，目标是尽快达到“事件已确认”或“最小业务可用”。</li><li><strong>后台恢复层</strong>按依赖图异步恢复非关键设备、重建连接、执行校准、补偿时间、刷新界面和恢复日志，避免阻塞关键路径。</li></ol><p>因此，用户或控制环真正关心的不是“所有设备是否已经恢复”，而是几个不同的时间点：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">T_irq        = 唤醒源触发到 CPU 开始执行唤醒入口的时间</span><br><span class="line">T_capture    = 唤醒原因被可靠锁存并完成最小去抖/合法性判断的时间</span><br><span class="line">T_ack        = 用户、上位机或控制状态机得到第一响应的时间</span><br><span class="line">T_service    = 本次业务所需最小设备集合恢复完成的时间</span><br><span class="line">T_full       = 全部后台设备与非关键服务恢复完成的时间</span><br></pre></td></tr></table></figure><p>系统设计应优先约束 <code>T_capture</code>、<code>T_ack</code> 和 <code>T_service</code>，而不是强行让 <code>T_full</code> 等于 <code>T_ack</code>。一个按键可以在主 PLL、Codec、外部 Flash 和无线协议栈完全恢复前先被记录并点亮低功耗 LED；一个工业告警可以先锁存故障、拉高告警输出并更新时间戳，再恢复网络上传；一个周期采样任务可以只恢复低速时钟、ADC 和 DMA，而不必唤醒显示与音频域。</p><h2 id="总体架构"><a href="#总体架构" class="headerlink" title="总体架构"></a>总体架构</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[业务任务 / 驱动 / 协议栈] --&gt; B[PM Intent API\n延迟约束 / 活动提示 / 设备引用]</span><br><span class="line">    B --&gt; C[Constraint Aggregator\n聚合最严格约束]</span><br><span class="line">    C --&gt; D[Power Policy Governor\n预计空闲时间 + 能量模型 + 历史行为]</span><br><span class="line">    D --&gt; E&#123;选择电源状态&#125;</span><br><span class="line">    E --&gt;|浅睡 / Idle| F[CPU Idle / Tickless]</span><br><span class="line">    E --&gt;|系统睡眠| G[Sleep Transaction Manager]</span><br><span class="line">    G --&gt; H[Wake Source Manager\n配置与验证唤醒源]</span><br><span class="line">    G --&gt; I[Context Manager\n保存 / CRC / 版本]</span><br><span class="line">    G --&gt; J[Device Dependency Graph\nprepare / suspend]</span><br><span class="line">    J --&gt; K[SoC PM Backend\n时钟 / 电源域 / WFI/WFE]</span><br><span class="line">    K --&gt; L[Always-on / RTC / PMU 域]</span><br><span class="line">    L --&gt; M[Wakeup ISR / Resume Stub]</span><br><span class="line">    M --&gt; N[Fast Resume Path\n捕获事件 + 最小反馈]</span><br><span class="line">    N --&gt; O[Critical Service Set\n关键设备恢复]</span><br><span class="line">    O --&gt; P[Deferred Resume Worker\n后台并行恢复]</span><br><span class="line">    P --&gt; Q[Telemetry &amp; Policy Tuning\n延迟 / 能量 / 失败统计]</span><br><span class="line">    Q --&gt; D</span><br></pre></td></tr></table></figure><p>这个架构中，应用不直接决定“立刻进 Deep Sleep”，驱动也不各自无序地关闭时钟。上层只描述意图和约束，例如“未来 5ms 内不能接受超过 100us 的退出延迟”“SPI DMA 传输期间禁止关闭该总线”“当前交互可能持续 3s”“网络下一次监听窗口在 120ms 后”。策略层把这些约束聚合后选择状态；事务层协调设备；平台后端完成具体寄存器操作；唤醒后快速路径和后台路径分开执行。</p><h2 id="机制与开源实现的对应关系"><a href="#机制与开源实现的对应关系" class="headerlink" title="机制与开源实现的对应关系"></a>机制与开源实现的对应关系</h2><table><thead><tr><th>本文机制</th><th>Linux 或开源机制</th><th>能否直接使用</th><th>主要参考价值</th></tr></thead><tbody><tr><td>多级休眠状态与驻留时间</td><td><a href="https://docs.zephyrproject.org/latest/services/pm/system.html">Zephyr System Power Management</a>、Linux <code>cpuidle</code></td><td>对应系统可直接使用；裸机或其他 RTOS 主要参考</td><td>每个状态定义最小驻留时间、退出延迟，策略只选择满足下一事件时间窗口的状态。</td></tr><tr><td>唤醒延迟约束</td><td><a href="https://docs.zephyrproject.org/latest/samples/subsys/pm/latency/README.html">Zephyr PM policy latency</a>、<a href="https://docs.kernel.org/power/pm_qos_interface.html">Linux PM QoS</a></td><td>Linux&#x2F;Zephyr 可直接使用接口</td><td>多个模块提交最大可接受退出延迟，系统取最严格值，禁止进入过深状态。</td></tr><tr><td>设备运行时电源管理</td><td><a href="https://docs.zephyrproject.org/latest/services/pm/device_runtime.html">Zephyr Device Runtime PM</a>、<a href="https://docs.kernel.org/power/runtime_pm.html">Linux Runtime PM</a></td><td>对应系统可直接使用</td><td>使用计数、自动挂起、运行时恢复，使设备功耗与系统级休眠解耦。</td></tr><tr><td>电源模式请求&#x2F;投票</td><td><a href="https://www.rt-thread.io/document/site/programming-manual/pm/pm/">RT-Thread PM</a></td><td>RT-Thread 可直接使用；其他系统参考</td><td>业务或驱动成对请求&#x2F;释放模式，未释放的请求限制系统进入更深状态。</td></tr><tr><td>活动驱动与 PM 域</td><td><a href="https://nuttx.apache.org/docs/latest/components/drivers/special/power/pm/index.html">Apache NuttX Power Management</a></td><td>NuttX 可直接使用；其他系统参考</td><td>驱动上报活动，按 PM domain 管理状态，并通过 prepare&#x2F;notify 回调协调驱动。</td></tr><tr><td>无周期 Tick 的空闲睡眠</td><td><a href="https://www.freertos.org/Documentation/02-Kernel/02-Kernel-features/07-Lower-power-support">FreeRTOS Tickless Idle</a></td><td>FreeRTOS 可直接配置</td><td>停止周期 Tick，按下一超时点设置低功耗定时器，唤醒后补偿内核时间。</td></tr><tr><td>浅睡、深睡与保留域</td><td><a href="https://docs.espressif.com/projects/esp-idf/en/latest/esp32/api-reference/system/sleep_modes.html">ESP-IDF Sleep Modes</a></td><td>ESP 芯片可直接使用；其他平台参考</td><td>区分状态保留的 Light Sleep 与大部分数字域掉电的 Deep Sleep，配置多个唤醒源和 RTC 保留资源。</td></tr><tr><td>唤醒源与设备依赖</td><td>Linux wakeup source、driver model、runtime PM</td><td>Linux 可直接使用；MCU 系统参考</td><td>明确设备是否具备唤醒能力、是否允许唤醒、父子设备恢复顺序及引用关系。</td></tr><tr><td>快速路径与延后恢复</td><td>Linux resume phases、workqueue；Zephyr work queue</td><td>需按产品设计封装</td><td>ISR&#x2F;恢复桩只做最小工作，非关键恢复通过工作队列异步执行。</td></tr></tbody></table><p>这些开源机制并不是可以机械拼接的“标准答案”。Linux 面向复杂 SoC 和完整驱动模型，Zephyr、RT-Thread、NuttX、FreeRTOS 更贴近 MCU&#x2F;RTOS，但它们共同体现了几个稳定原则：<strong>状态必须有可比较的延迟和能耗属性；模块必须能表达约束；设备必须按依赖和引用管理；系统必须知道下一次事件；睡眠时间必须补偿；唤醒必须可追踪和可失败。</strong></p><h2 id="先定义目标：系统不是只有“睡”和“醒”"><a href="#先定义目标：系统不是只有“睡”和“醒”" class="headerlink" title="先定义目标：系统不是只有“睡”和“醒”"></a>先定义目标：系统不是只有“睡”和“醒”</h2><p>低功耗架构应先把“可用”拆成不同等级，否则所有模块都会要求完整恢复，最终又退化为串行初始化。</p><table><thead><tr><th>可用等级</th><th>定义</th><th>典型动作</th><th>建议指标</th></tr></thead><tbody><tr><td>L0：事件已锁存</td><td>唤醒原因、边沿、时间戳不会丢失</td><td>AON GPIO&#x2F;RTC 捕获，写 retention record</td><td><code>T_capture</code></td></tr><tr><td>L1：最小反馈可用</td><td>可以给用户或控制系统第一确认</td><td>低功耗 LED、GPIO、蜂鸣器占位、状态机 ACK</td><td><code>T_ack</code></td></tr><tr><td>L2：关键业务可用</td><td>本次事件真正需要的设备已恢复</td><td>按键扫描、ADC、关键总线、显示首帧、协议应答</td><td><code>T_service</code></td></tr><tr><td>L3：完整业务可用</td><td>所有前台功能恢复</td><td>完整 UI、音频链路、无线连接、存储访问</td><td>前台恢复时间</td></tr><tr><td>L4：后台恢复完成</td><td>日志、统计、自检、同步和缓存重建完成</td><td>日志刷写、传感器重校准、云同步</td><td><code>T_full</code></td></tr></tbody></table><p>例如，按键唤醒并不天然要求立刻恢复所有 I2C 传感器；RTC 定时采样不需要点亮显示；BLE 连接事件不需要启动音频 Codec；充电插入事件可以先由 PMIC&#x2F;AON 域确认，再决定是否启动主系统。只有先定义“事件所需的最小服务集合”，才能把恢复路径真正并行化和分阶段化。</p><h2 id="电源状态模型"><a href="#电源状态模型" class="headerlink" title="电源状态模型"></a>电源状态模型</h2><p>不要只用 <code>ACTIVE</code>、<code>SLEEP</code> 两个枚举。通用框架至少应为每个系统状态记录以下属性：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">state_id</span><br><span class="line">entry_latency_us</span><br><span class="line">exit_latency_us_worst</span><br><span class="line">min_residency_us</span><br><span class="line">steady_power_uw</span><br><span class="line">retained_memory_mask</span><br><span class="line">available_wakeup_mask</span><br><span class="line">powered_domain_mask</span><br><span class="line">clock_domain_mask</span><br><span class="line">context_loss_level</span><br><span class="line">requires_reset_resume</span><br></pre></td></tr></table></figure><p>一个抽象状态表可以如下设计：</p><table><thead><tr><th>状态</th><th>典型硬件行为</th><th>上下文</th><th align="right">退出延迟</th><th>适用场景</th></tr></thead><tbody><tr><td>RUN_HIGH</td><td>主 PLL、高频总线、全部关键外设运行</td><td>全保留</td><td align="right">0</td><td>高负载、实时处理</td></tr><tr><td>RUN_LOW</td><td>降频或切换低速时钟，设备仍可运行</td><td>全保留</td><td align="right">数微秒到数十微秒</td><td>低负载连续任务</td></tr><tr><td>IDLE</td><td>CPU 停止取指，系统 Tick 可继续</td><td>全保留</td><td align="right">极低</td><td>极短空闲</td></tr><tr><td>LIGHT_SLEEP</td><td>CPU&#x2F;部分时钟关闭，主 SRAM 保留</td><td>大部分保留</td><td align="right">低到中等</td><td>短期空闲、交互活跃期</td></tr><tr><td>DEEP_SLEEP</td><td>主 PLL、总线、部分 SRAM 和外设断电</td><td>仅保留域</td><td align="right">中到较高</td><td>较长空闲、少量唤醒源</td></tr><tr><td>STANDBY</td><td>数字域大部分掉电，唤醒近似复位</td><td>少量备份寄存器&#x2F;RTC RAM</td><td align="right">高</td><td>长时间待机</td></tr><tr><td>SHUTDOWN</td><td>仅 PMIC&#x2F;AON 保持，主控重新上电</td><td>基本不保留</td><td align="right">最高</td><td>运输、极低电量、长期存放</td></tr></tbody></table><p>状态名称只是抽象，具体芯片的 Stop、Standby、Backup、System OFF、Hibernate 等模式必须映射到统一属性。策略层不应写死“模式 3 一定比模式 2 好”，而应通过属性判断：某状态即使静态功耗低，如果退出要 20ms、上下文恢复再要 30ms，而预测空闲只有 40ms，就没有实际收益。</p><h2 id="休眠状态选择原则"><a href="#休眠状态选择原则" class="headerlink" title="休眠状态选择原则"></a>休眠状态选择原则</h2><h3 id="基本约束"><a href="#基本约束" class="headerlink" title="基本约束"></a>基本约束</h3><p>候选状态 <code>s</code> 至少同时满足以下条件：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">L_exit_worst(s) + L_critical_restore(s, event_class) &lt;= L_budget_effective</span><br><span class="line">T_idle_predicted &gt;= T_entry(s) + T_exit(s) + T_min_residency(s) + T_guard</span><br><span class="line">WakeSourceRequired ⊆ WakeSourceAvailable(s)</span><br><span class="line">RequiredRetention ⊆ RetentionCapability(s)</span><br><span class="line">RequiredDomains ∩ PoweredOffDomains(s) = ∅</span><br><span class="line">NoUnreleasedLockBlocks(s)</span><br></pre></td></tr></table></figure><p>其中：</p><ul><li><code>L_budget_effective</code> 是所有活动模块提交的延迟约束中的最小值。</li><li><code>event_class</code> 表示当前最可能或必须支持的唤醒类别，不同事件的关键恢复集合不同。</li><li><code>T_guard</code> 用于覆盖时钟漂移、调度抖动、IRQ 屏蔽窗口和估计误差。</li><li><code>NoUnreleasedLockBlocks(s)</code> 表示 DMA、Flash 擦写、总线事务、关键区或协议时隙没有禁止该状态。</li></ul><h3 id="由浅到深还是由深到浅"><a href="#由浅到深还是由深到浅" class="headerlink" title="由浅到深还是由深到浅"></a>由浅到深还是由深到浅</h3><p>实现上建议从“最深状态”向“最浅状态”筛选，第一个满足所有约束的状态即为候选。这样策略的含义清晰：系统始终尝试在约束内获得最大节能收益，而不是靠大量业务分支手工选择模式。</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">for state in states_sorted_by_power_ascending:</span><br><span class="line">    if latency_ok(state) and residency_ok(state) and wake_source_ok(state) and</span><br><span class="line">       retention_ok(state) and domain_ok(state) and lock_ok(state):</span><br><span class="line">        return state</span><br><span class="line">return RUN_OR_IDLE</span><br></pre></td></tr></table></figure><p>但最终选择不能只看静态功耗，还必须看一次进入和退出的固定成本。</p><h2 id="能量盈亏平衡点"><a href="#能量盈亏平衡点" class="headerlink" title="能量盈亏平衡点"></a>能量盈亏平衡点</h2><p>深睡并非时间越短越省电。一次状态切换包含保存上下文、关时钟、配置唤醒源、进入硬件模式、退出、锁定 PLL、恢复内存和设备等成本。</p><p>令：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">P_shallow    = 浅状态稳态功耗</span><br><span class="line">P_deep       = 深状态稳态功耗</span><br><span class="line">E_entry      = 进入深状态的额外能量</span><br><span class="line">E_exit       = 退出深状态的额外能量</span><br><span class="line">E_restore    = 恢复上下文和设备的额外能量</span><br><span class="line">T_idle       = 实际空闲时间</span><br></pre></td></tr></table></figure><p>深睡相对浅睡的净收益近似为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">E_saved = (P_shallow - P_deep) * T_idle - (E_entry + E_exit + E_restore)</span><br></pre></td></tr></table></figure><p>只有 <code>E_saved &gt; 0</code> 时，深睡才有能量收益。对应的盈亏平衡时间为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">T_break_even = (E_entry + E_exit + E_restore) / (P_shallow - P_deep)</span><br></pre></td></tr></table></figure><p>策略中应使用：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">T_idle_predicted &gt;= T_break_even + T_guard</span><br></pre></td></tr></table></figure><p>而不是简单地配置“空闲超过 10ms 就 Deep Sleep”。如果外部晶振和 PLL 启动需要较大冲击电流，或者无线、Codec、传感器每次恢复都要校准，<code>E_restore</code> 可能相当可观。频繁深睡甚至可能比保持低频运行更耗电。</p><h3 id="加入提前唤醒概率"><a href="#加入提前唤醒概率" class="headerlink" title="加入提前唤醒概率"></a>加入提前唤醒概率</h3><p>实际空闲时间具有不确定性。可把候选状态的期望代价表示为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">Cost(s) = E_expected(s)</span><br><span class="line">        + lambda * Probability(L_service(s) &gt; L_budget)</span><br><span class="line">        + mu * Probability(resume_failure(s))</span><br><span class="line">        + nu * WakeupWearOrCalibrationCost(s)</span><br></pre></td></tr></table></figure><p>其中 <code>lambda</code>、<code>mu</code>、<code>nu</code> 表示产品对延迟违约、恢复失败和频繁校准的惩罚权重。资源受限 MCU 不一定要实现复杂概率模型，但至少应统计“预测空闲时间与实际空闲时间的误差”，动态调整阈值，避免长期使用拍脑袋常量。</p><h2 id="延迟预算必须分解"><a href="#延迟预算必须分解" class="headerlink" title="延迟预算必须分解"></a>延迟预算必须分解</h2><p>只要求“唤醒小于 20ms”不够，因为无法定位优化对象。应把总预算分配到各阶段：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">L_total = L_hw_exit</span><br><span class="line">        + L_clock_min</span><br><span class="line">        + L_vector_to_stub</span><br><span class="line">        + L_reason_capture</span><br><span class="line">        + L_scheduler_ready</span><br><span class="line">        + L_critical_devices</span><br><span class="line">        + L_first_response</span><br></pre></td></tr></table></figure><p>例如某类交互的预算可以是：</p><table><thead><tr><th>阶段</th><th align="right">预算</th><th>说明</th></tr></thead><tbody><tr><td>硬件退出与低速时钟可用</td><td align="right">1.5ms</td><td>从深睡到可执行恢复桩</td></tr><tr><td>唤醒原因锁存与去抖首判</td><td align="right">0.5ms</td><td>不做完整业务处理</td></tr><tr><td>内核与关键 RAM 恢复</td><td align="right">1ms</td><td>调度器、栈、关键队列可用</td></tr><tr><td>最小反馈设备恢复</td><td align="right">2ms</td><td>AON LED&#x2F;GPIO 或轻量显示路径</td></tr><tr><td>首次状态反馈</td><td align="right">1ms</td><td>用户或上位机可感知</td></tr><tr><td>关键业务设备恢复</td><td align="right">8ms</td><td>必需总线、传感器或协议</td></tr><tr><td>安全余量</td><td align="right">6ms</td><td>覆盖 P99&#x2F;P99.9 抖动</td></tr><tr><td>合计</td><td align="right">20ms</td><td>不包含后台完整恢复</td></tr></tbody></table><p>测量时要关注最坏值和高分位，而不是只看平均值。一次 Flash cache miss、PLL 偶发锁定变慢、I2C 设备上电自检、无线重连或高优先级中断抢占，都可能让平均 5ms 的路径出现 30ms 长尾。</p><h2 id="快速唤醒路径与完整恢复路径"><a href="#快速唤醒路径与完整恢复路径" class="headerlink" title="快速唤醒路径与完整恢复路径"></a>快速唤醒路径与完整恢复路径</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant W as AON/RTC 唤醒源</span><br><span class="line">    participant S as Resume Stub</span><br><span class="line">    participant F as Fast Path</span><br><span class="line">    participant C as Critical Resume</span><br><span class="line">    participant B as Background Resume</span><br><span class="line">    participant A as Application</span><br><span class="line"></span><br><span class="line">    W-&gt;&gt;S: 唤醒信号 + 硬件锁存</span><br><span class="line">    S-&gt;&gt;S: 建立最小栈/低速时钟</span><br><span class="line">    S-&gt;&gt;F: 读取并保存 wake reason</span><br><span class="line">    F-&gt;&gt;F: 首次去抖/合法性检查</span><br><span class="line">    F-&gt;&gt;A: 发布 WAKE_EVENT_CAPTURED</span><br><span class="line">    F-&gt;&gt;A: 最小反馈或状态机 ACK</span><br><span class="line">    F-&gt;&gt;C: 请求关键设备集合</span><br><span class="line">    par 关键设备按依赖恢复</span><br><span class="line">        C-&gt;&gt;C: PLL/总线/必要 RAM</span><br><span class="line">        C-&gt;&gt;C: 关键 GPIO/I2C/SPI/ADC</span><br><span class="line">    and 后台路径准备</span><br><span class="line">        F-&gt;&gt;B: 投递 deferred resume</span><br><span class="line">    end</span><br><span class="line">    C-&gt;&gt;A: 发布 SERVICE_READY</span><br><span class="line">    B-&gt;&gt;B: 恢复显示/音频/无线/日志</span><br><span class="line">    B-&gt;&gt;A: 发布 FULLY_RESUMED</span><br></pre></td></tr></table></figure><h3 id="快速路径应该做什么"><a href="#快速路径应该做什么" class="headerlink" title="快速路径应该做什么"></a>快速路径应该做什么</h3><p>快速路径只做具有以下特征的工作：</p><ol><li>必须在唤醒标志被其他初始化覆盖前完成。</li><li>执行时间短、上界明确、无需动态分配。</li><li>不依赖尚未恢复的复杂驱动。</li><li>失败后能留下可诊断信息。</li><li>可以安全地重复执行或检测重复执行。</li></ol><p>典型动作包括：读取 PMU&#x2F;RTC&#x2F;AON 唤醒寄存器，锁存 GPIO 电平与边沿，保存低功耗计数器时间戳，建立最小栈，恢复低速系统时钟，确认 retention record，清除必要的硬件标志，向无锁队列或静态事件槽写入事件，启动关键恢复状态机。</p><h3 id="快速路径不应该做什么"><a href="#快速路径不应该做什么" class="headerlink" title="快速路径不应该做什么"></a>快速路径不应该做什么</h3><p>不要在唤醒 ISR 或恢复桩中完成以下工作：</p><ul><li>扫描全部 I2C 地址并同步等待超时。</li><li>初始化文件系统、遍历目录或校验整个资源分区。</li><li>完整启动无线协议栈并等待入网。</li><li>执行 Codec 全寄存器配置、长时间校准或播放完整提示音。</li><li>刷新整个显示帧、加载字体和图片。</li><li>动态申请大块内存、打印大量日志或等待互斥锁。</li><li>直接调用可能睡眠、可能重入或依赖调度器的普通驱动 API。</li></ul><p>快速路径的目标是“可靠捕获并启动恢复”，不是“把整个系统一次性恢复完”。</p><h2 id="唤醒原因管理"><a href="#唤醒原因管理" class="headerlink" title="唤醒原因管理"></a>唤醒原因管理</h2><h3 id="唤醒事件记录"><a href="#唤醒事件记录" class="headerlink" title="唤醒事件记录"></a>唤醒事件记录</h3><p>建议在 retention RAM、备份寄存器或 AON SRAM 中维护固定格式的唤醒记录：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief Retained wakeup record shared by the low-power entry and resume paths.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="type">uint32_t</span> magic;</span><br><span class="line">    <span class="type">uint16_t</span> version;</span><br><span class="line">    <span class="type">uint16_t</span> size;</span><br><span class="line">    <span class="type">uint32_t</span> boot_sequence;</span><br><span class="line">    <span class="type">uint32_t</span> sleep_sequence;</span><br><span class="line">    <span class="type">uint64_t</span> sleep_enter_time;</span><br><span class="line">    <span class="type">uint64_t</span> wake_capture_time;</span><br><span class="line">    <span class="type">uint32_t</span> raw_wakeup_flags;</span><br><span class="line">    <span class="type">uint32_t</span> normalized_reason_mask;</span><br><span class="line">    <span class="type">uint32_t</span> gpio_snapshot;</span><br><span class="line">    <span class="type">uint32_t</span> pending_event_mask;</span><br><span class="line">    <span class="type">uint32_t</span> context_generation;</span><br><span class="line">    <span class="type">uint32_t</span> crc32;</span><br><span class="line">&#125; <span class="type">pm_retention_record_t</span>;</span><br></pre></td></tr></table></figure><p>记录中应区分：</p><ul><li><strong>原始硬件标志</strong>：便于分析 PMU、RTC、GPIO、通信外设的真实状态。</li><li><strong>归一化原因</strong>：例如 <code>BUTTON</code>、<code>TIMER</code>、<code>SENSOR</code>、<code>CHARGER</code>、<code>NETWORK</code>、<code>WATCHDOG</code>、<code>BROWNOUT</code>。</li><li><strong>事件快照</strong>：GPIO 电平、计数器、边沿状态、外部 PMIC 原因寄存器。</li><li><strong>序列号</strong>：区分本次休眠、重复中断、复位恢复和旧记录。</li><li><strong>版本与 CRC</strong>：防止固件升级或掉电造成结构不兼容和脏数据。</li></ul><h3 id="先读后清"><a href="#先读后清" class="headerlink" title="先读后清"></a>先读后清</h3><p>常见故障是恢复代码过早清除唤醒标志，随后驱动初始化又改变 GPIO 或外设寄存器，导致真实原因丢失。正确顺序一般是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">读取全部相关唤醒寄存器</span><br><span class="line">读取必要 GPIO/RTC/PMIC 快照</span><br><span class="line">写入 retention record</span><br><span class="line">执行内存屏障</span><br><span class="line">再次确认需要保持的锁存位</span><br><span class="line">按硬件规定清除中断/唤醒标志</span><br><span class="line">发布软件事件</span><br></pre></td></tr></table></figure><p>不同芯片对清除顺序、读清零、写 1 清零和跨域同步有不同要求，平台后端必须封装，不能让业务层直接操作寄存器。</p><h3 id="去抖与事件确认"><a href="#去抖与事件确认" class="headerlink" title="去抖与事件确认"></a>去抖与事件确认</h3><p>按键、门磁、霍尔和机械触点可能在唤醒时产生多次边沿。快速路径只需完成“首个事件锁存”和最小合法性判断；完整去抖可以在稳定时钟恢复后进行。</p><p>一个通用策略是：</p><ol><li>AON 域锁存首边沿与时间戳。</li><li>快速路径读取当前电平并创建候选事件。</li><li>启动低功耗定时器或短延时工作项。</li><li>到期后再次采样，确认事件类型。</li><li>使用 <code>sleep_sequence + source_id + edge_timestamp</code> 去重。</li><li>对持续低电平唤醒源启用屏蔽或电平反转，防止刚入睡又立即被唤醒。</li></ol><h2 id="上下文保留不是简单地-memcpy"><a href="#上下文保留不是简单地-memcpy" class="headerlink" title="上下文保留不是简单地 memcpy"></a>上下文保留不是简单地 memcpy</h2><h3 id="上下文分级"><a href="#上下文分级" class="headerlink" title="上下文分级"></a>上下文分级</h3><p>建议按恢复价值和一致性要求把上下文分为四类：</p><table><thead><tr><th>类别</th><th>示例</th><th>保存位置</th><th>恢复方式</th></tr></thead><tbody><tr><td>必须保留</td><td>唤醒原因、事务序列号、关键状态机阶段</td><td>备份寄存器&#x2F;AON RAM</td><td>恢复桩直接读取</td></tr><tr><td>快速恢复</td><td>当前页面、音量、连接状态摘要、传感器基线</td><td>retention RAM</td><td>版本和 CRC 校验后加载</td></tr><tr><td>可重建</td><td>缓存、索引、统计窗口、非关键队列</td><td>普通 SRAM 或外部存储</td><td>后台重建</td></tr><tr><td>不应保留</td><td>指向掉电内存的裸指针、锁拥有者、DMA 活动描述符</td><td>不保存</td><td>重新初始化并显式失效</td></tr></tbody></table><p>不能把整个 SRAM 原样保存后直接恢复，因为内存中可能包含外设寄存器影子、过期指针、锁状态、正在进行的 DMA、旧定时器和与固件版本绑定的数据结构。恢复时应使用有版本的逻辑状态，而不是盲目恢复物理运行现场。</p><h3 id="提交式保存"><a href="#提交式保存" class="headerlink" title="提交式保存"></a>提交式保存</h3><p>上下文保存应采用双槽或提交标志，避免保存过程中掉电留下“看似有效”的半份数据：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">slot A: header + payload + CRC + generation</span><br><span class="line">slot B: header + payload + CRC + generation</span><br><span class="line">commit_marker: active_slot + generation + inverse</span><br></pre></td></tr></table></figure><p>保存流程：</p><ol><li>选择非活动槽。</li><li>写入头部和 payload。</li><li>计算并写入 CRC。</li><li>执行缓存清理和内存屏障。</li><li>最后更新提交标志。</li></ol><p>恢复时选择 CRC 正确且 generation 最新的槽。对极小 MCU，可把必须保留内容压缩到固定结构中，但仍建议包含 magic、version、length、generation 和 CRC。</p><h2 id="设备依赖图与分阶段恢复"><a href="#设备依赖图与分阶段恢复" class="headerlink" title="设备依赖图与分阶段恢复"></a>设备依赖图与分阶段恢复</h2><p>设备不能按“驱动注册顺序”随意恢复。典型依赖关系如下：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    P[电源轨 / PMIC] --&gt; X[晶振 / PLL]</span><br><span class="line">    X --&gt; C[时钟树]</span><br><span class="line">    C --&gt; B[AHB/APB/总线控制器]</span><br><span class="line">    B --&gt; G[GPIO / Pinmux]</span><br><span class="line">    B --&gt; D[DMA]</span><br><span class="line">    B --&gt; I[I2C/SPI/UART 控制器]</span><br><span class="line">    G --&gt; I</span><br><span class="line">    I --&gt; S[传感器 / Codec / PMIC 子设备]</span><br><span class="line">    D --&gt; S</span><br><span class="line">    S --&gt; U[音频 / 显示 / 采集服务]</span><br><span class="line">    C --&gt; T[系统定时器 / Tick]</span><br><span class="line">    T --&gt; K[RTOS 调度与超时]</span><br><span class="line">    K --&gt; U</span><br></pre></td></tr></table></figure><p>恢复顺序至少要满足：父电源域先于子设备、时钟先于依赖时钟的外设、Pinmux 先于总线事务、总线控制器先于总线设备、内存和 DMA 描述符先于 DMA、系统时间恢复先于依赖绝对超时的协议。</p><h3 id="关键设备集合"><a href="#关键设备集合" class="headerlink" title="关键设备集合"></a>关键设备集合</h3><p>对每类唤醒事件预先定义最小关键设备集合：</p><table><thead><tr><th>唤醒类别</th><th>关键集合</th><th>可延后集合</th></tr></thead><tbody><tr><td>按键&#x2F;触摸</td><td>AON GPIO、低速时钟、输入状态机、最小反馈设备</td><td>全量显示、音频、无线、日志</td></tr><tr><td>RTC 采样</td><td>RTC、传感器电源、I2C&#x2F;SPI、ADC&#x2F;DMA、存储队列</td><td>UI、音频、网络</td></tr><tr><td>网络事件</td><td>高频时钟、网络控制器、协议定时器、必要密钥状态</td><td>显示、非关键传感器</td></tr><tr><td>告警输入</td><td>GPIO&#x2F;比较器、时间戳、告警输出、关键通信</td><td>统计、日志整理、UI 动画</td></tr><tr><td>充电&#x2F;电源事件</td><td>PMIC、ADC、充电状态机、热保护</td><td>用户界面、同步服务</td></tr></tbody></table><p>关键集合可以通过依赖图自动展开。例如业务声明需要 Codec，系统自动加入其电源轨、MCLK、I2C 控制器、Pinmux 和 DMA，而不是由业务代码逐个调用初始化函数。</p><h3 id="设备状态机"><a href="#设备状态机" class="headerlink" title="设备状态机"></a>设备状态机</h3><p>每个可管理设备建议至少支持：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">OFF -&gt; POWERING -&gt; RESETTING -&gt; CONFIGURING -&gt; READY</span><br><span class="line">READY -&gt; QUIESCING -&gt; SUSPENDED</span><br><span class="line">SUSPENDED -&gt; RESUMING -&gt; READY</span><br><span class="line">any state -&gt; ERROR</span><br></pre></td></tr></table></figure><p>驱动接口应区分：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">prepare_suspend()   检查是否可挂起，可拒绝</span><br><span class="line">suspend()           保存必要状态并关闭设备</span><br><span class="line">resume_early()      恢复关键寄存器或唤醒能力</span><br><span class="line">resume()            恢复普通功能</span><br><span class="line">resume_deferred()   校准、缓存重建、耗时检查</span><br><span class="line">abort_suspend()     休眠事务中止后的回滚</span><br></pre></td></tr></table></figure><p>只有一个 <code>resume()</code> 回调通常不够，因为它会迫使所有恢复工作串行执行。</p><h2 id="系统休眠应作为事务处理"><a href="#系统休眠应作为事务处理" class="headerlink" title="系统休眠应作为事务处理"></a>系统休眠应作为事务处理</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">stateDiagram-v2</span><br><span class="line">    [*] --&gt; Awake</span><br><span class="line">    Awake --&gt; Evaluating: idle candidate</span><br><span class="line">    Evaluating --&gt; Preparing: policy selects state</span><br><span class="line">    Preparing --&gt; Aborting: module rejects / event arrives</span><br><span class="line">    Preparing --&gt; Committing: all prepare callbacks pass</span><br><span class="line">    Committing --&gt; Sleeping: wake sources armed + context committed</span><br><span class="line">    Sleeping --&gt; ResumingEarly: wakeup</span><br><span class="line">    ResumingEarly --&gt; ResumingCritical: reason captured</span><br><span class="line">    ResumingCritical --&gt; Awake: critical service ready</span><br><span class="line">    ResumingCritical --&gt; ResumingDeferred: background work queued</span><br><span class="line">    ResumingDeferred --&gt; Awake: all optional work complete</span><br><span class="line">    Aborting --&gt; Awake: rollback</span><br></pre></td></tr></table></figure><h3 id="两阶段提交"><a href="#两阶段提交" class="headerlink" title="两阶段提交"></a>两阶段提交</h3><p>休眠入口可以借鉴事务的两阶段思想：</p><ol><li><strong>Prepare 阶段</strong>：冻结新请求，询问所有相关模块是否允许进入目标状态，等待可控的在途事务完成，配置候选唤醒源，但不真正切断关键资源。</li><li><strong>Commit 阶段</strong>：保存上下文，确认没有新事件，原子地标记休眠事务已提交，关闭设备和时钟，执行 WFI&#x2F;WFE 或平台休眠指令。</li></ol><p>如果 Prepare 期间任何模块返回忙、发生新唤醒事件、下一超时点改变或状态选择失效，应执行 <code>abort_suspend()</code>，恢复已修改的设备。不要在半数设备已关闭后直接返回普通运行，否则系统会处于不可预测状态。</p><h3 id="最后时刻检查"><a href="#最后时刻检查" class="headerlink" title="最后时刻检查"></a>最后时刻检查</h3><p>进入休眠前必须在受控临界区内再次检查：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">是否已有 pending IRQ</span><br><span class="line">是否出现更早的软件定时器</span><br><span class="line">是否新增 PM lock/latency request</span><br><span class="line">唤醒源是否已正确使能</span><br><span class="line">唤醒标志是否已清理到预期状态</span><br><span class="line">关键写缓冲、缓存和外设事务是否已完成</span><br><span class="line">目标状态是否仍满足约束</span><br></pre></td></tr></table></figure><p>典型竞态是：策略判断可以睡，随后按键中断到来并被置 pending，但代码仍执行 WFI；如果唤醒事件被错误清除或中断屏蔽顺序不当，可能造成丢事件或立即睡死。平台入口应使用芯片推荐的原子序列，并在关闭中断与执行休眠指令之间避免普通业务代码。</p><h2 id="PM-约束、锁和引用计数"><a href="#PM-约束、锁和引用计数" class="headerlink" title="PM 约束、锁和引用计数"></a>PM 约束、锁和引用计数</h2><h3 id="延迟约束"><a href="#延迟约束" class="headerlink" title="延迟约束"></a>延迟约束</h3><p>模块不应直接指定具体休眠模式，而应声明最大可接受退出延迟：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief Acquire a wakeup latency constraint.</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * @param max_exit_latency_us Maximum acceptable system exit latency.</span></span><br><span class="line"><span class="comment"> * @param owner               Static owner identifier used for diagnostics.</span></span><br><span class="line"><span class="comment"> * @return Constraint handle, or an invalid handle on failure.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="type">pm_constraint_handle_t</span> <span class="title function_">pm_latency_acquire</span><span class="params">(<span class="type">uint32_t</span> max_exit_latency_us,</span></span><br><span class="line"><span class="params">                                            <span class="type">const</span> <span class="type">char</span> *owner)</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief Release a previously acquired wakeup latency constraint.</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * @param handle Constraint handle returned by pm_latency_acquire().</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">pm_latency_release</span><span class="params">(<span class="type">pm_constraint_handle_t</span> handle)</span>;</span><br></pre></td></tr></table></figure><p>系统聚合时取最小值。这与 Linux PM QoS 和 Zephyr latency request 的思想一致：模块表达性能期望，策略负责把期望映射为允许的状态。</p><h3 id="状态锁"><a href="#状态锁" class="headerlink" title="状态锁"></a>状态锁</h3><p>对 DMA、Flash 擦写、协议时隙等“绝对不能进入某类状态”的区间，可使用状态锁：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">PM_LOCK_NO_IDLE</span><br><span class="line">PM_LOCK_NO_LIGHT_SLEEP</span><br><span class="line">PM_LOCK_NO_DEEP_SLEEP</span><br><span class="line">PM_LOCK_KEEP_HF_CLOCK</span><br><span class="line">PM_LOCK_KEEP_BUS</span><br><span class="line">PM_LOCK_KEEP_MEMORY_BANK</span><br></pre></td></tr></table></figure><p>锁必须成对获取和释放，并记录 owner、调用位置、获取时间和超时诊断。低功耗系统无法进入深睡的常见原因不是策略错误，而是某个异常路径漏释放锁。</p><h3 id="设备运行时引用"><a href="#设备运行时引用" class="headerlink" title="设备运行时引用"></a>设备运行时引用</h3><p>设备应使用 <code>get/put</code> 或 usage count，而不是业务随意调用 <code>power_on()</code>&#x2F;<code>power_off()</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">device_pm_get(dev)     usage_count++，必要时恢复设备</span><br><span class="line">device_pm_put(dev)     usage_count--，为 0 后允许自动挂起</span><br><span class="line">device_pm_get_async()  异步恢复，完成后通知</span><br><span class="line">device_pm_mark_busy()  延后 autosuspend 截止时间</span><br></pre></td></tr></table></figure><p>引用计数解决“多个模块共享一个 I2C 控制器、SPI Flash、电源轨或时钟”的问题。只有所有使用者都释放后，设备才可挂起。</p><h2 id="运行时-PM-与系统级-PM-应分离"><a href="#运行时-PM-与系统级-PM-应分离" class="headerlink" title="运行时 PM 与系统级 PM 应分离"></a>运行时 PM 与系统级 PM 应分离</h2><p>系统级休眠不是唯一节能手段。即使 CPU 仍在运行，长期空闲的显示、传感器、Codec、无线、外部 Flash 和高速时钟也应独立进入低功耗状态。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[系统处于 RUN/IDLE] --&gt; B&#123;设备 usage_count = 0?&#125;</span><br><span class="line">    B --&gt;|否| C[保持 ACTIVE]</span><br><span class="line">    B --&gt;|是| D[启动 autosuspend 延时]</span><br><span class="line">    D --&gt; E&#123;延时内再次使用?&#125;</span><br><span class="line">    E --&gt;|是| C</span><br><span class="line">    E --&gt;|否| F[运行时 suspend]</span><br><span class="line">    F --&gt; G[设备 SUSPENDED]</span><br><span class="line">    G --&gt; H&#123;新请求到达&#125;</span><br><span class="line">    H --&gt;|同步请求| I[同步 resume]</span><br><span class="line">    H --&gt;|异步请求| J[排队 resume + completion]</span><br><span class="line">    I --&gt; C</span><br><span class="line">    J --&gt; C</span><br></pre></td></tr></table></figure><p>这样做有两个好处：</p><ol><li>系统真正进入深睡前，许多设备已经处于稳定挂起状态，系统级 suspend 不必临时串行关闭全部设备。</li><li>业务活跃但只使用少数设备时，仍能获得显著节能收益。</li></ol><p>Linux Runtime PM 和 Zephyr Device Runtime PM 都采用使用关系驱动设备挂起&#x2F;恢复。MCU 系统可以实现简化版本：固定设备表、静态引用计数、可配置 autosuspend 时间和少量回调即可。</p><h2 id="自适应选择睡眠深度"><a href="#自适应选择睡眠深度" class="headerlink" title="自适应选择睡眠深度"></a>自适应选择睡眠深度</h2><h3 id="基于下一超时点"><a href="#基于下一超时点" class="headerlink" title="基于下一超时点"></a>基于下一超时点</h3><p>最基础的方法是读取调度器下一次定时事件：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">T_next = min(</span><br><span class="line">    next_rtos_timeout,</span><br><span class="line">    next_protocol_slot,</span><br><span class="line">    next_sensor_sample,</span><br><span class="line">    next_watchdog_service,</span><br><span class="line">    next_custom_deadline</span><br><span class="line">)</span><br></pre></td></tr></table></figure><p><code>T_idle_predicted = T_next - now</code>。Zephyr 的 residency 策略和 FreeRTOS Tickless Idle 都体现了这一思想：系统知道预计空闲时长后，才决定是否停止 Tick 或进入更深状态。</p><h3 id="基于交互热度"><a href="#基于交互热度" class="headerlink" title="基于交互热度"></a>基于交互热度</h3><p>仅看下一定时器无法预测用户在刚按完一次键后很可能继续操作。可以维护交互热度：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">heat(t) = heat(t0) * exp(-(t - t0) / tau) + event_weight</span><br></pre></td></tr></table></figure><p>当热度较高时，限制在浅睡或降低 autosuspend 激进程度；热度衰减后再允许深睡。无需浮点运算，可用定点衰减或分段计数器实现。</p><h3 id="基于迟滞与最小保持时间"><a href="#基于迟滞与最小保持时间" class="headerlink" title="基于迟滞与最小保持时间"></a>基于迟滞与最小保持时间</h3><p>如果状态阈值没有迟滞，系统会在 Light Sleep 和 Deep Sleep 之间抖动。建议配置：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">enter_deep_after_idle_ms</span><br><span class="line">stay_shallow_after_wakeup_ms</span><br><span class="line">min_active_hold_ms</span><br><span class="line">min_device_on_time_ms</span><br><span class="line">min_device_off_time_ms</span><br></pre></td></tr></table></figure><p>例如刚从深睡恢复后的 500ms 内保持浅睡，以覆盖连续按键；无线接收窗口结束后仍保持高速时钟 2ms，防止立即又收到重传；传感器上电后至少保持到稳定时间结束，避免频繁启停。</p><h3 id="基于电量与温度"><a href="#基于电量与温度" class="headerlink" title="基于电量与温度"></a>基于电量与温度</h3><p>低电量时可以提高深睡倾向、减少后台恢复；高温时可能降低主频、延后非关键任务。但策略变化不能破坏硬实时约束。电量只应影响“可选动作”和阈值，不应绕过唤醒源、告警和安全控制要求。</p><h2 id="异步恢复与就绪通知"><a href="#异步恢复与就绪通知" class="headerlink" title="异步恢复与就绪通知"></a>异步恢复与就绪通知</h2><p>业务调用设备时，可能遇到设备仍在恢复。框架应明确提供同步和异步语义：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">service_get_sync(service, timeout)</span><br><span class="line">service_get_async(service, callback, context)</span><br><span class="line">service_is_ready(service)</span><br><span class="line">service_release(service)</span><br></pre></td></tr></table></figure><p>关键路径可用短超时同步等待，非关键任务应异步等待。恢复完成后发布分级事件：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">PM_EVENT_WAKE_CAPTURED</span><br><span class="line">PM_EVENT_KERNEL_READY</span><br><span class="line">PM_EVENT_CRITICAL_CLOCK_READY</span><br><span class="line">PM_EVENT_SERVICE_READY(service_id)</span><br><span class="line">PM_EVENT_FULLY_RESUMED</span><br><span class="line">PM_EVENT_RESUME_DEGRADED</span><br><span class="line">PM_EVENT_RESUME_FAILED</span><br></pre></td></tr></table></figure><p>不要只用一个全局 <code>system_resumed = true</code>，因为它无法表达“系统已执行但 Codec 未就绪”“网络可用但外部 Flash 仍在唤醒”“显示可以首帧但背光尚未渐亮”等状态。</p><h2 id="时间基准与-Tick-补偿"><a href="#时间基准与-Tick-补偿" class="headerlink" title="时间基准与 Tick 补偿"></a>时间基准与 Tick 补偿</h2><p>深睡时 SysTick、高速定时器和部分外设计数器可能停止。系统必须用休眠期间仍工作的 RTC、低功耗定时器或 AON counter 计算睡眠时间，并更新内核时间。</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">sleep_ticks = convert_aon_delta_to_os_ticks(wake_count - sleep_count)</span><br><span class="line">os_tick_advance(sleep_ticks)</span><br><span class="line">monotonic_time_adjust(delta)</span><br></pre></td></tr></table></figure><p>需要处理：</p><ul><li>低速时钟精度和温漂。</li><li>计数器回绕。</li><li>读计数器的跨域同步。</li><li>唤醒延迟是否计入睡眠时间。</li><li>相对定时器和绝对定时器的差异。</li><li>网络协议、重传和证书时间对时间跳变的敏感性。</li></ul><p>FreeRTOS Tickless Idle、RT-Thread PM 时间补偿和 Zephyr 的超时管理都提供了可参考的框架。产品仍需根据硬件低功耗计时器精度进行校准，尤其不能假设标称 32.768kHz 在全温范围内完全准确。</p><h2 id="外设恢复策略"><a href="#外设恢复策略" class="headerlink" title="外设恢复策略"></a>外设恢复策略</h2><h3 id="PLL-与时钟树"><a href="#PLL-与时钟树" class="headerlink" title="PLL 与时钟树"></a>PLL 与时钟树</h3><p>PLL 恢复应分成“最小时钟可执行”和“高性能时钟就绪”两个阶段：</p><ol><li>先用内部 RC 或低速时钟执行恢复桩。</li><li>启动外部晶振和 PLL，但不在 ISR 中忙等完整超时。</li><li>对时钟稳定中断或状态位设置最大等待时间。</li><li>必要时先用降级频率运行关键业务。</li><li>PLL 失败时进入可诊断的降级模式，而不是无限等待。</li><li>时钟切换后更新 Flash wait state、总线分频、串口波特率、定时器和采样时钟。</li></ol><p>升频和降频顺序必须分别设计。一般升频前先提高供电档位和 Flash wait state，降频后再降低 wait state 和电压，具体以芯片手册为准。</p><h3 id="I2C-SPI-设备"><a href="#I2C-SPI-设备" class="headerlink" title="I2C&#x2F;SPI 设备"></a>I2C&#x2F;SPI 设备</h3><p>外部器件上电后可能需要稳定时间、复位脉冲、设备 ID 检查、寄存器恢复或校准。建议把驱动恢复拆为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">power_enable -&gt; reset_release -&gt; ready_wait -&gt; essential_config -&gt; ready</span><br><span class="line">                                              -&gt; full_config -&gt; calibrated</span><br></pre></td></tr></table></figure><p>关键路径只执行 <code>essential_config</code>。完整寄存器同步、FIFO 清理、自检和校准可以延后。I2C 总线还需处理睡眠期间从设备异常拉低 SDA、主控复位但从设备未复位、时钟频率改变导致时序配置失效等问题。</p><h3 id="外部-Flash-与文件系统"><a href="#外部-Flash-与文件系统" class="headerlink" title="外部 Flash 与文件系统"></a>外部 Flash 与文件系统</h3><p>外部 Flash 可能处于 Deep Power-Down。恢复时应先退出低功耗、等待器件规定的 <code>tRES</code>，再访问 JEDEC ID 或资源。不要让 UI 首次反馈依赖文件系统挂载和资源加载；常用图标、短提示和关键配置可以保存在内部 Flash、retention RAM 或预解压缓存中。</p><h3 id="显示"><a href="#显示" class="headerlink" title="显示"></a>显示</h3><p>显示恢复可分为：</p><ol><li>背光保持关闭或低亮。</li><li>恢复显示控制器和最小帧缓冲。</li><li>输出静态首帧或状态图标。</li><li>后台加载字体、图片和复杂页面。</li><li>渐亮背光，避免电流冲击和白屏。</li></ol><h3 id="音频"><a href="#音频" class="headerlink" title="音频"></a>音频</h3><p>Codec 和功放常有上电时序、偏置建立和 pop noise 风险。快速反馈可以先用低功耗蜂鸣器、片上 PWM 或预留提示，而不是直接在 ISR 中启动完整音频链路。恢复顺序通常涉及 MCLK、I2C 配置、模拟偏置、数字接口、静音解除和功放使能，应通过状态机和定时器推进。</p><h3 id="无线通信"><a href="#无线通信" class="headerlink" title="无线通信"></a>无线通信</h3><p>无线恢复成本可能远高于 MCU 唤醒本身。应区分：</p><ul><li>保持连接的 modem sleep。</li><li>控制器保留、主 CPU 休眠。</li><li>断开链路后的重连。</li><li>深睡后重新初始化射频和协议栈。</li></ul><p>如果业务对首包延迟敏感，应在连接参数、监听窗口和系统休眠策略之间协同，而不是只优化 MCU 的 1~2ms 唤醒时间。</p><h2 id="并发、竞态与幂等性"><a href="#并发、竞态与幂等性" class="headerlink" title="并发、竞态与幂等性"></a>并发、竞态与幂等性</h2><h3 id="重复恢复"><a href="#重复恢复" class="headerlink" title="重复恢复"></a>重复恢复</h3><p>多个唤醒源可能同时触发，或者一个事件在硬件和软件层重复上报。恢复入口必须使用原子状态防止重复执行：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">SLEEPING -&gt; RESUMING_EARLY -&gt; RESUMING_CRITICAL -&gt; AWAKE</span><br></pre></td></tr></table></figure><p>只有成功完成状态转换的执行单元负责全局恢复；其他路径只合并新的唤醒原因。恢复函数应尽量幂等：设备已经 READY 时重复 <code>resume</code> 不应再次复位或破坏状态。</p><h3 id="休眠期间新请求到达"><a href="#休眠期间新请求到达" class="headerlink" title="休眠期间新请求到达"></a>休眠期间新请求到达</h3><p>Prepare 阶段冻结普通设备请求后，新请求可以：</p><ol><li>设置 <code>abort_requested</code> 并等待事务回滚。</li><li>被放入 pending queue，唤醒后立即处理。</li><li>对极高优先级请求直接中止休眠。</li></ol><p>不能让新请求在设备关闭一半时直接进入普通驱动路径。</p><h3 id="锁顺序"><a href="#锁顺序" class="headerlink" title="锁顺序"></a>锁顺序</h3><p>PM 框架容易与驱动锁、总线锁、调度器锁形成死锁。建议：</p><ul><li>PM core 不在持有全局锁时调用可能阻塞的驱动回调。</li><li>Prepare 回调只做短检查和状态切换，不等待未知时长。</li><li>驱动恢复使用统一依赖顺序。</li><li>ISR 不获取普通互斥锁。</li><li>完成事件使用静态对象或无锁标志。</li><li>任何同步等待都必须有超时和失败路径。</li></ul><h2 id="失败、降级与回退"><a href="#失败、降级与回退" class="headerlink" title="失败、降级与回退"></a>失败、降级与回退</h2><p>唤醒并不保证成功。应为每个阶段定义超时和降级：</p><table><thead><tr><th>失败点</th><th>处理策略</th></tr></thead><tbody><tr><td>外部晶振&#x2F;PLL 未锁定</td><td>使用内部时钟降级运行，记录故障，限制高带宽功能</td></tr><tr><td>I2C 设备无响应</td><td>总线恢复、设备复位、隔离故障设备，不阻塞其他服务</td></tr><tr><td>Codec 校准失败</td><td>保持静音，使用替代提示或禁用音频</td></tr><tr><td>外部 Flash 未就绪</td><td>使用内置最小资源，延后重试，避免前台无限等待</td></tr><tr><td>retention CRC 错误</td><td>丢弃上下文，走冷启动恢复，标记原因</td></tr><tr><td>时间补偿异常</td><td>使用 RTC 绝对时间校正，重建定时器，记录跳变</td></tr><tr><td>关键设备恢复超时</td><td>返回 <code>RESUME_DEGRADED</code> 或受控重启</td></tr><tr><td>连续唤醒风暴</td><td>屏蔽故障源、指数退避、保留诊断快照</td></tr></tbody></table><p>系统应区分“可继续运行的降级”和“必须复位的不可恢复错误”。不要为了追求表面成功而吞掉错误；也不要因为一个非关键温湿度传感器失败就重启整个设备。</p><h2 id="通用-API-设计"><a href="#通用-API-设计" class="headerlink" title="通用 API 设计"></a>通用 API 设计</h2><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief Power state properties used by the policy governor.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="type">uint8_t</span> state_id;</span><br><span class="line">    <span class="type">uint32_t</span> entry_latency_us;</span><br><span class="line">    <span class="type">uint32_t</span> exit_latency_us;</span><br><span class="line">    <span class="type">uint32_t</span> min_residency_us;</span><br><span class="line">    <span class="type">uint32_t</span> steady_power_uw;</span><br><span class="line">    <span class="type">uint32_t</span> wakeup_source_mask;</span><br><span class="line">    <span class="type">uint32_t</span> retained_domain_mask;</span><br><span class="line">    <span class="type">uint32_t</span> powered_domain_mask;</span><br><span class="line">    <span class="type">bool</span> reset_resume;</span><br><span class="line">&#125; <span class="type">pm_state_desc_t</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief Runtime constraints aggregated before selecting a power state.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="type">uint32_t</span> max_exit_latency_us;</span><br><span class="line">    <span class="type">uint32_t</span> required_wakeup_mask;</span><br><span class="line">    <span class="type">uint32_t</span> required_retention_mask;</span><br><span class="line">    <span class="type">uint32_t</span> blocked_state_mask;</span><br><span class="line">    <span class="type">uint64_t</span> next_deadline_us;</span><br><span class="line">    <span class="type">uint32_t</span> activity_heat;</span><br><span class="line">&#125; <span class="type">pm_constraints_t</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief Normalized wakeup event delivered to the application layer.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="type">uint32_t</span> sequence;</span><br><span class="line">    <span class="type">uint32_t</span> reason_mask;</span><br><span class="line">    <span class="type">uint32_t</span> source_id;</span><br><span class="line">    <span class="type">uint64_t</span> timestamp_us;</span><br><span class="line">    <span class="type">uint32_t</span> raw_flags;</span><br><span class="line">    <span class="type">uint32_t</span> snapshot;</span><br><span class="line">&#125; <span class="type">pm_wakeup_event_t</span>;</span><br></pre></td></tr></table></figure><p>推荐的框架接口包括：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">pm_latency_acquire(max_us, owner)</span><br><span class="line">pm_latency_release(handle)</span><br><span class="line">pm_state_lock_acquire(mask, owner)</span><br><span class="line">pm_state_lock_release(handle)</span><br><span class="line">pm_activity_hint(type, expected_duration_ms)</span><br><span class="line">pm_wakeup_source_enable(source, config)</span><br><span class="line">pm_wakeup_source_disable(source)</span><br><span class="line">pm_service_get_async(service, callback)</span><br><span class="line">pm_service_release(service)</span><br><span class="line">pm_get_last_wakeup_event(event)</span><br><span class="line">pm_get_statistics(stats)</span><br><span class="line">pm_trace_enable(mask)</span><br></pre></td></tr></table></figure><p>接口应避免让上层依赖芯片特定模式名。业务说“最大退出延迟 2ms”和“需要 GPIO+RTC 唤醒”，比说“请进入 STOP2”更可移植。</p><h2 id="策略伪代码"><a href="#策略伪代码" class="headerlink" title="策略伪代码"></a>策略伪代码</h2><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief Select the deepest power state that satisfies all active constraints.</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * @param now_us       Current monotonic time.</span></span><br><span class="line"><span class="comment"> * @param constraints  Aggregated runtime constraints.</span></span><br><span class="line"><span class="comment"> * @return Selected power state descriptor.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="type">static</span> <span class="type">const</span> <span class="type">pm_state_desc_t</span> *<span class="title function_">pm_policy_select</span><span class="params">(<span class="type">uint64_t</span> now_us,</span></span><br><span class="line"><span class="params">                                                <span class="type">const</span> <span class="type">pm_constraints_t</span> *constraints)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">uint64_t</span> idle_us = <span class="number">0U</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (constraints-&gt;next_deadline_us &gt; now_us)</span><br><span class="line">    &#123;</span><br><span class="line">        idle_us = constraints-&gt;next_deadline_us - now_us;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">size_t</span> i = <span class="number">0U</span>; i &lt; g_state_count; ++i)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="type">const</span> <span class="type">pm_state_desc_t</span> *state = &amp;g_states_deep_to_shallow[i];</span><br><span class="line">        <span class="type">uint64_t</span> required_residency;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> ((constraints-&gt;blocked_state_mask &amp; (<span class="number">1UL</span> &lt;&lt; state-&gt;state_id)) != <span class="number">0U</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (state-&gt;exit_latency_us &gt; constraints-&gt;max_exit_latency_us)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> ((constraints-&gt;required_wakeup_mask &amp; ~state-&gt;wakeup_source_mask) != <span class="number">0U</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> ((constraints-&gt;required_retention_mask &amp; ~state-&gt;retained_domain_mask) != <span class="number">0U</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        required_residency = (<span class="type">uint64_t</span>)state-&gt;entry_latency_us +</span><br><span class="line">                             (<span class="type">uint64_t</span>)state-&gt;exit_latency_us +</span><br><span class="line">                             (<span class="type">uint64_t</span>)state-&gt;min_residency_us +</span><br><span class="line">                             pm_policy_guard_time_us(state, constraints);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (idle_us &lt; required_residency)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (!pm_energy_model_is_profitable(state, idle_us))</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> state;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> &amp;g_run_or_idle_state;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>真实实现还需加入：关键服务恢复时间、当前活动热度、设备 autosuspend 状态、平台 errata、温度、电量、最近恢复失败、唤醒源电平和系统安全策略。</p><h2 id="进入休眠流程"><a href="#进入休眠流程" class="headerlink" title="进入休眠流程"></a>进入休眠流程</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant Idle as Idle Task</span><br><span class="line">    participant Policy as PM Policy</span><br><span class="line">    participant Tx as PM Transaction</span><br><span class="line">    participant Dev as Devices</span><br><span class="line">    participant SoC as SoC Backend</span><br><span class="line"></span><br><span class="line">    Idle-&gt;&gt;Policy: next deadline + active constraints</span><br><span class="line">    Policy--&gt;&gt;Idle: selected state</span><br><span class="line">    Idle-&gt;&gt;Tx: begin(state)</span><br><span class="line">    Tx-&gt;&gt;Tx: freeze new PM-sensitive requests</span><br><span class="line">    Tx-&gt;&gt;Dev: prepare_suspend(state)</span><br><span class="line">    alt any device rejects</span><br><span class="line">        Dev--&gt;&gt;Tx: busy/error</span><br><span class="line">        Tx-&gt;&gt;Dev: abort_suspend()</span><br><span class="line">        Tx--&gt;&gt;Idle: aborted</span><br><span class="line">    else all prepared</span><br><span class="line">        Tx-&gt;&gt;Tx: save retention context</span><br><span class="line">        Tx-&gt;&gt;SoC: arm wake sources</span><br><span class="line">        Tx-&gt;&gt;Tx: final pending-event check</span><br><span class="line">        alt event pending</span><br><span class="line">            Tx-&gt;&gt;Dev: abort_suspend()</span><br><span class="line">            Tx--&gt;&gt;Idle: retry later</span><br><span class="line">        else commit</span><br><span class="line">            Tx-&gt;&gt;Dev: suspend in dependency order</span><br><span class="line">            Tx-&gt;&gt;SoC: enter low-power state</span><br><span class="line">            SoC--&gt;&gt;Tx: wakeup</span><br><span class="line">            Tx-&gt;&gt;Tx: capture wake reason</span><br><span class="line">            Tx-&gt;&gt;Dev: resume critical dependency set</span><br><span class="line">            Tx--&gt;&gt;Idle: service ready</span><br><span class="line">        end</span><br><span class="line">    end</span><br></pre></td></tr></table></figure><h2 id="开源方案原理详解"><a href="#开源方案原理详解" class="headerlink" title="开源方案原理详解"></a>开源方案原理详解</h2><h3 id="Zephyr：状态属性、驻留策略和延迟请求"><a href="#Zephyr：状态属性、驻留策略和延迟请求" class="headerlink" title="Zephyr：状态属性、驻留策略和延迟请求"></a>Zephyr：状态属性、驻留策略和延迟请求</h3><p>Zephyr 的系统电源管理把电源状态描述为带属性的对象，其中包括最小驻留时间和退出延迟。其 residency 思路是：只有“到下一调度事件的时间”足以覆盖最小驻留与退出延迟时，才选择该状态。Zephyr 还提供 latency request，使应用或驱动注册最大可接受退出延迟；策略不能选择超过该限制的状态。</p><p>可借鉴点：</p><ol><li>状态属性数据化，不把判断散落在业务代码中。</li><li>策略函数与平台进入低功耗的实现分离。</li><li>允许应用增加自定义下一事件时间来源。</li><li>延迟约束由多个模块注册，系统统一聚合。</li><li>系统级 PM 与设备 PM 分开，但可以协同。</li></ol><p>在小型 RTOS 中，可以用静态状态数组和少量约束槽实现同样思想，不必复制 Zephyr 的全部框架。</p><h3 id="Zephyr-Device-Runtime-PM：设备使用计数"><a href="#Zephyr-Device-Runtime-PM：设备使用计数" class="headerlink" title="Zephyr Device Runtime PM：设备使用计数"></a>Zephyr Device Runtime PM：设备使用计数</h3><p>Zephyr Device Runtime PM 允许驱动、子系统和应用通过使用关系决定设备是否保持活动。第一个使用者到来时恢复设备，最后一个使用者离开时允许挂起，并支持同步或异步操作。</p><p>可借鉴点：</p><ul><li>共享设备使用引用计数。</li><li>设备状态明确区分 ACTIVE、SUSPENDING、SUSPENDED。</li><li>系统进入低功耗前，不必重复挂起已经 runtime suspended 的设备。</li><li>高层不必了解设备具体寄存器，只声明使用关系。</li></ul><h3 id="Linux-cpuidle-与-PM-QoS：治理器不越过延迟上限"><a href="#Linux-cpuidle-与-PM-QoS：治理器不越过延迟上限" class="headerlink" title="Linux cpuidle 与 PM QoS：治理器不越过延迟上限"></a>Linux cpuidle 与 PM QoS：治理器不越过延迟上限</h3><p>Linux <code>cpuidle</code> 通过 governor 在多个 CPU idle state 中选择状态。状态具有退出延迟和目标驻留特征，governor 必须考虑 PM QoS 的 CPU 延迟约束，不应选择退出延迟超过有效约束的状态。</p><p>可借鉴点：</p><ul><li>策略和驱动分离：governor 决策，driver 执行平台状态。</li><li>约束聚合取最严格值。</li><li>选择结果可以根据实际驻留和命中情况反馈调整。</li><li>“最省电状态”必须服从延迟服务质量。</li></ul><h3 id="Linux-Runtime-PM：设备级自动挂起"><a href="#Linux-Runtime-PM：设备级自动挂起" class="headerlink" title="Linux Runtime PM：设备级自动挂起"></a>Linux Runtime PM：设备级自动挂起</h3><p>Linux Runtime PM 为设备提供 <code>runtime_suspend</code>、<code>runtime_resume</code>、<code>runtime_idle</code> 等回调，并由 PM core 维护状态和同步。设备可以根据 usage count 和 autosuspend 策略独立挂起，而不是等待系统级 suspend。</p><p>可借鉴点：</p><ul><li>设备电源状态由统一核心管理。</li><li>自动挂起需要防止并发恢复和重复挂起。</li><li>设备父子关系、总线和 PM domain 影响回调顺序。</li><li>runtime PM 与 system sleep 交互时要同步真实硬件状态。</li></ul><h3 id="Linux-wakeup-source：唤醒能力与是否允许唤醒分离"><a href="#Linux-wakeup-source：唤醒能力与是否允许唤醒分离" class="headerlink" title="Linux wakeup source：唤醒能力与是否允许唤醒分离"></a>Linux wakeup source：唤醒能力与是否允许唤醒分离</h3><p>一个设备“硬件上能唤醒系统”与“当前策略允许它作为唤醒源”是两个不同概念。产品中也应区分：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">wakeup_capable   硬件和驱动是否支持</span><br><span class="line">wakeup_enabled   当前模式是否允许</span><br><span class="line">wakeup_armed     本次休眠是否已经配置成功</span><br><span class="line">wakeup_pending   是否已有待处理事件</span><br></pre></td></tr></table></figure><p>这可以避免把所有可唤醒设备永久开启，导致漏电或唤醒风暴，也便于按场景切换允许的唤醒源。</p><h3 id="FreeRTOS-Tickless-Idle：停止无意义周期唤醒"><a href="#FreeRTOS-Tickless-Idle：停止无意义周期唤醒" class="headerlink" title="FreeRTOS Tickless Idle：停止无意义周期唤醒"></a>FreeRTOS Tickless Idle：停止无意义周期唤醒</h3><p>普通 RTOS Tick 会周期性唤醒 CPU，即使没有任务可运行。FreeRTOS Tickless Idle 在预计空闲期间停止周期 Tick，设置能覆盖下一任务超时的低功耗定时器，唤醒后校正内核 Tick。</p><p>可借鉴点：</p><ul><li>休眠深度选择必须知道预计空闲时长。</li><li>睡眠前后要处理 Tick 抑制和时间补偿。</li><li>外部中断提前唤醒时，补偿实际睡眠时间而不是计划时间。</li><li>Tickless 只解决周期唤醒，不自动解决设备依赖和深睡上下文。</li></ul><h3 id="RT-Thread-PM：模式投票、运行频率与设备回调"><a href="#RT-Thread-PM：模式投票、运行频率与设备回调" class="headerlink" title="RT-Thread PM：模式投票、运行频率与设备回调"></a>RT-Thread PM：模式投票、运行频率与设备回调</h3><p>RT-Thread PM 将运行模式与睡眠模式分开，并通过请求&#x2F;释放机制限制系统进入过深状态。应用或设备请求某个模式后，只有释放对应请求，系统才可能进入更低功耗模式。框架还支持设备 suspend&#x2F;resume、频率变化通知和睡眠时间补偿。</p><p>可借鉴点：</p><ul><li>投票接口简单，适合资源受限系统。</li><li>请求和释放必须成对，可用于保护 DMA 或关键事务。</li><li>运行时降频与休眠是两个独立维度。</li><li>外设对频率变化敏感，必须重新计算波特率、定时器和采样参数。</li></ul><p>需要额外补强的部分是：为投票增加 owner、超时、泄漏诊断和延迟数值约束，避免只有离散模式请求导致策略过于粗糙。</p><h3 id="Apache-NuttX-PM：活动上报、PM-Domain-与回调"><a href="#Apache-NuttX-PM：活动上报、PM-Domain-与回调" class="headerlink" title="Apache NuttX PM：活动上报、PM Domain 与回调"></a>Apache NuttX PM：活动上报、PM Domain 与回调</h3><p>NuttX PM 允许驱动上报活动，并按 domain 管理不同区域，例如 UI 域和网络域。状态改变前，驱动的 <code>prepare</code> 回调可以拒绝转换；统一状态改变后通过 <code>notify</code> 回调通知驱动。</p><p>可借鉴点：</p><ul><li>活动本身可以影响低功耗策略。</li><li>多 PM domain 适合独立电源岛或功能域。</li><li>Prepare 与 Notify 分开，便于拒绝和回滚。</li><li>平台 idle loop 保留最终决策权，可结合电量、硬件状态等信息。</li></ul><h3 id="ESP-IDF：状态保留域和多唤醒源"><a href="#ESP-IDF：状态保留域和多唤醒源" class="headerlink" title="ESP-IDF：状态保留域和多唤醒源"></a>ESP-IDF：状态保留域和多唤醒源</h3><p>ESP-IDF 清晰区分 Light Sleep 与 Deep Sleep：前者通常保留更多数字状态，后者关闭更多 CPU、RAM 和数字外设，只保留 RTC 控制器、RTC 外设和可配置的 RTC memory。系统可配置定时器、GPIO 等多种唤醒源。</p><p>可借鉴点：</p><ul><li>休眠模式本质上是电源域、时钟域、内存保留和唤醒源的组合。</li><li>深睡恢复可能接近冷启动，应用必须显式保存逻辑上下文。</li><li>AON&#x2F;RTC 域适合完成事件捕获、计时和极小状态机。</li><li>多唤醒源需要统一原因归一化和去重。</li></ul><h2 id="方案选型建议"><a href="#方案选型建议" class="headerlink" title="方案选型建议"></a>方案选型建议</h2><table><thead><tr><th>系统类型</th><th>推荐起点</th><th>重点补充</th></tr></thead><tbody><tr><td>裸机 MCU</td><td>静态状态表 + PM lock + AON 唤醒记录 + 分阶段初始化</td><td>原子休眠入口、时间补偿、设备依赖</td></tr><tr><td>FreeRTOS</td><td>Tickless Idle + 自定义 PM governor + 设备引用计数</td><td>延迟约束、系统级事务、后台恢复</td></tr><tr><td>RT-Thread</td><td>原生 PM 模式请求&#x2F;释放 + PM device</td><td>owner 诊断、数值延迟约束、关键服务集合</td></tr><tr><td>Zephyr</td><td>System PM + latency constraints + Device Runtime PM</td><td>产品能量模型、恢复服务分级、故障降级</td></tr><tr><td>Apache NuttX</td><td>PM domain + activity + prepare&#x2F;notify</td><td>量化退出延迟、能量盈亏、异步恢复</td></tr><tr><td>Linux SoC</td><td>cpuidle + PM QoS + Runtime PM + wakeup source</td><td>用户态服务启动顺序、设备树&#x2F;驱动依赖、整机 trace</td></tr><tr><td>ESP-IDF</td><td>Light&#x2F;Deep Sleep + wakeup source + RTC memory</td><td>逻辑上下文版本化、连接保持策略、恢复长尾</td></tr></tbody></table><h2 id="容量与资源预算"><a href="#容量与资源预算" class="headerlink" title="容量与资源预算"></a>容量与资源预算</h2><p>低功耗框架本身也要控制资源。小型 MCU 可以采用固定容量：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">PM constraint slots       8~32 个</span><br><span class="line">PM lock records           8~32 个</span><br><span class="line">Wake event queue          4~16 项</span><br><span class="line">Device descriptors        按静态设备数配置</span><br><span class="line">Retention record          64B~1KB</span><br><span class="line">Trace ring                1KB~8KB，可裁剪</span><br><span class="line">Deferred work items       按关键设备并发度配置</span><br></pre></td></tr></table></figure><p>所有关键路径对象应静态分配。Trace ring 可以使用定长记录：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief Compact power-management trace entry.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="type">uint32_t</span> timestamp_low;</span><br><span class="line">    <span class="type">uint16_t</span> event_id;</span><br><span class="line">    <span class="type">uint8_t</span> state_id;</span><br><span class="line">    <span class="type">uint8_t</span> cpu_or_domain;</span><br><span class="line">    <span class="type">uint32_t</span> argument;</span><br><span class="line">&#125; <span class="type">pm_trace_entry_t</span>;</span><br></pre></td></tr></table></figure><p>在极小系统中，即使只能保留 64 条记录，也足以定位“为什么没睡”“为何被唤醒”“哪个设备恢复超时”。</p><h2 id="可观测性与统计"><a href="#可观测性与统计" class="headerlink" title="可观测性与统计"></a>可观测性与统计</h2><p>至少统计以下指标：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">每个状态的进入次数</span><br><span class="line">每个状态的累计驻留时间</span><br><span class="line">计划驻留时间与实际驻留时间误差</span><br><span class="line">各唤醒源次数、误唤醒次数和唤醒风暴次数</span><br><span class="line">休眠 prepare 拒绝次数及 owner</span><br><span class="line">PM lock 当前持有者与最长持有时间</span><br><span class="line">T_irq / T_capture / T_ack / T_service / T_full 的分布</span><br><span class="line">各设备 suspend/resume 平均值、P99、最大值和失败数</span><br><span class="line">PLL、外部晶振、总线、Flash、无线等恢复超时数</span><br><span class="line">retention CRC 或版本失败数</span><br><span class="line">降级运行和受控重启次数</span><br><span class="line">每次交互或采样事务的估算能量</span><br></pre></td></tr></table></figure><p>推荐通过 GPIO 打点配合逻辑分析仪或示波器：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">GPIO_A: 唤醒源物理边沿</span><br><span class="line">GPIO_B: Resume Stub 开始</span><br><span class="line">GPIO_C: Wake reason captured</span><br><span class="line">GPIO_D: First ACK</span><br><span class="line">GPIO_E: Critical service ready</span><br><span class="line">GPIO_F: Full resume complete</span><br></pre></td></tr></table></figure><p>这样可以把软件 trace 与硬件波形对齐。只看串口日志会受到 UART 尚未恢复、日志缓冲和调度延迟影响，不能作为唯一的时序依据。</p><h2 id="测试矩阵"><a href="#测试矩阵" class="headerlink" title="测试矩阵"></a>测试矩阵</h2><h3 id="功能测试"><a href="#功能测试" class="headerlink" title="功能测试"></a>功能测试</h3><ol><li>对每个休眠状态验证所有声明的唤醒源。</li><li>验证不允许的唤醒源不会错误唤醒。</li><li>验证多个唤醒源同时触发时原因不会丢失。</li><li>验证 retention 正常、CRC 错误、版本变化和全丢失路径。</li><li>验证每个设备的 suspend、resume、abort 和重复调用。</li><li>验证睡眠期间 RTC、相对超时和绝对时间补偿。</li><li>验证深睡后冷启动式恢复与浅睡状态保留式恢复。</li></ol><h3 id="竞态测试"><a href="#竞态测试" class="headerlink" title="竞态测试"></a>竞态测试</h3><ol><li>在 Prepare、Commit 前、WFI 前、刚唤醒、关键恢复和后台恢复各阶段注入中断。</li><li>在设备 autosuspend 与新请求同时发生时重复压测。</li><li>随机打断 PLL、I2C、SPI、Flash、无线恢复步骤。</li><li>强制 PM lock 泄漏，验证诊断和 watchdog 策略。</li><li>重复触发电平型唤醒源，验证不会形成无限唤醒循环。</li><li>在日志写入、Flash 擦除、DMA 和协议时隙期间请求深睡。</li></ol><h3 id="时序测试"><a href="#时序测试" class="headerlink" title="时序测试"></a>时序测试</h3><p>对每类唤醒事件至少测量平均值、P95、P99、P99.9 和最大值。测试温度、电压、电量、不同晶振启动条件、存储忙状态和系统负载。不能只在实验室室温和空载下测一次。</p><h3 id="能耗测试"><a href="#能耗测试" class="headerlink" title="能耗测试"></a>能耗测试</h3><ol><li>测量每个状态的稳态电流。</li><li>测量进入和退出波形的能量积分。</li><li>测量一次完整用户交互或采样事务的能量，而不只看待机电流。</li><li>比较不同阈值下的日均能耗和延迟违约率。</li><li>统计误唤醒和过早唤醒造成的能量损失。</li><li>验证深睡阈值是否高于实测盈亏平衡时间。</li></ol><h2 id="常见反模式"><a href="#常见反模式" class="headerlink" title="常见反模式"></a>常见反模式</h2><h3 id="反模式一：每次-Idle-都进入最深睡眠"><a href="#反模式一：每次-Idle-都进入最深睡眠" class="headerlink" title="反模式一：每次 Idle 都进入最深睡眠"></a>反模式一：每次 Idle 都进入最深睡眠</h3><p>问题：忽略进入&#x2F;退出能量、恢复延迟、交互连续性和设备校准成本。短空闲会频繁抖动，实际功耗可能更高。</p><p>改进：使用预计空闲时间、盈亏平衡点、迟滞和交互热度选择状态。</p><h3 id="反模式二：唤醒后按固定顺序初始化所有设备"><a href="#反模式二：唤醒后按固定顺序初始化所有设备" class="headerlink" title="反模式二：唤醒后按固定顺序初始化所有设备"></a>反模式二：唤醒后按固定顺序初始化所有设备</h3><p>问题：关键事件被显示、日志、网络和非关键传感器阻塞，恢复时间随设备数量线性增长。</p><p>改进：按唤醒事件定义关键设备集合，依赖图展开，后台异步恢复剩余设备。</p><h3 id="反模式三：在-ISR-中完成完整恢复"><a href="#反模式三：在-ISR-中完成完整恢复" class="headerlink" title="反模式三：在 ISR 中完成完整恢复"></a>反模式三：在 ISR 中完成完整恢复</h3><p>问题：中断延迟不可控、可能等待锁或超时，影响其他实时任务并增加死锁风险。</p><p>改进：ISR 只捕获原因和发布静态事件，复杂恢复交给高优先级线程或工作队列。</p><h3 id="反模式四：只保存变量，不保存版本和一致性信息"><a href="#反模式四：只保存变量，不保存版本和一致性信息" class="headerlink" title="反模式四：只保存变量，不保存版本和一致性信息"></a>反模式四：只保存变量，不保存版本和一致性信息</h3><p>问题：固件升级、掉电或保存中断后，旧结构可能被误当作有效上下文。</p><p>改进：使用 magic、version、length、generation、CRC 和提交标志。</p><h3 id="反模式五：模块直接指定芯片休眠模式"><a href="#反模式五：模块直接指定芯片休眠模式" class="headerlink" title="反模式五：模块直接指定芯片休眠模式"></a>反模式五：模块直接指定芯片休眠模式</h3><p>问题：业务与芯片绑定，多个模块互相覆盖，无法统一权衡。</p><p>改进：模块声明延迟、唤醒源、保留域、设备使用和活动期限，由 governor 选择模式。</p><h3 id="反模式六：只看平均唤醒时间"><a href="#反模式六：只看平均唤醒时间" class="headerlink" title="反模式六：只看平均唤醒时间"></a>反模式六：只看平均唤醒时间</h3><p>问题：长尾由 PLL、总线超时、Flash、无线和任务抢占触发，用户真正感知的是偶发慢响应。</p><p>改进：测量 P99&#x2F;P99.9、最大值和分阶段耗时，并设置每阶段超时。</p><h3 id="反模式七：PM-lock-没有-owner"><a href="#反模式七：PM-lock-没有-owner" class="headerlink" title="反模式七：PM lock 没有 owner"></a>反模式七：PM lock 没有 owner</h3><p>问题：系统无法进入深睡时无法定位是谁持锁，最终只能关闭低功耗功能。</p><p>改进：记录 owner、获取时间、调用点、嵌套计数和最大允许持有时间。</p><h3 id="反模式八：把设备恢复成功等同于业务恢复成功"><a href="#反模式八：把设备恢复成功等同于业务恢复成功" class="headerlink" title="反模式八：把设备恢复成功等同于业务恢复成功"></a>反模式八：把设备恢复成功等同于业务恢复成功</h3><p>问题：驱动 READY 不代表协议会话、缓存、页面或控制状态机已经一致。</p><p>改进：分离设备就绪、服务就绪和业务状态恢复事件。</p><h2 id="一套可落地的最小实现"><a href="#一套可落地的最小实现" class="headerlink" title="一套可落地的最小实现"></a>一套可落地的最小实现</h2><p>对于资源有限、现有工程尚无 PM 框架的 MCU，可以按以下顺序增量实现：</p><ol><li>建立统一 <code>pm_state_desc</code> 状态表，记录退出延迟、最小驻留、保留域和唤醒源。</li><li>把 RTOS Idle Hook 或空闲线程接入统一 <code>pm_policy_select()</code>。</li><li>实现下一超时点计算和 Tickless&#x2F;低功耗定时器补偿。</li><li>实现固定容量 PM lock，并提供 owner 诊断。</li><li>在 retention 区保存唤醒原因、序列号、时间戳、版本和 CRC。</li><li>把唤醒 ISR 改为只锁存事件，创建高优先级 Fast Resume Worker。</li><li>为关键设备拆分 <code>resume_early</code>、<code>resume</code> 和 <code>resume_deferred</code>。</li><li>建立静态设备依赖表，至少保证电源、时钟、总线和子设备顺序正确。</li><li>对共享设备增加 usage count 和 autosuspend。</li><li>增加 GPIO 打点与定长 PM trace ring。</li><li>实测每个状态的稳态功耗、切换能量和延迟，计算盈亏平衡阈值。</li><li>最后再引入交互热度、动态阈值、分电源域和预测策略。</li></ol><p>这条路径避免一开始就构造过度复杂的通用框架，同时保留后续演进空间。</p><h2 id="最终回答组织方式"><a href="#最终回答组织方式" class="headerlink" title="最终回答组织方式"></a>最终回答组织方式</h2><p>回答这类题时，可以按以下顺序展开：</p><ol><li>先指出核心矛盾：最深睡眠降低稳态功耗，但增加进入&#x2F;退出成本、上下文丢失和恢复长尾，因此不能按单一模式解决。</li><li>给出总体原则：延迟约束、驻留时间、能量盈亏、唤醒源和设备依赖共同决定状态。</li><li>说明快速路径与完整恢复路径：先捕获事件和提供最小反馈，再恢复关键服务，最后后台恢复非关键设备。</li><li>说明系统休眠是事务：Prepare、Commit、Abort、Resume，处理并发和最后时刻竞态。</li><li>说明设备管理：运行时引用计数、autosuspend、依赖图和分阶段回调。</li><li>给出量化公式：退出延迟预算、最小驻留、能量盈亏平衡和安全余量。</li><li>对照开源实现：Zephyr latency&#x2F;residency、Linux cpuidle&#x2F;PM QoS&#x2F;Runtime PM、FreeRTOS Tickless、RT-Thread 投票、NuttX PM domain、ESP-IDF 保留域。</li><li>最后补充可观测性、失败降级和测试矩阵，证明方案可实现、可验证、可维护。</li></ol><p>一句话概括：<strong>低功耗系统不应在“最省电”和“最快唤醒”之间二选一，而应通过可量化的状态模型、业务约束聚合、休眠事务、事件锁存、关键服务分阶段恢复和在线反馈，让系统在每一次具体空闲窗口中选择最合适的能耗—延迟工作点。</strong></p><h2 id="参考链接"><a href="#参考链接" class="headerlink" title="参考链接"></a>参考链接</h2><ul><li><a href="https://docs.zephyrproject.org/latest/services/pm/system.html">Zephyr System Power Management</a></li><li><a href="https://docs.zephyrproject.org/latest/services/pm/device.html">Zephyr Device Power Management</a></li><li><a href="https://docs.zephyrproject.org/latest/services/pm/device_runtime.html">Zephyr Device Runtime Power Management</a></li><li><a href="https://docs.zephyrproject.org/latest/samples/subsys/pm/latency/README.html">Zephyr PM Policy Latency Sample</a></li><li><a href="https://docs.kernel.org/admin-guide/pm/cpuidle.html">Linux CPU Idle Time Management</a></li><li><a href="https://docs.kernel.org/power/pm_qos_interface.html">Linux PM Quality of Service Interface</a></li><li><a href="https://docs.kernel.org/power/runtime_pm.html">Linux Runtime Power Management Framework</a></li><li><a href="https://docs.kernel.org/driver-api/pm/devices.html">Linux Device Power Management Basics</a></li><li><a href="https://www.freertos.org/Documentation/02-Kernel/02-Kernel-features/07-Lower-power-support">FreeRTOS Low Power Support and Tickless Idle</a></li><li><a href="https://www.rt-thread.io/document/site/programming-manual/pm/pm/">RT-Thread Power Management</a></li><li><a href="https://nuttx.apache.org/docs/latest/components/drivers/special/power/pm/index.html">Apache NuttX Power Management</a></li><li><a href="https://nuttx.apache.org/docs/latest/implementation/power_management.html">Apache NuttX Power Management Implementation Notes</a></li><li><a href="https://docs.espressif.com/projects/esp-idf/en/latest/esp32/api-reference/system/sleep_modes.html">ESP-IDF Sleep Modes</a></li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;meta name=&quot;referrer&quot; content=&quot;no-referrer&quot; /&gt;

&lt;h1 id=&quot;嵌入式面试真题第-17-题：资源受限系统中的分层低功耗与低延迟唤醒架构设计&quot;&gt;&lt;a href=&quot;#嵌入式面试真题第-17-题：资源受限系统中的分层低功耗与低延迟唤醒架构</summary>
      
    
    
    
    <category term="嵌入式面试真题" scheme="https://wdfk-prog.space/categories/%E5%B5%8C%E5%85%A5%E5%BC%8F%E9%9D%A2%E8%AF%95%E7%9C%9F%E9%A2%98/"/>
    
    
    <category term="嵌入式面试真题" scheme="https://wdfk-prog.space/tags/%E5%B5%8C%E5%85%A5%E5%BC%8F%E9%9D%A2%E8%AF%95%E7%9C%9F%E9%A2%98/"/>
    
  </entry>
  
  <entry>
    <title>嵌入式设备休眠电流异常的通用定位与低功耗治理</title>
    <link href="https://wdfk-prog.space/posts/48ed9469/"/>
    <id>https://wdfk-prog.space/posts/48ed9469/</id>
    <published>2026-07-24T01:21:53.000Z</published>
    <updated>2026-07-24T01:23:23.930Z</updated>
    
    <content type="html"><![CDATA[<meta name="referrer" content="no-referrer" /><h1 id="嵌入式面试真题第-16-题：嵌入式设备休眠电流异常的通用定位与低功耗治理"><a href="#嵌入式面试真题第-16-题：嵌入式设备休眠电流异常的通用定位与低功耗治理" class="headerlink" title="嵌入式面试真题第 16 题：嵌入式设备休眠电流异常的通用定位与低功耗治理"></a>嵌入式面试真题第 16 题：嵌入式设备休眠电流异常的通用定位与低功耗治理</h1><p><img src="https://i-blog.csdnimg.cn/direct/edb9afff32504b02958a538d412b6bfe.png" alt="在这里插入图片描述"></p><h2 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h2><p>在可穿戴设备、无线传感器、资产追踪器、智能门锁、便携医疗设备、TWS&#x2F;骨传导耳机、遥控器、仪表终端、数据采集节点等电池供电系统中，产品通常要求设备在待机、休眠或运输模式下进入微安级甚至更低的功耗状态。</p><p>现在某台样机在功能上没有明显异常，但静态电流或长时间平均电流明显高于预算。例如，设计目标是几十微安，实测却达到数百微安甚至 1mA～数毫安。系统可能包含 MCU&#x2F;SoC、RTOS、多个传感器、Codec、Flash、无线芯片、充电与电量计、LDO&#x2F;DC-DC、负载开关、调试接口以及多个独立电源域。</p><p>你会如何建立一套通用、可复用的休眠电流异常定位方法？要求同时覆盖：</p><ol><li>如何确认测量结果可信，并区分稳定漏电、周期唤醒和量程伪差；</li><li>如何通过最小固件、功耗状态机和二分法划分硬件问题与软件问题；</li><li>如何系统检查 GPIO 反向供电、上下拉冲突、外部漏流电阻、模拟输入、调试接口和未断电外设；</li><li>如何检查 MCU&#x2F;SoC 的时钟树、电源域、RAM 保持、唤醒源、RTOS Tick 和中断 pending；</li><li>如何把一次性的“人工排查”沉淀为可持续的低功耗架构、自动化回归和量产测试方案；</li><li>有哪些 Linux、Zephyr、FreeRTOS、ESP-IDF、CMSIS 等开源或开放方案可以参考，它们各自解决了什么问题。</li></ol><h2 id="回答"><a href="#回答" class="headerlink" title="回答"></a>回答</h2><p>结论：休眠电流从微安级异常升高到数百微安或毫安级时，通常不是“正常波动”，而是某个电源状态没有闭合。常见根因包括：设备没有真正进入目标低功耗模式、某个时钟或电源域仍在运行、外设未进入 suspend&#x2F;deep power-down、GPIO 对已断电器件反向供电、上下拉形成直流通路、唤醒源持续触发、调试器阻止深睡，或板级器件本身存在静态漏流。</p><p>最有效的处理方式不是逐项猜测，而是把问题转化为一个可测量、可分层、可回退的状态闭环：</p><ol><li>先建立可信的电流波形和功耗预算；</li><li>再用最小固件把问题切成“板级静态漏流”与“业务软件未关断”；</li><li>按电源域、设备、GPIO、时钟和唤醒源逐层二分；</li><li>每次只改变一个变量，记录电流增量与状态证据；</li><li>最后把修复固化为统一的 suspend&#x2F;resume 框架、资源引用计数、低功耗自检和回归阈值。</li></ol><p>这类问题的核心不是“把 MCU 执行一条 <code>WFI</code> 就算完成”，而是验证整机是否从运行态完整迁移到目标电源态。CPU 休眠、外设挂起、时钟门控、电源域关闭、引脚静态状态、外部器件深睡和唤醒条件必须同时满足，任何一层没有闭合，都可能让整机停留在毫安级。</p><h2 id="总体诊断架构"><a href="#总体诊断架构" class="headerlink" title="总体诊断架构"></a>总体诊断架构</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[发现待机电流超预算] --&gt; B[建立可信测量基线]</span><br><span class="line">    B --&gt; C&#123;稳定直流还是周期脉冲?&#125;</span><br><span class="line">    C --&gt;|稳定直流| D[优先查静态漏流/未关断电源域]</span><br><span class="line">    C --&gt;|周期脉冲| E[优先查唤醒源/定时器/任务/无线维护]</span><br><span class="line">    C --&gt;|量程跳变或不可信| F[修正仪表接法与采样带宽]</span><br><span class="line"></span><br><span class="line">    D --&gt; G[最小固件 A/B]</span><br><span class="line">    E --&gt; G</span><br><span class="line">    F --&gt; B</span><br><span class="line"></span><br><span class="line">    G --&gt; H&#123;最小固件是否仍超标?&#125;</span><br><span class="line">    H --&gt;|是| I[硬件路径: 电源域/GPIO/电阻/器件/IQ/焊接]</span><br><span class="line">    H --&gt;|否| J[软件路径: 驱动/时钟/RTOS/唤醒/日志/资源锁]</span><br><span class="line"></span><br><span class="line">    I --&gt; K[逐电源域断开或旁路测量]</span><br><span class="line">    I --&gt; L[GPIO 状态矩阵与反灌检查]</span><br><span class="line">    I --&gt; M[器件静态电流与电阻网络核算]</span><br><span class="line"></span><br><span class="line">    J --&gt; N[逐模块 suspend 二分]</span><br><span class="line">    J --&gt; O[时钟树与低功耗模式核验]</span><br><span class="line">    J --&gt; P[唤醒原因与中断 pending 统计]</span><br><span class="line"></span><br><span class="line">    K --&gt; Q[形成电流增量表]</span><br><span class="line">    L --&gt; Q</span><br><span class="line">    M --&gt; Q</span><br><span class="line">    N --&gt; Q</span><br><span class="line">    O --&gt; Q</span><br><span class="line">    P --&gt; Q</span><br><span class="line"></span><br><span class="line">    Q --&gt; R[锁定根因并修复]</span><br><span class="line">    R --&gt; S[自动化回归/量产阈值/状态自检]</span><br></pre></td></tr></table></figure><p>诊断过程中要始终保留两条证据链：</p><ul><li><strong>电气证据链</strong>：电流波形、各电源域电流、关键引脚电压、静态电阻、时钟或中断活动；</li><li><strong>软件证据链</strong>：目标电源状态、设备 suspend 结果、资源引用计数、时钟门控寄存器、唤醒原因、实际睡眠驻留时间。</li></ul><p>只有当“电流下降”和“状态证据”同时成立，才能确认某个修改真正解决了问题。单纯看到平均电流下降，可能只是改变了周期或掩盖了唤醒峰值；单纯看到软件返回成功，也不能证明外部器件确实进入了低功耗状态。</p><h2 id="机制与开源方案的对应关系"><a href="#机制与开源方案的对应关系" class="headerlink" title="机制与开源方案的对应关系"></a>机制与开源方案的对应关系</h2><table><thead><tr><th>本题机制</th><th>开源或开放方案</th><th>能否直接使用</th><th>主要参考价值</th></tr></thead><tbody><tr><td>CPU 进入等待&#x2F;深睡</td><td><a href="https://arm-software.github.io/CMSIS_6/main/Core/group__intrinsic__CPU__gr.html">Arm CMSIS-Core <code>__WFI()</code> &#x2F; <code>__WFE()</code></a></td><td>Cortex-M 项目可直接使用基础指令封装，但具体深睡仍依赖 SoC 配置</td><td>区分“CPU 停止取指”和“整机进入深功耗状态”；统一访问 NVIC、SCB、SysTick 等核心寄存器。</td></tr><tr><td>RTOS 无 Tick 空闲</td><td><a href="https://www.freertos.org/Documentation/02-Kernel/02-Kernel-features/07-Lower-power-support">FreeRTOS Low Power Support</a></td><td>支持对应移植层的工程可直接启用</td><td>通过 <code>configUSE_TICKLESS_IDLE</code> 和 <code>portSUPPRESS_TICKS_AND_SLEEP()</code> 停止周期 Tick，延长连续休眠时间。</td></tr><tr><td>系统级电源状态选择</td><td><a href="https://docs.zephyrproject.org/latest/services/pm/system.html">Zephyr System Power Management</a></td><td>Zephyr 工程可直接使用</td><td>按预计空闲时间、最小驻留时间和退出延迟选择电源状态，并允许策略锁阻止不安全的深睡。</td></tr><tr><td>设备运行时挂起</td><td><a href="https://docs.zephyrproject.org/latest/services/pm/device_runtime.html">Zephyr Device Runtime PM</a></td><td>Zephyr 工程可直接使用</td><td>用引用计数管理设备 active&#x2F;suspended，避免多个使用者互相误关电或忘记释放设备。</td></tr><tr><td>设备电源状态调试</td><td><a href="https://docs.zephyrproject.org/latest/services/pm/device.html">Zephyr Device PM Shell</a></td><td>开启相关 Kconfig 后可直接使用</td><td>通过 shell 查看设备状态、usage count，并人工触发 suspend&#x2F;resume，适合构建功耗二分测试。</td></tr><tr><td>频率与睡眠约束</td><td><a href="https://docs.espressif.com/projects/esp-idf/en/stable/esp32/api-reference/system/power_management.html">ESP-IDF Power Management Locks</a></td><td>ESP-IDF 工程可直接使用</td><td>用资源锁表达“必须保持高频&#x2F;APB&#x2F;禁止 Light-sleep”的约束，锁未释放就是常见高功耗根因。</td></tr><tr><td>IO 隔离与电源域控制</td><td><a href="https://docs.espressif.com/projects/esp-idf/en/stable/esp32/api-reference/system/sleep_modes.html">ESP-IDF Sleep Modes</a></td><td>ESP-IDF 工程可直接使用</td><td>提供唤醒源、RTC 电源域、Flash&#x2F;PSRAM 低功耗和 GPIO isolate 等机制，直接对应反灌与上下拉冲突。</td></tr><tr><td>复杂系统设备运行时 PM</td><td><a href="https://docs.kernel.org/power/runtime_pm.html">Linux Runtime PM</a></td><td>Linux 驱动可直接使用；裸机&#x2F;RTOS 可参考设计</td><td><code>runtime_suspend</code>、<code>runtime_resume</code>、usage counter、autosuspend 和父子依赖是通用设备电源管理模型。</td></tr><tr><td>共享电源域建模</td><td><a href="https://docs.kernel.org/driver-api/pm/devices.html">Linux Device Power Management Basics</a></td><td>Linux 驱动可直接使用；多电源域 RTOS 可参考</td><td>将共享电源资源的多个设备组织成可嵌套 power domain，统一执行域级 suspend&#x2F;resume。</td></tr><tr><td>电源轨引用计数</td><td><a href="https://docs.kernel.org/driver-api/regulator.html">Linux Regulator Framework</a></td><td>Linux 驱动可直接使用；小系统可参考</td><td>把稳压器建模为 provider，把设备建模为 consumer，通过 enable count 和约束避免错误断电或常开。</td></tr><tr><td>时钟树统一门控</td><td><a href="https://docs.kernel.org/driver-api/clk.html">Linux Common Clock Framework</a></td><td>Linux 驱动可直接使用；MCU BSP 可参考</td><td>建立时钟拓扑、父子关系和 enable&#x2F;disable 引用，防止驱动私自开时钟后忘记关闭。</td></tr><tr><td>音频路径动态电源管理</td><td><a href="https://docs.kernel.org/sound/soc/dapm.html">Linux ASoC DAPM</a></td><td>Linux 音频系统可直接使用；Codec&#x2F;耳机类 MCU 项目可参考</td><td>按真实音频信号路径自动开启或关闭 Codec bias、mixer、ADC&#x2F;DAC、放大器等组件，避免整条音频域常开。</td></tr><tr><td>电源状态追踪</td><td><a href="https://docs.kernel.org/trace/events-power.html">Linux Power Tracepoints</a></td><td>Linux 可直接使用；RTOS 可仿照事件模型</td><td>记录 CPU idle、clock enable&#x2F;disable、频率和 power domain 状态变化，便于把软件事件与功耗波形对齐。</td></tr></tbody></table><p>这些方案并不是同一层级的替代品。CMSIS 解决 CPU 指令和核心寄存器访问；FreeRTOS Tickless Idle 解决周期 Tick 阻碍长时间空闲；Zephyr 和 Linux 进一步把设备、时钟、电源域和系统状态组织成可协调的框架；ESP-IDF 则展示了资源锁、睡眠电源域与 GPIO 隔离在具体 SoC 上如何落地。</p><h2 id="先确认测量结果是否可信"><a href="#先确认测量结果是否可信" class="headerlink" title="先确认测量结果是否可信"></a>先确认测量结果是否可信</h2><h3 id="不要只看一个“2mA”数字"><a href="#不要只看一个“2mA”数字" class="headerlink" title="不要只看一个“2mA”数字"></a>不要只看一个“2mA”数字</h3><p>万用表显示的 2mA 可能代表三种完全不同的问题：</p><ol><li><strong>稳定直流漏流</strong>：例如一个 LDO 未关闭、GPIO 通过 ESD 二极管反向供电、10kΩ 上拉与输出低电平形成恒定通路；</li><li><strong>周期唤醒的平均值</strong>：设备大部分时间只有几十微安，但每隔几十毫秒唤醒一次并持续数毫秒，万用表把脉冲平均成 2mA；</li><li><strong>测量链路伪差</strong>：量程自动切换、串联表内阻导致供电压降、调试器或 USB 形成第二供电路径、仪表采样太慢漏掉峰值。</li></ol><p>因此，第一步要回答的不是“哪个外设没关”，而是：电流随时间是什么形态？</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    A[电流观测] --&gt; B[稳定平台]</span><br><span class="line">    A --&gt; C[周期脉冲]</span><br><span class="line">    A --&gt; D[随机突发]</span><br><span class="line">    A --&gt; E[量程切换台阶]</span><br><span class="line"></span><br><span class="line">    B --&gt; B1[静态漏流/电源域常开/上下拉冲突]</span><br><span class="line">    C --&gt; C1[RTOS Tick/软件定时器/无线维护/传感器采样]</span><br><span class="line">    D --&gt; D1[浮空中断/异常重试/看门狗/通信噪声]</span><br><span class="line">    E --&gt; E1[仪表 burden voltage/自动量程/供电掉压]</span><br></pre></td></tr></table></figure><h3 id="推荐的测量组合"><a href="#推荐的测量组合" class="headerlink" title="推荐的测量组合"></a>推荐的测量组合</h3><table><thead><tr><th>工具</th><th>适合观察</th><th>局限</th></tr></thead><tbody><tr><td>普通数字万用表</td><td>稳定直流、粗略比较修改前后差异</td><td>采样慢，容易把脉冲平均；自动量程会引入跳变。</td></tr><tr><td>高精度万用表&#x2F;微安表</td><td>微安级稳定电流、静态漏流</td><td>大电流脉冲可能超量程；串联压降需要评估。</td></tr><tr><td>功耗分析仪</td><td>从纳安&#x2F;微安到毫安的动态波形、积分电荷、事件对齐</td><td>成本较高，需要正确设置采样率与量程。</td></tr><tr><td>分流电阻 + 示波器</td><td>快速脉冲、唤醒持续时间、周期、峰值</td><td>小电流分辨率受噪声限制；分流电阻过大会改变供电。</td></tr><tr><td>电源轨串联跳线&#x2F;0Ω 电阻</td><td>单独测某个电源域</td><td>需要硬件预留，量产板可能不便操作。</td></tr></tbody></table><h3 id="测量接法必须排除第二供电路径"><a href="#测量接法必须排除第二供电路径" class="headerlink" title="测量接法必须排除第二供电路径"></a>测量接法必须排除第二供电路径</h3><p>调试器、USB-UART、USB 数据线、示波器地、外部传感器板、充电线和治具都可能通过 IO、VBUS、ESD 保护或地线形成额外供电路径。低功耗测试时应明确：</p><ul><li>电池或实验电源是否是唯一电源；</li><li>SWD&#x2F;JTAG&#x2F;VCOM 是否会给目标板供电；</li><li>UART RX&#x2F;TX 在主板掉电或外设掉电时是否仍被外部拉高；</li><li>USB VBUS 检测电阻、Type-C CC 电阻或接口保护器件是否在消耗静态电流；</li><li>外接逻辑分析仪是否通过输入保护结构向目标 IO 注入电流。</li></ul><p>如果必须保留调试链路，应使用隔离缓冲、串联电阻、可断开的调试电源，或者只在进入休眠前记录状态，随后物理断开调试器再测量。</p><h2 id="建立功耗预算，而不是只设一个总阈值"><a href="#建立功耗预算，而不是只设一个总阈值" class="headerlink" title="建立功耗预算，而不是只设一个总阈值"></a>建立功耗预算，而不是只设一个总阈值</h2><p>低功耗定位必须先知道“理论上哪些器件应该消耗多少电流”。建议按电源域建立预算表。</p><table><thead><tr><th>电源域&#x2F;器件</th><th>目标状态</th><th align="right">典型预算项</th><th>需要核对的证据</th></tr></thead><tbody><tr><td>MCU&#x2F;SoC 核心域</td><td>STOP&#x2F;STANDBY&#x2F;Deep-sleep</td><td align="right">核心静态电流、保留 RAM、RTC、BOR</td><td>PWR&#x2F;PMU 状态、时钟寄存器、唤醒配置。</td></tr><tr><td>传感器域</td><td>suspend&#x2F;shutdown</td><td align="right">数据手册 shutdown current</td><td>状态寄存器、INT 引脚、供电轨电流。</td></tr><tr><td>音频&#x2F;Codec 域</td><td>soft power-down 或断电</td><td align="right">Codec、MIC bias、耳放、PLL</td><td>reset&#x2F;power register、MCLK、LDO 使能。</td></tr><tr><td>存储域</td><td>deep power-down</td><td align="right">NOR Flash、PSRAM、EEPROM</td><td>DPD 命令、CS&#x2F;SCK 状态、退出时序。</td></tr><tr><td>无线域</td><td>off&#x2F;modem sleep&#x2F;deep sleep</td><td align="right">RF、基带、晶振、保活周期</td><td>协议栈状态、RF 电源、唤醒周期。</td></tr><tr><td>电源管理域</td><td>always-on</td><td align="right">LDO IQ、DC-DC PFM、load switch leakage</td><td>器件 IQ、EN 电平、输出放电电阻。</td></tr><tr><td>接口与保护</td><td>高阻&#x2F;断开</td><td align="right">USB、ESD、level shifter、调试器</td><td>IO 电压、外部上拉、第二供电路径。</td></tr></tbody></table><p>整机稳定休眠电流可近似写成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">I_sleep_total = I_mcu_sleep</span><br><span class="line">              + ΣI_power_domain_on</span><br><span class="line">              + ΣI_peripheral_sleep</span><br><span class="line">              + ΣI_regulator_iq</span><br><span class="line">              + ΣI_gpio_leak</span><br><span class="line">              + ΣI_resistor_path</span><br><span class="line">              + I_board_leak</span><br><span class="line">              + I_measurement_path</span><br></pre></td></tr></table></figure><p>若存在周期唤醒，平均电流应按电荷或占空比计算：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">I_avg = (I_active × T_active + I_sleep × T_sleep) / (T_active + T_sleep)</span><br></pre></td></tr></table></figure><p>例如设备每 100ms 唤醒一次，活动电流 20mA，活动时间 10ms，休眠电流 20µA：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">I_avg ≈ (20mA × 10ms + 0.02mA × 90ms) / 100ms</span><br><span class="line">      ≈ 2.018mA</span><br></pre></td></tr></table></figure><p>万用表可能稳定显示约 2mA，但真正问题不是“存在 2mA 静态漏电”，而是设备每 100ms 被唤醒一次。此时盲目查电阻漏流很可能走错方向。</p><h2 id="最小固件是划分硬件与软件的第一条边界"><a href="#最小固件是划分硬件与软件的第一条边界" class="headerlink" title="最小固件是划分硬件与软件的第一条边界"></a>最小固件是划分硬件与软件的第一条边界</h2><h3 id="最小固件应该做什么"><a href="#最小固件应该做什么" class="headerlink" title="最小固件应该做什么"></a>最小固件应该做什么</h3><p>最小固件不是把业务代码中的任务删掉几个，而是构造一个可证明的最低功耗状态：</p><ol><li>启动后只完成必要的时钟与电源初始化；</li><li>禁止非必要外设和 DMA；</li><li>把 GPIO 配置到审查过的休眠状态；</li><li>清除并只保留一个可控唤醒源；</li><li>关闭 SysTick 或启用正确的 Tickless 逻辑；</li><li>配置目标 STOP&#x2F;STANDBY&#x2F;Deep-sleep 状态；</li><li>进入低功耗后不再打印日志；</li><li>使用一个测试 GPIO 在睡眠前后翻转，供示波器对齐。</li></ol><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant Boot as 启动代码</span><br><span class="line">    participant PM as 低功耗准备</span><br><span class="line">    participant HW as MCU/外设</span><br><span class="line">    participant Meter as 电流测量</span><br><span class="line"></span><br><span class="line">    Boot-&gt;&gt;PM: 初始化最小必需资源</span><br><span class="line">    PM-&gt;&gt;HW: 关闭外设/时钟/电源域</span><br><span class="line">    PM-&gt;&gt;HW: 配置 GPIO 休眠状态</span><br><span class="line">    PM-&gt;&gt;HW: 清中断 pending，仅保留测试唤醒源</span><br><span class="line">    PM-&gt;&gt;HW: 翻转 TEST_MARKER</span><br><span class="line">    PM-&gt;&gt;HW: 进入目标低功耗状态</span><br><span class="line">    HW--&gt;&gt;Meter: 形成稳定休眠电流平台</span><br><span class="line">    HW--&gt;&gt;PM: 可控唤醒事件</span><br><span class="line">    PM-&gt;&gt;HW: 记录唤醒原因到保留 RAM</span><br></pre></td></tr></table></figure><h3 id="最小固件的判定逻辑"><a href="#最小固件的判定逻辑" class="headerlink" title="最小固件的判定逻辑"></a>最小固件的判定逻辑</h3><table><thead><tr><th>结果</th><th>主要判断</th><th>下一步</th></tr></thead><tbody><tr><td>最小固件仍然明显超标</td><td>更偏向板级静态漏流、默认引脚状态、外部器件未关断、LDO IQ、焊接或器件异常</td><td>逐电源域、GPIO、电阻网络和器件断开。</td></tr><tr><td>最小固件达到预算，业务固件超标</td><td>更偏向驱动未 suspend、引用计数未释放、RTOS 周期唤醒、日志或通信栈活动</td><td>对业务模块做二分屏蔽并检查状态机。</td></tr><tr><td>最小固件偶发超标</td><td>可能有浮空唤醒脚、复位&#x2F;启动时序、外部器件状态不确定或测量重复性差</td><td>固定启动条件，记录波形与唤醒原因，增加多次统计。</td></tr><tr><td>连接调试器超标，断开后正常</td><td>调试域、SWD 时钟、DBGMCU 配置或调试器反向供电影响</td><td>采用脱机测量，并核对 debug-in-sleep 设置。</td></tr></tbody></table><p>最小固件测试的价值在于把问题空间从“所有业务代码与所有硬件”压缩到“最小板级静态状态”。如果连最小固件都无法达到预算，就不应继续在应用任务优先级、消息队列或业务逻辑上耗费时间。</p><h2 id="GPIO-是毫安级异常的高频根因"><a href="#GPIO-是毫安级异常的高频根因" class="headerlink" title="GPIO 是毫安级异常的高频根因"></a>GPIO 是毫安级异常的高频根因</h2><h3 id="反向供电的基本路径"><a href="#反向供电的基本路径" class="headerlink" title="反向供电的基本路径"></a>反向供电的基本路径</h3><p>当外设电源域关闭，但 MCU 仍通过通信引脚输出高电平时，电流可能经外设 IO 保护结构流入外设 VDD，形成反向供电。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    VMCU[MCU IO = High] --&gt; R[串联电阻/走线阻抗]</span><br><span class="line">    R --&gt; PAD[已断电外设 IO PAD]</span><br><span class="line">    PAD --&gt; D[ESD/钳位二极管]</span><br><span class="line">    D --&gt; VDD[外设 VDD = Off]</span><br><span class="line">    VDD --&gt; LOAD[外设内部电路]</span><br><span class="line">    LOAD --&gt; GND</span><br></pre></td></tr></table></figure><p>这种情况下，外设的 VDD 可能被抬到 0.5V～数伏之间，整机电流可能达到数百微安或毫安。即使外设数据手册标称 shutdown current 只有 1µA，只要它被 IO 反向供电，数据手册的正常 shutdown 条件就不再成立。</p><h3 id="上下拉冲突可以直接算出电流"><a href="#上下拉冲突可以直接算出电流" class="headerlink" title="上下拉冲突可以直接算出电流"></a>上下拉冲突可以直接算出电流</h3><p>如果某引脚外部有 10kΩ 上拉到 3.3V，而 MCU 在休眠时输出低电平，则静态电流约为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">I = V / R = 3.3V / 10kΩ = 330µA</span><br></pre></td></tr></table></figure><p>三个类似引脚就接近 1mA。若内部还启用了 30kΩ～50kΩ 的相反方向上下拉，也会形成额外通路。对毫安级问题，优先检查 1kΩ～100kΩ 范围内的常态电阻网络通常很有效。</p><h3 id="建立-GPIO-休眠状态矩阵"><a href="#建立-GPIO-休眠状态矩阵" class="headerlink" title="建立 GPIO 休眠状态矩阵"></a>建立 GPIO 休眠状态矩阵</h3><p>不要只说“无用 GPIO 全部设为模拟输入”。通用方法应按每个引脚的电气连接和外部器件供电状态确定休眠配置。</p><table><thead><tr><th>引脚类别</th><th>外部状态</th><th>推荐休眠配置</th><th>风险</th></tr></thead><tbody><tr><td>完全悬空、无外部连接</td><td>无确定电平</td><td>模拟输入或数字输入禁用、无上下拉</td><td>数字输入缓冲若停在阈值附近可能产生内部翻转电流。</td></tr><tr><td>外部固定上拉</td><td>外部器件同电源域且保持供电</td><td>输入无下拉，或输出高但必须确认无冲突</td><td>内部下拉会形成直流通路。</td></tr><tr><td>外部固定下拉</td><td>外部器件同电源域且保持供电</td><td>输入无上拉，或输出低但必须确认无冲突</td><td>内部上拉会形成直流通路。</td></tr><tr><td>连接已断电外设</td><td>外设 VDD &#x3D; 0</td><td>高阻、模拟输入、必要时隔离或先拉到安全电平再断电</td><td>输出高&#x2F;低都可能经保护结构反灌。</td></tr><tr><td>I2C SDA&#x2F;SCL</td><td>外部上拉所在电源域可能关闭</td><td>先确保总线空闲，再输入高阻；必要时开关上拉电源</td><td>外部上拉可能给已断电器件供电。</td></tr><tr><td>SPI CS&#x2F;SCK&#x2F;MOSI</td><td>Flash&#x2F;传感器可能断电</td><td>CS 置安全态后高阻；SCK&#x2F;MOSI 避免向断电域输出</td><td>CS 漂移可能误唤醒，MOSI&#x2F;SCK 可能反灌。</td></tr><tr><td>中断&#x2F;唤醒输入</td><td>外设保持供电</td><td>配置确定的上下拉和正确触发方式</td><td>浮空或电平型中断持续有效会反复唤醒。</td></tr><tr><td>模拟输入&#x2F;ADC</td><td>传感器可能断电</td><td>关闭数字输入缓冲，确认外部电压不超过掉电域限制</td><td>外部电压可通过模拟保护结构注入。</td></tr><tr><td>SWD&#x2F;JTAG&#x2F;UART</td><td>调试器可能连接</td><td>量产模式关闭或高阻，必要时物理断开</td><td>调试域保持、外部上拉和第二供电路径。</td></tr></tbody></table><p>建议为每个板级引脚建立可审计表：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">pin_name</span><br><span class="line">active_mode</span><br><span class="line">sleep_mode</span><br><span class="line">internal_pull</span><br><span class="line">external_pull</span><br><span class="line">external_power_domain</span><br><span class="line">wakeup_capable</span><br><span class="line">safe_level_before_power_off</span><br><span class="line">restore_order_after_power_on</span><br><span class="line">owner_driver</span><br></pre></td></tr></table></figure><p>GPIO 低功耗配置不能散落在多个驱动里互相覆盖。更可靠的方式是维护“运行态 pin state”和“休眠态 pin state”，由统一的 pinctrl 或 board power manager 在状态切换时一次性应用，并在编译期或启动时检查关键引脚是否有重复所有者。</p><h2 id="外部器件和电源域要按依赖顺序关闭"><a href="#外部器件和电源域要按依赖顺序关闭" class="headerlink" title="外部器件和电源域要按依赖顺序关闭"></a>外部器件和电源域要按依赖顺序关闭</h2><h3 id="软休眠不等于真正断电"><a href="#软休眠不等于真正断电" class="headerlink" title="软休眠不等于真正断电"></a>软休眠不等于真正断电</h3><p>外设通常有多级低功耗状态：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">stateDiagram-v2</span><br><span class="line">    [*] --&gt; Active</span><br><span class="line">    Active --&gt; Idle: 无业务但时钟仍开</span><br><span class="line">    Idle --&gt; Suspend: 驱动停止采样/传输</span><br><span class="line">    Suspend --&gt; DeepPowerDown: 器件内部大部分模块关闭</span><br><span class="line">    DeepPowerDown --&gt; PowerOff: 负载开关/LDO 关闭</span><br><span class="line"></span><br><span class="line">    PowerOff --&gt; DeepPowerDown: 上电并等待稳定</span><br><span class="line">    DeepPowerDown --&gt; Suspend: 退出 DPD/恢复寄存器</span><br><span class="line">    Suspend --&gt; Active: 恢复数据路径</span><br></pre></td></tr></table></figure><p>不同状态的电流可能相差几个数量级。驱动仅停止 DMA 或停止读取数据，并不代表器件已进入 deep power-down；写了休眠寄存器，也不代表外部 MCLK、晶振、MIC bias、模拟 LDO 或电荷泵已经关闭。</p><h3 id="关闭顺序必须避免反灌和总线异常"><a href="#关闭顺序必须避免反灌和总线异常" class="headerlink" title="关闭顺序必须避免反灌和总线异常"></a>关闭顺序必须避免反灌和总线异常</h3><p>推荐的通用关闭顺序是：</p><ol><li>停止上层业务请求，阻止新事务进入；</li><li>等待当前 I&#x2F;O、DMA、Flash 写入或音频帧结束；</li><li>关闭中断或把中断脚切到不会持续触发的状态；</li><li>向器件发送 suspend&#x2F;deep power-down 命令；</li><li>停止外部时钟、MCLK、PWM 和总线活动；</li><li>将跨电源域 GPIO 切到安全高阻或隔离状态；</li><li>关闭负载开关、LDO 或电源域；</li><li>检查电源良好信号和目标电流是否下降。</li></ol><p>恢复顺序通常相反，但要满足器件数据手册规定的电源稳定、复位、时钟和命令时序。若恢复顺序不正确，设备可能出现偶发初始化失败，最终又通过重试任务造成周期高功耗。</p><h3 id="逐电源域测量比整机猜测更高效"><a href="#逐电源域测量比整机猜测更高效" class="headerlink" title="逐电源域测量比整机猜测更高效"></a>逐电源域测量比整机猜测更高效</h3><p>硬件设计阶段应给关键电源域预留 0Ω 电阻、测流焊盘或可断跳线：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    BAT[电池/系统电源] --&gt; PMIC[PMIC/DC-DC]</span><br><span class="line">    PMIC --&gt; AON[Always-On 域]</span><br><span class="line">    PMIC --&gt; MCU[MCU 核心域]</span><br><span class="line">    PMIC --&gt; SENSOR[传感器域]</span><br><span class="line">    PMIC --&gt; AUDIO[音频域]</span><br><span class="line">    PMIC --&gt; RADIO[无线域]</span><br><span class="line">    PMIC --&gt; STORAGE[存储域]</span><br><span class="line"></span><br><span class="line">    AON --&gt; M1[测流点]</span><br><span class="line">    MCU --&gt; M2[测流点]</span><br><span class="line">    SENSOR --&gt; M3[测流点]</span><br><span class="line">    AUDIO --&gt; M4[测流点]</span><br><span class="line">    RADIO --&gt; M5[测流点]</span><br><span class="line">    STORAGE --&gt; M6[测流点]</span><br></pre></td></tr></table></figure><p>如果整机多出 1.8mA，而单独测得音频域多出 1.6mA，就可以快速把范围缩小到 Codec、耳放、MIC bias、MCLK 或音频 LDO，不必在所有 MCU 中断和传感器驱动上同时排查。</p><h2 id="电阻、LDO-和板级静态路径不能忽略"><a href="#电阻、LDO-和板级静态路径不能忽略" class="headerlink" title="电阻、LDO 和板级静态路径不能忽略"></a>电阻、LDO 和板级静态路径不能忽略</h2><h3 id="常见固定漏流路径"><a href="#常见固定漏流路径" class="headerlink" title="常见固定漏流路径"></a>常见固定漏流路径</h3><ul><li>电池电压分压网络长期接通；</li><li>NTC、按键、电阻编码、霍尔检测或插入检测形成常流；</li><li>LED 指示灯、三极管基极电阻、MOS 栅极下拉过小；</li><li>USB VBUS 检测、Type-C CC、电平转换器 OE 引脚或保护器件；</li><li>LDO 静态电流过高，或在轻载时没有进入低 IQ 模式；</li><li>DC-DC 在 PFM&#x2F;PWM 模式选择不当，轻载效率很差；</li><li>负载开关具有较大的 off leakage 或输出放电电阻；</li><li>焊剂残留、潮湿、PCB 污染、器件损伤或 ESD 造成异常漏电；</li><li>测试点、治具、飞线或外接模块形成额外通路。</li></ul><h3 id="用静态电阻估算可疑路径"><a href="#用静态电阻估算可疑路径" class="headerlink" title="用静态电阻估算可疑路径"></a>用静态电阻估算可疑路径</h3><p>在断电且电容放电后，可以测量可疑电源域对地、电池对地、IO 对已断电 VDD 的等效电阻。但要注意半导体器件具有非线性，万用表电阻档的测试电压也可能使保护二极管导通，因此电阻测量只能作为筛选，不应直接等同于工作状态电流。</p><p>更可靠的方法是结合工作电压估算：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">I_path ≈ (V_source - V_sink - V_diode) / R_path</span><br></pre></td></tr></table></figure><p>例如 3.3V 通过 1kΩ 串联电阻向已断电域注入，考虑约 0.3V～0.7V 的钳位压降，电流仍可能达到数毫安。若串联电阻是 100kΩ，则通常只有几十微安，量级判断可以帮助排序排查优先级。</p><h3 id="LDO-静态电流要计入预算"><a href="#LDO-静态电流要计入预算" class="headerlink" title="LDO 静态电流要计入预算"></a>LDO 静态电流要计入预算</h3><p>很多工程只看 LDO 输出负载，却忽略 LDO 自身 quiescent current。一个常开的高 IQ LDO 即使输出没有负载，也可能消耗几十微安到数百微安。多个 LDO 叠加后，MCU 已经进入 2µA 的深睡也无法让整机达到目标。</p><p>选型时应同时看：</p><ul><li>静态电流和关断电流；</li><li>轻载稳定性；</li><li>EN 引脚默认状态；</li><li>反向电流保护；</li><li>输出放电功能；</li><li>启动时间和浪涌；</li><li>低温、高温、输入电压和工艺角下的最大值，而不是只看典型值。</li></ul><h2 id="MCU-SoC-的时钟树与电源状态必须可证明"><a href="#MCU-SoC-的时钟树与电源状态必须可证明" class="headerlink" title="MCU&#x2F;SoC 的时钟树与电源状态必须可证明"></a>MCU&#x2F;SoC 的时钟树与电源状态必须可证明</h2><h3 id="WFI-只说明-CPU-等待中断"><a href="#WFI-只说明-CPU-等待中断" class="headerlink" title="WFI 只说明 CPU 等待中断"></a><code>WFI</code> 只说明 CPU 等待中断</h3><p>在 Cortex-M 上，CMSIS 的 <code>__WFI()</code> 会执行 Wait For Interrupt 指令，使处理器暂停执行直到满足唤醒条件。但最终进入的是普通 sleep 还是 deep sleep，哪些时钟、电源域、RAM 和外设仍保持，取决于 SCB、SoC PMU&#x2F;PWR、时钟配置以及芯片具体实现。</p><p>因此，看到代码执行 <code>__WFI()</code> 不能直接推出“整机已经进入最低功耗”。常见失败包括：</p><ul><li><code>SLEEPDEEP</code> 未设置，实际只进入浅睡；</li><li>某个高速时钟、PLL、HSE、USB 或外部晶振仍保持；</li><li>调试配置允许 sleep 但禁止 STOP&#x2F;STANDBY；</li><li>某外设声明 busy，系统策略退化到较浅状态；</li><li>RAM 保持范围过大；</li><li>BOR、内部参考源、ADC、比较器或模拟模块未关闭；</li><li>睡眠刚进入就被 pending 中断立即唤醒。</li></ul><h3 id="建立时钟树审计表"><a href="#建立时钟树审计表" class="headerlink" title="建立时钟树审计表"></a>建立时钟树审计表</h3><table><thead><tr><th>时钟节点</th><th>运行态</th><th>目标休眠态</th><th>保留原因</th><th>验证方式</th></tr></thead><tbody><tr><td>主 PLL</td><td>On</td><td>Off</td><td>无</td><td>读时钟状态寄存器或测 MCO。</td></tr><tr><td>HSE&#x2F;外部晶振</td><td>On</td><td>Off&#x2F;按需</td><td>无线或 RTC 可能需要</td><td>读取 ready 位，必要时示波器观察。</td></tr><tr><td>LSE&#x2F;RTC 时钟</td><td>On</td><td>On</td><td>唤醒和计时</td><td>记录 RTC 连续性。</td></tr><tr><td>SysTick</td><td>On</td><td>Off</td><td>Tickless 下不应周期唤醒</td><td>检查 CTRL 和唤醒波形。</td></tr><tr><td>UART&#x2F;I2C&#x2F;SPI 时钟</td><td>按需</td><td>Off</td><td>无</td><td>时钟门控寄存器和驱动 usage。</td></tr><tr><td>DMA 时钟</td><td>按需</td><td>Off</td><td>无在途事务</td><td>检查 channel enable&#x2F;flag。</td></tr><tr><td>USB&#x2F;CAN&#x2F;ETH 时钟</td><td>按需</td><td>Off 或保留唤醒</td><td>取决于业务</td><td>检查协议状态与 PM 约束。</td></tr><tr><td>调试时钟</td><td>开发态 On</td><td>量产 Off</td><td>仅调试</td><td>断开调试器复测。</td></tr></tbody></table><h3 id="时钟资源最好使用引用计数"><a href="#时钟资源最好使用引用计数" class="headerlink" title="时钟资源最好使用引用计数"></a>时钟资源最好使用引用计数</h3><p>驱动直接操作 RCC&#x2F;CCU 寄存器很容易出现“开了忘关”或多个驱动互相关闭的问题。Linux Common Clock Framework 的核心价值不是某个具体 API，而是把时钟建模为具有拓扑和引用关系的共享资源：使用者 acquire&#x2F;enable，完成后 disable&#x2F;release；只有最后一个使用者释放后，时钟才能真正关闭。</p><p>在 MCU&#x2F;RTOS 中也可以采用类似结构：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief Acquire a peripheral clock dependency.</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * @param id Clock resource identifier.</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * @return 0 on success, otherwise a negative error code.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pm_clock_get</span><span class="params">(<span class="keyword">enum</span> pm_clock_id id)</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief Release a peripheral clock dependency.</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * @param id Clock resource identifier.</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * @return 0 on success, otherwise a negative error code.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pm_clock_put</span><span class="params">(<span class="keyword">enum</span> pm_clock_id id)</span>;</span><br></pre></td></tr></table></figure><p>调试版本应能导出每个时钟的引用计数和最后一次 acquire 的调用者。休眠前若发现非白名单时钟引用不为零，应拒绝进入深睡并记录原因，而不是静默退化后让团队只看到“电流偏高”。</p><h2 id="RTOS-Tick、定时器和任务是周期唤醒的主要来源"><a href="#RTOS-Tick、定时器和任务是周期唤醒的主要来源" class="headerlink" title="RTOS Tick、定时器和任务是周期唤醒的主要来源"></a>RTOS Tick、定时器和任务是周期唤醒的主要来源</h2><h3 id="Tickless-Idle-的作用与边界"><a href="#Tickless-Idle-的作用与边界" class="headerlink" title="Tickless Idle 的作用与边界"></a>Tickless Idle 的作用与边界</h3><p>FreeRTOS 的低功耗支持通过 <code>configUSE_TICKLESS_IDLE</code> 和 <code>portSUPPRESS_TICKS_AND_SLEEP()</code> 在预计空闲时间足够长时抑制周期 Tick，使 MCU 可以持续停留在深睡状态，直到外部中断或下一个内核超时到期。</p><p>但 Tickless Idle 不能自动解决所有问题：</p><ul><li>任何高频软件定时器都会缩短可睡眠窗口；</li><li>周期轮询任务即使每次只运行几十微秒，也会显著抬高平均电流；</li><li>驱动持有“禁止深睡”锁时，内核只能进入浅睡；</li><li>错误的低功耗定时器补偿会造成时间漂移或频繁提前唤醒；</li><li>中断 pending 或电平型中断未清除会让 <code>WFI</code> 立即返回。</li></ul><h3 id="计算周期任务的真实代价"><a href="#计算周期任务的真实代价" class="headerlink" title="计算周期任务的真实代价"></a>计算周期任务的真实代价</h3><p>假设一个任务每 10ms 唤醒一次，每次运行 0.5ms，活动电流 12mA，休眠电流 20µA：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Duty = 0.5ms / 10ms = 5%</span><br><span class="line">I_avg ≈ 12mA × 5% + 0.02mA × 95%</span><br><span class="line">      ≈ 0.619mA</span><br></pre></td></tr></table></figure><p>仅一个看似很轻的轮询任务就可增加约 0.6mA。多个传感器、日志刷新、LED 状态机、按键扫描和协议保活叠加后，达到 2mA 并不罕见。</p><h3 id="休眠前检查清单"><a href="#休眠前检查清单" class="headerlink" title="休眠前检查清单"></a>休眠前检查清单</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">- 是否存在 ready 状态但未运行的高优先级任务</span><br><span class="line">- 最近一次预计 idle time 是多少</span><br><span class="line">- 下一个软件定时器何时到期</span><br><span class="line">- 是否存在永久 1ms/10ms 轮询任务</span><br><span class="line">- SysTick 是否在目标深睡状态继续运行</span><br><span class="line">- 低功耗定时器是否配置为唯一内核唤醒源</span><br><span class="line">- 是否有 ISR 持续置位事件或信号量</span><br><span class="line">- 是否有 work queue 因重试而持续排队</span><br><span class="line">- 网络/无线协议栈是否需要周期维护</span><br><span class="line">- 看门狗窗口是否迫使系统过于频繁唤醒</span><br></pre></td></tr></table></figure><h3 id="事件驱动优于轮询"><a href="#事件驱动优于轮询" class="headerlink" title="事件驱动优于轮询"></a>事件驱动优于轮询</h3><p>低功耗系统应尽量把周期轮询转换为：</p><ul><li>GPIO&#x2F;传感器中断；</li><li>RTC compare&#x2F;alarm；</li><li>DMA 完成事件；</li><li>外设 FIFO 水位中断；</li><li>批量采样与批量上传；</li><li>按最晚截止时间合并多个软件定时器；</li><li>由 PM policy 选择满足延迟约束的最深状态。</li></ul><p>Zephyr System PM 的 residency policy 提供了一个通用思路：只有当预计空闲时间大于“目标状态最小驻留时间 + 退出延迟”时，才进入更深的状态。这样可以避免频繁进入深睡后立即唤醒，反而增加能耗和延迟。</p><h2 id="唤醒源必须可观测、可计数、可归因"><a href="#唤醒源必须可观测、可计数、可归因" class="headerlink" title="唤醒源必须可观测、可计数、可归因"></a>唤醒源必须可观测、可计数、可归因</h2><h3 id="常见异常唤醒源"><a href="#常见异常唤醒源" class="headerlink" title="常见异常唤醒源"></a>常见异常唤醒源</h3><ul><li>GPIO 浮空、按键抖动、传感器 INT 锁存未清；</li><li>电平型中断在进入睡眠前已经处于有效电平；</li><li>RTC alarm、LPTIM、SysTick 或软件定时器配置错误；</li><li>UART RX 噪声、总线边沿、USB 插拔检测；</li><li>DMA&#x2F;外设完成中断 pending 未清；</li><li>无线协议栈、BLE 连接事件、Wi-Fi beacon、网络保活；</li><li>看门狗喂狗周期过短；</li><li>调试器产生 halt、trace 或 debug entry；</li><li>错误重试状态机持续唤醒。</li></ul><h3 id="不要在刚唤醒时立即用-UART-打印"><a href="#不要在刚唤醒时立即用-UART-打印" class="headerlink" title="不要在刚唤醒时立即用 UART 打印"></a>不要在刚唤醒时立即用 UART 打印</h3><p>UART 日志会开启时钟、保持 IO 活动、延长唤醒时间，并可能改变待测系统本身。推荐将唤醒原因和时间戳写入保留 RAM、RTC backup register 或小型环形缓冲区，等设备进入正常运行态后再批量导出。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief Record a wakeup event without using a blocking peripheral.</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * @param reason Wakeup reason bitmap.</span></span><br><span class="line"><span class="comment"> * @param timestamp Low-power timer timestamp.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="type">static</span> <span class="type">void</span> <span class="title function_">low_power_trace_record</span><span class="params">(<span class="type">uint32_t</span> reason, <span class="type">uint32_t</span> timestamp)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">low_power_trace</span> *<span class="title">entry</span> =</span> trace_ring_claim();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (entry == <span class="literal">NULL</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    entry-&gt;reason = reason;</span><br><span class="line">    entry-&gt;timestamp = timestamp;</span><br><span class="line">    entry-&gt;pending_irq = low_power_get_pending_irq_bitmap();</span><br><span class="line">    trace_ring_finish(entry);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="建立唤醒原因统计"><a href="#建立唤醒原因统计" class="headerlink" title="建立唤醒原因统计"></a>建立唤醒原因统计</h3><table><thead><tr><th>唤醒原因</th><th align="right">次数</th><th align="right">总活动时间</th><th align="right">平均间隔</th><th>允许&#x2F;异常</th></tr></thead><tbody><tr><td>RTC 计划任务</td><td align="right">120</td><td align="right">480ms</td><td align="right">30s</td><td>允许</td></tr><tr><td>按键</td><td align="right">2</td><td align="right">14ms</td><td align="right">不定</td><td>允许</td></tr><tr><td>Sensor INT</td><td align="right">86400</td><td align="right">12s</td><td align="right">1s</td><td>需评估</td></tr><tr><td>UART RX</td><td align="right">5832</td><td align="right">8.7s</td><td align="right">14.8s</td><td>异常</td></tr><tr><td>Unknown IRQ</td><td align="right">42000</td><td align="right">21s</td><td align="right">2.1s</td><td>严重异常</td></tr></tbody></table><p>仅看“睡眠电流”无法解释周期平均功耗。唤醒统计能把功耗问题转换为可优化的事件集合：减少次数、缩短活动时间、降低活动电流或合并处理。</p><h2 id="使用二分法而不是逐项随意尝试"><a href="#使用二分法而不是逐项随意尝试" class="headerlink" title="使用二分法而不是逐项随意尝试"></a>使用二分法而不是逐项随意尝试</h2><h3 id="软件模块二分"><a href="#软件模块二分" class="headerlink" title="软件模块二分"></a>软件模块二分</h3><p>如果最小固件正常、业务固件超标，可按功能组做二分：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[完整业务固件超标] --&gt; B&#123;禁用上半模块组&#125;</span><br><span class="line">    B --&gt;|电流恢复| C[根因在上半组]</span><br><span class="line">    B --&gt;|仍超标| D[根因在下半组或公共层]</span><br><span class="line">    C --&gt; E&#123;继续二分上半组&#125;</span><br><span class="line">    D --&gt; F&#123;继续二分下半组&#125;</span><br><span class="line">    E --&gt; G[锁定具体驱动/任务/资源锁]</span><br><span class="line">    F --&gt; G</span><br></pre></td></tr></table></figure><p>模块组可以按如下方式划分：</p><ol><li>无线&#x2F;网络；</li><li>音频&#x2F;显示；</li><li>传感器；</li><li>存储&#x2F;文件系统；</li><li>USB&#x2F;充电&#x2F;调试；</li><li>日志&#x2F;CLI&#x2F;诊断；</li><li>RTOS 定时器和公共中间件。</li></ol><p>二分时要保证每个测试版本只有一个主要变量。若同时关闭无线、日志、传感器和两个时钟，虽然电流可能下降，却无法建立明确因果关系。</p><h3 id="硬件电源域二分"><a href="#硬件电源域二分" class="headerlink" title="硬件电源域二分"></a>硬件电源域二分</h3><p>对于硬件路径，可以依次：</p><ul><li>断开某个负载开关或 0Ω；</li><li>强制拉低某个 LDO EN；</li><li>移除外设或断开总线串联电阻；</li><li>仅保留 MCU 核心板；</li><li>用外部独立电源给可疑域供电并单独测流。</li></ul><p>每次操作记录：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">test_id</span><br><span class="line">firmware_version</span><br><span class="line">board_serial</span><br><span class="line">power_source</span><br><span class="line">measurement_tool</span><br><span class="line">sample_rate</span><br><span class="line">ambient_temperature</span><br><span class="line">battery_voltage</span><br><span class="line">module_mask</span><br><span class="line">sleep_state</span><br><span class="line">current_min/current_avg/current_max</span><br><span class="line">wake_count</span><br><span class="line">notes</span><br></pre></td></tr></table></figure><h3 id="电流增量表比“正常-异常”更有价值"><a href="#电流增量表比“正常-异常”更有价值" class="headerlink" title="电流增量表比“正常&#x2F;异常”更有价值"></a>电流增量表比“正常&#x2F;异常”更有价值</h3><table><thead><tr><th>测试动作</th><th align="right">修改前平均电流</th><th align="right">修改后平均电流</th><th align="right">电流差值</th><th>结论</th></tr></thead><tbody><tr><td>断开无线电源域</td><td align="right">2.10mA</td><td align="right">2.02mA</td><td align="right">0.08mA</td><td>无线不是主因。</td></tr><tr><td>禁用 UART 日志</td><td align="right">2.02mA</td><td align="right">1.86mA</td><td align="right">0.16mA</td><td>日志有影响，但不是主因。</td></tr><tr><td>将 SPI MOSI 改为高阻</td><td align="right">1.86mA</td><td align="right">0.41mA</td><td align="right">1.45mA</td><td>高度怀疑对已断电 Flash 反向供电。</td></tr><tr><td>关闭内部下拉</td><td align="right">0.41mA</td><td align="right">0.08mA</td><td align="right">0.33mA</td><td>外部 10kΩ 上拉与内部&#x2F;输出低冲突。</td></tr><tr><td>启用 Tickless Idle</td><td align="right">0.08mA</td><td align="right">0.03mA</td><td align="right">0.05mA</td><td>周期 Tick 仍贡献部分平均功耗。</td></tr></tbody></table><p>差值能够直接对应某条电流路径或某个活动事件。最终总电流应接近各项预算之和，而不是依赖一次偶然的“最低读数”。</p><h2 id="设计统一的设备电源管理接口"><a href="#设计统一的设备电源管理接口" class="headerlink" title="设计统一的设备电源管理接口"></a>设计统一的设备电源管理接口</h2><h3 id="引用计数解决多使用者冲突"><a href="#引用计数解决多使用者冲突" class="headerlink" title="引用计数解决多使用者冲突"></a>引用计数解决多使用者冲突</h3><p>Linux Runtime PM、Zephyr Device Runtime PM 和 Linux Regulator Framework 都体现了同一原则：设备、电源轨和时钟是共享资源，不能由单个调用者任意开关。应使用 get&#x2F;put 或 acquire&#x2F;release 语义记录活跃使用者。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    A[Audio Client] --&gt;|get| PM[Device PM usage count]</span><br><span class="line">    B[Sensor Client] --&gt;|get| PM</span><br><span class="line">    C[Diagnostics] --&gt;|get| PM</span><br><span class="line">    PM --&gt; D&#123;usage count &gt; 0?&#125;</span><br><span class="line">    D --&gt;|是| E[保持设备/时钟/电源域 Active]</span><br><span class="line">    D --&gt;|否| F[执行 autosuspend / power off]</span><br></pre></td></tr></table></figure><p>如果某个模块 acquire 后没有 release，设备就会永久保持 active。调试接口应能输出：</p><ul><li>当前 usage count；</li><li>每个持有者的名称；</li><li>acquire 时间；</li><li>最后一次状态切换；</li><li>autosuspend 延迟；</li><li>suspend 失败原因。</li></ul><h3 id="通用接口示例"><a href="#通用接口示例" class="headerlink" title="通用接口示例"></a>通用接口示例</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief Power state of a managed device.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="class"><span class="keyword">enum</span> <span class="title">pm_device_state</span> &#123;</span></span><br><span class="line">    PM_DEVICE_STATE_OFF,</span><br><span class="line">    PM_DEVICE_STATE_SUSPENDED,</span><br><span class="line">    PM_DEVICE_STATE_ACTIVE,</span><br><span class="line">    PM_DEVICE_STATE_ERROR,</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief Suspend a managed device and release its dependent resources.</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * @param dev Device instance.</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * @return 0 on success, otherwise a negative error code.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pm_device_suspend</span><span class="params">(<span class="keyword">struct</span> pm_device *dev)</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief Resume a managed device and restore its dependent resources.</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * @param dev Device instance.</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * @return 0 on success, otherwise a negative error code.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pm_device_resume</span><span class="params">(<span class="keyword">struct</span> pm_device *dev)</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief Acquire a runtime reference for a managed device.</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * @param dev Device instance.</span></span><br><span class="line"><span class="comment"> * @param owner Resource owner identifier used for diagnostics.</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * @return 0 on success, otherwise a negative error code.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pm_device_get</span><span class="params">(<span class="keyword">struct</span> pm_device *dev, <span class="keyword">enum</span> pm_owner owner)</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief Release a runtime reference for a managed device.</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * @param dev Device instance.</span></span><br><span class="line"><span class="comment"> * @param owner Resource owner identifier used for diagnostics.</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * @return 0 on success, otherwise a negative error code.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pm_device_put</span><span class="params">(<span class="keyword">struct</span> pm_device *dev, <span class="keyword">enum</span> pm_owner owner)</span>;</span><br></pre></td></tr></table></figure><h3 id="suspend-失败不能静默忽略"><a href="#suspend-失败不能静默忽略" class="headerlink" title="suspend 失败不能静默忽略"></a>suspend 失败不能静默忽略</h3><p>如果 Flash 仍在写入、DMA 未完成、Codec 数据路径未停或 I2C 总线卡死，驱动 suspend 应返回明确错误。系统策略可以选择：</p><ul><li>延迟进入深睡；</li><li>降级到较浅状态；</li><li>超时后复位可恢复外设；</li><li>记录阻塞者并上报；</li><li>在量产测试中直接判失败。</li></ul><p>静默忽略 suspend 失败会导致功能上“看似正常”，但功耗长期超标，最终很难定位。</p><h2 id="系统级低功耗状态机"><a href="#系统级低功耗状态机" class="headerlink" title="系统级低功耗状态机"></a>系统级低功耗状态机</h2><p>建议把整机低功耗流程设计为显式状态机，而不是在 idle hook 中散落若干关外设调用。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">stateDiagram-v2</span><br><span class="line">    [*] --&gt; RUN</span><br><span class="line">    RUN --&gt; PREPARE_SLEEP: 无业务/超时/用户请求</span><br><span class="line">    PREPARE_SLEEP --&gt; ABORT: 新事务/设备 busy/唤醒源已有效</span><br><span class="line">    PREPARE_SLEEP --&gt; DEVICE_SUSPEND: 条件满足</span><br><span class="line">    DEVICE_SUSPEND --&gt; ABORT: 任一关键设备失败</span><br><span class="line">    DEVICE_SUSPEND --&gt; PIN_SLEEP: 全部设备已挂起</span><br><span class="line">    PIN_SLEEP --&gt; CLOCK_GATE: 应用休眠 pin state</span><br><span class="line">    CLOCK_GATE --&gt; SYSTEM_SLEEP: 关闭非必要时钟和电源域</span><br><span class="line">    SYSTEM_SLEEP --&gt; WAKE_EARLY: 唤醒事件</span><br><span class="line">    WAKE_EARLY --&gt; CLOCK_RESTORE</span><br><span class="line">    CLOCK_RESTORE --&gt; PIN_RESTORE</span><br><span class="line">    PIN_RESTORE --&gt; DEVICE_RESUME</span><br><span class="line">    DEVICE_RESUME --&gt; RUN</span><br><span class="line">    ABORT --&gt; RUN</span><br></pre></td></tr></table></figure><p>每个阶段都应具备：</p><ul><li>明确输入条件；</li><li>可检查的完成状态；</li><li>超时和错误处理；</li><li>对应的恢复顺序；</li><li>诊断计数器和最后失败原因。</li></ul><p>休眠前最后一次检查应尽量接近真正执行低功耗指令的位置，以避免“检查后又有中断&#x2F;任务产生新事务”的竞态。必要时在临界区内再次检查 pending 中断、设备 busy 和预计 idle time。</p><h2 id="调试器、日志和观察手段本身会改变功耗"><a href="#调试器、日志和观察手段本身会改变功耗" class="headerlink" title="调试器、日志和观察手段本身会改变功耗"></a>调试器、日志和观察手段本身会改变功耗</h2><h3 id="调试器常见影响"><a href="#调试器常见影响" class="headerlink" title="调试器常见影响"></a>调试器常见影响</h3><ul><li>通过 VTref、SWDIO、SWCLK 或串口反向供电；</li><li>设置 debug-in-sleep，使调试域和相关时钟保持；</li><li>halt 后系统未按正常路径关闭外设；</li><li>trace&#x2F;ITM&#x2F;SWO 持续活动；</li><li>断点破坏时序，导致外设事务未完成或超时重试；</li><li>调试器连接后芯片不能进入最深电源态。</li></ul><p>因此，最终功耗数据必须在“与量产使用条件一致”的连接方式下采集。调试器适合抓状态，不适合直接作为最终功耗认证环境。</p><h3 id="低侵入式观测方法"><a href="#低侵入式观测方法" class="headerlink" title="低侵入式观测方法"></a>低侵入式观测方法</h3><ol><li>用一个测试 GPIO 标记准备休眠、真正入睡、唤醒完成三个时间点；</li><li>将唤醒原因写入 retention RAM；</li><li>使用硬件计数器统计休眠驻留时间；</li><li>只在下次完整唤醒后批量输出日志；</li><li>对关键电源域引出测试点；</li><li>必要时使用逻辑分析仪观察中断线，但确认不会反向供电；</li><li>通过寄存器快照比较运行态与休眠前状态。</li></ol><h2 id="一套可执行的通用排查流程"><a href="#一套可执行的通用排查流程" class="headerlink" title="一套可执行的通用排查流程"></a>一套可执行的通用排查流程</h2><h3 id="阶段一：确认现象"><a href="#阶段一：确认现象" class="headerlink" title="阶段一：确认现象"></a>阶段一：确认现象</h3><ol><li>固定板卡、固件、电池电压、温度和连接方式；</li><li>断开非必要 USB、调试器和外接模块；</li><li>同时记录平均、最小、最大电流和波形；</li><li>判断是稳定直流还是周期活动；</li><li>确认测量仪表量程、带宽和串联压降不会改变系统状态。</li></ol><h3 id="阶段二：建立最小基线"><a href="#阶段二：建立最小基线" class="headerlink" title="阶段二：建立最小基线"></a>阶段二：建立最小基线</h3><ol><li>烧录最小休眠固件；</li><li>仅保留一个可控唤醒源；</li><li>关闭日志和调试保持；</li><li>应用 GPIO 休眠矩阵；</li><li>测量并与 MCU、LDO 和 always-on 域预算比较。</li></ol><h3 id="阶段三：划分硬件与软件"><a href="#阶段三：划分硬件与软件" class="headerlink" title="阶段三：划分硬件与软件"></a>阶段三：划分硬件与软件</h3><ul><li>最小固件仍超标：进入电源域、GPIO、电阻网络、器件和 PCB 路径；</li><li>最小固件正常：回到业务固件，按模块组二分；</li><li>仅连接调试器超标：先解决调试链路影响；</li><li>只有特定温度、湿度或电池电压异常：检查器件最大漏电、LDO 模式和板级污染。</li></ul><h3 id="阶段四：定位静态漏流"><a href="#阶段四：定位静态漏流" class="headerlink" title="阶段四：定位静态漏流"></a>阶段四：定位静态漏流</h3><ol><li>逐个关闭&#x2F;断开电源域；</li><li>测量已断电域 VDD 是否被抬高；</li><li>检查所有跨域 GPIO；</li><li>查外部上拉下拉、分压、LED、检测电阻；</li><li>核对 LDO IQ、load switch leakage 和输出放电；</li><li>必要时移除可疑器件或断开串联电阻验证。</li></ol><h3 id="阶段五：定位周期唤醒"><a href="#阶段五：定位周期唤醒" class="headerlink" title="阶段五：定位周期唤醒"></a>阶段五：定位周期唤醒</h3><ol><li>用功耗波形测量唤醒周期和持续时间；</li><li>与 RTOS Tick、软件定时器、传感器 ODR、无线连接间隔进行对齐；</li><li>记录唤醒原因和 pending IRQ；</li><li>逐个屏蔽唤醒源；</li><li>启用 Tickless Idle，合并定时任务，降低轮询频率；</li><li>检查错误重试和总线异常是否造成高频活动。</li></ol><h3 id="阶段六：验证修复"><a href="#阶段六：验证修复" class="headerlink" title="阶段六：验证修复"></a>阶段六：验证修复</h3><ol><li>多块样机复测；</li><li>覆盖高低温、电池高低电压、充电与非充电状态；</li><li>重复进入&#x2F;退出休眠数千次；</li><li>检查功能恢复、数据一致性和唤醒延迟；</li><li>将阈值和波形特征加入自动化回归与量产测试。</li></ol><h2 id="量产和持续集成中的低功耗回归"><a href="#量产和持续集成中的低功耗回归" class="headerlink" title="量产和持续集成中的低功耗回归"></a>量产和持续集成中的低功耗回归</h2><p>低功耗问题很容易在后续功能迭代中复发：新增日志、增加传感器轮询、驱动引用未释放、默认 GPIO 改动、协议栈参数变化，都可能让休眠电流重新升高。因此低功耗应被当作可测试的功能指标，而不是发布前的一次人工检查。</p><h3 id="建议的回归指标"><a href="#建议的回归指标" class="headerlink" title="建议的回归指标"></a>建议的回归指标</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">- 进入目标睡眠状态的成功率</span><br><span class="line">- 从休眠请求到电流稳定平台的时间</span><br><span class="line">- 稳态 sleep current 的 P50/P95/P99</span><br><span class="line">- 规定时间窗内的平均电流与积分电荷</span><br><span class="line">- 单位时间异常唤醒次数</span><br><span class="line">- 各唤醒源次数和活动持续时间</span><br><span class="line">- 设备/时钟/电源轨未释放引用计数</span><br><span class="line">- suspend/resume 失败次数</span><br><span class="line">- 高低温和高低电压边界下的最大值</span><br></pre></td></tr></table></figure><h3 id="自动化测试状态机"><a href="#自动化测试状态机" class="headerlink" title="自动化测试状态机"></a>自动化测试状态机</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    A[刷写指定固件] --&gt; B[上电并完成自检]</span><br><span class="line">    B --&gt; C[发送进入休眠命令]</span><br><span class="line">    C --&gt; D[等待电流进入稳定窗口]</span><br><span class="line">    D --&gt; E[采集电流波形与积分电荷]</span><br><span class="line">    E --&gt; F&#123;是否满足阈值?&#125;</span><br><span class="line">    F --&gt;|是| G[触发标准唤醒]</span><br><span class="line">    F --&gt;|否| H[保存波形/状态/唤醒原因]</span><br><span class="line">    G --&gt; I[验证功能恢复]</span><br><span class="line">    I --&gt; J[重复 N 次并生成报告]</span><br><span class="line">    H --&gt; J</span><br></pre></td></tr></table></figure><p>量产测试可以设置较宽的总电流门限，研发回归则应同时检查波形、事件和状态。仅用一个平均电流门限，可能漏掉低频但高能量的异常唤醒。</p><h2 id="典型根因与快速特征"><a href="#典型根因与快速特征" class="headerlink" title="典型根因与快速特征"></a>典型根因与快速特征</h2><table><thead><tr><th>现象</th><th>更可能的根因</th><th>快速验证</th></tr></thead><tbody><tr><td>电流稳定在 1mA～数毫安</td><td>外设常开、GPIO 反灌、上下拉冲突、LDO 常开</td><td>逐域断电、测已断电 VDD、修改跨域 GPIO。</td></tr><tr><td>每 1ms 或 10ms 一个小脉冲</td><td>SysTick、软件定时器、轮询任务</td><td>启用 Tickless，禁用周期任务，观察周期是否消失。</td></tr><tr><td>每 100ms～数秒一个大脉冲</td><td>无线连接事件、传感器采样、日志刷新、看门狗</td><td>对齐协议&#x2F;任务周期，记录唤醒原因。</td></tr><tr><td>刚进入睡眠就立即唤醒</td><td>pending IRQ、电平型中断有效、唤醒脚浮空</td><td>读取 pending 位，屏蔽唤醒源，检查引脚电平。</td></tr><tr><td>断开调试器后恢复正常</td><td>debug-in-sleep、反向供电、SWD&#x2F;UART 上拉</td><td>脱机测量，修改调试保持配置。</td></tr><tr><td>低温或高温才异常</td><td>器件漏电最大值、晶振&#x2F;时钟重试、LDO 模式、PCB 污染</td><td>温箱中分域测流并核对最大规格。</td></tr><tr><td>某次唤醒后电流不再下降</td><td>resume&#x2F;suspend 状态机不对称、引用计数泄漏</td><td>重复循环并导出 usage count 和最后持有者。</td></tr><tr><td>只有充电线连接时超标</td><td>充电 IC、USB VBUS 检测、Type-C&#x2F;接口路径</td><td>分别测电池模式和外部供电模式。</td></tr></tbody></table><h2 id="参考开源方案的设计原则"><a href="#参考开源方案的设计原则" class="headerlink" title="参考开源方案的设计原则"></a>参考开源方案的设计原则</h2><h3 id="1-Arm-CMSIS-Core：把-CPU-指令与-SoC-电源配置分开理解"><a href="#1-Arm-CMSIS-Core：把-CPU-指令与-SoC-电源配置分开理解" class="headerlink" title="1. Arm CMSIS-Core：把 CPU 指令与 SoC 电源配置分开理解"></a>1. Arm CMSIS-Core：把 CPU 指令与 SoC 电源配置分开理解</h3><p>CMSIS 提供 <code>__WFI()</code>、<code>__WFE()</code>、NVIC、SCB、SysTick 等标准访问接口。其关键启示是：CPU 等待指令是架构层能力，而具体时钟、电源域和外设状态属于 SoC&#x2F;板级实现。通用 PM 框架应把二者分层，避免把一条等待指令等价为完整系统休眠。</p><h3 id="2-FreeRTOS-Tickless-Idle：按“下一次必须运行时间”决定睡多久"><a href="#2-FreeRTOS-Tickless-Idle：按“下一次必须运行时间”决定睡多久" class="headerlink" title="2. FreeRTOS Tickless Idle：按“下一次必须运行时间”决定睡多久"></a>2. FreeRTOS Tickless Idle：按“下一次必须运行时间”决定睡多久</h3><p>FreeRTOS 在只有 Idle task 可运行且预计空闲时间足够长时抑制 Tick。其关键启示是：低功耗不是简单关闭时钟，而是调度器需要知道最早截止时间，并在睡眠前后正确补偿内核时间。轮询任务和高频软件定时器会直接压缩可休眠窗口。</p><h3 id="3-Zephyr-System-PM：用驻留时间、退出延迟和约束选择状态"><a href="#3-Zephyr-System-PM：用驻留时间、退出延迟和约束选择状态" class="headerlink" title="3. Zephyr System PM：用驻留时间、退出延迟和约束选择状态"></a>3. Zephyr System PM：用驻留时间、退出延迟和约束选择状态</h3><p>Zephyr 将系统电源状态、最小驻留时间、退出延迟和策略锁纳入统一决策。其关键启示是：最深状态不一定最省总能量；只有睡眠时间足够长、恢复延迟可接受、设备依赖允许时，才应进入更深状态。</p><h3 id="4-Zephyr-Device-Runtime-PM：用引用计数协调驱动、子系统和应用"><a href="#4-Zephyr-Device-Runtime-PM：用引用计数协调驱动、子系统和应用" class="headerlink" title="4. Zephyr Device Runtime PM：用引用计数协调驱动、子系统和应用"></a>4. Zephyr Device Runtime PM：用引用计数协调驱动、子系统和应用</h3><p>设备可以由多个上层共同使用。Zephyr 的 runtime PM 使用 usage count 管理 suspend&#x2F;resume，并考虑父子设备依赖。其关键启示是：低功耗资源必须有所有权和生命周期，不能依赖“某个业务模块记得关掉”。</p><h3 id="5-ESP-IDF-PM-Locks：把禁止降频或禁止睡眠变成显式资源锁"><a href="#5-ESP-IDF-PM-Locks：把禁止降频或禁止睡眠变成显式资源锁" class="headerlink" title="5. ESP-IDF PM Locks：把禁止降频或禁止睡眠变成显式资源锁"></a>5. ESP-IDF PM Locks：把禁止降频或禁止睡眠变成显式资源锁</h3><p>ESP-IDF 允许组件获取 CPU 最高频率、APB 最高频率或禁止 Light-sleep 的锁。其关键启示是：系统不能靠隐含约定判断是否可以睡眠，约束必须显式、可计数、可诊断。锁长期未释放，就是可以直接定位的高功耗缺陷。</p><h3 id="6-ESP-IDF-GPIO-isolate：跨电源域-IO-需要硬件隔离语义"><a href="#6-ESP-IDF-GPIO-isolate：跨电源域-IO-需要硬件隔离语义" class="headerlink" title="6. ESP-IDF GPIO isolate：跨电源域 IO 需要硬件隔离语义"></a>6. ESP-IDF GPIO isolate：跨电源域 IO 需要硬件隔离语义</h3><p>ESP-IDF 的睡眠文档明确提供 GPIO isolate 和电源域配置。其关键启示是：低功耗不仅是软件逻辑状态，还包括 pad、内部上下拉、hold 和外部供电关系。对已断电外设，普通“输入模式”未必足够，某些 SoC 需要专门的隔离或 hold 机制。</p><h3 id="7-Linux-Runtime-PM：设备空闲和系统休眠是两个相关但独立的问题"><a href="#7-Linux-Runtime-PM：设备空闲和系统休眠是两个相关但独立的问题" class="headerlink" title="7. Linux Runtime PM：设备空闲和系统休眠是两个相关但独立的问题"></a>7. Linux Runtime PM：设备空闲和系统休眠是两个相关但独立的问题</h3><p>Linux Runtime PM 允许设备在系统仍运行时独立 autosuspend，也会与系统级 suspend 协调。其关键启示是：设备应该在不用时就进入低功耗，而不是等整机睡眠前一次性关闭所有设备。这样既减少运行期功耗，也降低系统睡眠准备的复杂度。</p><h3 id="8-Linux-Regulator-与-Clock-Framework：共享资源必须有拓扑和引用关系"><a href="#8-Linux-Regulator-与-Clock-Framework：共享资源必须有拓扑和引用关系" class="headerlink" title="8. Linux Regulator 与 Clock Framework：共享资源必须有拓扑和引用关系"></a>8. Linux Regulator 与 Clock Framework：共享资源必须有拓扑和引用关系</h3><p>电源轨和时钟可能被多个设备共享。Linux 框架通过 provider&#x2F;consumer、父子拓扑和引用计数管理资源。其关键启示是：电源域与时钟不能由驱动直接无序操作，应有统一管理层、依赖顺序和诊断接口。</p><h3 id="9-Linux-Power-Domain：共享开关的设备必须作为一个整体管理"><a href="#9-Linux-Power-Domain：共享开关的设备必须作为一个整体管理" class="headerlink" title="9. Linux Power Domain：共享开关的设备必须作为一个整体管理"></a>9. Linux Power Domain：共享开关的设备必须作为一个整体管理</h3><p>多个设备可能共享同一个电源开关、参考时钟或隔离单元，无法独立掉电。Linux Device Power Management 将这类设备组织到可嵌套的 power domain 中，由域级回调协调开关顺序。其关键启示是：不要假设每个驱动都能独立控制物理电源；软件资源模型必须与真实电源拓扑一致。</p><h3 id="10-Linux-ASoC-DAPM：按有效信号路径关闭音频部件"><a href="#10-Linux-ASoC-DAPM：按有效信号路径关闭音频部件" class="headerlink" title="10. Linux ASoC DAPM：按有效信号路径关闭音频部件"></a>10. Linux ASoC DAPM：按有效信号路径关闭音频部件</h3><p>ASoC Dynamic Audio Power Management 根据音频流和 mixer&#x2F;mux 路径决定哪些 Codec、ADC、DAC、bias、耳放或外部放大器需要上电。其关键启示是：复杂模拟系统不能只用一个“audio_on”总开关，应把内部组件和信号连接建模成图，只保持当前有效路径上的节点。</p><h3 id="11-Linux-Power-Tracepoints：把状态切换变成可追踪事件"><a href="#11-Linux-Power-Tracepoints：把状态切换变成可追踪事件" class="headerlink" title="11. Linux Power Tracepoints：把状态切换变成可追踪事件"></a>11. Linux Power Tracepoints：把状态切换变成可追踪事件</h3><p>Linux power tracepoints 能记录 CPU idle、频率、时钟和电源域变化。其关键启示是：低功耗框架不仅要执行状态切换，还要产生轻量、时间可对齐的事件，使功耗波形能够与软件行为一一对应。RTOS 项目可以仿照这一模型，输出固定长度的事件记录，而不是依赖大量文本日志。</p><h2 id="从毫安级异常收敛到根因的通用案例推演"><a href="#从毫安级异常收敛到根因的通用案例推演" class="headerlink" title="从毫安级异常收敛到根因的通用案例推演"></a>从毫安级异常收敛到根因的通用案例推演</h2><p>下面给出一个不依赖具体产品类型的推演过程，用来说明如何把测量、代码和硬件操作组合成闭环。假设某电池设备目标休眠电流不超过 40µA，抽检样机实测约 2.2mA。系统包含 MCU、SPI Flash、两个传感器、无线模块、独立音频域和若干 LDO。</p><h3 id="第一步：识别波形类型"><a href="#第一步：识别波形类型" class="headerlink" title="第一步：识别波形类型"></a>第一步：识别波形类型</h3><p>功耗分析仪显示电流并非完全稳定，而是约 1.9mA 的平台上叠加每 100ms 一次的 6mA 脉冲。这里至少有两个问题：</p><ul><li>1.9mA 稳态平台说明存在静态常开或反灌路径；</li><li>100ms 脉冲说明还存在周期唤醒任务。</li></ul><p>如果只使用万用表，可能看到约 2.2mA，从而把两个独立根因误判成一个问题。因此应先分别处理平台电流和周期脉冲。</p><h3 id="第二步：使用最小固件切分问题"><a href="#第二步：使用最小固件切分问题" class="headerlink" title="第二步：使用最小固件切分问题"></a>第二步：使用最小固件切分问题</h3><p>烧录最小固件后，100ms 脉冲消失，但平台仍有 1.7mA。由此可以得到两个结论：</p><ol><li>周期脉冲来自业务软件、RTOS 定时器或某个驱动任务；</li><li>1.7mA 平台在最小固件下仍存在，更可能属于 GPIO、电源域、外设默认状态或板级器件。</li></ol><p>这个阶段不要立刻回业务代码查任务，因为主电流平台尚未解决。先让最小固件达到硬件可实现的最低基线，后续业务软件的增量才有意义。</p><h3 id="第三步：按电源域断开"><a href="#第三步：按电源域断开" class="headerlink" title="第三步：按电源域断开"></a>第三步：按电源域断开</h3><p>逐个断开测流跳线后得到：</p><table><thead><tr><th>电源域</th><th align="right">断开前</th><th align="right">断开后</th><th align="right">差值</th></tr></thead><tbody><tr><td>无线域</td><td align="right">1.70mA</td><td align="right">1.64mA</td><td align="right">0.06mA</td></tr><tr><td>传感器域</td><td align="right">1.64mA</td><td align="right">1.58mA</td><td align="right">0.06mA</td></tr><tr><td>音频域</td><td align="right">1.58mA</td><td align="right">1.49mA</td><td align="right">0.09mA</td></tr><tr><td>SPI Flash 域</td><td align="right">1.49mA</td><td align="right">0.11mA</td><td align="right">1.38mA</td></tr></tbody></table><p>Flash 域成为主要嫌疑。但此时不能直接断言 Flash 芯片损坏，还要判断是芯片没有进入 deep power-down，还是 MCU 通过 IO 反向供电。</p><h3 id="第四步：检查已断电域电压"><a href="#第四步：检查已断电域电压" class="headerlink" title="第四步：检查已断电域电压"></a>第四步：检查已断电域电压</h3><p>关闭 Flash LDO 后测得 Flash VDD 仍有约 1.6V，说明该域并未真正掉电。随后逐个将 CS、SCK、MOSI、MISO 改为高阻，发现 MOSI 改为高阻后 VDD 降到接近 0V，整机电流下降约 1.3mA。根因是 MCU 在 Flash 电源关闭后仍保持 MOSI 高电平，电流经 Flash IO 保护结构反向供电。</p><p>修复不应只改成“休眠时把 MOSI 拉低”，因为不同器件对掉电 IO 的允许条件不同。更稳妥的修复是：</p><ol><li>先发送 Flash deep power-down；</li><li>等待命令完成并让 CS 回到非选中态；</li><li>将跨域 SPI 引脚切到数据手册允许的高阻&#x2F;隔离状态；</li><li>最后关闭 Flash 电源域；</li><li>唤醒时先恢复电源并等待稳定，再恢复 pinmux 和发送退出 DPD 命令。</li></ol><h3 id="第五步：回到业务固件定位周期脉冲"><a href="#第五步：回到业务固件定位周期脉冲" class="headerlink" title="第五步：回到业务固件定位周期脉冲"></a>第五步：回到业务固件定位周期脉冲</h3><p>硬件平台电流降到约 30µA 后恢复业务固件，平均电流约 260µA，并仍有 100ms 脉冲。记录唤醒原因后发现某传感器任务每 100ms 轮询一次，即使传感器处于关闭状态也会唤醒 MCU 检查状态。</p><p>将轮询改为传感器中断加长周期健康检查，并启用 Tickless Idle 后，脉冲间隔从 100ms 延长到业务真正需要的事件间隔，平均电流下降到约 42µA。最后再将一个常开的调试 UART 时钟关闭，整机达到 35µA。</p><p>这个案例说明，休眠电流异常可能由多个根因叠加。正确方法是先分解波形，再依次消除静态平台和周期活动，而不是期待一次修改把所有电流同时降到目标值。</p><h2 id="休眠与唤醒必须保持严格对称"><a href="#休眠与唤醒必须保持严格对称" class="headerlink" title="休眠与唤醒必须保持严格对称"></a>休眠与唤醒必须保持严格对称</h2><p>很多低功耗问题并不是第一次进入休眠就出现，而是在反复唤醒后逐渐恶化。例如某驱动每次 resume 都增加一次时钟引用，却只在部分路径 release；某中断在 suspend 时关闭，但异常返回路径没有恢复；某电源域关闭前保存了状态，第二次进入休眠时却因为状态标志错误跳过关闭动作。</p><p>建议为每个受管设备定义对称操作：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">prepare -&gt; suspend -&gt; power_off</span><br><span class="line">power_on -&gt; resume -&gt; complete</span><br></pre></td></tr></table></figure><p>并保证以下不变量：</p><ul><li>每次成功的 <code>get</code> 必须对应一次 <code>put</code>；</li><li>每次成功的 clock enable 必须对应一次 disable；</li><li>每次成功的 regulator enable 必须对应一次 disable；</li><li>suspend 失败时，已经关闭的下游资源必须按逆序回滚；</li><li>resume 失败时，不应把设备错误地标记为 active；</li><li>重复调用 suspend&#x2F;resume 应具备幂等性或返回明确状态；</li><li>任何状态切换都应记录最后错误和执行次数。</li></ul><h3 id="事务式休眠准备"><a href="#事务式休眠准备" class="headerlink" title="事务式休眠准备"></a>事务式休眠准备</h3><p>可以把休眠准备看成一个事务：所有关键设备都成功挂起后才提交系统深睡；任一步失败，则按逆序恢复已修改的资源。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant PM as System PM</span><br><span class="line">    participant D1 as Device A</span><br><span class="line">    participant D2 as Device B</span><br><span class="line">    participant CLK as Clock/Power Domain</span><br><span class="line"></span><br><span class="line">    PM-&gt;&gt;D1: suspend</span><br><span class="line">    D1--&gt;&gt;PM: success</span><br><span class="line">    PM-&gt;&gt;D2: suspend</span><br><span class="line">    D2--&gt;&gt;PM: error</span><br><span class="line">    PM-&gt;&gt;D1: resume rollback</span><br><span class="line">    D1--&gt;&gt;PM: restored</span><br><span class="line">    PM--&gt;&gt;PM: abort deep sleep and record blocker</span><br></pre></td></tr></table></figure><p>这种方式比“尽量关，失败也继续睡”更容易保证数据一致性和可诊断性。对于允许降级的系统，也应明确区分：关键设备失败导致取消深睡；非关键设备失败则退化到浅睡，并记录功耗风险。</p><h2 id="跨电源域接口的四种组合"><a href="#跨电源域接口的四种组合" class="headerlink" title="跨电源域接口的四种组合"></a>跨电源域接口的四种组合</h2><p>判断 GPIO 是否安全，必须同时考虑 MCU 侧是否供电、外设侧是否供电。可将跨域接口分成四种状态：</p><table><thead><tr><th>MCU 域</th><th>外设域</th><th>主要风险</th><th>设计要求</th></tr></thead><tbody><tr><td>On</td><td>On</td><td>上下拉冲突、总线争用、错误电平</td><td>满足正常接口规范。</td></tr><tr><td>On</td><td>Off</td><td>MCU 向外设反向供电</td><td>IO 高阻&#x2F;隔离，检查失效模式和最大注入电流。</td></tr><tr><td>Off</td><td>On</td><td>外设向 MCU 反向供电</td><td>外设输出高阻或加入隔离&#x2F;串联限流。</td></tr><tr><td>Off</td><td>Off</td><td>残余电荷、外部上拉仍连接第三电源域</td><td>明确所有上拉电源来源和放电路径。</td></tr></tbody></table><p>复杂系统中，I2C 上拉可能接到 always-on 域，而传感器 VDD 接到可关闭域；UART 对端可能由 USB 供电；SPI Flash VDD 关闭但 MCU IO 仍保持；这些都属于电源域关系错误，而不只是 GPIO 配置错误。</p><p>硬件层可采用以下措施：</p><ul><li>选择带 Ioff 特性的电平转换器或缓冲器；</li><li>在跨域信号上增加合理串联电阻限制注入电流；</li><li>使用 load switch 与 IO isolation 协同控制；</li><li>让上拉电阻接到与器件一致的可关闭电源域；</li><li>对不可避免的跨域接口定义明确的掉电顺序；</li><li>在原理图审查中标注每个信号的源域、目标域和掉电行为。</li></ul><p>软件层则应把电源域状态与 pin state 联动，而不是由外设驱动和 GPIO 驱动各自独立处理。</p><h2 id="常见错误做法及其风险"><a href="#常见错误做法及其风险" class="headerlink" title="常见错误做法及其风险"></a>常见错误做法及其风险</h2><h3 id="错误一：先把所有-GPIO-都改成模拟输入"><a href="#错误一：先把所有-GPIO-都改成模拟输入" class="headerlink" title="错误一：先把所有 GPIO 都改成模拟输入"></a>错误一：先把所有 GPIO 都改成模拟输入</h3><p>这种做法可能对悬空引脚有效，但对唤醒引脚、外部固定上拉、需要保持片选的器件或必须满足掉电时序的接口可能造成新问题。正确做法是按连接关系建立逐引脚休眠状态矩阵。</p><h3 id="错误二：看到-WFI-就认为已经进入深睡"><a href="#错误二：看到-WFI-就认为已经进入深睡" class="headerlink" title="错误二：看到 WFI 就认为已经进入深睡"></a>错误二：看到 <code>WFI</code> 就认为已经进入深睡</h3><p><code>WFI</code> 只是一条处理器等待指令。实际功耗取决于 <code>SLEEPDEEP</code>、PMU&#x2F;PWR 配置、时钟、电源域和唤醒条件。必须通过寄存器、驻留时间和电流波形共同验证。</p><h3 id="错误三：只看平均电流，不看波形"><a href="#错误三：只看平均电流，不看波形" class="headerlink" title="错误三：只看平均电流，不看波形"></a>错误三：只看平均电流，不看波形</h3><p>平均值无法区分稳定漏流和周期唤醒。两者的修复路径完全不同。至少要记录最小、最大、平均、周期和积分电荷。</p><h3 id="错误四：一次关闭多个模块"><a href="#错误四：一次关闭多个模块" class="headerlink" title="错误四：一次关闭多个模块"></a>错误四：一次关闭多个模块</h3><p>这样只能证明“这些修改的组合有效”，无法知道哪个模块是真正根因，也无法判断是否存在多个问题。应使用二分法或单变量实验。</p><h3 id="错误五：用持续-UART-日志观察低功耗"><a href="#错误五：用持续-UART-日志观察低功耗" class="headerlink" title="错误五：用持续 UART 日志观察低功耗"></a>错误五：用持续 UART 日志观察低功耗</h3><p>日志本身会开启时钟、延长活动时间并改变引脚状态。应改用保留 RAM、事件计数器、测试 GPIO 或唤醒后批量导出。</p><h3 id="错误六：只在一块样机和室温下验证"><a href="#错误六：只在一块样机和室温下验证" class="headerlink" title="错误六：只在一块样机和室温下验证"></a>错误六：只在一块样机和室温下验证</h3><p>器件漏电、LDO IQ、晶振启动、PCB 污染和电池电压都会随温度和个体变化。低功耗指标应覆盖样本分布和环境边界。</p><h3 id="错误七：只修当前数值，不建立回归机制"><a href="#错误七：只修当前数值，不建立回归机制" class="headerlink" title="错误七：只修当前数值，不建立回归机制"></a>错误七：只修当前数值，不建立回归机制</h3><p>如果没有设备引用计数、状态自检和自动化阈值，后续新增功能很容易重新引入高功耗。修复应同时改进架构和测试。</p><h2 id="面向产品化的低功耗治理"><a href="#面向产品化的低功耗治理" class="headerlink" title="面向产品化的低功耗治理"></a>面向产品化的低功耗治理</h2><p>一次定位解决的是当前缺陷，产品化治理要解决“为什么此类问题能够进入集成阶段”。建议从设计、实现、验证和量产四个层面建立约束。</p><h3 id="设计阶段"><a href="#设计阶段" class="headerlink" title="设计阶段"></a>设计阶段</h3><ul><li>为每个工作模式建立功耗预算和允许的活跃模块清单；</li><li>原理图标注电源域、上拉电源、跨域 IO 和测流点；</li><li>选择具有低 IQ、Ioff、明确 shutdown 特性的器件；</li><li>定义睡眠深度、退出延迟、数据保持和唤醒源；</li><li>预留电流测量、隔离和调试断开能力。</li></ul><h3 id="实现阶段"><a href="#实现阶段" class="headerlink" title="实现阶段"></a>实现阶段</h3><ul><li>统一设备、时钟和电源轨的 get&#x2F;put 接口；</li><li>建立显式 suspend&#x2F;resume 状态机；</li><li>统一管理运行态与休眠态 pin configuration；</li><li>对资源锁、usage count 和 blocker 提供诊断接口；</li><li>用事件驱动替代高频轮询；</li><li>将唤醒原因和驻留时间纳入运行统计。</li></ul><h3 id="验证阶段"><a href="#验证阶段" class="headerlink" title="验证阶段"></a>验证阶段</h3><ul><li>同时测量稳态电流和动态积分电荷；</li><li>覆盖重复睡眠、异常中断、通信失败和电源抖动；</li><li>验证 suspend 失败回滚和 resume 错误处理；</li><li>覆盖温度、电压、样本差异和充电状态；</li><li>对每次版本构建保存功耗趋势。</li></ul><h3 id="量产阶段"><a href="#量产阶段" class="headerlink" title="量产阶段"></a>量产阶段</h3><ul><li>使用固定治具和连接方式，避免第二供电路径；</li><li>在规定时间窗内测量平均值和稳定平台；</li><li>对超标板自动保存序列号、固件版本和电流波形；</li><li>将异常板按电源域差值和唤醒特征分类；</li><li>把低功耗测试与功能唤醒测试组合，避免“低电流但无法恢复”的假通过。</li></ul><h2 id="最终回答组织方式"><a href="#最终回答组织方式" class="headerlink" title="最终回答组织方式"></a>最终回答组织方式</h2><p>回答这类题时，可以按以下顺序展开：</p><ol><li>先下结论：毫安级休眠异常通常意味着某个电源状态没有闭合，不应按随机误差处理；</li><li>再讲测量：先区分稳定漏流、周期唤醒和仪表伪差，并排除调试器&#x2F;USB 第二供电路径；</li><li>接着讲边界：烧录最小休眠固件，将问题切成硬件静态路径与业务软件路径；</li><li>然后讲硬件：按电源域、GPIO 反灌、上下拉、电阻网络、LDO IQ 和板级漏流排查；</li><li>再讲软件：核对目标睡眠深度、时钟树、设备 suspend、RTOS Tick、定时器、中断 pending 和唤醒原因；</li><li>强调方法：每次只改变一个变量，用二分法和电流差值表缩小范围；</li><li>最后讲架构：用设备&#x2F;时钟&#x2F;电源轨引用计数、显式状态机、资源锁和自动化回归避免问题复发。</li></ol><p>一句话概括：休眠电流定位不是“拿万用表逐个关外设”，而是把整机视为由 CPU 状态、设备状态、时钟树、电源域、GPIO 电气状态和唤醒事件共同组成的功耗状态机；通过可信测量、最小固件、分层二分、状态证据和量化预算，才能稳定地把毫安级异常收敛到具体器件、引脚、时钟、任务或资源引用。</p><h2 id="参考链接"><a href="#参考链接" class="headerlink" title="参考链接"></a>参考链接</h2><ul><li><a href="https://arm-software.github.io/CMSIS_6/main/Core/group__intrinsic__CPU__gr.html">Arm CMSIS-Core Intrinsic Functions for CPU Instructions</a></li><li><a href="https://arm-software.github.io/CMSIS_6/latest/Core/index.html">Arm CMSIS-Core Overview</a></li><li><a href="https://www.freertos.org/Documentation/02-Kernel/02-Kernel-features/07-Lower-power-support">FreeRTOS Low Power Support</a></li><li><a href="https://docs.zephyrproject.org/latest/services/pm/system.html">Zephyr System Power Management</a></li><li><a href="https://docs.zephyrproject.org/latest/services/pm/device.html">Zephyr Device Power Management</a></li><li><a href="https://docs.zephyrproject.org/latest/services/pm/device_runtime.html">Zephyr Device Runtime Power Management</a></li><li><a href="https://docs.espressif.com/projects/esp-idf/en/stable/esp32/api-reference/system/power_management.html">ESP-IDF Power Management</a></li><li><a href="https://docs.espressif.com/projects/esp-idf/en/stable/esp32/api-reference/system/sleep_modes.html">ESP-IDF Sleep Modes</a></li><li><a href="https://docs.kernel.org/power/runtime_pm.html">Linux Runtime Power Management Framework</a></li><li><a href="https://docs.kernel.org/driver-api/pm/devices.html">Linux Device Power Management Basics</a></li><li><a href="https://docs.kernel.org/driver-api/regulator.html">Linux Voltage and Current Regulator API</a></li><li><a href="https://docs.kernel.org/driver-api/clk.html">Linux Common Clock Framework</a></li><li><a href="https://docs.kernel.org/sound/soc/dapm.html">Linux ASoC Dynamic Audio Power Management</a></li><li><a href="https://docs.kernel.org/trace/events-power.html">Linux Power Tracepoints</a></li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;meta name=&quot;referrer&quot; content=&quot;no-referrer&quot; /&gt;

&lt;h1 id=&quot;嵌入式面试真题第-16-题：嵌入式设备休眠电流异常的通用定位与低功耗治理&quot;&gt;&lt;a href=&quot;#嵌入式面试真题第-16-题：嵌入式设备休眠电流异常的通用定位与低功耗治理</summary>
      
    
    
    
    <category term="嵌入式面试真题" scheme="https://wdfk-prog.space/categories/%E5%B5%8C%E5%85%A5%E5%BC%8F%E9%9D%A2%E8%AF%95%E7%9C%9F%E9%A2%98/"/>
    
    
    <category term="嵌入式面试真题" scheme="https://wdfk-prog.space/tags/%E5%B5%8C%E5%85%A5%E5%BC%8F%E9%9D%A2%E8%AF%95%E7%9C%9F%E9%A2%98/"/>
    
  </entry>
  
  <entry>
    <title>不可恢复异常后的通用崩溃快照、调用栈保存与离线分析架构</title>
    <link href="https://wdfk-prog.space/posts/6b8b9efe/"/>
    <id>https://wdfk-prog.space/posts/6b8b9efe/</id>
    <published>2026-07-22T01:26:10.000Z</published>
    <updated>2026-07-22T01:26:30.065Z</updated>
    
    <content type="html"><![CDATA[<meta name="referrer" content="no-referrer" /><h1 id="嵌入式面试真题第-15-题：不可恢复异常后的通用崩溃快照、调用栈保存与离线分析架构"><a href="#嵌入式面试真题第-15-题：不可恢复异常后的通用崩溃快照、调用栈保存与离线分析架构" class="headerlink" title="嵌入式面试真题第 15 题：不可恢复异常后的通用崩溃快照、调用栈保存与离线分析架构"></a>嵌入式面试真题第 15 题：不可恢复异常后的通用崩溃快照、调用栈保存与离线分析架构</h1><p><img src="https://i-blog.csdnimg.cn/direct/1bb9142b0b4647fb83792a1277c0800b.png" alt="在这里插入图片描述"></p><h2 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h2><p>在量产嵌入式设备中，系统可能在任意业务状态、任意线程、任意中断嵌套层级和任意外设工作阶段发生不可恢复异常，例如 Arm Cortex-M 的 HardFault、MemManage、BusFault、UsageFault、SecureFault，RISC-V 的同步异常或 NMI，Xtensa 的 panic，RTOS assert，栈溢出，内存破坏，非法指令，访问越界，双重释放，DMA 越界，Flash&#x2F;cache 异常，锁死后的看门狗复位，以及电压跌落前的紧急复位。</p><p>发生异常时，调度器、线程栈、堆、文件系统、日志系统、Flash 驱动甚至时钟和缓存状态都可能已经不可信。设备又可能位于用户现场，无法连接调试器，研发只能在返修、远程上报或下次启动后分析有限数据。</p><p>你会如何设计一套通用的嵌入式 Crash Snapshot&#x2F;Core Dump 机制，在异常入口的极短时间内，安全、确定且可验证地保存 CPU 上下文、故障寄存器、当前调用栈、关键线程状态、最近事件和固件身份，并在复位后可靠地转存、上报和离线符号化？设计时应说明：</p><ol><li>异常入口如何取得正确的栈指针和异常栈帧。</li><li>为什么不能在异常上下文中直接做完整调用栈分析。</li><li>如何防止栈损坏、二次 Fault、Flash 正在忙、掉电或看门狗超时导致快照再次失败。</li><li>Crash Record 如何实现版本兼容、完整性校验和原子提交。</li><li>如何兼容裸机、RTOS、多核、FPU、TrustZone、不同存储介质和不同 CPU 架构。</li><li>如何借鉴 CMSIS-View Fault Storage、CrashCatcher、Zephyr Coredump、ESP-IDF Core Dump、RT-Thread 异常框架、Memfault Firmware SDK 等现有方案。</li></ol><h2 id="回答"><a href="#回答" class="headerlink" title="回答"></a>回答</h2><p>结论：不要把“异常处理”设计成在 Fault Handler 中打印日志、遍历线程、回溯符号或写普通文件。正确方案是把整个流程拆成三个彼此隔离的阶段：</p><ol><li><strong>一级捕获阶段</strong>：异常入口只执行固定上界、无动态分配、无锁、尽量不依赖外设的最小快照逻辑，把原始寄存器、异常栈帧、故障状态和受边界保护的栈窗口写入保留 RAM、Backup SRAM、<code>.noinit</code> 区或预擦除的 Crash Slot。</li><li><strong>二级持久化阶段</strong>：系统复位后，在调度器、文件系统和网络尚未启动或刚恢复可信状态时，校验一级快照并转存到专用 Flash 分区、FRAM、eMMC、LittleFS 文件或远程上报队列。</li><li><strong>离线分析阶段</strong>：PC 工具结合与故障固件严格匹配的 ELF、MAP、DWARF、Build ID 和链接地址，完成符号化、栈展开、源码定位、故障寄存器解码和同类崩溃聚类。</li></ol><p>核心原则是：<strong>异常上下文只保存事实，不做复杂推理；复位后完成可靠存储；主机侧完成重型分析。</strong></p><h2 id="总体架构"><a href="#总体架构" class="headerlink" title="总体架构"></a>总体架构</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    A[不可恢复异常\nFault / Panic / Assert / WDT Pretimeout] --&gt; B[最小汇编入口]</span><br><span class="line">    B --&gt; C[识别原始 SP\nMSP / PSP / 安全域 / 核心号]</span><br><span class="line">    C --&gt; D[切换独立 Crash Stack]</span><br><span class="line">    D --&gt; E[一级快照引擎]</span><br><span class="line">    E --&gt; F[CPU 寄存器]</span><br><span class="line">    E --&gt; G[故障状态寄存器]</span><br><span class="line">    E --&gt; H[受边界保护的栈窗口]</span><br><span class="line">    E --&gt; I[线程/系统最小元数据]</span><br><span class="line">    E --&gt; J[最近事件 Ring Buffer]</span><br><span class="line">    F --&gt; K&#123;一级存储后端&#125;</span><br><span class="line">    G --&gt; K</span><br><span class="line">    H --&gt; K</span><br><span class="line">    I --&gt; K</span><br><span class="line">    J --&gt; K</span><br><span class="line">    K --&gt; L[Retention RAM / Backup SRAM / .noinit]</span><br><span class="line">    K --&gt; M[预擦除 Crash Flash Slot]</span><br><span class="line">    K --&gt; N[FRAM / MRAM / 主机直连]</span><br><span class="line">    L --&gt; O[系统复位]</span><br><span class="line">    M --&gt; O</span><br><span class="line">    N --&gt; O</span><br><span class="line">    O --&gt; P[Early Boot Crash Collector]</span><br><span class="line">    P --&gt; Q[校验 magic / version / length / CRC]</span><br><span class="line">    Q --&gt; R[持久化、去重、加密、上传]</span><br><span class="line">    R --&gt; S[ELF + MAP + DWARF + Build ID]</span><br><span class="line">    S --&gt; T[离线符号化 / GDB / 聚类分析]</span><br></pre></td></tr></table></figure><p>这个架构的关键不是保存尽可能多的数据，而是先保证最小快照在最差故障条件下仍有较高成功率。完整性和确定性优先于信息量。一个只包含 PC、LR、SP、CFSR、HFSR 和 256 字节栈窗口但 CRC 正确、固件版本匹配的记录，通常比一个写了一半、版本不明、来源不明的“全内存 Dump”更有价值。</p><h2 id="将问题抽象为通用-Crash-Capture，而不是只处理-HardFault"><a href="#将问题抽象为通用-Crash-Capture，而不是只处理-HardFault" class="headerlink" title="将问题抽象为通用 Crash Capture，而不是只处理 HardFault"></a>将问题抽象为通用 Crash Capture，而不是只处理 HardFault</h2><p>HardFault 只是不可恢复异常的一种入口。通用方案应把各种异常统一映射为 <code>crash_reason</code>，共用记录格式、存储后端和离线工具。</p><table><thead><tr><th>异常来源</th><th>典型触发原因</th><th>一级捕获入口</th><th>需要额外保存的信息</th></tr></thead><tbody><tr><td>HardFault</td><td>可配置 Fault 升级、向量表异常、非法返回</td><td><code>HardFault_Handler</code></td><td>HFSR、CFSR、原始 EXC_RETURN</td></tr><tr><td>MemManage</td><td>MPU 访问违规、栈限制越界</td><td><code>MemManage_Handler</code></td><td>MMFAR、MPU 配置、MSPLIM&#x2F;PSPLIM</td></tr><tr><td>BusFault</td><td>非法地址、总线超时、精确或非精确总线错误</td><td><code>BusFault_Handler</code></td><td>BFAR、总线矩阵&#x2F;外部存储状态</td></tr><tr><td>UsageFault</td><td>未定义指令、除零、非对齐、非法状态</td><td><code>UsageFault_Handler</code></td><td>CFSR.UFSR、PC 附近指令字</td></tr><tr><td>SecureFault</td><td>TrustZone 安全属性、非法跨域访问</td><td><code>SecureFault_Handler</code></td><td>SFSR、SFAR、安全域和安全栈信息</td></tr><tr><td>RTOS Assert&#x2F;Panic</td><td>内核一致性检查失败</td><td>assert&#x2F;panic hook</td><td>文件、行号、当前线程、调度状态</td></tr><tr><td>栈溢出</td><td>线程栈破坏、MSP&#x2F;PSP 越界</td><td>MPU Fault、RTOS hook</td><td>栈边界、水位、canary、TCB 指针</td></tr><tr><td>看门狗</td><td>死循环、锁死、中断关闭过久</td><td>WDT pretimeout&#x2F;NMI 或复位原因</td><td>当前 PC、任务心跳、最后喂狗点</td></tr><tr><td>Brownout</td><td>电源跌落、瞬态复位</td><td>Brownout IRQ&#x2F;NMI 或复位寄存器</td><td>电压状态、复位源、未完成事务</td></tr><tr><td>RISC-V Trap</td><td>访问错误、非法指令、断点</td><td>trap entry</td><td><code>mcause</code>、<code>mepc</code>、<code>mtval</code>、<code>mstatus</code></td></tr><tr><td>Xtensa Panic</td><td>IllegalInstruction、Load&#x2F;StoreProhibited</td><td>panic handler</td><td>EPC、EXCCAUSE、EXCVADDR、任务栈</td></tr></tbody></table><p>因此，接口层不应叫 <code>hardfault_save()</code>，而应抽象为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">crash_capture(reason, arch_context, platform_context)</span><br></pre></td></tr></table></figure><p>其中 <code>arch_context</code> 由 CPU 架构端口提供，<code>platform_context</code> 由 RTOS、SoC 和产品层按需扩展。</p><h2 id="机制与开源方案的对应关系"><a href="#机制与开源方案的对应关系" class="headerlink" title="机制与开源方案的对应关系"></a>机制与开源方案的对应关系</h2><table><thead><tr><th>本文机制</th><th>可参考组件或方案</th><th>核心原理</th><th>可直接使用程度</th><th>主要限制或注意事项</th></tr></thead><tbody><tr><td>最小 Fault 快照到未初始化 RAM</td><td><a href="https://arm-software.github.io/CMSIS-View/latest/group__Fault__Storage.html">CMSIS-View Fault Storage</a></td><td>从 Fault Handler 直接分支到无栈&#x2F;无堆保存函数，将寄存器、故障寄存器、magic、版本和 CRC 写入未初始化 RAM</td><td>Cortex-M 项目可直接集成或参考</td><td>重点是最小 Fault 信息，不等同于完整 RTOS Core Dump</td></tr><tr><td>独立 Crash Stack、寄存器和 RAM 区域 Dump</td><td><a href="https://github.com/adamgreen/CrashCatcher">CrashCatcher</a> + CrashDebug</td><td>汇编入口切换专用栈，收集硬件压栈寄存器、软件保存寄存器和指定 RAM 区域，主机侧进行事后调试</td><td>Cortex-M GCC 工程可集成</td><td>需要自行实现输出后端；示例中的文件系统写入不代表任何故障环境都安全</td></tr><tr><td>架构块 + 内存块 + 多后端 Core Dump</td><td><a href="https://docs.zephyrproject.org/latest/services/debugging/coredump.html">Zephyr Coredump</a></td><td>Fatal Error 时输出 CPU 寄存器和选定内存，支持 logging、UDP、Flash Partition 等后端，并由自定义 GDB Server 还原目标</td><td>Zephyr 项目可直接配置</td><td>Flash 后端仍依赖具体 Flash 控制器和驱动在异常场景下可用</td></tr><tr><td>任务 TCB&#x2F;栈快照、Flash&#x2F;UART、ELF 解析</td><td><a href="https://docs.espressif.com/projects/esp-idf/en/latest/esp32/api-guides/core_dump.html">ESP-IDF Core Dump</a></td><td>Panic Handler 保存任务快照和寄存器到专用分区或 UART，主机用 <code>idf.py coredump-info/debug</code> 分析</td><td>ESP-IDF 项目可直接使用</td><td>数据量与任务数、栈大小相关；Flash cache 损坏场景需要特别配置 IRAM panic 路径</td></tr><tr><td>RTOS 异常栈、异常钩子和 Backtrace</td><td><a href="https://github.com/RT-Thread/rt-thread">RT-Thread</a> Cortex-M CPU Port</td><td>利用异常栈结构、独立中断栈、异常钩子和架构 Backtrace 能力输出上下文</td><td>RT-Thread 项目可在现有异常框架上扩展</td><td>默认打印不等于持久化；需自行增加 Crash Record 和复位后转存</td></tr><tr><td>可配置 Coredump 区域、重启原因、远程聚类</td><td><a href="https://github.com/memfault/memfault-firmware-sdk">Memfault Firmware SDK</a></td><td>Firmware SDK 采集寄存器、任务和选定内存区域，经持久存储和分片上传后进行符号化与聚类</td><td>可参考或集成，云端服务商业化</td><td>SDK 使用 Memfault License，不应简单视为宽松许可证开源组件</td></tr><tr><td>掉电安全的复位后文件持久化</td><td><a href="https://github.com/littlefs-project/littlefs">littlefs</a></td><td>Copy-on-write、元数据日志、掉电回退、动态磨损均衡和有界 RAM</td><td>适合二级持久化</td><td>不应在 HardFault 最小路径中直接 mount&#x2F;open&#x2F;write&#x2F;close</td></tr></tbody></table><p>这些方案共同体现了几条成熟原则：异常入口必须尽量小；原始状态和解析工具分离；记录必须携带格式版本和固件身份；存储后端应可替换；内存 Dump 区域必须受控；真正的调用栈还原应在主机侧完成。</p><h2 id="三级处理模型"><a href="#三级处理模型" class="headerlink" title="三级处理模型"></a>三级处理模型</h2><h3 id="一级：Fault-Context-Capture"><a href="#一级：Fault-Context-Capture" class="headerlink" title="一级：Fault Context Capture"></a>一级：Fault Context Capture</h3><p>一级阶段的任务只有四类：</p><ol><li>取得原始异常上下文，而不是 Handler 已经修改后的上下文。</li><li>把最关键数据复制到可信目标区域。</li><li>以原子方式标记记录完成或部分完成。</li><li>在确定的时间上界内复位或停机。</li></ol><p>禁止项包括：</p><ul><li><code>printf</code>、<code>sprintf</code>、格式化日志和复杂串口驱动。</li><li><code>malloc/free</code>、C++ new&#x2F;delete、容器扩容。</li><li>mutex、semaphore、message queue 等可能阻塞的同步原语。</li><li>普通文件系统的 mount&#x2F;open&#x2F;write&#x2F;fsync&#x2F;close 链路。</li><li>依赖中断完成的 DMA、Flash、UART 驱动。</li><li>遍历不可信链表、线程表、堆块或文件系统元数据。</li><li>在故障处理器中执行符号解析、DWARF 解析或无限深度栈回溯。</li></ul><h3 id="二级：Early-Boot-Commit"><a href="#二级：Early-Boot-Commit" class="headerlink" title="二级：Early Boot Commit"></a>二级：Early Boot Commit</h3><p>二级阶段发生在复位后。此时可以逐步恢复更多能力，但仍应尽早执行，避免 <code>.bss</code> 初始化、内存清零、Bootloader 升级或新的崩溃覆盖一级记录。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant Fault as Fault/Panic Entry</span><br><span class="line">    participant RAM as Retention/.noinit</span><br><span class="line">    participant Reset as System Reset</span><br><span class="line">    participant Boot as Early Boot Collector</span><br><span class="line">    participant Flash as Crash Partition</span><br><span class="line">    participant App as Application</span><br><span class="line"></span><br><span class="line">    Fault-&gt;&gt;RAM: 写 header，state=WRITING</span><br><span class="line">    Fault-&gt;&gt;RAM: 写寄存器/栈窗口/事件</span><br><span class="line">    Fault-&gt;&gt;RAM: 计算 CRC</span><br><span class="line">    Fault-&gt;&gt;RAM: 最后写 state=VALID</span><br><span class="line">    Fault-&gt;&gt;Reset: 复位或等待 WDT</span><br><span class="line">    Reset-&gt;&gt;Boot: 启动早期检查</span><br><span class="line">    Boot-&gt;&gt;RAM: 校验 magic/version/length/CRC</span><br><span class="line">    alt 有效记录</span><br><span class="line">        Boot-&gt;&gt;Flash: 追加到预留 Crash Slot</span><br><span class="line">        Flash--&gt;&gt;Boot: 写入并读回校验</span><br><span class="line">        Boot-&gt;&gt;RAM: 标记已搬运或清除 magic</span><br><span class="line">    else 无效或半写记录</span><br><span class="line">        Boot-&gt;&gt;Boot: 记录 partial/corrupt 统计</span><br><span class="line">    end</span><br><span class="line">    Boot-&gt;&gt;App: 正常启动或进入 Safe Mode</span><br></pre></td></tr></table></figure><p>二级阶段可以执行：</p><ul><li>将 Retention RAM 快照搬到专用 Flash 分区。</li><li>使用 LittleFS、NVS 或数据库保存索引，但前提是文件系统可正常挂载。</li><li>加密、压缩、脱敏、去重和上传。</li><li>根据连续崩溃次数进入 Safe Mode、回滚固件或禁用高风险功能。</li><li>保留“首个故障”与“最近故障”的不同策略。</li></ul><h3 id="三级：Host-side-Post-mortem-Analysis"><a href="#三级：Host-side-Post-mortem-Analysis" class="headerlink" title="三级：Host-side Post-mortem Analysis"></a>三级：Host-side Post-mortem Analysis</h3><p>三级阶段由 PC 或服务器完成：</p><ul><li>依据 Build ID 找到精确 ELF、MAP、DWARF 和 ROM ELF。</li><li>解码 CPU Fault Status Register。</li><li>根据 PC、LR、SP、栈内容和 unwind 信息展开调用栈。</li><li>映射到函数、文件和源码行。</li><li>读取局部变量、线程状态和最近事件。</li><li>对大量设备记录按 fault signature 聚类。</li></ul><h2 id="Cortex-M-异常入口必须先理解硬件自动压栈"><a href="#Cortex-M-异常入口必须先理解硬件自动压栈" class="headerlink" title="Cortex-M 异常入口必须先理解硬件自动压栈"></a>Cortex-M 异常入口必须先理解硬件自动压栈</h2><p>以下实现以 Arm Cortex-M 为主要示例。其他架构应保留同样的分层思想，但用各自的 trap frame、cause register 和特权级状态替换。</p><p>Cortex-M 进入异常时，硬件会把基础异常栈帧压入异常前正在使用的栈。基础帧通常包含：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">R0</span><br><span class="line">R1</span><br><span class="line">R2</span><br><span class="line">R3</span><br><span class="line">R12</span><br><span class="line">LR</span><br><span class="line">PC</span><br><span class="line">xPSR</span><br></pre></td></tr></table></figure><p>CMSIS-Core 的 System Control Block 还提供 CFSR、HFSR、DFSR、MMFAR、BFAR、AFSR 等故障状态寄存器。具体存在性随 Cortex-M 代际和实现变化，应通过 CMSIS 设备头文件和对应架构手册判断，而不是硬编码所有芯片都存在相同寄存器。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">flowchart TB</span><br><span class="line">    A[异常前 Thread/Handler Context] --&gt; B&#123;原来使用哪个 SP?&#125;</span><br><span class="line">    B --&gt;|EXC_RETURN 指示 MSP| C[基础/扩展栈帧位于 MSP 指向区域]</span><br><span class="line">    B --&gt;|EXC_RETURN 指示 PSP| D[基础/扩展栈帧位于 PSP 指向区域]</span><br><span class="line">    C --&gt; E[R0-R3,R12,LR,PC,xPSR]</span><br><span class="line">    D --&gt; E</span><br><span class="line">    E --&gt; F&#123;是否存在 FPU 扩展帧?&#125;</span><br><span class="line">    F --&gt;|是| G[S0-S15,FPSCR,保留字及基础帧]</span><br><span class="line">    F --&gt;|否| H[仅基础帧]</span><br></pre></td></tr></table></figure><p>必须注意以下细节：</p><ol><li><strong>Handler Mode 使用的 SP 不一定等于故障线程原 SP</strong>。Fault Handler 开始执行后，处理器处于 Handler Mode；真正的异常栈帧位置应根据入口时 LR 中的 EXC_RETURN 解码。</li><li><strong>FPU 可能形成扩展栈帧</strong>。启用 FPU 和 lazy stacking 后，栈布局不能只按固定 8 个字处理。</li><li><strong>xPSR 可能指示栈对齐填充</strong>。离线恢复原始 SP 时要考虑异常入口的对齐规则。</li><li><strong>异常可能发生在另一个异常中</strong>。此时原上下文可能本来就处于 Handler Mode，必须保留 IPSR、ICSR 和异常嵌套信息。</li><li><strong>Armv8-M TrustZone 存在安全&#x2F;非安全状态和更多栈指针</strong>。安全固件必须明确哪些信息允许跨域保存和上报。</li><li><strong>栈已经损坏时，任何基于栈的 C 函数调用都可能再次失败</strong>。因此应尽早切换到独立 Crash Stack，或者采用 CMSIS-View Fault Storage 这类不使用栈的最小保存实现。</li></ol><h2 id="最小汇编入口"><a href="#最小汇编入口" class="headerlink" title="最小汇编入口"></a>最小汇编入口</h2><p>下面代码只表达设计方法，不是所有 Cortex-M、编译器、FPU 和 TrustZone 配置下可直接复制的最终版本。正式工程应针对 GCC、Arm Compiler、IAR 以及具体 CPU 架构分别验证生成指令。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief Cortex-M hardware-stacked basic exception frame.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="type">uint32_t</span> r0;</span><br><span class="line">    <span class="type">uint32_t</span> r1;</span><br><span class="line">    <span class="type">uint32_t</span> r2;</span><br><span class="line">    <span class="type">uint32_t</span> r3;</span><br><span class="line">    <span class="type">uint32_t</span> r12;</span><br><span class="line">    <span class="type">uint32_t</span> lr;</span><br><span class="line">    <span class="type">uint32_t</span> pc;</span><br><span class="line">    <span class="type">uint32_t</span> xpsr;</span><br><span class="line">&#125; <span class="type">crash_cm_basic_frame_t</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief Capture a fatal Cortex-M exception using the original exception frame.</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * @param frame      Pointer to the hardware-stacked exception frame.</span></span><br><span class="line"><span class="comment"> * @param exc_return EXC_RETURN value captured from LR at handler entry.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line">__attribute__((<span class="keyword">noreturn</span>))</span><br><span class="line"><span class="type">void</span> <span class="title function_">crash_capture_cortex_m</span><span class="params">(<span class="type">const</span> <span class="type">crash_cm_basic_frame_t</span> *frame, <span class="type">uint32_t</span> exc_return)</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief Minimal HardFault entry. No C prologue or local stack is allowed here.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line">__attribute__((naked)) <span class="type">void</span> <span class="title function_">HardFault_Handler</span><span class="params">(<span class="type">void</span>)</span></span><br><span class="line">&#123;</span><br><span class="line">    __asm <span class="title function_">volatile</span><span class="params">(</span></span><br><span class="line"><span class="params">        <span class="string">&quot;tst lr, #4            \n&quot;</span></span></span><br><span class="line"><span class="params">        <span class="string">&quot;ite eq                \n&quot;</span></span></span><br><span class="line"><span class="params">        <span class="string">&quot;mrseq r0, msp         \n&quot;</span></span></span><br><span class="line"><span class="params">        <span class="string">&quot;mrsne r0, psp         \n&quot;</span></span></span><br><span class="line"><span class="params">        <span class="string">&quot;mov r1, lr            \n&quot;</span></span></span><br><span class="line"><span class="params">        <span class="string">&quot;b crash_capture_cortex_m \n&quot;</span>)</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这段入口完成两件事：根据 EXC_RETURN 选择 MSP 或 PSP，并把 EXC_RETURN 传给后续捕获函数。更稳健的量产实现还会：</p><ul><li>在汇编中保存 R4-R11。</li><li>读取 MSP、PSP、CONTROL、PRIMASK、BASEPRI、FAULTMASK、IPSR。</li><li>尽早切换到单独的 Crash Stack。</li><li>对 FPU 扩展帧和 lazy stacking 做架构相关处理。</li><li>对 Armv8-M 保存 MSPLIM、PSPLIM、安全域状态和 SecureFault 寄存器。</li><li>对多核系统保存 core id，并通过原子变量争夺 Crash Owner。</li></ul><h2 id="为什么需要独立-Crash-Stack"><a href="#为什么需要独立-Crash-Stack" class="headerlink" title="为什么需要独立 Crash Stack"></a>为什么需要独立 Crash Stack</h2><p>如果故障是线程栈溢出、返回地址破坏、错误 SP、越界写或内存踩踏引起，继续使用原栈执行 C 代码会显著提高二次 Fault 概率。CrashCatcher 的重要设计就是切换到专用 <code>g_crashCatcherStack</code> 后再运行收集逻辑。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    A[原线程栈\n可能溢出/损坏] --&gt; B[异常硬件压栈]</span><br><span class="line">    B --&gt; C[汇编入口保存原 SP]</span><br><span class="line">    C --&gt; D[切换专用 Crash Stack]</span><br><span class="line">    D --&gt; E[执行有界 C 捕获函数]</span><br><span class="line">    E --&gt; F[写 Crash Buffer]</span><br></pre></td></tr></table></figure><p>Crash Stack 应满足：</p><ol><li>静态分配，链接时确定地址和大小。</li><li>与普通线程栈、堆、DMA Buffer 和 <code>.bss</code> 清零区域隔离。</li><li>最好带 MPU Guard 或 stack limit。</li><li>编译期和运行期测量最大使用量，保留足够余量。</li><li>不在 Crash Stack 上创建大数组；所有大数据复制到 Crash Buffer。</li><li>若处理器支持 TCM 或独立 SRAM，可放入更可靠且不依赖 cache 的 RAM。</li></ol><p>示例链接脚本：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">/* Retained across software reset; startup code must not clear this section. */</span><br><span class="line">.noinit.crash (NOLOAD) :</span><br><span class="line">&#123;</span><br><span class="line">    . = ALIGN(8);</span><br><span class="line">    __crash_record_start__ = .;</span><br><span class="line">    KEEP(*(.noinit.crash_record))</span><br><span class="line">    __crash_record_end__ = .;</span><br><span class="line"></span><br><span class="line">    . = ALIGN(8);</span><br><span class="line">    __crash_stack_bottom__ = .;</span><br><span class="line">    KEEP(*(.noinit.crash_stack))</span><br><span class="line">    __crash_stack_top__ = .;</span><br><span class="line">&#125; &gt; RETENTION_RAM</span><br></pre></td></tr></table></figure><p>如果芯片没有真正的 Retention RAM，而 <code>.noinit</code> 仍位于普通 SRAM，则必须确认复位类型不会断电或由启动代码清除。Power-on Reset、Brownout Reset 和某些 Boot ROM 流程可能不保留普通 SRAM，不能把 <code>.noinit</code> 当作绝对可靠的非易失存储。</p><h2 id="一级快照应该保存什么"><a href="#一级快照应该保存什么" class="headerlink" title="一级快照应该保存什么"></a>一级快照应该保存什么</h2><p>建议按优先级分层，而不是一次性把所有数据都塞入 Handler。</p><h3 id="P0：必须保存"><a href="#P0：必须保存" class="headerlink" title="P0：必须保存"></a>P0：必须保存</h3><ul><li>magic、格式版本、记录长度、状态、CRC。</li><li>crash reason、复位原因、CPU 架构、核心号、安全域。</li><li>固件 Build ID、镜像版本、镜像槽位、链接基址。</li><li>PC、LR、SP、xPSR&#x2F;状态寄存器。</li><li>EXC_RETURN 或对应架构的 exception return 信息。</li><li>Cortex-M 的 CFSR、HFSR、DFSR、MMFAR、BFAR、AFSR；RISC-V 的 <code>mcause/mepc/mtval/mstatus</code>；Xtensa 的 EPC、EXCCAUSE、EXCVADDR。</li><li>当前时间基准：uptime tick、RTC 时间、启动计数。</li><li>至少 128～512 字节的受保护栈窗口。</li></ul><h3 id="P1：高价值信息"><a href="#P1：高价值信息" class="headerlink" title="P1：高价值信息"></a>P1：高价值信息</h3><ul><li>R0-R12、MSP、PSP、CONTROL、PRIMASK、BASEPRI、FAULTMASK、IPSR。</li><li>FPU 状态、FPSCR 和必要的浮点寄存器。</li><li>当前线程 ID、TCB 地址、线程栈起止地址、线程名的固定长度副本。</li><li>最近几十条二进制事件记录。</li><li>当前中断号、临界区嵌套、调度锁嵌套、heap watermark。</li><li>关键外设状态，例如 DMA channel、Flash controller busy&#x2F;error、总线错误寄存器。</li></ul><h3 id="P2：资源允许时保存"><a href="#P2：资源允许时保存" class="headerlink" title="P2：资源允许时保存"></a>P2：资源允许时保存</h3><ul><li>当前线程完整已用栈。</li><li>所有线程的 TCB 和栈片段。</li><li>全局 RAM、特定内存池、消息队列和协议状态。</li><li>外部存储控制器寄存器和缓存标签。</li><li>业务级诊断区，例如播放状态机、网络连接状态、最近命令。</li></ul><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[Crash Capture Budget] --&gt; B[P0 最小记录\n必须成功]</span><br><span class="line">    B --&gt; C&#123;剩余时间和存储是否足够?&#125;</span><br><span class="line">    C --&gt;|否| D[提交 P0 并复位]</span><br><span class="line">    C --&gt;|是| E[P1 当前线程和最近事件]</span><br><span class="line">    E --&gt; F&#123;仍有预算?&#125;</span><br><span class="line">    F --&gt;|否| G[提交 P0+P1]</span><br><span class="line">    F --&gt;|是| H[P2 多线程/大内存区域]</span><br><span class="line">    H --&gt; I[提交完整记录]</span><br></pre></td></tr></table></figure><p>应通过配置决定层级，而不是把所有产品都编译成最大 Dump。资源很小的 MCU 可以只保存 P0；高端 MCU 或带 FRAM&#x2F;eMMC 的系统可以保存 P0+P1+部分 P2。</p><h2 id="Crash-Record-设计"><a href="#Crash-Record-设计" class="headerlink" title="Crash Record 设计"></a>Crash Record 设计</h2><p>一个可维护的记录格式应具备：</p><ul><li>固定头部，便于 Bootloader 或 PC 工具快速识别。</li><li>明确的 <code>format_version</code> 和 <code>header_size</code>。</li><li>整体长度和各段长度，避免解析越界。</li><li>架构无关头 + 架构相关块 + 平台扩展块。</li><li>Build ID、镜像版本和地址模型。</li><li>CRC32 或更强校验。</li><li>写入状态和 partial flags。</li><li>允许旧解析器跳过未知 TLV。</li></ul><p>推荐使用 TLV 或 section table，而不是一个永远扩大的 C 结构体。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    A[Fixed Header] --&gt; B[Arch Registers TLV]</span><br><span class="line">    B --&gt; C[Fault Status TLV]</span><br><span class="line">    C --&gt; D[Stack Region TLV]</span><br><span class="line">    D --&gt; E[RTOS Thread TLV]</span><br><span class="line">    E --&gt; F[Recent Events TLV]</span><br><span class="line">    F --&gt; G[Platform Registers TLV]</span><br><span class="line">    G --&gt; H[CRC / Commit Marker]</span><br></pre></td></tr></table></figure><p>参考结构：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> CRASH_RECORD_MAGIC          UINT32_C(0x43525348) <span class="comment">/* &quot;CRSH&quot; */</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> CRASH_RECORD_FORMAT_VERSION UINT16_C(2)</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief Persistent crash record state encoded as one-way Flash bit transitions.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">enum</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    CRASH_RECORD_STATE_ERASED  = <span class="number">0xFFFFFFFFu</span>,</span><br><span class="line">    CRASH_RECORD_STATE_WRITING = <span class="number">0xFFFFFFFEu</span>,</span><br><span class="line">    CRASH_RECORD_STATE_VALID   = <span class="number">0xFFFFFFFCu</span>,</span><br><span class="line">    CRASH_RECORD_STATE_USED    = <span class="number">0xFFFFFFF8u</span>,</span><br><span class="line">&#125; <span class="type">crash_record_state_t</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief Architecture-independent crash record header.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="type">uint32_t</span> magic;</span><br><span class="line">    <span class="type">uint16_t</span> format_version;</span><br><span class="line">    <span class="type">uint16_t</span> header_size;</span><br><span class="line">    <span class="type">uint32_t</span> total_size;</span><br><span class="line">    <span class="type">uint32_t</span> state;</span><br><span class="line">    <span class="type">uint32_t</span> flags;</span><br><span class="line">    <span class="type">uint32_t</span> sequence;</span><br><span class="line">    <span class="type">uint32_t</span> crash_reason;</span><br><span class="line">    <span class="type">uint32_t</span> reset_reason;</span><br><span class="line">    <span class="type">uint32_t</span> architecture;</span><br><span class="line">    <span class="type">uint32_t</span> core_id;</span><br><span class="line">    <span class="type">uint64_t</span> uptime_ticks;</span><br><span class="line">    <span class="type">uint8_t</span>  build_id[<span class="number">20</span>];</span><br><span class="line">    <span class="type">uint32_t</span> image_version;</span><br><span class="line">    <span class="type">uint32_t</span> image_slot;</span><br><span class="line">    <span class="type">uint32_t</span> payload_crc32;</span><br><span class="line">    <span class="type">uint32_t</span> header_crc32;</span><br><span class="line">&#125; <span class="type">crash_record_header_t</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief Generic TLV header used to extend crash records without breaking parsers.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="type">uint16_t</span> type;</span><br><span class="line">    <span class="type">uint16_t</span> version;</span><br><span class="line">    <span class="type">uint32_t</span> length;</span><br><span class="line">&#125; <span class="type">crash_tlv_header_t</span>;</span><br></pre></td></tr></table></figure><h3 id="为什么-Build-ID-是必需字段"><a href="#为什么-Build-ID-是必需字段" class="headerlink" title="为什么 Build ID 是必需字段"></a>为什么 Build ID 是必需字段</h3><p>仅记录“V1.2.3”通常不够，因为同一版本号可能存在不同编译时间、不同编译选项、不同链接布局或临时补丁。PC、LR 只有在与准确 ELF 对应时才有意义。</p><p>建议 CI 为每个镜像生成不可冲突的 Build ID，并归档：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">firmware.bin</span><br><span class="line">firmware.elf</span><br><span class="line">firmware.map</span><br><span class="line">firmware.sym</span><br><span class="line">compiler version</span><br><span class="line">linker script</span><br><span class="line">Kconfig/menuconfig</span><br><span class="line">source commit</span><br><span class="line">submodule commit</span><br></pre></td></tr></table></figure><p>可在 GNU 链接阶段启用或保留 ELF Build ID，也可以把 Git commit、配置哈希和镜像哈希组合成产品自己的 128&#x2F;160 位标识。Crash Record 中应保存二进制值，不要只存可能被截断的字符串。</p><h2 id="原子提交和掉电一致性"><a href="#原子提交和掉电一致性" class="headerlink" title="原子提交和掉电一致性"></a>原子提交和掉电一致性</h2><p>记录写入时必须假设随时会发生二次复位、看门狗到期或掉电。</p><p>推荐顺序：</p><ol><li>选择一个已擦除 Slot。</li><li>写 <code>state=WRITING</code> 或在 RAM 中清零记录区。</li><li>写固定头中除 CRC 和 VALID 外的字段。</li><li>写 TLV payload。</li><li>计算并写 payload CRC、header CRC。</li><li>执行必要的 memory barrier、cache clean 或存储同步。</li><li>最后一步把状态从 WRITING 单向编程为 VALID。</li></ol><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">stateDiagram-v2</span><br><span class="line">    [*] --&gt; ERASED</span><br><span class="line">    ERASED --&gt; WRITING: 开始新记录</span><br><span class="line">    WRITING --&gt; VALID: payload和CRC完成后最后提交</span><br><span class="line">    WRITING --&gt; CORRUPT: 中途复位/掉电</span><br><span class="line">    VALID --&gt; USED: 已上传或已转存</span><br><span class="line">    VALID --&gt; PRESERVED: 首故障保护</span><br><span class="line">    USED --&gt; ERASED: 正常运行期后台擦除</span><br><span class="line">    CORRUPT --&gt; ERASED: 启动后回收</span><br></pre></td></tr></table></figure><p>对 NOR Flash，应利用“擦除后为 1、编程只能从 1 变 0”的性质设计状态位，避免完成标记需要 0 变 1。Flash 擦除绝不能放在紧急 Fault 路径中，应在系统健康时预擦除 Crash Slot。</p><p>对 Retention RAM，可先写 payload 和 CRC，最后写 magic；启动时只有 magic、长度和 CRC 都合法才接收。若 CPU 有 data cache，必须确认写入目标是否 cacheable，并在复位前执行正确的 cache clean 和屏障，否则 RAM 或外部存储中的内容可能尚未真正落地。</p><h2 id="存储后端如何选择"><a href="#存储后端如何选择" class="headerlink" title="存储后端如何选择"></a>存储后端如何选择</h2><p>优先级通常如下：</p><ol><li><strong>Retention RAM&#x2F;Backup SRAM</strong>：写入快、依赖少，适合作为一级快照。</li><li><strong>预擦除的专用内部 Flash Slot</strong>：掉电后仍保留，但要求异常时 Flash 控制器可用。</li><li><strong>FRAM&#x2F;MRAM</strong>：字节写、低延迟、无需擦除，非常适合 Crash Record，但成本和容量受限。</li><li><strong>外部 QSPI NOR 的专用分区</strong>：容量大，但可能与代码执行、cache、XIP 和业务写入共享控制器。</li><li><strong>UART&#x2F;SWO&#x2F;USB&#x2F;网络直传</strong>：只适合实验室或确认链路简单可靠的场景，不应作为现场唯一方案。</li><li><strong>普通文件系统</strong>：仅用于复位后的二级持久化。</li></ol><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[选择一级存储] --&gt; B&#123;有 Retention/Backup RAM?&#125;</span><br><span class="line">    B --&gt;|有| C[先写 RAM，复位后转存]</span><br><span class="line">    B --&gt;|无| D&#123;有 FRAM/MRAM?&#125;</span><br><span class="line">    D --&gt;|有| E[直接写固定地址记录]</span><br><span class="line">    D --&gt;|无| F&#123;Flash 有预擦除 Slot\n且故障路径可用?&#125;</span><br><span class="line">    F --&gt;|有| G[RAMFUNC 轮询写入]</span><br><span class="line">    F --&gt;|无| H&#123;有可靠调试链路?&#125;</span><br><span class="line">    H --&gt;|有| I[最小 UART/SWO Hex Dump]</span><br><span class="line">    H --&gt;|无| J[至少保存复位原因和少量 Backup Register]</span><br></pre></td></tr></table></figure><h3 id="直接写-Flash-的前置条件"><a href="#直接写-Flash-的前置条件" class="headerlink" title="直接写 Flash 的前置条件"></a>直接写 Flash 的前置条件</h3><p>只有同时满足下列条件时，才建议在一级 Fault 路径直接写 Flash：</p><ul><li>Crash 区已经擦除，不需要执行耗时擦除。</li><li>写入函数、常量和必要数据位于 RAM&#x2F;ROM，不依赖当前不可访问的 XIP Flash。</li><li>驱动使用轮询，不依赖中断、DMA、mutex 或调度器。</li><li>Flash 控制器不处于擦除、编程、挂起或错误状态。</li><li>写入目标与当前执行 bank 的限制已评估。</li><li>电压和时钟满足编程条件。</li><li>有严格写入长度和超时上界。</li><li>看门狗预算允许；或者只保存 P0 后立即复位。</li></ul><p>ESP-IDF 文档特别说明，panic handler 若位于 Flash，而故障发生时 Flash cache 被关闭或损坏，重新启用 cache 会增加风险；把关键 panic 路径放入 IRAM 可以降低对 Flash 可执行路径的依赖。这个思路对所有 XIP MCU 都适用。</p><h2 id="安全复制栈窗口，避免二次-Fault"><a href="#安全复制栈窗口，避免二次-Fault" class="headerlink" title="安全复制栈窗口，避免二次 Fault"></a>安全复制栈窗口，避免二次 Fault</h2><p>不能直接执行：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">memcpy</span>(record-&gt;<span class="built_in">stack</span>, (<span class="type">void</span> *)sp, <span class="number">1024</span>);</span><br></pre></td></tr></table></figure><p>因为 <code>sp</code> 可能已经越界、未对齐、指向外设、指向不可访问安全域，或距离 SRAM 末端不足 1024 字节。</p><p>正确流程是：</p><ol><li>从链接脚本、MPU 配置或 RTOS TCB 获得允许读取的 RAM 区间。</li><li>判断 SP 是否落入已知栈区域。</li><li>对起止地址执行防溢出的范围裁剪。</li><li>只按架构允许的访问宽度读取。</li><li>限制最大长度。</li><li>若不能证明安全，只保存寄存器，不复制栈。</li></ol><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief Copy a bounded stack window from a validated RAM range.</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * @param dst          Destination buffer in the crash record.</span></span><br><span class="line"><span class="comment"> * @param dst_capacity Destination capacity in bytes.</span></span><br><span class="line"><span class="comment"> * @param sp           Fault-time stack pointer.</span></span><br><span class="line"><span class="comment"> * @param stack_low    Inclusive stack lower bound.</span></span><br><span class="line"><span class="comment"> * @param stack_high   Exclusive stack upper bound.</span></span><br><span class="line"><span class="comment"> * @return Number of bytes copied.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="type">static</span> <span class="type">size_t</span> <span class="title function_">crash_copy_stack_window</span><span class="params">(<span class="type">uint8_t</span> *dst,</span></span><br><span class="line"><span class="params">                                      <span class="type">size_t</span> dst_capacity,</span></span><br><span class="line"><span class="params">                                      <span class="type">uintptr_t</span> sp,</span></span><br><span class="line"><span class="params">                                      <span class="type">uintptr_t</span> stack_low,</span></span><br><span class="line"><span class="params">                                      <span class="type">uintptr_t</span> stack_high)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">size_t</span> available;</span><br><span class="line">    <span class="type">size_t</span> copy_len;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> ((dst == <span class="literal">NULL</span>) || (stack_low &gt;= stack_high) || (sp &lt; stack_low) || (sp &gt;= stack_high))</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0U</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    available = (<span class="type">size_t</span>)(stack_high - sp);</span><br><span class="line">    copy_len = (available &lt; dst_capacity) ? available : dst_capacity;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* The production implementation may use guarded word reads instead of libc memcpy. */</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">size_t</span> i = <span class="number">0U</span>; i &lt; copy_len; ++i)</span><br><span class="line">    &#123;</span><br><span class="line">        dst[i] = ((<span class="type">const</span> <span class="keyword">volatile</span> <span class="type">uint8_t</span> *)sp)[i];</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> copy_len;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这段代码仍假定整个声明的栈区可读。对于有 MPU、ECC、总线超时或安全域隔离的系统，应进一步采用平台安全读取函数，并允许在读取失败时提前结束、设置 <code>CRASH_FLAG_PARTIAL_STACK</code>，而不是让整个记录失败。</p><h2 id="调用栈保存不等于在-Handler-中完成-Backtrace"><a href="#调用栈保存不等于在-Handler-中完成-Backtrace" class="headerlink" title="调用栈保存不等于在 Handler 中完成 Backtrace"></a>调用栈保存不等于在 Handler 中完成 Backtrace</h2><p>“保存调用栈”通常有四个层级：</p><h3 id="层级一：PC-LR"><a href="#层级一：PC-LR" class="headerlink" title="层级一：PC + LR"></a>层级一：PC + LR</h3><p>最小成本，能定位当前指令和直接调用者，但对内联、尾调用、异常返回和 LR 已覆盖的场景信息有限。</p><h3 id="层级二：原始栈窗口-地址扫描"><a href="#层级二：原始栈窗口-地址扫描" class="headerlink" title="层级二：原始栈窗口 + 地址扫描"></a>层级二：原始栈窗口 + 地址扫描</h3><p>保存 SP 后的一段原始内存，PC 工具扫描落在可执行代码区的 Thumb 地址，再结合反汇编判断哪些值可能是返回地址。实现简单，但存在误报，不能把扫描结果当作严格调用链。</p><h3 id="层级三：帧指针链"><a href="#层级三：帧指针链" class="headerlink" title="层级三：帧指针链"></a>层级三：帧指针链</h3><p>使用固定帧指针可以较稳定地沿 frame chain 回溯，但会增加寄存器压力和代码开销。不同编译器、优化级别和 ABI 对 R7&#x2F;R11 使用不同，必须由实际工具链验证。可只对关键模块使用 <code>-fno-omit-frame-pointer</code>，不必全局关闭优化。</p><h3 id="层级四：Unwind-Table-DWARF-RTOS-Core-Dump"><a href="#层级四：Unwind-Table-DWARF-RTOS-Core-Dump" class="headerlink" title="层级四：Unwind Table &#x2F; DWARF &#x2F; RTOS Core Dump"></a>层级四：Unwind Table &#x2F; DWARF &#x2F; RTOS Core Dump</h3><p>保存完整寄存器和足够栈内存后，由 GDB、CrashDebug、Zephyr GDB Server、ESP-IDF espcoredump 或自研解析器根据 unwind tables、函数序言和调试信息还原调用栈。这是信息最完整的方式，但 ELF 和故障固件必须严格匹配。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    A[故障 PC/LR/SP] --&gt; B[原始栈字节]</span><br><span class="line">    B --&gt; C&#123;离线展开策略&#125;</span><br><span class="line">    C --&gt; D[帧指针链]</span><br><span class="line">    C --&gt; E[ARM EHABI/.ARM.exidx]</span><br><span class="line">    C --&gt; F[DWARF CFI]</span><br><span class="line">    C --&gt; G[启发式返回地址扫描]</span><br><span class="line">    D --&gt; H[符号化调用栈]</span><br><span class="line">    E --&gt; H</span><br><span class="line">    F --&gt; H</span><br><span class="line">    G --&gt; I[候选调用路径]</span><br></pre></td></tr></table></figure><p>因此，Fault Handler 中最合理的行为是保存“可供展开的原料”，而不是调用一个复杂 backtrace 函数打印几十层符号。尤其当故障本身由栈破坏引起时，在线回溯很容易再次访问非法地址。</p><h2 id="RTOS-场景下如何保存线程信息"><a href="#RTOS-场景下如何保存线程信息" class="headerlink" title="RTOS 场景下如何保存线程信息"></a>RTOS 场景下如何保存线程信息</h2><p>RTOS Core Dump 的难点不是读取当前线程，而是内核数据结构可能已经损坏，且遍历所有线程会扩大故障路径。</p><h3 id="当前线程最小信息"><a href="#当前线程最小信息" class="headerlink" title="当前线程最小信息"></a>当前线程最小信息</h3><p>优先保存：</p><ul><li>当前 TCB 指针。</li><li>当前线程静态 ID 和固定长度名称副本。</li><li>线程栈起止地址和当前 SP。</li><li>线程优先级、状态、CPU affinity。</li><li>调度锁、中断嵌套和临界区计数。</li></ul><p>这些信息最好在系统正常运行时维护到一个只读或简单的 <code>crash_runtime_info</code> 中，而不是故障时遍历内核对象。</p><h3 id="所有线程快照"><a href="#所有线程快照" class="headerlink" title="所有线程快照"></a>所有线程快照</h3><p>若系统资源允许，可参考 Zephyr <code>DEBUG_COREDUMP_MEMORY_DUMP_THREADS</code> 或 ESP-IDF 的任务 TCB&#x2F;Stack 快照：</p><ol><li>先保存当前故障线程。</li><li>再按固定上限保存其他线程的 TCB 摘要。</li><li>每个线程只保存已用栈或顶部固定窗口。</li><li>对所有指针做地址范围验证。</li><li>达到时间或容量预算立即停止，并设置 partial flag。</li></ol><h3 id="RT-Thread-的接入思路"><a href="#RT-Thread-的接入思路" class="headerlink" title="RT-Thread 的接入思路"></a>RT-Thread 的接入思路</h3><p>RT-Thread Cortex-M 端口已经具备异常栈结构、独立中断栈、异常处理和 backtrace 相关能力。量产方案可在 <code>rt_exception_hook</code> 或架构 Fault 入口中加入一级 Crash Capture，但不应只依赖 <code>rt_kprintf</code> 输出。更稳妥的做法是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">RT-Thread Fault Entry</span><br><span class="line">    -&gt; 保存 exception_stack 原始内容</span><br><span class="line">    -&gt; 保存 rt_thread_self() 的静态摘要</span><br><span class="line">    -&gt; 写 .noinit/Backup SRAM Crash Record</span><br><span class="line">    -&gt; 复位</span><br><span class="line">    -&gt; board early init 校验并转存到 Flash</span><br></pre></td></tr></table></figure><p>若当前故障发生在中断上下文，<code>rt_thread_self()</code> 只能表示被中断线程，不代表当前正在执行的 ISR，因此还应保存 IPSR&#x2F;IRQ number 和中断嵌套层级。</p><h2 id="最近事件-Ring-Buffer-比大量文本日志更适合-Crash-Capture"><a href="#最近事件-Ring-Buffer-比大量文本日志更适合-Crash-Capture" class="headerlink" title="最近事件 Ring Buffer 比大量文本日志更适合 Crash Capture"></a>最近事件 Ring Buffer 比大量文本日志更适合 Crash Capture</h2><p>Fault Handler 中临时打印文本往往失败，但系统可以在正常运行时维护一个固定大小的二进制 Flight Recorder。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief Compact event stored in a lock-free diagnostic ring.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="type">uint32_t</span> timestamp;</span><br><span class="line">    <span class="type">uint16_t</span> event_id;</span><br><span class="line">    <span class="type">uint16_t</span> source_id;</span><br><span class="line">    <span class="type">uint32_t</span> arg0;</span><br><span class="line">    <span class="type">uint32_t</span> arg1;</span><br><span class="line">&#125; <span class="type">crash_event_t</span>;</span><br></pre></td></tr></table></figure><p>事件可以包括：</p><ul><li>线程切换。</li><li>ISR 进入&#x2F;退出。</li><li>Flash 擦写开始&#x2F;完成。</li><li>DMA 启动&#x2F;完成&#x2F;超时。</li><li>内存分配失败。</li><li>协议状态切换。</li><li>看门狗喂狗点。</li><li>电源、电压和温度告警。</li><li>业务状态机关键节点。</li></ul><p>一级捕获只需要复制 ring 的 head、sequence 和最近 N 条固定长度记录，不进行字符串格式化。主机工具再根据 <code>event_id</code> 和固件版本映射为文本。这样既降低故障路径复杂度，也能还原“崩溃前发生了什么”。</p><h2 id="多核系统的-Crash-Owner-和其他核心冻结"><a href="#多核系统的-Crash-Owner-和其他核心冻结" class="headerlink" title="多核系统的 Crash Owner 和其他核心冻结"></a>多核系统的 Crash Owner 和其他核心冻结</h2><p>双核或多核 MCU 可能出现多个核心同时进入 panic，若都写同一 Crash Slot 会互相破坏。</p><p>推荐设计：</p><ol><li>使用位于共享、原子可访问 RAM 中的 <code>crash_owner</code>。</li><li>第一个进入的核心通过 compare-and-swap 成为 owner。</li><li>Owner 发送 NMI&#x2F;IPI 请求其他核心进入 secondary crash handler。</li><li>每个核心把自己的寄存器写到独立 per-core 区域。</li><li>Owner 负责最终 CRC、commit 和复位。</li><li>若其他核心在固定时间内未响应，设置 missing-core mask 后继续提交。</li></ol><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant C0 as Core 0</span><br><span class="line">    participant O as Crash Owner State</span><br><span class="line">    participant C1 as Core 1</span><br><span class="line">    participant R as Shared Crash Record</span><br><span class="line"></span><br><span class="line">    C0-&gt;&gt;O: CAS owner = Core0</span><br><span class="line">    O--&gt;&gt;C0: 成功</span><br><span class="line">    C0-&gt;&gt;C1: 发送 NMI/IPI Freeze</span><br><span class="line">    C1-&gt;&gt;R: 写 Core1 寄存器块</span><br><span class="line">    C1-&gt;&gt;C0: 设置 ready bit</span><br><span class="line">    C0-&gt;&gt;R: 写 Core0 和公共块</span><br><span class="line">    C0-&gt;&gt;R: CRC + VALID</span><br><span class="line">    C0-&gt;&gt;C0: 系统复位</span><br></pre></td></tr></table></figure><p>不能无限等待另一个核心，否则故障核心可能因为对方锁死而无法完成最小记录。所有等待都必须有 cycle counter 或硬件 timer 上界。</p><h2 id="看门狗、复位循环和-Safe-Mode"><a href="#看门狗、复位循环和-Safe-Mode" class="headerlink" title="看门狗、复位循环和 Safe Mode"></a>看门狗、复位循环和 Safe Mode</h2><p>Crash Capture 必须与 Watchdog 策略协同，而不是简单地在 Handler 中无限喂狗。</p><h3 id="推荐策略"><a href="#推荐策略" class="headerlink" title="推荐策略"></a>推荐策略</h3><ul><li>P0 快照时间必须小于最短看门狗剩余时间。</li><li>如果有 Window Watchdog，不要在非法窗口喂狗。</li><li>若有 pretimeout interrupt&#x2F;NMI，先保存轻量 Snapshot，再让最终 WDT Reset 发生。</li><li>Handler 最多喂狗一次或按严格次数喂狗，防止故障代码永久卡住。</li><li>Early Boot 统计连续崩溃次数和相同签名。</li><li>在短时间内重复崩溃超过阈值时进入 Safe Mode、回滚或关闭高风险模块。</li></ul><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">crash_loop_count += 1</span><br><span class="line">if same_build_id &amp;&amp; boot_uptime &lt; short_boot_threshold:</span><br><span class="line">    rapid_crash_count += 1</span><br><span class="line">else:</span><br><span class="line">    rapid_crash_count = 0</span><br><span class="line"></span><br><span class="line">if rapid_crash_count &gt;= safe_mode_threshold:</span><br><span class="line">    enter_safe_mode()</span><br></pre></td></tr></table></figure><p>Crash Loop 统计也必须掉电安全，并避免每次启动都擦写同一 Flash 字。可使用 Backup Register、FRAM、带磨损均衡的 KV 或追加式小日志。</p><h2 id="安全、隐私和可信边界"><a href="#安全、隐私和可信边界" class="headerlink" title="安全、隐私和可信边界"></a>安全、隐私和可信边界</h2><p>Crash Dump 可能包含密码、密钥、Token、用户数据、音频片段、网络报文和安全域内容。量产系统不能只考虑调试价值，还要考虑数据泄露。</p><p>建议：</p><ol><li>定义允许 Dump 的内存白名单，不默认 Dump 全 SRAM。</li><li>对密钥区、加密引擎上下文和用户隐私 Buffer 设置 exclusion region。</li><li>一级记录只做最小快照；复杂加密放到复位后的可信阶段。</li><li>Crash 分区设置访问控制，量产接口需要鉴权。</li><li>上传时使用设备身份认证和传输加密。</li><li>服务器按产品、版本、权限和保留周期管理符号和 Dump。</li><li>TrustZone 系统由 Secure 侧决定哪些 Secure Fault 信息可以暴露给 Non-secure 世界。</li><li>记录格式中加入 <code>redaction_policy_version</code>，便于审计。</li></ol><p>不能为了调试方便而把整个安全 RAM 或完整用户数据无条件写入可直接读取的外部 Flash。</p><h2 id="一级捕获伪代码"><a href="#一级捕获伪代码" class="headerlink" title="一级捕获伪代码"></a>一级捕获伪代码</h2><p>下面示例展示“最小记录优先、逐级扩展、最后提交”的结构。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief Capture a fatal exception into a preallocated crash record.</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * @param reason     Architecture-independent crash reason.</span></span><br><span class="line"><span class="comment"> * @param arch_ctx   Architecture-specific exception context.</span></span><br><span class="line"><span class="comment"> * @param platform   Platform runtime information maintained during normal operation.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line">__attribute__((<span class="keyword">noreturn</span>))</span><br><span class="line"><span class="type">void</span> <span class="title function_">crash_capture</span><span class="params">(<span class="type">uint32_t</span> reason,</span></span><br><span class="line"><span class="params">                   <span class="type">const</span> <span class="type">crash_arch_context_t</span> *arch_ctx,</span></span><br><span class="line"><span class="params">                   <span class="type">const</span> <span class="type">crash_platform_context_t</span> *platform)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">crash_record_writer_t</span> writer;</span><br><span class="line">    <span class="type">uint32_t</span> irq_state;</span><br><span class="line"></span><br><span class="line">    irq_state = crash_disable_maskable_interrupts();</span><br><span class="line">    crash_switch_to_dedicated_stack_if_required();</span><br><span class="line"></span><br><span class="line">    crash_writer_begin(&amp;writer, &amp;g_crash_record, <span class="keyword">sizeof</span>(g_crash_record));</span><br><span class="line">    crash_writer_set_state(&amp;writer, CRASH_RECORD_STATE_WRITING);</span><br><span class="line"></span><br><span class="line">    crash_write_fixed_header(&amp;writer, reason, arch_ctx, platform);</span><br><span class="line">    crash_write_arch_registers(&amp;writer, arch_ctx);</span><br><span class="line">    crash_write_fault_status(&amp;writer, arch_ctx);</span><br><span class="line">    crash_write_validated_stack_window(&amp;writer, arch_ctx, platform);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (crash_budget_remaining())</span><br><span class="line">    &#123;</span><br><span class="line">        crash_write_current_thread(&amp;writer, platform);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (crash_budget_remaining())</span><br><span class="line">    &#123;</span><br><span class="line">        crash_write_recent_events(&amp;writer, platform);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    crash_writer_finalize_crc(&amp;writer);</span><br><span class="line">    crash_storage_barrier();</span><br><span class="line">    crash_writer_commit_valid(&amp;writer);</span><br><span class="line">    crash_storage_barrier();</span><br><span class="line"></span><br><span class="line">    (<span class="type">void</span>)irq_state;</span><br><span class="line">    crash_platform_reset_or_halt();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>量产实现还应有“二次 Fault 保护”。最简单的方法是设置一个全局原子标志：如果进入 Fault 时发现 <code>crash_in_progress</code> 已经置位，则不再执行完整路径，只保存一个极小 secondary-fault 标志，然后立即复位。</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">if crash_in_progress was already set:</span><br><span class="line">    write SECONDARY_FAULT marker if possible</span><br><span class="line">    reset immediately</span><br></pre></td></tr></table></figure><h2 id="Early-Boot-Collector-伪代码"><a href="#Early-Boot-Collector-伪代码" class="headerlink" title="Early Boot Collector 伪代码"></a>Early Boot Collector 伪代码</h2><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief Validate and persist a retained crash record during early boot.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">crash_early_boot_collect</span><span class="params">(<span class="type">void</span>)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">const</span> <span class="type">crash_record_header_t</span> *record = crash_retained_record();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (!crash_record_header_sane(record))</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (!crash_record_crc_valid(record))</span><br><span class="line">    &#123;</span><br><span class="line">        crash_stats_note_corrupt_record();</span><br><span class="line">        crash_retained_record_clear();</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    crash_boot_policy_update(record);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (crash_flash_slot_available())</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> (crash_flash_append_and_verify(record) == <span class="number">0</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            crash_retained_record_mark_used();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (crash_boot_policy_requires_safe_mode())</span><br><span class="line">    &#123;</span><br><span class="line">        crash_enter_safe_mode();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>Early Boot Collector 的执行顺序很重要。它应位于会清除 Retention RAM 的初始化之前，也应位于可能再次触发同类故障的复杂驱动初始化之前。Bootloader 与应用必须对 Crash RAM 的所有权、布局、版本和清除策略达成一致。</p><h2 id="离线符号化流程"><a href="#离线符号化流程" class="headerlink" title="离线符号化流程"></a>离线符号化流程</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    A[设备 Crash Record] --&gt; B[解析 Header/TLV]</span><br><span class="line">    B --&gt; C[读取 Build ID]</span><br><span class="line">    C --&gt; D[从符号仓库定位 ELF/MAP/DWARF]</span><br><span class="line">    D --&gt; E[校验架构/地址基址/镜像槽]</span><br><span class="line">    E --&gt; F[解码 Fault Status]</span><br><span class="line">    E --&gt; G[恢复寄存器]</span><br><span class="line">    E --&gt; H[装载栈内存块]</span><br><span class="line">    F --&gt; I[根因候选]</span><br><span class="line">    G --&gt; J[GDB/CrashDebug/自研 Unwinder]</span><br><span class="line">    H --&gt; J</span><br><span class="line">    J --&gt; K[函数/文件/行号/局部变量]</span><br><span class="line">    K --&gt; L[生成故障签名并聚类]</span><br></pre></td></tr></table></figure><p>最基础的地址符号化可以使用：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">arm-none-eabi-addr2line -e firmware.elf -f -C 0x08012345 0x08006789</span><br></pre></td></tr></table></figure><p>但仅靠 <code>addr2line</code> 不能恢复完整运行时调用栈。完整分析通常需要把保存的寄存器和内存块呈现给 GDB。CrashCatcher 配套 CrashDebug、Zephyr 的 <code>coredump_gdbserver.py</code>、ESP-IDF 的 <code>idf.py coredump-debug</code> 都体现了这一模式。</p><h3 id="故障签名"><a href="#故障签名" class="headerlink" title="故障签名"></a>故障签名</h3><p>为了聚类，不能直接用完整 Dump 哈希，因为时间戳和栈变量会导致每条记录不同。可构造稳定签名：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">signature = hash(</span><br><span class="line">    build_id,</span><br><span class="line">    crash_reason,</span><br><span class="line">    normalized_pc,</span><br><span class="line">    normalized_lr,</span><br><span class="line">    top_3_call_frames,</span><br><span class="line">    selected_fault_status_bits</span><br><span class="line">)</span><br></pre></td></tr></table></figure><p>同一根因的设备通常会落入同一签名组，研发可以按设备数、版本、时间和硬件批次排序处理。</p><h2 id="记录容量和时间预算如何量化"><a href="#记录容量和时间预算如何量化" class="headerlink" title="记录容量和时间预算如何量化"></a>记录容量和时间预算如何量化</h2><p>Crash Record 容量不是越大越好，必须同时满足 RAM&#x2F;Flash 容量、写入时间和看门狗时间上限。</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">B_record = B_header</span><br><span class="line">         + B_arch_regs</span><br><span class="line">         + B_fault_regs</span><br><span class="line">         + B_current_stack</span><br><span class="line">         + B_thread_meta</span><br><span class="line">         + B_event_ring</span><br><span class="line">         + B_platform_regs</span><br><span class="line">         + B_crc_padding</span><br></pre></td></tr></table></figure><p>一级捕获时间近似为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">T_capture = T_entry</span><br><span class="line">          + T_register_copy</span><br><span class="line">          + T_memory_copy</span><br><span class="line">          + T_crc</span><br><span class="line">          + T_storage_program</span><br><span class="line">          + T_commit</span><br></pre></td></tr></table></figure><p>必须满足：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">T_capture &lt; min(T_watchdog_remaining, T_power_hold_up, T_product_recovery_budget)</span><br></pre></td></tr></table></figure><p>示例：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">固定头和寄存器          512 B</span><br><span class="line">当前栈窗口             1024 B</span><br><span class="line">最近事件 64 条          1024 B</span><br><span class="line">当前线程元数据           128 B</span><br><span class="line">平台寄存器               256 B</span><br><span class="line">对齐和 CRC               128 B</span><br><span class="line">--------------------------------</span><br><span class="line">总计                    3072 B</span><br></pre></td></tr></table></figure><p>3KB 的一级记录通常已经能提供较高诊断价值。若 Retention RAM 只有 1KB，可缩减为 512B 栈窗口和 16 条事件；若有 64KB Crash 分区，可以在复位后追加多个一级记录，而不是在 Fault 中保存所有任务完整栈。</p><h3 id="Crash-Slot-数量"><a href="#Crash-Slot-数量" class="headerlink" title="Crash Slot 数量"></a>Crash Slot 数量</h3><p>如果返修周期或上报周期较长，应估算：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">N_slots &gt;= peak_crash_rate_per_device * maximum_offline_duration</span><br></pre></td></tr></table></figure><p>但频繁重复崩溃不应无限占用 Flash。可采用：</p><ul><li>First crash wins：保留首次异常，后续只累计次数。</li><li>Ring slots：保留最近 N 次。</li><li>Signature dedup：相同签名只更新计数和最后时间。</li><li>Priority slots：未知新签名覆盖低价值重复签名。</li></ul><h2 id="Flash-Crash-分区布局"><a href="#Flash-Crash-分区布局" class="headerlink" title="Flash Crash 分区布局"></a>Flash Crash 分区布局</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    A[Crash Partition] --&gt; B[Superblock A]</span><br><span class="line">    A --&gt; C[Superblock B]</span><br><span class="line">    A --&gt; D[Slot 0]</span><br><span class="line">    A --&gt; E[Slot 1]</span><br><span class="line">    A --&gt; F[Slot 2]</span><br><span class="line">    A --&gt; G[...]</span><br><span class="line">    D --&gt; H[Header]</span><br><span class="line">    H --&gt; I[TLV Payload]</span><br><span class="line">    I --&gt; J[CRC]</span><br><span class="line">    J --&gt; K[Commit Word]</span><br></pre></td></tr></table></figure><p>建议：</p><ol><li>Slot 按 erase block 对齐，或多个小 Slot 共享一个预擦除 erase block。</li><li>Superblock 采用双副本、sequence 和 CRC，或完全避免必须更新的全局索引，启动时扫描 Slot。</li><li>系统健康时后台维持至少一个 ERASED Slot。</li><li>写完后读回关键头和 CRC。</li><li>禁止 Crash 分区与 OTA image slot、配置区或文件系统元数据区重叠。</li><li>Bootloader、应用和量产工具共享同一份分区定义。</li></ol><p>LittleFS 适合在二级阶段把 Crash Record 组织成文件，并提供掉电恢复和磨损均衡；但最小 Fault 路径更适合固定 Slot 和追加式写入，因为依赖更少、时间更容易上界化。</p><h2 id="常见失败设计及原因"><a href="#常见失败设计及原因" class="headerlink" title="常见失败设计及原因"></a>常见失败设计及原因</h2><table><thead><tr><th>失败设计</th><th>为什么危险</th><th>推荐替代方案</th></tr></thead><tbody><tr><td>Handler 中直接 <code>printf</code> 全部寄存器</td><td>stdio 可能加锁、分配、等待 UART 中断，格式化耗时不可控</td><td>保存二进制寄存器，复位后格式化</td></tr><tr><td>Handler 中打开 FAT&#x2F;LittleFS 文件</td><td>文件系统、块缓存、锁和 Flash 驱动可能已经不可信</td><td>先写 Retention RAM 或固定 Crash Slot</td></tr><tr><td>不判断 MSP&#x2F;PSP，直接把当前 SP 当故障 SP</td><td>可能读取 Handler 栈而不是故障线程栈</td><td>解码 EXC_RETURN 并保存原始 frame</td></tr><tr><td>固定复制 4KB 栈</td><td>SP 可能越界，引发二次 Fault</td><td>依据已知栈边界裁剪并限制长度</td></tr><tr><td>只保存 PC，不保存 Build ID</td><td>不同固件地址含义不同</td><td>Build ID + ELF 符号仓库强绑定</td></tr><tr><td>先写 VALID，再写 payload</td><td>掉电后会出现“看似有效”的半记录</td><td>payload&#x2F;CRC 完成后最后提交 VALID</td></tr><tr><td>Fault 中擦除 Flash</td><td>擦除时间长且电源&#x2F;看门狗风险高</td><td>正常运行期预擦除 Slot</td></tr><tr><td>在线完整 Backtrace</td><td>依赖损坏栈、unwind table、复杂访问</td><td>保存原始栈，主机侧展开</td></tr><tr><td>每次重复崩溃都写完整 Dump</td><td>快速耗尽 Slot 和擦写寿命</td><td>签名去重、计数、Safe Mode</td></tr><tr><td>Dump 全 SRAM</td><td>容量大、耗时长、可能泄露密钥和用户数据</td><td>内存白名单和分级采集</td></tr><tr><td>异常后直接继续运行</td><td>系统状态不可证明，可能造成更严重数据破坏</td><td>提交快照后受控复位或安全停机</td></tr></tbody></table><h2 id="验证与故障注入"><a href="#验证与故障注入" class="headerlink" title="验证与故障注入"></a>验证与故障注入</h2><p>Crash Capture 不能只在“手工空指针”场景验证。必须建立自动化故障注入矩阵。</p><h3 id="CPU-异常测试"><a href="#CPU-异常测试" class="headerlink" title="CPU 异常测试"></a>CPU 异常测试</h3><ul><li>空指针读写。</li><li>非法地址访问。</li><li>未对齐访问，并分别测试 trap 开关。</li><li>除零。</li><li>未定义指令。</li><li>非法异常返回。</li><li>MPU 读、写、执行违规。</li><li>精确和可构造的非精确 BusFault。</li><li>FPU 使用与未使用状态。</li><li>Thread Mode 使用 PSP、MSP 两种场景。</li><li>Fault 发生在 ISR 中。</li><li>Fault 发生在嵌套中断中。</li></ul><h3 id="栈和内存破坏测试"><a href="#栈和内存破坏测试" class="headerlink" title="栈和内存破坏测试"></a>栈和内存破坏测试</h3><ul><li>当前线程栈溢出。</li><li>MSP&#x2F;Interrupt Stack 溢出。</li><li>栈 canary 被破坏但 SP 仍合法。</li><li>SP 指向栈边界之外。</li><li>TCB 指针损坏。</li><li>Crash Buffer 前后 guard 被破坏。</li></ul><h3 id="存储和电源测试"><a href="#存储和电源测试" class="headerlink" title="存储和电源测试"></a>存储和电源测试</h3><ul><li>Flash 正在 program 时触发 Fault。</li><li>Flash 正在 erase 时触发 Fault。</li><li>XIP cache 关闭时触发 Fault。</li><li>一级写入任意字节位置断电。</li><li>VALID 标记前后断电。</li><li>CRC 破坏。</li><li>Crash 分区满。</li><li>没有预擦除 Slot。</li><li>Retention RAM 在不同复位类型下的保持性测试。</li></ul><h3 id="看门狗和二次-Fault-测试"><a href="#看门狗和二次-Fault-测试" class="headerlink" title="看门狗和二次 Fault 测试"></a>看门狗和二次 Fault 测试</h3><ul><li>一级捕获执行到每个阶段时触发 Watchdog。</li><li>Crash Handler 内故意访问非法地址，验证 secondary-fault 快速复位。</li><li>UART&#x2F;Flash 后端永久 busy，验证超时上界。</li><li>双核同时 panic，验证 Crash Owner。</li></ul><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[Fault Injection Test] --&gt; B[确认设备可复位]</span><br><span class="line">    B --&gt; C[确认记录 magic/version/length]</span><br><span class="line">    C --&gt; D[确认 CRC]</span><br><span class="line">    D --&gt; E[确认 Build ID 匹配]</span><br><span class="line">    E --&gt; F[确认 PC/LR/故障寄存器]</span><br><span class="line">    F --&gt; G[确认栈窗口边界]</span><br><span class="line">    G --&gt; H[确认离线调用栈]</span><br><span class="line">    H --&gt; I[确认重复崩溃和分区回收]</span><br></pre></td></tr></table></figure><p>验收标准不只是“能看到日志”，而应包括：记录成功率、最坏捕获时间、掉电恢复率、错误记录拒绝率、符号化正确率和对正常业务 RAM&#x2F;Flash&#x2F;实时性的影响。</p><h2 id="推荐的工程落地步骤"><a href="#推荐的工程落地步骤" class="headerlink" title="推荐的工程落地步骤"></a>推荐的工程落地步骤</h2><h3 id="第一步：先做-256B～1KB-的-P0-Retention-Snapshot"><a href="#第一步：先做-256B～1KB-的-P0-Retention-Snapshot" class="headerlink" title="第一步：先做 256B～1KB 的 P0 Retention Snapshot"></a>第一步：先做 256B～1KB 的 P0 Retention Snapshot</h3><p>保存：magic、版本、Build ID、PC、LR、SP、xPSR、CFSR、HFSR、BFAR、MMFAR 和 256B 栈窗口。不要一开始就做全线程 Dump。</p><h3 id="第二步：加入独立-Crash-Stack-和边界检查"><a href="#第二步：加入独立-Crash-Stack-和边界检查" class="headerlink" title="第二步：加入独立 Crash Stack 和边界检查"></a>第二步：加入独立 Crash Stack 和边界检查</h3><p>用栈溢出和错误 SP 故障注入验证二次 Fault 风险。</p><h3 id="第三步：实现-Early-Boot-转存"><a href="#第三步：实现-Early-Boot-转存" class="headerlink" title="第三步：实现 Early Boot 转存"></a>第三步：实现 Early Boot 转存</h3><p>把 <code>.noinit</code>&#x2F;Backup SRAM 记录追加到专用 Flash Slot，完成 CRC、读回和 Slot 管理。</p><h3 id="第四步：建立符号归档和-PC-工具"><a href="#第四步：建立符号归档和-PC-工具" class="headerlink" title="第四步：建立符号归档和 PC 工具"></a>第四步：建立符号归档和 PC 工具</h3><p>CI 自动归档 ELF&#x2F;MAP&#x2F;Build ID；PC 工具解析 Record，并调用 <code>addr2line</code>、GDB 或自研 unwinder。</p><h3 id="第五步：增加二进制-Flight-Recorder"><a href="#第五步：增加二进制-Flight-Recorder" class="headerlink" title="第五步：增加二进制 Flight Recorder"></a>第五步：增加二进制 Flight Recorder</h3><p>先记录调度、Flash、DMA、协议和看门狗关键事件，再根据实际故障补充业务事件。</p><h3 id="第六步：按产品资源扩展-RTOS-多线程和平台寄存器"><a href="#第六步：按产品资源扩展-RTOS-多线程和平台寄存器" class="headerlink" title="第六步：按产品资源扩展 RTOS 多线程和平台寄存器"></a>第六步：按产品资源扩展 RTOS 多线程和平台寄存器</h3><p>只增加能显著缩短定位时间的数据，所有扩展都必须有容量和时间上限。</p><h3 id="第七步：增加安全、去重、Safe-Mode-和远程上报"><a href="#第七步：增加安全、去重、Safe-Mode-和远程上报" class="headerlink" title="第七步：增加安全、去重、Safe Mode 和远程上报"></a>第七步：增加安全、去重、Safe Mode 和远程上报</h3><p>确保大量现场设备发生同类异常时不会反复磨损 Flash 或形成无限重启循环。</p><h2 id="不同资源等级的推荐配置"><a href="#不同资源等级的推荐配置" class="headerlink" title="不同资源等级的推荐配置"></a>不同资源等级的推荐配置</h2><table><thead><tr><th>设备等级</th><th>一级保存位置</th><th align="right">建议记录量</th><th>二级存储</th><th>分析能力</th></tr></thead><tbody><tr><td>超小 MCU，RAM &lt; 32KB</td><td>Backup Register &#x2F; 128～512B <code>.noinit</code></td><td align="right">PC、LR、SP、Fault Reg、少量栈</td><td>可选固定 Flash 页</td><td><code>addr2line</code> + 栈候选扫描</td></tr><tr><td>普通 Cortex-M + RTOS</td><td>Retention RAM 1～8KB</td><td align="right">全寄存器、当前栈、线程摘要、事件 ring</td><td>预留 Flash Crash 分区</td><td>GDB&#x2F;CrashDebug&#x2F;自研工具</td></tr><tr><td>高端 MCU，多核&#x2F;FPU</td><td>Retention SRAM 8～64KB</td><td align="right">per-core 寄存器、多个线程栈、平台寄存器</td><td>双 bank Flash、FRAM、eMMC</td><td>完整 Core Dump 和聚类</td></tr><tr><td>ESP-IDF 平台</td><td>框架 panic&#x2F;coredump</td><td align="right">TCB、任务栈、寄存器</td><td>Flash coredump partition 或 UART</td><td><code>idf.py coredump-info/debug</code></td></tr><tr><td>Zephyr 平台</td><td>Zephyr coredump backend</td><td align="right">MIN&#x2F;THREADS&#x2F;LINKER_RAM 可配置</td><td>logging、UDP、Flash Partition</td><td><code>coredump_gdbserver.py</code></td></tr><tr><td>联网量产设备</td><td>本地最小记录 + 上传队列</td><td align="right">P0&#x2F;P1 + Build ID + 事件</td><td>Flash&#x2F;FRAM + 云端</td><td>符号化、签名聚类、版本趋势</td></tr></tbody></table><h2 id="Cortex-M-故障寄存器应该如何解码"><a href="#Cortex-M-故障寄存器应该如何解码" class="headerlink" title="Cortex-M 故障寄存器应该如何解码"></a>Cortex-M 故障寄存器应该如何解码</h2><p>保存寄存器只是第一步，离线工具还需要把状态位转换成可执行的诊断结论。Cortex-M3&#x2F;M4&#x2F;M7&#x2F;M23&#x2F;M33&#x2F;M55 等内核的可用寄存器和位定义并不完全相同，解析器应根据 <code>CPUID</code>、架构编号和记录中的 feature flags 选择对应规则，不能假设所有 Cortex-M 都具备完整的 MemManage、BusFault 和 UsageFault 子寄存器。</p><h3 id="HFSR：先判断-HardFault-是原生故障还是升级结果"><a href="#HFSR：先判断-HardFault-是原生故障还是升级结果" class="headerlink" title="HFSR：先判断 HardFault 是原生故障还是升级结果"></a>HFSR：先判断 HardFault 是原生故障还是升级结果</h3><p>HardFault 常见来源包括：</p><ul><li>可配置 Fault 没有使能，最终升级为 HardFault。</li><li>Fault Handler 本身又发生故障。</li><li>向量表读取失败。</li><li>调试事件或强制升级。</li></ul><p>如果 HFSR 的 forced 类状态被置位，不能停留在“发生 HardFault”这个表面结论，而应继续解析 CFSR，找出最初的 MemManage、BusFault 或 UsageFault 原因。若只保存 HFSR 而不保存 CFSR，现场信息会严重不足。</p><h3 id="CFSR：按三个子域分别解释"><a href="#CFSR：按三个子域分别解释" class="headerlink" title="CFSR：按三个子域分别解释"></a>CFSR：按三个子域分别解释</h3><p>CFSR 逻辑上由 MemManage Fault Status、BusFault Status 和 UsageFault Status 组成。离线工具应输出原始值、有效位和人类可读解释，同时保留多个状态位并存的可能性。</p><table><thead><tr><th>子域</th><th>重点问题</th><th>典型分析方向</th></tr></thead><tbody><tr><td>MemManage</td><td>指令取址违规、数据访问违规、异常入栈&#x2F;出栈失败、懒浮点状态保存失败</td><td>MPU 配置、栈边界、安全域、错误函数指针</td></tr><tr><td>BusFault</td><td>精确数据总线错误、非精确异步错误、指令总线错误、异常栈操作失败</td><td>外部 SRAM&#x2F;QSPI、DMA、总线超时、ECC、Flash 控制器</td></tr><tr><td>UsageFault</td><td>未定义指令、非法状态、非法 PC、未对齐、除零、协处理器错误</td><td>返回地址破坏、函数指针错误、编译选项、FPU 配置</td></tr></tbody></table><p>“精确 BusFault”通常能把栈帧中的 PC 指向触发访问的指令；“非精确 BusFault”可能由缓冲写或异步总线事务延迟上报，故障 PC 未必是根因位置。此时最近事件、DMA 状态、总线矩阵寄存器和写缓冲相关配置比单纯 PC 更重要。</p><h3 id="BFAR-MMFAR-只能在地址有效位成立时使用"><a href="#BFAR-MMFAR-只能在地址有效位成立时使用" class="headerlink" title="BFAR&#x2F;MMFAR 只能在地址有效位成立时使用"></a>BFAR&#x2F;MMFAR 只能在地址有效位成立时使用</h3><p>BFAR 和 MMFAR 中的地址并非任何时候都有效。解析器必须先检查对应的 address-valid 状态，再显示“故障地址”。否则寄存器可能保留上一次值或无意义值，容易把研发引向错误方向。</p><h3 id="异常入栈或出栈失败意味着原始栈帧可能不完整"><a href="#异常入栈或出栈失败意味着原始栈帧可能不完整" class="headerlink" title="异常入栈或出栈失败意味着原始栈帧可能不完整"></a>异常入栈或出栈失败意味着原始栈帧可能不完整</h3><p>如果状态寄存器表明异常入口 stacking、异常返回 unstacking 或 lazy FPU stacking 失败，硬件自动压栈的数据可能没有完整写入。此时：</p><ol><li>不应盲目信任 frame 中所有 R0～xPSR 字段。</li><li>应提高 MSP、PSP、MSPLIM、PSPLIM、栈边界和 MPU 状态的诊断优先级。</li><li>PC 工具应把记录标为 <code>frame_may_be_incomplete</code>。</li><li>可以使用软件入口保存的 R4～R11、Handler LR、当前 MSP 等信息辅助判断。</li></ol><h3 id="保存原始值，不要只保存解码后的字符串"><a href="#保存原始值，不要只保存解码后的字符串" class="headerlink" title="保存原始值，不要只保存解码后的字符串"></a>保存原始值，不要只保存解码后的字符串</h3><p>固件中的解码逻辑可能有错误，也可能在未来新增架构支持。Crash Record 应保存所有原始寄存器数值，文本解释只由复位后或 PC 工具生成。这样工具升级后可以重新分析历史记录。</p><h2 id="FPU、Lazy-Stacking-和扩展异常栈帧"><a href="#FPU、Lazy-Stacking-和扩展异常栈帧" class="headerlink" title="FPU、Lazy Stacking 和扩展异常栈帧"></a>FPU、Lazy Stacking 和扩展异常栈帧</h2><p>带 FPU 的 Cortex-M4F、M7、M33、M55 等平台需要额外处理浮点上下文。异常发生时，处理器可能保存基础栈帧，也可能保存包含 S0～S15、FPSCR 等内容的扩展栈帧。启用 lazy stacking 后，浮点上下文是否已经真正压栈还与 FPCCR 等状态有关。</p><p>通用实现应遵循以下原则：</p><ol><li>保存 EXC_RETURN 原始值，不能在 C 函数中重新读取 Handler LR 代替。</li><li>根据架构定义判断基础帧或扩展帧，不能固定偏移读取 PC。</li><li>保存 FPCCR、FPCAR、FPDSCR 等实现存在的浮点控制状态。</li><li>如果捕获函数自身使用浮点指令，可能触发新的 lazy stacking 或覆盖现场，因此 Crash Capture 模块应使用禁止浮点的编译选项，并避免任何浮点运算。</li><li>完整保存 S16～S31 是否必要取决于 RTOS 上下文切换策略和故障线程是否使用 FPU；资源不足时至少保存栈中硬件帧、FPSCR 和 RTOS 已保存区的地址。</li><li>PC 工具要区分“寄存器未保存”“寄存器值为零”和“扩展帧读取失败”三种状态。</li></ol><p>在测试中，应分别构造：故障前从未使用 FPU、刚使用 FPU、ISR 使用 FPU、线程切换后使用 FPU、lazy stacking 过程中发生总线错误等场景。只测试普通整数代码会掩盖扩展帧偏移错误。</p><h2 id="TrustZone-和安全域故障"><a href="#TrustZone-和安全域故障" class="headerlink" title="TrustZone 和安全域故障"></a>TrustZone 和安全域故障</h2><p>Armv8-M TrustZone 系统至少要面对 Secure&#x2F;Non-secure 两套状态、不同栈指针和安全故障寄存器。通用 Crash 设计需要先确定安全策略，再决定技术实现。</p><h3 id="安全侧统一捕获"><a href="#安全侧统一捕获" class="headerlink" title="安全侧统一捕获"></a>安全侧统一捕获</h3><p>由 Secure Firmware 提供最终 Crash Owner，可以读取并保存允许的 Secure 和 Non-secure 上下文。优点是信息完整、可信度高；风险是 Crash Dump 本身可能包含高敏感密钥和安全服务状态。</p><h3 id="两侧分别捕获"><a href="#两侧分别捕获" class="headerlink" title="两侧分别捕获"></a>两侧分别捕获</h3><p>Secure 和 Non-secure 各自维护独立记录区。Non-secure 记录不能读取 Secure RAM，Secure 记录由安全服务控制导出。优点是边界清晰；缺点是跨域调用故障的关联分析更复杂。</p><h3 id="生产建议"><a href="#生产建议" class="headerlink" title="生产建议"></a>生产建议</h3><ul><li>Secure Dump 默认只保存故障类型、PC 范围、SFSR&#x2F;SFAR 和经过审核的栈窗口。</li><li>密钥、随机数种子、认证上下文和安全引擎寄存器加入永久排除列表。</li><li>Non-secure 侧只能获取经过脱敏的安全故障摘要。</li><li>记录中明确保存 <code>capture_security_state</code> 和 <code>fault_security_state</code>。</li><li>安全侧负责校验 Non-secure 提供的指针，不能直接信任其栈边界或 TCB 地址。</li></ul><h2 id="跨-CPU-架构的端口层设计"><a href="#跨-CPU-架构的端口层设计" class="headerlink" title="跨 CPU 架构的端口层设计"></a>跨 CPU 架构的端口层设计</h2><p>通用 Crash 框架不应把 Cortex-M 的寄存器直接写进公共头。推荐把公共层、架构层、RTOS 层和存储层分开。</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><span class="line">crash/core</span><br><span class="line">    record writer</span><br><span class="line">    TLV encoder</span><br><span class="line">    CRC</span><br><span class="line">    budget controller</span><br><span class="line"></span><br><span class="line">crash/arch/cortex_m</span><br><span class="line">    exception entry</span><br><span class="line">    EXC_RETURN decode</span><br><span class="line">    SCB fault registers</span><br><span class="line"></span><br><span class="line">crash/arch/riscv</span><br><span class="line">    trap entry</span><br><span class="line">    mcause/mepc/mtval/mstatus</span><br><span class="line"></span><br><span class="line">crash/arch/xtensa</span><br><span class="line">    panic frame</span><br><span class="line">    EPC/EXCCAUSE/EXCVADDR</span><br><span class="line"></span><br><span class="line">crash/rtos</span><br><span class="line">    baremetal</span><br><span class="line">    RT-Thread</span><br><span class="line">    FreeRTOS</span><br><span class="line">    Zephyr adapter</span><br><span class="line"></span><br><span class="line">crash/storage</span><br><span class="line">    retention RAM</span><br><span class="line">    internal flash slot</span><br><span class="line">    FRAM</span><br><span class="line">    UART/SWO</span><br></pre></td></tr></table></figure><p>公共层只理解 <code>architecture</code>、<code>reason</code> 和 TLV，不解析具体寄存器。PC 工具根据 architecture TLV version 加载对应解析器。这样增加 RISC-V 或新 Cortex-M 代际时，不需要修改旧记录格式。</p><h3 id="RISC-V-端口要点"><a href="#RISC-V-端口要点" class="headerlink" title="RISC-V 端口要点"></a>RISC-V 端口要点</h3><p>RISC-V 同步异常通常至少保存：</p><ul><li><code>mepc</code>：异常时程序计数器。</li><li><code>mcause</code>：中断&#x2F;异常及原因编号。</li><li><code>mtval</code>：故障地址、非法指令值或架构定义信息。</li><li><code>mstatus</code>：中断使能、前一特权级等状态。</li><li>通用寄存器和原始 SP。</li><li>若运行 S&#x2F;U Mode，还需保存对应级别的 cause、epc、tval 和 status。</li></ul><p>与 Cortex-M 一样，trap entry 应使用最小汇编保存原始寄存器，再切换到可信异常栈。不能在 C prologue 之后才尝试恢复已经被覆盖的临时寄存器。</p><h3 id="Xtensa-ESP-平台要点"><a href="#Xtensa-ESP-平台要点" class="headerlink" title="Xtensa&#x2F;ESP 平台要点"></a>Xtensa&#x2F;ESP 平台要点</h3><p>Xtensa 需要按 ABI 和 windowed&#x2F;call0 模式处理寄存器窗口。ESP-IDF 已经提供成熟 panic 和 coredump 流程，优先使用框架能力，而不是在其上叠加第二套相互冲突的异常入口。</p><h2 id="解析器必须按不可信输入设计"><a href="#解析器必须按不可信输入设计" class="headerlink" title="解析器必须按不可信输入设计"></a>解析器必须按不可信输入设计</h2><p>Crash Record 可能因掉电、内存破坏、传输错误或旧固件 Bug 而损坏。PC 解析器不能假定来自设备的数据可信。</p><p>必须检查：</p><ol><li><code>header_size</code>、<code>total_size</code> 是否小于实际文件长度。</li><li>加法和对齐计算是否整数溢出。</li><li>TLV 长度是否越界、是否导致死循环。</li><li>TLV 数量是否超过合理上限。</li><li>地址范围和内存块是否重叠或超出目标位宽。</li><li>Build ID 长度和编码是否合法。</li><li>压缩数据解压后大小是否受限。</li><li>架构版本未知时是否可以安全跳过。</li><li>CRC 不匹配时是否仍允许以“部分记录”模式提取固定头，而不是完全崩溃。</li><li>符号文件和 Dump 的 Build ID 是否一致，不一致时必须拒绝给出确定源码行。</li></ol><p>解析器应输出置信度。例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">PC symbolization: exact</span><br><span class="line">LR symbolization: exact</span><br><span class="line">stack frame #1: unwind table</span><br><span class="line">stack frame #2: frame pointer</span><br><span class="line">stack frame #3: heuristic candidate</span><br><span class="line">fault address: invalid/not-present</span><br><span class="line">record integrity: partial, payload CRC failed</span><br></pre></td></tr></table></figure><p>这样研发能区分客观现场、可靠推导和启发式候选，避免把扫描到的一个代码地址误认为真实调用链。</p><h2 id="量产运行指标"><a href="#量产运行指标" class="headerlink" title="量产运行指标"></a>量产运行指标</h2><p>Crash 系统本身也需要可观测性。建议长期统计：</p><ul><li>一级快照成功次数。</li><li>一级快照 CRC 失败次数。</li><li>Retention RAM 丢失次数。</li><li>Flash 转存成功&#x2F;失败次数。</li><li>无可用预擦除 Slot 次数。</li><li>secondary fault 次数。</li><li>平均和最大捕获时间。</li><li>平均记录大小和 partial record 比例。</li><li>相同 Build ID 下各 Crash Signature 数量。</li><li>快速重启循环和 Safe Mode 进入次数。</li><li>上传成功率、积压量和最旧记录年龄。</li></ul><p>这些指标可以发现“Crash 功能看似存在，但现场记录经常写坏”这类系统性问题。特别是捕获时间和 Flash 后端失败率，应在不同温度、电压、Flash 老化程度和最高业务负载下测量。</p><h2 id="开源方案的组合建议"><a href="#开源方案的组合建议" class="headerlink" title="开源方案的组合建议"></a>开源方案的组合建议</h2><p>不同平台不必从零实现全部功能，可以按场景组合：</p><h3 id="裸机-Cortex-M，小资源"><a href="#裸机-Cortex-M，小资源" class="headerlink" title="裸机 Cortex-M，小资源"></a>裸机 Cortex-M，小资源</h3><ul><li>用 CMSIS-View Fault Storage 作为 P0 最小寄存器记录。</li><li>增加产品自己的 Build ID、栈窗口和 Early Boot Flash Slot。</li><li>主机用 <code>addr2line</code> 或简单 GDB 脚本分析。</li></ul><h3 id="Cortex-M-自研-RTOS-RT-Thread"><a href="#Cortex-M-自研-RTOS-RT-Thread" class="headerlink" title="Cortex-M + 自研 RTOS&#x2F;RT-Thread"></a>Cortex-M + 自研 RTOS&#x2F;RT-Thread</h3><ul><li>借鉴 CrashCatcher 的独立 Crash Stack、内存区域回调和 CrashDebug 思路。</li><li>复用 RTOS 已有异常栈结构和线程栈边界。</li><li>自研 TLV Record、Retention RAM 和 Flash 分区管理。</li></ul><h3 id="Zephyr"><a href="#Zephyr" class="headerlink" title="Zephyr"></a>Zephyr</h3><ul><li>优先启用 Zephyr Coredump，选择 MIN、THREADS 或 LINKER_RAM 模式。</li><li>根据产品可靠性评估 logging、UDP、Flash Partition 或自定义 backend。</li><li>保留 Zephyr ELF，并使用官方 GDB Server 脚本。</li></ul><h3 id="ESP-IDF"><a href="#ESP-IDF" class="headerlink" title="ESP-IDF"></a>ESP-IDF</h3><ul><li>使用 ESP-IDF 自带 Core Dump 到 Flash 或 UART。</li><li>根据 Flash cache 故障风险评估 panic handler IRAM 配置。</li><li>配置合理的最大任务数和 coredump partition 容量。</li><li>使用 <code>idf.py coredump-info</code>、<code>idf.py coredump-debug</code> 和 ROM ELF。</li></ul><h3 id="大规模联网设备"><a href="#大规模联网设备" class="headerlink" title="大规模联网设备"></a>大规模联网设备</h3><ul><li>本地采用最小可靠记录和分级内存区域。</li><li>可评估 Memfault Firmware SDK 或自建类似的 Build ID、分片上传、符号化和聚类平台。</li><li>无论使用商业服务还是自研服务，都应保留原始 Record 和精确符号归档，避免平台迁移后历史数据无法重放。</li></ul><h2 id="面试回答时的组织方式"><a href="#面试回答时的组织方式" class="headerlink" title="面试回答时的组织方式"></a>面试回答时的组织方式</h2><p>回答这类题时，可以按以下顺序展开：</p><ol><li>先讲核心原则：Fault Handler 只做最小二进制快照，不做打印、文件系统和在线分析。</li><li>再讲三阶段架构：一级 RAM&#x2F;预擦除 Slot，二级启动后持久化，三级 PC 离线分析。</li><li>说明 Cortex-M 入口：根据 EXC_RETURN 选择 MSP&#x2F;PSP，保存硬件异常栈帧和 SCB Fault Registers。</li><li>说明可靠性：独立 Crash Stack、地址白名单、有界栈复制、二次 Fault 标记、看门狗时间上限。</li><li>说明记录格式：magic、版本、长度、Build ID、TLV、CRC、最后写 VALID。</li><li>说明 Flash：正常期预擦除 Crash Slot；Fault 中只允许 RAMFUNC 轮询编程；更推荐先写 Retention RAM。</li><li>说明调用栈：保存原始栈和寄存器，复位后或 PC 工具结合 ELF&#x2F;DWARF 展开。</li><li>说明 RTOS、多核、FPU、TrustZone、安全和隐私扩展。</li><li>最后列出现成方案：CMSIS-View Fault Storage、CrashCatcher、Zephyr Coredump、ESP-IDF Core Dump、RT-Thread、Memfault。</li></ol><p>一句话概括：<strong>把不可恢复异常处理设计成一个有时间上界、无动态依赖、可原子提交的“飞行数据记录器”；Fault 中只保存不可再现的现场，复位后再持久化，主机侧再完成符号化和根因分析。</strong></p><h2 id="参考链接"><a href="#参考链接" class="headerlink" title="参考链接"></a>参考链接</h2><ul><li><a href="https://arm-software.github.io/CMSIS-View/latest/group__Fault__Storage.html">CMSIS-View Fault Storage</a></li><li><a href="https://arm-software.github.io/CMSIS_6/latest/Core/regMap_pg.html">CMSIS-Core SCB Register Mapping</a></li><li><a href="https://arm-software.github.io/CMSIS_6/v6.0.0/Core/structSCB__Type.html">CMSIS-Core SCB_Type</a></li><li><a href="https://github.com/adamgreen/CrashCatcher">CrashCatcher</a></li><li><a href="https://docs.zephyrproject.org/latest/services/debugging/coredump.html">Zephyr Core Dump</a></li><li><a href="https://docs.espressif.com/projects/esp-idf/en/latest/esp32/api-guides/core_dump.html">ESP-IDF Core Dump</a></li><li><a href="https://docs.espressif.com/projects/esp-idf/en/latest/esp32/api-guides/fatal-errors.html">ESP-IDF Fatal Errors</a></li><li><a href="https://github.com/RT-Thread/rt-thread">RT-Thread Repository</a></li><li><a href="https://rt-thread.github.io/rt-thread/page_interrupt_management.html">RT-Thread Interrupt Management</a></li><li><a href="https://github.com/memfault/memfault-firmware-sdk">Memfault Firmware SDK</a></li><li><a href="https://docs.memfault.com/docs/mcu/coredumps">Memfault Coredump Collection</a></li><li><a href="https://github.com/littlefs-project/littlefs">littlefs</a></li></ul><div style="page-break-after: always;"></div>]]></content>
    
    
      
      
    <summary type="html">&lt;meta name=&quot;referrer&quot; content=&quot;no-referrer&quot; /&gt;

&lt;h1 id=&quot;嵌入式面试真题第-15-题：不可恢复异常后的通用崩溃快照、调用栈保存与离线分析架构&quot;&gt;&lt;a href=&quot;#嵌入式面试真题第-15-题：不可恢复异常后的通用崩溃快照、调用</summary>
      
    
    
    
    <category term="嵌入式面试真题" scheme="https://wdfk-prog.space/categories/%E5%B5%8C%E5%85%A5%E5%BC%8F%E9%9D%A2%E8%AF%95%E7%9C%9F%E9%A2%98/"/>
    
    
    <category term="嵌入式面试真题" scheme="https://wdfk-prog.space/tags/%E5%B5%8C%E5%85%A5%E5%BC%8F%E9%9D%A2%E8%AF%95%E7%9C%9F%E9%A2%98/"/>
    
  </entry>
  
  <entry>
    <title>防范业务逻辑死锁的系统健康监督与看门狗设计</title>
    <link href="https://wdfk-prog.space/posts/217594c2/"/>
    <id>https://wdfk-prog.space/posts/217594c2/</id>
    <published>2026-07-21T03:09:11.000Z</published>
    <updated>2026-07-21T03:10:05.791Z</updated>
    
    <content type="html"><![CDATA[<meta name="referrer" content="no-referrer" /><h1 id="嵌入式面试真题第-14-题：防范业务逻辑死锁的系统健康监督与看门狗设计"><a href="#嵌入式面试真题第-14-题：防范业务逻辑死锁的系统健康监督与看门狗设计" class="headerlink" title="嵌入式面试真题第 14 题：防范业务逻辑死锁的系统健康监督与看门狗设计"></a>嵌入式面试真题第 14 题：防范业务逻辑死锁的系统健康监督与看门狗设计</h1><p><img src="https://i-blog.csdnimg.cn/direct/eedbbc4953ca41149a438f7db657de62.png" alt="在这里插入图片描述"></p><h2 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h2><p>在多任务嵌入式系统、RTOS 固件、边缘网关、工业控制器、车载控制单元、消费电子设备或其他长期无人值守的软件系统中，通常会配置硬件看门狗，以便系统发生严重异常时自动复位。</p><p>传统实现往往由一个独立 Watchdog Task、Idle Hook、主循环或高优先级定时任务周期性喂狗。只要这个喂狗节点还能获得 CPU 时间，硬件看门狗就不会超时。然而，系统可能已经出现以下“进程还活着、业务却不再工作”的假活状态：</p><ul><li>两个或多个任务形成循环等待，业务路径死锁。</li><li>高优先级任务持续运行，低优先级关键任务长期饥饿。</li><li>任务不断重试或相互唤醒，但业务状态不再前进，形成活锁。</li><li>消息队列、缓冲区或对象池耗尽，生产者和消费者互相等待。</li><li>状态机卡在某个中间状态，周期函数仍在调用，但事务永远无法完成。</li><li>外设、总线、文件系统、网络栈或协处理器请求悬挂，任务仍在等待或轮询。</li><li>某个 CPU 核、调度 tick、关键中断或时间基准失效，而其他核仍能继续喂狗。</li><li>看门狗任务本身优先级过高、依赖过少，反而绕过了真正的业务健康状态。</li></ul><p>面对这种问题，如何把看门狗从“某个线程周期运行就喂狗”的简单定时器，重新设计成一套通用、可量化、可诊断、可扩展的系统健康监督机制？如何定义喂狗条件、采集健康证据、监控锁和队列、处理启动与低功耗阶段、保存复位现场，并参考已有开源组件构建可落地方案？</p><h2 id="回答"><a href="#回答" class="headerlink" title="回答"></a>回答</h2><p>结论：硬件看门狗只能作为最终执行器，不能直接代表系统健康。正确方案是在业务任务与硬件看门狗之间增加一个独立的 Health Supervisor，由所有关键执行单元提交可验证的健康证据；只有当任务执行、业务进度、时限约束、资源锁、队列流动、调度能力、时间基准和关键依赖均满足当前运行模式的健康契约时，Supervisor 才允许喂硬件看门狗。</p><p>喂狗条件必须从：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">“看门狗任务本轮获得了 CPU”</span><br></pre></td></tr></table></figure><p>升级为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">“系统在规定时间窗内完成了所有必要工作，并且没有出现不可接受的停滞、资源占用或依赖失效”</span><br></pre></td></tr></table></figure><p>这套设计的核心不是简单增加若干 heartbeat bit，而是建立四层监督链路：</p><ol><li><strong>执行层健康</strong>：关键任务、关键中断、每个 CPU 核和调度器是否仍有执行机会。</li><li><strong>业务层健康</strong>：事务、状态机、队列和数据流是否持续产生可观察的进展。</li><li><strong>资源层健康</strong>：Mutex、Semaphore、队列、内存池、总线和外设是否在限定时间内被释放或完成。</li><li><strong>恢复层健康</strong>：异常能否先局部恢复、再子系统重启、最终由硬件看门狗复位，并在复位前留下足够诊断证据。</li></ol><h2 id="总体架构"><a href="#总体架构" class="headerlink" title="总体架构"></a>总体架构</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    subgraph Business[业务与执行单元]</span><br><span class="line">        T1[关键任务 A]</span><br><span class="line">        T2[关键任务 B]</span><br><span class="line">        T3[状态机 / Worker]</span><br><span class="line">        ISR[关键 ISR / Tick / DMA]</span><br><span class="line">        CORE[每核调度探针]</span><br><span class="line">    end</span><br><span class="line"></span><br><span class="line">    subgraph Evidence[健康证据层]</span><br><span class="line">        HB[执行心跳与调度时间戳]</span><br><span class="line">        PG[业务进度计数 / Epoch]</span><br><span class="line">        DL[Deadline / 周期完成记录]</span><br><span class="line">        LK[锁 Owner / 持有时长 / 等待图]</span><br><span class="line">        Q[队列水位 / 入队出队速率]</span><br><span class="line">        RS[资源与依赖状态]</span><br><span class="line">    end</span><br><span class="line"></span><br><span class="line">    subgraph Supervisor[Health Supervisor]</span><br><span class="line">        SNAP[无锁快照采集]</span><br><span class="line">        RULE[Health Policy Engine]</span><br><span class="line">        MODE[启动 / 运行 / 升级 / 低功耗配置]</span><br><span class="line">        GATE&#123;Feed Gate&#125;</span><br><span class="line">        ESC[恢复与升级策略]</span><br><span class="line">        DUMP[故障快照与持久化]</span><br><span class="line">    end</span><br><span class="line"></span><br><span class="line">    subgraph Watchdog[复位执行链]</span><br><span class="line">        SW[软件任务看门狗]</span><br><span class="line">        HW[硬件看门狗]</span><br><span class="line">        EXT[外部监控 MCU / PMIC WDT]</span><br><span class="line">    end</span><br><span class="line"></span><br><span class="line">    T1 --&gt; HB</span><br><span class="line">    T2 --&gt; HB</span><br><span class="line">    T3 --&gt; PG</span><br><span class="line">    ISR --&gt; DL</span><br><span class="line">    CORE --&gt; HB</span><br><span class="line">    T1 --&gt; LK</span><br><span class="line">    T2 --&gt; LK</span><br><span class="line">    T3 --&gt; Q</span><br><span class="line">    T1 --&gt; RS</span><br><span class="line">    T2 --&gt; RS</span><br><span class="line"></span><br><span class="line">    HB --&gt; SNAP</span><br><span class="line">    PG --&gt; SNAP</span><br><span class="line">    DL --&gt; SNAP</span><br><span class="line">    LK --&gt; SNAP</span><br><span class="line">    Q --&gt; SNAP</span><br><span class="line">    RS --&gt; SNAP</span><br><span class="line">    SNAP --&gt; RULE</span><br><span class="line">    MODE --&gt; RULE</span><br><span class="line">    RULE --&gt; GATE</span><br><span class="line">    GATE --&gt;|全部满足| SW</span><br><span class="line">    SW --&gt; HW</span><br><span class="line">    HW -.可选级联.-&gt; EXT</span><br><span class="line">    GATE --&gt;|不满足| ESC</span><br><span class="line">    ESC --&gt; DUMP</span><br><span class="line">    ESC --&gt;|停止喂狗| HW</span><br></pre></td></tr></table></figure><p>业务任务不直接操作硬件 Watchdog 寄存器，也不应该各自独立喂同一个硬件看门狗。它们只负责提交自身职责范围内的健康证据。Health Supervisor 读取这些证据、执行规则评估，并作为唯一喂狗授权点。</p><p>硬件看门狗应被视为无法依赖常规软件路径时的最终恢复手段。软件 Supervisor、日志系统、任务调度器甚至整个内核都可能失效，因此监督链应尽量具备分层和独立性：软件任务看门狗负责多任务聚合，硬件看门狗负责调度器或系统级失效，必要时再由独立 PMIC、外部 MCU 或安全岛监控主处理器。</p><h2 id="问题应如何抽象"><a href="#问题应如何抽象" class="headerlink" title="问题应如何抽象"></a>问题应如何抽象</h2><p>这道题表面上是“两个任务互相等待 Mutex，而看门狗任务仍在喂狗”，但更通用的抽象是：</p><blockquote><p>系统存在至少一个仍可执行的路径，因此传统 watchdog heartbeat 持续更新；与此同时，决定系统服务能力的关键不变量已经失效，或者关键状态长时间没有推进。</p></blockquote><p>这类故障可统一称为 <strong>业务假活、逻辑停滞或 liveness failure</strong>。它不同于直接崩溃：程序计数器仍在变化，部分线程仍在调度，甚至日志仍有输出，但系统已无法完成其主要职责。</p><h3 id="常见故障类型"><a href="#常见故障类型" class="headerlink" title="常见故障类型"></a>常见故障类型</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br></pre></td><td class="code"><pre><span class="line">mindmap</span><br><span class="line">  root((业务假活与逻辑停滞))</span><br><span class="line">    等待型故障</span><br><span class="line">      Mutex 循环等待</span><br><span class="line">      Semaphore 永久等待</span><br><span class="line">      条件变量丢失唤醒</span><br><span class="line">      同步 RPC 相互调用</span><br><span class="line">    调度型故障</span><br><span class="line">      高优先级忙循环</span><br><span class="line">      低优先级关键任务饥饿</span><br><span class="line">      优先级反转</span><br><span class="line">      某 CPU 核局部失效</span><br><span class="line">    流控型故障</span><br><span class="line">      队列永久满</span><br><span class="line">      队列永久空</span><br><span class="line">      Buffer 不再推进</span><br><span class="line">      对象池耗尽</span><br><span class="line">    状态机故障</span><br><span class="line">      状态不转移</span><br><span class="line">      事务无完成</span><br><span class="line">      重试风暴</span><br><span class="line">      活锁</span><br><span class="line">    I/O 与依赖故障</span><br><span class="line">      总线事务悬挂</span><br><span class="line">      外设中断丢失</span><br><span class="line">      文件系统或网络假死</span><br><span class="line">      协处理器无响应</span><br><span class="line">    时间与平台故障</span><br><span class="line">      Tick 停止</span><br><span class="line">      中断长期关闭</span><br><span class="line">      时钟源跳变</span><br><span class="line">      低功耗恢复异常</span><br></pre></td></tr></table></figure><p>因此，单纯要求“每个任务每秒置位一次”仍然不够。一个任务完全可以在错误循环里持续置位；一个状态机可以反复进入同一状态；一个消息处理线程可以一直被调度，却没有成功消费任何有效消息。健康证据必须能区分以下三件事：</p><table><thead><tr><th>层次</th><th>需要回答的问题</th><th>仅靠普通 heartbeat 能否覆盖</th></tr></thead><tbody><tr><td>Execution</td><td>任务是否获得过 CPU、ISR 是否发生、调度 tick 是否工作</td><td>部分可以</td></tr><tr><td>Progress</td><td>任务是否完成了有意义的业务步骤</td><td>不能</td></tr><tr><td>Correctness</td><td>完成结果是否满足协议、资源和状态约束</td><td>不能</td></tr></tbody></table><h2 id="健康模型：从-Heartbeat-到-Health-Contract"><a href="#健康模型：从-Heartbeat-到-Health-Contract" class="headerlink" title="健康模型：从 Heartbeat 到 Health Contract"></a>健康模型：从 Heartbeat 到 Health Contract</h2><p>推荐把每个关键参与者定义成一个受监督实体 <code>health participant</code>。参与者不是必须等同于线程，它可以是：</p><ul><li>一个 RTOS 任务。</li><li>一个周期控制回路。</li><li>一个协议会话或连接。</li><li>一个状态机实例。</li><li>一个 DMA&#x2F;ISR 数据通路。</li><li>一个 CPU 核的调度探针。</li><li>一个关键外设或协处理器。</li><li>一个共享服务，如文件系统、存储服务、网络管理器或内存池。</li></ul><p>每个参与者注册一份 Health Contract，至少包含：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">participant_id       参与者唯一标识</span><br><span class="line">criticality          关键等级：critical / important / optional</span><br><span class="line">period               预期执行周期或检查周期</span><br><span class="line">progress_deadline    最长允许无业务进展时间</span><br><span class="line">startup_grace        启动阶段宽限时间</span><br><span class="line">failure_threshold    连续多少次异常才判定失败</span><br><span class="line">recovery_policy      局部恢复、子系统重启或系统复位</span><br><span class="line">required_in_modes    哪些运行模式下必须健康</span><br></pre></td></tr></table></figure><p>运行过程中，参与者提交不同类型的证据：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">execution_heartbeat  本任务或回调确实执行过</span><br><span class="line">progress_epoch       完成了一个有意义的业务阶段</span><br><span class="line">deadline_complete    指定周期或事务在期限内完成</span><br><span class="line">resource_snapshot    当前锁、队列、内存和 I/O 状态</span><br><span class="line">error_state          可恢复错误、永久错误或降级状态</span><br></pre></td></tr></table></figure><h3 id="为什么必须区分执行心跳与进度心跳"><a href="#为什么必须区分执行心跳与进度心跳" class="headerlink" title="为什么必须区分执行心跳与进度心跳"></a>为什么必须区分执行心跳与进度心跳</h3><p>假设音频任务每 10ms 醒来一次，并执行以下错误循环：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">while (true) &#123;</span><br><span class="line">    retry_same_failed_transaction();</span><br><span class="line">    heartbeat++;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>heartbeat</code> 持续变化，只能证明线程获得了 CPU，不能证明音频帧被成功处理。正确做法是同时维护：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">exec_counter++           每次调度周期都更新</span><br><span class="line">frame_commit_counter++   只有一帧真正提交到下游后才更新</span><br><span class="line">last_good_frame_time     记录最后一次成功输出时间</span><br></pre></td></tr></table></figure><p>Supervisor 可以据此区分：</p><table><thead><tr><th>观测结果</th><th>可能含义</th><th>推荐判断</th></tr></thead><tbody><tr><td>exec 不变，progress 不变</td><td>未调度、阻塞、死锁、线程退出</td><td>故障</td></tr><tr><td>exec 变化，progress 不变</td><td>活锁、重复失败、依赖阻塞、空转</td><td>超过业务期限后故障</td></tr><tr><td>exec 变化，progress 变化</td><td>正常推进</td><td>健康候选</td></tr><tr><td>exec 不变，progress 变化</td><td>证据采集设计错误、其他上下文代替更新</td><td>需要审计</td></tr></tbody></table><h2 id="喂狗门控：所有关键健康条件的合取"><a href="#喂狗门控：所有关键健康条件的合取" class="headerlink" title="喂狗门控：所有关键健康条件的合取"></a>喂狗门控：所有关键健康条件的合取</h2><p>硬件喂狗不应由任意一个参与者直接触发，而应由 Feed Gate 统一决策。最简单的逻辑是关键条件的 AND：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">feed_allowed =</span><br><span class="line">    all_required_participants_alive</span><br><span class="line">    &amp;&amp; all_required_progress_deadlines_met</span><br><span class="line">    &amp;&amp; no_fatal_lock_timeout</span><br><span class="line">    &amp;&amp; scheduler_and_timebase_healthy</span><br><span class="line">    &amp;&amp; no_unrecoverable_resource_stall</span><br><span class="line">    &amp;&amp; supervisor_snapshot_consistent</span><br><span class="line">    &amp;&amp; current_mode_policy_satisfied</span><br></pre></td></tr></table></figure><p>实际系统需要允许可选功能、降级运行和启动阶段宽限，因此不一定是机械的全局 AND。更合理的是按关键度和运行模式计算：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">critical participant failed  -&gt; 立即阻止喂狗或进入短暂诊断阶段</span><br><span class="line">important participant failed -&gt; 先局部恢复，恢复失败后阻止喂狗</span><br><span class="line">optional participant failed  -&gt; 隔离或降级，不一定系统复位</span><br></pre></td></tr></table></figure><h3 id="喂狗决策流程"><a href="#喂狗决策流程" class="headerlink" title="喂狗决策流程"></a>喂狗决策流程</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[Supervisor 周期到达] --&gt; B[读取所有健康快照]</span><br><span class="line">    B --&gt; C&#123;快照完整且时间基准正常?&#125;</span><br><span class="line">    C --&gt;|否| F[进入失败处理]</span><br><span class="line">    C --&gt;|是| D&#123;关键任务执行与业务进度满足?&#125;</span><br><span class="line">    D --&gt;|否| F</span><br><span class="line">    D --&gt;|是| E&#123;锁 / 队列 / I/O / 资源约束满足?&#125;</span><br><span class="line">    E --&gt;|否| F</span><br><span class="line">    E --&gt;|是| M&#123;当前模式允许喂狗?&#125;</span><br><span class="line">    M --&gt;|否| F</span><br><span class="line">    M --&gt;|是| G[喂软件层或硬件看门狗]</span><br><span class="line">    G --&gt; H[记录本轮 Feed Epoch]</span><br><span class="line"></span><br><span class="line">    F --&gt; I&#123;故障是否可局部恢复?&#125;</span><br><span class="line">    I --&gt;|是| J[执行有界恢复并重新评估]</span><br><span class="line">    I --&gt;|否| K[保存故障快照]</span><br><span class="line">    J --&gt; L&#123;恢复成功?&#125;</span><br><span class="line">    L --&gt;|是| H</span><br><span class="line">    L --&gt;|否| K</span><br><span class="line">    K --&gt; N[停止喂狗 / 主动复位 / 触发外部监控]</span><br></pre></td></tr></table></figure><p>Feed Gate 还应避免“旧证据重复使用”。每次喂狗必须消费一个新的监督周期或新的 <code>health epoch</code>，防止某个参与者早先提交过一次健康标志后永久有效。</p><p>推荐使用单调计数器而不是单纯布尔位：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">last_seen_exec_counter[i]</span><br><span class="line">last_seen_progress_counter[i]</span><br><span class="line">current_exec_counter[i]</span><br><span class="line">current_progress_counter[i]</span><br></pre></td></tr></table></figure><p>Supervisor 每轮比较新旧值，再更新时间戳。这样可以避免任务只在启动时置一次 bit，随后一直被误判为健康。</p><h2 id="关键任务应提交哪些健康证据"><a href="#关键任务应提交哪些健康证据" class="headerlink" title="关键任务应提交哪些健康证据"></a>关键任务应提交哪些健康证据</h2><p>不同任务不能用完全相同的 heartbeat 语义。健康点必须放在“任务确实完成核心职责”的位置，而不是任务循环入口、无条件定时回调或错误处理循环中。</p><table><thead><tr><th>任务类型</th><th>不推荐的健康点</th><th>推荐的健康点</th></tr></thead><tbody><tr><td>传感器采集</td><td>任务被唤醒时</td><td>完成一次采样、校验并发布有效数据后</td></tr><tr><td>控制回路</td><td>周期函数入口</td><td>控制量计算完成并成功写入执行器后</td></tr><tr><td>音频处理</td><td>每次循环开始</td><td>音频帧成功处理并提交 DMA 后</td></tr><tr><td>蓝牙&#x2F;网络</td><td>收到任意事件</td><td>事件队列持续消费、连接状态机推进或数据确认完成后</td></tr><tr><td>存储任务</td><td>开始处理请求</td><td>请求完成、提交或明确返回错误后</td></tr><tr><td>UI 渲染</td><td>刷新定时器触发</td><td>一帧渲染完成并提交显示控制器后</td></tr><tr><td>OTA 升级</td><td>下载线程仍在运行</td><td>分块下载、校验、写入和最终提交阶段分别推进后</td></tr><tr><td>文件系统服务</td><td>服务线程被调度</td><td>请求队列完成数增加且无长时间挂起请求</td></tr></tbody></table><h3 id="业务进度指标的设计原则"><a href="#业务进度指标的设计原则" class="headerlink" title="业务进度指标的设计原则"></a>业务进度指标的设计原则</h3><ol><li><strong>必须对应可验证完成点</strong>：在事务提交、帧输出、状态迁移或请求完成后更新。</li><li><strong>必须单调或可比较</strong>：优先使用递增计数、序号、epoch 或最后完成时间。</li><li><strong>不能依赖正在被监控的锁</strong>：否则 Supervisor 为读取健康状态也可能被同一死锁阻塞。</li><li><strong>不能由无关任务代写</strong>：健康证据应能定位责任实体。</li><li><strong>不能无限宽限</strong>：长期等待外部依赖也必须有业务级超时、降级或隔离策略。</li><li><strong>要允许合法空闲</strong>：任务没有输入时，不应强迫 progress counter 变化，而应证明“空闲是被允许的”。</li></ol><p>合法空闲可以通过以下条件表达：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">input_queue_empty</span><br><span class="line">&amp;&amp; no_inflight_transaction</span><br><span class="line">&amp;&amp; upstream_marked_idle</span><br><span class="line">&amp;&amp; task_execution_heartbeat_recent</span><br></pre></td></tr></table></figure><p>只有在没有待办工作时，缺少业务进度才可视为正常；一旦存在 pending work，就必须在 deadline 内看到完成数、消费指针或状态 epoch 推进。</p><h2 id="锁超时与死锁监控"><a href="#锁超时与死锁监控" class="headerlink" title="锁超时与死锁监控"></a>锁超时与死锁监控</h2><p>题目中的 Mutex 循环等待应由两类机制共同覆盖：</p><ol><li><strong>开发期预防与检测</strong>：固定锁顺序、静态分析、运行时 lock dependency 检查、超时断言和故障注入。</li><li><strong>产品运行期监督与恢复</strong>：记录锁 owner、持有时长、等待者和调用点，超过阈值后禁止喂狗并保存现场。</li></ol><h3 id="锁元数据"><a href="#锁元数据" class="headerlink" title="锁元数据"></a>锁元数据</h3><p>建议为关键锁增加轻量包装层，至少记录：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">lock_id</span><br><span class="line">owner_task_id</span><br><span class="line">acquire_timestamp</span><br><span class="line">owner_pc_or_callsite</span><br><span class="line">waiter_bitmap_or_wait_count</span><br><span class="line">max_hold_time</span><br><span class="line">acquire_count</span><br><span class="line">contention_count</span><br><span class="line">timeout_count</span><br></pre></td></tr></table></figure><p>锁获取和释放路径更新原子元数据：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant T as 业务任务</span><br><span class="line">    participant L as Monitored Mutex</span><br><span class="line">    participant M as Lock Metadata</span><br><span class="line">    participant S as Supervisor</span><br><span class="line"></span><br><span class="line">    T-&gt;&gt;L: lock(timeout)</span><br><span class="line">    L-&gt;&gt;M: 记录 waiter / 请求时间</span><br><span class="line">    alt 获取成功</span><br><span class="line">        L--&gt;&gt;T: success</span><br><span class="line">        T-&gt;&gt;M: 设置 owner 与 acquire_time</span><br><span class="line">        T-&gt;&gt;T: 执行业务临界区</span><br><span class="line">        T-&gt;&gt;L: unlock()</span><br><span class="line">        L-&gt;&gt;M: 清除 owner / 更新最大持有时长</span><br><span class="line">    else 等待超时</span><br><span class="line">        L--&gt;&gt;T: timeout</span><br><span class="line">        T-&gt;&gt;M: timeout_count++</span><br><span class="line">    end</span><br><span class="line"></span><br><span class="line">    S-&gt;&gt;M: 无阻塞读取快照</span><br><span class="line">    S-&gt;&gt;S: 计算持有时长和等待关系</span><br></pre></td></tr></table></figure><h3 id="Wait-for-Graph"><a href="#Wait-for-Graph" class="headerlink" title="Wait-for Graph"></a>Wait-for Graph</h3><p>若资源允许，可构造任务与锁的等待图：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    TA[Task A] --&gt;|等待| LB[Mutex B]</span><br><span class="line">    LB --&gt;|持有者| TB[Task B]</span><br><span class="line">    TB --&gt;|等待| LA[Mutex A]</span><br><span class="line">    LA --&gt;|持有者| TA</span><br></pre></td></tr></table></figure><p>图中出现有向环，意味着存在循环等待条件。小型 MCU 不一定要实时运行完整图算法，但可以采用以下折中：</p><ul><li>仅监控少量关键锁。</li><li>为锁定义全局层级 <code>lock_rank</code>，运行时断言只能按 rank 递增获取。</li><li>记录每个任务“当前持有锁集合”和“正在等待锁”。</li><li>仅在 Supervisor 发现超时后离线构造等待链。</li><li>在 Debug&#x2F;Fault-Injection 构建中启用完整依赖图，在 Release 构建中保留轻量持有时长监控。</li></ul><p>Linux 的 lockdep 是典型参考：它记录锁类别之间的获取顺序，并检测可能形成循环依赖的锁顺序。该机制主要用于开发和验证，不能替代产品中的恢复策略，但其“把锁获取顺序表示成依赖图”的思想非常适合嵌入式关键锁审计。</p><h3 id="锁超时不是简单地统一设为一个值"><a href="#锁超时不是简单地统一设为一个值" class="headerlink" title="锁超时不是简单地统一设为一个值"></a>锁超时不是简单地统一设为一个值</h3><p>锁的合理阈值应根据临界区最坏执行时间、可被抢占时间和平台抖动确定：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">T_lock_limit &gt;= T_critical_section_wc</span><br><span class="line">             + T_preemption_wc</span><br><span class="line">             + T_interrupt_jitter</span><br><span class="line">             + T_margin</span><br></pre></td></tr></table></figure><p>不同锁应有不同阈值：</p><table><thead><tr><th>锁类型</th><th>典型阈值策略</th></tr></thead><tbody><tr><td>寄存器或短链表保护锁</td><td>微秒至低毫秒级，超时通常是严重缺陷</td></tr><tr><td>Flash 擦写或文件系统事务锁</td><td>可到数十或数百毫秒，但应有阶段进度</td></tr><tr><td>网络连接管理锁</td><td>不应跨阻塞 I&#x2F;O；若必须存在，应有明确上限</td></tr><tr><td>全局配置锁</td><td>应避免长时间持有，更新过程可拆成 copy-then-swap</td></tr><tr><td>图形或音频大对象锁</td><td>优先改为双缓冲、消息传递或 ownership transfer</td></tr></tbody></table><p>不要把所有锁阈值都设置得大于硬件看门狗超时，否则锁监控失去意义。</p><h2 id="队列、缓冲区与对象池的进度监控"><a href="#队列、缓冲区与对象池的进度监控" class="headerlink" title="队列、缓冲区与对象池的进度监控"></a>队列、缓冲区与对象池的进度监控</h2><p>很多逻辑停滞不是 Mutex 死锁，而是流控闭环失效。例如：</p><ul><li>生产者等待队列空位，消费者等待另一个资源。</li><li>消费者仍在运行，但每次取到的都是无效消息。</li><li>队列持续满，任务不断超时重试。</li><li>消息被生产但从未完成确认，in-flight 数持续增长。</li><li>内存池耗尽，释放路径被某个错误状态跳过。</li></ul><p>因此 Supervisor 不应只检查线程心跳，还要检查队列和资源的“流动性”。推荐记录：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">enqueue_count</span><br><span class="line">dequeue_count</span><br><span class="line">complete_count</span><br><span class="line">drop_count</span><br><span class="line">queue_depth</span><br><span class="line">queue_high_watermark</span><br><span class="line">oldest_item_age</span><br><span class="line">inflight_count</span><br><span class="line">pool_free_count</span><br><span class="line">allocation_fail_count</span><br></pre></td></tr></table></figure><h3 id="队列健康判定示例"><a href="#队列健康判定示例" class="headerlink" title="队列健康判定示例"></a>队列健康判定示例</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">if queue_depth &gt; 0:</span><br><span class="line">    require dequeue_count changes within T_consume</span><br><span class="line">    require oldest_item_age &lt; T_item_deadline</span><br><span class="line"></span><br><span class="line">if queue_depth == capacity:</span><br><span class="line">    require producer_backpressure is active</span><br><span class="line">    require depth leaves full state within T_full_limit</span><br><span class="line"></span><br><span class="line">if inflight_count &gt; 0:</span><br><span class="line">    require complete_count changes within T_complete</span><br></pre></td></tr></table></figure><p>队列水位本身不是故障。队列长期满且出队计数不变，才是高价值故障信号；队列长期空也不一定有问题，除非上游明确存在待生产工作或数据源应持续输出。</p><h3 id="数据流监控图"><a href="#数据流监控图" class="headerlink" title="数据流监控图"></a>数据流监控图</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    P[Producer] --&gt;|enqueue_count| Q[(Queue)]</span><br><span class="line">    Q --&gt;|dequeue_count| C[Consumer]</span><br><span class="line">    C --&gt;|complete_count| O[Output / Commit]</span><br><span class="line">    Q --&gt; M[depth / oldest age]</span><br><span class="line">    P --&gt; M2[drop / timeout]</span><br><span class="line">    C --&gt; M3[inflight / error]</span><br><span class="line">    M --&gt; S[Supervisor]</span><br><span class="line">    M2 --&gt; S</span><br><span class="line">    M3 --&gt; S</span><br></pre></td></tr></table></figure><h2 id="状态机与事务推进监控"><a href="#状态机与事务推进监控" class="headerlink" title="状态机与事务推进监控"></a>状态机与事务推进监控</h2><p>状态机通常不会彻底停止执行，而是卡在某个等待状态、异常分支或重复重试路径中。仅监控任务是否周期运行无法发现这类问题。</p><p>建议每个关键状态机维护：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">current_state</span><br><span class="line">state_enter_timestamp</span><br><span class="line">transition_counter</span><br><span class="line">last_successful_transition</span><br><span class="line">transaction_id</span><br><span class="line">transaction_stage</span><br><span class="line">retry_count</span><br><span class="line">last_error</span><br></pre></td></tr></table></figure><p>Supervisor 根据状态定义最大驻留时间：</p><table><thead><tr><th>状态</th><th align="right">是否允许长期驻留</th><th>监控方式</th></tr></thead><tbody><tr><td>IDLE</td><td align="right">是</td><td>需要证明无待办事务</td></tr><tr><td>WAIT_INPUT</td><td align="right">视场景</td><td>检查输入源是否有 pending 数据</td></tr><tr><td>CONNECTING</td><td align="right">否</td><td>最大连接时限 + 重试预算</td></tr><tr><td>WAIT_RESPONSE</td><td align="right">否</td><td>请求 deadline + 取消机制</td></tr><tr><td>COMMITTING</td><td align="right">否</td><td>阶段进度 + 存储写入时限</td></tr><tr><td>ERROR_RETRY</td><td align="right">否</td><td>最大重试次数 + 退避 + 升级恢复</td></tr><tr><td>DEGRADED</td><td align="right">可以</td><td>必须明确降级能力和退出条件</td></tr></tbody></table><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">stateDiagram-v2</span><br><span class="line">    [*] --&gt; Startup</span><br><span class="line">    Startup --&gt; Ready: 初始化完成</span><br><span class="line">    Ready --&gt; Processing: 收到工作</span><br><span class="line">    Processing --&gt; Ready: 事务完成 / progress++</span><br><span class="line">    Processing --&gt; WaitingIO: 提交外部请求</span><br><span class="line">    WaitingIO --&gt; Processing: I/O 完成</span><br><span class="line">    WaitingIO --&gt; Recovering: 超过 I/O deadline</span><br><span class="line">    Processing --&gt; Recovering: 状态驻留超时</span><br><span class="line">    Recovering --&gt; Ready: 局部恢复成功</span><br><span class="line">    Recovering --&gt; Fatal: 重试预算耗尽</span><br><span class="line">    Fatal --&gt; [*]: 停止喂狗或主动复位</span><br></pre></td></tr></table></figure><p>需要避免一个常见错误：在每次状态机函数调用时更新 heartbeat。正确做法是在合法状态转换、事务阶段提交或确认完成时更新 <code>transition_counter</code> 和 <code>progress_epoch</code>。</p><h2 id="Supervisor-自身必须避免成为死锁参与者"><a href="#Supervisor-自身必须避免成为死锁参与者" class="headerlink" title="Supervisor 自身必须避免成为死锁参与者"></a>Supervisor 自身必须避免成为死锁参与者</h2><p>Supervisor 的健康检查路径应遵循“只读、有限时、无业务依赖”的设计原则。它不应为了检查系统健康而获取正在被检查的业务 Mutex，也不应同步调用可能阻塞的驱动、文件系统、网络或日志接口。</p><h3 id="推荐的数据采集方式"><a href="#推荐的数据采集方式" class="headerlink" title="推荐的数据采集方式"></a>推荐的数据采集方式</h3><ol><li><strong>原子变量</strong>：计数器、时间戳、状态枚举和 bitmask 使用原子读写。</li><li><strong>版本化快照</strong>：写侧先更新 sequence 为奇数，写完后变成偶数；读侧检查前后 sequence 一致。</li><li><strong>双缓冲快照</strong>：业务任务更新非活动页，再原子切换索引。</li><li><strong>单向事件记录</strong>：任务向无阻塞 ring buffer 追加事件，Supervisor 只读。</li><li><strong>调试寄存器或 TCB 只读访问</strong>：读取任务状态、栈水位和 PC&#x2F;LR，不等待业务锁。</li></ol><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">Supervisor 不应该做：</span><br><span class="line">- take(business_mutex, WAIT_FOREVER)</span><br><span class="line">- sync_read(file_system)</span><br><span class="line">- blocking_send(log_queue)</span><br><span class="line">- allocate_from_shared_heap_without_limit</span><br><span class="line">- call_into_untrusted_driver()</span><br></pre></td></tr></table></figure><p>若健康数据本身无法在限定时间内读取，应将“无法获得一致快照”视为异常，而不是继续等待。</p><h3 id="Snapshot-一致性示例"><a href="#Snapshot-一致性示例" class="headerlink" title="Snapshot 一致性示例"></a>Snapshot 一致性示例</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief Lightweight health snapshot published by one participant.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="type">atomic_uint_fast32_t</span> seq;</span><br><span class="line">    <span class="type">atomic_uint_fast32_t</span> exec_counter;</span><br><span class="line">    <span class="type">atomic_uint_fast32_t</span> progress_counter;</span><br><span class="line">    <span class="type">atomic_uint_fast32_t</span> state;</span><br><span class="line">    <span class="type">atomic_uint_fast64_t</span> last_progress_us;</span><br><span class="line">    <span class="type">atomic_uint_fast32_t</span> flags;</span><br><span class="line">&#125; <span class="type">health_snapshot_t</span>;</span><br></pre></td></tr></table></figure><p>写侧可以采用 sequence-lock 风格协议；读侧最多重试有限次数。若持续读不到一致快照，Supervisor 记录 <code>snapshot_unstable</code>，不得无限自旋。</p><h2 id="Supervisor-的优先级与调度设计"><a href="#Supervisor-的优先级与调度设计" class="headerlink" title="Supervisor 的优先级与调度设计"></a>Supervisor 的优先级与调度设计</h2><p>Supervisor 不一定要设为系统最高优先级。优先级过高可能掩盖低优先级关键任务长期得不到调度的问题，也可能在系统过载时持续抢占业务任务。</p><p>更合理的设计是：</p><ul><li>Supervisor 具有足够优先级，能在规定时间内运行并评估健康。</li><li>每个 CPU 核保留独立执行探针，检测局部调度停滞。</li><li>硬件看门狗时钟尽量独立于系统主时钟和调度 tick。</li><li>监控 Idle Task、调度 tick、关键周期任务和中断延迟，而不是只监控 Supervisor 本身。</li><li>监督周期要明显小于硬件 WDT timeout，留出诊断和抖动余量。</li></ul><h3 id="可检测的调度异常"><a href="#可检测的调度异常" class="headerlink" title="可检测的调度异常"></a>可检测的调度异常</h3><table><thead><tr><th>异常</th><th>典型证据</th></tr></thead><tbody><tr><td>高优先级任务忙循环</td><td>Idle counter 不再增加；低优先级任务 exec counter 停止</td></tr><tr><td>中断长期关闭</td><td>Tick&#x2F;关键 ISR counter 停止；硬件独立 WDT 最终超时</td></tr><tr><td>单核局部卡死</td><td>该核 per-core counter 停止，其他核仍推进</td></tr><tr><td>调度器异常</td><td>多个互不相关任务同时停止，但某些 ISR 仍运行</td></tr><tr><td>CPU 过载</td><td>任务仍有进展，但 deadline miss 持续增加</td></tr><tr><td>优先级反转</td><td>高优先级任务等待低优先级 owner，锁持有时长异常</td></tr></tbody></table><p>ESP-IDF 的 Task Watchdog 通过监控各 CPU 的 Idle Task 和可订阅任务来发现长时间不让出 CPU 的任务；Zephyr Task Watchdog 则提供多个软件 watchdog channel，并可选用硬件 watchdog 作为 fallback。这些机制说明“一个硬件 WDT 直接对应一个喂狗线程”不足以覆盖多任务系统，应先在软件层聚合多个受监督实体。</p><h2 id="软件任务看门狗与硬件看门狗的分层"><a href="#软件任务看门狗与硬件看门狗的分层" class="headerlink" title="软件任务看门狗与硬件看门狗的分层"></a>软件任务看门狗与硬件看门狗的分层</h2><p>建议采用至少两层：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[关键任务 / 状态机 / ISR] --&gt; B[软件健康通道]</span><br><span class="line">    B --&gt; C[Health Supervisor]</span><br><span class="line">    C --&gt; D[软件 Task Watchdog]</span><br><span class="line">    D --&gt; E[硬件 Watchdog]</span><br><span class="line">    E -.可选.-&gt; F[外部 PMIC / Safety MCU]</span><br></pre></td></tr></table></figure><h3 id="软件层负责"><a href="#软件层负责" class="headerlink" title="软件层负责"></a>软件层负责</h3><ul><li>多参与者注册、动态启停和模式切换。</li><li>每个参与者不同的周期、deadline 和故障阈值。</li><li>业务进度、锁、队列和资源规则。</li><li>局部恢复、降级、子系统重启和故障记录。</li><li>在硬件 WDT 到期前生成诊断快照。</li></ul><h3 id="硬件层负责"><a href="#硬件层负责" class="headerlink" title="硬件层负责"></a>硬件层负责</h3><ul><li>Supervisor 未运行。</li><li>内核或调度器失效。</li><li>中断被长期关闭。</li><li>软件层内存损坏或控制流跑飞。</li><li>CPU 核或时钟域异常。</li><li>软件无法主动完成复位。</li></ul><h3 id="外部监控层负责"><a href="#外部监控层负责" class="headerlink" title="外部监控层负责"></a>外部监控层负责</h3><p>在高可靠系统中，外部 PMIC 或独立 MCU 可监控主处理器的周期脉冲、挑战应答、通信序号和电源状态。外部监控不应只接收固定 GPIO 翻转，否则主 CPU 的错误循环仍可能继续翻转。更强的做法是 challenge-response：外部监控周期发送变化的 challenge，主系统必须经过受控路径计算并返回正确 response，且返回时序必须落在窗口内。</p><h2 id="Window-Watchdog-与双阶段超时"><a href="#Window-Watchdog-与双阶段超时" class="headerlink" title="Window Watchdog 与双阶段超时"></a>Window Watchdog 与双阶段超时</h2><p>普通 watchdog 只检查“是否太晚喂狗”，无法发现错误循环过于频繁地喂狗。Window Watchdog 同时限制最早和最晚喂狗时间：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">T_open &lt;= T_feed &lt;= T_close</span><br></pre></td></tr></table></figure><p>若系统在窗口打开前就喂狗，说明喂狗路径可能进入异常忙循环；若超过窗口关闭时间仍未喂狗，则说明系统停止推进或调度异常。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    A[上次喂狗] --&gt; B[禁止喂狗窗口]</span><br><span class="line">    B --&gt; C[允许喂狗窗口]</span><br><span class="line">    C --&gt; D[超时复位点]</span><br><span class="line">    B -.过早喂狗.-&gt; E[异常 / 复位]</span><br><span class="line">    C -.正常喂狗.-&gt; A</span><br><span class="line">    D --&gt; F[硬件复位]</span><br></pre></td></tr></table></figure><h3 id="Pretimeout-Early-Warning"><a href="#Pretimeout-Early-Warning" class="headerlink" title="Pretimeout &#x2F; Early Warning"></a>Pretimeout &#x2F; Early Warning</h3><p>若硬件支持预超时中断、双阶段 watchdog 或 early warning，可在最终复位前执行最小诊断：</p><ol><li>冻结普通日志写入，避免再次阻塞。</li><li>保存 reset reason、Supervisor failure bitmap 和最近 health epoch。</li><li>记录当前任务、每核 PC&#x2F;LR、栈指针和关键寄存器。</li><li>保存关键锁 owner、等待者和持有时长。</li><li>保存队列水位、最老消息年龄和状态机阶段。</li><li>将固定大小的 crash record 写入 retention RAM、备份寄存器、FRAM 或预留 Flash 区。</li><li>返回中断并让硬件 watchdog 执行最终复位。</li></ol><p>Linux watchdog API 中的 pretimeout 机制就是这一思想：在最终 timeout 之前先触发通知，以便记录 panic、core dump 或其他诊断信息。实现时必须确保 pretimeout handler 不依赖复杂锁、动态内存和可能阻塞的存储路径。</p><h2 id="恢复策略：从局部恢复到系统复位"><a href="#恢复策略：从局部恢复到系统复位" class="headerlink" title="恢复策略：从局部恢复到系统复位"></a>恢复策略：从局部恢复到系统复位</h2><p>发现异常后不应一律立即硬复位，也不能无限尝试局部恢复。推荐建立有界恢复阶梯：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[检测到健康异常] --&gt; B[隔离新请求 / 标记 Not Ready]</span><br><span class="line">    B --&gt; C[取消或超时当前事务]</span><br><span class="line">    C --&gt; D[复位单个外设 / 通信链路]</span><br><span class="line">    D --&gt; E[重启单个 Worker 或子系统]</span><br><span class="line">    E --&gt; F[重建依赖子树]</span><br><span class="line">    F --&gt; G&#123;恢复成功且通过健康观察窗?&#125;</span><br><span class="line">    G --&gt;|是| H[恢复服务]</span><br><span class="line">    G --&gt;|否| I[保存快照并停止喂狗]</span><br><span class="line">    I --&gt; J[硬件复位]</span><br></pre></td></tr></table></figure><h3 id="典型恢复级别"><a href="#典型恢复级别" class="headerlink" title="典型恢复级别"></a>典型恢复级别</h3><table><thead><tr><th>级别</th><th>动作</th><th>适用场景</th><th>风险</th></tr></thead><tbody><tr><td>L0</td><td>清除单个错误标志、重试一次</td><td>瞬态错误</td><td>可能掩盖持续故障</td></tr><tr><td>L1</td><td>取消事务、重置队列或连接</td><td>单事务卡住</td><td>需要保证资源回收完整</td></tr><tr><td>L2</td><td>复位外设、总线或协处理器</td><td>I&#x2F;O 状态机异常</td><td>可能影响共享用户</td></tr><tr><td>L3</td><td>重启任务组或业务子系统</td><td>局部服务失效</td><td>RTOS 任务重建必须安全</td></tr><tr><td>L4</td><td>软复位主系统</td><td>内核仍可控</td><td>可能无法处理调度器故障</td></tr><tr><td>L5</td><td>停止喂硬件 WDT，执行硬复位</td><td>全局不可恢复或软件不可信</td><td>需要持久化现场</td></tr><tr><td>L6</td><td>外部监控断电重启</td><td>主 SoC 或电源域异常</td><td>恢复代价最大</td></tr></tbody></table><p>Erlang&#x2F;OTP Supervisor 的 <code>one_for_one</code>、<code>one_for_all</code>、<code>rest_for_one</code> 和最大重启强度机制可作为恢复策略设计参考。嵌入式系统同样需要明确：是只重启故障任务、重启一组相互依赖任务，还是重启整个系统；同时要限制单位时间内重启次数，避免设备进入无限 reset loop。</p><h2 id="防止复位风暴"><a href="#防止复位风暴" class="headerlink" title="防止复位风暴"></a>防止复位风暴</h2><p>Watchdog 设计必须考虑“复位后仍立即触发同一问题”。推荐保存并评估：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">reset_reason</span><br><span class="line">watchdog_failure_code</span><br><span class="line">boot_attempt_counter</span><br><span class="line">last_boot_duration</span><br><span class="line">consecutive_watchdog_resets</span><br><span class="line">firmware_slot</span><br><span class="line">configuration_generation</span><br></pre></td></tr></table></figure><p>若在短时间内连续多次 watchdog reset，应切换到安全策略：</p><ul><li>回滚到上一个固件槽。</li><li>跳过非关键功能初始化。</li><li>使用最小安全配置。</li><li>禁止自动执行导致故障的任务。</li><li>进入诊断模式并等待维护。</li><li>对外报告稳定的故障码，而不是持续重启。</li></ul><p>复位计数应存放在 retention RAM、备份寄存器或具有耐久性设计的持久化区域，不能每次高频擦写同一 Flash 页。</p><h2 id="超时参数如何量化"><a href="#超时参数如何量化" class="headerlink" title="超时参数如何量化"></a>超时参数如何量化</h2><p>看门狗超时不能凭经验随意设置。至少要区分：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">T_supervisor_period   Supervisor 评估周期</span><br><span class="line">T_participant_deadline 关键参与者业务期限</span><br><span class="line">T_detection          从故障发生到被判定的时间</span><br><span class="line">T_snapshot           保存最小故障快照的时间</span><br><span class="line">T_recovery           允许局部恢复的最大时间</span><br><span class="line">T_feed_jitter        调度与时钟抖动</span><br><span class="line">T_hw_wdt             硬件看门狗超时</span><br></pre></td></tr></table></figure><p>基本约束可表示为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">T_detection &lt;= T_supervisor_period * failure_threshold + T_feed_jitter</span><br></pre></td></tr></table></figure><p>若允许在停止喂狗前保存现场并尝试一次短恢复，则应满足：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">T_hw_wdt &gt; T_detection + T_snapshot + T_recovery + T_margin</span><br></pre></td></tr></table></figure><p>但 <code>T_hw_wdt</code> 也不能过大，否则系统失去快速恢复能力。若硬件支持 pretimeout，可拆分为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">T_pretimeout = T_detection + T_recovery_budget</span><br><span class="line">T_reset      = T_pretimeout + T_snapshot_budget + T_hard_margin</span><br></pre></td></tr></table></figure><h3 id="示例"><a href="#示例" class="headerlink" title="示例"></a>示例</h3><p>假设：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">Supervisor 周期                 100 ms</span><br><span class="line">关键控制任务最长无进展时间       300 ms</span><br><span class="line">连续失败阈值                    2 次</span><br><span class="line">快照保存预算                    50 ms</span><br><span class="line">局部外设恢复预算                200 ms</span><br><span class="line">调度与时钟安全余量              150 ms</span><br></pre></td></tr></table></figure><p>则最坏检测时间约为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">T_detection &lt;= 100 ms * 2 + 100 ms 抖动 = 300 ms</span><br></pre></td></tr></table></figure><p>硬件 WDT 可先估算为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">T_hw_wdt &gt; 300 ms + 50 ms + 200 ms + 150 ms = 700 ms</span><br></pre></td></tr></table></figure><p>实际可取硬件支持的 800ms 或 1s 档位，并通过压力测试验证。若 Flash 写入、低功耗唤醒或无线校准存在合法长延迟，应使用运行模式 profile，而不是把全局 WDT 永久放宽到数十秒。</p><h2 id="运行模式与动态健康策略"><a href="#运行模式与动态健康策略" class="headerlink" title="运行模式与动态健康策略"></a>运行模式与动态健康策略</h2><p>系统在启动、正常运行、升级、工厂测试、深度睡眠和故障恢复阶段，对任务进度的要求不同。不能用一套固定 heartbeat 规则覆盖所有阶段。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">stateDiagram-v2</span><br><span class="line">    [*] --&gt; Boot</span><br><span class="line">    Boot --&gt; Startup: 内核与基础驱动就绪</span><br><span class="line">    Startup --&gt; Operational: 关键服务完成初始化</span><br><span class="line">    Operational --&gt; Maintenance: OTA / 校准 / 大块存储操作</span><br><span class="line">    Operational --&gt; LowPower: 进入低功耗</span><br><span class="line">    LowPower --&gt; Resume: 唤醒</span><br><span class="line">    Resume --&gt; Operational: 健康重新确认</span><br><span class="line">    Maintenance --&gt; Operational: 维护完成</span><br><span class="line">    Startup --&gt; Recovery: 初始化失败</span><br><span class="line">    Operational --&gt; Recovery: 健康异常</span><br><span class="line">    Recovery --&gt; Operational: 局部恢复成功</span><br><span class="line">    Recovery --&gt; Fatal: 超过恢复预算</span><br><span class="line">    Fatal --&gt; [*]</span><br></pre></td></tr></table></figure><h3 id="各模式监督重点"><a href="#各模式监督重点" class="headerlink" title="各模式监督重点"></a>各模式监督重点</h3><table><thead><tr><th>模式</th><th>必须健康的对象</th><th>可暂时豁免的对象</th><th>特殊要求</th></tr></thead><tbody><tr><td>Boot</td><td>时钟、内存、启动链、基础 WDT 接管</td><td>业务任务</td><td>Bootloader 与应用 WDT 无缝交接</td></tr><tr><td>Startup</td><td>初始化状态机和关键驱动</td><td>尚未创建的业务任务</td><td>每个初始化阶段有单独 deadline</td></tr><tr><td>Operational</td><td>全部关键业务参与者</td><td>被明确关闭的可选功能</td><td>使用严格业务进度检查</td></tr><tr><td>Maintenance</td><td>升级、存储、校准任务</td><td>暂停的普通业务任务</td><td>需要有界 lease，不得无限延长</td></tr><tr><td>LowPower</td><td>低功耗控制器和唤醒源</td><td>被挂起任务</td><td>硬件 WDT 需支持暂停或低功耗时钟</td></tr><tr><td>Resume</td><td>时钟恢复、外设恢复、任务恢复</td><td>尚未恢复的可选服务</td><td>恢复后必须重新建立 baseline</td></tr><tr><td>Recovery</td><td>Supervisor、诊断、恢复执行器</td><td>已隔离故障模块</td><td>总恢复时间必须受限</td></tr></tbody></table><h3 id="有界-Health-Lease"><a href="#有界-Health-Lease" class="headerlink" title="有界 Health Lease"></a>有界 Health Lease</h3><p>长 Flash 擦除、证书生成、无线校准等操作可能超过普通 deadline。不要简单 <code>watchdog_disable()</code> 或无限延长 timeout。可使用有界 lease：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">health_lease_begin(participant, operation, max_duration)</span><br><span class="line">health_lease_progress(participant, stage)</span><br><span class="line">health_lease_end(participant, result)</span><br></pre></td></tr></table></figure><p>Supervisor 只允许白名单操作申请 lease，并验证：</p><ul><li>最大持续时间不超过预配置上限。</li><li>操作期间仍有阶段进度。</li><li>其他关键任务仍健康。</li><li>lease 不能嵌套失控或反复续期。</li><li>超时后仍进入正常故障处理。</li></ul><h2 id="多核系统的特殊设计"><a href="#多核系统的特殊设计" class="headerlink" title="多核系统的特殊设计"></a>多核系统的特殊设计</h2><p>在 SMP 或 AMP 系统中，单个 Supervisor 任务可能只运行在一个核上，而另一个核已失效。若硬件 watchdog 只由健康核喂养，就会掩盖局部核故障。</p><h3 id="推荐方案"><a href="#推荐方案" class="headerlink" title="推荐方案"></a>推荐方案</h3><ol><li>每个核都有独立 <code>per_core_epoch</code>，由该核上的定时器、Idle Task 或调度钩子更新。</li><li>关键任务记录实际运行核和最近执行时间。</li><li>Supervisor 评估所有核的 epoch，而不是只检查自身。</li><li>如果 SoC 支持每核 watchdog、NMI 或 cross-core interrupt，应组合使用。</li><li>对 AMP 子系统使用 mailbox sequence、challenge-response 或共享内存 generation counter。</li><li>某核失效后先尝试局部核复位；若共享资源一致性无法保证，则升级为 SoC 复位。</li></ol><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">flowchart TB</span><br><span class="line">    C0[Core 0 Tasks] --&gt; E0[Core 0 Epoch]</span><br><span class="line">    C1[Core 1 Tasks] --&gt; E1[Core 1 Epoch]</span><br><span class="line">    I0[Core 0 ISR/Tick] --&gt; E0</span><br><span class="line">    I1[Core 1 ISR/Tick] --&gt; E1</span><br><span class="line">    E0 --&gt; S[Supervisor]</span><br><span class="line">    E1 --&gt; S</span><br><span class="line">    S --&gt; G&#123;所有必需 Core 推进?&#125;</span><br><span class="line">    G --&gt;|是| W[Feed HW WDT]</span><br><span class="line">    G --&gt;|否| R[抓取跨核现场 / 复位]</span><br></pre></td></tr></table></figure><p>需要注意 cache coherency 和内存屏障。跨核健康计数器应使用原子操作或明确的共享内存同步，不能依赖普通 <code>volatile</code> 推断可见性。</p><h2 id="外设、总线和外部依赖监控"><a href="#外设、总线和外部依赖监控" class="headerlink" title="外设、总线和外部依赖监控"></a>外设、总线和外部依赖监控</h2><p>业务任务可能没有死锁，而是等待永远不完成的 I&#x2F;O。每个外部请求都应具备：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">request_id</span><br><span class="line">submit_timestamp</span><br><span class="line">deadline</span><br><span class="line">completion_timestamp</span><br><span class="line">cancel_or_reset_path</span><br><span class="line">retry_budget</span><br></pre></td></tr></table></figure><h3 id="I-O-健康规则"><a href="#I-O-健康规则" class="headerlink" title="I&#x2F;O 健康规则"></a>I&#x2F;O 健康规则</h3><ul><li>任何同步 I&#x2F;O 都必须有有限 timeout。</li><li>异步请求必须记录 in-flight 年龄。</li><li>丢失中断时可由 timeout 路径轮询状态并复位外设。</li><li>总线复位不能在持有全局业务锁时执行。</li><li>共享总线恢复必须通知所有客户端重新同步。</li><li>外部云服务不可达通常应触发降级或重连，不应直接导致整机 watchdog reset。</li><li>对于本地关键执行器、传感器或安全协处理器，长期不可用可升级为系统复位或安全态。</li></ul><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant App as 业务任务</span><br><span class="line">    participant IO as I/O Manager</span><br><span class="line">    participant Dev as 外设 / 总线</span><br><span class="line">    participant Sup as Supervisor</span><br><span class="line"></span><br><span class="line">    App-&gt;&gt;IO: submit(request_id, deadline)</span><br><span class="line">    IO-&gt;&gt;Dev: start transaction</span><br><span class="line">    alt 正常完成</span><br><span class="line">        Dev--&gt;&gt;IO: completion IRQ</span><br><span class="line">        IO--&gt;&gt;App: result</span><br><span class="line">        IO-&gt;&gt;Sup: complete_count++</span><br><span class="line">    else 中断丢失或设备挂起</span><br><span class="line">        Sup-&gt;&gt;Sup: oldest_inflight_age &gt; deadline</span><br><span class="line">        Sup-&gt;&gt;IO: cancel / reset request</span><br><span class="line">        IO-&gt;&gt;Dev: reset peripheral or bus</span><br><span class="line">        alt 恢复成功</span><br><span class="line">            IO-&gt;&gt;Sup: recovery_epoch++</span><br><span class="line">        else 恢复失败</span><br><span class="line">            Sup-&gt;&gt;Sup: 阻止喂狗并保存现场</span><br><span class="line">        end</span><br><span class="line">    end</span><br></pre></td></tr></table></figure><h2 id="复位前留痕设计"><a href="#复位前留痕设计" class="headerlink" title="复位前留痕设计"></a>复位前留痕设计</h2><p>如果 Watchdog 最终一定会复位，复位前最有价值的工作不是打印大量日志，而是保存一条固定格式、可校验、可在下次启动读取的故障记录。</p><h3 id="建议保存字段"><a href="#建议保存字段" class="headerlink" title="建议保存字段"></a>建议保存字段</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">magic / version / length / crc</span><br><span class="line">reset_sequence</span><br><span class="line">failure_timestamp</span><br><span class="line">boot_id / firmware_version / build_id</span><br><span class="line">health_failure_bitmap</span><br><span class="line">first_failed_participant</span><br><span class="line">last_feed_epoch</span><br><span class="line">current_mode</span><br><span class="line">per_core_pc_lr_sp</span><br><span class="line">current_task_id</span><br><span class="line">critical_task_state[]</span><br><span class="line">lock_owner[] / hold_time[] / wait_lock[]</span><br><span class="line">queue_depth[] / oldest_item_age[]</span><br><span class="line">state_machine_state[] / transition_epoch[]</span><br><span class="line">heap_free / min_heap_free / stack_high_watermark[]</span><br><span class="line">last_n_events from lock-free trace ring</span><br><span class="line">hardware_reset_reason</span><br></pre></td></tr></table></figure><h3 id="存储优先级"><a href="#存储优先级" class="headerlink" title="存储优先级"></a>存储优先级</h3><ol><li>Retention RAM 或备份 SRAM：速度快、对软件路径依赖少。</li><li>RTC Backup Register：容量小，但适合保存故障码和计数。</li><li>FRAM&#x2F;MRAM：适合频繁记录，但取决于硬件。</li><li>预擦除 Flash 记录槽：必须避免在 pretimeout 时做擦除。</li><li>外部安全 MCU：可独立记录主处理器心跳丢失原因。</li></ol><h3 id="不应在最后阶段做的事"><a href="#不应在最后阶段做的事" class="headerlink" title="不应在最后阶段做的事"></a>不应在最后阶段做的事</h3><ul><li>动态分配大块内存。</li><li>获取可能被故障任务持有的锁。</li><li>执行文件系统 mount、sync 或复杂事务。</li><li>通过已经异常的网络发送长报告。</li><li>在看门狗中断里打印海量日志。</li><li>无限等待 DMA、Flash 或 UART 发送完成。</li></ul><p>故障记录必须是 best-effort，并且不能反过来阻止最终复位。</p><h2 id="参考开源组件与机制"><a href="#参考开源组件与机制" class="headerlink" title="参考开源组件与机制"></a>参考开源组件与机制</h2><p>下面列出的组件不应被机械拼装成同一套软件。它们分别提供了任务级 watchdog、硬件 WDT 接口、锁依赖检测、CPU lockup 检测、服务级健康探针和监督树等设计参考。</p><table><thead><tr><th>组件或机制</th><th>直接用途</th><th>可借鉴的核心原理</th><th>局限</th></tr></thead><tbody><tr><td>Zephyr Task Watchdog</td><td>多线程软件 watchdog channel，可选硬件 fallback</td><td>每个任务独立通道、统一超时管理、软件层聚合后交给硬件层</td><td>默认仍偏执行存活，需要业务 progress 扩展</td></tr><tr><td>Zephyr Hardware Watchdog API</td><td>驱动硬件 WDT、窗口和回调</td><td>timeout window、硬件独立复位、统一驱动接口</td><td>不理解业务语义</td></tr><tr><td>ESP-IDF TWDT &#x2F; IWDT</td><td>监控任务、Idle Task 和中断长时间阻塞</td><td>任务订阅、每核 Idle 探针、任务与中断 watchdog 分层</td><td>主要检测不让出 CPU，不等同业务完成</td></tr><tr><td>RT-Thread Watchdog Device</td><td>统一硬件 watchdog 设备接口</td><td>设备抽象、timeout 设置、keepalive 和 start&#x2F;stop</td><td>文档中的 Idle Hook 喂狗示例只能证明系统有空闲时间，不能证明业务健康</td></tr><tr><td>Linux <code>/dev/watchdog</code></td><td>用户态 daemon 驱动硬件 watchdog</td><td>用户态健康检查通过后才 ping；nowayout；pretimeout</td><td>通用 API 不替应用定义健康条件</td></tr><tr><td>Linux lockdep</td><td>开发期锁依赖验证</td><td>锁类别、获取顺序、依赖图、循环检测</td><td>主要用于验证，不负责产品恢复</td></tr><tr><td>Linux soft&#x2F;hard lockup detector</td><td>检测内核任务不调度或中断不响应</td><td>分开监控调度活性和中断活性</td><td>面向 Linux 内核，不覆盖业务状态机</td></tr><tr><td>FreeRTOS Event Groups &#x2F; Task Notifications</td><td>任务向 Supervisor 提交轻量事件和 bit</td><td>低开销事件聚合、从 ISR 延迟提交、任务通知</td><td>bit 置位必须防止旧值重复和无条件置位</td></tr><tr><td>Kubernetes liveness&#x2F;readiness&#x2F;startup probes</td><td>服务健康与重启控制</td><td>区分“能活”“能服务”“启动完成”，失败阈值与周期分离</td><td>面向容器，但健康语义可迁移到嵌入式模式管理</td></tr><tr><td>Erlang&#x2F;OTP Supervisor</td><td>监督进程与分层重启</td><td>one-for-one、one-for-all、rest-for-one、重启强度限制</td><td>进程隔离能力强于多数 MCU RTOS，需要做适配</td></tr></tbody></table><h3 id="Zephyr-Task-Watchdog"><a href="#Zephyr-Task-Watchdog" class="headerlink" title="Zephyr Task Watchdog"></a>Zephyr Task Watchdog</h3><p>Zephyr 的 Task Watchdog 为多个任务提供独立 channel。每个 channel 配置 reload period，任务通过 <code>task_wdt_feed(channel_id)</code> 更新自身通道；Task WDT 还可以把硬件 watchdog 作为 fallback。可借鉴点：</p><ul><li>软件层维护多个参与者，而不是所有任务直接碰硬件寄存器。</li><li>每个参与者有独立 timeout。</li><li>软件监督器失效时，硬件 WDT 仍可执行最终复位。</li><li>通道数量在配置时明确限制，适合资源受限系统。</li></ul><p>要覆盖业务假活，应在 channel feed 前加入本题所述的 progress、lock、queue 和 deadline 判断，而不是在任务循环中无条件调用 feed。</p><h3 id="ESP-IDF-Task-Watchdog-与-Interrupt-Watchdog"><a href="#ESP-IDF-Task-Watchdog-与-Interrupt-Watchdog" class="headerlink" title="ESP-IDF Task Watchdog 与 Interrupt Watchdog"></a>ESP-IDF Task Watchdog 与 Interrupt Watchdog</h3><p>ESP-IDF 将 Task Watchdog 和 Interrupt Watchdog 分开：Task WDT 主要检测任务长期不让出 CPU，可订阅 Idle Task、普通 task 或 user；Interrupt WDT 关注 ISR 或调度 tick 长时间被阻塞。可借鉴点：</p><ul><li>调度活性和中断活性是不同故障域，应分别监控。</li><li>多核系统需要检查各 CPU 的 Idle Task。</li><li>可订阅“user”而不仅是任务，便于监控周期函数或功能模块。</li><li>timeout 后先输出 backtrace，再决定 panic 或复位。</li></ul><p>但即便任务持续 yield，也可能出现业务状态不推进，因此还要在上层增加 Health Contract。</p><h3 id="RT-Thread-Watchdog-Device"><a href="#RT-Thread-Watchdog-Device" class="headerlink" title="RT-Thread Watchdog Device"></a>RT-Thread Watchdog Device</h3><p>RT-Thread 提供统一 Watchdog Device 接口，包括设置 timeout、查询剩余时间、keepalive、start 和 stop。文档示例常在 Idle Hook 中喂狗，这能检测“系统完全没有进入空闲任务”的部分 CPU 饥饿问题，但不能覆盖以下情况：</p><ul><li>死锁任务都处于阻塞态，Idle Task 仍大量运行。</li><li>关键业务任务退出或永远等待，系统仍有空闲时间。</li><li>状态机活锁但持续让出 CPU。</li><li>某个非关键任务正常，关键任务失效。</li></ul><p>因此，在 RT-Thread 项目中应保留设备驱动层，但把 <code>RT_DEVICE_CTRL_WDT_KEEPALIVE</code> 的调用移动到统一 Supervisor，并在调用前完成业务健康聚合。</p><h3 id="Linux-Watchdog-API"><a href="#Linux-Watchdog-API" class="headerlink" title="Linux Watchdog API"></a>Linux Watchdog API</h3><p>Linux <code>/dev/watchdog</code> 模型通常由用户态 daemon 定期 ping 硬件 WDT。官方文档明确给出一个更高级的思路：daemon 可以先检查 HTTP 服务或其他条件，确认系统正常后才写 watchdog。可借鉴点：</p><ul><li>喂狗 daemon 是策略执行者，而不是只负责 sleep + keepalive。</li><li><code>nowayout</code> 防止 daemon 异常退出时意外关闭 watchdog。</li><li>pretimeout 在最终复位前提供现场保存机会。</li><li>reset reason 和 boot status 应在启动后读取。</li></ul><h3 id="Linux-lockdep-与-lockup-detectors"><a href="#Linux-lockdep-与-lockup-detectors" class="headerlink" title="Linux lockdep 与 lockup detectors"></a>Linux lockdep 与 lockup detectors</h3><p>lockdep 通过锁获取顺序构建依赖关系，可检测复杂循环锁依赖。softlockup detector 关注内核线程长时间不被调度，hardlockup detector 关注 CPU 长时间不响应中断。两者共同说明：</p><ul><li>死锁预防需要依赖图或锁顺序验证。</li><li>调度卡死与中断卡死需要不同监控源。</li><li>检测后应保存当前栈回溯和锁状态。</li><li>单一 heartbeat 不能覆盖所有 lockup 类型。</li></ul><h3 id="FreeRTOS-Event-Groups-与-Task-Notifications"><a href="#FreeRTOS-Event-Groups-与-Task-Notifications" class="headerlink" title="FreeRTOS Event Groups 与 Task Notifications"></a>FreeRTOS Event Groups 与 Task Notifications</h3><p>FreeRTOS Event Groups 可以聚合多个任务的事件 bit，Task Notifications 可作为更轻量的 event flag 或计数器。它们适合构建第一版健康上报通道，但需注意：</p><ul><li>不要让任务在循环入口无条件设置健康 bit。</li><li>Supervisor 每轮必须清除或比较 generation，防止旧 bit 重用。</li><li>ISR 上报应只提交轻量事件，不做复杂健康判断。</li><li>如果任务数量超过 event bits，应使用数组、位图分组或计数器表。</li><li>Event Group 只负责通信，不负责定义“何时算业务健康”。</li></ul><h3 id="Kubernetes-健康探针的抽象价值"><a href="#Kubernetes-健康探针的抽象价值" class="headerlink" title="Kubernetes 健康探针的抽象价值"></a>Kubernetes 健康探针的抽象价值</h3><p>Kubernetes 区分 startup、readiness 和 liveness：</p><ul><li>Startup：应用是否完成启动，在此之前不应用普通 liveness 规则误杀慢启动服务。</li><li>Readiness：当前能否接受业务；失败时可以摘除流量，但不一定立即重启。</li><li>Liveness：是否进入只能通过重启恢复的失效状态。</li></ul><p>嵌入式系统可以采用同样分层：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">startup_health   初始化是否按阶段完成</span><br><span class="line">service_ready    当前是否能提供核心功能</span><br><span class="line">system_live      是否仍具备自主恢复和继续推进能力</span><br></pre></td></tr></table></figure><p>例如网络暂时断开可能使 <code>service_ready=false</code>，但不应立即触发整机复位；控制回路 deadlock 则可能直接使 <code>system_live=false</code>。</p><h3 id="Erlang-OTP-Supervisor-的抽象价值"><a href="#Erlang-OTP-Supervisor-的抽象价值" class="headerlink" title="Erlang&#x2F;OTP Supervisor 的抽象价值"></a>Erlang&#x2F;OTP Supervisor 的抽象价值</h3><p>OTP Supervisor 把故障恢复组织成监督树，并提供不同重启策略和最大重启强度。嵌入式项目可对应设计：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">one_for_one   -&gt; 只复位单个驱动或 Worker</span><br><span class="line">rest_for_one  -&gt; 重启故障模块及依赖它的后续模块</span><br><span class="line">one_for_all   -&gt; 重启一组共享状态的协作任务</span><br><span class="line">intensity     -&gt; 限制单位时间内重启次数，超过后升级整机复位</span><br></pre></td></tr></table></figure><p>对于没有进程隔离的 MCU RTOS，直接删除和重建任务可能遗留锁、内存和驱动状态，因此任务级重启必须经过严格设计。若无法证明局部重启安全，应直接重启整个子系统或整机。</p><h2 id="一个可落地的通用接口设计"><a href="#一个可落地的通用接口设计" class="headerlink" title="一个可落地的通用接口设计"></a>一个可落地的通用接口设计</h2><p>下面是一个与具体 RTOS 解耦的简化接口。实际项目可把原子操作、时间函数、任务标识和锁监控接入 FreeRTOS、RT-Thread、Zephyr 或自研内核。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief Health participant criticality.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">enum</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    HEALTH_CRITICAL,</span><br><span class="line">    HEALTH_IMPORTANT,</span><br><span class="line">    HEALTH_OPTIONAL,</span><br><span class="line">&#125; <span class="type">health_criticality_t</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief Runtime health state reported by a participant.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">enum</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    HEALTH_STATE_STARTING,</span><br><span class="line">    HEALTH_STATE_READY,</span><br><span class="line">    HEALTH_STATE_IDLE,</span><br><span class="line">    HEALTH_STATE_BUSY,</span><br><span class="line">    HEALTH_STATE_DEGRADED,</span><br><span class="line">    HEALTH_STATE_FAILED,</span><br><span class="line">&#125; <span class="type">health_state_t</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief Static health contract of one supervised participant.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="type">const</span> <span class="type">char</span> *name;</span><br><span class="line">    <span class="type">health_criticality_t</span> criticality;</span><br><span class="line">    <span class="type">uint32_t</span> exec_deadline_ms;</span><br><span class="line">    <span class="type">uint32_t</span> progress_deadline_ms;</span><br><span class="line">    <span class="type">uint32_t</span> startup_grace_ms;</span><br><span class="line">    <span class="type">uint8_t</span> failure_threshold;</span><br><span class="line">    <span class="type">uint32_t</span> required_mode_mask;</span><br><span class="line">&#125; <span class="type">health_contract_t</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief Register one participant and return its handle.</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * @param contract Static health contract copied or referenced by the manager.</span></span><br><span class="line"><span class="comment"> * @return Non-negative participant handle on success; negative error code otherwise.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">health_register</span><span class="params">(<span class="type">const</span> <span class="type">health_contract_t</span> *contract)</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief Report that the participant has obtained execution time.</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * @param handle Participant handle returned by health_register().</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">health_report_execution</span><span class="params">(<span class="type">int</span> handle)</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief Report completion of a meaningful business progress point.</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * @param handle Participant handle returned by health_register().</span></span><br><span class="line"><span class="comment"> * @param progress_id Monotonic stage, transaction, frame, or generation identifier.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">health_report_progress</span><span class="params">(<span class="type">int</span> handle, <span class="type">uint32_t</span> progress_id)</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief Publish the current business state without blocking.</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * @param handle Participant handle returned by health_register().</span></span><br><span class="line"><span class="comment"> * @param state Current participant health state.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">health_set_state</span><span class="params">(<span class="type">int</span> handle, <span class="type">health_state_t</span> state)</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief Start a bounded long-operation lease.</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * @param handle Participant handle returned by health_register().</span></span><br><span class="line"><span class="comment"> * @param max_duration_ms Maximum duration accepted by the policy.</span></span><br><span class="line"><span class="comment"> * @return 0 on success; negative error code otherwise.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">health_lease_begin</span><span class="params">(<span class="type">int</span> handle, <span class="type">uint32_t</span> max_duration_ms)</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief End a previously granted long-operation lease.</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * @param handle Participant handle returned by health_register().</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">health_lease_end</span><span class="params">(<span class="type">int</span> handle)</span>;</span><br></pre></td></tr></table></figure><h3 id="参与者上报示例"><a href="#参与者上报示例" class="headerlink" title="参与者上报示例"></a>参与者上报示例</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">for</span> (;;)</span><br><span class="line">&#123;</span><br><span class="line">    health_report_execution(audio_health);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (audio_input_available())</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> (audio_process_one_frame() == <span class="number">0</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            frame_sequence++;</span><br><span class="line">            health_report_progress(audio_health, frame_sequence);</span><br><span class="line">            health_set_state(audio_health, HEALTH_STATE_READY);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">        &#123;</span><br><span class="line">            health_set_state(audio_health, HEALTH_STATE_DEGRADED);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">    &#123;</span><br><span class="line">        health_set_state(audio_health, HEALTH_STATE_IDLE);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    task_wait_until_next_period();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>关键点是：<code>health_report_execution()</code> 与 <code>health_report_progress()</code> 分开。前者证明任务被调度，后者只有在业务完成点更新。</p><h3 id="Supervisor-主循环伪代码"><a href="#Supervisor-主循环伪代码" class="headerlink" title="Supervisor 主循环伪代码"></a>Supervisor 主循环伪代码</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">for</span> (;;)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">const</span> <span class="type">uint64_t</span> now = monotonic_time_us();</span><br><span class="line">    <span class="type">health_result_t</span> result;</span><br><span class="line"></span><br><span class="line">    health_collect_snapshot(&amp;snapshot, now);</span><br><span class="line">    result = health_evaluate(&amp;snapshot, current_mode, now);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (result.feed_allowed)</span><br><span class="line">    &#123;</span><br><span class="line">        hardware_watchdog_keepalive();</span><br><span class="line">        health_record_feed_epoch(now);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">    &#123;</span><br><span class="line">        health_isolate_failed_services(&amp;result);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (!health_try_bounded_recovery(&amp;result))</span><br><span class="line">        &#123;</span><br><span class="line">            health_store_crash_record(&amp;snapshot, &amp;result);</span><br><span class="line">            health_stop_feeding_watchdog();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    supervisor_wait_until_next_period();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里的 <code>health_try_bounded_recovery()</code> 必须有严格的时间预算。进入停止喂狗状态后，不应因为普通任务随后又更新了 heartbeat 就重新喂狗，除非系统明确完成一次受控恢复并通过健康观察窗。</p><h2 id="Feed-Epoch-与一致性协议"><a href="#Feed-Epoch-与一致性协议" class="headerlink" title="Feed Epoch 与一致性协议"></a>Feed Epoch 与一致性协议</h2><p>在多任务系统中，简单的 bitmask 方案容易出现竞态。例如 Supervisor 清 bit 的同时，任务又置 bit，可能丢失一轮心跳；或者任务启动时置位一次，Supervisor 未正确清除，导致后续一直健康。</p><p>推荐使用 epoch：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">supervisor_epoch = N</span><br><span class="line">participant_seen_epoch[i]</span><br><span class="line">participant_progress_epoch[i]</span><br></pre></td></tr></table></figure><p>一种协议是：Supervisor 每轮递增全局 epoch；参与者在完成健康点后把本地 <code>seen_epoch</code> 更新为当前 epoch。Supervisor 只接受本轮或允许窗口内的 epoch。</p><p>更简单且更健壮的方案是直接比较单调 counter：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">if current_exec_counter != previous_exec_counter:</span><br><span class="line">    execution_progressed = true</span><br><span class="line"></span><br><span class="line">if current_progress_counter != previous_progress_counter:</span><br><span class="line">    business_progressed = true</span><br></pre></td></tr></table></figure><p>对于低频任务，不要求每个 Supervisor 周期都变化，而是用最后变化时间与任务 contract 比较。</p><h2 id="任务退出、挂起和动态创建"><a href="#任务退出、挂起和动态创建" class="headerlink" title="任务退出、挂起和动态创建"></a>任务退出、挂起和动态创建</h2><p>监督系统必须处理任务生命周期，否则合法停用会被误判为故障，异常退出又可能被忽略。</p><h3 id="生命周期状态"><a href="#生命周期状态" class="headerlink" title="生命周期状态"></a>生命周期状态</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">UNREGISTERED</span><br><span class="line">STARTING</span><br><span class="line">ACTIVE</span><br><span class="line">SUSPENDED_BY_POLICY</span><br><span class="line">STOPPING</span><br><span class="line">STOPPED</span><br><span class="line">FAILED</span><br></pre></td></tr></table></figure><p>规则建议：</p><ul><li>任务创建后进入 STARTING，并获得有限 startup grace。</li><li>完成初始化后必须显式进入 ACTIVE。</li><li>只有 Supervisor 或系统模式管理器可批准 <code>SUSPENDED_BY_POLICY</code>。</li><li>业务任务不能自行永久关闭自身监督。</li><li>任务退出钩子要将状态改为 STOPPED 或 FAILED。</li><li>关键任务异常退出立即阻止喂狗或触发恢复。</li><li>动态任务的 health handle 必须有 generation，防止旧 handle 指向新任务。</li></ul><h2 id="时间基准的可靠性"><a href="#时间基准的可靠性" class="headerlink" title="时间基准的可靠性"></a>时间基准的可靠性</h2><p>所有 deadline 判断都依赖时间。如果系统 tick 停止或时钟被错误重配，Supervisor 可能永远认为没有超时。因此应至少有两个层次的时间证据：</p><ul><li>RTOS monotonic tick：用于普通软件 deadline。</li><li>独立硬件 watchdog clock、RTC、低速独立振荡器或外部监控时钟：用于最终超时。</li></ul><p>可选地交叉检查：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">delta_rtos_tick</span><br><span class="line">vs.</span><br><span class="line">delta_independent_timer</span><br></pre></td></tr></table></figure><p>若两者长期偏差超过阈值，应报告 timebase fault。进入低功耗前必须明确：硬件 WDT 是否暂停、是否继续计时、唤醒后剩余窗口是多少，以及软件基准是否发生跳变。</p><h2 id="内存与栈健康"><a href="#内存与栈健康" class="headerlink" title="内存与栈健康"></a>内存与栈健康</h2><p>逻辑停滞也可能由内存耗尽、栈接近溢出或内存破坏引发。可把以下指标纳入健康快照：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">heap_free</span><br><span class="line">heap_min_ever_free</span><br><span class="line">allocation_fail_count</span><br><span class="line">pool_free_count</span><br><span class="line">stack_high_watermark per critical task</span><br><span class="line">stack_overflow_hook_count</span><br><span class="line">memory_guard_error</span><br></pre></td></tr></table></figure><p>但阈值应避免过度敏感：堆空间低不等于立即复位；若关键分配已失败、恢复路径也无法分配，才可能升级为系统故障。更推荐对关键路径使用静态内存、专用内存池或预分配恢复资源，使 Supervisor 和 crash recorder 不依赖普通堆。</p><h2 id="安全性与故障注入考虑"><a href="#安全性与故障注入考虑" class="headerlink" title="安全性与故障注入考虑"></a>安全性与故障注入考虑</h2><p>Watchdog 通道本身也可能被错误代码或恶意输入滥用：</p><ul><li>任意模块都能直接写硬件 WDT 寄存器。</li><li>任务可以伪造其他参与者的 heartbeat。</li><li>越界写破坏 health table。</li><li>故障代码反复申请长 lease。</li><li>诊断接口可远程关闭 watchdog。</li></ul><p>建议：</p><ol><li>只允许 Supervisor 所在特权域访问硬件 WDT。</li><li>Health API 校验 handle generation 和调用者身份。</li><li>Watchdog 启动后使用 hardware lock 或 nowayout 类配置，禁止普通软件关闭。</li><li>Health table 放在受 MPU&#x2F;MMU 保护区域，业务任务只能写自身槽位。</li><li>Lease 类型、最大时长和次数由静态策略控制。</li><li>远程维护命令只能切换到预定义 maintenance profile，不能无期限停用 watchdog。</li><li>故障记录带 CRC、版本和单调序号，防止读取损坏数据后误诊。</li></ol><h2 id="测试与故障注入"><a href="#测试与故障注入" class="headerlink" title="测试与故障注入"></a>测试与故障注入</h2><p>只有能被主动触发并验证的 watchdog 方案才可信。测试不能只确认“正常运行时不会误复位”，还要确认每类异常都能在规定时间内被检测、留痕和恢复。</p><h3 id="故障注入矩阵"><a href="#故障注入矩阵" class="headerlink" title="故障注入矩阵"></a>故障注入矩阵</h3><table><thead><tr><th>注入故障</th><th>预期观测</th><th>预期动作</th></tr></thead><tbody><tr><td>任务永久阻塞</td><td>exec counter 停止</td><td>超过 deadline 后阻止喂狗</td></tr><tr><td>任务错误循环且持续 heartbeat</td><td>exec 变化、progress 不变</td><td>业务 deadline 到期后复位或恢复</td></tr><tr><td>两锁反向获取</td><td>lock hold 超时、等待环</td><td>保存 owner&#x2F;waiter，停止喂狗</td></tr><tr><td>高优先级忙循环</td><td>Idle counter 与低优先级任务停滞</td><td>Task&#x2F;CPU watchdog 触发</td></tr><tr><td>长时间关闭中断</td><td>Tick&#x2F;ISR 停止</td><td>硬件或 interrupt watchdog 触发</td></tr><tr><td>队列永久满</td><td>depth 满、dequeue 不变、oldest age 增长</td><td>隔离生产者，恢复失败后复位</td></tr><tr><td>外设不产生完成中断</td><td>in-flight age 超时</td><td>取消事务并复位外设</td></tr><tr><td>单核停滞</td><td>对应 per-core epoch 不变</td><td>抓取跨核现场并复位</td></tr><tr><td>Supervisor 自身挂起</td><td>无后续硬件 feed</td><td>硬件 WDT 直接复位</td></tr><tr><td>pretimeout 保存路径故障</td><td>crash record 不完整</td><td>最终复位仍必须发生</td></tr><tr><td>连续启动失败</td><td>reset counter 增长</td><td>进入安全模式或回滚槽位</td></tr><tr><td>合法长 Flash 操作</td><td>lease 有阶段进度</td><td>不误复位，超 lease 则失败</td></tr></tbody></table><h3 id="必测指标"><a href="#必测指标" class="headerlink" title="必测指标"></a>必测指标</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">fault_detection_latency</span><br><span class="line">recovery_latency</span><br><span class="line">watchdog_reset_latency</span><br><span class="line">false_positive_rate</span><br><span class="line">snapshot_success_rate</span><br><span class="line">snapshot_write_time</span><br><span class="line">maximum_supervisor_execution_time</span><br><span class="line">health_update_overhead</span><br><span class="line">lock_monitor_overhead</span><br><span class="line">reset_storm_escape_success</span><br></pre></td></tr></table></figure><h3 id="压力测试组合"><a href="#压力测试组合" class="headerlink" title="压力测试组合"></a>压力测试组合</h3><ul><li>CPU 100% 负载 + 高频中断 + Flash 写入。</li><li>低内存 + 队列拥塞 + 多任务优先级竞争。</li><li>Tickless idle + 频繁睡眠唤醒。</li><li>双核同时执行 cache-heavy 工作负载。</li><li>日志后端阻塞或 UART 拔除。</li><li>文件系统损坏、网络断开、外设 NACK 和 DMA 丢中断。</li><li>在 Supervisor 评估期间随机改变任务状态，验证快照一致性。</li></ul><h2 id="常见错误设计"><a href="#常见错误设计" class="headerlink" title="常见错误设计"></a>常见错误设计</h2><h3 id="错误一：在-Idle-Hook-无条件喂狗"><a href="#错误一：在-Idle-Hook-无条件喂狗" class="headerlink" title="错误一：在 Idle Hook 无条件喂狗"></a>错误一：在 Idle Hook 无条件喂狗</h3><p>Idle Hook 只能证明系统存在空闲 CPU 时间。若关键任务都死锁并进入阻塞态，Idle Task 反而会运行得更多，因此看门狗永远不会超时。</p><h3 id="错误二：由最高优先级-Watchdog-Task-无条件喂狗"><a href="#错误二：由最高优先级-Watchdog-Task-无条件喂狗" class="headerlink" title="错误二：由最高优先级 Watchdog Task 无条件喂狗"></a>错误二：由最高优先级 Watchdog Task 无条件喂狗</h3><p>它只能证明该任务未被阻塞。高优先级本身还可能掩盖低优先级关键任务的饥饿。</p><h3 id="错误三：每个任务独立直接喂硬件-WDT"><a href="#错误三：每个任务独立直接喂硬件-WDT" class="headerlink" title="错误三：每个任务独立直接喂硬件 WDT"></a>错误三：每个任务独立直接喂硬件 WDT</h3><p>只要任何一个任务成功喂狗，其他任务失效就可能被掩盖；同时无法统一评估业务依赖和故障优先级。</p><h3 id="错误四：每个任务只置一个布尔-heartbeat-bit"><a href="#错误四：每个任务只置一个布尔-heartbeat-bit" class="headerlink" title="错误四：每个任务只置一个布尔 heartbeat bit"></a>错误四：每个任务只置一个布尔 heartbeat bit</h3><p>旧 bit 可能被重复使用，任务也可能在错误循环中持续置位。应使用 generation、counter、deadline 和业务 progress。</p><h3 id="错误五：Supervisor-为检查状态而获取业务锁"><a href="#错误五：Supervisor-为检查状态而获取业务锁" class="headerlink" title="错误五：Supervisor 为检查状态而获取业务锁"></a>错误五：Supervisor 为检查状态而获取业务锁</h3><p>Supervisor 可能加入死锁环，导致无法保存现场。应使用原子元数据或无锁快照。</p><h3 id="错误六：发现异常后无限尝试恢复"><a href="#错误六：发现异常后无限尝试恢复" class="headerlink" title="错误六：发现异常后无限尝试恢复"></a>错误六：发现异常后无限尝试恢复</h3><p>恢复循环自身可能成为活锁并持续喂狗。必须限制恢复次数和总时间。</p><h3 id="错误七：为避免误复位把-WDT-timeout-调得极大"><a href="#错误七：为避免误复位把-WDT-timeout-调得极大" class="headerlink" title="错误七：为避免误复位把 WDT timeout 调得极大"></a>错误七：为避免误复位把 WDT timeout 调得极大</h3><p>这只会延迟故障恢复。应使用模式化 deadline、startup grace 和有界 lease，而不是永久放宽全局超时。</p><h3 id="错误八：复位前执行复杂日志和文件系统操作"><a href="#错误八：复位前执行复杂日志和文件系统操作" class="headerlink" title="错误八：复位前执行复杂日志和文件系统操作"></a>错误八：复位前执行复杂日志和文件系统操作</h3><p>故障时这些子系统可能正是被卡住的部分。应写固定大小、无阻塞、预分配的 crash record。</p><h3 id="错误九：把外部服务不可达等同于整机不健康"><a href="#错误九：把外部服务不可达等同于整机不健康" class="headerlink" title="错误九：把外部服务不可达等同于整机不健康"></a>错误九：把外部服务不可达等同于整机不健康</h3><p>云端断网、服务器维护或网络弱信号通常应降级、重连或标记 not ready，而不是直接整机复位。Watchdog 应关注本地系统是否仍能正确执行恢复策略。</p><h3 id="错误十：只做运行期复位，不做开发期锁验证"><a href="#错误十：只做运行期复位，不做开发期锁验证" class="headerlink" title="错误十：只做运行期复位，不做开发期锁验证"></a>错误十：只做运行期复位，不做开发期锁验证</h3><p>Watchdog 能恢复服务，但不能消除缺陷。应结合固定锁顺序、超时 API、静态分析、运行时依赖检查和故障注入，从源头降低死锁概率。</p><h2 id="推荐实施步骤"><a href="#推荐实施步骤" class="headerlink" title="推荐实施步骤"></a>推荐实施步骤</h2><h3 id="第一阶段：建立最小闭环"><a href="#第一阶段：建立最小闭环" class="headerlink" title="第一阶段：建立最小闭环"></a>第一阶段：建立最小闭环</h3><ol><li>选出真正影响设备核心功能的 3~8 个 critical participant。</li><li>禁止普通任务直接喂硬件 WDT。</li><li>建立唯一 Supervisor 和固定周期评估。</li><li>为每个参与者分别记录 execution counter 与 progress counter。</li><li>配置 startup、operational 两套 profile。</li><li>失败时保存最小 failure bitmap 和 reset reason。</li></ol><h3 id="第二阶段：补齐资源与流控监控"><a href="#第二阶段：补齐资源与流控监控" class="headerlink" title="第二阶段：补齐资源与流控监控"></a>第二阶段：补齐资源与流控监控</h3><ol><li>包装关键 Mutex，记录 owner 和 hold time。</li><li>监控核心队列的 depth、oldest item age 和 complete counter。</li><li>所有 I&#x2F;O 请求增加 deadline 和取消&#x2F;复位路径。</li><li>加入 per-core epoch、Idle counter 和关键 ISR counter。</li><li>使用 pretimeout 或 early warning 保存寄存器和任务快照。</li></ol><h3 id="第三阶段：加入恢复策略与工程化验证"><a href="#第三阶段：加入恢复策略与工程化验证" class="headerlink" title="第三阶段：加入恢复策略与工程化验证"></a>第三阶段：加入恢复策略与工程化验证</h3><ol><li>定义 L0~L5 恢复阶梯和升级条件。</li><li>引入 restart budget，防止恢复风暴。</li><li>实现 reset-loop 检测、安全模式和固件回滚。</li><li>建立系统级 fault injection 测试。</li><li>将检测时间、误复位率、快照成功率纳入发布指标。</li><li>在 Debug 构建中启用更强的锁依赖和断言检查。</li></ol><h2 id="最终回答组织方式"><a href="#最终回答组织方式" class="headerlink" title="最终回答组织方式"></a>最终回答组织方式</h2><p>面试或设计评审中，可以按以下顺序回答：</p><ol><li>先指出根因：独立喂狗任务只能证明自己被调度，不能证明系统业务健康。</li><li>提出分布式健康监督：关键任务提交执行、进度、deadline、锁和队列证据，由 Supervisor 统一判定。</li><li>说明喂狗门控：只有所有 critical health contract 满足，Supervisor 才喂硬件 WDT。</li><li>说明死锁覆盖：记录 Mutex owner、持有时间、等待者和锁顺序；Supervisor 无锁读取，超时后禁止喂狗。</li><li>扩展到通用故障：活锁、饥饿、队列堵塞、状态机停滞、I&#x2F;O 悬挂、单核失效和时间基准故障。</li><li>说明分层 watchdog：软件 Task WDT 聚合多参与者，硬件 WDT 兜底调度器和软件层失效，必要时外部监控再兜底。</li><li>说明恢复与留痕：先有界局部恢复，再子系统重启，最终停止喂狗；pretimeout 保存任务、锁、队列、PC&#x2F;LR 和最近事件。</li><li>说明参数量化：根据监督周期、业务 deadline、快照时间、恢复预算和抖动反推硬件 timeout。</li><li>说明工程落地：参考 Zephyr Task WDT、ESP-IDF TWDT&#x2F;IWDT、Linux watchdog&#x2F;lockdep、RT-Thread WDT、OTP Supervisor 等机制，并通过故障注入验证。</li></ol><p>一句话概括：看门狗不应监督“某个喂狗线程是否活着”，而应监督“系统是否仍在完成它必须完成的工作”；硬件喂狗只是所有关键健康证据通过后的最终动作。</p><h2 id="参考链接"><a href="#参考链接" class="headerlink" title="参考链接"></a>参考链接</h2><ul><li><a href="https://docs.zephyrproject.org/latest/services/task_wdt/index.html">Zephyr Task Watchdog</a></li><li><a href="https://docs.zephyrproject.org/latest/doxygen/html/group__task__wdt__api.html">Zephyr Task Watchdog APIs</a></li><li><a href="https://docs.espressif.com/projects/esp-idf/en/stable/esp32/api-reference/system/wdts.html">ESP-IDF Watchdogs</a></li><li><a href="https://www.rt-thread.io/document/site/programming-manual/device/watchdog/watchdog/">RT-Thread WATCHDOG Device</a></li><li><a href="https://docs.kernel.org/watchdog/watchdog-api.html">Linux Watchdog driver API</a></li><li><a href="https://docs.kernel.org/locking/lockdep-design.html">Linux Runtime locking correctness validator &#x2F; lockdep</a></li><li><a href="https://docs.kernel.org/admin-guide/lockup-watchdogs.html">Linux Softlockup and Hardlockup Detectors</a></li><li><a href="https://www.freertos.org/Documentation/02-Kernel/02-Kernel-features/06-Event-groups">FreeRTOS Event Groups</a></li><li><a href="https://freertos.org/RTOS_Task_Notification_As_Event_Group.html">FreeRTOS Task Notifications</a></li><li><a href="https://kubernetes.io/docs/tasks/configure-pod-container/configure-liveness-readiness-startup-probes/">Kubernetes Liveness, Readiness and Startup Probes</a></li><li><a href="https://www.erlang.org/doc/system/sup_princ.html">Erlang&#x2F;OTP Supervisor Behaviour</a></li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;meta name=&quot;referrer&quot; content=&quot;no-referrer&quot; /&gt;

&lt;h1 id=&quot;嵌入式面试真题第-14-题：防范业务逻辑死锁的系统健康监督与看门狗设计&quot;&gt;&lt;a href=&quot;#嵌入式面试真题第-14-题：防范业务逻辑死锁的系统健康监督与看门狗设计&quot; </summary>
      
    
    
    
    <category term="嵌入式面试真题" scheme="https://wdfk-prog.space/categories/%E5%B5%8C%E5%85%A5%E5%BC%8F%E9%9D%A2%E8%AF%95%E7%9C%9F%E9%A2%98/"/>
    
    
    <category term="嵌入式面试真题" scheme="https://wdfk-prog.space/tags/%E5%B5%8C%E5%85%A5%E5%BC%8F%E9%9D%A2%E8%AF%95%E7%9C%9F%E9%A2%98/"/>
    
  </entry>
  
</feed>
