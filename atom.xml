<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>wdfk-prog的个人博客</title>
  
  <subtitle>WORK-LIFE BALANCE.</subtitle>
  <link href="https://wdfk-prog.space/atom.xml" rel="self"/>
  
  <link href="https://wdfk-prog.space/"/>
  <updated>2026-07-07T01:13:49.492Z</updated>
  <id>https://wdfk-prog.space/</id>
  
  <author>
    <name>Liya Huang</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>资源受限设备中的低功耗短时事件识别架构</title>
    <link href="https://wdfk-prog.space/posts/2cc058af/"/>
    <id>https://wdfk-prog.space/posts/2cc058af/</id>
    <published>2026-07-07T01:12:51.000Z</published>
    <updated>2026-07-07T01:13:49.492Z</updated>
    
    <content type="html"><![CDATA[<meta name="referrer" content="no-referrer" /><h1 id="嵌入式面试真题第-04-题：资源受限设备中的低功耗短时事件识别架构"><a href="#嵌入式面试真题第-04-题：资源受限设备中的低功耗短时事件识别架构" class="headerlink" title="嵌入式面试真题第 04 题：资源受限设备中的低功耗短时事件识别架构"></a>嵌入式面试真题第 04 题：资源受限设备中的低功耗短时事件识别架构</h1><p><img src="https://i-blog.csdnimg.cn/direct/24fecef98d9f44f5a135fab51c028197.png" alt="在这里插入图片描述"></p><h2 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h2><p>在一款资源受限的嵌入式产品中，业务希望通过一个低成本、低维度、低功耗传感器识别某类短时用户意图或异常事件，例如敲击外壳、碰撞、低功耗唤醒、防拆振动、佩戴交互或机械冲击。但硬件受成本、体积和功耗限制，不能增加更高维度传感器、专用 DSP 或复杂边缘 AI 芯片。</p><p>这类事件通常持续时间很短，背景干扰又很多。例如跑步、咀嚼、走路、跌落轻碰、桌面摩擦、线缆拉扯或结构共振，都可能在传感器上产生相似波形。你会如何在 MCU 上设计一套极低功耗的短时事件识别算法，使系统尽量降低误触和漏检，并把“完美屏蔽干扰”这类不可验证需求转化为可落地的工程指标？</p><blockquote><p>具体到本题，目标事件可以实例化为“使用普通单轴加速度计识别双击设备外壳接听电话”。</p></blockquote><h2 id="回答"><a href="#回答" class="headerlink" title="回答"></a>回答</h2><p>结论：这类问题不能抽象成“某个传感器值超过固定阈值就触发事件”。更合理的通用设计是“低功耗粗筛 + 短窗口精采样 + 轻量信号处理 + 特征提取 + 状态机&#x2F;小分类器确认 + 干扰抑制 + 业务门控 + 置信度输出”的事件识别管线。</p><p>这类问题的关键不是追求一个孤立算法，而是把传感器信息量、MCU 算力、功耗预算、目标事件时序、干扰分布和业务触发范围一起设计。低维传感器只能观测真实世界事件的一部分投影。如果干扰事件在该观测维度上的短时波形与目标事件足够相似，算法没有额外信息可用于数学意义上的完美区分。因此工程回答必须明确边界：可以通过多级过滤和业务约束把误触率压到可接受范围，但不能承诺“完美屏蔽”。</p><p>推荐的通用实现步骤如下。</p><ol><li><strong>低功耗粗筛</strong>：传感器或外设长期工作在低功耗监听模式，只检测粗阈值、活动变化、边沿、能量突增或 wake-on-motion。MCU 大部分时间休眠。</li><li><strong>短窗口精采样</strong>：粗筛触发后，MCU 在很短时间内提高采样率，抓取目标事件前后波形，而不是持续高频采样。</li><li><strong>预处理与特征提取</strong>：对窗口数据做去直流、去慢变、限幅、高通&#x2F;带通或滑动能量计算，再提取峰值、峰宽、能量、频带比例、过零率、衰减时间、事件间隔和持续时间。</li><li><strong>状态机或小分类器确认</strong>：用时序状态机确认“候选事件是否符合目标事件结构”；数据足够时，可用小型逻辑回归、决策树或查表分类器替代部分人工规则。</li><li><strong>干扰抑制</strong>：识别持续性、周期性、低频占比高、峰数量异常、衰减过慢或背景活动过强的干扰窗口，并拒绝触发。</li><li><strong>自适应阈值</strong>：根据背景噪声、安装差异、用户使用方式和运动状态动态更新基线，使用 <code>baseline + margin</code>，不要依赖全场景固定阈值。</li><li><strong>业务门控</strong>：只在业务允许的状态下打开高灵敏识别；非目标业务状态下降低灵敏度、延长冷却时间或关闭该识别路径。</li><li><strong>指标化验收</strong>：把“完美屏蔽”改写为检出率、误触率、唤醒频率、平均功耗、响应延迟和边界场景覆盖率。</li></ol><p>一句话概括：低功耗外设负责粗筛，MCU 只在短窗口内做精筛，状态机约束事件时序，干扰模型拒绝高风险样本，业务状态限制触发范围，最终输出置信度而不是绝对真值。</p><h2 id="本题如何落回单轴双击识别"><a href="#本题如何落回单轴双击识别" class="headerlink" title="本题如何落回单轴双击识别"></a>本题如何落回单轴双击识别</h2><p>如果把上述通用问题实例化为“单轴加速度计双击接听电话”，各模块可以对应如下。</p><table><thead><tr><th>通用模块</th><th>单轴双击识别中的落地方式</th></tr></thead><tbody><tr><td>低功耗粗筛</td><td>加速度计低功耗阈值中断或 wake-on-motion 唤醒 MCU。</td></tr><tr><td>短窗口精采样</td><td>中断后临时提高 ODR，采集 100ms 到 300ms 的冲击波形。</td></tr><tr><td>特征提取</td><td>计算峰值、峰宽、短时能量、过零率、衰减时间、峰间隔和峰数量。</td></tr><tr><td>状态机确认</td><td><code>Idle -&gt; FirstTap -&gt; WaitSecondTap -&gt; Confirm/Reject</code>，第二击必须落在合理时间窗内。</td></tr><tr><td>干扰抑制</td><td>跑步看周期性、低频能量和持续时间；咀嚼看节律、连续峰数量和背景活动强度。</td></tr><tr><td>自适应阈值</td><td>根据佩戴松紧、外壳结构、环境噪声和运动评分更新触发阈值。</td></tr><tr><td>业务门控</td><td>只在来电或可接听状态下启用高灵敏双击识别；运动模式或非电话场景降敏。</td></tr><tr><td>验收指标</td><td>用双击检出率、跑步&#x2F;咀嚼误触率、平均电流和响应延迟验收，而不是承诺绝对无误触。</td></tr></tbody></table><p>因此，原题不是单纯考“单轴加速度计怎么识别双击”，而是在考“低成本传感器 + 低功耗 MCU + 强干扰场景下，如何设计一个可验收的事件识别系统”。</p><h2 id="总体架构"><a href="#总体架构" class="headerlink" title="总体架构"></a>总体架构</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    A[单轴加速度计</span><br><span class="line">低功耗活动检测] --&gt;|粗阈值中断| B[MCU 唤醒]</span><br><span class="line">    B --&gt; C[短窗口高频采样</span><br><span class="line">100ms ~ 300ms]</span><br><span class="line">    C --&gt; D[预处理</span><br><span class="line">去直流/高通/限幅]</span><br><span class="line">    D --&gt; E[特征提取</span><br><span class="line">峰值/峰宽/能量/过零率/衰减]</span><br><span class="line">    E --&gt; F&#123;场景抑制</span><br><span class="line">跑步/咀嚼/连续振动&#125;</span><br><span class="line">    F --&gt;|疑似干扰| G[Reject]</span><br><span class="line">    F --&gt;|候选敲击| H[双击状态机]</span><br><span class="line">    H --&gt; I&#123;业务门控</span><br><span class="line">来电/可接听/冷却时间&#125;</span><br><span class="line">    I --&gt;|允许| J[输出 Double Tap 事件]</span><br><span class="line">    I --&gt;|不允许| G</span><br><span class="line">    J --&gt; K[进入 refractory 时间]</span><br><span class="line">    G --&gt; L[回到低功耗监听]</span><br><span class="line">    K --&gt; L</span><br></pre></td></tr></table></figure><p>这个架构里，低功耗传感器中断只是候选事件入口，不直接等于双击事件。MCU 被唤醒后也不应长时间连续采样，而是只采一段足够覆盖敲击波形和第二击等待窗口的数据。真正的双击判断由短窗口波形特征、状态机、干扰抑制和业务上下文共同完成。</p><h2 id="通用机制与芯片-开源实现的对应关系"><a href="#通用机制与芯片-开源实现的对应关系" class="headerlink" title="通用机制与芯片&#x2F;开源实现的对应关系"></a>通用机制与芯片&#x2F;开源实现的对应关系</h2><table><thead><tr><th>通用机制</th><th>可参考机制</th><th>能否直接使用</th><th>主要参考价值</th></tr></thead><tbody><tr><td>低功耗唤醒</td><td>活动检测、wake-up interrupt、wake-on-motion</td><td>如果传感器支持可直接使用；普通单轴传感器至少要用阈值中断</td><td>让 MCU 长时间休眠，只在候选振动出现时唤醒。</td></tr><tr><td>短窗口采样</td><td>传感器高 ODR、FIFO、数据就绪中断</td><td>取决于具体传感器；没有 FIFO 时由 MCU 定时采样</td><td>中断后临时提高采样率，避免长期高功耗。</td></tr><tr><td>单&#x2F;双击时序</td><td>tap threshold、quiet time、latency window</td><td>高端加速度计可能内置；普通单轴传感器需要 MCU 实现</td><td>双击不是两个过阈值点，而是峰形、静默时间和第二击窗口组合。</td></tr><tr><td>轻量滤波</td><td>IIR&#x2F;FIR、高通、滑动能量、峰值检测</td><td>MCU 上直接实现；Cortex-M 可参考 CMSIS-DSP</td><td>去除姿态慢变和低频运动，只保留敲击相关瞬态。</td></tr><tr><td>干扰抑制</td><td>活动识别、步态周期、连续振动检测</td><td>单轴上只能近似实现</td><td>跑步和咀嚼多为持续性或周期性振动，不能只看瞬时峰值。</td></tr><tr><td>业务门控</td><td>来电态、佩戴态、运动态、冷却时间</td><td>必须结合产品业务实现</td><td>把算法工作范围限制到真正需要触发的场景。</td></tr></tbody></table><p>很多三轴加速度计已经内置 tap&#x2F;double tap、活动检测、FIFO 或自动低功耗切换功能；本题的限制是“最普通的单轴传感器”，所以要在 MCU 上软件化实现类似能力。参考这些芯片特性不是为了假设硬件已经具备功能，而是为了理解成熟实现通常会同时配置阈值、静默时间、最大双击间隔、唤醒中断和功耗状态切换。</p><h2 id="低功耗入口如何设计"><a href="#低功耗入口如何设计" class="headerlink" title="低功耗入口如何设计"></a>低功耗入口如何设计</h2><p>平时系统应处于粗筛状态，而不是一直高频采样。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">stateDiagram-v2</span><br><span class="line">    [*] --&gt; SleepListen</span><br><span class="line">    SleepListen --&gt; WakeCapture: 传感器粗阈值中断</span><br><span class="line">    WakeCapture --&gt; Analyze: 采样窗口结束</span><br><span class="line">    Analyze --&gt; WaitSecondTap: 第一击成立</span><br><span class="line">    Analyze --&gt; SleepListen: 拒绝</span><br><span class="line">    WaitSecondTap --&gt; Confirm: 第二击成立</span><br><span class="line">    WaitSecondTap --&gt; SleepListen: 超时或干扰</span><br><span class="line">    Confirm --&gt; Refractory</span><br><span class="line">    Refractory --&gt; SleepListen: 冷却结束</span><br></pre></td></tr></table></figure><p>低功耗入口建议分成三层。</p><table><thead><tr><th>层级</th><th>动作</th><th>目的</th></tr></thead><tbody><tr><td>传感器低功耗层</td><td>配置低 ODR、阈值中断、活动检测或 wake-on-motion</td><td>让传感器承担最低成本的粗筛。</td></tr><tr><td>MCU 睡眠层</td><td>MCU 进入 Stop&#x2F;Standby&#x2F;Light Sleep，只保留 GPIO&#x2F;EXTI&#x2F;RTC</td><td>降低常驻功耗。</td></tr><tr><td>临时精筛层</td><td>中断后提高采样率并开启短窗口算法</td><td>只在候选事件出现后消耗算力。</td></tr></tbody></table><p>传感器如果支持活动&#x2F;非活动联动，可以让传感器在低功耗模式下检测活动，并通过中断唤醒 MCU。若传感器只提供最简单的模拟或数字数据输出，没有活动中断能力，则需要用更低频的 RTC 周期采样做粗筛，但功耗会更高，响应也会更差。</p><h2 id="采样窗口如何设计"><a href="#采样窗口如何设计" class="headerlink" title="采样窗口如何设计"></a>采样窗口如何设计</h2><p>敲击外壳通常是短时冲击响应。它的典型表现不是持续大幅运动，而是一个陡峭峰值后接快速衰减的振铃。因此采样窗口要覆盖峰值、峰宽、衰减和双击间隔。</p><p>建议参数如下，实际需要按产品结构、传感器安装位置和外壳材料实测调整。</p><table><thead><tr><th>参数</th><th align="right">建议范围</th><th>说明</th></tr></thead><tbody><tr><td>粗筛 ODR</td><td align="right">6Hz ~ 50Hz</td><td>只用于唤醒候选事件，越低越省电但越容易错过短冲击。</td></tr><tr><td>精筛 ODR</td><td align="right">200Hz ~ 1000Hz</td><td>覆盖敲击瞬态和振铃；外壳刚性越强，所需频率越高。</td></tr><tr><td>单击分析窗口</td><td align="right">40ms ~ 120ms</td><td>判断峰值、峰宽、衰减和静默时间。</td></tr><tr><td>双击等待窗口</td><td align="right">80ms ~ 350ms</td><td>第二击太近可能是一击振铃，太远用户感知不像双击。</td></tr><tr><td>冷却时间</td><td align="right">500ms ~ 1500ms</td><td>防止同一次动作或连续振动重复触发。</td></tr></tbody></table><p>短窗口策略的功耗优势在于：高频采样不是常态，而是中断后的短时行为。平均功耗可以用以下方式估算：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">I_avg = I_sensor_lp</span><br><span class="line">      + N_wake_per_s * (I_sensor_hr * T_capture + I_mcu_active * T_compute)</span><br><span class="line">      + I_sleep_leakage</span><br></pre></td></tr></table></figure><p>其中 <code>N_wake_per_s</code> 是每秒候选唤醒次数。误唤醒越多，平均功耗越高，所以粗筛阈值不能过低。粗筛阈值的目标不是尽量灵敏，而是在漏检和唤醒频率之间取得平衡。</p><h2 id="预处理与特征提取"><a href="#预处理与特征提取" class="headerlink" title="预处理与特征提取"></a>预处理与特征提取</h2><p>单轴数据可以记为 <code>x[n]</code>。预处理目标是去掉静态重力投影、佩戴姿态慢变和低频运动，只保留敲击瞬态。</p><p>推荐处理顺序如下。</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">raw x[n]</span><br><span class="line">  -&gt; 去直流/慢变基线</span><br><span class="line">  -&gt; 高通或带通滤波</span><br><span class="line">  -&gt; 限幅与异常点处理</span><br><span class="line">  -&gt; 峰值检测</span><br><span class="line">  -&gt; 特征计算</span><br></pre></td></tr></table></figure><p>一个低成本高通可以用一阶 IIR 实现：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">baseline[n] = baseline[n-1] + alpha * (x[n] - baseline[n-1])</span><br><span class="line">x_hp[n]     = x[n] - baseline[n]</span><br></pre></td></tr></table></figure><p>其中 <code>alpha</code> 要足够小，使 baseline 跟踪姿态和慢变运动，而不吞掉敲击瞬态。若 MCU 有足够余量，也可以用短阶 FIR&#x2F;IIR 带通，把低频步态和高频噪声压掉。</p><p>建议提取以下特征。</p><table><thead><tr><th>特征</th><th>含义</th><th>对误触抑制的价值</th></tr></thead><tbody><tr><td><code>peak_abs</code></td><td>窗口内最大绝对峰值</td><td>粗略判断是否有冲击。</td></tr><tr><td><code>peak_width</code></td><td>超过阈值的持续时间</td><td>敲击通常窄；挤压、晃动通常宽。</td></tr><tr><td><code>energy_short</code></td><td>短窗口能量</td><td>判断冲击强度。</td></tr><tr><td><code>energy_low_ratio</code></td><td>低频能量占比</td><td>跑步和晃动低频占比通常更高。</td></tr><tr><td><code>zero_cross_rate</code></td><td>过零次数或符号翻转次数</td><td>敲击振铃可能更明显；慢变运动较少。</td></tr><tr><td><code>decay_time</code></td><td>峰后能量衰减时间</td><td>外壳敲击通常快速衰减；持续振动衰减慢。</td></tr><tr><td><code>peak_count</code></td><td>时间窗内候选峰数量</td><td>跑步、咀嚼可能出现连续节律峰。</td></tr><tr><td><code>inter_tap_gap</code></td><td>两次敲击间隔</td><td>双击必须落在合理时间窗内。</td></tr></tbody></table><p>不要用单一峰值阈值替代这些特征。单轴传感器在跑步落地、线缆碰撞、牙齿咬合或外壳刮擦时都可能出现大峰值；峰形、时序和上下文比峰值本身更重要。</p><h2 id="单击候选如何判定"><a href="#单击候选如何判定" class="headerlink" title="单击候选如何判定"></a>单击候选如何判定</h2><p>单击候选可以用硬门限加评分的方式判断。硬门限用于快速拒绝明显不符合的波形，评分用于处理边界样本。</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">is_tap_candidate =</span><br><span class="line">    peak_abs      &gt;= tap_peak_th       &amp;&amp;</span><br><span class="line">    peak_width    &lt;= tap_width_max     &amp;&amp;</span><br><span class="line">    energy_short  &gt;= tap_energy_th     &amp;&amp;</span><br><span class="line">    decay_time    &lt;= tap_decay_max     &amp;&amp;</span><br><span class="line">    low_ratio     &lt;= low_ratio_max     &amp;&amp;</span><br><span class="line">    peak_count    &lt;= peak_count_max</span><br></pre></td></tr></table></figure><p>然后再计算置信度：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">score = w1 * norm_peak</span><br><span class="line">      + w2 * norm_energy</span><br><span class="line">      + w3 * norm_decay</span><br><span class="line">      + w4 * norm_zcr</span><br><span class="line">      - w5 * norm_low_ratio</span><br><span class="line">      - w6 * norm_periodic_score</span><br></pre></td></tr></table></figure><p>如果产品没有足够训练数据，优先使用规则状态机，不要一开始就上复杂模型。等产测和用户测试积累了足够样本后，可以把规则特征输入到一个很小的逻辑回归、决策树或查表分类器中。低功耗 MCU 上不建议把问题一开始就设计成大模型推理。</p><h2 id="双击状态机如何设计"><a href="#双击状态机如何设计" class="headerlink" title="双击状态机如何设计"></a>双击状态机如何设计</h2><p>双击不是两个峰值过阈值这么简单。第一击后需要有静默约束，第二击需要落在合理间隔内，并且两次峰形都要像敲击。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">stateDiagram-v2</span><br><span class="line">    [*] --&gt; Idle</span><br><span class="line">    Idle --&gt; FirstTap: tap_candidate &amp;&amp; context_ok</span><br><span class="line">    FirstTap --&gt; WaitSecondTap: quiet_ok</span><br><span class="line">    FirstTap --&gt; Reject: quiet_fail / ringing_too_long</span><br><span class="line">    WaitSecondTap --&gt; Confirm: second_tap &amp;&amp; gap_ok &amp;&amp; score_ok</span><br><span class="line">    WaitSecondTap --&gt; Reject: timeout / motion_suppress / context_fail</span><br><span class="line">    Confirm --&gt; Refractory</span><br><span class="line">    Reject --&gt; Idle</span><br><span class="line">    Refractory --&gt; Idle: refractory_timeout</span><br></pre></td></tr></table></figure><p>建议状态参数如下。</p><table><thead><tr><th>参数</th><th align="right">典型范围</th><th>作用</th></tr></thead><tbody><tr><td><code>tap_width_max</code></td><td align="right">10ms ~ 60ms</td><td>限制单击候选的冲击持续时间。</td></tr><tr><td><code>quiet_min</code></td><td align="right">30ms ~ 80ms</td><td>第一击后必须有短静默，避免把振铃当第二击。</td></tr><tr><td><code>gap_min</code></td><td align="right">80ms ~ 120ms</td><td>第二击不能太近。</td></tr><tr><td><code>gap_max</code></td><td align="right">250ms ~ 450ms</td><td>第二击不能太远。</td></tr><tr><td><code>score_confirm_th</code></td><td align="right">按实测标定</td><td>双击确认阈值。</td></tr><tr><td><code>refractory_ms</code></td><td align="right">500ms ~ 1500ms</td><td>触发后冷却，避免重复识别。</td></tr></tbody></table><p>状态机必须把 reject 原因记录下来，例如 <code>REJECT_TOO_LONG</code>、<code>REJECT_PERIODIC</code>、<code>REJECT_LOW_FREQ</code>、<code>REJECT_CONTEXT</code>、<code>REJECT_TIMEOUT</code>。这些原因对后续调参非常重要，否则现场误触只能看到一个失败结果，无法定位是阈值、状态机还是业务门控问题。</p><h2 id="如何抑制跑步误触"><a href="#如何抑制跑步误触" class="headerlink" title="如何抑制跑步误触"></a>如何抑制跑步误触</h2><p>跑步误触的典型问题是周期性冲击。落地冲击可能在单轴上产生很高峰值，但跑步不是孤立双击，而是连续、周期性、持续时间较长的振动序列。</p><p>抑制策略如下。</p><table><thead><tr><th>检测项</th><th>跑步特征</th><th>拒绝策略</th></tr></thead><tbody><tr><td>周期性</td><td>300ms ~ 900ms 左右重复出现峰值，具体与步频有关</td><td>维护 1s ~ 3s 活动窗口，若峰值周期稳定则提高阈值或禁用双击。</td></tr><tr><td>持续能量</td><td>能量不是一次快速衰减，而是持续存在</td><td>若连续多个窗口能量高于背景，进入 <code>motion_suppress</code>。</td></tr><tr><td>低频占比</td><td>步态低频成分更强</td><td><code>energy_low_ratio</code> 超阈值时拒绝。</td></tr><tr><td>峰数量</td><td>一个等待窗口内可能出现多个非敲击峰</td><td><code>peak_count</code> 超过上限时拒绝。</td></tr><tr><td>业务状态</td><td>跑步模式、运动检测、用户正在移动</td><td>由业务状态提高门槛或关闭该手势。</td></tr></tbody></table><p>工程上可以维护一个活动评分：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">motion_score = a * low_freq_energy</span><br><span class="line">             + b * periodic_peak_score</span><br><span class="line">             + c * continuous_energy_time</span><br><span class="line">             + d * wake_frequency</span><br></pre></td></tr></table></figure><p>当 <code>motion_score</code> 高于阈值时，不是直接永久关闭识别，而是进入抑制状态：提高 <code>tap_peak_th</code>、缩短等待窗口、要求更高静默质量，或者仅在来电前几秒内开启识别。</p><h2 id="如何抑制咀嚼误触"><a href="#如何抑制咀嚼误触" class="headerlink" title="如何抑制咀嚼误触"></a>如何抑制咀嚼误触</h2><p>咀嚼误触在耳机、眼镜、头戴或贴近头部的设备上更常见。咀嚼的振动可能沿结构传到外壳，单轴上也可能出现明显峰值。但咀嚼通常具有持续性和节律性，不像两次孤立敲击后快速静默。</p><p>抑制策略如下。</p><table><thead><tr><th>检测项</th><th>咀嚼特征</th><th>拒绝策略</th></tr></thead><tbody><tr><td>持续时间</td><td>往往持续数秒，而不是 300ms 内结束</td><td>连续活动时间超过阈值时进入抑制。</td></tr><tr><td>节律性</td><td>多个相似峰按节律出现</td><td><code>peak_count</code> 和周期稳定性超阈值时拒绝。</td></tr><tr><td>峰形</td><td>可能较宽、衰减慢，或伴随多个小峰</td><td>限制 <code>peak_width</code>、<code>decay_time</code> 和峰后静默。</td></tr><tr><td>触发上下文</td><td>非来电状态下误触价值低</td><td>非电话可接听状态关闭高灵敏度识别。</td></tr></tbody></table><p>需要注意，咀嚼和轻敲在某些结构上可能高度相似。单轴加速度计无法可靠识别“用户意图”，只能识别波形模式。因此文档、需求评审和测试报告里不要写“完全屏蔽咀嚼”，应写“在定义测试集和业务场景下，误触率低于目标阈值”。</p><h2 id="自适应阈值如何做"><a href="#自适应阈值如何做" class="headerlink" title="自适应阈值如何做"></a>自适应阈值如何做</h2><p>固定阈值的问题是样机、量产机、佩戴松紧、结构胶、外壳材料和个体习惯都会改变波形幅值。阈值应随背景噪声和场景调整。</p><p>建议维护以下统计量。</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">noise_floor      = 背景高通能量的滑动估计</span><br><span class="line">peak_baseline    = 非触发窗口峰值的分位数估计</span><br><span class="line">wake_rate        = 单位时间粗筛唤醒次数</span><br><span class="line">motion_score     = 当前运动干扰评分</span><br><span class="line">user_sensitivity = 用户或产品档位</span><br></pre></td></tr></table></figure><p>阈值可以写成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">tap_peak_th   = max(factory_min_peak, peak_baseline + k1 * noise_floor + k2 * motion_score)</span><br><span class="line">tap_energy_th = max(factory_min_energy, energy_baseline + k3 * noise_floor)</span><br><span class="line">score_th      = base_score_th + k4 * motion_score</span><br></pre></td></tr></table></figure><p>自适应要有两个限制。</p><ol><li>只在非候选敲击窗口更新背景，避免把真正敲击学习成噪声。</li><li>设置上下限，避免阈值越调越高导致漏检，或越调越低导致误触。</li></ol><p>如果业务允许，可以把灵敏度分成高、中、低三档。来电态用中高灵敏度，运动态用低灵敏度，非电话态关闭或只保留很粗的事件记录。</p><h2 id="业务门控必须参与识别"><a href="#业务门控必须参与识别" class="headerlink" title="业务门控必须参与识别"></a>业务门控必须参与识别</h2><p>双击接听电话不是通用常开手势，而是强业务相关事件。业务门控越严格，误触越容易控制。</p><p>推荐门控条件如下。</p><table><thead><tr><th>业务状态</th><th>策略</th></tr></thead><tbody><tr><td>无来电、非通话、非可接听</td><td>不输出接听事件；可不启用高灵敏度识别。</td></tr><tr><td>来电响铃</td><td>开启高灵敏度双击识别。</td></tr><tr><td>已接通通话</td><td>根据产品定义切换为挂断、静音或关闭该手势。</td></tr><tr><td>音乐播放</td><td>若双击已有其他语义，避免复用造成冲突。</td></tr><tr><td>运动模式或高活动评分</td><td>提高阈值，要求更严格静默，必要时关闭。</td></tr><tr><td>刚触发过事件</td><td>进入 refractory 时间，不重复输出。</td></tr></tbody></table><p>很多误触不是算法本身完全解决的，而是靠业务状态降低损失。例如用户跑步时没有来电，算法即使检测到疑似双击，也不应触发接听动作。把手势限定在“来电可接听”这一窄场景，误触风险会显著下降。</p><h2 id="MCU-实现建议"><a href="#MCU-实现建议" class="headerlink" title="MCU 实现建议"></a>MCU 实现建议</h2><p>MCU 上建议使用定点实现，避免浮点和复杂频域计算。短窗口样本数很少，规则特征足够高效。</p><h3 id="数据结构"><a href="#数据结构" class="headerlink" title="数据结构"></a>数据结构</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> TAP_MAX_SAMPLES        256U</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> TAP_FEATURE_Q          15</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">enum</span> &#123;</span></span><br><span class="line">    TAP_STATE_IDLE = <span class="number">0</span>,</span><br><span class="line">    TAP_STATE_FIRST_TAP,</span><br><span class="line">    TAP_STATE_WAIT_SECOND,</span><br><span class="line">    TAP_STATE_CONFIRMED,</span><br><span class="line">    TAP_STATE_REFRACTORY,</span><br><span class="line">&#125; <span class="type">tap_state_t</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> &#123;</span></span><br><span class="line">    <span class="type">int16_t</span> peak_abs;</span><br><span class="line">    <span class="type">uint16_t</span> peak_width_ms;</span><br><span class="line">    <span class="type">uint32_t</span> energy_short;</span><br><span class="line">    <span class="type">uint16_t</span> zero_cross_count;</span><br><span class="line">    <span class="type">uint16_t</span> decay_time_ms;</span><br><span class="line">    <span class="type">uint16_t</span> peak_count;</span><br><span class="line">    <span class="type">uint16_t</span> low_energy_ratio_q15;</span><br><span class="line">    <span class="type">uint16_t</span> score_q15;</span><br><span class="line">&#125; <span class="type">tap_feature_t</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> &#123;</span></span><br><span class="line">    <span class="type">tap_state_t</span> state;</span><br><span class="line">    <span class="type">uint32_t</span> first_tap_time_ms;</span><br><span class="line">    <span class="type">uint32_t</span> refractory_until_ms;</span><br><span class="line">    <span class="type">int32_t</span> baseline;</span><br><span class="line">    <span class="type">uint32_t</span> noise_floor;</span><br><span class="line">    <span class="type">uint16_t</span> motion_score_q15;</span><br><span class="line">&#125; <span class="type">tap_detector_t</span>;</span><br></pre></td></tr></table></figure><h3 id="处理流程"><a href="#处理流程" class="headerlink" title="处理流程"></a>处理流程</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">accel_irq_handler()</span><br><span class="line">  -&gt; 记录唤醒时间</span><br><span class="line">  -&gt; 启动高频采样定时器或传感器高 ODR</span><br><span class="line">  -&gt; 采满短窗口后投递 TAP_PROCESS 事件</span><br><span class="line"></span><br><span class="line">tap_process_window(samples)</span><br><span class="line">  -&gt; preprocess()</span><br><span class="line">  -&gt; extract_feature()</span><br><span class="line">  -&gt; update_motion_score()</span><br><span class="line">  -&gt; tap_state_machine()</span><br><span class="line">  -&gt; 输出 NONE / DOUBLE_TAP</span><br><span class="line">  -&gt; 恢复低功耗监听</span><br></pre></td></tr></table></figure><h3 id="计算复杂度"><a href="#计算复杂度" class="headerlink" title="计算复杂度"></a>计算复杂度</h3><p>若窗口为 256 点，每点只做一阶滤波、能量累加、峰值检测和少量比较，则一次识别通常是 O(N) 复杂度。对 Cortex-M0&#x2F;M3&#x2F;M4 这类 MCU 来说，这比持续 FFT 或神经网络推理更适合低功耗场景。</p><h2 id="参数如何量化"><a href="#参数如何量化" class="headerlink" title="参数如何量化"></a>参数如何量化</h2><p>参数不能只凭感觉设置，应先由产品目标反推。</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">P_false_target = 允许误触率，例如每用户每天小于 1 次</span><br><span class="line">P_miss_target  = 允许漏检率，例如标准敲击样本检出率大于 95%</span><br><span class="line">T_response     = 用户可接受响应延迟，例如小于 500ms</span><br><span class="line">I_budget       = 常驻平均电流预算</span><br></pre></td></tr></table></figure><p>然后建立测试集。</p><table><thead><tr><th>数据集</th><th>内容</th><th>用途</th></tr></thead><tbody><tr><td>正样本</td><td>不同用户、不同力度、不同位置、不同佩戴状态下的双击</td><td>标定检出率和双击时间窗。</td></tr><tr><td>跑步负样本</td><td>慢跑、快跑、上下楼、跳跃、手持晃动</td><td>标定运动抑制策略。</td></tr><tr><td>咀嚼负样本</td><td>轻咀嚼、用力咀嚼、说话、大笑、头部动作</td><td>标定持续节律干扰。</td></tr><tr><td>结构干扰</td><td>桌面碰撞、外壳摩擦、线缆拉扯、跌落轻碰</td><td>标定峰宽、衰减和冷却时间。</td></tr><tr><td>业务场景</td><td>来电、非来电、通话中、音乐中、运动模式</td><td>验证业务门控。</td></tr></tbody></table><p>评价指标至少包括检出率、误触率、唤醒频率、平均功耗和响应延迟。只看识别准确率是不够的，因为低功耗识别系统的失败可能表现为功耗超标、响应慢或频繁误唤醒。</p><h2 id="为什么不能承诺完美屏蔽"><a href="#为什么不能承诺完美屏蔽" class="headerlink" title="为什么不能承诺完美屏蔽"></a>为什么不能承诺完美屏蔽</h2><p>原因很直接：单轴传感器只输出一个方向上的加速度投影，它没有三维方向信息，也没有声音、压力、电容触摸或结构应变信息。如果干扰在该轴上的时间波形与真实敲击足够接近，算法没有额外信息可用。</p><p>可以做到的是：</p><ol><li>用波形特征降低非敲击冲击误判。</li><li>用状态机降低振铃和单击误判。</li><li>用运动抑制降低跑步、咀嚼等持续节律振动误判。</li><li>用业务门控降低误触造成业务动作的概率。</li><li>用测试数据定义误触率目标，而不是承诺绝对无误触。</li></ol><p>需求评审时应把“完美屏蔽”改写为可验证指标，例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">在定义佩戴方式和测试动作集合下，来电态双击检出率 &gt;= 95%；</span><br><span class="line">跑步、咀嚼、说话、日常走动负样本误触率 &lt;= X 次/小时；</span><br><span class="line">常驻平均电流增加 &lt;= Y uA；</span><br><span class="line">双击触发响应延迟 &lt;= Z ms。</span><br></pre></td></tr></table></figure><p>这比“完美屏蔽”更适合工程验收。</p><h2 id="调试与量产验证"><a href="#调试与量产验证" class="headerlink" title="调试与量产验证"></a>调试与量产验证</h2><p>建议固件保留轻量日志能力，但不要长期记录原始高频数据。量产固件可只记录特征和 reject reason。</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">timestamp</span><br><span class="line">wake_reason</span><br><span class="line">peak_abs</span><br><span class="line">peak_width_ms</span><br><span class="line">energy_short</span><br><span class="line">low_energy_ratio</span><br><span class="line">zero_cross_count</span><br><span class="line">decay_time_ms</span><br><span class="line">peak_count</span><br><span class="line">motion_score</span><br><span class="line">state</span><br><span class="line">reject_reason</span><br><span class="line">business_context</span><br></pre></td></tr></table></figure><p>调试阶段可以通过串口、RTT、BLE debug 或离线日志导出少量原始窗口数据。量产阶段只保留统计计数，避免功耗、存储和隐私风险。</p><p>参数更新建议采用灰度策略。先在实验室和小批量用户中收集误触与漏检样本，再更新阈值表或固件参数。不要只凭单个开发板上的手敲效果就固化量产阈值。</p><h2 id="最终回答组织方式"><a href="#最终回答组织方式" class="headerlink" title="最终回答组织方式"></a>最终回答组织方式</h2><p>回答这类题时，可以按以下顺序展开：</p><ol><li>先把问题抽象为“资源受限设备中的低功耗短时事件识别”，而不是只讨论某个传感器阈值。</li><li>再说明约束边界：低维传感器信息不足，不能承诺数学意义上的完美屏蔽，只能工程化降低误触率。</li><li>接着讲低功耗架构：外设低功耗粗筛，MCU 短窗口精采样，避免长期高频运行。</li><li>然后讲信号处理：去直流、高通&#x2F;带通、峰值、峰宽、能量、频带比例、过零率和衰减时间。</li><li>再讲事件确认：用状态机或小分类器约束目标事件的时序、形态和置信度。</li><li>补充干扰抑制：持续性、周期性、低频占比高或背景活动过强的窗口优先拒绝。</li><li>最后讲业务门控和验收指标：只在业务相关状态下启用高灵敏识别，用检出率、误触率、功耗和延迟验收。</li></ol><p>一句话概括：这类题的通用答案不是“某个动作怎么判”，而是“如何用弱传感器在强约束下构建一条低功耗、可解释、可调参、可验收的短时事件识别管线”；单轴双击接听电话只是这个框架下的一个具体实例。</p><h2 id="参考链接"><a href="#参考链接" class="headerlink" title="参考链接"></a>参考链接</h2><ul><li><a href="https://www.analog.com/media/en/technical-documentation/data-sheets/adxl362.pdf">Analog Devices ADXL362 Datasheet</a></li><li><a href="https://www.bosch-sensortec.com/media/boschsensortec/downloads/datasheets/bst-bma400-ds000.pdf">Bosch Sensortec BMA400 Datasheet</a></li><li><a href="https://www.st.com/resource/en/datasheet/lis2dw12.pdf">STMicroelectronics LIS2DW12 Datasheet</a></li><li><a href="https://arm-software.github.io/CMSIS-DSP/latest/group__FIR.html">Arm CMSIS-DSP FIR filter documentation</a></li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;meta name=&quot;referrer&quot; content=&quot;no-referrer&quot; /&gt;

&lt;h1 id=&quot;嵌入式面试真题第-04-题：资源受限设备中的低功耗短时事件识别架构&quot;&gt;&lt;a href=&quot;#嵌入式面试真题第-04-题：资源受限设备中的低功耗短时事件识别架构&quot; clas</summary>
      
    
    
    
    <category term="嵌入式面试真题" scheme="https://wdfk-prog.space/categories/%E5%B5%8C%E5%85%A5%E5%BC%8F%E9%9D%A2%E8%AF%95%E7%9C%9F%E9%A2%98/"/>
    
    
    <category term="嵌入式面试真题" scheme="https://wdfk-prog.space/tags/%E5%B5%8C%E5%85%A5%E5%BC%8F%E9%9D%A2%E8%AF%95%E7%9C%9F%E9%A2%98/"/>
    
  </entry>
  
  <entry>
    <title>固定周期后台事件导致实时链路瞬态扰动的定位与自动化测试</title>
    <link href="https://wdfk-prog.space/posts/21995736/"/>
    <id>https://wdfk-prog.space/posts/21995736/</id>
    <published>2026-07-06T01:08:37.000Z</published>
    <updated>2026-07-06T01:12:05.144Z</updated>
    
    <content type="html"><![CDATA[<meta name="referrer" content="no-referrer" /><h1 id="嵌入式面试真题第-03-题：固定周期后台事件导致实时链路瞬态扰动的定位与自动化测试"><a href="#嵌入式面试真题第-03-题：固定周期后台事件导致实时链路瞬态扰动的定位与自动化测试" class="headerlink" title="嵌入式面试真题第 03 题：固定周期后台事件导致实时链路瞬态扰动的定位与自动化测试"></a>嵌入式面试真题第 03 题：固定周期后台事件导致实时链路瞬态扰动的定位与自动化测试</h1><p><img src="https://i-blog.csdnimg.cn/direct/4df2c05889b1403d8494e6ac934dbd99.png" alt="在这里插入图片描述"></p><p><strong>问题</strong></p><p>在一个长时间连续运行的实时数据流系统中，前台数据链路需要按稳定节奏完成输入、缓冲、处理和输出。测试发现，系统整体没有死机、没有重启，普通业务也能继续运行，但实时输出链路每隔一个相对固定的时间间隔就会出现一次极短暂的质量劣化，例如瞬时噪声、卡顿、丢帧、控制抖动、采样尖峰、输出断点或数据不连续。面对这种固定周期、短持续时间、低频复现的瞬态异常，你首先怀疑哪类软件模块或系统行为？又会如何设计自动化测试环境，把异常发生点与系统内部事件对齐，从而定位根因？</p><p><strong>回答</strong></p><p>结论：这类问题本质上不是某一个业务模块的孤立异常，而是“长时间运行的实时数据流系统中，固定周期后台事件对前台实时链路造成瞬态扰动”。排查时不应先陷入具体表现，例如音频 Pop&#x2F;Click、视频丢帧、马达抖动或传感器尖峰，而应先把问题提升到系统层面：前台实时链路需要稳定、连续、低抖动地处理数据；后台周期事件在某个固定时间点触发；两者在 CPU、总线、存储、锁、内存、时钟、电源或调度资源上发生了短时间冲突，最终表现为一次很短的输出质量劣化。</p><p>这类问题的关键特征有三个：第一，它是固定周期或近似固定周期出现的；第二，它持续时间很短，通常不足以触发系统级崩溃；第三，它影响的是实时链路的连续性，而不是普通业务逻辑的正确性。只要具备这三个特征，首先怀疑方向就应该从“随机硬件噪声”转向“周期性软件行为、计数边界、资源竞争或系统状态切换”。</p><h2 id="把具体现象归纳为实时链路被瞬时打断"><a href="#把具体现象归纳为实时链路被瞬时打断" class="headerlink" title="把具体现象归纳为实时链路被瞬时打断"></a>把具体现象归纳为实时链路被瞬时打断</h2><p>在嵌入式系统里，很多前台业务都是实时数据流。音频播放是 PCM 数据流，摄像头是图像帧流，马达控制是控制量输出流，传感器采集是采样数据流，蓝牙或网络传输是包流。这些链路的共同点是：数据必须按固定节奏进入、处理和输出。一旦某个环节短时间停顿，虽然系统还活着，但输出质量就会立刻变差。</p><p>因此，这类系统通常可以看成一条实时链路：</p><p><code>实时输入 -&gt; 缓冲 -&gt; 处理任务 -&gt; 输出外设/DMA -&gt; 用户可感知结果</code></p><p>在这条链路旁边，系统还存在大量后台行为：</p><p><code>日志写入、Flash 擦写、统计任务、状态保存、连接维护、校准任务、低功耗切换、看门狗检查、传感器轮询、协议栈维护</code></p><p>当前台实时链路和后台行为共享某些资源时，只要后台行为在固定周期触发，并且触发瞬间占用了关键资源，就可能造成前台链路短暂断供。这个断供可能只有几毫秒甚至更短，但对音频、视频、控制和传感器链路来说已经足以产生可观察异常。</p><p>所以，这道题真正考察的是能否识别出一种通用故障模式：长时间运行后，周期性后台事件破坏了实时路径的时序确定性。</p><h2 id="固定周期异常优先指向软件时序和资源边界"><a href="#固定周期异常优先指向软件时序和资源边界" class="headerlink" title="固定周期异常优先指向软件时序和资源边界"></a>固定周期异常优先指向软件时序和资源边界</h2><p>如果异常完全随机，可能需要优先考虑电磁干扰、焊接、连接器、外设不稳定或模拟链路噪声。但如果异常每隔固定时间出现一次，例如 10 分钟、30 分钟、1 小时或每天某个时刻附近出现，软件系统的可疑度会明显上升。</p><p>固定周期通常来自以下几类源头。</p><p>第一类是定时触发。系统可能有周期任务、软件定时器、统计窗口、连接维护、心跳上报、日志落盘或状态保存。它们平时不影响实时链路，但一旦触发时执行时间过长，就会短暂抢占资源。</p><p>第二类是计数边界。运行时长、帧计数、采样计数、tick、timestamp、buffer index、文件偏移、日志序号等变量可能在某个时间点发生回绕、进位或边界切换。边界处理不当时，异常会稳定出现在相近的运行时间。</p><p>第三类是资源维护。Flash 垃圾回收、文件系统元数据更新、日志块轮换、链路参数维护、同步校准、电量估算、温度补偿、PLL 校准和低功耗策略刷新都可能以较低频率运行。这些任务不一定频繁，但一旦运行，可能占用总线、Flash、CPU、锁或中断时间。</p><p>第四类是状态切换。系统可能在运行一段时间后进入另一个策略区间，例如降低功耗、切换时钟、刷新校准参数、调整连接参数、保存统计数据或切换日志文件。状态切换瞬间往往比稳定状态更容易暴露问题。</p><p>因此，固定周期本身就是一个强线索。它提示定位时要先找“系统内部还有什么东西也按这个周期发生”，而不是只盯着异常表现所在的业务模块。</p><h2 id="首先怀疑的是系统行为，而不是单个函数"><a href="#首先怀疑的是系统行为，而不是单个函数" class="headerlink" title="首先怀疑的是系统行为，而不是单个函数"></a>首先怀疑的是系统行为，而不是单个函数</h2><h3 id="怀疑周期性后台任务"><a href="#怀疑周期性后台任务" class="headerlink" title="怀疑周期性后台任务"></a>怀疑周期性后台任务</h3><p>首先要列出系统中所有低频周期任务，包括电量统计、温度采样、传感器校准、日志落盘、Flash 擦写、进度保存、连接保活、链路质量统计、协议栈维护、App 同步、老化统计和诊断任务。</p><p>这些任务通常优先级不高，看起来也不属于实时链路。但如果它们在执行过程中持有共享锁、关闭中断、访问慢速 Flash、占用 SPI&#x2F;I2C 总线、触发 cache 维护或输出大量日志，就可能间接阻塞高优先级实时任务。</p><h3 id="怀疑计数器、时间戳和缓冲区边界"><a href="#怀疑计数器、时间戳和缓冲区边界" class="headerlink" title="怀疑计数器、时间戳和缓冲区边界"></a>怀疑计数器、时间戳和缓冲区边界</h3><p>实时系统通常依赖大量递增计数。音频有采样计数和帧计数，视频有帧号，通信有包序号，控制系统有周期计数，日志有文件偏移和块序号。只要计数宽度、单位换算、取模逻辑或比较方式存在问题，就可能在固定运行时间附近触发异常。</p><p>典型问题包括 16-bit 或 32-bit 计数回绕、毫秒和 tick 单位混用、有符号和无符号比较错误、环形缓冲读写指针边界处理错误、DMA 半满&#x2F;全满边界处理不一致、时间戳跳变后状态机误判等。</p><h3 id="怀疑共享资源竞争"><a href="#怀疑共享资源竞争" class="headerlink" title="怀疑共享资源竞争"></a>怀疑共享资源竞争</h3><p>前台实时链路并不一定被自己的代码打断，更多时候是被共享资源拖住。例如实时任务需要从 buffer 取数据，但 buffer 生产者被低优先级任务阻塞；实时任务需要访问总线，但总线被日志或传感器任务占用；实时任务需要分配内存，但内存池被后台诊断任务占满；实时任务需要调度，但系统处于长临界区或中断被屏蔽。</p><p>这类问题的表象通常很短，因为资源很快释放，系统也不会崩溃。但对实时链路来说，一次短暂延迟就可能造成输出断点。</p><h3 id="怀疑时钟、电源和低功耗状态切换"><a href="#怀疑时钟、电源和低功耗状态切换" class="headerlink" title="怀疑时钟、电源和低功耗状态切换"></a>怀疑时钟、电源和低功耗状态切换</h3><p>实时链路通常依赖稳定时钟。音频依赖采样时钟，视频依赖像素时钟或帧同步，马达控制依赖 PWM 和控制周期，通信依赖连接事件和包调度。后台如果定期调整 PLL、切换 DVFS、进入浅睡、刷新时钟配置或切换 DC-DC 模式，就可能造成短时抖动。</p><p>这类问题不一定表现为逻辑错误，而可能表现为输出相位突变、瞬时欠载、I&#x2F;O 时序变化、电源纹波增加或外设 FIFO 状态异常。</p><h3 id="怀疑日志和诊断代码"><a href="#怀疑日志和诊断代码" class="headerlink" title="怀疑日志和诊断代码"></a>怀疑日志和诊断代码</h3><p>日志系统经常被低估。为了定位问题，系统可能打开了较多日志；为了保存现场，日志可能定期写入 Flash；为了轮换文件，日志可能定期擦除块或更新元数据。日志不属于主业务，但很容易访问慢速存储、持有锁、关闭中断或造成总线拥塞。</p><p>如果异常周期与日志 flush、文件轮换、Flash block 擦写或诊断快照保存接近，应优先检查日志路径是否影响了实时链路。</p><h2 id="自动化测试的目标是建立因果证据"><a href="#自动化测试的目标是建立因果证据" class="headerlink" title="自动化测试的目标是建立因果证据"></a>自动化测试的目标是建立因果证据</h2><p>这类问题最难的地方不是“看到一次异常”，而是异常持续时间短、周期长、人工观察效率低。自动化测试的核心目标不是简单跑长测，而是把异常前后的系统状态保留下来，并且把输出异常和内部事件放到同一条时间轴上。</p><p>如果没有统一时间轴，即使抓到了异常，也只能知道“某个时间点出问题了”，很难证明是哪个后台事件导致的。反过来，如果能证明每次异常都和某个内部事件稳定对齐，定位范围会迅速缩小。</p><p>自动化测试环境应围绕四类数据构建：输入可控、输出可检测、内部可观测、事件可对齐。</p><h2 id="使用可控输入降低干扰"><a href="#使用可控输入降低干扰" class="headerlink" title="使用可控输入降低干扰"></a>使用可控输入降低干扰</h2><p>测试输入应尽量稳定。音频系统可以使用 1kHz 正弦、粉噪、扫频或固定码流；视频系统可以使用固定测试图卡或固定帧源；传感器系统可以使用静态输入或可重复激励；控制系统可以使用固定负载和固定目标曲线。</p><p>输入越稳定，输出异常越容易被算法检测。否则内容本身的变化会掩盖异常，导致测试脚本难以判断某个尖峰、断点或抖动到底是输入变化还是系统问题。</p><p>对蓝牙、网络或无线链路，还需要控制环境变量。可以先在屏蔽箱、近距离、固定设备或固定信号源下复现，排除外部干扰，再逐步加入真实环境因素。</p><h2 id="对输出做自动检测"><a href="#对输出做自动检测" class="headerlink" title="对输出做自动检测"></a>对输出做自动检测</h2><p>输出侧需要接入采集设备，并由脚本自动分析异常。音频可以采集模拟输出或数字 I2S 数据，检测尖峰、削顶、瞬时静音、相位突变、频谱异常和短时能量突变。视频可以检测帧间隔、丢帧、重复帧、撕裂和亮度突变。控制系统可以检测 PWM 占空比突变、位置误差尖峰、电流尖峰或速度抖动。传感器系统可以检测采样间隔、异常值和连续性。</p><p>自动检测必须记录异常发生的精确时间。这个时间最好来自同一个单调递增时间源，或者至少能和固件日志、GPIO Marker、Trace 工具进行换算。</p><p>对于极短异常，检测逻辑可以使用短窗口能量、斜率、瞬时峰值、与参考信号的残差、频谱高频成分突变等指标。只要算法能稳定标记异常点，就能把长时间测试转化为可分析数据。</p><h2 id="在固件中建立内部事件观测点"><a href="#在固件中建立内部事件观测点" class="headerlink" title="在固件中建立内部事件观测点"></a>在固件中建立内部事件观测点</h2><p>只采集外部输出是不够的。必须让固件在关键事件发生时留下时间标记。可以使用环形日志、RTOS Trace、性能计数器、GPIO Marker、硬件 timer capture 或内存中的事件缓冲区。</p><p>建议至少记录以下信息。</p><p>第一，实时链路状态。包括 buffer 水位、DMA 回调时间、任务唤醒延迟、处理耗时、underrun&#x2F;overrun 计数、输入包间隔、输出帧间隔和丢包重传信息。</p><p>第二，后台事件状态。包括周期任务进入和退出、Flash 擦写、日志 flush、文件轮换、状态保存、连接维护、校准任务、电源状态变化和时钟配置变化。</p><p>第三，调度状态。包括任务切换、最高关中断时长、mutex owner、锁持有时间、队列积压、内存池占用、CPU 峰值占用和 ISR 执行时间。</p><p>第四，硬件状态。包括电源电压、关键时钟、PLL lock 状态、总线错误、外设 FIFO 状态、cache 维护操作和 DMA 错误标志。</p><p>这些数据不需要全部永久保存。可以使用环形缓冲，只保留最近几十秒或几分钟。一旦外部脚本检测到异常，再触发回捞或由固件冻结异常前后的日志窗口。</p><h2 id="用-GPIO-Marker-连接软件事件和物理波形"><a href="#用-GPIO-Marker-连接软件事件和物理波形" class="headerlink" title="用 GPIO Marker 连接软件事件和物理波形"></a>用 GPIO Marker 连接软件事件和物理波形</h2><p>对于瞬态问题，GPIO Marker 非常有效。固件在关键路径进入和退出时翻转 GPIO，示波器或逻辑分析仪同时采集 GPIO、输出信号、关键时钟和电源纹波，就能把软件事件和物理现象对齐。</p><p>例如，实时链路的 DMA 半满&#x2F;全满回调可以打一组 Marker，Flash 擦写可以打一组 Marker，日志 flush 可以打一组 Marker，PLL 切换可以打一组 Marker，低功耗进入和退出也可以打一组 Marker。只要输出异常总是贴近某个 Marker，就能形成强证据。</p><p>GPIO Marker 的优势是时间精度高，不依赖串口打印，也不会引入大量日志开销。对于毫秒级甚至微秒级问题，它比普通 printf 日志更可靠。</p><h2 id="让测试脚本自动回捞异常窗口"><a href="#让测试脚本自动回捞异常窗口" class="headerlink" title="让测试脚本自动回捞异常窗口"></a>让测试脚本自动回捞异常窗口</h2><p>长时间测试不应该只生成一大堆日志。更合理的方式是让测试脚本持续监测输出，一旦检测到异常，就自动保存异常点前后的一段窗口，例如前 10 秒到后 10 秒。保存内容包括输出波形、固件环形日志、RTOS Trace、通信日志、GPIO 时间线、电源波形和关键状态快照。</p><p>这样做有两个好处。第一，数据量可控，不需要人工在数小时日志中查找。第二，异常前后的上下文完整，便于判断问题是异常点之前已经积累，还是某个事件瞬间触发。</p><p>对于固定周期问题，测试脚本还应统计多次异常的发生时间，计算周期稳定性。如果异常间隔高度稳定，再把内部事件按周期展开，就可以快速找出同频事件。</p><h2 id="用事件表收敛嫌疑范围"><a href="#用事件表收敛嫌疑范围" class="headerlink" title="用事件表收敛嫌疑范围"></a>用事件表收敛嫌疑范围</h2><p>定位时应建立一张周期事件表。表中记录每个可疑事件的触发周期、首次触发时间、执行耗时、优先级、访问资源、是否持锁、是否关中断、是否访问 Flash、是否影响时钟或电源、是否和实时链路共享资源。</p><p>然后把异常时间点和事件表对齐。判断规则不是“某事件看起来可疑”，而是看它是否满足三个条件：时间上长期对齐，资源上存在影响路径，干预后异常发生变化。</p><p>例如，如果每次异常都发生在日志 flush 后 5ms 内，并且日志 flush 会持有 Flash mutex，而实时链路也需要同一 SPI&#x2F;Flash 资源，那么它就是高优先级嫌疑。如果临时关闭日志 flush 后异常消失，或者把日志 flush 延迟到 buffer 高水位时执行后异常消失，就可以进一步确认因果链。</p><p>如果某事件只偶尔接近异常，但关闭后异常不变，通常只是时间上的巧合。</p><h2 id="验证根因要控制变量"><a href="#验证根因要控制变量" class="headerlink" title="验证根因要控制变量"></a>验证根因要控制变量</h2><p>这类问题不能用“一次修改后没复现”来轻易下结论。由于周期长、复现慢，验证必须控制变量。</p><p>比较可靠的方法包括：</p><ol><li>保持固件不变，只打开观测点，确认异常和事件的时间关系。</li><li>只调整一个可疑事件的周期，看异常周期是否随之移动。</li><li>只关闭一个可疑事件，看异常是否消失。</li><li>只改变实时链路 buffer 水位或任务优先级，看异常严重程度是否变化。</li><li>只改变资源访问方式，例如把 Flash 写入移到低风险窗口，看异常是否消失。</li><li>长时间重复运行，确认不是偶然未复现。</li></ol><p>如果改变某个后台事件的周期后，异常周期也跟着变化，这是非常强的证据。如果关闭该事件后异常消失，再恢复该事件后异常重新出现，基本可以确认方向。</p><h2 id="最终修复应保护实时链路"><a href="#最终修复应保护实时链路" class="headerlink" title="最终修复应保护实时链路"></a>最终修复应保护实时链路</h2><p>找到根因后，修复思路通常不是简单提高某个任务优先级，也不是把异常点静音或丢掉，而是要保护实时链路的确定性。</p><p>常见修复方向包括：后台任务分片执行，Flash 写入避开实时链路低水位，日志改成异步和可丢弃，长临界区拆短，共享锁改为无阻塞队列或双缓冲，实时路径不访问慢速存储，周期任务增加预算限制，时钟切换增加投票机制，低功耗切换避开播放或控制窗口，计数器使用更安全的宽度和回绕比较方式。</p><p>对于输出侧，还可以增加防护策略。例如音频欠载时做短淡出和静音填充，视频丢帧时做重复帧或平滑过渡，控制输出突变时做限斜率。但这些只是兜底，不能替代根因修复。</p><h2 id="面试回答可以这样组织"><a href="#面试回答可以这样组织" class="headerlink" title="面试回答可以这样组织"></a>面试回答可以这样组织</h2><p>如果在面试中回答，可以按四层说。</p><p>第一层先定性：这是实时数据流系统的固定周期瞬态扰动，不是普通业务逻辑错误，也不是优先归因于随机硬件噪声。</p><p>第二层说怀疑对象：优先怀疑周期任务、计数边界、缓冲区回绕、后台 Flash&#x2F;日志写入、链路维护、时钟&#x2F;电源切换、长临界区和资源竞争。</p><p>第三层说测试系统：构建固定输入、自动输出检测、固件内部事件打点、RTOS Trace、GPIO Marker、环形日志和多信号同步采集。</p><p>第四层说收敛方法：把所有周期事件映射到统一时间轴，观察异常点是否长期与某个事件稳定对齐，再通过关闭、改周期、改资源访问方式等控制变量验证因果关系。</p><p>简洁表达可以是：</p><blockquote><p>我会把它看作实时数据流系统中的固定周期瞬态扰动。排查重点不是先猜某个输出模块坏了，而是找系统中是否存在同周期后台事件、计数回绕、资源竞争或时钟&#x2F;电源切换。测试上要建立统一时间轴，把输出异常、RTOS 调度、关键日志、GPIO Marker、总线&#x2F;时钟&#x2F;电源波形对齐。只要异常点长期与某个内部事件稳定对齐，再通过改变该事件周期或关闭该事件验证，就能把问题从偶现现象收敛到具体根因。</p></blockquote><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>固定周期瞬态异常的核心不是“短暂异常很难抓”，而是“周期性本身已经提供了定位线索”。只要系统没有崩溃、异常持续时间很短、发生间隔又相对稳定，就应优先从系统时序、后台任务、计数边界和资源竞争入手。</p><p>自动化测试的价值也不只是长时间运行，而是建立证据链：什么时间发生了输出异常，异常前后系统内部发生了什么，哪个后台事件与异常长期对齐，哪个资源构成了影响路径，改变该事件后异常是否随之变化。</p><p>一旦完成这条证据链，问题就不再是“偶现杂音”“偶现丢帧”或“偶现抖动”，而是一个可以复现、可以解释、可以修复的实时系统确定性问题。</p><div style="page-break-after: always;"></div>]]></content>
    
    
      
      
    <summary type="html">&lt;meta name=&quot;referrer&quot; content=&quot;no-referrer&quot; /&gt;


&lt;h1 id=&quot;嵌入式面试真题第-03-题：固定周期后台事件导致实时链路瞬态扰动的定位与自动化测试&quot;&gt;&lt;a href=&quot;#嵌入式面试真题第-03-题：固定周期后台事件导致实时链路瞬态</summary>
      
    
    
    
    <category term="嵌入式面试真题" scheme="https://wdfk-prog.space/categories/%E5%B5%8C%E5%85%A5%E5%BC%8F%E9%9D%A2%E8%AF%95%E7%9C%9F%E9%A2%98/"/>
    
    
    <category term="嵌入式面试真题" scheme="https://wdfk-prog.space/tags/%E5%B5%8C%E5%85%A5%E5%BC%8F%E9%9D%A2%E8%AF%95%E7%9C%9F%E9%A2%98/"/>
    
  </entry>
  
  <entry>
    <title>多源信号时间戳偏差的补偿与重同步架构</title>
    <link href="https://wdfk-prog.space/posts/c31de07a/"/>
    <id>https://wdfk-prog.space/posts/c31de07a/</id>
    <published>2026-07-06T00:21:13.000Z</published>
    <updated>2026-07-06T00:43:53.085Z</updated>
    
    <content type="html"><![CDATA[<meta name="referrer" content="no-referrer" /><h1 id="嵌入式面试真题第-02-题：多源信号时间戳偏差的补偿与重同步架构"><a href="#嵌入式面试真题第-02-题：多源信号时间戳偏差的补偿与重同步架构" class="headerlink" title="嵌入式面试真题第 02 题：多源信号时间戳偏差的补偿与重同步架构"></a>嵌入式面试真题第 02 题：多源信号时间戳偏差的补偿与重同步架构</h1><p><img src="https://i-blog.csdnimg.cn/direct/f9d6db2ef5734764b2a3898d81b1f1b7.png" alt="在这里插入图片描述"></p><h2 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h2><p>在一个通用的分布式系统或嵌入式系统中，多个独立信号源会通过有线或无线链路把数据发送到同一个接收端或业务终端，例如手机、网关、主控 MCU、边缘计算节点、上位机、机器人主机、工业控制器或车载域控制器。</p><p>这些信号源可以是多个穿戴设备、传感器节点、摄像头、IMU、定位模块、工业采集节点、车载节点、控制器、音频&#x2F;振动&#x2F;压力&#x2F;生理信号采集器，或其他需要按照时间关系组合处理的数据源。链路也不限定于无线，可以是 BLE、Wi-Fi、UWB、私有 2.4G、以太网、CAN、RS485、UART、SPI、I2C、USB、PCIe 或多级网关转发链路。</p><p>由于射频干扰、链路重传、调度抖动、时钟漂移、包乱序、短时丢包、链路拥塞、DMA&#x2F;中断延迟、网关排队、低功耗唤醒延迟或多任务抢占，同一时间段产生的数据到达接收端时可能出现明显时间偏差。偏差可能达到几十毫秒级，导致业务层出现融合错误、左右偏移、梳状滤波、控制滞后、误触发、短暂卡顿、波形失真、状态误判或时序相关算法失效。</p><p>如果让你设计一套通用的软件补偿策略和重同步状态机，你会如何建立时间基准、估计 offset&#x2F;drift、处理乱序和丢包，并在偏差明显时恢复业务一致性？哪些开源组件或成熟系统的设计思想可以参考？</p><h2 id="回答"><a href="#回答" class="headerlink" title="回答"></a>回答</h2><p>结论：这类问题本质上不是“某一路包晚到了怎么办”，而是“多源事件流如何在接收端建立统一事件时间，并在有限延迟预算内可靠交付给业务”。完整方案应分为三层：底层做时钟测量和漂移估计，中间层做乱序重排、水位线和迟到包处理，上层按业务 deadline 决定等待、插补、丢弃、降级或硬重同步。</p><p>NTP&#x2F;ntpd&#x2F;chrony 的参考价值在于时钟纪律，也就是如何测 offset、delay、jitter、drift，并决定小偏差慢慢 slew、大偏差在受控条件下 step。PTP 和 Linux 硬件时间戳的参考价值在于把时间戳尽量打在 MAC、PHY、控制器、DMA 或捕获寄存器边界，减少应用线程调度抖动。Apache Flink 的 Event Time &#x2F; Watermark 可以参考“什么时候认为更早事件不会再来”。GStreamer <code>rtpjitterbuffer</code> 可以参考乱序包、迟到包、丢包和重传的处理。ROS <code>message_filters</code> 可以参考多源 exact sync 与 approximate sync 的交付模型。</p><p>关键原则是：业务不能按“包到达时间”直接处理多源数据，而要按“事件发生时间”或“目标生效时间”处理。每个数据包至少要有源 ID、单调序号、源端事件时间、采样计数或事件计数。接收端维护每个源到参考时间轴的映射模型，再通过 reorder buffer、watermark、alignment window 和状态机决定何时交付、等待、丢弃、插补或重同步。</p><h2 id="总体架构"><a href="#总体架构" class="headerlink" title="总体架构"></a>总体架构</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    S1[Source 1\n本地时钟/序号/事件时间] --&gt; L1[链路适配\nBLE/Wi-Fi/UWB/CAN/UART/SPI/Ethernet]</span><br><span class="line">    S2[Source 2\n本地时钟/序号/事件时间] --&gt; L2[链路适配]</span><br><span class="line">    SN[Source N\n本地时钟/序号/事件时间] --&gt; LN[链路适配]</span><br><span class="line"></span><br><span class="line">    L1 --&gt; TS[Ingress Timestamp\n硬件/驱动/ISR/应用时间戳]</span><br><span class="line">    L2 --&gt; TS</span><br><span class="line">    LN --&gt; TS</span><br><span class="line"></span><br><span class="line">    TS --&gt; V[Packet Validator\nCRC/序号/源ID/长度]</span><br><span class="line">    V --&gt; RB[Per-source Reorder Buffer\n去重/乱序/缺口/迟到]</span><br><span class="line">    RB --&gt; CE[Clock Estimator\noffset/drift/jitter/skew]</span><br><span class="line">    CE --&gt; TM[Time Mapper\n源时间 -&gt; 统一参考时间]</span><br><span class="line">    TM --&gt; WC[Watermark &amp; Deadline Controller\n窗口关闭/等待边界/迟到策略]</span><br><span class="line">    WC --&gt; SYNC[Multi-source Synchronizer\nexact / approximate / degraded]</span><br><span class="line">    SYNC --&gt; APP[Application\n融合/控制/渲染/检测/状态机]</span><br><span class="line"></span><br><span class="line">    CE --&gt; QM[Quality Monitor\nRSSI/重传/RTT/队列/丢包/时钟质量]</span><br><span class="line">    QM --&gt; SM[Resync State Machine]</span><br><span class="line">    SM --&gt; RB</span><br><span class="line">    SM --&gt; WC</span><br><span class="line">    SM --&gt; APP</span><br></pre></td></tr></table></figure><p>这个架构里，链路层只负责把数据送到接收端；时间层负责把不同源的本地时间映射到统一时间轴；流处理层负责在有限延迟内处理乱序、迟到和缺失；业务层只消费“已经对齐、已降级或已明确标记不完整”的数据。</p><h2 id="机制与开源实现的对应关系"><a href="#机制与开源实现的对应关系" class="headerlink" title="机制与开源实现的对应关系"></a>机制与开源实现的对应关系</h2><table><thead><tr><th>本题机制</th><th>Linux 或开源机制</th><th>能否直接使用</th><th>主要参考价值</th></tr></thead><tbody><tr><td>offset &#x2F; delay &#x2F; jitter 估计</td><td>NTPv4 &#x2F; RFC 5905、ntpd 思路</td><td>通用协议栈不能直接照搬到 MCU 业务流，但算法思想可迁移</td><td>四时间戳交换、offset&#x2F;delay&#x2F;jitter&#x2F;dispersion、样本滤波、异常样本剔除。</td></tr><tr><td>慢速校正与大偏差跳变</td><td>chrony 的 slew &#x2F; step、makestep、maxslewrate 思路</td><td>Linux 可直接使用 chrony；嵌入式通常参考控制策略</td><td>小误差通过调快&#x2F;调慢时钟消除，大误差只在受控阶段 step，避免运行中时间突变。</td></tr><tr><td>硬件时间戳和 PTP 时钟</td><td>Linux SO_TIMESTAMPING、PTP Hardware Clock、IEEE 1588&#x2F;PTP</td><td>Linux 以太网&#x2F;工业网场景可直接用；MCU 映射为 timer capture 或 MAC&#x2F;PHY timestamp</td><td>时间戳越靠近物理收发边界，越能排除线程调度、队列和中断延迟。</td></tr><tr><td>乱序重排和迟到包处理</td><td>GStreamer <code>rtpjitterbuffer</code></td><td>RTP&#x2F;媒体流可直接用；其他协议可参考机制</td><td>用序号和流时间戳重排、去重、等待缺失包，超时后触发 lost&#x2F;retransmission&#x2F;degrade。</td></tr><tr><td>事件时间与水位线</td><td>Apache Flink Event Time &#x2F; Watermark</td><td>大数据流处理可直接用；嵌入式可参考概念</td><td>用 watermark 表示“早于某时间的事件基本不会再来”，决定窗口何时关闭。</td></tr><tr><td>多源同步交付</td><td>ROS <code>message_filters</code> 的 <code>TimeSynchronizer</code>、<code>ApproximateTimeSynchronizer</code></td><td>机器人&#x2F;传感器融合可直接参考；小系统一般自研</td><td>exact sync 与 approximate sync 的区别，适合多源融合窗口设计。</td></tr></tbody></table><p>这些组件分别解决问题的不同切面。NTP&#x2F;chrony 处理“钟怎么调”；PTP&#x2F;硬件时间戳处理“时间戳在哪里打才准”；GStreamer 处理“乱序包和迟到包怎么排”；Flink 处理“什么时候可以关闭事件时间窗口”；ROS message_filters 处理“多个输入流怎样组成一组可交付样本”。通用嵌入式方案应把它们组合成一套分层机制，而不是只引用其中一个。</p><h2 id="先定义时间语义"><a href="#先定义时间语义" class="headerlink" title="先定义时间语义"></a>先定义时间语义</h2><p>多源系统里至少要区分六种时间。</p><table><thead><tr><th>时间</th><th>含义</th><th>是否适合作为业务对齐依据</th></tr></thead><tbody><tr><td><code>event_ts</code></td><td>事件真实发生或采样发生的源端时间</td><td>最适合，但前提是源端时钟可映射到参考时间。</td></tr><tr><td><code>sample_count</code></td><td>源端采样计数、帧计数或事件计数</td><td>很适合，可用于连续信号对齐和 drift 估计。</td></tr><tr><td><code>tx_ts</code></td><td>源端发送时间</td><td>可辅助估计排队和链路延迟，但不等于事件发生时间。</td></tr><tr><td><code>rx_hw_ts</code></td><td>接收端硬件或驱动层收到包的时间</td><td>适合做链路测量和时间映射，优于应用层时间戳。</td></tr><tr><td><code>rx_app_ts</code></td><td>应用线程读到包的时间</td><td>只能作为诊断和调度延迟指标，不应作为精确事件时间。</td></tr><tr><td><code>deadline_ts</code></td><td>业务最晚可接受交付时间</td><td>用于决定等待、降级、丢弃或硬重同步。</td></tr></tbody></table><p>如果源端能提供稳定的 <code>event_ts</code> 或 <code>sample_count</code>，优先用它作为事件时间。若源端只能提供序号，接收端需要用首包接收时间、固定采样周期和序号外推事件时间。若没有序号，也没有事件时间，系统只能按到达时间近似处理，基本无法可靠修复乱序、丢包和漂移。</p><h2 id="数据包字段设计"><a href="#数据包字段设计" class="headerlink" title="数据包字段设计"></a>数据包字段设计</h2><p>建议每个源上报的数据包至少包含：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">source_id          源 ID，用于区分多个信号源</span><br><span class="line">stream_id          同一源内的流 ID，例如 sensor/audio/control/event</span><br><span class="line">seq                单调递增序号，用于去重、乱序、丢包检测</span><br><span class="line">sample_count       采样计数或事件计数，用于连续信号 drift 估计</span><br><span class="line">event_ts           源端事件时间戳，来自源端单调时钟</span><br><span class="line">tx_ts              可选，源端发送时间戳</span><br><span class="line">payload_duration   数据覆盖的时间长度，例如一帧 10ms 或一个采样窗口</span><br><span class="line">quality            可选，源端质量，例如采样溢出、重传、低电压、温度、RSSI</span><br><span class="line">flags              discontinuity/keyframe/retransmit/interpolated 等标志</span><br><span class="line">crc/mac            完整性校验或认证字段</span><br></pre></td></tr></table></figure><p>这些字段的目标是让接收端能回答四个问题：这个包来自谁、在该源内排第几、对应哪个源端时间段、是否还能和其他源的数据形成有效业务窗口。</p><h2 id="用四时间戳交换估计-offset-和-delay"><a href="#用四时间戳交换估计-offset-和-delay" class="headerlink" title="用四时间戳交换估计 offset 和 delay"></a>用四时间戳交换估计 offset 和 delay</h2><p>如果系统支持接收端与源端双向通信，可以参考 NTP 的四时间戳交换。它不是只能用于互联网 NTP，也可以抽象成任意两个节点之间的“短同步握手”。</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">T1 = A 端发出同步请求时，A 的本地时间</span><br><span class="line">T2 = B 端收到同步请求时，B 的本地时间</span><br><span class="line">T3 = B 端发出同步响应时，B 的本地时间</span><br><span class="line">T4 = A 端收到同步响应时，A 的本地时间</span><br></pre></td></tr></table></figure><p>按 NTPv4 的公式，B 相对 A 的 offset 和往返 delay 可以估计为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">offset = 1/2 * [(T2 - T1) + (T3 - T4)]</span><br><span class="line">delay  = (T4 - T1) - (T3 - T2)</span><br></pre></td></tr></table></figure><p>直观理解如下。</p><ol><li><code>T2 - T1</code> 包含 A 到 B 的链路延迟和 B 相对 A 的时钟偏移。</li><li><code>T3 - T4</code> 包含 B 到 A 的链路延迟的负向项和同一个时钟偏移。</li><li>两者相加后，如果往返链路延迟近似对称，链路延迟会相互抵消，剩下的就是 offset 的两倍。</li><li><code>delay</code> 则用整个往返时间减去 B 端处理请求到发送响应的停留时间，得到链路往返延迟。</li></ol><p>这个模型可以迁移到嵌入式系统中，例如：手机向两个传感器节点发同步请求，节点在协议栈尽量靠近收发边界的位置填 <code>T2/T3</code>，手机记录 <code>T1/T4</code>，再估计每个节点相对手机参考时钟的 offset 和 delay。</p><p>但四时间戳交换有几个工程限制。</p><table><thead><tr><th>限制</th><th>影响</th><th>工程处理</th></tr></thead><tbody><tr><td>假设往返路径延迟近似对称</td><td>BLE 连接事件、无线重传、网关排队可能让上行和下行不对称</td><td>多次采样，选低 delay 样本，拒绝异常 RTT。</td></tr><tr><td>时间戳位置不一致</td><td>应用层打时间戳会混入线程调度和队列延迟</td><td>尽量使用硬件、驱动、ISR 或协议栈边界时间戳。</td></tr><tr><td>源端本地时钟会漂移</td><td>单次 offset 只能校正相位，不能长期保证频率一致</td><td>连续估计 drift，用 PLL&#x2F;FLL&#x2F;线性回归修正。</td></tr><tr><td>单向广播流无法直接四时间戳</td><td>只有源到端的数据，没有反向同步包</td><td>加控制通道；或用接收时间 + 采样计数做单向漂移估计。</td></tr><tr><td>大偏差不能随意 step</td><td>业务运行中时间轴突变会引发状态机错误</td><td>小偏差 slew，大偏差进入重同步窗口后 step。</td></tr></tbody></table><p>因此，四时间戳交换适合建立时间映射模型，但不能单独解决多源数据交付。它还需要 reorder buffer、watermark、deadline 和业务降级策略配合。</p><h2 id="硬件时间戳和-PTP-时钟"><a href="#硬件时间戳和-PTP-时钟" class="headerlink" title="硬件时间戳和 PTP 时钟"></a>硬件时间戳和 PTP 时钟</h2><p>如果系统对同步精度要求高，时间戳应尽量靠近物理收发边界。Linux 的 <code>SO_TIMESTAMPING</code> 和 PTP Hardware Clock 提供的思路是：不要等应用线程被调度后再读系统时间，而是在网卡、MAC、PHY、驱动或内核收发路径上记录时间。Linux PTP 硬件时钟基础设施支持设置&#x2F;读取时钟、原子偏移调整、频率调整、外部事件时间戳、周期输出和 PPS 同步能力。</p><p>迁移到 MCU 或小型 RTOS 时，可以按下面的优先级选择时间戳位置。</p><table><thead><tr><th>时间戳位置</th><th>精度和可信度</th><th>说明</th></tr></thead><tbody><tr><td>PHY&#x2F;MAC&#x2F;无线控制器硬件时间戳</td><td>最高</td><td>最接近空口或线缆收发边界，适合 PTP、UWB、工业以太网。</td></tr><tr><td>DMA 完成或外设 FIFO 边界</td><td>高</td><td>能排除大部分应用线程调度抖动。</td></tr><tr><td>ISR 入口读取 free-running timer</td><td>中</td><td>实现简单，但包含中断响应延迟。</td></tr><tr><td>协议栈任务收到消息时读 tick</td><td>低</td><td>混入队列、锁、任务调度和优先级影响。</td></tr><tr><td>应用回调里读系统时间</td><td>最低</td><td>只能用于日志和粗略诊断，不适合精确同步。</td></tr></tbody></table><p>硬件时间戳并不等于 PTP 本身。PTP 是协议和时钟同步体系，硬件时间戳是提升同步精度的基础设施。在通用多源系统中，即使不实现完整 PTP，也应尽量在每个链路适配层提供“可信接收时间戳”。</p><h2 id="建立源时间到参考时间的映射"><a href="#建立源时间到参考时间的映射" class="headerlink" title="建立源时间到参考时间的映射"></a>建立源时间到参考时间的映射</h2><p>对每个源维护一个从源端时间到接收端参考时间的线性模型：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">T_ref = a_i * T_src + b_i</span><br></pre></td></tr></table></figure><p>其中：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">T_src = 源 i 的本地事件时间，或 sample_count 换算出的源时间</span><br><span class="line">T_ref = 接收端统一参考时间轴</span><br><span class="line">b_i   = offset，相位差或固定偏移</span><br><span class="line">a_i   = drift ratio，频率比例，理想情况下接近 1.0</span><br></pre></td></tr></table></figure><p>接收端不应每收到一个包就立刻大幅修改 <code>a_i/b_i</code>。合理做法是滑动窗口估计：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">sample_i = (T_src_i, rx_hw_ts_i, seq_i, quality_i)</span><br><span class="line">filter samples with CRC error, duplicate, retransmit-late, abnormal RTT</span><br><span class="line">estimate b_i from low-delay samples</span><br><span class="line">estimate a_i from offset slope over time</span><br><span class="line">compute jitter_i = RMS(offset_i - smoothed_offset_i)</span><br><span class="line">compute confidence_i from jitter/loss/retransmit/watermark lag</span><br></pre></td></tr></table></figure><p>工程上可用从简单到复杂的估计器。</p><table><thead><tr><th>估计器</th><th>适用场景</th><th>说明</th></tr></thead><tbody><tr><td>EMA 指数平均</td><td>资源很小、偏差变化慢</td><td>实现简单，但对异常样本敏感。</td></tr><tr><td>低 RTT 样本筛选 + EMA</td><td>无线链路、重传明显</td><td>参考 NTP clock filter，优先相信低 delay 样本。</td></tr><tr><td>线性回归</td><td>有连续 sample_count 或事件时间</td><td>可估计 drift，适合传感器和连续采样流。</td></tr><tr><td>软件 PLL&#x2F;FLL</td><td>需要平滑跟踪本地时钟</td><td>控制输出连续，适合长期运行。</td></tr><tr><td>Kalman &#x2F; 鲁棒估计</td><td>噪声模型复杂、多传感器融合</td><td>可同时估计 offset、drift 和不确定度，但资源开销更高。</td></tr></tbody></table><h2 id="用-GStreamer-rtpjitterbuffer-理解乱序和迟到包"><a href="#用-GStreamer-rtpjitterbuffer-理解乱序和迟到包" class="headerlink" title="用 GStreamer rtpjitterbuffer 理解乱序和迟到包"></a>用 GStreamer <code>rtpjitterbuffer</code> 理解乱序和迟到包</h2><p>GStreamer 的 <code>rtpjitterbuffer</code> 是一个很好的流式参考模型。它会对 RTP 包进行重排和去重，基于 payload clock-rate 估计延迟，用 <code>latency</code> 属性限制包最多在 buffer 中等待多久；太晚到的包会被认为丢失；启用 retransmission 时，它还能根据包序和包间隔估计某个包应该何时到达，并提前发起重传请求。</p><p>在通用多源系统里，可把它抽象成 per-source reorder buffer：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[收到包 seq/event_ts] --&gt; B&#123;CRC/源ID/长度有效?&#125;</span><br><span class="line">    B --&gt;|否| X[丢弃并计数]</span><br><span class="line">    B --&gt;|是| C&#123;重复包?&#125;</span><br><span class="line">    C --&gt;|是| D[丢弃 duplicate]</span><br><span class="line">    C --&gt;|否| E[插入按 seq/event_ts 排序的 buffer]</span><br><span class="line">    E --&gt; F&#123;缺口是否超过等待时间?&#125;</span><br><span class="line">    F --&gt;|否| G[继续等待]</span><br><span class="line">    F --&gt;|是| H[标记 missing/lost]</span><br><span class="line">    H --&gt; I&#123;是否允许重传?&#125;</span><br><span class="line">    I --&gt;|是| J[请求重传]</span><br><span class="line">    I --&gt;|否| K[插补/降级/关闭窗口]</span><br><span class="line">    G --&gt; L[按 watermark/deadline 交付]</span><br><span class="line">    K --&gt; L</span><br></pre></td></tr></table></figure><p>关键参数包括：</p><table><thead><tr><th>参数</th><th>含义</th><th>设计建议</th></tr></thead><tbody><tr><td><code>reorder_depth</code></td><td>允许乱序的包深度</td><td>根据链路乱序统计配置，不能无限大。</td></tr><tr><td><code>late_timeout</code></td><td>等待缺失包的最长时间</td><td>必须小于业务 deadline。</td></tr><tr><td><code>max_dropout</code></td><td>认为源发生连续丢包或断流的阈值</td><td>超过后进入降级或重同步。</td></tr><tr><td><code>max_misorder</code></td><td>认为乱序异常的阈值</td><td>超过后提高 holdback 或触发链路质量告警。</td></tr><tr><td><code>drop_on_latency</code></td><td>buffer 满时是否丢最旧数据</td><td>低延迟业务通常要丢旧包，离线业务可继续等待。</td></tr><tr><td><code>retransmit_policy</code></td><td>是否请求重传以及重传次数</td><td>实时链路要谨慎，重传可能已超过业务时限。</td></tr></tbody></table><p>注意，jitter buffer 解决的是“单源流在接收端恢复顺序和节奏”，不负责多源之间的同步交付。多源同步还需要 alignment window 和 watermark。</p><h2 id="用-Flink-Event-Time-Watermark-理解窗口关闭"><a href="#用-Flink-Event-Time-Watermark-理解窗口关闭" class="headerlink" title="用 Flink Event Time &#x2F; Watermark 理解窗口关闭"></a>用 Flink Event Time &#x2F; Watermark 理解窗口关闭</h2><p>Apache Flink 把时间分成 processing time 和 event time。processing time 是处理节点的系统时间，性能好但受数据到达速度、链路和调度影响；event time 是事件真实发生时间，通常嵌在记录里。Flink 用 watermark 表示事件时间的推进：<code>Watermark(t)</code> 表示该流中时间戳小于等于 <code>t</code> 的事件理论上不应再到达。多个输入流的 operator 通常以输入 watermark 的最小值作为当前可推进的事件时间。</p><p>这套思想可以直接迁移到嵌入式多源同步：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">source_watermark_i = max_event_time_seen_i - allowed_lateness_i</span><br><span class="line">global_watermark   = min(source_watermark_i for required sources)</span><br></pre></td></tr></table></figure><p>当某个业务窗口的结束时间 <code>window_end &lt;= global_watermark</code>，接收端就可以关闭窗口并交付给业务。若某个源迟迟不推进 watermark，说明它可能丢包、断流、时钟异常或链路拥塞；系统不能无限等待，必须按 deadline 执行降级策略。</p><p>不同业务的 watermark 策略不同。</p><table><thead><tr><th>业务类型</th><th>Watermark &#x2F; Deadline 策略</th></tr></thead><tbody><tr><td>闭环控制</td><td>deadline 优先；迟到数据不回放，通常丢弃或用于诊断。</td></tr><tr><td>传感器融合</td><td>approximate sync；在有限窗口内等齐，缺源则降低置信度。</td></tr><tr><td>波形&#x2F;媒体渲染</td><td>holdback + jitter buffer；小抖动平滑，大偏差重同步。</td></tr><tr><td>告警检测</td><td>前台结果按 deadline 输出；迟到数据可修正历史统计但不能无限拖延。</td></tr><tr><td>日志&#x2F;离线分析</td><td>完整性优先；可等待更久并按 event time 重排。</td></tr></tbody></table><p>Watermark 的价值在于把“等多久”从隐式经验变成显式策略。它能防止系统因为某一路慢源永久卡住，也能让业务明确知道某个输出窗口是否完整。</p><h2 id="用-ROS-message-filters-理解-exact-sync-和-approximate-sync"><a href="#用-ROS-message-filters-理解-exact-sync-和-approximate-sync" class="headerlink" title="用 ROS message_filters 理解 exact sync 和 approximate sync"></a>用 ROS <code>message_filters</code> 理解 exact sync 和 approximate sync</h2><p>ROS 的 <code>message_filters</code> 适合参考多源同步交付。<code>TimeSynchronizer</code> 要求多个 topic 的消息时间戳严格相同，才触发同步回调；<code>ApproximateTimeSynchronizer</code> 允许多个 topic 的时间戳存在一个小的 <code>max_delay</code>，在容差范围内组成一组样本交付给回调。</p><p>这对应嵌入式系统里的两种模式。</p><h3 id="Exact-sync"><a href="#Exact-sync" class="headerlink" title="Exact sync"></a>Exact sync</h3><p>Exact sync 适合源端本来就有共同触发、共同采样时钟或硬同步脉冲的系统，例如多相 ADC 同步采样、同步曝光相机、同一 FPGA 采样出的多路信号。</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">if ts_a == ts_b == ts_c:</span><br><span class="line">    deliver(a, b, c)</span><br><span class="line">else:</span><br><span class="line">    wait until timeout or mark missing</span><br></pre></td></tr></table></figure><p>优点是语义清晰，融合结果可信；缺点是对时钟和时间戳一致性要求高。只要某个源存在轻微漂移、量化误差或传输乱序，就可能长时间无法触发同步回调。</p><h3 id="Approximate-sync"><a href="#Approximate-sync" class="headerlink" title="Approximate sync"></a>Approximate sync</h3><p>Approximate sync 适合多节点独立采样、无线链路、传感器融合、机器人感知等场景。它允许多个源在一个容差窗口内匹配。</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">sync_tolerance = 5ms</span><br><span class="line">find messages from required sources where max(mapped_ts) - min(mapped_ts) &lt;= sync_tolerance</span><br><span class="line">if found before deadline:</span><br><span class="line">    deliver group with confidence</span><br><span class="line">else:</span><br><span class="line">    deliver degraded group or drop window</span><br></pre></td></tr></table></figure><p>核心参数包括：</p><table><thead><tr><th>参数</th><th>含义</th><th>影响</th></tr></thead><tbody><tr><td><code>queue_size</code></td><td>每个源保留多少候选消息</td><td>太小会错过配对，太大会增加延迟和内存。</td></tr><tr><td><code>sync_tolerance</code></td><td>多源时间戳最大允许偏差</td><td>太小导致配不上，太大导致融合误差。</td></tr><tr><td><code>required_sources</code></td><td>必须参与同步的源集合</td><td>控制完整性与可用性的权衡。</td></tr><tr><td><code>deadline</code></td><td>最晚交付时间</td><td>到点还不完整就降级或丢弃。</td></tr><tr><td><code>confidence</code></td><td>输出置信度</td><td>由偏差、缺源、插补、链路质量共同决定。</td></tr></tbody></table><p>这部分是很多嵌入式回答容易漏掉的点：NTP&#x2F;chrony 能让时钟更接近，但业务最终要消费的是“一组按业务时间对齐的数据”。这组数据怎么凑齐、凑不齐怎么办，属于 message_filters &#x2F; watermark &#x2F; deadline 层的问题。</p><h2 id="用-chrony-的-slew-step-思路设计补偿边界"><a href="#用-chrony-的-slew-step-思路设计补偿边界" class="headerlink" title="用 chrony 的 slew &#x2F; step 思路设计补偿边界"></a>用 chrony 的 slew &#x2F; step 思路设计补偿边界</h2><p>chrony 的思路可以抽象成两种校正方式：</p><table><thead><tr><th>方式</th><th>含义</th><th>适用场景</th><th>风险</th></tr></thead><tbody><tr><td>slew</td><td>不直接跳时间，而是让本地时钟略快或略慢，逐渐消除偏差</td><td>小偏差、正常运行期、业务不能接受时间突变</td><td>收敛慢，大偏差时业务会长时间处在错位状态。</td></tr><tr><td>step</td><td>直接把时间跳到新位置</td><td>启动阶段、尚未对外输出、严重偏差后的受控重同步</td><td>运行中随意 step 会让业务时间倒退或跳变。</td></tr></tbody></table><p>迁移到多源业务流时，不一定真的去调系统 RTC，而是调“源时间到参考时间的映射模型”和“业务输出节奏”。</p><p>小偏差时：</p><ol><li>微调 <code>a_i/b_i</code> 映射参数。</li><li>调整 holdback buffer 目标水位。</li><li>对连续信号做轻微重采样、插值或采样点微调。</li><li>对离散事件扩大很小的 matching tolerance。</li><li>对控制输出做预测&#x2F;外推，但标记置信度。</li></ol><p>大偏差时：</p><ol><li>不再慢慢追，因为业务会持续错位。</li><li>冻结受影响源或业务窗口。</li><li>丢弃过旧数据，选择新的 event time anchor。</li><li>将该源映射模型 step 到新位置。</li><li>进入 Recover 阶段，观察 offset、drift、jitter 和 watermark 是否恢复稳定。</li></ol><p>一个通用阈值表可以这样设计，实际数值要按业务重配：</p><table><thead><tr><th>偏差等级</th><th>例子</th><th>策略</th></tr></thead><tbody><tr><td><code>abs(offset) &lt;= soft_threshold</code></td><td>微秒到少量毫秒</td><td>slew&#x2F;PLL&#x2F;插值&#x2F;小窗口容差。</td></tr><tr><td><code>soft_threshold &lt; abs(offset) &lt;= fast_threshold</code></td><td>数毫秒到十几毫秒</td><td>快速收敛，临时扩大 holdback，限制非关键流量。</td></tr><tr><td><code>fast_threshold &lt; abs(offset) &lt;= hard_threshold</code></td><td>十几到几十毫秒</td><td>进入 Degraded&#x2F;HardResync，冻结或插补业务输出。</td></tr><tr><td><code>abs(offset) &gt; hard_threshold</code></td><td>明显跨窗口或跨帧</td><td>丢弃旧窗口，重新锚定时间，必要时重建链路。</td></tr></tbody></table><p>这里的阈值不能固定套用。闭环控制可能 2ms 就严重；离线日志分析几百毫秒也可接受；音频、振动和波形类业务对相位更敏感；温度、湿度这类慢变量可容忍更大偏差。</p><h2 id="多源-Buffer-设计"><a href="#多源-Buffer-设计" class="headerlink" title="多源 Buffer 设计"></a>多源 Buffer 设计</h2><p>通用实现不应只靠一个 FIFO。建议每个源维护自己的 reorder buffer，再由全局 synchronizer 组合交付。</p><table><thead><tr><th>Buffer</th><th>作用</th><th>关键参数</th></tr></thead><tbody><tr><td>Per-source Reorder Buffer</td><td>单源内按 <code>seq/event_ts</code> 重排、去重、等待缺失包</td><td><code>reorder_depth</code>、<code>late_timeout</code>、<code>max_gap</code>。</td></tr><tr><td>Alignment Window</td><td>多源之间按统一时间轴找同一业务窗口的数据</td><td><code>window_size</code>、<code>sync_tolerance</code>、<code>required_sources</code>。</td></tr><tr><td>Holdback Buffer</td><td>故意延迟一点交付，吸收乱序和链路抖动</td><td><code>holdback_ms</code>、<code>max_latency_budget</code>。</td></tr><tr><td>Degraded Buffer</td><td>缺源、迟到、丢包时保存上一次有效值或插补数据</td><td><code>hold_last_ms</code>、<code>interpolate_limit</code>。</td></tr><tr><td>Diagnostic Ring</td><td>保存异常前后的时间戳、序号、链路质量</td><td><code>dump_window</code>、<code>error_counter</code>。</td></tr></tbody></table><p>容量不能按包数拍脑袋，应按业务最大等待时间计算：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">B_source &gt;= R_source * (T_link_jitter_tail + T_sched_jitter + T_reorder_wait + T_margin)</span><br></pre></td></tr></table></figure><p>其中：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">R_source           = 该源最大数据速率</span><br><span class="line">T_link_jitter_tail = 链路长尾抖动，例如重传、拥塞、调度延迟</span><br><span class="line">T_sched_jitter     = 接收端任务调度、DMA、中断、锁竞争抖动</span><br><span class="line">T_reorder_wait     = 为乱序包保留的等待时间</span><br><span class="line">T_margin           = 安全余量</span><br></pre></td></tr></table></figure><p>如果业务只允许 20ms 端到端延迟，而链路长尾重传本身可能 50ms，那么软件不能同时保证“等齐所有源”和“20ms 内输出”。此时必须明确选择：低延迟优先则丢弃&#x2F;插补迟到源；完整性优先则增加等待延迟。</p><h2 id="补偿策略"><a href="#补偿策略" class="headerlink" title="补偿策略"></a>补偿策略</h2><p>偏差处理分四层。</p><h3 id="小偏差：软补偿"><a href="#小偏差：软补偿" class="headerlink" title="小偏差：软补偿"></a>小偏差：软补偿</h3><p>小偏差通常来自晶振 ppm 误差、调度微抖动、短时链路排队或采样周期量化。处理方式是保持业务连续性：</p><ol><li>更新源时间到参考时间的映射参数。</li><li>使用软件 PLL&#x2F;FLL 慢慢消除 offset。</li><li>调整 holdback buffer 水位。</li><li>连续信号使用重采样、插值或相位微调。</li><li>离散事件使用 approximate matching tolerance。</li></ol><h3 id="中等偏差：快速收敛"><a href="#中等偏差：快速收敛" class="headerlink" title="中等偏差：快速收敛"></a>中等偏差：快速收敛</h3><p>中等偏差已经可能影响业务，但还未完全失控。处理方式是短时间提高同步强度：</p><ol><li>提高同步包频率或临时启用四时间戳交换。</li><li>临时扩大 alignment window，但不超过业务 deadline。</li><li>对迟到源降低融合权重。</li><li>限制非关键链路流量，减少拥塞和排队。</li><li>观察连续 N 个窗口是否恢复稳定。</li></ol><h3 id="大偏差：硬重同步"><a href="#大偏差：硬重同步" class="headerlink" title="大偏差：硬重同步"></a>大偏差：硬重同步</h3><p>几十毫秒级偏差通常不能长期慢追。处理方式是有边界地重建时间关系：</p><ol><li>标记受影响源为 <code>UNSTABLE</code>。</li><li>停止用该源参与关键融合或控制。</li><li>丢弃已过 deadline 的旧数据。</li><li>选择新的 anchor，例如最近一个完整同步包、关键帧、完整采样块或共同触发事件。</li><li>重新设置 <code>b_i</code> 或重新初始化该源的 mapping model。</li><li>恢复时先以 degraded 输出，再回到 normal。</li></ol><h3 id="链路异常：降级或隔离"><a href="#链路异常：降级或隔离" class="headerlink" title="链路异常：降级或隔离"></a>链路异常：降级或隔离</h3><p>如果问题来自持续丢包、长时间 watermark 不推进、时钟质量过低或源端重启，单纯补偿没有意义。此时应降级或隔离：</p><ol><li>该源暂时不参与关键业务。</li><li>使用上一次有效值、模型预测或空值标记。</li><li>上报低置信度。</li><li>重建连接或重启源端同步状态机。</li><li>记录异常前后诊断日志。</li></ol><h2 id="重同步状态机"><a href="#重同步状态机" class="headerlink" title="重同步状态机"></a>重同步状态机</h2><p>推荐状态机如下。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">stateDiagram-v2</span><br><span class="line">    [*] --&gt; Init</span><br><span class="line">    Init --&gt; Syncing: 建立初始 offset/drift</span><br><span class="line">    Syncing --&gt; Normal: 连续 M 个窗口稳定</span><br><span class="line">    Normal --&gt; Suspect: offset/jitter/loss/watermark 异常</span><br><span class="line">    Suspect --&gt; Normal: 异常未持续</span><br><span class="line">    Suspect --&gt; SoftResync: 小到中等偏差持续存在</span><br><span class="line">    SoftResync --&gt; Normal: slew/窗口调整后稳定</span><br><span class="line">    SoftResync --&gt; HardResync: 偏差超过硬阈值或 deadline 连续失败</span><br><span class="line">    Suspect --&gt; HardResync: 突发大偏差/源端重启/序号跳变</span><br><span class="line">    HardResync --&gt; Recover: 重新锚定时间并清理旧窗口</span><br><span class="line">    Recover --&gt; Normal: 连续 M 个窗口稳定</span><br><span class="line">    Recover --&gt; Degraded: 部分源仍不稳定</span><br><span class="line">    Degraded --&gt; Recover: 源重新可用</span><br><span class="line">    Degraded --&gt; Isolated: 源长期不可用</span><br><span class="line">    Isolated --&gt; Syncing: 链路重建或源端复位</span><br></pre></td></tr></table></figure><p>各状态职责如下。</p><table><thead><tr><th>状态</th><th>进入条件</th><th>动作</th><th>退出条件</th></tr></thead><tbody><tr><td><code>Init</code></td><td>启动或源首次加入</td><td>清空历史，等待基础时间戳和序号</td><td>收到足够样本。</td></tr><tr><td><code>Syncing</code></td><td>需要建立初始时间模型</td><td>四时间戳交换、低 delay 样本筛选、估计 offset&#x2F;drift</td><td>模型稳定。</td></tr><tr><td><code>Normal</code></td><td>offset、jitter、loss、watermark 正常</td><td>按业务窗口交付</td><td>异常超过阈值。</td></tr><tr><td><code>Suspect</code></td><td>单次或少量异常</td><td>继续收样本，避免误判</td><td>恢复正常或异常持续。</td></tr><tr><td><code>SoftResync</code></td><td>小&#x2F;中偏差持续存在</td><td>slew、扩大 holdback、提高同步频率</td><td>收敛或恶化。</td></tr><tr><td><code>HardResync</code></td><td>大偏差、序号跳变、源重启、deadline 连续失败</td><td>冻结&#x2F;降级业务，丢弃过期窗口，重新锚定</td><td>新 anchor 建立。</td></tr><tr><td><code>Recover</code></td><td>重同步后观察期</td><td>逐步恢复源权重，降低 buffer 冗余</td><td>连续稳定或失败。</td></tr><tr><td><code>Degraded</code></td><td>部分源不可用但业务可继续</td><td>插补、hold last、降低置信度</td><td>源恢复或长期不可用。</td></tr><tr><td><code>Isolated</code></td><td>源长期异常或质量过低</td><td>隔离该源，避免污染业务</td><td>链路重建。</td></tr></tbody></table><p><code>Suspect</code> 很重要。偶发重传、单包乱序或一次调度抖动不能立即触发硬重同步，否则系统会在边界条件下频繁抖动。</p><h2 id="业务交付策略"><a href="#业务交付策略" class="headerlink" title="业务交付策略"></a>业务交付策略</h2><p>统一时间轴建立后，业务交付仍然要按场景选择策略。</p><table><thead><tr><th>业务类型</th><th>优先级</th><th>推荐处理</th></tr></thead><tbody><tr><td>闭环控制</td><td>deadline &gt; 完整性</td><td>到点即输出；迟到数据丢弃或只用于诊断；不能回放旧控制。</td></tr><tr><td>多传感器融合</td><td>一致性 + deadline</td><td>approximate sync；缺源时降置信度；严重错位时暂停融合。</td></tr><tr><td>波形&#x2F;媒体渲染</td><td>连续性 + 相位</td><td>jitter buffer；小偏差平滑；大偏差淡化、跳转或重同步。</td></tr><tr><td>告警检测</td><td>及时性 + 可解释</td><td>先按当前窗口输出，迟到数据可修正历史统计，但要记录版本。</td></tr><tr><td>离线日志&#x2F;分析</td><td>完整性 &gt; 低延迟</td><td>可长时间等待和重排，不必实时降级。</td></tr></tbody></table><p>如果输出结果依赖多个源，建议每次交付都带 metadata：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">output_ts</span><br><span class="line">sources_used</span><br><span class="line">max_source_skew</span><br><span class="line">missing_sources</span><br><span class="line">interpolated_sources</span><br><span class="line">confidence</span><br><span class="line">resync_state</span><br><span class="line">watermark_lag</span><br></pre></td></tr></table></figure><p>这样业务层不会把“降级拼出来的数据”误当成完整同步数据。</p><h2 id="伪代码"><a href="#伪代码" class="headerlink" title="伪代码"></a>伪代码</h2><p>下面是一个简化的处理骨架。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">on_packet_rx</span><span class="params">(<span class="type">packet_t</span> *pkt, <span class="type">timestamp_t</span> rx_hw_ts)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">if</span> (!packet_validate(pkt)) &#123;</span><br><span class="line">        diag_count(DIAG_BAD_PACKET);</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="type">source_ctx_t</span> *src = get_source(pkt-&gt;source_id);</span><br><span class="line"></span><br><span class="line">    reorder_insert(&amp;src-&gt;reorder, pkt, rx_hw_ts);</span><br><span class="line">    source_update_link_quality(src, pkt, rx_hw_ts);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (reorder_has_ready_packet(&amp;src-&gt;reorder)) &#123;</span><br><span class="line">        <span class="type">packet_t</span> *p = reorder_pop_ready(&amp;src-&gt;reorder);</span><br><span class="line"></span><br><span class="line">        <span class="type">time_sample_t</span> s = build_time_sample(p, rx_hw_ts);</span><br><span class="line">        <span class="keyword">if</span> (time_sample_is_reliable(&amp;s)) &#123;</span><br><span class="line">            clock_estimator_update(&amp;src-&gt;clock, &amp;s);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="type">timestamp_t</span> ref_ts = time_mapper_src_to_ref(&amp;src-&gt;clock, p-&gt;event_ts, p-&gt;sample_count);</span><br><span class="line">        alignment_insert(&amp;global_align, p, ref_ts);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    watermark_update_source(src);</span><br><span class="line">    global_watermark_update(&amp;global_align);</span><br><span class="line">    resync_state_machine_update(src, &amp;global_align);</span><br><span class="line">    deliver_ready_windows(&amp;global_align);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>交付窗口可以按如下逻辑：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">deliver_ready_windows</span><span class="params">(<span class="type">alignment_t</span> *a)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">while</span> (window_end(a-&gt;head) &lt;= a-&gt;global_watermark) &#123;</span><br><span class="line">        <span class="type">sync_group_t</span> g;</span><br><span class="line">        <span class="type">sync_result_t</span> r = build_sync_group(a, &amp;g);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (r == SYNC_EXACT || r == SYNC_APPROX) &#123;</span><br><span class="line">            app_deliver(&amp;g);</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (deadline_expired(a-&gt;head)) &#123;</span><br><span class="line">            build_degraded_group(a, &amp;g);</span><br><span class="line">            app_deliver_degraded(&amp;g);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        alignment_release_window(a, a-&gt;head);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="观测与测试"><a href="#观测与测试" class="headerlink" title="观测与测试"></a>观测与测试</h2><p>没有观测就无法证明同步策略有效。建议至少记录以下指标。</p><table><thead><tr><th>指标</th><th>用途</th></tr></thead><tbody><tr><td>per-source offset</td><td>判断各源相对参考时间的偏移。</td></tr><tr><td>per-source drift ppm</td><td>判断晶振或调度长期漂移。</td></tr><tr><td>jitter &#x2F; RTT &#x2F; delay</td><td>判断链路抖动和同步样本质量。</td></tr><tr><td>seq gap &#x2F; duplicate &#x2F; late packet</td><td>判断乱序、丢包和重传。</td></tr><tr><td>reorder buffer fill level</td><td>判断 buffer 是否过小或 holdback 是否过大。</td></tr><tr><td>source watermark lag</td><td>判断某个源是否拖慢全局交付。</td></tr><tr><td>sync group skew</td><td>每次交付时多源最大时间差。</td></tr><tr><td>degraded output count</td><td>业务是否长期处于降级。</td></tr><tr><td>resync transition log</td><td>复盘何时从 Normal 进入 Soft&#x2F;HardResync。</td></tr></tbody></table><p>测试矩阵应覆盖：</p><ol><li>固定 offset 注入，例如 +5ms、+20ms、+50ms。</li><li>频率漂移注入，例如 ±20ppm、±100ppm。</li><li>乱序注入，例如相邻包交换、N 包后到。</li><li>短时丢包和 burst loss。</li><li>单源重启，序号回绕或时间戳跳变。</li><li>链路重传和 RTT 长尾。</li><li>接收端任务阻塞、DMA 延迟、中断屏蔽。</li><li>多源数量增加后的 watermark 拖尾。</li><li>exact sync 与 approximate sync 的边界条件。</li><li>hard resync 期间业务是否出现非法时间倒退或状态突变。</li></ol><h2 id="面试回答组织方式"><a href="#面试回答组织方式" class="headerlink" title="面试回答组织方式"></a>面试回答组织方式</h2><p>回答这类题时，可以按以下顺序展开：</p><ol><li>先给核心结论：不能按到达时间直接处理，必须建立统一事件时间和重同步状态机。</li><li>再把问题分层：时钟测量、数据流重排、多源同步交付、业务降级。</li><li>介绍四时间戳交换：用 <code>T1/T2/T3/T4</code> 估计 offset 和 delay，但说明对称链路假设和异常样本过滤。</li><li>说明硬件时间戳&#x2F;PTP：时间戳应尽量靠近物理收发边界，应用线程时间戳只能做诊断。</li><li>说明 jitter buffer：每个源先按 seq&#x2F;event_ts 去重、重排、等待、超时、丢弃或重传。</li><li>说明 watermark：多源窗口不能无限等待，按 event time 和 deadline 决定窗口关闭。</li><li>说明 exact&#x2F;approximate sync：严格同步适合硬同步采样，近似同步适合独立源和无线链路。</li><li>说明 slew&#x2F;step：小偏差软补偿，大偏差受控重同步，不能运行中随意跳时间。</li><li>最后落到状态机：<code>Init -&gt; Syncing -&gt; Normal -&gt; Suspect -&gt; SoftResync -&gt; HardResync -&gt; Recover -&gt; Normal/Degraded/Isolated</code>。</li></ol><p>一句话概括：NTP&#x2F;chrony 解决“时间怎么估和怎么调”，PTP&#x2F;硬件时间戳解决“时间戳在哪里打才可信”，GStreamer&#x2F;Flink&#x2F;ROS 解决“乱序、迟到和多源窗口怎么交付”。真正的工程方案要把这些机制组合起来，用统一事件时间、per-source reorder buffer、watermark、deadline 和重同步状态机保护业务。</p><h2 id="参考链接"><a href="#参考链接" class="headerlink" title="参考链接"></a>参考链接</h2><ul><li><a href="https://www.rfc-editor.org/rfc/rfc5905.html">RFC 5905: Network Time Protocol Version 4</a></li><li><a href="https://docs.kernel.org/networking/timestamping.html">Linux kernel timestamping documentation</a></li><li><a href="https://docs.kernel.org/driver-api/ptp.html">Linux PTP hardware clock infrastructure</a></li><li><a href="https://nightlies.apache.org/flink/flink-docs-stable/docs/concepts/time/">Apache Flink: Timely Stream Processing, Event Time, and Watermarks</a></li><li><a href="https://gstreamer.freedesktop.org/documentation/rtpmanager/rtpjitterbuffer.html">GStreamer rtpjitterbuffer</a></li><li><a href="https://chrony-project.org/doc/4.6/chrony.conf.html">chrony configuration documentation</a></li><li><a href="https://raw.githubusercontent.com/ros2/message_filters/rolling/doc/Tutorials/Writing-A-Time-Synchronizer-Python.rst">ROS 2 message_filters Time Synchronizer tutorial source</a></li><li><a href="https://raw.githubusercontent.com/ros2/message_filters/rolling/doc/Tutorials/Approximate-Synchronizer-Python.rst">ROS 2 message_filters Approximate Time Synchronizer tutorial source</a></li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;meta name=&quot;referrer&quot; content=&quot;no-referrer&quot; /&gt;


&lt;h1 id=&quot;嵌入式面试真题第-02-题：多源信号时间戳偏差的补偿与重同步架构&quot;&gt;&lt;a href=&quot;#嵌入式面试真题第-02-题：多源信号时间戳偏差的补偿与重同步架构&quot; class</summary>
      
    
    
    
    <category term="嵌入式面试真题" scheme="https://wdfk-prog.space/categories/%E5%B5%8C%E5%85%A5%E5%BC%8F%E9%9D%A2%E8%AF%95%E7%9C%9F%E9%A2%98/"/>
    
    
    <category term="嵌入式面试真题" scheme="https://wdfk-prog.space/tags/%E5%B5%8C%E5%85%A5%E5%BC%8F%E9%9D%A2%E8%AF%95%E7%9C%9F%E9%A2%98/"/>
    
  </entry>
  
  <entry>
    <title>低速 Flash 与 1MB SRAM 下的通用缓存架构设计</title>
    <link href="https://wdfk-prog.space/posts/a87313c6/"/>
    <id>https://wdfk-prog.space/posts/a87313c6/</id>
    <published>2026-07-04T08:55:22.000Z</published>
    <updated>2026-07-04T09:40:53.050Z</updated>
    
    <content type="html"><![CDATA[<meta name="referrer" content="no-referrer" /><h1 id="嵌入式面试真题第-01-题：低速-Flash-与-1MB-SRAM-下的通用缓存架构设计"><a href="#嵌入式面试真题第-01-题：低速-Flash-与-1MB-SRAM-下的通用缓存架构设计" class="headerlink" title="嵌入式面试真题第 01 题：低速 Flash 与 1MB SRAM 下的通用缓存架构设计"></a>嵌入式面试真题第 01 题：低速 Flash 与 1MB SRAM 下的通用缓存架构设计</h1><p><img src="https://i-blog.csdnimg.cn/direct/1f2cb251ed2347749ad2dbf07d286b14.png" alt="在这里插入图片描述"></p><h2 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h2><p>在一款资源受限的嵌入式设备中，系统只有 1MB SRAM 和一块读延迟不稳定的低速 SPI Flash。业务需要从 Flash 中持续或按需读取资源包、配置表、字库、语音片段、模型分块、日志回放数据或其他离线资源，并供应用线程实时或准实时消费。</p><p>如果应用线程直接等待 Flash 读取，系统可能因为低速 I&#x2F;O、随机访问、文件碎片、擦写干扰或后台任务抢占而出现卡顿、超时或数据供应不连续。你会如何设计一套通用 Buffer&#x2F;Cache 机制，使应用线程尽量读取 SRAM 中已经准备好的数据，而不是直接阻塞等待 Flash？</p><h2 id="回答"><a href="#回答" class="headerlink" title="回答"></a>回答</h2><p>结论：不要让应用线程直接等待低速 Flash。更合理的设计是在应用和 Flash 之间增加一层 SRAM Cache&#x2F;Buffer Manager：应用优先读 SRAM 中已经准备好的数据；后台 I&#x2F;O Worker 根据访问模式、缓存水位和请求压力提前把 Flash 数据搬入 SRAM；缓存层再用 Ring Buffer、Block Cache 或 Segment Cache 组织数据。</p><p>这类问题的关键不是“缓存越大越好”，而是把低速存储的长尾延迟、随机访问开销、请求碎片化和后台 I&#x2F;O 抖动从应用实时路径中隔离出来。Buffer 容量和水位线必须按应用消费速率、Flash 最坏读取延迟、后台 I&#x2F;O 调度抖动和安全余量量化，而不是按平均读速率拍脑袋。</p><h2 id="总体架构"><a href="#总体架构" class="headerlink" title="总体架构"></a>总体架构</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    A[应用线程 / 业务模块] --&gt; B[Cache API]</span><br><span class="line">    B --&gt; C&#123;SRAM Cache/Buffer Manager&#125;</span><br><span class="line">    C --&gt; D[Ring Buffer\n顺序数据流]</span><br><span class="line">    C --&gt; E[Block Cache\n随机块访问]</span><br><span class="line">    C --&gt; F[Segment Cache\n顺序窗口 + 随机块缓存]</span><br><span class="line">    C --&gt; G[Watermark Controller\n高/目标/低/危险/空水位]</span><br><span class="line">    G --&gt; H[Request Queue\n预读/紧急读/后台读]</span><br><span class="line">    H --&gt; I[I/O Worker]</span><br><span class="line">    I --&gt; J[请求合并\n对齐读取\n访问模式判断]</span><br><span class="line">    J --&gt; K[低速 Flash / 块设备 / 文件]</span><br><span class="line">    K --&gt; I</span><br><span class="line">    I --&gt; C</span><br><span class="line">    C --&gt; B</span><br></pre></td></tr></table></figure><p>这个架构里，应用看到的是缓存 API，而不是 Flash API。应用线程不直接调用底层 <code>flash_read()</code>，也不把低速 I&#x2F;O 放进关键路径。Cache&#x2F;Buffer Manager 负责命中判断、预读窗口、随机块缓存、请求合并、淘汰策略和水位线控制。</p><h2 id="机制与-Linux-开源实现的对应关系"><a href="#机制与-Linux-开源实现的对应关系" class="headerlink" title="机制与 Linux&#x2F;开源实现的对应关系"></a>机制与 Linux&#x2F;开源实现的对应关系</h2><table><thead><tr><th>本题机制</th><th>Linux 或开源机制</th><th>能否直接使用</th><th>主要参考价值</th></tr></thead><tbody><tr><td>Ring Buffer</td><td><a href="https://docs.kernel.org/core-api/kernel-api.html">Linux kfifo</a>, <a href="https://man7.org/linux/man-pages/man7/io_uring.7.html">io_uring SQ&#x2F;CQ ring</a></td><td>Linux 内核&#x2F;用户态可直接使用对应接口；小系统多为参考</td><td>单生产者&#x2F;单消费者 FIFO、共享 ring、提交队列&#x2F;完成队列。</td></tr><tr><td>Block Cache</td><td><a href="https://docs.kernel.org/mm/page_cache.html">Linux page cache</a>, <a href="https://docs.kernel.org/core-api/xarray.html">XArray</a>, <a href="https://docs.kernel.org/admin-guide/device-mapper/cache.html">dm-cache</a>, <a href="https://docs.kernel.org/admin-guide/bcache.html">bcache</a></td><td>Linux 下 page cache 是默认机制；dm-cache&#x2F;bcache 可作为块层缓存使用</td><td>offset 到缓存页&#x2F;块的映射、缓存块状态、淘汰、脏块、块级 promotion&#x2F;demotion。</td></tr><tr><td>Segment Cache</td><td><a href="https://man7.org/linux/man-pages/man2/readahead.2.html">readahead(2)</a>, <a href="https://man7.org/linux/man-pages/man2/posix_fadvise.2.html">posix_fadvise(2)</a>, page cache</td><td>Linux 用户态可通过 <code>readahead()</code>&#x2F;<code>posix_fadvise()</code>影响内核预读；自定义缓存可参考</td><td>顺序窗口、访问模式提示、顺序读扩大窗口、随机读关闭预读、已用数据释放。</td></tr><tr><td>水位线机制</td><td>Linux zone watermarks、<code>vm.min_free_kbytes</code>、dirty writeback thresholds</td><td>不能直接作为业务缓存组件使用，但机制高度类似</td><td>高&#x2F;低水位触发后台回收或写回，危险水位触发同步回收或限流。</td></tr><tr><td>缓存容量量化</td><td>page cache&#x2F;readahead 的动态策略、块设备吞吐与尾延迟建模</td><td>Linux 已动态处理大部分场景；受限 SRAM 仍需显式计算</td><td>用消费速率、最坏延迟、调度抖动、吞吐余量反推缓存大小。</td></tr></tbody></table><h2 id="应用线程应该如何读数据"><a href="#应用线程应该如何读数据" class="headerlink" title="应用线程应该如何读数据"></a>应用线程应该如何读数据</h2><p>应用线程不建议直接调用底层 Flash 读接口，而应通过缓存层访问数据，例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">cache_read(handle, offset, buf, len, timeout)</span><br><span class="line">cache_acquire(handle, offset, len, &amp;ptr, &amp;actual_len, timeout)</span><br><span class="line">cache_release(handle, ptr, actual_len)</span><br><span class="line">cache_prefetch(handle, offset, len, priority)</span><br></pre></td></tr></table></figure><p><code>cache_read()</code> 适合普通拷贝式读取；<code>cache_acquire()</code> &#x2F; <code>cache_release()</code> 适合零拷贝或少拷贝读取；<code>cache_prefetch()</code> 用于应用提前告诉缓存层“接下来大概率会读这段数据”。</p><p>应用线程遇到缓存不命中时，不应无限期阻塞。应按业务类型选择策略。</p><table><thead><tr><th>场景</th><th>推荐策略</th><th>说明</th></tr></thead><tbody><tr><td>实时数据流</td><td>优先从缓存读；缓存不足则短超时等待；超时后降级</td><td>持续渲染、连续资源读取、协议流式处理等。</td></tr><tr><td>普通资源读取</td><td>允许等待，但必须有超时</td><td>字库、图标、配置表、索引表等。</td></tr><tr><td>后台任务</td><td>异步提交请求并等待事件或回调</td><td>日志扫描、资源遍历、后台校验等。</td></tr></tbody></table><p>Linux 用户态对应的思想是：普通 <code>read()</code> 本身通常会经过 page cache；如果应用知道访问模式，可以通过 <code>posix_fadvise()</code> 提示 <code>POSIX_FADV_SEQUENTIAL</code>、<code>POSIX_FADV_RANDOM</code>、<code>POSIX_FADV_WILLNEED</code> 或 <code>POSIX_FADV_DONTNEED</code>；如果要显式预热某段文件，可以调用 <code>readahead()</code>。这些接口不等价于自己写 Cache Manager，但它们体现了同一个原则：应用声明访问模式，让缓存层提前或减少 I&#x2F;O。</p><h2 id="Buffer-形态如何选"><a href="#Buffer-形态如何选" class="headerlink" title="Buffer 形态如何选"></a>Buffer 形态如何选</h2><p>通用 Buffer 不应只用一种结构。应根据访问模式分成三类：顺序数据流使用 Ring Buffer，随机访问资源使用 Block Cache，混合访问使用 Segment Cache。</p><h3 id="顺序数据流使用-Ring-Buffer"><a href="#顺序数据流使用-Ring-Buffer" class="headerlink" title="顺序数据流使用 Ring Buffer"></a>顺序数据流使用 Ring Buffer</h3><p>如果访问模式是顺序消费，例如连续资源读取、日志回放、语音资源流、模型分块读取，最适合使用 Ring Buffer。</p><p>Ring Buffer 的特点是读写指针单调推进，内存固定，不产生碎片。后台 I&#x2F;O Worker 负责把后续数据写入 Ring Buffer；应用线程负责从 Ring Buffer 读取并消费。只要底层存储的长期有效吞吐高于应用消费速率，并且 Ring Buffer 能覆盖底层 I&#x2F;O 最坏延迟，应用就不会频繁卡在 Flash 上。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    W[Writer / I/O Worker] --&gt;|append| R[(Ring Buffer)]</span><br><span class="line">    R --&gt;|consume| C[Reader / 应用线程]</span><br><span class="line">    R --&gt; M[write index / read index\nfree space / readable bytes]</span><br></pre></td></tr></table></figure><p>高吞吐场景下，Ring Buffer 最好支持 claim&#x2F;finish 语义：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">ring_put_claim()  -&gt; 取得可写 SRAM 地址</span><br><span class="line">io_read_async()   -&gt; I/O 直接写入该地址或写入 staging buffer</span><br><span class="line">ring_put_finish() -&gt; I/O 完成后提交有效长度</span><br><span class="line">ring_get_claim()  -&gt; 应用取得可读地址</span><br><span class="line">ring_get_finish() -&gt; 应用消费后释放空间</span><br></pre></td></tr></table></figure><h4 id="可参考的-Linux-开源实现"><a href="#可参考的-Linux-开源实现" class="headerlink" title="可参考的 Linux&#x2F;开源实现"></a>可参考的 Linux&#x2F;开源实现</h4><ol><li><p><a href="https://docs.kernel.org/core-api/kernel-api.html">Linux kfifo</a></p><p><code>kfifo</code> 是 Linux 内核通用 FIFO&#x2F;ring buffer 工具。它提供 <code>kfifo_in()</code>、<code>kfifo_out()</code>、<code>kfifo_put()</code>、<code>kfifo_get()</code> 等接口。官方文档明确说明，在只有一个并发读者和一个并发写者时，相关宏不需要额外锁；这正好对应“一个预读生产者 + 一个应用消费者”的典型 Ring Buffer 模式。</p><p>可参考点：</p><ul><li>固定内存 FIFO，不做运行期频繁分配。</li><li>单生产者&#x2F;单消费者可以简化锁设计。</li><li>多生产者或多消费者需要外部锁或带锁变体。</li><li><code>in/out</code> 指针推进与 wrap-around 处理可以作为 Ring Buffer 实现参考。</li></ul></li><li><p><a href="https://man7.org/linux/man-pages/man7/io_uring.7.html">io_uring</a></p><p><code>io_uring</code> 不是普通数据缓存，但它的提交队列 SQ 和完成队列 CQ 是共享 ring buffer 模型。用户态把 I&#x2F;O 请求写入 SQ，内核把完成结果写入 CQ。这个机制非常适合作为“请求队列 + 完成队列”的参考。</p><p>可参考点：</p><ul><li>数据路径与控制路径分离。</li><li>SQ 表示应用提交的 I&#x2F;O 请求，CQ 表示后台 I&#x2F;O 完成事件。</li><li>多个请求可以异步在途，完成顺序不一定等于提交顺序，因此每个请求必须有 <code>user_data</code> 或 request id。</li><li>批量提交和批量完成可以减少上下文切换和系统调用成本。</li></ul></li></ol><h3 id="随机访问资源使用-Block-Cache"><a href="#随机访问资源使用-Block-Cache" class="headerlink" title="随机访问资源使用 Block Cache"></a>随机访问资源使用 Block Cache</h3><p>如果访问模式是随机读取，例如字库、图标、配置表、索引、模型权重片段，应使用定长块缓存。</p><p>Block Cache 把底层数据按固定粒度切块，例如 512B、1KB、2KB、4KB 或更大的资源块。每个缓存块记录逻辑地址和状态。应用读取某个 offset 时，缓存层先把 offset 映射到 block id，再查块表；命中则直接返回 SRAM 数据，未命中则提交块读取请求。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[read resource_id + offset + len] --&gt; B[计算 block key]</span><br><span class="line">    B --&gt; C&#123;Block Table 命中?&#125;</span><br><span class="line">    C --&gt;|命中| D[pin block]</span><br><span class="line">    D --&gt; E[返回块内数据]</span><br><span class="line">    E --&gt; F[release / unpin]</span><br><span class="line">    C --&gt;|未命中| G[选择 victim block]</span><br><span class="line">    G --&gt; H[状态置为 LOADING]</span><br><span class="line">    H --&gt; I[提交底层块读取]</span><br><span class="line">    I --&gt; J[校验完成状态]</span><br><span class="line">    J --&gt; K[状态置为 VALID]</span><br><span class="line">    K --&gt; D</span><br></pre></td></tr></table></figure><p>每个缓存块建议至少记录：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">resource id</span><br><span class="line">logical block index</span><br><span class="line">logical offset</span><br><span class="line">valid length</span><br><span class="line">state: EMPTY / LOADING / VALID / DIRTY / ERROR</span><br><span class="line">last access tick or access generation</span><br><span class="line">pin count</span><br></pre></td></tr></table></figure><p>随机访问缓存通常需要 LRU、clock、2Q 或更简单的近似淘汰策略。对关键块要支持 <code>pin count</code>，避免应用正在使用的块被回收。只读资源可以不考虑 DIRTY；如果缓存层也承接写入，则必须增加 DIRTY 状态、写回策略和掉电一致性设计。</p><h4 id="可参考的-Linux-开源实现-1"><a href="#可参考的-Linux-开源实现-1" class="headerlink" title="可参考的 Linux&#x2F;开源实现"></a>可参考的 Linux&#x2F;开源实现</h4><ol><li><p><a href="https://docs.kernel.org/mm/page_cache.html">Linux page cache</a></p><p>Linux 文档说明，page cache 是用户和内核与文件系统交互的主要方式，普通 reads、writes 和 mmaps 都会经过 page cache，除非使用类似 <code>O_DIRECT</code> 的方式绕过。它本质上就是文件 offset 到内存页&#x2F;folio 的缓存映射。</p><p>可参考点：</p><ul><li>文件数据先进入内存缓存，再供读写路径使用。</li><li>缓存粒度以页&#x2F;folio 为单位，而不是任意小字节。</li><li>普通读路径不一定每次都打到底层块设备。</li><li>在内存压力下，干净页可以回收，脏页要先写回。</li></ul></li><li><p><a href="https://docs.kernel.org/core-api/xarray.html">Linux XArray</a></p><p>XArray 是 Linux 内核里的稀疏数组&#x2F;索引结构，文档说明它像一个很大的指针数组，也能满足 hash 或动态数组的部分需求，并且 page cache 是 XArray 最重要的使用者之一。</p><p>可参考点：</p><ul><li>用逻辑页号或块号作为索引，查找缓存对象。</li><li>适合密集或局部密集的整数索引。</li><li>支持无锁读侧查找思路，即读多写少路径要尽量轻。</li><li>比链表遍历更适合 Block Cache 的按 offset 查找。</li></ul></li><li><p><a href="https://docs.kernel.org/admin-guide/device-mapper/cache.html">dm-cache</a></p><p>dm-cache 是 Linux device-mapper 的块缓存 target，用较小较快设备缓存较大较慢设备的数据。文档中它把 origin device 划成固定大小块，并由策略模块决定哪些块 promotion 到 cache device、哪些块 demotion 回 origin device。</p><p>可参考点：</p><ul><li>固定块大小。</li><li>缓存数据和元数据分离。</li><li>缓存策略插件化。</li><li>promotion&#x2F;demotion 与 Block Cache 的装入&#x2F;淘汰类似。</li></ul></li><li><p><a href="https://docs.kernel.org/admin-guide/bcache.html">bcache</a></p><p>bcache 是 Linux 块层缓存。文档说明它使用混合 btree&#x2F;log 跟踪 cached extents，并且会识别大顺序 I&#x2F;O，默认倾向跳过大顺序流，把缓存资源留给随机 I&#x2F;O。</p><p>可参考点：</p><ul><li>缓存不应该机械缓存所有数据。</li><li>大顺序流可能绕过随机块缓存，避免污染缓存。</li><li>block&#x2F;extents 的元数据索引和错误处理机制值得参考。</li><li>顺序 I&#x2F;O 与随机 I&#x2F;O 要采用不同策略。</li></ul></li></ol><h3 id="混合访问使用-Segment-Cache"><a href="#混合访问使用-Segment-Cache" class="headerlink" title="混合访问使用 Segment Cache"></a>混合访问使用 Segment Cache</h3><p>如果资源大体顺序读取，但中间偶尔跳转，例如媒体文件、资源包、数据库式索引、模型权重分段加载，可以使用 Segment Cache。它不是单一固定数据结构，而是 Ring&#x2F;Window + Block Cache 的组合：一部分 SRAM 做顺序预读窗口，另一部分 SRAM 做小型随机块缓存。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[应用读取 offset] --&gt; B&#123;访问模式判断&#125;</span><br><span class="line">    B --&gt;|连续递增| C[Sequential Window]</span><br><span class="line">    B --&gt;|小范围回跳/索引读取| D[Random Block Cache]</span><br><span class="line">    B --&gt;|大跳转| E[重置顺序窗口]</span><br><span class="line">    C --&gt; F[预读 offset 后续区间]</span><br><span class="line">    D --&gt; G[缓存索引块/热点块]</span><br><span class="line">    E --&gt; H[丢弃旧窗口或降权]</span><br><span class="line">    F --&gt; I[返回数据]</span><br><span class="line">    G --&gt; I</span><br><span class="line">    H --&gt; I</span><br></pre></td></tr></table></figure><p>典型拆法如下：</p><table><thead><tr><th>SRAM 区域</th><th>作用</th><th align="right">典型比例</th></tr></thead><tbody><tr><td>Sequential Window</td><td>顺序预读窗口，保障连续吞吐</td><td align="right">50% ~ 75%</td></tr><tr><td>Random Block Cache</td><td>缓存索引、字库、图标、跳转块</td><td align="right">15% ~ 35%</td></tr><tr><td>I&#x2F;O Staging Buffer</td><td>处理对齐、回绕和非连续目标</td><td align="right">5% ~ 15%</td></tr><tr><td>Cache Metadata</td><td>块表、请求队列、统计信息</td><td align="right">1% ~ 5%</td></tr></tbody></table><p>Segment Cache 的核心是“不要让顺序流污染随机热点块，也不要让随机块缓存承担连续吞吐”。顺序窗口负责吞吐，Block Cache 负责跳转和热点。</p><h4 id="可参考的-Linux-开源实现-2"><a href="#可参考的-Linux-开源实现-2" class="headerlink" title="可参考的 Linux&#x2F;开源实现"></a>可参考的 Linux&#x2F;开源实现</h4><ol><li><p><a href="https://man7.org/linux/man-pages/man2/readahead.2.html">readahead(2)</a></p><p><code>readahead()</code> 会把指定文件区域提前读入 page cache，使后续读取尽量从缓存满足，而不是阻塞在磁盘 I&#x2F;O 上。它对应 Segment Cache 里的 Sequential Window：提前把将要顺序消费的数据放进内存。</p></li><li><p><a href="https://man7.org/linux/man-pages/man2/posix_fadvise.2.html">posix_fadvise(2)</a></p><p><code>posix_fadvise()</code> 允许应用声明访问模式。Linux 下 <code>POSIX_FADV_SEQUENTIAL</code> 会扩大 readahead window，<code>POSIX_FADV_RANDOM</code> 会关闭文件 readahead，<code>POSIX_FADV_WILLNEED</code> 会发起非阻塞预读，<code>POSIX_FADV_DONTNEED</code> 可请求释放不再需要的缓存页。</p><p>这几个 advice 与 Segment Cache 的策略直接对应：</p><table><thead><tr><th>advice</th><th>对应策略</th></tr></thead><tbody><tr><td><code>POSIX_FADV_SEQUENTIAL</code></td><td>顺序窗口扩大，积极预读。</td></tr><tr><td><code>POSIX_FADV_RANDOM</code></td><td>不做盲目顺序预读，转向 Block Cache。</td></tr><tr><td><code>POSIX_FADV_WILLNEED</code></td><td>提前装入即将访问的 segment。</td></tr><tr><td><code>POSIX_FADV_DONTNEED</code></td><td>已消费 segment 降权或释放。</td></tr></tbody></table></li><li><p><a href="https://docs.kernel.org/admin-guide/bcache.html">bcache</a></p><p>bcache 文档明确提到，它会检测顺序 I&#x2F;O 并跳过缓存，因为大顺序 I&#x2F;O 通常不值得进入随机缓存。这是 Segment Cache 里“顺序流和随机热点分开处理”的典型参考。</p></li></ol><h2 id="预读线程与应用线程之间的机制"><a href="#预读线程与应用线程之间的机制" class="headerlink" title="预读线程与应用线程之间的机制"></a>预读线程与应用线程之间的机制</h2><p>推荐使用“请求队列 + 后台 Worker + 完成事件 + 水位线控制”的模型。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant App as 应用线程</span><br><span class="line">    participant Cache as Cache/Buffer Manager</span><br><span class="line">    participant Q as Request Queue</span><br><span class="line">    participant Worker as I/O Worker</span><br><span class="line">    participant Store as Flash/文件/块设备</span><br><span class="line"></span><br><span class="line">    App-&gt;&gt;Cache: cache_read(offset, len, timeout)</span><br><span class="line">    Cache-&gt;&gt;Cache: 查询 Ring/Block/Segment Cache</span><br><span class="line">    alt 命中</span><br><span class="line">        Cache--&gt;&gt;App: 返回 SRAM 数据</span><br><span class="line">    else 未命中</span><br><span class="line">        Cache-&gt;&gt;Q: 提交 read request</span><br><span class="line">        Cache-&gt;&gt;Worker: 唤醒或提升预读服务能力</span><br><span class="line">        Worker-&gt;&gt;Q: 合并相邻请求 / 排序 / 对齐</span><br><span class="line">        Worker-&gt;&gt;Store: 发起底层读取</span><br><span class="line">        Store--&gt;&gt;Worker: 返回数据或完成事件</span><br><span class="line">        Worker-&gt;&gt;Cache: 更新缓存块状态</span><br><span class="line">        Cache--&gt;&gt;App: 唤醒等待者 / 返回数据</span><br><span class="line">    end</span><br></pre></td></tr></table></figure><p>应用线程负责提出需求和消费数据，不直接访问 Flash，不直接操作底层 I&#x2F;O，不在实时路径里无限等待。后台 Worker 平时可以休眠；被低水位、缓存 miss 或显式预取请求唤醒后，合并相邻请求，转换为对齐读，把数据搬到 SRAM 缓存区或 staging buffer。完成后，Worker 更新缓存块状态并唤醒等待的应用线程。</p><p>对应 Linux 机制可以参考 <code>io_uring</code>：应用通过提交队列投递请求，内核异步处理后通过完成队列返回结果。即使不用 <code>io_uring</code>，这种“提交队列&#x2F;完成队列&#x2F;请求 id&#x2F;异步完成”的结构也适合抽象后台预读。</p><h2 id="优先级与资源仲裁"><a href="#优先级与资源仲裁" class="headerlink" title="优先级与资源仲裁"></a>优先级与资源仲裁</h2><p>预读 Worker 不应简单固定为最低优先级。更合理的策略是动态服务能力或资源仲裁。</p><table><thead><tr><th>状态</th><th>预读策略</th></tr></thead><tbody><tr><td>缓存高水位</td><td>Worker 休眠或降低预读强度。</td></tr><tr><td>缓存目标水位</td><td>维持稳定预读窗口。</td></tr><tr><td>缓存低水位</td><td>唤醒 Worker，合并读取，增大单次 read size。</td></tr><tr><td>缓存危险水位</td><td>暂停非关键读写，优先补充缓存。</td></tr><tr><td>应用实时读取即将超时</td><td>urgent request 插队，但要避免长期饿死普通请求。</td></tr><tr><td>底层存储正在写入或擦除</td><td>实时读优先；非关键写入延迟。</td></tr></tbody></table><p>Linux 里类似的思想不是“把某个线程提优先级”，而是全局资源管理。例如：</p><ul><li>page allocator 通过 min&#x2F;low&#x2F;high watermark 决定是否触发直接回收、唤醒 kswapd 或停止回收。</li><li>dirty writeback 通过 <code>dirty_background_bytes</code>、<code>dirty_bytes</code> 等阈值决定后台写回和同步写回压力。</li><li>bcache 会根据缓存设备延迟做拥塞控制，延迟过高时降低相关流量。</li></ul><p>因此，在本题里，“优先级”不应只理解成线程优先级，而应理解成资源仲裁：谁可以占用 Flash&#x2F;I&#x2F;O 通道，谁可以触发预读，谁必须被限流，谁可以插队。</p><h2 id="水位线机制"><a href="#水位线机制" class="headerlink" title="水位线机制"></a>水位线机制</h2><p>水位线应同时管理可用数据量、空闲空间和请求压力。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">stateDiagram-v2</span><br><span class="line">    [*] --&gt; Target</span><br><span class="line">    Target --&gt; High: 可读数据 &gt;= 高水位</span><br><span class="line">    High --&gt; Target: 应用消费后回落</span><br><span class="line">    Target --&gt; Low: 可读数据 &lt; 低水位</span><br><span class="line">    Low --&gt; Danger: 补充速度仍低于消费速度</span><br><span class="line">    Danger --&gt; Empty: 缓存耗尽或等待超时</span><br><span class="line">    Low --&gt; Target: 预读补足</span><br><span class="line">    Danger --&gt; Target: 紧急预读成功</span><br><span class="line">    Empty --&gt; Target: 重新装载成功</span><br></pre></td></tr></table></figure><table><thead><tr><th>水位</th><th>触发条件</th><th>动作</th></tr></thead><tbody><tr><td>高水位</td><td>缓存数据已足够多，空闲空间不足</td><td>停止或降低预读，释放底层 I&#x2F;O 给其他任务。</td></tr><tr><td>目标水位</td><td>正常稳定水位</td><td>维持预读窗口，保持吞吐平衡。</td></tr><tr><td>低水位</td><td>应用消费快于补充</td><td>唤醒 Worker，合并读取，增大单次 read size。</td></tr><tr><td>危险水位</td><td>即将不够应用消费</td><td>暂停非关键 I&#x2F;O，允许紧急请求插队，优先补缓存。</td></tr><tr><td>空水位</td><td>发生 underrun 或 cache miss timeout</td><td>返回错误、阻塞到超时、填默认数据或进入业务降级。</td></tr></tbody></table><p>水位线不能只按百分比配置，还应按时间配置。比如缓存中还有 64KB 数据并不一定安全；如果应用消费 2MB&#x2F;s，它只能支撑约 32ms。如果底层存储最坏延迟可能达到 80ms，64KB 就不够。</p><h4 id="可参考的-Linux-开源实现-3"><a href="#可参考的-Linux-开源实现-3" class="headerlink" title="可参考的 Linux&#x2F;开源实现"></a>可参考的 Linux&#x2F;开源实现</h4><ol><li><p><a href="https://docs.kernel.org/mm/physical_memory.html">Linux physical memory watermarks</a></p><p>Linux 的 <code>struct zone</code> 里有 <code>_watermark</code>。官方文档说明：空闲页低于 min watermark 时可能触发 direct reclaim&#x2F;compaction；低于 low watermark 时唤醒 kswapd；高于 high watermark 时 kswapd 停止回收。这个机制和缓存水位线非常接近：低水位触发后台补救，高水位停止后台动作，危险水位触发同步或强制动作。</p></li><li><p><a href="https://docs.kernel.org/admin-guide/sysctl/vm.html">vm.min_free_kbytes</a></p><p><code>vm.min_free_kbytes</code> 用于让 Linux VM 保留最低空闲内存，并据此计算各 lowmem zone 的 <code>WMARK_MIN</code>。这对应本题里的“危险水位&#x2F;最低保底容量”：不是看到缓存快空了才动作，而是必须保留能覆盖最坏延迟的安全垫。</p></li><li><p><a href="https://docs.kernel.org/admin-guide/sysctl/vm.html">dirty writeback thresholds</a></p><p><code>dirty_background_bytes</code> 表示达到多少脏内存后后台 flusher 开始写回；<code>dirty_bytes</code> 表示进程自己开始参与写回的阈值。它对应“高水位&#x2F;低水位触发后台动作”的另一类例子：阈值不是为了显示百分比，而是为了改变系统行为。</p></li></ol><h2 id="缓存容量如何量化"><a href="#缓存容量如何量化" class="headerlink" title="缓存容量如何量化"></a>缓存容量如何量化</h2><p>最小缓存容量应基于最坏情况，而不是平均情况。</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">R_app      = 应用最大连续消费速率，单位 B/s</span><br><span class="line">T_io_tail  = 底层 I/O 最坏读取延迟，包括命令、寻址、文件/块映射、等待和搬运</span><br><span class="line">T_sched    = 后台 Worker 调度抖动和高优先级任务占用时间</span><br><span class="line">T_jitter   = 应用消费抖动、请求合并等待、锁竞争等额外不确定性</span><br><span class="line">T_margin   = 安全余量</span><br><span class="line">B_min      = 最小可用缓存容量</span><br></pre></td></tr></table></figure><p>至少满足：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">B_min &gt;= R_app * (T_io_tail + T_sched + T_jitter + T_margin)</span><br></pre></td></tr></table></figure><p>同时还要满足长期吞吐条件：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">R_io_effective &gt; R_app_peak_average</span><br></pre></td></tr></table></figure><p>如果底层 I&#x2F;O 的长期有效吞吐小于应用平均消费速率，单纯加 Buffer 只能推迟卡顿，不能从根本解决问题。此时应降低应用消费速率、压缩数据、改变资源布局、减少随机访问或换更快的存储。</p><h3 id="顺序-Ring-Buffer-容量"><a href="#顺序-Ring-Buffer-容量" class="headerlink" title="顺序 Ring Buffer 容量"></a>顺序 Ring Buffer 容量</h3><p>Ring Buffer 主要覆盖连续消费过程中的 I&#x2F;O 空窗期。</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">B_ring_min = R_stream * T_cover</span><br><span class="line">T_cover    = T_io_tail + T_sched + T_margin</span><br></pre></td></tr></table></figure><p>若应用消费速率为 2MB&#x2F;s，底层最坏读延迟为 80ms，调度抖动 20ms，安全余量 50ms：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">B_ring_min = 2MB/s * (80ms + 20ms + 50ms)</span><br><span class="line">           = 2MB/s * 150ms</span><br><span class="line">           = 300KB</span><br></pre></td></tr></table></figure><p>这说明 64KB 缓存即使看起来不小，也只能支撑约 32ms 的 2MB&#x2F;s 连续消费，无法覆盖 80ms 的长尾延迟。</p><h3 id="Block-Cache-容量"><a href="#Block-Cache-容量" class="headerlink" title="Block Cache 容量"></a>Block Cache 容量</h3><p>Block Cache 的容量不能只按时间算，还要看工作集大小和命中率。</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">B_block = N_blocks * block_size</span><br><span class="line">N_blocks &gt;= hot_working_set_blocks + pinned_blocks + miss_inflight_blocks</span><br></pre></td></tr></table></figure><p>其中：</p><ul><li><code>hot_working_set_blocks</code> 是短时间内会重复访问的热点块数量。</li><li><code>pinned_blocks</code> 是应用正在持有、不能淘汰的块数量。</li><li><code>miss_inflight_blocks</code> 是同时在途读取的未命中块数量。</li></ul><p>如果随机访问没有局部性，Block Cache 命中率会很差。此时应该优化资源布局或建立索引，而不是盲目扩大缓存。</p><h3 id="Segment-Cache-容量"><a href="#Segment-Cache-容量" class="headerlink" title="Segment Cache 容量"></a>Segment Cache 容量</h3><p>Segment Cache 需要在顺序窗口和随机块缓存之间分配预算。</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">B_total_cache = B_seq_window + B_random_cache + B_staging + B_meta</span><br><span class="line">B_seq_window &gt;= R_stream * T_cover</span><br><span class="line">B_random_cache &gt;= hot_random_working_set</span><br></pre></td></tr></table></figure><p>经验上，应先保证顺序窗口能覆盖最坏延迟，再把剩余空间分给随机块缓存。否则连续流会先卡住；而随机缓存不足通常只是命中率下降，不一定立即造成连续性失败。</p><h3 id="Linux-机制里的容量参考"><a href="#Linux-机制里的容量参考" class="headerlink" title="Linux 机制里的容量参考"></a>Linux 机制里的容量参考</h3><p>Linux page cache 会根据系统内存压力动态增减，不需要应用显式指定固定大小；但它仍然遵循相同的本质约束：page cache 命中时读路径快，未命中时要等待底层 I&#x2F;O。<code>readahead()</code> 的作用是提前把文件区域读入 page cache；<code>posix_fadvise()</code> 的作用是告诉内核访问模式，让内核调整 readahead window 或缓存回收倾向。</p><p>对于受限 SRAM 的自定义缓存，不能假设系统会自动帮你保留足够 page cache，因此容量必须显式计算。</p><h2 id="Flash-读取策略"><a href="#Flash-读取策略" class="headerlink" title="Flash 读取策略"></a>Flash 读取策略</h2><p>底层读取应尽量顺序、大块、对齐、可合并。</p><p>推荐策略如下：</p><ol><li>顺序预读，不要让应用每次读几十字节都打到底层 Flash。</li><li>读请求按 Flash 页、逻辑块、资源包块和 I&#x2F;O 对齐要求对齐。</li><li>合并相邻小请求，减少命令开销和片选切换。</li><li>资源索引、目录表、偏移表在打开资源时提前加载。</li><li>避免在实时读取期间做长时间写入或擦除。</li><li>若必须读写共存，建立 I&#x2F;O Arbiter：实时读优先，非关键写入延迟或降级。</li><li>对于只读资源，优先使用连续布局，减少碎片和跨块跳转。</li></ol><p>Linux 里的对应机制包括：</p><ul><li><code>readahead()</code>：提前把后续数据放进 page cache。</li><li><code>POSIX_FADV_SEQUENTIAL</code>：告诉内核顺序访问，扩大 readahead window。</li><li><code>POSIX_FADV_RANDOM</code>：告诉内核随机访问，避免错误的顺序预读。</li><li>bcache 的 sequential bypass：大顺序 I&#x2F;O 不一定值得进入随机缓存。</li></ul><p>缓存解决的是底层存储长尾延迟，不是长期带宽不足。因此读块大小和水位线要按最坏读延迟配置，而不是按实验室平均读速率配置。</p><h2 id="I-O-搬运与完成路径"><a href="#I-O-搬运与完成路径" class="headerlink" title="I&#x2F;O 搬运与完成路径"></a>I&#x2F;O 搬运与完成路径</h2><p>I&#x2F;O 搬运不应被设计成“应用线程同步读到底”。更好的方式是异步提交、后台完成、完成后提交缓存状态。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    A[应用提交读取意图] --&gt; B[Request Queue]</span><br><span class="line">    B --&gt; C[I/O Worker]</span><br><span class="line">    C --&gt; D[底层 read / async read / DMA]</span><br><span class="line">    D --&gt; E[Completion Event]</span><br><span class="line">    E --&gt; F[更新缓存状态]</span><br><span class="line">    F --&gt; G[唤醒等待应用]</span><br></pre></td></tr></table></figure><p>对应 Linux 实现里，<code>io_uring</code> 的 SQ&#x2F;CQ ring 是很好的参考：请求与完成解耦，完成事件带结果和 request id；多个请求可以在途，完成顺序不保证等于提交顺序。即使系统没有 <code>io_uring</code>，这个模型仍然适合作为缓存层的内部结构。</p><h2 id="最终回答组织方式"><a href="#最终回答组织方式" class="headerlink" title="最终回答组织方式"></a>最终回答组织方式</h2><p>回答这类题时，可以按以下顺序展开：</p><ol><li>先讲核心原则：应用线程不要直接等 Flash，要读 SRAM 缓存。</li><li>再讲分层架构：应用接口、Cache Manager、后台 Worker、底层 I&#x2F;O。</li><li>接着讲 Buffer 选型：顺序流用 Ring Buffer，随机读用 Block Cache，混合读用 Segment Cache。</li><li>然后讲线程协作：应用提交请求，Worker 合并请求并异步读取，完成后提交缓存状态。</li><li>再讲水位线：高水位停预读，低水位启动预读，危险水位优先补缓存并限制非关键 I&#x2F;O。</li><li>最后讲容量计算：按应用消费速率、底层最坏延迟、调度抖动和安全余量反推缓存容量。</li></ol><p>一句话概括：Flash 上的数据只是存储形态，应用真正依赖的是 SRAM 缓存层；缓存层通过 Ring&#x2F;Block&#x2F;Segment 三类结构、预读、水位线、异步 I&#x2F;O 和资源仲裁，把低速 Flash 的不确定性挡在应用实时路径之外。</p><h2 id="参考链接"><a href="#参考链接" class="headerlink" title="参考链接"></a>参考链接</h2><ul><li><a href="https://docs.kernel.org/core-api/kernel-api.html">Linux kfifo API</a></li><li><a href="https://man7.org/linux/man-pages/man7/io_uring.7.html">io_uring Linux manual page</a></li><li><a href="https://docs.kernel.org/mm/page_cache.html">Linux Page Cache documentation</a></li><li><a href="https://docs.kernel.org/core-api/xarray.html">Linux XArray documentation</a></li><li><a href="https://man7.org/linux/man-pages/man2/readahead.2.html">readahead(2)</a></li><li><a href="https://man7.org/linux/man-pages/man2/posix_fadvise.2.html">posix_fadvise(2)</a></li><li><a href="https://docs.kernel.org/mm/physical_memory.html">Linux physical memory watermarks</a></li><li><a href="https://docs.kernel.org/admin-guide/sysctl/vm.html">Linux &#x2F;proc&#x2F;sys&#x2F;vm documentation</a></li><li><a href="https://docs.kernel.org/admin-guide/device-mapper/cache.html">dm-cache documentation</a></li><li><a href="https://docs.kernel.org/admin-guide/bcache.html">bcache documentation</a></li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;meta name=&quot;referrer&quot; content=&quot;no-referrer&quot; /&gt;

&lt;h1 id=&quot;嵌入式面试真题第-01-题：低速-Flash-与-1MB-SRAM-下的通用缓存架构设计&quot;&gt;&lt;a href=&quot;#嵌入式面试真题第-01-题：低速-Flash-与-1MB</summary>
      
    
    
    
    <category term="嵌入式面试真题" scheme="https://wdfk-prog.space/categories/%E5%B5%8C%E5%85%A5%E5%BC%8F%E9%9D%A2%E8%AF%95%E7%9C%9F%E9%A2%98/"/>
    
    
    <category term="嵌入式面试真题" scheme="https://wdfk-prog.space/tags/%E5%B5%8C%E5%85%A5%E5%BC%8F%E9%9D%A2%E8%AF%95%E7%9C%9F%E9%A2%98/"/>
    
  </entry>
  
  <entry>
    <title>CANopen CiA 协议全景：核心内容、重点与组合关系</title>
    <link href="https://wdfk-prog.space/posts/526a5796/"/>
    <id>https://wdfk-prog.space/posts/526a5796/</id>
    <published>2026-06-30T01:02:18.000Z</published>
    <updated>2026-06-30T01:34:09.229Z</updated>
    
    <content type="html"><![CDATA[<meta name="referrer" content="no-referrer" /><h1 id="CANopen-CiA-协议全景：重点规范、核心内容与组合关系"><a href="#CANopen-CiA-协议全景：重点规范、核心内容与组合关系" class="headerlink" title="CANopen CiA 协议全景：重点规范、核心内容与组合关系"></a>CANopen CiA 协议全景：重点规范、核心内容与组合关系</h1><p><img src="https://i-blog.csdnimg.cn/direct/d01a10a671a8404b8a829e99ce4b48cc.png" alt="在这里插入图片描述"></p><p>@[toc]<br><img src="https://i-blog.csdnimg.cn/direct/68dbdb8141e84689b207b121f1142d8f.png" alt="在这里插入图片描述"></p><blockquote><p>本文面向 CANopen 工程学习、设备选型、对象字典设计和协议组合判断。<br>本版按你的要求移除了原文末尾的项目建议和评审清单类章节。<br>图片均为 PNG，不包含 SVG；交付格式为 Markdown + images 目录。</p></blockquote><h2 id="1-结论"><a href="#1-结论" class="headerlink" title="1. 结论"></a>1. 结论</h2><p>CANopen 的协议体系可以按三层理解：</p><ol><li><strong>通信基础层</strong>：以 CiA 301 &#x2F; CiA 1301 为核心，定义对象字典、通信服务和网络管理。</li><li><strong>横向功能层</strong>：例如 CiA 320 处理 sleep&#x2F;wake-up，CiA 710 处理 bootloader，CiA 305 处理 LSS，CiA 306 处理 EDS&#x2F;DCF。</li><li><strong>设备与应用 Profile 层</strong>：例如 CiA 401、402、406、445、461、434 等，为某类设备定义应用对象、过程数据、配置参数、诊断信息和默认 PDO 思路。</li></ol><p>本版重点单独展开 8 个规范：<strong>CiA 320、CiA 710、CiA 401、CiA 402、CiA 445、CiA 406、CiA 461、CiA 434</strong>。</p><p><img src="https://i-blog.csdnimg.cn/direct/b503b49794324da4a28635d80572fe92.png" alt="在这里插入图片描述"></p><h2 id="2-CANopen-协议族总体结构"><a href="#2-CANopen-协议族总体结构" class="headerlink" title="2. CANopen 协议族总体结构"></a>2. CANopen 协议族总体结构</h2><p>CANopen 不是单个协议文件，而是一组围绕对象字典和通信对象构建的规范族。CiA 301 定义 CANopen Classic 的应用层和通信 Profile，包括数据类型、编码规则、对象字典对象、通信服务、网络管理服务，以及 Emergency、Time-stamp、Sync 等通信对象内容。CiA 1301 则定义 CANopen FD 的应用层和通信 Profile，覆盖 CAN FD 数据链路层映射和 CAN 物理介质附加选项。</p><p>工程上可以按下面方式划分：</p><table><thead><tr><th>层级</th><th>典型规范</th><th>作用</th><th>主要风险</th></tr></thead><tbody><tr><td>基础通信</td><td>CiA 301 &#x2F; CiA 1301</td><td>NMT、SDO&#x2F;USDO、PDO、SYNC、TIME、EMCY、Heartbeat、对象字典</td><td>CAN-ID、PDO 映射、NMT 状态、错误控制</td></tr><tr><td>配置与描述</td><td>CiA 305 &#x2F; 306 &#x2F; 1305 &#x2F; 1311</td><td>LSS、EDS&#x2F;DCF、XDD、设备配置</td><td>工具文件与固件对象字典不一致</td></tr><tr><td>横向功能</td><td>CiA 320 &#x2F; 710</td><td>低功耗、休眠唤醒、固件升级</td><td>休眠与升级冲突、升级失败不可恢复</td></tr><tr><td>设备 Profile</td><td>CiA 401 &#x2F; 402 &#x2F; 406 &#x2F; 445 &#x2F; 461 等</td><td>某类设备的应用对象、PDO、参数、诊断</td><td>对象区间、默认 PDO、设备行为冲突</td></tr><tr><td>应用 Profile</td><td>CiA 417 &#x2F; 422 &#x2F; 434 等</td><td>系统级虚拟设备和交互关系</td><td>虚拟设备、广播 PDO、错误处理策略冲突</td></tr></tbody></table><h2 id="3-CiA-320：Sleep-Wake-up-处理"><a href="#3-CiA-320：Sleep-Wake-up-处理" class="headerlink" title="3. CiA 320：Sleep &#x2F; Wake-up 处理"></a>3. CiA 320：Sleep &#x2F; Wake-up 处理</h2><h3 id="3-1-定位"><a href="#3-1-定位" class="headerlink" title="3.1 定位"></a>3.1 定位</h3><p>CiA 320 定义 CANopen 设备的 sleep 和 wake-up 处理服务与协议。它面向支持低功耗模式或 selective wake-up 能力的 CAN 收发器，也适用于轻型电动车、特种车辆附加设备、服务机器人、AGV、电池供电模块等能源受限系统。</p><h3 id="3-2-大概内容"><a href="#3-2-大概内容" class="headerlink" title="3.2 大概内容"></a>3.2 大概内容</h3><p>CiA 320 通过 power manager（PM）管理网络中的 power management device（PM device）。PM 通常位于当前有效的 CANopen NMT commander 中，用于控制本地 Sleep FSA 以及网络中 PM device 的远程 Sleep FSA。</p><p>公开资料显示：PM 使用 CAN-ID <code>691h</code> 的 CAN 数据帧向 PM device 发送命令；PM device 使用 CAN-ID <code>690h</code> 的 CAN 数据帧以及 CiA 301 中定义的 EMCY write protocol 向 PM 发送请求。</p><h3 id="3-3-核心与重点"><a href="#3-3-核心与重点" class="headerlink" title="3.3 核心与重点"></a>3.3 核心与重点</h3><ul><li><strong>核心对象不是运动或 I&#x2F;O，而是电源状态管理</strong>。</li><li>需要定义哪些节点可以睡眠、哪些节点必须保持在线。</li><li>Sleep &#x2F; wake-up 要和 NMT、Heartbeat、EMCY、LSS、Bootloader 规则统一。</li><li>若节点睡眠后 Heartbeat 消失，主站不能简单把它当作故障掉线，必须区分“计划睡眠”和“异常离线”。</li><li>若节点在执行固件下载或关键动作，必须禁止进入睡眠。</li></ul><h3 id="3-4-组合关系"><a href="#3-4-组合关系" class="headerlink" title="3.4 组合关系"></a>3.4 组合关系</h3><table><thead><tr><th>组合对象</th><th>结论</th><th>说明</th></tr></thead><tbody><tr><td>CiA 320 + CiA 301</td><td>可结合</td><td>CiA 320 是 CANopen 服务扩展，仍需要基础通信服务。</td></tr><tr><td>CiA 320 + CiA 401&#x2F;402&#x2F;406&#x2F;445&#x2F;461&#x2F;434</td><td>可结合但需约束</td><td>低功耗会改变节点在线状态，影响 PDO、Heartbeat 和诊断。</td></tr><tr><td>CiA 320 + CiA 710</td><td>高风险组合</td><td>固件升级期间进入睡眠可能导致下载中断或校验失败。</td></tr><tr><td>CiA 320 + Safety</td><td>高约束</td><td>安全相关节点是否允许睡眠要经过系统安全设计。</td></tr></tbody></table><p><img src="https://i-blog.csdnimg.cn/direct/2f2d9677123a421eb32ffb676468247d.png" alt="在这里插入图片描述"></p><h2 id="4-CiA-710：Generic-CANopen-Bootloader"><a href="#4-CiA-710：Generic-CANopen-Bootloader" class="headerlink" title="4. CiA 710：Generic CANopen Bootloader"></a>4. CiA 710：Generic CANopen Bootloader</h2><h3 id="4-1-定位"><a href="#4-1-定位" class="headerlink" title="4.1 定位"></a>4.1 定位</h3><p>CiA 710 是通用 CANopen bootloader 规范，用于统一 CANopen CC 和 CANopen FD 设备的软件更新行为。CiA 技术文档页面显示，CiA 710 version 1.0.0 的 scope 是定义 CANopen 设备的 bootloader mode，以及 bootloader mode 与 application mode 之间的切换。</p><h3 id="4-2-大概内容"><a href="#4-2-大概内容" class="headerlink" title="4.2 大概内容"></a>4.2 大概内容</h3><p>CiA 710 关注固件升级流程，而不是普通过程数据控制。它要解决的问题包括：</p><ul><li>设备如何从 application mode 切换到 bootloader mode；</li><li>设备如何保持 bootloader 可访问；</li><li>固件如何下载、校验、写入；</li><li>下载中断或固件不完整时如何恢复；</li><li>固件完整性和有效性校验通过后，如何重新启动应用。</li></ul><h3 id="4-3-核心与重点"><a href="#4-3-核心与重点" class="headerlink" title="4.3 核心与重点"></a>4.3 核心与重点</h3><ul><li><strong>升级模式必须和正常应用模式隔离</strong>。进入 bootloader 后，正常 PDO 控制不应继续驱动设备动作。</li><li><strong>必须考虑掉电、通信中断、下载不完整、校验失败</strong>。</li><li><strong>应用有效性检查是关键</strong>。设备不应启动无效固件。</li><li><strong>恢复能力比下载速度更重要</strong>。工业设备升级失败后仍应能重新进入 bootloader。</li><li><strong>权限与安全策略需要产品级补充</strong>。CiA 710 提供通用框架，但实际项目还要考虑认证、版本回滚、加密签名、升级窗口等。</li></ul><h3 id="4-4-组合关系"><a href="#4-4-组合关系" class="headerlink" title="4.4 组合关系"></a>4.4 组合关系</h3><table><thead><tr><th>组合对象</th><th>结论</th><th>说明</th></tr></thead><tbody><tr><td>CiA 710 + CiA 301 &#x2F; 1301</td><td>可结合</td><td>Bootloader 可面向 Classic 与 FD 设备。</td></tr><tr><td>CiA 710 + 任意设备 Profile</td><td>可结合</td><td>固件升级是横向功能，不改变设备 Profile 的应用语义。</td></tr><tr><td>CiA 710 + CiA 320</td><td>需谨慎</td><td>升级过程中通常应禁止自动睡眠。</td></tr><tr><td>CiA 710 + CiA 309 Gateway</td><td>可结合但要限权</td><td>远程升级要增加权限、日志、超时、恢复策略。</td></tr></tbody></table><h2 id="5-CiA-401：Generic-I-O-Device-Profile"><a href="#5-CiA-401：Generic-I-O-Device-Profile" class="headerlink" title="5. CiA 401：Generic I&#x2F;O Device Profile"></a>5. CiA 401：Generic I&#x2F;O Device Profile</h2><h3 id="5-1-定位"><a href="#5-1-定位" class="headerlink" title="5.1 定位"></a>5.1 定位</h3><p>CiA 401 是通用 I&#x2F;O 设备 Profile，定义带数字输入、数字输出、模拟输入、模拟输出的模块化 CANopen 设备接口。CiA 官方页面说明，最新版本拆为 CiA 401-B（功能行为与参数）、CiA 401-C（映射到 CANopen CC）、CiA 401-F（映射到 CANopen FD）。</p><h3 id="5-2-大概内容"><a href="#5-2-大概内容" class="headerlink" title="5.2 大概内容"></a>5.2 大概内容</h3><p>CiA 401 关注 I&#x2F;O 模块的标准化表达。它通常包括：</p><ul><li>数字输入和数字输出对象；</li><li>模拟输入和模拟输出对象；</li><li>默认 TPDO &#x2F; RPDO；</li><li>输入触发条件；</li><li>输出在故障情况下的行为；</li><li>可选的 bit、word、double-word 访问；</li><li>不同模拟分辨率和厂商自定义分辨率。</li></ul><p>公开资料指出，CiA 401 的第一个默认 TPDO&#x2F;RPDO 支持最多 64 路数字输入&#x2F;输出，并且后续默认 PDO 可传输或接收最多 12 路 16-bit 分辨率的模拟输入&#x2F;输出。</p><h3 id="5-3-核心与重点"><a href="#5-3-核心与重点" class="headerlink" title="5.3 核心与重点"></a>5.3 核心与重点</h3><ul><li><strong>重点不是“GPIO 编号”，而是 I&#x2F;O 语义标准化</strong>。</li><li>输入侧要关注变化触发、滤波、极性、故障值、PDO 触发条件。</li><li>输出侧要关注默认值、掉线行为、故障行为、安全状态。</li><li>对模块化设备，要处理模块槽位、对象实例和设备描述文件一致性。</li><li>I&#x2F;O 和驱动器、RFID、称重、实验室单元结合时，I&#x2F;O 常作为触发、互锁、状态灯、按钮、工位信号使用。</li></ul><h3 id="5-4-组合关系"><a href="#5-4-组合关系" class="headerlink" title="5.4 组合关系"></a>5.4 组合关系</h3><table><thead><tr><th>组合对象</th><th>结论</th><th>风险</th></tr></thead><tbody><tr><td>CiA 401 + CiA 402</td><td>可组合</td><td>驱动器带本地 I&#x2F;O 时容易出现 PDO 和 Device type 表达不清。</td></tr><tr><td>CiA 401 + CiA 445</td><td>可组合</td><td>I&#x2F;O 可触发 RFID 读写，但要防止重复触发和边沿误判。</td></tr><tr><td>CiA 401 + CiA 461</td><td>可组合</td><td>I&#x2F;O 可承载称重设备状态灯、按钮、报警输出。</td></tr><tr><td>CiA 401 + CiA 434</td><td>可组合</td><td>实验室单元可使用 I&#x2F;O 作为工位、夹具、传感器、执行器接口。</td></tr></tbody></table><h2 id="6-CiA-402：Drives-and-Motion-Control"><a href="#6-CiA-402：Drives-and-Motion-Control" class="headerlink" title="6. CiA 402：Drives and Motion Control"></a>6. CiA 402：Drives and Motion Control</h2><h3 id="6-1-定位"><a href="#6-1-定位" class="headerlink" title="6.1 定位"></a>6.1 定位</h3><p>CiA 402 是驱动器与运动控制设备 Profile。CiA 官方说明，该系列规范标准化伺服驱动器、变频器和步进电机控制器的功能行为，并引入多个 operation mode 及对应配置参数。规范描述中包含 finite state automaton（FSA），用于定义驱动器各状态下的内部与外部行为。</p><h3 id="6-2-大概内容"><a href="#6-2-大概内容" class="headerlink" title="6.2 大概内容"></a>6.2 大概内容</h3><p>CiA 402 的典型内容包括：</p><ul><li>Power Drive System FSA；</li><li><code>Controlword</code> 和 <code>Statusword</code>；</li><li>operation mode 和 mode display；</li><li>profile position、profile velocity、homing、cyclic synchronous position&#x2F;velocity&#x2F;torque 等模式；</li><li>目标位置、实际位置、目标速度、实际速度、目标转矩、实际转矩；</li><li>默认 RPDO&#x2F;TPDO；</li><li>fault、quick stop、enable operation 等状态转换；</li><li>运动相关安全参数和 SRDO 参数扩展。</li></ul><p>CiA 官方资料说明，host-controller 通过 control-word 改变状态，当前状态通过 status-word 指示；control-word 和命令值可映射到默认 RPDO，status-word 和实际值可映射到 TPDO。</p><h3 id="6-3-核心与重点"><a href="#6-3-核心与重点" class="headerlink" title="6.3 核心与重点"></a>6.3 核心与重点</h3><ul><li><strong>核心是状态机 + 控制字&#x2F;状态字 + 操作模式</strong>。</li><li>运动控制不是只写目标位置。必须满足 FSA 的状态转换顺序。</li><li>PDO 映射要服务控制周期：控制字、目标值、模式、状态字、实际值、错误状态要放在合适的 PDO 中。</li><li>402 和 406 编码器组合时，要关注同步、采样时刻、位置数据新鲜度。</li><li>402 和 320 组合时，驱动器不可在带功率或执行运动过程中随意休眠。</li><li>402 和 710 组合时，进入 bootloader 前必须让驱动进入安全、非使能、非运动状态。</li></ul><h3 id="6-4-组合关系"><a href="#6-4-组合关系" class="headerlink" title="6.4 组合关系"></a>6.4 组合关系</h3><table><thead><tr><th>组合对象</th><th>结论</th><th>风险</th></tr></thead><tbody><tr><td>CiA 402 + CiA 406</td><td>常见组合</td><td>编码器数据延迟或不同步会影响位置闭环或诊断。</td></tr><tr><td>CiA 402 + CiA 401</td><td>可组合</td><td>本地 I&#x2F;O 可用于限位、抱闸、使能，但安全功能不能只靠普通 I&#x2F;O&#x2F;PDO。</td></tr><tr><td>CiA 402 + CiA 320</td><td>高约束</td><td>带功率状态和睡眠状态需要明确边界。</td></tr><tr><td>CiA 402 + CiA 710</td><td>可组合但需停机</td><td>升级前应停止运动、关断功率级或进入安全状态。</td></tr></tbody></table><h2 id="7-CiA-445：RFID-Device-Profile"><a href="#7-CiA-445：RFID-Device-Profile" class="headerlink" title="7. CiA 445：RFID Device Profile"></a>7. CiA 445：RFID Device Profile</h2><h3 id="7-1-定位"><a href="#7-1-定位" class="headerlink" title="7.1 定位"></a>7.1 定位</h3><p>CiA 445 定义简单和智能 RFID 设备的 CANopen 接口，这类设备用于读取或写入 RFID transponder，也就是 RFID tag。</p><h3 id="7-2-大概内容"><a href="#7-2-大概内容" class="headerlink" title="7.2 大概内容"></a>7.2 大概内容</h3><p>CiA 445 包含 RFID 设备的物理层、数据链路层、应用层接口，以及过程数据、配置参数和诊断信息。公开资料中提到，该 Profile 定义了四类 RFID 设备，从单 transponder、单频率的 reader，到支持多个 transponder 和多个频率的 reader&#x2F;writer。</p><p>RFID 控制通常涉及：</p><ul><li>select command：确定目标 tag 组；</li><li>inventory command：识别某个 tag 组中的单个 tag；</li><li>access command：读取&#x2F;写入 tag 存储区，或执行 lock、kill 等 tag 操作；</li><li>外部触发命令；</li><li>tag 进入&#x2F;离开读取区域事件；</li><li>读取区域内 transponder 数量；</li><li>最近一次读写错误。</li></ul><h3 id="7-3-核心与重点"><a href="#7-3-核心与重点" class="headerlink" title="7.3 核心与重点"></a>7.3 核心与重点</h3><ul><li><strong>核心是识别对象和读写命令的标准化</strong>。</li><li>要区分“读到 tag”“读写成功”“tag 离开区域”“读写错误”这几类事件。</li><li>外部触发与自动 inventory 需要明确策略，避免重复读取、漏读或读写竞争。</li><li>与 I&#x2F;O 结合时，I&#x2F;O 通常负责到位、夹紧、门开关、触发信号；RFID 负责身份确认。</li><li>与实验室自动化或物流设备结合时，RFID 可用于样本、耗材、工装、载具、托盘识别。</li></ul><h3 id="7-4-组合关系"><a href="#7-4-组合关系" class="headerlink" title="7.4 组合关系"></a>7.4 组合关系</h3><table><thead><tr><th>组合对象</th><th>结论</th><th>风险</th></tr></thead><tbody><tr><td>CiA 445 + CiA 401</td><td>可组合</td><td>I&#x2F;O 触发 RFID 时要处理边沿、去抖、重复读写。</td></tr><tr><td>CiA 445 + CiA 434</td><td>可组合</td><td>可用于样本&#x2F;耗材识别，但要定义 tag 与任务的绑定关系。</td></tr><tr><td>CiA 445 + CiA 320</td><td>可组合但需约束</td><td>休眠后 reader 不在线，可能影响工位识别流程。</td></tr><tr><td>CiA 445 + CiA 710</td><td>可组合</td><td>升级期间应禁止正常读写任务。</td></tr></tbody></table><h2 id="8-CiA-406：Encoder-Profile"><a href="#8-CiA-406：Encoder-Profile" class="headerlink" title="8. CiA 406：Encoder Profile"></a>8. CiA 406：Encoder Profile</h2><h3 id="8-1-定位"><a href="#8-1-定位" class="headerlink" title="8.1 定位"></a>8.1 定位</h3><p>CiA 406 是编码器 Profile，面向绝对旋转编码器和线性编码器。CiA 官方页面说明，最新版本包含 CiA 406-B（功能行为与参数）、CiA 406-C（映射到 CANopen CC）、CiA 406-F（映射到 CANopen FD）、CiA 406-J（映射到 J1939）。</p><h3 id="8-2-大概内容"><a href="#8-2-大概内容" class="headerlink" title="8.2 大概内容"></a>8.2 大概内容</h3><p>CiA 406 定义编码器设备的应用接口，通常包括：</p><ul><li>位置值；</li><li>速度值；</li><li>加速度值；</li><li>jerk 值；</li><li>方向、分辨率、量程、预置值；</li><li>诊断信息；</li><li>CANopen CC &#x2F; CANopen FD &#x2F; J1939 映射。</li></ul><h3 id="8-3-核心与重点"><a href="#8-3-核心与重点" class="headerlink" title="8.3 核心与重点"></a>8.3 核心与重点</h3><ul><li><strong>核心是“位置测量设备”的标准化，而不是驱动器控制</strong>。</li><li>与 CiA 402 不同，406 本身不定义驱动状态机，不负责使能功率级。</li><li>编码器数据通常通过 TPDO 周期或同步发送，主站或驱动器使用这些数据进行位置反馈、状态监测或同步控制。</li><li>需要重点关注采样周期、SYNC、PDO 触发类型、数据新鲜度、分辨率和单位。</li><li>多圈编码器、线性编码器和高分辨率位置值要注意数据长度、溢出和标定。</li></ul><h3 id="8-4-组合关系"><a href="#8-4-组合关系" class="headerlink" title="8.4 组合关系"></a>8.4 组合关系</h3><table><thead><tr><th>组合对象</th><th>结论</th><th>风险</th></tr></thead><tbody><tr><td>CiA 406 + CiA 402</td><td>常见组合</td><td>位置反馈若不同步，会影响运动控制质量。</td></tr><tr><td>CiA 406 + CiA 401</td><td>可组合</td><td>I&#x2F;O 可提供 reference、limit、home、trigger 信号。</td></tr><tr><td>CiA 406 + CiA 320</td><td>可组合但需约束</td><td>睡眠导致反馈中断，必须让控制器知道数据不可用。</td></tr><tr><td>CiA 406 + CiA 710</td><td>可组合</td><td>升级期间不要把编码器数据当作有效过程数据。</td></tr></tbody></table><h2 id="9-CiA-461：Weighing-Devices"><a href="#9-CiA-461：Weighing-Devices" class="headerlink" title="9. CiA 461：Weighing Devices"></a>9. CiA 461：Weighing Devices</h2><h3 id="9-1-定位"><a href="#9-1-定位" class="headerlink" title="9.1 定位"></a>9.1 定位</h3><p>CiA 461 是称重设备 Profile。CiA 的通用设备 Profile 列表中仍列出 CiA 461 series 作为 weighing devices Profile；但 CiA 技术文档页面中可见 CiA 461-1&#x2F;2&#x2F;3&#x2F;4 version 2.0.0 条目显示为 withdrawn。因此，工程项目中使用 CiA 461 时，应先向 CiA 或设备厂商确认当前正式状态、可用版本和兼容性要求。</p><h3 id="9-2-大概内容"><a href="#9-2-大概内容" class="headerlink" title="9.2 大概内容"></a>9.2 大概内容</h3><p>公开技术文档列表显示 CiA 461 被分为多个部分：</p><table><thead><tr><th>部分</th><th>内容</th></tr></thead><tbody><tr><td>CiA 461-1</td><td>general definitions and PDOs</td></tr><tr><td>CiA 461-2</td><td>load cells</td></tr><tr><td>CiA 461-3</td><td>scales</td></tr><tr><td>CiA 461-4</td><td>human machine interface and indicators</td></tr></tbody></table><p>根据这些 scope，可推断其关注点包括：</p><ul><li>称重设备的 CANopen interface functionality；</li><li>general communication parameters；</li><li>Process Data Objects；</li><li>lower-layer requirements；</li><li>load cell、scale、HMI、indicator 的过程数据和配置参数；</li><li>PDO communication and mapping parameters。</li></ul><h3 id="9-3-核心与重点"><a href="#9-3-核心与重点" class="headerlink" title="9.3 核心与重点"></a>9.3 核心与重点</h3><ul><li><strong>核心是称重过程值、稳定状态、标定、诊断和显示&#x2F;指示器的标准化</strong>。</li><li>称重值不是普通模拟输入值，还需要考虑稳定时间、去皮、零点、量程、过载、故障状态。</li><li>与 HMI&#x2F;indicator 结合时，要区分“测量值”“显示值”“报警&#x2F;状态输出”。</li><li>与 I&#x2F;O 结合时，I&#x2F;O 可用于去皮按钮、清零按钮、报警灯、夹具状态等。</li><li>与实验室自动化结合时，称重过程可能成为一条工作计划中的关键步骤。</li></ul><h3 id="9-4-组合关系"><a href="#9-4-组合关系" class="headerlink" title="9.4 组合关系"></a>9.4 组合关系</h3><table><thead><tr><th>组合对象</th><th>结论</th><th>风险</th></tr></thead><tbody><tr><td>CiA 461 + CiA 401</td><td>可组合</td><td>按钮、状态灯、报警输出可走 I&#x2F;O，但称重过程值应保持清晰语义。</td></tr><tr><td>CiA 461 + CiA 434</td><td>可组合</td><td>实验室称重单元可纳入 LAC&#x2F;LAU 流程，但要处理稳定等待、错误恢复。</td></tr><tr><td>CiA 461 + CiA 320</td><td>可组合但需约束</td><td>休眠前后需重新确认标定、稳定性和过程值有效性。</td></tr><tr><td>CiA 461 + CiA 710</td><td>可组合</td><td>升级后应考虑标定参数保留和版本兼容。</td></tr></tbody></table><h2 id="10-CiA-434：Laboratory-Automation-Systems"><a href="#10-CiA-434：Laboratory-Automation-Systems" class="headerlink" title="10. CiA 434：Laboratory Automation Systems"></a>10. CiA 434：Laboratory Automation Systems</h2><h3 id="10-1-定位"><a href="#10-1-定位" class="headerlink" title="10.1 定位"></a>10.1 定位</h3><p>CiA 434 是实验室自动化系统的 CANopen device profile 集合，定义 laboratory automation controller（LAC）与多个 laboratory automation units（LAUs）之间的通信。</p><h3 id="10-2-大概内容"><a href="#10-2-大概内容" class="headerlink" title="10.2 大概内容"></a>10.2 大概内容</h3><p>CiA 434 包含通用定义和多个设备子类。公开资料中列出的内容包括：</p><ul><li>CiA 434-1：general definitions；</li><li>CiA 434-2：dilutors、dispensers、pump units；</li><li>CiA 434-3：heating、cooling、shaking units；</li><li>其他部分还可包括 pipette control units、multi axis control units、drive units、washing units、centrifuge units、stacker units、detection units。</li></ul><p>CiA 434 定义 LAU 在 LAC 视角下的 FSA。状态转换可由内部设备事件触发，例如设备错误或本地输入，也可由 LAC 发出的 FSA command 触发。无错误设备可能处于 idle、configuration、interrupted 或 command processor active 等状态。</p><h3 id="10-3-核心与重点"><a href="#10-3-核心与重点" class="headerlink" title="10.3 核心与重点"></a>10.3 核心与重点</h3><ul><li><strong>核心是 LAC&#x2F;LAU 主从模型与实验室单元状态机</strong>。</li><li>单元不是只暴露简单 I&#x2F;O，而是具备任务执行、批处理、配置和中断恢复能力。</li><li>CiA 434 支持单个 operating command，也支持预配置 work plan 这类 quasi-autonomous 操作。</li><li>实验室系统常涉及泵、移液、加热、冷却、振荡、检测、堆栈、离心、多轴运动等多种单元。</li><li>与 402&#x2F;406&#x2F;401&#x2F;445&#x2F;461 组合时，需要明确各自状态机层级：LAU 的任务状态机不能直接等同于驱动器 FSA 或 RFID 命令状态。</li></ul><h3 id="10-4-组合关系"><a href="#10-4-组合关系" class="headerlink" title="10.4 组合关系"></a>10.4 组合关系</h3><table><thead><tr><th>组合对象</th><th>结论</th><th>风险</th></tr></thead><tbody><tr><td>CiA 434 + CiA 301</td><td>必然依赖</td><td>CiA 434 设备使用 CiA 301 通信技术。</td></tr><tr><td>CiA 434 + CiA 302</td><td>可结合</td><td>复杂配置、程序&#x2F;工作计划管理可使用附加应用层功能。</td></tr><tr><td>CiA 434 + CiA 402</td><td>可组合</td><td>多轴、泵或驱动单元可能需要运动控制，但状态机要分层。</td></tr><tr><td>CiA 434 + CiA 445</td><td>可组合</td><td>RFID 可做样本、耗材、托盘、工位识别。</td></tr><tr><td>CiA 434 + CiA 461</td><td>可组合</td><td>称重单元可作为实验流程中的测量步骤。</td></tr></tbody></table><h2 id="11-8-个重点规范的组合矩阵"><a href="#11-8-个重点规范的组合矩阵" class="headerlink" title="11. 8 个重点规范的组合矩阵"></a>11. 8 个重点规范的组合矩阵</h2><p><img src="https://i-blog.csdnimg.cn/direct/9bca6232338a4fe780ed7a8f508db545.png" alt="在这里插入图片描述"></p><h2 id="12-设备-Profile-设计关注点"><a href="#12-设备-Profile-设计关注点" class="headerlink" title="12. 设备 Profile 设计关注点"></a>12. 设备 Profile 设计关注点</h2><p><img src="https://i-blog.csdnimg.cn/direct/506f6be398c747f8bb751bb5fcafb69b.png" alt="在这里插入图片描述"></p><p>设备 Profile 之间能否组合，主要不是看编号，而是看以下资源是否冲突：</p><table><thead><tr><th>冲突资源</th><th>典型问题</th><th>处理方式</th></tr></thead><tbody><tr><td>Node-ID</td><td>两个节点或逻辑设备使用相同 Node-ID</td><td>统一分配 Node-ID，必要时使用 LSS</td></tr><tr><td>COB-ID &#x2F; CAN-ID</td><td>默认 PDO、EMCY、Heartbeat 或自定义 CAN-ID 冲突</td><td>重新规划 COB-ID 和 PDO 参数</td></tr><tr><td>对象字典 index</td><td>多个 Profile 或厂商对象使用同一 index</td><td>按正式 Profile 选择对象，厂商对象放入合适区间</td></tr><tr><td>PDO 映射</td><td>控制周期、数据长度、同步方式不一致</td><td>明确 TPDO&#x2F;RPDO 的角色、周期、触发类型和映射对象</td></tr><tr><td>状态机</td><td>402 FSA、434 LAU FSA、bootloader mode、sleep FSA 互相混淆</td><td>分层定义状态机，不把横向功能当设备动作状态</td></tr><tr><td>安全边界</td><td>用普通 PDO 实现安全停机或安全互锁</td><td>安全功能必须按安全通信和认证要求处理</td></tr><tr><td>电源状态</td><td>睡眠导致 Heartbeat 消失、过程数据无效</td><td>区分计划睡眠和异常掉线，定义 PM 策略</td></tr><tr><td>固件升级</td><td>升级中断、升级后参数不兼容</td><td>Bootloader 具备恢复机制，保留&#x2F;迁移关键参数</td></tr></tbody></table><h2 id="13-组合建议总结"><a href="#13-组合建议总结" class="headerlink" title="13. 组合建议总结"></a>13. 组合建议总结</h2><table><thead><tr><th>场景</th><th>推荐组合</th><th>注意事项</th></tr></thead><tbody><tr><td>通用 I&#x2F;O 模块</td><td>CiA 301 + CiA 401</td><td>关注数字&#x2F;模拟 I&#x2F;O 默认 PDO 和故障输出行为</td></tr><tr><td>伺服&#x2F;步进&#x2F;变频器</td><td>CiA 301 + CiA 402</td><td>关注 FSA、Controlword&#x2F;Statusword、操作模式和 PDO 周期</td></tr><tr><td>外部编码器反馈</td><td>CiA 301 + CiA 406</td><td>关注同步、数据新鲜度、分辨率和预置值</td></tr><tr><td>RFID 工位识别</td><td>CiA 301 + CiA 445 + 可选 CiA 401</td><td>I&#x2F;O 触发与 RFID 读写事件要去抖和去重</td></tr><tr><td>称重&#x2F;计量</td><td>CiA 301 + CiA 461</td><td>先确认规范当前状态；关注标定、稳定值、HMI&#x2F;indicator</td></tr><tr><td>实验室自动化</td><td>CiA 301 + CiA 434 + 可选 402&#x2F;445&#x2F;461</td><td>LAC&#x2F;LAU 状态机与底层设备状态机要分层</td></tr><tr><td>低功耗系统</td><td>CiA 301 + CiA 320 + 设备 Profile</td><td>睡眠&#x2F;唤醒要与 NMT、Heartbeat、EMCY 一致</td></tr><tr><td>可升级设备</td><td>CiA 301&#x2F;1301 + CiA 710 + 设备 Profile</td><td>升级期间禁止危险动作和睡眠，保证中断恢复</td></tr></tbody></table><h2 id="14-资料来源"><a href="#14-资料来源" class="headerlink" title="14. 资料来源"></a>14. 资料来源</h2><ol><li>CAN in Automation, CANopen CC overview: <a href="https://www.can-cia.org/can-knowledge/canopen">https://www.can-cia.org/can-knowledge/canopen</a></li><li>CAN in Automation, CiA 401: Generic I&#x2F;O device profile: <a href="https://www.can-cia.org/can-knowledge/cia-401-series-i/o-device-profile">https://www.can-cia.org/can-knowledge/cia-401-series-i/o-device-profile</a></li><li>CAN in Automation, CiA 402: Drives and motion control: <a href="https://www.can-cia.org/can-knowledge/cia-402-series-canopen-device-profile-for-drives-and-motion-control">https://www.can-cia.org/can-knowledge/cia-402-series-canopen-device-profile-for-drives-and-motion-control</a></li><li>CAN in Automation, CiA 406: Encoder profile: <a href="https://www.can-cia.org/can-knowledge/cia-406-encoder-profile">https://www.can-cia.org/can-knowledge/cia-406-encoder-profile</a></li><li>CAN in Automation, CiA 445: RFID device profile: <a href="https://www.can-cia.org/can-knowledge/cia-445-canopen-device-profile-for-rfid-devices">https://www.can-cia.org/can-knowledge/cia-445-canopen-device-profile-for-rfid-devices</a></li><li>CAN in Automation, CiA 434: Laboratory automation systems: <a href="https://www.can-cia.org/can-knowledge/cia-434-canopen-device-profile-for-laboratory-automation-systems">https://www.can-cia.org/can-knowledge/cia-434-canopen-device-profile-for-laboratory-automation-systems</a></li><li>CAN in Automation, CiA 320: Sleep and wake-up handling: <a href="https://www.can-cia.org/can-knowledge/cia-320-canopen-services-and-protocols-for-sleep-and-wake-up-handling">https://www.can-cia.org/can-knowledge/cia-320-canopen-services-and-protocols-for-sleep-and-wake-up-handling</a></li><li>CAN in Automation, Technical documents: <a href="https://www.can-cia.org/cia-groups/technical-documents">https://www.can-cia.org/cia-groups/technical-documents</a></li><li>CAN in Automation, Generic device profiles: <a href="https://www.can-cia.org/can-knowledge/generic-device-profiles">https://www.can-cia.org/can-knowledge/generic-device-profiles</a></li></ol>]]></content>
    
    
      
      
    <summary type="html">&lt;meta name=&quot;referrer&quot; content=&quot;no-referrer&quot; /&gt;

&lt;h1 id=&quot;CANopen-CiA-协议全景：重点规范、核心内容与组合关系&quot;&gt;&lt;a href=&quot;#CANopen-CiA-协议全景：重点规范、核心内容与组合关系&quot; class=&quot;h</summary>
      
    
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/categories/canopen/"/>
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/tags/canopen/"/>
    
  </entry>
  
  <entry>
    <title>CANopenEditor 从新建工程到导出 EDS  OD</title>
    <link href="https://wdfk-prog.space/posts/ca8bdea3/"/>
    <id>https://wdfk-prog.space/posts/ca8bdea3/</id>
    <published>2026-06-30T00:45:30.000Z</published>
    <updated>2026-07-07T00:26:47.635Z</updated>
    
    <content type="html"><![CDATA[<meta name="referrer" content="no-referrer" /><h1 id="CANopenEditor-从新建工程到导出-EDS-OD-c-OD-h"><a href="#CANopenEditor-从新建工程到导出-EDS-OD-c-OD-h" class="headerlink" title="CANopenEditor 从新建工程到导出 EDS &#x2F; OD.c &#x2F; OD.h"></a>CANopenEditor 从新建工程到导出 EDS &#x2F; OD.c &#x2F; OD.h</h1><p><img src="https://i-blog.csdnimg.cn/direct/2849518e91c74e44a98019560d60e9ac.png" alt="在这里插入图片描述"></p><h2 id="从新建工程开始"><a href="#从新建工程开始" class="headerlink" title="从新建工程开始"></a>从新建工程开始</h2><h3 id="先新建空工程"><a href="#先新建空工程" class="headerlink" title="先新建空工程"></a>先新建空工程</h3><p>执行：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">File -&gt; New</span><br></pre></td></tr></table></figure><p>此时左侧显示 <strong>New Product</strong>，Object Dictionary 里还没有完整的 CiA 301 通信对象。不要直接在空工程里逐个手写 <code>0x1000</code>、<code>0x1018</code>、<code>0x1200</code>、<code>0x1800</code>、<code>0x1A00</code> 等标准对象，容易漏子索引、数据类型、访问权限和 CANopenNode 导出属性。</p><h3 id="插入-DS301-profile"><a href="#插入-DS301-profile" class="headerlink" title="插入 DS301 profile"></a>插入 DS301 profile</h3><p>执行：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Insert Profile -&gt; DS301_profile.xpd</span><br></pre></td></tr></table></figure><p><img src="https://i-blog.csdnimg.cn/direct/3a1e99dae87b475da09163522602eafb.png" alt="在这里插入图片描述"></p><p>这里的含义是：</p><table><thead><tr><th>项目</th><th>含义</th></tr></thead><tbody><tr><td><code>DS301_profile.xpd</code></td><td>CiA 301 通信对象 profile，提供标准通信对象候选项</td></tr><tr><td><code>DS301_profile_old.xpd</code></td><td>旧版本 profile，除非兼容旧工程，否则不优先使用</td></tr><tr><td><code>DS401_profile.xpd</code></td><td>I&#x2F;O 类设备 profile，只有做对应设备类型时再插入</td></tr><tr><td><code>DSP302-NMTMaster.xpd</code></td><td>NMT Master 相关 profile，只有设备要作为管理节点时再插入</td></tr></tbody></table><p>这一步仍然是“从新建开始”：工程先通过 <strong>File -&gt; New</strong> 创建，再把 profile 中的标准对象插入当前空工程；不要用 <strong>File -&gt; Open</strong> 打开现成模板工程。</p><h2 id="File-菜单功能"><a href="#File-菜单功能" class="headerlink" title="File 菜单功能"></a>File 菜单功能</h2><p><img src="https://i-blog.csdnimg.cn/direct/2ed3e3955c6d48789aa510ece1d80664.png" alt="在这里插入图片描述"></p><table><thead><tr><th>菜单项</th><th>作用</th><th>建议用法</th></tr></thead><tbody><tr><td><strong>New</strong></td><td>新建空工程</td><td>第一入口</td></tr><tr><td><strong>Open</strong></td><td>打开已有工程文件</td><td>只用于继续编辑已有 <code>.xdd</code> &#x2F; <code>.xpd</code></td></tr><tr><td><strong>Save Project</strong></td><td>保存当前工程</td><td>修改后及时保存</td></tr><tr><td><strong>Save Project As…</strong></td><td>另存为新工程</td><td>新产品第一次保存时使用</td></tr><tr><td><strong>Open Network XML</strong></td><td>打开网络级 XML</td><td>多节点、多网络工程才用</td></tr><tr><td><strong>Save Network XML</strong></td><td>保存网络级 XML</td><td>多节点、多网络工程才用</td></tr><tr><td><strong>Recent Files</strong></td><td>最近文件</td><td>快速打开历史工程</td></tr><tr><td><strong>Export…</strong></td><td>导出 EDS &#x2F; XDD &#x2F; 文档等</td><td>导出给主站或配置工具的 EDS</td></tr><tr><td><strong>Export CanOpenNode…</strong></td><td>导出 CANopenNode C 文件</td><td>生成 <code>OD.c</code> &#x2F; <code>OD.h</code></td></tr><tr><td><strong>Save All</strong></td><td>保存全部打开文件</td><td>修改多个 profile &#x2F; 工程时使用</td></tr><tr><td><strong>Close file</strong></td><td>关闭当前文件</td><td>切换工程时使用</td></tr><tr><td><strong>Quit</strong></td><td>退出工具</td><td>退出前先保存</td></tr></tbody></table><h2 id="Reports-菜单功能"><a href="#Reports-菜单功能" class="headerlink" title="Reports 菜单功能"></a>Reports 菜单功能</h2><p><img src="https://i-blog.csdnimg.cn/direct/cf05c4a395b947f1ad54ab0a383910c2.png" alt="在这里插入图片描述"></p><table><thead><tr><th>菜单项</th><th>作用</th><th>使用场景</th></tr></thead><tbody><tr><td><strong>Documentation</strong></td><td>生成对象字典相关文档</td><td>需要把当前 OD 配置导出给同事检查时使用</td></tr><tr><td><strong>Network PDO</strong></td><td>生成网络 PDO 视图&#x2F;报告</td><td>检查多个节点的 PDO COB-ID 和映射关系时使用</td></tr></tbody></table><p>普通单节点学习工程可以先不用 Reports。配置完成后，再用 <strong>Documentation</strong> 导出说明文档，或用 <strong>Network PDO</strong> 检查 PDO 通道是否冲突。</p><h2 id="Preferences-里选择导出器"><a href="#Preferences-里选择导出器" class="headerlink" title="Preferences 里选择导出器"></a>Preferences 里选择导出器</h2><p>执行：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Tools -&gt; Preferences</span><br></pre></td></tr></table></figure><p><img src="https://i-blog.csdnimg.cn/direct/24d364d3a13149538feb307bfd21429f.png" alt="在这里插入图片描述"></p><p>重点字段：</p><table><thead><tr><th>字段</th><th>作用</th><th>建议</th></tr></thead><tbody><tr><td><strong>Selected exporter</strong></td><td>选择 CANopenNode C 文件导出格式</td><td>新工程优先选 <code>CANOPENNODE_V4</code></td></tr><tr><td><strong>Show Generic Warnings</strong></td><td>显示通用警告</td><td>保持勾选</td></tr><tr><td><strong>Show Rename Warnings</strong></td><td>显示重命名警告</td><td>保持勾选</td></tr><tr><td><strong>Show Build Warnings</strong></td><td>显示生成警告</td><td>保持勾选</td></tr><tr><td><strong>Show String Warnings</strong></td><td>显示字符串警告</td><td>保持勾选</td></tr><tr><td><strong>Show Struct Warnings</strong></td><td>显示结构体警告</td><td>保持勾选</td></tr></tbody></table><p>不建议关闭这些警告。关闭后可能会漏掉对象未启用、名称冲突、字符串长度不足、结构体生成异常等问题。</p><h2 id="Device-Info-页面怎么填"><a href="#Device-Info-页面怎么填" class="headerlink" title="Device Info 页面怎么填"></a>Device Info 页面怎么填</h2><p><img src="https://i-blog.csdnimg.cn/direct/052d36fa445543aeb4b37438782a0503.png" alt="在这里插入图片描述"></p><h3 id="Device-Info-区域"><a href="#Device-Info-区域" class="headerlink" title="Device Info 区域"></a>Device Info 区域</h3><table><thead><tr><th>字段</th><th>作用</th><th>建议配置</th><th>不建议配置</th></tr></thead><tbody><tr><td><strong>Product name</strong></td><td>产品名称，写入设备描述文件</td><td>填清晰的产品名，例如 <code>Demo IO Slave</code></td><td>不要长期保持 <code>New Product</code></td></tr><tr><td><strong>Product ID</strong></td><td>产品编号 &#x2F; 产品代码</td><td>使用公司内部产品码，通常与 <code>0x1018:02</code> 逻辑一致</td><td>不要每次随意改</td></tr><tr><td><strong>Vendor name</strong></td><td>厂商名称</td><td>填公司或项目名称</td><td>不要空着交付给主站侧</td></tr><tr><td><strong>Vendor ID</strong></td><td>厂商 ID</td><td>有正式 CiA Vendor-ID 时填正式值；学习阶段可填测试值</td><td>量产设备不要使用 <code>0x00000000</code></td></tr></tbody></table><h3 id="File-Info-区域"><a href="#File-Info-区域" class="headerlink" title="File Info 区域"></a>File Info 区域</h3><table><thead><tr><th>字段</th><th>作用</th><th>建议配置</th><th>不建议配置</th></tr></thead><tbody><tr><td><strong>File version</strong></td><td>EDS&#x2F;XDD 文件版本</td><td>每次发布递增</td><td>不要与固件版本混用</td></tr><tr><td><strong>Description</strong></td><td>文件说明</td><td>写设备用途和关键能力</td><td>不要写过长实现细节</td></tr><tr><td><strong>Creation Date&#x2F;Time</strong></td><td>创建时间</td><td>工具自动生成</td><td>不要手动伪造</td></tr><tr><td><strong>Created By</strong></td><td>创建者</td><td>可填团队或人员</td><td>不影响 OD 生成</td></tr><tr><td><strong>Modification Date&#x2F;Time</strong></td><td>修改时间</td><td>工具自动维护</td><td>不建议手动改</td></tr><tr><td><strong>Modified by</strong></td><td>修改者</td><td>可填维护者</td><td>不影响 OD 生成</td></tr></tbody></table><h3 id="Project-Info-区域"><a href="#Project-Info-区域" class="headerlink" title="Project Info 区域"></a>Project Info 区域</h3><table><thead><tr><th>字段</th><th>作用</th><th>说明</th></tr></thead><tbody><tr><td><strong>Project file (version)</strong></td><td>当前工程文件</td><td>保存 <code>.xdd</code> 后显示路径&#x2F;版本</td></tr><tr><td><strong>XDD v1.1 file - stripped</strong></td><td>裁剪后的 XDD</td><td>通常由工具生成，不手工改</td></tr><tr><td><strong>EDS file</strong></td><td>导出的 EDS 路径</td><td><strong>File -&gt; Export…</strong> 后出现</td></tr><tr><td><strong>DCF file</strong></td><td>设备配置文件路径</td><td>给具体节点配置实际参数时使用</td></tr><tr><td><strong>CANopenNode file (ver)</strong></td><td>导出的 CANopenNode 文件路径&#x2F;版本</td><td><strong>File -&gt; Export CanOpenNode…</strong> 后出现</td></tr><tr><td><strong>Documentation file</strong></td><td>导出的说明文档路径</td><td><strong>Reports -&gt; Documentation</strong> 后出现</td></tr></tbody></table><p><strong>固件构建只需要 <code>OD.c / OD.h</code>；这些文件主要给工具、主站、文档或调试用。</strong></p><table><thead><tr><th>文件</th><th>作用</th><th>RTT demo 是否必须</th></tr></thead><tbody><tr><td><code>project.eds</code></td><td>给 CANopen 主站&#x2F;调试工具识别设备对象字典</td><td>建议保留</td></tr><tr><td><code>project.dcf</code></td><td>某个具体节点的配置文件，包含实际 Node-ID、PDO、参数值</td><td>可选</td></tr><tr><td><code>project.md</code></td><td>对象字典说明文档，给人看</td><td>可选</td></tr><tr><td><code>project.json</code></td><td>工具导出的 JSON 格式 OD 描述，方便脚本&#x2F;工具处理</td><td>可选</td></tr><tr><td><code>project.binpb</code></td><td>二进制 protobuf 格式 OD&#x2F;项目信息，主要给工具链使用</td><td>可选</td></tr></tbody></table><p>最重要的是：</p><figure class="highlight text"><figcaption><span>id</span></figcaption><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">固件编译：</span><br><span class="line">  OD.c</span><br><span class="line">  OD.h</span><br><span class="line"></span><br><span class="line">给主站使用：</span><br><span class="line">  project.eds</span><br><span class="line"></span><br><span class="line">给具体节点配置：</span><br><span class="line">  project.dcf</span><br><span class="line"></span><br><span class="line">给人看/检查：</span><br><span class="line">  project.md</span><br><span class="line"></span><br><span class="line">给自动化工具处理：</span><br><span class="line">  project.json</span><br><span class="line">  project.binpb</span><br></pre></td></tr></table></figure><h3 id="DCF-是什么"><a href="#DCF-是什么" class="headerlink" title="DCF 是什么"></a>DCF 是什么</h3><p><code>DCF</code> 是 <strong>Device Configuration File</strong>。</p><p>区别：</p><table><thead><tr><th>文件</th><th>用途</th></tr></thead><tbody><tr><td>EDS</td><td>描述设备“支持什么对象、默认值是什么”</td></tr><tr><td>DCF</td><td>描述某个具体节点“实际配置成什么值”</td></tr></tbody></table><p>例如同一型号设备有 3 台：</p><table><thead><tr><th>节点</th><th align="right">Node-ID</th><th align="right">TPDO1 COB-ID</th><th align="right">心跳时间</th></tr></thead><tbody><tr><td>设备 A</td><td align="right">1</td><td align="right"><code>0x181</code></td><td align="right"><code>1000 ms</code></td></tr><tr><td>设备 B</td><td align="right">2</td><td align="right"><code>0x182</code></td><td align="right"><code>1000 ms</code></td></tr><tr><td>设备 C</td><td align="right">3</td><td align="right"><code>0x183</code></td><td align="right"><code>500 ms</code></td></tr></tbody></table><p>这类“具体节点实际值”适合放在 DCF 或网络配置里。学习生成 <code>OD.c</code> &#x2F; <code>OD.h</code> 时，可以暂时不使用 DCF。</p><h3 id="Baudrates-区域"><a href="#Baudrates-区域" class="headerlink" title="Baudrates 区域"></a>Baudrates 区域</h3><table><thead><tr><th>字段</th><th>作用</th><th>建议</th></tr></thead><tbody><tr><td><strong>10 kbps ~ 1000 kbps</strong></td><td>声明设备支持的 CAN bit rate</td><td>勾选固件实际支持的速率</td></tr><tr><td><strong>auto</strong></td><td>声明支持自动波特率</td><td>固件没有实现自动检测时不要勾选</td></tr></tbody></table><p>只勾选固件实际能配置和验证过的速率。EDS 写支持但固件不支持，会造成主站配置后无法通信。</p><h3 id="General-and-Master-Features-区域"><a href="#General-and-Master-Features-区域" class="headerlink" title="General and Master Features 区域"></a>General and Master Features 区域</h3><table><thead><tr><th>字段</th><th>作用</th><th>建议</th></tr></thead><tbody><tr><td><strong>Granularity</strong></td><td>PDO 映射粒度，常见为 8 bit</td><td>普通经典 CANopen 保持 <code>8</code></td></tr><tr><td><strong>RPDO count</strong></td><td>当前 RPDO 数量</td><td>由对象字典 &#x2F; PDO 页面联动，不手填</td></tr><tr><td><strong>TPDO count</strong></td><td>当前 TPDO 数量</td><td>由对象字典 &#x2F; PDO 页面联动，不手填</td></tr><tr><td><strong>LSS Slave</strong></td><td>设备支持被 LSS 配置 Node-ID &#x2F; bit rate</td><td>固件实现 LSS slave 时再勾选</td></tr><tr><td><strong>LSS Master</strong></td><td>设备支持作为 LSS 配置主节点</td><td>普通从站不要勾选</td></tr><tr><td><strong>Node guarding Slave</strong></td><td>支持被主站 Node guarding</td><td>新项目通常优先用 heartbeat</td></tr><tr><td><strong>Node guarding Master</strong></td><td>支持监控其他节点</td><td>只有设备作为管理节点时勾选</td></tr><tr><td><strong>No of monitored node</strong></td><td>被监控节点数量</td><td>未启用相关 master 功能时灰色是正常的</td></tr></tbody></table><p><code>No of monitored node</code> 灰色通常不是异常。它不是普通手填对象字段，而是受 <strong>Node guarding Master</strong> 或监控节点配置联动。普通从站不需要改。若要做节点监控，应先明确使用 heartbeat consumer 还是 node guarding master，再去对象字典中配置对应对象。</p><h3 id="Device-commissioning-区域"><a href="#Device-commissioning-区域" class="headerlink" title="Device commissioning 区域"></a>Device commissioning 区域</h3><table><thead><tr><th>字段</th><th>作用</th><th>建议</th></tr></thead><tbody><tr><td><strong>Concrete node ID</strong></td><td>具体节点 ID</td><td>做 DCF &#x2F; 网络配置时填写，普通 EDS 可先保持 0</td></tr><tr><td><strong>Node name</strong></td><td>具体节点名称</td><td>多节点网络中用于区分设备</td></tr><tr><td><strong>Baudrate</strong></td><td>具体节点 bit rate</td><td>DCF 实际配置值</td></tr><tr><td><strong>Net number</strong></td><td>网络编号</td><td>多条 CANopen 网络时用于区分</td></tr><tr><td><strong>Net name</strong></td><td>网络名称</td><td>多条 CANopen 网络时用于标识</td></tr><tr><td><strong>CANopen Manager</strong></td><td>标识该节点是否为管理节点</td><td>普通从站不要勾选</td></tr><tr><td><strong>LSS Serial no</strong></td><td>LSS 识别用序列号</td><td>不使用 LSS 时可先不管</td></tr></tbody></table><p>“多网络配置”指一个工程同时描述多条 CANopen 总线，例如 <code>CANopen_NET_1</code> 和 <code>CANopen_NET_2</code>。普通单 CAN 总线工程不需要配置 <code>Net number</code> &#x2F; <code>Net name</code>。</p><h2 id="Object-Dictionary-页面怎么看"><a href="#Object-Dictionary-页面怎么看" class="headerlink" title="Object Dictionary 页面怎么看"></a>Object Dictionary 页面怎么看</h2><p>对象字典按索引范围分区：</p><table><thead><tr><th>区域</th><th align="right">索引范围</th><th>作用</th></tr></thead><tbody><tr><td>Communication Specific Parameters</td><td align="right"><code>0x1000~0x1FFF</code></td><td>CANopen 通信对象，例如 SDO、PDO、heartbeat、identity</td></tr><tr><td>Manufacturer Specific Parameters</td><td align="right"><code>0x2000~0x5FFF</code></td><td>厂商自定义对象，建议放应用过程变量</td></tr><tr><td>Device Profile Specific Parameters</td><td align="right"><code>0x6000~0x9FFF</code></td><td>设备 profile 规定的对象</td></tr></tbody></table><p>普通自定义变量优先放到 <code>0x2000~0x5FFF</code>，例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">0x2100:00  status_value      UNSIGNED16  ro  TPDO</span><br><span class="line">0x2101:00  command_value     UNSIGNED16  rw  RPDO</span><br><span class="line">0x2102:00  measured_value    INTEGER32   ro  TPDO</span><br><span class="line">0x2103:00  setpoint_value    INTEGER32   rw  RPDO</span><br></pre></td></tr></table></figure><h2 id="Object-settings-每个字段的作用"><a href="#Object-settings-每个字段的作用" class="headerlink" title="Object settings 每个字段的作用"></a>Object settings 每个字段的作用</h2><p><img src="https://i-blog.csdnimg.cn/direct/8e95e09283c7406c901088ebec21d0a0.png" alt="在这里插入图片描述"></p><table><thead><tr><th>字段</th><th>可选项 &#x2F; 格式</th><th>作用</th><th>建议配置</th><th>不允许或不建议配置</th></tr></thead><tbody><tr><td><strong>Index</strong></td><td><code>0x0000~0xFFFF</code></td><td>对象字典索引</td><td>标准对象按 CiA 301；自定义对象用 <code>0x2000~0x5FFF</code></td><td>不要占用保留区；不要和标准对象冲突</td></tr><tr><td><strong>Sub Index</strong></td><td><code>0x00~0xFF</code></td><td>子索引</td><td><code>VAR</code> 通常只有 <code>0x00</code>；<code>ARRAY/RECORD</code> 需要多个子索引</td><td>不要遗漏 <code>0x00</code> 最大子索引</td></tr><tr><td><strong>Name</strong></td><td>字符串</td><td>对象名，影响生成代码命名</td><td>使用英文、无歧义、少空格</td><td>不要用重复名、特殊符号、中文变量名</td></tr><tr><td><strong>Denotation</strong></td><td>字符串</td><td>标识&#x2F;简称</td><td>可空</td><td>不要依赖它生成关键逻辑</td></tr><tr><td><strong>Description</strong></td><td>字符串</td><td>描述对象用途</td><td>对应用对象写清单位和方向</td><td>不要把实现代码细节写进去</td></tr><tr><td><strong>Object Type</strong></td><td><code>VAR</code> &#x2F; <code>ARRAY</code> &#x2F; <code>RECORD</code> &#x2F; 其他类型定义项</td><td>决定对象结构</td><td>单变量用 <code>VAR</code>；同类型数组用 <code>ARRAY</code>；多字段记录用 <code>RECORD</code></td><td>不要把多字段对象硬做成多个无关 <code>VAR</code>，也不要把单变量做成 <code>RECORD</code></td></tr><tr><td><strong>Data Type</strong></td><td><code>BOOLEAN</code>、<code>INTEGER8/16/32</code>、<code>UNSIGNED8/16/32</code>、<code>VISIBLE_STRING</code> 等</td><td>决定数据长度和编码</td><td>与固件变量类型严格一致</td><td>不要把 16 位变量配置成 32 位；不要把字符串当整数</td></tr><tr><td><strong>Access SDO</strong></td><td><code>no</code> &#x2F; <code>ro</code> &#x2F; <code>wo</code> &#x2F; <code>rw</code></td><td>SDO 访问权限</td><td>状态量 <code>ro</code>；命令&#x2F;配置量 <code>rw</code>；内部量 <code>no</code></td><td>不要把只读状态配成 <code>rw</code>；不要把必须配置的对象设成 <code>no</code></td></tr><tr><td><strong>Access PDO</strong></td><td><code>no</code> &#x2F; <code>t</code> &#x2F; <code>r</code> &#x2F; <code>tr</code></td><td>PDO 映射方向</td><td>发送到总线的状态量用 <code>t</code>；从总线接收的命令量用 <code>r</code></td><td>不要把 SDO 通信参数映射进 PDO；不要把同一控制量随意设 <code>tr</code></td></tr><tr><td><strong>Access SRDO</strong></td><td><code>no</code> &#x2F; <code>tx</code> &#x2F; <code>rx</code> &#x2F; <code>trx</code></td><td>安全相关数据对象访问</td><td>不使用 SRDO 时保持 <code>no</code></td><td>未实现安全通信时不要启用</td></tr><tr><td><strong>Default value</strong></td><td>十进制或十六进制</td><td>默认值，生成到 OD 数据</td><td>配置对象填合理默认值</td><td>不要填超出数据类型范围的值</td></tr><tr><td><strong>HighLimit</strong></td><td>数值</td><td>上限</td><td>需要 EDS 限制时填写</td><td>不要与固件实际范围矛盾</td></tr><tr><td><strong>LowLimit</strong></td><td>数值</td><td>下限</td><td>需要 EDS 限制时填写</td><td>不要上限小于下限</td></tr><tr><td><strong>Actual Value</strong></td><td>数值</td><td>具体配置值，偏 DCF</td><td>做具体节点配置时使用</td><td>不要把它当成通用默认值</td></tr><tr><td><strong>String Len Min</strong></td><td>数值</td><td>字符串最小长度</td><td><code>VISIBLE_STRING</code> 需要保留空间时填写</td><td>非字符串对象不要乱填</td></tr><tr><td><strong>Count Label</strong></td><td>字符串，例如 <code>NMT</code>、<code>SDO_SRV</code>、<code>RPDO</code>、<code>TPDO</code></td><td>CANopenNode 导出计数宏</td><td>通信对象按类型设置；普通自定义对象可留空或统一标签</td><td>不要给不同类别乱用同一标签</td></tr><tr><td><strong>Storage Group</strong></td><td><code>RAM</code> &#x2F; <code>PERSIST_COMM</code> &#x2F; 其他工程定义组</td><td>决定变量所属存储组</td><td>运行时变量用 <code>RAM</code>；需要保存的通信参数用 <code>PERSIST_COMM</code></td><td>不需要掉电保存的实时变量不要放持久组</td></tr><tr><td><strong>Enabled</strong></td><td>勾选 &#x2F; 不勾选</td><td>是否参与导出</td><td>要生成到 EDS &#x2F; <code>OD.c</code> &#x2F; <code>OD.h</code> 的对象必须勾选</td><td>不要以为列表里存在就一定会导出</td></tr><tr><td><strong>Save Changes</strong></td><td>按钮</td><td>保存当前对象修改</td><td>修改字段后点击</td><td>不保存就切换页面可能丢修改</td></tr><tr><td><strong>Autosave changes</strong></td><td>勾选 &#x2F; 不勾选</td><td>自动保存字段变化</td><td>熟悉后可打开</td><td>初学阶段建议手动保存，便于确认修改点</td></tr></tbody></table><h3 id="Object-Type-怎么选"><a href="#Object-Type-怎么选" class="headerlink" title="Object Type 怎么选"></a>Object Type 怎么选</h3><table><thead><tr><th>Object Type</th><th>用途</th><th>示例</th></tr></thead><tbody><tr><td><code>VAR</code></td><td>单个变量</td><td><code>0x2100:00 status_value</code></td></tr><tr><td><code>ARRAY</code></td><td>多个同类型子项</td><td>多个相同类型的通道值</td></tr><tr><td><code>RECORD</code></td><td>多个不同含义子项</td><td><code>0x1018 Identity</code>、PDO 通信参数</td></tr><tr><td><code>DOMAIN</code></td><td>大块数据</td><td>文件、固件块、EDS 存储等</td></tr><tr><td><code>DEFTYPE</code> &#x2F; <code>DEFSTRUCT</code></td><td>类型定义</td><td>profile 内部类型定义，通常不手动新建</td></tr></tbody></table><h3 id="Data-Type-常用选择"><a href="#Data-Type-常用选择" class="headerlink" title="Data Type 常用选择"></a>Data Type 常用选择</h3><table><thead><tr><th>Data Type</th><th align="right">长度</th><th>C 侧常见类型</th><th>用途</th></tr></thead><tbody><tr><td><code>BOOLEAN</code></td><td align="right">1 bit &#x2F; 工具按类型处理</td><td><code>bool_t</code></td><td>开关量</td></tr><tr><td><code>INTEGER8</code></td><td align="right">8 bit</td><td><code>int8_t</code></td><td>有符号 8 位量</td></tr><tr><td><code>INTEGER16</code></td><td align="right">16 bit</td><td><code>int16_t</code></td><td>有符号 16 位量</td></tr><tr><td><code>INTEGER32</code></td><td align="right">32 bit</td><td><code>int32_t</code></td><td>有符号 32 位量</td></tr><tr><td><code>UNSIGNED8</code></td><td align="right">8 bit</td><td><code>uint8_t</code></td><td>状态、枚举、小计数</td></tr><tr><td><code>UNSIGNED16</code></td><td align="right">16 bit</td><td><code>uint16_t</code></td><td>状态字、配置值</td></tr><tr><td><code>UNSIGNED32</code></td><td align="right">32 bit</td><td><code>uint32_t</code></td><td>计数、时间、COB-ID、映射项</td></tr><tr><td><code>VISIBLE_STRING</code></td><td align="right">可变</td><td><code>char[]</code></td><td>设备名、版本号</td></tr><tr><td><code>DOMAIN</code></td><td align="right">可变</td><td>自定义缓冲区</td><td>大块数据传输</td></tr></tbody></table><h3 id="Access-PDO-的方向"><a href="#Access-PDO-的方向" class="headerlink" title="Access PDO 的方向"></a>Access PDO 的方向</h3><table><thead><tr><th>Access PDO</th><th>含义</th><th>出现位置</th></tr></thead><tbody><tr><td><code>no</code></td><td>不允许 PDO 映射</td><td>配置对象、SDO 参数、内部对象</td></tr><tr><td><code>t</code></td><td>允许映射到 TPDO</td><td>设备发送到总线的数据</td></tr><tr><td><code>r</code></td><td>允许映射到 RPDO</td><td>设备从总线接收的数据</td></tr><tr><td><code>tr</code></td><td>同时允许 TPDO&#x2F;RPDO</td><td>少数双向变量，谨慎使用</td></tr></tbody></table><h2 id="为什么有灰色对象或灰色字段"><a href="#为什么有灰色对象或灰色字段" class="headerlink" title="为什么有灰色对象或灰色字段"></a>为什么有灰色对象或灰色字段</h2><p>灰色一般表示：</p><ol><li>profile 里有这个标准对象，但当前没有启用；</li><li>当前对象由其他页面或 profile 管理；</li><li>当前字段由对象类型、功能勾选或子对象数量联动；</li><li>该字段只在 DCF &#x2F; 网络配置场景可编辑；</li><li>当前对象没有勾选 <strong>Enabled</strong>，不会导出到 CANopenNode 文件。</li></ol><p>判断一个对象是否真正导出，不要只看颜色，按下面顺序检查：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">对象是否插入当前 OD</span><br><span class="line">    ↓</span><br><span class="line">Enabled 是否勾选</span><br><span class="line">    ↓</span><br><span class="line">Access / Data Type / Storage Group 是否正确</span><br><span class="line">    ↓</span><br><span class="line">导出后 OD.c / OD.h 里是否存在该对象</span><br><span class="line">    ↓</span><br><span class="line">EDS 重新打开后是否存在该对象</span><br></pre></td></tr></table></figure><h2 id="Insert-Profile-对象列表怎么选"><a href="#Insert-Profile-对象列表怎么选" class="headerlink" title="Insert Profile 对象列表怎么选"></a>Insert Profile 对象列表怎么选</h2><p>插入 profile 后会出现对象选择列表。</p><p><img src="https://i-blog.csdnimg.cn/direct/74e08a35cbfa4cd08af073b3f3520c2a.png" alt="在这里插入图片描述"></p><table><thead><tr><th>列</th><th>含义</th></tr></thead><tbody><tr><td><strong>Insert</strong></td><td>是否把该对象插入当前工程</td></tr><tr><td><strong>Original Object</strong></td><td>profile 中的原始对象</td></tr><tr><td><strong>Offset 0</strong></td><td>插入到当前工程后的目标索引；Offset 为 0 时通常与原索引一致</td></tr></tbody></table><p>不要一次性把所有对象都插入。初学普通从站建议先插入最小集合：</p><table><thead><tr><th align="right">对象</th><th>是否建议插入</th><th>原因</th></tr></thead><tbody><tr><td align="right"><code>0x1000</code></td><td>是</td><td>Device type，基础对象</td></tr><tr><td align="right"><code>0x1001</code></td><td>是</td><td>Error register，基础错误状态</td></tr><tr><td align="right"><code>0x1003</code></td><td>可选</td><td>Pre-defined error field，需要错误历史时启用</td></tr><tr><td align="right"><code>0x1017</code></td><td>是</td><td>Producer heartbeat time，建议使用 heartbeat</td></tr><tr><td align="right"><code>0x1018</code></td><td>是</td><td>Identity，设备身份对象</td></tr><tr><td align="right"><code>0x1200</code></td><td>是</td><td>SDO server parameter，通常需要 SDO</td></tr><tr><td align="right"><code>0x1400</code></td><td>需要 RPDO 时插入</td><td>RPDO1 通信参数</td></tr><tr><td align="right"><code>0x1600</code></td><td>需要 RPDO 时插入</td><td>RPDO1 映射参数</td></tr><tr><td align="right"><code>0x1800</code></td><td>需要 TPDO 时插入</td><td>TPDO1 通信参数</td></tr><tr><td align="right"><code>0x1A00</code></td><td>需要 TPDO 时插入</td><td>TPDO1 映射参数</td></tr></tbody></table><h2 id="插入对象列表中每个-index-的作用和配置边界"><a href="#插入对象列表中每个-index-的作用和配置边界" class="headerlink" title="插入对象列表中每个 index 的作用和配置边界"></a>插入对象列表中每个 index 的作用和配置边界</h2><table><thead><tr><th align="right">Index</th><th>名称</th><th>作用</th><th>怎么配置</th><th>不允许或不建议怎么配置</th></tr></thead><tbody><tr><td align="right"><code>0x1000</code></td><td>Device type</td><td>设备类型</td><td><code>UNSIGNED32</code>、<code>ro</code>，按设备类别填写</td><td>不要设为 <code>rw</code>；不要填随机值</td></tr><tr><td align="right"><code>0x1001</code></td><td>Error register</td><td>标准错误寄存器</td><td><code>UNSIGNED8</code>、<code>ro</code>、可 TPDO 映射</td><td>不要让主站通过 SDO 写</td></tr><tr><td align="right"><code>0x1002</code></td><td>Manufacturer status register</td><td>厂商状态寄存器</td><td>有厂商状态定义时启用，<code>UNSIGNED32</code>、<code>ro</code></td><td>没定义状态位时不要启用凑数</td></tr><tr><td align="right"><code>0x1003</code></td><td>Pre-defined error field</td><td>错误历史</td><td>需要 EMCY&#x2F;error history 时启用，<code>ARRAY</code></td><td>不要把它映射到 PDO；不要手动乱写历史</td></tr><tr><td align="right"><code>0x1005</code></td><td>COB-ID SYNC message</td><td>SYNC COB-ID</td><td>需要同步通信时启用，默认常见 <code>0x80</code></td><td>不使用 SYNC 时不要随意启用生产者</td></tr><tr><td align="right"><code>0x1006</code></td><td>Communication cycle period</td><td>SYNC 周期</td><td>使用同步周期时配置，单位按规范</td><td>异步 PDO 调试阶段可不启用</td></tr><tr><td align="right"><code>0x1007</code></td><td>Synchronous window length</td><td>同步窗口</td><td>严格同步应用才配置</td><td>不理解同步窗口时不要启用</td></tr><tr><td align="right"><code>0x1008</code></td><td>Manufacturer device name</td><td>设备名</td><td><code>VISIBLE_STRING</code>、<code>const</code>&#x2F;<code>ro</code></td><td>不要放运行时变化值</td></tr><tr><td align="right"><code>0x1009</code></td><td>Manufacturer hardware version</td><td>硬件版本</td><td><code>VISIBLE_STRING</code>、<code>const</code>&#x2F;<code>ro</code></td><td>不要每次上电变化</td></tr><tr><td align="right"><code>0x100A</code></td><td>Manufacturer software version</td><td>软件版本</td><td><code>VISIBLE_STRING</code>、<code>const</code>&#x2F;<code>ro</code></td><td>不要与 EDS file version 混淆</td></tr><tr><td align="right"><code>0x100C</code></td><td>Guard time</td><td>Node guarding 时间</td><td>只有使用 Node guarding 时配置</td><td>新项目若只用 heartbeat，不要启用</td></tr><tr><td align="right"><code>0x100D</code></td><td>Life time factor</td><td>Node guarding 生命因子</td><td>与 <code>0x100C</code> 配套</td><td>不要单独启用</td></tr><tr><td align="right"><code>0x1010</code></td><td>Store parameters</td><td>保存参数</td><td>支持 SDO 保存参数时启用</td><td>固件未实现保存逻辑时不要启用</td></tr><tr><td align="right"><code>0x1011</code></td><td>Restore default parameters</td><td>恢复默认参数</td><td>支持恢复默认参数时启用</td><td>固件未实现恢复逻辑时不要启用</td></tr><tr><td align="right"><code>0x1012</code></td><td>COB-ID time stamp object</td><td>TIME 对象 COB-ID</td><td>使用 TIME 对象时启用</td><td>普通从站不需要</td></tr><tr><td align="right"><code>0x1013</code></td><td>High resolution time stamp</td><td>高分辨率时间戳</td><td>需要时间戳时启用</td><td>不要当普通计数器使用</td></tr><tr><td align="right"><code>0x1014</code></td><td>COB-ID EMCY</td><td>EMCY COB-ID</td><td>使用 Emergency 时启用，常见为 <code>0x80 + Node-ID</code></td><td>不要与其他 CAN-ID 冲突</td></tr><tr><td align="right"><code>0x1015</code></td><td>Inhibit time EMCY</td><td>EMCY 抑制时间</td><td>EMCY 可能频繁触发时配置</td><td>不要设过小导致总线刷屏</td></tr><tr><td align="right"><code>0x1016</code></td><td>Consumer heartbeat time</td><td>消费者心跳超时</td><td>设备需要监控其他节点时启用</td><td>普通只被监控的从站可不启用</td></tr><tr><td align="right"><code>0x1017</code></td><td>Producer heartbeat time</td><td>生产者心跳周期</td><td>建议启用，例如 <code>1000 ms</code></td><td>不要设为 0 后又期望设备发心跳</td></tr><tr><td align="right"><code>0x1018</code></td><td>Identity</td><td>设备身份</td><td>配 <code>Vendor-ID/Product code/Revision/Serial</code></td><td>量产设备不要所有序列号相同</td></tr><tr><td align="right"><code>0x1019</code></td><td>Synchronous counter overflow value</td><td>SYNC 计数溢出值</td><td>使用带计数 SYNC 时配置</td><td>不使用 SYNC 计数时不要启用</td></tr><tr><td align="right"><code>0x1020</code></td><td>Verify configuration</td><td>配置校验</td><td>需要校验配置一致性时启用</td><td>固件不检查时不要写成必需能力</td></tr><tr><td align="right"><code>0x1021</code></td><td>Store EDS</td><td>存储 EDS</td><td>设备支持从对象字典读取 EDS 时启用</td><td>普通嵌入式从站通常不启用</td></tr><tr><td align="right"><code>0x1022</code></td><td>Store format</td><td>存储格式</td><td>与 <code>0x1021</code> 配套</td><td>不要单独启用</td></tr><tr><td align="right"><code>0x1023</code></td><td>OS command</td><td>OS 命令</td><td>有命令通道时启用</td><td>普通设备不要开放无实现接口</td></tr><tr><td align="right"><code>0x1024</code></td><td>OS command mode</td><td>OS 命令模式</td><td>与 <code>0x1023</code> 配套</td><td>不要单独启用</td></tr><tr><td align="right"><code>0x1025</code></td><td>OS debugger interface</td><td>OS 调试接口</td><td>有调试协议时启用</td><td>不要在量产设备暴露无保护调试接口</td></tr><tr><td align="right"><code>0x1026</code></td><td>OS prompt</td><td>OS 提示符</td><td>与 OS 命令相关</td><td>普通从站不启用</td></tr><tr><td align="right"><code>0x1027</code></td><td>Module list</td><td>模块列表</td><td>模块化设备才启用</td><td>固定功能设备不要启用</td></tr><tr><td align="right"><code>0x1028</code></td><td>Emergency consumer object</td><td>EMCY 消费对象</td><td>需要接收其他节点 EMCY 时启用</td><td>普通从站通常不启用</td></tr><tr><td align="right"><code>0x1029</code></td><td>Error behavior object</td><td>错误行为</td><td>定义通信错误后的状态行为</td><td>未实现状态处理时不要启用</td></tr><tr><td align="right"><code>0x1200</code></td><td>SDO server parameter</td><td>SDO 服务器参数</td><td>普通从站通常启用 1 组</td><td>不要映射到 PDO</td></tr><tr><td align="right"><code>0x1201</code></td><td>SDO server parameter</td><td>第 2 组 SDO server</td><td>确实需要多个 SDO server 才启用</td><td>不要为了“多通道”随意启用</td></tr><tr><td align="right"><code>0x1280</code></td><td>SDO client parameter</td><td>SDO 客户端参数</td><td>设备要主动访问其他节点时启用</td><td>普通从站不要启用</td></tr><tr><td align="right"><code>0x1400</code></td><td>RPDO communication parameter</td><td>RPDO1 通信参数</td><td>需要接收 PDO 时启用</td><td>不要与 TPDO COB-ID 冲突</td></tr><tr><td align="right"><code>0x1401</code></td><td>RPDO communication parameter</td><td>RPDO2 通信参数</td><td>RPDO1 不够用再启用</td><td>不要空启用</td></tr><tr><td align="right"><code>0x1402</code></td><td>RPDO communication parameter</td><td>RPDO3 通信参数</td><td>需要第三组 RPDO 时启用</td><td>不要空启用</td></tr><tr><td align="right"><code>0x1403</code></td><td>RPDO communication parameter</td><td>RPDO4 通信参数</td><td>需要第四组 RPDO 时启用</td><td>不要空启用</td></tr><tr><td align="right"><code>0x1404</code></td><td>RPDO communication parameter</td><td>RPDO5 通信参数</td><td>超过 4 组时才启用</td><td>不要误以为必须启满</td></tr><tr><td align="right"><code>0x1600</code></td><td>RPDO mapping parameter</td><td>RPDO1 映射参数</td><td>与 <code>0x1400</code> 成对启用</td><td>不要映射只读对象</td></tr><tr><td align="right"><code>0x1601</code></td><td>RPDO mapping parameter</td><td>RPDO2 映射参数</td><td>与 <code>0x1401</code> 成对启用</td><td>不要超过 64 bit</td></tr><tr><td align="right"><code>0x1602</code></td><td>RPDO mapping parameter</td><td>RPDO3 映射参数</td><td>与 <code>0x1402</code> 成对启用</td><td>不要映射 SDO 参数</td></tr><tr><td align="right"><code>0x1603</code></td><td>RPDO mapping parameter</td><td>RPDO4 映射参数</td><td>与 <code>0x1403</code> 成对启用</td><td>不要留无效映射项</td></tr><tr><td align="right"><code>0x1604</code></td><td>RPDO mapping parameter</td><td>RPDO5 映射参数</td><td>与 <code>0x1404</code> 成对启用</td><td>不要只启映射不启通信参数</td></tr><tr><td align="right"><code>0x1800</code></td><td>TPDO communication parameter</td><td>TPDO1 通信参数</td><td>需要发送 PDO 时启用</td><td>不要与 RPDO COB-ID 冲突</td></tr><tr><td align="right"><code>0x1801</code></td><td>TPDO communication parameter</td><td>TPDO2 通信参数</td><td>TPDO1 不够用再启用</td><td>不要空启用</td></tr><tr><td align="right"><code>0x1802</code></td><td>TPDO communication parameter</td><td>TPDO3 通信参数</td><td>需要第三组 TPDO 时启用</td><td>不要误选为对象数据</td></tr><tr><td align="right"><code>0x1803</code></td><td>TPDO communication parameter</td><td>TPDO4 通信参数</td><td>需要第四组 TPDO 时启用</td><td>不要空启用</td></tr><tr><td align="right"><code>0x1804</code></td><td>TPDO communication parameter</td><td>TPDO5 通信参数</td><td>超过 4 组时才启用</td><td>不要误以为必须启满</td></tr><tr><td align="right"><code>0x1A00</code></td><td>TPDO mapping parameter</td><td>TPDO1 映射参数</td><td>与 <code>0x1800</code> 成对启用</td><td>不要映射写入型命令量</td></tr><tr><td align="right"><code>0x1A01</code></td><td>TPDO mapping parameter</td><td>TPDO2 映射参数</td><td>与 <code>0x1801</code> 成对启用</td><td>不要超过 64 bit</td></tr><tr><td align="right"><code>0x1A02</code></td><td>TPDO mapping parameter</td><td>TPDO3 映射参数</td><td>与 <code>0x1802</code> 成对启用</td><td>不要映射 SDO 参数</td></tr><tr><td align="right"><code>0x1A03</code></td><td>TPDO mapping parameter</td><td>TPDO4 映射参数</td><td>与 <code>0x1803</code> 成对启用</td><td>不要只启映射不启通信参数</td></tr></tbody></table><h3 id="0x1000-Device-type-设备类型"><a href="#0x1000-Device-type-设备类型" class="headerlink" title="0x1000 | Device type | 设备类型"></a><code>0x1000</code> | Device type | 设备类型</h3><p>可以填的值看设备类型：</p><table><thead><tr><th>设备类型</th><th align="right"><code>0x1000</code> 建议值</th></tr></thead><tbody><tr><td>通用 demo &#x2F; 只测 DS301 通信</td><td align="right"><code>0x00000000</code></td></tr><tr><td>CiA 401 I&#x2F;O 设备</td><td align="right"><code>0x00000191</code></td></tr><tr><td>CiA 402 电机驱动</td><td align="right"><code>0x00000192</code></td></tr><tr><td>CiA 406 编码器</td><td align="right"><code>0x00000196</code></td></tr></tbody></table><p>规则：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">0x1000 低 16 bit = Device Profile Number</span><br><span class="line">0x1000 高 16 bit = profile-specific 信息</span><br></pre></td></tr></table></figure><h3 id="0x1001-Error-register-标准错误寄存器"><a href="#0x1001-Error-register-标准错误寄存器" class="headerlink" title="0x1001 | Error register | 标准错误寄存器"></a><code>0x1001</code> | Error register | 标准错误寄存器</h3><p><code>0x1001</code> 建议填：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">Default value = 0x00</span><br><span class="line">Data Type     = UNSIGNED8</span><br><span class="line">Access SDO    = ro</span><br><span class="line">Access PDO    = t 或 no</span><br><span class="line">Storage       = RAM</span><br></pre></td></tr></table></figure><p>demo 里直接用 <code>0x00</code> 就行，表示当前无错误。</p><p>常见 bit 含义：</p><table><thead><tr><th align="right">bit</th><th>含义</th></tr></thead><tbody><tr><td align="right">0</td><td>Generic error</td></tr><tr><td align="right">1</td><td>Current error</td></tr><tr><td align="right">2</td><td>Voltage error</td></tr><tr><td align="right">3</td><td>Temperature error</td></tr><tr><td align="right">4</td><td>Communication error</td></tr><tr><td align="right">5</td><td>Device profile specific</td></tr><tr><td align="right">6</td><td>Reserved</td></tr><tr><td align="right">7</td><td>Manufacturer specific</td></tr></tbody></table><p>不要在 EDS 默认值里填错误值；错误状态应由运行时 EMCY &#x2F; error 逻辑更新。</p><h3 id="0x1002-Manufacturer-status-register-厂商状态寄存器"><a href="#0x1002-Manufacturer-status-register-厂商状态寄存器" class="headerlink" title="0x1002 | Manufacturer status register | 厂商状态寄存器"></a><code>0x1002</code> | Manufacturer status register | 厂商状态寄存器</h3><p><code>0x1002</code> 是<strong>厂商自定义状态寄存器</strong>，类型固定建议：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">UNSIGNED32</span><br><span class="line">Access SDO = ro</span><br><span class="line">Access PDO = t 或 no</span><br><span class="line">Default = 0x00000000</span><br></pre></td></tr></table></figure><p>demo 如果要启用，可以这样定义：</p><table><thead><tr><th align="right">bit</th><th>含义</th></tr></thead><tbody><tr><td align="right">bit0</td><td>application initialized</td></tr><tr><td align="right">bit1</td><td>CAN driver error</td></tr><tr><td align="right">bit2</td><td>OD&#x2F;application error</td></tr><tr><td align="right">bit3</td><td>PDO data updated</td></tr><tr><td align="right">bit4~31</td><td>reserved</td></tr></tbody></table><p>默认值：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">0x00000000</span><br></pre></td></tr></table></figure><p>运行时示例：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">0x00000001  // app 已初始化</span><br><span class="line">0x00000003  // app 已初始化 + CAN driver error</span><br></pre></td></tr></table></figure><p>如果现在只是最小 demo，<strong>建议先不启用 <code>0x1002</code></strong>。没有明确状态位定义时，它只是凑数对象。</p><h3 id="0x1003-Pre-defined-error-field-错误历史"><a href="#0x1003-Pre-defined-error-field-错误历史" class="headerlink" title="0x1003 | Pre-defined error field | 错误历史"></a><code>0x1003</code> | Pre-defined error field | 错误历史</h3><p><code>0x1003</code> 可以先不启用。</p><p>如果要启用，demo 建议填成“错误历史数组”：</p><table><thead><tr><th align="right">子索引</th><th>含义</th><th>类型</th><th>权限</th><th align="right">默认值</th></tr></thead><tbody><tr><td align="right"><code>0x1003:00</code></td><td>当前错误数量</td><td><code>UNSIGNED8</code></td><td><code>rw</code></td><td align="right"><code>0</code></td></tr><tr><td align="right"><code>0x1003:01</code></td><td>最新错误记录</td><td><code>UNSIGNED32</code></td><td><code>ro</code></td><td align="right"><code>0</code></td></tr><tr><td align="right"><code>0x1003:02</code></td><td>次新错误记录</td><td><code>UNSIGNED32</code></td><td><code>ro</code></td><td align="right"><code>0</code></td></tr><tr><td align="right"><code>0x1003:03~08</code></td><td>更多历史错误</td><td><code>UNSIGNED32</code></td><td><code>ro</code></td><td align="right"><code>0</code></td></tr></tbody></table><p>建议 demo 用 8 条即可：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">0x1003:00 = 8   # 最大错误记录数量/最大子索引</span><br><span class="line">0x1003:01~08 = 0</span><br></pre></td></tr></table></figure><p>注意：<code>0x1003</code> 不要映射 PDO。它只给 SDO 读取错误历史用；写 <code>0x1003:00 = 0</code> 通常用于清空错误历史。</p><h3 id="0x1005-COB-ID-SYNC-message-SYNC-COB-ID"><a href="#0x1005-COB-ID-SYNC-message-SYNC-COB-ID" class="headerlink" title="0x1005 | COB-ID SYNC message | SYNC COB-ID"></a><code>0x1005</code> | COB-ID SYNC message | SYNC COB-ID</h3><p><code>0x1005</code> 的作用是定义 SYNC 报文的 COB-ID，也就是 CAN-ID。</p><table><thead><tr><th>角色</th><th><code>0x1005</code> 作用</th></tr></thead><tbody><tr><td>SYNC 生产者</td><td>决定自己用哪个 CAN-ID 发送 SYNC</td></tr><tr><td>SYNC 消费者</td><td>决定接收哪个 CAN-ID 的 SYNC</td></tr><tr><td>不使用 SYNC</td><td>基本没作用，可以不启用</td></tr></tbody></table><p>所以：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">0x1005 = 0x00000080</span><br></pre></td></tr></table></figure><p>表示：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">本节点不生产 SYNC</span><br><span class="line">但认为 CAN-ID 0x80 是 SYNC 报文</span><br></pre></td></tr></table></figure><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">0x1005 = 0x40000080</span><br></pre></td></tr></table></figure><p>表示：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">本节点生产 SYNC</span><br><span class="line">并用 CAN-ID 0x80 发送</span><br></pre></td></tr></table></figure><p>demo 如果不用同步 PDO，<code>0x1005</code> 可以不启用。如果想保留 SYNC 消费能力，填 <code>0x00000080</code> 就行。</p><h3 id="0x1006-Communication-cycle-period-SYNC-周期"><a href="#0x1006-Communication-cycle-period-SYNC-周期" class="headerlink" title="0x1006 | Communication cycle period | SYNC 周期"></a><code>0x1006</code> | Communication cycle period | SYNC 周期</h3><p><code>0x1006</code> 填 <strong>SYNC 周期</strong>，单位通常是 <strong>微秒 us</strong>。</p><p>demo 如果不做同步 PDO：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">不启用 0x1006</span><br></pre></td></tr></table></figure><p>如果想测试 SYNC，可以填：</p><table><thead><tr><th>周期</th><th align="right"><code>0x1006</code> 值</th></tr></thead><tbody><tr><td>10 ms</td><td align="right"><code>10000</code></td></tr><tr><td>100 ms</td><td align="right"><code>100000</code></td></tr><tr><td>1 s</td><td align="right"><code>1000000</code></td></tr></tbody></table><p>建议 demo 先用：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">0x1006 = 100000   # 100 ms</span><br></pre></td></tr></table></figure><p>但前提是也启用了 <code>0x1005</code>，并且系统里确实有人发送 SYNC。否则 <code>0x1006</code> 没实际意义。</p><h3 id="0x1007-Synchronous-window-length-同步窗口"><a href="#0x1007-Synchronous-window-length-同步窗口" class="headerlink" title="0x1007 | Synchronous window length | 同步窗口"></a><code>0x1007</code> | Synchronous window length | 同步窗口</h3><p><code>0x1007</code> 建议 demo <strong>不启用</strong>。</p><p>如果必须填：</p><table><thead><tr><th>场景</th><th align="right">建议值</th></tr></thead><tbody><tr><td>异步 PDO demo</td><td align="right"><code>0</code></td></tr><tr><td>不使用 SYNC</td><td align="right"><code>0</code></td></tr><tr><td>使用 SYNC，周期 10 ms</td><td align="right"><code>1000 ~ 5000</code></td></tr><tr><td>使用 SYNC，周期 1 ms</td><td align="right"><code>100 ~ 500</code></td></tr></tbody></table><p>含义：<code>0x1007</code> 是 <strong>Synchronous window length</strong>，单位通常是 <strong>μs</strong>。它必须小于 <code>0x1006</code> 的 SYNC 周期。</p><p>例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">0x1006 = 10000   ; 10 ms SYNC 周期</span><br><span class="line">0x1007 = 5000    ; SYNC 后 5 ms 窗口</span><br></pre></td></tr></table></figure><p>demo 推荐：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">0x1007 = 0</span><br></pre></td></tr></table></figure><p>或者直接不启用。</p><h3 id="0x1010-Store-parameters-保存参数"><a href="#0x1010-Store-parameters-保存参数" class="headerlink" title="0x1010 | Store parameters | 保存参数"></a><code>0x1010</code> | Store parameters | 保存参数</h3><p>结论：demo <strong>建议先不要启用 <code>0x1010</code></strong>。除非你已经实现了参数保存到 Flash &#x2F; filesystem &#x2F; KV 的逻辑。</p><p>如果只是想先保留对象用于演示，可以这样填：</p><table><thead><tr><th align="right">Sub</th><th>名称</th><th>类型</th><th>访问</th><th align="right">默认值</th><th>建议</th></tr></thead><tbody><tr><td align="right"><code>0x00</code></td><td>Highest sub-index supported</td><td><code>UNSIGNED8</code></td><td><code>ro</code></td><td align="right"><code>4</code></td><td>有 4 个保存项就填 <code>4</code></td></tr><tr><td align="right"><code>0x01</code></td><td>Save all parameters</td><td><code>UNSIGNED32</code></td><td><code>rw</code></td><td align="right"><code>0x00000001</code> 或 <code>0</code></td><td>不实现保存时不建议启</td></tr><tr><td align="right"><code>0x02</code></td><td>Save communication parameters</td><td><code>UNSIGNED32</code></td><td><code>rw</code></td><td align="right"><code>0x00000001</code> 或 <code>0</code></td><td>不实现保存时不建议启</td></tr><tr><td align="right"><code>0x03</code></td><td>Save application parameters</td><td><code>UNSIGNED32</code></td><td><code>rw</code></td><td align="right"><code>0x00000001</code> 或 <code>0</code></td><td>demo 可不启</td></tr><tr><td align="right"><code>0x04</code></td><td>Save manufacturer defined parameters</td><td><code>UNSIGNED32</code></td><td><code>rw</code></td><td align="right"><code>0x00000001</code> 或 <code>0</code></td><td>demo 可不启</td></tr></tbody></table><p><code>0x1010</code> 的写入触发值是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">0x65766173</span><br></pre></td></tr></table></figure><p>也就是 ASCII：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">&quot;save&quot;</span><br></pre></td></tr></table></figure><p>主站写 <code>0x65766173</code> 到 <code>0x1010:01</code> &#x2F; <code>:02</code> &#x2F; <code>:03</code> &#x2F; <code>:04</code>，设备才执行保存。</p><h3 id="0x1012-TIME-时间戳"><a href="#0x1012-TIME-时间戳" class="headerlink" title="0x1012 | TIME | 时间戳"></a><code>0x1012</code> | TIME | 时间戳</h3><table><thead><tr><th>用途</th><th align="right">值</th></tr></thead><tbody><tr><td>不使用 TIME</td><td align="right">不启用 <code>0x1012</code></td></tr><tr><td>只消费 TIME，CAN-ID <code>0x100</code></td><td align="right"><code>0x80000100</code></td></tr><tr><td>只生产 TIME，CAN-ID <code>0x100</code></td><td align="right"><code>0x40000100</code></td></tr><tr><td>生产 + 消费 TIME，CAN-ID <code>0x100</code></td><td align="right"><code>0xC0000100</code></td></tr></tbody></table><p>建议 demo 不配它：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">0x1012: disabled</span><br></pre></td></tr></table></figure><p>如果以后要让设备接收主站时间戳，再用：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">0x1012 = 0x80000100</span><br></pre></td></tr></table></figure><p>注意：同一条 CANopen 网络里通常只应有一个 TIME producer。</p><h3 id="0x1015-EMCY-抑制时间-错误抑制时间"><a href="#0x1015-EMCY-抑制时间-错误抑制时间" class="headerlink" title="0x1015 | EMCY 抑制时间 | 错误抑制时间"></a><code>0x1015</code> | EMCY 抑制时间 | 错误抑制时间</h3><p><code>0x1015 EMCY 抑制时间</code> 抑制的是 <strong>Emergency 报文的连续发送频率</strong>。</p><p>也就是：设备发生多个错误时，不能无限快速地连续发 EMCY，必须至少间隔 <code>0x1015</code> 指定的时间。</p><p>例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">0x1015 = 0      不抑制，错误触发就可立即发 EMCY</span><br><span class="line">0x1015 = 100    两帧 EMCY 至少间隔 10 ms</span><br></pre></td></tr></table></figure><p>它<strong>不抑制错误本身</strong>，也<strong>不阻止错误记录</strong>，只是限制 EMCY CAN 报文不要刷屏。</p><p>demo 简单用：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">0x1015 = 0</span><br></pre></td></tr></table></figure><p>后面如果错误很多、EMCY 太频繁，再设 <code>100</code>。</p><h3 id="0x1016-Consumer-heartbeat-time-消费者心跳超时"><a href="#0x1016-Consumer-heartbeat-time-消费者心跳超时" class="headerlink" title="0x1016 | Consumer heartbeat time | 消费者心跳超时"></a><code>0x1016</code> | Consumer heartbeat time | 消费者心跳超时</h3><p>：<strong>demo 普通从站不监控其他节点时，<code>0x1016</code> 可以不启用；如果已默认有 8 个子项，全部填 <code>0x00000000</code> 即可。</strong></p><table><thead><tr><th align="right">子项</th><th align="right">建议值</th><th>含义</th></tr></thead><tbody><tr><td align="right"><code>0x1016:00</code></td><td align="right"><code>8</code></td><td>支持 8 个 monitored node</td></tr><tr><td align="right"><code>0x1016:01</code></td><td align="right"><code>0x00000000</code></td><td>未使用</td></tr><tr><td align="right"><code>0x1016:02</code></td><td align="right"><code>0x00000000</code></td><td>未使用</td></tr><tr><td align="right"><code>0x1016:03~08</code></td><td align="right"><code>0x00000000</code></td><td>未使用</td></tr></tbody></table><p>如果要监控节点，比如监控 <strong>Node-ID &#x3D; 1</strong>，对方 <code>0x1017 = 1000 ms</code>，建议填：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">0x1016:01 = 0x000105DC</span><br></pre></td></tr></table></figure><p>计算方式：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">(Node ID &lt;&lt; 16) | timeout_ms</span><br><span class="line">(1 &lt;&lt; 16) | 1500 = 0x000105DC</span><br></pre></td></tr></table></figure><p>常用例子：</p><table><thead><tr><th align="right">监控节点</th><th align="right">超时时间</th><th align="right">填充值</th></tr></thead><tbody><tr><td align="right">Node 1</td><td align="right">1500 ms</td><td align="right"><code>0x000105DC</code></td></tr><tr><td align="right">Node 1</td><td align="right">2000 ms</td><td align="right"><code>0x000107D0</code></td></tr><tr><td align="right">Node 2</td><td align="right">1500 ms</td><td align="right"><code>0x000205DC</code></td></tr><tr><td align="right">Node 2</td><td align="right">2000 ms</td><td align="right"><code>0x000207D0</code></td></tr></tbody></table><p>demo 推荐：**先全部填 0，不做 heartbeat consumer。</p><h3 id="0x1019-Synchronous-counter-overflow-value-SYNC-计数溢出值"><a href="#0x1019-Synchronous-counter-overflow-value-SYNC-计数溢出值" class="headerlink" title="0x1019 | Synchronous counter overflow value | SYNC 计数溢出值"></a><code>0x1019</code> | Synchronous counter overflow value | SYNC 计数溢出值</h3><p>结论：<strong>demo 里建议不启用 <code>0x1019</code>，或填 <code>0</code>。</strong></p><table><thead><tr><th>场景</th><th align="right"><code>0x1019</code> 建议值</th></tr></thead><tbody><tr><td>不用 SYNC</td><td align="right">不启用</td></tr><tr><td>用 SYNC，但不带 counter</td><td align="right"><code>0</code></td></tr><tr><td>用带 counter 的 SYNC</td><td align="right"><code>2 ~ 240</code></td></tr><tr><td>普通 RTT demo</td><td align="right"><code>0</code> 或不启用</td></tr></tbody></table><p>推荐：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">0x1019 = 0</span><br></pre></td></tr></table></figure><p>含义：SYNC 报文数据长度为 0，不带 counter。</p><p>只有你要做这种同步计数场景时才填 <code>2~240</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">SYNC COB-ID: 0x80</span><br><span class="line">SYNC data:   1 byte counter</span><br><span class="line">counter:     1, 2, 3 ... overflow 后重新循环</span><br></pre></td></tr></table></figure><p>例如测试可以填：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">0x1019 = 10</span><br></pre></td></tr></table></figure><p>表示 SYNC counter 到 10 后循环。</p><h3 id="0x1020-Verify-configuration-配置校验"><a href="#0x1020-Verify-configuration-配置校验" class="headerlink" title="0x1020 | Verify configuration | 配置校验"></a><code>0x1020</code> | Verify configuration | 配置校验</h3><p>demo <strong>不建议启用 <code>0x1020</code></strong>。</p><p><code>0x1020 Verify configuration</code> 是给主站校验“设备配置是否匹配”的，普通 demo 用不到。</p><p>如果一定要启用，填：</p><table><thead><tr><th>子项</th><th>名称</th><th>建议值</th></tr></thead><tbody><tr><td><code>0x1020:00</code></td><td>Highest sub-index supported</td><td><code>2</code></td></tr><tr><td><code>0x1020:01</code></td><td>Configuration date</td><td><code>0x00000000</code></td></tr><tr><td><code>0x1020:02</code></td><td>Configuration time</td><td><code>0x00000000</code></td></tr></tbody></table><p>也就是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">0x1020:00 = 2</span><br><span class="line">0x1020:01 = 0</span><br><span class="line">0x1020:02 = 0</span><br></pre></td></tr></table></figure><h3 id="0x1021-store-EDS-存储-EDS"><a href="#0x1021-store-EDS-存储-EDS" class="headerlink" title="0x1021 | store EDS | 存储 EDS"></a><code>0x1021</code> | store EDS | 存储 EDS</h3><p><code>0x1021</code> 是 <strong>Store EDS</strong>，表示设备内部可通过对象字典读取 EDS 文件内容。普通嵌入式从站通常不启用。</p><p>建议：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">0x1021 Store EDS: 不启用</span><br><span class="line">0x1022 Store format: 不启用</span><br></pre></td></tr></table></figure><p>如果强行启用，<code>0x1021</code> 不是填普通数字，而是 <code>DOMAIN</code> 类型，里面应放设备内置的 EDS 内容；固件还要实现对应读取逻辑。</p><h3 id="0x1023-Store-format-存储格式"><a href="#0x1023-Store-format-存储格式" class="headerlink" title="0x1023 | Store format | 存储格式"></a><code>0x1023</code> | Store format | 存储格式</h3><p>结论：*** demo 不建议启用 <code>0x1023~0x1026</code>，全部不填、不启用即可。**</p><table><thead><tr><th align="right">Index</th><th>RTT demo 建议</th><th>原因</th></tr></thead><tbody><tr><td align="right"><code>0x1023</code> OS command</td><td>不启用</td><td>需要设备支持通过 CANopen 下发 OS 命令，demo 没必要</td></tr><tr><td align="right"><code>0x1024</code> OS command mode</td><td>不启用</td><td>依赖 <code>0x1023</code>，不能单独用</td></tr><tr><td align="right"><code>0x1025</code> OS debugger interface</td><td>不启用</td><td>属于调试接口，没实现协议就不要暴露</td></tr><tr><td align="right"><code>0x1026</code> OS prompt</td><td>不启用</td><td>和 OS command 配套，普通从站不用</td></tr></tbody></table><p><code>OS command</code> 可以理解成：主站通过对象字典给设备下发一段命令字符串或命令数据，让设备执行类似 shell&#x2F;系统命令的动作。</p><p>例如理论上可能用于：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">reboot</span><br><span class="line">get_log</span><br><span class="line">clear_error</span><br><span class="line">start_test</span><br></pre></td></tr></table></figure><p>但这要求固件里真的实现命令解析、权限控制、执行逻辑和返回机制。</p><h2 id="TX-PDO-Mapping-页面干什么"><a href="#TX-PDO-Mapping-页面干什么" class="headerlink" title="TX PDO Mapping 页面干什么"></a>TX PDO Mapping 页面干什么</h2><p><strong>TX PDO Mapping</strong> 配置“本节点发送到 CAN 总线的数据”。</p><p><img src="https://i-blog.csdnimg.cn/direct/cefa409c46c44f87ac5a3b57f3782629.png" alt="在这里插入图片描述"></p><p>页面分为三部分：</p><table><thead><tr><th>区域</th><th>作用</th></tr></thead><tbody><tr><td><strong>Available Objects for PDO</strong></td><td>当前允许映射到 PDO 的对象列表</td></tr><tr><td><strong>Communication parameters</strong></td><td>当前选中的 TPDO 通信参数</td></tr><tr><td>底部字节表</td><td>当前 TPDO 的数据布局</td></tr></tbody></table><h3 id="TPDO-通道和对象的对应关系"><a href="#TPDO-通道和对象的对应关系" class="headerlink" title="TPDO 通道和对象的对应关系"></a>TPDO 通道和对象的对应关系</h3><table><thead><tr><th align="right">TPDO 通道</th><th align="right">通信参数</th><th align="right">映射参数</th><th align="right">常用预定义 CAN-ID 基址</th></tr></thead><tbody><tr><td align="right">TPDO1</td><td align="right"><code>0x1800</code></td><td align="right"><code>0x1A00</code></td><td align="right"><code>0x180 + Node-ID</code></td></tr><tr><td align="right">TPDO2</td><td align="right"><code>0x1801</code></td><td align="right"><code>0x1A01</code></td><td align="right"><code>0x280 + Node-ID</code></td></tr><tr><td align="right">TPDO3</td><td align="right"><code>0x1802</code></td><td align="right"><code>0x1A02</code></td><td align="right"><code>0x380 + Node-ID</code></td></tr><tr><td align="right">TPDO4</td><td align="right"><code>0x1803</code></td><td align="right"><code>0x1A03</code></td><td align="right"><code>0x480 + Node-ID</code></td></tr></tbody></table><p>底部 <code>0xC0000180</code>、<code>0xC0000280</code> 这类值表示 PDO 当前禁用，因为 bit31 为 1。</p><p>常见位含义：</p><table><thead><tr><th>位</th><th>含义</th><th>典型表现</th></tr></thead><tbody><tr><td>bit31</td><td>PDO valid&#x2F;invalid 控制位</td><td><code>1</code> 表示 invalid，PDO 禁用</td></tr><tr><td>bit30</td><td>RTR 控制位</td><td><code>1</code> 通常表示不允许 RTR</td></tr><tr><td>bit29</td><td>11-bit &#x2F; 29-bit CAN-ID 控制</td><td>普通 CANopen 多用 11-bit</td></tr><tr><td>bit0<del>10 或 bit0</del>28</td><td>CAN-ID</td><td>具体报文标识符</td></tr></tbody></table><h3 id="Communication-parameters-怎么填"><a href="#Communication-parameters-怎么填" class="headerlink" title="Communication parameters 怎么填"></a>Communication parameters 怎么填</h3><p><img src="https://i-blog.csdnimg.cn/direct/ac1b085c41c74b7094b2123cc8a95f61.png" alt="在这里插入图片描述"></p><table><thead><tr><th>字段</th><th>作用</th><th>初学建议</th></tr></thead><tbody><tr><td><strong>Communication</strong></td><td>当前 TPDO 通信参数对象</td><td>TPDO1 为 <code>0x1800</code></td></tr><tr><td><strong>Mapping</strong></td><td>当前 TPDO 映射参数对象</td><td>TPDO1 为 <code>0x1A00</code></td></tr><tr><td><strong>COB</strong></td><td>TPDO 使用的 COB-ID &#x2F; CAN-ID 信息</td><td>取消 invalid 后确认最终 CAN-ID 不冲突</td></tr><tr><td><strong>Type</strong></td><td>传输类型</td><td>调试可用 <code>254</code> 或 <code>255</code>；同步应用再用 <code>1~240</code></td></tr><tr><td><strong>Inhibit</strong></td><td>抑制时间，限制最小发送间隔</td><td>调试可先 <code>0</code>，稳定后再设置</td></tr><tr><td><strong>Event Timer</strong></td><td>事件定时器，周期触发发送</td><td>调试可设 <code>100</code> 或 <code>1000</code> ms，按工具单位确认</td></tr><tr><td><strong>Sync start</strong></td><td>同步起始值</td><td>非同步 PDO 保持 <code>0</code></td></tr><tr><td><strong>Invalid</strong></td><td>是否禁用该 PDO</td><td>要启用 TPDO 时取消勾选</td></tr></tbody></table><p>不建议：</p><ul><li>不要让两个 PDO 使用相同 COB-ID。</li><li>不要把禁用的 <code>0xC000xxxx</code> 当成已经启用。</li><li>不要只改右侧字段但不点 <strong>Save</strong>。</li><li>不要把 SDO 通信参数对象映射到 TPDO 数据区。</li></ul><h3 id="为什么不能直接选其他-Byte"><a href="#为什么不能直接选其他-Byte" class="headerlink" title="为什么不能直接选其他 Byte"></a>为什么不能直接选其他 Byte</h3><p>CANopenEditor 的 PDO 映射不是随便点击 Byte0、Byte3、Byte7 填值。它按映射顺序连续打包：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">0x1A00:01 -&gt; 从 bit 0 开始</span><br><span class="line">0x1A00:02 -&gt; 接在上一项后面</span><br><span class="line">0x1A00:03 -&gt; 再继续往后排</span><br></pre></td></tr></table></figure><p>对象长度决定占用多少 bit：</p><table><thead><tr><th>数据类型</th><th align="right">位数</th><th align="right">占用</th></tr></thead><tbody><tr><td><code>UNSIGNED8</code> &#x2F; <code>INTEGER8</code></td><td align="right">8 bit</td><td align="right">1 byte</td></tr><tr><td><code>UNSIGNED16</code> &#x2F; <code>INTEGER16</code></td><td align="right">16 bit</td><td align="right">2 byte</td></tr><tr><td><code>UNSIGNED32</code> &#x2F; <code>INTEGER32</code></td><td align="right">32 bit</td><td align="right">4 byte</td></tr></tbody></table><p>所以你不能直接跳到 Byte4。必须先放前面的对象，后面的空位才会继续出现。</p><h3 id="TPDO-里可以放一个对象，也可以放多个对象"><a href="#TPDO-里可以放一个对象，也可以放多个对象" class="headerlink" title="TPDO 里可以放一个对象，也可以放多个对象"></a>TPDO 里可以放一个对象，也可以放多个对象</h3><p><img src="https://i-blog.csdnimg.cn/direct/3d576e918e9c4de28b547eee124427c2.png" alt="在这里插入图片描述"></p><p>一个 TPDO 的总长度最大是 64 bit，也就是 8 byte。</p><p>只放一个对象：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">0x1A00:00 = 1</span><br><span class="line">0x1A00:01 = 0x21000010   # 0x2100:00, 16 bit</span><br></pre></td></tr></table></figure><p>放多个对象：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">0x1A00:00 = 3</span><br><span class="line">0x1A00:01 = 0x21000010   # 0x2100:00, 16 bit</span><br><span class="line">0x1A00:02 = 0x21010010   # 0x2101:00, 16 bit</span><br><span class="line">0x1A00:03 = 0x21020020   # 0x2102:00, 32 bit</span><br></pre></td></tr></table></figure><p>布局：</p><table><thead><tr><th>Byte</th><th>内容</th></tr></thead><tbody><tr><td>Byte0~1</td><td><code>0x2100:00</code></td></tr><tr><td>Byte2~3</td><td><code>0x2101:00</code></td></tr><tr><td>Byte4~7</td><td><code>0x2102:00</code></td></tr></tbody></table><h3 id="COB-ID-不是用来选择-Byte"><a href="#COB-ID-不是用来选择-Byte" class="headerlink" title="COB-ID 不是用来选择 Byte"></a>COB-ID 不是用来选择 Byte</h3><p>正确关系是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">CAN 帧 COB-ID</span><br><span class="line">    ↓</span><br><span class="line">识别这是 TPDO1 / TPDO2 / RPDO1 / RPDO2 哪个 PDO</span><br><span class="line">    ↓</span><br><span class="line">找到对应的 Mapping 对象，例如 0x1A00</span><br><span class="line">    ↓</span><br><span class="line">按 Mapping 解释 Data[0..7]</span><br></pre></td></tr></table></figure><p>PDO 报文本身不携带对象 <code>Index/SubIndex</code>。接收方必须提前知道该 COB-ID 对应的映射规则。</p><p>错误理解：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">PDO -&gt; Index -&gt; COB -&gt; Byte</span><br></pre></td></tr></table></figure><p>正确理解：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">COB-ID -&gt; PDO 通道 -&gt; Mapping 对象 -&gt; Byte 布局</span><br></pre></td></tr></table></figure><h3 id="不要把这些对象映射到-TPDO"><a href="#不要把这些对象映射到-TPDO" class="headerlink" title="不要把这些对象映射到 TPDO"></a>不要把这些对象映射到 TPDO</h3><p>PDO Mapping 页面下拉框里可能出现 <code>0x1200</code>、<code>0x1280</code> 等对象。这些是 SDO 通信参数，不是过程数据。</p><p>不建议映射：</p><table><thead><tr><th align="right">对象</th><th>原因</th></tr></thead><tbody><tr><td align="right"><code>0x1200</code></td><td>SDO server 参数，不是过程数据</td></tr><tr><td align="right"><code>0x1201</code></td><td>SDO server 参数，不是过程数据</td></tr><tr><td align="right"><code>0x1280</code></td><td>SDO client 参数，不是过程数据</td></tr><tr><td align="right"><code>0x1800~0x1804</code></td><td>TPDO 通信参数，不是过程数据本体</td></tr><tr><td align="right"><code>0x1A00~0x1A03</code></td><td>TPDO 映射参数，不是过程数据本体</td></tr></tbody></table><p>推荐映射厂商自定义过程变量，例如：</p><table><thead><tr><th align="right">对象</th><th>方向</th><th>示例含义</th></tr></thead><tbody><tr><td align="right"><code>0x2100:00</code></td><td>TPDO</td><td>设备状态值</td></tr><tr><td align="right"><code>0x2101:00</code></td><td>TPDO</td><td>测量值</td></tr><tr><td align="right"><code>0x2102:00</code></td><td>TPDO</td><td>计数值</td></tr><tr><td align="right"><code>0x2200:00</code></td><td>RPDO</td><td>控制命令</td></tr><tr><td align="right"><code>0x2201:00</code></td><td>RPDO</td><td>目标值</td></tr></tbody></table><h2 id="配置一个通用-TPDO-示例"><a href="#配置一个通用-TPDO-示例" class="headerlink" title="配置一个通用 TPDO 示例"></a>配置一个通用 TPDO 示例</h2><p>目标：TPDO1 周期发送 3 个厂商自定义状态量。</p><h3 id="第一步：添加可-TPDO-映射对象"><a href="#第一步：添加可-TPDO-映射对象" class="headerlink" title="第一步：添加可 TPDO 映射对象"></a>第一步：添加可 TPDO 映射对象</h3><p>在 <strong>Object Dictionary -&gt; Manufacturer Specific Parameters</strong> 中新增：</p><table><thead><tr><th align="right">Index</th><th>Name</th><th>Object Type</th><th>Data Type</th><th>Access SDO</th><th>Access PDO</th><th align="right">Default</th><th>Storage</th></tr></thead><tbody><tr><td align="right"><code>0x2100</code></td><td><code>status_value</code></td><td><code>VAR</code></td><td><code>UNSIGNED16</code></td><td><code>ro</code></td><td><code>t</code></td><td align="right"><code>0</code></td><td><code>RAM</code></td></tr><tr><td align="right"><code>0x2101</code></td><td><code>measured_value</code></td><td><code>VAR</code></td><td><code>UNSIGNED16</code></td><td><code>ro</code></td><td><code>t</code></td><td align="right"><code>0</code></td><td><code>RAM</code></td></tr><tr><td align="right"><code>0x2102</code></td><td><code>counter_value</code></td><td><code>VAR</code></td><td><code>UNSIGNED32</code></td><td><code>ro</code></td><td><code>t</code></td><td align="right"><code>0</code></td><td><code>RAM</code></td></tr></tbody></table><p>保存后，回到 <strong>TX PDO Mapping</strong>，这些对象应出现在 <strong>Available Objects for PDO</strong> 和下拉框中。</p><h3 id="第二步：启用-TPDO1"><a href="#第二步：启用-TPDO1" class="headerlink" title="第二步：启用 TPDO1"></a>第二步：启用 TPDO1</h3><p>选择底部第一行 TPDO1，然后设置：</p><table><thead><tr><th>字段</th><th>值</th></tr></thead><tbody><tr><td><strong>Communication</strong></td><td><code>0x1800</code></td></tr><tr><td><strong>Mapping</strong></td><td><code>0x1A00</code></td></tr><tr><td><strong>COB</strong></td><td>按 Node-ID 确认最终值，不与其他 CAN-ID 冲突</td></tr><tr><td><strong>Type</strong></td><td><code>254</code> 或 <code>255</code></td></tr><tr><td><strong>Inhibit</strong></td><td>调试可先 <code>0</code></td></tr><tr><td><strong>Event Timer</strong></td><td>调试可先 <code>100</code> 或 <code>1000</code></td></tr><tr><td><strong>Sync start</strong></td><td><code>0</code></td></tr><tr><td><strong>Invalid</strong></td><td>取消勾选</td></tr></tbody></table><p>然后点 <strong>Save</strong>。</p><h3 id="第三步：配置-TPDO1-映射"><a href="#第三步：配置-TPDO1-映射" class="headerlink" title="第三步：配置 TPDO1 映射"></a>第三步：配置 TPDO1 映射</h3><p>按顺序选择：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">0x2100/00/status_value      16 bit</span><br><span class="line">0x2101/00/measured_value    16 bit</span><br><span class="line">0x2102/00/counter_value     32 bit</span><br></pre></td></tr></table></figure><p>总长度：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">16 + 16 + 32 = 64 bit = 8 byte</span><br></pre></td></tr></table></figure><p>等价对象字典：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">0x1A00:00 = 3</span><br><span class="line">0x1A00:01 = 0x21000010</span><br><span class="line">0x1A00:02 = 0x21010010</span><br><span class="line">0x1A00:03 = 0x21020020</span><br></pre></td></tr></table></figure><p>PDO 数据布局：</p><table><thead><tr><th>Byte</th><th>内容</th></tr></thead><tbody><tr><td>Byte0~1</td><td><code>0x2100:00 status_value</code></td></tr><tr><td>Byte2~3</td><td><code>0x2101:00 measured_value</code></td></tr><tr><td>Byte4~7</td><td><code>0x2102:00 counter_value</code></td></tr></tbody></table><h2 id="RX-PDO-Mapping-页面怎么用"><a href="#RX-PDO-Mapping-页面怎么用" class="headerlink" title="RX PDO Mapping 页面怎么用"></a>RX PDO Mapping 页面怎么用</h2><p><strong>RX PDO Mapping</strong> 配置“本节点从 CAN 总线接收的数据”。</p><p>RPDO 对象成对出现：</p><table><thead><tr><th align="right">RPDO 通道</th><th align="right">通信参数</th><th align="right">映射参数</th><th align="right">常用预定义 CAN-ID 基址</th></tr></thead><tbody><tr><td align="right">RPDO1</td><td align="right"><code>0x1400</code></td><td align="right"><code>0x1600</code></td><td align="right"><code>0x200 + Node-ID</code></td></tr><tr><td align="right">RPDO2</td><td align="right"><code>0x1401</code></td><td align="right"><code>0x1601</code></td><td align="right"><code>0x300 + Node-ID</code></td></tr><tr><td align="right">RPDO3</td><td align="right"><code>0x1402</code></td><td align="right"><code>0x1602</code></td><td align="right"><code>0x400 + Node-ID</code></td></tr><tr><td align="right">RPDO4</td><td align="right"><code>0x1403</code></td><td align="right"><code>0x1603</code></td><td align="right"><code>0x500 + Node-ID</code></td></tr></tbody></table><p>通用 RPDO 示例：</p><table><thead><tr><th align="right">Index</th><th>Name</th><th>Data Type</th><th>Access SDO</th><th>Access PDO</th></tr></thead><tbody><tr><td align="right"><code>0x2200</code></td><td><code>control_value</code></td><td><code>UNSIGNED16</code></td><td><code>rw</code></td><td><code>r</code></td></tr><tr><td align="right"><code>0x2201</code></td><td><code>target_value</code></td><td><code>INTEGER32</code></td><td><code>rw</code></td><td><code>r</code></td></tr><tr><td align="right"><code>0x2202</code></td><td><code>mode_value</code></td><td><code>UNSIGNED8</code></td><td><code>rw</code></td><td><code>r</code></td></tr></tbody></table><p>RPDO1 映射：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">0x1600:00 = 3</span><br><span class="line">0x1600:01 = 0x22000010</span><br><span class="line">0x1600:02 = 0x22010020</span><br><span class="line">0x1600:03 = 0x22020008</span><br></pre></td></tr></table></figure><p>总长度：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">16 + 32 + 8 = 56 bit</span><br></pre></td></tr></table></figure><p>剩余 8 bit 可以留空，也可以用 Dummy 填充到 64 bit，取决于主站配置和固件接收处理。</p><h2 id="Modules-页面干什么"><a href="#Modules-页面干什么" class="headerlink" title="Modules 页面干什么"></a>Modules 页面干什么</h2><p><strong>Modules</strong> 页面用于描述模块化 CANopen 设备，不是用来配置 C 代码模块，也不是用来配置 PDO。</p><p>典型使用场景：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">一个远程 I/O 耦合器</span><br><span class="line">    ├── 插槽 1：数字输入模块</span><br><span class="line">    ├── 插槽 2：数字输出模块</span><br><span class="line">    ├── 插槽 3：模拟输入模块</span><br><span class="line">    └── 插槽 4：温度采集模块</span><br></pre></td></tr></table></figure><p>这种设备的对象字典可能会随模块组合变化，所以需要在 EDS&#x2F;XDD 中描述支持哪些模块、每个模块有哪些对象。</p><p>字段含义：</p><table><thead><tr><th>字段</th><th>作用</th></tr></thead><tbody><tr><td><strong>Nr Supported Modules</strong></td><td>支持的模块数量</td></tr><tr><td><strong>List of modules</strong></td><td>支持的模块列表</td></tr><tr><td><strong>Index</strong></td><td>模块编号</td></tr><tr><td><strong>Name</strong></td><td>模块名称</td></tr><tr><td><strong>Version</strong></td><td>模块版本</td></tr><tr><td><strong>Revision</strong></td><td>模块修订</td></tr><tr><td><strong>OrderCode</strong></td><td>订货号 &#x2F; 料号</td></tr><tr><td><strong>Module comments</strong></td><td>模块说明</td></tr><tr><td><strong>Module objects</strong></td><td>模块关联的对象字典对象</td></tr></tbody></table><p>普通固定功能从站可以保持：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Nr Supported Modules = 0</span><br></pre></td></tr></table></figure><p>不需要配置 Modules 页面。</p><h2 id="导出-EDS"><a href="#导出-EDS" class="headerlink" title="导出 EDS"></a>导出 EDS</h2><p>执行：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">File -&gt; Export...</span><br></pre></td></tr></table></figure><p>选择 EDS 输出格式，生成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">my_device.eds</span><br></pre></td></tr></table></figure><p>EDS 交给主站、配置软件或测试工具使用。导出后建议重新用 CANopenEditor 打开 EDS 检查：</p><ol><li><code>0x1000</code>、<code>0x1001</code>、<code>0x1018</code> 是否存在；</li><li><code>0x1200</code> 是否存在；</li><li>启用的 <code>0x1400/0x1600</code>、<code>0x1800/0x1A00</code> 是否存在；</li><li>厂商自定义对象是否存在；</li><li>PDO 映射总长度是否不超过 64 bit。</li></ol><h2 id="导出-CANopenNode-的-OD-c-OD-h"><a href="#导出-CANopenNode-的-OD-c-OD-h" class="headerlink" title="导出 CANopenNode 的 OD.c &#x2F; OD.h"></a>导出 CANopenNode 的 OD.c &#x2F; OD.h</h2><p>先确认导出器：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Tools -&gt; Preferences -&gt; Selected exporter -&gt; CANOPENNODE_V4</span><br></pre></td></tr></table></figure><p>然后执行：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">File -&gt; Export CanOpenNode...</span><br></pre></td></tr></table></figure><p>输出通常包括：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">OD.c</span><br><span class="line">OD.h</span><br></pre></td></tr></table></figure><p>如果工程设置了对象字典后缀，也可能生成带后缀的文件名。导出后要检查固件工程：</p><ol><li><code>OD.c</code> 已加入编译；</li><li>include path 能找到 <code>OD.h</code>；</li><li>工程引用的 OD 名称与导出文件一致；</li><li><code>OD_CNT_*</code> 宏符合启用对象数量；</li><li>PDO 初始化数量与 <code>0x1400/0x1600/0x1800/0x1A00</code> 一致。</li></ol><h2 id="最小可用配置建议"><a href="#最小可用配置建议" class="headerlink" title="最小可用配置建议"></a>最小可用配置建议</h2><p>普通从站学习工程可以从下面对象开始：</p><table><thead><tr><th align="right">对象</th><th>用途</th><th>建议</th></tr></thead><tbody><tr><td align="right"><code>0x1000</code></td><td>Device type</td><td>启用</td></tr><tr><td align="right"><code>0x1001</code></td><td>Error register</td><td>启用</td></tr><tr><td align="right"><code>0x1017</code></td><td>Producer heartbeat time</td><td>启用，便于主站监控</td></tr><tr><td align="right"><code>0x1018</code></td><td>Identity</td><td>启用</td></tr><tr><td align="right"><code>0x1200</code></td><td>SDO server parameter</td><td>启用</td></tr><tr><td align="right"><code>0x2100~0x21FF</code></td><td>厂商自定义状态量</td><td>按需添加，TPDO 方向</td></tr><tr><td align="right"><code>0x2200~0x22FF</code></td><td>厂商自定义控制量</td><td>按需添加，RPDO 方向</td></tr><tr><td align="right"><code>0x1800/0x1A00</code></td><td>TPDO1</td><td>需要周期发送状态时启用</td></tr><tr><td align="right"><code>0x1400/0x1600</code></td><td>RPDO1</td><td>需要接收控制命令时启用</td></tr></tbody></table><h2 id="常见错误和处理"><a href="#常见错误和处理" class="headerlink" title="常见错误和处理"></a>常见错误和处理</h2><table><thead><tr><th>现象</th><th>原因</th><th>处理</th></tr></thead><tbody><tr><td>灰色字段不能改</td><td>受功能勾选或对象类型联动</td><td>先启用对应功能或对象</td></tr><tr><td>导出的 <code>OD.c</code> 没有对象</td><td><code>Enabled</code> 未勾选或对象未插入</td><td>勾选 Enabled 后重新导出</td></tr><tr><td>PDO 下拉框没有目标对象</td><td>对象没有设置 Access PDO</td><td>设置为 <code>t</code>、<code>r</code> 或 <code>tr</code></td></tr><tr><td>TPDO 不发送</td><td><code>Invalid</code> 仍勾选或 Event Timer 为 0</td><td>取消 Invalid，设置触发条件</td></tr><tr><td>只能选 Byte0，不能选 Byte4</td><td>PDO 映射按顺序连续排列</td><td>先放前面的对象，后续位置自动出现</td></tr><tr><td>主站无法解释 PDO 数据</td><td>主站 EDS&#x2F;DCF 与实际 Mapping 不一致</td><td>重新导出 EDS 并同步主站配置</td></tr><tr><td>映射超过 64 bit</td><td>一个 PDO 超过 8 byte</td><td>拆到下一组 PDO</td></tr><tr><td>误选 <code>0x1200</code> &#x2F; <code>0x1280</code> 到 PDO</td><td>把 SDO 参数当成过程数据</td><td>删除映射，改选过程变量</td></tr></tbody></table><h2 id="资料依据"><a href="#资料依据" class="headerlink" title="资料依据"></a>资料依据</h2><ul><li>CANopenEditor 官方仓库说明：支持导入&#x2F;导出 EDS、XDD，并导出 CANopenNode C 文件。</li><li>CANopenNode 对象字典文档：说明 XDD&#x2F;XPD&#x2F;EDS&#x2F;DCF 的关系，以及 <code>CO_disabled</code>、<code>CO_countLabel</code>、<code>CO_storageGroup</code> 等 CANopenNode 导出属性。</li><li>CiA 301 V4.2.0：定义 <code>0x1000~0x1FFF</code> 通信对象、SDO 参数、PDO 通信参数、PDO 映射参数、Identity、heartbeat、EMCY 等对象范围和用途。</li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;meta name=&quot;referrer&quot; content=&quot;no-referrer&quot; /&gt;

&lt;h1 id=&quot;CANopenEditor-从新建工程到导出-EDS-OD-c-OD-h&quot;&gt;&lt;a href=&quot;#CANopenEditor-从新建工程到导出-EDS-OD-c-OD-h</summary>
      
    
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/categories/canopen/"/>
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/tags/canopen/"/>
    
  </entry>
  
  <entry>
    <title>由于 Cortex-M 栈未满足 8 字节对齐导致的问题与原因分析</title>
    <link href="https://wdfk-prog.space/posts/e168822b/"/>
    <id>https://wdfk-prog.space/posts/e168822b/</id>
    <published>2026-06-29T03:38:10.000Z</published>
    <updated>2026-06-29T03:39:06.069Z</updated>
    
    <content type="html"><![CDATA[<meta name="referrer" content="no-referrer" /># 由于 Cortex-M 栈未满足 8 字节对齐导致的问题与原因分析<p><img src="https://i-blog.csdnimg.cn/direct/78b67cce78234d8c867be45b9a509992.png" alt="在这里插入图片描述"><br>@[toc]</p><p>嵌入式工程中，栈对齐通常不会被单独拿出来讨论。很多 Cortex-M 工程长期使用 4 字节对齐的内存访问，也能正常启动、跑任务、处理中断，因此“栈必须 8 字节对齐”看起来不像一个硬性问题。真正容易暴露问题的场景，往往出现在变参函数、64 位数据、浮点参数、启动阶段 C 代码、异常入口，以及手写汇编与编译器生成代码混用的边界上。</p><p>这篇文章从规定开始，解释为什么 Cortex-M 工程需要关心栈的 8 字节对齐；再说明不满足时可能出现什么现象；最后给出不同工具链脚本的查看方法和修改方法。</p><p><img src="https://i-blog.csdnimg.cn/direct/a7e2a2aa544e42759c0c5c05aeb513c1.png" alt="在这里插入图片描述"></p><h2 id="一、规定：公开接口处-SP-必须-8-字节对齐"><a href="#一、规定：公开接口处-SP-必须-8-字节对齐" class="headerlink" title="一、规定：公开接口处 SP 必须 8 字节对齐"></a>一、规定：公开接口处 SP 必须 8 字节对齐</h2><p>AAPCS32 是 Arm 32 位架构的过程调用标准。它规定函数之间如何传参、如何返回、哪些寄存器由调用者保存、哪些寄存器由被调用者保存，以及栈指针需要满足什么约束。</p><p>在 AAPCS32 中，栈有两层约束：</p><table><thead><tr><th>位置</th><th>要求</th><th>含义</th></tr></thead><tbody><tr><td>任意时刻</td><td><code>SP mod 4 = 0</code></td><td>栈指针至少按字对齐</td></tr><tr><td>函数公开调用边界</td><td><code>SP mod 8 = 0</code></td><td>栈指针必须按双字对齐</td></tr></tbody></table><p>这里的“函数公开调用边界”可以理解为：编译器、库函数、汇编代码、启动代码、异常入口或运行时环境之间按照 ABI 互相调用的位置。只要进入的是符合 AAPCS32 的 C&#x2F;C++ 函数，就应保证此时 SP 是 8 字节对齐的。</p><p>这条规定不是 Thumb 指令集规定的。Thumb &#x2F; Thumb-2 是 Cortex-M 执行的指令编码；AAPCS32 是 C&#x2F;C++ 函数调用的 ABI。Cortex-M 运行 Thumb 指令，但函数调用仍然按照 AAPCS32 组织。</p><h2 id="二、为什么这样规定"><a href="#二、为什么这样规定" class="headerlink" title="二、为什么这样规定"></a>二、为什么这样规定</h2><h3 id="2-1-8-字节数据需要可靠的栈上位置"><a href="#2-1-8-字节数据需要可靠的栈上位置" class="headerlink" title="2.1 8 字节数据需要可靠的栈上位置"></a>2.1 8 字节数据需要可靠的栈上位置</h3><p>C 代码中存在需要 8 字节对齐或 8 字节宽度的数据，例如：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">double</span></span><br><span class="line"><span class="type">long</span> <span class="type">long</span></span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span> <span class="type">long</span></span><br><span class="line"><span class="type">uint64_t</span></span><br><span class="line"><span class="type">int64_t</span></span><br></pre></td></tr></table></figure><p>这些类型不一定都会放到栈上。小参数可能放在寄存器里，硬浮点 ABI 下的非变参浮点参数还可能走浮点寄存器。但是一旦 8 字节对齐的数据被放到栈上，编译器就会按 ABI 假设它位于合适的双字边界。</p><p>因此，规则的核心不是“所有 <code>double</code> 都一定有问题”，而是：</p><blockquote><p>当 8 字节对齐的数据实际位于栈上时，入口 SP 的 8 字节对齐状态会影响后续栈上参数和局部对象的位置判断。</p></blockquote><h3 id="2-2-变参函数更容易暴露问题"><a href="#2-2-变参函数更容易暴露问题" class="headerlink" title="2.2 变参函数更容易暴露问题"></a>2.2 变参函数更容易暴露问题</h3><p>变参函数是典型场景。例如：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdarg.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">static</span> <span class="type">int</span> <span class="title function_">read_value</span><span class="params">(<span class="type">int</span> count, ...)</span></span><br><span class="line">&#123;</span><br><span class="line">    va_list ap;</span><br><span class="line">    va_start(ap, count);</span><br><span class="line">    <span class="type">double</span> v = va_arg(ap, <span class="type">double</span>);</span><br><span class="line">    va_end(ap);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> (<span class="type">int</span>)v;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>变参函数和普通函数不同。AAPCS32 规定，变参函数始终按 base standard 进行参数组织。即使工程使用硬浮点 ABI，变参函数也不会像普通非变参浮点函数那样简单依赖浮点寄存器传参。</p><p>当入口 SP 未满足 8 字节对齐时，编译器在编译期对“是否需要插入填充字”的判断，可能与 <code>va_arg</code> 在运行期对实际地址对齐状态的判断不一致。结果不是“所有 double 都错”，而是在满足特定条件时，<code>va_arg</code> 可能从错误地址取 8 字节数据。</p><p><img src="https://i-blog.csdnimg.cn/direct/26af3e24fa474e2886f1bac9b0cad356.png" alt="在这里插入图片描述"></p><h3 id="2-3-操作系统、RTOS-和运行时需要提供正确入口"><a href="#2-3-操作系统、RTOS-和运行时需要提供正确入口" class="headerlink" title="2.3 操作系统、RTOS 和运行时需要提供正确入口"></a>2.3 操作系统、RTOS 和运行时需要提供正确入口</h3><p>Arm 的 ABI advisory note 对这点说得更直接：操作系统、RTOS 和运行时环境在调用符合 AAPCS 的代码前，应保证 SP 是 8 字节对齐的。也就是说，不能只依赖 C 编译器生成正确的函数序言；启动代码、链接脚本、异常入口、上下文切换代码同样属于 ABI 正确性的组成部分。</p><h2 id="三、Cortex-M-上的-MSP-和-PSP"><a href="#三、Cortex-M-上的-MSP-和-PSP" class="headerlink" title="三、Cortex-M 上的 MSP 和 PSP"></a>三、Cortex-M 上的 MSP 和 PSP</h2><p>Cortex-M 有两个常见栈指针：</p><table><thead><tr><th>名称</th><th>中文含义</th><th>常见用途</th></tr></thead><tbody><tr><td>MSP</td><td>主栈指针</td><td>复位后启动、早期初始化、异常和中断处理</td></tr><tr><td>PSP</td><td>进程栈指针</td><td>RTOS 任务或线程运行时常用</td></tr></tbody></table><p>复位后，处理器通常从向量表第 0 项取初始栈顶并装入 MSP。启动代码执行期间，清零 BSS、复制 DATA、调用系统初始化和 <code>main()</code> 等流程，往往还在使用 MSP。RTOS 调度器启动后，普通任务常切换到 PSP，每个任务使用自己的栈空间。</p><p>因此，链接脚本里的初始栈符号通常主要影响 MSP；任务栈是否安全，还要看任务栈分配和上下文初始化函数是否在把地址装入 PSP 前做了 8 字节对齐。</p><p><img src="https://i-blog.csdnimg.cn/direct/028aa0deceb84e7fb9c454a288634569.png" alt="在这里插入图片描述"></p><h2 id="四、不遵守-8-字节对齐可能出现什么后果"><a href="#四、不遵守-8-字节对齐可能出现什么后果" class="headerlink" title="四、不遵守 8 字节对齐可能出现什么后果"></a>四、不遵守 8 字节对齐可能出现什么后果</h2><p>SP 未满足 8 字节对齐时，不一定立刻 HardFault。很多场景下，程序仍然可以继续运行，但数据已经不可靠。</p><p>常见现象包括：</p><ul><li><code>va_arg(ap, double)</code> 读取到异常值；</li><li><code>printf</code>、<code>sprintf</code>、日志函数中的浮点或 64 位变参输出异常；</li><li>多个变参后续参数错位；</li><li>64 位局部对象或栈上传参在特定编译选项下表现异常；</li><li>少数场景下进入 UsageFault、BusFault、HardFault；</li><li>手写汇编、异常入口、上下文切换代码与 C 代码混用时出现偶发跑飞。</li></ul><p>这些现象具有隐蔽性。原因是 4 字节对齐已经满足很多 32 位访问要求，简单整数参数、普通函数调用、短路径启动代码可能完全看不出问题。只有当代码路径涉及 8 字节对齐的栈上数据，或者变参读取逻辑依赖实际对齐状态时，问题才更容易出现。</p><h2 id="五、为什么不是所有代码都会受影响"><a href="#五、为什么不是所有代码都会受影响" class="headerlink" title="五、为什么不是所有代码都会受影响"></a>五、为什么不是所有代码都会受影响</h2><h3 id="5-1-参数可能没有进入栈"><a href="#5-1-参数可能没有进入栈" class="headerlink" title="5.1 参数可能没有进入栈"></a>5.1 参数可能没有进入栈</h3><p>AAPCS32 base standard 优先使用 <code>r0</code> 到 <code>r3</code> 传递参数。参数较少时，很多值不会进入栈。没有进入栈，就不会触发“栈上 8 字节对齐数据”的问题。</p><h3 id="5-2-非变参浮点参数可能走浮点寄存器"><a href="#5-2-非变参浮点参数可能走浮点寄存器" class="headerlink" title="5.2 非变参浮点参数可能走浮点寄存器"></a>5.2 非变参浮点参数可能走浮点寄存器</h3><p>在带 FPU 且启用硬浮点 ABI 的工程中，普通非变参函数的浮点参数可能使用浮点寄存器传递。此时不能简单推断“函数里出现 double 就会错”。</p><p>但变参函数不同。AAPCS32 明确规定变参函数按 base standard 处理，且变参过程不使用 VFP 协处理器寄存器候选参数规则。因此，<code>printf</code> 类接口、日志封装、<code>va_arg</code> 读取 <code>double</code> 或 64 位数据，更容易成为触发点。</p><h3 id="5-3-RTOS-任务栈通常还有一次对齐"><a href="#5-3-RTOS-任务栈通常还有一次对齐" class="headerlink" title="5.3 RTOS 任务栈通常还有一次对齐"></a>5.3 RTOS 任务栈通常还有一次对齐</h3><p>RTOS 启动后，任务栈通常来自堆或静态数组，但“来自堆”不等于“运行时使用堆作为栈”。堆只是分配内存的来源；任务真正运行时使用的是一段 stack buffer。</p><p>成熟的 Cortex-M RTOS 移植层通常会在任务栈初始化时，把将要装入 PSP 的栈顶调整到 8 字节边界。这样一来，普通任务栈的风险会低很多。早期 MSP、异常入口、自定义汇编入口、手写上下文切换代码，仍需要单独确认。</p><h2 id="六、怎么查看工程是否遵守"><a href="#六、怎么查看工程是否遵守" class="headerlink" title="六、怎么查看工程是否遵守"></a>六、怎么查看工程是否遵守</h2><h3 id="6-1-先找最终装入-SP-的符号"><a href="#6-1-先找最终装入-SP-的符号" class="headerlink" title="6.1 先找最终装入 SP 的符号"></a>6.1 先找最终装入 SP 的符号</h3><p>不同工具链写法不同，常见符号包括：</p><table><thead><tr><th>工具链</th><th>常见符号或区域</th></tr></thead><tbody><tr><td>GCC&#x2F;ld</td><td><code>_estack</code>、<code>_sstack</code>、<code>.stack</code>、<code>._user_heap_stack</code></td></tr><tr><td>Keil&#x2F;ARMCC&#x2F;Armclang</td><td><code>__initial_sp</code>、<code>Stack_Mem</code>、startup 汇编中的 <code>AREA STACK</code></td></tr><tr><td>IAR</td><td><code>CSTACK</code>、<code>__ICFEDIT_size_cstack__</code></td></tr><tr><td>RTOS 任务栈</td><td>任务栈初始化函数中最终写入 PSP 的地址</td></tr></tbody></table><p>仅看源码中的 <code>ALIGN(4)</code> 或 <code>ALIGN(8)</code> 还不够。最终是否满足要求，要看 map 文件里的实际符号地址：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">_estack       = 0x20020000</span><br><span class="line">__initial_sp  = 0x20020000</span><br><span class="line">CSTACK end    = 0x20020000</span><br></pre></td></tr></table></figure><p>判断方式很简单：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">地址 % 8 == 0  合格</span><br><span class="line">地址 % 8 == 4  不合格</span><br></pre></td></tr></table></figure><h3 id="6-2-GCC-ld：关注-estack-的来源"><a href="#6-2-GCC-ld：关注-estack-的来源" class="headerlink" title="6.2 GCC&#x2F;ld：关注 _estack 的来源"></a>6.2 GCC&#x2F;ld：关注 <code>_estack</code> 的来源</h3><p>一种存在风险的写法是把 <code>.stack</code> 放在 <code>.data</code> 后面，并且只做 4 字节对齐：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">.stack :</span><br><span class="line">&#123;</span><br><span class="line">    . = ALIGN(4);</span><br><span class="line">    _sstack = .;</span><br><span class="line">    . = . + STACK_SIZE;</span><br><span class="line">    . = ALIGN(4);</span><br><span class="line">    _estack = .;</span><br><span class="line">&#125; &gt; RAM</span><br></pre></td></tr></table></figure><p>这类写法只能保证 <code>_estack % 4 == 0</code>，不能保证 <code>_estack % 8 == 0</code>。如果 <code>.data</code> 结束地址刚好是 <code>4 mod 8</code>，最终 <code>_estack</code> 也可能是 <code>4 mod 8</code>。</p><p>另一种常见写法是让 <code>_estack</code> 直接等于 RAM 末尾：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">_estack = ORIGIN(RAM) + LENGTH(RAM);</span><br></pre></td></tr></table></figure><p>只要 <code>ORIGIN(RAM)</code> 和 <code>LENGTH(RAM)</code> 都是 8 字节对齐的，<code>_estack</code> 通常天然满足 8 字节对齐。</p><p><img src="https://i-blog.csdnimg.cn/direct/c43ebbec05b849ceb18303b1300f0a5a.png" alt="在这里插入图片描述"></p><h3 id="6-3-Keil：不能只看-sct"><a href="#6-3-Keil：不能只看-sct" class="headerlink" title="6.3 Keil：不能只看 .sct"></a>6.3 Keil：不能只看 <code>.sct</code></h3><p>Keil 工程的 scatter 文件可能只定义 ROM 和 RAM 区域，不直接定义栈。例如：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">LR_IROM1 0x08000000 0x00100000 &#123;</span><br><span class="line">  ER_IROM1 0x08000000 0x00100000 &#123;</span><br><span class="line">    *.o (RESET, +First)</span><br><span class="line">    *(InRoot$$Sections)</span><br><span class="line">    .ANY (+RO)</span><br><span class="line">  &#125;</span><br><span class="line">  RW_IRAM1 0x20000000 0x00020000 &#123;</span><br><span class="line">    .ANY (+RW +ZI)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这种情况下，栈可能在 startup 汇编里定义。常见写法是：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">AREA    STACK, NOINIT, READWRITE, ALIGN=3</span><br><span class="line">Stack_Mem       SPACE   Stack_Size</span><br><span class="line">__initial_sp</span><br></pre></td></tr></table></figure><p><code>ALIGN=3</code> 表示按 <code>2^3</code> 对齐，也就是 8 字节对齐。因此，Keil 工程要同时查看 startup 文件和 map 文件，确认 <code>__initial_sp</code> 的最终地址。</p><h3 id="6-4-IAR：关注-CSTACK"><a href="#6-4-IAR：关注-CSTACK" class="headerlink" title="6.4 IAR：关注 CSTACK"></a>6.4 IAR：关注 <code>CSTACK</code></h3><p>IAR <code>.icf</code> 中常见写法是：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">define block CSTACK with alignment = 8, size = __ICFEDIT_size_cstack__ &#123; &#125;;</span><br><span class="line"></span><br><span class="line">place in RAM1_region &#123; readwrite, last block CSTACK &#125;;</span><br></pre></td></tr></table></figure><p>如果 <code>CSTACK</code> 显式设置 <code>alignment = 8</code>，并且 RAM 区域末尾也满足 8 字节对齐，则初始栈通常没有同类问题。</p><h2 id="七、怎么修改"><a href="#七、怎么修改" class="headerlink" title="七、怎么修改"></a>七、怎么修改</h2><h3 id="7-1-修-GCC-ld-中的-stack-边界"><a href="#7-1-修-GCC-ld-中的-stack-边界" class="headerlink" title="7.1 修 GCC&#x2F;ld 中的 .stack 边界"></a>7.1 修 GCC&#x2F;ld 中的 <code>.stack</code> 边界</h3><p>如果初始栈来自 <code>.stack</code> 段，并且当前只使用 <code>ALIGN(4)</code>，可以把栈段边界改为 8 字节对齐：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">.stack :</span><br><span class="line">&#123;</span><br><span class="line">    . = ALIGN(8);</span><br><span class="line">    _sstack = .;</span><br><span class="line">    . = . + STACK_SIZE;</span><br><span class="line">    . = ALIGN(8);</span><br><span class="line">    _estack = .;</span><br><span class="line">&#125; &gt; RAM</span><br></pre></td></tr></table></figure><p>如果 <code>STACK_SIZE</code> 本身是 8 的倍数，那么 <code>_sstack</code> 8 对齐后，<code>_estack</code> 通常也能保持 8 对齐。末尾继续保留 <code>ALIGN(8)</code> 更直观，也能抵抗后续栈大小改动带来的风险。</p><h3 id="7-2-把初始栈顶放在-8-对齐的-RAM-末尾"><a href="#7-2-把初始栈顶放在-8-对齐的-RAM-末尾" class="headerlink" title="7.2 把初始栈顶放在 8 对齐的 RAM 末尾"></a>7.2 把初始栈顶放在 8 对齐的 RAM 末尾</h3><p>另一种写法是直接使用 RAM 末尾作为初始栈顶：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">_estack = ORIGIN(RAM) + LENGTH(RAM);</span><br></pre></td></tr></table></figure><p>使用这种写法时，需要确认 RAM 起始地址和长度都按 8 字节对齐。STM32 这类常见 SRAM 区域通常满足这一点，但外部 RAM、分区 RAM、自定义 bootloader 偏移场景仍需检查。</p><h3 id="7-3-修任务栈初始化"><a href="#7-3-修任务栈初始化" class="headerlink" title="7.3 修任务栈初始化"></a>7.3 修任务栈初始化</h3><p>任务栈不一定由链接脚本中的初始栈符号决定。如果任务栈来自堆或静态数组，关键是把最终写入 PSP 的栈顶地址对齐到 8 字节边界。</p><p>常见原则是：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">stack_top = ALIGN_DOWN(stack_top, <span class="number">8</span>);</span><br></pre></td></tr></table></figure><p>或等价处理。具体使用向上还是向下对齐，取决于栈增长方向和移植层约定。Cortex-M 常见任务栈向低地址增长，因此通常对栈顶做向下对齐。</p><p><img src="https://i-blog.csdnimg.cn/direct/f26c8e206b4d45f195de1b2e7acfc0f6.png" alt="在这里插入图片描述"></p><h2 id="八、为什么修改栈边界就能解决问题"><a href="#八、为什么修改栈边界就能解决问题" class="headerlink" title="八、为什么修改栈边界就能解决问题"></a>八、为什么修改栈边界就能解决问题</h2><p>初始 SP 的值来自某个地址来源：向量表、链接符号、startup 汇编标签或 RTOS 栈初始化结果。C 编译器在生成函数调用和栈上对象访问代码时，会假设进入符合 AAPCS32 的函数时 SP 已经满足 8 字节对齐。也就是说，编译器不会在每个普通函数入口都无条件修复错误的 SP。</p><p>因此，正确做法是在源头修复：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">链接脚本 / startup / 栈初始化函数</span><br><span class="line">        ↓</span><br><span class="line">生成 8 字节对齐的初始 SP 或任务 SP</span><br><span class="line">        ↓</span><br><span class="line">进入 AAPCS32 函数时满足 ABI</span><br><span class="line">        ↓</span><br><span class="line">栈上传递的 8 字节对齐数据位置可靠</span><br></pre></td></tr></table></figure><p>如果只修改某个使用 <code>double</code> 的函数，问题仍然可能在其他变参函数、日志函数、异常入口或库函数中出现。修栈边界不是为了某一个类型服务，而是为了让整个调用链恢复 ABI 前提。</p><h2 id="九、一个更准确的理解方式"><a href="#九、一个更准确的理解方式" class="headerlink" title="九、一个更准确的理解方式"></a>九、一个更准确的理解方式</h2><p>这类问题可以按以下链路理解：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">AAPCS32 要求函数公开调用边界 SP 8 字节对齐</span><br><span class="line">        ↓</span><br><span class="line">启动代码、运行时、RTOS、汇编入口必须提供正确 SP</span><br><span class="line">        ↓</span><br><span class="line">若初始栈或任务栈只保证 4 字节对齐，就可能破坏 ABI 前提</span><br><span class="line">        ↓</span><br><span class="line">普通 32 位路径不一定暴露问题</span><br><span class="line">        ↓</span><br><span class="line">变参函数、栈上传递的 8 字节对齐数据、异常入口更容易暴露问题</span><br></pre></td></tr></table></figure><p>所以，现象上可能是 <code>double</code>、<code>uint64_t</code> 或日志格式化函数异常；根因上则是进入 C ABI 边界时 SP 没有满足 8 字节对齐。</p><h2 id="十、收束"><a href="#十、收束" class="headerlink" title="十、收束"></a>十、收束</h2><p>Cortex-M 栈 8 字节对齐问题，本质上不是某个 RTOS、某个芯片或某个 <code>double</code> 变量的局部问题，而是 ABI、链接脚本、启动代码和运行时上下文之间的配合问题。</p><p>Thumb 指令集解决“CPU 如何执行指令”；AAPCS32 解决“函数之间如何互相调用”。只要工程使用符合 Arm EABI 的 C&#x2F;C++ 工具链，函数公开调用边界处的 SP 8 字节对齐要求就需要被满足。</p><p>排查时，重点不在于搜索所有 <code>double</code>，而在于确认这些地址是否 8 字节对齐：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">初始 MSP</span><br><span class="line">异常入口使用的 MSP</span><br><span class="line">任务启动时写入 PSP 的栈顶</span><br><span class="line">手写汇编跳入 C 函数前的 SP</span><br></pre></td></tr></table></figure><p>满足这些前提后，<code>double</code>、64 位整数、变参函数和库函数才能处在编译器预期的 ABI 环境中。栈对齐是一条底层约束；一旦在源头破坏，表现出来的可能只是某个上层函数偶发读错。</p><h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ul><li>Arm, <em>Procedure Call Standard for the Arm Architecture (AAPCS32)</em>: <a href="https://github.com/ARM-software/abi-aa/blob/main/aapcs32/aapcs32.rst">https://github.com/ARM-software/abi-aa/blob/main/aapcs32/aapcs32.rst</a></li><li>Arm, <em>ABI Advisory Note – SP must be 8-byte aligned on entry to AAPCS-conforming functions</em>: <a href="https://github.com/ARM-software/abi-aa/blob/main/advnote132/advnote132.rst">https://github.com/ARM-software/abi-aa/blob/main/advnote132/advnote132.rst</a></li><li>GNU Binutils, <em>LD Builtin Functions &#x2F; ALIGN</em>: <a href="https://sourceware.org/binutils/docs/ld/Builtin-Functions.html">https://sourceware.org/binutils/docs/ld/Builtin-Functions.html</a></li><li>FreeRTOS Kernel, <em>portable&#x2F;GCC&#x2F;ARM_CM4F&#x2F;portmacro.h</em>: <a href="https://github.com/FreeRTOS/FreeRTOS-Kernel/blob/main/portable/GCC/ARM_CM4F/portmacro.h">https://github.com/FreeRTOS/FreeRTOS-Kernel/blob/main/portable/GCC/ARM_CM4F/portmacro.h</a></li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;meta name=&quot;referrer&quot; content=&quot;no-referrer&quot; /&gt;
# 由于 Cortex-M 栈未满足 8 字节对齐导致的问题与原因分析

&lt;p&gt;&lt;img src=&quot;https://i-blog.csdnimg.cn/direct/78b67cce78</summary>
      
    
    
    
    <category term="MCU" scheme="https://wdfk-prog.space/categories/MCU/"/>
    
    
    <category term="MCU" scheme="https://wdfk-prog.space/tags/MCU/"/>
    
  </entry>
  
  <entry>
    <title>CANopen PDO 运行流程</title>
    <link href="https://wdfk-prog.space/posts/1d4e9f18/"/>
    <id>https://wdfk-prog.space/posts/1d4e9f18/</id>
    <published>2026-06-29T03:05:47.000Z</published>
    <updated>2026-06-29T03:06:13.204Z</updated>
    
    <content type="html"><![CDATA[<meta name="referrer" content="no-referrer" /><h1 id="CANopen-PDO-运行流程"><a href="#CANopen-PDO-运行流程" class="headerlink" title="CANopen PDO 运行流程"></a>CANopen PDO 运行流程</h1><p><img src="https://i-blog.csdnimg.cn/direct/4df105b1eb654b1790ec9dc81427e235.png" alt="在这里插入图片描述"></p><p>@[toc]</p><p>PDO 不是“带 index&#x2F;sub-index 的小 SDO”，也不是“谁发给谁的一问一答报文”。它更像一条预先约定好格式的实时数据通道：发送方把对象字典里的几个变量按固定顺序塞进一个 CAN 数据帧，接收方再按自己的 PDO 映射表把这些字节拆回本地对象字典变量。</p><p>理解 PDO 时要先把两个问题分开：</p><ol><li><strong>协议规定 PDO 应该是什么样。</strong> 包括它为什么只有过程数据、为什么要靠对象字典配置、为什么要区分 RPDO&#x2F;TPDO、为什么 transmission type 会影响触发时机。</li><li><strong>CANopenNode 源码怎样实现这些规定。</strong> 包括 <code>CO_CANopenInitPDO()</code> 如何初始化每个 PDO，<code>CO_PDO_receive()</code> 为什么只缓存，<code>CO_RPDO_process()</code> 为什么等到主循环才写 OD，<code>CO_TPDO_process()</code> 又为什么要同时看 SYNC、event timer、inhibit timer 和 <code>sendRequest</code>。</li></ol><p>本文按这个顺序讲。前半部分先把协议模型讲清楚，后半部分再对照 CANopenNode 的 <code>CO_PDO.h/.c</code> 看源码运行路径。</p><p><img src="https://i-blog.csdnimg.cn/direct/cd5269f35c4b4e488b6601f9f6852138.png" alt="在这里插入图片描述"></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    A[&quot;节点 A 应用变量&lt;br/&gt;速度/位置/IO&quot;] --&gt; B[&quot;TPDO&lt;br/&gt;按映射打包成 CAN 帧&quot;]</span><br><span class="line">    B --&gt; C[&quot;CAN 总线&lt;br/&gt;广播一帧&quot;]</span><br><span class="line">    C --&gt; D[&quot;节点 B RPDO&lt;br/&gt;按本地映射解释&quot;]</span><br><span class="line">    C --&gt; E[&quot;节点 C RPDO&lt;br/&gt;也可监听同一 CAN-ID&quot;]</span><br><span class="line">    OD[&quot;对象字典 OD&lt;br/&gt;communication parameter 决定 CAN-ID/触发方式&lt;br/&gt;mapping parameter 决定字节含义&quot;] --&gt; B</span><br><span class="line">    OD --&gt; D</span><br></pre></td></tr></table></figure><h2 id="1-PDO-解决的不是“访问对象字典”，而是“实时搬运过程数据”"><a href="#1-PDO-解决的不是“访问对象字典”，而是“实时搬运过程数据”" class="headerlink" title="1. PDO 解决的不是“访问对象字典”，而是“实时搬运过程数据”"></a>1. PDO 解决的不是“访问对象字典”，而是“实时搬运过程数据”</h2><p>CANopen 里有很多通信对象。SDO 适合配置、诊断和读写对象字典；它能带 index&#x2F;sub-index，语义清楚，但协议交互更重。PDO 的目标相反：少解释、少握手、少字节，把高频过程数据尽快放到总线上。</p><p>CiA 对 PDO 的公开说明是：PDO 用于广播高优先级控制与状态信息；在经典 CANopen 中，一个 PDO 是一个 CAN Classic 数据帧，最多携带 8 字节纯应用数据。也就是说，PDO payload 里不再放“我要访问哪个 index&#x2F;sub-index”这种协议字段。CANopenNode 官方 PDO 文档同样把 PDO 描述为用于实时数据传输、没有协议开销的对象。</p><p>这就是读 PDO 源码时最重要的前提：<strong>PDO 帧里只有数据，数据含义不在帧内，而在对象字典里的映射表中。</strong></p><p>假设一个从站周期上报 3 个量：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Byte0      : digital input 8bit</span><br><span class="line">Byte1..2   : analog input 16bit</span><br><span class="line">Byte3..4   : status word 16bit</span><br></pre></td></tr></table></figure><p>实际发到 CAN 总线上的 TPDO 可能只是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">CAN-ID = 0x181</span><br><span class="line">DLC    = 5</span><br><span class="line">Data   = 0x01 0x34 0x12 0x27 0x00</span><br></pre></td></tr></table></figure><p>这帧里看不到 <code>0x6000:01</code>、<code>0x6411:01</code>、<code>0x6041:00</code>。接收方之所以知道 Byte0 是数字输入、Byte1..2 是模拟量，是因为它的 RPDO mapping 事先写好了“这些字节对应哪些 OD 对象”。</p><p><img src="https://i-blog.csdnimg.cn/direct/0819aeec8e4b4dbd9bf5f6b3d814b3c7.png" alt="在这里插入图片描述"></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    F[&quot;CAN 帧&lt;br/&gt;CAN-ID + DLC + Data[0..7]&quot;] --&gt; D[&quot;Data 字节流&lt;br/&gt;例如 01 34 12 7F ...&quot;]</span><br><span class="line">    D --&gt; M[&quot;PDO mapping&lt;br/&gt;0x62000108 -&gt; Byte0&lt;br/&gt;0x64110110 -&gt; Byte1..2&lt;br/&gt;0x60410010 -&gt; Byte3..4&quot;]</span><br><span class="line">    M --&gt; OD[&quot;对象字典变量&lt;br/&gt;0x6200:01&lt;br/&gt;0x6411:01&lt;br/&gt;0x6041:00&quot;]</span><br></pre></td></tr></table></figure><p>这也是为什么初学时容易把 PDO 看乱：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">SDO 报文里直接出现 index/sub-index。</span><br><span class="line">PDO 报文里不出现 index/sub-index。</span><br><span class="line">PDO 的 index/sub-index 只存在于配置阶段的 mapping entry 中。</span><br></pre></td></tr></table></figure><h2 id="2-RPDO-和-TPDO-是“从本节点视角命名”的"><a href="#2-RPDO-和-TPDO-是“从本节点视角命名”的" class="headerlink" title="2. RPDO 和 TPDO 是“从本节点视角命名”的"></a>2. RPDO 和 TPDO 是“从本节点视角命名”的</h2><p>RPDO 是 Receive PDO，TPDO 是 Transmit PDO。这个名字不是站在整个网络的绝对视角，而是站在当前设备的视角。</p><p>同一条 CAN 帧，对发送设备来说是 TPDO，对接收设备来说可以是 RPDO。例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">驱动器 A 发出 CAN-ID 0x181 的 TPDO1</span><br><span class="line">    -&gt; 控制器 B 把 CAN-ID 0x181 配成 RPDO</span><br><span class="line">    -&gt; 显示器 C 也可以把 CAN-ID 0x181 配成 RPDO</span><br></pre></td></tr></table></figure><p>这里没有“一发一收绑定”的概念。CAN 是广播总线，只要 CAN-ID 匹配，多个节点都可以消费同一个 PDO。CANopenNode 官方文档也强调：一个 TPDO 的 CAN-ID 应该唯一，由单个源产生；零个或多个 RPDO 可以配置成接收这个 TPDO 的 CAN-ID。</p><p>对源码来说，这个模型会落成两个方向：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">TPDO：从本地 OD 变量读取数据 -&gt; 打包 -&gt; 发 CAN 帧</span><br><span class="line">RPDO：收到 CAN 帧 -&gt; 缓存 -&gt; 按映射写入本地 OD 变量</span><br></pre></td></tr></table></figure><p>这解释了 <code>CO_TPDOsend()</code> 和 <code>CO_RPDO_process()</code> 的方向差异。TPDO 调 <code>OD_IO.read()</code>，RPDO 调 <code>OD_IO.write()</code>。同样是 mapping entry，方向不同，源码检查的 OD 属性也不同：RPDO 要求目标变量允许 <code>ODA_RPDO</code>，TPDO 要求源变量允许 <code>ODA_TPDO</code>。</p><h2 id="3-一个-PDO-由两组对象字典参数定义"><a href="#3-一个-PDO-由两组对象字典参数定义" class="headerlink" title="3. 一个 PDO 由两组对象字典参数定义"></a>3. 一个 PDO 由两组对象字典参数定义</h2><p>CiA 的 PDO 说明把 PDO 参数分成两类：communication parameter 和 mapping parameter。CANopenNode 头文件注释也采用同一套分法：</p><p><img src="https://i-blog.csdnimg.cn/direct/717a47cdb8414f3982700445418c535b.png" alt="在这里插入图片描述"></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    R1[&quot;0x1400..0x15FF&lt;br/&gt;RPDO communication parameter&quot;] --&gt; X[&quot;运行时结果&lt;br/&gt;CAN-ID + 触发条件 + Data 布局&quot;]</span><br><span class="line">    R2[&quot;0x1600..0x17FF&lt;br/&gt;RPDO mapping parameter&quot;] --&gt; X</span><br><span class="line">    T1[&quot;0x1800..0x19FF&lt;br/&gt;TPDO communication parameter&quot;] --&gt; X</span><br><span class="line">    T2[&quot;0x1A00..0x1BFF&lt;br/&gt;TPDO mapping parameter&quot;] --&gt; X</span><br></pre></td></tr></table></figure><h3 id="3-1-communication-parameter-决定“这条-PDO-怎么通信”"><a href="#3-1-communication-parameter-决定“这条-PDO-怎么通信”" class="headerlink" title="3.1 communication parameter 决定“这条 PDO 怎么通信”"></a>3.1 communication parameter 决定“这条 PDO 怎么通信”</h3><p>对 RPDO 来说，<code>0x1400 + n</code> 是第 n 个 RPDO 的通信参数。对 TPDO 来说，<code>0x1800 + n</code> 是第 n 个 TPDO 的通信参数。</p><p>这里最核心的是 <code>sub-index 1</code>：COB-ID。它至少表达两个信息：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">bit31     : PDO 是否无效，1 表示禁用，0 表示启用</span><br><span class="line">bit10..0  : 11-bit CAN-ID</span><br></pre></td></tr></table></figure><p>在 CANopenNode 经典 11-bit CAN-ID 场景中，源码会检查 bit 11..29 是否非法置位。启用动态配置时，写 <code>OD_write_14xx()</code> 或 <code>OD_write_18xx()</code> 修改 COB-ID，源码不会盲目接受，而是会检查 PDO 是否允许在当前状态下改变、CAN-ID 是否有效、mapping 是否已经配置好。</p><p>communication parameter 还会包含 transmission type、event timer、inhibit time、SYNC start value 等字段。它们不改变 payload 的字节含义，但会改变“什么时候处理或发送这帧”。</p><h3 id="3-2-mapping-parameter-决定“这-8-字节怎么解释”"><a href="#3-2-mapping-parameter-决定“这-8-字节怎么解释”" class="headerlink" title="3.2 mapping parameter 决定“这 8 字节怎么解释”"></a>3.2 mapping parameter 决定“这 8 字节怎么解释”</h3><p>对 RPDO 来说，<code>0x1600 + n</code> 是第 n 个 RPDO 的映射参数。对 TPDO 来说，<code>0x1A00 + n</code> 是第 n 个 TPDO 的映射参数。</p><p><code>sub-index 0</code> 不代表一个被映射的变量，它表示当前启用多少个映射项。</p><p><code>sub-index 1..n</code> 才是每个映射项。每个映射项是 32-bit，格式如下：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">bit31..16 : index</span><br><span class="line">bit15..8  : sub-index</span><br><span class="line">bit7..0   : mapped length in bits</span><br></pre></td></tr></table></figure><p>例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">0x62000108</span><br><span class="line">    index    = 0x6200</span><br><span class="line">    subIndex = 0x01</span><br><span class="line">    length   = 0x08 = 8 bit = 1 byte</span><br></pre></td></tr></table></figure><p>这条映射项的含义不是“PDO 报文里会带 0x6200 和 0x01”，而是“PDO 数据区的当前位置对应对象字典 <code>0x6200:01</code>，长度 8 bit”。</p><p>如果一个 PDO mapping 是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">sub0 = 3</span><br><span class="line">sub1 = 0x62000108</span><br><span class="line">sub2 = 0x64110110</span><br><span class="line">sub3 = 0x60410010</span><br></pre></td></tr></table></figure><p>那它的 payload 解释顺序就是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Byte0      -&gt; 0x6200:01，8 bit</span><br><span class="line">Byte1..2   -&gt; 0x6411:01，16 bit</span><br><span class="line">Byte3..4   -&gt; 0x6041:00，16 bit</span><br></pre></td></tr></table></figure><p>源码里的 <code>PDO_initMapping()</code> 和 <code>PDOconfigMap()</code> 主要就是把这些 32-bit 映射项解析成运行时可以使用的 OD 访问接口或内存指针。</p><h2 id="4-transmission-type-决定-PDO-的时机"><a href="#4-transmission-type-决定-PDO-的时机" class="headerlink" title="4. transmission type 决定 PDO 的时机"></a>4. transmission type 决定 PDO 的时机</h2><p>PDO 的内容由 mapping 决定；PDO 的时机由 transmission type 决定。</p><p>常见取值可以这样理解：</p><table><thead><tr><th align="right">transmission type</th><th>协议含义</th><th>RPDO 侧直观行为</th><th>TPDO 侧直观行为</th></tr></thead><tbody><tr><td align="right"><code>0</code></td><td>同步、非周期</td><td>收到后缓存，等下一次 SYNC 后处理</td><td>事件先置请求，下一次 SYNC 才发送</td></tr><tr><td align="right"><code>1..240</code></td><td>同步、周期，每 N 次 SYNC</td><td>收到后缓存，等下一次 SYNC 后处理</td><td>每 N 次 SYNC 发送一次</td></tr><tr><td align="right"><code>254</code></td><td>事件型，制造商特定</td><td>收到后不等 SYNC，主循环满足条件即可处理</td><td>由 event timer、应用请求或 OD flag 触发</td></tr><tr><td align="right"><code>255</code></td><td>事件型，设备&#x2F;profile 特定</td><td>收到后不等 SYNC，主循环满足条件即可处理</td><td>由 event timer、应用请求或 OD flag 触发</td></tr></tbody></table><p>这里要区分“收到 CAN 帧”和“处理 OD 变量”。对 RPDO 来说，即使 CAN 接收中断已经收到帧，同步 RPDO 仍然不会马上写对象字典，而是等到下一个 SYNC 边界。CANopenNode 的头文件注释也明确写到，同步 RPDO 会在下一次 SYNC 消息之后处理。</p><p>对 TPDO 来说，事件型并不等于“立刻发”。如果配置了 inhibit time，发送会被最小间隔限制住；如果配置了 event timer，timer 到期会触发发送请求；如果应用调用 <code>CO_TPDOsendRequest()</code>，也只是把 <code>sendRequest</code> 置位，真正发送仍由 <code>CO_TPDO_process()</code> 判断条件后完成。</p><h2 id="5-动态-可变映射为什么必须按顺序操作"><a href="#5-动态-可变映射为什么必须按顺序操作" class="headerlink" title="5. 动态&#x2F;可变映射为什么必须按顺序操作"></a>5. 动态&#x2F;可变映射为什么必须按顺序操作</h2><p>PDO mapping 不能随便一边通信一边改。原因很直接：如果 PDO 正在有效状态，你突然把 Byte0 的含义从“数字输入”改成“控制字低字节”，接收方可能在半配置状态下按错误布局解释数据。</p><p>CiA 的 PDO mapping procedure 给出固定步骤：先让 PDO 无效，再让 mapping 无效，修改 mapping，重新启用 mapping，最后重新启用 PDO。CANopenNode 官方文档也给出同样的步骤。</p><p><img src="https://i-blog.csdnimg.cn/direct/a4fe9a8d52b74030b831a28b2205fe47.png" alt="在这里插入图片描述"></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    A[&quot;1 禁用 PDO&lt;br/&gt;COB-ID bit31 = 1&quot;] --&gt; B[&quot;2 禁用映射&lt;br/&gt;mapping sub0 = 0&quot;]</span><br><span class="line">    B --&gt; C[&quot;3 写 sub1..n&lt;br/&gt;index/sub/length&quot;]</span><br><span class="line">    C --&gt; D[&quot;4 启用映射&lt;br/&gt;sub0 = n&quot;]</span><br><span class="line">    D --&gt; E[&quot;5 启用 PDO&lt;br/&gt;COB-ID bit31 = 0&quot;]</span><br></pre></td></tr></table></figure><p>CANopenNode 的 <code>OD_write_PDO_mapping()</code> 也按这个思路约束：如果 <code>PDO-&gt;valid</code> 仍为真，就不允许写 mapping；如果 <code>mappedObjectsCount</code> 不为 0，又去写具体 mapping 子项，也会被拒绝。换句话说，源码不是只“建议”你按顺序做，而是在动态配置路径里直接检查这个顺序。</p><h2 id="6-CO-CONFIG-PDO-不是普通配置表，而是源码裁剪开关"><a href="#6-CO-CONFIG-PDO-不是普通配置表，而是源码裁剪开关" class="headerlink" title="6. CO_CONFIG_PDO 不是普通配置表，而是源码裁剪开关"></a>6. <code>CO_CONFIG_PDO</code> 不是普通配置表，而是源码裁剪开关</h2><p>你补充的这段 <code>CO_CONFIG_PDO</code> 很关键。它不是运行时变量，而是编译期能力组合。不同 flag 会改变结构体成员、函数参数、初始化步骤和运行时分支。</p><p><img src="https://i-blog.csdnimg.cn/direct/0fa01ff51a884d4c89aaabc43c0bf76b.png"></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">flowchart TB</span><br><span class="line">    A[&quot;CO_CONFIG_PDO&lt;br/&gt;按位 OR 的能力开关&quot;] --&gt; B[&quot;结构体字段&lt;br/&gt;SYNC 指针 / timer / OD_IO / flag / callback&quot;]</span><br><span class="line">    A --&gt; C[&quot;初始化参数&lt;br/&gt;是否传 SYNC&lt;br/&gt;是否安装 OD extension&quot;]</span><br><span class="line">    A --&gt; D[&quot;运行分支&lt;br/&gt;NMT/SYNC/timerNext&lt;br/&gt;OD_IO 或直接内存&quot;]</span><br><span class="line">    D --&gt; E[&quot;RTOS 影响&lt;br/&gt;RPDO callback pre&lt;br/&gt;任务唤醒 / 低功耗调度&quot;]</span><br></pre></td></tr></table></figure><p>下面按“打开后源码会多出什么”来讲。</p><h3 id="6-1-CO-CONFIG-RPDO-ENABLE：编译接收-PDO"><a href="#6-1-CO-CONFIG-RPDO-ENABLE：编译接收-PDO" class="headerlink" title="6.1 CO_CONFIG_RPDO_ENABLE：编译接收 PDO"></a>6.1 <code>CO_CONFIG_RPDO_ENABLE</code>：编译接收 PDO</h3><p>打开它后，<code>CO_RPDO_t</code>、<code>CO_RPDO_init()</code>、<code>CO_RPDO_process()</code>、<code>CO_PDO_receive()</code> 等 RPDO 相关代码才参与编译。</p><p>在 <code>CO_CANopenInitPDO()</code> 中，只有满足：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">if</span> ((CO_CONFIG_PDO)&amp;CO_CONFIG_RPDO_ENABLE) != 0</span></span><br></pre></td></tr></table></figure><p>才会进入 RPDO 初始化循环。这个循环会取 <code>0x1400</code> 和 <code>0x1600</code> 两组 OD entry，然后逐个调用 <code>CO_RPDO_init()</code>。</p><p>如果你的 STM32 设备只负责上报状态，不接收主站下发的过程控制量，理论上可以关闭 RPDO，减少对象、buffer 和处理分支。</p><h3 id="6-2-CO-CONFIG-TPDO-ENABLE：编译发送-PDO"><a href="#6-2-CO-CONFIG-TPDO-ENABLE：编译发送-PDO" class="headerlink" title="6.2 CO_CONFIG_TPDO_ENABLE：编译发送 PDO"></a>6.2 <code>CO_CONFIG_TPDO_ENABLE</code>：编译发送 PDO</h3><p>打开它后，<code>CO_TPDO_t</code>、<code>CO_TPDO_init()</code>、<code>CO_TPDO_process()</code>、<code>CO_TPDOsendRequest()</code>、内部 <code>CO_TPDOsend()</code> 等 TPDO 相关代码才参与编译。</p><p><code>CO_CANopenInitPDO()</code> 中 TPDO 的初始化循环会取 <code>0x1800</code> 和 <code>0x1A00</code>，逐个调用 <code>CO_TPDO_init()</code>。如果设备只接收外部命令而不周期上报过程数据，可以关闭 TPDO。</p><h3 id="6-3-CO-CONFIG-RPDO-TIMERS-ENABLE：给-RPDO-加超时监控"><a href="#6-3-CO-CONFIG-RPDO-TIMERS-ENABLE：给-RPDO-加超时监控" class="headerlink" title="6.3 CO_CONFIG_RPDO_TIMERS_ENABLE：给 RPDO 加超时监控"></a>6.3 <code>CO_CONFIG_RPDO_TIMERS_ENABLE</code>：给 RPDO 加超时监控</h3><p>RPDO 的 event timer 在接收方向通常被 CANopenNode 用作 timeout 监控。打开这个 flag 后，<code>CO_RPDO_t</code> 会增加：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">timeoutTime_us</span><br><span class="line">timeoutTimer</span><br></pre></td></tr></table></figure><p><code>CO_RPDO_init()</code> 会读取 RPDO communication parameter 的 event timer，把毫秒换算成微秒。<code>CO_RPDO_process()</code> 每轮会根据 <code>timeDifference_us</code> 累加计时。如果一直没有收到新的 RPDO，超过超时时间后会上报 <code>CO_EM_RPDO_TIME_OUT</code>。</p><p>这适合控制类设备。例如电机驱动器期望主站每 10 ms 下发控制字或目标速度。如果主站掉线但 NMT&#x2F;Heartbeat 还没来得及反映，RPDO timeout 可以作为更贴近过程数据的保护。</p><h3 id="6-4-CO-CONFIG-TPDO-TIMERS-ENABLE：给-TPDO-加-inhibit-和-event-timer"><a href="#6-4-CO-CONFIG-TPDO-TIMERS-ENABLE：给-TPDO-加-inhibit-和-event-timer" class="headerlink" title="6.4 CO_CONFIG_TPDO_TIMERS_ENABLE：给 TPDO 加 inhibit 和 event timer"></a>6.4 <code>CO_CONFIG_TPDO_TIMERS_ENABLE</code>：给 TPDO 加 inhibit 和 event timer</h3><p>打开后，<code>CO_TPDO_t</code> 会增加：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">inhibitTime_us</span><br><span class="line">eventTime_us</span><br><span class="line">inhibitTimer</span><br><span class="line">eventTimer</span><br></pre></td></tr></table></figure><p>这两个 timer 解决的是不同问题：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">event timer  : 最长多久主动发一次，避免状态一直不变导致对端长期没新数据</span><br><span class="line">inhibit time : 最短间隔多久才能再发一次，避免事件抖动导致总线被刷爆</span><br></pre></td></tr></table></figure><p>在源码里，event timer 到期会让 TPDO 具备发送请求；inhibit timer 未到期时，即使 <code>sendRequest</code> 已经置位，也不会立即发送。它们共同决定事件型 TPDO 的节奏。</p><h3 id="6-5-CO-CONFIG-PDO-SYNC-ENABLE：让-PDO-受-SYNC-节拍控制"><a href="#6-5-CO-CONFIG-PDO-SYNC-ENABLE：让-PDO-受-SYNC-节拍控制" class="headerlink" title="6.5 CO_CONFIG_PDO_SYNC_ENABLE：让 PDO 受 SYNC 节拍控制"></a>6.5 <code>CO_CONFIG_PDO_SYNC_ENABLE</code>：让 PDO 受 SYNC 节拍控制</h3><p>打开后，RPDO 和 TPDO 结构体中都会出现与 SYNC 相关的字段。</p><p>RPDO 侧会多出：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">CO_SYNC_t *SYNC;</span><br><span class="line"><span class="type">bool_t</span> synchronous;</span><br></pre></td></tr></table></figure><p>并且 <code>CO_RPDO_CAN_BUFFERS_COUNT</code> 会从 1 变成 2。双 buffer 的意义是：同步 RPDO 的接收和处理存在 SYNC 边界，源码需要避免“刚收到的新帧”和“本轮应按上一同步窗口处理的帧”互相覆盖。</p><p>TPDO 侧会多出：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">CO_SYNC_t *SYNC;</span><br><span class="line"><span class="type">uint8_t</span> syncStartValue;</span><br><span class="line"><span class="type">uint8_t</span> syncCounter;</span><br></pre></td></tr></table></figure><p><code>CO_TPDO_process()</code> 会在 <code>syncWas == true</code> 时才处理同步发送逻辑。<code>transmissionType = 1</code> 表示每次 SYNC 都发，<code>2</code> 表示每 2 次 SYNC 发，直到 <code>240</code>。<code>transmissionType = 0</code> 则是同步非周期：应用或 OD flag 先提出发送请求，真正发送等下一次 SYNC。</p><p>如果你的从站不使用同步采样&#x2F;同步控制，只做事件型 PDO，可以关闭这个能力，减少字段、分支和一个 RPDO buffer。</p><h3 id="6-6-CO-CONFIG-PDO-OD-IO-ACCESS：用-OD-IO-读写，而不是直接访问内存"><a href="#6-6-CO-CONFIG-PDO-OD-IO-ACCESS：用-OD-IO-读写，而不是直接访问内存" class="headerlink" title="6.6 CO_CONFIG_PDO_OD_IO_ACCESS：用 OD_IO 读写，而不是直接访问内存"></a>6.6 <code>CO_CONFIG_PDO_OD_IO_ACCESS</code>：用 OD_IO 读写，而不是直接访问内存</h3><p>这是 CANopenNode PDO 模块里很重要的设计分叉。</p><p>打开它时，<code>CO_PDO_common_t</code> 中保存的是：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">OD_IO_t OD_IO[CO_PDO_MAX_MAPPED_ENTRIES];</span><br></pre></td></tr></table></figure><p>RPDO 写入时调用 <code>OD_IO.write()</code>；TPDO 发送时调用 <code>OD_IO.read()</code>。优点是灵活：OD 变量可以不是一个裸内存地址，也可以通过回调访问硬件寄存器、做范围检查、做应用层封装。代价是多一点 RAM 和函数调用开销。</p><p>关闭它时，源码走简化路径，mapping 初始化阶段会把 OD 变量的原始地址拆成 <code>mapPointer[]</code>。运行时 RPDO&#x2F;TPDO 直接按字节复制内存。这更省资源，但不适合需要自定义 OD read&#x2F;write 行为的对象。</p><p>对 STM32 裸机项目来说，如果对象字典变量都只是普通 RAM 变量，直接内存方式更轻；如果你希望 PDO 映射到带副作用的对象，例如读取 ADC 快照、写入经过保护的控制量，<code>OD_IO_ACCESS</code> 更安全、也更符合 CANopenNode 的通用模型。</p><h3 id="6-7-CO-CONFIG-PDO-BITWISE-MAPPING：允许按-bit-映射"><a href="#6-7-CO-CONFIG-PDO-BITWISE-MAPPING：允许按-bit-映射" class="headerlink" title="6.7 CO_CONFIG_PDO_BITWISE_MAPPING：允许按 bit 映射"></a>6.7 <code>CO_CONFIG_PDO_BITWISE_MAPPING</code>：允许按 bit 映射</h3><p>默认情况下，CANopenNode PDO mapping 是按字节粒度处理的。也就是说，mapping length 通常要是 8 的倍数。</p><p>打开 bitwise mapping 后，<code>OD_IO.stream.dataOffset</code> 不再临时保存“字节数”，而是保存“bit 数”。源码会在 RPDO 写入和 TPDO 读取时使用 64-bit 临时缓冲做移位和掩码。</p><p>注意它依赖 <code>CO_CONFIG_PDO_OD_IO_ACCESS</code>。源码开头有直接编译期检查：如果开启 bitwise mapping 却没有开启 OD_IO access，会触发 <code>#error</code>。</p><p>这个功能适合把多个布尔量或小位宽状态打包进一个 PDO，但它会增加处理复杂度。对初学者和多数 STM32 控制类项目，先用字节映射更容易调试。</p><h3 id="6-8-CO-CONFIG-FLAG-CALLBACK-PRE：RPDO-预处理后唤醒任务"><a href="#6-8-CO-CONFIG-FLAG-CALLBACK-PRE：RPDO-预处理后唤醒任务" class="headerlink" title="6.8 CO_CONFIG_FLAG_CALLBACK_PRE：RPDO 预处理后唤醒任务"></a>6.8 <code>CO_CONFIG_FLAG_CALLBACK_PRE</code>：RPDO 预处理后唤醒任务</h3><p>这个 flag 会让 RPDO 支持 <code>CO_RPDO_initCallbackPre()</code>。</p><p>CAN 驱动收到 RPDO 后，<code>CO_PDO_receive()</code> 复制数据、置位 <code>CANrxNew</code>，然后如果配置了 callback pre，就调用用户注册的回调。它的典型用途不是在回调里直接写 OD，而是唤醒 RTOS 任务，让任务尽快执行 <code>CO_RPDO_process()</code>。</p><p>这很适合 FreeRTOS 场景：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">CAN RX ISR / 驱动回调</span><br><span class="line">    -&gt; CO_PDO_receive()</span><br><span class="line">    -&gt; pFunctSignalPre()</span><br><span class="line">    -&gt; 释放信号量或通知 CANopen task</span><br><span class="line">    -&gt; CANopen task 调 CO_RPDO_process()</span><br></pre></td></tr></table></figure><h3 id="6-9-CO-CONFIG-FLAG-TIMERNEXT：让-process-告诉上层下一次最晚何时再调用"><a href="#6-9-CO-CONFIG-FLAG-TIMERNEXT：让-process-告诉上层下一次最晚何时再调用" class="headerlink" title="6.9 CO_CONFIG_FLAG_TIMERNEXT：让 process 告诉上层下一次最晚何时再调用"></a>6.9 <code>CO_CONFIG_FLAG_TIMERNEXT</code>：让 process 告诉上层下一次最晚何时再调用</h3><p>CANopenNode 的 process 函数通常会接收 <code>timerNext_us</code>。打开这个 flag 后，PDO 处理函数会根据内部 timer 计算下一次需要被调用的时间。</p><p>对裸机超级循环来说，它可以减少无意义轮询。对 RTOS 或低功耗 MCU 来说，它可以帮助决定 task delay 或休眠时间。</p><p>注意你贴出的注释写的是 “inside <code>CO_TPDO_process()</code>”，但上传的 <code>CO_PDO.c</code> 中 RPDO timeout 分支也会在相关条件下更新 <code>timerNext_us</code>。实际工程应以当前源码为准。</p><h3 id="6-10-CO-CONFIG-FLAG-OD-DYNAMIC：允许运行时通过写-OD-重配-PDO"><a href="#6-10-CO-CONFIG-FLAG-OD-DYNAMIC：允许运行时通过写-OD-重配-PDO" class="headerlink" title="6.10 CO_CONFIG_FLAG_OD_DYNAMIC：允许运行时通过写 OD 重配 PDO"></a>6.10 <code>CO_CONFIG_FLAG_OD_DYNAMIC</code>：允许运行时通过写 OD 重配 PDO</h3><p>打开后，<code>CO_PDO_common_t</code> 会保存：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">isRPDO</span><br><span class="line">OD</span><br><span class="line">CANdevIdx</span><br><span class="line">preDefinedCanId</span><br><span class="line">configuredCanId</span><br><span class="line">OD_communicationParam_ext</span><br><span class="line">OD_mappingParam_extension</span><br></pre></td></tr></table></figure><p>初始化时，<code>CO_RPDO_init()</code> 和 <code>CO_TPDO_init()</code> 会给 communication parameter 和 mapping parameter 安装 OD extension。之后通过 SDO 写 <code>0x1400/0x1600/0x1800/0x1A00</code> 时，不再只是改 OD 存储值，而会进入 <code>OD_write_14xx()</code>、<code>OD_write_18xx()</code>、<code>OD_write_PDO_mapping()</code>，同步更新 CAN RX&#x2F;TX buffer、mapping、valid 状态等运行时对象。</p><p>如果关闭它，PDO 配置主要在 communication reset 初始化阶段确定，运行时写 OD 不会自动重配底层 CAN buffer。这更简单，也更符合固定从站固件的裁剪思路。</p><h2 id="7-CANopenNode-的-PDO-初始化主线"><a href="#7-CANopenNode-的-PDO-初始化主线" class="headerlink" title="7. CANopenNode 的 PDO 初始化主线"></a>7. CANopenNode 的 PDO 初始化主线</h2><p>有了上面的协议背景，再看你贴出的 <code>CO_CANopenInitPDO()</code> 就清楚很多。它不是在发送或接收 PDO，而是在“把对象字典中的 PDO 配置翻译成运行时对象”。</p><p><img src="https://i-blog.csdnimg.cn/direct/a55a9fb8e40d4c87ae97a6f04d9cf238.png" alt="在这里插入图片描述"></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">flowchart TB</span><br><span class="line">    A[&quot;CO_CANopenInitPDO()&quot;] --&gt; B&#123;&quot;co/nodeId 合法?&quot;&#125;</span><br><span class="line">    B --&gt;|否| E[&quot;返回错误&quot;]</span><br><span class="line">    B --&gt;|是| R[&quot;遍历 RPDO&lt;br/&gt;OD_GET 0x1400/0x1600&quot;]</span><br><span class="line">    R --&gt; RI[&quot;CO_RPDO_init()&lt;br/&gt;配置 RX buffer&quot;]</span><br><span class="line">    RI --&gt; T[&quot;遍历 TPDO&lt;br/&gt;OD_GET 0x1800/0x1A00&quot;]</span><br><span class="line">    T --&gt; TI[&quot;CO_TPDO_init()&lt;br/&gt;配置 TX buffer&quot;]</span><br><span class="line">    TI --&gt; OK[&quot;CO_ERROR_NO&quot;]</span><br></pre></td></tr></table></figure><h3 id="7-1-为什么要先算-preDefinedCanId"><a href="#7-1-为什么要先算-preDefinedCanId" class="headerlink" title="7.1 为什么要先算 preDefinedCanId"></a>7.1 为什么要先算 <code>preDefinedCanId</code></h3><p>CANopen 预定义连接集中，前四个 PDO 常用 CAN-ID 是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">TPDO1 = 0x180 + Node-ID</span><br><span class="line">TPDO2 = 0x280 + Node-ID</span><br><span class="line">TPDO3 = 0x380 + Node-ID</span><br><span class="line">TPDO4 = 0x480 + Node-ID</span><br><span class="line"></span><br><span class="line">RPDO1 = 0x200 + Node-ID</span><br><span class="line">RPDO2 = 0x300 + Node-ID</span><br><span class="line">RPDO3 = 0x400 + Node-ID</span><br><span class="line">RPDO4 = 0x500 + Node-ID</span><br></pre></td></tr></table></figure><p>你贴出的 <code>CO_CANopenInitPDO()</code> 会根据 <code>i</code> 和 <code>nodeId</code> 计算这个默认 CAN-ID，再传给 <code>CO_RPDO_init()</code> 或 <code>CO_TPDO_init()</code>。这个值有两个作用：</p><ol><li>初始化时，如果 OD 中保存的是默认 CAN-ID 的“基值”，源码可以补上当前 Node-ID。</li><li>动态 OD 读写时，如果识别为预定义 CAN-ID，源码可以把 OD 内部存储和当前 Node-ID 解耦，避免 LSS 改 Node-ID 后默认 CAN-ID 不更新。</li></ol><p>这也是为什么 <code>preDefinedCanId</code> 看起来像“多余参数”，但实际和 Node-ID 动态变化相关。</p><h3 id="7-2-CO-RPDO-init-初始化的是接收路径"><a href="#7-2-CO-RPDO-init-初始化的是接收路径" class="headerlink" title="7.2 CO_RPDO_init() 初始化的是接收路径"></a>7.2 <code>CO_RPDO_init()</code> 初始化的是接收路径</h3><p><code>CO_RPDO_init()</code> 做的事情可以按顺序理解：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">1. 清空 CO_RPDO_t</span><br><span class="line">2. 保存 Emergency 指针和 CAN 模块指针</span><br><span class="line">3. 调 PDO_initMapping(..., isRPDO = true)</span><br><span class="line">4. 读取 0x14xx sub1 COB-ID</span><br><span class="line">5. 判断 valid、CAN_ID、mapping 是否有效</span><br><span class="line">6. 调 CO_CANrxBufferInit() 注册接收过滤和 CO_PDO_receive()</span><br><span class="line">7. 读取 transmission type，判断是否 synchronous</span><br><span class="line">8. 读取 event timer，换算为 timeoutTime_us</span><br><span class="line">9. 若启用 OD_DYNAMIC，安装 OD extension</span><br></pre></td></tr></table></figure><p>这里最关键的是第 6 步。RPDO 初始化会把 CAN 接收 buffer 配好，并把回调函数设为 <code>CO_PDO_receive()</code>。之后硬件或驱动层收到匹配 CAN-ID 的帧，先走这个短路径回调。</p><h3 id="7-3-CO-TPDO-init-初始化的是发送路径"><a href="#7-3-CO-TPDO-init-初始化的是发送路径" class="headerlink" title="7.3 CO_TPDO_init() 初始化的是发送路径"></a>7.3 <code>CO_TPDO_init()</code> 初始化的是发送路径</h3><p><code>CO_TPDO_init()</code> 的顺序略有不同：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">1. 清空 CO_TPDO_t</span><br><span class="line">2. 保存 Emergency 指针和 CAN 模块指针</span><br><span class="line">3. 调 PDO_initMapping(..., isRPDO = false)</span><br><span class="line">4. 读取 0x18xx sub2 transmission type</span><br><span class="line">5. 读取 0x18xx sub1 COB-ID</span><br><span class="line">6. 根据 dataLength 初始化 CANtxBuff 和 DLC</span><br><span class="line">7. 读取 inhibit time、event timer</span><br><span class="line">8. 读取 SYNC start value</span><br><span class="line">9. 若启用 OD_DYNAMIC，安装 OD extension</span><br></pre></td></tr></table></figure><p>TPDO 初始化时还会把 <code>sendRequest</code> 置为 true。不要把它理解成“初始化马上发 CAN 帧”。真正发送仍然要等 <code>CO_TPDO_process()</code> 在 Operational 状态下判断 transmission type、timer 和 SYNC 条件。</p><h2 id="8-mapping-初始化：源码把-32-bit-映射项翻译成运行时访问方式"><a href="#8-mapping-初始化：源码把-32-bit-映射项翻译成运行时访问方式" class="headerlink" title="8. mapping 初始化：源码把 32-bit 映射项翻译成运行时访问方式"></a>8. mapping 初始化：源码把 32-bit 映射项翻译成运行时访问方式</h2><p><code>PDO_initMapping()</code> 是 RPDO 和 TPDO 共用的。它不知道自己最终是接收还是发送，只通过 <code>isRPDO</code> 参数决定检查 <code>ODA_RPDO</code> 还是 <code>ODA_TPDO</code>。</p><p>核心流程是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">PDO_initMapping()</span><br><span class="line">    -&gt; 读取 mapping sub0，得到 mappedObjectsCount</span><br><span class="line">    -&gt; 遍历 sub1..sub8</span><br><span class="line">    -&gt; 读取 32-bit map</span><br><span class="line">    -&gt; PDOconfigMap() 解析 index/subIndex/length</span><br><span class="line">    -&gt; 检查目标 OD 对象是否存在、长度是否足够、属性是否允许 PDO 映射</span><br><span class="line">    -&gt; 累加 PDO-&gt;dataLength</span><br></pre></td></tr></table></figure><p><code>PDOconfigMap()</code> 做的检查非常实际：</p><ul><li>总长度不能超过 <code>CO_PDO_MAX_SIZE</code>，经典 CAN 默认 8 字节。</li><li>未启用 bitwise mapping 时，映射长度必须按字节对齐。</li><li>被映射对象必须带正确方向的 PDO 属性。RPDO 不能写一个只允许 TPDO 的对象；TPDO 也不能读一个没有 TPDO 映射权限的对象。</li></ul><p>这解释了一个常见现象：<strong>对象字典里有这个变量，不代表它一定能映射到 PDO。</strong> 是否可映射要看 OD 属性。</p><p>如果启用 <code>CO_CONFIG_PDO_OD_IO_ACCESS</code>，源码会把每个映射项保存成 <code>OD_IO_t</code>，运行时通过 <code>read()</code> 或 <code>write()</code> 访问。如果未启用，源码会在初始化时保存变量内存地址，运行时直接字节复制。</p><h2 id="9-RPDO-运行流程：收到帧不是终点，只是开始"><a href="#9-RPDO-运行流程：收到帧不是终点，只是开始" class="headerlink" title="9. RPDO 运行流程：收到帧不是终点，只是开始"></a>9. RPDO 运行流程：收到帧不是终点，只是开始</h2><p>RPDO 的运行可以分成两段：CAN 接收短路径和主循环处理路径。</p><p><img src="https://i-blog.csdnimg.cn/direct/d101e4b614ee465fa5ee21c8ba226ffc.png" alt="在这里插入图片描述"></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">flowchart TB</span><br><span class="line">    A[&quot;CAN 硬件收到匹配 CAN-ID 的帧&quot;] --&gt; B[&quot;CO_PDO_receive()&lt;br/&gt;检查 valid/DLC&quot;]</span><br><span class="line">    B --&gt; C[&quot;复制到 RPDO-&gt;CANrxData[]&lt;br/&gt;置 CANrxNew[]&quot;]</span><br><span class="line">    C --&gt; D[&quot;可选 callback pre&lt;br/&gt;唤醒 RTOS task&quot;]</span><br><span class="line">    E[&quot;CO_RPDO_process() 周期调用&quot;] --&gt; F[&quot;检查 PDO valid + NMT Operational&lt;br/&gt;同步 RPDO 还要 syncWas&quot;]</span><br><span class="line">    F --&gt; G[&quot;按 mapping 切片&quot;]</span><br><span class="line">    G --&gt; H[&quot;OD_IO.write() 写对象字典变量&quot;]</span><br><span class="line">    H --&gt; I[&quot;检查 RPDO timeout&quot;]</span><br></pre></td></tr></table></figure><h3 id="9-1-CO-PDO-receive-为什么不直接写-OD"><a href="#9-1-CO-PDO-receive-为什么不直接写-OD" class="headerlink" title="9.1 CO_PDO_receive() 为什么不直接写 OD"></a>9.1 <code>CO_PDO_receive()</code> 为什么不直接写 OD</h3><p><code>CO_PDO_receive()</code> 通常由 CAN 接收中断或驱动回调调用。它做的事情很少：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">1. 读取 DLC 和 data 指针</span><br><span class="line">2. 检查 PDO-&gt;valid</span><br><span class="line">3. 检查 DLC 是否短于/长于 mapping 计算出的 dataLength</span><br><span class="line">4. 选择接收 buffer</span><br><span class="line">5. memcpy 到 RPDO-&gt;CANrxData[bufNo]</span><br><span class="line">6. CO_FLAG_SET(RPDO-&gt;CANrxNew[bufNo])</span><br><span class="line">7. 如果启用 callback pre，则唤醒上层处理任务</span><br></pre></td></tr></table></figure><p>它不写 OD，是因为 OD 写入可能涉及锁、回调、应用逻辑、大端转换、bitwise 处理、错误上报等，放在中断里会让实时性和可维护性变差。</p><p>CANopenNode 的设计是：中断里只保存最新帧，主循环或 CANopen task 再做较重处理。源码注释也说明，如果新 CAN 帧到来而上一帧还没处理，上一帧会被新帧覆盖。这对 PDO 是合理的：PDO 表示过程数据，通常最新值比历史值更重要。</p><h3 id="9-2-CO-RPDO-process-什么时候才写-OD"><a href="#9-2-CO-RPDO-process-什么时候才写-OD" class="headerlink" title="9.2 CO_RPDO_process() 什么时候才写 OD"></a>9.2 <code>CO_RPDO_process()</code> 什么时候才写 OD</h3><p><code>CO_RPDO_process()</code> 周期调用，但不是每次都处理。它至少要求：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">PDO-&gt;valid &amp;&amp; NMTisOperational</span><br></pre></td></tr></table></figure><p>如果启用了 SYNC，还要满足：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">syncWas || !RPDO-&gt;synchronous</span><br></pre></td></tr></table></figure><p>所以：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">事件型 RPDO：收到后，下一次 process 且处于 Operational 就可写 OD。</span><br><span class="line">同步型 RPDO：收到后先缓存，等下一次 SYNC 后的 process 才写 OD。</span><br></pre></td></tr></table></figure><p>这个行为和 CANopen 的 NMT 状态有关。节点在 Pre-operational 中可以用 SDO 配置 PDO，但 PDO 本身要到 Operational 才正常收发。</p><h3 id="9-3-RPDO-写-OD-的细节"><a href="#9-3-RPDO-写-OD-的细节" class="headerlink" title="9.3 RPDO 写 OD 的细节"></a>9.3 RPDO 写 OD 的细节</h3><p>进入处理后，源码会遍历 <code>PDO-&gt;mappedObjectsCount</code>。每个映射项处理一次：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">取 mappedLength</span><br><span class="line">从 dataRPDO 当前指针切出对应长度</span><br><span class="line">如果 OD 变量比映射长度大，则补零</span><br><span class="line">如果大端且多字节，则换序</span><br><span class="line">调用 OD_IO.write()</span><br><span class="line">dataRPDO += mappedLength</span><br></pre></td></tr></table></figure><p>这里有一个不直观但很重要的细节：CANopenNode 把 <code>OD_IO.stream.dataOffset</code> 临时用来保存 mapped length。写入 OD 前会把 <code>dataOffset</code> 清 0，调用 <code>write()</code>，写完再恢复成 mapped length。这样同一套 OD_IO 接口既能表达普通 OD 访问，又能在 PDO 场景中携带“本映射项用了多少字节&#x2F;bit”的信息。</p><h3 id="9-4-RPDO-timeout-不是“CAN-超时”，而是“过程数据超时”"><a href="#9-4-RPDO-timeout-不是“CAN-超时”，而是“过程数据超时”" class="headerlink" title="9.4 RPDO timeout 不是“CAN 超时”，而是“过程数据超时”"></a>9.4 RPDO timeout 不是“CAN 超时”，而是“过程数据超时”</h3><p>如果打开 <code>CO_CONFIG_RPDO_TIMERS_ENABLE</code>，并且 RPDO communication parameter 的 event timer 非 0，<code>CO_RPDO_process()</code> 会监控该 RPDO 是否长期没有新数据。</p><p>收到新 RPDO 时：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">如果之前已经超时，恢复错误</span><br><span class="line">把 timeoutTimer 重新启用</span><br></pre></td></tr></table></figure><p>没收到时：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">累加 timeoutTimer</span><br><span class="line">超过 timeoutTime_us 后报告 CO_EM_RPDO_TIME_OUT</span><br></pre></td></tr></table></figure><p>这和 Heartbeat timeout 不同。Heartbeat 监控节点是否在线，RPDO timeout 监控某条过程数据是否按期更新。实际控制系统里两者都可能需要。</p><h2 id="10-TPDO-运行流程：触发条件满足后才打包发送"><a href="#10-TPDO-运行流程：触发条件满足后才打包发送" class="headerlink" title="10. TPDO 运行流程：触发条件满足后才打包发送"></a>10. TPDO 运行流程：触发条件满足后才打包发送</h2><p>TPDO 的运行主线和 RPDO 相反。RPDO 是“收到帧 -&gt; 写 OD”；TPDO 是“从 OD 读变量 -&gt; 发帧”。</p><p><img src="https://i-blog.csdnimg.cn/direct/a9cf071b5c5242f793efd6a2cdcefc90.png" alt="在这里插入图片描述"></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">flowchart TB</span><br><span class="line">    A[&quot;CO_TPDO_process() 周期调用&quot;] --&gt; B[&quot;检查触发来源&lt;br/&gt;SYNC / event timer / sendRequest / OD flag&quot;]</span><br><span class="line">    B --&gt; C[&quot;检查 inhibit timer&lt;br/&gt;是否允许本轮发送&quot;]</span><br><span class="line">    C --&gt; D[&quot;CO_TPDOsend()&lt;br/&gt;OD_IO.read() 读取映射变量&quot;]</span><br><span class="line">    D --&gt; E[&quot;填 CANtxBuff-&gt;data[]&quot;]</span><br><span class="line">    E --&gt; F[&quot;CO_CANsend()&quot;]</span><br></pre></td></tr></table></figure><h3 id="10-1-TPDO-的触发来源有多种"><a href="#10-1-TPDO-的触发来源有多种" class="headerlink" title="10.1 TPDO 的触发来源有多种"></a>10.1 TPDO 的触发来源有多种</h3><p>CANopenNode 官方 PDO 文档列出 <code>CO_TPDO_process()</code> 的触发来源：SYNC、event timer、应用调用 <code>CO_TPDOsendRequest()</code>、或者对象字典变量通过 <code>OD_requestTPDO()</code> 请求发送。</p><p>可以把它理解成几条线汇合到一个变量：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">应用想发          -&gt; CO_TPDOsendRequest() -&gt; sendRequest = true</span><br><span class="line">OD 变量变化想发    -&gt; OD_requestTPDO() flag -&gt; process 扫描后触发</span><br><span class="line">event timer 到期   -&gt; eventTimer 归零       -&gt; sendRequest = true</span><br><span class="line">同步周期到来       -&gt; syncWas/syncCounter   -&gt; 同步发送判定</span><br></pre></td></tr></table></figure><p>但是触发不等于马上发。<code>CO_TPDO_process()</code> 还会看 NMT 状态、PDO valid、inhibit timer、CAN TX buffer 是否 busy。</p><h3 id="10-2-inhibit-time-是防刷屏的"><a href="#10-2-inhibit-time-是防刷屏的" class="headerlink" title="10.2 inhibit time 是防刷屏的"></a>10.2 <code>inhibit time</code> 是防刷屏的</h3><p>事件型 TPDO 很容易被状态变化频繁触发。如果每一次变量抖动都马上发一帧，总线可能被单个节点占满。</p><p><code>inhibit time</code> 就是最小发送间隔。即使 <code>sendRequest</code> 已经为 true，只要 inhibit timer 还没到期，本轮也不会发。</p><p>这对实际 STM32 从站很有用。例如一个输入采样值在阈值附近抖动，应用不断请求 TPDO。配置 inhibit time 后，设备可以保证“最多每 N 微秒发一次”，避免总线被抖动事件放大。</p><h3 id="10-3-event-timer-是保底更新周期"><a href="#10-3-event-timer-是保底更新周期" class="headerlink" title="10.3 event timer 是保底更新周期"></a>10.3 <code>event timer</code> 是保底更新周期</h3><p>如果过程变量长时间不变，事件型 TPDO 可能一直不发。对接收方来说，这会带来一个问题：不知道发送方是“值没变”，还是“已经不再更新”。</p><p><code>event timer</code> 解决的是保底发送：到了周期，即使没有明显事件，也可以发一次当前值。这样接收方能持续看到新鲜数据。</p><h3 id="10-4-同步-TPDO-受-SYNC-节拍控制"><a href="#10-4-同步-TPDO-受-SYNC-节拍控制" class="headerlink" title="10.4 同步 TPDO 受 SYNC 节拍控制"></a>10.4 同步 TPDO 受 SYNC 节拍控制</h3><p>同步 TPDO 只在 <code>syncWas == true</code> 的处理周期考虑发送。</p><ul><li><code>transmissionType = 1</code>：每次 SYNC 发。</li><li><code>transmissionType = 2</code>：每 2 次 SYNC 发。</li><li><code>transmissionType = 240</code>：每 240 次 SYNC 发。</li><li><code>transmissionType = 0</code>：应用或 OD flag 先请求发送，但真正发送等下一次 SYNC。</li></ul><p>如果配置了 <code>syncStartValue</code>，源码还会等 SYNC counter 到达指定值后才开始同步发送。这用于多个节点在同一个 SYNC 网络里错峰或对齐启动。</p><h3 id="10-5-CO-TPDOsend-才是真正打包函数"><a href="#10-5-CO-TPDOsend-才是真正打包函数" class="headerlink" title="10.5 CO_TPDOsend() 才是真正打包函数"></a>10.5 <code>CO_TPDOsend()</code> 才是真正打包函数</h3><p><code>CO_TPDOsend()</code> 是内部静态函数。它做的事情和 RPDO 写入相反：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">1. 检查 CANtxBuff 是否 busy</span><br><span class="line">2. 遍历 PDO-&gt;mappedObjectsCount</span><br><span class="line">3. 对每个映射项调用 OD_IO.read()</span><br><span class="line">4. 把读到的数据按 mappedLength 填入 CANtxBuff-&gt;data[]</span><br><span class="line">5. 必要时处理大端换序或 bitwise mapping</span><br><span class="line">6. 清理相关 TPDO request flag</span><br><span class="line">7. 重装 eventTimer / inhibitTimer</span><br><span class="line">8. 调 CO_CANsend()</span><br></pre></td></tr></table></figure><p>所以，应用层调用 <code>CO_TPDOsendRequest()</code> 时，不要把它理解成“直接发”。它只是让 <code>CO_TPDO_process()</code> 在合适时机调用 <code>CO_TPDOsend()</code>。</p><h2 id="11-把一个-PDO-例子从协议走到源码"><a href="#11-把一个-PDO-例子从协议走到源码" class="headerlink" title="11. 把一个 PDO 例子从协议走到源码"></a>11. 把一个 PDO 例子从协议走到源码</h2><p>假设节点 1 要用 TPDO1 上报 1 字节数字输入和 2 字节模拟输入。预定义 CAN-ID 是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">TPDO1 = 0x180 + Node-ID = 0x181</span><br></pre></td></tr></table></figure><p>对象字典可以这样配置：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">0x1800:01 = 0x00000181    ; TPDO1 enabled, CAN-ID 0x181</span><br><span class="line">0x1800:02 = 254           ; event-driven</span><br><span class="line">0x1800:03 = 100           ; inhibit time = 10 ms，单位 100us</span><br><span class="line">0x1800:05 = 1000          ; event timer = 1000 ms，单位 1ms</span><br><span class="line"></span><br><span class="line">0x1A00:00 = 2             ; 两个映射项</span><br><span class="line">0x1A00:01 = 0x60000108    ; 0x6000:01，8 bit</span><br><span class="line">0x1A00:02 = 0x64010110    ; 0x6401:01，16 bit</span><br></pre></td></tr></table></figure><p>初始化阶段：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">CO_CANopenInitPDO()</span><br><span class="line">    -&gt; CO_TPDO_init()</span><br><span class="line">       -&gt; PDO_initMapping() 解析 0x1A00</span><br><span class="line">       -&gt; dataLength = 3</span><br><span class="line">       -&gt; CO_CANtxBufferInit(..., CAN_ID = 0x181, DLC = 3)</span><br></pre></td></tr></table></figure><p>运行阶段：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">应用更新 0x6000:01 或 0x6401:01</span><br><span class="line">    -&gt; 应用调用 CO_TPDOsendRequest()，或 OD flag 请求 TPDO</span><br><span class="line">    -&gt; CO_TPDO_process() 看到 sendRequest</span><br><span class="line">    -&gt; inhibit timer 到期</span><br><span class="line">    -&gt; CO_TPDOsend()</span><br><span class="line">       -&gt; OD_IO.read(0x6000:01) -&gt; Data[0]</span><br><span class="line">       -&gt; OD_IO.read(0x6401:01) -&gt; Data[1..2]</span><br><span class="line">       -&gt; CO_CANsend() 发 CAN-ID 0x181, DLC 3</span><br></pre></td></tr></table></figure><p>接收方如果希望消费这条数据，就把某个 RPDO 的 COB-ID 配成 <code>0x181</code>，并把 RPDO mapping 配成同样的字节布局。接收方收到 CAN-ID <code>0x181</code> 后，<code>CO_PDO_receive()</code> 先缓存数据；下一轮 <code>CO_RPDO_process()</code> 再把 Byte0、Byte1..2 写入接收方自己的 OD 变量。</p><h2 id="14-一句话收束"><a href="#14-一句话收束" class="headerlink" title="14. 一句话收束"></a>14. 一句话收束</h2><p>PDO 的本质是：<strong>用对象字典提前约定一帧 CAN 数据的字节布局，再用 communication parameter 决定它的 CAN-ID 和触发时机。</strong></p><p>CANopenNode 的源码实现也围绕这句话展开：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">初始化阶段：读取 0x14xx/0x16xx/0x18xx/0x1Axx，把协议参数翻译成 CO_RPDO_t / CO_TPDO_t。</span><br><span class="line">RPDO 运行：CAN 回调只缓存，CO_RPDO_process() 在 Operational/SYNC 条件满足后按 mapping 写 OD。</span><br><span class="line">TPDO 运行：CO_TPDO_process() 根据 SYNC/event/request/timer 判断时机，CO_TPDOsend() 按 mapping 读 OD 并发 CAN。</span><br><span class="line">配置裁剪：CO_CONFIG_PDO 决定这些结构体字段、分支、timer、动态配置和 RTOS 唤醒能力是否存在。</span><br></pre></td></tr></table></figure><p>读源码时只要抓住“communication parameter 管时机与 CAN-ID，mapping parameter 管数据布局，process 函数管运行时节奏”，PDO 就不会再像一堆散乱的宏和分支。</p><hr><p>注：本文协议语义参考 CiA PDO 公开说明、CANopenNode 官方 Doxygen 与 CANopenNode 官方 GitHub 仓库；源码解释以当前上传的 <code>CO_PDO.h</code>、<code>CO_PDO.c</code>、<code>CO_config.h</code> 和你贴出的 <code>CO_CANopenInitPDO()</code> 片段为主。若实际工程使用固定旧版本，应以工程内源码和对象字典生成为准。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;meta name=&quot;referrer&quot; content=&quot;no-referrer&quot; /&gt;

&lt;h1 id=&quot;CANopen-PDO-运行流程&quot;&gt;&lt;a href=&quot;#CANopen-PDO-运行流程&quot; class=&quot;headerlink&quot; title=&quot;CANopen PDO </summary>
      
    
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/categories/canopen/"/>
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/tags/canopen/"/>
    
  </entry>
  
  <entry>
    <title>CANopen  TIME/SYNC 运行流程</title>
    <link href="https://wdfk-prog.space/posts/7a565dc9/"/>
    <id>https://wdfk-prog.space/posts/7a565dc9/</id>
    <published>2026-06-28T09:04:03.000Z</published>
    <updated>2026-06-28T09:06:14.576Z</updated>
    
    <content type="html"><![CDATA[<h1 id="CANopen-TIME-SYNC-运行流程"><a href="#CANopen-TIME-SYNC-运行流程" class="headerlink" title="CANopen  TIME&#x2F;SYNC 运行流程"></a>CANopen  TIME&#x2F;SYNC 运行流程</h1><p><img src="https://i-blog.csdnimg.cn/direct/2cade79a39b343278452dd769e3a01e8.png" alt="在这里插入图片描述"></p><p>@[toc]<br><img src="https://i-blog.csdnimg.cn/direct/10280797911f42aea1f3a3fd162066a1.png" alt="在这里插入图片描述"></p><hr><h2 id="1-先把-TIME-和-SYNC-分开"><a href="#1-先把-TIME-和-SYNC-分开" class="headerlink" title="1. 先把 TIME 和 SYNC 分开"></a>1. 先把 TIME 和 SYNC 分开</h2><p>TIME 和 SYNC 都属于 CANopen 的特殊功能通信对象，但它们解决的问题不同：</p><table><thead><tr><th>对象</th><th>核心问题</th><th align="right">默认 CAN-ID</th><th align="right">数据长度</th><th>典型用途</th></tr></thead><tbody><tr><td><code>SYNC</code></td><td>“什么时候进入一个同步周期？”</td><td align="right"><code>0x080</code></td><td align="right"><code>0</code> 或 <code>1</code></td><td>同步 TPDO&#x2F;RPDO、周期采样、周期控制</td></tr><tr><td><code>TIME</code></td><td>“网络当前时间是多少？”</td><td align="right"><code>0x100</code></td><td align="right"><code>6</code></td><td>网络时间校准、时间戳、日志时间基准</td></tr></tbody></table><p>一句话区分：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">SYNC 是节拍；TIME 是时钟。</span><br></pre></td></tr></table></figure><p><code>SYNC</code> 不携带实际过程数据，它只广播一个同步事件。同步 PDO 在这个事件前后按配置动作。<code>TIME</code> 也不携带过程数据，它只携带“当天毫秒数 + 自 1984-01-01 起的天数”。</p><hr><h2 id="2-协议侧：SYNC-规定了什么"><a href="#2-协议侧：SYNC-规定了什么" class="headerlink" title="2. 协议侧：SYNC 规定了什么"></a>2. 协议侧：SYNC 规定了什么</h2><h3 id="2-1-SYNC-的角色"><a href="#2-1-SYNC-的角色" class="headerlink" title="2.1 SYNC 的角色"></a>2.1 SYNC 的角色</h3><p>SYNC 使用 producer&#x2F;consumer 模型：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    P[&quot;SYNC producer&lt;br/&gt;周期发送同步对象&quot;] --&gt;|SYNC frame| C1[&quot;SYNC consumer A&quot;]</span><br><span class="line">    P --&gt;|SYNC frame| C2[&quot;SYNC consumer B&quot;]</span><br><span class="line">    P --&gt;|SYNC frame| C3[&quot;SYNC consumer C&quot;]</span><br><span class="line">    C1 --&gt; A1[&quot;同步 TPDO/RPDO 动作&quot;]</span><br><span class="line">    C2 --&gt; A2[&quot;同步采样或控制&quot;]</span><br><span class="line">    C3 --&gt; A3[&quot;同步窗口判断&quot;]</span><br></pre></td></tr></table></figure><p>网络中通常只有一个 SYNC producer，其他节点作为 consumer。producer 的任务是周期性发送 SYNC 帧；consumer 收到后把它当成同步边界。</p><h3 id="2-2-SYNC-帧格式"><a href="#2-2-SYNC-帧格式" class="headerlink" title="2.2 SYNC 帧格式"></a>2.2 SYNC 帧格式</h3><p><img src="https://i-blog.csdnimg.cn/direct/229333dce590492facfa6d9b05664f58.png" alt="在这里插入图片描述"></p><p>SYNC 默认是一个空数据帧：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">CAN-ID = 0x080</span><br><span class="line">DLC    = 0</span><br></pre></td></tr></table></figure><p>如果启用同步计数器，则变成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">CAN-ID = 0x080</span><br><span class="line">DLC    = 1</span><br><span class="line">Byte0  = counter</span><br></pre></td></tr></table></figure><p>计数器从 <code>1</code> 开始递增，到 <code>0x1019 Synchronous counter overflow value</code> 指定的最大值后回到 <code>1</code>。如果本地配置要求 <code>DLC=0</code>，却收到 <code>DLC=1</code>；或配置要求 <code>DLC=1</code>，却收到其他长度，consumer 应视为同步数据长度错误。</p><h3 id="2-3-与-SYNC-直接相关的对象字典"><a href="#2-3-与-SYNC-直接相关的对象字典" class="headerlink" title="2.3 与 SYNC 直接相关的对象字典"></a>2.3 与 SYNC 直接相关的对象字典</h3><table><thead><tr><th align="right">OD</th><th>名称</th><th>对源码的影响</th></tr></thead><tbody><tr><td align="right"><code>0x1005</code></td><td>COB-ID SYNC message</td><td>决定 SYNC CAN-ID；bit30 决定本节点是否 producer</td></tr><tr><td align="right"><code>0x1006</code></td><td>Communication cycle period</td><td>producer 的发送周期；consumer 的超时监控基准</td></tr><tr><td align="right"><code>0x1007</code></td><td>Synchronous window length</td><td>同步窗口；窗口外同步 PDO 应被限制</td></tr><tr><td align="right"><code>0x1019</code></td><td>Synchronous counter overflow value</td><td>为 <code>0</code> 时 SYNC 无数据；大于 <code>1</code> 时 SYNC 携带 1 字节 counter</td></tr></tbody></table><p><code>0x1006 = 0</code> 等价于不启用周期 SYNC；<code>0x1007 = 0</code> 等价于关闭同步窗口约束；<code>0x1019 = 0</code> 等价于不使用 SYNC counter。</p><h3 id="2-4-同步窗口的含义"><a href="#2-4-同步窗口的含义" class="headerlink" title="2.4 同步窗口的含义"></a>2.4 同步窗口的含义</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    S[&quot;收到/发送 SYNC&lt;br/&gt;timer = 0&quot;] --&gt; W[&quot;0 .. 0x1007&lt;br/&gt;同步窗口内&quot;]</span><br><span class="line">    W --&gt; O[&quot;timer &gt; 0x1007&lt;br/&gt;同步窗口外&quot;]</span><br><span class="line">    O --&gt; N[&quot;下一次 SYNC&lt;br/&gt;重新进入窗口&quot;]</span><br></pre></td></tr></table></figure><p>同步窗口是从 SYNC 边沿开始的一段时间。窗口内，同步 PDO 可以按配置处理；窗口外，源码用 <code>syncIsOutsideWindow</code> 记录状态，并通过 <code>CO_SYNC_PASSED_WINDOW</code> 通知上层“刚刚越过窗口”。</p><hr><h2 id="3-协议侧：TIME-规定了什么"><a href="#3-协议侧：TIME-规定了什么" class="headerlink" title="3. 协议侧：TIME 规定了什么"></a>3. 协议侧：TIME 规定了什么</h2><h3 id="3-1-TIME-的角色"><a href="#3-1-TIME-的角色" class="headerlink" title="3.1 TIME 的角色"></a>3.1 TIME 的角色</h3><p>TIME 也是 producer&#x2F;consumer 模型：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    T[&quot;TIME producer&lt;br/&gt;网络时间源&quot;] --&gt;|6-byte TIME stamp| A[&quot;TIME consumer A&quot;]</span><br><span class="line">    T --&gt;|6-byte TIME stamp| B[&quot;TIME consumer B&quot;]</span><br><span class="line">    T --&gt;|6-byte TIME stamp| C[&quot;TIME consumer C&quot;]</span><br><span class="line">    A --&gt; A1[&quot;校准本地 ms/days&quot;]</span><br><span class="line">    B --&gt; B1[&quot;日志/事件时间戳&quot;]</span><br><span class="line">    C --&gt; C1[&quot;时间相关应用&quot;]</span><br></pre></td></tr></table></figure><p>TIME 解决的是“网络时间一致性”，不是“周期动作边界”。周期动作边界仍应看 SYNC。</p><h3 id="3-2-TIME-帧格式"><a href="#3-2-TIME-帧格式" class="headerlink" title="3.2 TIME 帧格式"></a>3.2 TIME 帧格式</h3><p><img src="https://i-blog.csdnimg.cn/direct/32454a1a445a4e81a6ac190a896aeac7.png" alt="在这里插入图片描述"></p><p>TIME 的数据长度固定为 6 字节：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">CAN-ID = 0x100 默认值</span><br><span class="line">DLC    = 6</span><br><span class="line">Byte0..Byte3 = milliseconds after midnight，低 28 bit 有效</span><br><span class="line">Byte4..Byte5 = days since 1984-01-01</span><br></pre></td></tr></table></figure><p>源码中的结构对应为：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">uint32_t</span> ms;   <span class="comment">/* milliseconds after midnight */</span></span><br><span class="line"><span class="type">uint16_t</span> days; <span class="comment">/* days since January 1, 1984 */</span></span><br></pre></td></tr></table></figure><h3 id="3-3-与-TIME-直接相关的对象字典"><a href="#3-3-与-TIME-直接相关的对象字典" class="headerlink" title="3.3 与 TIME 直接相关的对象字典"></a>3.3 与 TIME 直接相关的对象字典</h3><table><thead><tr><th align="right">OD</th><th>名称</th><th>对源码的影响</th></tr></thead><tbody><tr><td align="right"><code>0x1012</code></td><td>COB-ID time stamp object</td><td>bit31 使能 consumer；bit30 使能 producer；bits0..10 是 CAN-ID</td></tr><tr><td align="right"><code>0x1013</code></td><td>High resolution time stamp</td><td>高分辨率时间戳对象；当前上传的 <code>CO_TIME.*</code> 源码没有处理它</td></tr></tbody></table><p>在 CANopenNode 的 <code>CO_TIME.h</code> 中，TIME 的 producer&#x2F;consumer 角色完全由 <code>0x1012</code> 解析得到：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">bit31 = 1 -&gt; isConsumer = true</span><br><span class="line">bit30 = 1 -&gt; isProducer = true</span><br><span class="line">bits0..10 -&gt; COB-ID，默认 0x100</span><br></pre></td></tr></table></figure><hr><h2 id="4-源码阅读地图"><a href="#4-源码阅读地图" class="headerlink" title="4. 源码阅读地图"></a>4. 源码阅读地图</h2><p><img src="https://i-blog.csdnimg.cn/direct/1358a924ba2d4142be3e89651d882210.png" alt="在这里插入图片描述"></p><h3 id="4-1-四个文件各看什么"><a href="#4-1-四个文件各看什么" class="headerlink" title="4.1 四个文件各看什么"></a>4.1 四个文件各看什么</h3><table><thead><tr><th>文件</th><th>先看位置</th><th>作用</th></tr></thead><tbody><tr><td><code>CO_SYNC.h</code></td><td><code>CO_SYNC_t</code>、<code>CO_SYNC_status_t</code>、<code>CO_SYNCsend()</code>、<code>CO_SYNC_process()</code> 声明</td><td>理解 SYNC 对象状态、返回值和发送行为</td></tr><tr><td><code>CO_SYNC.c</code></td><td><code>CO_SYNC_receive()</code>、<code>OD_write_1005()</code>、<code>OD_write_1019()</code>、<code>CO_SYNC_init()</code>、<code>CO_SYNC_process()</code></td><td>理解接收中断、OD 动态写、初始化、周期处理</td></tr><tr><td><code>CO_TIME.h</code></td><td><code>CO_TIME_t</code>、<code>CO_TIME_set()</code>、<code>CO_TIME_process()</code> 声明</td><td>理解 TIME 对象字段、设置当前时间和 producer 周期</td></tr><tr><td><code>CO_TIME.c</code></td><td><code>CO_TIME_receive()</code>、<code>OD_write_1012()</code>、<code>CO_TIME_init()</code>、<code>CO_TIME_process()</code></td><td>理解接收、OD 动态写、初始化、时间推进和发送</td></tr></tbody></table><h3 id="4-2-两个模块的共同模式"><a href="#4-2-两个模块的共同模式" class="headerlink" title="4.2 两个模块的共同模式"></a>4.2 两个模块的共同模式</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">flowchart TB</span><br><span class="line">    A[&quot;CO_xxx_init()&quot;] --&gt; B[&quot;读取对象字典&quot;]</span><br><span class="line">    B --&gt; C[&quot;配置 CAN RX buffer&quot;]</span><br><span class="line">    B --&gt; D[&quot;配置 CAN TX buffer&lt;br/&gt;如果启用 producer&quot;]</span><br><span class="line">    C --&gt; E[&quot;CAN receive callback&lt;br/&gt;只做短路径处理&quot;]</span><br><span class="line">    E --&gt; F[&quot;设置 CANrxNew flag&quot;]</span><br><span class="line">    F --&gt; G[&quot;CO_xxx_process()&lt;br/&gt;主循环/任务中处理&quot;]</span><br><span class="line">    D --&gt; G</span><br></pre></td></tr></table></figure><p>这也是 CANopenNode 很常见的设计方式：<strong>中断回调只做轻量接收和置标志，实际协议状态推进放到周期性 <code>process()</code> 里。</strong></p><hr><h2 id="5-SYNC-源码运行流程"><a href="#5-SYNC-源码运行流程" class="headerlink" title="5. SYNC 源码运行流程"></a>5. SYNC 源码运行流程</h2><h3 id="5-1-初始化：CO-SYNC-init"><a href="#5-1-初始化：CO-SYNC-init" class="headerlink" title="5.1 初始化：CO_SYNC_init()"></a>5.1 初始化：<code>CO_SYNC_init()</code></h3><p><code>CO_SYNC_init()</code> 做的事情可以按对象字典展开：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">flowchart TB</span><br><span class="line">    A[&quot;CO_SYNC_init()&quot;] --&gt; B[&quot;读取 0x1005&lt;br/&gt;COB-ID SYNC&quot;]</span><br><span class="line">    A --&gt; C[&quot;读取 0x1006&lt;br/&gt;communication cycle period&quot;]</span><br><span class="line">    A --&gt; D[&quot;读取 0x1007&lt;br/&gt;sync window，可选&quot;]</span><br><span class="line">    A --&gt; E[&quot;读取 0x1019&lt;br/&gt;counter overflow，可选&quot;]</span><br><span class="line">    B --&gt; F[&quot;解析 CAN-ID / isProducer&quot;]</span><br><span class="line">    E --&gt; G[&quot;决定 TX/RX DLC&lt;br/&gt;0 或 1&quot;]</span><br><span class="line">    F --&gt; H[&quot;CO_CANrxBufferInit()&lt;br/&gt;注册 CO_SYNC_receive&quot;]</span><br><span class="line">    G --&gt; I[&quot;CO_CANtxBufferInit()&lt;br/&gt;配置发送 buffer&quot;]</span><br></pre></td></tr></table></figure><p>关键点：</p><ol><li><code>0x1005</code> 决定 SYNC CAN-ID，并在启用 producer 时决定 <code>isProducer</code>。</li><li><code>0x1006</code> 对 producer 是必要参数；对纯 consumer，可用于超时监控。</li><li><code>0x1007</code> 是可选参数；为空或为 <code>0</code> 时不启用同步窗口。</li><li><code>0x1019</code> 是可选参数；源码会把非法值做约束：<code>1</code> 修正为 <code>2</code>，大于 <code>240</code> 修正为 <code>240</code>。</li><li>RX buffer 注册的回调是 <code>CO_SYNC_receive()</code>。</li></ol><h3 id="5-2-接收中断：CO-SYNC-receive"><a href="#5-2-接收中断：CO-SYNC-receive" class="headerlink" title="5.2 接收中断：CO_SYNC_receive()"></a>5.2 接收中断：<code>CO_SYNC_receive()</code></h3><p>接收回调只做三类事情：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">flowchart TB</span><br><span class="line">    A[&quot;收到 CAN frame&quot;] --&gt; B&#123;&quot;counterOverflowValue == 0 ?&quot;&#125;</span><br><span class="line">    B --&gt;|是| C&#123;&quot;DLC == 0 ?&quot;&#125;</span><br><span class="line">    B --&gt;|否| D&#123;&quot;DLC == 1 ?&quot;&#125;</span><br><span class="line">    C --&gt;|是| E[&quot;syncReceived = true&quot;]</span><br><span class="line">    C --&gt;|否| F[&quot;receiveError = DLC | 0x40&quot;]</span><br><span class="line">    D --&gt;|是| G[&quot;counter = data[0]&lt;br/&gt;syncReceived = true&quot;]</span><br><span class="line">    D --&gt;|否| H[&quot;receiveError = DLC | 0x80&quot;]</span><br><span class="line">    E --&gt; I[&quot;CANrxToggle 翻转&quot;]</span><br><span class="line">    G --&gt; I</span><br><span class="line">    I --&gt; J[&quot;CO_FLAG_SET(CANrxNew)&quot;]</span><br><span class="line">    J --&gt; K[&quot;可选 callback 唤醒任务&quot;]</span><br></pre></td></tr></table></figure><p>这里最重要的变量是：</p><table><thead><tr><th>变量</th><th>含义</th></tr></thead><tbody><tr><td><code>CANrxNew</code></td><td>通知 <code>CO_SYNC_process()</code> 有新 SYNC 到达</td></tr><tr><td><code>CANrxToggle</code></td><td>给同步 RPDO 双缓冲使用；每个 SYNC 边沿翻转</td></tr><tr><td><code>receiveError</code></td><td>延迟到主循环里上报 EMCY，避免中断里做重处理</td></tr><tr><td><code>counter</code></td><td>启用 1 字节 counter 时保存收到的 counter</td></tr></tbody></table><p><code>CANrxToggle</code> 容易被忽略。源码注释说明，同步 RPDO 需要双接收缓冲；SYNC 边沿到来时切换 buffer，使“正在接收”和“正在处理”的 RPDO buffer 分离。</p><h3 id="5-3-发送：CO-SYNCsend"><a href="#5-3-发送：CO-SYNCsend" class="headerlink" title="5.3 发送：CO_SYNCsend()"></a>5.3 发送：<code>CO_SYNCsend()</code></h3><p><code>CO_SYNCsend()</code> 是 <code>CO_SYNC.h</code> 中的 <code>static inline</code> 函数。它的行为很短：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">flowchart TB</span><br><span class="line">    A[&quot;CO_SYNCsend()&quot;] --&gt; B[&quot;counter++&quot;]</span><br><span class="line">    B --&gt; C&#123;&quot;counter &gt; counterOverflowValue ?&quot;&#125;</span><br><span class="line">    C --&gt;|是| D[&quot;counter = 1&quot;]</span><br><span class="line">    C --&gt;|否| E[&quot;保持当前 counter&quot;]</span><br><span class="line">    D --&gt; F[&quot;timer = 0&quot;]</span><br><span class="line">    E --&gt; F</span><br><span class="line">    F --&gt; G[&quot;CANrxToggle 翻转&quot;]</span><br><span class="line">    G --&gt; H[&quot;data[0] = counter&quot;]</span><br><span class="line">    H --&gt; I[&quot;CO_CANsend()&quot;]</span><br></pre></td></tr></table></figure><p>注意：即使 <code>counterOverflowValue == 0</code>，TX buffer 的 DLC 已在初始化时配置为 <code>0</code>，因此 <code>data[0]</code> 写入不会让帧变成 1 字节帧。帧长度由 <code>CO_CANtxBufferInit()</code> 的 DLC 参数决定。</p><h3 id="5-4-周期处理：CO-SYNC-process"><a href="#5-4-周期处理：CO-SYNC-process" class="headerlink" title="5.4 周期处理：CO_SYNC_process()"></a>5.4 周期处理：<code>CO_SYNC_process()</code></h3><p><img src="https://i-blog.csdnimg.cn/direct/34f4f3f5ad184b359e17409b85eb7ed7.png" alt="在这里插入图片描述"></p><p><code>CO_SYNC_process()</code> 是 SYNC 的主状态推进函数：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line">flowchart TB</span><br><span class="line">    A[&quot;CO_SYNC_process()&quot;] --&gt; B&#123;&quot;NMT pre-operational&lt;br/&gt;or operational ?&quot;&#125;</span><br><span class="line">    B --&gt;|否| Z[&quot;清 CANrxNew / receiveError / counter / timer&lt;br/&gt;返回 NONE&quot;]</span><br><span class="line">    B --&gt;|是| C[&quot;timer += timeDifference_us&quot;]</span><br><span class="line">    C --&gt; D&#123;&quot;CANrxNew ?&quot;&#125;</span><br><span class="line">    D --&gt;|是| E[&quot;timer = 0&lt;br/&gt;status = RX_TX&lt;br/&gt;clear CANrxNew&quot;]</span><br><span class="line">    D --&gt;|否| F[&quot;继续&quot;]</span><br><span class="line">    E --&gt; G[&quot;读取 0x1006 period&quot;]</span><br><span class="line">    F --&gt; G</span><br><span class="line">    G --&gt; H&#123;&quot;period &gt; 0 ?&quot;&#125;</span><br><span class="line">    H --&gt;|否| M[&quot;只做窗口/错误检查&quot;]</span><br><span class="line">    H --&gt;|是| I&#123;&quot;isProducer ?&quot;&#125;</span><br><span class="line">    I --&gt;|是| J&#123;&quot;timer &gt;= period ?&quot;&#125;</span><br><span class="line">    J --&gt;|是| K[&quot;CO_SYNCsend()&lt;br/&gt;status = RX_TX&quot;]</span><br><span class="line">    J --&gt;|否| L[&quot;更新 timerNext&quot;]</span><br><span class="line">    I --&gt;|否| N[&quot;consumer 超时监控&lt;br/&gt;timer &gt; 1.5 × period 则 EMCY&quot;]</span><br><span class="line">    K --&gt; M</span><br><span class="line">    L --&gt; M</span><br><span class="line">    N --&gt; M</span><br><span class="line">    M --&gt; O&#123;&quot;timer &gt; 0x1007 ?&quot;&#125;</span><br><span class="line">    O --&gt;|首次越过| P[&quot;status = PASSED_WINDOW&quot;]</span><br><span class="line">    O --&gt;|否则| Q[&quot;更新 syncIsOutsideWindow&quot;]</span><br><span class="line">    P --&gt; R[&quot;处理 receiveError&lt;br/&gt;返回 status&quot;]</span><br><span class="line">    Q --&gt; R</span><br></pre></td></tr></table></figure><p>返回值只有三种：</p><table><thead><tr><th>返回值</th><th>含义</th><th>上层怎么用</th></tr></thead><tbody><tr><td><code>CO_SYNC_NONE</code></td><td>本轮没有 SYNC 事件</td><td>常规空转</td></tr><tr><td><code>CO_SYNC_RX_TX</code></td><td>本轮收到或发送了 SYNC</td><td>驱动同步 PDO &#x2F; 应用同步动作</td></tr><tr><td><code>CO_SYNC_PASSED_WINDOW</code></td><td>本轮刚越过同步窗口</td><td>可用于禁止窗口外同步 PDO 或触发检查</td></tr></tbody></table><h3 id="5-5-consumer-超时监控"><a href="#5-5-consumer-超时监控" class="headerlink" title="5.5 consumer 超时监控"></a>5.5 consumer 超时监控</h3><p>当 <code>0x1006</code> 非零、当前节点不是 producer，并且已经收到过至少一次 SYNC 后，源码开始超时监控：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">periodTimeout = 1.5 × OD_1006_period</span><br><span class="line">if timer &gt; periodTimeout:</span><br><span class="line">    CO_errorReport(CO_EM_SYNC_TIME_OUT, ...)</span><br></pre></td></tr></table></figure><p>收到新的 SYNC 后，如果之前处于超时错误状态，则调用 <code>CO_errorReset()</code> 清除错误。</p><h3 id="5-6-0x1005-0x1019-动态写入"><a href="#5-6-0x1005-0x1019-动态写入" class="headerlink" title="5.6 0x1005 &#x2F; 0x1019 动态写入"></a>5.6 <code>0x1005</code> &#x2F; <code>0x1019</code> 动态写入</h3><p>如果启用了 <code>CO_CONFIG_FLAG_OD_DYNAMIC</code>，写对象字典时会进入扩展写函数：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">flowchart TB</span><br><span class="line">    A[&quot;SDO/本地写 OD&quot;] --&gt; B&#123;&quot;写 0x1005 ?&quot;&#125;</span><br><span class="line">    A --&gt; C&#123;&quot;写 0x1019 ?&quot;&#125;</span><br><span class="line">    B --&gt;|是| D[&quot;OD_write_1005()&lt;br/&gt;校验 COB-ID 和 producer bit&quot;]</span><br><span class="line">    D --&gt; E[&quot;必要时重新初始化 RX/TX buffer&quot;]</span><br><span class="line">    C --&gt;|是| F[&quot;OD_write_1019()&lt;br/&gt;校验 0 或 2..240&quot;]</span><br><span class="line">    F --&gt; G&#123;&quot;0x1006 period == 0 ?&quot;&#125;</span><br><span class="line">    G --&gt;|是| H[&quot;重新配置 TX DLC 0/1&quot;]</span><br><span class="line">    G --&gt;|否| I[&quot;拒绝：ODR_DATA_DEV_STATE&quot;]</span><br></pre></td></tr></table></figure><p>这里的限制很实际：<code>0x1019</code> 会改变 SYNC 帧 DLC，源码要求在通信周期未启用时修改，避免运行中改变帧格式造成网络不一致。</p><hr><h2 id="6-TIME-源码运行流程"><a href="#6-TIME-源码运行流程" class="headerlink" title="6. TIME 源码运行流程"></a>6. TIME 源码运行流程</h2><h3 id="6-1-初始化：CO-TIME-init"><a href="#6-1-初始化：CO-TIME-init" class="headerlink" title="6.1 初始化：CO_TIME_init()"></a>6.1 初始化：<code>CO_TIME_init()</code></h3><p><code>CO_TIME_init()</code> 的主线比 SYNC 更短：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">flowchart TB</span><br><span class="line">    A[&quot;CO_TIME_init()&quot;] --&gt; B[&quot;读取 0x1012&lt;br/&gt;COB-ID time stamp&quot;]</span><br><span class="line">    B --&gt; C[&quot;解析 CAN-ID&quot;]</span><br><span class="line">    B --&gt; D[&quot;bit31 -&gt; isConsumer&quot;]</span><br><span class="line">    B --&gt; E[&quot;bit30 -&gt; isProducer&quot;]</span><br><span class="line">    D --&gt; F&#123;&quot;isConsumer ?&quot;&#125;</span><br><span class="line">    F --&gt;|是| G[&quot;CO_CANrxBufferInit()&lt;br/&gt;注册 CO_TIME_receive&quot;]</span><br><span class="line">    F --&gt;|否| H[&quot;不注册接收&quot;]</span><br><span class="line">    E --&gt; I[&quot;CO_CANtxBufferInit()&lt;br/&gt;DLC = 6&quot;]</span><br></pre></td></tr></table></figure><p>关键点：</p><ol><li>TIME 的启停方向由 <code>0x1012</code> 控制，不是由 Node-ID 控制。</li><li>consumer 使能后才注册接收 buffer。</li><li>producer 相关编译能力还受 <code>CO_CONFIG_TIME_PRODUCER</code> 控制；编译时没打开 producer，运行时 <code>bit30</code> 也不能让它真正发送。</li><li>TX buffer 的 DLC 固定是 <code>CO_TIME_MSG_LENGTH = 6</code>。</li></ol><h3 id="6-2-接收中断：CO-TIME-receive"><a href="#6-2-接收中断：CO-TIME-receive" class="headerlink" title="6.2 接收中断：CO_TIME_receive()"></a>6.2 接收中断：<code>CO_TIME_receive()</code></h3><p>TIME 的接收回调只接受 6 字节帧：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">flowchart TB</span><br><span class="line">    A[&quot;收到 TIME CAN frame&quot;] --&gt; B&#123;&quot;DLC == CO_TIME_MSG_LENGTH(6) ?&quot;&#125;</span><br><span class="line">    B --&gt;|否| C[&quot;忽略&quot;]</span><br><span class="line">    B --&gt;|是| D[&quot;memcpy(data -&gt; TIME-&gt;timeStamp)&quot;]</span><br><span class="line">    D --&gt; E[&quot;CO_FLAG_SET(CANrxNew)&quot;]</span><br><span class="line">    E --&gt; F[&quot;可选 callback 唤醒任务&quot;]</span><br></pre></td></tr></table></figure><p>TIME 源码没有像 SYNC 那样记录接收长度错误。长度不等于 6 时直接忽略。</p><h3 id="6-3-周期处理：CO-TIME-process"><a href="#6-3-周期处理：CO-TIME-process" class="headerlink" title="6.3 周期处理：CO_TIME_process()"></a>6.3 周期处理：<code>CO_TIME_process()</code></h3><p><img src="https://i-blog.csdnimg.cn/direct/28e31cc88b704d5099ba7515ad76fcfd.png" alt="在这里插入图片描述"></p><p><code>CO_TIME_process()</code> 有三段逻辑：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line">flowchart TB</span><br><span class="line">    A[&quot;CO_TIME_process()&quot;] --&gt; B&#123;&quot;NMT pre-operational&lt;br/&gt;or operational&lt;br/&gt;并且 isConsumer ?&quot;&#125;</span><br><span class="line">    B --&gt;|是| C&#123;&quot;CANrxNew ?&quot;&#125;</span><br><span class="line">    C --&gt;|是| D[&quot;解析 timeStamp[0..5]&quot;]</span><br><span class="line">    D --&gt; E[&quot;ms = swapped32 &amp; 0x0FFFFFFF&lt;br/&gt;days = swapped16&lt;br/&gt;residual_us = 0&quot;]</span><br><span class="line">    E --&gt; F[&quot;timestampReceived = true&lt;br/&gt;clear CANrxNew&quot;]</span><br><span class="line">    C --&gt;|否| G[&quot;没有新 TIME&quot;]</span><br><span class="line">    B --&gt;|否| H[&quot;clear CANrxNew&quot;]</span><br><span class="line">    F --&gt; I&#123;&quot;本轮是否未收到 TIME&lt;br/&gt;且 timeDifference_us &gt; 0 ?&quot;&#125;</span><br><span class="line">    G --&gt; I</span><br><span class="line">    H --&gt; I</span><br><span class="line">    I --&gt;|是| J[&quot;用 timeDifference_us 本地累计 ms/days&quot;]</span><br><span class="line">    I --&gt;|否| K[&quot;跳过本地累计&quot;]</span><br><span class="line">    J --&gt; L&#123;&quot;isProducer 且 producerInterval_ms &gt; 0 ?&quot;&#125;</span><br><span class="line">    K --&gt; L</span><br><span class="line">    L --&gt;|是| M&#123;&quot;producerTimer_ms 到期 ?&quot;&#125;</span><br><span class="line">    M --&gt;|是| N[&quot;打包 ms/days 到 CANtxBuff&lt;br/&gt;CO_CANsend()&quot;]</span><br><span class="line">    M --&gt;|否| O[&quot;producerTimer_ms += 本轮新增 ms&quot;]</span><br><span class="line">    L --&gt;|否| P[&quot;producerTimer_ms = producerInterval_ms&quot;]</span><br><span class="line">    N --&gt; Q[&quot;返回 timestampReceived&quot;]</span><br><span class="line">    O --&gt; Q</span><br><span class="line">    P --&gt; Q</span><br></pre></td></tr></table></figure><h3 id="6-4-接收到-TIME-后如何解析"><a href="#6-4-接收到-TIME-后如何解析" class="headerlink" title="6.4 接收到 TIME 后如何解析"></a>6.4 接收到 TIME 后如何解析</h3><p>源码解析逻辑：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">uint32_t</span> ms_swapped = CO_getUint32(&amp;TIME-&gt;timeStamp[<span class="number">0</span>]);</span><br><span class="line"><span class="type">uint16_t</span> days_swapped = CO_getUint16(&amp;TIME-&gt;timeStamp[<span class="number">4</span>]);</span><br><span class="line"></span><br><span class="line">TIME-&gt;ms = CO_SWAP_32(ms_swapped) &amp; <span class="number">0x0FFFFFFFU</span>;</span><br><span class="line">TIME-&gt;days = CO_SWAP_16(days_swapped);</span><br><span class="line">TIME-&gt;residual_us = <span class="number">0</span>;</span><br></pre></td></tr></table></figure><p>要点：</p><ol><li><code>ms</code> 只取低 28 bit。</li><li><code>days</code> 是 16 bit。</li><li>收到 TIME 后清零 <code>residual_us</code>，避免前一次本地微秒余数污染对时结果。</li><li>返回值 <code>true</code> 只表示本轮刚收到并处理了 TIME stamp，不表示 producer 已发送 TIME。</li></ol><h3 id="6-5-没有收到-TIME-时如何本地走时"><a href="#6-5-没有收到-TIME-时如何本地走时" class="headerlink" title="6.5 没有收到 TIME 时如何本地走时"></a>6.5 没有收到 TIME 时如何本地走时</h3><p>如果本轮没有新 TIME，源码用 <code>timeDifference_us</code> 累加本地时间：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">uint32_t</span> us = timeDifference_us + TIME-&gt;residual_us;</span><br><span class="line">ms = us / <span class="number">1000U</span>;</span><br><span class="line">TIME-&gt;residual_us = us % <span class="number">1000U</span>;</span><br><span class="line">TIME-&gt;ms += ms;</span><br></pre></td></tr></table></figure><p>当 <code>ms</code> 超过一天时：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (TIME-&gt;ms &gt;= <span class="number">24</span>h_ms) &#123;</span><br><span class="line">    TIME-&gt;ms -= <span class="number">24</span>h_ms;</span><br><span class="line">    TIME-&gt;days += <span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这说明 TIME consumer 并不是“只靠网络 TIME 才能走时”。收到 TIME 是校准点；两次校准之间，它仍靠本地周期时间差继续推进。</p><h3 id="6-6-TIME-producer-如何启动"><a href="#6-6-TIME-producer-如何启动" class="headerlink" title="6.6 TIME producer 如何启动"></a>6.6 TIME producer 如何启动</h3><p>TIME producer 需要两件事同时成立：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">编译期：CO_CONFIG_TIME_PRODUCER 打开</span><br><span class="line">运行期：0x1012 bit30 = 1，并且 CO_TIME_set(..., producerInterval_ms &gt; 0)</span><br></pre></td></tr></table></figure><p><code>CO_TIME_set()</code> 做了三件事：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">TIME-&gt;residual_us = <span class="number">0</span>;</span><br><span class="line">TIME-&gt;ms = ms;</span><br><span class="line">TIME-&gt;days = days;</span><br><span class="line">TIME-&gt;producerTimer_ms = TIME-&gt;producerInterval_ms = producerInterval_ms;</span><br></pre></td></tr></table></figure><p>因此，producer 至少要先调用一次 <code>CO_TIME_set()</code> 设置当前时间和发送周期。只配置 <code>0x1012 bit30</code> 不足以产生周期 TIME 帧。</p><hr><h2 id="7-TIME-与-SYNC-在主循环中的关系"><a href="#7-TIME-与-SYNC-在主循环中的关系" class="headerlink" title="7. TIME 与 SYNC 在主循环中的关系"></a>7. TIME 与 SYNC 在主循环中的关系</h2><p>在 CANopenNode 的常规处理模型中，SYNC 通常先被处理，再用它的结果驱动同步 PDO：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">flowchart TB</span><br><span class="line">    A[&quot;周期 tick / RTOS task&quot;] --&gt; B[&quot;CO_process_SYNC() 或 CO_SYNC_process()&quot;]</span><br><span class="line">    B --&gt; C&#123;&quot;syncWas ?&quot;&#125;</span><br><span class="line">    C --&gt;|是| D[&quot;处理同步 RPDO/TPDO&quot;]</span><br><span class="line">    C --&gt;|否| E[&quot;处理非同步对象&quot;]</span><br><span class="line">    A --&gt; F[&quot;CO_TIME_process()&quot;]</span><br><span class="line">    F --&gt; G&#123;&quot;timestampReceived ?&quot;&#125;</span><br><span class="line">    G --&gt;|是| H[&quot;应用获得新的网络时间&quot;]</span><br><span class="line">    G --&gt;|否| I[&quot;本地时间继续递增&quot;]</span><br></pre></td></tr></table></figure><p>SYNC 和 TIME 没有直接依赖关系：</p><table><thead><tr><th>问题</th><th>看哪个对象</th></tr></thead><tbody><tr><td>周期 PDO 为什么没发？</td><td>先看 SYNC、PDO 传输类型、NMT 状态</td></tr><tr><td>网络时间为什么没更新？</td><td>先看 TIME、<code>0x1012</code>、DLC 是否为 6</td></tr><tr><td>同步窗口外 PDO 为什么被丢？</td><td>看 <code>0x1007</code>、<code>syncIsOutsideWindow</code>、PDO 处理顺序</td></tr><tr><td>节点是否收到过 SYNC？</td><td>看 <code>CO_SYNC_process()</code> 返回值和 <code>timeoutError</code></td></tr><tr><td>TIME producer 为什么没发？</td><td>看 <code>CO_TIME_set()</code> 是否设置了非零 <code>producerInterval_ms</code></td></tr></tbody></table><hr><h2 id="8-最小心智模型"><a href="#8-最小心智模型" class="headerlink" title="8. 最小心智模型"></a>8. 最小心智模型</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">flowchart TB</span><br><span class="line">    A[&quot;对象字典决定角色和参数&quot;] --&gt; B[&quot;init() 读取 OD 并配置 CAN buffer&quot;]</span><br><span class="line">    B --&gt; C[&quot;CAN receive callback 只搬运数据和置 flag&quot;]</span><br><span class="line">    C --&gt; D[&quot;process() 根据 NMT 状态、timer 和 flag 推进状态&quot;]</span><br><span class="line">    D --&gt; E[&quot;SYNC 输出同步事件/窗口状态&quot;]</span><br><span class="line">    D --&gt; F[&quot;TIME 输出 timestampReceived 并维护 ms/days&quot;]</span><br><span class="line">    E --&gt; G[&quot;PDO/应用同步逻辑&quot;]</span><br><span class="line">    F --&gt; H[&quot;日志/时间戳/应用时间基准&quot;]</span><br></pre></td></tr></table></figure><p>记住这三条就不容易读偏：</p><ol><li><code>SYNC</code> 看 <code>0x1005/0x1006/0x1007/0x1019</code>，核心是“周期边界 + 同步窗口 + counter”。</li><li><code>TIME</code> 看 <code>0x1012</code>，核心是“6 字节时间戳 + 本地走时 + 可选周期发送”。</li><li>CANopenNode 的 <code>receive()</code> 不是完整协议处理，它只是中断短路径；真正的状态变化主要在 <code>process()</code>。</li></ol><hr><h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ul><li>CiA 301 V4.2.0（中文注释版），用户上传 PDF：第 7.2.5、7.2.6、7.5.2.5、7.5.2.6、7.5.2.7、7.5.2.15、7.5.2.22 节。</li><li>CAN in Automation: Special function protocols, <a href="https://www.can-cia.org/can-knowledge/special-function-protocols">https://www.can-cia.org/can-knowledge/special-function-protocols</a></li><li>CAN in Automation: SYNC protocol, <a href="https://www.can-cia.org/can-knowledge/sync-protocol">https://www.can-cia.org/can-knowledge/sync-protocol</a></li><li>CANopenNode Doxygen: SYNC, <a href="https://canopennode.github.io/CANopenNode/group__CO__SYNC.html">https://canopennode.github.io/CANopenNode/group__CO__SYNC.html</a></li><li>CANopenNode Doxygen: TIME, <a href="https://canopennode.github.io/CANopenNode/group__CO__TIME.html">https://canopennode.github.io/CANopenNode/group__CO__TIME.html</a></li><li>CANopenNode GitHub repository, <a href="https://github.com/CANopenNode/CANopenNode">https://github.com/CANopenNode/CANopenNode</a></li><li>上传源码：<code>CO_SYNC.h</code>、<code>CO_SYNC.c</code>、<code>CO_TIME.h</code>、<code>CO_TIME.c</code></li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;CANopen-TIME-SYNC-运行流程&quot;&gt;&lt;a href=&quot;#CANopen-TIME-SYNC-运行流程&quot; class=&quot;headerlink&quot; title=&quot;CANopen  TIME&amp;#x2F;SYNC 运行流程&quot;&gt;&lt;/a&gt;CANopen  TIME&amp;</summary>
      
    
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/categories/canopen/"/>
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/tags/canopen/"/>
    
  </entry>
  
  <entry>
    <title>CANopen 搞懂 SDO client/server 运行流程</title>
    <link href="https://wdfk-prog.space/posts/e120336f/"/>
    <id>https://wdfk-prog.space/posts/e120336f/</id>
    <published>2026-06-28T08:36:03.000Z</published>
    <updated>2026-06-28T09:05:52.649Z</updated>
    
    <content type="html"><![CDATA[<meta name="referrer" content="no-referrer" /><h1 id="CANopen-搞懂-SDO-client-server-运行流程"><a href="#CANopen-搞懂-SDO-client-server-运行流程" class="headerlink" title="CANopen 搞懂 SDO client&#x2F;server 运行流程"></a>CANopen 搞懂 SDO client&#x2F;server 运行流程</h1><p><img src="https://i-blog.csdnimg.cn/direct/d156cff4789841ad82e7c3b9cf5af332.png" alt="在这里插入图片描述"></p><p>@[toc]</p><h2 id="先给结论"><a href="#先给结论" class="headerlink" title="先给结论"></a>先给结论</h2><p>SDO（Service Data Object）是 CANopen 里用于<strong>读写对象字典</strong>的标准服务。它不是实时过程数据通道，也不是 Heartbeat 那类状态上报通道。它更像“标准化的配置&#x2F;诊断访问入口”：</p><ol><li><p><strong>SDO 永远由 client 发起。</strong><br>client 访问另一个节点的对象字典；拥有对象字典的一方是 server。</p></li><li><p><strong>download &#x2F; upload 是站在 server 角度命名。</strong>  </p><ul><li><code>SDO download</code>：client 把数据写入 server 的对象字典。  </li><li><code>SDO upload</code>：client 从 server 的对象字典读取数据。</li></ul></li><li><p><strong>普通 CANopen 从机必须有 SDO server；SDO client 通常是可选的。</strong><br>一个普通 STM32 从机被主站配置，只需要 <code>CO_SDOserver</code>；只有当前节点要主动读写别的节点时，才需要 <code>CO_SDOclient</code>。</p></li><li><p><strong>CANopenNode 的 SDO 实现是非阻塞状态机。</strong><br>CAN 接收回调只做预处理和置位；真正协议推进发生在 <code>CO_SDOserver_process()</code>、<code>CO_SDOclientDownload()</code>、<code>CO_SDOclientUpload()</code> 周期调用里。</p></li><li><p><strong>源码运行主线可以压缩成一句话：</strong><br><code>CAN 帧 -&gt; SDO state -&gt; OD_find/OD_getSub -&gt; OD_IO.read/write -&gt; response/abort -&gt; state idle</code></p></li></ol><p><img src="https://i-blog.csdnimg.cn/direct/fb699c64c6c547659ce6c255f9c86586.png" alt="在这里插入图片描述"></p><hr><h2 id="1-先看协议：SDO-解决什么问题"><a href="#1-先看协议：SDO-解决什么问题" class="headerlink" title="1. 先看协议：SDO 解决什么问题"></a>1. 先看协议：SDO 解决什么问题</h2><p>CANopen 的对象字典（Object Dictionary，OD）把设备参数、通信参数和应用参数统一放在 <code>index + subIndex</code> 地址空间里。SDO 的作用，就是通过 CAN 帧访问这些对象字典条目。</p><p>SDO 采用 client&#x2F;server 模型：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant C as SDO Client</span><br><span class="line">    participant CAN as CAN bus</span><br><span class="line">    participant S as SDO Server</span><br><span class="line">    participant OD as Object Dictionary</span><br><span class="line"></span><br><span class="line">    C-&gt;&gt;CAN: request(index, subIndex, command)</span><br><span class="line">    CAN-&gt;&gt;S: client -&gt;&gt; server COB-ID</span><br><span class="line">    S-&gt;&gt;OD: find index/subIndex + check access</span><br><span class="line">    OD--&gt;&gt;S: read/write result</span><br><span class="line">    S-&gt;&gt;CAN: response or abort</span><br><span class="line">    CAN--&gt;&gt;C: server -&gt;&gt; client COB-ID</span><br></pre></td></tr></table></figure><p>CiA 对 SDO 的公开说明中，SDO 用于访问 CANopen CC 设备对象字典的所有条目；一个 SDO 通道由两个不同 CAN identifier 的 CAN 帧组成，并且是确认式通信服务。CANopenNode 官方文档也说明，SDO 可访问对象字典任意条目，并通过 segmented&#x2F;block 方式传输超过单帧的数据。[^cia-sdo][^canopennode-sdo-server]</p><h3 id="1-1-SDO-和-PDO-的边界"><a href="#1-1-SDO-和-PDO-的边界" class="headerlink" title="1.1 SDO 和 PDO 的边界"></a>1.1 SDO 和 PDO 的边界</h3><table><thead><tr><th>对比项</th><th>SDO</th><th>PDO</th></tr></thead><tbody><tr><td>通信模型</td><td>client&#x2F;server，请求&#x2F;响应</td><td>producer&#x2F;consumer，通常无确认</td></tr><tr><td>主要用途</td><td>配置、诊断、参数读写、大块数据</td><td>实时过程数据</td></tr><tr><td>数据地址</td><td>每次携带 <code>index + subIndex</code></td><td>由 PDO mapping 预配置</td></tr><tr><td>开销</td><td>较高，有命令字和确认</td><td>低，适合周期&#x2F;事件触发实时数据</td></tr><tr><td>是否适合高频控制环</td><td>不适合</td><td>适合</td></tr></tbody></table><h3 id="1-2-download-upload-的方向不要看反"><a href="#1-2-download-upload-的方向不要看反" class="headerlink" title="1.2 download &#x2F; upload 的方向不要看反"></a>1.2 download &#x2F; upload 的方向不要看反</h3><p>SDO 的术语容易和“PC 上传&#x2F;下载”混淆。CANopen 按 <strong>server 对象字典</strong>视角命名：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    C[&quot;SDO client&quot;] -- &quot;download&lt;br/&gt;写数据到 server OD&quot; --&gt; S[&quot;SDO server&quot;]</span><br><span class="line">    S -- &quot;upload&lt;br/&gt;从 server OD 返回数据&quot; --&gt; C</span><br></pre></td></tr></table></figure><p>因此：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">CO_SDOclientDownload*()  -&gt; client 写远端 OD</span><br><span class="line">CO_SDOclientUpload*()    -&gt; client 读远端 OD</span><br></pre></td></tr></table></figure><hr><h2 id="2-SDO-通道和对象字典参数"><a href="#2-SDO-通道和对象字典参数" class="headerlink" title="2. SDO 通道和对象字典参数"></a>2. SDO 通道和对象字典参数</h2><p>SDO 通信至少需要两条 CAN 报文方向：</p><table><thead><tr><th>方向</th><th align="right">默认 COB-ID</th><th>语义</th></tr></thead><tbody><tr><td>client -&gt; server</td><td align="right"><code>0x600 + Node-ID</code></td><td>client 发请求，server 接收</td></tr><tr><td>server -&gt; client</td><td align="right"><code>0x580 + Node-ID</code></td><td>server 发响应，client 接收</td></tr></tbody></table><p>CiA 301 的对象字典中，<code>0x1200..0x127F</code> 描述 SDO server 参数，<code>0x1280..0x12FF</code> 描述 SDO client 参数；子索引 <code>01h</code>、<code>02h</code> 指定两条 SDO COB-ID，客户端参数还包含 server Node-ID。[^cia301-sdo-od]</p><p>源码对应关系：</p><table><thead><tr><th>OD 对象</th><th>CANopenNode 模块</th><th>当前节点角色</th></tr></thead><tbody><tr><td><code>0x1200+ SDO server parameter</code></td><td><code>CO_SDOserver</code></td><td>别人访问本节点 OD</td></tr><tr><td><code>0x1280+ SDO client parameter</code></td><td><code>CO_SDOclient</code></td><td>本节点主动访问别人 OD</td></tr></tbody></table><hr><h2 id="3-SDO-帧格式：先读懂-Byte-0"><a href="#3-SDO-帧格式：先读懂-Byte-0" class="headerlink" title="3. SDO 帧格式：先读懂 Byte 0"></a>3. SDO 帧格式：先读懂 Byte 0</h2><p><img src="https://i-blog.csdnimg.cn/direct/1d84e9aa8065452ebcc7304be4e7aa99.png" alt="在这里插入图片描述"></p><p>经典 CAN 帧数据区只有 8 字节。SDO 把前 4 字节固定用于协议寻址和命令控制：</p><table><thead><tr><th align="right">字节</th><th>作用</th></tr></thead><tbody><tr><td align="right">Byte 0</td><td>command specifier + 标志位</td></tr><tr><td align="right">Byte 1</td><td>index 低字节</td></tr><tr><td align="right">Byte 2</td><td>index 高字节</td></tr><tr><td align="right">Byte 3</td><td>subIndex</td></tr><tr><td align="right">Byte 4..7</td><td>数据、长度、CRC 或 abort code</td></tr></tbody></table><p>CANopenNode 的 <code>CO_SDO_state_t</code> 注释直接按这些帧格式描述状态。例如：</p><table><thead><tr><th>阶段</th><th>Byte 0 典型形式</th><th>说明</th></tr></thead><tbody><tr><td>initiate download request</td><td><code>0010nnes</code></td><td>写对象；<code>e=1</code> 表示 expedited，<code>s=1</code> 表示大小有效</td></tr><tr><td>initiate download response</td><td><code>01100000</code> &#x2F; <code>0x60</code></td><td>server 确认启动写入</td></tr><tr><td>download segment request</td><td><code>000tnnnc</code></td><td>每段最多 7 字节；<code>t</code> 是 toggle；<code>c=1</code> 表示最后一段</td></tr><tr><td>download segment response</td><td><code>001t0000</code></td><td>server 确认 segment</td></tr><tr><td>initiate upload request</td><td><code>01000000</code> &#x2F; <code>0x40</code></td><td>读对象</td></tr><tr><td>initiate upload response</td><td><code>0100nnes</code></td><td>server 返回短数据或返回后续分段长度</td></tr><tr><td>upload segment request</td><td><code>011t0000</code></td><td>client 请求下一段</td></tr><tr><td>upload segment response</td><td><code>000tnnnc</code></td><td>server 返回下一段</td></tr><tr><td>abort</td><td><code>10000000</code> &#x2F; <code>0x80</code></td><td>Byte 4..7 放 SDO abort code</td></tr></tbody></table><hr><h2 id="4-三种传输方式"><a href="#4-三种传输方式" class="headerlink" title="4. 三种传输方式"></a>4. 三种传输方式</h2><h3 id="4-1-Expedited-transfer：≤-4-字节直接放启动帧"><a href="#4-1-Expedited-transfer：≤-4-字节直接放启动帧" class="headerlink" title="4.1 Expedited transfer：≤ 4 字节直接放启动帧"></a>4.1 Expedited transfer：≤ 4 字节直接放启动帧</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant C as Client</span><br><span class="line">    participant S as Server / OD</span><br><span class="line"></span><br><span class="line">    C-&gt;&gt;S: initiate download req&lt;br/&gt;Byte4..7 = data</span><br><span class="line">    S--&gt;&gt;C: initiate download rsp 0x60</span><br></pre></td></tr></table></figure><p>或读对象：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant C as Client</span><br><span class="line">    participant S as Server / OD</span><br><span class="line"></span><br><span class="line">    C-&gt;&gt;S: initiate upload req 0x40</span><br><span class="line">    S--&gt;&gt;C: initiate upload rsp&lt;br/&gt;Byte4..7 = data</span><br></pre></td></tr></table></figure><p>适用场景：<code>UNSIGNED8/16/32</code>、短枚举、控制字、参数标量。<br>协议开销最小，一次请求 + 一次响应即可完成。</p><h3 id="4-2-Segmented-transfer：每段最多-7-字节"><a href="#4-2-Segmented-transfer：每段最多-7-字节" class="headerlink" title="4.2 Segmented transfer：每段最多 7 字节"></a>4.2 Segmented transfer：每段最多 7 字节</h3><p>如果数据超过 4 字节，或者对象类型需要流式读写，SDO 进入 segmented transfer。每个 segment 的 Byte 0 用于协议控制，Byte 1..7 放数据，所以每段最多传 7 字节。CiA 对 SDO 的公开说明也明确指出 normal&#x2F;segmented transfer 每段最多携带 7 字节应用数据。[^cia-sdo]</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[&quot;initiate request&lt;br/&gt;携带 index/subIndex 和 size&quot;] --&gt; B[&quot;server 确认&quot;]</span><br><span class="line">    B --&gt; C[&quot;segment 0&lt;br/&gt;toggle = 0&quot;]</span><br><span class="line">    C --&gt; D[&quot;segment response&lt;br/&gt;确认 toggle&quot;]</span><br><span class="line">    D --&gt; E&#123;&quot;最后一段 c=1 ?&quot;&#125;</span><br><span class="line">    E -- &quot;否&quot; --&gt; F[&quot;segment 1&lt;br/&gt;toggle 翻转&quot;]</span><br><span class="line">    F --&gt; D</span><br><span class="line">    E -- &quot;是&quot; --&gt; G[&quot;结束，state 回 IDLE&quot;]</span><br></pre></td></tr></table></figure><p>这里 toggle 的作用不是加密，也不是序号；它主要用于发现“重复帧或错序帧”。源码里 <code>toggle</code> 在 segment 请求&#x2F;响应之间来回翻转。</p><h3 id="4-3-Block-transfer：大块数据降低确认开销"><a href="#4-3-Block-transfer：大块数据降低确认开销" class="headerlink" title="4.3 Block transfer：大块数据降低确认开销"></a>4.3 Block transfer：大块数据降低确认开销</h3><p>Block transfer 把多个 segment 合并成一个 block，每个 segment 使用 <code>seqno</code>，一个 block 结束后才确认 <code>ackseq</code>。CiA 对 SDO 的公开说明中，block transfer 每块最多 127 个 segment，接收方按块确认，因此适合较大数据。[^cia-sdo]</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant C as Client</span><br><span class="line">    participant S as Server</span><br><span class="line"></span><br><span class="line">    C-&gt;&gt;S: block initiate</span><br><span class="line">    S--&gt;&gt;C: block initiate response&lt;br/&gt;blksize / CRC capability</span><br><span class="line">    loop seqno = 1..blksize</span><br><span class="line">        C-&gt;&gt;S: sub-block segment&lt;br/&gt;seqno + data[7]</span><br><span class="line">    end</span><br><span class="line">    S--&gt;&gt;C: sub-block response&lt;br/&gt;ackseq + next blksize</span><br><span class="line">    C-&gt;&gt;S: block end&lt;br/&gt;noData + CRC</span><br><span class="line">    S--&gt;&gt;C: block end response</span><br></pre></td></tr></table></figure><p>CANopenNode 中 block transfer 受配置宏约束。server 侧打开 block 时必须同时打开 segmented 和 CRC16；client 侧打开 block 时还要求 FIFO 的 <code>ALT_READ</code> 和 <code>CRC16_CCITT</code> 能力。CANopenNode 官方配置文档也列出了这些依赖关系。[^canopennode-sdo-config]</p><hr><h2 id="5-源码文件分工"><a href="#5-源码文件分工" class="headerlink" title="5. 源码文件分工"></a>5. 源码文件分工</h2><p><img src="https://i-blog.csdnimg.cn/direct/0caed15e875749ea9873eff9a093888b.png" alt="在这里插入图片描述"></p><p>本文按下面顺序读源码：</p><table><thead><tr><th>文件</th><th>作用</th><th>阅读重点</th></tr></thead><tbody><tr><td><code>CO_SDOserver.h</code></td><td>协议公共定义和 server API</td><td><code>CO_SDO_state_t</code>、abort code、return code、server 对象结构</td></tr><tr><td><code>CO_SDOserver.c</code></td><td>SDO server 实现</td><td>CAN RX 回调、OD 查找、权限检查、download&#x2F;upload 应答、timeout&#x2F;abort</td></tr><tr><td><code>CO_SDOclient.h</code></td><td>client 对象和 API</td><td>FIFO 缓冲区、setup、download&#x2F;upload 调用模型</td></tr><tr><td><code>CO_SDOclient.c</code></td><td>SDO client 实现</td><td>client 状态机、非阻塞循环、本地传输、block 分支</td></tr></tbody></table><hr><h2 id="6-CO-SDO-state-t：源码状态机的骨架"><a href="#6-CO-SDO-state-t：源码状态机的骨架" class="headerlink" title="6. CO_SDO_state_t：源码状态机的骨架"></a>6. <code>CO_SDO_state_t</code>：源码状态机的骨架</h2><p><img src="https://i-blog.csdnimg.cn/direct/53c9a8e28f1642e1bdfc47ae7a4b5be9.png" alt="在这里插入图片描述"></p><p><code>CO_SDOserver.h</code> 把 SDO 状态集中定义在 <code>CO_SDO_state_t</code>。它的数值设计很规整：</p><table><thead><tr><th align="right">数值范围</th><th>含义</th></tr></thead><tbody><tr><td align="right"><code>0x00</code></td><td><code>IDLE</code> &#x2F; <code>ABORT</code></td></tr><tr><td align="right"><code>0x10</code></td><td>download 相关状态</td></tr><tr><td align="right"><code>0x20</code></td><td>upload 相关状态</td></tr><tr><td align="right"><code>0x40</code> flag</td><td>block mode 相关状态</td></tr></tbody></table><p>这和源码里的判断方式一致：先看当前状态属于 download、upload 还是 block，再进入具体分支处理。</p><p>几个主状态链路：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line">download normal:</span><br><span class="line">IDLE -&gt; DOWNLOAD_INITIATE_REQ -&gt; DOWNLOAD_INITIATE_RSP</span><br><span class="line">     -&gt; DOWNLOAD_SEGMENT_REQ -&gt; DOWNLOAD_SEGMENT_RSP -&gt; IDLE</span><br><span class="line"></span><br><span class="line">upload normal:</span><br><span class="line">IDLE -&gt; UPLOAD_INITIATE_REQ -&gt; UPLOAD_INITIATE_RSP</span><br><span class="line">     -&gt; UPLOAD_SEGMENT_REQ -&gt; UPLOAD_SEGMENT_RSP -&gt; IDLE</span><br><span class="line"></span><br><span class="line">block download:</span><br><span class="line">IDLE -&gt; DOWNLOAD_BLK_INITIATE_REQ -&gt; DOWNLOAD_BLK_INITIATE_RSP</span><br><span class="line">     -&gt; DOWNLOAD_BLK_SUBBLOCK_REQ/RSP</span><br><span class="line">     -&gt; DOWNLOAD_BLK_END_REQ/RSP -&gt; IDLE</span><br><span class="line"></span><br><span class="line">block upload:</span><br><span class="line">IDLE -&gt; UPLOAD_BLK_INITIATE_REQ -&gt; UPLOAD_BLK_INITIATE_RSP</span><br><span class="line">     -&gt; UPLOAD_BLK_INITIATE_REQ2</span><br><span class="line">     -&gt; UPLOAD_BLK_SUBBLOCK_SREQ/CRSP</span><br><span class="line">     -&gt; UPLOAD_BLK_END_SREQ/CRSP -&gt; IDLE</span><br></pre></td></tr></table></figure><hr><h2 id="7-Server-初始化：从-0x1200-到-CAN-RX-TX-buffer"><a href="#7-Server-初始化：从-0x1200-到-CAN-RX-TX-buffer" class="headerlink" title="7. Server 初始化：从 0x1200 到 CAN RX&#x2F;TX buffer"></a>7. Server 初始化：从 <code>0x1200</code> 到 CAN RX&#x2F;TX buffer</h2><p><code>CO_SDOserver_init()</code> 必须在 communication reset 阶段调用。它做的事情可以拆成 5 步：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[&quot;CO_SDOserver_init()&quot;] --&gt; B[&quot;检查参数&lt;br/&gt;SDO / OD / CANdevRx / CANdevTx&quot;]</span><br><span class="line">    B --&gt; C[&quot;绑定 OD、nodeId、timeout、state = IDLE&quot;]</span><br><span class="line">    C --&gt; D&#123;&quot;OD_1200_SDOsrvPar 是否为空？&quot;&#125;</span><br><span class="line">    D -- &quot;空：默认通道&quot; --&gt; E[&quot;COB-ID = 0x600/0x580 + nodeId&quot;]</span><br><span class="line">    D -- &quot;非空：读取 OD 参数&quot; --&gt; F[&quot;读取 0x1200+ sub1/sub2&lt;br/&gt;必要时安装 OD extension&quot;]</span><br><span class="line">    E --&gt; G[&quot;CO_SDOserver_init_canRxTx()&quot;]</span><br><span class="line">    F --&gt; G</span><br><span class="line">    G --&gt; H[&quot;配置 CANrxBufferInit()&lt;br/&gt;接收 client -&gt; server&quot;]</span><br><span class="line">    G --&gt; I[&quot;配置 CANtxBufferInit()&lt;br/&gt;发送 server -&gt; client&quot;]</span><br></pre></td></tr></table></figure><p><code>CO_SDOserver_init_canRxTx()</code> 会检查 COB-ID bit31 的 valid 位。如果 client-to-server 和 server-to-client 两个 CAN-ID 都有效，<code>SDO-&gt;valid = true</code>；否则把 CAN-ID 清零并认为通道无效。</p><p>如果启用了 <code>CO_CONFIG_FLAG_OD_DYNAMIC</code>，写 <code>0x1201+</code> 的 SDO server 参数会调用 <code>OD_write_1201_additional()</code>，再进入 <code>CO_SDOserver_init_canRxTx()</code> 重新配置 CAN RX&#x2F;TX。</p><hr><h2 id="8-Server-接收回调：只做轻量预处理"><a href="#8-Server-接收回调：只做轻量预处理" class="headerlink" title="8. Server 接收回调：只做轻量预处理"></a>8. Server 接收回调：只做轻量预处理</h2><p><code>CO_SDO_receive()</code> 是 CAN RX 回调。它不是完整协议处理函数，只处理“收到帧以后先放哪里”的问题：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[&quot;CAN 收到匹配 COB-ID 的 8 字节帧&quot;] --&gt; B&#123;&quot;DLC == 8 ?&quot;&#125;</span><br><span class="line">    B -- &quot;否&quot; --&gt; Z[&quot;忽略&quot;]</span><br><span class="line">    B -- &quot;是&quot; --&gt; C&#123;&quot;data[0] == 0x80 ?&quot;&#125;</span><br><span class="line">    C -- &quot;是，client abort&quot; --&gt; D[&quot;state = IDLE&quot;]</span><br><span class="line">    C -- &quot;否&quot; --&gt; E&#123;&quot;上一帧 CANrxNew 未处理？&quot;&#125;</span><br><span class="line">    E -- &quot;是&quot; --&gt; F[&quot;忽略，避免覆盖&quot;]</span><br><span class="line">    E -- &quot;否&quot; --&gt; G&#123;&quot;block download sub-block ?&quot;&#125;</span><br><span class="line">    G -- &quot;是&quot; --&gt; H[&quot;直接复制 7 字节数据&lt;br/&gt;检查 seqno / blksize / last&quot;]</span><br><span class="line">    G -- &quot;否&quot; --&gt; I[&quot;复制到 CANrxData[8]&lt;br/&gt;置 CANrxNew&quot;]</span><br><span class="line">    I --&gt; J[&quot;可选 pFunctSignalPre() 唤醒任务&quot;]</span><br><span class="line">    H --&gt; J</span><br></pre></td></tr></table></figure><p>普通 expedited&#x2F;segmented 帧只是复制到 <code>SDO-&gt;CANrxData</code> 并置 <code>CANrxNew</code>。block download 的子块数据较密集，源码在 RX 回调里会直接复制数据到 server 缓冲区，同时根据 <code>seqno</code> 判断是否需要进入响应状态。</p><hr><h2 id="9-CO-SDOserver-process-：server-协议推进核心"><a href="#9-CO-SDOserver-process-：server-协议推进核心" class="headerlink" title="9. CO_SDOserver_process()：server 协议推进核心"></a>9. <code>CO_SDOserver_process()</code>：server 协议推进核心</h2><p>server 的主循环处理可以理解为三段：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[&quot;CO_SDOserver_process()&quot;] --&gt; B&#123;&quot;参数 / NMT / valid 是否允许？&quot;&#125;</span><br><span class="line">    B -- &quot;不允许&quot; --&gt; C[&quot;清 CANrxNew&lt;br/&gt;state = IDLE&quot;]</span><br><span class="line">    B -- &quot;允许&quot; --&gt; D&#123;&quot;是否有新 CAN 帧？&quot;&#125;</span><br><span class="line">    D -- &quot;无&quot; --&gt; T[&quot;累计 timeout&lt;br/&gt;必要时 timerNext_us&quot;]</span><br><span class="line">    D -- &quot;有&quot; --&gt; E&#123;&quot;state == IDLE ?&quot;&#125;</span><br><span class="line">    E -- &quot;是&quot; --&gt; F[&quot;解析 Byte0&lt;br/&gt;选择 download/upload/block&quot;]</span><br><span class="line">    F --&gt; G[&quot;从 Byte1..3 取 index/subIndex&quot;]</span><br><span class="line">    G --&gt; H[&quot;OD_find + OD_getSub&quot;]</span><br><span class="line">    H --&gt; I&#123;&quot;访问权限匹配？&quot;&#125;</span><br><span class="line">    I -- &quot;否&quot; --&gt; AB[&quot;state = ABORT&quot;]</span><br><span class="line">    I -- &quot;是&quot; --&gt; J[&quot;进入具体状态处理&quot;]</span><br><span class="line">    E -- &quot;否&quot; --&gt; J</span><br><span class="line">    J --&gt; K&#123;&quot;download / upload / block 分支&quot;&#125;</span><br><span class="line">    K --&gt; L[&quot;读写 OD 或复制 buffer&quot;]</span><br><span class="line">    L --&gt; M[&quot;准备 response CAN 帧&quot;]</span><br><span class="line">    M --&gt; N&#123;&quot;完成？&quot;&#125;</span><br><span class="line">    N -- &quot;是&quot; --&gt; O[&quot;state = IDLE&lt;br/&gt;return communicationEnd&quot;]</span><br><span class="line">    N -- &quot;否&quot; --&gt; P[&quot;等待下一帧或下一轮 process&quot;]</span><br><span class="line">    AB --&gt; Q[&quot;发送 abort frame&lt;br/&gt;state = IDLE&quot;]</span><br></pre></td></tr></table></figure><p>关键点：</p><ol><li><p><strong>SDO server 只在 Pre-operational 或 Operational 状态下工作。</strong><br><code>NMTisPreOrOperational</code> 为 false 时，server 会回到 idle。</p></li><li><p><strong>新请求必须从 idle 开始。</strong><br>如果 idle 下收到 <code>Byte0</code>，源码会识别：</p><ul><li><code>0x20</code> mask：download initiate</li><li><code>0x40</code>：upload initiate</li><li>block download&#x2F;upload initiate</li><li>其他命令：abort</li></ul></li><li><p><strong>对象字典访问先于传输细节。</strong><br>server 在新请求阶段就会解析 <code>index/subIndex</code>，调用 <code>OD_find()</code>、<code>OD_getSub()</code>，再检查 <code>ODA_SDO_RW</code>、<code>ODA_SDO_R</code>、<code>ODA_SDO_W</code>。</p></li><li><p><strong>读写最终通过 OD interface 完成。</strong><br>这意味着 SDO server 不直接知道应用变量怎么存，它只通过 <code>OD_IO.read()</code> &#x2F; <code>OD_IO.write()</code> 访问对象。</p></li></ol><hr><h2 id="10-Server-download：client-写本节点对象字典"><a href="#10-Server-download：client-写本节点对象字典" class="headerlink" title="10. Server download：client 写本节点对象字典"></a>10. Server download：client 写本节点对象字典</h2><p>download 是“client 把数据下载到 server 的对象字典”。</p><h3 id="10-1-Expedited-download"><a href="#10-1-Expedited-download" class="headerlink" title="10.1 Expedited download"></a>10.1 Expedited download</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant C as Client</span><br><span class="line">    participant S as Server</span><br><span class="line">    participant OD as OD entry</span><br><span class="line"></span><br><span class="line">    C-&gt;&gt;S: 0x2F/0x2B/0x23...&lt;br/&gt;index/subIndex + data</span><br><span class="line">    S-&gt;&gt;OD: OD_getSub() + check writable</span><br><span class="line">    S-&gt;&gt;OD: OD_IO.write(data)</span><br><span class="line">    S--&gt;&gt;C: 0x60 initiate download response</span><br></pre></td></tr></table></figure><p>源码中的判断思路：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">如果 initiate download 中 e=1：</span><br><span class="line">    Byte4..7 已包含全部数据</span><br><span class="line">    根据 n 计算真实数据长度</span><br><span class="line">    写入 OD</span><br><span class="line">    finished = true</span><br><span class="line">    发送 0x60</span><br><span class="line">    state 回 IDLE</span><br></pre></td></tr></table></figure><h3 id="10-2-Segmented-download"><a href="#10-2-Segmented-download" class="headerlink" title="10.2 Segmented download"></a>10.2 Segmented download</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[&quot;收到 initiate download&lt;br/&gt;e=0，可带 size&quot;] --&gt; B[&quot;检查 OD 可写&quot;]</span><br><span class="line">    B --&gt; C[&quot;server 回 0x60&quot;]</span><br><span class="line">    C --&gt; D[&quot;等待 DOWNLOAD_SEGMENT_REQ&quot;]</span><br><span class="line">    D --&gt; E[&quot;检查 toggle&quot;]</span><br><span class="line">    E --&gt; F[&quot;复制 Byte1..7 到 buffer&quot;]</span><br><span class="line">    F --&gt; G&#123;&quot;c == 1 ?&quot;&#125;</span><br><span class="line">    G -- &quot;否&quot; --&gt; H[&quot;回 segment response&lt;br/&gt;toggle 翻转&quot;]</span><br><span class="line">    H --&gt; D</span><br><span class="line">    G -- &quot;是&quot; --&gt; I[&quot;validateAndWriteToOD()&quot;]</span><br><span class="line">    I --&gt; J[&quot;OD_IO.write()&quot;]</span><br><span class="line">    J --&gt; K[&quot;回最后一个 segment response&lt;br/&gt;state = IDLE&quot;]</span><br></pre></td></tr></table></figure><p><code>validateAndWriteToOD()</code> 是 download 写入的关键 helper。它主要做：</p><ul><li>校验最终收到长度是否等于 <code>sizeInd</code>。</li><li>必要时处理大端平台字节序。</li><li>对字符串补终止零。</li><li>block 模式下计算或校验 CRC。</li><li>调用 <code>OD_IO.write()</code> 写入对象字典。</li><li>如果 OD 写入返回错误，转换成 SDO abort code。</li></ul><hr><h2 id="11-Server-upload：client-读本节点对象字典"><a href="#11-Server-upload：client-读本节点对象字典" class="headerlink" title="11. Server upload：client 读本节点对象字典"></a>11. Server upload：client 读本节点对象字典</h2><p>upload 是“client 从 server 的对象字典上传数据”。</p><h3 id="11-1-Expedited-upload"><a href="#11-1-Expedited-upload" class="headerlink" title="11.1 Expedited upload"></a>11.1 Expedited upload</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant C as Client</span><br><span class="line">    participant S as Server</span><br><span class="line">    participant OD as OD entry</span><br><span class="line"></span><br><span class="line">    C-&gt;&gt;S: 0x40 + index/subIndex</span><br><span class="line">    S-&gt;&gt;OD: OD_getSub() + check readable</span><br><span class="line">    S-&gt;&gt;OD: OD_IO.read(max 4 bytes)</span><br><span class="line">    S--&gt;&gt;C: 0x43/0x4B/0x4F... + data</span><br></pre></td></tr></table></figure><p>如果对象大小 ≤ 4 字节，server 可以把数据直接放在 upload initiate response 的 Byte4..7 中。</p><h3 id="11-2-Segmented-upload"><a href="#11-2-Segmented-upload" class="headerlink" title="11.2 Segmented upload"></a>11.2 Segmented upload</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[&quot;client 发 0x40 upload initiate&quot;] --&gt; B[&quot;server 查 OD 并确认可读&quot;]</span><br><span class="line">    B --&gt; C&#123;&quot;size &lt;= 4 ?&quot;&#125;</span><br><span class="line">    C -- &quot;是&quot; --&gt; D[&quot;expedited response&lt;br/&gt;Byte4..7 = data&quot;]</span><br><span class="line">    C -- &quot;否&quot; --&gt; E[&quot;response 指示 segmented&lt;br/&gt;可携带 size&quot;]</span><br><span class="line">    E --&gt; F[&quot;client 请求 segment&lt;br/&gt;011t0000&quot;]</span><br><span class="line">    F --&gt; G[&quot;server 从 OD_IO.read() 取最多 7 字节&quot;]</span><br><span class="line">    G --&gt; H[&quot;server 回 segment&lt;br/&gt;000tnnnc + data&quot;]</span><br><span class="line">    H --&gt; I&#123;&quot;最后一段？&quot;&#125;</span><br><span class="line">    I -- &quot;否&quot; --&gt; F</span><br><span class="line">    I -- &quot;是&quot; --&gt; J[&quot;state = IDLE&quot;]</span><br></pre></td></tr></table></figure><p>这里要注意：server 侧不一定一次性把整个 OD 对象读到 RAM。它可以通过 <code>OD_IO.stream</code> 流式读取，尤其适合字符串、数组、domain 或应用自定义 read hook。</p><hr><h2 id="12-Client-初始化：从-0x1280-到-FIFO"><a href="#12-Client-初始化：从-0x1280-到-FIFO" class="headerlink" title="12. Client 初始化：从 0x1280 到 FIFO"></a>12. Client 初始化：从 <code>0x1280</code> 到 FIFO</h2><p><code>CO_SDOclient_init()</code> 的主线：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[&quot;CO_SDOclient_init()&quot;] --&gt; B[&quot;检查 OD_1280_SDOcliPar index 范围&quot;]</span><br><span class="line">    B --&gt; C[&quot;绑定 CANdevRx/CANdevTx/nodeId&quot;]</span><br><span class="line">    C --&gt; D[&quot;CO_fifo_init() 初始化内部环形缓冲&quot;]</span><br><span class="line">    D --&gt; E[&quot;读取 0x1280+ sub0..sub3&quot;]</span><br><span class="line">    E --&gt; F&#123;&quot;启用 OD dynamic ?&quot;&#125;</span><br><span class="line">    F -- &quot;是&quot; --&gt; G[&quot;安装 OD_write_1280 extension&quot;]</span><br><span class="line">    F -- &quot;否&quot; --&gt; H[&quot;跳过&quot;]</span><br><span class="line">    G --&gt; I[&quot;CO_SDOclient_setup()&quot;]</span><br><span class="line">    H --&gt; I</span><br><span class="line">    I --&gt; J[&quot;配置接收 server-&gt;client&lt;br/&gt;配置发送 client-&gt;server&quot;]</span><br></pre></td></tr></table></figure><p><code>CO_SDOclient_setup()</code> 既可由初始化调用，也可由应用在访问不同远端节点前主动调用。CANopenNode 官方文档给出的典型用法也是先 <code>setup()</code>，再 <code>UploadInitiate()</code> 或 <code>DownloadInitiate()</code>，随后循环调用处理函数直到返回值 <code>&lt;= 0</code>。[^canopennode-sdo-client]</p><p>client 对象内部有 <code>CO_fifo_t bufFifo</code>。download 时应用先把要写的数据放入 FIFO；upload 时协议栈把收到的数据放进 FIFO，应用再读出。</p><hr><h2 id="13-Client-download：本节点主动写远端-OD"><a href="#13-Client-download：本节点主动写远端-OD" class="headerlink" title="13. Client download：本节点主动写远端 OD"></a>13. Client download：本节点主动写远端 OD</h2><p>client 侧 download 的调用链：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[&quot;CO_SDOclient_setup()&quot;] --&gt; B[&quot;CO_SDOclientDownloadInitiate(index, subIndex, size, timeout, blockEnable)&quot;]</span><br><span class="line">    B --&gt; C[&quot;CO_SDOclientDownloadBufWrite(data)&quot;]</span><br><span class="line">    C --&gt; D[&quot;周期调用 CO_SDOclientDownload(dt, ...)&quot;]</span><br><span class="line">    D --&gt; E&#123;&quot;返回值 &gt; 0 ?&quot;&#125;</span><br><span class="line">    E -- &quot;是&quot; --&gt; D</span><br><span class="line">    E -- &quot;否，=0&quot; --&gt; F[&quot;写入成功，通信结束&quot;]</span><br><span class="line">    E -- &quot;否，&lt;0&quot; --&gt; G[&quot;读取 abortCode，处理错误&quot;]</span><br></pre></td></tr></table></figure><p>普通 segmented&#x2F;expedited download 状态推进：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant App as Application</span><br><span class="line">    participant C as SDO Client</span><br><span class="line">    participant S as SDO Server</span><br><span class="line"></span><br><span class="line">    App-&gt;&gt;C: DownloadInitiate()</span><br><span class="line">    App-&gt;&gt;C: DownloadBufWrite()</span><br><span class="line">    loop cyclic</span><br><span class="line">        App-&gt;&gt;C: CO_SDOclientDownload(dt)</span><br><span class="line">        C-&gt;&gt;S: initiate/segment request</span><br><span class="line">        S--&gt;&gt;C: response</span><br><span class="line">    end</span><br><span class="line">    C--&gt;&gt;App: communicationEnd or abortCode</span><br></pre></td></tr></table></figure><p>源码中的关键决策：</p><table><thead><tr><th>条件</th><th>client 状态</th></tr></thead><tbody><tr><td><code>nodeIDOfTheSDOServer == nodeId</code> 且启用 local</td><td><code>CO_SDO_ST_DOWNLOAD_LOCAL_TRANSFER</code></td></tr><tr><td><code>blockEnable == true</code> 且数据未知或大于阈值</td><td><code>CO_SDO_ST_DOWNLOAD_BLK_INITIATE_REQ</code></td></tr><tr><td>其他</td><td><code>CO_SDO_ST_DOWNLOAD_INITIATE_REQ</code></td></tr></tbody></table><p><code>CO_CONFIG_SDO_CLI_PST</code> 是 client block transfer 的协议切换阈值，默认 <code>21U</code>。也就是说，小数据不值得走 block；大数据才可能启用 block。</p><hr><h2 id="14-Client-upload：本节点主动读远端-OD"><a href="#14-Client-upload：本节点主动读远端-OD" class="headerlink" title="14. Client upload：本节点主动读远端 OD"></a>14. Client upload：本节点主动读远端 OD</h2><p>client 侧 upload 的调用链：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[&quot;CO_SDOclient_setup()&quot;] --&gt; B[&quot;CO_SDOclientUploadInitiate(index, subIndex, timeout, blockEnable)&quot;]</span><br><span class="line">    B --&gt; C[&quot;周期调用 CO_SDOclientUpload(dt, ...)&quot;]</span><br><span class="line">    C --&gt; D&#123;&quot;返回值 &gt; 0 ?&quot;&#125;</span><br><span class="line">    D -- &quot;是&quot; --&gt; C</span><br><span class="line">    D -- &quot;否，=0&quot; --&gt; E[&quot;CO_SDOclientUploadBufRead() 读出数据&quot;]</span><br><span class="line">    D -- &quot;否，&lt;0&quot; --&gt; F[&quot;读取 abortCode，处理错误&quot;]</span><br></pre></td></tr></table></figure><p>upload 和 download 的差别在于数据流向：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant App as Application</span><br><span class="line">    participant C as SDO Client</span><br><span class="line">    participant S as SDO Server</span><br><span class="line"></span><br><span class="line">    App-&gt;&gt;C: UploadInitiate(index/subIndex)</span><br><span class="line">    loop cyclic</span><br><span class="line">        App-&gt;&gt;C: CO_SDOclientUpload(dt)</span><br><span class="line">        C-&gt;&gt;S: initiate or segment request</span><br><span class="line">        S--&gt;&gt;C: response with data</span><br><span class="line">    end</span><br><span class="line">    App-&gt;&gt;C: UploadBufRead(buf)</span><br></pre></td></tr></table></figure><p><code>CO_SDOclientUploadBufRead()</code> 可以多次调用。官方文档也说明，数据较长时可以在多个周期里从 client 内部 FIFO 读取。[^canopennode-sdo-client]</p><hr><h2 id="15-Abort-与-timeout：错误如何结束"><a href="#15-Abort-与-timeout：错误如何结束" class="headerlink" title="15. Abort 与 timeout：错误如何结束"></a>15. Abort 与 timeout：错误如何结束</h2><p>SDO abort 帧格式固定：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">Byte0    = 0x80</span><br><span class="line">Byte1..2 = index</span><br><span class="line">Byte3    = subIndex</span><br><span class="line">Byte4..7 = abort code，little endian</span><br></pre></td></tr></table></figure><p>server 和 client 都可以发送 abort。源码中的常见触发点：</p><table><thead><tr><th>场景</th><th>结果</th></tr></thead><tbody><tr><td>command specifier 不合法</td><td><code>CO_SDO_AB_CMD</code></td></tr><tr><td>OD index&#x2F;subIndex 不存在</td><td>由 <code>OD_getSDOabCode()</code> 转换</td></tr><tr><td>没有 SDO 读写权限</td><td>read-only &#x2F; write-only &#x2F; unsupported access</td></tr><tr><td>实际长度和声明长度不一致</td><td>data long &#x2F; data short</td></tr><tr><td>segmented toggle 不匹配</td><td>toggle bit error</td></tr><tr><td>等待响应超时</td><td>timeout abort</td></tr><tr><td>CRC 不匹配</td><td>block CRC abort</td></tr></tbody></table><p>timeout 的实现方式也很直接：每轮 process 把 <code>timeDifference_us</code> 累加到 <code>timeoutTimer</code>。如果超过 <code>SDOtimeoutTime_us</code>，就转入 <code>CO_SDO_ST_ABORT</code>，准备发送 abort 帧。</p><hr><h2 id="16-CO-CONFIG-SDO-：编译能力和运行-OD-要同时满足"><a href="#16-CO-CONFIG-SDO-：编译能力和运行-OD-要同时满足" class="headerlink" title="16. CO_CONFIG_SDO_*：编译能力和运行 OD 要同时满足"></a>16. <code>CO_CONFIG_SDO_*</code>：编译能力和运行 OD 要同时满足</h2><p>CANopenNode 的配置宏只决定“代码是否编译进来”，不等价于 OD 一定配置正确。SDO 至少要同时满足：</p><ol><li>编译宏启用对应能力。</li><li>OD 中存在对应参数对象。</li><li>COB-ID 有效，且不与受限 CAN-ID 冲突。</li><li>CAN RX&#x2F;TX buffer 数量和下标正确。</li><li>NMT 状态允许 SDO 工作。</li></ol><p>常见配置判断：</p><table><thead><tr><th>需求</th><th>建议</th></tr></thead><tbody><tr><td>普通 STM32 从机，被主站读写参数</td><td>保留 SDO server；不启用 SDO client</td></tr><tr><td>当前节点要主动配置别的节点</td><td>启用 <code>CO_CONFIG_SDO_CLI_ENABLE</code></td></tr><tr><td>要读写字符串、数组、domain 等 &gt;4 字节对象</td><td>启用 segmented</td></tr><tr><td>要做固件&#x2F;大块 domain 传输</td><td>考虑 block，同时补齐 FIFO&#x2F;CRC 配置</td></tr><tr><td>RTOS 中收到 SDO 后要唤醒任务</td><td>启用 <code>CO_CONFIG_FLAG_CALLBACK_PRE</code></td></tr><tr><td>调度器想知道下一次最晚调用时间</td><td>启用 <code>CO_CONFIG_FLAG_TIMERNEXT</code></td></tr><tr><td>允许运行时改 SDO COB-ID</td><td>启用 <code>CO_CONFIG_FLAG_OD_DYNAMIC</code>，并配置对应 OD</td></tr></tbody></table><p>CANopenNode 官方配置文档说明：SDO server 可选 segmented、block、callback、timerNext、OD dynamic；SDO client 可选 enable、segmented、block、local、callback、timerNext、OD dynamic，并说明 block&#x2F;fifo&#x2F;buffer 的依赖。[^canopennode-sdo-config]</p><hr><h2 id="17-把源码放进-STM32-从机主循环"><a href="#17-把源码放进-STM32-从机主循环" class="headerlink" title="17. 把源码放进 STM32 从机主循环"></a>17. 把源码放进 STM32 从机主循环</h2><p>一个普通从机的 SDO server 调用模型通常是：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[&quot;CAN RX ISR / driver&quot;] --&gt; B[&quot;CO_SDO_receive()&lt;br/&gt;复制帧 + CANrxNew&quot;]</span><br><span class="line">    B --&gt; C[&quot;可选 callback 唤醒主循环&quot;]</span><br><span class="line">    C --&gt; D[&quot;主循环 / RTOS task&quot;]</span><br><span class="line">    D --&gt; E[&quot;CO_SDOserver_process(SDO, NMTisPreOrOperational, dt, timerNext)&quot;]</span><br><span class="line">    E --&gt; F[&quot;OD read/write&quot;]</span><br><span class="line">    F --&gt; G[&quot;CO_CANsend(response/abort)&quot;]</span><br></pre></td></tr></table></figure><p>伪代码结构：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/* communication reset section */</span></span><br><span class="line">CO_SDOserver_init(...);</span><br><span class="line"></span><br><span class="line"><span class="comment">/* main loop or CANopen task */</span></span><br><span class="line"><span class="keyword">for</span> (;;) &#123;</span><br><span class="line">    <span class="type">uint32_t</span> timerNext_us = UINT32_MAX;</span><br><span class="line">    <span class="type">bool_t</span> nmtOk = (NMTstate == CO_NMT_PRE_OPERATIONAL)</span><br><span class="line">                || (NMTstate == CO_NMT_OPERATIONAL);</span><br><span class="line"></span><br><span class="line">    (<span class="type">void</span>)CO_SDOserver_process(SDO, nmtOk, timeDifference_us, &amp;timerNext_us);</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* sleep until next CAN event or timerNext_us */</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>如果当前节点不是配置器&#x2F;网关，不要为了“支持 SDO”去打开 <code>CO_SDOclient</code>。server 和 client 是两个角色，普通从机被访问只需要 server。</p><hr><h2 id="18-几个工程判断"><a href="#18-几个工程判断" class="headerlink" title="18. 几个工程判断"></a>18. 几个工程判断</h2><h3 id="18-1-“支持-SDO”通常先看-server，不是-client"><a href="#18-1-“支持-SDO”通常先看-server，不是-client" class="headerlink" title="18.1 “支持 SDO”通常先看 server，不是 client"></a>18.1 “支持 SDO”通常先看 server，不是 client</h3><p>普通从机需要主站能读写它的对象字典，所以要确认：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">0x1200 SDO server parameter</span><br><span class="line">CO_CONFIG_SDO_SRV</span><br><span class="line">CO_RX/TX buffer for SDO server</span><br><span class="line">CO_SDOserver_process() 被周期调用</span><br></pre></td></tr></table></figure><p>只有当前节点要主动访问其他节点对象字典，才看：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">0x1280 SDO client parameter</span><br><span class="line">CO_CONFIG_SDO_CLI_ENABLE</span><br><span class="line">CO_SDOclient_setup()</span><br><span class="line">CO_SDOclientDownload/Upload()</span><br></pre></td></tr></table></figure><h3 id="18-2-CANrxNew-不是“收到了就清”"><a href="#18-2-CANrxNew-不是“收到了就清”" class="headerlink" title="18.2 CANrxNew 不是“收到了就清”"></a>18.2 <code>CANrxNew</code> 不是“收到了就清”</h3><p>server&#x2F;client 的 CAN RX 回调只设置新帧标志。真正清除发生在 process 函数已经取走并处理该帧之后。这样可以避免主循环还没处理完时被下一帧覆盖。</p><h3 id="18-3-block-transfer-不只是打开一个宏"><a href="#18-3-block-transfer-不只是打开一个宏" class="headerlink" title="18.3 block transfer 不只是打开一个宏"></a>18.3 block transfer 不只是打开一个宏</h3><p>block 需要更大的缓冲、CRC&#x2F;FIFO 支持和更复杂的时序。资源紧张 MCU 上，先跑通 expedited + segmented，再考虑 block。</p><h3 id="18-4-OD-权限错误不是-CAN-驱动错误"><a href="#18-4-OD-权限错误不是-CAN-驱动错误" class="headerlink" title="18.4 OD 权限错误不是 CAN 驱动错误"></a>18.4 OD 权限错误不是 CAN 驱动错误</h3><p>如果 SDO abort 显示只读、只写、对象不存在、类型不匹配，优先查对象字典生成结果、访问属性和 subIndex，而不是先查 CAN 波特率或滤波器。</p><hr><h2 id="19-建议阅读顺序"><a href="#19-建议阅读顺序" class="headerlink" title="19. 建议阅读顺序"></a>19. 建议阅读顺序</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[&quot;CO_SDOserver.h&lt;br/&gt;先读 state / abort / return&quot;] --&gt; B[&quot;CO_SDOserver.c::CO_SDOserver_init()&quot;]</span><br><span class="line">    B --&gt; C[&quot;CO_SDO_receive()&quot;]</span><br><span class="line">    C --&gt; D[&quot;CO_SDOserver_process()&quot;]</span><br><span class="line">    D --&gt; E[&quot;download 分支&quot;]</span><br><span class="line">    D --&gt; F[&quot;upload 分支&quot;]</span><br><span class="line">    E --&gt; G[&quot;validateAndWriteToOD()&quot;]</span><br><span class="line">    F --&gt; H[&quot;OD_IO.read() 流式读&quot;]</span><br><span class="line">    H --&gt; I[&quot;CO_SDOclient.h&lt;br/&gt;理解 client API&quot;]</span><br><span class="line">    G --&gt; I</span><br><span class="line">    I --&gt; J[&quot;CO_SDOclient.c::setup/init&quot;]</span><br><span class="line">    J --&gt; K[&quot;CO_SDOclientDownload()&quot;]</span><br><span class="line">    J --&gt; L[&quot;CO_SDOclientUpload()&quot;]</span><br></pre></td></tr></table></figure><hr><h2 id="20-一句话串起运行流程"><a href="#20-一句话串起运行流程" class="headerlink" title="20. 一句话串起运行流程"></a>20. 一句话串起运行流程</h2><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">client 选择 server Node-ID</span><br><span class="line">    -&gt; 用 0x600+Node-ID 发起 request</span><br><span class="line">    -&gt; server 收到后按 Byte0 判断 download/upload/block</span><br><span class="line">    -&gt; server 用 index/subIndex 找 OD 条目并检查权限</span><br><span class="line">    -&gt; expedited 直接读写 Byte4..7</span><br><span class="line">    -&gt; segmented/block 通过 buffer/FIFO 分段推进</span><br><span class="line">    -&gt; 任何错误都转成 abort frame</span><br><span class="line">    -&gt; 成功或中止后双方 state 回 IDLE</span><br></pre></td></tr></table></figure><p>SDO 的重点不是“怎么发 8 字节 CAN 帧”，而是 <strong>CAN 帧、对象字典、访问权限、非阻塞状态机</strong> 这四层如何连起来。读源码时只要抓住 <code>state</code>、<code>CANrxNew</code>、<code>OD_IO</code>、<code>timeoutTimer</code> 这几个变量，<code>CO_SDOserver.c</code> 和 <code>CO_SDOclient.c</code> 的分支就会清晰很多。</p><hr><h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><p>[^cia-sdo]: CAN in Automation, “SDO protocol: CAN in Automation”, <a href="https://www.can-cia.org/can-knowledge/sdo-protocol">https://www.can-cia.org/can-knowledge/sdo-protocol</a></p><p>[^canopennode-sdo-server]: CANopenNode Doxygen, “SDO server”, <a href="https://canopennode.github.io/CANopenNode/group__CO__SDOserver.html">https://canopennode.github.io/CANopenNode/group__CO__SDOserver.html</a></p><p>[^canopennode-sdo-client]: CANopenNode Doxygen, “SDO client”, <a href="https://canopennode.github.io/CANopenNode/group__CO__SDOclient.html">https://canopennode.github.io/CANopenNode/group__CO__SDOclient.html</a></p><p>[^canopennode-sdo-config]: CANopenNode Doxygen, “SDO server&#x2F;client configuration”, <a href="https://canopennode.github.io/CANopenNode/group__CO__STACK__CONFIG__SDO.html">https://canopennode.github.io/CANopenNode/group__CO__STACK__CONFIG__SDO.html</a></p><p>[^canopennode-github]: CANopenNode GitHub repository, <a href="https://github.com/CANopenNode/CANopenNode">https://github.com/CANopenNode/CANopenNode</a></p><p>[^cia301-sdo-od]: 你上传的《CiA301 V4.2.0（中文注释版）》中，目录列出 <code>7.2.4 服务数据对象(SDO)</code>，通信协议对象规范列出 <code>0x1200..0x127F SDO server parameter</code> 与 <code>0x1280..0x12FF SDO client parameter</code>。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;meta name=&quot;referrer&quot; content=&quot;no-referrer&quot; /&gt;


&lt;h1 id=&quot;CANopen-搞懂-SDO-client-server-运行流程&quot;&gt;&lt;a href=&quot;#CANopen-搞懂-SDO-client-server-运行流程&quot; cla</summary>
      
    
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/categories/canopen/"/>
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/tags/canopen/"/>
    
  </entry>
  
  <entry>
    <title>CANopene Heartbeat运行流程</title>
    <link href="https://wdfk-prog.space/posts/38629736/"/>
    <id>https://wdfk-prog.space/posts/38629736/</id>
    <published>2026-06-28T08:14:11.000Z</published>
    <updated>2026-06-28T08:14:35.800Z</updated>
    
    <content type="html"><![CDATA[<meta name="referrer" content="no-referrer" /><h1 id="CANopene-Heartbeat运行流程"><a href="#CANopene-Heartbeat运行流程" class="headerlink" title="CANopene Heartbeat运行流程"></a>CANopene Heartbeat运行流程</h1><p><img src="https://i-blog.csdnimg.cn/direct/e35939f8365d4d4cb74d576768f2882e.png" alt="在这里插入图片描述"></p><p>@[toc]</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    subgraph PNode[&quot;远端 Heartbeat Producer&lt;br/&gt;Node-ID = n&quot;]</span><br><span class="line">        P_OD[&quot;OD 0x1017&lt;br/&gt;Producer heartbeat time&lt;br/&gt;配置发送周期&quot;]</span><br><span class="line">        P[&quot;Heartbeat producer&lt;br/&gt;周期 = producer time&quot;]</span><br><span class="line">        P_OD --&gt; P</span><br><span class="line">    end</span><br><span class="line"></span><br><span class="line">    P --&gt;|周期广播| Frame[&quot;CAN 数据帧&lt;br/&gt;COB-ID = 0x700 + n&lt;br/&gt;DLC = 1&lt;br/&gt;Byte0 = NMT state&quot;]</span><br><span class="line">    Frame --&gt;|CAN 接收过滤| Cons[&quot;Heartbeat consumer&lt;br/&gt;按 expected time 等待下一帧&quot;]</span><br><span class="line"></span><br><span class="line">    subgraph CNode[&quot;本节点 Heartbeat Consumer&quot;]</span><br><span class="line">        C_OD[&quot;OD 0x1016&lt;br/&gt;高 16 位: producer Node-ID&lt;br/&gt;低 16 位: expected time(ms)&quot;]</span><br><span class="line">        C_OD --&gt;|配置监控对象| Cons</span><br><span class="line">        Cons --&gt;|未按时收到或收到 boot-up| Event[&quot;超时/远端复位事件&lt;br/&gt;上报 EMCY / 更新状态&quot;]</span><br><span class="line">    end</span><br></pre></td></tr></table></figure><h2 id="先给结论"><a href="#先给结论" class="headerlink" title="先给结论"></a>先给结论</h2><p>Heartbeat Consumer 的本质是：<strong>本节点用对象字典 <code>0x1016</code> 配置要监控的远端节点，然后监听远端节点按 <code>0x700 + Node-ID</code> 发出的 1 字节 Heartbeat；如果已开始监控后在规定时间内没收到下一帧，就把该远端节点判为 heartbeat timeout，并通过 Emergency 模块上报错误。</strong></p><p>在 CANopenNode 的 <code>CO_HBconsumer.c/h</code> 中，这条链路可以压缩成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">OD 0x1016 sub-index 写入/初始化</span><br><span class="line">    -&gt; 解析 producer Node-ID 和 consumer heartbeat time</span><br><span class="line">    -&gt; 为每个被监控节点配置一个 CAN RX buffer: 0x700 + Node-ID</span><br><span class="line">    -&gt; CAN RX 回调只保存 data[0] 和 CANrxNew 标志</span><br><span class="line">    -&gt; CO_HBconsumer_process() 周期处理</span><br><span class="line">    -&gt; 区分 boot-up / heartbeat / timeout / NMT state change</span><br><span class="line">    -&gt; 更新 HBstate、NMTstate、allMonitoredActive、allMonitoredOperational</span><br><span class="line">    -&gt; 必要时调用 CO_errorReport() / CO_errorReset()</span><br></pre></td></tr></table></figure><p>这里最重要的分工是：<strong>CAN 接收回调只做“收到了什么”的预处理；真正的状态机、超时判断、EMCY 上报都在 <code>CO_HBconsumer_process()</code> 中完成。</strong></p><hr><h2 id="1-协议先行：Heartbeat-Consumer-解决什么问题"><a href="#1-协议先行：Heartbeat-Consumer-解决什么问题" class="headerlink" title="1. 协议先行：Heartbeat Consumer 解决什么问题"></a>1. 协议先行：Heartbeat Consumer 解决什么问题</h2><p>CANopen 的 NMT 负责节点通信状态管理，Heartbeat 则是错误控制的一部分：远端节点作为 <strong>heartbeat producer</strong> 周期性广播自己的 NMT 状态；本节点作为 <strong>heartbeat consumer</strong> 接收这些状态并判断远端节点是否还在线、是否处于期望状态。</p><p>CiA 对 NMT 的公开说明中，NMT 状态机包含 Initialization、Pre-operational、Operational、Stopped；设备初始化完成后会进入 Pre-operational，并通过 boot-up message 表示已经准备好工作。NMT 命令本身由 active NMT manager 发送，CAN-ID 为 <code>0x000</code>，数据长度为 2 字节，byte0 是命令，byte1 是目标 Node-ID，Node-ID 为 <code>0</code> 表示所有节点。[^cia-nmt]</p><p>Heartbeat 与 NMT 的关系是：<strong>Heartbeat 的 1 字节数据就是远端节点当前的 NMT state。</strong> 常见状态值如下：</p><table><thead><tr><th align="right">Byte0</th><th>含义</th><th>在源码中的典型名字</th></tr></thead><tbody><tr><td align="right"><code>0x00</code></td><td>boot-up &#x2F; Initializing</td><td><code>CO_NMT_INITIALIZING</code></td></tr><tr><td align="right"><code>0x04</code></td><td>Stopped</td><td><code>CO_NMT_STOPPED</code></td></tr><tr><td align="right"><code>0x05</code></td><td>Operational</td><td><code>CO_NMT_OPERATIONAL</code></td></tr><tr><td align="right"><code>0x7F</code></td><td>Pre-operational</td><td><code>CO_NMT_PRE_OPERATIONAL</code></td></tr></tbody></table><p>Heartbeat 的 CAN 帧格式很小：</p><table><thead><tr><th>字段</th><th>值</th></tr></thead><tbody><tr><td>CAN-ID &#x2F; COB-ID</td><td><code>0x700 + producer Node-ID</code></td></tr><tr><td>DLC</td><td><code>1</code></td></tr><tr><td>Byte0</td><td>producer 当前 NMT state</td></tr></tbody></table><p>对于 consumer 来说，核心配置在 <code>0x1016 Consumer heartbeat time</code>；对于 producer 来说，核心配置在 <code>0x1017 Producer heartbeat time</code>。<code>0x1016</code> 的一个子项是 32 位值，高 16 位给出 producer Node-ID，低 16 位给出 expected time，单位为 ms。consumer 必须在该时间窗内收到被监控 producer 的 heartbeat，否则产生 heartbeat event。[^kollmorgen-hb]</p><hr><h2 id="2-0x1016-与-0x1017：不要把“我监控别人”和“我发心跳”混在一起"><a href="#2-0x1016-与-0x1017：不要把“我监控别人”和“我发心跳”混在一起" class="headerlink" title="2. 0x1016 与 0x1017：不要把“我监控别人”和“我发心跳”混在一起"></a>2. <code>0x1016</code> 与 <code>0x1017</code>：不要把“我监控别人”和“我发心跳”混在一起</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    P1017[&quot;远端节点&lt;br/&gt;0x1017 Producer heartbeat time&quot;] --&gt;|决定发送周期| HB[&quot;heartbeat 帧&lt;br/&gt;0x700 + Node-ID&lt;br/&gt;DLC = 1&lt;br/&gt;Byte0 = NMT state&quot;]</span><br><span class="line">    C1016[&quot;本节点&lt;br/&gt;0x1016 Consumer heartbeat time&quot;] --&gt;|决定监听 Node-ID&lt;br/&gt;和 expected time| Watch[&quot;Heartbeat consumer 监控窗口&quot;]</span><br><span class="line">    HB --&gt;|按周期到达| Watch</span><br><span class="line">    Watch --&gt;|超出 expected time 未收到| Timeout[&quot;heartbeat event / EMCY&quot;]</span><br></pre></td></tr></table></figure><table><thead><tr><th>对象</th><th>所在节点</th><th>方向</th><th>作用</th></tr></thead><tbody><tr><td><code>0x1017 Producer heartbeat time</code></td><td>被监控节点</td><td>发出</td><td>规定该节点多久广播一次自己的 heartbeat</td></tr><tr><td><code>0x1016 Consumer heartbeat time</code></td><td>监控者节点</td><td>接收</td><td>规定本节点要监听哪个 producer，以及多久收不到算超时</td></tr></tbody></table><p>一个典型配置是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">远端节点 3：0x1017 = 10 ms   -&gt; 每 10 ms 发一次 heartbeat</span><br><span class="line">本节点：   0x1016:01 = 0x0003_001E</span><br><span class="line">          高 16 位 0x0003 =&gt; 监听 Node-ID 3</span><br><span class="line">          低 16 位 0x001E =&gt; 30 ms 内没收到就超时</span><br></pre></td></tr></table></figure><p>通常 consumer 的 expected time 应设置得比 producer 的发送周期稍长，给 CAN 调度、主循环抖动和短时仲裁延迟留余量。公开设备文档也给出类似建议：producer 的 heartbeat 输出周期应略短于 consumer 的 expected time。[^kollmorgen-hb]</p><hr><h2 id="3-CANopenNode-里-CO-HBconsumer-t-保存了哪些状态"><a href="#3-CANopenNode-里-CO-HBconsumer-t-保存了哪些状态" class="headerlink" title="3. CANopenNode 里 CO_HBconsumer_t 保存了哪些状态"></a>3. CANopenNode 里 <code>CO_HBconsumer_t</code> 保存了哪些状态</h2><p><code>CO_HBconsumer.h</code> 中有两层结构：</p><ol><li><code>CO_HBconsumer_t</code>：整个 Heartbeat Consumer 对象。</li><li><code>CO_HBconsNode_t</code>：数组中的一个被监控远端节点。</li></ol><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    U[&quot;UNCONFIGURED&lt;br/&gt;未配置&lt;br/&gt;nodeId = 0 或 time = 0&quot;]</span><br><span class="line">    K[&quot;UNKNOWN&lt;br/&gt;已配置，尚未收到有效 heartbeat&lt;br/&gt;或收到 boot-up 后等待下一帧&quot;]</span><br><span class="line">    A[&quot;ACTIVE&lt;br/&gt;收到 heartbeat&lt;br/&gt;timeoutTimer 运行&quot;]</span><br><span class="line">    T[&quot;TIMEOUT&lt;br/&gt;超过 consumer time&lt;br/&gt;NMTstate = UNKNOWN&quot;]</span><br><span class="line"></span><br><span class="line">    U --&gt;|0x1016 写入 nodeId + time| K</span><br><span class="line">    K --&gt;|收到非 0 NMT state heartbeat&lt;br/&gt;start monitoring| A</span><br><span class="line">    A --&gt;|按时收到 heartbeat&lt;br/&gt;timeoutTimer = 0| A</span><br><span class="line">    A --&gt;|收到 boot-up(0)&lt;br/&gt;远端复位| K</span><br><span class="line">    A --&gt;|timeoutTimer &gt;= time_us| T</span><br><span class="line">    T --&gt;|再次收到 heartbeat| A</span><br><span class="line">    K --&gt;|0x1016 清零| U</span><br><span class="line">    A --&gt;|0x1016 清零| U</span><br><span class="line">    T --&gt;|0x1016 清零| U</span><br></pre></td></tr></table></figure><h3 id="3-1-每个被监控节点的状态"><a href="#3-1-每个被监控节点的状态" class="headerlink" title="3.1 每个被监控节点的状态"></a>3.1 每个被监控节点的状态</h3><p><code>CO_HBconsumer_state_t</code> 有 4 个状态：</p><table><thead><tr><th>状态</th><th>触发条件</th><th>含义</th></tr></thead><tbody><tr><td><code>CO_HBconsumer_UNCONFIGURED</code></td><td><code>nodeId == 0</code> 或 <code>consumerTime == 0</code></td><td>该子项未启用，不监控</td></tr><tr><td><code>CO_HBconsumer_UNKNOWN</code></td><td>已配置但未收到有效 heartbeat，或收到 boot-up 后等待下一帧</td><td>还不能确认远端在线</td></tr><tr><td><code>CO_HBconsumer_ACTIVE</code></td><td>收到非 boot-up heartbeat，且未超时</td><td>远端 heartbeat 正常</td></tr><tr><td><code>CO_HBconsumer_TIMEOUT</code></td><td>已 active 后超过 <code>time_us</code> 未收到下一帧</td><td>远端 heartbeat 超时</td></tr></tbody></table><p>源码中的关键成员可以按用途分成几组：</p><table><thead><tr><th>成员</th><th>用途</th></tr></thead><tbody><tr><td><code>nodeId</code></td><td>被监控 producer 的 Node-ID</td></tr><tr><td><code>NMTstate</code></td><td>最近一次 heartbeat 的 Byte0，即远端 NMT state</td></tr><tr><td><code>HBstate</code></td><td>本 consumer 对该远端节点的监控状态</td></tr><tr><td><code>timeoutTimer</code></td><td>自上次 heartbeat 后累计的时间</td></tr><tr><td><code>time_us</code></td><td>从 <code>0x1016</code> 低 16 位转换得到的超时时间，单位 us</td></tr><tr><td><code>CANrxNew</code></td><td>CAN 接收回调置位，<code>process()</code> 读取后清零</td></tr><tr><td>回调函数指针</td><td>可选：pre callback、NMT changed、heartbeat started、timeout、remote reset</td></tr></tbody></table><p><code>CO_HBconsumer_t</code> 还维护两个聚合标志：</p><table><thead><tr><th>成员</th><th>含义</th></tr></thead><tbody><tr><td><code>allMonitoredActive</code></td><td>所有已配置节点都处于 <code>ACTIVE</code>，或者没有节点被监控</td></tr><tr><td><code>allMonitoredOperational</code></td><td>所有已配置节点的 <code>NMTstate</code> 都是 <code>CO_NMT_OPERATIONAL</code>，或者没有节点被监控</td></tr></tbody></table><p>这两个标志适合应用层做“所有关键节点都在线&#x2F;都进入 Operational 了吗”的快速判断。</p><hr><h2 id="4-初始化流程：从-0x1016-到-CAN-RX-buffer"><a href="#4-初始化流程：从-0x1016-到-CAN-RX-buffer" class="headerlink" title="4. 初始化流程：从 0x1016 到 CAN RX buffer"></a>4. 初始化流程：从 <code>0x1016</code> 到 CAN RX buffer</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    Start[&quot;CO_HBconsumer_init()&lt;br/&gt;通信复位阶段调用&quot;] --&gt; Verify[&quot;检查 HBcons / em / OD / CAN 参数&quot;]</span><br><span class="line">    Verify --&gt; Bind[&quot;清零 CO_HBconsumer_t&lt;br/&gt;绑定 em、monitoredNodes、CANdevRx、CANdevRxIdxStart&quot;]</span><br><span class="line">    Bind --&gt; Count[&quot;numberOfMonitoredNodes =&lt;br/&gt;min(OD 0x1016 子项数 - 1, 外部数组容量)&quot;]</span><br><span class="line">    Count --&gt; Read[&quot;遍历 0x1016 子索引&lt;br/&gt;val = OD_get_u32()&quot;]</span><br><span class="line">    Read --&gt; Parse[&quot;解析 val&lt;br/&gt;nodeId = val &gt;&gt; 16&lt;br/&gt;consumer_time = val &amp;amp; 0xFFFF&quot;]</span><br><span class="line">    Parse --&gt; Entry[&quot;CO_HBconsumer_initEntry()&lt;br/&gt;校验重复 nodeId&lt;br/&gt;设置 nodeId / time_us / NMTstate / HBstate&quot;]</span><br><span class="line">    Entry --&gt; Rx[&quot;CO_CANrxBufferInit()&lt;br/&gt;COB-ID = 0x700 + nodeId&lt;br/&gt;mask = 0x7FF&lt;br/&gt;callback = CO_HBcons_receive&quot;]</span><br><span class="line">    Rx --&gt; More&#123;&quot;还有 0x1016 子项?&quot;&#125;</span><br><span class="line">    More --&gt;|是| Read</span><br><span class="line">    More --&gt;|否| OdDyn&#123;&quot;启用 OD_DYNAMIC?&quot;&#125;</span><br><span class="line">    OdDyn --&gt;|是| OdExt[&quot;安装 OD extension&lt;br/&gt;OD_write_1016() 写入时重新 initEntry&quot;]</span><br><span class="line">    OdDyn --&gt;|否| Done[&quot;返回 CO_ERROR_NO&lt;br/&gt;或 OD/参数错误&quot;]</span><br><span class="line">    OdExt --&gt; Done</span><br></pre></td></tr></table></figure><p><code>CO_HBconsumer_init()</code> 必须在 communication reset 阶段调用。它主要做 5 件事。</p><h3 id="4-1-绑定外部资源"><a href="#4-1-绑定外部资源" class="headerlink" title="4.1 绑定外部资源"></a>4.1 绑定外部资源</h3><p>函数先校验参数，然后清零 <code>CO_HBconsumer_t</code>，绑定：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">HBcons-&gt;em                  -&gt; Emergency 对象</span><br><span class="line">HBcons-&gt;monitoredNodes      -&gt; 外部提供的 CO_HBconsNode_t 数组</span><br><span class="line">HBcons-&gt;CANdevRx            -&gt; CAN RX 模块</span><br><span class="line">HBcons-&gt;CANdevRxIdxStart    -&gt; HB consumer 使用的 RX buffer 起始下标</span><br></pre></td></tr></table></figure><p>注意：<code>monitoredNodes</code> 数组由外部提供，CANopenNode 不在 <code>CO_HBconsumer_init()</code> 里为它动态分配内存。</p><h3 id="4-2-计算实际监控项数量"><a href="#4-2-计算实际监控项数量" class="headerlink" title="4.2 计算实际监控项数量"></a>4.2 计算实际监控项数量</h3><p>源码取下面两者的较小值：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">OD_1016_HBcons-&gt;subEntriesCount - 1</span><br><span class="line">monitoredNodesCount</span><br></pre></td></tr></table></figure><p>这表示：对象字典里配置了多少个 <code>0x1016</code> 子项是一层上限，外部数组容量又是一层上限。</p><h3 id="4-3-逐项读取-0x1016"><a href="#4-3-逐项读取-0x1016" class="headerlink" title="4.3 逐项读取 0x1016"></a>4.3 逐项读取 <code>0x1016</code></h3><p>对每个子索引：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">val = OD_get_u32(OD_1016_HBcons, i + <span class="number">1U</span>, ...);</span><br><span class="line">nodeId = (val &gt;&gt; <span class="number">16</span>) &amp; <span class="number">0xFFU</span>;</span><br><span class="line">consumer_time = val &amp; <span class="number">0xFFFFU</span>;</span><br><span class="line">CO_HBconsumer_initEntry(HBcons, i, nodeId, consumer_time);</span><br></pre></td></tr></table></figure><p>这里 <code>consumer_time</code> 单位是 ms；进入 <code>CO_HBconsumer_initEntry()</code> 后会乘以 <code>1000U</code> 转成 <code>time_us</code>。</p><h3 id="4-4-CO-HBconsumer-initEntry-配置一个监控项"><a href="#4-4-CO-HBconsumer-initEntry-配置一个监控项" class="headerlink" title="4.4 CO_HBconsumer_initEntry() 配置一个监控项"></a>4.4 <code>CO_HBconsumer_initEntry()</code> 配置一个监控项</h3><p>该函数做了几个关键判断：</p><ol><li>参数为空或 <code>idx</code> 越界，返回非法参数。</li><li>如果 <code>consumerTime_ms != 0</code> 且 <code>nodeId != 0</code>，检查是否有重复 nodeId。</li><li>若 <code>nodeId != 0</code> 且 <code>time_us != 0</code>：<ul><li><code>COB_ID = 0x700 + nodeId</code></li><li><code>HBstate = CO_HBconsumer_UNKNOWN</code></li></ul></li><li>否则：<ul><li><code>COB_ID = 0</code></li><li><code>time_us = 0</code></li><li><code>HBstate = CO_HBconsumer_UNCONFIGURED</code></li></ul></li><li>最后调用 <code>CO_CANrxBufferInit()</code>，把 CAN 接收过滤器和 <code>CO_HBcons_receive()</code> 回调绑定起来。</li></ol><h3 id="4-5-动态写-0x1016"><a href="#4-5-动态写-0x1016" class="headerlink" title="4.5 动态写 0x1016"></a>4.5 动态写 <code>0x1016</code></h3><p>如果启用了 <code>CO_CONFIG_FLAG_OD_DYNAMIC</code>，<code>CO_HBconsumer_init()</code> 会给 <code>0x1016</code> 安装 OD extension。之后应用或 SDO 写 <code>0x1016</code> 时，<code>OD_write_1016()</code> 会重新解析 32 位值并调用 <code>CO_HBconsumer_initEntry()</code>，从而动态改变被监控节点和接收过滤器。</p><p>这也是为什么 <code>CO_CONFIG_HB_CONS</code> 默认包含 <code>CO_CONFIG_GLOBAL_FLAG_OD_DYNAMIC</code>：调试或主站配置阶段可以直接通过对象字典改变监控关系。</p><hr><h2 id="5-接收回调：中断上下文只做轻量预处理"><a href="#5-接收回调：中断上下文只做轻量预处理" class="headerlink" title="5. 接收回调：中断上下文只做轻量预处理"></a>5. 接收回调：中断上下文只做轻量预处理</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    subgraph IRQ[&quot;CAN RX 中断/驱动回调上下文&quot;]</span><br><span class="line">        Can[&quot;CAN RX 匹配&lt;br/&gt;COB-ID = 0x700 + nodeId&lt;br/&gt;DLC = 1&quot;] --&gt; Recv[&quot;CO_HBcons_receive()&lt;br/&gt;只做预处理&quot;]</span><br><span class="line">        Recv --&gt; Copy[&quot;NMTstate = data[0]&lt;br/&gt;CANrxNew = true&lt;br/&gt;可选 pre callback 唤醒任务&quot;]</span><br><span class="line">    end</span><br><span class="line"></span><br><span class="line">    Copy --&gt;|共享标志| Main</span><br><span class="line"></span><br><span class="line">    subgraph Task[&quot;主循环 / RTOS 任务上下文&quot;]</span><br><span class="line">        Main[&quot;CO_HBconsumer_process()&quot;] --&gt; Logic[&quot;区分 boot-up / heartbeat&lt;br/&gt;更新 HBstate&lt;br/&gt;检查超时&lt;br/&gt;触发回调 / EMCY&quot;]</span><br><span class="line">    end</span><br></pre></td></tr></table></figure><p><code>CO_HBcons_receive()</code> 是 CAN RX 回调。它只接受 DLC 为 1 的帧：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (DLC == <span class="number">1U</span>) &#123;</span><br><span class="line">    HBconsNode-&gt;NMTstate = (CO_NMT_internalState_t)data[<span class="number">0</span>];</span><br><span class="line">    CO_FLAG_SET(HBconsNode-&gt;CANrxNew);</span><br><span class="line">    ... optional pre callback ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这段代码有两个设计点：</p><ol><li><strong>不在 CAN 回调里判断超时、不上报 EMCY、不做复杂状态机。</strong></li><li><strong>只把最新的 NMT state 和 <code>CANrxNew</code> 标志交给周期处理函数。</strong></li></ol><p>如果启用了 <code>CO_CONFIG_FLAG_CALLBACK_PRE</code>，收到 heartbeat 后可以调用 pre callback。这个回调常用于 RTOS 场景下唤醒处理 <code>CO_HBconsumer_process()</code> 的任务。</p><hr><h2 id="6-主处理流程：CO-HBconsumer-process-真正运行状态机"><a href="#6-主处理流程：CO-HBconsumer-process-真正运行状态机" class="headerlink" title="6. 主处理流程：CO_HBconsumer_process() 真正运行状态机"></a>6. 主处理流程：<code>CO_HBconsumer_process()</code> 真正运行状态机</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    Start[&quot;CO_HBconsumer_process(HBcons,&lt;br/&gt;NMTisPreOrOperational, timeDifference_us, timerNext_us)&quot;] --&gt; Gate&#123;&quot;本节点本次和上次都处于&lt;br/&gt;Pre-operational 或 Operational?&quot;&#125;</span><br><span class="line"></span><br><span class="line">    Gate --&gt;|否，但边界变化| ResetBoundary[&quot;状态边界变化&lt;br/&gt;清 CANrxNew&lt;br/&gt;NMTstate = UNKNOWN&lt;br/&gt;HBstate 回到 UNKNOWN&quot;]</span><br><span class="line">    ResetBoundary --&gt; Save</span><br><span class="line"></span><br><span class="line">    Gate --&gt;|是| Loop[&quot;遍历 monitoredNodes[]&quot;]</span><br><span class="line">    Loop --&gt; Unconfigured&#123;&quot;HBstate == UNCONFIGURED?&quot;&#125;</span><br><span class="line">    Unconfigured --&gt;|是| Next[&quot;下一个监控项&quot;]</span><br><span class="line">    Unconfigured --&gt;|否| NewFrame&#123;&quot;CANrxNew?&quot;&#125;</span><br><span class="line"></span><br><span class="line">    NewFrame --&gt;|有新帧| IsBoot&#123;&quot;NMTstate == INITIALIZING(0)?&quot;&#125;</span><br><span class="line">    IsBoot --&gt;|是| BootAction[&quot;识别为 boot-up&lt;br/&gt;可选 remote reset callback&lt;br/&gt;若此前 ACTIVE: 报告远端复位&lt;br/&gt;HBstate = UNKNOWN&quot;]</span><br><span class="line">    IsBoot --&gt;|否| HbAction[&quot;识别为 heartbeat&lt;br/&gt;可选 hb started callback&lt;br/&gt;HBstate = ACTIVE&lt;br/&gt;timeoutTimer = 0&quot;]</span><br><span class="line">    HbAction --&gt; ActiveCheck</span><br><span class="line">    BootAction --&gt; Aggregate</span><br><span class="line"></span><br><span class="line">    NewFrame --&gt;|无新帧| ActiveCheck&#123;&quot;HBstate == ACTIVE?&quot;&#125;</span><br><span class="line">    ActiveCheck --&gt;|否| Aggregate[&quot;更新 allMonitoredActive / Operational&lt;br/&gt;必要时触发 NMT changed callback&quot;]</span><br><span class="line">    ActiveCheck --&gt;|是| AddTime[&quot;timeoutTimer += delta&quot;]</span><br><span class="line">    AddTime --&gt; Expired&#123;&quot;timeoutTimer &gt;= time_us?&quot;&#125;</span><br><span class="line">    Expired --&gt;|是| Timeout[&quot;可选 timeout callback&lt;br/&gt;CO_errorReport(HB consumer)&lt;br/&gt;NMTstate = UNKNOWN&lt;br/&gt;HBstate = TIMEOUT&quot;]</span><br><span class="line">    Expired --&gt;|否| TimerNext[&quot;未超时则更新 timerNext_us&quot;]</span><br><span class="line">    Timeout --&gt; Aggregate</span><br><span class="line">    TimerNext --&gt; Aggregate</span><br><span class="line"></span><br><span class="line">    Aggregate --&gt; Next</span><br><span class="line">    Next --&gt; More&#123;&quot;还有监控项?&quot;&#125;</span><br><span class="line">    More --&gt;|是| Loop</span><br><span class="line">    More --&gt;|否| ClearErr&#123;&quot;所有监控项从非全 active&lt;br/&gt;变为全 active?&quot;&#125;</span><br><span class="line">    ClearErr --&gt;|是| ErrReset[&quot;CO_errorReset 两类 HB 错误&quot;]</span><br><span class="line">    ClearErr --&gt;|否| Save[&quot;保存 allMonitored*&lt;br/&gt;保存 NMTisPreOrOperationalPrev&quot;]</span><br><span class="line">    ErrReset --&gt; Save</span><br></pre></td></tr></table></figure><p><code>CO_HBconsumer_process()</code> 周期调用，入参有 3 个关键量：</p><table><thead><tr><th>参数</th><th>作用</th></tr></thead><tbody><tr><td><code>NMTisPreOrOperational</code></td><td>本节点当前是否处于 Pre-operational 或 Operational</td></tr><tr><td><code>timeDifference_us</code></td><td>距离上次调用过去了多少 us</td></tr><tr><td><code>timerNext_us</code></td><td>可选：告诉上层下一次最晚多久后需要再次调用</td></tr></tbody></table><h3 id="6-1-本节点-NMT-状态门控"><a href="#6-1-本节点-NMT-状态门控" class="headerlink" title="6.1 本节点 NMT 状态门控"></a>6.1 本节点 NMT 状态门控</h3><p>源码只有在下面条件为真时，才执行正常监控逻辑：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (NMTisPreOrOperational &amp;&amp; HBcons-&gt;NMTisPreOrOperationalPrev) &#123;</span><br><span class="line">    ... normal processing ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>也就是本节点需要连续处于 Pre-operational 或 Operational。若刚进入或刚离开这些状态，源码会清理每个监控项：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">NMTstate = CO_NMT_UNKNOWN</span><br><span class="line">NMTstatePrev = CO_NMT_UNKNOWN</span><br><span class="line">CANrxNew = false</span><br><span class="line">非 UNCONFIGURED 项的 HBstate = UNKNOWN</span><br></pre></td></tr></table></figure><p>这能避免本节点 NMT 状态切换边界上的旧 heartbeat 状态被误用。</p><h3 id="6-2-有新帧：先区分-boot-up-和-heartbeat"><a href="#6-2-有新帧：先区分-boot-up-和-heartbeat" class="headerlink" title="6.2 有新帧：先区分 boot-up 和 heartbeat"></a>6.2 有新帧：先区分 boot-up 和 heartbeat</h3><p>对每个已配置节点，<code>process()</code> 先看 <code>CANrxNew</code>。</p><p>如果 <code>NMTstate == CO_NMT_INITIALIZING</code>，这帧被视为 <strong>boot-up message</strong>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">可选调用 remote reset callback</span><br><span class="line">若之前 HBstate == ACTIVE：报告 CO_EM_HB_CONSUMER_REMOTE_RESET</span><br><span class="line">HBstate = UNKNOWN</span><br></pre></td></tr></table></figure><p>这里有一个关键点：<strong>boot-up 不会把节点变成 ACTIVE。</strong> 它只说明远端刚启动或通信刚复位，consumer 会回到 <code>UNKNOWN</code>，等待后续第一帧真正的 heartbeat。</p><p>如果 <code>NMTstate != CO_NMT_INITIALIZING</code>，这帧才被视为正常 heartbeat：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">如果此前不是 ACTIVE：可选调用 heartbeat started callback</span><br><span class="line">HBstate = ACTIVE</span><br><span class="line">timeoutTimer = 0</span><br><span class="line">timeDifference_us_copy = 0</span><br></pre></td></tr></table></figure><p><code>timeDifference_us_copy = 0</code> 的目的，是避免“刚收到新 heartbeat 的同一轮 process 又把本轮 delta 加到 timeoutTimer 上”。</p><h3 id="6-3-无新帧或处理完新帧后：检查超时"><a href="#6-3-无新帧或处理完新帧后：检查超时" class="headerlink" title="6.3 无新帧或处理完新帧后：检查超时"></a>6.3 无新帧或处理完新帧后：检查超时</h3><p>只有 <code>HBstate == ACTIVE</code> 时，源码才累加 <code>timeoutTimer</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">timeoutTimer += timeDifference_us_copy</span><br></pre></td></tr></table></figure><p>如果：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">timeoutTimer &gt;= time_us</span><br></pre></td></tr></table></figure><p>就执行 timeout 处理：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">可选调用 timeout callback</span><br><span class="line">CO_errorReport(HBcons-&gt;em, CO_EM_HEARTBEAT_CONSUMER, CO_EMC_HEARTBEAT, i)</span><br><span class="line">NMTstate = CO_NMT_UNKNOWN</span><br><span class="line">HBstate = CO_HBconsumer_TIMEOUT</span><br></pre></td></tr></table></figure><p>这说明：<strong>未收到第一帧 heartbeat 前不会超时；只有进入 ACTIVE 后，consumer 才开始计时。</strong> CANopenNode 官方文档也明确写到，monitoring starts after the reception of the first HeartBeat, not bootup。[^canopennode-hbc]</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    T0[&quot;t0&lt;br/&gt;收到 heartbeat&lt;br/&gt;HBstate = ACTIVE&lt;br/&gt;timeoutTimer = 0&quot;] --&gt; T1[&quot;t1&lt;br/&gt;process 周期调用&lt;br/&gt;timeoutTimer += dt&quot;]</span><br><span class="line">    T1 --&gt; T2[&quot;t2&lt;br/&gt;仍未收到下一帧&lt;br/&gt;无 CANrxNew&lt;br/&gt;timeoutTimer 继续累加&quot;]</span><br><span class="line">    T2 --&gt; T3[&quot;t3 &gt;= time_us&lt;br/&gt;超过 0x1016 时间窗&lt;br/&gt;判定 TIMEOUT&lt;br/&gt;CO_errorReport&quot;]</span><br><span class="line">    T3 --&gt; T4[&quot;之后重新收到 heartbeat&lt;br/&gt;远端恢复发送&lt;br/&gt;HBstate 回 ACTIVE&lt;br/&gt;timeoutTimer = 0&quot;]</span><br></pre></td></tr></table></figure><h3 id="6-4-聚合状态和-NMT-changed-callback"><a href="#6-4-聚合状态和-NMT-changed-callback" class="headerlink" title="6.4 聚合状态和 NMT changed callback"></a>6.4 聚合状态和 NMT changed callback</h3><p>每轮遍历时，源码会同步计算：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">allMonitoredActiveCurrent</span><br><span class="line">allMonitoredOperationalCurrent</span><br></pre></td></tr></table></figure><p>判断规则很直接：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">只要有一个已配置节点不是 ACTIVE，allMonitoredActiveCurrent = false</span><br><span class="line">只要有一个已配置节点 NMTstate 不是 OPERATIONAL，allMonitoredOperationalCurrent = false</span><br></pre></td></tr></table></figure><p>如果启用了 <code>CO_CONFIG_HB_CONS_CALLBACK_CHANGE</code> 或 <code>CO_CONFIG_HB_CONS_CALLBACK_MULTI</code>，源码还会比较：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">monitoredNode-&gt;NMTstate != monitoredNode-&gt;NMTstatePrev</span><br></pre></td></tr></table></figure><p>变化时触发 NMT changed callback，然后更新 <code>NMTstatePrev</code>。</p><h3 id="6-5-所有节点恢复-active-后清错误"><a href="#6-5-所有节点恢复-active-后清错误" class="headerlink" title="6.5 所有节点恢复 active 后清错误"></a>6.5 所有节点恢复 active 后清错误</h3><p>源码最后有一段容易忽略：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (!HBcons-&gt;allMonitoredActive &amp;&amp; allMonitoredActiveCurrent) &#123;</span><br><span class="line">    CO_errorReset(HBcons-&gt;em, CO_EM_HEARTBEAT_CONSUMER, <span class="number">0</span>);</span><br><span class="line">    CO_errorReset(HBcons-&gt;em, CO_EM_HB_CONSUMER_REMOTE_RESET, <span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>含义是：当系统从“不是所有被监控节点都 active”变为“所有被监控节点都 active”时，清除 heartbeat consumer timeout 和 remote reset 相关错误。</p><p>源码注释也提醒：Heartbeat consumer 对所有被监控节点共用一个 emergency index，所以 reset 时用 <code>0</code> 清该类错误；而 report 时用循环下标 <code>i</code> 作为 info code，便于定位是哪一个 <code>0x1016</code> 子项对应的节点。</p><hr><h2 id="7-回调配置：公共回调与每节点回调不能同时启用"><a href="#7-回调配置：公共回调与每节点回调不能同时启用" class="headerlink" title="7. 回调配置：公共回调与每节点回调不能同时启用"></a>7. 回调配置：公共回调与每节点回调不能同时启用</h2><p><code>CO_config.h</code> 对 Heartbeat Consumer 的配置宏做了分层：</p><table><thead><tr><th>宏&#x2F;flag</th><th>作用</th></tr></thead><tbody><tr><td><code>CO_CONFIG_HB_CONS_ENABLE</code></td><td>编译并启用 Heartbeat Consumer</td></tr><tr><td><code>CO_CONFIG_HB_CONS_CALLBACK_CHANGE</code></td><td>启用“公共 NMT state changed 回调”</td></tr><tr><td><code>CO_CONFIG_HB_CONS_CALLBACK_MULTI</code></td><td>启用每个被监控节点独立的多类回调</td></tr><tr><td><code>CO_CONFIG_HB_CONS_QUERY_FUNCT</code></td><td>启用查询函数：按 Node-ID 或 idx 查状态</td></tr><tr><td><code>CO_CONFIG_FLAG_CALLBACK_PRE</code></td><td>收到 heartbeat 后先调用轻量 pre callback</td></tr><tr><td><code>CO_CONFIG_FLAG_TIMERNEXT</code></td><td>在 <code>process()</code> 中计算 <code>timerNext_us</code></td></tr><tr><td><code>CO_CONFIG_FLAG_OD_DYNAMIC</code></td><td>写 <code>0x1016</code> 时动态重配置监控项</td></tr></tbody></table><p><code>CO_HBconsumer.c</code> 明确禁止同时启用：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">CO_CONFIG_HB_CONS_CALLBACK_CHANGE</span><br><span class="line">CO_CONFIG_HB_CONS_CALLBACK_MULTI</span><br></pre></td></tr></table></figure><p>原因是这两种回调模型一个是公共回调，一个是每节点回调，语义上互斥。</p><p><code>CALLBACK_MULTI</code> 打开后可以分别注册：</p><table><thead><tr><th>回调</th><th>触发时机</th></tr></thead><tbody><tr><td><code>CO_HBconsumer_initCallbackNmtChanged()</code></td><td>远端 NMT state 变化</td></tr><tr><td><code>CO_HBconsumer_initCallbackHeartbeatStarted()</code></td><td>consumer 从非 ACTIVE 收到有效 heartbeat，转入 ACTIVE</td></tr><tr><td><code>CO_HBconsumer_initCallbackTimeout()</code></td><td>ACTIVE 后超时</td></tr><tr><td><code>CO_HBconsumer_initCallbackRemoteReset()</code></td><td>收到 boot-up，识别到远端复位&#x2F;通信复位</td></tr></tbody></table><hr><h2 id="8-查询接口：只在启用-CO-CONFIG-HB-CONS-QUERY-FUNCT-时存在"><a href="#8-查询接口：只在启用-CO-CONFIG-HB-CONS-QUERY-FUNCT-时存在" class="headerlink" title="8. 查询接口：只在启用 CO_CONFIG_HB_CONS_QUERY_FUNCT 时存在"></a>8. 查询接口：只在启用 <code>CO_CONFIG_HB_CONS_QUERY_FUNCT</code> 时存在</h2><p>如果打开 <code>CO_CONFIG_HB_CONS_QUERY_FUNCT</code>，源码会编译 3 个查询函数：</p><table><thead><tr><th>函数</th><th>用途</th></tr></thead><tbody><tr><td><code>CO_HBconsumer_getIdxByNodeId()</code></td><td>用 producer Node-ID 查 <code>monitoredNodes[]</code> 下标</td></tr><tr><td><code>CO_HBconsumer_getState()</code></td><td>用 idx 查询当前 <code>CO_HBconsumer_state_t</code></td></tr><tr><td><code>CO_HBconsumer_getNmtState()</code></td><td>用 idx 查询远端 NMT state</td></tr></tbody></table><p>其中 <code>CO_HBconsumer_getNmtState()</code> 有一个约束：只有该节点 <code>HBstate == CO_HBconsumer_ACTIVE</code> 时，返回的 NMT state 才有效；否则返回 <code>-1</code>。</p><p>这与协议语义一致：如果没收到有效 heartbeat，或者已经 timeout，远端 NMT 状态就是未知的。</p><hr><h2 id="9-STM32-RTOS-工程里怎么接入这段逻辑"><a href="#9-STM32-RTOS-工程里怎么接入这段逻辑" class="headerlink" title="9. STM32&#x2F;RTOS 工程里怎么接入这段逻辑"></a>9. STM32&#x2F;RTOS 工程里怎么接入这段逻辑</h2><p>在 CANopenNode 工程里，Heartbeat Consumer 通常不是普通从机最小配置必须项。普通 STM32 从机通常先需要：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">NMT slave + Heartbeat producer + SDO server + EMCY producer + PDO + OD</span><br></pre></td></tr></table></figure><p>只有当本节点还需要监控主站或其他从站时，才需要打开 Heartbeat Consumer。</p><p>最小接入路径如下：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">1. CO_config.h 中打开 CO_CONFIG_HB_CONS_ENABLE</span><br><span class="line">2. OD 中存在 0x1016 Consumer heartbeat time</span><br><span class="line">3. 为 monitoredNodes[] 提供足够容量</span><br><span class="line">4. CO_new()/初始化阶段分配 HBcons 与 monitoredNodes</span><br><span class="line">5. communication reset 阶段调用 CO_HBconsumer_init()</span><br><span class="line">6. 主循环或 RTOS 任务周期调用 CO_HBconsumer_process()</span><br><span class="line">7. 需要事件通知时注册 callback</span><br><span class="line">8. 应用层读取 allMonitoredActive / allMonitoredOperational 或查询接口</span><br></pre></td></tr></table></figure><p>在 RTOS 中，如果启用了 <code>CO_CONFIG_FLAG_CALLBACK_PRE</code>，可以让 <code>CO_HBcons_receive()</code> 在收到 CAN 帧后唤醒任务；但仍建议把真正处理放在任务上下文，不要在 CAN 接收中断中直接做复杂业务。</p><hr><h2 id="10-调试时重点看这几类现象"><a href="#10-调试时重点看这几类现象" class="headerlink" title="10. 调试时重点看这几类现象"></a>10. 调试时重点看这几类现象</h2><table><thead><tr><th>现象</th><th>优先检查</th></tr></thead><tbody><tr><td>一直是 <code>UNCONFIGURED</code></td><td><code>0x1016</code> 子项是否为 0；Node-ID 或 time 是否为 0</td></tr><tr><td>一直是 <code>UNKNOWN</code></td><td>是否只收到 boot-up；producer 的 <code>0x1017</code> 是否启用；CAN-ID 是否为 <code>0x700 + Node-ID</code></td></tr><tr><td>一进入 <code>ACTIVE</code> 很快 timeout</td><td><code>0x1016</code> expected time 是否太短；主循环周期是否太长；producer 周期是否比 consumer expected time 短</td></tr><tr><td>收到帧但没有处理</td><td><code>CO_HBconsumer_process()</code> 是否周期调用；本节点是否连续处于 Pre-operational&#x2F;Operational</td></tr><tr><td>写 <code>0x1016</code> 后没生效</td><td>是否启用 <code>CO_CONFIG_FLAG_OD_DYNAMIC</code>；写入值是否触发 <code>OD_write_1016()</code></td></tr><tr><td><code>allMonitoredOperational</code> 一直 false</td><td>被监控节点可能只是 Pre-operational；它必须上报 <code>CO_NMT_OPERATIONAL</code> 才会为 true</td></tr><tr><td>远端复位后没有 timeout</td><td>收到 boot-up 后源码把状态置为 <code>UNKNOWN</code>，监控等待下一帧 heartbeat；这不是普通 timeout 路径</td></tr></tbody></table><hr><h2 id="11-一句话总结"><a href="#11-一句话总结" class="headerlink" title="11. 一句话总结"></a>11. 一句话总结</h2><p><code>CO_HBconsumer.c/h</code> 的主线不是“收到 heartbeat 就保存一下”这么简单，而是：<strong>用 <code>0x1016</code> 建立本节点对远端 producer 的监控表，用 CAN RX 回调捕获 <code>0x700 + Node-ID</code> 的 1 字节 NMT state，再由 <code>CO_HBconsumer_process()</code> 周期性完成 boot-up 识别、ACTIVE&#x2F;TIMEOUT 状态转换、NMT 状态变化回调、聚合在线状态，以及 heartbeat 相关 Emergency 上报与恢复清除。</strong></p><hr><h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><p>[^canopennode-hbc]: CANopenNode 官方 Doxygen，Heartbeat consumer：<a href="https://canopennode.github.io/CANopenNode/group__CO__HBconsumer.html">https://canopennode.github.io/CANopenNode/group__CO__HBconsumer.html</a><br>[^canopennode-github]: CANopenNode GitHub 仓库说明：<a href="https://github.com/CANopenNode/CANopenNode">https://github.com/CANopenNode/CANopenNode</a><br>[^canopennode-config]: CANopenNode <code>CO_config.h</code> Doxygen&#x2F;GitHub：<a href="https://github.com/CANopenNode/CANopenNode/blob/master/301/CO_config.h">https://github.com/CANopenNode/CANopenNode/blob/master/301/CO_config.h</a><br>[^cia-nmt]: CAN in Automation，Network management：<a href="https://www.can-cia.org/can-knowledge/network-management">https://www.can-cia.org/can-knowledge/network-management</a><br>[^kollmorgen-hb]: Kollmorgen AKD2G CANopen Heartbeat 文档：<a href="https://webhelp.kollmorgen.com/akd2g/english/content/AKD2G%20CANopen/CANopen_06_04_08%20Heartbeat.htm">https://webhelp.kollmorgen.com/akd2g/english/content/AKD2G%20CANopen/CANopen_06_04_08%20Heartbeat.htm</a><br>[^deditec-1016]: DEDITEC，0x1016 Consumer Heartbeat Time：<a href="https://www.deditec.de/media/manuals/en/manual_cos_serie/0x1016-consumer-heartbeat-time.htm">https://www.deditec.de/media/manuals/en/manual_cos_serie/0x1016-consumer-heartbeat-time.htm</a></p><p>本地材料：<code>CO_HBconsumer.h</code>、<code>CO_HBconsumer.c</code>、<code>CO_NMT_Heartbeat.h</code>、<code>CO_config.h</code>、<code>CiA301 V4.2.0（中文注释版）.pdf</code>。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;meta name=&quot;referrer&quot; content=&quot;no-referrer&quot; /&gt;

&lt;h1 id=&quot;CANopene-Heartbeat运行流程&quot;&gt;&lt;a href=&quot;#CANopene-Heartbeat运行流程&quot; class=&quot;headerlink&quot; title=&quot;</summary>
      
    
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/categories/canopen/"/>
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/tags/canopen/"/>
    
  </entry>
  
  <entry>
    <title>CANopen NMT：搞懂 NMT 运行流程</title>
    <link href="https://wdfk-prog.space/posts/b404ac99/"/>
    <id>https://wdfk-prog.space/posts/b404ac99/</id>
    <published>2026-06-28T07:39:18.000Z</published>
    <updated>2026-06-28T07:39:36.863Z</updated>
    
    <content type="html"><![CDATA[<meta name="referrer" content="no-referrer" /><h1 id="CANopen-NMT：搞懂-NMT-运行流程"><a href="#CANopen-NMT：搞懂-NMT-运行流程" class="headerlink" title="CANopen NMT：搞懂 NMT 运行流程"></a>CANopen NMT：搞懂 NMT 运行流程</h1><p><img src="https://i-blog.csdnimg.cn/direct/41254601d57e47fbaf077ee564a9d957.png" alt="在这里插入图片描述"></p><p>@[toc]</p><p><img src="https://i-blog.csdnimg.cn/direct/2210dd1e90124443ade35658ab190539.png" alt="在这里插入图片描述"></p><h2 id="先给结论"><a href="#先给结论" class="headerlink" title="先给结论"></a>先给结论</h2><p>NMT 是 CANopen 的<strong>网络管理状态机控制协议</strong>，Heartbeat 是配套的<strong>节点在线与状态上报机制</strong>。在 CANopenNode 里，<code>CO_NMT_Heartbeat.c/h</code> 不是单纯“收一帧命令就切状态”的代码，而是把以下功能合在一个对象 <code>CO_NMT_t</code> 里：</p><ol><li><strong>NMT slave &#x2F; NMT consumer</strong>：接收 CAN-ID <code>0x000</code> 的 NMT 命令，目标 Node-ID 为 <code>0</code> 或本节点时才处理。</li><li><strong>Heartbeat producer</strong>：用 CAN-ID <code>0x700 + nodeId</code> 周期发送本节点 NMT 状态；初始化阶段还会发 boot-up message。</li><li><strong>NMT 状态机执行点</strong>：真正状态切换不在 CAN 接收回调里完成，而是在 <code>CO_NMT_process()</code> 周期调用中完成。</li><li><strong>错误条件联动</strong>：可根据 CAN bus off、Heartbeat consumer 错误、Error register mask，把 Operational 自动降到 Pre-operational 或 Stopped。</li><li><strong>复位请求返回</strong>：<code>Reset node</code> &#x2F; <code>Reset communication</code> 不在 NMT 模块里直接重启 MCU，而是通过 <code>CO_NMT_process()</code> 返回 <code>CO_RESET_APP</code> &#x2F; <code>CO_RESET_COMM</code> 交给应用层执行。</li></ol><p>源码运行链路可以压缩成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">CAN-ID 0x000 NMT command</span><br><span class="line">    -&gt; CO_NMT_receive() 校验 DLC 和 Node-ID</span><br><span class="line">    -&gt; NMT-&gt;internalCommand = command</span><br><span class="line">    -&gt; CO_NMT_process() 周期执行</span><br><span class="line">    -&gt; 切换 NMT 状态或返回 resetCommand</span><br><span class="line">    -&gt; 发送 boot-up / heartbeat：0x700 + nodeId, data[0] = NMT state</span><br></pre></td></tr></table></figure><hr><h2 id="1-先看协议：NMT-解决什么问题"><a href="#1-先看协议：NMT-解决什么问题" class="headerlink" title="1. 先看协议：NMT 解决什么问题"></a>1. 先看协议：NMT 解决什么问题</h2><h3 id="1-1-NMT-的定位"><a href="#1-1-NMT-的定位" class="headerlink" title="1.1 NMT 的定位"></a>1.1 NMT 的定位</h3><p>CANopen 节点不是上电后立刻就能随便发 PDO。协议把设备运行分成几个 NMT 状态，主站或管理端通过 NMT 命令控制节点进入启动、停止、预运行、运行或复位流程。</p><p>在工程上，NMT 主要解决三件事：</p><table><thead><tr><th>问题</th><th>NMT&#x2F;Heartbeat 的做法</th></tr></thead><tbody><tr><td>什么时候允许 PDO 工作</td><td>只有 Operational 状态下 PDO 才工作</td></tr><tr><td>主站如何启动、停止或复位从站</td><td>发送 CAN-ID <code>0x000</code> 的 NMT command</td></tr><tr><td>主站如何知道从站当前在线状态</td><td>从站周期发送 Heartbeat，数据字节携带 NMT state</td></tr></tbody></table><p>CANopenNode 官方仓库说明它支持 NMT slave、simple NMT master，以及 heartbeat producer&#x2F;consumer error control。CiA 的 NMT 公开说明也明确：NMT 协议由 active NMT manager 发送，接收后会强制 CANopen 设备转入被命令的 NMT 状态；报文是单帧 2 字节，CAN-ID 为 <code>0</code>。</p><h3 id="1-2-NMT-命令帧格式"><a href="#1-2-NMT-命令帧格式" class="headerlink" title="1.2 NMT 命令帧格式"></a>1.2 NMT 命令帧格式</h3><p>NMT command 固定使用最高优先级的标准 CAN-ID：</p><table><thead><tr><th>字段</th><th>值&#x2F;含义</th></tr></thead><tbody><tr><td>CAN-ID</td><td><code>0x000</code></td></tr><tr><td>DLC</td><td><code>2</code></td></tr><tr><td>Byte 0</td><td>NMT command specifier</td></tr><tr><td>Byte 1</td><td>目标 Node-ID；<code>0</code> 表示广播所有节点</td></tr></tbody></table><p>常用命令值在 <code>CO_NMT_command_t</code> 中直接定义：</p><table><thead><tr><th>CANopenNode 枚举</th><th align="right">数值</th><th>协议含义</th></tr></thead><tbody><tr><td><code>CO_NMT_ENTER_OPERATIONAL</code></td><td align="right"><code>0x01</code></td><td>Start remote node，进入 Operational</td></tr><tr><td><code>CO_NMT_ENTER_STOPPED</code></td><td align="right"><code>0x02</code></td><td>Stop remote node，进入 Stopped</td></tr><tr><td><code>CO_NMT_ENTER_PRE_OPERATIONAL</code></td><td align="right"><code>0x80</code></td><td>Enter pre-operational</td></tr><tr><td><code>CO_NMT_RESET_NODE</code></td><td align="right"><code>0x81</code></td><td>Reset node，完整节点复位</td></tr><tr><td><code>CO_NMT_RESET_COMMUNICATION</code></td><td align="right"><code>0x82</code></td><td>Reset communication，仅通信部分复位</td></tr></tbody></table><p>示例：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"># 启动所有节点进入 Operational</span><br><span class="line">CAN-ID = 0x000, DLC = 2, Data = 01 00</span><br><span class="line"></span><br><span class="line"># 让节点 5 进入 Pre-operational</span><br><span class="line">CAN-ID = 0x000, DLC = 2, Data = 80 05</span><br><span class="line"></span><br><span class="line"># 让节点 5 执行通信复位</span><br><span class="line">CAN-ID = 0x000, DLC = 2, Data = 82 05</span><br></pre></td></tr></table></figure><h3 id="1-3-Heartbeat-与-boot-up-报文格式"><a href="#1-3-Heartbeat-与-boot-up-报文格式" class="headerlink" title="1.3 Heartbeat 与 boot-up 报文格式"></a>1.3 Heartbeat 与 boot-up 报文格式</h3><p>Heartbeat 是错误控制协议的一部分，用于确认网络参与者仍然在线，并且仍处于预期的 NMT FSA 状态。</p><table><thead><tr><th>字段</th><th>值&#x2F;含义</th></tr></thead><tbody><tr><td>CAN-ID</td><td><code>0x700 + Node-ID</code></td></tr><tr><td>DLC</td><td><code>1</code></td></tr><tr><td>Byte 0</td><td>当前 NMT state</td></tr></tbody></table><p>状态值在 <code>CO_NMT_internalState_t</code> 中定义：</p><table><thead><tr><th>状态</th><th align="right">数值</th><th>运行含义</th></tr></thead><tbody><tr><td><code>CO_NMT_INITIALIZING</code></td><td align="right"><code>0</code></td><td>初始化中；也用于 boot-up message 的数据字节</td></tr><tr><td><code>CO_NMT_PRE_OPERATIONAL</code></td><td align="right"><code>127 / 0x7F</code></td><td>预运行；SDO 等对象可用，PDO 不工作</td></tr><tr><td><code>CO_NMT_OPERATIONAL</code></td><td align="right"><code>5 / 0x05</code></td><td>运行；PDO 开始工作</td></tr><tr><td><code>CO_NMT_STOPPED</code></td><td align="right"><code>4 / 0x04</code></td><td>停止；只保留少量通信能力</td></tr><tr><td><code>CO_NMT_UNKNOWN</code></td><td align="right"><code>-1</code></td><td>Heartbeat consumer 侧用于表示未知状态</td></tr></tbody></table><p>注意：boot-up message 和 heartbeat 使用同一个 CAN-ID 规则。boot-up 是节点初始化完成时发出的状态值 <code>0</code>，后续 heartbeat 则周期性发送当前状态。</p><h3 id="1-4-四个状态要先分清"><a href="#1-4-四个状态要先分清" class="headerlink" title="1.4 四个状态要先分清"></a>1.4 四个状态要先分清</h3><p><img src="https://i-blog.csdnimg.cn/direct/d19278c43f774e8c91805536af8c6155.png" alt="在这里插入图片描述"></p><p>CANopenNode 在头文件注释里给出了状态边界：</p><table><thead><tr><th>状态</th><th>CANopenNode 注释中的语义</th><th>对 STM32 从机的直接影响</th></tr></thead><tbody><tr><td>Initializing</td><td>CANopen 初始化完成前的活动状态</td><td>还没进入正常 CANopen 通信流程</td></tr><tr><td>Pre-operational</td><td>除 PDO 外所有 CANopen 对象可用</td><td>常用于 SDO 配置、参数下载、映射检查</td></tr><tr><td>Operational</td><td>PDO 也可用</td><td>正式跑过程数据</td></tr><tr><td>Stopped</td><td>只保留 Heartbeat producer 和 NMT consumer</td><td>应用输出应进入安全&#x2F;停止策略</td></tr></tbody></table><hr><h2 id="2-再看源码：文件分工和阅读顺序"><a href="#2-再看源码：文件分工和阅读顺序" class="headerlink" title="2. 再看源码：文件分工和阅读顺序"></a>2. 再看源码：文件分工和阅读顺序</h2><p><img src="https://i-blog.csdnimg.cn/direct/8b15d0e4c02d42faa284b352ad61c8d7.png" alt="在这里插入图片描述"></p><p>建议阅读顺序如下：</p><table><thead><tr><th align="right">顺序</th><th>位置</th><th>先看什么</th><th>目的</th></tr></thead><tbody><tr><td align="right">1</td><td><code>CO_NMT_Heartbeat.h</code> 顶部 Doxygen</td><td>模块说明、NMT&#x2F;Heartbeat 报文内容</td><td>建立协议边界</td></tr><tr><td align="right">2</td><td><code>CO_NMT_internalState_t</code></td><td>NMT 状态值</td><td>对照 heartbeat byte0</td></tr><tr><td align="right">3</td><td><code>CO_NMT_command_t</code></td><td>NMT 命令值</td><td>对照 NMT command byte0</td></tr><tr><td align="right">4</td><td><code>CO_NMT_reset_cmd_t</code></td><td>返回给应用的复位动作</td><td>理解 reset 不在模块内部直接执行</td></tr><tr><td align="right">5</td><td><code>CO_NMT_control_t</code> 宏</td><td><code>NMTcontrol</code> 的位域</td><td>理解自启动、错误降级、错误恢复</td></tr><tr><td align="right">6</td><td><code>CO_NMT_t</code></td><td>状态、定时器、CAN buffer、回调</td><td>理解运行时对象保存了什么</td></tr><tr><td align="right">7</td><td><code>CO_NMT_init()</code></td><td>初始化对象、OD 0x1017、CAN RX&#x2F;TX</td><td>理解初始化阶段做了什么</td></tr><tr><td align="right">8</td><td><code>CO_NMT_receive()</code></td><td>接收 NMT 命令</td><td>理解接收回调只做预处理</td></tr><tr><td align="right">9</td><td><code>CO_NMT_process()</code></td><td>状态机主逻辑</td><td>这是运行流程核心</td></tr><tr><td align="right">10</td><td><code>CO_NMT_sendCommand()</code></td><td>可选 simple NMT master 发送能力</td><td>理解 <code>CO_CONFIG_NMT_MASTER</code> 不是完整主站</td></tr></tbody></table><hr><h2 id="3-CO-NMT-Heartbeat-h：先把协议值和对象结构看懂"><a href="#3-CO-NMT-Heartbeat-h：先把协议值和对象结构看懂" class="headerlink" title="3. CO_NMT_Heartbeat.h：先把协议值和对象结构看懂"></a>3. <code>CO_NMT_Heartbeat.h</code>：先把协议值和对象结构看懂</h2><h3 id="3-1-CO-CONFIG-NMT-默认配置"><a href="#3-1-CO-CONFIG-NMT-默认配置" class="headerlink" title="3.1 CO_CONFIG_NMT 默认配置"></a>3.1 <code>CO_CONFIG_NMT</code> 默认配置</h3><p>头文件中默认配置是：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">ifndef</span> CO_CONFIG_NMT</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> CO_CONFIG_NMT (CO_CONFIG_GLOBAL_FLAG_CALLBACK_PRE | CO_CONFIG_GLOBAL_FLAG_TIMERNEXT)</span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br></pre></td></tr></table></figure><p>含义：如果工程没有自己定义 <code>CO_CONFIG_NMT</code>，默认打开两个公共能力：</p><table><thead><tr><th>flag</th><th>含义</th></tr></thead><tbody><tr><td><code>CO_CONFIG_GLOBAL_FLAG_CALLBACK_PRE</code></td><td>收到 NMT CAN 帧预处理后可触发回调，常用于 RTOS 唤醒处理任务</td></tr><tr><td><code>CO_CONFIG_GLOBAL_FLAG_TIMERNEXT</code></td><td><code>CO_NMT_process()</code> 可给出下一次建议调用时间 <code>timerNext_us</code></td></tr></tbody></table><p>如果想让本节点也能主动发 NMT command，需要额外打开 <code>CO_CONFIG_NMT_MASTER</code>。这只表示“simple NMT master 发送能力”，不是完整主站配置器。</p><h3 id="3-2-CO-NMT-internalState-t：Heartbeat-发送的就是这个值"><a href="#3-2-CO-NMT-internalState-t：Heartbeat-发送的就是这个值" class="headerlink" title="3.2 CO_NMT_internalState_t：Heartbeat 发送的就是这个值"></a>3.2 <code>CO_NMT_internalState_t</code>：Heartbeat 发送的就是这个值</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">enum</span> &#123;</span></span><br><span class="line">    CO_NMT_UNKNOWN = <span class="number">-1</span>,</span><br><span class="line">    CO_NMT_INITIALIZING = <span class="number">0</span>,</span><br><span class="line">    CO_NMT_PRE_OPERATIONAL = <span class="number">127</span>,</span><br><span class="line">    CO_NMT_OPERATIONAL = <span class="number">5</span>,</span><br><span class="line">    CO_NMT_STOPPED = <span class="number">4</span></span><br><span class="line">&#125; CO_NMT_internalState_t;</span><br></pre></td></tr></table></figure><p><code>CO_NMT_process()</code> 发送 heartbeat 时会把当前状态写到 <code>HB_TXbuff-&gt;data[0]</code>。所以调试 CAN 报文时看到：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">0x705  [1]  7F   # 节点 5 处于 Pre-operational</span><br><span class="line">0x705  [1]  05   # 节点 5 处于 Operational</span><br><span class="line">0x705  [1]  04   # 节点 5 处于 Stopped</span><br><span class="line">0x705  [1]  00   # boot-up message / Initializing</span><br></pre></td></tr></table></figure><h3 id="3-3-CO-NMT-command-t：NMT-命令-byte0-的来源"><a href="#3-3-CO-NMT-command-t：NMT-命令-byte0-的来源" class="headerlink" title="3.3 CO_NMT_command_t：NMT 命令 byte0 的来源"></a>3.3 <code>CO_NMT_command_t</code>：NMT 命令 byte0 的来源</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">enum</span> &#123;</span></span><br><span class="line">    CO_NMT_NO_COMMAND = <span class="number">0</span>,</span><br><span class="line">    CO_NMT_ENTER_OPERATIONAL = <span class="number">1</span>,</span><br><span class="line">    CO_NMT_ENTER_STOPPED = <span class="number">2</span>,</span><br><span class="line">    CO_NMT_ENTER_PRE_OPERATIONAL = <span class="number">128</span>,</span><br><span class="line">    CO_NMT_RESET_NODE = <span class="number">129</span>,</span><br><span class="line">    CO_NMT_RESET_COMMUNICATION = <span class="number">130</span></span><br><span class="line">&#125; CO_NMT_command_t;</span><br></pre></td></tr></table></figure><p><code>CO_NMT_receive()</code> 从 NMT command 报文的 <code>data[0]</code> 读出这个值；如果本节点启用了 simple master，<code>CO_NMT_sendCommand()</code> 也会把这个值写回 <code>NMT_TXbuff-&gt;data[0]</code>。</p><h3 id="3-4-CO-NMT-reset-cmd-t：复位命令为什么是返回值"><a href="#3-4-CO-NMT-reset-cmd-t：复位命令为什么是返回值" class="headerlink" title="3.4 CO_NMT_reset_cmd_t：复位命令为什么是返回值"></a>3.4 <code>CO_NMT_reset_cmd_t</code>：复位命令为什么是返回值</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">enum</span> &#123;</span></span><br><span class="line">    CO_RESET_NOT = <span class="number">0</span>,</span><br><span class="line">    CO_RESET_COMM = <span class="number">1</span>,</span><br><span class="line">    CO_RESET_APP = <span class="number">2</span>,</span><br><span class="line">    CO_RESET_QUIT = <span class="number">3</span></span><br><span class="line">&#125; CO_NMT_reset_cmd_t;</span><br></pre></td></tr></table></figure><p>这体现了 CANopenNode 的边界：协议栈识别“需要复位”，但不直接决定 MCU 怎么复位。</p><ul><li><code>CO_RESET_COMM</code>：应用进入通信复位段，重建 CANopen 通信对象。</li><li><code>CO_RESET_APP</code>：应用执行完整设备复位，常见做法是 MCU reset 或回到主初始化入口。</li><li><code>CO_RESET_NOT</code>：正常运行。</li></ul><h3 id="3-5-NMTcontrol：不是协议帧，而是本地策略开关"><a href="#3-5-NMTcontrol：不是协议帧，而是本地策略开关" class="headerlink" title="3.5 NMTcontrol：不是协议帧，而是本地策略开关"></a>3.5 <code>NMTcontrol</code>：不是协议帧，而是本地策略开关</h3><p><code>NMTcontrol</code> 是传给 <code>CO_NMT_init()</code> 的本地控制位域，用于决定自启动和错误行为：</p><table><thead><tr><th>宏</th><th>作用</th></tr></thead><tbody><tr><td><code>CO_NMT_ERR_REG_MASK</code></td><td>低 8 位作为 Error register mask</td></tr><tr><td><code>CO_NMT_STARTUP_TO_OPERATIONAL</code></td><td>初始化后直接进入 Operational，否则进入 Pre-operational</td></tr><tr><td><code>CO_NMT_ERR_ON_BUSOFF_HB</code></td><td>Operational 下遇到 CAN bus off 或 heartbeat consumer 错误时降级</td></tr><tr><td><code>CO_NMT_ERR_ON_ERR_REG</code></td><td>Operational 下 Error register 命中 mask 时降级</td></tr><tr><td><code>CO_NMT_ERR_TO_STOPPED</code></td><td>错误触发时降到 Stopped，否则降到 Pre-operational</td></tr><tr><td><code>CO_NMT_ERR_FREE_TO_OPERATIONAL</code></td><td>错误消失后可从 Pre-operational 自动恢复到 Operational</td></tr></tbody></table><p>这一组宏是源码学习时很容易漏掉的点。它解释了一个现象：节点没有收到新的 NMT command，也可能因为本地错误自动从 Operational 掉到 Pre-operational 或 Stopped。</p><hr><h2 id="4-CO-NMT-init-：初始化时把协议入口全部接好"><a href="#4-CO-NMT-init-：初始化时把协议入口全部接好" class="headerlink" title="4. CO_NMT_init()：初始化时把协议入口全部接好"></a>4. <code>CO_NMT_init()</code>：初始化时把协议入口全部接好</h2><p><code>CO_NMT_init()</code> 必须在 communication reset section 调用。它主要做 6 件事。</p><h3 id="4-1-清空对象并写入初始状态"><a href="#4-1-清空对象并写入初始状态" class="headerlink" title="4.1 清空对象并写入初始状态"></a>4.1 清空对象并写入初始状态</h3><p>源码先 <code>memset(NMT, 0, sizeof(CO_NMT_t))</code>，然后设置：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">NMT-&gt;operatingState = CO_NMT_INITIALIZING;</span><br><span class="line">NMT-&gt;operatingStatePrev = CO_NMT_INITIALIZING;</span><br><span class="line">NMT-&gt;nodeId = nodeId;</span><br><span class="line">NMT-&gt;NMTcontrol = NMTcontrol;</span><br><span class="line">NMT-&gt;em = em;</span><br><span class="line">NMT-&gt;HBproducerTimer = (<span class="type">uint32_t</span>)firstHBTime_ms * <span class="number">1000U</span>;</span><br></pre></td></tr></table></figure><p>这里的关键是：初始状态固定为 <code>CO_NMT_INITIALIZING</code>，后面第一次 <code>CO_NMT_process()</code> 会发送 boot-up message，然后再进入 Pre-operational 或 Operational。</p><h3 id="4-2-读取并扩展-OD-0x1017-Producer-heartbeat-time"><a href="#4-2-读取并扩展-OD-0x1017-Producer-heartbeat-time" class="headerlink" title="4.2 读取并扩展 OD 0x1017 Producer heartbeat time"></a>4.2 读取并扩展 OD <code>0x1017 Producer heartbeat time</code></h3><p><code>0x1017</code> 是 producer heartbeat time。<code>CO_NMT_init()</code> 会读取它，并保存到微秒单位：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">OD_get_u16(OD_1017_ProducerHbTime, <span class="number">0</span>, &amp;HBprodTime_ms, <span class="literal">true</span>);</span><br><span class="line">NMT-&gt;HBproducerTime_us = (<span class="type">uint32_t</span>)HBprodTime_ms * <span class="number">1000U</span>;</span><br></pre></td></tr></table></figure><p>随后它给 <code>0x1017</code> 安装 OD extension：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">NMT-&gt;OD_1017_extension.object = NMT;</span><br><span class="line">NMT-&gt;OD_1017_extension.read = OD_readOriginal;</span><br><span class="line">NMT-&gt;OD_1017_extension.write = OD_write_1017;</span><br><span class="line">OD_extension_init(OD_1017_ProducerHbTime, &amp;NMT-&gt;OD_1017_extension);</span><br></pre></td></tr></table></figure><p>这样运行时如果通过 SDO 写 <code>0x1017</code>，<code>OD_write_1017()</code> 会同步更新 <code>HBproducerTime_us</code>，并把 <code>HBproducerTimer</code> 清零，使新的 heartbeat 周期立即生效。</p><h3 id="4-3-配置-NMT-命令接收"><a href="#4-3-配置-NMT-命令接收" class="headerlink" title="4.3 配置 NMT 命令接收"></a>4.3 配置 NMT 命令接收</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">CO_CANrxBufferInit(NMT_CANdevRx, NMT_rxIdx, CANidRxNMT, <span class="number">0x7FF</span>, <span class="literal">false</span>, (<span class="type">void</span>*)NMT, CO_NMT_receive);</span><br></pre></td></tr></table></figure><p>典型 <code>CANidRxNMT</code> 是 <code>0x000</code>。这一步把 CAN 接收 buffer 和 <code>CO_NMT_receive()</code> 绑定起来。</p><h3 id="4-4-可选配置-simple-NMT-master-发送"><a href="#4-4-可选配置-simple-NMT-master-发送" class="headerlink" title="4.4 可选配置 simple NMT master 发送"></a>4.4 可选配置 simple NMT master 发送</h3><p>只有 <code>CO_CONFIG_NMT_MASTER</code> 打开时，才会编译和初始化：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">NMT-&gt;NMT_TXbuff = CO_CANtxBufferInit(NMT_CANdevTx, NMT_txIdx, CANidTxNMT, <span class="literal">false</span>, <span class="number">2</span>, <span class="literal">false</span>);</span><br></pre></td></tr></table></figure><p>这对应发送 CAN-ID <code>0x000</code>、DLC 2 的 NMT command。</p><h3 id="4-5-配置-Heartbeat-发送"><a href="#4-5-配置-Heartbeat-发送" class="headerlink" title="4.5 配置 Heartbeat 发送"></a>4.5 配置 Heartbeat 发送</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">NMT-&gt;HB_TXbuff = CO_CANtxBufferInit(HB_CANdevTx, HB_txIdx, CANidTxHB, <span class="literal">false</span>, <span class="number">1</span>, <span class="literal">false</span>);</span><br></pre></td></tr></table></figure><p>典型 <code>CANidTxHB</code> 是 <code>0x700 + nodeId</code>，DLC 为 1。</p><hr><h2 id="5-CO-NMT-receive-：接收回调只做“预处理”"><a href="#5-CO-NMT-receive-：接收回调只做“预处理”" class="headerlink" title="5. CO_NMT_receive()：接收回调只做“预处理”"></a>5. <code>CO_NMT_receive()</code>：接收回调只做“预处理”</h2><p><code>CO_NMT_receive()</code> 的逻辑很短，但很关键：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">uint8_t</span> DLC = CO_CANrxMsg_readDLC(msg);</span><br><span class="line"><span class="type">const</span> <span class="type">uint8_t</span>* data = CO_CANrxMsg_readData(msg);</span><br><span class="line">CO_NMT_command_t command = (CO_NMT_command_t)data[<span class="number">0</span>];</span><br><span class="line"><span class="type">uint8_t</span> nodeId = data[<span class="number">1</span>];</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> ((DLC == <span class="number">2U</span>) &amp;&amp; ((nodeId == <span class="number">0U</span>) || (nodeId == NMT-&gt;nodeId))) &#123;</span><br><span class="line">    NMT-&gt;internalCommand = command;</span><br><span class="line">    ... optional callback ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里有三个结论：</p><ol><li><strong>NMT 命令必须 DLC&#x3D;2</strong>，否则不处理。</li><li><strong>Node-ID 为 0 是广播</strong>，或者等于本节点 Node-ID 才处理。</li><li><strong>接收回调不直接切状态</strong>，只写 <code>NMT-&gt;internalCommand</code>，真正动作留给 <code>CO_NMT_process()</code>。</li></ol><p>这种设计适合裸机和 RTOS：CAN 中断&#x2F;驱动回调里只做极短预处理，复杂状态切换和可能触发的 reset 交给主循环或任务上下文。</p><hr><h2 id="6-CO-NMT-process-：NMT-运行流程核心"><a href="#6-CO-NMT-process-：NMT-运行流程核心" class="headerlink" title="6. CO_NMT_process()：NMT 运行流程核心"></a>6. <code>CO_NMT_process()</code>：NMT 运行流程核心</h2><p><img src="https://i-blog.csdnimg.cn/direct/58b5b9d03ebd4579811fb24b259bce4d.png" alt="在这里插入图片描述"></p><p><code>CO_NMT_process()</code> 是整个模块最重要的函数。它的执行顺序可以拆成 7 层。</p><h3 id="6-1-更新-Heartbeat-定时器"><a href="#6-1-更新-Heartbeat-定时器" class="headerlink" title="6.1 更新 Heartbeat 定时器"></a>6.1 更新 Heartbeat 定时器</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">NMT-&gt;HBproducerTimer = (NMT-&gt;HBproducerTimer &gt; timeDifference_us)</span><br><span class="line">                     ? (NMT-&gt;HBproducerTimer - timeDifference_us)</span><br><span class="line">                     : <span class="number">0U</span>;</span><br></pre></td></tr></table></figure><p><code>timeDifference_us</code> 是应用每次调用时传入的时间差。NMT 模块不自己读硬件定时器，而是依赖外部调度传入 elapsed time。</p><h3 id="6-2-发送-boot-up-或-heartbeat"><a href="#6-2-发送-boot-up-或-heartbeat" class="headerlink" title="6.2 发送 boot-up 或 heartbeat"></a>6.2 发送 boot-up 或 heartbeat</h3><p>发送条件是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">当前是 Initializing</span><br><span class="line">或</span><br><span class="line">0x1017 不为 0，并且 heartbeat timer 到期或 NMT 状态变化</span><br></pre></td></tr></table></figure><p>发送内容是：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">NMT-&gt;HB_TXbuff-&gt;data[<span class="number">0</span>] = (<span class="type">uint8_t</span>)NMTstateCpy;</span><br><span class="line">CO_CANsend(NMT-&gt;HB_CANdevTx, NMT-&gt;HB_TXbuff);</span><br></pre></td></tr></table></figure><p>如果当前状态是 <code>CO_NMT_INITIALIZING</code>，这帧就是 boot-up message。发完后，源码再根据 <code>CO_NMT_STARTUP_TO_OPERATIONAL</code> 决定初始运行状态：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">NMTstateCpy = (NMTcontrol &amp; CO_NMT_STARTUP_TO_OPERATIONAL)</span><br><span class="line">            ? CO_NMT_OPERATIONAL</span><br><span class="line">            : CO_NMT_PRE_OPERATIONAL;</span><br></pre></td></tr></table></figure><p>所以：</p><table><thead><tr><th><code>NMTcontrol</code> 设置</th><th>boot-up 后进入</th></tr></thead><tbody><tr><td>未设置 <code>CO_NMT_STARTUP_TO_OPERATIONAL</code></td><td>Pre-operational</td></tr><tr><td>设置 <code>CO_NMT_STARTUP_TO_OPERATIONAL</code></td><td>Operational</td></tr></tbody></table><h3 id="6-3-处理收到的内部命令"><a href="#6-3-处理收到的内部命令" class="headerlink" title="6.3 处理收到的内部命令"></a>6.3 处理收到的内部命令</h3><p><code>internalCommand</code> 可能来自两个入口：</p><ul><li><code>CO_NMT_receive()</code>：总线上收到 NMT command。</li><li><code>CO_NMT_sendCommand()</code>：本节点作为 simple NMT master 发送命令时，如果目标是自己或广播，也会写入本地 <code>internalCommand</code>。</li></ul><p>处理逻辑：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">switch</span> (NMT-&gt;internalCommand) &#123;</span><br><span class="line">    <span class="keyword">case</span> CO_NMT_ENTER_OPERATIONAL:</span><br><span class="line">        NMTstateCpy = CO_NMT_OPERATIONAL;</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">    <span class="keyword">case</span> CO_NMT_ENTER_STOPPED:</span><br><span class="line">        NMTstateCpy = CO_NMT_STOPPED;</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">    <span class="keyword">case</span> CO_NMT_ENTER_PRE_OPERATIONAL:</span><br><span class="line">        NMTstateCpy = CO_NMT_PRE_OPERATIONAL;</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">    <span class="keyword">case</span> CO_NMT_RESET_NODE:</span><br><span class="line">        resetCommand = CO_RESET_APP;</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">    <span class="keyword">case</span> CO_NMT_RESET_COMMUNICATION:</span><br><span class="line">        resetCommand = CO_RESET_COMM;</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">&#125;</span><br><span class="line">NMT-&gt;internalCommand = CO_NMT_NO_COMMAND;</span><br></pre></td></tr></table></figure><p>注意：<code>CO_NMT_RESET_NODE</code> 和 <code>CO_NMT_RESET_COMMUNICATION</code> 不会在这里调用 <code>NVIC_SystemReset()</code>。源码只设置返回值，应用层必须处理这个返回值。</p><h3 id="6-4-根据错误条件自动降级或恢复"><a href="#6-4-根据错误条件自动降级或恢复" class="headerlink" title="6.4 根据错误条件自动降级或恢复"></a>6.4 根据错误条件自动降级或恢复</h3><p>源码读取 Emergency 模块中的错误状态：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">ErrBusOff = CO_isError(NMT-&gt;em, CO_EM_CAN_TX_BUS_OFF);</span><br><span class="line">ErrHbCons = CO_isError(NMT-&gt;em, CO_EM_HEARTBEAT_CONSUMER);</span><br><span class="line">ErrHbConsRemote = CO_isError(NMT-&gt;em, CO_EM_HB_CONSUMER_REMOTE_RESET);</span><br></pre></td></tr></table></figure><p>然后结合 <code>NMTcontrol</code> 判断：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">busOff_HB = CO_NMT_ERR_ON_BUSOFF_HB &amp;&amp; (busOff 或 heartbeat consumer 错误)</span><br><span class="line">errRegMasked = CO_NMT_ERR_ON_ERR_REG &amp;&amp; (Error register 命中 NMTcontrol 低 8 位 mask)</span><br></pre></td></tr></table></figure><p>最终策略：</p><table><thead><tr><th>当前状态</th><th>条件</th><th>结果</th></tr></thead><tbody><tr><td>Operational</td><td><code>busOff_HB</code> 或 <code>errRegMasked</code></td><td>降级到 Pre-operational，或在 <code>CO_NMT_ERR_TO_STOPPED</code> 设置时降到 Stopped</td></tr><tr><td>Pre-operational</td><td><code>CO_NMT_ERR_FREE_TO_OPERATIONAL</code> 设置，且错误条件全部消失</td><td>自动恢复到 Operational</td></tr></tbody></table><p>这解释了调试中常见的“明明没发 NMT stop，节点为什么掉到 Pre-op”：通常是 <code>NMTcontrol</code> 配置让错误寄存器或 heartbeat consumer 错误参与了 NMT 状态控制。</p><h3 id="6-5-可选状态变化回调"><a href="#6-5-可选状态变化回调" class="headerlink" title="6.5 可选状态变化回调"></a>6.5 可选状态变化回调</h3><p>如果打开 <code>CO_CONFIG_NMT_CALLBACK_CHANGE</code>，状态变化后会调用：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">NMT-&gt;pFunctNMT(NMTstateCpy);</span><br></pre></td></tr></table></figure><p>这适合应用层统一处理状态进入&#x2F;退出，比如进入 Stopped 时关闭输出、进入 Operational 时允许业务周期运行。</p><h3 id="6-6-可选-timerNext-us"><a href="#6-6-可选-timerNext-us" class="headerlink" title="6.6 可选 timerNext_us"></a>6.6 可选 <code>timerNext_us</code></h3><p>如果打开 <code>CO_CONFIG_FLAG_TIMERNEXT</code>，<code>CO_NMT_process()</code> 会根据 heartbeat timer 更新 <code>timerNext_us</code>，供外部调度器决定下一次最晚什么时候再调用。</p><h3 id="6-7-更新状态并返回-reset-命令"><a href="#6-7-更新状态并返回-reset-命令" class="headerlink" title="6.7 更新状态并返回 reset 命令"></a>6.7 更新状态并返回 reset 命令</h3><p>最后：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">NMT-&gt;operatingState = NMTstateCpy;</span><br><span class="line"><span class="keyword">if</span> (NMTstate != <span class="literal">NULL</span>) &#123;</span><br><span class="line">    *NMTstate = NMTstateCpy;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">return</span> resetCommand;</span><br></pre></td></tr></table></figure><p>应用层应该保存或检查 <code>NMTstate</code>，并处理 <code>resetCommand</code>。</p><hr><h2 id="7-CO-NMT-sendCommand-：simple-NMT-master-不是完整主站"><a href="#7-CO-NMT-sendCommand-：simple-NMT-master-不是完整主站" class="headerlink" title="7. CO_NMT_sendCommand()：simple NMT master 不是完整主站"></a>7. <code>CO_NMT_sendCommand()</code>：simple NMT master 不是完整主站</h2><p><code>CO_NMT_sendCommand()</code> 只有在 <code>CO_CONFIG_NMT_MASTER</code> 打开时才编译。它做两件事。</p><h3 id="7-1-如果目标包含自己，先写本地-internalCommand"><a href="#7-1-如果目标包含自己，先写本地-internalCommand" class="headerlink" title="7.1 如果目标包含自己，先写本地 internalCommand"></a>7.1 如果目标包含自己，先写本地 <code>internalCommand</code></h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> ((nodeID == <span class="number">0U</span>) || (nodeID == NMT-&gt;nodeId)) &#123;</span><br><span class="line">    NMT-&gt;internalCommand = command;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这样本节点广播启动所有节点时，自己也会在下一次 <code>CO_NMT_process()</code> 中执行同一命令。</p><h3 id="7-2-发送-NMT-command-到总线"><a href="#7-2-发送-NMT-command-到总线" class="headerlink" title="7.2 发送 NMT command 到总线"></a>7.2 发送 NMT command 到总线</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">NMT-&gt;NMT_TXbuff-&gt;data[<span class="number">0</span>] = (<span class="type">uint8_t</span>)command;</span><br><span class="line">NMT-&gt;NMT_TXbuff-&gt;data[<span class="number">1</span>] = nodeID;</span><br><span class="line"><span class="keyword">return</span> CO_CANsend(NMT-&gt;NMT_CANdevTx, NMT-&gt;NMT_TXbuff);</span><br></pre></td></tr></table></figure><p>这只负责发 <code>0x000</code> NMT command，不包含：</p><ul><li>自动扫描节点；</li><li>自动配置对象字典；</li><li>自动写 PDO 映射；</li><li>自动监控所有 heartbeat；</li><li>自动恢复故障节点。</li></ul><p>所以 <code>CO_CONFIG_NMT_MASTER</code> 的准确理解是：<strong>让本节点具备发送 NMT command 的能力</strong>，不是把 STM32 从机“变成完整 CANopen 主站”。</p><hr><h2 id="8-STM32-从机里应该怎么用-NMT"><a href="#8-STM32-从机里应该怎么用-NMT" class="headerlink" title="8. STM32 从机里应该怎么用 NMT"></a>8. STM32 从机里应该怎么用 NMT</h2><p><img src="https://i-blog.csdnimg.cn/direct/0fd4102cefe8403ea33b1e39e52b6a54.png" alt="在这里插入图片描述"></p><h3 id="8-1-最小运行链路"><a href="#8-1-最小运行链路" class="headerlink" title="8.1 最小运行链路"></a>8.1 最小运行链路</h3><p>普通 STM32 CANopen 从机通常只需要默认 NMT slave + Heartbeat producer：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">1. 初始化 CAN/FDCAN 驱动和 CANopen 对象</span><br><span class="line">2. 在 communication reset 段调用 CO_NMT_init()</span><br><span class="line">3. 主循环或 RTOS task 周期调用 CO_NMT_process()</span><br><span class="line">4. 根据返回的 resetCommand 决定是否通信复位或整机复位</span><br><span class="line">5. 根据 NMTstate 决定应用是否允许输出、是否允许 PDO 业务</span><br></pre></td></tr></table></figure><p>伪代码：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">for</span> (;;) &#123;</span><br><span class="line">    <span class="type">uint32_t</span> dt_us = get_elapsed_us();</span><br><span class="line">    <span class="type">uint32_t</span> timerNext_us = UINT32_MAX;</span><br><span class="line">    CO_NMT_internalState_t nmtState;</span><br><span class="line"></span><br><span class="line">    CO_NMT_reset_cmd_t reset = CO_NMT_process(CO-&gt;NMT, &amp;nmtState, dt_us, &amp;timerNext_us);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (reset == CO_RESET_COMM) &#123;</span><br><span class="line">        <span class="comment">/* exit to communication reset section */</span></span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (reset == CO_RESET_APP) &#123;</span><br><span class="line">        <span class="comment">/* execute application reset policy, for example MCU reset */</span></span><br><span class="line">        system_reset();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (nmtState == CO_NMT_OPERATIONAL) &#123;</span><br><span class="line">        <span class="comment">/* allow normal process-data behavior */</span></span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="comment">/* keep outputs safe or skip operational-only behavior */</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="8-2-0x1017-Producer-heartbeat-time-怎么配"><a href="#8-2-0x1017-Producer-heartbeat-time-怎么配" class="headerlink" title="8.2 0x1017 Producer heartbeat time 怎么配"></a>8.2 <code>0x1017 Producer heartbeat time</code> 怎么配</h3><p><code>0x1017</code> 的单位是毫秒，CANopenNode 读出后换算成微秒保存到 <code>HBproducerTime_us</code>。</p><p>建议调试阶段先设置一个容易观察的值，例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">0x1017 = 1000 ms</span><br></pre></td></tr></table></figure><p>这样节点每秒发一次 heartbeat。CAN 分析仪上应该能看到：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">0x700 + nodeId  [1]  7F / 05 / 04</span><br></pre></td></tr></table></figure><p>如果 <code>0x1017 = 0</code>，周期 heartbeat 被禁用；但初始化阶段的 boot-up message 仍由 <code>CO_NMT_process()</code> 在 Initializing 状态下发送。</p><h3 id="8-3-主站不发-Start-时，为什么-PDO-不工作"><a href="#8-3-主站不发-Start-时，为什么-PDO-不工作" class="headerlink" title="8.3 主站不发 Start 时，为什么 PDO 不工作"></a>8.3 主站不发 Start 时，为什么 PDO 不工作</h3><p>如果没有设置 <code>CO_NMT_STARTUP_TO_OPERATIONAL</code>，节点 boot-up 后默认进入 Pre-operational。这个状态下 SDO 可用，但 PDO 不工作。</p><p>这时有两种选择：</p><table><thead><tr><th>方式</th><th>行为</th><th>适用场景</th></tr></thead><tbody><tr><td>主站发送 <code>01 nodeId</code> 或 <code>01 00</code></td><td>从站进入 Operational</td><td>规范主站管理流程</td></tr><tr><td>本地设置 <code>CO_NMT_STARTUP_TO_OPERATIONAL</code></td><td>boot-up 后自动进入 Operational</td><td>简单单节点、实验或固定系统</td></tr></tbody></table><h3 id="8-4-收到-reset-command-后该做什么"><a href="#8-4-收到-reset-command-后该做什么" class="headerlink" title="8.4 收到 reset command 后该做什么"></a>8.4 收到 reset command 后该做什么</h3><p>NMT reset 命令不是“协议栈内部立即复位”。应用必须检查 <code>CO_NMT_process()</code> 返回值：</p><table><thead><tr><th>返回值</th><th>应用侧动作</th></tr></thead><tbody><tr><td><code>CO_RESET_COMM</code></td><td>退出当前通信循环，重新初始化 CANopen 通信对象，通常不需要重启 MCU</td></tr><tr><td><code>CO_RESET_APP</code></td><td>执行完整应用复位策略，可能包括 MCU reset</td></tr><tr><td><code>CO_RESET_NOT</code></td><td>正常继续</td></tr></tbody></table><hr><h2 id="9-一句话总结"><a href="#9-一句话总结" class="headerlink" title="9. 一句话总结"></a>9. 一句话总结</h2><p><code>CO_NMT_Heartbeat.c/h</code> 的核心不是“发心跳”这么简单，而是：<strong>用 <code>CO_NMT_t</code> 保存本节点 NMT 状态和 heartbeat 定时器，用 <code>CO_NMT_receive()</code> 把总线 NMT 命令转成内部命令，再由 <code>CO_NMT_process()</code> 在主循环&#x2F;任务上下文中统一完成 boot-up、heartbeat、状态切换、错误降级和复位请求返回。</strong></p><hr><h2 id="10-资料依据"><a href="#10-资料依据" class="headerlink" title="10. 资料依据"></a>10. 资料依据</h2><table><thead><tr><th>类型</th><th>资料</th><th>本文使用点</th></tr></thead><tbody><tr><td>协议公开说明</td><td>CiA：Network management</td><td>NMT command 由 active NMT manager 发送；CAN-ID 0；DLC 2；byte0 为命令，byte1 为 node-ID；node-ID 为 0 表示所有节点</td></tr><tr><td>协议公开说明</td><td>CiA：Error control protocols</td><td>Heartbeat 用于确认网络参与者仍在线且处于预期 NMT FSA 状态</td></tr><tr><td>官方实现资料</td><td>CANopenNode：NMT and Heartbeat Doxygen</td><td><code>CO_NMT_init()</code>、<code>CO_NMT_process()</code>、回调、<code>CO_NMT_sendCommand()</code> 的官方语义</td></tr><tr><td>官方实现资料</td><td>CANopenNode：<code>CO_NMT_Heartbeat.h</code> Doxygen</td><td>NMT 状态、NMT command、Heartbeat&#x2F;NMT 报文内容、<code>NMTcontrol</code> 宏</td></tr><tr><td>官方仓库</td><td>CANopenNode GitHub</td><td>CANopenNode 是 ANSI C 协议栈，支持 NMT slave、simple NMT master、Heartbeat producer&#x2F;consumer</td></tr><tr><td>上传源码</td><td><code>CO_NMT_Heartbeat.h</code> &#x2F; <code>CO_NMT_Heartbeat.c</code></td><td>本文源码流程、状态值、命令值、初始化与 process 逻辑</td></tr><tr><td>上传规范译注</td><td>《CiA301 V4.2.0（中文注释版）》</td><td>NMT、boot-up、状态机、Heartbeat&#x2F;Node guarding、对象 <code>0x1017</code> 的章节定位</td></tr></tbody></table>]]></content>
    
    
      
      
    <summary type="html">&lt;meta name=&quot;referrer&quot; content=&quot;no-referrer&quot; /&gt;

&lt;h1 id=&quot;CANopen-NMT：搞懂-NMT-运行流程&quot;&gt;&lt;a href=&quot;#CANopen-NMT：搞懂-NMT-运行流程&quot; class=&quot;headerlink&quot; title</summary>
      
    
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/categories/canopen/"/>
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/tags/canopen/"/>
    
  </entry>
  
  <entry>
    <title>CANopen Emergency： 搞懂 EM 运行流程</title>
    <link href="https://wdfk-prog.space/posts/81b37f02/"/>
    <id>https://wdfk-prog.space/posts/81b37f02/</id>
    <published>2026-06-28T07:14:34.000Z</published>
    <updated>2026-06-28T07:28:38.274Z</updated>
    
    <content type="html"><![CDATA[<meta name="referrer" content="no-referrer" /><h1 id="CANopen-Emergency：-搞懂-EM-运行流程"><a href="#CANopen-Emergency：-搞懂-EM-运行流程" class="headerlink" title="CANopen Emergency： 搞懂 EM 运行流程"></a>CANopen Emergency： 搞懂 EM 运行流程</h1><p><img src="https://i-blog.csdnimg.cn/direct/78d44ed389f44fc78fe5353bbd9bc8d2.png" alt="在这里插入图片描述"></p><p>@[toc]<br><img src="https://i-blog.csdnimg.cn/direct/130203f055a549be8c426d9c493b552e.png" alt="在这里插入图片描述"></p><h2 id="先给结论"><a href="#先给结论" class="headerlink" title="先给结论"></a>先给结论</h2><p>Emergency（下文简称 <strong>EMCY</strong> 或 <strong>EM</strong>）不是普通日志，也不是 PDO。它是 CANopen 用于故障事件上报的标准机制。CAN in Automation 对 EMCY 的公开说明包括：由设备内部错误触发、映射到单个 CAN Classic 帧、内容包含 <code>0x1001</code> error register、16 位 emergency error code 和最多 5 字节制造商信息，默认 CAN-ID 为 <code>0x80 + node-ID</code>，且同一 error event 只发送一次。[^cia-emcy]</p><p>在 CANopenNode 的 <code>301/CO_Emergency.c/h</code> 中，运行链路可以压缩成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">应用/协议栈检测到错误变化</span><br><span class="line">    -&gt; CO_errorReport() / CO_errorReset()</span><br><span class="line">    -&gt; CO_error() 修改 errorStatusBits[] 并写入 FIFO</span><br><span class="line">    -&gt; CO_EM_process() 周期运行</span><br><span class="line">    -&gt; 计算 OD 0x1001 Error register</span><br><span class="line">    -&gt; 补齐 EMCY byte2 并按 NMT/TX/inhibit 条件发送 CAN 帧</span><br><span class="line">    -&gt; OD 0x1003 可读取最近错误历史</span><br></pre></td></tr></table></figure><p>关键点：<strong><code>CO_errorReport()</code> 不直接发 CAN 帧。</strong> 它只把错误变化写入内部状态和 FIFO；真正发帧发生在 <code>CO_EM_process()</code>。</p><hr><h2 id="1-协议层：EMCY-解决什么问题"><a href="#1-协议层：EMCY-解决什么问题" class="headerlink" title="1. 协议层：EMCY 解决什么问题"></a>1. 协议层：EMCY 解决什么问题</h2><p>CANopen 中，PDO 负责过程数据，SDO 负责对象字典访问，NMT 负责节点状态控制，Heartbeat 负责在线监控。EMCY 的职责不同：它把设备内部错误、通信错误或应用错误，以事件方式通知网络中的其他节点。</p><p>CiA 公开说明中还强调：EMCY producer 发送后，零个或多个 EMCY consumer 可以接收这些消息，并执行应用相关的反应。也就是说，EMCY 本身只定义错误信息的上报通道；收到后要停机、报警、降级还是忽略，是设备或系统策略决定的。[^cia-emcy]</p><p>这解释了 CANopenNode 源码里的几个设计：</p><table><thead><tr><th>协议要求</th><th>CANopenNode 源码对应</th></tr></thead><tbody><tr><td>错误事件触发，不周期重复发送</td><td><code>CO_error()</code> 检查状态位是否变化；重复 report&#x2F;reset 直接返回</td></tr><tr><td>EMCY 是 8 字节 CAN 帧</td><td>producer 初始化 TX buffer 时使用 DLC <code>8U</code></td></tr><tr><td>默认 CAN-ID 为 <code>0x80 + nodeId</code></td><td><code>CO_CAN_ID_EMERGENCY + nodeId</code>，由 <code>0x1014</code> 参与配置</td></tr><tr><td>帧中包含错误寄存器</td><td><code>CO_EM_process()</code> 计算 <code>errorRegister</code> 后写入 byte2</td></tr><tr><td>可保存错误历史</td><td><code>CO_CONFIG_EM_HISTORY</code> 使能 <code>0x1003</code> 读写扩展</td></tr><tr><td>可限制过密发送</td><td><code>CO_CONFIG_EM_PROD_INHIBIT</code> 使能 <code>0x1015</code> 抑制时间</td></tr></tbody></table><hr><h2 id="2-帧格式和相关-OD-对象"><a href="#2-帧格式和相关-OD-对象" class="headerlink" title="2. 帧格式和相关 OD 对象"></a>2. 帧格式和相关 OD 对象</h2><p><img src="https://i-blog.csdnimg.cn/direct/d35fc01645c547859c87b60d37182e67.png" alt="在这里插入图片描述"></p><p>CANopenNode 官方 Doxygen 给出的 Emergency producer 消息内容为：bytes <code>0..1</code> 是 error code，byte <code>2</code> 是 error register，byte <code>3</code> 是 error condition index，bytes <code>4..7</code> 是 <code>CO_errorReport()</code> 的附加信息。[^canopennode-em]</p><table><thead><tr><th align="right">字节</th><th>内容</th><th>CANopenNode 来源</th></tr></thead><tbody><tr><td align="right"><code>0..1</code></td><td>Emergency error code</td><td><code>CO_errorReport()</code> 的 <code>errorCode</code>；reset 时变成 <code>CO_EMC_NO_ERROR</code></td></tr><tr><td align="right"><code>2</code></td><td>Error register</td><td><code>CO_EM_process()</code> 根据 <code>errorStatusBits[]</code> 计算</td></tr><tr><td align="right"><code>3</code></td><td>Error condition index</td><td><code>CO_EM_errorStatusBits_t</code> 中的 <code>errorBit</code></td></tr><tr><td align="right"><code>4..7</code></td><td>Additional information</td><td><code>CO_errorReport()</code> 的 <code>infoCode</code></td></tr></tbody></table><h3 id="2-1-0x1001-Error-register"><a href="#2-1-0x1001-Error-register" class="headerlink" title="2.1 0x1001 Error register"></a>2.1 <code>0x1001 Error register</code></h3><p><code>0x1001</code> 是 CANopen 的错误寄存器。CANopenNode 头文件把它描述为必需对象，<code>CO_EM_init()</code> 会通过 <code>OD_getPtr()</code> 取得 <code>0x1001,00</code> 的真实存储地址；之后 <code>CO_EM_process()</code> 直接写这个地址。</p><p>它不是每个错误的明细列表，而是错误类别汇总：generic、current、voltage、temperature、communication、device profile、manufacturer 等。CANopenNode 先把具体错误记录在 <code>errorStatusBits[]</code>，再通过 <code>CO_CONFIG_ERR_CONDITION_xxx</code> 宏汇总到 <code>0x1001</code>。</p><h3 id="2-2-0x1003-Pre-defined-error-field"><a href="#2-2-0x1003-Pre-defined-error-field" class="headerlink" title="2.2 0x1003 Pre-defined error field"></a>2.2 <code>0x1003 Pre-defined error field</code></h3><p>如果启用 <code>CO_CONFIG_EM_HISTORY</code>，最新错误可以从对象字典 <code>0x1003</code> 读取；CANopenNode 官方说明指出 <code>0x1003</code> 内容对应 EMCY message 的 bytes <code>0..3</code>。[^canopennode-em]</p><p>这意味着：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">0x1003 保存：errorCode + errorRegister + errorBit</span><br><span class="line">0x1003 不保存：infoCode，也就是 EMCY bytes 4..7</span><br></pre></td></tr></table></figure><p>源码上，<code>0x1003</code> 与 producer 发送共用 <code>CO_EM_fifo_t</code>。<code>fifo.msg</code> 保存 bytes <code>0..3</code>，<code>fifo.info</code> 保存 producer 发帧用的 bytes <code>4..7</code>。</p><h3 id="2-3-0x1014-COB-ID-EMCY"><a href="#2-3-0x1014-COB-ID-EMCY" class="headerlink" title="2.3 0x1014 COB-ID EMCY"></a>2.3 <code>0x1014 COB-ID EMCY</code></h3><p><code>0x1014</code> 决定 EMCY producer 使用的 COB-ID。若 <code>CO_CONFIG_EM_PROD_CONFIGURABLE</code> 未启用，CANopenNode 使用默认 <code>CO_CAN_ID_EMERGENCY + nodeId</code>；若启用，则 <code>OD_write_1014()</code> 会校验写入值，尤其避免 producer 已启用时随意切换正在使用的 CAN-ID。</p><h3 id="2-4-0x1015-Inhibit-time-EMCY"><a href="#2-4-0x1015-Inhibit-time-EMCY" class="headerlink" title="2.4 0x1015 Inhibit time EMCY"></a>2.4 <code>0x1015 Inhibit time EMCY</code></h3><p><code>0x1015</code> 用于限制两帧 EMCY 的最小间隔。源码中 <code>OD_write_1015()</code> 把 OD 中的 <code>UNSIGNED16</code> 数值按 <code>100 us</code> 单位换算为微秒：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">em-&gt;inhibitEmTime_us = (<span class="type">uint32_t</span>)CO_getUint16(buf) * <span class="number">100U</span>;</span><br><span class="line">em-&gt;inhibitEmTimer = <span class="number">0</span>;</span><br></pre></td></tr></table></figure><p>因此，<code>0x1015 = 10</code> 表示 <code>1000 us</code>，即 <code>1 ms</code>。</p><hr><h2 id="3-源码文件分工"><a href="#3-源码文件分工" class="headerlink" title="3. 源码文件分工"></a>3. 源码文件分工</h2><p>建议先读 <code>CO_Emergency.h</code>，再读 <code>CO_Emergency.c</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">CO_Emergency.h</span><br><span class="line">  -&gt; 默认配置宏</span><br><span class="line">  -&gt; CO_errorRegister_t</span><br><span class="line">  -&gt; CO_EM_errorCode_t</span><br><span class="line">  -&gt; CO_EM_errorStatusBits_t</span><br><span class="line">  -&gt; CO_EM_fifo_t</span><br><span class="line">  -&gt; CO_EM_t</span><br><span class="line">  -&gt; CO_EM_init / CO_error / CO_EM_process 声明</span><br><span class="line"></span><br><span class="line">CO_Emergency.c</span><br><span class="line">  -&gt; OD 读写扩展函数</span><br><span class="line">  -&gt; CO_EM_init()</span><br><span class="line">  -&gt; CO_EM_receive() / callback</span><br><span class="line">  -&gt; CO_EM_process()</span><br><span class="line">  -&gt; CO_error()</span><br></pre></td></tr></table></figure><p><code>CO_EM_t</code> 是运行态中心结构体，可以按功能拆成：</p><table><thead><tr><th>字段</th><th>作用</th></tr></thead><tbody><tr><td><code>errorStatusBits[]</code></td><td>当前内部错误位图，一个错误条件对应一个 bit</td></tr><tr><td><code>errorRegister</code></td><td>指向 OD <code>0x1001,00</code> 的指针</td></tr><tr><td><code>CANerrorStatusOld</code></td><td>保存上次 CAN driver 错误状态，用于检测变化</td></tr><tr><td><code>fifo/fifoWrPtr/fifoPpPtr/fifoCount/fifoOverflow</code></td><td>环形 FIFO，用于 producer 发送和 <code>0x1003</code> 历史</td></tr><tr><td><code>producerEnabled/nodeId/CANtxBuff/inhibitEmTimer</code></td><td>producer 发送状态</td></tr><tr><td><code>OD_1014_extension/OD_1015_extension/OD_1003_extension/OD_statusBits_extension</code></td><td>OD 动态访问扩展</td></tr><tr><td><code>pFunctSignalRx/pFunctSignalPre</code></td><td>consumer 回调和 pre callback</td></tr></tbody></table><hr><h2 id="4-CO-EM-init-：初始化时把-OD、FIFO-和-CAN-buffer-接起来"><a href="#4-CO-EM-init-：初始化时把-OD、FIFO-和-CAN-buffer-接起来" class="headerlink" title="4. CO_EM_init()：初始化时把 OD、FIFO 和 CAN buffer 接起来"></a>4. <code>CO_EM_init()</code>：初始化时把 OD、FIFO 和 CAN buffer 接起来</h2><p><img src="https://i-blog.csdnimg.cn/direct/01714cb4f15c4b0fb8450f9626a588c6.png" alt="在这里插入图片描述"></p><p>你贴的初始化调用本质上是在把 CiA 301 EMCY 需要的对象连接到 CANopenNode 运行时：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line">err = CO_EM_init(co-&gt;em, co-&gt;CANmodule, OD_GET(H1001, OD_H1001_ERR_REG),</span><br><span class="line"><span class="meta">#<span class="keyword">if</span> ((CO_CONFIG_EM) &amp; (CO_CONFIG_EM_PRODUCER | CO_CONFIG_EM_HISTORY)) != 0</span></span><br><span class="line">                 co-&gt;em_fifo, (CO_GET_CNT(ARR_1003) + <span class="number">1U</span>),</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">if</span> ((CO_CONFIG_EM)&amp;CO_CONFIG_EM_PRODUCER) != 0</span></span><br><span class="line">                 OD_GET(H1014, OD_H1014_COBID_EMERGENCY), CO_GET_CO(TX_IDX_EM_PROD),</span><br><span class="line"><span class="meta">#<span class="keyword">if</span> ((CO_CONFIG_EM)&amp;CO_CONFIG_EM_PROD_INHIBIT) != 0</span></span><br><span class="line">                 OD_GET(H1015, OD_H1015_INHIBIT_TIME_EMCY),</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">if</span> ((CO_CONFIG_EM)&amp;CO_CONFIG_EM_HISTORY) != 0</span></span><br><span class="line">                 OD_GET(H1003, OD_H1003_PREDEF_ERR_FIELD),</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">if</span> ((CO_CONFIG_EM)&amp;CO_CONFIG_EM_STATUS_BITS) != 0</span></span><br><span class="line">                 OD_statusBits,</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">if</span> ((CO_CONFIG_EM)&amp;CO_CONFIG_EM_CONSUMER) != 0</span></span><br><span class="line">                 co-&gt;CANmodule, CO_GET_CO(RX_IDX_EM_CONS),</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line">                 nodeId, errInfo);</span><br></pre></td></tr></table></figure><p>逐项看：</p><table><thead><tr><th>初始化参数</th><th>含义</th><th>影响</th></tr></thead><tbody><tr><td><code>co-&gt;em</code></td><td>Emergency 对象实例</td><td>保存运行态：状态位、FIFO、OD 扩展、TX&#x2F;RX 回调</td></tr><tr><td><code>co-&gt;CANmodule</code></td><td>CAN 模块</td><td>用于 TX，也用于读取 <code>CANerrorStatus</code></td></tr><tr><td><code>OD_GET(H1001, ...)</code></td><td><code>0x1001 Error register</code></td><td><code>CO_EM_process()</code> 计算后写入</td></tr><tr><td><code>co-&gt;em_fifo</code></td><td>EM FIFO</td><td>producer 发送队列和 <code>0x1003</code> 历史共用</td></tr><tr><td><code>CO_GET_CNT(ARR_1003) + 1U</code></td><td>FIFO 数组大小</td><td>实际可保存历史条数是 <code>ARR_1003</code></td></tr><tr><td><code>OD_GET(H1014, ...)</code></td><td><code>0x1014 COB-ID EMCY</code></td><td>决定 producer CAN-ID 和 enabled 状态</td></tr><tr><td><code>CO_GET_CO(TX_IDX_EM_PROD)</code></td><td>TX buffer index</td><td>EMCY producer 使用的 CAN TX buffer</td></tr><tr><td><code>OD_GET(H1015, ...)</code></td><td><code>0x1015 Inhibit time</code></td><td>控制 EMCY 最小发送间隔</td></tr><tr><td><code>OD_GET(H1003, ...)</code></td><td><code>0x1003 Pre-defined error field</code></td><td>挂接错误历史读写扩展</td></tr><tr><td><code>OD_statusBits</code></td><td>自定义内部状态位 OD 入口</td><td>仅在 <code>CO_CONFIG_EM_STATUS_BITS</code> 打开时出现</td></tr><tr><td><code>CO_GET_CO(RX_IDX_EM_CONS)</code></td><td>consumer RX buffer index</td><td>接收其他节点默认范围 EMCY</td></tr><tr><td><code>nodeId</code></td><td>当前节点号</td><td>默认 EMCY CAN-ID &#x3D; <code>0x80 + nodeId</code></td></tr></tbody></table><h3 id="4-1-为什么-em-fifo-是-ARR-1003-1"><a href="#4-1-为什么-em-fifo-是-ARR-1003-1" class="headerlink" title="4.1 为什么 em_fifo 是 ARR_1003 + 1"></a>4.1 为什么 <code>em_fifo</code> 是 <code>ARR_1003 + 1</code></h3><p>环形 FIFO 需要保留一个空槽来区分“空”和“满”：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">fifoWrPtr == fifoPpPtr       -&gt; 空</span><br><span class="line">fifoWrPtrNext == fifoPpPtr   -&gt; 满</span><br></pre></td></tr></table></figure><p>所以：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">fifoSize = 0x1003 历史容量 + 1</span><br><span class="line">实际容量 = fifoSize - 1</span><br></pre></td></tr></table></figure><p><code>CO_Emergency.c</code> 文件顶部也用 <code>fifoSize = 7</code>、实际容量 <code>6</code> 的图示说明了这个关系。</p><hr><h2 id="5-CO-error-：错误变化如何进入内部状态和-FIFO"><a href="#5-CO-error-：错误变化如何进入内部状态和-FIFO" class="headerlink" title="5. CO_error()：错误变化如何进入内部状态和 FIFO"></a>5. <code>CO_error()</code>：错误变化如何进入内部状态和 FIFO</h2><p><img src="https://i-blog.csdnimg.cn/direct/c47247f95d644dfca8446624bf44c405.png" alt="在这里插入图片描述"></p><p><code>CO_errorReport()</code> 和 <code>CO_errorReset()</code> 都是宏，最终进入 <code>CO_error()</code>：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> CO_errorReport(em, errorBit, errorCode, infoCode) CO_error(em, true, errorBit, errorCode, infoCode)</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> CO_errorReset(em, errorBit, infoCode)             CO_error(em, false, errorBit, CO_EMC_NO_ERROR, infoCode)</span></span><br></pre></td></tr></table></figure><h3 id="5-1-errorBit-转成数组下标和位掩码"><a href="#5-1-errorBit-转成数组下标和位掩码" class="headerlink" title="5.1 errorBit 转成数组下标和位掩码"></a>5.1 <code>errorBit</code> 转成数组下标和位掩码</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">uint8_t</span> index = errorBit &gt;&gt; <span class="number">3</span>;</span><br><span class="line"><span class="type">uint8_t</span> bitmask = <span class="number">1U</span> &lt;&lt; (errorBit &amp; <span class="number">0x7U</span>);</span><br></pre></td></tr></table></figure><p>例如 <code>errorBit = 0x12</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">index   = 0x12 &gt;&gt; 3 = 2</span><br><span class="line">bitmask = 1 &lt;&lt; 2 = 0x04</span><br><span class="line">目标位  = errorStatusBits[2] 的 bit2</span><br></pre></td></tr></table></figure><h3 id="5-2-重复调用不会重复入队"><a href="#5-2-重复调用不会重复入队" class="headerlink" title="5.2 重复调用不会重复入队"></a>5.2 重复调用不会重复入队</h3><p>源码先判断状态是否变化：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (setError) &#123;</span><br><span class="line">    <span class="keyword">if</span> (errorStatusBitMasked != <span class="number">0U</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (errorStatusBitMasked == <span class="number">0U</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    errorCode = CO_EMC_NO_ERROR;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>效果如下：</p><table><thead><tr><th>调用</th><th align="right">当前 bit</th><th>结果</th></tr></thead><tbody><tr><td>report</td><td align="right">1</td><td>直接返回</td></tr><tr><td>report</td><td align="right">0</td><td>置位并写 FIFO</td></tr><tr><td>reset</td><td align="right">0</td><td>直接返回</td></tr><tr><td>reset</td><td align="right">1</td><td>清位并写 FIFO，error code 改成 <code>0x0000</code></td></tr></tbody></table><p>这与 EMCY “同一 error event 只发送一次”的协议原则一致。</p><h3 id="5-3-CO-error-先留空-error-register"><a href="#5-3-CO-error-先留空-error-register" class="headerlink" title="5.3 CO_error() 先留空 error register"></a>5.3 <code>CO_error()</code> 先留空 error register</h3><p><code>CO_error()</code> 准备的 <code>errMsg</code> 是：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">uint32_t</span> errMsg = ((<span class="type">uint32_t</span>)errorBit &lt;&lt; <span class="number">24</span>) | CO_SWAP_16(errorCode);</span><br></pre></td></tr></table></figure><p>此时只放入：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">byte0..1 = errorCode</span><br><span class="line">byte2    = 暂空</span><br><span class="line">byte3    = errorBit</span><br></pre></td></tr></table></figure><p>byte2 不在这里填，是因为 error register 需要综合全部 <code>errorStatusBits[]</code> 和 <code>CO_CONFIG_ERR_CONDITION_xxx</code> 宏计算。这个工作放在周期性的 <code>CO_EM_process()</code> 更合适。</p><hr><h2 id="6-CO-EM-process-：计算-0x1001-并发送-EMCY"><a href="#6-CO-EM-process-：计算-0x1001-并发送-EMCY" class="headerlink" title="6. CO_EM_process()：计算 0x1001 并发送 EMCY"></a>6. <code>CO_EM_process()</code>：计算 <code>0x1001</code> 并发送 EMCY</h2><p><img src="https://i-blog.csdnimg.cn/direct/19b59dd77f00425bad3fea97a9d842c4.png" alt="在这里插入图片描述"></p><p>CANopenNode 官方 Doxygen 说明 <code>CO_EM_process()</code> 必须周期调用，它会检查部分通信错误、计算 OD <code>0x1001</code>，并在必要时发送 EMCY。[^canopennode-em]</p><p>源码顺序是：</p><ol><li>检查 CAN driver 的错误状态变化。</li><li>根据 <code>errorStatusBits[]</code> 计算 <code>errorRegister</code>，写入 <code>*em-&gt;errorRegister</code>。</li><li>若当前上下文不允许发送，则返回。</li><li>若 producer、FIFO、TX buffer、inhibit time 条件满足，则发送一帧 EMCY。</li></ol><h3 id="6-1-CAN-driver-错误也会变成-EM-输入"><a href="#6-1-CAN-driver-错误也会变成-EM-输入" class="headerlink" title="6.1 CAN driver 错误也会变成 EM 输入"></a>6.1 CAN driver 错误也会变成 EM 输入</h3><p><code>CO_EM_process()</code> 读取：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">uint16_t</span> CANerrSt = em-&gt;CANdevTx-&gt;CANerrorStatus;</span><br></pre></td></tr></table></figure><p>如果与 <code>CANerrorStatusOld</code> 不同，就把变化转换成 <code>CO_error()</code> 调用。例如：</p><table><thead><tr><th>CAN driver 状态</th><th>CANopenNode errorBit</th><th>errorCode</th></tr></thead><tbody><tr><td>TX&#x2F;RX warning</td><td><code>CO_EM_CAN_BUS_WARNING</code></td><td><code>CO_EMC_NO_ERROR</code></td></tr><tr><td>TX passive</td><td><code>CO_EM_CAN_TX_BUS_PASSIVE</code></td><td><code>CO_EMC_CAN_PASSIVE</code></td></tr><tr><td>TX bus off</td><td><code>CO_EM_CAN_TX_BUS_OFF</code></td><td><code>CO_EMC_BUS_OFF_RECOVERED</code></td></tr><tr><td>TX overflow</td><td><code>CO_EM_CAN_TX_OVERFLOW</code></td><td><code>CO_EMC_CAN_OVERRUN</code></td></tr><tr><td>RX overflow</td><td><code>CO_EM_CAN_RXB_OVERFLOW</code></td><td><code>CO_EMC_CAN_OVERRUN</code></td></tr></tbody></table><h3 id="6-2-0x1001-是由条件宏汇总的"><a href="#6-2-0x1001-是由条件宏汇总的" class="headerlink" title="6.2 0x1001 是由条件宏汇总的"></a>6.2 <code>0x1001</code> 是由条件宏汇总的</h3><p>默认配置中，CANopenNode 预定义了三类条件：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> CO_CONFIG_ERR_CONDITION_GENERIC       (em-&gt;errorStatusBits[5] != 0U)</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> CO_CONFIG_ERR_CONDITION_COMMUNICATION ((em-&gt;errorStatusBits[2] != 0U) || (em-&gt;errorStatusBits[3] != 0U))</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> CO_CONFIG_ERR_CONDITION_MANUFACTURER  ((em-&gt;errorStatusBits[8] != 0U) || (em-&gt;errorStatusBits[9] != 0U))</span></span><br></pre></td></tr></table></figure><p>这就是 <code>errorStatusBits[]</code> 与 <code>0x1001</code> 的关系：前者是细粒度内部状态，后者是 CANopen 标准对象中对错误类别的汇总。</p><h3 id="6-3-发送前补齐-byte2"><a href="#6-3-发送前补齐-byte2" class="headerlink" title="6.3 发送前补齐 byte2"></a>6.3 发送前补齐 byte2</h3><p>producer 分支中，满足条件后源码补齐 byte2：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">em-&gt;fifo[fifoPpPtr].msg |= (<span class="type">uint32_t</span>)errorRegister &lt;&lt; <span class="number">16</span>;</span><br></pre></td></tr></table></figure><p>然后复制 8 字节并发送：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">(<span class="type">void</span>)<span class="built_in">memcpy</span>((<span class="type">void</span>*)em-&gt;CANtxBuff-&gt;data, (<span class="type">void</span>*)&amp;em-&gt;fifo[fifoPpPtr].msg, <span class="keyword">sizeof</span>(em-&gt;CANtxBuff-&gt;data));</span><br><span class="line">(<span class="type">void</span>)CO_CANsend(em-&gt;CANdevTx, em-&gt;CANtxBuff);</span><br></pre></td></tr></table></figure><p><code>CO_EM_fifo_t</code> 的字段顺序是 <code>msg</code> 后接 <code>info</code>，所以这次 8 字节复制会把 <code>msg</code> 和 <code>info</code> 一起送入 CAN TX buffer。</p><hr><h2 id="7-FIFO-与-0x1003：事件队列和历史读取共用一套存储"><a href="#7-FIFO-与-0x1003：事件队列和历史读取共用一套存储" class="headerlink" title="7. FIFO 与 0x1003：事件队列和历史读取共用一套存储"></a>7. FIFO 与 <code>0x1003</code>：事件队列和历史读取共用一套存储</h2><p><img src="https://i-blog.csdnimg.cn/direct/6294824811c744669485577ef9c39a10.png" alt="在这里插入图片描述"></p><p><code>errorStatusBits[]</code> 保存当前状态；FIFO 保存状态变化事件。例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">过压出现       -&gt; report -&gt; FIFO 加一条错误事件</span><br><span class="line">过压仍然存在   -&gt; report -&gt; 状态未变，不加</span><br><span class="line">过压恢复       -&gt; reset  -&gt; FIFO 加一条恢复事件</span><br><span class="line">过压已经恢复   -&gt; reset  -&gt; 状态未变，不加</span><br></pre></td></tr></table></figure><p><code>OD_read_1003()</code> 中，subindex <code>0</code> 返回 <code>fifoCount</code>，subindex <code>1</code> 返回最新错误。它从 <code>fifoWrPtr</code> 往回数，因此 <code>0x1003:01</code> 是最新一条，<code>0x1003:02</code> 是次新一条。</p><p>写 <code>0x1003:00 = 0</code> 会清空错误历史计数：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (CO_getUint8(buf) != <span class="number">0U</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> ODR_INVALID_VALUE;</span><br><span class="line">&#125;</span><br><span class="line">em-&gt;fifoCount = <span class="number">0</span>;</span><br></pre></td></tr></table></figure><p>它清的是历史读取计数，不是直接清 <code>errorStatusBits[]</code> 当前状态。</p><hr><h2 id="8-CO-CONFIG-EM：配置宏影响哪些源码路径"><a href="#8-CO-CONFIG-EM：配置宏影响哪些源码路径" class="headerlink" title="8. CO_CONFIG_EM：配置宏影响哪些源码路径"></a>8. <code>CO_CONFIG_EM</code>：配置宏影响哪些源码路径</h2><p><img src="https://i-blog.csdnimg.cn/direct/59e5a4cbfc9c43ea86afcd2a8a5f5a1a.png" alt="在这里插入图片描述"></p><p>CANopenNode 官方配置文档列出 <code>CO_CONFIG_EM</code> 的可选 flag：producer、producer COB-ID configurable、producer inhibit、history、consumer、status bits、callback pre、timer next。[^canopennode-config]</p><table><thead><tr><th>配置</th><th>作用</th><th>主要源码路径</th></tr></thead><tbody><tr><td><code>CO_CONFIG_EM_PRODUCER</code></td><td>启用本节点 EMCY 发送</td><td><code>CO_EM_init()</code> 初始化 <code>0x1014</code> 和 TX buffer；<code>CO_EM_process()</code> 发送</td></tr><tr><td><code>CO_CONFIG_EM_PROD_CONFIGURABLE</code></td><td>允许运行期配置 producer COB-ID</td><td><code>OD_read_1014()</code> &#x2F; <code>OD_write_1014()</code></td></tr><tr><td><code>CO_CONFIG_EM_PROD_INHIBIT</code></td><td>启用发送抑制时间</td><td><code>OD_write_1015()</code>、<code>inhibitEmTimer</code></td></tr><tr><td><code>CO_CONFIG_EM_HISTORY</code></td><td>启用错误历史</td><td><code>OD_read_1003()</code> &#x2F; <code>OD_write_1003()</code></td></tr><tr><td><code>CO_CONFIG_EM_CONSUMER</code></td><td>接收其他节点 EMCY</td><td><code>CO_EM_receive()</code>、<code>CO_EM_initCallbackRx()</code></td></tr><tr><td><code>CO_CONFIG_EM_STATUS_BITS</code></td><td>通过 OD 访问内部 <code>errorStatusBits[]</code></td><td><code>OD_read_statusBits()</code> &#x2F; <code>OD_write_statusBits()</code></td></tr><tr><td><code>CO_CONFIG_FLAG_CALLBACK_PRE</code></td><td>错误变化后触发回调</td><td><code>CO_EM_initCallbackPre()</code>、<code>CO_error()</code> 末尾回调</td></tr><tr><td><code>CO_CONFIG_FLAG_TIMERNEXT</code></td><td>计算下次处理时间</td><td>inhibit 分支更新 <code>timerNext_us</code></td></tr></tbody></table><hr><h2 id="9-CO-CONFIG-EM-STATUS-BITS：补充说明"><a href="#9-CO-CONFIG-EM-STATUS-BITS：补充说明" class="headerlink" title="9. CO_CONFIG_EM_STATUS_BITS：补充说明"></a>9. <code>CO_CONFIG_EM_STATUS_BITS</code>：补充说明</h2><p><img src="https://i-blog.csdnimg.cn/direct/6233fe4735e741cdae11ce617e4d7e8e.png" alt="在这里插入图片描述"></p><p><code>CO_CONFIG_EM_STATUS_BITS</code> 容易被忽略，因为它不影响 EMCY 帧格式，也不是 CiA 301 标准的固定对象号。CANopenNode 官方配置文档对它的定义是：从 OD 访问 Error status bits。[^canopennode-config]</p><p>它的作用可以概括为：<strong>把 <code>CO_EM_t.errorStatusBits[]</code> 暴露给一个自定义 OD 项，供 SDO&#x2F;调试工具&#x2F;厂商扩展读取或写入内部错误位图。</strong></p><h3 id="9-1-初始化时需要-OD-statusBits"><a href="#9-1-初始化时需要-OD-statusBits" class="headerlink" title="9.1 初始化时需要 OD_statusBits"></a>9.1 初始化时需要 <code>OD_statusBits</code></h3><p>头文件的 <code>CO_EM_init()</code> 参数说明写明：<code>OD_statusBits</code> 是用于访问 <code>CO_EM_t</code> 中 <code>errorStatusBits</code> 的自定义 OD entry；这个 entry 需要在 subindex <code>0</code> 上提供 <code>(CO_CONFIG_EM_ERR_STATUS_BITS_COUNT / 8)</code> 字节变量，并要求 IO extension。源码在 <code>CO_CONFIG_EM_STATUS_BITS</code> 打开时才会展开这个参数。</p><p>对应初始化分支：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">if</span> ((CO_CONFIG_EM)&amp;CO_CONFIG_EM_STATUS_BITS) != 0</span></span><br><span class="line">    em-&gt;OD_statusBits_extension.object = em;</span><br><span class="line">    em-&gt;OD_statusBits_extension.read = OD_read_statusBits;</span><br><span class="line">    em-&gt;OD_statusBits_extension.write = OD_write_statusBits;</span><br><span class="line">    (<span class="type">void</span>)OD_extension_init(OD_statusBits, &amp;em-&gt;OD_statusBits_extension);</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br></pre></td></tr></table></figure><h3 id="9-2-读路径：读取内部错误位图"><a href="#9-2-读路径：读取内部错误位图" class="headerlink" title="9.2 读路径：读取内部错误位图"></a>9.2 读路径：读取内部错误位图</h3><p><code>OD_read_statusBits()</code> 的核心行为是把内部状态位复制出去：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">OD_size_t countReadLocal = CO_CONFIG_EM_ERR_STATUS_BITS_COUNT / <span class="number">8U</span>;</span><br><span class="line">...</span><br><span class="line">(<span class="type">void</span>)<span class="built_in">memcpy</span>((<span class="type">void</span>*)(buf), (<span class="type">const</span> <span class="type">void</span>*)(&amp;em-&gt;errorStatusBits[<span class="number">0</span>]), countReadLocal);</span><br></pre></td></tr></table></figure><p>因此，如果你通过 SDO 读取这个自定义 OD 项，读到的是当前 <code>errorStatusBits[]</code> 的原始位图。它比 <code>0x1001</code> 更细，但也更偏 CANopenNode 内部实现。</p><h3 id="9-3-写路径：可直接改内部错误位图"><a href="#9-3-写路径：可直接改内部错误位图" class="headerlink" title="9.3 写路径：可直接改内部错误位图"></a>9.3 写路径：可直接改内部错误位图</h3><p><code>OD_write_statusBits()</code> 的核心行为是反向复制：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">OD_size_t countWrite = CO_CONFIG_EM_ERR_STATUS_BITS_COUNT / <span class="number">8U</span>;</span><br><span class="line">...</span><br><span class="line">(<span class="type">void</span>)<span class="built_in">memcpy</span>((<span class="type">void</span>*)(&amp;em-&gt;errorStatusBits[<span class="number">0</span>]), (<span class="type">const</span> <span class="type">void</span>*)(buf), countWrite);</span><br></pre></td></tr></table></figure><p>这说明它不是只读诊断入口，而是可写入口。写入后，下一次 <code>CO_EM_process()</code> 会基于新的 <code>errorStatusBits[]</code> 重新计算 <code>0x1001</code>。但要注意：直接写 <code>errorStatusBits[]</code> 不会像 <code>CO_errorReport()</code> 那样自动生成 FIFO 事件，也不会自动形成一帧 EMCY。需要事件上报时，应用仍应调用 <code>CO_errorReport()</code> &#x2F; <code>CO_errorReset()</code>。</p><h3 id="9-4-它和-0x1001、0x1003-的区别"><a href="#9-4-它和-0x1001、0x1003-的区别" class="headerlink" title="9.4 它和 0x1001、0x1003 的区别"></a>9.4 它和 <code>0x1001</code>、<code>0x1003</code> 的区别</h3><table><thead><tr><th>对象&#x2F;入口</th><th>内容</th><th align="right">是否标准固定对象</th><th align="right">是否触发 EMCY</th></tr></thead><tbody><tr><td><code>0x1001</code></td><td>错误类别汇总</td><td align="right">是</td><td align="right">否，只是当前状态输出</td></tr><tr><td><code>0x1003</code></td><td>最近错误历史 bytes <code>0..3</code></td><td align="right">是</td><td align="right">否，只是历史读取</td></tr><tr><td><code>OD_statusBits</code></td><td>内部 <code>errorStatusBits[]</code> 原始位图</td><td align="right">否，由工程自定义</td><td align="right">否，直接读写不产生事件</td></tr><tr><td><code>CO_errorReport()</code></td><td>错误发生事件输入</td><td align="right">API</td><td align="right">是，入 FIFO 后由 <code>CO_EM_process()</code> 发送</td></tr></tbody></table><h3 id="9-5-什么时候需要打开"><a href="#9-5-什么时候需要打开" class="headerlink" title="9.5 什么时候需要打开"></a>9.5 什么时候需要打开</h3><p>建议按用途决定：</p><table><thead><tr><th>场景</th><th>建议</th></tr></thead><tbody><tr><td>只学习 EMCY producer&#x2F;history 主流程</td><td>可不打开，避免混淆</td></tr><tr><td>需要通过 SDO 观察内部每个 <code>CO_EM_xxx</code> bit</td><td>打开，并在 OD 中放一个自定义 byte array</td></tr><tr><td>需要调试 <code>CO_CONFIG_ERR_CONDITION_xxx</code> 为什么使 <code>0x1001</code> 置位</td><td>打开有帮助</td></tr><tr><td>希望主站通过 OD 强行清&#x2F;置内部错误位</td><td>可以打开，但要明确这不会自动生成 EMCY 事件</td></tr></tbody></table><p>工程上更推荐：应用错误的正常生命周期仍用 <code>CO_errorReport()</code> &#x2F; <code>CO_errorReset()</code>；<code>OD_statusBits</code> 主要作为调试和诊断入口。</p><hr><h2 id="10-从机工程中的最小使用路径"><a href="#10-从机工程中的最小使用路径" class="headerlink" title="10. 从机工程中的最小使用路径"></a>10. 从机工程中的最小使用路径</h2><p>应用层上报自定义错误时，优先使用 manufacturer 范围的 <code>errorBit</code>：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> APP_EM_MOTOR_OVERLOAD  (CO_EM_MANUFACTURER_START + 0U)</span></span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">app_report_motor_overload</span><span class="params">(CO_t *co, <span class="type">uint32_t</span> detail)</span></span><br><span class="line">&#123;</span><br><span class="line">    CO_errorReport(co-&gt;em, APP_EM_MOTOR_OVERLOAD, CO_EMC_DEVICE_SPECIFIC, detail);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">app_reset_motor_overload</span><span class="params">(CO_t *co, <span class="type">uint32_t</span> detail)</span></span><br><span class="line">&#123;</span><br><span class="line">    CO_errorReset(co-&gt;em, APP_EM_MOTOR_OVERLOAD, detail);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>调试时按这个顺序检查：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">1. CAN 总线上是否出现 0x80 + Node-ID 的 8 字节帧</span><br><span class="line">2. SDO 读 0x1001，看错误类别位是否变化</span><br><span class="line">3. SDO 读 0x1003:00，看历史条数</span><br><span class="line">4. SDO 读 0x1003:01，看最新错误 bytes 0..3</span><br><span class="line">5. 确认 CO_EM_process() 被周期调用</span><br><span class="line">6. 确认当前上下文允许发送 EMCY</span><br><span class="line">7. 检查 0x1014 bit31 是否禁用了 producer</span><br><span class="line">8. 检查 0x1015 是否导致发送被抑制</span><br><span class="line">9. 若打开 STATUS_BITS，读自定义 OD_statusBits 看内部 bit 是否符合预期</span><br></pre></td></tr></table></figure><p>最小移植检查表：</p><table><thead><tr><th>检查项</th><th>预期</th></tr></thead><tbody><tr><td><code>CO_GET_CNT(EM) == 1</code></td><td>工程里确实有 Emergency 对象</td></tr><tr><td><code>0x1001</code></td><td>存在，<code>UNSIGNED8</code>，可被 EM 模块绑定</td></tr><tr><td><code>0x1003</code></td><td>启用 history 时存在，容量和 <code>ARR_1003</code> 一致</td></tr><tr><td><code>0x1014</code></td><td>启用 producer 时存在，默认值能得到 <code>0x80 + nodeId</code></td></tr><tr><td><code>0x1015</code></td><td>启用 inhibit 时存在，单位是 <code>100 us</code></td></tr><tr><td><code>OD_statusBits</code></td><td>仅启用 <code>CO_CONFIG_EM_STATUS_BITS</code> 时需要自定义 OD entry</td></tr><tr><td><code>CO_EM_process()</code></td><td>在 CANopen 主循环中周期调用</td></tr><tr><td>TX buffer index</td><td><code>TX_IDX_EM_PROD</code> 不与其他对象冲突</td></tr></tbody></table><hr><h2 id="11-参考资料"><a href="#11-参考资料" class="headerlink" title="11. 参考资料"></a>11. 参考资料</h2><p>[^cia-emcy]: CAN in Automation (CiA), “Special function protocols”，Emergency protocol 说明 EMCY 用于通知设备内部错误、映射到单个 CAN CC 帧、默认 CAN-ID 为 <code>80h + node-ID</code>，且每个 error event 只发送一次。<a href="https://www.can-cia.org/can-knowledge/special-function-protocols">https://www.can-cia.org/can-knowledge/special-function-protocols</a></p><p>[^canopennode-em]: CANopenNode 官方 Doxygen，“Emergency”，说明 <code>CO_EM_init()</code>、<code>CO_EM_process()</code>、<code>CO_error()</code> 以及 Emergency producer message bytes 布局和 <code>0x1003</code> 历史。<a href="https://canopennode.github.io/CANopenNode/group__CO__Emergency.html">https://canopennode.github.io/CANopenNode/group__CO__Emergency.html</a></p><p>[^canopennode-config]: CANopenNode 官方 Doxygen，“Emergency producer&#x2F;consumer”，说明 <code>CO_CONFIG_EM</code> 的 producer、configurable、inhibit、history、consumer、status bits、callback pre、timer next 等配置，以及 <code>CO_CONFIG_EM_ERR_STATUS_BITS_COUNT</code> 默认值和范围。<a href="https://canopennode.github.io/CANopenNode/group__CO__STACK__CONFIG__EMERGENCY.html">https://canopennode.github.io/CANopenNode/group__CO__STACK__CONFIG__EMERGENCY.html</a></p><p>[^canopennode-repo]: CANopenNode GitHub README 说明 CANopenNode 可以运行在不同设备或微控制器上，具体设备接口通常在独立工程中维护。<a href="https://github.com/CANopenNode/CANopenNode">https://github.com/CANopenNode/CANopenNode</a></p>]]></content>
    
    
      
      
    <summary type="html">&lt;meta name=&quot;referrer&quot; content=&quot;no-referrer&quot; /&gt;

&lt;h1 id=&quot;CANopen-Emergency：-搞懂-EM-运行流程&quot;&gt;&lt;a href=&quot;#CANopen-Emergency：-搞懂-EM-运行流程&quot; class=&quot;heade</summary>
      
    
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/categories/canopen/"/>
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/tags/canopen/"/>
    
  </entry>
  
  <entry>
    <title>CiA 303_3 CANopenNode 如何计算 RUN ERROR 指示灯</title>
    <link href="https://wdfk-prog.space/posts/71b5d5a9/"/>
    <id>https://wdfk-prog.space/posts/71b5d5a9/</id>
    <published>2026-06-28T03:10:51.000Z</published>
    <updated>2026-06-28T03:11:23.290Z</updated>
    
    <content type="html"><![CDATA[<meta name="referrer" content="no-referrer" /><h1 id="按-LED-源码学习-CiA-303-3：CANopenNode-如何计算-RUN-ERROR-指示灯"><a href="#按-LED-源码学习-CiA-303-3：CANopenNode-如何计算-RUN-ERROR-指示灯" class="headerlink" title="按 LED 源码学习 CiA 303-3：CANopenNode 如何计算 RUN &#x2F; ERROR 指示灯"></a>按 LED 源码学习 CiA 303-3：CANopenNode 如何计算 RUN &#x2F; ERROR 指示灯</h1><p>@[toc]</p><h2 id="结论"><a href="#结论" class="headerlink" title="结论"></a>结论</h2><p><code>303/CO_LEDs.c/h</code> 的核心不是“驱动 LED”，而是实现 <strong>CiA 303-3 CANopen indicators</strong>：把 CANopen 设备的通信相关状态，转换成标准的 <strong>绿色 RUN LED</strong> 与 <strong>红色 ERROR LED</strong> 指示模式。</p><p>本模块的输入来自 CANopen 状态与错误上下文，例如 NMT 状态、LSS 配置状态、CAN bus-off、CAN warning、Heartbeat consumer 错误、SYNC 超时、RPDO event timer 超时、其他错误、固件下载状态等；输出是 <code>LEDred</code> &#x2F; <code>LEDgreen</code> bitfield 中的 <code>CO_LED_CANopen</code> 位。</p><p>本文只讨论 CANopenNode 经典 CANopen 的：</p><ul><li><code>303/CO_LEDs.h</code></li><li><code>303/CO_LEDs.c</code></li></ul><p>NMT、LSS、Heartbeat consumer、SYNC、PDO、CAN bus-off 只作为 LED 输入上下文出现，不展开这些协议模块本身。</p><p><img src="https://i-blog.csdnimg.cn/direct/cc074b4cca994686854d8c5fa7d46aa0.png" alt="在这里插入图片描述"></p><h2 id="1-先理解-CiA-303-3：它规定的是“通信状态怎么被看见”"><a href="#1-先理解-CiA-303-3：它规定的是“通信状态怎么被看见”" class="headerlink" title="1. 先理解 CiA 303-3：它规定的是“通信状态怎么被看见”"></a>1. 先理解 CiA 303-3：它规定的是“通信状态怎么被看见”</h2><p>CiA 303 是 CANopen 相关设备与网络设计建议的一组文档。CiA 官方资料说明，CiA 303-3 是 <strong>indicator specification</strong>，用于描述 CANopen 设备上的通信相关指示器；额外的应用相关指示器应由设备 profile 或制造商自行定义。官方说明还指出，303-3 推荐用 LED 闪烁模式指示设备状态，帮助服务人员在没有完整诊断工具时识别通信问题。[^cia303]</p><p>所以，CiA 303-3 的重点不是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">怎么配置 NMT？</span><br><span class="line">怎么收 Heartbeat？</span><br><span class="line">怎么发 SYNC？</span><br><span class="line">怎么映射 PDO？</span><br></pre></td></tr></table></figure><p>而是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">当这些 CANopen 通信状态已经产生之后，设备应该用什么 LED 模式表达出来？</span><br></pre></td></tr></table></figure><p>CANopenNode 的 Doxygen 明确把 <code>CO_LEDs.h</code> 标为 <code>CANopen Indicator specification (CiA 303-3 v1.4.0)</code>，并说明 CiA 303-3 使用绿色 RUN LED、红色 ERROR LED 或红绿双色 LED 来反映 CANopen 设备状态。[^coleds]</p><h2 id="2-协议层面的两类指示灯"><a href="#2-协议层面的两类指示灯" class="headerlink" title="2. 协议层面的两类指示灯"></a>2. 协议层面的两类指示灯</h2><h3 id="2-1-RUN-绿灯：回答“设备运行到哪一步了”"><a href="#2-1-RUN-绿灯：回答“设备运行到哪一步了”" class="headerlink" title="2.1 RUN 绿灯：回答“设备运行到哪一步了”"></a>2.1 RUN 绿灯：回答“设备运行到哪一步了”</h3><p>CANopenNode 对 RUN LED 的语义列在 <code>CO_LEDs.h</code> 注释和 Doxygen 中：[^coleds]</p><table><thead><tr><th>RUN LED 模式</th><th>表示的 CANopen 状态</th></tr></thead><tbody><tr><td><code>flickering</code></td><td>LSS configuration state is active</td></tr><tr><td><code>blinking</code></td><td>device is in NMT pre-operational state</td></tr><tr><td><code>single flash</code></td><td>device is in NMT stopped state</td></tr><tr><td><code>triple flash</code></td><td>software download is running in the device</td></tr><tr><td><code>on</code></td><td>device is in NMT operational state</td></tr></tbody></table><p>这里的 RUN LED 不是“程序有没有运行”的通用心跳灯，而是 CANopen 通信状态指示灯。它重点表达设备处于配置、预操作、停止、运行或固件下载等状态。</p><h3 id="2-2-ERROR-红灯：回答“通信-配置是否异常”"><a href="#2-2-ERROR-红灯：回答“通信-配置是否异常”" class="headerlink" title="2.2 ERROR 红灯：回答“通信&#x2F;配置是否异常”"></a>2.2 ERROR 红灯：回答“通信&#x2F;配置是否异常”</h3><p>ERROR LED 的语义同样来自 <code>CO_LEDs.h</code> 注释和 Doxygen：[^coleds]</p><table><thead><tr><th>ERROR LED 模式</th><th>表示的 CANopen 错误状态</th></tr></thead><tbody><tr><td><code>off</code></td><td>no error</td></tr><tr><td><code>flickering</code></td><td>LSS node ID is not configured, CANopen is not initialized</td></tr><tr><td><code>blinking</code></td><td>invalid configuration, general error</td></tr><tr><td><code>single flash</code></td><td>CAN warning limit reached</td></tr><tr><td><code>double flash</code></td><td>heartbeat consumer error in remote monitored node</td></tr><tr><td><code>triple flash</code></td><td>sync message reception timeout</td></tr><tr><td><code>quadruple flash</code></td><td>PDO has not been received before the event timer elapsed</td></tr><tr><td><code>on</code></td><td>CAN bus off</td></tr></tbody></table><p>ERROR LED 不是错误日志，也不是错误队列。它在某一时刻只能显示一个最终模式。多个错误同时存在时，CANopenNode 在 <code>CO_LEDs.c</code> 里用固定优先级选择最终显示内容。</p><p><img src="https://i-blog.csdnimg.cn/direct/cb0abad1892b41d0862a092a528ac72d.png" alt="在这里插入图片描述"></p><h2 id="3-CANopenNode-为什么把它放在-303-目录"><a href="#3-CANopenNode-为什么把它放在-303-目录" class="headerlink" title="3. CANopenNode 为什么把它放在 303/ 目录"></a>3. CANopenNode 为什么把它放在 <code>303/</code> 目录</h2><p>CANopenNode 仓库结构中，<code>301/</code> 是 CANopen application layer and communication profile，里面有 NMT、Heartbeat、Emergency、PDO、SYNC 等模块；<code>303/</code> 被标为 CANopen Recommendation，并包含 <code>CO_LEDs.h/.c - CANopen LED Indicators</code>。[^repo]</p><p>这说明 <code>CO_LEDs</code> 的定位不是 CAN 收发驱动，也不是 NMT 状态机本体，而是 CANopen recommendation 层的显示规则实现：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">301/ 模块产生状态或错误上下文</span><br><span class="line">        ↓</span><br><span class="line">303/CO_LEDs.c 按 CiA 303-3 语义选择 LED 模式</span><br><span class="line">        ↓</span><br><span class="line">LEDred / LEDgreen 输出最终 bitfield</span><br></pre></td></tr></table></figure><h2 id="4-从-CO-LEDs-h-开始读：头文件先给出协议语义"><a href="#4-从-CO-LEDs-h-开始读：头文件先给出协议语义" class="headerlink" title="4. 从 CO_LEDs.h 开始读：头文件先给出协议语义"></a>4. 从 <code>CO_LEDs.h</code> 开始读：头文件先给出协议语义</h2><p><code>CO_LEDs.h</code> 里最重要的内容有三类。</p><h3 id="4-1-模式-bitmask"><a href="#4-1-模式-bitmask" class="headerlink" title="4.1 模式 bitmask"></a>4.1 模式 bitmask</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> CO_LED_flicker  0x01U  <span class="comment">/**&lt; LED flickering 10Hz */</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> CO_LED_blink    0x02U  <span class="comment">/**&lt; LED blinking 2,5Hz */</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> CO_LED_flash_1  0x04U  <span class="comment">/**&lt; LED single flash */</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> CO_LED_flash_2  0x08U  <span class="comment">/**&lt; LED double flash */</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> CO_LED_flash_3  0x10U  <span class="comment">/**&lt; LED triple flash */</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> CO_LED_flash_4  0x20U  <span class="comment">/**&lt; LED quadruple flash */</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> CO_LED_CANopen  0x80U  <span class="comment">/**&lt; LED CANopen according to CiA 303-3 */</span></span></span><br></pre></td></tr></table></figure><p>这些宏分成两层：</p><table><thead><tr><th>宏</th><th>作用</th></tr></thead><tbody><tr><td><code>CO_LED_flicker</code> &#x2F; <code>CO_LED_blink</code> &#x2F; <code>CO_LED_flash_1..4</code></td><td>表示某种闪烁模式当前相位是否为 ON</td></tr><tr><td><code>CO_LED_CANopen</code></td><td>表示 CANopen 指示灯最终输出是否为 ON</td></tr></tbody></table><p>也就是说，<code>CO_LED_flash_3</code> 不是“错误类型”，而是“三闪模式在当前时间片是否点亮”的节拍位；<code>CO_LED_CANopen</code> 才是应用层最终要读取的 CANopen LED 开关结果。</p><h3 id="4-2-输出读取宏"><a href="#4-2-输出读取宏" class="headerlink" title="4.2 输出读取宏"></a>4.2 输出读取宏</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> CO_LED_RED(LEDs, BITMASK)   ((((LEDs)-&gt;LEDred &amp; BITMASK) != 0U) ? 1U : 0U)</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> CO_LED_GREEN(LEDs, BITMASK) ((((LEDs)-&gt;LEDgreen &amp; BITMASK) != 0U) ? 1U : 0U)</span></span><br></pre></td></tr></table></figure><p>对 CANopen 指示灯而言，读取方式是：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">CO_LED_RED(LEDs, CO_LED_CANopen)</span><br><span class="line">CO_LED_GREEN(LEDs, CO_LED_CANopen)</span><br></pre></td></tr></table></figure><p>这里仍然只是在读 <code>CO_LEDs_t</code> 内部计算结果，不涉及具体板级输出。</p><h3 id="4-3-CO-LEDs-t-保存时基、模式相位和输出结果"><a href="#4-3-CO-LEDs-t-保存时基、模式相位和输出结果" class="headerlink" title="4.3 CO_LEDs_t 保存时基、模式相位和输出结果"></a>4.3 <code>CO_LEDs_t</code> 保存时基、模式相位和输出结果</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> &#123;</span></span><br><span class="line">    <span class="type">uint32_t</span> LEDtmr50ms;</span><br><span class="line">    <span class="type">uint8_t</span> LEDtmr200ms;</span><br><span class="line">    <span class="type">uint8_t</span> LEDtmrflash_1;</span><br><span class="line">    <span class="type">uint8_t</span> LEDtmrflash_2;</span><br><span class="line">    <span class="type">uint8_t</span> LEDtmrflash_3;</span><br><span class="line">    <span class="type">uint8_t</span> LEDtmrflash_4;</span><br><span class="line">    <span class="type">uint8_t</span> LEDred;</span><br><span class="line">    <span class="type">uint8_t</span> LEDgreen;</span><br><span class="line">&#125; CO_LEDs_t;</span><br></pre></td></tr></table></figure><p>这个结构体不是只保存两个 LED 的开关状态，而是同时保存：</p><ol><li><code>LEDtmr50ms</code>：50 ms 基准时钟累计值；</li><li><code>LEDtmr200ms</code>：200 ms 模式时间槽计数；</li><li><code>LEDtmrflash_1..4</code>：单闪、双闪、三闪、四闪的状态机计数；</li><li><code>LEDred</code> &#x2F; <code>LEDgreen</code>：红绿两路 bitfield，里面既有模式相位位，也有最终 <code>CO_LED_CANopen</code> 输出位。<br><img src="https://i-blog.csdnimg.cn/direct/3761e845fba9476dac16132abcc5e418.png" alt="在这里插入图片描述"></li></ol><h2 id="5-再读-CO-LEDs-c：源码把协议模式拆成“时基-优先级-输出位”"><a href="#5-再读-CO-LEDs-c：源码把协议模式拆成“时基-优先级-输出位”" class="headerlink" title="5. 再读 CO_LEDs.c：源码把协议模式拆成“时基 + 优先级 + 输出位”"></a>5. 再读 <code>CO_LEDs.c</code>：源码把协议模式拆成“时基 + 优先级 + 输出位”</h2><p><code>CO_LEDs.c</code> 只有两个公开函数：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">CO_ReturnError_t <span class="title function_">CO_LEDs_init</span><span class="params">(CO_LEDs_t* LEDs)</span>;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">CO_LEDs_process</span><span class="params">(CO_LEDs_t* LEDs,</span></span><br><span class="line"><span class="params">                     <span class="type">uint32_t</span> timeDifference_us,</span></span><br><span class="line"><span class="params">                     CO_NMT_internalState_t NMTstate,</span></span><br><span class="line"><span class="params">                     <span class="type">bool_t</span> LSSconfig,</span></span><br><span class="line"><span class="params">                     <span class="type">bool_t</span> ErrCANbusOff,</span></span><br><span class="line"><span class="params">                     <span class="type">bool_t</span> ErrCANbusWarn,</span></span><br><span class="line"><span class="params">                     <span class="type">bool_t</span> ErrRpdo,</span></span><br><span class="line"><span class="params">                     <span class="type">bool_t</span> ErrSync,</span></span><br><span class="line"><span class="params">                     <span class="type">bool_t</span> ErrHbCons,</span></span><br><span class="line"><span class="params">                     <span class="type">bool_t</span> ErrOther,</span></span><br><span class="line"><span class="params">                     <span class="type">bool_t</span> firmwareDownload,</span></span><br><span class="line"><span class="params">                     <span class="type">uint32_t</span>* timerNext_us)</span>;</span><br></pre></td></tr></table></figure><p>Doxygen 说明 <code>CO_LEDs_process()</code> 必须周期调用，并且参数中包含 NMT operating state、LSS configuration indication、CAN bus-off、CAN warning、RPDO timeout、SYNC timeout、Heartbeat consumer error、other error、firmware download 等输入。[^coleds]</p><p>因此，它的运行逻辑可以分成三层。</p><p><img src="https://i-blog.csdnimg.cn/direct/2d1567ef44c0422583db5e1b1a15cf29.png" alt="在这里插入图片描述"></p><h3 id="5-1-第一层：累计时间，只在-50-ms-tick-上更新"><a href="#5-1-第一层：累计时间，只在-50-ms-tick-上更新" class="headerlink" title="5.1 第一层：累计时间，只在 50 ms tick 上更新"></a>5.1 第一层：累计时间，只在 50 ms tick 上更新</h3><p>源码先做：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">LEDs-&gt;LEDtmr50ms += timeDifference_us;</span><br><span class="line"><span class="keyword">while</span> (LEDs-&gt;LEDtmr50ms &gt;= <span class="number">50000U</span>) &#123;</span><br><span class="line">    tick = <span class="literal">true</span>;</span><br><span class="line">    LEDs-&gt;LEDtmr50ms -= <span class="number">50000U</span>;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>含义是：<code>CO_LEDs_process()</code> 可以被更高频调用，但 LED 模式本身以 50 ms 为基本离散时间片更新。</p><h3 id="5-2-第二层：生成-flicker-blink-flash-的当前相位"><a href="#5-2-第二层：生成-flicker-blink-flash-的当前相位" class="headerlink" title="5.2 第二层：生成 flicker &#x2F; blink &#x2F; flash 的当前相位"></a>5.2 第二层：生成 flicker &#x2F; blink &#x2F; flash 的当前相位</h3><p>每个 50 ms tick 都会更新 <code>flicker</code> 相位；每累计 4 个 50 ms tick，即 200 ms，再更新 <code>blink</code> 和 <code>flash_1..4</code>。</p><p><code>CO_LEDs.h</code> 给出的模式位含义是：</p><table><thead><tr><th>模式位</th><th>频率&#x2F;形态</th></tr></thead><tbody><tr><td><code>CO_LED_flicker</code></td><td>10 Hz flickering</td></tr><tr><td><code>CO_LED_blink</code></td><td>2.5 Hz blinking</td></tr><tr><td><code>CO_LED_flash_1</code></td><td>single flash</td></tr><tr><td><code>CO_LED_flash_2</code></td><td>double flash</td></tr><tr><td><code>CO_LED_flash_3</code></td><td>triple flash</td></tr><tr><td><code>CO_LED_flash_4</code></td><td>quadruple flash</td></tr></tbody></table><p>源码内部用 <code>rd</code> 和 <code>gr</code> 两个临时 bitfield 生成红、绿两套相位。后面 ERROR LED 只取 <code>rd</code> 中对应模式位，RUN LED 只取 <code>gr</code> 中对应模式位。</p><p>这一步还没有决定“当前是什么 CANopen 状态”，只是准备好各种标准模式在当前时刻的 ON&#x2F;OFF 相位。</p><h3 id="5-3-第三层：用输入状态选择最终-CANopen-输出位"><a href="#5-3-第三层：用输入状态选择最终-CANopen-输出位" class="headerlink" title="5.3 第三层：用输入状态选择最终 CANopen 输出位"></a>5.3 第三层：用输入状态选择最终 CANopen 输出位</h3><p>当 <code>tick == true</code> 时，源码开始计算：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">uint8_t</span> rd_co, gr_co;</span><br></pre></td></tr></table></figure><p>其中：</p><ul><li><code>rd_co</code>：红色 ERROR LED 在当前时刻是否点亮；</li><li><code>gr_co</code>：绿色 RUN LED 在当前时刻是否点亮。</li></ul><p>最后再写回：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (rd_co != <span class="number">0U</span>) &#123;</span><br><span class="line">    rd |= CO_LED_CANopen;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">if</span> (gr_co != <span class="number">0U</span>) &#123;</span><br><span class="line">    gr |= CO_LED_CANopen;</span><br><span class="line">&#125;</span><br><span class="line">LEDs-&gt;LEDred = rd;</span><br><span class="line">LEDs-&gt;LEDgreen = gr;</span><br></pre></td></tr></table></figure><p>所以最终输出的关键不是 <code>rd_co</code> &#x2F; <code>gr_co</code> 变量本身，而是 <code>LEDred</code> &#x2F; <code>LEDgreen</code> 里的 <code>CO_LED_CANopen</code> bit。</p><h2 id="6-ERROR-红灯：协议错误语义在源码中的优先级"><a href="#6-ERROR-红灯：协议错误语义在源码中的优先级" class="headerlink" title="6. ERROR 红灯：协议错误语义在源码中的优先级"></a>6. ERROR 红灯：协议错误语义在源码中的优先级</h2><p><code>CO_LEDs.c</code> 对 ERROR LED 的判断顺序如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (ErrCANbusOff) &#123;</span><br><span class="line">    rd_co = <span class="number">1</span>;</span><br><span class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span> (NMTstate == CO_NMT_INITIALIZING) &#123;</span><br><span class="line">    rd_co = rd &amp; CO_LED_flicker;</span><br><span class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span> (ErrRpdo) &#123;</span><br><span class="line">    rd_co = rd &amp; CO_LED_flash_4;</span><br><span class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span> (ErrSync) &#123;</span><br><span class="line">    rd_co = rd &amp; CO_LED_flash_3;</span><br><span class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span> (ErrHbCons) &#123;</span><br><span class="line">    rd_co = rd &amp; CO_LED_flash_2;</span><br><span class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span> (ErrCANbusWarn) &#123;</span><br><span class="line">    rd_co = rd &amp; CO_LED_flash_1;</span><br><span class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span> (ErrOther) &#123;</span><br><span class="line">    rd_co = rd &amp; CO_LED_blink;</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    rd_co = <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>对应关系如下：</p><table><thead><tr><th align="right">优先级</th><th>输入条件</th><th>ERROR LED 模式</th><th>语义</th></tr></thead><tbody><tr><td align="right">1</td><td><code>ErrCANbusOff</code></td><td><code>on</code></td><td>CAN bus off</td></tr><tr><td align="right">2</td><td><code>NMTstate == CO_NMT_INITIALIZING</code></td><td><code>flickering</code></td><td>CANopen 未初始化 &#x2F; Node-ID 未配置语义</td></tr><tr><td align="right">3</td><td><code>ErrRpdo</code></td><td><code>quadruple flash</code></td><td>RPDO event timer timeout</td></tr><tr><td align="right">4</td><td><code>ErrSync</code></td><td><code>triple flash</code></td><td>SYNC receive timeout</td></tr><tr><td align="right">5</td><td><code>ErrHbCons</code></td><td><code>double flash</code></td><td>Heartbeat consumer error</td></tr><tr><td align="right">6</td><td><code>ErrCANbusWarn</code></td><td><code>single flash</code></td><td>CAN warning limit reached</td></tr><tr><td align="right">7</td><td><code>ErrOther</code></td><td><code>blinking</code></td><td>invalid configuration &#x2F; general error</td></tr><tr><td align="right">8</td><td>无以上条件</td><td><code>off</code></td><td>no error</td></tr></tbody></table><p><img src="https://i-blog.csdnimg.cn/direct/d049e430a6724213a4dd5d9e19abd7f6.png" alt="在这里插入图片描述"></p><p>这里要注意两点：</p><ol><li><strong>表格中的协议语义不是多个 LED 同时叠加显示。</strong> 由于源码使用 <code>if / else-if</code>，同一时刻只选择一个 ERROR 模式。</li><li><strong><code>ErrCANbusOff</code> 是最高优先级。</strong> 一旦 bus-off 为真，红灯直接常亮，不再显示其他错误模式。</li></ol><h2 id="7-RUN-绿灯：运行状态在源码中的优先级"><a href="#7-RUN-绿灯：运行状态在源码中的优先级" class="headerlink" title="7. RUN 绿灯：运行状态在源码中的优先级"></a>7. RUN 绿灯：运行状态在源码中的优先级</h2><p>RUN LED 的源码判断顺序如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (LSSconfig) &#123;</span><br><span class="line">    gr_co = gr &amp; CO_LED_flicker;</span><br><span class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span> (firmwareDownload) &#123;</span><br><span class="line">    gr_co = gr &amp; CO_LED_flash_3;</span><br><span class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span> (NMTstate == CO_NMT_STOPPED) &#123;</span><br><span class="line">    gr_co = gr &amp; CO_LED_flash_1;</span><br><span class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span> (NMTstate == CO_NMT_PRE_OPERATIONAL) &#123;</span><br><span class="line">    gr_co = gr &amp; CO_LED_blink;</span><br><span class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span> (NMTstate == CO_NMT_OPERATIONAL) &#123;</span><br><span class="line">    gr_co = <span class="number">1</span>;</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    gr_co = <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>对应关系如下：</p><table><thead><tr><th align="right">优先级</th><th>输入条件</th><th>RUN LED 模式</th><th>语义</th></tr></thead><tbody><tr><td align="right">1</td><td><code>LSSconfig</code></td><td><code>flickering</code></td><td>LSS configuration state is active</td></tr><tr><td align="right">2</td><td><code>firmwareDownload</code></td><td><code>triple flash</code></td><td>software download is running</td></tr><tr><td align="right">3</td><td><code>NMTstate == CO_NMT_STOPPED</code></td><td><code>single flash</code></td><td>NMT stopped</td></tr><tr><td align="right">4</td><td><code>NMTstate == CO_NMT_PRE_OPERATIONAL</code></td><td><code>blinking</code></td><td>NMT pre-operational</td></tr><tr><td align="right">5</td><td><code>NMTstate == CO_NMT_OPERATIONAL</code></td><td><code>on</code></td><td>NMT operational</td></tr><tr><td align="right">6</td><td>其他状态</td><td><code>off</code></td><td>不显示 RUN 状态</td></tr></tbody></table><p><img src="https://i-blog.csdnimg.cn/direct/81972a00856b4d11bbeae7075d8da108.png" alt="在这里插入图片描述"></p><p>从这个顺序可以看出，RUN LED 不是单纯显示 NMT 状态。<code>LSSconfig</code> 和 <code>firmwareDownload</code> 会优先覆盖 NMT 状态显示。</p><h2 id="8-把完整流程串起来"><a href="#8-把完整流程串起来" class="headerlink" title="8. 把完整流程串起来"></a>8. 把完整流程串起来</h2><p><code>CO_LEDs_process()</code> 每次被调用时，逻辑可以概括为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">输入：</span><br><span class="line">  timeDifference_us</span><br><span class="line">  NMTstate</span><br><span class="line">  LSSconfig</span><br><span class="line">  ErrCANbusOff / ErrCANbusWarn / ErrRpdo / ErrSync / ErrHbCons / ErrOther</span><br><span class="line">  firmwareDownload</span><br><span class="line"></span><br><span class="line">处理：</span><br><span class="line">  1. 累计到 50 ms tick</span><br><span class="line">  2. 生成 flicker / blink / flash_1..4 当前相位</span><br><span class="line">  3. 按 ERROR 优先级得到 rd_co</span><br><span class="line">  4. 按 RUN 优先级得到 gr_co</span><br><span class="line">  5. rd_co / gr_co 非 0 时，置位 CO_LED_CANopen</span><br><span class="line"></span><br><span class="line">输出：</span><br><span class="line">  LEDs-&gt;LEDred   bitfield</span><br><span class="line">  LEDs-&gt;LEDgreen bitfield</span><br></pre></td></tr></table></figure><p>最终，CANopen 指示灯的开关状态由以下两个表达式读取：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">CO_LED_RED(LEDs, CO_LED_CANopen)</span><br><span class="line">CO_LED_GREEN(LEDs, CO_LED_CANopen)</span><br></pre></td></tr></table></figure><p>这就是 <code>303/CO_LEDs.c/h</code> 的核心：</p><blockquote><p>先按 CiA 303-3 的模式定义生成标准闪烁相位，再按 CANopen 状态与错误输入选择当前应显示的 RUN &#x2F; ERROR 语义，最后写成红绿 LED 的 <code>CO_LED_CANopen</code> 输出位。</p></blockquote><h2 id="9-学习这份源码时的主线"><a href="#9-学习这份源码时的主线" class="headerlink" title="9. 学习这份源码时的主线"></a>9. 学习这份源码时的主线</h2><p>建议按下面顺序理解，而不是一开始就陷入定时器细节：</p><ol><li><strong>先看 CiA 303-3 要表达什么。</strong> 绿色 RUN 表示运行&#x2F;配置状态，红色 ERROR 表示错误&#x2F;异常状态。</li><li><strong>再看 <code>CO_LEDs.h</code>。</strong> 它把协议里的 flicker、blink、single flash、double flash、triple flash、quadruple flash、CANopen 输出位定义成 bitmask。</li><li><strong>再看 <code>CO_LEDs_t</code>。</strong> 它不是两个 bool，而是保存 50 ms&#x2F;200 ms 计时器、flash 状态机和红绿 bitfield。</li><li><strong>最后看 <code>CO_LEDs_process()</code>。</strong> 它先生成模式相位，再按 ERROR 与 RUN 两条优先级链选择最终输出。</li></ol><p>只要抓住这条主线，<code>CO_LEDs.c</code> 就不再是零散的闪灯代码，而是 CiA 303-3 指示语义在 CANopenNode 里的一个小型状态显示器。</p><h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><p>[^cia303]: CAN in Automation, “CiA® 303 series: CANopen-related documents”。该页面说明 CiA 303-3 是 CANopen indicators，并描述其用于通信相关指示器和 LED 闪烁模式。<a href="https://www.can-cia.org/can-knowledge/cia-303-series-canopen-related-documents">https://www.can-cia.org/can-knowledge/cia-303-series-canopen-related-documents</a></p><p>[^coleds]: CANopenNode Doxygen, “LED indicators” 与 “303&#x2F;CO_LEDs.h File Reference”。该文档说明 <code>CO_LEDs.h</code> 是 <code>CANopen Indicator specification (CiA 303-3 v1.4.0)</code>，列出 RUN&#x2F;ERROR LED 模式，并给出 <code>CO_LEDs_process()</code> 输入参数。<a href="https://canopennode.github.io/CANopenNode/group__CO__LEDs.html">https://canopennode.github.io/CANopenNode/group__CO__LEDs.html</a></p><p>[^repo]: CANopenNode GitHub repository README, “File structure”。该仓库结构说明 <code>303/ - CANopen Recommendation</code> 下包含 <code>CO_LEDs.h/.c - CANopen LED Indicators</code>。<a href="https://github.com/CANopenNode/CANopenNode">https://github.com/CANopenNode/CANopenNode</a></p>]]></content>
    
    
      
      
    <summary type="html">&lt;meta name=&quot;referrer&quot; content=&quot;no-referrer&quot; /&gt;

&lt;h1 id=&quot;按-LED-源码学习-CiA-303-3：CANopenNode-如何计算-RUN-ERROR-指示灯&quot;&gt;&lt;a href=&quot;#按-LED-源码学习-CiA-303-3：</summary>
      
    
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/categories/canopen/"/>
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/tags/canopen/"/>
    
  </entry>
  
  <entry>
    <title>CANopenNode LSS运行流程</title>
    <link href="https://wdfk-prog.space/posts/5e93a7d8/"/>
    <id>https://wdfk-prog.space/posts/5e93a7d8/</id>
    <published>2026-06-28T02:53:20.000Z</published>
    <updated>2026-06-28T02:53:59.883Z</updated>
    
    <content type="html"><![CDATA[<meta name="referrer" content="no-referrer" /><h1 id="从源码看-CANopenNode-LSS：搞懂主从机运行流程"><a href="#从源码看-CANopenNode-LSS：搞懂主从机运行流程" class="headerlink" title="从源码看 CANopenNode LSS：搞懂主从机运行流程"></a>从源码看 CANopenNode LSS：搞懂主从机运行流程</h1><p><img src="https://i-blog.csdnimg.cn/direct/8fab50c79f164b13a1fd8e5bedfdcd52.png" alt="在这里插入图片描述"></p><p>@[toc]<br><img src="https://i-blog.csdnimg.cn/direct/f48232d8d0c54beb85c6be082d4a826b.png" alt="在这里插入图片描述"></p><h2 id="先给结论"><a href="#先给结论" class="headerlink" title="先给结论"></a>先给结论</h2><p>LSS（Layer Setting Services）不是用来传过程数据的，也不是普通 SDO 配置。它解决的是<strong>设备还没有有效 Node-ID，或者需要统一切换 CAN 位率时，主站如何在 CAN 总线上找到并配置从站</strong>的问题。</p><p>在 CANopenNode 的 <code>305</code> 目录中，LSS 的运行核心可以压缩成三句话：</p><ol><li><strong>主站只通过 0x7E5 发命令，从站只通过 0x7E4 回响应。</strong> 这是 LSS 固定使用的两条 CAN 帧通道。</li><li><strong>从站通过 0x1018 身份对象形成 128 bit LSS Address。</strong> 主站可以已知地址精准选择，也可以对未配置节点做 Fastscan。</li><li><strong>Node-ID 和 bit rate 先进入 pending 值。</strong> Node-ID 通常要经过保存和通信复位后才真正成为 CANopen 栈运行用的 ID；bit rate 的激活更危险，必须保证全网节点同步切换。</li></ol><h2 id="1-LSS-解决的工程问题"><a href="#1-LSS-解决的工程问题" class="headerlink" title="1. LSS 解决的工程问题"></a>1. LSS 解决的工程问题</h2><p>在普通 CANopen 网络中，很多通信对象都依赖 Node-ID。例如心跳常见为 <code>0x700 + Node-ID</code>，SDO 服务器默认接收&#x2F;发送 COB-ID 也与 Node-ID 相关。问题是：</p><ul><li>新设备刚出厂可能没有 Node-ID。</li><li>多个同型号设备可能不能靠 Node-ID 区分。</li><li>有时需要在现场统一修改 bit rate。</li><li>如果 Node-ID 无效，就不能指望先用 SDO 写对象字典，因为 SDO 自身也需要 Node-ID。</li></ul><p>所以 LSS 被设计成一个<strong>低层配置入口</strong>：即使设备还不能作为完整 CANopen 节点运行，仍能通过 LSS 从站逻辑接收主站配置。</p><p>CiA 的 LSS 说明中明确区分 LSS manager 和 LSS server，并说明 LSS manager 可修改 server 的 CANopen Node-ID、切换整个网络的数据率；其固定使用 <code>0x7E5</code> 作为 manager 到 server 的命令帧、<code>0x7E4</code> 作为 server 到 manager 的响应帧。CANopenNode 官方 305 组文档也把 LSS 描述为用于查询或修改 Node-ID、物理层 bit timing，以及与 0x1018 身份对象一致的 LSS address。</p><h2 id="2-CANopen-设备模型中，LSS-站在哪里"><a href="#2-CANopen-设备模型中，LSS-站在哪里" class="headerlink" title="2. CANopen 设备模型中，LSS 站在哪里"></a>2. CANopen 设备模型中，LSS 站在哪里</h2><p>理解 LSS 之前，要先分清 CANopen 的三层对象关系：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">CANopen 设备</span><br><span class="line">├── 通信对象：NMT、Heartbeat、SDO、PDO、SYNC、TIME、EMCY、LSS ...</span><br><span class="line">├── 对象字典：通信参数、设备参数、应用参数的统一索引接口</span><br><span class="line">└── 应用程序：真实控制逻辑、传感器/执行器业务逻辑</span><br></pre></td></tr></table></figure><p>《CiA301 V4.2.0》把 CANopen 设备描述为由通信、对象字典和应用程序组成：通信单元提供通信对象和底层网络传输能力；对象字典集合影响应用程序对象、通信对象和状态机行为的数据项；对象字典是通信对象和应用程序对象之间的接口。</p><p>LSS 的特殊点是：它与普通对象字典访问不同。普通 SDO 读写对象字典依赖 Node-ID，而 LSS 要处理的正是“Node-ID 不存在或需要改变”的阶段。因此，CANopenNode 让 LSS slave 可以在完整 CANopen 初始化之前先工作。</p><h2 id="3-305-目录源码分工"><a href="#3-305-目录源码分工" class="headerlink" title="3. 305 目录源码分工"></a>3. <code>305</code> 目录源码分工</h2><p><img src="https://i-blog.csdnimg.cn/direct/8052f77cc438433f9f2a85e515fa804b.png" alt="在这里插入图片描述"></p><p>CANopenNode GitHub 仓库 <code>305/</code> 目录中，LSS 相关核心文件主要有 5 个：</p><table><thead><tr><th>文件</th><th>角色</th><th>阅读重点</th></tr></thead><tbody><tr><td><code>305/CO_LSS.h</code></td><td>公共协议定义</td><td>命令码、错误码、LSS address、LSS 状态、bit timing 表、Node-ID 合法性宏</td></tr><tr><td><code>305/CO_LSSmaster.h</code></td><td>主站对象和 API 声明</td><td>返回值、非阻塞调用模型、Fastscan 参数结构</td></tr><tr><td><code>305/CO_LSSmaster.c</code></td><td>主站状态机实现</td><td>发送 0x7E5、等待 0x7E4、超时、选择节点、配置 Node-ID、配置 bit rate、Fastscan</td></tr><tr><td><code>305/CO_LSSslave.h</code></td><td>从站对象和 API 声明</td><td><code>pendingBitRate</code>、<code>pendingNodeID</code>、回调接口、未配置节点处理</td></tr><tr><td><code>305/CO_LSSslave.c</code></td><td>从站接收和响应实现</td><td>CAN RX 回调预处理、主循环处理、配置响应、保存回调、触发通信复位</td></tr></tbody></table><p>源码阅读建议先看 <code>CO_LSS.h</code>，再看 <code>CO_LSSslave.c</code>，最后看 <code>CO_LSSmaster.c</code>。原因是从站更接近协议响应本质，主站源码里有更多异步状态机和 Fastscan 细节。</p><h2 id="4-CO-LSS-h：公共协议层是整套流程的字典"><a href="#4-CO-LSS-h：公共协议层是整套流程的字典" class="headerlink" title="4. CO_LSS.h：公共协议层是整套流程的字典"></a>4. <code>CO_LSS.h</code>：公共协议层是整套流程的字典</h2><p><code>CO_LSS.h</code> 里最重要的是五类定义。</p><h3 id="4-1-命令码决定-data-0-的含义"><a href="#4-1-命令码决定-data-0-的含义" class="headerlink" title="4.1 命令码决定 data[0] 的含义"></a>4.1 命令码决定 <code>data[0]</code> 的含义</h3><p>LSS 所有请求&#x2F;响应都是 8 字节 CAN 数据帧，<code>data[0]</code> 是 command specifier。源码中常见命令如下：</p><table><thead><tr><th>命令</th><th align="right">值</th><th>方向</th><th>用途</th></tr></thead><tbody><tr><td><code>CO_LSS_SWITCH_STATE_GLOBAL</code></td><td align="right"><code>0x04</code></td><td>Master -&gt; Slave(s)</td><td>全局切换 waiting&#x2F;configuration</td></tr><tr><td><code>CO_LSS_SWITCH_STATE_SEL_VENDOR</code></td><td align="right"><code>0x40</code></td><td>Master -&gt; Slave(s)</td><td>选择指定 LSS 地址：Vendor-ID</td></tr><tr><td><code>CO_LSS_SWITCH_STATE_SEL_PRODUCT</code></td><td align="right"><code>0x41</code></td><td>Master -&gt; Slave(s)</td><td>选择指定 LSS 地址：Product Code</td></tr><tr><td><code>CO_LSS_SWITCH_STATE_SEL_REV</code></td><td align="right"><code>0x42</code></td><td>Master -&gt; Slave(s)</td><td>选择指定 LSS 地址：Revision Number</td></tr><tr><td><code>CO_LSS_SWITCH_STATE_SEL_SERIAL</code></td><td align="right"><code>0x43</code></td><td>Master -&gt; Slave(s)</td><td>选择指定 LSS 地址：Serial Number</td></tr><tr><td><code>CO_LSS_SWITCH_STATE_SEL</code></td><td align="right"><code>0x44</code></td><td>Slave -&gt; Master</td><td>指定选择成功响应</td></tr><tr><td><code>CO_LSS_CFG_NODE_ID</code></td><td align="right"><code>0x11</code></td><td>双向</td><td>配置 Node-ID，请求和响应复用同一命令码</td></tr><tr><td><code>CO_LSS_CFG_BIT_TIMING</code></td><td align="right"><code>0x13</code></td><td>双向</td><td>配置 pending bit rate</td></tr><tr><td><code>CO_LSS_CFG_ACTIVATE_BIT_TIMING</code></td><td align="right"><code>0x15</code></td><td>Master -&gt; Slave(s)</td><td>激活 pending bit rate，无确认</td></tr><tr><td><code>CO_LSS_CFG_STORE</code></td><td align="right"><code>0x17</code></td><td>双向</td><td>保存 pending Node-ID&#x2F;bit rate</td></tr><tr><td><code>CO_LSS_IDENT_FASTSCAN</code></td><td align="right"><code>0x51</code></td><td>Master -&gt; Slave(s)</td><td>Fastscan 请求</td></tr><tr><td><code>CO_LSS_IDENT_SLAVE</code></td><td align="right"><code>0x4F</code></td><td>Slave -&gt; Master</td><td>Fastscan 应答</td></tr><tr><td><code>CO_LSS_INQUIRE_*</code></td><td align="right"><code>0x5A..0x5E</code></td><td>双向</td><td>查询 0x1018 各字段或 Node-ID</td></tr></tbody></table><h3 id="4-2-LSS-Address-直接对应对象-0x1018"><a href="#4-2-LSS-Address-直接对应对象-0x1018" class="headerlink" title="4.2 LSS Address 直接对应对象 0x1018"></a>4.2 LSS Address 直接对应对象 0x1018</h3><p>源码定义：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">union</span> &#123;</span></span><br><span class="line">    <span class="type">uint32_t</span> addr[<span class="number">4</span>];</span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> &#123;</span></span><br><span class="line">        <span class="type">uint32_t</span> vendorID;</span><br><span class="line">        <span class="type">uint32_t</span> productCode;</span><br><span class="line">        <span class="type">uint32_t</span> revisionNumber;</span><br><span class="line">        <span class="type">uint32_t</span> serialNumber;</span><br><span class="line">    &#125; identity;</span><br><span class="line">&#125; CO_LSS_address_t;</span><br></pre></td></tr></table></figure><p>这 4 个 <code>uint32_t</code> 就是 LSS 的 128 bit 地址。主站如果已知这四段值，可以直接发选择序列；如果不知道，就要靠 Fastscan 搜索。</p><p>这里有一个工程风险：<strong>0x1018 必须真正唯一</strong>。如果两个未配置设备的 Vendor-ID、Product Code、Revision Number、Serial Number 完全相同，LSS 主站无法可靠地区分它们。CANopenNode 的 LSS 教程也强调 LSS 地址依赖 0x1018 的唯一性。</p><h3 id="4-3-LSS-状态只有-waiting-和-configuration"><a href="#4-3-LSS-状态只有-waiting-和-configuration" class="headerlink" title="4.3 LSS 状态只有 waiting 和 configuration"></a>4.3 LSS 状态只有 waiting 和 configuration</h3><p>源码定义：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> CO_LSS_STATE_WAITING       0x00U</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> CO_LSS_STATE_CONFIGURATION 0x01U</span></span><br></pre></td></tr></table></figure><p>不要把 LSS 状态机和 NMT 状态机混在一起。LSS waiting&#x2F;configuration 只决定 LSS 从站是否接受配置命令；NMT pre-operational&#x2F;operational&#x2F;stopped 决定的是普通 CANopen 通信服务可用性。</p><h3 id="4-4-Node-ID-合法性宏体现了-LSS-的“未配置”语义"><a href="#4-4-Node-ID-合法性宏体现了-LSS-的“未配置”语义" class="headerlink" title="4.4 Node-ID 合法性宏体现了 LSS 的“未配置”语义"></a>4.4 Node-ID 合法性宏体现了 LSS 的“未配置”语义</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> CO_LSS_NODE_ID_ASSIGNMENT 0xFFU</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> CO_LSS_NODE_ID_VALID(nid) (((nid &gt;= 1U) &amp;&amp; (nid &lt;= 0x7FU)) || (nid == CO_LSS_NODE_ID_ASSIGNMENT))</span></span><br></pre></td></tr></table></figure><p>含义：</p><ul><li><code>1..0x7F</code> 是有效 CANopen Node-ID。</li><li><code>0xFF</code> 在 CANopenNode LSS 中表示“需要分配 Node-ID”。</li><li><code>0</code> 不是有效普通节点 ID；广播 Node-ID 0 属于 NMT 等协议语义，不能分配给一个普通从站。</li></ul><h3 id="4-5-Bit-timing-表把协议值映射成人能读的-kbit-s"><a href="#4-5-Bit-timing-表把协议值映射成人能读的-kbit-s" class="headerlink" title="4.5 Bit timing 表把协议值映射成人能读的 kbit&#x2F;s"></a>4.5 Bit timing 表把协议值映射成人能读的 kbit&#x2F;s</h3><p><code>CO_LSS_BIT_TIMING_*</code> 里的表索引对应常见 CANopen 位率：1000、800、500、250、125、50、20、10 kbit&#x2F;s，还有 <code>CO_LSS_BIT_TIMING_AUTO</code>。主站 API <code>CO_LSSmaster_configureBitTiming()</code> 接收的是数值 bit rate，再在函数里转换成 LSS 表索引。</p><h2 id="5-从站上电：为什么-LSS-要插在-CANopen-初始化中间"><a href="#5-从站上电：为什么-LSS-要插在-CANopen-初始化中间" class="headerlink" title="5. 从站上电：为什么 LSS 要插在 CANopen 初始化中间"></a>5. 从站上电：为什么 LSS 要插在 CANopen 初始化中间</h2><p><img src="https://i-blog.csdnimg.cn/direct/52189dfbc7f64de5b744eda4bae702d2.png" alt="在这里插入图片描述"></p><p><code>CO_LSSslave.h</code> 的注释说明了一个关键启动顺序：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">CO_CANinit()</span><br><span class="line">CO_LSSinit() / CO_LSSslave_init()</span><br><span class="line">CO_CANopenInit()</span><br></pre></td></tr></table></figure><p>原因是：LSS slave 和 NMT slave 需要共存，但如果节点没有有效 Node-ID，完整 CANopen 对象不能正常初始化。CANopenNode 的处理方式是：</p><ol><li>应用先从 NVM、拨码开关或默认配置读出 <code>pendingBitRate</code> 和 <code>pendingNodeID</code>。</li><li>调用 <code>CO_LSSslave_init()</code>，把这两个变量的指针交给 LSS 从站对象。</li><li>如果 <code>pendingNodeID</code> 是 <code>1..0x7F</code>，它立刻成为 <code>activeNodeID</code>，后续 NMT、SDO、PDO 等对象正常初始化。</li><li>如果 <code>pendingNodeID == 0xFF</code>，CANopenNode 只初始化并处理 LSS slave；其他 CANopen 模块会跳过或暂停。</li><li>当主站通过 LSS 配置出有效 Node-ID 后，从站在合适时机请求 <code>CO_NMT_RESET_COMMUNICATION</code>，重新走通信对象初始化流程。</li></ol><p>这就是 LSS 比普通 SDO 更底层的原因：<strong>没有 Node-ID 时，SDO 不能作为配置入口；LSS 可以。</strong></p><h2 id="6-从站对象-CO-LSSslave-t-的字段如何映射运行流程"><a href="#6-从站对象-CO-LSSslave-t-的字段如何映射运行流程" class="headerlink" title="6. 从站对象 CO_LSSslave_t 的字段如何映射运行流程"></a>6. 从站对象 <code>CO_LSSslave_t</code> 的字段如何映射运行流程</h2><p><code>CO_LSSslave_t</code> 可以按四组理解。</p><h3 id="6-1-身份和状态"><a href="#6-1-身份和状态" class="headerlink" title="6.1 身份和状态"></a>6.1 身份和状态</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">CO_LSS_address_t lssAddress;</span><br><span class="line"><span class="type">uint8_t</span> lssState;</span><br><span class="line">CO_LSS_address_t lssSelect;</span><br><span class="line">CO_LSS_address_t lssFastscan;</span><br><span class="line"><span class="type">uint8_t</span> fastscanPos;</span><br></pre></td></tr></table></figure><ul><li><code>lssAddress</code> 是本设备真实 128 bit LSS 地址。</li><li><code>lssState</code> 是 waiting&#x2F;configuration。</li><li><code>lssSelect</code> 暂存主站通过 0x40&#x2F;0x41&#x2F;0x42&#x2F;0x43 发来的四段地址。</li><li><code>lssFastscan</code> 和 <code>fastscanPos</code> 服务 Fastscan 搜索过程。</li></ul><h3 id="6-2-pending-值和-active-值"><a href="#6-2-pending-值和-active-值" class="headerlink" title="6.2 pending 值和 active 值"></a>6.2 pending 值和 active 值</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">uint16_t</span>* pendingBitRate;</span><br><span class="line"><span class="type">uint8_t</span>* pendingNodeID;</span><br><span class="line"><span class="type">uint8_t</span> activeNodeID;</span><br></pre></td></tr></table></figure><p>这里是理解 LSS 的重点：</p><ul><li><code>pendingNodeID</code> 是主站配置的新值，可能还没真正成为栈使用的 Node-ID。</li><li><code>activeNodeID</code> 是当前 CAN 接口&#x2F;通信对象正在使用的 Node-ID。</li><li>对未配置节点，<code>activeNodeID == 0xFF</code>，配置成功后通过 reset communication 让 pending 值生效。</li></ul><h3 id="6-3-接收缓存和发送标志"><a href="#6-3-接收缓存和发送标志" class="headerlink" title="6.3 接收缓存和发送标志"></a>6.3 接收缓存和发送标志</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">volatile</span> <span class="type">void</span>* sendResponse;</span><br><span class="line"><span class="type">uint8_t</span> service;</span><br><span class="line"><span class="type">uint8_t</span> CANdata[<span class="number">8</span>];</span><br></pre></td></tr></table></figure><p>CAN RX 回调中不做复杂处理，只保存服务类型和数据，并设置 <code>sendResponse</code>。主循环或协议处理任务随后调用 <code>CO_LSSslave_process()</code> 处理。</p><h3 id="6-4-应用回调"><a href="#6-4-应用回调" class="headerlink" title="6.4 应用回调"></a>6.4 应用回调</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">bool_t</span> (*pFunctLSScheckBitRate)(<span class="type">void</span>* object, <span class="type">uint16_t</span> bitRate);</span><br><span class="line"><span class="type">void</span> (*pFunctLSSactivateBitRate)(<span class="type">void</span>* object, <span class="type">uint16_t</span> delay);</span><br><span class="line"><span class="type">bool_t</span> (*pFunctLSScfgStore)(<span class="type">void</span>* object, <span class="type">uint8_t</span> id, <span class="type">uint16_t</span> bitRate);</span><br></pre></td></tr></table></figure><p>这三类回调是 LSS 从站把硬件能力检查、位率激活和参数持久化交给应用层处理的接口：</p><table><thead><tr><th>回调</th><th>触发命令</th><th>应用要做什么</th></tr></thead><tbody><tr><td><code>pFunctLSScheckBitRate</code></td><td><code>0x13 Configure Bit Timing</code></td><td>判断目标 bit rate 是否被当前 CAN 外设时钟和位时序支持</td></tr><tr><td><code>pFunctLSSactivateBitRate</code></td><td><code>0x15 Activate Bit Timing</code></td><td>按延时要求停止发送、重配 CAN bit timing、恢复总线</td></tr><tr><td><code>pFunctLSScfgStore</code></td><td><code>0x17 Store Configuration</code></td><td>把 Node-ID 和 bit rate 写入 Flash&#x2F;EEPROM&#x2F;外部 NVM</td></tr></tbody></table><p>如果没有注册相关回调，CANopenNode 会用“不响应”或错误响应表达不支持。这一点对调试很重要：<strong>主站超时不一定是总线坏了，也可能是从站故意 no-ack 表示不支持该服务。</strong></p><h2 id="7-从站接收路径：ISR-做短处理，主循环做协议动作"><a href="#7-从站接收路径：ISR-做短处理，主循环做协议动作" class="headerlink" title="7. 从站接收路径：ISR 做短处理，主循环做协议动作"></a>7. 从站接收路径：ISR 做短处理，主循环做协议动作</h2><p><img src="https://i-blog.csdnimg.cn/direct/8933451f95774be1b8eef7c2f17c2e5a.png" alt="在这里插入图片描述"></p><p><code>CO_LSSslave_receive()</code> 是 CAN RX 回调，通常在中断或驱动接收上下文中调用。它的策略是：</p><ol><li>只接受 DLC 为 8 的 LSS 帧。</li><li>如果已有待响应帧还没处理，就不覆盖当前请求。</li><li>对简单状态切换、选择地址、Fastscan 匹配做必要判断。</li><li>需要主循环进一步处理时，设置 <code>service</code>、复制 <code>CANdata</code>、置位 <code>sendResponse</code>。</li><li>如果启用了 <code>CO_CONFIG_FLAG_CALLBACK_PRE</code>，调用 <code>pFunctSignalPre()</code> 唤醒处理任务。</li></ol><p>这种分工适合周期主循环或事件驱动任务。不要在 CAN RX 回调里写 Flash、重配 CAN、等待定时器，这些都应放到 <code>CO_LSSslave_process()</code> 或应用回调中。</p><h2 id="8-已知-LSS-Address-时：选择指定从站"><a href="#8-已知-LSS-Address-时：选择指定从站" class="headerlink" title="8. 已知 LSS Address 时：选择指定从站"></a>8. 已知 LSS Address 时：选择指定从站</h2><p><img src="https://i-blog.csdnimg.cn/direct/e444b6be0fd24c45b0f7af13a91a94aa.png" alt="在这里插入图片描述"></p><p>主站已知从站的 128 bit LSS Address 时，调用：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">CO_LSSmaster_swStateSelect(LSSmaster, timeDifference_us, &amp;lssAddress);</span><br></pre></td></tr></table></figure><p>主站源码里的动作是连续发送四帧：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">0x40 + Vendor-ID</span><br><span class="line">0x41 + Product Code</span><br><span class="line">0x42 + Revision Number</span><br><span class="line">0x43 + Serial Number</span><br></pre></td></tr></table></figure><p>从站在 waiting 状态下逐段收集到 <code>lssSelect</code>。当收到最后一段 Serial Number 后，执行：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">CO_LSS_ADDRESS_EQUAL(LSSslave-&gt;lssAddress, LSSslave-&gt;lssSelect)</span><br></pre></td></tr></table></figure><p>如果完全匹配：</p><ul><li>从站进入 <code>CO_LSS_STATE_CONFIGURATION</code>。</li><li>从站设置 <code>service = CO_LSS_SWITCH_STATE_SEL_SERIAL</code>。</li><li>后续 <code>CO_LSSslave_process()</code> 发送 <code>0x44</code> 响应。</li><li>主站收到 <code>0x44</code> 后认为选择成功。</li></ul><p>如果不匹配，从站继续保持 waiting，不响应。对主站来说，这会表现为超时。</p><h2 id="9-配置-Node-ID：为什么配置后还要-reset-communication"><a href="#9-配置-Node-ID：为什么配置后还要-reset-communication" class="headerlink" title="9. 配置 Node-ID：为什么配置后还要 reset communication"></a>9. 配置 Node-ID：为什么配置后还要 reset communication</h2><p><img src="https://i-blog.csdnimg.cn/direct/09958b1c25914537920829c911673886.png" alt="在这里插入图片描述"></p><p>前置条件：从站已经处于 LSS configuration。通常是通过指定地址选择，或者 Fastscan 选中了一个未配置节点。</p><p>主站调用：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">CO_LSSmaster_configureNodeId(LSSmaster, timeDifference_us, nodeId);</span><br></pre></td></tr></table></figure><p>主站发送：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">data[0] = 0x11  // CO_LSS_CFG_NODE_ID</span><br><span class="line">data[1] = nodeId</span><br></pre></td></tr></table></figure><p>从站处理逻辑：</p><ol><li>检查 <code>nodeId</code> 是否满足 <code>CO_LSS_NODE_ID_VALID()</code>。</li><li>若有效，则写入 <code>*pendingNodeID</code>。</li><li>响应 <code>0x11, errorCode=0</code>。</li><li>若无效，则响应 <code>0x11, errorCode=1</code> 或厂商错误。</li></ol><p>关键点是：配置命令改变的是 <code>pendingNodeID</code>，不一定立刻改变 <code>activeNodeID</code>。如果设备之前是未配置状态，后续从 configuration 切回 waiting 时，<code>CO_LSSslave_receive()</code> 会检测到：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">activeNodeID == 0xFF</span><br><span class="line">pendingNodeID != 0xFF</span><br></pre></td></tr></table></figure><p>于是让 <code>CO_LSSslave_process()</code> 返回 true，表示请求 <code>CO_NMT_RESET_COMMUNICATION</code>。这次通信复位后，新的 pending Node-ID 才会被完整 CANopen 栈采用。</p><p>CANopenNode 的 LSS 示例也提醒：修改 Node-ID 后，Node-ID 变化不是立即完成，通常要 reset communication&#x2F;node 才会生效。</p><h2 id="10-保存配置：0x17-不是普通变量赋值，而是-NVM-边界"><a href="#10-保存配置：0x17-不是普通变量赋值，而是-NVM-边界" class="headerlink" title="10. 保存配置：0x17 不是普通变量赋值，而是 NVM 边界"></a>10. 保存配置：<code>0x17</code> 不是普通变量赋值，而是 NVM 边界</h2><p>主站调用：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">CO_LSSmaster_configureStore(LSSmaster, timeDifference_us);</span><br></pre></td></tr></table></figure><p>从站收到 <code>CO_LSS_CFG_STORE</code> 后，如果注册了 <code>pFunctLSScfgStore()</code>，会调用应用层保存：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">pFunctLSScfgStore(object, pendingNodeID, pendingBitRate)</span><br></pre></td></tr></table></figure><p>这一步不是协议栈自己能替你完成的。不同工程的 NVM 后端可能是：</p><ul><li>MCU 内部 Flash 某个参数页。</li><li>外部 EEPROM。</li><li>外部 SPI NOR Flash。</li><li>上层配置文件或 Bootloader 参数区。</li></ul><p>保存成功才应返回 OK。保存失败要返回错误，否则下次上电后主站以为配置已持久化，实际设备又回到旧 Node-ID 或旧 bit rate，排查会很困难。</p><h2 id="11-配置-bit-rate：比配置-Node-ID-更危险"><a href="#11-配置-bit-rate：比配置-Node-ID-更危险" class="headerlink" title="11. 配置 bit rate：比配置 Node-ID 更危险"></a>11. 配置 bit rate：比配置 Node-ID 更危险</h2><p><img src="https://i-blog.csdnimg.cn/direct/f16d58621a034b068bf1ced1c659acce.png" alt="在这里插入图片描述"></p><p>bit rate 的 LSS 流程分两步：</p><ol><li><code>0x13 Configure Bit Timing</code>：把新 bit rate 设置成 pending 值。</li><li><code>0x15 Activate Bit Timing</code>：让节点在指定延时后切换到 pending bit rate。</li></ol><p>主站 API：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">CO_LSSmaster_configureBitTiming(LSSmaster, timeDifference_us, bit);</span><br><span class="line">CO_LSSmaster_ActivateBit(LSSmaster, switchDelay_ms);</span><br></pre></td></tr></table></figure><p>源码中 <code>configureBitTiming()</code> 接收的 <code>bit</code> 是 <code>1000/800/500/250/125/50/20/10/0</code> 这类值，然后转换为 LSS 表索引。<code>0</code> 对应自动 bit rate 检测。</p><p>从站收到 <code>0x13</code> 后：</p><ul><li>检查 table index 是否有效。</li><li>查表得到实际 kbit&#x2F;s。</li><li>调 <code>pFunctLSScheckBitRate()</code> 让应用确认当前硬件是否支持。</li><li>支持则写入 <code>*pendingBitRate</code> 并响应 OK。</li></ul><p>真正危险的是 <code>0x15 Activate Bit Timing</code>：CANopenNode 主站源码要求它只能在 <code>CFG_GLOBAL</code> 状态调用，因为切 bit rate 应该全网同时进行。官方文档也明确提示 bit rate 切换是关键步骤，失败会让网络不可用。</p><p>实现 <code>pFunctLSSactivateBitRate()</code> 时，关键约束是主站与所有目标从站必须按同一延时策略切换到同一 bit rate；否则后续 CAN 帧将无法互通。</p><h2 id="12-Fastscan：不知道地址时如何找出一个未配置节点"><a href="#12-Fastscan：不知道地址时如何找出一个未配置节点" class="headerlink" title="12. Fastscan：不知道地址时如何找出一个未配置节点"></a>12. Fastscan：不知道地址时如何找出一个未配置节点</h2><p><img src="https://i-blog.csdnimg.cn/direct/64dd7a441a9746c293c13e278b812210.png" alt="在这里插入图片描述"></p><p>Fastscan 的目标不是“一次列出所有设备”，而是：<strong>在一组未配置 LSS 从站中，识别出一个唯一的 LSS Address，并让这个从站进入 configuration 状态。</strong> 之后主站给它分配 Node-ID，再扫描下一个。</p><p>CANopenNode 主站入口：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">CO_LSSmaster_IdentifyFastscan(LSSmaster, timeDifference_us, &amp;fastscan);</span><br></pre></td></tr></table></figure><p><code>fastscan</code> 参数里每一段地址有三种策略：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">enum</span> &#123;</span></span><br><span class="line">    CO_LSSmaster_FS_SCAN = <span class="number">0</span>,</span><br><span class="line">    CO_LSSmaster_FS_SKIP = <span class="number">1</span>,</span><br><span class="line">    CO_LSSmaster_FS_MATCH = <span class="number">2</span>,</span><br><span class="line">&#125; CO_LSSmaster_scantype_t;</span><br></pre></td></tr></table></figure><p>含义：</p><table><thead><tr><th>策略</th><th>含义</th><th>适用场景</th></tr></thead><tbody><tr><td><code>FS_SCAN</code></td><td>这 32 bit 完全扫描</td><td>完全未知</td></tr><tr><td><code>FS_MATCH</code></td><td>已知该 32 bit，只验证</td><td>已知 Vendor-ID 或 Product Code</td></tr><tr><td><code>FS_SKIP</code></td><td>跳过该段</td><td>只允许有限跳过，不能随意跳过 Vendor-ID</td></tr></tbody></table><p>源码状态机注释很清楚：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">check for non-configured nodes</span><br><span class="line">scan vendor ID</span><br><span class="line">verify vendor ID, switch node state</span><br><span class="line">scan product code</span><br><span class="line">verify product code, switch node state</span><br><span class="line">scan revision number</span><br><span class="line">verify revision number, switch node state</span><br><span class="line">scan serial number</span><br><span class="line">verify serial number, switch node to LSS configuration mode</span><br></pre></td></tr></table></figure><h3 id="12-1-“有响应”和“没响应”都携带信息"><a href="#12-1-“有响应”和“没响应”都携带信息" class="headerlink" title="12.1 “有响应”和“没响应”都携带信息"></a>12.1 “有响应”和“没响应”都携带信息</h3><p>Fastscan 的一个不直观之处是：主站不是等从站返回具体位值，而是用“是否有人应答”来推断当前假设是否成立。</p><p>主站从 bit31 扫到 bit0。源码里当超时后如果没有收到 <code>CO_LSS_IDENT_SLAVE</code>，会把当前 bit 置 1：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/* no response received, assumption is wrong */</span></span><br><span class="line">LSSmaster-&gt;fsIdNumber |= <span class="number">1UL</span> &lt;&lt; LSSmaster-&gt;fsBitChecked;</span><br></pre></td></tr></table></figure><p>简化理解：</p><ul><li>主站先假设某些高位为 0，发请求。</li><li>如果仍有从站匹配，就会应答，说明假设可以保留。</li><li>如果无人应答，说明假设错了，把当前位改成 1 再继续。</li></ul><p>这样逐位收敛出 32 bit，再换下一段地址。</p><h3 id="12-2-从站只在未配置状态参与-Fastscan"><a href="#12-2-从站只在未配置状态参与-Fastscan" class="headerlink" title="12.2 从站只在未配置状态参与 Fastscan"></a>12.2 从站只在未配置状态参与 Fastscan</h3><p><code>CO_LSSslave_receive()</code> 中对 Fastscan 有明确限制：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">pendingNodeID == 0xFF</span><br><span class="line">activeNodeID == 0xFF</span><br></pre></td></tr></table></figure><p>也就是说，CANopenNode 的 Fastscan 只用于未配置节点。已经有有效 Node-ID 的设备，通常可通过 SDO 读取 0x1018，或通过指定地址选择进入 LSS configuration。</p><h2 id="13-主站-API-为什么都要“循环调用”"><a href="#13-主站-API-为什么都要“循环调用”" class="headerlink" title="13. 主站 API 为什么都要“循环调用”"></a>13. 主站 API 为什么都要“循环调用”</h2><p><img src="https://i-blog.csdnimg.cn/direct/91e42660808a4da0ac7b4ae7e95582fa.png" alt="在这里插入图片描述"></p><p>CANopenNode 的 LSS master API 大多是非阻塞函数。典型模式是：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">CO_LSSmaster_return_t ret;</span><br><span class="line"><span class="type">uint32_t</span> dt_us;</span><br><span class="line"></span><br><span class="line">ret = CO_LSSmaster_configureNodeId(&amp;lssMaster, <span class="number">0</span>, newNodeId);</span><br><span class="line"><span class="keyword">while</span> (ret == CO_LSSmaster_WAIT_SLAVE) &#123;</span><br><span class="line">    dt_us = get_elapsed_us();</span><br><span class="line">    ret = CO_LSSmaster_configureNodeId(&amp;lssMaster, dt_us, newNodeId);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (ret == CO_LSSmaster_OK) &#123;</span><br><span class="line">    <span class="comment">/* request completed */</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>第一次调用时：</p><ul><li>检查状态机是否空闲。</li><li>填充 <code>TXbuff-&gt;data[]</code>。</li><li>调 <code>CO_CANsend()</code> 发 0x7E5。</li><li>返回 <code>CO_LSSmaster_WAIT_SLAVE</code>。</li></ul><p>后续调用时：</p><ul><li>检查 <code>CANrxNew</code> 是否收到响应。</li><li>若收到，解析 <code>CANrxData[]</code>。</li><li>若未收到，累计 <code>timeoutTimer</code>。</li><li>超时则返回 <code>CO_LSSmaster_TIMEOUT</code> 或 <code>CO_LSSmaster_SCAN_NOACK</code>。</li></ul><p>这种设计适合周期主循环或事件驱动任务，不会在协议栈里阻塞等待 CAN 响应。</p><h2 id="14-主站返回值怎么读"><a href="#14-主站返回值怎么读" class="headerlink" title="14. 主站返回值怎么读"></a>14. 主站返回值怎么读</h2><p><code>CO_LSSmaster_return_t</code> 里有三类返回值。</p><h3 id="14-1-正常过程值"><a href="#14-1-正常过程值" class="headerlink" title="14.1 正常过程值"></a>14.1 正常过程值</h3><table><thead><tr><th>返回值</th><th>含义</th></tr></thead><tbody><tr><td><code>CO_LSSmaster_WAIT_SLAVE</code></td><td>请求已发出，还在等从站响应，下一周期继续调用同一个 API</td></tr><tr><td><code>CO_LSSmaster_OK</code></td><td>请求完成，且从站确认成功</td></tr><tr><td><code>CO_LSSmaster_SCAN_FINISHED</code></td><td>Fastscan 某个扫描阶段完成或整个扫描成功</td></tr></tbody></table><h3 id="14-2-本地主站错误"><a href="#14-2-本地主站错误" class="headerlink" title="14.2 本地主站错误"></a>14.2 本地主站错误</h3><table><thead><tr><th>返回值</th><th>常见原因</th></tr></thead><tbody><tr><td><code>CO_LSSmaster_ILLEGAL_ARGUMENT</code></td><td>传入 NULL、非法 Node-ID、非法 bit rate、Fastscan 参数不满足限制</td></tr><tr><td><code>CO_LSSmaster_INVALID_STATE</code></td><td>上一个 LSS 命令还没完成，或当前主站状态不允许发该命令</td></tr><tr><td><code>CO_LSSmaster_TIMEOUT</code></td><td>普通请求超时，无从站响应</td></tr><tr><td><code>CO_LSSmaster_SCAN_NOACK</code></td><td>Fastscan 阶段没有符合条件的从站应答</td></tr><tr><td><code>CO_LSSmaster_SCAN_FAILED</code></td><td>Fastscan 过程中收到错误响应或状态机无法继续</td></tr></tbody></table><h3 id="14-3-从站拒绝，但协议完成"><a href="#14-3-从站拒绝，但协议完成" class="headerlink" title="14.3 从站拒绝，但协议完成"></a>14.3 从站拒绝，但协议完成</h3><table><thead><tr><th>返回值</th><th>含义</th></tr></thead><tbody><tr><td><code>CO_LSSmaster_OK_ILLEGAL_ARGUMENT</code></td><td>从站收到了请求，但拒绝该参数，比如不支持的 bit timing</td></tr><tr><td><code>CO_LSSmaster_OK_MANUFACTURER</code></td><td>从站返回厂商特定错误</td></tr></tbody></table><p>这类返回值容易误判。它不是 CAN 总线失败，而是 LSS 从站有明确反馈：请求到了，但参数不被接受。</p><h2 id="15-主从机典型运行流程一：已知地址，修改-Node-ID"><a href="#15-主从机典型运行流程一：已知地址，修改-Node-ID" class="headerlink" title="15. 主从机典型运行流程一：已知地址，修改 Node-ID"></a>15. 主从机典型运行流程一：已知地址，修改 Node-ID</h2><p>适用场景：设备有唯一 0x1018 身份对象，主站知道这四个字段。目标是把 Node-ID 改成 10 并保存。</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">1. Master: swStateSelect(vendor, product, revision, serial)</span><br><span class="line">2. Slave : 地址完全匹配，进入 LSS configuration，响应 0x44</span><br><span class="line">3. Master: inquire node-id，可选</span><br><span class="line">4. Master: configureNodeId(10)</span><br><span class="line">5. Slave : pendingNodeID = 10，响应 OK</span><br><span class="line">6. Master: configureStore()</span><br><span class="line">7. Slave : 调应用 NVM 保存回调，响应 OK</span><br><span class="line">8. Master: swStateDeselect() 或 global waiting</span><br><span class="line">9. Slave : 若原来未配置，则请求 reset communication</span><br><span class="line">10. CANopen 栈使用 Node-ID 10 重新初始化，开始正常 NMT/Heartbeat/SDO/PDO</span><br></pre></td></tr></table></figure><p>对应 CANopenNode LSS demo 的典型命令形态是：先 <code>lss_switch_sel</code>，再 <code>lss_get_node</code>、<code>lss_set_node</code>、<code>lss_store</code>，最后切回 waiting 并 reset communication。</p><h2 id="16-主从机典型运行流程二：未知地址，Fastscan-后分配-Node-ID"><a href="#16-主从机典型运行流程二：未知地址，Fastscan-后分配-Node-ID" class="headerlink" title="16. 主从机典型运行流程二：未知地址，Fastscan 后分配 Node-ID"></a>16. 主从机典型运行流程二：未知地址，Fastscan 后分配 Node-ID</h2><p>适用场景：现场接入了一个或多个未配置节点，主站不知道它们的 0x1018 完整地址。</p><p>推荐流程：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">1. Master: IdentifyFastscan()</span><br><span class="line">2. Slave : 未配置节点参与 Fastscan，应答匹配请求</span><br><span class="line">3. Master: 得到 fastscan.found 里的 128 bit LSS Address</span><br><span class="line">4. Slave : 被 Fastscan 选中的一个节点进入 LSS configuration</span><br><span class="line">5. Master: configureNodeId(newId)</span><br><span class="line">6. Master: configureStore()</span><br><span class="line">7. Master: swStateDeselect() 或 global waiting</span><br><span class="line">8. Slave : reset communication，使新 Node-ID 生效</span><br><span class="line">9. Master: 如还有未配置节点，重复 Fastscan</span><br></pre></td></tr></table></figure><p>注意：Fastscan 的目标通常是一次选出一个节点。多节点批量配置时，不要尝试给多个未配置节点同时写同一个 Node-ID。</p><h2 id="17-主从机典型运行流程三：切换全网-bit-rate"><a href="#17-主从机典型运行流程三：切换全网-bit-rate" class="headerlink" title="17. 主从机典型运行流程三：切换全网 bit rate"></a>17. 主从机典型运行流程三：切换全网 bit rate</h2><p>适用场景：所有节点当前能以同一 bit rate 通信，且都支持目标 bit rate。</p><p>最低风险流程：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">1. 确认所有节点在线，并已知各自 LSS address 或能进入 LSS configuration</span><br><span class="line">2. Master: 全局切换到 LSS configuration</span><br><span class="line">3. Master: 对每个节点 configureBitTiming(targetBitRate)</span><br><span class="line">4. Slave : 各自通过 checkBitRate 回调确认支持</span><br><span class="line">5. Master: 可选 configureStore()</span><br><span class="line">6. Master: ActivateBit(switchDelay_ms)</span><br><span class="line">7. 所有从站和主站按同一延时策略切换 CAN bit timing</span><br><span class="line">8. Master: 在新 bit rate 下重新建立 CANopen 通信</span><br></pre></td></tr></table></figure><p>不建议在以下情况下做全网 bit rate 激活：</p><ul><li>有节点不确定是否支持目标位率。</li><li>主站自身切换 bit rate 的时序不可控。</li><li>总线上还有非 CANopen 或不可控设备。</li><li>没有现场恢复手段，一旦切错只能拆机单独重配。</li></ul><h2 id="18-与-NMT-状态机的关系"><a href="#18-与-NMT-状态机的关系" class="headerlink" title="18. 与 NMT 状态机的关系"></a>18. 与 NMT 状态机的关系</h2><p>LSS 和 NMT 是两套状态机，但会在启动和 reset communication 处交汇。</p><p>《CiA301 V4.2.0》中，NMT 初始化态包含初始化、复位应用、复位通信三个子状态；复位通信完成后执行 boot-up 写服务并进入配置态。配置态允许 SDO 通信但不允许 PDO 通信；运行态允许所有通信服务；停止态会终止大部分通信服务，只保留节点保护&#x2F;心跳等错误控制服务。</p><p>对 LSS 学习来说，重点不是背 NMT 图，而是理解：</p><ul><li>LSS 可以先于完整 CANopen 通信对象工作。</li><li>LSS 配置有效 Node-ID 后，通过 reset communication 让普通 CANopen 通信对象按新 Node-ID 初始化。</li><li>进入 NMT operational 后才是 PDO 等过程数据通信的正常阶段。</li></ul><h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ul><li>CANopenNode 官方 Doxygen：CANopen 305 &#x2F; LSS &#x2F; LSS Master &#x2F; LSS Slave。</li><li>CANopenNode GitHub：<a href="https://github.com/CANopenNode/CANopenNode%EF%BC%8C%E9%87%8D%E7%82%B9%E6%96%87%E4%BB%B6%E4%B8%BA">https://github.com/CANopenNode/CANopenNode，重点文件为</a> <code>305/CO_LSS.h</code>、<code>305/CO_LSSmaster.c/h</code>、<code>305/CO_LSSslave.c/h</code>。</li><li>CiA：CiA 305 Layer Setting Services 公开介绍。</li><li>《CiA301 V4.2.0（中文注释版）》：用于 CANopen 设备模型、对象字典、NMT 状态机和通信对象上下文。</li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;meta name=&quot;referrer&quot; content=&quot;no-referrer&quot; /&gt;

&lt;h1 id=&quot;从源码看-CANopenNode-LSS：搞懂主从机运行流程&quot;&gt;&lt;a href=&quot;#从源码看-CANopenNode-LSS：搞懂主从机运行流程&quot; class=&quot;hea</summary>
      
    
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/categories/canopen/"/>
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/tags/canopen/"/>
    
  </entry>
  
  <entry>
    <title>CANopenNode `CO_new()` 与 `CO_config</title>
    <link href="https://wdfk-prog.space/posts/cdeebf0b/"/>
    <id>https://wdfk-prog.space/posts/cdeebf0b/</id>
    <published>2026-06-28T02:35:07.000Z</published>
    <updated>2026-06-28T02:53:33.907Z</updated>
    
    <content type="html"><![CDATA[<meta name="referrer" content="no-referrer" /><h1 id="CANopenNode-CO-new-与-CO-config-h-宏机制详解"><a href="#CANopenNode-CO-new-与-CO-config-h-宏机制详解" class="headerlink" title="CANopenNode CO_new() 与 CO_config.h 宏机制详解"></a>CANopenNode <code>CO_new()</code> 与 <code>CO_config.h</code> 宏机制详解</h1><p>@[toc]<br><img src="https://i-blog.csdnimg.cn/direct/4a9041b78b5845c1ad907b5709415e23.png" alt="在这里插入图片描述"></p><p><code>CO_new()</code> 不是一个单纯的 <code>malloc()</code> 包装函数。它是 CANopenNode 把“协议栈功能选择”落到“对象实例、内存占用、CAN 接收过滤槽位、CAN 发送缓冲槽位”的集中入口。</p><p>换句话说，<code>CO_config.h</code> 里的宏回答的是：这个协议能力是否参与编译、是否保留对应代码路径、是否启用某些回调或动态配置特性。<code>CO_new()</code> 回答的是：在当前对象字典或 <code>CO_config_t</code> 配置下，实际要创建几个对象、每个对象占多少内存、每类 CAN 报文在 <code>CANrx[]</code> 和 <code>CANtx[]</code> 中从哪个下标开始。</p><p><img src="https://i-blog.csdnimg.cn/direct/1ef8fbb5fc4140c1927c817649a53114.png" alt="在这里插入图片描述"></p><h2 id="CO-new-的核心任务不是协议通信，而是对象装配"><a href="#CO-new-的核心任务不是协议通信，而是对象装配" class="headerlink" title="CO_new() 的核心任务不是协议通信，而是对象装配"></a><code>CO_new()</code> 的核心任务不是协议通信，而是对象装配</h2><p>从代码结构看，<code>CO_new()</code> 可以分为四层：参数检查、对象分配、CAN 报文槽位统计、最终对象返回。</p><p>第一层是配置合法性检查。只有在 <code>CO_MULTIPLE_OD</code> 被定义时，函数才会把 <code>config</code> 视为有效配置来源，并检查 <code>CNT_NMT</code>、<code>CNT_HB_CONS</code>、<code>CNT_EM</code>、<code>CNT_SDO_SRV</code>、<code>CNT_SDO_CLI</code>、<code>CNT_SYNC</code>、<code>CNT_RPDO</code>、<code>CNT_TPDO</code>、<code>CNT_TIME</code>、<code>CNT_LEDS</code>、<code>CNT_GFC</code>、<code>CNT_SRDO</code>、<code>CNT_LSS_SLV</code>、<code>CNT_LSS_MST</code>、<code>CNT_GTWA</code> 等数量上限。这里的上限不是协议任意值，而是 CANopenNode 对通用对象工厂的实现约束，例如 NMT&#x2F;SYNC&#x2F;TIME&#x2F;GFC&#x2F;LSS&#x2F;Gateway 这类对象通常只能有 0 或 1 个，而 RPDO&#x2F;TPDO 可扩展到更大数量。</p><p>第二层是按模块分配对象。宏判断出某模块被编译启用后，<code>CO_new()</code> 再用 <code>CO_GET_CNT(xxx)</code> 判断当前实例数量是否大于 0。只有两个条件同时满足时，对象才会真正分配。例如 <code>CO_CONFIG_SYNC_ENABLE</code> 打开但 <code>CO_GET_CNT(SYNC) == 0</code>，<code>co-&gt;SYNC</code> 不会被创建。反过来，如果对象字典里有 SYNC 条目，但 <code>CO_CONFIG_SYNC_ENABLE</code> 被关闭，相关代码块也不会进入编译。</p><p>第三层是计算 <code>CANrx[]</code> 和 <code>CANtx[]</code>。每个协议对象需要若干接收槽位和发送槽位。NMT slave 需要接收 NMT 命令，Heartbeat producer 需要发送 heartbeat，SDO server 每个通道需要一个 RX 和一个 TX，SRDO 每个对象需要两路 RX 和两路 TX。<code>CO_new()</code> 使用 <code>RX_CNT_xxx</code>、<code>TX_CNT_xxx</code> 统计数量，再用线性累加生成 <code>RX_IDX_xxx</code>、<code>TX_IDX_xxx</code>。</p><p>第四层是失败处理。<code>CO_alloc_break_on_fail()</code> 体现的是“一处失败，整体失败”的分配策略。只要任意对象分配失败，流程跳出，后续通过 <code>CO_delete(co)</code> 释放已经分配的对象，避免部分初始化对象泄漏。</p><p><img src="https://i-blog.csdnimg.cn/direct/6dc2d12c0f924c2faa79e68412b2d2f3.png" alt="在这里插入图片描述"></p><h2 id="CO-config-h-的宏本质是-bitmask，不是单选枚举"><a href="#CO-config-h-的宏本质是-bitmask，不是单选枚举" class="headerlink" title="CO_config.h 的宏本质是 bitmask，不是单选枚举"></a><code>CO_config.h</code> 的宏本质是 bitmask，不是单选枚举</h2><p><code>CO_config.h</code> 中的多数配置宏都按 bit 组合。例如：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> CO_CONFIG_EM_PRODUCER          0x01</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> CO_CONFIG_EM_PROD_CONFIGURABLE 0x02</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> CO_CONFIG_EM_PROD_INHIBIT      0x04</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> CO_CONFIG_EM_HISTORY           0x08</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> CO_CONFIG_EM_STATUS_BITS       0x10</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> CO_CONFIG_EM_CONSUMER          0x20</span></span><br></pre></td></tr></table></figure><p>这类宏的使用方式通常是：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> CO_CONFIG_EM \</span></span><br><span class="line"><span class="meta">    (CO_CONFIG_EM_PRODUCER | CO_CONFIG_EM_HISTORY | CO_CONFIG_FLAG_TIMERNEXT)</span></span><br></pre></td></tr></table></figure><p>所以 <code>CO_CONFIG_EM</code> 不是“Emergency 的模式编号”，而是一组功能位集合。<code>#if ((CO_CONFIG_EM) &amp; CO_CONFIG_EM_PRODUCER) != 0</code> 这种判断表示：只要 producer 位存在，就编译 producer 相关代码；其他位是否存在不会影响这个判断。</p><p>这种设计的好处是模块可以细粒度裁剪。Emergency 可以只做 producer，不做 consumer；SDO client 可以只支持 segmented transfer，不支持 block transfer；PDO 可以打开同步触发，但不使用 bitwise mapping。代价是宏之间存在依赖关系，某些高级功能位不能孤立打开。例如 SDO block transfer 依赖 segmented transfer；PDO bitwise mapping 依赖 OD_IO access；Gateway ASCII 的 SDO&#x2F;LSS&#x2F;NMT 命令能力依赖底层对应主站模块。</p><h2 id="CO-MULTIPLE-OD：把对象数量从静态宏切换到运行期配置结构"><a href="#CO-MULTIPLE-OD：把对象数量从静态宏切换到运行期配置结构" class="headerlink" title="CO_MULTIPLE_OD：把对象数量从静态宏切换到运行期配置结构"></a><code>CO_MULTIPLE_OD</code>：把对象数量从静态宏切换到运行期配置结构</h2><p><code>CO_MULTIPLE_OD</code> 不是协议对象宏，而是 CANopenNode 对“多对象字典”场景的构建宏。</p><p>未定义 <code>CO_MULTIPLE_OD</code> 时，<code>CO_new()</code> 会忽略 <code>config</code> 参数，数量通常来自生成的 <code>OD.h</code> 中的静态计数宏。这适合一个固件只绑定一个对象字典的典型 MCU 从机场景。</p><p>定义 <code>CO_MULTIPLE_OD</code> 后，<code>CO_new()</code> 会使用 <code>CO_config_t *config</code> 中的 <code>CNT_xxx</code> 和 <code>ENTRY_Hxxxx</code> 字段。此时同一个协议栈代码可以服务不同对象字典实例，适合网关、多虚拟设备、测试工具或需要运行期选择 OD 的应用。代价是 <code>CO_new()</code> 必须计算每个模块的 RX&#x2F;TX 数量和起始下标，并把这些结果保存在 <code>co-&gt;RX_IDX_xxx</code>、<code>co-&gt;TX_IDX_xxx</code>、<code>co-&gt;CNT_ALL_RX_MSGS</code>、<code>co-&gt;CNT_ALL_TX_MSGS</code> 中。</p><h3 id="CO-GET-CNT-xxx-的作用"><a href="#CO-GET-CNT-xxx-的作用" class="headerlink" title="CO_GET_CNT(xxx) 的作用"></a><code>CO_GET_CNT(xxx)</code> 的作用</h3><p><code>CO_GET_CNT(xxx)</code> 是 <code>CO_new()</code> 中最关键的数量抽象。它把“我要几个对象”这个问题隐藏起来：单 OD 模式下从编译期 OD 宏取值，多 OD 模式下从 <code>config-&gt;CNT_xxx</code> 取值。</p><p>例如：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (CO_GET_CNT(SYNC) == <span class="number">1U</span>) &#123;</span><br><span class="line">    CO_alloc_break_on_fail(co-&gt;SYNC, CO_GET_CNT(SYNC), <span class="keyword">sizeof</span>(*co-&gt;SYNC));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里表达的不是“SYNC 一定存在”，而是“如果当前配置中 SYNC 数量为 1，则创建 SYNC 对象”。<code>CO_CONFIG_SYNC_ENABLE</code> 决定这段代码是否存在，<code>CO_GET_CNT(SYNC)</code> 决定这段代码运行时是否分配。</p><h3 id="ON-MULTI-OD-expr-的作用"><a href="#ON-MULTI-OD-expr-的作用" class="headerlink" title="ON_MULTI_OD(expr) 的作用"></a><code>ON_MULTI_OD(expr)</code> 的作用</h3><p><code>ON_MULTI_OD(expr)</code> 是为了让某些变量和赋值只在多 OD 模式下存在。比如：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">ON_MULTI_OD(<span class="type">uint8_t</span> RX_CNT_SYNC = <span class="number">0</span>);</span><br><span class="line">ON_MULTI_OD(RX_CNT_SYNC = <span class="number">1</span>);</span><br></pre></td></tr></table></figure><p>这些中间变量的主要用途是给后面的 <code>RX_IDX_xxx</code>、<code>TX_IDX_xxx</code> 计算服务。单 OD 模式下，CANopenNode 可以通过静态宏推导下标，不需要在 <code>CO_new()</code> 中保存这些运行期值。</p><h3 id="CO-alloc-break-on-fail-ptr-count-size-的作用"><a href="#CO-alloc-break-on-fail-ptr-count-size-的作用" class="headerlink" title="CO_alloc_break_on_fail(ptr, count, size) 的作用"></a><code>CO_alloc_break_on_fail(ptr, count, size)</code> 的作用</h3><p>这个宏是 <code>CO_new()</code> 的统一分配入口。它承担三件事：按 <code>count * size</code> 分配内存、把分配量计入 <code>mem</code>、失败时跳出当前 <code>do { ... } while(0)</code> 流程。</p><p>它的意义不只是简化代码，而是保证所有对象分配具有一致的失败处理策略。没有这个宏，<code>CO_new()</code> 中每个对象都需要重复写“分配、判空、累计内存、失败清理”的逻辑，很容易出现某个分支遗漏清理或遗漏内存统计。</p><h3 id="CO-USE-GLOBALS-的作用"><a href="#CO-USE-GLOBALS-的作用" class="headerlink" title="CO_USE_GLOBALS 的作用"></a><code>CO_USE_GLOBALS</code> 的作用</h3><p><code>CO_USE_GLOBALS</code> 不是 <code>CO_config.h</code> 中的协议功能位，但它会改变对象存储方式。定义它时，CANopenNode 可以使用全局静态对象代替堆分配。对于裸机、强 MISRA 约束、禁止动态内存的工程，这个宏的意义很大：对象布局在编译期确定，启动期不依赖 <code>malloc/calloc</code>，失败路径也更容易受控。</p><p>它的代价是灵活性降低。需要的对象数量和模块组合通常要在编译期固定，不能像堆分配模式那样按不同配置动态装配。</p><h2 id="CANrx-与-CANtx-：宏最终会变成-CAN-驱动资源"><a href="#CANrx-与-CANtx-：宏最终会变成-CAN-驱动资源" class="headerlink" title="CANrx[] 与 CANtx[]：宏最终会变成 CAN 驱动资源"></a><code>CANrx[]</code> 与 <code>CANtx[]</code>：宏最终会变成 CAN 驱动资源</h2><p>CANopenNode 把接收过滤对象抽象为 <code>CO_CANrx_t</code>，把发送缓冲对象抽象为 <code>CO_CANtx_t</code>。<code>CO_new()</code> 最终分配：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">co-&gt;CANrx = ... CO_GET_CO(CNT_ALL_RX_MSGS) ...</span><br><span class="line">co-&gt;CANtx = ... CO_GET_CO(CNT_ALL_TX_MSGS) ...</span><br></pre></td></tr></table></figure><p>因此每打开一个协议能力，除了代码尺寸和对象内存，还可能增加 CAN 驱动层资源需求。STM32 bxCAN&#x2F;FDCAN 驱动移植时，虽然 CANopenNode 的 <code>CANrx[]</code> 是软件对象，但最终仍要映射到底层过滤、回调分发或软件匹配逻辑。对象数量越多，初始化表越大，接收分发路径越长。</p><p><img src="https://i-blog.csdnimg.cn/direct/fb2ce6a5247a4774b4a8436ee6fe9917.png" alt="在这里插入图片描述"></p><h2 id="公共宏：所有模块共享的行为增强开关"><a href="#公共宏：所有模块共享的行为增强开关" class="headerlink" title="公共宏：所有模块共享的行为增强开关"></a>公共宏：所有模块共享的行为增强开关</h2><h3 id="CO-CONFIG-FLAG-CALLBACK-PRE"><a href="#CO-CONFIG-FLAG-CALLBACK-PRE" class="headerlink" title="CO_CONFIG_FLAG_CALLBACK_PRE"></a><code>CO_CONFIG_FLAG_CALLBACK_PRE</code></h3><p><code>CO_CONFIG_FLAG_CALLBACK_PRE</code> 的值是 <code>0x1000</code>，它表示“在高优先级接收或预处理路径完成后，允许调用一个预处理回调”。</p><p>这个宏不直接启用某个 CANopen 协议对象，而是给多个对象增加“收到事件后唤醒低优先级处理”的能力。典型场景是 RTOS：CAN RX 中断或高优先级线程只做轻量解析，然后通过 callback 通知主循环、任务或事件组继续处理。</p><p>它的原理是把实时接收路径和主处理路径解耦。CANopenNode 中很多对象都有 <code>initCallbackPre()</code> 类接口，例如 NMT、Heartbeat consumer、SDO、SYNC、TIME、SRDO、LSS master 等。打开这个位后，对应对象会编译进 callback 支撑字段和调用点。</p><p>在裸机轮询场景中，如果主循环足够快，可以不打开这个位；在 RTOS 中，如果希望 CAN 接收后立即唤醒处理任务，这个位通常有价值。</p><h3 id="CO-CONFIG-FLAG-TIMERNEXT"><a href="#CO-CONFIG-FLAG-TIMERNEXT" class="headerlink" title="CO_CONFIG_FLAG_TIMERNEXT"></a><code>CO_CONFIG_FLAG_TIMERNEXT</code></h3><p><code>CO_CONFIG_FLAG_TIMERNEXT</code> 的值是 <code>0x2000</code>，它表示对象处理函数会计算 <code>timerNext_us</code>。</p><p><code>timerNext_us</code> 不是协议字段，而是调度辅助量。它告诉上层“下一次最迟多久以后需要再次调用处理函数”。这对 RTOS 低功耗、tickless idle、定时器驱动主循环都有意义。</p><p>例如 Heartbeat consumer 需要监视超时，TPDO 需要 event timer 和 inhibit timer，SYNC producer 需要周期发送。打开 <code>TIMERNEXT</code> 后，这些对象不仅处理当前周期，还会给出下一次处理的时间建议。</p><p>如果工程主循环固定 1 ms 或更快轮询，这个位不是必须；如果工程希望根据协议对象最近的超时时间安排睡眠&#x2F;唤醒，这个位可以减少无意义轮询。</p><h3 id="CO-CONFIG-FLAG-OD-DYNAMIC"><a href="#CO-CONFIG-FLAG-OD-DYNAMIC" class="headerlink" title="CO_CONFIG_FLAG_OD_DYNAMIC"></a><code>CO_CONFIG_FLAG_OD_DYNAMIC</code></h3><p><code>CO_CONFIG_FLAG_OD_DYNAMIC</code> 的值是 <code>0x4000</code>，它表示对象字典变量被写入后，可以在正常运行期间动态重配置对应协议对象。</p><p>它解决的问题是：CANopen 的许多通信参数存在对象字典中，例如 Heartbeat consumer 的 0x1016、SYNC 的 0x1005&#x2F;0x1006、TIME 的 0x1012、PDO 的通信参数和映射参数、附加 SDO 通道参数等。如果不打开动态行为，这些 OD 项通常只在通信复位或初始化阶段生效；打开后，SDO 写入 OD 项可能立即触发对象重配置。</p><p>这个宏让设备更符合“在线配置”的 CANopen 使用方式，但也增加运行期状态管理复杂度。对资源紧张且配置固定的从机，可以关闭它；对需要通过上位机实时调 PDO 映射、SYNC 参数、Heartbeat 监控表的设备，打开它更合适。</p><h3 id="全局公共宏"><a href="#全局公共宏" class="headerlink" title="全局公共宏"></a>全局公共宏</h3><p><code>CO_CONFIG_GLOBAL_FLAG_CALLBACK_PRE</code>、<code>CO_CONFIG_GLOBAL_RT_FLAG_CALLBACK_PRE</code>、<code>CO_CONFIG_GLOBAL_FLAG_TIMERNEXT</code>、<code>CO_CONFIG_GLOBAL_FLAG_OD_DYNAMIC</code> 是公共开关的批量注入入口。</p><p>它们本身不代表新功能，而是为了避免在每个模块配置宏中重复写相同 flag。例如把 <code>CO_CONFIG_GLOBAL_RT_FLAG_CALLBACK_PRE</code> 定义为 <code>CO_CONFIG_FLAG_CALLBACK_PRE</code> 后，SYNC 和 PDO 等实时对象可以一起获得预处理回调能力。把 <code>CO_CONFIG_GLOBAL_FLAG_OD_DYNAMIC</code> 定义为 0，则默认关闭全局动态 OD 行为，只在需要的模块中单独打开。</p><h2 id="NMT-与-Heartbeat：节点状态机和在线状态广播"><a href="#NMT-与-Heartbeat：节点状态机和在线状态广播" class="headerlink" title="NMT 与 Heartbeat：节点状态机和在线状态广播"></a>NMT 与 Heartbeat：节点状态机和在线状态广播</h2><p><img src="https://i-blog.csdnimg.cn/direct/450733ad6ccf439482af80f1e44ed7d9.png" alt="在这里插入图片描述"></p><h3 id="CO-CONFIG-NMT"><a href="#CO-CONFIG-NMT" class="headerlink" title="CO_CONFIG_NMT"></a><code>CO_CONFIG_NMT</code></h3><p><code>CO_CONFIG_NMT</code> 配置的是 <code>CO_NMT_Heartbeat</code> 对象。这个对象在 CANopenNode 中同时承担 NMT consumer 和 Heartbeat producer 的基础职责。</p><p>在 <code>CO_new()</code> 中，只要 <code>CO_GET_CNT(NMT) == 1U</code>，就会分配 <code>co-&gt;NMT</code>。随后一定会预留 NMT slave 的接收槽位 <code>RX_CNT_NMT_SLV = 1</code>，因为节点需要接收 NMT master 的状态控制命令；同时会预留 Heartbeat producer 的发送槽位 <code>TX_CNT_HB_PROD = 1</code>，因为本节点需要向网络广播状态。</p><p>协议层面，NMT 用于控制节点状态，Heartbeat 用于报告节点状态。NMT command 的数据一般包含命令字节和目标 Node-ID；Heartbeat 的数据通常是一个 NMT 状态字节。PDO 的可用性与 NMT 状态强相关：Pre-operational 下 SDO 等配置服务可用，但 PDO 不参与过程数据；Operational 下 PDO 才进入正常过程数据通信。</p><h3 id="CO-CONFIG-NMT-CALLBACK-CHANGE"><a href="#CO-CONFIG-NMT-CALLBACK-CHANGE" class="headerlink" title="CO_CONFIG_NMT_CALLBACK_CHANGE"></a><code>CO_CONFIG_NMT_CALLBACK_CHANGE</code></h3><p>这个位打开 NMT 状态变化回调。它关注的是“状态已经变了”，不是“收到了一帧 NMT 报文”。</p><p>例如设备从 Pre-operational 进入 Operational 时，应用层可能需要打开电机控制输出、允许采样数据进入 TPDO、启动某些周期任务。状态变化回调给应用一个统一入口，不需要应用自己反复查询 NMT 状态。</p><h3 id="CO-CONFIG-NMT-MASTER"><a href="#CO-CONFIG-NMT-MASTER" class="headerlink" title="CO_CONFIG_NMT_MASTER"></a><code>CO_CONFIG_NMT_MASTER</code></h3><p>这个位启用简单 NMT master 能力。打开后，<code>CO_new()</code> 会在 NMT 对象存在时增加 <code>TX_CNT_NMT_MST = 1</code>，也就是本节点可以发送 NMT 命令。</p><p>普通从机通常不需要该功能。主站、网关、测试工具、产线配置工具可能需要它。Gateway ASCII 如果要支持 NMT 命令，底层也要有 NMT master 发送能力。</p><h3 id="CO-CONFIG-HB-CONS"><a href="#CO-CONFIG-HB-CONS" class="headerlink" title="CO_CONFIG_HB_CONS"></a><code>CO_CONFIG_HB_CONS</code></h3><p><code>CO_CONFIG_HB_CONS</code> 配置 Heartbeat consumer。它不是本节点发送心跳，而是本节点监视其他节点心跳。</p><p>在 <code>CO_new()</code> 中，只有 <code>CO_CONFIG_HB_CONS_ENABLE</code> 打开时才会进入 Heartbeat consumer 分支。若 <code>CO_GET_CNT(HB_CONS) == 1U</code>，函数会分配 <code>co-&gt;HBcons</code>，并根据 <code>CO_GET_CNT(ARR_1016)</code> 分配 <code>co-&gt;HBconsMonitoredNodes</code>。这里 <code>ARR_1016</code> 对应 OD 0x1016 的监控项数量，每个监控项通常对应一个远端节点。</p><p>Heartbeat consumer 的本质是超时监控。远端节点按自己的 0x1017 周期发送 heartbeat，本节点按 0x1016 配置的超时时间判断远端是否掉线、是否重新启动、NMT 状态是否变化。</p><h3 id="CO-CONFIG-HB-CONS-ENABLE"><a href="#CO-CONFIG-HB-CONS-ENABLE" class="headerlink" title="CO_CONFIG_HB_CONS_ENABLE"></a><code>CO_CONFIG_HB_CONS_ENABLE</code></h3><p>这个位是 Heartbeat consumer 的总开关。没有它，后续 callback、query、dynamic OD 等细分位都没有意义。</p><p><code>CO_new()</code> 中的对象分配和 RX 槽位统计都受它控制。打开后，实际 RX 槽位数量不是固定 1，而是等于被监控节点数量。因为每个远端 heartbeat 的 CAN-ID 通常是 <code>0x700 + nodeId</code>，每个监控对象需要匹配自己的 CAN-ID。</p><h3 id="CO-CONFIG-HB-CONS-CALLBACK-CHANGE"><a href="#CO-CONFIG-HB-CONS-CALLBACK-CHANGE" class="headerlink" title="CO_CONFIG_HB_CONS_CALLBACK_CHANGE"></a><code>CO_CONFIG_HB_CONS_CALLBACK_CHANGE</code></h3><p>这个位启用“公共 NMT 状态变化回调”。多个被监控节点共用一个状态变化回调入口。它适合只需要知道“有节点状态变化了”的应用。</p><h3 id="CO-CONFIG-HB-CONS-CALLBACK-MULTI"><a href="#CO-CONFIG-HB-CONS-CALLBACK-MULTI" class="headerlink" title="CO_CONFIG_HB_CONS_CALLBACK_MULTI"></a><code>CO_CONFIG_HB_CONS_CALLBACK_MULTI</code></h3><p>这个位启用更细的多回调能力。它可以分别给 heartbeat started、timeout、remote reset、NMT changed 等事件注册回调。它适合网关、主站、诊断设备，因为这类设备要区分远端节点到底是启动、丢失、复位还是状态切换。</p><p><code>CO_CONFIG_HB_CONS_CALLBACK_CHANGE</code> 和 <code>CO_CONFIG_HB_CONS_CALLBACK_MULTI</code> 不能同时使用。前者是简化公共回调，后者是按事件和节点细分的回调模型。</p><h3 id="CO-CONFIG-HB-CONS-QUERY-FUNCT"><a href="#CO-CONFIG-HB-CONS-QUERY-FUNCT" class="headerlink" title="CO_CONFIG_HB_CONS_QUERY_FUNCT"></a><code>CO_CONFIG_HB_CONS_QUERY_FUNCT</code></h3><p>这个位启用查询函数，例如按节点或下标查询 Heartbeat 状态、NMT 状态。它不改变协议报文，只增加应用读取状态的 API。</p><p>如果应用只靠回调驱动，不需要主动查询，可以关闭。若有诊断界面、状态机或日志任务需要随时读取远端节点状态，应打开。</p><h2 id="Node-Guarding：Heartbeat-之前的旧式节点监控机制"><a href="#Node-Guarding：Heartbeat-之前的旧式节点监控机制" class="headerlink" title="Node Guarding：Heartbeat 之前的旧式节点监控机制"></a>Node Guarding：Heartbeat 之前的旧式节点监控机制</h2><p>Node Guarding 是 CANopen 早期的节点监控机制，基于远程帧请求和应答。它与 Heartbeat 的差异在于：Heartbeat 是 producer 周期主动发送状态，Node Guarding 是 master 周期轮询 slave。</p><h3 id="CO-CONFIG-NODE-GUARDING"><a href="#CO-CONFIG-NODE-GUARDING" class="headerlink" title="CO_CONFIG_NODE_GUARDING"></a><code>CO_CONFIG_NODE_GUARDING</code></h3><p>这个宏配置 Node Guarding slave&#x2F;master 对象。上传的 <code>CO_config.h</code> 中默认值为 0，说明默认不启用。对新工程而言，Heartbeat 更常用，Node Guarding 多用于兼容旧主站或旧设备。</p><h3 id="CO-CONFIG-NODE-GUARDING-SLAVE-ENABLE"><a href="#CO-CONFIG-NODE-GUARDING-SLAVE-ENABLE" class="headerlink" title="CO_CONFIG_NODE_GUARDING_SLAVE_ENABLE"></a><code>CO_CONFIG_NODE_GUARDING_SLAVE_ENABLE</code></h3><p>打开后，<code>CO_new()</code> 会分配 <code>co-&gt;NGslave</code>。这个对象让本节点响应 Node Guarding master 的远程请求。相关 OD 通常包括 0x100C Guard time 和 0x100D Life time factor。</p><h3 id="CO-CONFIG-NODE-GUARDING-MASTER-ENABLE"><a href="#CO-CONFIG-NODE-GUARDING-MASTER-ENABLE" class="headerlink" title="CO_CONFIG_NODE_GUARDING_MASTER_ENABLE"></a><code>CO_CONFIG_NODE_GUARDING_MASTER_ENABLE</code></h3><p>打开后，<code>CO_new()</code> 会分配 <code>co-&gt;NGmaster</code>。这个对象让本节点主动轮询其他节点。它属于主站或诊断工具能力，普通从机通常不需要。</p><h3 id="CO-CONFIG-NODE-GUARDING-MASTER-COUNT"><a href="#CO-CONFIG-NODE-GUARDING-MASTER-COUNT" class="headerlink" title="CO_CONFIG_NODE_GUARDING_MASTER_COUNT"></a><code>CO_CONFIG_NODE_GUARDING_MASTER_COUNT</code></h3><p>这个宏限制 master 可监控节点数量。它不在 <code>CO_new()</code> 的分配片段中直接出现，但影响 Node Guarding master 内部表的容量和编译期资源。</p><h2 id="Emergency：把内部错误转换成-CANopen-EMCY-事件"><a href="#Emergency：把内部错误转换成-CANopen-EMCY-事件" class="headerlink" title="Emergency：把内部错误转换成 CANopen EMCY 事件"></a>Emergency：把内部错误转换成 CANopen EMCY 事件</h2><p><img src="https://i-blog.csdnimg.cn/direct/e4171530b56a4e0ead493156a8be9702.png" alt="在这里插入图片描述"></p><h3 id="CO-CONFIG-EM"><a href="#CO-CONFIG-EM" class="headerlink" title="CO_CONFIG_EM"></a><code>CO_CONFIG_EM</code></h3><p><code>CO_CONFIG_EM</code> 配置 Emergency 对象。Emergency 是 CANopen 的错误控制与故障事件通知机制。它不负责“修复错误”，而是负责维护错误状态、生成错误寄存器、记录错误历史，并在错误首次发生或复位时发送 EMCY 报文。</p><p>在 <code>CO_new()</code> 中，只要 <code>CO_GET_CNT(EM) == 1U</code>，就会分配 <code>co-&gt;em</code>。如果 producer 或 history 被启用，函数还会根据 <code>ARR_1003 + 1</code> 分配 <code>co-&gt;em_fifo</code>。这里的 <code>ARR_1003</code> 对应预定义错误域 0x1003 的历史容量。</p><h3 id="CO-CONFIG-EM-PRODUCER"><a href="#CO-CONFIG-EM-PRODUCER" class="headerlink" title="CO_CONFIG_EM_PRODUCER"></a><code>CO_CONFIG_EM_PRODUCER</code></h3><p>这个位让本节点成为 Emergency producer。应用或协议栈调用错误上报接口时，首次出现的错误会触发 EMCY 报文。<code>CO_new()</code> 会增加 <code>TX_CNT_EM_PROD = 1</code>。</p><p>普通从机通常应启用 producer，因为主站需要知道从机错误。关闭 producer 后，错误可能仍在本地状态位中存在，但不会通过 EMCY 主动通知网络。</p><h3 id="CO-CONFIG-EM-PROD-CONFIGURABLE"><a href="#CO-CONFIG-EM-PROD-CONFIGURABLE" class="headerlink" title="CO_CONFIG_EM_PROD_CONFIGURABLE"></a><code>CO_CONFIG_EM_PROD_CONFIGURABLE</code></h3><p>这个位允许 EMCY COB-ID 通过 OD 0x1014 配置。如果关闭，COB-ID 通常按预定义连接集固定为 <code>CO_CAN_ID_EMERGENCY + nodeId</code>，OD 0x1014 更接近只读常量。</p><p>它适合需要非默认 COB-ID、网关隔离或特殊网络规划的系统。普通从机使用默认 COB-ID 即可。</p><h3 id="CO-CONFIG-EM-PROD-INHIBIT"><a href="#CO-CONFIG-EM-PROD-INHIBIT" class="headerlink" title="CO_CONFIG_EM_PROD_INHIBIT"></a><code>CO_CONFIG_EM_PROD_INHIBIT</code></h3><p>这个位启用 OD 0x1015 EMCY inhibit time。inhibit time 用于限制 EMCY 报文最小发送间隔，避免故障抖动时连续刷屏导致总线拥塞。</p><p>它不改变错误是否存在，只改变报文发送节奏。对于电机驱动、IO 模块等可能短时间连续报错的设备，这个位可以保护总线。</p><h3 id="CO-CONFIG-EM-HISTORY"><a href="#CO-CONFIG-EM-HISTORY" class="headerlink" title="CO_CONFIG_EM_HISTORY"></a><code>CO_CONFIG_EM_HISTORY</code></h3><p>这个位启用 OD 0x1003 预定义错误域。<code>CO_new()</code> 因此会分配 <code>em_fifo</code>，容量来自 <code>ARR_1003 + 1</code>。</p><p>错误历史让主站可以在故障发生后通过 SDO 查询过去出现过的错误。没有 history 时，EMCY 更偏向实时事件；有 history 时，设备具备基本故障追溯能力。</p><h3 id="CO-CONFIG-EM-STATUS-BITS"><a href="#CO-CONFIG-EM-STATUS-BITS" class="headerlink" title="CO_CONFIG_EM_STATUS_BITS"></a><code>CO_CONFIG_EM_STATUS_BITS</code></h3><p>这个位让内部 <code>CO_EM_errorStatusBits_t</code> 可从对象字典访问。它把协议栈内部错误状态位暴露给诊断工具或上位机。</p><p>它的风险是暴露实现细节。若设备只需要标准错误寄存器和 EMCY 报文，不一定需要打开；若需要深度诊断，可打开。</p><h3 id="CO-CONFIG-EM-CONSUMER"><a href="#CO-CONFIG-EM-CONSUMER" class="headerlink" title="CO_CONFIG_EM_CONSUMER"></a><code>CO_CONFIG_EM_CONSUMER</code></h3><p>这个位让本节点接收其他节点的 EMCY。<code>CO_new()</code> 会增加 <code>RX_CNT_EM_CONS = 1</code>。</p><p>普通从机通常不需要消费别人 EMCY；主站、网关、安全协调器或集中诊断设备才需要。它表示“我关心别人报错”，不是“我会报告自己的错误”。</p><h3 id="CO-CONFIG-EM-ERR-STATUS-BITS-COUNT"><a href="#CO-CONFIG-EM-ERR-STATUS-BITS-COUNT" class="headerlink" title="CO_CONFIG_EM_ERR_STATUS_BITS_COUNT"></a><code>CO_CONFIG_EM_ERR_STATUS_BITS_COUNT</code></h3><p>这个宏定义内部错误状态位数量。默认 <code>(10 * 8)</code>，也就是 80 bit。CANopenNode 栈自身使用前若干 bit，剩余部分可给制造商或设备 profile 使用。</p><p>如果应用要定义更多自有错误位，必须扩大这个值，否则错误位索引可能越界或无法表达。</p><h3 id="CO-CONFIG-ERR-CONDITION-GENERIC"><a href="#CO-CONFIG-ERR-CONDITION-GENERIC" class="headerlink" title="CO_CONFIG_ERR_CONDITION_GENERIC"></a><code>CO_CONFIG_ERR_CONDITION_GENERIC</code></h3><p>这个宏定义 0x1001 error register 中 generic error 位如何由内部 <code>errorStatusBits[]</code> 汇总得出。默认与 <code>em-&gt;errorStatusBits[5]</code> 相关。</p><p>它不是“上报 generic error 的 API”，而是“把一组内部错误状态归并到标准错误寄存器 bit0 的条件表达式”。</p><h3 id="CO-CONFIG-ERR-CONDITION-CURRENT"><a href="#CO-CONFIG-ERR-CONDITION-CURRENT" class="headerlink" title="CO_CONFIG_ERR_CONDITION_CURRENT"></a><code>CO_CONFIG_ERR_CONDITION_CURRENT</code></h3><p>这个宏定义 0x1001 中 current error 位的归并条件。默认未定义具体条件。电流类设备可以把过流、欠流、电流采样异常等内部错误位纳入该表达式。</p><h3 id="CO-CONFIG-ERR-CONDITION-VOLTAGE"><a href="#CO-CONFIG-ERR-CONDITION-VOLTAGE" class="headerlink" title="CO_CONFIG_ERR_CONDITION_VOLTAGE"></a><code>CO_CONFIG_ERR_CONDITION_VOLTAGE</code></h3><p>这个宏定义 voltage error 位的归并条件。电源、电池、驱动器类设备可以把过压、欠压、母线异常等错误位纳入。</p><h3 id="CO-CONFIG-ERR-CONDITION-TEMPERATURE"><a href="#CO-CONFIG-ERR-CONDITION-TEMPERATURE" class="headerlink" title="CO_CONFIG_ERR_CONDITION_TEMPERATURE"></a><code>CO_CONFIG_ERR_CONDITION_TEMPERATURE</code></h3><p>这个宏定义 temperature error 位的归并条件。温度传感器、功率器件、驱动器可以把过温、温度传感器断线等错误位纳入。</p><h3 id="CO-CONFIG-ERR-CONDITION-COMMUNICATION"><a href="#CO-CONFIG-ERR-CONDITION-COMMUNICATION" class="headerlink" title="CO_CONFIG_ERR_CONDITION_COMMUNICATION"></a><code>CO_CONFIG_ERR_CONDITION_COMMUNICATION</code></h3><p>这个宏定义 communication error 位的归并条件。默认会考虑协议栈内部通信错误状态位。通信错误通常包括 CAN 被动、bus off、PDO&#x2F;SYNC&#x2F;Heartbeat 相关异常等。</p><h3 id="CO-CONFIG-ERR-CONDITION-DEV-PROFILE"><a href="#CO-CONFIG-ERR-CONDITION-DEV-PROFILE" class="headerlink" title="CO_CONFIG_ERR_CONDITION_DEV_PROFILE"></a><code>CO_CONFIG_ERR_CONDITION_DEV_PROFILE</code></h3><p>这个宏定义 device profile specific error 位的归并条件。设备 profile 有特定错误分类时可以使用。</p><h3 id="CO-CONFIG-ERR-CONDITION-MANUFACTURER"><a href="#CO-CONFIG-ERR-CONDITION-MANUFACTURER" class="headerlink" title="CO_CONFIG_ERR_CONDITION_MANUFACTURER"></a><code>CO_CONFIG_ERR_CONDITION_MANUFACTURER</code></h3><p>这个宏定义 manufacturer specific error 位的归并条件。厂家自定义错误如果不适合归入 generic&#x2F;current&#x2F;voltage&#x2F;temperature&#x2F;communication，可归入 manufacturer bit。</p><h2 id="SDO-server：远端访问本节点对象字典的服务器"><a href="#SDO-server：远端访问本节点对象字典的服务器" class="headerlink" title="SDO server：远端访问本节点对象字典的服务器"></a>SDO server：远端访问本节点对象字典的服务器</h2><p><img src="https://i-blog.csdnimg.cn/direct/bad125f2aa5c47b0960251561433346c.png" alt="在这里插入图片描述"></p><h3 id="CO-CONFIG-SDO-SRV"><a href="#CO-CONFIG-SDO-SRV" class="headerlink" title="CO_CONFIG_SDO_SRV"></a><code>CO_CONFIG_SDO_SRV</code></h3><p><code>CO_CONFIG_SDO_SRV</code> 配置 SDO server。SDO server 是 CANopen 设备可配置、可诊断的基础能力。远端 SDO client 通过 server 读取或写入本节点对象字典。</p><p>在 <code>CO_new()</code> 中，只要 <code>CO_GET_CNT(SDO_SRV) &gt; 0U</code>，就会分配 <code>co-&gt;SDOserver</code> 数组。每个 SDO server 通道都需要一个接收槽位和一个发送槽位，因此 <code>RX_CNT_SDO_SRV</code> 和 <code>TX_CNT_SDO_SRV</code> 都等于 <code>CNT_SDO_SRV</code>。</p><p>普通从机至少需要一个默认 SDO server，否则主站难以通过标准方式配置对象字典。</p><h3 id="CO-CONFIG-SDO-SRV-SEGMENTED"><a href="#CO-CONFIG-SDO-SRV-SEGMENTED" class="headerlink" title="CO_CONFIG_SDO_SRV_SEGMENTED"></a><code>CO_CONFIG_SDO_SRV_SEGMENTED</code></h3><p>这个位启用 SDO server segmented transfer。Expedited transfer 适合 4 字节以内的小数据；segmented transfer 用于更长数据分段传输。</p><p>如果设备对象字典中存在较长字符串、域数据、较大数组，或需要兼容通用上位机，通常应启用 segmented transfer。</p><h3 id="CO-CONFIG-SDO-SRV-BLOCK"><a href="#CO-CONFIG-SDO-SRV-BLOCK" class="headerlink" title="CO_CONFIG_SDO_SRV_BLOCK"></a><code>CO_CONFIG_SDO_SRV_BLOCK</code></h3><p>这个位启用 SDO server block transfer。Block transfer 适合更大数据量，效率高于普通 segmented transfer。它依赖 segmented transfer，不能孤立使用。</p><p>在 MCU 上打开 block transfer 会增加缓冲需求和协议状态机复杂度。小型从机只做参数配置时，segmented 往往已经足够。</p><h3 id="CO-CONFIG-SDO-SRV-BUFFER-SIZE"><a href="#CO-CONFIG-SDO-SRV-BUFFER-SIZE" class="headerlink" title="CO_CONFIG_SDO_SRV_BUFFER_SIZE"></a><code>CO_CONFIG_SDO_SRV_BUFFER_SIZE</code></h3><p>这个宏定义 SDO server 内部数据缓冲区大小。缓冲太小，大对象需要更多段；缓冲更大，RAM 占用更高。</p><p>如果启用 block transfer，缓冲区通常要显著增大；如果只做少量标量参数读写，可以保持较小值。</p><h2 id="SDO-client：本节点主动访问其他节点对象字典"><a href="#SDO-client：本节点主动访问其他节点对象字典" class="headerlink" title="SDO client：本节点主动访问其他节点对象字典"></a>SDO client：本节点主动访问其他节点对象字典</h2><h3 id="CO-CONFIG-SDO-CLI"><a href="#CO-CONFIG-SDO-CLI" class="headerlink" title="CO_CONFIG_SDO_CLI"></a><code>CO_CONFIG_SDO_CLI</code></h3><p><code>CO_CONFIG_SDO_CLI</code> 配置 SDO client。SDO client 是主站、网关、配置工具常用能力：它可以主动读写远端节点对象字典。</p><p>在 <code>CO_new()</code> 中，只有 <code>CO_CONFIG_SDO_CLI_ENABLE</code> 打开才会编译相关分支。若 <code>CO_GET_CNT(SDO_CLI) &gt; 0U</code>，函数会分配 <code>co-&gt;SDOclient</code> 数组，并为每个 client 通道增加一个 RX 和一个 TX。</p><p>普通纯从机通常不需要 SDO client。设备如果需要开机后自动配置其他节点、作为简易主站或实现 Gateway ASCII 的 SDO 命令，则需要它。</p><h3 id="CO-CONFIG-SDO-CLI-ENABLE"><a href="#CO-CONFIG-SDO-CLI-ENABLE" class="headerlink" title="CO_CONFIG_SDO_CLI_ENABLE"></a><code>CO_CONFIG_SDO_CLI_ENABLE</code></h3><p>这个位是 SDO client 总开关。没有它，后续 segmented、block、local 等位都不会进入 <code>CO_new()</code> 的对象分配路径。</p><h3 id="CO-CONFIG-SDO-CLI-SEGMENTED"><a href="#CO-CONFIG-SDO-CLI-SEGMENTED" class="headerlink" title="CO_CONFIG_SDO_CLI_SEGMENTED"></a><code>CO_CONFIG_SDO_CLI_SEGMENTED</code></h3><p>这个位启用 SDO client segmented transfer。它决定本节点作为 client 时是否能处理较长数据的分段上传&#x2F;下载。</p><h3 id="CO-CONFIG-SDO-CLI-BLOCK"><a href="#CO-CONFIG-SDO-CLI-BLOCK" class="headerlink" title="CO_CONFIG_SDO_CLI_BLOCK"></a><code>CO_CONFIG_SDO_CLI_BLOCK</code></h3><p>这个位启用 SDO client block transfer。它依赖 segmented transfer，也依赖 FIFO 的某些能力，例如替代读取和 CRC16 支撑。它适合固件块、EDS&#x2F;DOMAIN 数据或较大参数块传输。</p><h3 id="CO-CONFIG-SDO-CLI-LOCAL"><a href="#CO-CONFIG-SDO-CLI-LOCAL" class="headerlink" title="CO_CONFIG_SDO_CLI_LOCAL"></a><code>CO_CONFIG_SDO_CLI_LOCAL</code></h3><p>这个位启用 local transfer。如果 client 要访问的 server Node-ID 与本节点相同，数据可以直接在本地对象字典中搬运，不经过 CAN 总线。</p><p>这对 Gateway 或测试工具很有用：同一设备既有 SDO client，又有 SDO server 时，可以用相同 API 访问本地 OD 和远端 OD。</p><h3 id="CO-CONFIG-SDO-CLI-BUFFER-SIZE"><a href="#CO-CONFIG-SDO-CLI-BUFFER-SIZE" class="headerlink" title="CO_CONFIG_SDO_CLI_BUFFER_SIZE"></a><code>CO_CONFIG_SDO_CLI_BUFFER_SIZE</code></h3><p>这个宏定义 SDO client 的循环缓冲大小。Segmented transfer 可以使用较小缓冲；block transfer 通常需要更大缓冲以提高 block size 和吞吐。</p><h2 id="TIME：网络时间戳通信对象"><a href="#TIME：网络时间戳通信对象" class="headerlink" title="TIME：网络时间戳通信对象"></a>TIME：网络时间戳通信对象</h2><p><img src="https://i-blog.csdnimg.cn/direct/7ef7dee6fa2140e58c81ff6f4dc57962.png" alt="在这里插入图片描述"></p><h3 id="CO-CONFIG-TIME"><a href="#CO-CONFIG-TIME" class="headerlink" title="CO_CONFIG_TIME"></a><code>CO_CONFIG_TIME</code></h3><p><code>CO_CONFIG_TIME</code> 配置 CANopen TIME 对象。TIME 的作用是把时间戳作为通信对象在网络上传递。它关注“现在是什么时间”，而不是“什么时候触发 PDO”。</p><p>在 <code>CO_new()</code> 中，只有 <code>CO_CONFIG_TIME_ENABLE</code> 打开时才会进入 TIME 分支。若 <code>CO_GET_CNT(TIME) == 1U</code>，函数分配 <code>co-&gt;TIME</code>，并为 consumer 预留一个 RX。如果 <code>CO_CONFIG_TIME_PRODUCER</code> 也打开，再预留一个 TX。</p><p>TIME 常用于日志、事件时间戳、分布式设备时间对齐。它不替代 RTOS tick，也不等价于 SYNC 周期。</p><h3 id="CO-CONFIG-TIME-ENABLE"><a href="#CO-CONFIG-TIME-ENABLE" class="headerlink" title="CO_CONFIG_TIME_ENABLE"></a><code>CO_CONFIG_TIME_ENABLE</code></h3><p>这个位启用 TIME object 和 TIME consumer。它表示本节点可以接收 TIME 报文并更新本地 CANopen 时间对象。</p><h3 id="CO-CONFIG-TIME-PRODUCER"><a href="#CO-CONFIG-TIME-PRODUCER" class="headerlink" title="CO_CONFIG_TIME_PRODUCER"></a><code>CO_CONFIG_TIME_PRODUCER</code></h3><p>这个位让本节点可以产生 TIME 报文。网络中通常只应有一个明确的时间源，除非系统设计允许多时间源并有仲裁策略。</p><h3 id="TIME-与-OD-动态配置"><a href="#TIME-与-OD-动态配置" class="headerlink" title="TIME 与 OD 动态配置"></a>TIME 与 OD 动态配置</h3><p><code>CO_CONFIG_FLAG_OD_DYNAMIC</code> 对 TIME 的意义主要体现在 OD 0x1012。写 0x1012 可以启用&#x2F;禁用 TIME producer 或 consumer，或者改变 COB-ID。打开动态配置后，这类写入不必等通信复位才生效。</p><h2 id="SYNC：同步窗口和-PDO-同步触发的基础"><a href="#SYNC：同步窗口和-PDO-同步触发的基础" class="headerlink" title="SYNC：同步窗口和 PDO 同步触发的基础"></a>SYNC：同步窗口和 PDO 同步触发的基础</h2><h3 id="CO-CONFIG-SYNC"><a href="#CO-CONFIG-SYNC" class="headerlink" title="CO_CONFIG_SYNC"></a><code>CO_CONFIG_SYNC</code></h3><p><code>CO_CONFIG_SYNC</code> 配置 SYNC 对象。SYNC 的作用是给网络提供一个同步事件，使多个节点在同一节拍上采样输入、更新输出或发送同步 PDO。</p><p>在 <code>CO_new()</code> 中，只有 <code>CO_CONFIG_SYNC_ENABLE</code> 打开时才会进入 SYNC 分支。若 <code>CO_GET_CNT(SYNC) == 1U</code>，函数会分配 <code>co-&gt;SYNC</code>，并为 consumer 预留一个 RX。如果 <code>CO_CONFIG_SYNC_PRODUCER</code> 也打开，再预留一个 TX。</p><p>SYNC 与 PDO 的关系很紧密，但二者不是同一个对象。SYNC 是节拍；PDO 是数据。PDO 是否受 SYNC 控制，还取决于 <code>CO_CONFIG_PDO_SYNC_ENABLE</code> 以及对象字典中 PDO communication parameter 的 transmission type。</p><h3 id="CO-CONFIG-SYNC-ENABLE"><a href="#CO-CONFIG-SYNC-ENABLE" class="headerlink" title="CO_CONFIG_SYNC_ENABLE"></a><code>CO_CONFIG_SYNC_ENABLE</code></h3><p>这个位启用 SYNC object 和 SYNC consumer。普通从机如果需要根据主站同步节拍采样或发送 PDO，就需要打开。</p><h3 id="CO-CONFIG-SYNC-PRODUCER"><a href="#CO-CONFIG-SYNC-PRODUCER" class="headerlink" title="CO_CONFIG_SYNC_PRODUCER"></a><code>CO_CONFIG_SYNC_PRODUCER</code></h3><p>这个位启用 SYNC producer。通常主站或同步源节点才需要。多个 SYNC producer 同时存在会造成同步源冲突，除非网络设计明确处理。</p><h3 id="SYNC-的-OD-动态配置"><a href="#SYNC-的-OD-动态配置" class="headerlink" title="SYNC 的 OD 动态配置"></a>SYNC 的 OD 动态配置</h3><p>SYNC 相关 OD 通常包括 0x1005 COB-ID SYNC message、0x1006 communication cycle period、0x1007 synchronous window length、0x1019 synchronous counter overflow value。打开 <code>CO_CONFIG_FLAG_OD_DYNAMIC</code> 后，写这些对象可以动态改变 SYNC 行为。</p><h2 id="PDO：无协议开销的实时过程数据"><a href="#PDO：无协议开销的实时过程数据" class="headerlink" title="PDO：无协议开销的实时过程数据"></a>PDO：无协议开销的实时过程数据</h2><h3 id="CO-CONFIG-PDO"><a href="#CO-CONFIG-PDO" class="headerlink" title="CO_CONFIG_PDO"></a><code>CO_CONFIG_PDO</code></h3><p><code>CO_CONFIG_PDO</code> 配置 RPDO 和 TPDO。PDO 是 CANopen 中实时过程数据通道，报文数据区不携带 index&#x2F;subindex，通信含义由对象字典映射表预先定义。</p><p>在 <code>CO_new()</code> 中，<code>CO_CONFIG_RPDO_ENABLE</code> 打开时才会分配 <code>co-&gt;RPDO</code>；<code>CO_CONFIG_TPDO_ENABLE</code> 打开时才会分配 <code>co-&gt;TPDO</code>。RPDO 消耗 RX 槽位，TPDO 消耗 TX 槽位。数量分别来自 <code>CNT_RPDO</code> 和 <code>CNT_TPDO</code>。</p><p>PDO 的优势是效率高：没有 SDO 那种命令字、index&#x2F;subindex、响应确认。代价是配置必须先完成。主站和从机必须对同一个 COB-ID、长度和映射内容有一致理解。</p><h3 id="CO-CONFIG-RPDO-ENABLE"><a href="#CO-CONFIG-RPDO-ENABLE" class="headerlink" title="CO_CONFIG_RPDO_ENABLE"></a><code>CO_CONFIG_RPDO_ENABLE</code></h3><p>这个位启用 Receive PDO。对本节点而言，RPDO 表示“接收来自网络的过程数据，并写入本地对象字典或应用接口”。</p><p>例如主站给从机下发控制字、目标速度、输出开关量时，从机通常通过 RPDO 接收。</p><h3 id="CO-CONFIG-TPDO-ENABLE"><a href="#CO-CONFIG-TPDO-ENABLE" class="headerlink" title="CO_CONFIG_TPDO_ENABLE"></a><code>CO_CONFIG_TPDO_ENABLE</code></h3><p>这个位启用 Transmit PDO。对本节点而言，TPDO 表示“把本地过程数据发布到网络”。</p><p>例如从机上报状态字、实际速度、输入状态、传感器采样值时，通常通过 TPDO 发送。</p><h3 id="CO-CONFIG-RPDO-TIMERS-ENABLE"><a href="#CO-CONFIG-RPDO-TIMERS-ENABLE" class="headerlink" title="CO_CONFIG_RPDO_TIMERS_ENABLE"></a><code>CO_CONFIG_RPDO_TIMERS_ENABLE</code></h3><p>这个位启用 RPDO 定时器，主要用于 RPDO 超时监控。若某个 RPDO 应该周期到达，但超过 event timer 未收到，设备可以报告通信异常。</p><p>它使 RPDO 不只是“收到就处理”，还具备“没有收到也能判错”的能力。</p><h3 id="CO-CONFIG-TPDO-TIMERS-ENABLE"><a href="#CO-CONFIG-TPDO-TIMERS-ENABLE" class="headerlink" title="CO_CONFIG_TPDO_TIMERS_ENABLE"></a><code>CO_CONFIG_TPDO_TIMERS_ENABLE</code></h3><p>这个位启用 TPDO inhibit timer 和 event timer。Inhibit timer 限制最小发送间隔，event timer 提供周期触发。</p><p>没有这个位时，TPDO 仍可由同步或事件触发，但缺少标准定时器行为。对于状态量周期上报或抑制高频变化报文，这个位很重要。</p><h3 id="CO-CONFIG-PDO-SYNC-ENABLE"><a href="#CO-CONFIG-PDO-SYNC-ENABLE" class="headerlink" title="CO_CONFIG_PDO_SYNC_ENABLE"></a><code>CO_CONFIG_PDO_SYNC_ENABLE</code></h3><p>这个位让 PDO 支持 SYNC 相关行为。它不自动创建 SYNC 对象，但让 PDO 处理函数理解同步触发语义。</p><p>如果关闭，即使 <code>CO_CONFIG_SYNC_ENABLE</code> 打开，PDO 模块也不会使用同步触发能力。若需要 synchronous PDO，两个宏都要匹配：SYNC 对象存在，PDO 模块支持 SYNC。</p><h3 id="CO-CONFIG-PDO-OD-IO-ACCESS"><a href="#CO-CONFIG-PDO-OD-IO-ACCESS" class="headerlink" title="CO_CONFIG_PDO_OD_IO_ACCESS"></a><code>CO_CONFIG_PDO_OD_IO_ACCESS</code></h3><p>这个位让 PDO 映射变量通过 <code>OD_IO_t</code> 读写函数访问，而不是直接从 OD 分配的内存取值。</p><p>它的价值是灵活：应用可以在读写回调中做边界检查、硬件寄存器同步、数据转换、互斥保护。代价是每次 PDO 访问更复杂，内存和 CPU 成本更高。</p><h3 id="CO-CONFIG-PDO-BITWISE-MAPPING"><a href="#CO-CONFIG-PDO-BITWISE-MAPPING" class="headerlink" title="CO_CONFIG_PDO_BITWISE_MAPPING"></a><code>CO_CONFIG_PDO_BITWISE_MAPPING</code></h3><p>这个位启用 bitwise mapping。默认 PDO 映射通常按字节处理；bitwise mapping 允许按 bit 粒度映射对象。</p><p>它依赖 <code>CO_CONFIG_PDO_OD_IO_ACCESS</code>，因为 bit 级偏移需要更灵活的访问模型。对于紧凑数字 IO、布尔量打包很有用；对于普通 8&#x2F;16&#x2F;32-bit 变量，byte-wise mapping 更简单。</p><h2 id="Storage：0x1010-0x1011-背后的保存和恢复能力"><a href="#Storage：0x1010-0x1011-背后的保存和恢复能力" class="headerlink" title="Storage：0x1010&#x2F;0x1011 背后的保存和恢复能力"></a>Storage：0x1010&#x2F;0x1011 背后的保存和恢复能力</h2><p><img src="https://i-blog.csdnimg.cn/direct/bbe13e063f7047fbab57869ced5c96ba.png" alt="在这里插入图片描述"></p><h3 id="CO-CONFIG-STORAGE"><a href="#CO-CONFIG-STORAGE" class="headerlink" title="CO_CONFIG_STORAGE"></a><code>CO_CONFIG_STORAGE</code></h3><p><code>CO_CONFIG_STORAGE</code> 配置 CANopenNode 的存储基础能力。它对应 CANopen 中通过 OD 0x1010 保存参数、通过 OD 0x1011 恢复默认参数的机制。</p><p>需要注意的是，<code>CO_new()</code> 上传片段没有直接分配 storage 对象。Storage 通常和目标平台 EEPROM、Flash、文件系统或 NVM 驱动绑定，属于协议栈与平台持久化之间的桥。</p><h3 id="CO-CONFIG-STORAGE-ENABLE"><a href="#CO-CONFIG-STORAGE-ENABLE" class="headerlink" title="CO_CONFIG_STORAGE_ENABLE"></a><code>CO_CONFIG_STORAGE_ENABLE</code></h3><p>这个位启用 storage。打开后，设备可以响应标准保存&#x2F;恢复对象的写入请求，把指定 OD 组保存到非易失介质，或者恢复默认值。</p><p>在 STM32 上，真正的可靠性取决于底层 Flash 擦写策略、掉电保护、磨损均衡、参数校验。宏只打开协议栈侧框架，不自动保证 NVM 的电气可靠性。</p><h2 id="LEDs：把-CANopen-状态转换为红绿灯语义"><a href="#LEDs：把-CANopen-状态转换为红绿灯语义" class="headerlink" title="LEDs：把 CANopen 状态转换为红绿灯语义"></a>LEDs：把 CANopen 状态转换为红绿灯语义</h2><h3 id="CO-CONFIG-LEDS"><a href="#CO-CONFIG-LEDS" class="headerlink" title="CO_CONFIG_LEDS"></a><code>CO_CONFIG_LEDS</code></h3><p><code>CO_CONFIG_LEDS</code> 配置 CANopen LED indicators。它通常遵循 CiA 303-3 中红灯、绿灯的状态语义，用于把 NMT 状态、错误状态、LSS 状态转换为可显示的 LED 闪烁模式。</p><p>在 <code>CO_new()</code> 中，只有 <code>CO_CONFIG_LEDS_ENABLE</code> 打开且 <code>CO_GET_CNT(LEDS) == 1U</code> 时才会分配 <code>co-&gt;LEDs</code>。</p><h3 id="CO-CONFIG-LEDS-ENABLE"><a href="#CO-CONFIG-LEDS-ENABLE" class="headerlink" title="CO_CONFIG_LEDS_ENABLE"></a><code>CO_CONFIG_LEDS_ENABLE</code></h3><p>这个位启用 LED 状态计算。它不会自动操作 GPIO，而是计算 LED 语义状态。应用或驱动需要把计算结果映射到实际引脚、定时器或 LED 控制任务。</p><p>因此它适合标准 CANopen 指示灯产品；如果设备没有 LED 或 LED 由应用完全自定义，可以关闭。</p><h2 id="GFC：Global-Fail-safe-Command，全局安全命令"><a href="#GFC：Global-Fail-safe-Command，全局安全命令" class="headerlink" title="GFC：Global Fail-safe Command，全局安全命令"></a>GFC：Global Fail-safe Command，全局安全命令</h2><p><img src="https://i-blog.csdnimg.cn/direct/f7c3bf8e094042fabe278e77a2933609.png" alt="在这里插入图片描述"></p><h3 id="CO-CONFIG-GFC"><a href="#CO-CONFIG-GFC" class="headerlink" title="CO_CONFIG_GFC"></a><code>CO_CONFIG_GFC</code></h3><p><code>CO_CONFIG_GFC</code> 配置 GFC 对象。GFC 属于 CANopen safety 相关能力，用于在网络上传播全局 fail-safe 命令。</p><p>在 <code>CO_new()</code> 中，只有 <code>CO_CONFIG_GFC_ENABLE</code> 打开时才会进入 GFC 分支。若 <code>CO_GET_CNT(GFC) == 1</code>，函数分配 <code>co-&gt;GFC</code>，并设置 <code>RX_CNT_GFC = 1</code>、<code>TX_CNT_GFC = 1</code>。这表示 CANopenNode 的 GFC 对象同时预留 consumer 和 producer 所需槽位，具体行为再由 producer&#x2F;consumer 位决定。</p><p>GFC 的协议语义比较直接：某个安全相关 producer 发送全局 fail-safe command，consumer 收到后进入应用定义的安全处理路径。它不是周期数据，也不是被定时监控的链路健康机制。</p><h3 id="CO-CONFIG-GFC-ENABLE"><a href="#CO-CONFIG-GFC-ENABLE" class="headerlink" title="CO_CONFIG_GFC_ENABLE"></a><code>CO_CONFIG_GFC_ENABLE</code></h3><p>这个位是 GFC 总开关。没有它，GFC producer&#x2F;consumer 位没有实际意义。</p><h3 id="CO-CONFIG-GFC-CONSUMER"><a href="#CO-CONFIG-GFC-CONSUMER" class="headerlink" title="CO_CONFIG_GFC_CONSUMER"></a><code>CO_CONFIG_GFC_CONSUMER</code></h3><p>这个位启用 GFC consumer。它表示本节点会接收全局安全命令，并把该事件传递给安全相关应用逻辑。</p><h3 id="CO-CONFIG-GFC-PRODUCER"><a href="#CO-CONFIG-GFC-PRODUCER" class="headerlink" title="CO_CONFIG_GFC_PRODUCER"></a><code>CO_CONFIG_GFC_PRODUCER</code></h3><p>这个位启用 GFC producer。它表示本节点可以在检测到安全相关条件时发送全局 fail-safe command。</p><p>普通非安全从机通常不需要 GFC。若工程涉及安全链路，应结合系统安全需求、EN 50325-5&#x2F;CiA 304 设计和硬件安全状态机一起评估，不能只靠打开宏完成安全设计。</p><h2 id="SRDO：Safety-Related-Data-Object，安全相关数据对象"><a href="#SRDO：Safety-Related-Data-Object，安全相关数据对象" class="headerlink" title="SRDO：Safety Related Data Object，安全相关数据对象"></a>SRDO：Safety Related Data Object，安全相关数据对象</h2><h3 id="CO-CONFIG-SRDO"><a href="#CO-CONFIG-SRDO" class="headerlink" title="CO_CONFIG_SRDO"></a><code>CO_CONFIG_SRDO</code></h3><p><code>CO_CONFIG_SRDO</code> 配置 SRDO 对象。SRDO 与普通 PDO 相似，都是过程数据，但 SRDO 面向安全相关数据，通常采用双报文、互补校验和时间约束来降低错误数据被误采信的概率。</p><p>在 <code>CO_new()</code> 中，只有 <code>CO_CONFIG_SRDO_ENABLE</code> 打开时才会进入 SRDO 分支。若 <code>CO_GET_CNT(SRDO) &gt; 0U</code>，函数会分配一个 <code>co-&gt;SRDOGuard</code>，再按 SRDO 数量分配 <code>co-&gt;SRDO</code> 数组。每个 SRDO 会消耗 <code>2 * CNT_SRDO</code> 个 RX 和 <code>2 * CNT_SRDO</code> 个 TX，因为安全相关通信通常需要成对报文。</p><h3 id="CO-CONFIG-SRDO-ENABLE"><a href="#CO-CONFIG-SRDO-ENABLE" class="headerlink" title="CO_CONFIG_SRDO_ENABLE"></a><code>CO_CONFIG_SRDO_ENABLE</code></h3><p>这个位启用 SRDO object。打开后，SRDO 的初始化、处理函数和对象内存才会参与构建。</p><p>SRDO 不应被当成“更可靠的 PDO”简单替换。它需要对象字典中对应 SRDO 参数、映射参数、校验参数匹配，并需要应用在通信未建立或校验失败时进入安全态。</p><h3 id="CO-CONFIG-SRDO-CHECK-TX"><a href="#CO-CONFIG-SRDO-CHECK-TX" class="headerlink" title="CO_CONFIG_SRDO_CHECK_TX"></a><code>CO_CONFIG_SRDO_CHECK_TX</code></h3><p>这个位启用发送前数据检查。它让 producer 在发出 SRDO 前检查映射数据关系，避免明显不一致的数据进入总线。</p><p>该检查提高发送端防错能力，但不能替代接收端校验和系统级安全状态机。安全通信必须按 producer 和 consumer 两端共同成立来理解。</p><h3 id="CO-CONFIG-SRDO-MINIMUM-DELAY"><a href="#CO-CONFIG-SRDO-MINIMUM-DELAY" class="headerlink" title="CO_CONFIG_SRDO_MINIMUM_DELAY"></a><code>CO_CONFIG_SRDO_MINIMUM_DELAY</code></h3><p>这个宏定义 SRDO 两帧发送之间的最小延时，单位为微秒。它影响 SRDO producer 发送第一帧和第二帧之间的时间关系。</p><p>延时过小可能不满足某些安全时序假设；延时过大可能影响响应时间。该值应结合总线负载、安全周期、驱动发送路径和系统安全需求确定。</p><h2 id="LSS：Layer-Setting-Services，节点-ID-和位速率配置服务"><a href="#LSS：Layer-Setting-Services，节点-ID-和位速率配置服务" class="headerlink" title="LSS：Layer Setting Services，节点 ID 和位速率配置服务"></a>LSS：Layer Setting Services，节点 ID 和位速率配置服务</h2><p><img src="https://i-blog.csdnimg.cn/direct/caa97908d9b74cceb31e58bb39438e64.png" alt="在这里插入图片描述"></p><h3 id="CO-CONFIG-LSS"><a href="#CO-CONFIG-LSS" class="headerlink" title="CO_CONFIG_LSS"></a><code>CO_CONFIG_LSS</code></h3><p><code>CO_CONFIG_LSS</code> 配置 LSS master&#x2F;slave。LSS 用于在节点尚未拥有确定 Node-ID，或者需要通过设备身份参数重新配置 Node-ID&#x2F;bit-rate 时进行网络层设置。</p><p>在 <code>CO_new()</code> 中，LSS slave 和 LSS master 分开处理。<code>CO_CONFIG_LSS_SLAVE</code> 打开时，如果 <code>CO_GET_CNT(LSS_SLV) == 1U</code>，函数分配 <code>co-&gt;LSSslave</code>，并增加一个 RX 和一个 TX。<code>CO_CONFIG_LSS_MASTER</code> 打开时，如果 <code>CO_GET_CNT(LSS_MST) == 1U</code>，函数分配 <code>co-&gt;LSSmaster</code>，也增加一个 RX 和一个 TX。</p><p>LSS 的核心不是普通过程数据，而是“识别设备并配置通信参数”。它通常依据 Vendor-ID、Product code、Revision number、Serial number 等身份信息定位设备。</p><h3 id="CO-CONFIG-LSS-SLAVE"><a href="#CO-CONFIG-LSS-SLAVE" class="headerlink" title="CO_CONFIG_LSS_SLAVE"></a><code>CO_CONFIG_LSS_SLAVE</code></h3><p>这个位启用 LSS slave。它适合需要出厂后由主站分配 Node-ID 的设备，尤其是同型号多节点同时接入总线、无法靠拨码开关区分 ID 的系统。</p><p>若设备 Node-ID 固定在固件或硬件拨码中，且不需要远程配置 bit-rate，可以关闭 LSS slave。</p><h3 id="CO-CONFIG-LSS-SLAVE-FASTSCAN-DIRECT-RESPOND"><a href="#CO-CONFIG-LSS-SLAVE-FASTSCAN-DIRECT-RESPOND" class="headerlink" title="CO_CONFIG_LSS_SLAVE_FASTSCAN_DIRECT_RESPOND"></a><code>CO_CONFIG_LSS_SLAVE_FASTSCAN_DIRECT_RESPOND</code></h3><p>这个位让 LSS slave 在 fastscan 接收路径中直接响应。它的目标是改善 fastscan 响应时延。</p><p>代价是接收路径承担更多工作。对于严格中断最小化的移植层，需要确认直接响应是否符合驱动设计；对于需要快速 LSS 扫描的设备，它可以缩短发现流程。</p><h3 id="CO-CONFIG-LSS-MASTER"><a href="#CO-CONFIG-LSS-MASTER" class="headerlink" title="CO_CONFIG_LSS_MASTER"></a><code>CO_CONFIG_LSS_MASTER</code></h3><p>这个位启用 LSS master。它适合主站、网关、配置工具。普通从机通常不需要。</p><p>Gateway ASCII 若要支持 LSS 命令，底层必须有 LSS master 能力；否则文本命令即使被解析，也无法真正对总线上的 LSS slave 执行配置。</p><h2 id="Gateway-ASCII：把外部文本接口映射到-CANopen-操作"><a href="#Gateway-ASCII：把外部文本接口映射到-CANopen-操作" class="headerlink" title="Gateway ASCII：把外部文本接口映射到 CANopen 操作"></a>Gateway ASCII：把外部文本接口映射到 CANopen 操作</h2><p><img src="https://i-blog.csdnimg.cn/direct/286f9754994c484f8a14d3ad142d9b61.png" alt="在这里插入图片描述"></p><h3 id="CO-CONFIG-GTW"><a href="#CO-CONFIG-GTW" class="headerlink" title="CO_CONFIG_GTW"></a><code>CO_CONFIG_GTW</code></h3><p><code>CO_CONFIG_GTW</code> 配置 CANopen gateway。Gateway 的重点是“从其他网络或文本接口访问 CANopen 网络”。在 CANopenNode 中，常见实现是 ASCII mapping，也就是通过串口、终端、stdio 或 socket 输入文本命令，再转为 SDO、NMT、LSS 等 CANopen 操作。</p><p>在 <code>CO_new()</code> 中，只有 <code>CO_CONFIG_GTW_ASCII</code> 打开时才会进入 gateway 分支。若 <code>CO_GET_CNT(GTWA) == 1U</code>，函数分配 <code>co-&gt;gtwa</code>。</p><p>Gateway 不等于 SDO client，但通常依赖 SDO client；不等于 NMT master，但可能调用 NMT master；不等于 LSS master，但可以暴露 LSS 命令。它更像协议栈外部的命令入口。</p><h3 id="CO-CONFIG-GTW-MULTI-NET"><a href="#CO-CONFIG-GTW-MULTI-NET" class="headerlink" title="CO_CONFIG_GTW_MULTI_NET"></a><code>CO_CONFIG_GTW_MULTI_NET</code></h3><p>这个位表示多网络接口 gateway 能力。上传的 <code>CO_config.h</code> 注释说明该功能当前未实现。因此在阅读代码时应把它视为接口预留，而不是可直接使用的多 CAN 网络网关能力。</p><h3 id="CO-CONFIG-GTW-ASCII"><a href="#CO-CONFIG-GTW-ASCII" class="headerlink" title="CO_CONFIG_GTW_ASCII"></a><code>CO_CONFIG_GTW_ASCII</code></h3><p>这个位启用 ASCII gateway。打开后，<code>CO_new()</code> 才会分配 <code>co-&gt;gtwa</code>。</p><p>它通常还需要 FIFO 的 ASCII command 能力作为输入输出缓冲支撑。否则 gateway 对象即使存在，也缺少适合文本命令流的缓冲解析基础。</p><h3 id="CO-CONFIG-GTW-ASCII-SDO"><a href="#CO-CONFIG-GTW-ASCII-SDO" class="headerlink" title="CO_CONFIG_GTW_ASCII_SDO"></a><code>CO_CONFIG_GTW_ASCII_SDO</code></h3><p>这个位让 ASCII gateway 支持 SDO read&#x2F;write。它依赖 SDO client 能力，并通常需要 FIFO 的 ASCII datatype 解析能力。</p><h3 id="CO-CONFIG-GTW-ASCII-NMT"><a href="#CO-CONFIG-GTW-ASCII-NMT" class="headerlink" title="CO_CONFIG_GTW_ASCII_NMT"></a><code>CO_CONFIG_GTW_ASCII_NMT</code></h3><p>这个位让 ASCII gateway 支持 NMT master 命令，例如启动、停止、复位远端节点。底层需要 NMT master 发送能力。</p><h3 id="CO-CONFIG-GTW-ASCII-LSS"><a href="#CO-CONFIG-GTW-ASCII-LSS" class="headerlink" title="CO_CONFIG_GTW_ASCII_LSS"></a><code>CO_CONFIG_GTW_ASCII_LSS</code></h3><p>这个位让 ASCII gateway 支持 LSS master 命令，例如扫描未配置节点、设置 Node-ID、设置 bit-rate。底层需要 LSS master 能力。</p><h3 id="CO-CONFIG-GTW-ASCII-LOG"><a href="#CO-CONFIG-GTW-ASCII-LOG" class="headerlink" title="CO_CONFIG_GTW_ASCII_LOG"></a><code>CO_CONFIG_GTW_ASCII_LOG</code></h3><p>这个位启用非标准 message log 读取。它更偏调试和诊断，不是 CiA 301 过程数据能力。</p><h3 id="CO-CONFIG-GTW-ASCII-ERROR-DESC"><a href="#CO-CONFIG-GTW-ASCII-ERROR-DESC" class="headerlink" title="CO_CONFIG_GTW_ASCII_ERROR_DESC"></a><code>CO_CONFIG_GTW_ASCII_ERROR_DESC</code></h3><p>这个位让 gateway 在响应 SDO 或 gateway 错误时输出额外错误描述。它提高人工调试体验，但会增加字符串和代码尺寸。</p><h3 id="CO-CONFIG-GTW-ASCII-PRINT-HELP"><a href="#CO-CONFIG-GTW-ASCII-PRINT-HELP" class="headerlink" title="CO_CONFIG_GTW_ASCII_PRINT_HELP"></a><code>CO_CONFIG_GTW_ASCII_PRINT_HELP</code></h3><p>这个位启用非标准 <code>help</code> 命令输出。它适合终端交互式调试，不适合极限裁剪固件。</p><h3 id="CO-CONFIG-GTW-ASCII-PRINT-LEDS"><a href="#CO-CONFIG-GTW-ASCII-PRINT-LEDS" class="headerlink" title="CO_CONFIG_GTW_ASCII_PRINT_LEDS"></a><code>CO_CONFIG_GTW_ASCII_PRINT_LEDS</code></h3><p>这个位让 gateway 显示 CANopen 红绿 LED 状态。它依赖 LED 状态计算结果，适合命令行查看节点状态。</p><h3 id="CO-CONFIG-GTW-BLOCK-DL-LOOP"><a href="#CO-CONFIG-GTW-BLOCK-DL-LOOP" class="headerlink" title="CO_CONFIG_GTW_BLOCK_DL_LOOP"></a><code>CO_CONFIG_GTW_BLOCK_DL_LOOP</code></h3><p>这个宏定义 SDO block download 进行时，gateway 内部可连续调用 <code>CO_SDOclientDownload()</code> 的循环次数。它用于提升 block download 吞吐。</p><p>值越大，单次 gateway 处理可能占用更长 CPU 时间；值越小，响应更分散但吞吐较低。RTOS 中要考虑任务调度延迟，裸机中要考虑主循环阻塞时间。</p><h3 id="CO-CONFIG-GTWA-COMM-BUF-SIZE"><a href="#CO-CONFIG-GTWA-COMM-BUF-SIZE" class="headerlink" title="CO_CONFIG_GTWA_COMM_BUF_SIZE"></a><code>CO_CONFIG_GTWA_COMM_BUF_SIZE</code></h3><p>这个宏定义 ASCII gateway 命令缓冲区大小。普通短命令 200 字节可能足够；大量 block transfer 或长字符串写入时需要增大。</p><h3 id="CO-CONFIG-GTWA-LOG-BUF-SIZE"><a href="#CO-CONFIG-GTWA-LOG-BUF-SIZE" class="headerlink" title="CO_CONFIG_GTWA_LOG_BUF_SIZE"></a><code>CO_CONFIG_GTWA_LOG_BUF_SIZE</code></h3><p>这个宏定义 message log 缓冲区大小。它只影响 gateway 日志能力，不影响 CANopen 核心协议正确性。日志越大，RAM 占用越高。</p><h2 id="CRC16：块传输和流式数据的校验支撑"><a href="#CRC16：块传输和流式数据的校验支撑" class="headerlink" title="CRC16：块传输和流式数据的校验支撑"></a>CRC16：块传输和流式数据的校验支撑</h2><p><img src="https://i-blog.csdnimg.cn/direct/aa2d5471d68a4bb49dadd793ec6b0375.png" alt="在这里插入图片描述"></p><h3 id="CO-CONFIG-CRC16"><a href="#CO-CONFIG-CRC16" class="headerlink" title="CO_CONFIG_CRC16"></a><code>CO_CONFIG_CRC16</code></h3><p><code>CO_CONFIG_CRC16</code> 配置 CRC 16 CCITT 计算。它不是一个 CANopen 通信对象，不会在 <code>CO_new()</code> 中分配 <code>co-&gt;CRC</code> 这类对象。它是 SDO block transfer、FIFO 校验或其他数据完整性场景的算法支撑。</p><h3 id="CO-CONFIG-CRC16-ENABLE"><a href="#CO-CONFIG-CRC16-ENABLE" class="headerlink" title="CO_CONFIG_CRC16_ENABLE"></a><code>CO_CONFIG_CRC16_ENABLE</code></h3><p>这个位启用 CANopenNode 内部 CRC16 实现。需要 block transfer CRC 或 FIFO CRC 时应打开。</p><h3 id="CO-CONFIG-CRC16-EXTERNAL"><a href="#CO-CONFIG-CRC16-EXTERNAL" class="headerlink" title="CO_CONFIG_CRC16_EXTERNAL"></a><code>CO_CONFIG_CRC16_EXTERNAL</code></h3><p>这个位表示使用外部 CRC16 实现。适合 MCU 有硬件 CRC、项目已有统一 CRC 库，或希望把算法实现放到平台层的场景。</p><h2 id="FIFO：SDO-和-Gateway-的流式缓冲基础"><a href="#FIFO：SDO-和-Gateway-的流式缓冲基础" class="headerlink" title="FIFO：SDO 和 Gateway 的流式缓冲基础"></a>FIFO：SDO 和 Gateway 的流式缓冲基础</h2><h3 id="CO-CONFIG-FIFO"><a href="#CO-CONFIG-FIFO" class="headerlink" title="CO_CONFIG_FIFO"></a><code>CO_CONFIG_FIFO</code></h3><p><code>CO_CONFIG_FIFO</code> 配置 FIFO circular buffer。FIFO 不是独立协议，而是给 SDO client、Gateway ASCII、block transfer 等连续数据流提供缓冲能力。</p><h3 id="CO-CONFIG-FIFO-ENABLE"><a href="#CO-CONFIG-FIFO-ENABLE" class="headerlink" title="CO_CONFIG_FIFO_ENABLE"></a><code>CO_CONFIG_FIFO_ENABLE</code></h3><p>这个位启用 FIFO。没有它，依赖 FIFO 的高级数据流能力无法工作。</p><h3 id="CO-CONFIG-FIFO-ALT-READ"><a href="#CO-CONFIG-FIFO-ALT-READ" class="headerlink" title="CO_CONFIG_FIFO_ALT_READ"></a><code>CO_CONFIG_FIFO_ALT_READ</code></h3><p>这个位启用替代读取方式。SDO client block transfer 可能依赖它来提高连续读写效率。</p><h3 id="CO-CONFIG-FIFO-CRC16-CCITT"><a href="#CO-CONFIG-FIFO-CRC16-CCITT" class="headerlink" title="CO_CONFIG_FIFO_CRC16_CCITT"></a><code>CO_CONFIG_FIFO_CRC16_CCITT</code></h3><p>这个位让 FIFO 支持 CRC16 CCITT 计算。它通常与 block transfer 的数据校验有关，也可能被 Gateway 大块传输使用。</p><h3 id="CO-CONFIG-FIFO-ASCII-COMMANDS"><a href="#CO-CONFIG-FIFO-ASCII-COMMANDS" class="headerlink" title="CO_CONFIG_FIFO_ASCII_COMMANDS"></a><code>CO_CONFIG_FIFO_ASCII_COMMANDS</code></h3><p>这个位让 FIFO 支持 ASCII command 流。Gateway ASCII 需要文本命令解析时会依赖它。</p><h3 id="CO-CONFIG-FIFO-ASCII-DATATYPES"><a href="#CO-CONFIG-FIFO-ASCII-DATATYPES" class="headerlink" title="CO_CONFIG_FIFO_ASCII_DATATYPES"></a><code>CO_CONFIG_FIFO_ASCII_DATATYPES</code></h3><p>这个位让 FIFO 支持 ASCII 数据类型解析。Gateway 做 SDO read&#x2F;write 时，需要把文本参数转换成 CANopen 数据类型，或把数据转换成文本输出。</p><h2 id="Trace：记录变量随时间变化的非标准观测能力"><a href="#Trace：记录变量随时间变化的非标准观测能力" class="headerlink" title="Trace：记录变量随时间变化的非标准观测能力"></a>Trace：记录变量随时间变化的非标准观测能力</h2><h3 id="CO-CONFIG-TRACE"><a href="#CO-CONFIG-TRACE" class="headerlink" title="CO_CONFIG_TRACE"></a><code>CO_CONFIG_TRACE</code></h3><p><code>CO_CONFIG_TRACE</code> 配置 trace recorder。它不是 CiA 301 标准对象，而是 CANopenNode 提供的变量记录能力。</p><p>在 <code>CO_new()</code> 中，只要 <code>CO_CONFIG_TRACE_ENABLE</code> 打开且 <code>CO_GET_CNT(TRACE) &gt; 0</code>，就会分配 <code>co-&gt;trace</code> 数组。</p><p>Trace 对调试现场问题很有价值，例如记录某个 OD 变量、状态字、错误位随时间变化。但它会占用 RAM 和 CPU，并可能暴露内部变量，不适合所有量产固件默认打开。</p><h3 id="CO-CONFIG-TRACE-ENABLE"><a href="#CO-CONFIG-TRACE-ENABLE" class="headerlink" title="CO_CONFIG_TRACE_ENABLE"></a><code>CO_CONFIG_TRACE_ENABLE</code></h3><p>这个位启用 trace recorder。</p><h3 id="CO-CONFIG-TRACE-OWN-INTTYPES"><a href="#CO-CONFIG-TRACE-OWN-INTTYPES" class="headerlink" title="CO_CONFIG_TRACE_OWN_INTTYPES"></a><code>CO_CONFIG_TRACE_OWN_INTTYPES</code></h3><p>这个位表示不包含标准 <code>inttypes.h</code>，而由工程自己提供 <code>PRIu32</code>、<code>PRId32</code> 等格式宏。它解决某些嵌入式 C 库不完整或格式宏不兼容的问题。</p><h2 id="Debug：按模块打开调试输出宏"><a href="#Debug：按模块打开调试输出宏" class="headerlink" title="Debug：按模块打开调试输出宏"></a>Debug：按模块打开调试输出宏</h2><h3 id="CO-CONFIG-DEBUG"><a href="#CO-CONFIG-DEBUG" class="headerlink" title="CO_CONFIG_DEBUG"></a><code>CO_CONFIG_DEBUG</code></h3><p><code>CO_CONFIG_DEBUG</code> 配置 CANopenNode 不同部分的 debug message。它不改变协议行为，而是改变日志输出能力。</p><h3 id="CO-CONFIG-DEBUG-COMMON"><a href="#CO-CONFIG-DEBUG-COMMON" class="headerlink" title="CO_CONFIG_DEBUG_COMMON"></a><code>CO_CONFIG_DEBUG_COMMON</code></h3><p>这个位定义默认 <code>CO_DEBUG_COMMON(msg)</code> 宏。目标平台需要提供实际输出方式，例如串口、RTT、日志缓冲或空实现。</p><h3 id="CO-CONFIG-DEBUG-SDO-CLIENT"><a href="#CO-CONFIG-DEBUG-SDO-CLIENT" class="headerlink" title="CO_CONFIG_DEBUG_SDO_CLIENT"></a><code>CO_CONFIG_DEBUG_SDO_CLIENT</code></h3><p>这个位启用 SDO client 调试输出。用于分析远端读写、超时、abort 等问题。</p><h3 id="CO-CONFIG-DEBUG-SDO-SERVER"><a href="#CO-CONFIG-DEBUG-SDO-SERVER" class="headerlink" title="CO_CONFIG_DEBUG_SDO_SERVER"></a><code>CO_CONFIG_DEBUG_SDO_SERVER</code></h3><p>这个位启用 SDO server 调试输出。用于分析本节点 OD 访问、下载&#x2F;上传请求、abort 原因等问题。</p><h2 id="CO-new-中每个协议分支的实际结果"><a href="#CO-new-中每个协议分支的实际结果" class="headerlink" title="CO_new() 中每个协议分支的实际结果"></a>CO_new() 中每个协议分支的实际结果</h2><p>下面不是配置建议，而是代码阅读视角：每个 <code>#if</code> 分支最终会让 <code>CO_new()</code> 多做哪些事。</p><h3 id="NMT-Heartbeat-分支"><a href="#NMT-Heartbeat-分支" class="headerlink" title="NMT &#x2F; Heartbeat 分支"></a>NMT &#x2F; Heartbeat 分支</h3><p>触发条件是 <code>CO_GET_CNT(NMT) == 1U</code>。对象结果是 <code>co-&gt;NMT</code>。RX 结果是 NMT slave 接收槽位。TX 结果是 Heartbeat producer 发送槽位；若 <code>CO_CONFIG_NMT_MASTER</code> 打开，还会增加 NMT master 发送槽位。</p><p>协议含义是：节点可被 NMT master 控制状态，并周期发送自身状态。</p><h3 id="Heartbeat-consumer-分支"><a href="#Heartbeat-consumer-分支" class="headerlink" title="Heartbeat consumer 分支"></a>Heartbeat consumer 分支</h3><p>触发条件是 <code>CO_CONFIG_HB_CONS_ENABLE</code> 打开且 <code>CO_GET_CNT(HB_CONS) == 1U</code>。对象结果是 <code>co-&gt;HBcons</code> 和 <code>co-&gt;HBconsMonitoredNodes</code>。RX 数量来自 <code>ARR_1016</code>。</p><p>协议含义是：本节点监控多个远端 heartbeat producer。</p><h3 id="Node-Guarding-分支"><a href="#Node-Guarding-分支" class="headerlink" title="Node Guarding 分支"></a>Node Guarding 分支</h3><p>触发条件是 slave&#x2F;master 对应 enable 位打开。对象结果是 <code>co-&gt;NGslave</code> 或 <code>co-&gt;NGmaster</code>。</p><p>协议含义是：兼容旧式 master 轮询型节点监控。</p><h3 id="Emergency-分支"><a href="#Emergency-分支" class="headerlink" title="Emergency 分支"></a>Emergency 分支</h3><p>触发条件是 <code>CO_GET_CNT(EM) == 1U</code>。对象结果是 <code>co-&gt;em</code>。若 producer 或 history 打开，会分配 <code>co-&gt;em_fifo</code>。RX&#x2F;TX 由 consumer&#x2F;producer 位决定。</p><p>协议含义是：本节点可以发送错误事件，也可以接收远端错误事件。</p><h3 id="SDO-server-分支"><a href="#SDO-server-分支" class="headerlink" title="SDO server 分支"></a>SDO server 分支</h3><p>触发条件是 <code>CO_GET_CNT(SDO_SRV) &gt; 0U</code>。对象结果是 <code>co-&gt;SDOserver</code> 数组。每个 server 一个 RX、一个 TX。</p><p>协议含义是：远端 client 可以访问本节点对象字典。</p><h3 id="SDO-client-分支"><a href="#SDO-client-分支" class="headerlink" title="SDO client 分支"></a>SDO client 分支</h3><p>触发条件是 <code>CO_CONFIG_SDO_CLI_ENABLE</code> 打开且 <code>CO_GET_CNT(SDO_CLI) &gt; 0U</code>。对象结果是 <code>co-&gt;SDOclient</code> 数组。每个 client 一个 RX、一个 TX。</p><p>协议含义是：本节点可以主动访问远端对象字典。</p><h3 id="TIME-分支"><a href="#TIME-分支" class="headerlink" title="TIME 分支"></a>TIME 分支</h3><p>触发条件是 <code>CO_CONFIG_TIME_ENABLE</code> 打开且 <code>CO_GET_CNT(TIME) == 1U</code>。对象结果是 <code>co-&gt;TIME</code>。consumer 增加 RX，producer 增加 TX。</p><p>协议含义是：接收或发布网络时间戳。</p><h3 id="SYNC-分支"><a href="#SYNC-分支" class="headerlink" title="SYNC 分支"></a>SYNC 分支</h3><p>触发条件是 <code>CO_CONFIG_SYNC_ENABLE</code> 打开且 <code>CO_GET_CNT(SYNC) == 1U</code>。对象结果是 <code>co-&gt;SYNC</code>。consumer 增加 RX，producer 增加 TX。</p><p>协议含义是：接收或发布同步节拍。</p><h3 id="RPDO-分支"><a href="#RPDO-分支" class="headerlink" title="RPDO 分支"></a>RPDO 分支</h3><p>触发条件是 <code>CO_CONFIG_RPDO_ENABLE</code> 打开且 <code>CO_GET_CNT(RPDO) &gt; 0U</code>。对象结果是 <code>co-&gt;RPDO</code>。RX 数量来自 <code>CNT_RPDO</code>。</p><p>协议含义是：本节点消费过程数据。</p><h3 id="TPDO-分支"><a href="#TPDO-分支" class="headerlink" title="TPDO 分支"></a>TPDO 分支</h3><p>触发条件是 <code>CO_CONFIG_TPDO_ENABLE</code> 打开且 <code>CO_GET_CNT(TPDO) &gt; 0U</code>。对象结果是 <code>co-&gt;TPDO</code>。TX 数量来自 <code>CNT_TPDO</code>。</p><p>协议含义是：本节点生产过程数据。</p><h3 id="LEDs-分支"><a href="#LEDs-分支" class="headerlink" title="LEDs 分支"></a>LEDs 分支</h3><p>触发条件是 <code>CO_CONFIG_LEDS_ENABLE</code> 打开且 <code>CO_GET_CNT(LEDS) == 1U</code>。对象结果是 <code>co-&gt;LEDs</code>。</p><p>协议含义是：计算 CANopen 标准红绿 LED 状态，不直接驱动 GPIO。</p><h3 id="GFC-分支"><a href="#GFC-分支" class="headerlink" title="GFC 分支"></a>GFC 分支</h3><p>触发条件是 <code>CO_CONFIG_GFC_ENABLE</code> 打开且 <code>CO_GET_CNT(GFC) == 1</code>。对象结果是 <code>co-&gt;GFC</code>。RX 和 TX 各增加一个。</p><p>协议含义是：全局安全命令的 producer&#x2F;consumer 支撑。</p><h3 id="SRDO-分支"><a href="#SRDO-分支" class="headerlink" title="SRDO 分支"></a>SRDO 分支</h3><p>触发条件是 <code>CO_CONFIG_SRDO_ENABLE</code> 打开且 <code>CO_GET_CNT(SRDO) &gt; 0U</code>。对象结果是 <code>co-&gt;SRDOGuard</code> 和 <code>co-&gt;SRDO</code>。RX&#x2F;TX 都是 <code>CNT_SRDO * 2</code>。</p><p>协议含义是：安全相关过程数据，每个对象消耗成对 CAN 报文槽位。</p><h3 id="LSS-slave-分支"><a href="#LSS-slave-分支" class="headerlink" title="LSS slave 分支"></a>LSS slave 分支</h3><p>触发条件是 <code>CO_CONFIG_LSS_SLAVE</code> 打开且 <code>CO_GET_CNT(LSS_SLV) == 1U</code>。对象结果是 <code>co-&gt;LSSslave</code>。RX&#x2F;TX 各一个。</p><p>协议含义是：本节点可被 LSS master 识别和配置 Node-ID&#x2F;bit-rate。</p><h3 id="LSS-master-分支"><a href="#LSS-master-分支" class="headerlink" title="LSS master 分支"></a>LSS master 分支</h3><p>触发条件是 <code>CO_CONFIG_LSS_MASTER</code> 打开且 <code>CO_GET_CNT(LSS_MST) == 1U</code>。对象结果是 <code>co-&gt;LSSmaster</code>。RX&#x2F;TX 各一个。</p><p>协议含义是：本节点可发现和配置其他 LSS slave。</p><h3 id="Gateway-ASCII-分支"><a href="#Gateway-ASCII-分支" class="headerlink" title="Gateway ASCII 分支"></a>Gateway ASCII 分支</h3><p>触发条件是 <code>CO_CONFIG_GTW_ASCII</code> 打开且 <code>CO_GET_CNT(GTWA) == 1U</code>。对象结果是 <code>co-&gt;gtwa</code>。</p><p>协议含义是：通过文本接口访问 CANopen 网络，底层依赖 SDO client、NMT master、LSS master、FIFO 等能力组合。</p><h3 id="Trace-分支"><a href="#Trace-分支" class="headerlink" title="Trace 分支"></a>Trace 分支</h3><p>触发条件是 <code>CO_CONFIG_TRACE_ENABLE</code> 打开且 <code>CO_GET_CNT(TRACE) &gt; 0</code>。对象结果是 <code>co-&gt;trace</code>。</p><p>协议含义是：非标准变量记录能力，用于观测和调试。</p><h2 id="宏、协议对象和对象字典的三层关系"><a href="#宏、协议对象和对象字典的三层关系" class="headerlink" title="宏、协议对象和对象字典的三层关系"></a>宏、协议对象和对象字典的三层关系</h2><p>宏只决定“代码是否存在”。对象数量决定“对象是否创建”。对象字典决定“协议行为参数是什么”。这三层必须同时成立。</p><p>例如：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> CO_CONFIG_SYNC (CO_CONFIG_SYNC_ENABLE | CO_CONFIG_SYNC_PRODUCER)</span></span><br></pre></td></tr></table></figure><p>这只表示 SYNC 相关代码被编译进来，并允许 producer。是否真的分配 <code>co-&gt;SYNC</code>，还要看 <code>CO_GET_CNT(SYNC)</code>。SYNC COB-ID、周期、同步窗口和计数器溢出值，还要看 OD 0x1005、0x1006、0x1007、0x1019。</p><p>再例如：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> CO_CONFIG_PDO (CO_CONFIG_RPDO_ENABLE | CO_CONFIG_TPDO_ENABLE | CO_CONFIG_PDO_SYNC_ENABLE)</span></span><br></pre></td></tr></table></figure><p>这只表示 RPDO&#x2F;TPDO 代码和同步 PDO 语义被编译进来。实际有几个 PDO，要看 <code>CNT_RPDO</code>、<code>CNT_TPDO</code>。每个 PDO 发什么、收什么、何时触发，要看 0x1400&#x2F;0x1600、0x1800&#x2F;0x1A00 系列对象。</p><p>这也是阅读 <code>CO_new()</code> 时最重要的结论：不要把 <code>CO_CONFIG_xxx</code> 当成对象实例；不要把 OD 条目当成功能已启用；不要把 <code>CO_new()</code> 当成协议初始化的全部。<code>CO_new()</code> 创建容器和对象，后续 <code>CO_CANopenInit()</code>、<code>CO_CANopenInitPDO()</code>、<code>CO_CANopenInitSRDO()</code> 等初始化函数才会把这些对象与 OD 条目、CAN-ID、nodeId、CAN module 下标真正绑定起来。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;meta name=&quot;referrer&quot; content=&quot;no-referrer&quot; /&gt;

&lt;h1 id=&quot;CANopenNode-CO-new-与-CO-config-h-宏机制详解&quot;&gt;&lt;a href=&quot;#CANopenNode-CO-new-与-CO-config-h-宏</summary>
      
    
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/categories/canopen/"/>
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/tags/canopen/"/>
    
  </entry>
  
  <entry>
    <title>CANopen 网络拓扑限制</title>
    <link href="https://wdfk-prog.space/posts/4f21cda1/"/>
    <id>https://wdfk-prog.space/posts/4f21cda1/</id>
    <published>2026-06-26T06:00:28.000Z</published>
    <updated>2026-06-28T02:53:29.886Z</updated>
    
    <content type="html"><![CDATA[<meta name="referrer" content="no-referrer" /><h1 id="从-Node-ID-到线长：读懂-CAN-CANopen-网络拓扑限制"><a href="#从-Node-ID-到线长：读懂-CAN-CANopen-网络拓扑限制" class="headerlink" title="从 Node-ID 到线长：读懂 CAN&#x2F;CANopen 网络拓扑限制"></a>从 Node-ID 到线长：读懂 CAN&#x2F;CANopen 网络拓扑限制</h1><p><img src="https://i-blog.csdnimg.cn/direct/b915bdc3c90348998420c7620de55400.png" alt="在这里插入图片描述"></p><p>@[toc]</p><h2 id="结论先行"><a href="#结论先行" class="headerlink" title="结论先行"></a>结论先行</h2><p>“CANopen 逻辑上限制到 128，物理上一段常见只能挂 64 个节点；1 Mbit&#x2F;s 可到 25 m，50 kbit&#x2F;s 可到 1000 m”这段话把两类约束放在了一起：一类是 CANopen 协议层的地址空间，另一类是 CAN 物理层的线缆、终端、收发器负载和传播延迟。两者都影响网络规模，但判断方式完全不同。</p><ul><li>CANopen 的 Node-ID 是逻辑地址问题，7 bit 编码提供 0<del>127 的编号空间；普通节点通常使用 1</del>127，0 用于广播或特殊用途。</li><li>CAN 的线性拓扑是物理层问题。一个 CAN 总线段应尽量保持主干线连续，两端各 120 Ω 终端，节点通过短支线接入。</li><li>线长受位速率限制。速率越高，单个 bit 的时间越短，信号传播、收发器延迟和采样裕量越紧张，所以允许总线越短。</li><li>节点数量受收发器和线缆负载限制。协议允许的地址数量不等于一段总线的可靠驱动能力。</li><li>中继器可以在低速下扩展网络规模和构造分段&#x2F;树形结构，但中继器本身也会引入传播延迟。</li></ul><p><img src="https://i-blog.csdnimg.cn/direct/76efff45e69841bf900508b2d0f05489.png" alt="在这里插入图片描述"></p><h2 id="一、先把“逻辑网络”和“物理总线”分开"><a href="#一、先把“逻辑网络”和“物理总线”分开" class="headerlink" title="一、先把“逻辑网络”和“物理总线”分开"></a>一、先把“逻辑网络”和“物理总线”分开</h2><p>CANopen 是建立在 CAN 之上的高层通信体系，主要规定通信对象、网络管理、设备对象字典和应用&#x2F;设备配置方式；底层数据链路和物理传输仍然依赖 CAN 及其物理层实现。工程上讨论“最多多少节点”时，必须先说明是在讲协议地址空间，还是在讲单段总线的电气承载能力。</p><p>协议地址空间回答的是“这些设备能否被区分”；物理总线能力回答的是“这些设备挂上去以后，波形、仲裁和采样是否仍可靠”。前者由协议字段决定，后者由收发器、线缆、终端、支线、隔离器件、速率和 EMC 环境共同决定。</p><p><img src="https://i-blog.csdnimg.cn/direct/949974be314d4c2f8b1d24111c5a0aab.png" alt="在这里插入图片描述"></p><h2 id="二、为什么-CANopen-会出现-128、127-和-0"><a href="#二、为什么-CANopen-会出现-128、127-和-0" class="headerlink" title="二、为什么 CANopen 会出现 128、127 和 0"></a>二、为什么 CANopen 会出现 128、127 和 0</h2><p>CANopen 的预定义连接集通常把 11 bit CAN 标识符拆成两部分：功能码和 Node-ID。Node-ID 占 7 bit，因此编码空间共有 128 种，也就是 0~127。</p><p>但普通设备节点通常不是 0<del>127 共 128 个，而是 1</del>127 共 127 个。Node-ID 0 通常用于广播或特殊用途，例如网络管理命令可以面向所有节点。因此原文中的“logically limited by CANopen to 128”更准确地说是：Node-ID 字段宽度提供 128 种编码，但普通可配置节点通常为 127 个。</p><table><thead><tr><th>说法</th><th>含义</th><th>工程解释</th></tr></thead><tbody><tr><td>7 bit Node-ID</td><td>2^7 &#x3D; 128 种编码</td><td>这是字段宽度，不是普通设备数量</td></tr><tr><td>Node-ID 1~127</td><td>普通设备节点编号</td><td>每个节点应唯一</td></tr><tr><td>Node-ID 0</td><td>广播或特殊用途</td><td>不作为普通设备地址使用</td></tr><tr><td>“最多 128”</td><td>常见的简化表达</td><td>容易把编码空间和普通节点数混在一起</td></tr></tbody></table><h2 id="三、为什么-CAN-物理拓扑强调“线性”"><a href="#三、为什么-CAN-物理拓扑强调“线性”" class="headerlink" title="三、为什么 CAN 物理拓扑强调“线性”"></a>三、为什么 CAN 物理拓扑强调“线性”</h2><p>CAN 使用差分总线传输。为了让总线阻抗连续并减少反射，一个 CAN 物理段通常按主干线布线，两端各放一个 120 Ω 终端电阻，节点用短支线接入。这里的“线性”不是指设备必须在空间里排成一条直线，而是指电气拓扑尽量接近一条连续传输线。</p><p>星形、长分支和随意树形的问题在于：每个分叉点都会改变阻抗，长支线会在信号边沿产生反射。反射叠加到有效信号上，可能导致接收器在采样点附近误判 dominant&#x2F;recessive 状态。速率越高，位时间越短，反射留给系统恢复的时间越少，所以高速网络对支线和拓扑更敏感。</p><p>因此，单段 CAN 设计的基本原则是：主干尽量连续，终端只在物理两端，支线尽量短，不把多个长分支直接并在一个中心点上。</p><h2 id="四、为什么一段总线常见只能挂有限节点"><a href="#四、为什么一段总线常见只能挂有限节点" class="headerlink" title="四、为什么一段总线常见只能挂有限节点"></a>四、为什么一段总线常见只能挂有限节点</h2><p>CANopen 的 Node-ID 空间允许 127 个普通节点，但这并不意味着一个物理段就能可靠挂满 127 个收发器。每个节点都通过 CAN 收发器连接 CAN_H&#x2F;CAN_L，会给总线带来输入电阻、输入电容、ESD&#x2F;保护器件寄生参数和支线长度。节点越多，总线负载越重，边沿越慢，抗干扰裕量越小。</p><p>不同资料和收发器会给出不同节点数量。例如，高速 CAN 标准相关资料常以 1 Mbit&#x2F;s、40 m、30 个节点作为基准示例；现代低负载收发器可能支持更多节点。原文中的“64 nodes in one network segment”应理解为某类驱动器&#x2F;收发器条件下的工程能力，而不是 CANopen 协议本身的硬限制。</p><p>工程判断应以目标收发器 datasheet 为准，同时核算：</p><ol><li>总线两端终端是否正确；</li><li>全部节点的等效输入负载是否在收发器驱动能力内；</li><li>线缆阻抗、衰减和传播延迟是否满足速率要求；</li><li>支线长度和累计支线长度是否符合设计表；</li><li>是否存在隔离器、共模电感、防护器件带来的额外延迟和寄生参数。</li></ol><h2 id="五、为什么-1-Mbit-s-只能约-25-m，而-50-kbit-s-可以约-1000-m"><a href="#五、为什么-1-Mbit-s-只能约-25-m，而-50-kbit-s-可以约-1000-m" class="headerlink" title="五、为什么 1 Mbit&#x2F;s 只能约 25 m，而 50 kbit&#x2F;s 可以约 1000 m"></a>五、为什么 1 Mbit&#x2F;s 只能约 25 m，而 50 kbit&#x2F;s 可以约 1000 m</h2><p>CAN 的仲裁和 ACK 机制要求节点在发送时也能读回总线状态。两个最远节点之间的信号传播，以及收发器、隔离器、控制器和采样点配置的延迟，都必须落在一个 bit 的时序预算内。</p><p>位速率越高，单 bit 时间越短：</p><table><thead><tr><th align="right">位速率</th><th align="right">单 bit 时间</th></tr></thead><tbody><tr><td align="right">1 Mbit&#x2F;s</td><td align="right">1 µs</td></tr><tr><td align="right">500 kbit&#x2F;s</td><td align="right">2 µs</td></tr><tr><td align="right">250 kbit&#x2F;s</td><td align="right">4 µs</td></tr><tr><td align="right">125 kbit&#x2F;s</td><td align="right">8 µs</td></tr><tr><td align="right">50 kbit&#x2F;s</td><td align="right">20 µs</td></tr></tbody></table><p>当速率从 50 kbit&#x2F;s 提高到 1 Mbit&#x2F;s，单 bit 时间从 20 µs 缩短到 1 µs，时序预算压缩为原来的 1&#x2F;20。此时同样的线缆传播延迟、收发器延迟和反射恢复时间会占据更大比例，所以必须缩短总线。</p><p>CAN in Automation 给出的 CANopen CC 典型位时序表可以直接体现这种关系：</p><table><thead><tr><th align="right">位速率</th><th align="right">最大总线长度</th><th align="right">最大未终端支线长度</th><th align="right">累计支线长度</th></tr></thead><tbody><tr><td align="right">1 Mbit&#x2F;s</td><td align="right">25 m</td><td align="right">1.5 m</td><td align="right">7.5 m</td></tr><tr><td align="right">800 kbit&#x2F;s</td><td align="right">50 m</td><td align="right">2.5 m</td><td align="right">12.5 m</td></tr><tr><td align="right">500 kbit&#x2F;s</td><td align="right">100 m</td><td align="right">5.5 m</td><td align="right">27.5 m</td></tr><tr><td align="right">250 kbit&#x2F;s</td><td align="right">250 m</td><td align="right">11 m</td><td align="right">55 m</td></tr><tr><td align="right">125 kbit&#x2F;s</td><td align="right">500 m</td><td align="right">22 m</td><td align="right">110 m</td></tr><tr><td align="right">50 kbit&#x2F;s</td><td align="right">1000 m</td><td align="right">55 m</td><td align="right">275 m</td></tr><tr><td align="right">20 kbit&#x2F;s</td><td align="right">2500 m</td><td align="right">137.5 m</td><td align="right">687.5 m</td></tr><tr><td align="right">10 kbit&#x2F;s</td><td align="right">5000 m</td><td align="right">275 m</td><td align="right">1375 m</td></tr></tbody></table><p><img src="https://i-blog.csdnimg.cn/direct/64ed19ce1e684cbd882e6d6596490598.png" alt="在这里插入图片描述"></p><p>这些值不是任意规定，而是把传播延迟、采样点、线缆参数和支线反射折中后的工程表。实际项目还要考虑线缆质量、隔离器延迟、收发器型号、供电容差和环境噪声。</p><h2 id="六、为什么低速下可以用-repeater-扩展网络"><a href="#六、为什么低速下可以用-repeater-扩展网络" class="headerlink" title="六、为什么低速下可以用 repeater 扩展网络"></a>六、为什么低速下可以用 repeater 扩展网络</h2><p>中继器可以把一个 CAN 网络拆成多个物理段。每一段重新驱动信号、重新满足终端和支线约束，并降低单段节点负载。因此在低速场景下，可以通过中继器扩展总线长度，或者构造分段式、近似树形的网络。</p><p>但中继器不是“免费”的。它会增加传播延迟，而 CAN 仲裁、ACK 和采样都要消耗同一份 bit 时间预算。速率越高，时序预算越紧，留给中继器和长线缆的空间越小。因此“低速下可以通过 repeater 做树形”并不意味着 CAN 单段天然支持任意树形，也不意味着可以无限级联中继器。</p><p><img src="https://i-blog.csdnimg.cn/direct/530bcd2182d34d73aa9f85e5ad95df04.png" alt="在这里插入图片描述"></p><h2 id="七、工程落地时应怎样取值"><a href="#七、工程落地时应怎样取值" class="headerlink" title="七、工程落地时应怎样取值"></a>七、工程落地时应怎样取值</h2><p>如果只是短线调试，两块板或一块板加 USB-CAN 适配器，通常更关注终端电阻、波特率一致和接线正确。若进入设备网络设计，应保守处理线长、节点数和支线：</p><table><thead><tr><th>设计项</th><th>建议做法</th><th>原因</th></tr></thead><tbody><tr><td>拓扑</td><td>主干线 + 短支线</td><td>降低阻抗突变和反射</td></tr><tr><td>终端</td><td>只在物理两端各 120 Ω</td><td>匹配线缆特性阻抗</td></tr><tr><td>位速率</td><td>按线长反推，不盲目取最高速</td><td>高速会压缩传播延迟预算</td></tr><tr><td>节点数</td><td>按收发器 datasheet 和总负载核算</td><td>协议地址数量不等于驱动能力</td></tr><tr><td>支线</td><td>高速时尽量短</td><td>长支线会产生反射</td></tr><tr><td>中继器</td><td>低速、长距离或大节点数时再考虑</td><td>中继器降低负载，但增加延迟</td></tr><tr><td>EMC</td><td>评估屏蔽、共模电感、隔离和保护器件</td><td>可靠性通常受系统级细节影响</td></tr></tbody></table><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>这段拓扑说明真正想表达的是：CANopen 给了设备一个逻辑地址体系，但 CAN 网络能跑多远、能挂多少节点，主要由物理层决定。Node-ID 的 7 bit 编码解释了 128&#x2F;127 的来源；线性主干、两端终端和短支线解释了为什么 CAN 不适合任意星形；位时间和传播延迟解释了为什么 1 Mbit&#x2F;s 只能短距离，而 50 kbit&#x2F;s 可以做到千米级；中继器则是在低速和分段设计下扩展网络的工程手段。</p><p>因此，正确的设计顺序不是先问“CANopen 最多多少节点”，而是先确定距离、速率、节点数、收发器和布线方式，再检查这些条件是否同时满足 CAN 物理层和 CANopen 地址层的约束。</p><h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ol><li>CAN in Automation, “CANopen lower layers,” CANopen CC bit timing table, bus length, stub length, and physical layer notes.</li><li>CAN in Automation, “CANopen CC – The standardized embedded network,” CANopen as a CAN-based communication system and lower-layer overview.</li><li>Open Vehicles Documentation, “CANopen Basics,” node IDs 1–127 and node address 0 for broadcasts.</li><li>Texas Instruments, “Controller Area Network Physical Layer Requirements,” SLLA270, physical topology, 120 Ω termination, suggested cable length vs. signaling rate, and delay-budget discussion.</li><li>Microchip Support, “MCP2561&#x2F;MCP25625 — What is the maximum number of nodes on the CAN bus?”, node count as a transceiver-drive&#x2F;load question.</li></ol>]]></content>
    
    
      
      
    <summary type="html">&lt;meta name=&quot;referrer&quot; content=&quot;no-referrer&quot; /&gt;

&lt;h1 id=&quot;从-Node-ID-到线长：读懂-CAN-CANopen-网络拓扑限制&quot;&gt;&lt;a href=&quot;#从-Node-ID-到线长：读懂-CAN-CANopen-网络拓扑限制&quot; </summary>
      
    
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/categories/canopen/"/>
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/tags/canopen/"/>
    
  </entry>
  
  <entry>
    <title>Lely CANopen 交叉构建说明</title>
    <link href="https://wdfk-prog.space/posts/0/"/>
    <id>https://wdfk-prog.space/posts/0/</id>
    <published>2026-06-25T08:54:06.000Z</published>
    <updated>2026-06-25T08:54:51.078Z</updated>
    
    <content type="html"><![CDATA[<meta name="referrer" content="no-referrer" /><h1 id="Lely-CANopen-交叉构建说明：为什么执行-make-install-DESTDIR-PWD-stage-，以及-lib-pkgconfig-的作用"><a href="#Lely-CANopen-交叉构建说明：为什么执行-make-install-DESTDIR-PWD-stage-，以及-lib-pkgconfig-的作用" class="headerlink" title="Lely CANopen 交叉构建说明：为什么执行 make install DESTDIR=&quot;${PWD}/stage&quot;，以及 lib/pkgconfig 的作用"></a>Lely CANopen 交叉构建说明：为什么执行 <code>make install DESTDIR=&quot;${PWD}/stage&quot;</code>，以及 <code>lib/pkgconfig</code> 的作用</h1><p>@[toc]</p><p><img src="https://i-blog.csdnimg.cn/direct/e1a6fb0ea0e3442180526cd78cbc8031.png" alt="在这里插入图片描述"></p><h2 id="结论"><a href="#结论" class="headerlink" title="结论"></a>结论</h2><p>在 Lely CANopen &#x2F; lely-core 的交叉编译流程中：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">make -j<span class="string">&quot;<span class="subst">$(nproc)</span>&quot;</span></span><br><span class="line">make install DESTDIR=<span class="string">&quot;<span class="variable">$&#123;PWD&#125;</span>/stage&quot;</span></span><br></pre></td></tr></table></figure><p>这两条命令不是重复动作。它们分别解决两个问题：</p><ol><li><code>make</code>：把源码编译、链接成目标平台可用的库和工具。</li><li><code>make install DESTDIR=&quot;${PWD}/stage&quot;</code>：把已构建产物按最终安装目录布局复制到 staging 目录，便于检查、打包、部署和给后续应用交叉编译使用。</li></ol><p>本次构建还安装了 <code>lib/pkgconfig/*.pc</code> 文件。它们不是库本体，而是给 <code>pkg-config</code> 查询的构建元数据，用来自动输出依赖 Lely 库时需要的 <code>-I</code>、<code>-L</code>、<code>-l</code> 等编译和链接参数。</p><h2 id="先看完整流程"><a href="#先看完整流程" class="headerlink" title="先看完整流程"></a>先看完整流程</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[加载 Yocto SDK 环境] --&gt; B[配置 lely-core: ../configure]</span><br><span class="line">    B --&gt; C[生成 Makefile / config.h / libtool]</span><br><span class="line">    C --&gt; D[make -j]</span><br><span class="line">    D --&gt; E[编译 .lo / .o]</span><br><span class="line">    E --&gt; F[链接 .la / .so / .a / 工具]</span><br><span class="line">    F --&gt; G[make install DESTDIR=stage]</span><br><span class="line">    G --&gt; H[安装 include/lely 头文件]</span><br><span class="line">    G --&gt; I[安装 lib 动态库/静态库]</span><br><span class="line">    G --&gt; J[安装 lib/pkgconfig/*.pc]</span><br><span class="line">    G --&gt; K[安装 bin 工具]</span><br><span class="line">    G --&gt; L[安装 etc 默认 DCF]</span><br><span class="line">    J --&gt; M[后续应用通过 pkg-config 获取 cflags/libs]</span><br><span class="line">    H --&gt; N[形成可打包/可部署 staging 目录]</span><br><span class="line">    I --&gt; N</span><br><span class="line">    K --&gt; N</span><br><span class="line">    L --&gt; N</span><br></pre></td></tr></table></figure><p>对应关系可以简化成：</p><table><thead><tr><th>阶段</th><th>命令</th><th>主要作用</th><th>输出位置</th></tr></thead><tbody><tr><td>SDK 环境</td><td><code>source &lt;YOCTO_SDK&gt;/environment-setup-armv8a-poky-linux</code></td><td>设置交叉编译器、sysroot、CFLAGS、LDFLAGS 等</td><td>当前 shell 环境</td></tr><tr><td>配置</td><td><code>../configure --host=... --prefix=...</code></td><td>生成适合目标平台和功能裁剪的 Makefile</td><td><code>&lt;BUILD_DIR&gt;</code></td></tr><tr><td>构建</td><td><code>make -j&quot;$(nproc)&quot;</code></td><td>编译对象文件并链接库&#x2F;工具</td><td><code>&lt;BUILD_DIR&gt;/src</code>、<code>&lt;BUILD_DIR&gt;/tools</code> 等</td></tr><tr><td>安装到 staging</td><td><code>make install DESTDIR=&quot;${PWD}/stage&quot;</code></td><td>把构建产物复制成最终安装布局</td><td><code>&lt;LELY_STAGE&gt;&lt;INSTALL_PREFIX&gt;</code></td></tr><tr><td>查询依赖参数</td><td><code>pkg-config --cflags/--libs liblely-coapp</code></td><td>给后续应用编译输出头文件路径和链接参数</td><td>标准输出</td></tr></tbody></table><h2 id="为什么要执行-make-install-DESTDIR-PWD-stage"><a href="#为什么要执行-make-install-DESTDIR-PWD-stage" class="headerlink" title="为什么要执行 make install DESTDIR=&quot;${PWD}/stage&quot;"></a>为什么要执行 <code>make install DESTDIR=&quot;${PWD}/stage&quot;</code></h2><h3 id="DESTDIR-是-staging-根目录，不是最终安装前缀"><a href="#DESTDIR-是-staging-根目录，不是最终安装前缀" class="headerlink" title="DESTDIR 是 staging 根目录，不是最终安装前缀"></a><code>DESTDIR</code> 是 staging 根目录，不是最终安装前缀</h3><p>GNU Automake 文档把 <code>DESTDIR</code> 定义为 staged installation。包仍然按最终路径配置，例如 <code>--prefix=/usr</code>；执行 <code>make install DESTDIR=/tmp/stage</code> 时，安装动作会被重定向到 <code>/tmp/stage/usr/...</code>，便于检查安装结果、生成文件清单或打包部署。</p><p>本次流程可以抽象成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">DESTDIR          = &lt;LELY_STAGE&gt;</span><br><span class="line">prefix           = &lt;INSTALL_PREFIX&gt;</span><br><span class="line">实际写入路径     = &lt;LELY_STAGE&gt;&lt;INSTALL_PREFIX&gt;</span><br></pre></td></tr></table></figure><p>例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">&lt;LELY_STAGE&gt;&lt;INSTALL_PREFIX&gt;/include</span><br><span class="line">&lt;LELY_STAGE&gt;&lt;INSTALL_PREFIX&gt;/lib</span><br><span class="line">&lt;LELY_STAGE&gt;&lt;INSTALL_PREFIX&gt;/lib/pkgconfig</span><br><span class="line">&lt;LELY_STAGE&gt;&lt;INSTALL_PREFIX&gt;/bin</span><br><span class="line">&lt;LELY_STAGE&gt;&lt;INSTALL_PREFIX&gt;/etc</span><br></pre></td></tr></table></figure><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    A[configure --prefix=&lt;INSTALL_PREFIX&gt;] --&gt; C[记录最终安装路径语义]</span><br><span class="line">    B[make install DESTDIR=&lt;LELY_STAGE&gt;] --&gt; D[临时安装根]</span><br><span class="line">    C --&gt; E[&lt;LELY_STAGE&gt;&lt;INSTALL_PREFIX&gt;]</span><br><span class="line">    D --&gt; E</span><br></pre></td></tr></table></figure><p>这一步的价值是：</p><ol><li>不把 AArch64 目标库安装到 x86_64 构建主机的系统目录。</li><li>可以直接检查最终会安装哪些文件。</li><li>可以把 <code>&lt;LELY_STAGE&gt;</code> 打包进 SDK 或 rootfs 生成流程。</li><li>可以让后续应用把 <code>&lt;LELY_STAGE&gt;</code> 当成 Lely 的临时 sysroot&#x2F;staging root。</li><li>可以保留 <code>--prefix</code> 的最终部署路径语义。</li></ol><h3 id="DESTDIR-和-prefix-不要混用"><a href="#DESTDIR-和-prefix-不要混用" class="headerlink" title="DESTDIR 和 prefix 不要混用"></a><code>DESTDIR</code> 和 <code>prefix</code> 不要混用</h3><p>二者职责不同：</p><table><thead><tr><th>项目</th><th>作用</th><th align="right">是否进入 <code>.pc</code> &#x2F; <code>.la</code> 等安装元数据</th><th>典型值</th></tr></thead><tbody><tr><td><code>--prefix</code></td><td>最终安装前缀</td><td align="right">是</td><td><code>/usr</code>、<code>/usr/local</code>、<code>&lt;INSTALL_PREFIX&gt;</code></td></tr><tr><td><code>DESTDIR</code></td><td>临时安装根目录</td><td align="right">否，通常只用于安装时重定向</td><td><code>&lt;BUILD_DIR&gt;/stage</code></td></tr></tbody></table><p>如果目标板最终运行时也使用 <code>&lt;INSTALL_PREFIX&gt;</code>，当前配置是匹配的。</p><p>如果最终要部署到目标板的 <code>/usr</code>，更常见的配置应改成：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">../configure \</span><br><span class="line">    --host=aarch64-poky-linux \</span><br><span class="line">    --prefix=/usr \</span><br><span class="line">    --disable-python \</span><br><span class="line">    --disable-cython \</span><br><span class="line">    --disable-tests \</span><br><span class="line">    --disable-unit-tests</span><br><span class="line"></span><br><span class="line">make -j<span class="string">&quot;<span class="subst">$(nproc)</span>&quot;</span></span><br><span class="line">make install DESTDIR=<span class="string">&quot;<span class="variable">$&#123;PWD&#125;</span>/stage&quot;</span></span><br></pre></td></tr></table></figure><p>这样安装树会是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">stage/usr/include</span><br><span class="line">stage/usr/lib</span><br><span class="line">stage/usr/lib/pkgconfig</span><br><span class="line">stage/usr/bin</span><br></pre></td></tr></table></figure><p>而不是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">stage/&lt;INSTALL_PREFIX&gt;/include</span><br><span class="line">stage/&lt;INSTALL_PREFIX&gt;/lib</span><br><span class="line">stage/&lt;INSTALL_PREFIX&gt;/lib/pkgconfig</span><br><span class="line">stage/&lt;INSTALL_PREFIX&gt;/bin</span><br></pre></td></tr></table></figure><h2 id="make-构建了什么"><a href="#make-构建了什么" class="headerlink" title="make 构建了什么"></a><code>make</code> 构建了什么</h2><p>日志显示 <code>make -j&quot;$(nproc)&quot;</code> 进入了 <code>doc</code>、<code>include</code>、<code>src</code>、<code>python</code>、<code>pkgconfig</code> 和 <code>tools</code> 等目录。真正发生大量编译和链接的是 <code>src</code> 和 <code>tools</code>。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line">graph TD</span><br><span class="line">    libc[liblely-libc] --&gt; util[liblely-util]</span><br><span class="line">    libc --&gt; tap[liblely-tap]</span><br><span class="line">    libc --&gt; can[liblely-can]</span><br><span class="line">    util --&gt; can</span><br><span class="line">    libc --&gt; co[liblely-co]</span><br><span class="line">    util --&gt; co</span><br><span class="line">    can --&gt; co</span><br><span class="line">    libc --&gt; ev[liblely-ev]</span><br><span class="line">    util --&gt; ev</span><br><span class="line">    can --&gt; io[liblely-io]</span><br><span class="line">    util --&gt; io</span><br><span class="line">    libc --&gt; io2[liblely-io2]</span><br><span class="line">    util --&gt; io2</span><br><span class="line">    can --&gt; io2</span><br><span class="line">    ev --&gt; io2</span><br><span class="line">    co --&gt; coapp[liblely-coapp]</span><br><span class="line">    io2 --&gt; coapp</span><br><span class="line">    ev --&gt; coapp</span><br><span class="line">    co --&gt; tools[can2udp / coctl / cocat / cocatd / dcf2c]</span><br><span class="line">    io --&gt; tools</span><br></pre></td></tr></table></figure><h3 id="主要库产物"><a href="#主要库产物" class="headerlink" title="主要库产物"></a>主要库产物</h3><table><thead><tr><th>构建产物</th><th>类型</th><th>作用</th></tr></thead><tbody><tr><td><code>liblely-libc.la</code></td><td>兼容层库</td><td>提供 C11、POSIX 以及部分 C++ 兼容能力，是多个 Lely 库的基础。</td></tr><tr><td><code>liblely-tap.la</code></td><td>测试辅助库</td><td>TAP 测试协议实现，主要服务测试套件。</td></tr><tr><td><code>liblely-util.la</code></td><td>工具基础库</td><td>提供数据结构、错误处理、诊断、内存&#x2F;文件缓冲等公共功能。</td></tr><tr><td><code>liblely-can.la</code></td><td>CAN 基础库</td><td>提供 CAN 帧、CAN 网络对象和帧处理辅助接口。</td></tr><tr><td><code>liblely-co.la</code></td><td>CANopen 协议栈库</td><td>提供对象字典、NMT、PDO、SDO、SYNC、EMCY、LSS 等 CANopen 服务。</td></tr><tr><td><code>liblely-io.la</code></td><td>旧 I&#x2F;O 库</td><td>旧版 I&#x2F;O 抽象。Lely 文档中该库属于 deprecated 方向。</td></tr><tr><td><code>liblely-ev.la</code></td><td>事件库</td><td>提供事件循环、future、task、strand、fiber executor 等异步基础设施。</td></tr><tr><td><code>liblely-io2.la</code></td><td>新 I&#x2F;O 库</td><td>提供异步 I&#x2F;O，重点服务 timer、signal handler 和 CAN device。</td></tr><tr><td><code>liblely-coapp.la</code></td><td>C++ CANopen 应用库</td><td>C++ 层 CANopen 应用封装，后续 C++ master&#x2F;slave 应用通常优先链接它。</td></tr></tbody></table><h3 id="主要工具产物"><a href="#主要工具产物" class="headerlink" title="主要工具产物"></a>主要工具产物</h3><table><thead><tr><th>工具</th><th>作用</th></tr></thead><tbody><tr><td><code>can2udp</code></td><td>CAN 与 UDP 转发&#x2F;隧道相关工具。</td></tr><tr><td><code>coctl</code></td><td>CANopen control tool，可用于命令行控制 CANopen master&#x2F;slave 过程。</td></tr><tr><td><code>cocat</code></td><td>CANopen cat 工具。</td></tr><tr><td><code>cocatd</code></td><td>CANopen cat daemon。</td></tr><tr><td><code>dcf2c</code></td><td>把 EDS&#x2F;DCF 转成 C 静态设备描述，适合不想在运行时解析 DCF 的场景。</td></tr></tbody></table><h2 id="日志中的关键字是什么意思"><a href="#日志中的关键字是什么意思" class="headerlink" title="日志中的关键字是什么意思"></a>日志中的关键字是什么意思</h2><h3 id="make-all-recursive"><a href="#make-all-recursive" class="headerlink" title="make all-recursive"></a><code>make all-recursive</code></h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">make  all-recursive</span><br><span class="line">Making all in src</span><br><span class="line">Making all in tools</span><br></pre></td></tr></table></figure><p>表示 Automake 生成的顶层 Makefile 正在递归进入子目录执行 <code>all</code> 目标。<code>doc</code>、<code>include</code>、<code>pkgconfig</code> 等目录里可能没有需要编译的内容，所以会看到：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Nothing to be done for &#x27;all&#x27;.</span><br></pre></td></tr></table></figure><p>这不是错误，只表示该目录在当前目标下没有新增工作。</p><h3 id="CC-CXX-CCLD-CXXLD"><a href="#CC-CXX-CCLD-CXXLD" class="headerlink" title="CC &#x2F; CXX &#x2F; CCLD &#x2F; CXXLD"></a><code>CC</code> &#x2F; <code>CXX</code> &#x2F; <code>CCLD</code> &#x2F; <code>CXXLD</code></h3><table><thead><tr><th>日志前缀</th><th>含义</th></tr></thead><tbody><tr><td><code>CC</code></td><td>使用 C 编译器编译 C 源文件。</td></tr><tr><td><code>CXX</code></td><td>使用 C++ 编译器编译 C++ 源文件。</td></tr><tr><td><code>CCLD</code></td><td>使用 C 链接器驱动进行链接。</td></tr><tr><td><code>CXXLD</code></td><td>使用 C++ 链接器驱动进行链接。</td></tr></tbody></table><p>示例：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">CC       liblely_co_la-nmt.lo</span><br><span class="line">CC       liblely_co_la-pdo.lo</span><br><span class="line">CCLD     liblely-co.la</span><br></pre></td></tr></table></figure><p>含义是：先编译 CANopen NMT、PDO 等模块，再链接出 <code>liblely-co.la</code> 这个 libtool library。</p><h3 id="lo、-la、-so、-a-的区别"><a href="#lo、-la、-so、-a-的区别" class="headerlink" title=".lo、.la、.so、.a 的区别"></a><code>.lo</code>、<code>.la</code>、<code>.so</code>、<code>.a</code> 的区别</h3><table><thead><tr><th>后缀</th><th>含义</th><th align="right">是否最终部署重点</th></tr></thead><tbody><tr><td><code>.lo</code></td><td>libtool object，构建中间文件</td><td align="right">否</td></tr><tr><td><code>.la</code></td><td>libtool archive，描述库依赖、版本和安装路径的元数据</td><td align="right">通常会安装，但目标运行时不直接加载它</td></tr><tr><td><code>.so.*</code></td><td>动态库实体</td><td align="right">是</td></tr><tr><td><code>.so</code></td><td>开发链接用符号链接</td><td align="right">开发包需要</td></tr><tr><td><code>.a</code></td><td>静态库</td><td align="right">静态链接或 SDK 场景需要</td></tr></tbody></table><h3 id="copying-selected-object-files-to-avoid-basename-conflicts"><a href="#copying-selected-object-files-to-avoid-basename-conflicts" class="headerlink" title="copying selected object files to avoid basename conflicts..."></a><code>copying selected object files to avoid basename conflicts...</code></h3><p>这通常出现在 <code>io2</code> 目录，因为不同子目录中可能存在同名源文件或对象文件。libtool&#x2F;Makefile 为避免对象文件 basename 冲突，会复制选定对象文件。这不是错误。</p><h3 id="libtool-warning-relinking"><a href="#libtool-warning-relinking" class="headerlink" title="libtool: warning: relinking ..."></a><code>libtool: warning: relinking ...</code></h3><p>安装 libtool 库时，可能出现：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">libtool: warning: relinking &#x27;liblely-xxx.la&#x27;</span><br></pre></td></tr></table></figure><p>含义是安装阶段根据最终 <code>-rpath</code>、依赖库路径和 staging 前缀重新链接库，使安装后的库记录正确的安装目录语义。GNU Libtool 文档说明，使用临时 staging area 时，<code>libtool --mode=install</code> 会根据安装目标路径和构建时的安装路径进行处理，必要时会触发 relink。</p><p>在这类交叉构建中，只要后续没有 <code>error</code>、<code>No such file</code>、<code>undefined reference</code> 或安装命令失败，单独的 <code>relinking</code> warning 通常不是失败信号。</p><h3 id="libtool-warning-remember-to-run-libtool-finish"><a href="#libtool-warning-remember-to-run-libtool-finish" class="headerlink" title="libtool: warning: remember to run &#39;libtool --finish ...&#39;"></a><code>libtool: warning: remember to run &#39;libtool --finish ...&#39;</code></h3><p>示例：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">libtool: warning: remember to run &#x27;libtool --finish &lt;INSTALL_PREFIX&gt;/lib&#x27;</span><br></pre></td></tr></table></figure><p>含义是 libtool 提示最终安装到真实运行目录后，可能还需要让系统动态链接器识别库路径，例如更新动态链接器缓存或设置运行时库搜索路径。</p><p>在 staging 场景下，这条 warning 需要按部署方式判断：</p><table><thead><tr><th>部署方式</th><th>是否需要手动处理</th></tr></thead><tbody><tr><td>打包进 rootfs 的标准库目录，如 <code>/usr/lib</code></td><td>通常由 rootfs&#x2F;package 机制处理</td></tr><tr><td>部署到非标准目录，如 <code>&lt;INSTALL_PREFIX&gt;/lib</code></td><td>需要确保目标板运行时能找到 <code>.so</code></td></tr><tr><td>只用于交叉编译应用，不在构建主机运行目标程序</td><td>构建主机上通常不需要执行 <code>libtool --finish</code></td></tr></tbody></table><p>目标板运行时可能需要：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">export</span> LD_LIBRARY_PATH=&lt;INSTALL_PREFIX&gt;/lib:<span class="variable">$&#123;LD_LIBRARY_PATH&#125;</span></span><br></pre></td></tr></table></figure><p>或者把库路径加入目标系统的动态链接器配置。实际做法取决于 rootfs、发行版和部署策略。</p><h2 id="make-install-DESTDIR-安装了什么"><a href="#make-install-DESTDIR-安装了什么" class="headerlink" title="make install DESTDIR 安装了什么"></a><code>make install DESTDIR</code> 安装了什么</h2><p>日志显示安装阶段主要复制了以下内容：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">graph TD</span><br><span class="line">    S[&lt;LELY_STAGE&gt;&lt;INSTALL_PREFIX&gt;] --&gt; I[include/lely/...]</span><br><span class="line">    S --&gt; L[lib/]</span><br><span class="line">    S --&gt; P[lib/pkgconfig/]</span><br><span class="line">    S --&gt; B[bin/]</span><br><span class="line">    S --&gt; E[etc/]</span><br><span class="line">    I --&gt; IH[公共 C/C++ 头文件]</span><br><span class="line">    L --&gt; SO[动态库 .so.* 和 .so 符号链接]</span><br><span class="line">    L --&gt; A[静态库 .a]</span><br><span class="line">    L --&gt; LA[libtool .la]</span><br><span class="line">    P --&gt; PC[liblely-*.pc]</span><br><span class="line">    B --&gt; T[can2udp / coctl / cocat / cocatd / dcf2c]</span><br><span class="line">    E --&gt; CFG[coctl.dcf / cocatd.dcf]</span><br></pre></td></tr></table></figure><h3 id="目标安装树"><a href="#目标安装树" class="headerlink" title="目标安装树"></a>目标安装树</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br></pre></td><td class="code"><pre><span class="line">&lt;LELY_STAGE&gt;&lt;INSTALL_PREFIX&gt;/</span><br><span class="line">├── include/</span><br><span class="line">│   └── lely/</span><br><span class="line">│       ├── can/</span><br><span class="line">│       ├── co/</span><br><span class="line">│       ├── coapp/</span><br><span class="line">│       ├── ev/</span><br><span class="line">│       ├── io/</span><br><span class="line">│       ├── io2/</span><br><span class="line">│       ├── libc/</span><br><span class="line">│       ├── tap/</span><br><span class="line">│       └── util/</span><br><span class="line">├── lib/</span><br><span class="line">│   ├── liblely-*.so.*</span><br><span class="line">│   ├── liblely-*.so</span><br><span class="line">│   ├── liblely-*.a</span><br><span class="line">│   ├── liblely-*.la</span><br><span class="line">│   └── pkgconfig/</span><br><span class="line">│       ├── liblely-can.pc</span><br><span class="line">│       ├── liblely-co.pc</span><br><span class="line">│       ├── liblely-coapp.pc</span><br><span class="line">│       ├── liblely-ev.pc</span><br><span class="line">│       ├── liblely-io.pc</span><br><span class="line">│       ├── liblely-io2.pc</span><br><span class="line">│       ├── liblely-libc.pc</span><br><span class="line">│       ├── liblely-libc_rt.pc</span><br><span class="line">│       ├── liblely-tap.pc</span><br><span class="line">│       └── liblely-util.pc</span><br><span class="line">├── bin/</span><br><span class="line">│   ├── can2udp</span><br><span class="line">│   ├── coctl</span><br><span class="line">│   ├── cocat</span><br><span class="line">│   ├── cocatd</span><br><span class="line">│   └── dcf2c</span><br><span class="line">└── etc/</span><br><span class="line">    ├── coctl.dcf</span><br><span class="line">    └── cocatd.dcf</span><br></pre></td></tr></table></figure><p>其中 <code>lib/pkgconfig</code> 是后续应用交叉编译时最容易被忽略、但实际很关键的一部分。</p><h2 id="lib-pkgconfig-是什么"><a href="#lib-pkgconfig-是什么" class="headerlink" title="lib/pkgconfig 是什么"></a><code>lib/pkgconfig</code> 是什么</h2><h3 id="它保存的是-pc-元数据文件"><a href="#它保存的是-pc-元数据文件" class="headerlink" title="它保存的是 .pc 元数据文件"></a>它保存的是 <code>.pc</code> 元数据文件</h3><p><code>lib/pkgconfig</code> 目录用于存放 <code>pkg-config</code> 的 <code>.pc</code> 文件。<code>.pc</code> 文件描述一个库的：</p><ol><li>头文件目录。</li><li>库文件目录。</li><li>链接库名。</li><li>版本号。</li><li>依赖库关系。</li><li>静态链接时可能额外需要的依赖。</li></ol><p><code>pkg-config</code> 的核心用途就是从这些 <code>.pc</code> 文件中读取元数据，然后输出编译器和链接器需要的参数。</p><p>一个典型 <code>.pc</code> 文件结构如下：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">prefix=&lt;INSTALL_PREFIX&gt;</span><br><span class="line">exec_prefix=$&#123;prefix&#125;</span><br><span class="line">libdir=$&#123;exec_prefix&#125;/lib</span><br><span class="line">includedir=$&#123;prefix&#125;/include</span><br><span class="line"></span><br><span class="line">Name: liblely-coapp</span><br><span class="line">Description: Lely C++ CANopen application library</span><br><span class="line">Version: &lt;VERSION&gt;</span><br><span class="line">Requires: liblely-io2 liblely-ev liblely-co</span><br><span class="line">Libs: -L$&#123;libdir&#125; -llely-coapp</span><br><span class="line">Cflags: -I$&#123;includedir&#125;</span><br></pre></td></tr></table></figure><p>实际字段以 lely-core 生成结果为准。这里重点是理解字段用途：</p><table><thead><tr><th>字段</th><th>作用</th></tr></thead><tbody><tr><td><code>prefix</code></td><td>最终安装前缀，来自 <code>../configure --prefix=...</code>。</td></tr><tr><td><code>libdir</code></td><td>库搜索目录，通常是 <code>${prefix}/lib</code>。</td></tr><tr><td><code>includedir</code></td><td>头文件搜索目录，通常是 <code>${prefix}/include</code>。</td></tr><tr><td><code>Requires</code></td><td>依赖的其他 pkg-config module。</td></tr><tr><td><code>Libs</code></td><td>链接当前库需要的 <code>-L</code> 和 <code>-l</code> 参数。</td></tr><tr><td><code>Cflags</code></td><td>编译时需要的 <code>-I</code> 等参数。</td></tr></tbody></table><h3 id="为什么-Lely-会安装多个-pc-文件"><a href="#为什么-Lely-会安装多个-pc-文件" class="headerlink" title="为什么 Lely 会安装多个 .pc 文件"></a>为什么 Lely 会安装多个 <code>.pc</code> 文件</h3><p>日志显示安装了这些 <code>.pc</code> 文件：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">liblely-can.pc</span><br><span class="line">liblely-co.pc</span><br><span class="line">liblely-coapp.pc</span><br><span class="line">liblely-ev.pc</span><br><span class="line">liblely-io.pc</span><br><span class="line">liblely-io2.pc</span><br><span class="line">liblely-libc.pc</span><br><span class="line">liblely-libc_rt.pc</span><br><span class="line">liblely-tap.pc</span><br><span class="line">liblely-util.pc</span><br></pre></td></tr></table></figure><p>这是因为 lely-core 不是单个库，而是一组分层库。每个库都有自己的 <code>.pc</code> 文件，后续应用只需要声明最上层依赖，例如 <code>liblely-coapp</code>，<code>pkg-config</code> 会根据 <code>.pc</code> 中的依赖关系输出它需要的其他 Lely 库。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    APP[你的 CANopen C++ 应用] --&gt; PC[pkg-config liblely-coapp]</span><br><span class="line">    PC --&gt; CFLAGS[-I&lt;LELY_STAGE&gt;&lt;INSTALL_PREFIX&gt;/include]</span><br><span class="line">    PC --&gt; LIBS[-L&lt;LELY_STAGE&gt;&lt;INSTALL_PREFIX&gt;/lib -llely-coapp ...]</span><br><span class="line">    LIBS --&gt; COAPP[liblely-coapp]</span><br><span class="line">    COAPP --&gt; IO2[liblely-io2]</span><br><span class="line">    COAPP --&gt; EV[liblely-ev]</span><br><span class="line">    COAPP --&gt; CO[liblely-co]</span><br><span class="line">    CO --&gt; CAN[liblely-can]</span><br><span class="line">    CO --&gt; UTIL[liblely-util]</span><br><span class="line">    UTIL --&gt; LIBC[liblely-libc]</span><br></pre></td></tr></table></figure><h3 id="pkg-config-cflags-和-pkg-config-libs-分别解决什么问题"><a href="#pkg-config-cflags-和-pkg-config-libs-分别解决什么问题" class="headerlink" title="pkg-config --cflags 和 pkg-config --libs 分别解决什么问题"></a><code>pkg-config --cflags</code> 和 <code>pkg-config --libs</code> 分别解决什么问题</h3><table><thead><tr><th>命令</th><th>输出内容</th><th>用在编译&#x2F;链接的哪一步</th></tr></thead><tbody><tr><td><code>pkg-config --cflags liblely-coapp</code></td><td>头文件路径等编译参数，例如 <code>-I.../include</code></td><td>编译 <code>.c/.cpp</code> 时</td></tr><tr><td><code>pkg-config --libs liblely-coapp</code></td><td>库目录和库名，例如 <code>-L.../lib -llely-coapp ...</code></td><td>链接可执行程序时</td></tr><tr><td><code>pkg-config --libs --static liblely-coapp</code></td><td>静态链接所需的更完整依赖</td><td>静态链接时</td></tr><tr><td><code>pkg-config --modversion liblely-coapp</code></td><td>版本号</td><td>配置检查或诊断时</td></tr><tr><td><code>pkg-config --exists liblely-coapp</code></td><td>只检查是否存在</td><td>脚本判断时</td></tr></tbody></table><p>后续应用可以这样使用：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="variable">$&#123;CXX&#125;</span> main.cpp -o canopen_app \</span><br><span class="line">    $(pkg-config --cflags liblely-coapp) \</span><br><span class="line">    $(pkg-config --libs liblely-coapp)</span><br></pre></td></tr></table></figure><p>如果是 Makefile：</p><figure class="highlight make"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">CXXFLAGS += <span class="variable">$(<span class="built_in">shell</span> pkg-config --cflags liblely-coapp)</span></span><br><span class="line">LDLIBS   += <span class="variable">$(<span class="built_in">shell</span> pkg-config --libs liblely-coapp)</span></span><br></pre></td></tr></table></figure><p>如果使用 Autotools，也可以通过 <code>PKG_CHECK_MODULES</code> 让配置阶段自动检查并填充 <code>&lt;PREFIX&gt;_CFLAGS</code> 和 <code>&lt;PREFIX&gt;_LIBS</code>。</p><h2 id="加入本次-pkg-config-环境变量说明"><a href="#加入本次-pkg-config-环境变量说明" class="headerlink" title="加入本次 pkg-config 环境变量说明"></a>加入本次 <code>pkg-config</code> 环境变量说明</h2><p>你补充的操作可以脱敏整理为：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">source</span> &lt;YOCTO_SDK&gt;/environment-setup-armv8a-poky-linux</span><br><span class="line"></span><br><span class="line"><span class="built_in">export</span> WORK_ROOT=&lt;WORK_ROOT&gt;</span><br><span class="line"><span class="built_in">export</span> LELY_STAGE=<span class="variable">$&#123;WORK_ROOT&#125;</span>/lely-core/build-imx8p/stage</span><br><span class="line"></span><br><span class="line"><span class="built_in">export</span> PKG_CONFIG_SYSROOT_DIR=<span class="variable">$&#123;LELY_STAGE&#125;</span></span><br><span class="line"><span class="built_in">export</span> PKG_CONFIG_LIBDIR=<span class="variable">$&#123;LELY_STAGE&#125;</span>&lt;INSTALL_PREFIX&gt;/lib/pkgconfig:<span class="variable">$&#123;SDKTARGETSYSROOT&#125;</span>/usr/lib/pkgconfig:<span class="variable">$&#123;SDKTARGETSYSROOT&#125;</span>/usr/share/pkgconfig</span><br><span class="line"></span><br><span class="line">pkg-config --cflags liblely-coapp</span><br><span class="line">pkg-config --libs liblely-coapp</span><br></pre></td></tr></table></figure><h3 id="source-environment-setup-armv8a-poky-linux-做什么"><a href="#source-environment-setup-armv8a-poky-linux-做什么" class="headerlink" title="source environment-setup-armv8a-poky-linux 做什么"></a><code>source environment-setup-armv8a-poky-linux</code> 做什么</h3><p>这一步加载 Yocto SDK 的交叉编译环境，通常会设置：</p><table><thead><tr><th>变量</th><th>常见作用</th></tr></thead><tbody><tr><td><code>CC</code> &#x2F; <code>CXX</code></td><td>指向目标平台交叉编译器。</td></tr><tr><td><code>AR</code> &#x2F; <code>RANLIB</code> &#x2F; <code>STRIP</code></td><td>指向目标平台 binutils 工具。</td></tr><tr><td><code>SDKTARGETSYSROOT</code></td><td>指向 Yocto SDK 自带目标 sysroot。</td></tr><tr><td><code>CFLAGS</code> &#x2F; <code>CXXFLAGS</code></td><td>附带目标架构、sysroot、安全编译参数等。</td></tr><tr><td><code>LDFLAGS</code></td><td>附带目标链接参数。</td></tr><tr><td><code>PKG_CONFIG_*</code></td><td>有些 SDK 会设置默认 pkg-config 交叉环境。</td></tr></tbody></table><p>加载 SDK 后再设置 Lely 相关 <code>PKG_CONFIG_*</code>，目的是让后续应用既能找到 Lely 自己的 <code>.pc</code> 文件，也能继续找到 Yocto sysroot 里的系统库 <code>.pc</code> 文件。</p><h3 id="PKG-CONFIG-SYSROOT-DIR-LELY-STAGE-做什么"><a href="#PKG-CONFIG-SYSROOT-DIR-LELY-STAGE-做什么" class="headerlink" title="PKG_CONFIG_SYSROOT_DIR=${LELY_STAGE} 做什么"></a><code>PKG_CONFIG_SYSROOT_DIR=${LELY_STAGE}</code> 做什么</h3><p><code>PKG_CONFIG_SYSROOT_DIR</code> 用于交叉编译 sysroot 前缀处理。对 <code>.pc</code> 文件里的 <code>-I${includedir}</code>、<code>-L${libdir}</code> 这类路径，<code>pkg-config</code> 会把 sysroot 加到路径前面。</p><p>假设 <code>liblely-coapp.pc</code> 中写的是：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">prefix=&lt;INSTALL_PREFIX&gt;</span><br><span class="line">includedir=$&#123;prefix&#125;/include</span><br><span class="line">libdir=$&#123;prefix&#125;/lib</span><br><span class="line">Cflags: -I$&#123;includedir&#125;</span><br><span class="line">Libs: -L$&#123;libdir&#125; -llely-coapp</span><br></pre></td></tr></table></figure><p>设置：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">export</span> PKG_CONFIG_SYSROOT_DIR=&lt;LELY_STAGE&gt;</span><br></pre></td></tr></table></figure><p>查询结果就会变成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">-I&lt;LELY_STAGE&gt;&lt;INSTALL_PREFIX&gt;/include</span><br><span class="line">-L&lt;LELY_STAGE&gt;&lt;INSTALL_PREFIX&gt;/lib -llely-coapp ...</span><br></pre></td></tr></table></figure><p>这正好指向 staging 目录中的目标头文件和目标库，而不是构建主机上的路径。</p><h3 id="PKG-CONFIG-LIBDIR-做什么"><a href="#PKG-CONFIG-LIBDIR-做什么" class="headerlink" title="PKG_CONFIG_LIBDIR=... 做什么"></a><code>PKG_CONFIG_LIBDIR=...</code> 做什么</h3><p><code>PKG_CONFIG_LIBDIR</code> 指定 <code>.pc</code> 文件搜索目录。交叉编译时更推荐明确设置它，而不是只追加 <code>PKG_CONFIG_PATH</code>，因为要避免 <code>pkg-config</code> 意外搜索到构建主机上的 x86_64 <code>.pc</code> 文件。</p><p>本次设置可以拆开看：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">PKG_CONFIG_LIBDIR=</span><br><span class="line">  $&#123;LELY_STAGE&#125;&lt;INSTALL_PREFIX&gt;/lib/pkgconfig</span><br><span class="line">  :$&#123;SDKTARGETSYSROOT&#125;/usr/lib/pkgconfig</span><br><span class="line">  :$&#123;SDKTARGETSYSROOT&#125;/usr/share/pkgconfig</span><br></pre></td></tr></table></figure><table><thead><tr><th>路径</th><th>作用</th></tr></thead><tbody><tr><td><code>${LELY_STAGE}&lt;INSTALL_PREFIX&gt;/lib/pkgconfig</code></td><td>找到本次 staged install 生成的 <code>liblely-*.pc</code>。</td></tr><tr><td><code>${SDKTARGETSYSROOT}/usr/lib/pkgconfig</code></td><td>找到 Yocto 目标 sysroot 中库相关 <code>.pc</code> 文件。</td></tr><tr><td><code>${SDKTARGETSYSROOT}/usr/share/pkgconfig</code></td><td>找到 Yocto 目标 sysroot 中架构无关 <code>.pc</code> 文件。</td></tr></tbody></table><p>这样配置后，<code>pkg-config</code> 查询 <code>liblely-coapp</code> 时会优先从 Lely staging 目录读取：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">&lt;LELY_STAGE&gt;&lt;INSTALL_PREFIX&gt;/lib/pkgconfig/liblely-coapp.pc</span><br></pre></td></tr></table></figure><p>再根据它的 <code>Requires</code>、<code>Libs</code>、<code>Cflags</code> 输出完整参数。</p><h3 id="本次查询输出说明"><a href="#本次查询输出说明" class="headerlink" title="本次查询输出说明"></a>本次查询输出说明</h3><p>脱敏后的查询输出是：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">pkg-config --cflags liblely-coapp</span><br><span class="line"><span class="comment"># -I&lt;LELY_STAGE&gt;&lt;INSTALL_PREFIX&gt;/include</span></span><br><span class="line"></span><br><span class="line">pkg-config --libs liblely-coapp</span><br><span class="line"><span class="comment"># -L&lt;LELY_STAGE&gt;&lt;INSTALL_PREFIX&gt;/lib -llely-coapp -llely-io2 -llely-ev -llely-co -llely-can -llely-util -llely-libc -lrt</span></span><br></pre></td></tr></table></figure><p>逐项解释：</p><table><thead><tr><th>输出片段</th><th>含义</th></tr></thead><tbody><tr><td><code>-I&lt;LELY_STAGE&gt;&lt;INSTALL_PREFIX&gt;/include</code></td><td>编译阶段搜索 Lely 头文件，例如 <code>&lt;lely/coapp/master.hpp&gt;</code>。</td></tr><tr><td><code>-L&lt;LELY_STAGE&gt;&lt;INSTALL_PREFIX&gt;/lib</code></td><td>链接阶段搜索 Lely 库文件目录。</td></tr><tr><td><code>-llely-coapp</code></td><td>链接 C++ CANopen application library。</td></tr><tr><td><code>-llely-io2</code></td><td>链接新 I&#x2F;O 异步接口库。</td></tr><tr><td><code>-llely-ev</code></td><td>链接事件库。</td></tr><tr><td><code>-llely-co</code></td><td>链接 CANopen 协议栈库。</td></tr><tr><td><code>-llely-can</code></td><td>链接 CAN 基础库。</td></tr><tr><td><code>-llely-util</code></td><td>链接公共工具库。</td></tr><tr><td><code>-llely-libc</code></td><td>链接 Lely 兼容层库。</td></tr><tr><td><code>-lrt</code></td><td>链接 POSIX realtime 库。某些平台&#x2F;工具链上 timer、clock 等接口需要它。</td></tr></tbody></table><p>这说明 <code>lib/pkgconfig/liblely-coapp.pc</code> 和它依赖的 <code>.pc</code> 文件已经能被当前 shell 中的 <code>pkg-config</code> 正确找到。</p><h3 id="推荐增加的检查命令"><a href="#推荐增加的检查命令" class="headerlink" title="推荐增加的检查命令"></a>推荐增加的检查命令</h3><p>为了确认没有误用主机 <code>.pc</code> 文件，可以执行：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">pkg-config --path liblely-coapp</span><br><span class="line">pkg-config --modversion liblely-coapp</span><br><span class="line">pkg-config --print-errors --exists liblely-coapp</span><br><span class="line">pkg-config --cflags --libs liblely-coapp</span><br></pre></td></tr></table></figure><p>期望 <code>--path</code> 输出在：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">&lt;LELY_STAGE&gt;&lt;INSTALL_PREFIX&gt;/lib/pkgconfig/liblely-coapp.pc</span><br></pre></td></tr></table></figure><p>如果输出跑到 <code>/usr/lib/pkgconfig</code>、<code>/usr/share/pkgconfig</code> 或主机其他路径，说明交叉编译环境有污染风险。</p><h2 id="后续应用如何使用-staged-Lely"><a href="#后续应用如何使用-staged-Lely" class="headerlink" title="后续应用如何使用 staged Lely"></a>后续应用如何使用 staged Lely</h2><h3 id="编译一个依赖-liblely-coapp-的-C-应用"><a href="#编译一个依赖-liblely-coapp-的-C-应用" class="headerlink" title="编译一个依赖 liblely-coapp 的 C++ 应用"></a>编译一个依赖 <code>liblely-coapp</code> 的 C++ 应用</h3><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">source</span> &lt;YOCTO_SDK&gt;/environment-setup-armv8a-poky-linux</span><br><span class="line"></span><br><span class="line"><span class="built_in">export</span> WORK_ROOT=&lt;WORK_ROOT&gt;</span><br><span class="line"><span class="built_in">export</span> LELY_STAGE=<span class="variable">$&#123;WORK_ROOT&#125;</span>/lely-core/build-imx8p/stage</span><br><span class="line"><span class="built_in">export</span> PKG_CONFIG_SYSROOT_DIR=<span class="variable">$&#123;LELY_STAGE&#125;</span></span><br><span class="line"><span class="built_in">export</span> PKG_CONFIG_LIBDIR=<span class="variable">$&#123;LELY_STAGE&#125;</span>&lt;INSTALL_PREFIX&gt;/lib/pkgconfig:<span class="variable">$&#123;SDKTARGETSYSROOT&#125;</span>/usr/lib/pkgconfig:<span class="variable">$&#123;SDKTARGETSYSROOT&#125;</span>/usr/share/pkgconfig</span><br><span class="line"></span><br><span class="line"><span class="variable">$&#123;CXX&#125;</span> main.cpp -o canopen_app \</span><br><span class="line">    $(pkg-config --cflags liblely-coapp) \</span><br><span class="line">    $(pkg-config --libs liblely-coapp)</span><br></pre></td></tr></table></figure><p>如果你的应用还依赖 Yocto sysroot 中的其他库，例如 <code>libsocketcan</code>、<code>glib-2.0</code> 或其他目标库，也应通过同一个交叉 <code>pkg-config</code> 环境查询，避免混入主机依赖。</p><h3 id="CMake-项目示例"><a href="#CMake-项目示例" class="headerlink" title="CMake 项目示例"></a>CMake 项目示例</h3><figure class="highlight cmake"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">find_package</span>(PkgConfig REQUIRED)</span><br><span class="line">pkg_check_modules(LELY_COAPP REQUIRED liblely-coapp)</span><br><span class="line"></span><br><span class="line"><span class="keyword">add_executable</span>(canopen_app main.cpp)</span><br><span class="line"><span class="keyword">target_include_directories</span>(canopen_app PRIVATE <span class="variable">$&#123;LELY_COAPP_INCLUDE_DIRS&#125;</span>)</span><br><span class="line"><span class="keyword">target_link_directories</span>(canopen_app PRIVATE <span class="variable">$&#123;LELY_COAPP_LIBRARY_DIRS&#125;</span>)</span><br><span class="line"><span class="keyword">target_link_libraries</span>(canopen_app PRIVATE <span class="variable">$&#123;LELY_COAPP_LIBRARIES&#125;</span>)</span><br><span class="line"><span class="keyword">target_compile_options</span>(canopen_app PRIVATE <span class="variable">$&#123;LELY_COAPP_CFLAGS_OTHER&#125;</span>)</span><br></pre></td></tr></table></figure><p>执行 CMake 前仍需要在 shell 中准备好 <code>PKG_CONFIG_SYSROOT_DIR</code> 和 <code>PKG_CONFIG_LIBDIR</code>。</p><h2 id="安装结果应该如何检查"><a href="#安装结果应该如何检查" class="headerlink" title="安装结果应该如何检查"></a>安装结果应该如何检查</h2><h3 id="检查-staging-安装树"><a href="#检查-staging-安装树" class="headerlink" title="检查 staging 安装树"></a>检查 staging 安装树</h3><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">find &lt;LELY_STAGE&gt;&lt;INSTALL_PREFIX&gt; -maxdepth 3 -<span class="built_in">type</span> f | <span class="built_in">sort</span></span><br></pre></td></tr></table></figure><p>重点检查：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">&lt;LELY_STAGE&gt;&lt;INSTALL_PREFIX&gt;/include/lely/coapp/master.hpp</span><br><span class="line">&lt;LELY_STAGE&gt;&lt;INSTALL_PREFIX&gt;/lib/liblely-coapp.so</span><br><span class="line">&lt;LELY_STAGE&gt;&lt;INSTALL_PREFIX&gt;/lib/liblely-coapp.a</span><br><span class="line">&lt;LELY_STAGE&gt;&lt;INSTALL_PREFIX&gt;/lib/pkgconfig/liblely-coapp.pc</span><br><span class="line">&lt;LELY_STAGE&gt;&lt;INSTALL_PREFIX&gt;/bin/dcf2c</span><br></pre></td></tr></table></figure><h3 id="检查库架构"><a href="#检查库架构" class="headerlink" title="检查库架构"></a>检查库架构</h3><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">file &lt;LELY_STAGE&gt;&lt;INSTALL_PREFIX&gt;/lib/liblely-coapp.so.*</span><br><span class="line">file &lt;LELY_STAGE&gt;&lt;INSTALL_PREFIX&gt;/bin/dcf2c</span><br></pre></td></tr></table></figure><p>期望结果应显示目标架构，例如 AArch64，而不是构建主机架构。</p><h3 id="检查-pkg-config-输出"><a href="#检查-pkg-config-输出" class="headerlink" title="检查 pkg-config 输出"></a>检查 pkg-config 输出</h3><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">pkg-config --path liblely-coapp</span><br><span class="line">pkg-config --cflags liblely-coapp</span><br><span class="line">pkg-config --libs liblely-coapp</span><br></pre></td></tr></table></figure><p>如果 <code>--cflags</code> 和 <code>--libs</code> 都指向 <code>&lt;LELY_STAGE&gt;&lt;INSTALL_PREFIX&gt;</code>，说明 staged Lely 对后续应用编译是可见的。</p><h2 id="常见问题"><a href="#常见问题" class="headerlink" title="常见问题"></a>常见问题</h2><h3 id="为什么-make-install-阶段还会-relink"><a href="#为什么-make-install-阶段还会-relink" class="headerlink" title="为什么 make install 阶段还会 relink"></a>为什么 <code>make install</code> 阶段还会 relink</h3><p>因为 libtool 需要在安装阶段确认库的最终 <code>rpath</code>、依赖关系和安装目录语义。使用 <code>DESTDIR</code> 时，它会把临时 staging 根和最终 prefix 区分开。日志出现 relink warning 并不等于失败，关键看最终是否安装出 <code>.so</code>、<code>.a</code>、<code>.la</code> 和符号链接。</p><h3 id="为什么-pkg-config-libs-liblely-coapp-输出了一串-Lely-库"><a href="#为什么-pkg-config-libs-liblely-coapp-输出了一串-Lely-库" class="headerlink" title="为什么 pkg-config --libs liblely-coapp 输出了一串 Lely 库"></a>为什么 <code>pkg-config --libs liblely-coapp</code> 输出了一串 Lely 库</h3><p>因为 <code>liblely-coapp</code> 是高层 C++ 应用库，它依赖 <code>io2</code>、<code>ev</code>、<code>co</code>、<code>can</code>、<code>util</code>、<code>libc</code> 等底层库。<code>pkg-config</code> 会根据 <code>.pc</code> 中的依赖关系输出链接所需库，避免你手工维护顺序。</p><h3 id="为什么使用-PKG-CONFIG-LIBDIR，而不是只设置-PKG-CONFIG-PATH"><a href="#为什么使用-PKG-CONFIG-LIBDIR，而不是只设置-PKG-CONFIG-PATH" class="headerlink" title="为什么使用 PKG_CONFIG_LIBDIR，而不是只设置 PKG_CONFIG_PATH"></a>为什么使用 <code>PKG_CONFIG_LIBDIR</code>，而不是只设置 <code>PKG_CONFIG_PATH</code></h3><p><code>PKG_CONFIG_PATH</code> 通常是追加搜索路径；交叉编译时，如果默认搜索路径仍包含构建主机的 <code>/usr/lib/pkgconfig</code>，就可能混入主机库参数。<code>PKG_CONFIG_LIBDIR</code> 用于指定基础搜索路径，更适合隔离目标 sysroot 和 staged dependency。</p><h3 id="PKG-CONFIG-SYSROOT-DIR-会影响所有路径吗"><a href="#PKG-CONFIG-SYSROOT-DIR-会影响所有路径吗" class="headerlink" title="PKG_CONFIG_SYSROOT_DIR 会影响所有路径吗"></a><code>PKG_CONFIG_SYSROOT_DIR</code> 会影响所有路径吗</h3><p>它主要影响 <code>.pc</code> 输出中的编译&#x2F;链接路径，例如 <code>-I</code>、<code>-L</code> 后面的路径。对 <code>pkg-config --variable</code> 查询出来的普通变量，不一定自动加 sysroot。因此交叉编译脚本中不要假设所有变量都会被 sysroot 化。</p><h3 id="lib-pkgconfig-目录需要部署到目标板吗"><a href="#lib-pkgconfig-目录需要部署到目标板吗" class="headerlink" title="lib/pkgconfig 目录需要部署到目标板吗"></a><code>lib/pkgconfig</code> 目录需要部署到目标板吗</h3><p>如果目标板只运行程序，通常不需要 <code>.pc</code> 文件；目标板运行时需要的是 <code>.so</code>、配置文件和可执行文件。</p><p>如果目标板本身还要进行本机编译，或者你要把该目录作为 SDK 给其他工程使用，就应保留 <code>lib/pkgconfig/*.pc</code>。</p><h2 id="推荐的最终命令模板"><a href="#推荐的最终命令模板" class="headerlink" title="推荐的最终命令模板"></a>推荐的最终命令模板</h2><p>下面是脱敏后的推荐命令模板：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 1. 加载 Yocto SDK 交叉编译环境</span></span><br><span class="line"><span class="built_in">source</span> &lt;YOCTO_SDK&gt;/environment-setup-armv8a-poky-linux</span><br><span class="line"></span><br><span class="line"><span class="comment"># 2. 配置工作目录</span></span><br><span class="line"><span class="built_in">export</span> WORK_ROOT=&lt;WORK_ROOT&gt;</span><br><span class="line"><span class="built_in">export</span> BUILD_DIR=<span class="variable">$&#123;WORK_ROOT&#125;</span>/lely-core/build-imx8p</span><br><span class="line"><span class="built_in">export</span> LELY_STAGE=<span class="variable">$&#123;BUILD_DIR&#125;</span>/stage</span><br><span class="line"></span><br><span class="line"><span class="comment"># 3. 配置 Lely 源码</span></span><br><span class="line"><span class="built_in">mkdir</span> -p <span class="string">&quot;<span class="variable">$&#123;BUILD_DIR&#125;</span>&quot;</span></span><br><span class="line"><span class="built_in">cd</span> <span class="string">&quot;<span class="variable">$&#123;BUILD_DIR&#125;</span>&quot;</span></span><br><span class="line"></span><br><span class="line">../configure \</span><br><span class="line">    --host=aarch64-poky-linux \</span><br><span class="line">    --prefix=&lt;INSTALL_PREFIX&gt; \</span><br><span class="line">    --disable-python \</span><br><span class="line">    --disable-cython \</span><br><span class="line">    --disable-tests \</span><br><span class="line">    --disable-unit-tests</span><br><span class="line"></span><br><span class="line"><span class="comment"># 4. 构建 Lely 库和工具</span></span><br><span class="line">make -j<span class="string">&quot;<span class="subst">$(nproc)</span>&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 5. 安装到 staging 目录</span></span><br><span class="line">make install DESTDIR=<span class="string">&quot;<span class="variable">$&#123;LELY_STAGE&#125;</span>&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 6. 配置 pkg-config，让后续应用找到 staged Lely</span></span><br><span class="line"><span class="built_in">export</span> PKG_CONFIG_SYSROOT_DIR=<span class="variable">$&#123;LELY_STAGE&#125;</span></span><br><span class="line"><span class="built_in">export</span> PKG_CONFIG_LIBDIR=<span class="variable">$&#123;LELY_STAGE&#125;</span>&lt;INSTALL_PREFIX&gt;/lib/pkgconfig:<span class="variable">$&#123;SDKTARGETSYSROOT&#125;</span>/usr/lib/pkgconfig:<span class="variable">$&#123;SDKTARGETSYSROOT&#125;</span>/usr/share/pkgconfig</span><br><span class="line"></span><br><span class="line"><span class="comment"># 7. 验证 pkg-config 查询结果</span></span><br><span class="line">pkg-config --path liblely-coapp</span><br><span class="line">pkg-config --cflags liblely-coapp</span><br><span class="line">pkg-config --libs liblely-coapp</span><br></pre></td></tr></table></figure><h2 id="关键判断标准"><a href="#关键判断标准" class="headerlink" title="关键判断标准"></a>关键判断标准</h2><p>构建和安装是否成功，可以按下面几项判断：</p><table><thead><tr><th>检查项</th><th>期望结果</th></tr></thead><tbody><tr><td><code>make</code> 结束</td><td>没有 <code>Error</code>，生成 <code>liblely-*.la</code> 和工具。</td></tr><tr><td><code>make install DESTDIR=...</code> 结束</td><td>staging 中出现 <code>include</code>、<code>lib</code>、<code>lib/pkgconfig</code>、<code>bin</code>、<code>etc</code>。</td></tr><tr><td><code>file liblely-coapp.so.*</code></td><td>显示目标架构，例如 AArch64。</td></tr><tr><td><code>pkg-config --path liblely-coapp</code></td><td>指向 <code>&lt;LELY_STAGE&gt;&lt;INSTALL_PREFIX&gt;/lib/pkgconfig/liblely-coapp.pc</code>。</td></tr><tr><td><code>pkg-config --cflags liblely-coapp</code></td><td>输出 <code>-I&lt;LELY_STAGE&gt;&lt;INSTALL_PREFIX&gt;/include</code>。</td></tr><tr><td><code>pkg-config --libs liblely-coapp</code></td><td>输出 <code>-L&lt;LELY_STAGE&gt;&lt;INSTALL_PREFIX&gt;/lib</code> 和 Lely 依赖库。</td></tr></tbody></table><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p><code>make</code> 负责“把源码变成库和工具”；<code>make install DESTDIR=&quot;${PWD}/stage&quot;</code> 负责“把库和工具整理成可部署目录树”。</p><p>对本次 Lely CANopen 交叉构建来说，<code>DESTDIR</code> 的主要意义是安全地形成 staging 安装树，而 <code>lib/pkgconfig/*.pc</code> 的主要意义是让后续应用可以通过 <code>pkg-config</code> 自动拿到正确的交叉编译参数。</p><p>你补充的：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">export</span> PKG_CONFIG_SYSROOT_DIR=<span class="variable">$&#123;LELY_STAGE&#125;</span></span><br><span class="line"><span class="built_in">export</span> PKG_CONFIG_LIBDIR=<span class="variable">$&#123;LELY_STAGE&#125;</span>&lt;INSTALL_PREFIX&gt;/lib/pkgconfig:<span class="variable">$&#123;SDKTARGETSYSROOT&#125;</span>/usr/lib/pkgconfig:<span class="variable">$&#123;SDKTARGETSYSROOT&#125;</span>/usr/share/pkgconfig</span><br></pre></td></tr></table></figure><p>是把 staged Lely 接入后续应用交叉编译流程的关键步骤。只要 <code>pkg-config --path liblely-coapp</code> 指向 staging 里的 <code>.pc</code> 文件，并且 <code>--cflags</code>、<code>--libs</code> 输出指向 <code>&lt;LELY_STAGE&gt;&lt;INSTALL_PREFIX&gt;</code>，后续应用就可以用这套 Lely 头文件和 AArch64 库进行交叉编译。</p><h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ul><li>GNU Automake Manual, “Building Binary Packages Using DESTDIR”: <a href="https://www.gnu.org/software/automake/manual/html_node/DESTDIR.html">https://www.gnu.org/software/automake/manual/html_node/DESTDIR.html</a></li><li>GNU Libtool Manual, “Install mode”: <a href="https://www.gnu.org/software/libtool/manual/html_node/Install-mode.html">https://www.gnu.org/software/libtool/manual/html_node/Install-mode.html</a></li><li>GNU Libtool Manual, “Installing libraries”: <a href="https://www.gnu.org/software/libtool/manual/html_node/Installing-libraries.html">https://www.gnu.org/software/libtool/manual/html_node/Installing-libraries.html</a></li><li>Lely CANopen Documentation, “Installation”: <a href="https://opensource.lely.com/canopen/docs/installation/">https://opensource.lely.com/canopen/docs/installation/</a></li><li>Lely CANopen Documentation, “Library overview”: <a href="https://opensource.lely.com/canopen/docs/overview/">https://opensource.lely.com/canopen/docs/overview/</a></li><li>pkg-config Guide: <a href="https://people.freedesktop.org/~dbn/pkg-config-guide.html">https://people.freedesktop.org/~dbn/pkg-config-guide.html</a></li><li>pkgconf manual page: <a href="https://man.archlinux.org/man/pkgconf.1.en">https://man.archlinux.org/man/pkgconf.1.en</a></li><li>Autotools Mythbuster, “Supporting Cross-Compilation”: <a href="https://autotools.info/pkgconfig/cross-compiling.html">https://autotools.info/pkgconfig/cross-compiling.html</a></li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;meta name=&quot;referrer&quot; content=&quot;no-referrer&quot; /&gt;


&lt;h1 id=&quot;Lely-CANopen-交叉构建说明：为什么执行-make-install-DESTDIR-PWD-stage-，以及-lib-pkgconfig-的作用&quot;&gt;&lt;a </summary>
      
    
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/categories/canopen/"/>
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/tags/canopen/"/>
    
  </entry>
  
  <entry>
    <title>Lely CANopen configure 配置项与日志解读</title>
    <link href="https://wdfk-prog.space/posts/5f186818/"/>
    <id>https://wdfk-prog.space/posts/5f186818/</id>
    <published>2026-06-25T08:24:55.000Z</published>
    <updated>2026-06-25T08:25:42.856Z</updated>
    
    <content type="html"><![CDATA[<meta name="referrer" content="no-referrer" /><h1 id="Lely-CANopen-configure-配置项与日志解读"><a href="#Lely-CANopen-configure-配置项与日志解读" class="headerlink" title="Lely CANopen configure 配置项与日志解读"></a>Lely CANopen configure 配置项与日志解读</h1><p><img src="https://i-blog.csdnimg.cn/direct/9f8ef8dd24dd4769b9d42d6822537b1c.png" alt="在这里插入图片描述"></p><p>@[toc]</p><h2 id="1-configure-在构建链路中的位置"><a href="#1-configure-在构建链路中的位置" class="headerlink" title="1. configure 在构建链路中的位置"></a>1. configure 在构建链路中的位置</h2><p><code>configure</code> 属于 GNU Build System 的配置阶段。它不会直接把 Lely 源码编译成库，而是读取命令行参数、探测工具链和目标系统能力，然后生成 <code>Makefile</code>、<code>config.h</code>、<code>.pc</code> 文件和 libtool 相关脚本。后续 <code>make</code> 才进入编译阶段。</p><p><img src="https://i-blog.csdnimg.cn/direct/3c948953cf5547f8b921bdc93b047766.png" alt="在这里插入图片描述"></p><p>一个典型的交叉编译配置命令可以写成：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">../configure \</span><br><span class="line">  --host=aarch64-poky-linux \</span><br><span class="line">  --prefix=&lt;INSTALL_PREFIX&gt; \</span><br><span class="line">  --disable-python \</span><br><span class="line">  --disable-cython \</span><br><span class="line">  --disable-tests \</span><br><span class="line">  --disable-unit-tests</span><br></pre></td></tr></table></figure><p>这条命令包含两类信息：一类是“目标平台和安装位置”，另一类是“功能裁剪策略”。前者影响工具链选择和安装目录，后者影响 Python、测试、CANopen 模块、C++ 接口等是否进入构建目标。</p><h2 id="2-configure-可传入配置的主要类别"><a href="#2-configure-可传入配置的主要类别" class="headerlink" title="2. configure 可传入配置的主要类别"></a>2. configure 可传入配置的主要类别</h2><p><img src="https://i-blog.csdnimg.cn/direct/69503d4ef7004f6caf91bcba052aa807.png" alt="在这里插入图片描述"></p><p>下面按用途列出本地 <code>configure --help</code> 中出现的主要参数。表中的“禁用”通常意味着对应代码、构建目标或条件宏会被关闭；但具体是否彻底移除，还要看该模块与其他模块之间的依赖关系。</p><h3 id="2-1-通用控制"><a href="#2-1-通用控制" class="headerlink" title="2.1 通用控制"></a>2.1 通用控制</h3><table><thead><tr><th>参数</th><th>作用</th></tr></thead><tbody><tr><td><code>-h, --help</code></td><td>显示帮助并退出。</td></tr><tr><td><code>--help=short</code></td><td>只显示当前包相关的短帮助。</td></tr><tr><td><code>--help=recursive</code></td><td>显示当前包及子包的短帮助。</td></tr><tr><td><code>-V, --version</code></td><td>显示 configure 版本信息。</td></tr><tr><td><code>-q, --quiet, --silent</code></td><td>减少 <code>checking ...</code> 输出，适合脚本环境。</td></tr><tr><td><code>--cache-file=FILE</code></td><td>把探测结果缓存到指定文件；重复配置时可减少探测开销。</td></tr><tr><td><code>-C, --config-cache</code></td><td>等价于 <code>--cache-file=config.cache</code>。</td></tr><tr><td><code>-n, --no-create</code></td><td>只执行探测，不生成 Makefile&#x2F;config.h 等输出文件。</td></tr><tr><td><code>--srcdir=DIR</code></td><td>指定源码目录；用于 out-of-tree build。</td></tr></tbody></table><h3 id="2-2-安装路径"><a href="#2-2-安装路径" class="headerlink" title="2.2 安装路径"></a>2.2 安装路径</h3><table><thead><tr><th>参数</th><th>作用</th></tr></thead><tbody><tr><td><code>--prefix=PREFIX</code></td><td>安装根目录，默认 <code>/usr/local</code>。头文件、库、工具默认都会落到该前缀下。</td></tr><tr><td><code>--exec-prefix=EPREFIX</code></td><td>架构相关文件的安装根目录，默认继承 <code>PREFIX</code>。</td></tr><tr><td><code>--bindir=DIR</code></td><td>用户可执行程序安装目录，默认 <code>EPREFIX/bin</code>。</td></tr><tr><td><code>--sbindir=DIR</code></td><td>系统管理程序安装目录，默认 <code>EPREFIX/sbin</code>。</td></tr><tr><td><code>--libexecdir=DIR</code></td><td>内部辅助可执行程序安装目录。</td></tr><tr><td><code>--sysconfdir=DIR</code></td><td>只读单机配置目录，默认 <code>PREFIX/etc</code>。</td></tr><tr><td><code>--sharedstatedir=DIR</code></td><td>可修改、架构无关的共享状态目录。</td></tr><tr><td><code>--localstatedir=DIR</code></td><td>可修改、单机本地状态目录。</td></tr><tr><td><code>--runstatedir=DIR</code></td><td>运行期状态目录。</td></tr><tr><td><code>--libdir=DIR</code></td><td>库文件安装目录，交叉编译多架构安装时常显式指定。</td></tr><tr><td><code>--includedir=DIR</code></td><td>C&#x2F;C++ 头文件安装目录。</td></tr><tr><td><code>--oldincludedir=DIR</code></td><td>非 GCC 场景的传统头文件目录。</td></tr><tr><td><code>--datarootdir=DIR</code></td><td>架构无关只读数据根目录。</td></tr><tr><td><code>--datadir=DIR</code></td><td>架构无关数据目录。</td></tr><tr><td><code>--infodir=DIR</code></td><td>info 文档目录。</td></tr><tr><td><code>--localedir=DIR</code></td><td>本地化数据目录。</td></tr><tr><td><code>--mandir=DIR</code></td><td>man 文档目录。</td></tr><tr><td><code>--docdir=DIR</code></td><td>项目文档安装根目录。</td></tr><tr><td><code>--htmldir=DIR / --dvidir=DIR / --pdfdir=DIR / --psdir=DIR</code></td><td>不同格式文档的安装目录；是否生成文档还受 <code>--disable-doc</code>、Doxygen 等影响。</td></tr></tbody></table><h3 id="2-3-程序名变换"><a href="#2-3-程序名变换" class="headerlink" title="2.3 程序名变换"></a>2.3 程序名变换</h3><table><thead><tr><th>参数</th><th>作用</th></tr></thead><tbody><tr><td><code>--program-prefix=PREFIX</code></td><td>给安装的程序名添加前缀。</td></tr><tr><td><code>--program-suffix=SUFFIX</code></td><td>给安装的程序名添加后缀。</td></tr><tr><td><code>--program-transform-name=PROGRAM</code></td><td>用 sed 表达式转换安装程序名。</td></tr></tbody></table><h3 id="2-4-系统类型与交叉编译"><a href="#2-4-系统类型与交叉编译" class="headerlink" title="2.4 系统类型与交叉编译"></a>2.4 系统类型与交叉编译</h3><table><thead><tr><th>参数</th><th>作用</th></tr></thead><tbody><tr><td><code>--build=BUILD</code></td><td>构建机三元组；通常自动推断。</td></tr><tr><td><code>--host=HOST</code></td><td>目标运行平台三元组；与 build 不同时进入交叉编译。</td></tr><tr><td><code>--target=TARGET</code></td><td>主要用于构建编译器等工具链；普通库项目一般等于 host。</td></tr></tbody></table><h3 id="2-5-Autotools-Libtool-构建行为"><a href="#2-5-Autotools-Libtool-构建行为" class="headerlink" title="2.5 Autotools&#x2F;Libtool 构建行为"></a>2.5 Autotools&#x2F;Libtool 构建行为</h3><table><thead><tr><th>参数</th><th>作用</th></tr></thead><tbody><tr><td><code>--disable-option-checking</code></td><td>忽略无法识别的 <code>--enable-*</code> &#x2F; <code>--with-*</code> 选项。</td></tr><tr><td><code>--enable-silent-rules / --disable-silent-rules</code></td><td>控制 make 输出详细程度。</td></tr><tr><td><code>--disable-maintainer-mode</code></td><td>关闭维护者规则，避免普通构建触发 Autotools 重新生成。</td></tr><tr><td><code>--enable-code-coverage</code></td><td>开启覆盖率构建，通常用于测试和 CI。</td></tr><tr><td><code>--enable-valgrind</code></td><td>在单元测试中启用 Valgrind。</td></tr><tr><td><code>--disable-valgrind-memcheck</code></td><td>跳过 Valgrind memcheck 测试。</td></tr><tr><td><code>--disable-valgrind-helgrind</code></td><td>跳过 Valgrind helgrind 线程检测。</td></tr><tr><td><code>--disable-valgrind-drd</code></td><td>跳过 Valgrind drd 线程检测。</td></tr><tr><td><code>--disable-valgrind-sgcheck</code></td><td>跳过 Valgrind sgcheck。</td></tr><tr><td><code>--enable-dependency-tracking</code></td><td>启用依赖跟踪，增量构建更准确但可能更慢。</td></tr><tr><td><code>--disable-dependency-tracking</code></td><td>关闭依赖跟踪，加快一次性构建。</td></tr><tr><td><code>--enable-shared[=PKGS]</code></td><td>构建共享库，默认 yes。</td></tr><tr><td><code>--enable-static[=PKGS]</code></td><td>构建静态库，默认 yes。</td></tr><tr><td><code>--enable-fast-install[=PKGS]</code></td><td>启用 libtool 快速安装优化。</td></tr><tr><td><code>--disable-libtool-lock</code></td><td>禁用 libtool 锁；可能影响并行构建可靠性。</td></tr></tbody></table><h3 id="2-6-平台能力裁剪"><a href="#2-6-平台能力裁剪" class="headerlink" title="2.6 平台能力裁剪"></a>2.6 平台能力裁剪</h3><table><thead><tr><th>参数</th><th>作用</th></tr></thead><tbody><tr><td><code>--disable-rt</code></td><td>禁用 Realtime Extensions。</td></tr><tr><td><code>--disable-threads</code></td><td>禁用多线程支持；官方文档对应 <code>LELY_NO_THREADS</code>。</td></tr><tr><td><code>--enable-ecss-compliance</code></td><td>启用 ECSS 合规构建路径，主要面向特定合规&#x2F;测试需求。</td></tr><tr><td><code>--disable-errno</code></td><td>禁用 errno；官方文档说明这也会禁用 C++ 支持，因为 C++ API 依赖 errno 处理异常。</td></tr><tr><td><code>--disable-malloc</code></td><td>禁用动态内存分配；官方文档说明该支持仍是 preliminary，且会禁用 C++ 支持。</td></tr><tr><td><code>--disable-stdio</code></td><td>禁用标准 I&#x2F;O；errno 或 malloc 被禁用时也会自动禁用 stdio。</td></tr><tr><td><code>--disable-cxx</code></td><td>禁用 C++ 接口，并禁用整个 C++ CANopen application library。</td></tr><tr><td><code>--disable-daemon</code></td><td>禁用 daemon&#x2F;service 支持；stdio 被禁用时会自动禁用。</td></tr><tr><td><code>--disable-diag</code></td><td>禁用诊断函数输出；诊断接口仍可调用但无效果。</td></tr><tr><td><code>--disable-canfd</code></td><td>禁用 CAN FD，缩小通用 CAN 消息结构；编译库和应用时宏设置需要一致。</td></tr></tbody></table><h3 id="2-7-EDS-DCF-与对象字典裁剪"><a href="#2-7-EDS-DCF-与对象字典裁剪" class="headerlink" title="2.7 EDS&#x2F;DCF 与对象字典裁剪"></a>2.7 EDS&#x2F;DCF 与对象字典裁剪</h3><table><thead><tr><th>参数</th><th>作用</th></tr></thead><tbody><tr><td><code>--disable-dcf</code></td><td>禁用 EDS&#x2F;DCF 支持。</td></tr><tr><td><code>--disable-dcf-restore</code></td><td>不保存&#x2F;恢复 application parameters 的 concise DCF。</td></tr><tr><td><code>--disable-obj-default</code></td><td>禁用对象字典默认值。</td></tr><tr><td><code>--disable-obj-file</code></td><td>禁用对象字典 UploadFile&#x2F;DownloadFile 支持。</td></tr><tr><td><code>--disable-obj-limits</code></td><td>禁用对象字典最小值&#x2F;最大值约束。</td></tr><tr><td><code>--disable-obj-name</code></td><td>禁用对象字典名称字符串。</td></tr><tr><td><code>--disable-obj-upload</code></td><td>禁用对象字典自定义 upload indication 函数。</td></tr><tr><td><code>--disable-sdev</code></td><td>禁用 static device description 支持。</td></tr></tbody></table><h3 id="2-8-CANopen-协议服务裁剪"><a href="#2-8-CANopen-协议服务裁剪" class="headerlink" title="2.8 CANopen 协议服务裁剪"></a>2.8 CANopen 协议服务裁剪</h3><table><thead><tr><th>参数</th><th>作用</th></tr></thead><tbody><tr><td><code>--disable-ssdo-blk</code></td><td>禁用 Server-SDO block transfer。</td></tr><tr><td><code>--disable-csdo</code></td><td>禁用 Client-SDO。</td></tr><tr><td><code>--disable-rpdo</code></td><td>禁用 Receive-PDO。</td></tr><tr><td><code>--disable-tpdo</code></td><td>禁用 Transmit-PDO。</td></tr><tr><td><code>--disable-mpdo</code></td><td>禁用 Multiplex PDO。</td></tr><tr><td><code>--disable-sync</code></td><td>禁用 SYNC 对象。</td></tr><tr><td><code>--disable-time</code></td><td>禁用 TIME 时间戳对象。</td></tr><tr><td><code>--disable-emcy</code></td><td>禁用 EMCY emergency 对象。</td></tr><tr><td><code>--disable-lss</code></td><td>禁用 LSS Layer Setting Services。</td></tr><tr><td><code>--disable-wtm</code></td><td>禁用 WTM Wireless Transmission Media 支持。</td></tr><tr><td><code>--disable-master</code></td><td>禁用 CANopen master 支持。</td></tr><tr><td><code>--disable-ng</code></td><td>禁用 node guarding。</td></tr><tr><td><code>--disable-nmt-boot</code></td><td>禁用 NMT boot slave。</td></tr><tr><td><code>--disable-nmt-cfg</code></td><td>禁用 NMT configuration request。</td></tr><tr><td><code>--disable-gw</code></td><td>禁用 gateway 支持。</td></tr><tr><td><code>--disable-gw-txt</code></td><td>禁用 ASCII gateway 支持。</td></tr></tbody></table><h3 id="2-9-C-application、工具、测试、Python、文档"><a href="#2-9-C-application、工具、测试、Python、文档" class="headerlink" title="2.9 C++ application、工具、测试、Python、文档"></a>2.9 C++ application、工具、测试、Python、文档</h3><table><thead><tr><th>参数</th><th>作用</th></tr></thead><tbody><tr><td><code>--disable-coapp-master</code></td><td>禁用 C++ CANopen application master 支持。</td></tr><tr><td><code>--disable-coapp-slave</code></td><td>禁用 C++ CANopen application slave 支持。</td></tr><tr><td><code>--disable-tools</code></td><td>禁用命令行工具构建。</td></tr><tr><td><code>--disable-tests</code></td><td>禁用测试。</td></tr><tr><td><code>--disable-unit-tests</code></td><td>禁用单元测试。</td></tr><tr><td><code>--disable-unit-tests-memleak</code></td><td>禁用单元测试内存泄漏检测。</td></tr><tr><td><code>--disable-python</code></td><td>禁用 Python tools and bindings；官方交叉编译文档说明该选项会隐含 <code>--disable-cython</code>，并禁用 DCF tools。</td></tr><tr><td><code>--disable-python2</code></td><td>禁用 Python 2 tools and bindings。</td></tr><tr><td><code>--disable-python3</code></td><td>禁用 Python 3 tools and bindings。</td></tr><tr><td><code>--disable-cython</code></td><td>不使用 Cython 生成 Python bindings；官方文档说明该绑定已属于 deprecated Python bindings。</td></tr><tr><td><code>--disable-doc</code></td><td>禁用 Doxygen 文档生成。</td></tr></tbody></table><h3 id="2-10-外部包与工具链搜索"><a href="#2-10-外部包与工具链搜索" class="headerlink" title="2.10 外部包与工具链搜索"></a>2.10 外部包与工具链搜索</h3><table><thead><tr><th>参数</th><th>作用</th></tr></thead><tbody><tr><td><code>--with-gcov=GCOV</code></td><td>指定覆盖率工具 gcov。</td></tr><tr><td><code>--with-pic[=PKGS]</code></td><td>控制 PIC&#x2F;non-PIC 对象使用策略。</td></tr><tr><td>&#96;–with-aix-soname&#x3D;aix</td><td>svr4</td></tr><tr><td><code>--with-gnu-ld</code></td><td>假设 C 编译器使用 GNU ld。</td></tr><tr><td><code>--with-sysroot[=DIR]</code></td><td>在指定 sysroot 或编译器 sysroot 中搜索依赖库。</td></tr><tr><td><code>--with-ixxat[=DIR]</code></td><td>搜索 IXXAT VCI SDK；官方文档说明会在 DIR&#x2F;inc 下查找 VCI 头文件。</td></tr><tr><td><code>--with-vci[=DIR]</code></td><td>同 IXXAT VCI SDK 搜索相关。</td></tr></tbody></table><h3 id="2-11-常用环境变量"><a href="#2-11-常用环境变量" class="headerlink" title="2.11 常用环境变量"></a>2.11 常用环境变量</h3><table><thead><tr><th>参数</th><th>作用</th></tr></thead><tbody><tr><td><code>CC / CXX</code></td><td>指定 C&#x2F;C++ 编译器命令。</td></tr><tr><td><code>CFLAGS / CXXFLAGS</code></td><td>指定 C&#x2F;C++ 编译参数，例如优化、告警、目标架构。</td></tr><tr><td><code>CPPFLAGS</code></td><td>指定预处理参数，例如额外 include 路径。</td></tr><tr><td><code>LDFLAGS</code></td><td>指定链接器参数，例如额外库目录。</td></tr><tr><td><code>LIBS</code></td><td>显式追加链接库。</td></tr><tr><td><code>CPP / CXXCPP</code></td><td>指定 C&#x2F;C++ 预处理器。</td></tr><tr><td><code>PKG_CONFIG / PKG_CONFIG_PATH / PKG_CONFIG_LIBDIR</code></td><td>控制 pkg-config 工具及搜索路径；交叉编译时尤其重要。</td></tr><tr><td><code>CPPUTEST_CFLAGS / CPPUTEST_LIBS</code></td><td>覆盖 CppUTest 的编译与链接参数。</td></tr><tr><td><code>LT_SYS_LIBRARY_PATH</code></td><td>用户定义的运行期库搜索路径。</td></tr></tbody></table><h2 id="3-常见配置组合如何理解"><a href="#3-常见配置组合如何理解" class="headerlink" title="3. 常见配置组合如何理解"></a>3. 常见配置组合如何理解</h2><table><thead><tr><th>场景</th><th>配置示例</th><th>说明</th></tr></thead></table><p>| 交叉编译到 ARM64 Linux | <code>--host=aarch64-poky-linux --prefix=&lt;INSTALL_PREFIX&gt;</code> | <code>--host</code> 触发交叉编译路径；<code>--prefix</code> 控制安装根目录。 |</p><p>| 不构建 Python 工具&#x2F;绑定 | <code>--disable-python --disable-cython</code> | <code>--disable-python</code> 会关闭 Python tools and bindings，并隐含关闭 Cython 绑定；显式写出 <code>--disable-cython</code> 可增强命令可读性。 |</p><p>| 不跑测试 | <code>--disable-tests --disable-unit-tests</code> | 关闭测试和单元测试目标；这不等于 configure 完全不探测 CppUTest，因为脚本仍可能检查可选依赖。 |</p><p>| 只保留 C 库接口 | <code>--disable-cxx</code> | 关闭 C++ 接口和 C++ CANopen application library。 |</p><p>| 减少目标运行时依赖 | <code>--disable-threads --disable-stdio --disable-daemon</code> | 面向资源受限或无完整 POSIX 能力的运行时；需要确认应用是否依赖这些能力。 |</p><p>| 不需要 CAN FD | <code>--disable-canfd</code> | 缩小 CAN 消息结构；库和应用编译宏需要保持一致。 |</p><p>| 不生成文档 | <code>--disable-doc</code> | 避免 Doxygen&#x2F;Graphviz 成为文档生成路径的依赖。 |</p><h2 id="4-configure-日志应按阶段阅读"><a href="#4-configure-日志应按阶段阅读" class="headerlink" title="4. configure 日志应按阶段阅读"></a>4. configure 日志应按阶段阅读</h2><p><img src="https://i-blog.csdnimg.cn/direct/185c582522fc4e40b4b141e0eb04557f.png" alt="在这里插入图片描述"></p><p>configure 日志的格式通常是 <code>checking ... yes/no/value</code>。其中 <code>yes</code> 表示检查通过，<code>no</code> 表示未找到或不支持，<code>(cached)</code> 表示复用缓存。判断一条日志是否异常，不能只看 <code>yes/no</code>，还要看它检查的是必需依赖、可选依赖，还是被配置项关闭的功能。</p><p><img src="https://i-blog.csdnimg.cn/direct/cefe4037ffff4254baaf16fa73a55491.png" alt="在这里插入图片描述"></p><h2 id="5-输入日志逐段说明"><a href="#5-输入日志逐段说明" class="headerlink" title="5. 输入日志逐段说明"></a>5. 输入日志逐段说明</h2><h3 id="5-1-命令行与-site-script"><a href="#5-1-命令行与-site-script" class="headerlink" title="5.1 命令行与 site script"></a>5.1 命令行与 site script</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">../configure --host=aarch64-poky-linux --prefix=&lt;INSTALL_PREFIX&gt; \</span><br><span class="line">  --disable-python --disable-cython --disable-tests --disable-unit-tests</span><br><span class="line">configure: loading site script &lt;YOCTO_SDK&gt;/site-config-armv8a-poky-linux</span><br></pre></td></tr></table></figure><p>第一行是传入 configure 的显式策略：目标平台由 <code>--host</code> 指定，安装根目录由 <code>--prefix</code> 指定，Python、Cython、测试和单元测试被关闭。第二行说明 Yocto SDK 提供了 site 配置，configure 会从中读取交叉编译相关默认值，例如 sysroot、工具链路径或缓存结果。</p><h3 id="5-2-build-host-target"><a href="#5-2-build-host-target" class="headerlink" title="5.2 build &#x2F; host &#x2F; target"></a>5.2 build &#x2F; host &#x2F; target</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">checking build system type... x86_64-pc-linux-gnu</span><br><span class="line">checking host system type... aarch64-poky-linux-gnu</span><br><span class="line">checking target system type... aarch64-poky-linux-gnu</span><br><span class="line">checking whether we are cross compiling... yes</span><br></pre></td></tr></table></figure><p><code>build</code> 是执行构建命令的机器，<code>host</code> 是生成程序未来运行的目标系统，<code>target</code> 对普通库项目通常跟 host 一致。这里 build 与 host 不同，因此进入交叉编译路径。</p><h3 id="5-3-基础构建工具"><a href="#5-3-基础构建工具" class="headerlink" title="5.3 基础构建工具"></a>5.3 基础构建工具</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">checking for a BSD-compatible install... /usr/bin/install -c</span><br><span class="line">checking for a thread-safe mkdir -p... /usr/bin/mkdir -p</span><br><span class="line">checking for gawk... gawk</span><br><span class="line">checking whether make sets $(MAKE)... yes</span><br><span class="line">checking for a sed that does not truncate output... (cached) sed</span><br></pre></td></tr></table></figure><p>这些是 Autotools&#x2F;Automake 构建脚本所需的基础命令。<code>(cached)</code> 表示该结果已被缓存或前面已经探测过，不是异常。</p><h3 id="5-4-覆盖率与-Valgrind-开关"><a href="#5-4-覆盖率与-Valgrind-开关" class="headerlink" title="5.4 覆盖率与 Valgrind 开关"></a>5.4 覆盖率与 Valgrind 开关</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">checking whether to build with code coverage support... no</span><br><span class="line">checking for valgrind... no</span><br></pre></td></tr></table></figure><p>覆盖率未启用，Valgrind 可执行程序未找到。若没有开启相关测试目标，这类 <code>no</code> 不构成构建中断。后面又检测到 <code>valgrind/valgrind.h</code>，说明头文件与可执行程序是两类独立检查。</p><h3 id="5-5-C-编译器探测"><a href="#5-5-C-编译器探测" class="headerlink" title="5.5 C 编译器探测"></a>5.5 C 编译器探测</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">checking for aarch64-poky-linux-gcc... aarch64-poky-linux-gcc ... --sysroot=&lt;TARGET_SYSROOT&gt;</span><br><span class="line">checking whether the C compiler works... yes</span><br><span class="line">checking whether we are using the GNU C compiler... yes</span><br><span class="line">checking ... option to accept ISO C89... none needed</span><br><span class="line">checking ... option to accept ISO C99... none needed</span><br><span class="line">checking how to run the C preprocessor... aarch64-poky-linux-gcc -E ...</span><br></pre></td></tr></table></figure><p>configure 找到了交叉 C 编译器，并验证它可以编译最小测试程序。<code>none needed</code> 表示编译器默认模式已能满足对应 C 标准测试，无需额外 <code>-std=</code> 参数。</p><h3 id="5-6-系统头文件探测"><a href="#5-6-系统头文件探测" class="headerlink" title="5.6 系统头文件探测"></a>5.6 系统头文件探测</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">checking for ANSI C header files... yes</span><br><span class="line">checking for sys/types.h... yes</span><br><span class="line">checking for stdint.h... yes</span><br><span class="line">checking minix/config.h usability... no</span><br><span class="line">checking whether it is safe to define __EXTENSIONS__... yes</span><br></pre></td></tr></table></figure><p>这些检查用于判断目标 sysroot 中的 C 库头文件能力。<code>minix/config.h... no</code> 只是说明目标系统不是 Minix 风格，不是错误。<code>__EXTENSIONS__</code> 与系统兼容宏有关。</p><h3 id="5-7-C-编译器与-C-11"><a href="#5-7-C-编译器与-C-11" class="headerlink" title="5.7 C++ 编译器与 C++11"></a>5.7 C++ 编译器与 C++11</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">checking whether we are using the GNU C++ compiler... yes</span><br><span class="line">checking whether aarch64-poky-linux-g++ supports C++11 features with -std=gnu++11... yes</span><br></pre></td></tr></table></figure><p>Lely core 是 C 库为主，但提供部分 C++ 接口和 C++ CANopen application library。该段确认目标 C++ 编译器可用，并能通过 <code>-std=gnu++11</code> 支持 C++11。</p><h3 id="5-8-Libtool、链接器与-binutils"><a href="#5-8-Libtool、链接器与-binutils" class="headerlink" title="5.8 Libtool、链接器与 binutils"></a>5.8 Libtool、链接器与 binutils</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">checking for ld used by aarch64-poky-linux-gcc... aarch64-poky-linux-ld --sysroot=&lt;TARGET_SYSROOT&gt;</span><br><span class="line">checking if the linker ... is GNU ld... yes</span><br><span class="line">checking for BSD- or MS-compatible name lister (nm)... aarch64-poky-linux-nm</span><br><span class="line">checking for aarch64-poky-linux-ar... aarch64-poky-linux-ar</span><br><span class="line">checking for aarch64-poky-linux-ranlib... aarch64-poky-linux-ranlib</span><br></pre></td></tr></table></figure><p>这一段属于 libtool 初始化。它确定链接器、符号表工具、归档工具和 ranlib 是否与目标三元组匹配，这决定后续 <code>.so</code>、<code>.a</code> 和 libtool wrapper 的生成方式。</p><h3 id="5-9-非前缀工具警告"><a href="#5-9-非前缀工具警告" class="headerlink" title="5.9 非前缀工具警告"></a>5.9 非前缀工具警告</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">checking for aarch64-poky-linux-mt... no</span><br><span class="line">checking for mt... mt</span><br><span class="line">configure: WARNING: using cross tools not prefixed with host triplet</span><br><span class="line">checking if mt is a manifest tool... no</span><br></pre></td></tr></table></figure><p><code>mt</code> 是 Windows manifest 相关工具。当前目标是 Linux&#x2F;ARM64，日志中虽然出现 warning，但紧接着判断 <code>mt</code> 不是 manifest tool；对 Linux CANopen 库构建一般不是关键路径。文章中应解释为“非关键工具探测警告”，而不是当作主失败点。</p><h3 id="5-10-PIC、共享库与静态链接测试"><a href="#5-10-PIC、共享库与静态链接测试" class="headerlink" title="5.10 PIC、共享库与静态链接测试"></a>5.10 PIC、共享库与静态链接测试</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">checking ... option to produce PIC... -fPIC -DPIC</span><br><span class="line">checking ... PIC flag -fPIC -DPIC works... yes</span><br><span class="line">checking ... static flag -static works... no</span><br><span class="line">checking whether ... linker supports shared libraries... yes</span><br><span class="line">checking whether to build shared libraries... yes</span><br><span class="line">checking whether to build static libraries... yes</span><br></pre></td></tr></table></figure><p><code>-fPIC</code> 通过，说明可生成位置无关代码，满足共享库构建。<code>-static works... no</code> 表示用 <code>-static</code> 生成完全静态可执行文件的测试没有通过；它不等同于不能生成静态库，因为后面 <code>build static libraries... yes</code> 仍然成立。</p><h3 id="5-11-运行时库、线程和-pkg-config"><a href="#5-11-运行时库、线程和-pkg-config" class="headerlink" title="5.11 运行时库、线程和 pkg-config"></a>5.11 运行时库、线程和 pkg-config</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">checking for qemu-aarch64... qemu-aarch64</span><br><span class="line">checking for timer_create in -lrt... yes</span><br><span class="line">checking whether pthreads work with &quot;-pthread&quot; and &quot;-lpthread&quot;... yes</span><br><span class="line">checking for PTHREAD_PRIO_INHERIT... yes</span><br><span class="line">checking for aarch64-poky-linux-pkg-config... no</span><br><span class="line">checking for pkg-config... &lt;SDK_HOST_SYSROOT&gt;/usr/bin/pkg-config</span><br><span class="line">checking pkg-config is at least version 0.9.0... yes</span><br></pre></td></tr></table></figure><p>这部分确认 QEMU、POSIX timer、pthread 和 pkg-config。未找到带目标前缀的 pkg-config，但找到了 SDK host sysroot 中的 pkg-config；交叉编译场景需要进一步关注它是否配套设置了 <code>PKG_CONFIG_SYSROOT_DIR</code>、<code>PKG_CONFIG_LIBDIR</code> 等。</p><h3 id="5-12-测试、Valgrind-头文件、SocketCAN-与-Linux-接口"><a href="#5-12-测试、Valgrind-头文件、SocketCAN-与-Linux-接口" class="headerlink" title="5.12 测试、Valgrind 头文件、SocketCAN 与 Linux 接口"></a>5.12 测试、Valgrind 头文件、SocketCAN 与 Linux 接口</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">checking for CPPUTEST... no</span><br><span class="line">checking valgrind/valgrind.h usability... yes</span><br><span class="line">checking linux/can.h usability... yes</span><br><span class="line">checking linux/can/raw.h presence... yes</span><br><span class="line">checking sys/epoll.h presence... yes</span><br><span class="line">checking sys/ioctl.h presence... yes</span><br></pre></td></tr></table></figure><p>CppUTest 未找到，但单元测试已通过配置参数关闭；SocketCAN、CAN error、CAN raw、netlink、rtnetlink、epoll、ioctl 等 Linux 头文件可用，说明目标 sysroot 支持 Lely Linux CAN&#x2F;I&#x2F;O 相关构建路径。</p><h3 id="5-13-Bluetooth-与-BlueZ"><a href="#5-13-Bluetooth-与-BlueZ" class="headerlink" title="5.13 Bluetooth 与 BlueZ"></a>5.13 Bluetooth 与 BlueZ</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">checking for bluetooth/bluetooth.h... yes</span><br><span class="line">checking for bluetooth/rfcomm.h... yes</span><br><span class="line">checking for library containing str2ba... -lbluetooth</span><br></pre></td></tr></table></figure><p>Lely 的 I&#x2F;O 层在 Linux 下可探测 BlueZ&#x2F;Bluetooth RFCOMM。官方安装文档说明 Bluetooth 支持不是 CANopen stack 的必需条件，因此这类能力更多影响可选 I&#x2F;O 支持。</p><h3 id="5-14-Doxygen-与生成阶段"><a href="#5-14-Doxygen-与生成阶段" class="headerlink" title="5.14 Doxygen 与生成阶段"></a>5.14 Doxygen 与生成阶段</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">checking for doxygen... no</span><br><span class="line">checking that generated files are newer than configure... done</span><br><span class="line">configure: creating ./config.status</span><br><span class="line">config.status: creating Makefile</span><br><span class="line">config.status: creating config.h</span><br><span class="line">config.status: executing depfiles commands</span><br><span class="line">config.status: executing libtool commands</span><br></pre></td></tr></table></figure><p>Doxygen 未找到只影响 API 文档生成。末尾进入 <code>config.status</code> 并生成顶层 Makefile、各子目录 Makefile、pkg-config 文件和 <code>config.h</code>，表示 configure 已完成“生成构建系统文件”这一阶段。</p><h2 id="6-日志中容易误读的点"><a href="#6-日志中容易误读的点" class="headerlink" title="6. 日志中容易误读的点"></a>6. 日志中容易误读的点</h2><table><thead><tr><th>日志现象</th><th>正确理解</th></tr></thead></table><p>| <code>checking for CPPUTEST... no</code> | CppUTest 未找到；如果单元测试目标已关闭，它不是主构建路径的中断点。 |</p><p>| <code>checking for doxygen... no</code> | Doxygen 未找到；通常只影响 API 文档生成。 |</p><p>| <code>checking ... static flag -static works... no</code> | 表示完全静态链接测试未通过；不等于不能生成 <code>.a</code> 静态库。 |</p><p>| <code>configure: WARNING: using cross tools not prefixed with host triplet</code> | 某个工具没有目标三元组前缀；需要结合具体工具判断。这里出现在 <code>mt</code> 相关探测中，通常不是 Linux&#x2F;ARM CANopen 库的关键工具。 |</p><p>| <code>python/.../Makefile</code> 仍被创建 | Autotools 可生成子目录 Makefile，但实际是否参与构建由 Automake 条件和 configure 变量决定。 |</p><p>| <code>pkg-config</code> 没有目标前缀 | 交叉编译中常见；关键是是否使用 SDK 提供的 pkg-config，并且路径变量是否指向目标 sysroot。 |</p><h2 id="7-判断-configure-是否进入生成阶段"><a href="#7-判断-configure-是否进入生成阶段" class="headerlink" title="7. 判断 configure 是否进入生成阶段"></a>7. 判断 configure 是否进入生成阶段</h2><p>configure 日志末尾出现以下模式时，说明它已经完成探测并开始生成构建系统文件：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">configure: creating ./config.status</span><br><span class="line">config.status: creating Makefile</span><br><span class="line">config.status: creating config.h</span><br><span class="line">config.status: executing depfiles commands</span><br><span class="line">config.status: executing libtool commands</span><br></pre></td></tr></table></figure><p>如果出现 <code>configure: error: ...</code>，则通常表示配置阶段被中断，需要根据错误项补依赖、修工具链路径或调整 <code>--enable/--disable/--with</code> 参数。</p><h2 id="9-参考资料"><a href="#9-参考资料" class="headerlink" title="9. 参考资料"></a>9. 参考资料</h2><ul><li><p>Lely CANopen：Installation。说明 GNU Build System 构建流程、依赖项、<code>autoreconf -i</code>、<code>../configure --disable-cython</code>、<code>make</code>、<code>make check</code>、<code>make install</code>。</p></li><li><p>Lely CANopen：Cross-compilation。说明 <code>--host=HOST</code> 交叉编译，以及 <code>--disable-python</code> 会隐含 <code>--disable-cython</code> 并禁用 DCF tools。</p></li><li><p>Lely CANopen：Build configuration。说明 <code>--disable-cxx</code>、<code>--disable-threads</code>、<code>--disable-errno</code>、<code>--disable-malloc</code>、<code>--disable-stdio</code>、<code>--disable-daemon</code>、<code>--disable-diag</code>、<code>--disable-canfd</code> 等配置项和对应预处理宏。</p></li><li><p>Lely core libraries Doxygen 首页。说明可通过 <code>./configure --help</code> 查看完整配置项。</p></li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;meta name=&quot;referrer&quot; content=&quot;no-referrer&quot; /&gt;


&lt;h1 id=&quot;Lely-CANopen-configure-配置项与日志解读&quot;&gt;&lt;a href=&quot;#Lely-CANopen-configure-配置项与日志解读&quot; class=&quot;</summary>
      
    
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/categories/canopen/"/>
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/tags/canopen/"/>
    
  </entry>
  
  <entry>
    <title>从 `autoreconf -i` 看懂 Autoconf：以 Lely CANopen 交叉编译为例</title>
    <link href="https://wdfk-prog.space/posts/d676ae7f/"/>
    <id>https://wdfk-prog.space/posts/d676ae7f/</id>
    <published>2026-06-25T07:35:20.000Z</published>
    <updated>2026-06-25T07:37:36.291Z</updated>
    
    <content type="html"><![CDATA[<meta name="referrer" content="no-referrer" /><h1 id="从-autoreconf-i-看懂-Autoconf：以-Lely-CANopen-交叉编译为例"><a href="#从-autoreconf-i-看懂-Autoconf：以-Lely-CANopen-交叉编译为例" class="headerlink" title="从 autoreconf -i 看懂 Autoconf：以 Lely CANopen 交叉编译为例"></a>从 <code>autoreconf -i</code> 看懂 Autoconf：以 Lely CANopen 交叉编译为例</h1><blockquote><p>本文面向第一次接触 Autotools &#x2F; Autoconf 的读者。目标不是背命令，而是看懂：<code>autoreconf -i</code> 为什么要执行、从哪个文件开始、会生成什么文件、这些文件又怎样参与 Lely CANopen 的交叉编译。</p></blockquote><p><img src="https://i-blog.csdnimg.cn/direct/21b8c65c1e85437bab0f9165fc294b51.png" alt="在这里插入图片描述"></p><p>@[toc]</p><h2 id="先给结论"><a href="#先给结论" class="headerlink" title="先给结论"></a>先给结论</h2><p><code>autoreconf -i</code> 的作用是：<strong>根据源码中的 Autotools 描述文件，生成后续构建需要的 <code>configure</code> 脚本、<code>Makefile.in</code> 模板、<code>config.h.in</code> 模板和一批辅助脚本</strong>。</p><p>它不是编译命令，也不会生成 ARM&#x2F;aarch64 目标程序。真正编译 Lely 的动作发生在后面的 <code>make</code> 阶段。</p><p>完整链路如下：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[configure.ac&lt;br/&gt;Autoconf 入口] --&gt; B[autoreconf -i]</span><br><span class="line">    C[Makefile.am&lt;br/&gt;Automake 输入] --&gt; B</span><br><span class="line">    D[m4/*.m4&lt;br/&gt;宏文件] --&gt; B</span><br><span class="line"></span><br><span class="line">    B --&gt; E[configure&lt;br/&gt;配置脚本]</span><br><span class="line">    B --&gt; F[Makefile.in&lt;br/&gt;Makefile 模板]</span><br><span class="line">    B --&gt; G[config.h.in&lt;br/&gt;配置头模板]</span><br><span class="line">    B --&gt; H[辅助脚本&lt;br/&gt;config.guess / config.sub / depcomp / ltmain.sh]</span><br><span class="line"></span><br><span class="line">    E --&gt; I[../configure --host=&amp;lt;TARGET_TRIPLET&amp;gt;]</span><br><span class="line">    F --&gt; I</span><br><span class="line">    G --&gt; I</span><br><span class="line"></span><br><span class="line">    I --&gt; J[Makefile]</span><br><span class="line">    I --&gt; K[config.h]</span><br><span class="line">    I --&gt; L[libtool]</span><br><span class="line">    I --&gt; M[pkgconfig/*.pc]</span><br><span class="line"></span><br><span class="line">    J --&gt; N[make]</span><br><span class="line">    N --&gt; O[.o / .so / .a / coctl]</span><br><span class="line">    O --&gt; P[make install DESTDIR=stage]</span><br></pre></td></tr></table></figure><p>一句话记忆：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">configure.ac + Makefile.am</span><br><span class="line">        ↓ autoreconf -i</span><br><span class="line">configure + Makefile.in</span><br><span class="line">        ↓ ../configure --host=...</span><br><span class="line">Makefile + config.h</span><br><span class="line">        ↓ make</span><br><span class="line">真正编译 Lely</span><br></pre></td></tr></table></figure><h2 id="为什么会有-Autoconf"><a href="#为什么会有-Autoconf" class="headerlink" title="为什么会有 Autoconf"></a>为什么会有 Autoconf</h2><p>C&#x2F;C++ 项目在不同平台上构建时，不能只靠一份固定 <code>Makefile</code>。原因包括：</p><ul><li>编译器名字可能不同，例如 <code>gcc</code>、<code>clang</code>、<code>aarch64-poky-linux-gcc</code>。</li><li>系统头文件可能不同，例如 Linux 下才有 SocketCAN 相关头文件。</li><li>库的名字和链接参数可能不同，例如 <code>pthread</code>、<code>rt</code>。</li><li>目标平台可能不是当前主机，例如 x86 Ubuntu 上交叉编译到 ARM64 Linux。</li><li>项目可能要按配置开启或关闭功能，例如 Python、Cython、测试、共享库、静态库。</li></ul><p>GNU Autoconf 官方说明：Autoconf 是一套 M4 宏，用来生成可以自动配置源码包的 shell 脚本；这些脚本会根据不同类 Unix 系统的特性调整构建配置。参考来源：<a href="https://www.gnu.org/software/autoconf/">GNU Autoconf introduction</a>。</p><p>所以 Autoconf 的核心工作不是“编译”，而是生成并运行一个配置脚本：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Autoconf 输入：configure.ac</span><br><span class="line">Autoconf 输出：configure</span><br><span class="line">configure 输出：Makefile、config.h 等真正构建文件</span><br></pre></td></tr></table></figure><h2 id="autoreconf-i-不是一个工具在单干"><a href="#autoreconf-i-不是一个工具在单干" class="headerlink" title="autoreconf -i 不是一个工具在单干"></a><code>autoreconf -i</code> 不是一个工具在单干</h2><p><code>autoreconf</code> 可以理解成 Autotools 的总调度器。它会按照项目需要调用多个工具。</p><p>GNU Automake 手册说明，<code>autoreconf</code> 会以正确顺序调用 <code>autoconf</code>、<code>automake</code> 和相关命令；核心分工是：Autoconf 根据 <code>configure.ac</code> 生成 <code>configure</code>，Automake 根据 <code>Makefile.am</code> 和 <code>configure.ac</code> 生成 <code>Makefile.in</code>。参考来源：<a href="https://www.gnu.org/software/automake/manual/html_node/Creating-amhello.html">GNU Automake: Creating amhello</a>。</p><p>在 Lely 这类项目中，可以把 <code>autoreconf -i</code> 简化理解成：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    A[autoreconf -i] --&gt; B[aclocal]</span><br><span class="line">    A --&gt; C[libtoolize]</span><br><span class="line">    A --&gt; D[autoheader]</span><br><span class="line">    A --&gt; E[autoconf]</span><br><span class="line">    A --&gt; F[automake]</span><br><span class="line"></span><br><span class="line">    B --&gt; B1[生成 aclocal.m4]</span><br><span class="line">    C --&gt; C1[复制 ltmain.sh 和 m4/libtool*.m4]</span><br><span class="line">    D --&gt; D1[生成 config.h.in]</span><br><span class="line">    E --&gt; E1[生成 configure]</span><br><span class="line">    F --&gt; F1[生成 Makefile.in]</span><br></pre></td></tr></table></figure><p>各工具职责如下：</p><table><thead><tr><th>工具</th><th>输入</th><th>输出</th><th>作用</th></tr></thead><tbody><tr><td><code>aclocal</code></td><td><code>configure.ac</code>、<code>m4/*.m4</code>、系统宏</td><td><code>aclocal.m4</code></td><td>收集宏定义，供 Autoconf 使用</td></tr><tr><td><code>libtoolize</code></td><td><code>configure.ac</code> 中的 Libtool 宏</td><td><code>ltmain.sh</code>、<code>m4/libtool*.m4</code></td><td>准备动态库&#x2F;静态库构建支持</td></tr><tr><td><code>autoheader</code></td><td><code>configure.ac</code></td><td><code>config.h.in</code></td><td>生成配置头文件模板</td></tr><tr><td><code>autoconf</code></td><td><code>configure.ac</code>、<code>aclocal.m4</code></td><td><code>configure</code></td><td>生成配置脚本</td></tr><tr><td><code>automake</code></td><td><code>Makefile.am</code>、<code>configure.ac</code></td><td><code>Makefile.in</code></td><td>生成 Makefile 模板</td></tr></tbody></table><h2 id="i-是什么作用"><a href="#i-是什么作用" class="headerlink" title="-i 是什么作用"></a><code>-i</code> 是什么作用</h2><p><code>-i</code> 是 <code>--install</code> 的短写。</p><p>它的作用是：<strong>发现项目缺少标准辅助文件时，自动复制这些文件到源码树中</strong>。</p><p>GNU Autoconf 手册说明，<code>autoreconf --install</code> 会安装缺失的标准辅助文件；默认是复制文件，也可以配合 <code>--symlink</code> 改成符号链接。它还可能触发 <code>automake --add-missing</code>、<code>libtoolize</code> 等动作。参考来源：<a href="https://www.gnu.org/software/autoconf/manual/autoconf-2.72/html_node/autoreconf-Invocation.html">GNU Autoconf: autoreconf invocation</a>。</p><p>典型辅助文件包括：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">compile</span><br><span class="line">config.guess</span><br><span class="line">config.sub</span><br><span class="line">install-sh</span><br><span class="line">missing</span><br><span class="line">depcomp</span><br><span class="line">test-driver</span><br><span class="line">tap-driver.sh</span><br><span class="line">ltmain.sh</span><br><span class="line">m4/libtool.m4</span><br><span class="line">m4/ltoptions.m4</span><br><span class="line">m4/ltsugar.m4</span><br><span class="line">m4/ltversion.m4</span><br><span class="line">m4/lt~obsolete.m4</span><br></pre></td></tr></table></figure><p>这些文件大多不是项目业务源码，而是构建系统为了跨平台、依赖跟踪、测试和库构建而使用的脚本或宏文件。</p><h2 id="什么时候需要执行-autoreconf-i"><a href="#什么时候需要执行-autoreconf-i" class="headerlink" title="什么时候需要执行 autoreconf -i"></a>什么时候需要执行 <code>autoreconf -i</code></h2><p>通常在这些情况下需要执行：</p><table><thead><tr><th>场景</th><th align="right">是否需要</th><th>原因</th></tr></thead><tbody><tr><td>从 Git 仓库克隆 Lely 源码后首次构建</td><td align="right">需要</td><td>源码树可能没有现成 <code>configure</code>，需要先生成</td></tr><tr><td>修改了 <code>configure.ac</code></td><td align="right">需要</td><td><code>configure</code> 由它生成，入口变了要重新生成</td></tr><tr><td>修改了某个 <code>Makefile.am</code></td><td align="right">需要</td><td><code>Makefile.in</code> 由它生成，模板变了要重新生成</td></tr><tr><td>更新了 Autoconf &#x2F; Automake &#x2F; Libtool 版本</td><td align="right">建议</td><td>生成文件可能需要按新工具刷新</td></tr><tr><td>只是重新执行 <code>make</code></td><td align="right">通常不需要</td><td>已有 <code>Makefile</code> 时，直接编译即可</td></tr><tr><td>只是换一个 <code>--host</code> 重新配置</td><td align="right">不一定需要</td><td>一般重新跑 <code>../configure --host=...</code> 即可</td></tr></tbody></table><p>Lely 仓库说明：项目使用 GNU Build System，即 <code>configure</code>、<code>make</code>、<code>make install</code>；初次 clone 或下载源码后，需要在项目根目录运行 <code>autoreconf -i</code> 生成 <code>configure</code> 脚本，这一步在 <code>configure.ac</code> 或 <code>Makefile.am</code> 变化时才需要重复。参考来源：<a href="https://github.com/lely-industries/lely-core">Lely core README</a>。</p><h2 id="补充：M4-宏到底是什么"><a href="#补充：M4-宏到底是什么" class="headerlink" title="补充：M4 宏到底是什么"></a>补充：M4 宏到底是什么</h2><p>前面多次提到 <code>m4/*.m4</code>，这里单独解释。先给结论：<strong>M4 是一个文本宏处理器；<code>.m4</code> 文件就是给 Autoconf 使用的宏定义文件。</strong></p><p>GNU M4 手册对 M4 的定义是：M4 会把输入复制到输出，同时展开遇到的宏；宏可以是内置宏，也可以是用户自定义宏，并且可以带参数。参考来源：<a href="https://www.gnu.org/software/m4/manual/m4.html">GNU M4 manual</a>。</p><p>可以先把它理解成“比 C 预处理器更通用的文本展开器”：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">输入文本里写：某个宏名(参数1, 参数2)</span><br><span class="line">        ↓ M4 展开</span><br><span class="line">输出文本里变成：一段更长、更具体的文本</span><br></pre></td></tr></table></figure><p>在 Autoconf 体系里，M4 的输出通常不是 C 代码，而是 shell 脚本片段。例如：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">AC_CHECK_HEADER([linux/can.h])</span><br></pre></td></tr></table></figure><p>这不是 shell 命令本身，而是一个 Autoconf 宏调用。Autoconf 会借助 M4 把它展开成一段 shell 检测逻辑，用来判断目标构建环境里是否有 <code>linux/can.h</code> 这个头文件。</p><p>所以关系是：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    A[configure.ac&lt;br/&gt;写着 AC_ / AM_ / AX_ / LT_ 宏] --&gt; B[Autoconf]</span><br><span class="line">    C[m4/*.m4&lt;br/&gt;宏定义] --&gt; B</span><br><span class="line">    D[aclocal.m4&lt;br/&gt;宏汇总] --&gt; B</span><br><span class="line">    B --&gt; E[configure&lt;br/&gt;shell 脚本]</span><br></pre></td></tr></table></figure><h3 id="M4-宏和-C-宏有什么区别"><a href="#M4-宏和-C-宏有什么区别" class="headerlink" title="M4 宏和 C 宏有什么区别"></a>M4 宏和 C 宏有什么区别</h3><table><thead><tr><th>对比项</th><th>C 预处理宏</th><th>M4 &#x2F; Autoconf 宏</th></tr></thead><tbody><tr><td>常见文件</td><td><code>.h</code>、<code>.c</code>、<code>.cpp</code></td><td><code>.m4</code>、<code>configure.ac</code></td></tr><tr><td>处理工具</td><td>C&#x2F;C++ 预处理器</td><td><code>m4</code> &#x2F; <code>autoconf</code> &#x2F; <code>aclocal</code></td></tr><tr><td>输入</td><td>C&#x2F;C++ 源码</td><td>Autoconf 配置描述文本</td></tr><tr><td>输出</td><td>预处理后的 C&#x2F;C++ 源码</td><td><code>configure</code> shell 脚本、<code>aclocal.m4</code> 等</td></tr><tr><td>用途</td><td>条件编译、常量、代码替换</td><td>生成平台检测逻辑、生成构建脚本</td></tr></tbody></table><p>一句话：<strong>C 宏服务于编译源码；M4 宏服务于生成构建脚本。</strong></p><h2 id="为什么-Lely-的-m4-目录里有这些-m4-文件"><a href="#为什么-Lely-的-m4-目录里有这些-m4-文件" class="headerlink" title="为什么 Lely 的 m4/ 目录里有这些 .m4 文件"></a>为什么 Lely 的 <code>m4/</code> 目录里有这些 <code>.m4</code> 文件</h2><p>你看到的目录内容类似：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">-rw-rw-r--  1 &lt;USER&gt; &lt;GROUP&gt;   1415 &lt;DATE&gt; ax_check_python.m4</span><br><span class="line">-rw-rw-r--  1 &lt;USER&gt; &lt;GROUP&gt;  12111 &lt;DATE&gt; ax_code_coverage.m4</span><br><span class="line">-rw-rw-r--  1 &lt;USER&gt; &lt;GROUP&gt;  21126 &lt;DATE&gt; ax_cxx_compile_stdcxx.m4</span><br><span class="line">-rw-rw-r--  1 &lt;USER&gt; &lt;GROUP&gt;  22556 &lt;DATE&gt; ax_pthread.m4</span><br><span class="line">-rw-rw-r--  1 &lt;USER&gt; &lt;GROUP&gt;   8939 &lt;DATE&gt; ax_valgrind_check.m4</span><br><span class="line">-rw-r--r--  1 &lt;USER&gt; &lt;GROUP&gt; 306675 &lt;DATE&gt; libtool.m4</span><br><span class="line">-rw-r--r--  1 &lt;USER&gt; &lt;GROUP&gt;   6140 &lt;DATE&gt; lt~obsolete.m4</span><br><span class="line">-rw-r--r--  1 &lt;USER&gt; &lt;GROUP&gt;  14514 &lt;DATE&gt; ltoptions.m4</span><br><span class="line">-rw-r--r--  1 &lt;USER&gt; &lt;GROUP&gt;   4384 &lt;DATE&gt; ltsugar.m4</span><br><span class="line">-rw-r--r--  1 &lt;USER&gt; &lt;GROUP&gt;    699 &lt;DATE&gt; ltversion.m4</span><br></pre></td></tr></table></figure><p>这些文件大体分两类：</p><table><thead><tr><th>文件类型</th><th>示例</th><th>来源</th><th>作用</th></tr></thead><tbody><tr><td>项目自带 &#x2F; 第三方 Autoconf 宏</td><td><code>ax_check_python.m4</code>、<code>ax_pthread.m4</code>、<code>ax_cxx_compile_stdcxx.m4</code></td><td>Lely 仓库中已有，或来自 Autoconf Archive &#x2F; 项目维护者</td><td>给 <code>configure.ac</code> 提供额外检测能力</td></tr><tr><td>Libtool 宏</td><td><code>libtool.m4</code>、<code>ltoptions.m4</code>、<code>ltsugar.m4</code>、<code>ltversion.m4</code>、<code>lt~obsolete.m4</code></td><td><code>libtoolize</code> 从系统 Libtool 安装目录复制</td><td>支持生成跨平台库构建逻辑</td></tr></tbody></table><p>Lely 的 <code>configure.ac</code> 中指定了：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">AC_CONFIG_MACRO_DIR([m4])</span><br></pre></td></tr></table></figure><p>这表示：<strong>项目本地的 Autoconf 宏目录是 <code>m4/</code>。</strong> Autoconf&#x2F;Automake&#x2F;Libtool 在处理这个项目时，会把这个目录作为项目宏目录使用。Lely 仓库的 <code>m4/</code> 目录中列出了 <code>ax_check_python.m4</code>、<code>ax_code_coverage.m4</code>、<code>ax_cxx_compile_stdcxx.m4</code>、<code>ax_pthread.m4</code> 和 <code>ax_valgrind_check.m4</code> 等文件。参考来源：<a href="https://github.com/lely-industries/lely-core/tree/master/m4">lely-core&#x2F;m4</a>。</p><h2 id="这些-m4-文件是怎么“生成”或“复制”出来的"><a href="#这些-m4-文件是怎么“生成”或“复制”出来的" class="headerlink" title="这些 .m4 文件是怎么“生成”或“复制”出来的"></a>这些 <code>.m4</code> 文件是怎么“生成”或“复制”出来的</h2><p>这里要区分三件事：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">项目已有的 .m4 文件</span><br><span class="line">自动复制进来的 .m4 文件</span><br><span class="line">汇总生成的 aclocal.m4 文件</span><br></pre></td></tr></table></figure><h3 id="1-ax-m4：通常不是本次-autoreconf-i-临时生成的"><a href="#1-ax-m4：通常不是本次-autoreconf-i-临时生成的" class="headerlink" title="1. ax_*.m4：通常不是本次 autoreconf -i 临时生成的"></a>1. <code>ax_*.m4</code>：通常不是本次 <code>autoreconf -i</code> 临时生成的</h3><p>像这些文件：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">ax_check_python.m4</span><br><span class="line">ax_code_coverage.m4</span><br><span class="line">ax_cxx_compile_stdcxx.m4</span><br><span class="line">ax_pthread.m4</span><br><span class="line">ax_valgrind_check.m4</span><br></pre></td></tr></table></figure><p>一般属于 <strong>Autoconf 扩展宏</strong>。</p><p>它们的名字以 <code>AX_</code> 开头，常见来源有两种：</p><ol><li>项目维护者自己写的宏。</li><li>从 GNU Autoconf Archive 复制进项目的宏。</li></ol><p>GNU Autoconf Archive 是一个收集大量可复用 Autoconf 宏的项目；其仓库说明它收集了 500 多个由社区贡献的 GNU Autoconf 宏。参考来源：<a href="https://github.com/autoconf-archive/autoconf-archive">GNU Autoconf Archive mirror</a>。</p><p>在 Lely 里，<code>ax_check_python.m4</code> 是 Lely 自己维护的宏文件，文件说明中写明它定义 <code>AX_CHECK_PYTHON(MAJOR-VERSION, ...)</code>，用于检查指定 Python 主版本是否可用，并设置 <code>PYTHON2</code> 或 <code>PYTHON3</code> 这类输出变量。参考来源：<a href="https://github.com/lely-industries/lely-core/blob/master/m4/ax_check_python.m4">lely-core&#x2F;m4&#x2F;ax_check_python.m4</a>。</p><p><code>ax_pthread.m4</code> 这类宏的典型作用是检测 POSIX threads。GNU Autoconf Archive 对 <code>AX_PTHREAD</code> 的说明是：它会判断如何构建使用 POSIX threads 的 C 程序，并设置 <code>PTHREAD_LIBS</code>、<code>PTHREAD_CFLAGS</code> 等输出变量。参考来源：<a href="https://www.gnu.org/software/autoconf-archive/ax_pthread.html">Autoconf Archive: AX_PTHREAD</a>。</p><h3 id="2-libtool-m4-lt-m4：通常是-libtoolize-复制进来的"><a href="#2-libtool-m4-lt-m4：通常是-libtoolize-复制进来的" class="headerlink" title="2. libtool.m4 &#x2F; lt*.m4：通常是 libtoolize 复制进来的"></a>2. <code>libtool.m4</code> &#x2F; <code>lt*.m4</code>：通常是 <code>libtoolize</code> 复制进来的</h3><p>你看到的这些文件：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">libtool.m4</span><br><span class="line">ltoptions.m4</span><br><span class="line">ltsugar.m4</span><br><span class="line">ltversion.m4</span><br><span class="line">lt~obsolete.m4</span><br></pre></td></tr></table></figure><p>来自 GNU Libtool。</p><p>Lely 的 <code>configure.ac</code> 里有：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">LT_INIT([win32-dll])</span><br></pre></td></tr></table></figure><p>这个宏表示项目启用 Libtool 支持。<code>autoreconf -i</code> 看到项目使用 Libtool 后，会调用 <code>libtoolize</code>。<code>libtoolize</code> 会把 Libtool 所需文件复制到项目中，例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">ltmain.sh</span><br><span class="line">m4/libtool.m4</span><br><span class="line">m4/ltoptions.m4</span><br><span class="line">m4/ltsugar.m4</span><br><span class="line">m4/ltversion.m4</span><br><span class="line">m4/lt~obsolete.m4</span><br></pre></td></tr></table></figure><p>GNU Libtool 手册说明，Libtool 用一个通用接口隐藏不同平台构建共享库和静态库时的差异。参考来源：<a href="https://www.gnu.org/s/libtool/manual/libtool.html">GNU Libtool manual</a>。</p><p>所以这几个文件不是 Lely 业务代码，也不是编译产物，而是 <strong>Libtool 的宏和脚本支持文件</strong>。</p><h3 id="3-aclocal-m4：这是-aclocal-汇总生成的"><a href="#3-aclocal-m4：这是-aclocal-汇总生成的" class="headerlink" title="3. aclocal.m4：这是 aclocal 汇总生成的"></a>3. <code>aclocal.m4</code>：这是 <code>aclocal</code> 汇总生成的</h3><p><code>m4/*.m4</code> 是单个宏文件，而：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">aclocal.m4</span><br></pre></td></tr></table></figure><p>是 <code>aclocal</code> 生成的宏汇总文件。</p><p>处理关系如下：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[configure.ac&lt;br/&gt;使用 AC_ / AM_ / AX_ / LT_ 宏] --&gt; B[aclocal -I m4]</span><br><span class="line">    C[m4/ax_*.m4&lt;br/&gt;项目宏] --&gt; B</span><br><span class="line">    D[m4/libtool.m4 + lt*.m4&lt;br/&gt;Libtool 宏] --&gt; B</span><br><span class="line">    E[系统 aclocal 宏目录&lt;br/&gt;Automake / Autoconf Archive 宏] --&gt; B</span><br><span class="line">    B --&gt; F[aclocal.m4&lt;br/&gt;宏汇总]</span><br><span class="line">    F --&gt; G[autoconf]</span><br><span class="line">    A --&gt; G</span><br><span class="line">    G --&gt; H[configure]</span><br></pre></td></tr></table></figure><p>因此可以这样记：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">m4/*.m4      是宏来源文件</span><br><span class="line">aclocal.m4   是宏汇总文件</span><br><span class="line">configure    是宏展开后生成的 shell 配置脚本</span><br></pre></td></tr></table></figure><h2 id="libtoolize-又是哪来的"><a href="#libtoolize-又是哪来的" class="headerlink" title="libtoolize 又是哪来的"></a><code>libtoolize</code> 又是哪来的</h2><p><code>libtoolize</code> 是 GNU Libtool 软件包提供的命令，不是 Lely 自己写的命令。</p><p>可以用下面命令确认它来自哪里：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">which</span> libtoolize</span><br><span class="line">libtoolize --version</span><br></pre></td></tr></table></figure><p>常见位置类似：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">/usr/bin/libtoolize</span><br></pre></td></tr></table></figure><p>它通常由系统包管理器安装，例如 Ubuntu&#x2F;Debian 系统中常由 <code>libtool</code> 包提供。</p><p>它被调用的条件通常是：</p><ol><li>项目 <code>configure.ac</code> 中使用了 <code>LT_INIT</code> 等 Libtool 宏。</li><li>执行 <code>autoreconf -i</code>。</li><li><code>autoreconf</code> 判断需要初始化&#x2F;刷新 Libtool 支持文件。</li></ol><p>Lely 中正是因为 <code>configure.ac</code> 里有 <code>LT_INIT([win32-dll])</code>，并且设置了 <code>AC_CONFIG_MACRO_DIR([m4])</code>，所以 <code>libtoolize</code> 会把 Libtool 的宏复制到 <code>m4/</code>，把 <code>ltmain.sh</code> 复制到源码根目录。</p><h2 id="这些文件逐个是干嘛的"><a href="#这些文件逐个是干嘛的" class="headerlink" title="这些文件逐个是干嘛的"></a>这些文件逐个是干嘛的</h2><table><thead><tr><th>文件</th><th>类别</th><th>主要作用</th><th>在 Lely 构建中的意义</th></tr></thead><tbody><tr><td><code>ax_check_python.m4</code></td><td>Lely 自定义 AX 宏</td><td>检查指定 Python 主版本，设置 Python 解释器变量</td><td>支持 Lely 配置 Python 相关工具；交叉编译时可用 <code>--disable-python</code> 降低复杂度</td></tr><tr><td><code>ax_code_coverage.m4</code></td><td>AX 扩展宏</td><td>增加代码覆盖率相关配置和 make 规则</td><td>主要服务测试&#x2F;覆盖率，不是目标板运行 <code>coctl</code> 的核心依赖</td></tr><tr><td><code>ax_cxx_compile_stdcxx.m4</code></td><td>AX 扩展宏</td><td>检查 C++ 标准支持，必要时添加 <code>-std=...</code></td><td>Lely 有 C++ 接口，例如 <code>liblely-coapp</code>，所以需要检测 C++ 编译能力</td></tr><tr><td><code>ax_pthread.m4</code></td><td>AX 扩展宏</td><td>检查 pthread 编译&#x2F;链接参数</td><td>Lely 在 Linux 下通常需要线程支持</td></tr><tr><td><code>ax_valgrind_check.m4</code></td><td>AX 扩展宏</td><td>增加 Valgrind 测试相关规则</td><td>主要用于开发测试，不是部署到目标板的核心依赖</td></tr><tr><td><code>libtool.m4</code></td><td>Libtool 宏</td><td>定义 Libtool 主宏，例如 <code>LT_INIT</code> 相关展开逻辑</td><td>支持生成跨平台库构建逻辑</td></tr><tr><td><code>ltoptions.m4</code></td><td>Libtool 宏</td><td>处理 Libtool 配置选项</td><td>支持 <code>--enable-shared</code>、<code>--enable-static</code> 等库构建选项</td></tr><tr><td><code>ltsugar.m4</code></td><td>Libtool 宏</td><td>Libtool 内部辅助宏</td><td>给 Libtool 宏展开使用，通常不手工改</td></tr><tr><td><code>ltversion.m4</code></td><td>Libtool 宏</td><td>记录 Libtool 宏版本信息</td><td>用于版本一致性检查</td></tr><tr><td><code>lt~obsolete.m4</code></td><td>Libtool 宏</td><td>兼容旧 Libtool 宏</td><td>避免旧宏调用导致配置失败</td></tr></tbody></table><h2 id="这些-m4-文件在-Lely-中怎么被用起来"><a href="#这些-m4-文件在-Lely-中怎么被用起来" class="headerlink" title="这些 .m4 文件在 Lely 中怎么被用起来"></a>这些 <code>.m4</code> 文件在 Lely 中怎么被用起来</h2><p>结合 Lely，完整链路是：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[configure.ac] --&gt; B[声明 AC_CONFIG_MACRO_DIR\([m4]\)]</span><br><span class="line">    A --&gt; C[调用 AX_CHECK_PYTHON / AX_PTHREAD / LT_INIT 等宏]</span><br><span class="line">    D[m4/ax_*.m4] --&gt; E[aclocal 收集]</span><br><span class="line">    F[m4/libtool.m4 + lt*.m4] --&gt; E</span><br><span class="line">    E --&gt; G[aclocal.m4]</span><br><span class="line">    G --&gt; H[autoconf]</span><br><span class="line">    A --&gt; H</span><br><span class="line">    H --&gt; I[configure]</span><br><span class="line">    I --&gt; J[../configure --host=&amp;lt;TARGET_TRIPLET&amp;gt;]</span><br><span class="line">    J --&gt; K[Makefile / config.h / libtool / pkgconfig/*.pc]</span><br></pre></td></tr></table></figure><p>分阶段看：</p><table><thead><tr><th>阶段</th><th>读哪些文件</th><th>生成什么</th><th>说明</th></tr></thead><tbody><tr><td><code>libtoolize</code></td><td><code>configure.ac</code></td><td><code>ltmain.sh</code>、<code>m4/libtool.m4</code>、<code>m4/lt*.m4</code></td><td>因为 Lely 使用 <code>LT_INIT</code></td></tr><tr><td><code>aclocal</code></td><td><code>configure.ac</code>、<code>m4/*.m4</code></td><td><code>aclocal.m4</code></td><td>把宏定义汇总起来</td></tr><tr><td><code>autoconf</code></td><td><code>configure.ac</code>、<code>aclocal.m4</code></td><td><code>configure</code></td><td>把 M4 宏展开成 shell 脚本</td></tr><tr><td><code>automake</code></td><td><code>Makefile.am</code>、<code>configure.ac</code></td><td><code>Makefile.in</code></td><td>生成 Makefile 模板</td></tr><tr><td><code>../configure</code></td><td><code>configure</code>、<code>Makefile.in</code>、<code>config.h.in</code></td><td><code>Makefile</code>、<code>config.h</code>、<code>libtool</code>、<code>.pc</code></td><td>按目标平台生成最终构建文件</td></tr></tbody></table><h2 id="为什么有些-m4-看起来像“生成的”"><a href="#为什么有些-m4-看起来像“生成的”" class="headerlink" title="为什么有些 .m4 看起来像“生成的”"></a>为什么有些 <code>.m4</code> 看起来像“生成的”</h2><p>判断一个 <code>.m4</code> 是项目自带还是工具复制，不能只看时间戳。你执行 <code>autoreconf -i</code> 后，很多文件的修改时间会变成同一时间，所以容易误以为都是“生成”的。</p><p>更准确的判断方法是：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 如果是 Git 仓库，先看文件是否被版本管理</span></span><br><span class="line"> git ls-files m4/*.m4</span><br><span class="line"></span><br><span class="line"><span class="comment"># 看执行 autoreconf 后哪些文件变成未跟踪或被修改</span></span><br><span class="line"> git status --short</span><br><span class="line"></span><br><span class="line"><span class="comment"># 看某个文件来源和头部说明</span></span><br><span class="line"> <span class="built_in">head</span> -40 m4/ax_check_python.m4</span><br><span class="line"> <span class="built_in">head</span> -40 m4/libtool.m4</span><br></pre></td></tr></table></figure><p>常见结论：</p><table><thead><tr><th>现象</th><th>更可能的原因</th></tr></thead><tbody><tr><td><code>ax_*.m4</code> 本来就在仓库里</td><td>项目维护者把宏随源码一起提交，保证别人构建时不依赖系统额外宏包</td></tr><tr><td><code>libtool.m4</code>、<code>lt*.m4</code> 执行后出现或刷新</td><td><code>libtoolize</code> 从系统 Libtool 安装目录复制</td></tr><tr><td><code>aclocal.m4</code> 执行后生成或刷新</td><td><code>aclocal</code> 汇总所有需要的宏</td></tr><tr><td><code>configure</code> 执行后生成或刷新</td><td><code>autoconf</code> 根据 <code>configure.ac</code> 和宏定义生成</td></tr></tbody></table><h2 id="对你这批文件的直接结论"><a href="#对你这批文件的直接结论" class="headerlink" title="对你这批文件的直接结论"></a>对你这批文件的直接结论</h2><p>你列出来的文件可以这样理解：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">m4/ax_*.m4</span><br><span class="line">    作用：给 configure.ac 提供额外检测宏</span><br><span class="line">    来源：Lely 自带或来自 Autoconf Archive / 项目维护者</span><br><span class="line">    是否编译进程序：否</span><br><span class="line">    是否部署到板端：否</span><br><span class="line"></span><br><span class="line">m4/libtool.m4 和 m4/lt*.m4</span><br><span class="line">    作用：给 Libtool 提供宏定义</span><br><span class="line">    来源：libtoolize 从系统 Libtool 复制</span><br><span class="line">    是否编译进程序：否</span><br><span class="line">    是否部署到板端：否</span><br><span class="line"></span><br><span class="line">aclocal.m4</span><br><span class="line">    作用：汇总 m4/*.m4 和系统宏</span><br><span class="line">    来源：aclocal 生成</span><br><span class="line">    是否编译进程序：否</span><br><span class="line">    是否部署到板端：否</span><br></pre></td></tr></table></figure><p>这些文件最终只服务于一件事：<strong>让 <code>autoconf</code> 能够生成正确的 <code>configure</code>，再让 <code>configure</code> 生成适合当前目标平台的构建文件。</strong></p><h2 id="Lely-中的入口文件是-configure-ac"><a href="#Lely-中的入口文件是-configure-ac" class="headerlink" title="Lely 中的入口文件是 configure.ac"></a>Lely 中的入口文件是 <code>configure.ac</code></h2><p>在 Lely 源码根目录中，Autotools 的主入口是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">&lt;LELY_SRC&gt;/configure.ac</span><br></pre></td></tr></table></figure><p>Lely 的 <code>configure.ac</code> 中包含这些关键宏：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">AC_PREREQ([2.69])</span><br><span class="line">LT_PREREQ(2.4.2)</span><br><span class="line">AC_INIT([Lely core libraries], [2.4.0], [], [lely-core])</span><br><span class="line">AC_CONFIG_MACRO_DIR([m4])</span><br><span class="line">AC_CANONICAL_TARGET</span><br><span class="line">AM_INIT_AUTOMAKE([foreign subdir-objects])</span><br><span class="line">AC_LANG([C])</span><br><span class="line">AC_PROG_CC_STDC</span><br><span class="line">AC_LANG([C++])</span><br><span class="line">AC_PROG_CXX</span><br><span class="line">LT_INIT([win32-dll])</span><br><span class="line">...</span><br><span class="line">AC_OUTPUT</span><br></pre></td></tr></table></figure><p>这些宏决定了 Lely 的配置系统要做什么。Lely 当前公开仓库中的 <code>configure.ac</code> 确实包含 <code>AC_INIT</code>、<code>AC_CONFIG_MACRO_DIR([m4])</code>、<code>AC_CANONICAL_TARGET</code>、<code>AM_INIT_AUTOMAKE</code>、<code>AC_PROG_CXX</code> 和 <code>LT_INIT([win32-dll])</code> 等宏。参考来源：<a href="https://github.com/lely-industries/lely-core/blob/master/configure.ac">lely-core&#x2F;configure.ac</a>。</p><h3 id="关键宏解释"><a href="#关键宏解释" class="headerlink" title="关键宏解释"></a>关键宏解释</h3><table><thead><tr><th>宏</th><th>在 Lely 中的作用</th></tr></thead><tbody><tr><td><code>AC_INIT</code></td><td>定义包名、版本等基础信息</td></tr><tr><td><code>AC_CONFIG_MACRO_DIR([m4])</code></td><td>指定额外宏目录是 <code>m4/</code></td></tr><tr><td><code>AC_CANONICAL_TARGET</code></td><td>启用平台三元组识别，例如 <code>&lt;TARGET_TRIPLET&gt;</code></td></tr><tr><td><code>AM_INIT_AUTOMAKE</code></td><td>启用 Automake，根据 <code>Makefile.am</code> 生成 <code>Makefile.in</code></td></tr><tr><td><code>AC_PROG_CC_STDC</code></td><td>检查 C 编译器能力</td></tr><tr><td><code>AC_PROG_CXX</code></td><td>检查 C++ 编译器能力</td></tr><tr><td><code>LT_INIT</code></td><td>启用 Libtool，用于构建库文件</td></tr><tr><td><code>AC_OUTPUT</code></td><td>触发输出文件生成，例如 Makefile、config.status 等</td></tr></tbody></table><p>GNU Autoconf 手册说明，每个 <code>configure</code> 脚本必须调用 <code>AC_INIT</code>，另一个必需宏是 <code>AC_OUTPUT</code>；<code>AC_OUTPUT</code> 会生成并运行 <code>config.status</code>，由它创建 Makefile 和其他配置输出文件。参考来源：<a href="https://www.gnu.org/software/autoconf/manual/autoconf-2.70/html_node/Initializing-configure.html">GNU Autoconf: Initializing configure</a>、<a href="https://www.gnu.org/software/autoconf/manual/autoconf-2.60/html_node/Output.html">GNU Autoconf: Outputting files</a>。</p><h2 id="Lely-中还有哪些输入文件"><a href="#Lely-中还有哪些输入文件" class="headerlink" title="Lely 中还有哪些输入文件"></a>Lely 中还有哪些输入文件</h2><p>除了 <code>configure.ac</code>，Lely 还会使用多个 <code>Makefile.am</code>。这些文件是 Automake 的输入。</p><p>GNU Automake 手册说明，Automake 会从 <code>Makefile.am</code> 自动生成 <code>Makefile.in</code>；<code>Makefile.am</code> 本质上是一系列 make 变量定义，偶尔带有规则。参考来源：<a href="https://www.gnu.org/software/automake/manual/html_node/Introduction.html">GNU Automake introduction</a>。</p><p>在 Lely 中，常见输入关系如下：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">&lt;LELY_SRC&gt;/configure.ac</span><br><span class="line">&lt;LELY_SRC&gt;/Makefile.am</span><br><span class="line">&lt;LELY_SRC&gt;/src/can/Makefile.am</span><br><span class="line">&lt;LELY_SRC&gt;/src/co/Makefile.am</span><br><span class="line">&lt;LELY_SRC&gt;/src/coapp/Makefile.am</span><br><span class="line">&lt;LELY_SRC&gt;/src/io/Makefile.am</span><br><span class="line">&lt;LELY_SRC&gt;/src/io2/Makefile.am</span><br><span class="line">&lt;LELY_SRC&gt;/tools/Makefile.am</span><br><span class="line">&lt;LELY_SRC&gt;/test/Makefile.am</span><br><span class="line">&lt;LELY_SRC&gt;/unit-tests/Makefile.am</span><br><span class="line">&lt;LELY_SRC&gt;/m4/*.m4</span><br></pre></td></tr></table></figure><p>这些输入文件经过 <code>autoreconf -i</code> 后，会产生对应的模板文件：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">&lt;LELY_SRC&gt;/configure</span><br><span class="line">&lt;LELY_SRC&gt;/Makefile.in</span><br><span class="line">&lt;LELY_SRC&gt;/src/can/Makefile.in</span><br><span class="line">&lt;LELY_SRC&gt;/src/co/Makefile.in</span><br><span class="line">&lt;LELY_SRC&gt;/src/coapp/Makefile.in</span><br><span class="line">&lt;LELY_SRC&gt;/src/io/Makefile.in</span><br><span class="line">&lt;LELY_SRC&gt;/src/io2/Makefile.in</span><br><span class="line">&lt;LELY_SRC&gt;/tools/Makefile.in</span><br><span class="line">&lt;LELY_SRC&gt;/test/Makefile.in</span><br><span class="line">&lt;LELY_SRC&gt;/unit-tests/Makefile.in</span><br><span class="line">&lt;LELY_SRC&gt;/config.h.in</span><br><span class="line">&lt;LELY_SRC&gt;/aclocal.m4</span><br><span class="line">&lt;LELY_SRC&gt;/ltmain.sh</span><br><span class="line">&lt;LELY_SRC&gt;/m4/libtool*.m4</span><br></pre></td></tr></table></figure><h2 id="Lely-中执行-autoreconf-i-的位置"><a href="#Lely-中执行-autoreconf-i-的位置" class="headerlink" title="Lely 中执行 autoreconf -i 的位置"></a>Lely 中执行 <code>autoreconf -i</code> 的位置</h2><p>在 Lely 源码根目录执行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">cd</span> &lt;LELY_SRC&gt;</span><br><span class="line">autoreconf -i</span><br></pre></td></tr></table></figure><p>注意：必须在有 <code>configure.ac</code> 的目录执行。不要在 <code>build</code> 目录里执行。</p><p>正确顺序：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 1. 进入源码目录</span></span><br><span class="line"><span class="built_in">cd</span> &lt;LELY_SRC&gt;</span><br><span class="line"></span><br><span class="line"><span class="comment"># 2. 生成 configure 和 Makefile.in 等文件</span></span><br><span class="line">autoreconf -i</span><br><span class="line"></span><br><span class="line"><span class="comment"># 3. 再创建独立构建目录</span></span><br><span class="line"><span class="built_in">mkdir</span> -p &lt;BUILD_DIR&gt;</span><br><span class="line"><span class="built_in">cd</span> &lt;BUILD_DIR&gt;</span><br><span class="line"></span><br><span class="line"><span class="comment"># 4. 执行 configure，生成真正 Makefile</span></span><br><span class="line">../configure \</span><br><span class="line">    --host=&lt;TARGET_TRIPLET&gt; \</span><br><span class="line">    --prefix=&lt;INSTALL_PREFIX&gt; \</span><br><span class="line">    --disable-python \</span><br><span class="line">    --disable-cython</span><br><span class="line"></span><br><span class="line"><span class="comment"># 5. 真正开始编译</span></span><br><span class="line">make -j<span class="string">&quot;<span class="subst">$(nproc)</span>&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 6. 安装到临时 staging 目录</span></span><br><span class="line">make install DESTDIR=<span class="string">&quot;<span class="variable">$&#123;PWD&#125;</span>/stage&quot;</span></span><br></pre></td></tr></table></figure><p>Lely 官方交叉编译文档给出的流程也是：clone 源码后进入 <code>lely-core</code>，执行 <code>autoreconf -i</code>，创建 build 目录，然后执行 <code>../configure --host=HOST --disable-python</code>，最后 <code>make</code> 和 <code>make install</code>。参考来源：<a href="https://opensource.lely.com/canopen/docs/cross-compilation/">Lely CANopen cross-compilation</a>。</p><h2 id="autoreconf-i-之后文件怎样流动"><a href="#autoreconf-i-之后文件怎样流动" class="headerlink" title="autoreconf -i 之后文件怎样流动"></a><code>autoreconf -i</code> 之后文件怎样流动</h2><p>下面这张图从“文件输入输出”的角度看整个流程：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    subgraph Source[源码目录：&lt;LELY_SRC&gt;]</span><br><span class="line">        A[configure.ac]</span><br><span class="line">        B[Makefile.am]</span><br><span class="line">        C[src/*/Makefile.am]</span><br><span class="line">        D[m4/*.m4]</span><br><span class="line">    end</span><br><span class="line"></span><br><span class="line">    subgraph Bootstrap[执行 autoreconf -i]</span><br><span class="line">        E[aclocal]</span><br><span class="line">        F[autoconf]</span><br><span class="line">        G[autoheader]</span><br><span class="line">        H[automake]</span><br><span class="line">        I[libtoolize]</span><br><span class="line">    end</span><br><span class="line"></span><br><span class="line">    subgraph Generated[源码目录中新生成或更新]</span><br><span class="line">        J[configure]</span><br><span class="line">        K[aclocal.m4]</span><br><span class="line">        L[config.h.in]</span><br><span class="line">        M[Makefile.in]</span><br><span class="line">        N[src/*/Makefile.in]</span><br><span class="line">        O[ltmain.sh / m4/libtool*.m4]</span><br><span class="line">        P[config.guess / config.sub / depcomp 等]</span><br><span class="line">    end</span><br><span class="line"></span><br><span class="line">    subgraph Build[构建目录：&lt;BUILD_DIR&gt;]</span><br><span class="line">        Q[../configure --host=&lt;TARGET_TRIPLET&gt;]</span><br><span class="line">        R[Makefile]</span><br><span class="line">        S[config.h]</span><br><span class="line">        T[libtool]</span><br><span class="line">        U[pkgconfig/*.pc]</span><br><span class="line">        V[make]</span><br><span class="line">        W[目标库和工具]</span><br><span class="line">    end</span><br><span class="line"></span><br><span class="line">    A --&gt; E</span><br><span class="line">    D --&gt; E</span><br><span class="line">    E --&gt; K</span><br><span class="line">    A --&gt; F</span><br><span class="line">    K --&gt; F</span><br><span class="line">    F --&gt; J</span><br><span class="line">    A --&gt; G</span><br><span class="line">    G --&gt; L</span><br><span class="line">    B --&gt; H</span><br><span class="line">    C --&gt; H</span><br><span class="line">    H --&gt; M</span><br><span class="line">    H --&gt; N</span><br><span class="line">    A --&gt; I</span><br><span class="line">    I --&gt; O</span><br><span class="line">    I --&gt; P</span><br><span class="line"></span><br><span class="line">    J --&gt; Q</span><br><span class="line">    M --&gt; Q</span><br><span class="line">    N --&gt; Q</span><br><span class="line">    L --&gt; Q</span><br><span class="line">    Q --&gt; R</span><br><span class="line">    Q --&gt; S</span><br><span class="line">    Q --&gt; T</span><br><span class="line">    Q --&gt; U</span><br><span class="line">    R --&gt; V</span><br><span class="line">    V --&gt; W</span><br></pre></td></tr></table></figure><h2 id="你看到的输出是什么意思"><a href="#你看到的输出是什么意思" class="headerlink" title="你看到的输出是什么意思"></a>你看到的输出是什么意思</h2><p>示例输出：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">libtoolize: putting auxiliary files in &#x27;.&#x27;.</span><br><span class="line">libtoolize: copying file &#x27;./ltmain.sh&#x27;</span><br><span class="line">libtoolize: putting macros in AC_CONFIG_MACRO_DIRS, &#x27;m4&#x27;.</span><br><span class="line">libtoolize: copying file &#x27;m4/libtool.m4&#x27;</span><br><span class="line">libtoolize: copying file &#x27;m4/ltoptions.m4&#x27;</span><br><span class="line">libtoolize: copying file &#x27;m4/ltsugar.m4&#x27;</span><br><span class="line">libtoolize: copying file &#x27;m4/ltversion.m4&#x27;</span><br><span class="line">libtoolize: copying file &#x27;m4/lt~obsolete.m4&#x27;</span><br><span class="line">configure.ac:17: installing &#x27;./compile&#x27;</span><br><span class="line">configure.ac:6: installing &#x27;./config.guess&#x27;</span><br><span class="line">configure.ac:6: installing &#x27;./config.sub&#x27;</span><br><span class="line">configure.ac:7: installing &#x27;./install-sh&#x27;</span><br><span class="line">configure.ac:7: installing &#x27;./missing&#x27;</span><br><span class="line">configure.ac:614: installing &#x27;./tap-driver.sh&#x27;</span><br><span class="line">src/can/Makefile.am: installing &#x27;./depcomp&#x27;</span><br><span class="line">parallel-tests: installing &#x27;./test-driver&#x27;</span><br></pre></td></tr></table></figure><p>这些不是错误，是正常的“补齐构建系统文件”日志。</p><table><thead><tr><th>输出</th><th>含义</th></tr></thead><tbody><tr><td><code>copying file &#39;./ltmain.sh&#39;</code></td><td>Lely 使用 Libtool，需要复制 Libtool 主脚本</td></tr><tr><td><code>copying file &#39;m4/libtool.m4&#39;</code></td><td>把 Libtool 的 M4 宏放到 <code>m4/</code> 目录</td></tr><tr><td><code>installing &#39;./compile&#39;</code></td><td>安装通用编译包装脚本</td></tr><tr><td><code>installing &#39;./config.guess&#39;</code></td><td>安装平台识别脚本，用于猜测系统类型</td></tr><tr><td><code>installing &#39;./config.sub&#39;</code></td><td>安装平台三元组规范化脚本</td></tr><tr><td><code>installing &#39;./install-sh&#39;</code></td><td>安装通用安装脚本</td></tr><tr><td><code>installing &#39;./missing&#39;</code></td><td>安装缺失工具提示脚本</td></tr><tr><td><code>installing &#39;./tap-driver.sh&#39;</code></td><td>安装 TAP 测试驱动脚本</td></tr><tr><td><code>installing &#39;./depcomp&#39;</code></td><td>安装依赖跟踪脚本，用于头文件依赖分析</td></tr><tr><td><code>installing &#39;./test-driver&#39;</code></td><td>安装 Automake 测试驱动脚本</td></tr></tbody></table><p>如果这一步失败，通常后面不会有可执行的 <code>configure</code>。如果这一步成功，下一步才是执行 <code>../configure</code>。</p><h2 id="configure-host-又做什么"><a href="#configure-host-又做什么" class="headerlink" title="../configure --host=... 又做什么"></a><code>../configure --host=...</code> 又做什么</h2><p><code>autoreconf -i</code> 生成的是通用模板，尚未绑定你的交叉编译目标。真正指定目标架构的是：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">../configure --host=&lt;TARGET_TRIPLET&gt; ...</span><br></pre></td></tr></table></figure><p>它会做几类事情：</p><ol><li>检查交叉编译器是否存在。</li><li>判断目标平台类型。</li><li>根据 <code>--enable-*</code> &#x2F; <code>--disable-*</code> 参数决定功能开关。</li><li>根据 <code>Makefile.in</code> 生成当前构建目录下的 <code>Makefile</code>。</li><li>根据 <code>config.h.in</code> 生成 <code>config.h</code>。</li><li>生成 <code>libtool</code>，用于后续构建库文件。</li><li>生成 <code>pkgconfig/*.pc</code>，供后续应用通过 <code>pkg-config</code> 链接 Lely。</li></ol><p>输出关系如下：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    A[configure] --&gt; D[../configure --host=&lt;TARGET_TRIPLET&gt;]</span><br><span class="line">    B[Makefile.in] --&gt; D</span><br><span class="line">    C[config.h.in] --&gt; D</span><br><span class="line"></span><br><span class="line">    D --&gt; E[Makefile]</span><br><span class="line">    D --&gt; F[config.h]</span><br><span class="line">    D --&gt; G[config.log]</span><br><span class="line">    D --&gt; H[config.status]</span><br><span class="line">    D --&gt; I[libtool]</span><br><span class="line">    D --&gt; J[pkgconfig/liblely-*.pc]</span><br></pre></td></tr></table></figure><p>这里容易混淆的是：</p><table><thead><tr><th>文件</th><th>谁生成</th><th>什么时候生成</th><th>作用</th></tr></thead><tbody><tr><td><code>Makefile.am</code></td><td>开发者写</td><td>源码中已有</td><td>Automake 输入</td></tr><tr><td><code>Makefile.in</code></td><td><code>automake</code> 生成</td><td><code>autoreconf -i</code> 阶段</td><td>Makefile 模板</td></tr><tr><td><code>Makefile</code></td><td><code>configure</code> 生成</td><td><code>../configure</code> 阶段</td><td><code>make</code> 真正读取的构建文件</td></tr><tr><td><code>config.h.in</code></td><td><code>autoheader</code> 生成</td><td><code>autoreconf -i</code> 阶段</td><td>配置头模板</td></tr><tr><td><code>config.h</code></td><td><code>configure</code> 生成</td><td><code>../configure</code> 阶段</td><td>C&#x2F;C++ 源码实际包含的配置头</td></tr></tbody></table><h2 id="make-阶段才是真正编译"><a href="#make-阶段才是真正编译" class="headerlink" title="make 阶段才是真正编译"></a><code>make</code> 阶段才是真正编译</h2><p>执行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">make -j<span class="string">&quot;<span class="subst">$(nproc)</span>&quot;</span></span><br></pre></td></tr></table></figure><p>这一步会读取 <code>&lt;BUILD_DIR&gt;/Makefile</code>，然后调用交叉编译器，例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">&lt;TARGET_TRIPLET&gt;-gcc</span><br><span class="line">&lt;TARGET_TRIPLET&gt;-g++</span><br></pre></td></tr></table></figure><p>生成 Lely 的库和工具。典型产物包括：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">liblely-*.so</span><br><span class="line">liblely-*.a</span><br><span class="line">coctl</span><br><span class="line">其他工具程序</span><br><span class="line">中间目标文件 .o / .lo</span><br></pre></td></tr></table></figure><p>所以如果你要判断是否真的编译到了目标架构，应检查 <code>make</code> 之后的产物，而不是检查 <code>autoreconf -i</code> 的输出。</p><p>例如：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">file &lt;BUILD_DIR&gt;/stage/&lt;INSTALL_PREFIX&gt;/bin/coctl</span><br></pre></td></tr></table></figure><p>期望看到类似：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ELF 64-bit LSB executable, ARM aarch64</span><br></pre></td></tr></table></figure><h2 id="make-install-DESTDIR-是收集产物"><a href="#make-install-DESTDIR-是收集产物" class="headerlink" title="make install DESTDIR=... 是收集产物"></a><code>make install DESTDIR=...</code> 是收集产物</h2><p>执行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">make install DESTDIR=<span class="string">&quot;<span class="variable">$&#123;PWD&#125;</span>/stage&quot;</span></span><br></pre></td></tr></table></figure><p>它会把编译出的文件安装到一个临时目录，而不是直接写入系统目录。</p><p>可以理解成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">--prefix=&lt;INSTALL_PREFIX&gt;</span><br><span class="line">DESTDIR=&lt;BUILD_DIR&gt;/stage</span><br><span class="line"></span><br><span class="line">最终安装位置：</span><br><span class="line">&lt;BUILD_DIR&gt;/stage/&lt;INSTALL_PREFIX&gt;/...</span><br></pre></td></tr></table></figure><p>典型结构：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">&lt;BUILD_DIR&gt;/stage/&lt;INSTALL_PREFIX&gt;/</span><br><span class="line">├── bin/</span><br><span class="line">│   └── coctl</span><br><span class="line">├── lib/</span><br><span class="line">│   ├── liblely-*.so</span><br><span class="line">│   ├── liblely-*.a</span><br><span class="line">│   └── pkgconfig/</span><br><span class="line">│       └── liblely-*.pc</span><br><span class="line">├── include/</span><br><span class="line">│   └── lely/</span><br><span class="line">└── share/ 或 etc/</span><br></pre></td></tr></table></figure><p>这样做的好处是不会污染构建主机的 <code>/usr</code> 或 <code>/usr/local</code>，也便于后续打包部署到目标板。</p><h2 id="在-Lely-交叉编译中，推荐按这个顺序执行"><a href="#在-Lely-交叉编译中，推荐按这个顺序执行" class="headerlink" title="在 Lely 交叉编译中，推荐按这个顺序执行"></a>在 Lely 交叉编译中，推荐按这个顺序执行</h2><p>下面是脱敏后的完整命令模板：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 1. 准备环境变量</span></span><br><span class="line"><span class="built_in">export</span> SDK_ROOT=&lt;SDK_ROOT&gt;</span><br><span class="line"><span class="built_in">export</span> WORK_ROOT=&lt;WORK_ROOT&gt;</span><br><span class="line"><span class="built_in">export</span> LELY_SRC=<span class="variable">$&#123;WORK_ROOT&#125;</span>/lely-core</span><br><span class="line"><span class="built_in">export</span> BUILD_DIR=<span class="variable">$&#123;LELY_SRC&#125;</span>/build-target</span><br><span class="line"><span class="built_in">export</span> INSTALL_PREFIX=&lt;INSTALL_PREFIX&gt;</span><br><span class="line"></span><br><span class="line"><span class="comment"># 2. 加载交叉编译 SDK 环境</span></span><br><span class="line"><span class="built_in">source</span> <span class="variable">$&#123;SDK_ROOT&#125;</span>/environment-setup-&lt;target-env&gt;</span><br><span class="line"></span><br><span class="line"><span class="comment"># 3. 获取并进入 Lely 源码</span></span><br><span class="line"><span class="built_in">cd</span> <span class="variable">$&#123;WORK_ROOT&#125;</span></span><br><span class="line">git <span class="built_in">clone</span> https://gitlab.com/lely_industries/lely-core.git</span><br><span class="line"><span class="built_in">cd</span> <span class="variable">$&#123;LELY_SRC&#125;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 4. 生成 configure 和 Makefile.in</span></span><br><span class="line">autoreconf -i</span><br><span class="line"></span><br><span class="line"><span class="comment"># 5. 创建独立构建目录</span></span><br><span class="line"><span class="built_in">mkdir</span> -p <span class="variable">$&#123;BUILD_DIR&#125;</span></span><br><span class="line"><span class="built_in">cd</span> <span class="variable">$&#123;BUILD_DIR&#125;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 6. 配置交叉编译</span></span><br><span class="line">../configure \</span><br><span class="line">    --host=&lt;TARGET_TRIPLET&gt; \</span><br><span class="line">    --prefix=<span class="variable">$&#123;INSTALL_PREFIX&#125;</span> \</span><br><span class="line">    --disable-python \</span><br><span class="line">    --disable-cython</span><br><span class="line"></span><br><span class="line"><span class="comment"># 7. 编译</span></span><br><span class="line">make -j<span class="string">&quot;<span class="subst">$(nproc)</span>&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 8. 安装到 stage</span></span><br><span class="line">make install DESTDIR=<span class="string">&quot;<span class="variable">$&#123;PWD&#125;</span>/stage&quot;</span></span><br></pre></td></tr></table></figure><p>如果 <code>&lt;TARGET_TRIPLET&gt;</code> 是 ARM64 Linux 工具链，可替换为类似：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">aarch64-poky-linux</span><br></pre></td></tr></table></figure><p>但本文不写入真实 SDK 路径和真实部署目录。</p><h2 id="常见误解"><a href="#常见误解" class="headerlink" title="常见误解"></a>常见误解</h2><h3 id="误解-1：autoreconf-i-是交叉编译"><a href="#误解-1：autoreconf-i-是交叉编译" class="headerlink" title="误解 1：autoreconf -i 是交叉编译"></a>误解 1：<code>autoreconf -i</code> 是交叉编译</h3><p>不是。<code>autoreconf -i</code> 只是生成构建系统。它不会调用目标交叉编译器，也不会产生 ARM ELF。</p><p>真正和目标平台有关的是：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">../configure --host=&lt;TARGET_TRIPLET&gt;</span><br><span class="line">make</span><br></pre></td></tr></table></figure><h3 id="误解-2：configure-ac-会直接被-make-使用"><a href="#误解-2：configure-ac-会直接被-make-使用" class="headerlink" title="误解 2：configure.ac 会直接被 make 使用"></a>误解 2：<code>configure.ac</code> 会直接被 <code>make</code> 使用</h3><p>不会。<code>make</code> 读的是 <code>Makefile</code>。</p><p>关系是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">configure.ac → configure → Makefile → make</span><br></pre></td></tr></table></figure><h3 id="误解-3：Makefile-am-和-Makefile-是一回事"><a href="#误解-3：Makefile-am-和-Makefile-是一回事" class="headerlink" title="误解 3：Makefile.am 和 Makefile 是一回事"></a>误解 3：<code>Makefile.am</code> 和 <code>Makefile</code> 是一回事</h3><p>不是。</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Makefile.am   人写的 Automake 输入</span><br><span class="line">Makefile.in   autoreconf/automake 生成的模板</span><br><span class="line">Makefile      configure 生成的最终构建文件</span><br></pre></td></tr></table></figure><h3 id="误解-4：执行一次-autoreconf-i-后永远不用再执行"><a href="#误解-4：执行一次-autoreconf-i-后永远不用再执行" class="headerlink" title="误解 4：执行一次 autoreconf -i 后永远不用再执行"></a>误解 4：执行一次 <code>autoreconf -i</code> 后永远不用再执行</h3><p>也不是。修改 <code>configure.ac</code> 或 <code>Makefile.am</code> 后，应该重新执行。更换交叉编译目标时，通常只需要重新建构建目录并执行 <code>../configure --host=...</code>，不一定要重新 <code>autoreconf -i</code>。</p><h3 id="误解-5：看到-installing-就是安装到了系统"><a href="#误解-5：看到-installing-就是安装到了系统" class="headerlink" title="误解 5：看到 installing 就是安装到了系统"></a>误解 5：看到 <code>installing</code> 就是安装到了系统</h3><p>不是。<code>autoreconf -i</code> 输出里的 <code>installing &#39;./config.sub&#39;</code> 这类信息，是把构建辅助脚本复制到源码树，不是把 Lely 安装到系统。</p><p>真正安装 Lely 产物的是：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">make install DESTDIR=<span class="string">&quot;<span class="variable">$&#123;PWD&#125;</span>/stage&quot;</span></span><br></pre></td></tr></table></figure><h2 id="排查时优先看这些文件"><a href="#排查时优先看这些文件" class="headerlink" title="排查时优先看这些文件"></a>排查时优先看这些文件</h2><p>当 Lely 构建失败时，不同阶段看不同文件。</p><table><thead><tr><th>阶段</th><th>关注文件</th><th>常见问题</th></tr></thead><tbody><tr><td><code>autoreconf -i</code></td><td><code>configure.ac</code>、<code>Makefile.am</code>、<code>m4/*.m4</code></td><td>宏缺失、Autotools 版本不足、Libtool 文件缺失</td></tr><tr><td><code>../configure</code></td><td><code>config.log</code></td><td>编译器找不到、头文件检测失败、库检测失败</td></tr><tr><td><code>make</code></td><td>编译日志、对应 <code>Makefile</code></td><td>C&#x2F;C++ 编译错误、链接错误、目标库缺失</td></tr><tr><td><code>make install</code></td><td><code>stage/</code> 目录</td><td>安装路径不符合预期、<code>.pc</code> 文件缺失、工具未安装</td></tr><tr><td>运行目标程序</td><td><code>file</code>、<code>ldd</code>、<code>LD_LIBRARY_PATH</code></td><td>架构错误、动态库找不到、运行环境缺失</td></tr></tbody></table><p>建议检查顺序：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 检查 configure 是否生成</span></span><br><span class="line"><span class="built_in">ls</span> -l &lt;LELY_SRC&gt;/configure</span><br><span class="line"></span><br><span class="line"><span class="comment"># 检查 Makefile.in 是否生成</span></span><br><span class="line">find &lt;LELY_SRC&gt; -name Makefile.<span class="keyword">in</span> | <span class="built_in">head</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 检查 configure 结果</span></span><br><span class="line">less &lt;BUILD_DIR&gt;/config.log</span><br><span class="line"></span><br><span class="line"><span class="comment"># 检查最终 Makefile</span></span><br><span class="line"><span class="built_in">ls</span> -l &lt;BUILD_DIR&gt;/Makefile</span><br><span class="line"></span><br><span class="line"><span class="comment"># 检查目标程序架构</span></span><br><span class="line">file &lt;BUILD_DIR&gt;/stage/&lt;INSTALL_PREFIX&gt;/bin/coctl</span><br></pre></td></tr></table></figure><h2 id="最小心智模型"><a href="#最小心智模型" class="headerlink" title="最小心智模型"></a>最小心智模型</h2><p>如果只记一张图，记这张：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[人维护&lt;br/&gt;configure.ac + Makefile.am] --&gt; B[autoreconf -i]</span><br><span class="line">    B --&gt; C[生成&lt;br/&gt;configure + Makefile.in]</span><br><span class="line">    C --&gt; D[../configure --host=目标平台]</span><br><span class="line">    D --&gt; E[生成&lt;br/&gt;Makefile + config.h]</span><br><span class="line">    E --&gt; F[make]</span><br><span class="line">    F --&gt; G[生成目标平台产物]</span><br><span class="line">    G --&gt; H[make install DESTDIR=stage]</span><br><span class="line">    H --&gt; I[打包部署]</span><br></pre></td></tr></table></figure><p>对应到 Lely：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">Lely 源码目录</span><br><span class="line">  ↓ autoreconf -i</span><br><span class="line">得到 configure</span><br><span class="line">  ↓ ../configure --host=&lt;TARGET_TRIPLET&gt;</span><br><span class="line">得到交叉编译用 Makefile</span><br><span class="line">  ↓ make</span><br><span class="line">得到 Lely 库和 coctl</span><br><span class="line">  ↓ make install DESTDIR=stage</span><br><span class="line">得到可打包部署目录</span><br></pre></td></tr></table></figure><h2 id="参考来源"><a href="#参考来源" class="headerlink" title="参考来源"></a>参考来源</h2><ul><li><p><a href="https://www.gnu.org/software/autoconf/">GNU Autoconf introduction</a></p></li><li><p><a href="https://www.gnu.org/software/autoconf/manual/autoconf-2.72/html_node/autoreconf-Invocation.html">GNU Autoconf: autoreconf invocation</a></p></li><li><p><a href="https://www.gnu.org/software/autoconf/manual/autoconf-2.70/html_node/Initializing-configure.html">GNU Autoconf: Initializing configure</a></p></li><li><p><a href="https://www.gnu.org/software/autoconf/manual/autoconf-2.60/html_node/Output.html">GNU Autoconf: Outputting files</a></p></li><li><p><a href="https://www.gnu.org/software/automake/manual/html_node/Introduction.html">GNU Automake introduction</a></p></li><li><p><a href="https://www.gnu.org/software/automake/manual/html_node/Creating-amhello.html">GNU Automake: Creating amhello</a></p></li><li><p><a href="https://github.com/lely-industries/lely-core">Lely core README</a></p></li><li><p><a href="https://opensource.lely.com/canopen/docs/cross-compilation/">Lely CANopen cross-compilation</a></p></li><li><p><a href="https://github.com/lely-industries/lely-core/blob/master/configure.ac">lely-core&#x2F;configure.ac</a></p></li><li><p><a href="https://www.gnu.org/software/m4/manual/m4.html">GNU M4 manual</a></p></li><li><p><a href="https://www.gnu.org/s/libtool/manual/libtool.html">GNU Libtool manual</a></p></li><li><p><a href="https://github.com/autoconf-archive/autoconf-archive">GNU Autoconf Archive mirror</a></p></li><li><p><a href="https://www.gnu.org/software/autoconf-archive/ax_pthread.html">Autoconf Archive: AX_PTHREAD</a></p></li><li><p><a href="https://github.com/lely-industries/lely-core/tree/master/m4">lely-core&#x2F;m4</a></p></li><li><p><a href="https://github.com/lely-industries/lely-core/blob/master/m4/ax_check_python.m4">lely-core&#x2F;m4&#x2F;ax_check_python.m4</a></p></li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;meta name=&quot;referrer&quot; content=&quot;no-referrer&quot; /&gt;

&lt;h1 id=&quot;从-autoreconf-i-看懂-Autoconf：以-Lely-CANopen-交叉编译为例&quot;&gt;&lt;a href=&quot;#从-autoreconf-i-看懂-Autoco</summary>
      
    
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/categories/canopen/"/>
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/tags/canopen/"/>
    
  </entry>
  
</feed>
