<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>wdfk-prog的个人博客</title>
  
  <subtitle>WORK-LIFE BALANCE.</subtitle>
  <link href="https://wdfk-prog.space/atom.xml" rel="self"/>
  
  <link href="https://wdfk-prog.space/"/>
  <updated>2026-09-03T07:50:26.372Z</updated>
  <id>https://wdfk-prog.space/</id>
  
  <author>
    <name>Liya Huang</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>01-Docker开发环境容器化：从基本概念到Ubuntu20</title>
    <link href="https://wdfk-prog.space/posts/ff462634/"/>
    <id>https://wdfk-prog.space/posts/ff462634/</id>
    <published>2026-09-03T07:50:11.000Z</published>
    <updated>2026-09-03T07:50:26.372Z</updated>
    
    <content type="html"><![CDATA[<meta name="referrer" content="no-referrer" /><h1 id="Docker-开发环境容器化：从基本概念到-Ubuntu-20-04-安装排查"><a href="#Docker-开发环境容器化：从基本概念到-Ubuntu-20-04-安装排查" class="headerlink" title="Docker 开发环境容器化：从基本概念到 Ubuntu 20.04 安装排查"></a>Docker 开发环境容器化：从基本概念到 Ubuntu 20.04 安装排查</h1><blockquote><p>摘要：从 Image、Container、Dockerfile 和挂载建立 Docker 心智模型，并结合 Ubuntu 20.04 的安装冲突说明为什么要把旧开发环境容器化。</p></blockquote><p><img src="https://i-blog.csdnimg.cn/direct/c46c889358594d2fb2ba84c995c43fa2.png" alt="在这里插入图片描述"><br>@[toc]<br>学习 Docker 最容易走偏的方式，是先背几十条命令，再去想这些命令解决什么问题。对于开发环境容器化，更合理的顺序正好相反：先明确工程为什么需要 Docker，再理解 Image、Container、Dockerfile、挂载和权限这些概念，最后才把命令放进实际场景。</p><p>本文从一个典型的嵌入式交叉编译环境出发：旧工程长期依赖 Ubuntu 20.04、厂商 Yocto SDK、固定 CMake、代码生成工具和若干宿主机软件。旧机器能稳定构建，但一旦换电脑、升级系统或让其他开发者接手，就很难准确复刻环境。</p><p>Docker 在这里的目标不是“把 Linux 再装一遍”，而是：</p><blockquote><p>把构建环境固定成可重复生成的镜像，把日常修改的源码继续留在宿主机。</p></blockquote><h2 id="1-为什么开发环境值得容器化"><a href="#1-为什么开发环境值得容器化" class="headerlink" title="1. 为什么开发环境值得容器化"></a>1. 为什么开发环境值得容器化</h2><p>传统开发机往往随着项目迭代逐渐变成这样：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">Ubuntu 20.04</span><br><span class="line">├── apt 安装的软件</span><br><span class="line">├── /opt 下的厂商 SDK</span><br><span class="line">├── /usr/local 下的自定义工具</span><br><span class="line">├── 多个版本的 CMake / Python</span><br><span class="line">├── 临时手工修改过的权限</span><br><span class="line">└── 多个项目源码</span><br></pre></td></tr></table></figure><p>这套环境的问题不在于“不能工作”，而在于它的状态很难描述。</p><p>例如一个工程能够编译，可能依赖：</p><ul><li>某个固定版本的交叉编译器；</li><li>某个已经 <code>source</code> 过的 SDK 环境脚本；</li><li><code>/usr/local/bin</code> 下某个历史工具；</li><li>某个开发者手工安装过、但 README 没写的软件包；</li><li>某些目录恰好属于当前用户，因此权限看起来一直正常。</li></ul><p>当这些条件全堆在宿主机里，旧机器本身就成了“唯一可工作的环境副本”。换机器时，人只能靠记忆复刻。</p><p>容器化之后，希望形成的是：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    Dockerfile[&quot;Dockerfile\n环境配方&quot;] --&gt;|build| Image[&quot;Image\n固定开发环境&quot;]</span><br><span class="line">    Image --&gt;|run| Container[&quot;Container\n一次开发会话&quot;]</span><br><span class="line">    Source[&quot;宿主机 Git 源码&quot;] --&gt;|bind mount| Container</span><br><span class="line">    Container --&gt;|cross build| Artifact[&quot;目标平台构建产物&quot;]</span><br></pre></td></tr></table></figure><p>这里最重要的边界是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">镜像保存：OS 用户态、SDK、编译工具、公共依赖、启动逻辑</span><br><span class="line">宿主机保存：Git 源码、个人分支、编辑器配置、日常修改内容</span><br><span class="line">容器负责：在固定环境中运行 configure / compile / link / test</span><br></pre></td></tr></table></figure><p>这样以后宿主机升级到更新的 Ubuntu，旧工程仍可以在 Ubuntu 20.04 用户态容器里构建。</p><h2 id="2-Docker-Engine、CLI、Image、Container-到底是什么"><a href="#2-Docker-Engine、CLI、Image、Container-到底是什么" class="headerlink" title="2. Docker Engine、CLI、Image、Container 到底是什么"></a>2. Docker Engine、CLI、Image、Container 到底是什么</h2><p>很多初学者把 Docker 当成一个程序，实际上日常使用至少要区分几个对象。</p><h3 id="2-1-Docker-Engine"><a href="#2-1-Docker-Engine" class="headerlink" title="2.1 Docker Engine"></a>2.1 Docker Engine</h3><p>Docker Engine 是真正管理容器的后台系统。Linux 上通常包含 Docker daemon，并通过底层运行时管理容器生命周期。</p><p>你在终端输入：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">docker run ubuntu:24.04</span><br></pre></td></tr></table></figure><p>并不是 CLI 自己去创建 namespace、挂载文件系统。CLI 会向 Docker Engine 发请求，由 Engine 完成后续工作。</p><p>可以简化理解为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">Docker CLI</span><br><span class="line">    │</span><br><span class="line">    │ Docker API</span><br><span class="line">    ▼</span><br><span class="line">Docker Engine / dockerd</span><br><span class="line">    │</span><br><span class="line">    ├── Image</span><br><span class="line">    ├── Container</span><br><span class="line">    ├── Network</span><br><span class="line">    └── Volume</span><br></pre></td></tr></table></figure><h3 id="2-2-Image：环境模板"><a href="#2-2-Image：环境模板" class="headerlink" title="2.2 Image：环境模板"></a>2.2 Image：环境模板</h3><p>Image 是创建 Container 的模板。它通常包含：</p><ul><li>基础 Ubuntu 用户态；</li><li><code>/usr/bin</code>、<code>/usr/lib</code> 等文件；</li><li>编译器和 SDK；</li><li>环境变量；</li><li><code>ENTRYPOINT</code> 与 <code>CMD</code>；</li><li>一组只读镜像层。</li></ul><p>查看镜像：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">docker image <span class="built_in">ls</span></span><br></pre></td></tr></table></figure><p>例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">REPOSITORY   TAG      IMAGE ID       SIZE</span><br><span class="line">arm64-dev    20.04    123456789abc   27GB</span><br></pre></td></tr></table></figure><p><code>arm64-dev:20.04</code> 只是一个镜像，不是正在运行的 Ubuntu。</p><h3 id="2-3-Container：Image-的一次运行实例"><a href="#2-3-Container：Image-的一次运行实例" class="headerlink" title="2.3 Container：Image 的一次运行实例"></a>2.3 Container：Image 的一次运行实例</h3><p>执行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">docker run --<span class="built_in">rm</span> -it arm64-dev:20.04</span><br></pre></td></tr></table></figure><p>Docker 会基于 Image 创建一个 Container，再启动其中的进程。</p><p>参数可以拆开看：</p><table><thead><tr><th>参数</th><th>含义</th></tr></thead><tbody><tr><td><code>docker run</code></td><td>创建并启动新的 Container</td></tr><tr><td><code>--rm</code></td><td>主进程退出后自动删除这个 Container</td></tr><tr><td><code>-i</code></td><td>保持标准输入打开</td></tr><tr><td><code>-t</code></td><td>分配伪终端，交互式 shell 更自然</td></tr><tr><td><code>arm64-dev:20.04</code></td><td>选择哪个 Image</td></tr></tbody></table><p>一个很重要的认识是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">删除 Container ≠ 删除 Image</span><br></pre></td></tr></table></figure><p>Container 是一次运行实例，Image 才是可重复创建环境的模板。</p><p>开发容器常使用 <code>--rm</code>，正是因为真正需要长期保存的状态应该在镜像、源码仓库或明确的持久化存储中，而不是偷偷堆在某个旧 Container 的可写层里。</p><h3 id="2-4-Dockerfile：构建-Image-的配方"><a href="#2-4-Dockerfile：构建-Image-的配方" class="headerlink" title="2.4 Dockerfile：构建 Image 的配方"></a>2.4 Dockerfile：构建 Image 的配方</h3><p>Dockerfile 是文本文件，例如：</p><figure class="highlight dockerfile"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">FROM</span> ubuntu:<span class="number">20.04</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">RUN</span><span class="language-bash"> apt-get update \</span></span><br><span class="line"><span class="language-bash">    &amp;&amp; apt-get install -y --no-install-recommends \</span></span><br><span class="line"><span class="language-bash">        git cmake make \</span></span><br><span class="line"><span class="language-bash">    &amp;&amp; <span class="built_in">rm</span> -rf /var/lib/apt/lists/*</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">WORKDIR</span><span class="language-bash"> /workspace</span></span><br><span class="line"><span class="keyword">CMD</span><span class="language-bash"> [<span class="string">&quot;/bin/bash&quot;</span>]</span></span><br></pre></td></tr></table></figure><p>它描述“镜像应该如何生成”。</p><p>所以：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Dockerfile --build--&gt; Image --run--&gt; Container</span><br></pre></td></tr></table></figure><p>这是后面理解所有 Docker 操作的主线。</p><h2 id="3-Docker-和虚拟机有什么区别"><a href="#3-Docker-和虚拟机有什么区别" class="headerlink" title="3. Docker 和虚拟机有什么区别"></a>3. Docker 和虚拟机有什么区别</h2><p>Docker 容器与虚拟机都能提供隔离环境，但模型不同。</p><p>虚拟机通常是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">Host OS</span><br><span class="line">└── Hypervisor</span><br><span class="line">    └── Guest OS</span><br><span class="line">        ├── Guest Kernel</span><br><span class="line">        └── User Space</span><br></pre></td></tr></table></figure><p>Linux 容器更接近：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">Host Linux Kernel</span><br><span class="line">├── Container A user space</span><br><span class="line">├── Container B user space</span><br><span class="line">└── Host user space</span><br></pre></td></tr></table></figure><p>容器共享宿主 Linux 内核，所以镜像一般比完整 VM 更轻，也更适合快速创建、删除和复制。</p><p>这对交叉编译尤其合适：开发机仍是 x86-64，容器里运行的 CMake、Thrift、GCC driver 也是 x86-64 程序，但交叉编译器生成 ARM64 目标文件。并不需要用 ARM64 虚拟机来完成普通交叉构建。</p><p>当然，Docker 不是万能虚拟机。内核驱动、USB&#x2F;CAN 设备、特殊内核能力仍然和宿主有关。本文讨论的重点是用户态开发环境和交叉构建链路。</p><h2 id="4-为什么“环境进镜像，源码留宿主机”"><a href="#4-为什么“环境进镜像，源码留宿主机”" class="headerlink" title="4. 为什么“环境进镜像，源码留宿主机”"></a>4. 为什么“环境进镜像，源码留宿主机”</h2><p>如果把源码也 <code>COPY</code> 进镜像：</p><figure class="highlight dockerfile"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">COPY</span><span class="language-bash"> ./project /workspace/project</span></span><br></pre></td></tr></table></figure><p>每修改一次源码，镜像里的代码就过时；想继续编译就要重新 build Image。对于日常 C&#x2F;C++ 开发非常别扭。</p><p>更自然的方式是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">宿主机</span><br><span class="line">/home/dev/work/demo-project</span><br><span class="line">        │</span><br><span class="line">        │ bind mount</span><br><span class="line">        ▼</span><br><span class="line">Container</span><br><span class="line">/workspace</span><br></pre></td></tr></table></figure><p>启动：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">docker run --<span class="built_in">rm</span> -it \</span><br><span class="line">    --mount <span class="built_in">type</span>=<span class="built_in">bind</span>,src=/home/dev/work/demo-project,dst=/workspace \</span><br><span class="line">    -w /workspace \</span><br><span class="line">    arm64-dev:20.04</span><br></pre></td></tr></table></figure><p>此时：</p><ul><li>宿主机 IDE 修改源码，Container 立即看到；</li><li>Container 里编译生成的文件也直接出现在宿主目录；</li><li>删除 Container 不会删除 Git 仓库；</li><li>修改源码不需要重建几十 GB 的开发镜像。</li></ul><p>这就是开发场景中 bind mount 的核心价值。</p><h2 id="5-为什么源码通常不优先放-Docker-volume"><a href="#5-为什么源码通常不优先放-Docker-volume" class="headerlink" title="5. 为什么源码通常不优先放 Docker volume"></a>5. 为什么源码通常不优先放 Docker volume</h2><p>Docker 常见持久化方式可以先分三类：</p><table><thead><tr><th>类型</th><th>数据实际位置</th><th>适合什么</th></tr></thead><tbody><tr><td>bind mount</td><td>你明确指定的宿主机目录</td><td>源码、配置、构建产物</td></tr><tr><td>volume</td><td>Docker 管理的宿主机存储位置</td><td>数据库、缓存、长期服务数据</td></tr><tr><td>Container writable layer</td><td>Container 自己的可写层</td><td>临时运行数据</td></tr></tbody></table><p>Docker volume 当然可以保存源码，技术上没有禁止。但“能用”和“适合日常源码开发”是两回事。</p><p>源码的特点是宿主机上的很多非 Docker 工具都需要直接操作它：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">Git</span><br><span class="line">VS Code / CLion</span><br><span class="line">ripgrep</span><br><span class="line">代码审查工具</span><br><span class="line">脚本</span><br><span class="line">备份软件</span><br><span class="line">文件管理器</span><br></pre></td></tr></table></figure><p>如果源码放在 Docker 管理的 volume 中，宿主机不再有一个自然、明确、应该直接操作的工程目录。官方文档也明确指出：当文件需要同时由 Host 与 Container 访问时，bind mount 更合适；Volume 更适合由 Docker 管理的持久数据。</p><p>因此开发项目通常选择：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">Git 源码        -&gt; bind mount</span><br><span class="line">ccache          -&gt; volume（可选）</span><br><span class="line">数据库数据      -&gt; volume</span><br><span class="line">临时输出        -&gt; Container layer / tmpfs（按需要）</span><br></pre></td></tr></table></figure><p>注意，bind mount 默认可写，Container 对 <code>/workspace</code> 的删除、改名和 <code>chown</code> 都可能直接改变宿主文件。这也引出了后面很重要的 UID&#x2F;GID 问题。</p><h2 id="6-v-和-mount-怎么选"><a href="#6-v-和-mount-怎么选" class="headerlink" title="6. -v 和 --mount 怎么选"></a>6. <code>-v</code> 和 <code>--mount</code> 怎么选</h2><p>同一个 bind mount 可以写成：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">docker run -v /home/dev/work/demo-project:/workspace image</span><br></pre></td></tr></table></figure><p>也可以写成：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">docker run \</span><br><span class="line">    --mount <span class="built_in">type</span>=<span class="built_in">bind</span>,src=/home/dev/work/demo-project,dst=/workspace \</span><br><span class="line">    image</span><br></pre></td></tr></table></figure><p>短写 <code>-v</code> 输入快；<code>--mount</code> 语义更明确，也更适合工程脚本。</p><p>另一个实用差异是：Host 源路径不存在时，<code>--mount</code> 默认会直接报错，而 <code>-v</code> 在某些 bind mount 用法中会创建目录。</p><p>对于开发环境，路径拼错时“立即失败”通常比“悄悄创建一个空目录”更安全，所以正式脚本更适合使用明确的 mount 配置或 Compose 长语法。</p><h2 id="7-为什么-bind-mount-会带来-UID-GID-问题"><a href="#7-为什么-bind-mount-会带来-UID-GID-问题" class="headerlink" title="7. 为什么 bind mount 会带来 UID&#x2F;GID 问题"></a>7. 为什么 bind mount 会带来 UID&#x2F;GID 问题</h2><p>Linux 文件权限真正识别的是数字 UID&#x2F;GID。</p><p>假设宿主机用户：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">uid=1001 gid=1001</span><br></pre></td></tr></table></figure><p>而 Container 里的开发用户是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">uid=1000 gid=1000</span><br></pre></td></tr></table></figure><p>即使两边都叫 <code>devuser</code>，Linux 仍认为它们不是同一个身份。</p><p>于是可能出现：</p><ul><li>Container 不能修改 Host 源码；</li><li>Container 创建的文件在 Host 上显示成另一个 UID；</li><li>如果直接用 root 编译，Host 上产生 root-owned <code>build/</code>。</li></ul><p>因此开发容器通常会把宿主的 UID&#x2F;GID传给 Container，在入口脚本中调整普通开发用户的数字 ID，再以普通用户执行真正的构建命令。</p><p>这里有一个必须坚持的边界：</p><blockquote><p>不要因为权限问题就在 entrypoint 中对 bind-mounted <code>/workspace</code> 做 <code>chown -R</code>。</p></blockquote><p>因为 <code>/workspace</code> 背后就是 Host 的真实 Git 工作树。递归 <code>chown</code> 实际是在改宿主文件权限。</p><h2 id="8-Ubuntu-20-04-为什么是一个特殊场景"><a href="#8-Ubuntu-20-04-为什么是一个特殊场景" class="headerlink" title="8. Ubuntu 20.04 为什么是一个特殊场景"></a>8. Ubuntu 20.04 为什么是一个特殊场景</h2><p>截至 2026-09-03，Docker Engine 官方 Ubuntu 安装页列出的 64 位 Ubuntu 版本是：</p><ul><li>Ubuntu 26.04 LTS；</li><li>Ubuntu 25.10；</li><li>Ubuntu 24.04 LTS；</li><li>Ubuntu 22.04 LTS。</li></ul><p>Ubuntu 20.04 已不在当前官方支持列表中。</p><p>必须区分：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">不在官方支持矩阵</span><br><span class="line">        ≠</span><br><span class="line">技术上绝对不能运行</span><br></pre></td></tr></table></figure><p>旧机器上可能已经有能够工作的 Docker 版本，但不能因为“我这里能运行”就推导成“当前官方仍支持 Focal”。</p><p>对于一个不能马上升级的遗留开发机，更务实的目标不是强行追最新 Docker CE，而是先得到一套稳定 Docker 环境，把依赖 Ubuntu 20.04 的旧工具链固化进 Image。以后 Host 再升级到新的 Ubuntu，旧 userspace 继续由 Container 保留。</p><h2 id="9-docker-io-安装失败：为什么不是“包不存在”"><a href="#9-docker-io-安装失败：为什么不是“包不存在”" class="headerlink" title="9. docker.io 安装失败：为什么不是“包不存在”"></a>9. <code>docker.io</code> 安装失败：为什么不是“包不存在”</h2><p>遗留 Ubuntu 20.04 上可能执行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">sudo</span> apt install -y docker.io</span><br></pre></td></tr></table></figure><p>却得到：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">docker.io : Depends: containerd (&gt;= ...)</span><br><span class="line">E: Unable to correct problems, you have held broken packages.</span><br></pre></td></tr></table></figure><p>这段错误应该读成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">APT 已经找到 docker.io</span><br><span class="line">        ↓</span><br><span class="line">docker.io 依赖发行版 containerd</span><br><span class="line">        ↓</span><br><span class="line">APT 当前无法选出可安装的 containerd 组合</span><br><span class="line">        ↓</span><br><span class="line">依赖解析失败</span><br></pre></td></tr></table></figure><p>而不是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">docker.io 不存在</span><br></pre></td></tr></table></figure><p>Docker 生态里还有两组很容易混淆的软件包：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">Ubuntu / Debian 发行版：</span><br><span class="line">    docker.io</span><br><span class="line">    containerd</span><br><span class="line">    runc</span><br><span class="line"></span><br><span class="line">Docker 官方仓库：</span><br><span class="line">    docker-ce</span><br><span class="line">    docker-ce-cli</span><br><span class="line">    containerd.io</span><br><span class="line">    docker-buildx-plugin</span><br><span class="line">    docker-compose-plugin</span><br></pre></td></tr></table></figure><p>Docker 官方安装文档明确要求在安装官方 Docker Engine 前处理可能冲突的发行版 <code>containerd</code> &#x2F; <code>runc</code> 等包。反过来，如果机器历史上装过 Docker 官方的 <code>containerd.io</code>，后来又切回 Ubuntu 的 <code>docker.io</code>，同样可能出现依赖来源冲突。</p><h2 id="10-遇到-APT-冲突时先看证据"><a href="#10-遇到-APT-冲突时先看证据" class="headerlink" title="10. 遇到 APT 冲突时先看证据"></a>10. 遇到 APT 冲突时先看证据</h2><p>不要一看到依赖错误就立刻删包。先做只读检查。</p><h3 id="看当前安装了什么"><a href="#看当前安装了什么" class="headerlink" title="看当前安装了什么"></a>看当前安装了什么</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">dpkg -l | grep -E <span class="string">&#x27;docker|containerd|runc&#x27;</span></span><br></pre></td></tr></table></figure><p>重点看有没有同时存在：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">docker.io + docker-ce</span><br><span class="line">containerd + containerd.io</span><br></pre></td></tr></table></figure><h3 id="看-APT-准备选择哪个版本"><a href="#看-APT-准备选择哪个版本" class="headerlink" title="看 APT 准备选择哪个版本"></a>看 APT 准备选择哪个版本</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">apt-cache policy docker.io containerd containerd.io runc</span><br></pre></td></tr></table></figure><p><code>Installed</code> 是当前版本，<code>Candidate</code> 是 APT 准备安装&#x2F;升级的版本。来源地址还能帮助判断包来自 Ubuntu 还是 Docker 官方仓库。</p><h3 id="看有没有-hold"><a href="#看有没有-hold" class="headerlink" title="看有没有 hold"></a>看有没有 hold</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">apt-mark showhold</span><br></pre></td></tr></table></figure><p>如果关键依赖被 hold，APT 可能无法自动调整版本。</p><h3 id="看软件源"><a href="#看软件源" class="headerlink" title="看软件源"></a>看软件源</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">grep -R --no-filename -h <span class="string">&#x27;^deb &#x27;</span> \</span><br><span class="line">    /etc/apt/sources.list \</span><br><span class="line">    /etc/apt/sources.list.d/*.list 2&gt;/dev/null</span><br></pre></td></tr></table></figure><p>目标不是“源越多越好”，而是确认有没有混用不同发行渠道、错误 Ubuntu codename 或失效源。</p><p>真正处理时应尽量选定一套包体系，而不是继续混装。</p><h2 id="11-Docker-安装后怎样做最小验证"><a href="#11-Docker-安装后怎样做最小验证" class="headerlink" title="11. Docker 安装后怎样做最小验证"></a>11. Docker 安装后怎样做最小验证</h2><p>只运行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">docker --version</span><br></pre></td></tr></table></figure><p>只能证明 CLI 存在。</p><p>更有效的是：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">docker version</span><br></pre></td></tr></table></figure><p>它会同时尝试获取 Client&#x2F;Server 信息。</p><p>再看：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">docker info</span><br></pre></td></tr></table></figure><p>这一步会连接 daemon，可以看到 storage driver、Docker Root Dir、镜像和 Container 数量等信息。</p><p>最后运行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">docker run --<span class="built_in">rm</span> hello-world</span><br></pre></td></tr></table></figure><p>成功至少证明：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">CLI 能连接 Engine</span><br><span class="line">        ↓</span><br><span class="line">Image 能获取/读取</span><br><span class="line">        ↓</span><br><span class="line">Container 能创建</span><br><span class="line">        ↓</span><br><span class="line">Container 主进程能启动和退出</span><br></pre></td></tr></table></figure><p>这才算完成最小 Docker 链路验证。</p><h2 id="12-一条开发命令应该怎样阅读"><a href="#12-一条开发命令应该怎样阅读" class="headerlink" title="12. 一条开发命令应该怎样阅读"></a>12. 一条开发命令应该怎样阅读</h2><p>以后可能看到：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">docker run --<span class="built_in">rm</span> -it \</span><br><span class="line">    --name arm64-dev-shell \</span><br><span class="line">    --mount <span class="built_in">type</span>=<span class="built_in">bind</span>,src=/home/dev/work,dst=/workspace \</span><br><span class="line">    -w /workspace \</span><br><span class="line">    arm64-dev:20.04</span><br></pre></td></tr></table></figure><p>不要把它当一串需要背的参数，而是按对象读：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">docker run</span><br><span class="line">    │</span><br><span class="line">    ├── --rm           本次 Container 退出即删除</span><br><span class="line">    ├── -it            交互式终端</span><br><span class="line">    ├── --name         给运行实例一个名字</span><br><span class="line">    ├── --mount        Host 源码映射到 Container</span><br><span class="line">    ├── -w             初始工作目录</span><br><span class="line">    └── arm64-dev...   使用哪个 Image</span><br></pre></td></tr></table></figure><p>这条命令背后的设计比参数本身重要：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Image 是环境</span><br><span class="line">Container 是临时会话</span><br><span class="line">Host 目录是真实源码</span><br></pre></td></tr></table></figure><h2 id="13-这一阶段应该真正记住什么"><a href="#13-这一阶段应该真正记住什么" class="headerlink" title="13. 这一阶段应该真正记住什么"></a>13. 这一阶段应该真正记住什么</h2><p>Docker 初学阶段不需要研究所有 runtime internals。先建立下面几个判断已经足够：</p><ol><li><strong>Dockerfile 是环境配方，Image 是模板，Container 是运行实例。</strong></li><li><strong>开发源码优先留在 Host，通过 bind mount 给 Container 使用。</strong> Volume 更适合 Docker 管理的持久数据和缓存。</li><li><strong>Container 与虚拟机不是同一种东西。</strong> 容器共享 Host kernel，但可以固定一套旧 Ubuntu userspace。</li><li><strong>Ubuntu 20.04 已不在 Docker 当前官方 Ubuntu Engine 支持列表中。</strong> 遗留机要把“能工作”和“官方支持”分开。</li><li><strong>APT 依赖错误先看包来源和候选版本。</strong> <code>docker.io/containerd</code> 与 <code>docker-ce/containerd.io</code> 不要无目的混装。</li><li><strong>容器化完成的标准不是 <code>hello-world</code>。</strong> 最终还要让真实工程在新 Container 中完成 configure、compile 和 link。</li></ol><p>下一篇进入真正的工程化部分：当厂商 SDK 本身已经达到约 25 GiB 时，为什么要使用 BuildKit&#x2F;Buildx named context，怎样设计 Dockerfile、Compose、entrypoint 和一组脚本，让这个开发环境可以构建、验证和迁移。</p><h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ul><li>Docker Engine on Ubuntu: <a href="https://docs.docker.com/engine/install/ubuntu/">https://docs.docker.com/engine/install/ubuntu/</a></li><li>Docker Engine storage: <a href="https://docs.docker.com/engine/storage/">https://docs.docker.com/engine/storage/</a></li><li>Docker bind mounts: <a href="https://docs.docker.com/engine/storage/bind-mounts/">https://docs.docker.com/engine/storage/bind-mounts/</a></li><li>Docker volumes: <a href="https://docs.docker.com/engine/storage/volumes/">https://docs.docker.com/engine/storage/volumes/</a></li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;meta name=&quot;referrer&quot; content=&quot;no-referrer&quot; /&gt;

&lt;h1 id=&quot;Docker-开发环境容器化：从基本概念到-Ubuntu-20-04-安装排查&quot;&gt;&lt;a href=&quot;#Docker-开发环境容器化：从基本概念到-Ubuntu-20-0</summary>
      
    
    
    
    <category term="docker" scheme="https://wdfk-prog.space/categories/docker/"/>
    
    
    <category term="docker" scheme="https://wdfk-prog.space/tags/docker/"/>
    
  </entry>
  
  <entry>
    <title>02-大型SDK开发镜像工程化：BuildKit、Buildx与项目脚本</title>
    <link href="https://wdfk-prog.space/posts/ae40f623/"/>
    <id>https://wdfk-prog.space/posts/ae40f623/</id>
    <published>2026-09-03T07:50:11.000Z</published>
    <updated>2026-09-03T07:50:26.376Z</updated>
    
    <content type="html"><![CDATA[<meta name="referrer" content="no-referrer" /><h1 id="大型-SDK-开发镜像工程化：BuildKit、Buildx-与项目脚本"><a href="#大型-SDK-开发镜像工程化：BuildKit、Buildx-与项目脚本" class="headerlink" title="大型 SDK 开发镜像工程化：BuildKit、Buildx 与项目脚本"></a>大型 SDK 开发镜像工程化：BuildKit、Buildx 与项目脚本</h1><blockquote><p>摘要：以约 25 GiB 的交叉编译 SDK 为例，解释 BuildKit、Buildx、named context、镜像层，以及 Dockerfile、Compose 和配套脚本怎样组成完整开发环境。</p></blockquote><p>@[toc]</p><p><img src="https://i-blog.csdnimg.cn/direct/a53efed3ee894225a306b24144c40079.png" alt="在这里插入图片描述"></p><p>普通 Docker 教程经常从一个几十 MB 的源码目录开始：写 Dockerfile、<code>COPY . .</code>、执行 <code>docker build</code>，几秒钟就能看到结果。但真实嵌入式开发环境可能完全不是这个规模。</p><p>假设已有一套厂商 Yocto SDK，展开后约 25 GiB，还需要固定版本的 CMake、host 侧代码生成工具以及一套 UID&#x2F;GID 兼容逻辑。此时问题已经从“会不会写 Dockerfile”变成了：</p><ul><li>25 GiB SDK 怎样进入 Image；</li><li>怎样避免为了构建镜像再制造一个巨大的 SDK 压缩包；</li><li>BuildKit 和 Buildx 分别负责什么；</li><li>为什么使用额外 build context；</li><li>为什么源码不和 SDK 一起做进 Image；</li><li>为什么需要 <code>entrypoint.sh</code>、Compose 和多个脚本；</li><li>怎样在镜像生成后自动确认它真的能交叉编译。</li></ul><p>这一篇围绕这条完整链路展开。</p><h2 id="1-先看最终结构"><a href="#1-先看最终结构" class="headerlink" title="1. 先看最终结构"></a>1. 先看最终结构</h2><p>脱敏后的工程可以理解为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">docker-dev/</span><br><span class="line">├── Dockerfile</span><br><span class="line">├── compose.yaml</span><br><span class="line">├── .dockerignore</span><br><span class="line">├── docker/</span><br><span class="line">│   └── entrypoint.sh</span><br><span class="line">├── scripts/</span><br><span class="line">│   ├── prepare-assets.sh</span><br><span class="line">│   ├── build-image.sh</span><br><span class="line">│   ├── check-sdk-permissions.sh</span><br><span class="line">│   ├── dev.sh</span><br><span class="line">│   ├── export-image.sh</span><br><span class="line">│   ├── import-image.sh</span><br><span class="line">│   └── verify-env.sh</span><br><span class="line">└── workspace/</span><br><span class="line">    └── .gitkeep</span><br></pre></td></tr></table></figure><p>各文件按职责分成四组：</p><table><thead><tr><th>组别</th><th>文件</th><th>主要责任</th></tr></thead><tbody><tr><td>镜像定义</td><td><code>Dockerfile</code>、<code>.dockerignore</code></td><td>Image 里装什么、主 context 传什么</td></tr><tr><td>构建入口</td><td><code>prepare-assets.sh</code>、<code>build-image.sh</code></td><td>检查输入、启动 Buildx、构建后验证</td></tr><tr><td>运行入口</td><td><code>compose.yaml</code>、<code>dev.sh</code>、<code>entrypoint.sh</code></td><td>挂载源码、映射 UID&#x2F;GID、加载 SDK</td></tr><tr><td>验证&#x2F;迁移</td><td><code>verify-env.sh</code>、<code>check-sdk-permissions.sh</code>、<code>export-image.sh</code>、<code>import-image.sh</code></td><td>smoke test、权限回归、离线迁移</td></tr></tbody></table><h2 id="2-为什么-25-GiB-SDK-不适合先压成-tar-gz"><a href="#2-为什么-25-GiB-SDK-不适合先压成-tar-gz" class="headerlink" title="2. 为什么 25 GiB SDK 不适合先压成 tar.gz"></a>2. 为什么 25 GiB SDK 不适合先压成 tar.gz</h2><p>最自然的第一版方案通常是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">Host SDK</span><br><span class="line">  ↓</span><br><span class="line">tar -czf sdk.tar.gz</span><br><span class="line">  ↓</span><br><span class="line">放进 Docker 工程</span><br><span class="line">  ↓</span><br><span class="line">COPY sdk.tar.gz</span><br><span class="line">  ↓</span><br><span class="line">RUN tar -xzf ...</span><br></pre></td></tr></table></figure><p>对于几百 MB 文件没有大问题，但 SDK 展开后约 25 GiB 时，磁盘峰值会很难看：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">原始 SDK          ~25 GiB</span><br><span class="line">SDK 压缩包         额外若干 GiB</span><br><span class="line">Docker build cache 额外空间</span><br><span class="line">最终 Image         ~25 GiB+</span><br></pre></td></tr></table></figure><p>如果开发机根分区只有百余 GiB，构建过程中非常容易逼近磁盘上限。</p><p>所以更合理的思路是：</p><blockquote><p>SDK 保持原目录不动，让 Builder 直接把该目录作为一个构建输入，而不是先复制到项目目录或先压成大包。</p></blockquote><p>这就是 Buildx additional build context 发挥作用的地方。</p><h2 id="3-BuildKit-和-Buildx-到底有什么区别"><a href="#3-BuildKit-和-Buildx-到底有什么区别" class="headerlink" title="3. BuildKit 和 Buildx 到底有什么区别"></a>3. BuildKit 和 Buildx 到底有什么区别</h2><p>这两个名字经常一起出现，但不是同一个东西。</p><h3 id="BuildKit：真正执行构建的后端"><a href="#BuildKit：真正执行构建的后端" class="headerlink" title="BuildKit：真正执行构建的后端"></a>BuildKit：真正执行构建的后端</h3><p>BuildKit 是现代 Docker 镜像构建后端。它负责解析 Dockerfile、建立依赖关系、执行 build step、管理 cache、处理 <code>RUN --mount</code> 等能力。</p><p>可以把它理解成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">Dockerfile</span><br><span class="line">   ↓</span><br><span class="line">BuildKit</span><br><span class="line">   ↓</span><br><span class="line">执行构建图、缓存、生成结果</span><br></pre></td></tr></table></figure><h3 id="Buildx：面向-BuildKit-的-Docker-CLI-扩展"><a href="#Buildx：面向-BuildKit-的-Docker-CLI-扩展" class="headerlink" title="Buildx：面向 BuildKit 的 Docker CLI 扩展"></a>Buildx：面向 BuildKit 的 Docker CLI 扩展</h3><p>Buildx 是 <code>docker</code> CLI 的构建前端&#x2F;扩展。常见命令：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">docker buildx build ...</span><br></pre></td></tr></table></figure><p>Docker 官方文档对它的描述很直接：<code>docker buildx build</code> 使用 BuildKit 启动构建。</p><p>Buildx 在日常工程里很有价值，因为它把这些能力暴露成稳定 CLI：</p><ul><li>additional build context；</li><li>多平台构建；</li><li>多种 output；</li><li>cache import&#x2F;export；</li><li>builder 实例管理；</li><li><code>--load</code>、<code>--push</code> 等输出控制。</li></ul><p>所以可以简化成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Buildx = 你怎样请求一次高级构建</span><br><span class="line">BuildKit = 后面真正执行这次构建的引擎</span><br></pre></td></tr></table></figure><h2 id="4-如果不用-docker-buildx-build-会怎样"><a href="#4-如果不用-docker-buildx-build-会怎样" class="headerlink" title="4. 如果不用 docker buildx build 会怎样"></a>4. 如果不用 <code>docker buildx build</code> 会怎样</h2><p>这里不能简单说“不用 Buildx 就不能构建”。现代 Docker 中，普通：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">docker build .</span><br></pre></td></tr></table></figure><p>本身通常也会使用 BuildKit，而且 Docker 当前 CLI 文档甚至把 <code>docker build</code> 列为 <code>docker buildx build</code> 的别名之一。</p><p>真正区别在于这个工程明确依赖：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">--build-context sdk=/path/to/large-sdk</span><br></pre></td></tr></table></figure><p>以及 Dockerfile 里的：</p><figure class="highlight dockerfile"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">RUN</span><span class="language-bash"> --mount=<span class="built_in">type</span>=<span class="built_in">bind</span>,from=sdk,...</span></span><br></pre></td></tr></table></figure><p>使用 <code>docker buildx build</code> 是为了把构建能力和参数写得明确、可审查，并确保 <code>--build-context</code>、<code>--load</code> 等行为一致。</p><p>如果坚持只用“单一主 context”的思维，那么通常要选择其他方案：</p><ol><li>把 SDK 移到 Docker 工程目录下；</li><li>把整个 SDK 放进主 build context；</li><li>先生成一个大型 tar，再 <code>COPY</code>；</li><li>改用其他外部下载&#x2F;挂载方案。</li></ol><p>对 25 GiB 本地 SDK 来说，这几种方式不是绝对不可行，而是更容易制造额外磁盘占用、破坏目录边界，或者让项目仓库和本机 SDK 强耦合。</p><h2 id="5-Build-context-到底是什么"><a href="#5-Build-context-到底是什么" class="headerlink" title="5. Build context 到底是什么"></a>5. Build context 到底是什么</h2><p>执行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">docker buildx build /home/dev/docker-dev</span><br></pre></td></tr></table></figure><p>最后这个路径就是主 build context。</p><p>Dockerfile 中普通：</p><figure class="highlight dockerfile"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">COPY</span><span class="language-bash"> docker/entrypoint.sh /usr/local/bin/entrypoint</span></span><br></pre></td></tr></table></figure><p>读取的是主 context 里的文件。</p><p>但 SDK 在：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">/opt/vendor-sdk/6.1-release</span><br></pre></td></tr></table></figure><p>并不属于项目目录。</p><p>于是构建命令可以增加：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">--build-context sdk=/opt/vendor-sdk/6.1-release</span><br></pre></td></tr></table></figure><p>得到：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">主 context：/home/dev/docker-dev</span><br><span class="line">    ├── Dockerfile</span><br><span class="line">    ├── scripts/</span><br><span class="line">    └── docker/</span><br><span class="line"></span><br><span class="line">额外 context：sdk</span><br><span class="line">    └── /opt/vendor-sdk/6.1-release</span><br></pre></td></tr></table></figure><p>Dockerfile 便可以通过 <code>from=sdk</code> 使用它。</p><h2 id="6-正式-build-命令逐项解释"><a href="#6-正式-build-命令逐项解释" class="headerlink" title="6. 正式 build 命令逐项解释"></a>6. 正式 build 命令逐项解释</h2><p>一个脱敏后的构建命令：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">docker buildx build \</span><br><span class="line">    --progress=plain \</span><br><span class="line">    --build-context <span class="string">&quot;sdk=/opt/vendor-sdk/6.1-release&quot;</span> \</span><br><span class="line">    --load \</span><br><span class="line">    -t <span class="string">&quot;arm64-dev:20.04&quot;</span> \</span><br><span class="line">    /home/dev/docker-dev</span><br></pre></td></tr></table></figure><h3 id="docker-buildx-build"><a href="#docker-buildx-build" class="headerlink" title="docker buildx build"></a><code>docker buildx build</code></h3><p>使用 Buildx 发起 BuildKit 构建。</p><h3 id="progress-plain"><a href="#progress-plain" class="headerlink" title="--progress=plain"></a><code>--progress=plain</code></h3><p>使用纯文本日志，便于大型 SDK 构建时保存和定位具体 step。</p><h3 id="build-context-sdk"><a href="#build-context-sdk" class="headerlink" title="--build-context &quot;sdk=...&quot;"></a><code>--build-context &quot;sdk=...&quot;</code></h3><p>注册额外 build context，名字叫 <code>sdk</code>。</p><p>它是<strong>构建期输入</strong>，不是运行 Container 时的 Volume，也不是日常 bind mount。</p><h3 id="load"><a href="#load" class="headerlink" title="--load"></a><code>--load</code></h3><p>把单平台构建结果加载进当前 Docker Engine 的本地 Image store。</p><p>这样构建完成后可以直接：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">docker image <span class="built_in">ls</span> arm64-dev:20.04</span><br><span class="line">docker run arm64-dev:20.04</span><br></pre></td></tr></table></figure><p>官方文档把 <code>--load</code> 定义为 <code>--output=type=docker</code> 的快捷方式。</p><h3 id="t-arm64-dev-20-04"><a href="#t-arm64-dev-20-04" class="headerlink" title="-t arm64-dev:20.04"></a><code>-t arm64-dev:20.04</code></h3><p>给最终 Image 设置 repository 和 tag。</p><h3 id="最后的项目目录"><a href="#最后的项目目录" class="headerlink" title="最后的项目目录"></a>最后的项目目录</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">/home/dev/docker-dev</span><br></pre></td></tr></table></figure><p>才是主 context。Dockerfile、Compose 附近脚本、小型资产都从这里来。</p><h2 id="7-Dockerfile-怎样把-SDK-写进-Image"><a href="#7-Dockerfile-怎样把-SDK-写进-Image" class="headerlink" title="7. Dockerfile 怎样把 SDK 写进 Image"></a>7. Dockerfile 怎样把 SDK 写进 Image</h2><p>关键结构可以抽象成：</p><figure class="highlight dockerfile"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># syntax=docker/dockerfile:1.7</span></span><br><span class="line"><span class="keyword">FROM</span> ubuntu:<span class="number">20.04</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">ENV</span> SDK_ROOT=/opt/vendor-sdk/<span class="number">6.1</span>-release</span><br><span class="line"></span><br><span class="line"><span class="keyword">RUN</span><span class="language-bash"> --mount=<span class="built_in">type</span>=<span class="built_in">bind</span>,from=sdk,<span class="built_in">source</span>=/,target=/mnt/sdk,ro \</span></span><br><span class="line"><span class="language-bash">    <span class="built_in">mkdir</span> -p <span class="string">&quot;<span class="variable">$SDK_ROOT</span>&quot;</span> \</span></span><br><span class="line"><span class="language-bash">    &amp;&amp; <span class="built_in">cp</span> -a --no-preserve=ownership /mnt/sdk/. <span class="string">&quot;<span class="variable">$SDK_ROOT</span>&quot;</span>/ \</span></span><br><span class="line"><span class="language-bash">    &amp;&amp; <span class="built_in">chmod</span> -R a+rX <span class="string">&quot;<span class="variable">$SDK_ROOT</span>&quot;</span></span></span><br></pre></td></tr></table></figure><p>这段代码有三个重点。</p><h3 id="构建期-bind-mount-不是运行期-bind-mount"><a href="#构建期-bind-mount-不是运行期-bind-mount" class="headerlink" title="构建期 bind mount 不是运行期 bind mount"></a>构建期 bind mount 不是运行期 bind mount</h3><figure class="highlight dockerfile"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">RUN</span><span class="language-bash"> --mount=<span class="built_in">type</span>=<span class="built_in">bind</span>,from=sdk,...</span></span><br></pre></td></tr></table></figure><p>只在这个 <code>RUN</code> 执行期间存在。</p><p>流程是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">外部 SDK context</span><br><span class="line">    ↓ 临时挂到 /mnt/sdk</span><br><span class="line">RUN step</span><br><span class="line">    ↓ cp</span><br><span class="line">最终 Image 中的 /opt/vendor-sdk/...</span><br></pre></td></tr></table></figure><p>真正进入 Image 的是 <code>cp</code> 结果，不是 mount 本身。</p><h3 id="cp-a-no-preserve-ownership"><a href="#cp-a-no-preserve-ownership" class="headerlink" title="cp -a --no-preserve=ownership"></a><code>cp -a --no-preserve=ownership</code></h3><p><code>-a</code> 尽量保留目录结构、符号链接、时间戳和可执行属性；<code>--no-preserve=ownership</code> 不把 Host 上某个开发者的 UID&#x2F;GID 原样固化进 Image。</p><p>SDK 在 Image 中由 root 持有是合理的，因为它属于只读开发环境，而不是普通用户日常编辑的源码。</p><h3 id="chmod-R-a-rX"><a href="#chmod-R-a-rX" class="headerlink" title="chmod -R a+rX"></a><code>chmod -R a+rX</code></h3><p>这里的 <code>X</code> 非常重要：</p><ul><li>目录会获得 traverse&#x2F;execute；</li><li>原本就是 executable 的工具继续可执行；</li><li>普通 header&#x2F;library 不会因为一个粗暴 <code>chmod -R 777</code> 全变成 executable。</li></ul><p>最终权限策略是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">root 拥有 SDK</span><br><span class="line">普通开发用户可以读取全部 SDK 文件</span><br><span class="line">普通开发用户可以进入全部 SDK 目录</span><br><span class="line">已有 SDK 工具仍可执行</span><br></pre></td></tr></table></figure><p>这正好对应后续真实排障中发现的 SDK 权限问题。</p><h2 id="9-dockerignore-在这里控制什么"><a href="#9-dockerignore-在这里控制什么" class="headerlink" title="9. .dockerignore 在这里控制什么"></a>9. <code>.dockerignore</code> 在这里控制什么</h2><p>当前工程的 <code>.dockerignore</code> 类似：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">.git</span><br><span class="line">.gitignore</span><br><span class="line">logs</span><br><span class="line">*.log</span><br><span class="line">workspace/*</span><br><span class="line">!workspace/.gitkeep</span><br><span class="line">*.zip</span><br><span class="line">*.tar</span><br><span class="line">*.tar.gz</span><br><span class="line">!assets/cmake-3.20.6-linux-x86_64.tar.gz</span><br></pre></td></tr></table></figure><p>它控制的是<strong>主 build context</strong>。</p><p>几个规则很有代表性：</p><ul><li><code>.git</code>：镜像构建不需要把整个 Git 数据库发给 Builder；</li><li><code>workspace/*</code>：开发源码运行时用 bind mount，不进入 Image；</li><li><code>*.zip</code>、<code>*.tar.gz</code>：避免历史归档包不小心进入 context；</li><li><code>!workspace/.gitkeep</code>：重新包含一个占位文件；</li><li><code>!assets/cmake-...tar.gz</code>：虽然全局排除了 <code>*.tar.gz</code>，但这个固定 CMake 资产是 Dockerfile 必须使用的构建输入，所以重新包含。</li></ul><p>需要注意：<code>.dockerignore</code> 不负责过滤 named context <code>sdk</code> 的内容。主 context 与 additional context 是两条输入通道。</p><h2 id="11-prepare-assets-sh：构建前把错误尽早暴露"><a href="#11-prepare-assets-sh：构建前把错误尽早暴露" class="headerlink" title="11. prepare-assets.sh：构建前把错误尽早暴露"></a>11. <code>prepare-assets.sh</code>：构建前把错误尽早暴露</h2><p>这个脚本不是“真正 build Image”，而是准备阶段。</p><p>它主要做：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">检查 SDK 目录</span><br><span class="line">  ↓</span><br><span class="line">检查 environment-setup 可读</span><br><span class="line">  ↓</span><br><span class="line">在子 shell source SDK 并检查交叉编译器</span><br><span class="line">  ↓</span><br><span class="line">确认固定 CMake 版本</span><br><span class="line">  ↓</span><br><span class="line">生成小型 CMake asset</span><br><span class="line">  ↓</span><br><span class="line">确认 docker / buildx / compose 可用</span><br><span class="line">  ↓</span><br><span class="line">打印磁盘信息</span><br></pre></td></tr></table></figure><p>关键点是 vendor SDK 环境放在子 shell 中加载：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">(</span><br><span class="line">    <span class="built_in">set</span> -e</span><br><span class="line">    . <span class="string">&quot;<span class="variable">$SDK_ENV</span>&quot;</span></span><br><span class="line">    <span class="built_in">command</span> -v aarch64-poky-linux-gcc</span><br><span class="line">)</span><br></pre></td></tr></table></figure><p>这样 Yocto 脚本导出的变量不会污染当前 Host shell。</p><p>如果 SDK 路径、权限、CMake 版本或 Docker 插件有问题，最好在传输 25 GiB 之前就失败。</p><h2 id="12-build-image-sh：为什么要有唯一正式构建入口"><a href="#12-build-image-sh：为什么要有唯一正式构建入口" class="headerlink" title="12. build-image.sh：为什么要有唯一正式构建入口"></a>12. <code>build-image.sh</code>：为什么要有唯一正式构建入口</h2><p>核心逻辑是：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">docker buildx build \</span><br><span class="line">    --progress=plain \</span><br><span class="line">    --build-context <span class="string">&quot;sdk=<span class="variable">$SDK_DIR</span>&quot;</span> \</span><br><span class="line">    --load \</span><br><span class="line">    -t <span class="string">&quot;<span class="variable">$DEV_IMAGE</span>&quot;</span> \</span><br><span class="line">    <span class="string">&quot;<span class="variable">$PROJECT_ROOT</span>&quot;</span></span><br></pre></td></tr></table></figure><p>脚本在调用前还会检查：</p><ul><li>Docker 是否存在；</li><li>Buildx 是否可用；</li><li>SDK 是否存在且环境脚本可读；</li><li>小型 CMake asset 是否准备好；</li><li>当前用户不是 root。</li></ul><p>为什么不鼓励每个人手写 build 命令？</p><p>因为长命令很容易产生微小差异：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">有人忘记 --load</span><br><span class="line">有人 SDK 路径不同</span><br><span class="line">有人 tag 不同</span><br><span class="line">有人把 context 写错</span><br></pre></td></tr></table></figure><p>把正式入口写成脚本后，工程只需要维护一份真实构建方式。</p><p>镜像完成后脚本立即运行 <code>verify-env.sh</code>，这是另一个重要设计：<strong>build 成功只是“文件写进 Image”，不代表环境能工作。</strong></p><h2 id="13-verify-env-sh：为什么不能只检查-gcc-version"><a href="#13-verify-env-sh：为什么不能只检查-gcc-version" class="headerlink" title="13. verify-env.sh：为什么不能只检查 gcc --version"></a>13. <code>verify-env.sh</code>：为什么不能只检查 <code>gcc --version</code></h2><p><code>gcc --version</code> 只能证明程序存在，不能证明交叉构建链可用。因此验证脚本还会检查普通用户身份、固定 CMake、host Thrift、<code>SDKTARGETSYSROOT</code>、目标 header&#x2F;library，并实际生成 ARM64 C&#x2F;C++ 文件、执行 <code>-lthrift</code> 链接和最小 CMake build。</p><p>这类 smoke test 的价值是：把真实工程踩过的环境坑提前变成自动门禁。</p><h2 id="14-check-sdk-permissions-sh：为什么单独做全量权限扫描"><a href="#14-check-sdk-permissions-sh：为什么单独做全量权限扫描" class="headerlink" title="14. check-sdk-permissions.sh：为什么单独做全量权限扫描"></a>14. <code>check-sdk-permissions.sh</code>：为什么单独做全量权限扫描</h2><p>完整 SDK 约 25 GiB，没必要每次进入 Container 都扫描全部文件。</p><p>所以快速验证只检查关键文件，而独立脚本可以执行全量审计：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">find <span class="string">&quot;<span class="variable">$SDK_ROOT</span>&quot;</span> -<span class="built_in">type</span> d ! -perm -0001 -<span class="built_in">print</span> -quit</span><br><span class="line">find <span class="string">&quot;<span class="variable">$SDK_ROOT</span>&quot;</span> -<span class="built_in">type</span> f ! -perm -0004 -<span class="built_in">print</span> -quit</span><br></pre></td></tr></table></figure><p>第一条找“other 没有 execute&#x2F;traverse”的目录，第二条找普通用户无法读取的文件。</p><p><code>-print -quit</code> 找到第一个错误就停止，便于快速给出明确失败点。</p><p>这和 Dockerfile 的 <code>chmod -R a+rX</code> 正好形成前后对应：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">构建时建立权限策略</span><br><span class="line">        ↓</span><br><span class="line">审计脚本验证策略没有回退</span><br></pre></td></tr></table></figure><h2 id="15-compose-yaml、dev-sh-与-entrypoint-sh-怎样配合"><a href="#15-compose-yaml、dev-sh-与-entrypoint-sh-怎样配合" class="headerlink" title="15. compose.yaml、dev.sh 与 entrypoint.sh 怎样配合"></a>15. <code>compose.yaml</code>、<code>dev.sh</code> 与 <code>entrypoint.sh</code> 怎样配合</h2><p>镜像构建好以后，日常开发不应该继续手写一大串 <code>docker run</code>。</p><p>Compose 可以保存稳定的运行配置：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">services:</span></span><br><span class="line">  <span class="attr">dev:</span></span><br><span class="line">    <span class="attr">image:</span> <span class="string">&quot;$&#123;DEV_IMAGE:-arm64-dev:20.04&#125;&quot;</span></span><br><span class="line">    <span class="attr">environment:</span></span><br><span class="line">      <span class="attr">HOST_UID:</span> <span class="string">&quot;$&#123;HOST_UID:-1000&#125;&quot;</span></span><br><span class="line">      <span class="attr">HOST_GID:</span> <span class="string">&quot;$&#123;HOST_GID:-1000&#125;&quot;</span></span><br><span class="line">    <span class="attr">volumes:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="attr">type:</span> <span class="string">bind</span></span><br><span class="line">        <span class="attr">source:</span> <span class="string">&quot;$&#123;WORKSPACE_DIR:-./workspace&#125;&quot;</span></span><br><span class="line">        <span class="attr">target:</span> <span class="string">/workspace</span></span><br><span class="line">    <span class="attr">working_dir:</span> <span class="string">/workspace</span></span><br><span class="line">    <span class="attr">stdin_open:</span> <span class="literal">true</span></span><br><span class="line">    <span class="attr">tty:</span> <span class="literal">true</span></span><br></pre></td></tr></table></figure><p>这里：</p><ul><li><code>image</code> 选择开发镜像；</li><li><code>environment</code> 把 Host UID&#x2F;GID 交给入口脚本；</li><li><code>volumes</code> 使用 bind mount 暴露真实源码；</li><li><code>working_dir</code> 进入 <code>/workspace</code>；</li><li><code>stdin_open + tty</code> 对应交互式开发 shell。</li></ul><p><code>dev.sh</code> 则负责读取当前 Host 的动态信息：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">HOST_UID=$(<span class="built_in">id</span> -u)</span><br><span class="line">HOST_GID=$(<span class="built_in">id</span> -g)</span><br></pre></td></tr></table></figure><p>并检查 workspace 真正可读、可写、可进入，再：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">exec</span> docker compose -f compose.yaml run --<span class="built_in">rm</span> dev <span class="string">&quot;<span class="variable">$@</span>&quot;</span></span><br></pre></td></tr></table></figure><p>这样使用者只需要：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">./scripts/dev.sh /home/dev/work/demo-project</span><br></pre></td></tr></table></figure><p>而不用记住 Compose 的所有参数。</p><h2 id="16-entrypoint-sh-为什么是运行时核心"><a href="#16-entrypoint-sh-为什么是运行时核心" class="headerlink" title="16. entrypoint.sh 为什么是运行时核心"></a>16. <code>entrypoint.sh</code> 为什么是运行时核心</h2><p>Image 中可以预建用户 <code>devuser</code>，但不同 Host 的 UID&#x2F;GID 可能是 1000、1001 或其他值。</p><p>入口脚本先以 root 启动，做几件只能 root 完成的事：</p><ol><li>校验 <code>HOST_UID/HOST_GID</code>；</li><li>调整 Container 开发用户的数字 UID&#x2F;GID；</li><li>只修正 <code>/home/devuser</code> 的 ownership；</li><li><code>source</code> Yocto SDK 环境；</li><li>把固定 CMake 放回 <code>PATH</code> 前部；</li><li>根据已验证工程契约正规化 <code>CC/CXX</code>；</li><li>使用 <code>setpriv</code> 降权；</li><li>最终 <code>exec</code> 真正的 Bash&#x2F;CMake&#x2F;Make。</li></ol><p>核心思想是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">让 Container 用户适应 Host 文件身份</span><br><span class="line">而不是让 Container 去改 Host 源码所有权</span><br></pre></td></tr></table></figure><p>所以入口脚本明确不能：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">chown</span> -R /workspace</span><br></pre></td></tr></table></figure><p>因为 <code>/workspace</code> 是 bind mount。</p><p>最后：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">exec</span> setpriv ... -- <span class="string">&quot;<span class="variable">$@</span>&quot;</span></span><br></pre></td></tr></table></figure><p>让真正命令替换 entrypoint shell，退出码和信号传播也更自然。</p><h2 id="17-export-image-sh-和-import-image-sh-为什么也属于开发环境工程"><a href="#17-export-image-sh-和-import-image-sh-为什么也属于开发环境工程" class="headerlink" title="17. export-image.sh 和 import-image.sh 为什么也属于开发环境工程"></a>17. <code>export-image.sh</code> 和 <code>import-image.sh</code> 为什么也属于开发环境工程</h2><p>大镜像构建耗时较长，新 Host 可以直接迁移已经验证的 Image。<code>export-image.sh</code> 用 <code>docker save</code> 导出、gzip 压缩并生成 SHA256；<code>import-image.sh</code> 解压后调用 <code>docker load</code>。脚本刻意把保存和压缩拆成独立步骤，便于在 POSIX <code>sh</code> 下准确判断失败。</p><p>注意：<code>docker save/load</code> 迁移的是 Image，不会把 bind-mounted Git 源码一起打包。</p><h2 id="20-把整个工程串起来"><a href="#20-把整个工程串起来" class="headerlink" title="20. 把整个工程串起来"></a>20. 把整个工程串起来</h2><p>最终链路是：<code>prepare-assets.sh</code> 检查输入 → <code>build-image.sh</code> 调用 Buildx → Dockerfile 生成 Image → <code>verify-env.sh</code> 做 smoke test → <code>dev.sh + compose.yaml + entrypoint.sh</code> 提供日常开发环境；权限审计和 Image 导入导出则用于回归与迁移。</p><p>这套结构把旧开发机上的隐式环境变成了显式、可验证、可迁移的工程。下一篇继续讨论真实工程构建时暴露的 CMakeCache、host&#x2F;target、权限、flags 与链接问题。</p><h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ul><li>Docker Buildx build: <a href="https://docs.docker.com/reference/cli/docker/buildx/build/">https://docs.docker.com/reference/cli/docker/buildx/build/</a></li><li>Docker bind mounts: <a href="https://docs.docker.com/engine/storage/bind-mounts/">https://docs.docker.com/engine/storage/bind-mounts/</a></li><li>Docker storage: <a href="https://docs.docker.com/engine/storage/">https://docs.docker.com/engine/storage/</a></li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;meta name=&quot;referrer&quot; content=&quot;no-referrer&quot; /&gt;

&lt;h1 id=&quot;大型-SDK-开发镜像工程化：BuildKit、Buildx-与项目脚本&quot;&gt;&lt;a href=&quot;#大型-SDK-开发镜像工程化：BuildKit、Buildx-与项目脚本</summary>
      
    
    
    
    <category term="docker" scheme="https://wdfk-prog.space/categories/docker/"/>
    
    
    <category term="docker" scheme="https://wdfk-prog.space/tags/docker/"/>
    
  </entry>
  
  <entry>
    <title>03-交叉编译开发容器故障排查：从CMakeCache到目标库链接</title>
    <link href="https://wdfk-prog.space/posts/83a8516e/"/>
    <id>https://wdfk-prog.space/posts/83a8516e/</id>
    <published>2026-09-03T07:50:11.000Z</published>
    <updated>2026-09-03T07:50:26.388Z</updated>
    
    <content type="html"><![CDATA[<meta name="referrer" content="no-referrer" /><h1 id="交叉编译开发容器故障排查：从-CMakeCache-到目标库链接"><a href="#交叉编译开发容器故障排查：从-CMakeCache-到目标库链接" class="headerlink" title="交叉编译开发容器故障排查：从 CMakeCache 到目标库链接"></a>交叉编译开发容器故障排查：从 CMakeCache 到目标库链接</h1><blockquote><p>摘要：按真实首次失败点复盘旧 CMakeCache、host&#x2F;target Thrift、SDK 权限、编译参数冲突和目标库链接，说明怎样沿证据链逐层收敛问题。</p></blockquote><p>@[toc]</p><p><img src="https://i-blog.csdnimg.cn/direct/bcaea9ba1a6e423ea477096981003349.png" alt="在这里插入图片描述"></p><p>Docker 能启动、交叉编译器能执行，只能证明环境最外层入口正常。历史 C&#x2F;C++ 工程仍可能依次暴露旧 CMake 缓存、宿主工具、目标库、权限和编译参数策略。</p><p>这次排查最重要的原则是：<strong>围绕当前第一次失败收集证据，证明后再继续向后。</strong></p><p>最终故障链可以概括为：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    Cache[&quot;旧 CMakeCache\n编译器路径属于旧 Host&quot;] --&gt;|干净 build tree| Compiler[&quot;交叉 GCC 正确识别&quot;]</span><br><span class="line">    Compiler --&gt; Thrift[&quot;host Thrift 查找失败&quot;]</span><br><span class="line">    Thrift --&gt;|确认 host generator| Header[&quot;target header Permission denied&quot;]</span><br><span class="line">    Header --&gt;|修 SDK 可读/可遍历| Flags[&quot;SDK flags 与项目 flags 冲突&quot;]</span><br><span class="line">    Flags --&gt;|分离工具链与项目编译策略| Link[&quot;ld: cannot find -lthrift&quot;]</span><br><span class="line">    Link --&gt;|确认 target library 与权限| Success[&quot;完整工程链接成功&quot;]</span><br></pre></td></tr></table></figure><h2 id="1-先把排障范围分成三层"><a href="#1-先把排障范围分成三层" class="headerlink" title="1. 先把排障范围分成三层"></a>1. 先把排障范围分成三层</h2><p>遇到构建失败时，先不要把所有错误都归类成“Docker 问题”。</p><p>可以先划三层：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line">Docker 层</span><br><span class="line">├── Image 是否存在</span><br><span class="line">├── Container 是否能启动</span><br><span class="line">├── bind mount 是否正确</span><br><span class="line">└── UID/GID 是否合理</span><br><span class="line"></span><br><span class="line">工具链/SDK 层</span><br><span class="line">├── environment-setup 是否加载</span><br><span class="line">├── CC/CXX 是否正确</span><br><span class="line">├── sysroot 是否正确</span><br><span class="line">├── target header/library 是否存在、可读</span><br><span class="line">└── host 工具是否齐全</span><br><span class="line"></span><br><span class="line">工程层</span><br><span class="line">├── CMakeCache</span><br><span class="line">├── CMakeLists/toolchain 文件</span><br><span class="line">├── 项目自己的 flags</span><br><span class="line">├── 硬编码路径</span><br><span class="line">└── 真实源码/链接依赖</span><br></pre></td></tr></table></figure><p>如果 Container 和交叉 GCC 已正常，而 CMake 仍报旧绝对路径，优先检查工程状态，不要先重建 Image。</p><h2 id="2-第一层故障：CMake-为什么还在找旧机器的-usr-local-bin-cc"><a href="#2-第一层故障：CMake-为什么还在找旧机器的-usr-local-bin-cc" class="headerlink" title="2. 第一层故障：CMake 为什么还在找旧机器的 /usr/local/bin/cc"></a>2. 第一层故障：CMake 为什么还在找旧机器的 <code>/usr/local/bin/cc</code></h2><p>容器里可以确认：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">command</span> -v aarch64-poky-linux-gcc</span><br><span class="line">aarch64-poky-linux-gcc --version</span><br></pre></td></tr></table></figure><p>输出正确，但项目 configure 却报：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">The CMAKE_C_COMPILER:</span><br><span class="line">  /usr/local/bin/cc</span><br><span class="line">is not a full path to an existing compiler tool.</span><br></pre></td></tr></table></figure><p>第一反应很容易是：“SDK 没加载。”</p><p>但错误本身已经给出一个更值得追的线索：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">/usr/local/bin/cc</span><br></pre></td></tr></table></figure><p>这明显像旧 Host 上的历史路径。</p><p>于是先读 CMake cache：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">grep -E <span class="string">&#x27;CMAKE_(C|CXX)_COMPILER|CMAKE_TOOLCHAIN_FILE&#x27;</span> \</span><br><span class="line">    build/CMakeCache.txt | <span class="built_in">head</span> -30</span><br></pre></td></tr></table></figure><p>如果看到：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">CMAKE_C_COMPILER:FILEPATH=/usr/local/bin/cc</span><br><span class="line">CMAKE_CXX_COMPILER:FILEPATH=/usr/local/bin/c++</span><br></pre></td></tr></table></figure><p>链路就很清楚了：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">Host 原工程已有 build/</span><br><span class="line">        ↓</span><br><span class="line">源码通过 bind mount 进入 Container</span><br><span class="line">        ↓</span><br><span class="line">build/ 和 CMakeCache.txt 也一起进来了</span><br><span class="line">        ↓</span><br><span class="line">CMake 继续使用旧机器缓存的 compiler path</span><br><span class="line">        ↓</span><br><span class="line">Container 没有同一路径</span><br></pre></td></tr></table></figure><h3 id="为什么不要第一时间删除-Host-的-build"><a href="#为什么不要第一时间删除-Host-的-build" class="headerlink" title="为什么不要第一时间删除 Host 的 build/"></a>为什么不要第一时间删除 Host 的 <code>build/</code></h3><p>bind mount 下 <code>/workspace/build</code> 就是 Host 的真实 build 目录。为了验证假设，没必要先破坏旧构建结果，可以直接创建全新的临时 build tree：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">rm</span> -rf /tmp/demo-build</span><br><span class="line"></span><br><span class="line">cmake \</span><br><span class="line">    -S /workspace \</span><br><span class="line">    -B /tmp/demo-build \</span><br><span class="line">    -DPROJECT=demo</span><br></pre></td></tr></table></figure><p>如果新目录能够识别：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">The C compiler identification is GNU 12.2.0</span><br><span class="line">The CXX compiler identification is GNU 12.2.0</span><br></pre></td></tr></table></figure><p>并选择 Yocto AArch64 compiler，那么第一层已经闭环：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">不是 Docker 没有 compiler</span><br><span class="line">不是 SDK 本身损坏</span><br><span class="line">而是旧 CMakeCache 把旧 Host 环境带进了 Container</span><br></pre></td></tr></table></figure><p>结论是：<strong>源码相同，不代表 build tree 可以跨环境直接复用。</strong></p><h2 id="3-第二层故障：Thrift-compiler-not-found-到底缺谁"><a href="#3-第二层故障：Thrift-compiler-not-found-到底缺谁" class="headerlink" title="3. 第二层故障：Thrift compiler not found 到底缺谁"></a>3. 第二层故障：<code>Thrift compiler not found</code> 到底缺谁</h2><p>CMakeCache 问题解决后，configure 继续推进，新的第一次失败变成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Thrift compiler not found.</span><br></pre></td></tr></table></figure><p>看到 Thrift 时，必须先区分 host 和 target。</p><p>典型交叉编译流程：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">*.thrift</span><br><span class="line">   │</span><br><span class="line">   │ x86-64 Host 上执行 thrift</span><br><span class="line">   ▼</span><br><span class="line">生成 C++ .cpp/.h</span><br><span class="line">   │</span><br><span class="line">   │ aarch64-poky-linux-g++</span><br><span class="line">   ▼</span><br><span class="line">ARM64 object</span><br><span class="line">   │</span><br><span class="line">   │ target libthrift</span><br><span class="line">   ▼</span><br><span class="line">ARM64 可执行程序</span><br></pre></td></tr></table></figure><p>这里至少有三样不同的东西：</p><table><thead><tr><th>对象</th><th>架构&#x2F;位置</th><th>作用</th></tr></thead><tbody><tr><td><code>thrift</code> 可执行程序</td><td>x86-64 Container Host side</td><td>根据 <code>.thrift</code> 生成代码</td></tr><tr><td>Thrift headers</td><td>ARM64 target sysroot</td><td>交叉编译生成代码</td></tr><tr><td><code>libthrift.a/.so</code></td><td>ARM64 target sysroot</td><td>最终链接目标程序</td></tr></tbody></table><p>所以配置阶段说 <code>Thrift compiler not found</code>，首先检查的是 host 工具：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">command</span> -v thrift</span><br><span class="line">thrift -version</span><br></pre></td></tr></table></figure><p>如果没有，再临时安装 host 侧：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">apt-get update</span><br><span class="line">apt-get install -y --no-install-recommends thrift-compiler</span><br></pre></td></tr></table></figure><p>不能用 Ubuntu x86-64 <code>libthrift-dev</code> 替代 target ARM64 library。</p><h2 id="4-usr-bin-thrift-已经存在，为什么-CMake-仍然报找不到"><a href="#4-usr-bin-thrift-已经存在，为什么-CMake-仍然报找不到" class="headerlink" title="4. /usr/bin/thrift 已经存在，为什么 CMake 仍然报找不到"></a>4. <code>/usr/bin/thrift</code> 已经存在，为什么 CMake 仍然报找不到</h2><p>更隐蔽的一种情况是：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">command</span> -v thrift</span><br><span class="line"><span class="comment"># /usr/bin/thrift</span></span><br></pre></td></tr></table></figure><p>工具明明存在，但 CMake 仍然失败。</p><p>此时要继续检查项目自己的查找逻辑。如果历史 CMake 写成类似：</p><figure class="highlight cmake"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">find_program</span>(THRIFT_COMPILER /usr/local/bin/thrift)</span><br></pre></td></tr></table></figure><p>那问题就不再是“没有 Thrift”，而是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Container 实际安装：/usr/bin/thrift</span><br><span class="line">项目历史契约：      /usr/local/bin/thrift</span><br></pre></td></tr></table></figure><p>为了先证明根因，不需要马上重构生产 CMake。可以显式传入：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">cmake \</span><br><span class="line">    -DTHRIFT_COMPILER=/usr/bin/thrift \</span><br><span class="line">    -S /workspace \</span><br><span class="line">    -B /tmp/demo-build</span><br></pre></td></tr></table></figure><p>如果能够越过这个检查，路径契约不一致就被证实。</p><p>长期更通用的工程写法可以是：</p><figure class="highlight cmake"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">find_program</span>(THRIFT_COMPILER NAMES thrift)</span><br></pre></td></tr></table></figure><p>或保留可由外部 <code>-DTHRIFT_COMPILER=...</code> 覆盖的变量。</p><p><code>not found</code> 既可能是程序没装，也可能只是搜索路径不匹配。</p><h2 id="5-第三层故障：头文件明明存在，却-Permission-denied"><a href="#5-第三层故障：头文件明明存在，却-Permission-denied" class="headerlink" title="5. 第三层故障：头文件明明存在，却 Permission denied"></a>5. 第三层故障：头文件明明存在，却 <code>Permission denied</code></h2><p>Thrift generator 通过后，编译开始，新的错误变成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">.../usr/include/thrift/TDispatchProcessor.h: Permission denied</span><br></pre></td></tr></table></figure><p>它和 <code>No such file or directory</code> 是两类问题。</p><p>先看目录和文件：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">ls</span> -ld <span class="string">&quot;<span class="variable">$TARGET_SYSROOT</span>/usr/include/thrift&quot;</span></span><br><span class="line"><span class="built_in">ls</span> -l <span class="string">&quot;<span class="variable">$TARGET_SYSROOT</span>/usr/include/thrift/TDispatchProcessor.h&quot;</span></span><br></pre></td></tr></table></figure><p>当时的典型问题是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Container:</span><br><span class="line">root:root 770</span><br></pre></td></tr></table></figure><p>而编译进程使用普通开发用户。</p><p><code>770</code> 表示：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">owner: rwx</span><br><span class="line">group: rwx</span><br><span class="line">other: ---</span><br></pre></td></tr></table></figure><p>如果普通开发用户既不是 owner，也不在该 group，它甚至无法 traverse 目录。</p><p>临时验证可以只改当前范围：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">find <span class="string">&quot;<span class="variable">$THRIFT_DIR</span>&quot;</span> -<span class="built_in">type</span> d -<span class="built_in">exec</span> <span class="built_in">chmod</span> a+rx &#123;&#125; \;</span><br><span class="line">find <span class="string">&quot;<span class="variable">$THRIFT_DIR</span>&quot;</span> -<span class="built_in">type</span> f -<span class="built_in">exec</span> <span class="built_in">chmod</span> a+r &#123;&#125; \;</span><br></pre></td></tr></table></figure><p>然后直接用：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">head</span> -n 2 <span class="string">&quot;<span class="variable">$THRIFT_HEADER</span>&quot;</span></span><br></pre></td></tr></table></figure><p>确认当前用户是否真的可读。</p><p>权限修正后编译继续推进，说明判断成立。</p><h2 id="6-为什么-Host-上能读，COPY-进-Image-后却不能读"><a href="#6-为什么-Host-上能读，COPY-进-Image-后却不能读" class="headerlink" title="6. 为什么 Host 上能读，COPY 进 Image 后却不能读"></a>6. 为什么 Host 上能读，COPY 进 Image 后却不能读</h2><p>这是理解整个 SDK 权限问题的关键。</p><p>假设 Host SDK 是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">owner=devuser</span><br><span class="line">group=devuser</span><br><span class="line">mode=770</span><br></pre></td></tr></table></figure><p>Host 的开发用户正好是 owner，所以一直正常。</p><p>导入 Image 后如果变成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">owner=root</span><br><span class="line">group=root</span><br><span class="line">mode=770</span><br></pre></td></tr></table></figure><p>mode 没变，但访问主体变了：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Host devuser = owner -&gt; rwx</span><br><span class="line">Container devuser = other -&gt; ---</span><br></pre></td></tr></table></figure><p>于是以前“看起来没有问题”的 SDK 权限依赖，在容器化后暴露出来。</p><p>这说明旧 SDK 权限策略依赖特定 owner&#x2F;group。最终修复不应停留在“哪个文件报错就 chmod 哪个文件”，而应在 Image 构建阶段建立统一只读 SDK 权限策略：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">chmod</span> -R a+rX <span class="string">&quot;<span class="variable">$SDK_ROOT</span>&quot;</span></span><br></pre></td></tr></table></figure><p>然后用独立权限审计脚本检查全 SDK。</p><h2 id="7-第四层故障：-O2、-O0、FORTIFY-和-format-security-为什么会撞在一起"><a href="#7-第四层故障：-O2、-O0、FORTIFY-和-format-security-为什么会撞在一起" class="headerlink" title="7. 第四层故障：-O2、-O0、FORTIFY 和 format-security 为什么会撞在一起"></a>7. 第四层故障：<code>-O2</code>、<code>-O0</code>、FORTIFY 和 format-security 为什么会撞在一起</h2><p>权限问题解决后，真正的 compiler command 开始执行，却出现：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">-Wformat-security ignored without -Wformat [-Werror=format-security]</span><br><span class="line">_FORTIFY_SOURCE requires compiling with optimization (-O)</span><br></pre></td></tr></table></figure><p>这种错误只看最后一行很难判断，应该直接打印完整命令：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">cmake --build /tmp/demo-build --verbose -j1 \</span><br><span class="line">    2&gt;&amp;1 | <span class="built_in">tee</span> /tmp/demo-build.log</span><br></pre></td></tr></table></figure><p>这里几个参数都很有用：</p><ul><li><code>--verbose</code>：看到 CMake 最终调用的 compiler&#x2F;linker command；</li><li><code>-j1</code>：先单线程，避免并行日志把第一条失败命令淹没；</li><li><code>2&gt;&amp;1</code>：合并 stderr&#x2F;stdout；</li><li><code>tee</code>：终端显示的同时保存日志。</li></ul><p>完整命令显示 SDK 环境先注入：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">-O2</span><br><span class="line">-D_FORTIFY_SOURCE=2</span><br><span class="line">-Wformat</span><br><span class="line">-Wformat-security</span><br><span class="line">-Werror=format-security</span><br></pre></td></tr></table></figure><p>而项目自己的 CMake 后面又加入：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">-O0</span><br><span class="line">-Wno-format</span><br></pre></td></tr></table></figure><p>这时错误就能解释了。</p><p>例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">SDK：希望开启 format-security</span><br><span class="line">项目：显式关闭 format warning</span><br><span class="line"></span><br><span class="line">SDK：FORTIFY_SOURCE + -O2</span><br><span class="line">项目：最终选择 -O0</span><br></pre></td></tr></table></figure><p>问题不是“GCC 突然坏了”，而是两套编译策略叠在同一条 command line 上。</p><h2 id="8-为什么不推荐直接加-Wno-error-format-security"><a href="#8-为什么不推荐直接加-Wno-error-format-security" class="headerlink" title="8. 为什么不推荐直接加 -Wno-error=format-security"></a>8. 为什么不推荐直接加 <code>-Wno-error=format-security</code></h2><p>追加：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">-Wno-error=format-security</span><br></pre></td></tr></table></figure><p>也许可以让某条 warning 暂时不作为 error，但它没有回答几个核心问题：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">最终优化等级应该由谁控制？</span><br><span class="line">FORTIFY 在 -O0 下的预期是什么？</span><br><span class="line">项目为什么主动 -Wno-format？</span><br><span class="line">SDK 的 security flags 是工具链必要条件，还是默认 policy？</span><br></pre></td></tr></table></figure><p>真正需要拆分的是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">工具链能力</span><br><span class="line">    与</span><br><span class="line">项目编译策略</span><br></pre></td></tr></table></figure><p>Yocto <code>environment-setup</code> 往往不只提供 flags，它还提供：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">PATH</span><br><span class="line">SDKTARGETSYSROOT</span><br><span class="line">OECORE_*</span><br><span class="line">PKG_CONFIG_*</span><br><span class="line">交叉工具位置</span><br></pre></td></tr></table></figure><p>所以也不能简单“不要 source SDK”。</p><p>这次已经验证可工作的兼容策略是：保留 SDK、sysroot 和工具路径，但让业务工程继续拥有自己的 <code>-O0</code>、warning policy。</p><p>例如入口脚本中把：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">CC=<span class="string">&quot;aarch64-poky-linux-gcc --sysroot=<span class="variable">$SDKTARGETSYSROOT</span>&quot;</span></span><br><span class="line">CXX=<span class="string">&quot;aarch64-poky-linux-g++ --sysroot=<span class="variable">$SDKTARGETSYSROOT</span>&quot;</span></span><br><span class="line"><span class="built_in">export</span> CC CXX</span><br><span class="line"><span class="built_in">unset</span> CFLAGS CXXFLAGS CPPFLAGS LDFLAGS</span><br></pre></td></tr></table></figure><p>作为这个特定工程的兼容契约。</p><p>这里必须强调：这不是所有 Yocto 工程都应该照抄的通用规则。某些项目确实依赖 SDK 导出的 flags。它只适用于已经通过真实编译证据确认“项目自己拥有编译策略”的场景。</p><h2 id="9-第五层故障：编译到链接阶段才报-cannot-find-lthrift"><a href="#9-第五层故障：编译到链接阶段才报-cannot-find-lthrift" class="headerlink" title="9. 第五层故障：编译到链接阶段才报 cannot find -lthrift"></a>9. 第五层故障：编译到链接阶段才报 <code>cannot find -lthrift</code></h2><p>当编译已经推进很远，最后出现：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ld: cannot find -lthrift</span><br></pre></td></tr></table></figure><p>此时首先应该承认一个积极事实：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">代码生成通过</span><br><span class="line">header 编译通过</span><br><span class="line">大量 C/C++ object 已生成</span><br><span class="line">当前第一次失败已经进入 link stage</span><br></pre></td></tr></table></figure><p>然后再问：<code>-lthrift</code> 到底要找什么？</p><p>GCC&#x2F;ld 的：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">-lthrift</span><br></pre></td></tr></table></figure><p>通常表示在 library search path 中查找类似：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">libthrift.so</span><br><span class="line">libthrift.a</span><br></pre></td></tr></table></figure><p>但这是交叉编译，所以需要的是 <strong>ARM64 target library</strong>，不是 Host Ubuntu 的 x86-64 <code>libthrift-dev</code>。</p><p>先搜 target sysroot：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">find <span class="string">&quot;<span class="variable">$TARGET_SYSROOT</span>/usr/lib&quot;</span> <span class="string">&quot;<span class="variable">$TARGET_SYSROOT</span>/lib&quot;</span> \</span><br><span class="line">    \( -name <span class="string">&#x27;libthrift.so*&#x27;</span> -o -name <span class="string">&#x27;libthrift.a&#x27;</span> \) \</span><br><span class="line">    -<span class="built_in">ls</span> 2&gt;/dev/null</span><br></pre></td></tr></table></figure><p>如果已经找到：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$TARGET_SYSROOT/lib/libthrift.a</span><br></pre></td></tr></table></figure><p>就说明“库不存在”这个假设并不成立。</p><p>下一步检查当前用户是否能读：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">test</span> -r <span class="string">&quot;<span class="variable">$TARGET_SYSROOT</span>/lib/libthrift.a&quot;</span> \</span><br><span class="line">    &amp;&amp; <span class="built_in">echo</span> <span class="string">&quot;THRIFT READABLE&quot;</span> \</span><br><span class="line">    || <span class="built_in">echo</span> <span class="string">&quot;THRIFT NOT READABLE&quot;</span></span><br></pre></td></tr></table></figure><p>这比只 <code>ls</code> 更贴近 linker 的实际需求。</p><h2 id="10-print-sysroot-显示奇怪路径，为什么不能立刻判定根因"><a href="#10-print-sysroot-显示奇怪路径，为什么不能立刻判定根因" class="headerlink" title="10. --print-sysroot 显示奇怪路径，为什么不能立刻判定根因"></a>10. <code>--print-sysroot</code> 显示奇怪路径，为什么不能立刻判定根因</h2><p><code>aarch64-poky-linux-g++ --print-sysroot</code> 只反映 compiler driver 的默认状态。CMake 项目可能在真实 link command 中显式传入 <code>--sysroot</code>，所以应继续查看：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">cat</span> CMakeFiles/&lt;target&gt;.<span class="built_in">dir</span>/link.txt</span><br></pre></td></tr></table></figure><p>如果实际命令已有正确 <code>--sysroot=/opt/vendor-sdk/...</code>，就应以当前失败现场真正执行的命令为准，而不是孤立解释 <code>--print-sysroot</code>。</p><h2 id="11-为什么-target-library-存在、sysroot-也正确，linker-仍可能说找不到"><a href="#11-为什么-target-library-存在、sysroot-也正确，linker-仍可能说找不到" class="headerlink" title="11. 为什么 target library 存在、sysroot 也正确，linker 仍可能说找不到"></a>11. 为什么 target library 存在、sysroot 也正确，linker 仍可能说找不到</h2><p>前面已经证明：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">真实 link command 有正确 --sysroot</span><br><span class="line">目标 libthrift.a 在 sysroot 标准 lib 目录</span><br></pre></td></tr></table></figure><p>但如果这个文件仍然是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">root:root 770</span><br></pre></td></tr></table></figure><p>普通开发用户无法读取，linker 可能最终表现成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">cannot find -lthrift</span><br></pre></td></tr></table></figure><p>修正目录 traverse 和文件 read 权限以后：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">test</span> -r <span class="string">&quot;<span class="variable">$TARGET_SYSROOT</span>/lib/libthrift.a&quot;</span></span><br></pre></td></tr></table></figure><p>成功，再重新 link，构建完成。</p><p>这说明错误文本里的“找不到”，真实原因也可能是“路径存在但当前用户无权读取”。</p><h2 id="13-为什么一次只改变一个变量"><a href="#13-为什么一次只改变一个变量" class="headerlink" title="13. 为什么一次只改变一个变量"></a>13. 为什么一次只改变一个变量</h2><p>这次迁移里，如果同时：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">改 Dockerfile</span><br><span class="line">改 CMakeLists</span><br><span class="line">改 toolchain.cmake</span><br><span class="line">换 Thrift 版本</span><br><span class="line">加 LIBRARY_PATH</span><br><span class="line">改 SDK 权限</span><br></pre></td></tr></table></figure><p>即使最后“能编译”，也很难知道到底哪个改动真正必要。</p><p>更有证据力的顺序是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">1. 观察当前第一次失败</span><br><span class="line">2. 提一个具体假设</span><br><span class="line">3. 做最小实验</span><br><span class="line">4. 观察结果</span><br><span class="line">5. 确认/排除假设</span><br><span class="line">6. 再进入下一个 blocker</span><br></pre></td></tr></table></figure><p>例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">旧 compiler path</span><br><span class="line">→ 只换干净 build tree</span><br><span class="line">→ compiler 正确</span><br><span class="line">→ 第一层闭环</span><br><span class="line"></span><br><span class="line">Thrift not found</span><br><span class="line">→ 只指定 /usr/bin/thrift</span><br><span class="line">→ configure 继续</span><br><span class="line">→ 第二层闭环</span><br><span class="line"></span><br><span class="line">Permission denied</span><br><span class="line">→ 只修当前 SDK 读取权限</span><br><span class="line">→ compile 继续</span><br><span class="line">→ 第三层闭环</span><br></pre></td></tr></table></figure><p>这种方法的价值是每一步都能留下可复核的因果关系。</p><h2 id="14-怎样把临时修复固化进开发-Image"><a href="#14-怎样把临时修复固化进开发-Image" class="headerlink" title="14. 怎样把临时修复固化进开发 Image"></a>14. 怎样把临时修复固化进开发 Image</h2><p>真正完成容器化以后，不应该每次手工：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">apt install thrift-compiler</span><br><span class="line">-DTHRIFT_COMPILER=/usr/bin/thrift</span><br><span class="line">chmod 某个 header</span><br><span class="line">chmod 某个 libthrift.a</span><br><span class="line">unset 一串 flags</span><br></pre></td></tr></table></figure><p>这些知识应该分别进入稳定位置：</p><table><thead><tr><th>已确认问题</th><th>更合理的固化位置</th></tr></thead><tbody><tr><td>host Thrift 缺失</td><td>Dockerfile 安装 host package</td></tr><tr><td>SDK owner&#x2F;mode 导致普通用户不可读</td><td>Dockerfile 导入 SDK 时统一 <code>a+rX</code></td></tr><tr><td>UID&#x2F;GID 与 Host 不一致</td><td><code>dev.sh</code> + <code>entrypoint.sh</code> 运行时映射</td></tr><tr><td>SDK 与项目 flags 冲突</td><td>entrypoint&#x2F;toolchain 的明确兼容策略</td></tr><tr><td>只检查 compiler 太弱</td><td><code>verify-env.sh</code> 做 compile&#x2F;link smoke test</td></tr><tr><td>旧 CMakeCache 迁移风险</td><td>首次跨环境构建使用干净 build tree</td></tr></tbody></table><p>这样下一次 Image rebuild 或换 Host 时，旧问题要么被自动修正，要么在 smoke test 阶段直接失败。</p><h2 id="16-这次排障最终真正留下什么"><a href="#16-这次排障最终真正留下什么" class="headerlink" title="16. 这次排障最终真正留下什么"></a>16. 这次排障最终真正留下什么</h2><p>最值得保留的不是“Thrift 怎么修”，而是一套可迁移的方法：</p><ol><li><strong>先分层。</strong> Docker、SDK、CMake 和源码不是同一个问题域。</li><li><strong>围绕第一次失败。</strong> 当前 compile 还没过，就不要提前讨论 deploy。</li><li><strong>读真实命令。</strong> verbose compiler&#x2F;linker command 比高层错误摘要更接近事实。</li><li><strong>Host&#x2F;Target 必须分开。</strong> 交叉编译里同名库和工具可能属于完全不同架构。</li><li><strong>权限错误优先按权限查。</strong> <code>Permission denied</code> 不等于 include path 错。</li><li><strong>旧 build tree 也是环境状态。</strong> bind mount 会把历史 cache 一起带进新 Container。</li><li><strong>临时实验不是最终设计。</strong> 被排除的 <code>LIBRARY_PATH</code>、手工 chmod 不应该残留。</li><li><strong>修复最后要自动化。</strong> 把结论固化进 Dockerfile、entrypoint 和验证脚本。</li></ol><p>迁移完成后，正确环境应能自动重建，错误环境应在验证阶段失败。</p><h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ul><li>Docker bind mounts: <a href="https://docs.docker.com/engine/storage/bind-mounts/">https://docs.docker.com/engine/storage/bind-mounts/</a></li><li>Docker Buildx: <a href="https://docs.docker.com/reference/cli/docker/buildx/build/">https://docs.docker.com/reference/cli/docker/buildx/build/</a></li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;meta name=&quot;referrer&quot; content=&quot;no-referrer&quot; /&gt;

&lt;h1 id=&quot;交叉编译开发容器故障排查：从-CMakeCache-到目标库链接&quot;&gt;&lt;a href=&quot;#交叉编译开发容器故障排查：从-CMakeCache-到目标库链接&quot; class=</summary>
      
    
    
    
    <category term="docker" scheme="https://wdfk-prog.space/categories/docker/"/>
    
    
    <category term="docker" scheme="https://wdfk-prog.space/tags/docker/"/>
    
  </entry>
  
  <entry>
    <title>04-Docker常用语法与命令：CLI、Dockerfile与Compose速查</title>
    <link href="https://wdfk-prog.space/posts/68b5751a/"/>
    <id>https://wdfk-prog.space/posts/68b5751a/</id>
    <published>2026-09-03T07:50:11.000Z</published>
    <updated>2026-09-03T07:50:26.380Z</updated>
    
    <content type="html"><![CDATA[<meta name="referrer" content="no-referrer" /><h1 id="交叉编译开发容器故障排查：从-CMakeCache-到目标库链接"><a href="#交叉编译开发容器故障排查：从-CMakeCache-到目标库链接" class="headerlink" title="交叉编译开发容器故障排查：从 CMakeCache 到目标库链接"></a>交叉编译开发容器故障排查：从 CMakeCache 到目标库链接</h1><blockquote><p>摘要：按真实首次失败点复盘旧 CMakeCache、host&#x2F;target Thrift、SDK 权限、编译参数冲突和目标库链接，说明怎样沿证据链逐层收敛问题。</p></blockquote><p>@[toc]</p><p><img src="https://i-blog.csdnimg.cn/direct/bcaea9ba1a6e423ea477096981003349.png" alt="在这里插入图片描述"></p><p>Docker 能启动、交叉编译器能执行，只能证明环境最外层入口正常。历史 C&#x2F;C++ 工程仍可能依次暴露旧 CMake 缓存、宿主工具、目标库、权限和编译参数策略。</p><p>这次排查最重要的原则是：<strong>围绕当前第一次失败收集证据，证明后再继续向后。</strong></p><p>最终故障链可以概括为：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    Cache[&quot;旧 CMakeCache\n编译器路径属于旧 Host&quot;] --&gt;|干净 build tree| Compiler[&quot;交叉 GCC 正确识别&quot;]</span><br><span class="line">    Compiler --&gt; Thrift[&quot;host Thrift 查找失败&quot;]</span><br><span class="line">    Thrift --&gt;|确认 host generator| Header[&quot;target header Permission denied&quot;]</span><br><span class="line">    Header --&gt;|修 SDK 可读/可遍历| Flags[&quot;SDK flags 与项目 flags 冲突&quot;]</span><br><span class="line">    Flags --&gt;|分离工具链与项目编译策略| Link[&quot;ld: cannot find -lthrift&quot;]</span><br><span class="line">    Link --&gt;|确认 target library 与权限| Success[&quot;完整工程链接成功&quot;]</span><br></pre></td></tr></table></figure><h2 id="1-先把排障范围分成三层"><a href="#1-先把排障范围分成三层" class="headerlink" title="1. 先把排障范围分成三层"></a>1. 先把排障范围分成三层</h2><p>遇到构建失败时，先不要把所有错误都归类成“Docker 问题”。</p><p>可以先划三层：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line">Docker 层</span><br><span class="line">├── Image 是否存在</span><br><span class="line">├── Container 是否能启动</span><br><span class="line">├── bind mount 是否正确</span><br><span class="line">└── UID/GID 是否合理</span><br><span class="line"></span><br><span class="line">工具链/SDK 层</span><br><span class="line">├── environment-setup 是否加载</span><br><span class="line">├── CC/CXX 是否正确</span><br><span class="line">├── sysroot 是否正确</span><br><span class="line">├── target header/library 是否存在、可读</span><br><span class="line">└── host 工具是否齐全</span><br><span class="line"></span><br><span class="line">工程层</span><br><span class="line">├── CMakeCache</span><br><span class="line">├── CMakeLists/toolchain 文件</span><br><span class="line">├── 项目自己的 flags</span><br><span class="line">├── 硬编码路径</span><br><span class="line">└── 真实源码/链接依赖</span><br></pre></td></tr></table></figure><p>如果 Container 和交叉 GCC 已正常，而 CMake 仍报旧绝对路径，优先检查工程状态，不要先重建 Image。</p><h2 id="2-第一层故障：CMake-为什么还在找旧机器的-usr-local-bin-cc"><a href="#2-第一层故障：CMake-为什么还在找旧机器的-usr-local-bin-cc" class="headerlink" title="2. 第一层故障：CMake 为什么还在找旧机器的 /usr/local/bin/cc"></a>2. 第一层故障：CMake 为什么还在找旧机器的 <code>/usr/local/bin/cc</code></h2><p>容器里可以确认：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">command</span> -v aarch64-poky-linux-gcc</span><br><span class="line">aarch64-poky-linux-gcc --version</span><br></pre></td></tr></table></figure><p>输出正确，但项目 configure 却报：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">The CMAKE_C_COMPILER:</span><br><span class="line">  /usr/local/bin/cc</span><br><span class="line">is not a full path to an existing compiler tool.</span><br></pre></td></tr></table></figure><p>第一反应很容易是：“SDK 没加载。”</p><p>但错误本身已经给出一个更值得追的线索：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">/usr/local/bin/cc</span><br></pre></td></tr></table></figure><p>这明显像旧 Host 上的历史路径。</p><p>于是先读 CMake cache：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">grep -E <span class="string">&#x27;CMAKE_(C|CXX)_COMPILER|CMAKE_TOOLCHAIN_FILE&#x27;</span> \</span><br><span class="line">    build/CMakeCache.txt | <span class="built_in">head</span> -30</span><br></pre></td></tr></table></figure><p>如果看到：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">CMAKE_C_COMPILER:FILEPATH=/usr/local/bin/cc</span><br><span class="line">CMAKE_CXX_COMPILER:FILEPATH=/usr/local/bin/c++</span><br></pre></td></tr></table></figure><p>链路就很清楚了：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">Host 原工程已有 build/</span><br><span class="line">        ↓</span><br><span class="line">源码通过 bind mount 进入 Container</span><br><span class="line">        ↓</span><br><span class="line">build/ 和 CMakeCache.txt 也一起进来了</span><br><span class="line">        ↓</span><br><span class="line">CMake 继续使用旧机器缓存的 compiler path</span><br><span class="line">        ↓</span><br><span class="line">Container 没有同一路径</span><br></pre></td></tr></table></figure><h3 id="为什么不要第一时间删除-Host-的-build"><a href="#为什么不要第一时间删除-Host-的-build" class="headerlink" title="为什么不要第一时间删除 Host 的 build/"></a>为什么不要第一时间删除 Host 的 <code>build/</code></h3><p>bind mount 下 <code>/workspace/build</code> 就是 Host 的真实 build 目录。为了验证假设，没必要先破坏旧构建结果，可以直接创建全新的临时 build tree：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">rm</span> -rf /tmp/demo-build</span><br><span class="line"></span><br><span class="line">cmake \</span><br><span class="line">    -S /workspace \</span><br><span class="line">    -B /tmp/demo-build \</span><br><span class="line">    -DPROJECT=demo</span><br></pre></td></tr></table></figure><p>如果新目录能够识别：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">The C compiler identification is GNU 12.2.0</span><br><span class="line">The CXX compiler identification is GNU 12.2.0</span><br></pre></td></tr></table></figure><p>并选择 Yocto AArch64 compiler，那么第一层已经闭环：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">不是 Docker 没有 compiler</span><br><span class="line">不是 SDK 本身损坏</span><br><span class="line">而是旧 CMakeCache 把旧 Host 环境带进了 Container</span><br></pre></td></tr></table></figure><p>结论是：<strong>源码相同，不代表 build tree 可以跨环境直接复用。</strong></p><h2 id="3-第二层故障：Thrift-compiler-not-found-到底缺谁"><a href="#3-第二层故障：Thrift-compiler-not-found-到底缺谁" class="headerlink" title="3. 第二层故障：Thrift compiler not found 到底缺谁"></a>3. 第二层故障：<code>Thrift compiler not found</code> 到底缺谁</h2><p>CMakeCache 问题解决后，configure 继续推进，新的第一次失败变成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Thrift compiler not found.</span><br></pre></td></tr></table></figure><p>看到 Thrift 时，必须先区分 host 和 target。</p><p>典型交叉编译流程：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">*.thrift</span><br><span class="line">   │</span><br><span class="line">   │ x86-64 Host 上执行 thrift</span><br><span class="line">   ▼</span><br><span class="line">生成 C++ .cpp/.h</span><br><span class="line">   │</span><br><span class="line">   │ aarch64-poky-linux-g++</span><br><span class="line">   ▼</span><br><span class="line">ARM64 object</span><br><span class="line">   │</span><br><span class="line">   │ target libthrift</span><br><span class="line">   ▼</span><br><span class="line">ARM64 可执行程序</span><br></pre></td></tr></table></figure><p>这里至少有三样不同的东西：</p><table><thead><tr><th>对象</th><th>架构&#x2F;位置</th><th>作用</th></tr></thead><tbody><tr><td><code>thrift</code> 可执行程序</td><td>x86-64 Container Host side</td><td>根据 <code>.thrift</code> 生成代码</td></tr><tr><td>Thrift headers</td><td>ARM64 target sysroot</td><td>交叉编译生成代码</td></tr><tr><td><code>libthrift.a/.so</code></td><td>ARM64 target sysroot</td><td>最终链接目标程序</td></tr></tbody></table><p>所以配置阶段说 <code>Thrift compiler not found</code>，首先检查的是 host 工具：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">command</span> -v thrift</span><br><span class="line">thrift -version</span><br></pre></td></tr></table></figure><p>如果没有，再临时安装 host 侧：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">apt-get update</span><br><span class="line">apt-get install -y --no-install-recommends thrift-compiler</span><br></pre></td></tr></table></figure><p>不能用 Ubuntu x86-64 <code>libthrift-dev</code> 替代 target ARM64 library。</p><h2 id="4-usr-bin-thrift-已经存在，为什么-CMake-仍然报找不到"><a href="#4-usr-bin-thrift-已经存在，为什么-CMake-仍然报找不到" class="headerlink" title="4. /usr/bin/thrift 已经存在，为什么 CMake 仍然报找不到"></a>4. <code>/usr/bin/thrift</code> 已经存在，为什么 CMake 仍然报找不到</h2><p>更隐蔽的一种情况是：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">command</span> -v thrift</span><br><span class="line"><span class="comment"># /usr/bin/thrift</span></span><br></pre></td></tr></table></figure><p>工具明明存在，但 CMake 仍然失败。</p><p>此时要继续检查项目自己的查找逻辑。如果历史 CMake 写成类似：</p><figure class="highlight cmake"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">find_program</span>(THRIFT_COMPILER /usr/local/bin/thrift)</span><br></pre></td></tr></table></figure><p>那问题就不再是“没有 Thrift”，而是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Container 实际安装：/usr/bin/thrift</span><br><span class="line">项目历史契约：      /usr/local/bin/thrift</span><br></pre></td></tr></table></figure><p>为了先证明根因，不需要马上重构生产 CMake。可以显式传入：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">cmake \</span><br><span class="line">    -DTHRIFT_COMPILER=/usr/bin/thrift \</span><br><span class="line">    -S /workspace \</span><br><span class="line">    -B /tmp/demo-build</span><br></pre></td></tr></table></figure><p>如果能够越过这个检查，路径契约不一致就被证实。</p><p>长期更通用的工程写法可以是：</p><figure class="highlight cmake"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">find_program</span>(THRIFT_COMPILER NAMES thrift)</span><br></pre></td></tr></table></figure><p>或保留可由外部 <code>-DTHRIFT_COMPILER=...</code> 覆盖的变量。</p><p><code>not found</code> 既可能是程序没装，也可能只是搜索路径不匹配。</p><h2 id="5-第三层故障：头文件明明存在，却-Permission-denied"><a href="#5-第三层故障：头文件明明存在，却-Permission-denied" class="headerlink" title="5. 第三层故障：头文件明明存在，却 Permission denied"></a>5. 第三层故障：头文件明明存在，却 <code>Permission denied</code></h2><p>Thrift generator 通过后，编译开始，新的错误变成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">.../usr/include/thrift/TDispatchProcessor.h: Permission denied</span><br></pre></td></tr></table></figure><p>它和 <code>No such file or directory</code> 是两类问题。</p><p>先看目录和文件：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">ls</span> -ld <span class="string">&quot;<span class="variable">$TARGET_SYSROOT</span>/usr/include/thrift&quot;</span></span><br><span class="line"><span class="built_in">ls</span> -l <span class="string">&quot;<span class="variable">$TARGET_SYSROOT</span>/usr/include/thrift/TDispatchProcessor.h&quot;</span></span><br></pre></td></tr></table></figure><p>当时的典型问题是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Container:</span><br><span class="line">root:root 770</span><br></pre></td></tr></table></figure><p>而编译进程使用普通开发用户。</p><p><code>770</code> 表示：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">owner: rwx</span><br><span class="line">group: rwx</span><br><span class="line">other: ---</span><br></pre></td></tr></table></figure><p>如果普通开发用户既不是 owner，也不在该 group，它甚至无法 traverse 目录。</p><p>临时验证可以只改当前范围：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">find <span class="string">&quot;<span class="variable">$THRIFT_DIR</span>&quot;</span> -<span class="built_in">type</span> d -<span class="built_in">exec</span> <span class="built_in">chmod</span> a+rx &#123;&#125; \;</span><br><span class="line">find <span class="string">&quot;<span class="variable">$THRIFT_DIR</span>&quot;</span> -<span class="built_in">type</span> f -<span class="built_in">exec</span> <span class="built_in">chmod</span> a+r &#123;&#125; \;</span><br></pre></td></tr></table></figure><p>然后直接用：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">head</span> -n 2 <span class="string">&quot;<span class="variable">$THRIFT_HEADER</span>&quot;</span></span><br></pre></td></tr></table></figure><p>确认当前用户是否真的可读。</p><p>权限修正后编译继续推进，说明判断成立。</p><h2 id="6-为什么-Host-上能读，COPY-进-Image-后却不能读"><a href="#6-为什么-Host-上能读，COPY-进-Image-后却不能读" class="headerlink" title="6. 为什么 Host 上能读，COPY 进 Image 后却不能读"></a>6. 为什么 Host 上能读，COPY 进 Image 后却不能读</h2><p>这是理解整个 SDK 权限问题的关键。</p><p>假设 Host SDK 是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">owner=devuser</span><br><span class="line">group=devuser</span><br><span class="line">mode=770</span><br></pre></td></tr></table></figure><p>Host 的开发用户正好是 owner，所以一直正常。</p><p>导入 Image 后如果变成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">owner=root</span><br><span class="line">group=root</span><br><span class="line">mode=770</span><br></pre></td></tr></table></figure><p>mode 没变，但访问主体变了：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Host devuser = owner -&gt; rwx</span><br><span class="line">Container devuser = other -&gt; ---</span><br></pre></td></tr></table></figure><p>于是以前“看起来没有问题”的 SDK 权限依赖，在容器化后暴露出来。</p><p>这说明旧 SDK 权限策略依赖特定 owner&#x2F;group。最终修复不应停留在“哪个文件报错就 chmod 哪个文件”，而应在 Image 构建阶段建立统一只读 SDK 权限策略：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">chmod</span> -R a+rX <span class="string">&quot;<span class="variable">$SDK_ROOT</span>&quot;</span></span><br></pre></td></tr></table></figure><p>然后用独立权限审计脚本检查全 SDK。</p><h2 id="7-第四层故障：-O2、-O0、FORTIFY-和-format-security-为什么会撞在一起"><a href="#7-第四层故障：-O2、-O0、FORTIFY-和-format-security-为什么会撞在一起" class="headerlink" title="7. 第四层故障：-O2、-O0、FORTIFY 和 format-security 为什么会撞在一起"></a>7. 第四层故障：<code>-O2</code>、<code>-O0</code>、FORTIFY 和 format-security 为什么会撞在一起</h2><p>权限问题解决后，真正的 compiler command 开始执行，却出现：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">-Wformat-security ignored without -Wformat [-Werror=format-security]</span><br><span class="line">_FORTIFY_SOURCE requires compiling with optimization (-O)</span><br></pre></td></tr></table></figure><p>这种错误只看最后一行很难判断，应该直接打印完整命令：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">cmake --build /tmp/demo-build --verbose -j1 \</span><br><span class="line">    2&gt;&amp;1 | <span class="built_in">tee</span> /tmp/demo-build.log</span><br></pre></td></tr></table></figure><p>这里几个参数都很有用：</p><ul><li><code>--verbose</code>：看到 CMake 最终调用的 compiler&#x2F;linker command；</li><li><code>-j1</code>：先单线程，避免并行日志把第一条失败命令淹没；</li><li><code>2&gt;&amp;1</code>：合并 stderr&#x2F;stdout；</li><li><code>tee</code>：终端显示的同时保存日志。</li></ul><p>完整命令显示 SDK 环境先注入：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">-O2</span><br><span class="line">-D_FORTIFY_SOURCE=2</span><br><span class="line">-Wformat</span><br><span class="line">-Wformat-security</span><br><span class="line">-Werror=format-security</span><br></pre></td></tr></table></figure><p>而项目自己的 CMake 后面又加入：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">-O0</span><br><span class="line">-Wno-format</span><br></pre></td></tr></table></figure><p>这时错误就能解释了。</p><p>例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">SDK：希望开启 format-security</span><br><span class="line">项目：显式关闭 format warning</span><br><span class="line"></span><br><span class="line">SDK：FORTIFY_SOURCE + -O2</span><br><span class="line">项目：最终选择 -O0</span><br></pre></td></tr></table></figure><p>问题不是“GCC 突然坏了”，而是两套编译策略叠在同一条 command line 上。</p><h2 id="8-为什么不推荐直接加-Wno-error-format-security"><a href="#8-为什么不推荐直接加-Wno-error-format-security" class="headerlink" title="8. 为什么不推荐直接加 -Wno-error=format-security"></a>8. 为什么不推荐直接加 <code>-Wno-error=format-security</code></h2><p>追加：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">-Wno-error=format-security</span><br></pre></td></tr></table></figure><p>也许可以让某条 warning 暂时不作为 error，但它没有回答几个核心问题：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">最终优化等级应该由谁控制？</span><br><span class="line">FORTIFY 在 -O0 下的预期是什么？</span><br><span class="line">项目为什么主动 -Wno-format？</span><br><span class="line">SDK 的 security flags 是工具链必要条件，还是默认 policy？</span><br></pre></td></tr></table></figure><p>真正需要拆分的是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">工具链能力</span><br><span class="line">    与</span><br><span class="line">项目编译策略</span><br></pre></td></tr></table></figure><p>Yocto <code>environment-setup</code> 往往不只提供 flags，它还提供：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">PATH</span><br><span class="line">SDKTARGETSYSROOT</span><br><span class="line">OECORE_*</span><br><span class="line">PKG_CONFIG_*</span><br><span class="line">交叉工具位置</span><br></pre></td></tr></table></figure><p>所以也不能简单“不要 source SDK”。</p><p>这次已经验证可工作的兼容策略是：保留 SDK、sysroot 和工具路径，但让业务工程继续拥有自己的 <code>-O0</code>、warning policy。</p><p>例如入口脚本中把：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">CC=<span class="string">&quot;aarch64-poky-linux-gcc --sysroot=<span class="variable">$SDKTARGETSYSROOT</span>&quot;</span></span><br><span class="line">CXX=<span class="string">&quot;aarch64-poky-linux-g++ --sysroot=<span class="variable">$SDKTARGETSYSROOT</span>&quot;</span></span><br><span class="line"><span class="built_in">export</span> CC CXX</span><br><span class="line"><span class="built_in">unset</span> CFLAGS CXXFLAGS CPPFLAGS LDFLAGS</span><br></pre></td></tr></table></figure><p>作为这个特定工程的兼容契约。</p><p>这里必须强调：这不是所有 Yocto 工程都应该照抄的通用规则。某些项目确实依赖 SDK 导出的 flags。它只适用于已经通过真实编译证据确认“项目自己拥有编译策略”的场景。</p><h2 id="9-第五层故障：编译到链接阶段才报-cannot-find-lthrift"><a href="#9-第五层故障：编译到链接阶段才报-cannot-find-lthrift" class="headerlink" title="9. 第五层故障：编译到链接阶段才报 cannot find -lthrift"></a>9. 第五层故障：编译到链接阶段才报 <code>cannot find -lthrift</code></h2><p>当编译已经推进很远，最后出现：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ld: cannot find -lthrift</span><br></pre></td></tr></table></figure><p>此时首先应该承认一个积极事实：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">代码生成通过</span><br><span class="line">header 编译通过</span><br><span class="line">大量 C/C++ object 已生成</span><br><span class="line">当前第一次失败已经进入 link stage</span><br></pre></td></tr></table></figure><p>然后再问：<code>-lthrift</code> 到底要找什么？</p><p>GCC&#x2F;ld 的：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">-lthrift</span><br></pre></td></tr></table></figure><p>通常表示在 library search path 中查找类似：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">libthrift.so</span><br><span class="line">libthrift.a</span><br></pre></td></tr></table></figure><p>但这是交叉编译，所以需要的是 <strong>ARM64 target library</strong>，不是 Host Ubuntu 的 x86-64 <code>libthrift-dev</code>。</p><p>先搜 target sysroot：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">find <span class="string">&quot;<span class="variable">$TARGET_SYSROOT</span>/usr/lib&quot;</span> <span class="string">&quot;<span class="variable">$TARGET_SYSROOT</span>/lib&quot;</span> \</span><br><span class="line">    \( -name <span class="string">&#x27;libthrift.so*&#x27;</span> -o -name <span class="string">&#x27;libthrift.a&#x27;</span> \) \</span><br><span class="line">    -<span class="built_in">ls</span> 2&gt;/dev/null</span><br></pre></td></tr></table></figure><p>如果已经找到：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$TARGET_SYSROOT/lib/libthrift.a</span><br></pre></td></tr></table></figure><p>就说明“库不存在”这个假设并不成立。</p><p>下一步检查当前用户是否能读：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">test</span> -r <span class="string">&quot;<span class="variable">$TARGET_SYSROOT</span>/lib/libthrift.a&quot;</span> \</span><br><span class="line">    &amp;&amp; <span class="built_in">echo</span> <span class="string">&quot;THRIFT READABLE&quot;</span> \</span><br><span class="line">    || <span class="built_in">echo</span> <span class="string">&quot;THRIFT NOT READABLE&quot;</span></span><br></pre></td></tr></table></figure><p>这比只 <code>ls</code> 更贴近 linker 的实际需求。</p><h2 id="10-print-sysroot-显示奇怪路径，为什么不能立刻判定根因"><a href="#10-print-sysroot-显示奇怪路径，为什么不能立刻判定根因" class="headerlink" title="10. --print-sysroot 显示奇怪路径，为什么不能立刻判定根因"></a>10. <code>--print-sysroot</code> 显示奇怪路径，为什么不能立刻判定根因</h2><p><code>aarch64-poky-linux-g++ --print-sysroot</code> 只反映 compiler driver 的默认状态。CMake 项目可能在真实 link command 中显式传入 <code>--sysroot</code>，所以应继续查看：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">cat</span> CMakeFiles/&lt;target&gt;.<span class="built_in">dir</span>/link.txt</span><br></pre></td></tr></table></figure><p>如果实际命令已有正确 <code>--sysroot=/opt/vendor-sdk/...</code>，就应以当前失败现场真正执行的命令为准，而不是孤立解释 <code>--print-sysroot</code>。</p><h2 id="11-为什么-target-library-存在、sysroot-也正确，linker-仍可能说找不到"><a href="#11-为什么-target-library-存在、sysroot-也正确，linker-仍可能说找不到" class="headerlink" title="11. 为什么 target library 存在、sysroot 也正确，linker 仍可能说找不到"></a>11. 为什么 target library 存在、sysroot 也正确，linker 仍可能说找不到</h2><p>前面已经证明：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">真实 link command 有正确 --sysroot</span><br><span class="line">目标 libthrift.a 在 sysroot 标准 lib 目录</span><br></pre></td></tr></table></figure><p>但如果这个文件仍然是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">root:root 770</span><br></pre></td></tr></table></figure><p>普通开发用户无法读取，linker 可能最终表现成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">cannot find -lthrift</span><br></pre></td></tr></table></figure><p>修正目录 traverse 和文件 read 权限以后：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">test</span> -r <span class="string">&quot;<span class="variable">$TARGET_SYSROOT</span>/lib/libthrift.a&quot;</span></span><br></pre></td></tr></table></figure><p>成功，再重新 link，构建完成。</p><p>这说明错误文本里的“找不到”，真实原因也可能是“路径存在但当前用户无权读取”。</p><h2 id="13-为什么一次只改变一个变量"><a href="#13-为什么一次只改变一个变量" class="headerlink" title="13. 为什么一次只改变一个变量"></a>13. 为什么一次只改变一个变量</h2><p>这次迁移里，如果同时：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">改 Dockerfile</span><br><span class="line">改 CMakeLists</span><br><span class="line">改 toolchain.cmake</span><br><span class="line">换 Thrift 版本</span><br><span class="line">加 LIBRARY_PATH</span><br><span class="line">改 SDK 权限</span><br></pre></td></tr></table></figure><p>即使最后“能编译”，也很难知道到底哪个改动真正必要。</p><p>更有证据力的顺序是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">1. 观察当前第一次失败</span><br><span class="line">2. 提一个具体假设</span><br><span class="line">3. 做最小实验</span><br><span class="line">4. 观察结果</span><br><span class="line">5. 确认/排除假设</span><br><span class="line">6. 再进入下一个 blocker</span><br></pre></td></tr></table></figure><p>例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">旧 compiler path</span><br><span class="line">→ 只换干净 build tree</span><br><span class="line">→ compiler 正确</span><br><span class="line">→ 第一层闭环</span><br><span class="line"></span><br><span class="line">Thrift not found</span><br><span class="line">→ 只指定 /usr/bin/thrift</span><br><span class="line">→ configure 继续</span><br><span class="line">→ 第二层闭环</span><br><span class="line"></span><br><span class="line">Permission denied</span><br><span class="line">→ 只修当前 SDK 读取权限</span><br><span class="line">→ compile 继续</span><br><span class="line">→ 第三层闭环</span><br></pre></td></tr></table></figure><p>这种方法的价值是每一步都能留下可复核的因果关系。</p><h2 id="14-怎样把临时修复固化进开发-Image"><a href="#14-怎样把临时修复固化进开发-Image" class="headerlink" title="14. 怎样把临时修复固化进开发 Image"></a>14. 怎样把临时修复固化进开发 Image</h2><p>真正完成容器化以后，不应该每次手工：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">apt install thrift-compiler</span><br><span class="line">-DTHRIFT_COMPILER=/usr/bin/thrift</span><br><span class="line">chmod 某个 header</span><br><span class="line">chmod 某个 libthrift.a</span><br><span class="line">unset 一串 flags</span><br></pre></td></tr></table></figure><p>这些知识应该分别进入稳定位置：</p><table><thead><tr><th>已确认问题</th><th>更合理的固化位置</th></tr></thead><tbody><tr><td>host Thrift 缺失</td><td>Dockerfile 安装 host package</td></tr><tr><td>SDK owner&#x2F;mode 导致普通用户不可读</td><td>Dockerfile 导入 SDK 时统一 <code>a+rX</code></td></tr><tr><td>UID&#x2F;GID 与 Host 不一致</td><td><code>dev.sh</code> + <code>entrypoint.sh</code> 运行时映射</td></tr><tr><td>SDK 与项目 flags 冲突</td><td>entrypoint&#x2F;toolchain 的明确兼容策略</td></tr><tr><td>只检查 compiler 太弱</td><td><code>verify-env.sh</code> 做 compile&#x2F;link smoke test</td></tr><tr><td>旧 CMakeCache 迁移风险</td><td>首次跨环境构建使用干净 build tree</td></tr></tbody></table><p>这样下一次 Image rebuild 或换 Host 时，旧问题要么被自动修正，要么在 smoke test 阶段直接失败。</p><h2 id="16-这次排障最终真正留下什么"><a href="#16-这次排障最终真正留下什么" class="headerlink" title="16. 这次排障最终真正留下什么"></a>16. 这次排障最终真正留下什么</h2><p>最值得保留的不是“Thrift 怎么修”，而是一套可迁移的方法：</p><ol><li><strong>先分层。</strong> Docker、SDK、CMake 和源码不是同一个问题域。</li><li><strong>围绕第一次失败。</strong> 当前 compile 还没过，就不要提前讨论 deploy。</li><li><strong>读真实命令。</strong> verbose compiler&#x2F;linker command 比高层错误摘要更接近事实。</li><li><strong>Host&#x2F;Target 必须分开。</strong> 交叉编译里同名库和工具可能属于完全不同架构。</li><li><strong>权限错误优先按权限查。</strong> <code>Permission denied</code> 不等于 include path 错。</li><li><strong>旧 build tree 也是环境状态。</strong> bind mount 会把历史 cache 一起带进新 Container。</li><li><strong>临时实验不是最终设计。</strong> 被排除的 <code>LIBRARY_PATH</code>、手工 chmod 不应该残留。</li><li><strong>修复最后要自动化。</strong> 把结论固化进 Dockerfile、entrypoint 和验证脚本。</li></ol><p>迁移完成后，正确环境应能自动重建，错误环境应在验证阶段失败。</p><h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ul><li>Docker bind mounts: <a href="https://docs.docker.com/engine/storage/bind-mounts/">https://docs.docker.com/engine/storage/bind-mounts/</a></li><li>Docker Buildx: <a href="https://docs.docker.com/reference/cli/docker/buildx/build/">https://docs.docker.com/reference/cli/docker/buildx/build/</a></li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;meta name=&quot;referrer&quot; content=&quot;no-referrer&quot; /&gt;

&lt;h1 id=&quot;交叉编译开发容器故障排查：从-CMakeCache-到目标库链接&quot;&gt;&lt;a href=&quot;#交叉编译开发容器故障排查：从-CMakeCache-到目标库链接&quot; class=</summary>
      
    
    
    
    <category term="docker" scheme="https://wdfk-prog.space/categories/docker/"/>
    
    
    <category term="docker" scheme="https://wdfk-prog.space/tags/docker/"/>
    
  </entry>
  
  <entry>
    <title>docker学习笔记系列</title>
    <link href="https://wdfk-prog.space/posts/b8fb0325/"/>
    <id>https://wdfk-prog.space/posts/b8fb0325/</id>
    <published>2026-09-03T07:50:11.000Z</published>
    <updated>2026-09-03T07:50:26.384Z</updated>
    
    <content type="html"><![CDATA[<h1 id="docker学习笔记系列"><a href="#docker学习笔记系列" class="headerlink" title="docker学习笔记系列"></a>docker学习笔记系列</h1><ul><li><a href="./01-Docker%E5%BC%80%E5%8F%91%E7%8E%AF%E5%A2%83%E5%AE%B9%E5%99%A8%E5%8C%96%EF%BC%9A%E4%BB%8E%E5%9F%BA%E6%9C%AC%E6%A6%82%E5%BF%B5%E5%88%B0Ubuntu20.04%E5%AE%89%E8%A3%85%E6%8E%92%E6%9F%A5.md">1. Docker 开发环境容器化：从基本概念到 Ubuntu 20.04 安装排查</a> (<a href="https://wdfk-prog.blog.csdn.net/article/details/164333630">CSDN链接</a>)</li><li><a href="./02-%E5%A4%A7%E5%9E%8BSDK%E5%BC%80%E5%8F%91%E9%95%9C%E5%83%8F%E5%B7%A5%E7%A8%8B%E5%8C%96%EF%BC%9ABuildKit%E3%80%81Buildx%E4%B8%8E%E9%A1%B9%E7%9B%AE%E8%84%9A%E6%9C%AC.md">2. 大型 SDK 开发镜像工程化：BuildKit、Buildx 与项目脚本</a> (<a href="https://wdfk-prog.blog.csdn.net/article/details/164333682">CSDN链接</a>)</li><li><a href="./03-%E4%BA%A4%E5%8F%89%E7%BC%96%E8%AF%91%E5%BC%80%E5%8F%91%E5%AE%B9%E5%99%A8%E6%95%85%E9%9A%9C%E6%8E%92%E6%9F%A5%EF%BC%9A%E4%BB%8ECMakeCache%E5%88%B0%E7%9B%AE%E6%A0%87%E5%BA%93%E9%93%BE%E6%8E%A5.md">3. 交叉编译开发容器故障排查：从 CMakeCache 到目标库链接</a></li><li><a href="./04-Docker%E5%B8%B8%E7%94%A8%E8%AF%AD%E6%B3%95%E4%B8%8E%E5%91%BD%E4%BB%A4%EF%BC%9ACLI%E3%80%81Dockerfile%E4%B8%8ECompose%E9%80%9F%E6%9F%A5.md">4. 交叉编译开发容器故障排查：从 CMakeCache 到目标库链接</a></li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;docker学习笔记系列&quot;&gt;&lt;a href=&quot;#docker学习笔记系列&quot; class=&quot;headerlink&quot; title=&quot;docker学习笔记系列&quot;&gt;&lt;/a&gt;docker学习笔记系列&lt;/h1&gt;&lt;ul&gt;
&lt;li&gt;&lt;a href=&quot;./01-Docker%E5%</summary>
      
    
    
    
    <category term="docker" scheme="https://wdfk-prog.space/categories/docker/"/>
    
    
    <category term="docker" scheme="https://wdfk-prog.space/tags/docker/"/>
    
  </entry>
  
  <entry>
    <title>canopennode-rtt推荐，不只可以做从站，也可以承担主站角色</title>
    <link href="https://wdfk-prog.space/posts/dd9fd79f/"/>
    <id>https://wdfk-prog.space/posts/dd9fd79f/</id>
    <published>2026-09-01T13:54:50.000Z</published>
    <updated>2026-09-03T07:46:23.190Z</updated>
    
    <content type="html"><![CDATA[<meta name="referrer" content="no-referrer" /><h1 id="canopennode-rtt推荐，不只可以做从站，也可以承担主站角色"><a href="#canopennode-rtt推荐，不只可以做从站，也可以承担主站角色" class="headerlink" title="canopennode-rtt推荐，不只可以做从站，也可以承担主站角色"></a>canopennode-rtt推荐，不只可以做从站，也可以承担主站角色</h1><blockquote><p>摘要：canopennode-rtt 将 CANopenNode 接入 RT-Thread，可同时承载设备与控制器角色；本文还对比 RTT-CanFestival 的架构、CiA 402、维护与选型差异。</p></blockquote><blockquote><p>仓库地址：<a href="https://github.com/wdfk-prog/canopennode-rtt">https://github.com/wdfk-prog/canopennode-rtt</a><br>在线文档：<a href="https://wdfk-prog.space/canopennode-rtt/">https://wdfk-prog.space/canopennode-rtt/</a><br>上游协议栈：<a href="https://github.com/CANopenNode/CANopenNode">https://github.com/CANopenNode/CANopenNode</a><br>本文基于 <code>canopennode-rtt</code> <code>master</code> 分支 2026-09-01 的公开状态整理，参考提交：<code>b5a9dc1a242d784358ebc8a5bf81d5afcdc8f4ba</code>。</p></blockquote><p><img src="https://i-blog.csdnimg.cn/direct/d00e9fcf49fc40f3836a84ae3aec6e2d.png" alt="在这里插入图片描述"></p><p>@[toc]</p><p>如果你正在 RT-Thread 上做伺服驱动、运动控制器、工业 I&#x2F;O、传感器节点、执行器、网关或者任何需要 CANopen 的 MCU 项目，那么我比较推荐关注一下这个仓库：<code>wdfk-prog/canopennode-rtt</code>。</p><p>它不是重新写了一套 CANopen 协议栈，而是把成熟的 CANopenNode V4 作为上游核心，通过 RT-Thread 的 CAN 设备框架、线程、信号量、互斥锁、Kconfig、SCons、DFS&#x2F;EEPROM 等基础设施，把协议栈真正包装成一个适合 RT-Thread 工程使用的软件包。</p><p>这里我想先强调一个很容易被名字或者传统项目结构限制住的点：</p><p><strong>这个软件包并不应该被理解成“CANopen 从站软件包”。它当然可以很方便地做从站，但它同样可以利用 CANopenNode 已有的 NMT Master、SDO Client、LSS Master、SYNC Producer、Heartbeat Consumer、PDO 等能力，构建 MCU 侧的 CANopen 主站、控制器或者 commissioning 工具。</strong></p><p>更准确地说，<code>canopennode-rtt</code> 是一个“CANopenNode 的 RT-Thread 集成与运行时层”。它本身保持角色中立，最终设备在 CANopen 网络里承担什么角色，由你打开哪些协议能力、怎样设计 Object Dictionary，以及应用层如何组织网络决定。</p><p>这也是我认为这个仓库比较有价值的地方：它没有为了“做一个从站 Demo”把能力封死，而是尽量保留 CANopenNode 原本的组合能力，让同一个 MCU 既可以作为一个正常 CANopen 节点运行，也可以同时去管理、配置和控制总线上的其他节点。</p><p>下面我从工程角度把这个软件包为什么值得用、它现在已经具备什么能力、为什么可以同时做从站和主站、RT-Thread 侧是怎样组织运行时的，以及后续 CiA 402（历史上也常称 DS402）规划到了什么边界，完整梳理一遍。</p><hr><h2 id="1-为什么不直接把-CANopenNode-源码扔进-RT-Thread-工程？"><a href="#1-为什么不直接把-CANopenNode-源码扔进-RT-Thread-工程？" class="headerlink" title="1. 为什么不直接把 CANopenNode 源码扔进 RT-Thread 工程？"></a>1. 为什么不直接把 CANopenNode 源码扔进 RT-Thread 工程？</h2><p>CANopenNode 本身已经是一套成熟的开源 CANopen 协议栈，它提供了 CiA 301 核心通信对象，并包含 LSS、Gateway、Safety 等扩展能力。问题是，上游协议栈故意把硬件相关部分抽象成 target driver，因此“协议代码能编译”与“在 RT-Thread 项目里好用”其实是两回事。</p><p>如果直接把 CANopenNode 源码复制进一个 BSP，通常很快就会遇到几类工程问题：</p><ol><li>CAN 控制器怎样与 <code>rt_device</code> 设备框架对接？</li><li>CAN RX 回调到底在中断里做多少工作，怎样把协议处理安全地移到线程上下文？</li><li><code>CO_process()</code>、SYNC、RPDO、TPDO 应该跑在同一个线程还是拆开？</li><li>realtime 周期从哪里来，微秒级 <code>timeDifference_us</code> 怎样得到？</li><li>CANopenNode 的各种 <code>CO_CONFIG_*</code> 宏怎样与 RT-Thread <code>menuconfig</code> 对接？</li><li>开 SDO Client、SRDO、Gateway 后，SCons 怎样只编译真正需要的源文件？</li><li>Object Dictionary 是用 Demo 还是产品自己的 XDD 生成文件？</li><li>0x1010&#x2F;0x1011 参数保存怎样接 DFS、EEPROM 或用户自己的非易失存储？</li><li>一个工程里如果既要做 SDO Server，又要做 SDO Client，锁、生命周期和线程上下文怎样约束？</li><li>Communication Reset 以后对象被重新创建，回调、线程、存储、过滤器、测试夹具怎样重新绑定？</li></ol><p>这些问题都不是 CANopen 规范本身的问题，而是“协议栈如何产品化接入 RTOS”的问题。</p><p><code>canopennode-rtt</code> 的主要工作正是在这里。仓库 README 对自己的定位写得很清楚：CANopen 协议核心来自 <code>CANopenNode</code> git 子模块，本仓库负责 RT-Thread target driver、package wrapper、Kconfig&#x2F;SCons、运行线程、storage backend、Demo OD 和相关验证支持。</p><p>换句话说，它把最容易在每个项目里重复造一遍的胶水层集中维护了。</p><h3 id="1-1-整体分层"><a href="#1-1-整体分层" class="headerlink" title="1.1 整体分层"></a>1.1 整体分层</h3><p>先看整个软件包的位置。重点观察：CANopenNode core 没有被 RT-Thread 私有逻辑侵入，RT-Thread 相关代码集中在 wrapper 与 target driver 层。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">flowchart TB</span><br><span class="line">    APP[&quot;用户应用 / 产品逻辑&quot;] --&gt; WRAP[&quot;canopennode-rtt 运行封装&quot;]</span><br><span class="line">    WRAP --&gt; CORE[&quot;CANopenNode V4 Core&quot;]</span><br><span class="line">    WRAP --&gt; OD[&quot;Object Dictionary&quot;]</span><br><span class="line">    WRAP --&gt; STORAGE[&quot;DFS / AT24CXX / User Storage&quot;]</span><br><span class="line">    CORE --&gt; DRIVER[&quot;RT-Thread CAN Target Driver&quot;]</span><br><span class="line">    DRIVER --&gt; RTDEV[&quot;RT-Thread CAN Device&quot;]</span><br><span class="line">    RTDEV --&gt; CANBUS[&quot;CAN Bus&quot;]</span><br><span class="line">    KCFG[&quot;Kconfig&quot;] --&gt; WRAP</span><br><span class="line">    KCFG --&gt; CORE</span><br><span class="line">    SCONS[&quot;SConscript&quot;] --&gt; WRAP</span><br><span class="line">    SCONS --&gt; CORE</span><br></pre></td></tr></table></figure><p>这张图其实就是仓库最核心的价值：你仍然使用上游 CANopenNode 的协议实现，而不是 fork 出一套越来越难升级的私有协议栈；RT-Thread 特有的线程、设备、存储和构建逻辑则由软件包统一处理。</p><h3 id="1-2-为什么使用-git-submodule"><a href="#1-2-为什么使用-git-submodule" class="headerlink" title="1.2 为什么使用 git submodule"></a>1.2 为什么使用 git submodule</h3><p>仓库把 <code>CANopenNode/</code> 作为 git 子模块固定在明确 revision 上。这个做法对于协议栈集成很重要，因为“CANopenNode V4”并不是一个永远静止的 API。上游的配置宏、Object Dictionary API、SDO Client、Gateway、driver contract 都会随着版本演进。</p><p>通过子模块固定 revision，可以让以下几件事变得可追踪：</p><ul><li>当前 RT-Thread port 到底对应哪一个 CANopenNode 版本；</li><li>升级上游时能明确看到协议核心版本变化；</li><li>CI 和目标板验证可以绑定到同一个上游 commit；</li><li>产品工程不会因为某次无意中的 <code>git pull</code> 直接换掉协议核心。</li></ul><p>因此首次拉取仓库时要使用：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git <span class="built_in">clone</span> --recursive https://github.com/wdfk-prog/canopennode-rtt.git</span><br></pre></td></tr></table></figure><p>已经克隆但缺少子模块时则执行：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git submodule update --init --recursive</span><br></pre></td></tr></table></figure><p>如果编译时出现 <code>CANopenNode/301</code> 缺失，优先检查子模块，而不是手工从别的版本复制一份源码进来。</p><hr><h2 id="2-先把“从站”和“主站”说清楚：CANopen-不是只有一个-Master-Slave-开关"><a href="#2-先把“从站”和“主站”说清楚：CANopen-不是只有一个-Master-Slave-开关" class="headerlink" title="2. 先把“从站”和“主站”说清楚：CANopen 不是只有一个 Master&#x2F;Slave 开关"></a>2. 先把“从站”和“主站”说清楚：CANopen 不是只有一个 Master&#x2F;Slave 开关</h2><p>很多人第一次接触 CANopen 时会把它类比 Modbus RTU：一个 Master 对多个 Slave，然后自然会问“这个协议栈到底是主站还是从站？”</p><p>CANopen 实际上更适合从“协议对象的角色组合”来理解。</p><p>一个节点可以同时具有：</p><ul><li>NMT Slave；</li><li>NMT Master；</li><li>SDO Server；</li><li>SDO Client；</li><li>Heartbeat Producer；</li><li>Heartbeat Consumer；</li><li>RPDO 接收；</li><li>TPDO 发送；</li><li>SYNC Consumer；</li><li>SYNC Producer；</li><li>TIME Consumer；</li><li>TIME Producer；</li><li>LSS Slave；</li><li>LSS Master。</li></ul><p>也就是说，“设备自己是一个 CANopen 节点”和“设备控制其他 CANopen 节点”并不冲突。</p><p>例如一个运动控制 MCU 可以有自己的 Node-ID 1，对外提供自己的 SDO Server、Heartbeat 和状态 PDO；与此同时，它还可以作为 NMT Master 向 Node 2、3、4 发送 NMT 命令，通过 SDO Client 给远端驱动写参数，通过 Heartbeat Consumer 监控远端节点，通过 SYNC Producer 驱动同步 PDO 周期。</p><p>这时你把它叫“主站”“控制器节点”或者“带本地 CANopen 节点能力的控制器”都可以，但从实现上看，它仍然是多个 CANopen 角色的组合。</p><h3 id="2-1-角色矩阵"><a href="#2-1-角色矩阵" class="headerlink" title="2.1 角色矩阵"></a>2.1 角色矩阵</h3><p>下面这张图可以直观看到：同一个 <code>CANopenNodeRTT</code> 实例并不是只能选左边或者右边。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    NODE[&quot;CANopenNodeRTT Node-ID 1&quot;]</span><br><span class="line"></span><br><span class="line">    subgraph DEVICE[&quot;本节点作为设备&quot;]</span><br><span class="line">        NMT_S[&quot;NMT Slave&quot;]</span><br><span class="line">        SDO_S[&quot;SDO Server&quot;]</span><br><span class="line">        HB_P[&quot;Heartbeat Producer&quot;]</span><br><span class="line">        RPDO[&quot;RPDO Consumer&quot;]</span><br><span class="line">        TPDO[&quot;TPDO Producer&quot;]</span><br><span class="line">        LSS_S[&quot;LSS Slave&quot;]</span><br><span class="line">    end</span><br><span class="line"></span><br><span class="line">    subgraph CONTROL[&quot;本节点作为控制器 / 主站能力&quot;]</span><br><span class="line">        NMT_M[&quot;NMT Master&quot;]</span><br><span class="line">        SDO_C[&quot;SDO Client&quot;]</span><br><span class="line">        HB_C[&quot;Heartbeat Consumer&quot;]</span><br><span class="line">        SYNC_P[&quot;SYNC Producer&quot;]</span><br><span class="line">        LSS_M[&quot;LSS Master&quot;]</span><br><span class="line">        GTW[&quot;CiA 309-3 Gateway&quot;]</span><br><span class="line">    end</span><br><span class="line"></span><br><span class="line">    NODE --&gt; DEVICE</span><br><span class="line">    NODE --&gt; CONTROL</span><br></pre></td></tr></table></figure><p>这也是为什么我特别不建议把 <code>canopennode-rtt</code> 宣传成“CANopen 从站包”。这样会把它已经暴露出来的很多能力直接忽略掉。</p><h3 id="2-2-上游-CANopenNode-本身就支持这些角色"><a href="#2-2-上游-CANopenNode-本身就支持这些角色" class="headerlink" title="2.2 上游 CANopenNode 本身就支持这些角色"></a>2.2 上游 CANopenNode 本身就支持这些角色</h3><p>CANopenNode 官方 README 明确列出了：</p><ul><li>NMT slave，同时提供 simple NMT master；</li><li>SDO server；</li><li>SDO client，可访问网络中其他 CANopen 设备的 Object Dictionary；</li><li>Heartbeat producer&#x2F;consumer；</li><li>SYNC producer&#x2F;consumer；</li><li>TIME producer&#x2F;consumer；</li><li>LSS master&#x2F;slave；</li><li>CiA 309-3 ASCII gateway，映射 NMT master、LSS master 和 SDO client。</li></ul><p><code>canopennode-rtt</code> 做的事情，是把这些能力通过 Kconfig、SCons 和 RT-Thread runtime 暴露出来，而不是在 port 层把它们删掉。</p><p>当前配置中已经可以看到很明确的主站侧选项：</p><table><thead><tr><th>能力</th><th>关键 Kconfig</th><th>当前含义</th></tr></thead><tbody><tr><td>NMT Master</td><td><code>PKG_CANOPENNODE_NMT_MASTER</code></td><td>允许本节点发送简单 NMT master 命令</td></tr><tr><td>SDO Client</td><td><code>PKG_CANOPENNODE_USING_SDO_CLIENT</code></td><td>访问远端节点 OD</td></tr><tr><td>Heartbeat Consumer</td><td><code>PKG_CANOPENNODE_USING_HB_CONS</code></td><td>监控远端节点在线与 NMT 状态</td></tr><tr><td>SYNC Producer</td><td><code>PKG_CANOPENNODE_SYNC_PRODUCER</code></td><td>本节点产生 SYNC</td></tr><tr><td>LSS Master</td><td><code>PKG_CANOPENNODE_USING_LSS_MASTER</code></td><td>commissioning &#x2F; gateway 场景</td></tr><tr><td>ASCII Gateway</td><td><code>PKG_CANOPENNODE_USING_GATEWAY_ASCII</code></td><td>编译 CiA 309-3 gateway helper</td></tr><tr><td>EMCY Consumer</td><td><code>PKG_CANOPENNODE_EM_CONSUMER</code></td><td>接收远端节点 EMCY</td></tr></tbody></table><p>因此从能力基础上说，“可以作为主站使用”是有明确代码和配置支撑的，不是一个宣传口号。</p><p>但这里也要把边界说严谨：</p><p><strong>这些能力可以组成主站或控制器应用，不等于仓库现在已经提供了一套面向所有场景的完整 CANopen Network Manager。</strong></p><p>例如远端节点发现策略、设备类型识别、上电编排、参数下发顺序、故障恢复、设备替换、CiA 402 控制状态机等，仍然属于应用层或 profile 层需要设计的内容。</p><p>这反而是合理的边界：基础通信能力在软件包里，产品策略留给产品。</p><hr><h2 id="3-RT-Thread-侧不是简单“起一个线程跑-CO-process”"><a href="#3-RT-Thread-侧不是简单“起一个线程跑-CO-process”" class="headerlink" title="3. RT-Thread 侧不是简单“起一个线程跑 CO_process”"></a>3. RT-Thread 侧不是简单“起一个线程跑 CO_process”</h2><p>这个软件包另一个值得推荐的点，是它没有把所有协议处理粗暴地塞进一个 while(1) 里。</p><p>当前运行模型把 CAN 接收、实时 CANopen 路径和 mainline 异步处理分开：</p><ul><li><code>co_rx</code>：CAN RX 辅助线程；</li><li><code>co_rt</code>：realtime CANopen 处理线程；</li><li><code>co_main</code>：mainline 处理线程；</li><li><code>co_tmr</code>：周期 timer，用于唤醒 realtime 路径。</li></ul><p>README 和集成文档给出的职责非常清楚：</p><ul><li>RX 线程从 RT-Thread CAN 设备读取帧并分发到 CANopenNode RX callback；</li><li>realtime 线程处理 SYNC、SRDO、RPDO、TPDO；</li><li>mainline 线程处理 NMT、Heartbeat、SDO、Emergency、Storage、LSS、Gateway 等异步逻辑。</li></ul><h3 id="3-1-运行时数据流"><a href="#3-1-运行时数据流" class="headerlink" title="3.1 运行时数据流"></a>3.1 运行时数据流</h3><p>观察下面这张图时，重点看 RX 与 realtime&#x2F;mainline 的解耦。CAN 驱动回调只负责唤醒，不把复杂协议逻辑堆在 ISR 里。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">flowchart TB</span><br><span class="line">    IRQ[&quot;CAN RX indication / ISR side&quot;] --&gt; WAKE[&quot;唤醒 co_rx&quot;]</span><br><span class="line">    WAKE --&gt; RX[&quot;co_rx 读取 CAN frame&quot;]</span><br><span class="line">    RX --&gt; DISPATCH[&quot;CANopenNode RX dispatch&quot;]</span><br><span class="line"></span><br><span class="line">    TIMER[&quot;co_tmr 周期 timer&quot;] --&gt; SEM[&quot;rtSem&quot;]</span><br><span class="line">    SEM --&gt; RT[&quot;co_rt&quot;]</span><br><span class="line">    RT --&gt; SYNC[&quot;CO_process_SYNC&quot;]</span><br><span class="line">    SYNC --&gt; RPDO2[&quot;CO_process_RPDO&quot;]</span><br><span class="line">    RPDO2 --&gt; TPDO2[&quot;CO_process_TPDO&quot;]</span><br><span class="line">    TPDO2 --&gt; SRDO[&quot;CO_process_SRDO&quot;]</span><br><span class="line"></span><br><span class="line">    EVT[&quot;周期 / callback-pre / timerNext&quot;] --&gt; MAIN[&quot;co_main&quot;]</span><br><span class="line">    MAIN --&gt; PROC[&quot;CO_process + mainline objects&quot;]</span><br></pre></td></tr></table></figure><p>这种拆法对 MCU 很实用，因为 PDO&#x2F;SYNC 路径与 SDO、Storage 之类的异步工作对时延的要求明显不同。</p><h3 id="3-2-默认优先级关系"><a href="#3-2-默认优先级关系" class="headerlink" title="3.2 默认优先级关系"></a>3.2 默认优先级关系</h3><p>当前配置指南给出的默认线程参数为：</p><table><thead><tr><th>线程</th><th align="right">默认优先级</th><th align="right">默认栈</th></tr></thead><tbody><tr><td>RX helper</td><td align="right">2</td><td align="right">2048 B</td></tr><tr><td>realtime</td><td align="right">3</td><td align="right">2048 B</td></tr><tr><td>mainline</td><td align="right">10</td><td align="right">2048 B</td></tr></tbody></table><p>RT-Thread 的优先级数值越小，优先级越高，因此默认关系满足：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">RX helper priority &lt;= realtime priority &lt; mainline priority</span><br></pre></td></tr></table></figure><p>这不是说所有项目都必须照抄这三个数字，而是说明这个软件包已经把不同路径的实时性等级考虑进运行模型了。</p><p>如果你的 BSP 里还有 EtherCAT、中断密集 ADC、运动控制 fast loop、USB、文件系统或者高优先级安全任务，那么仍然应该基于整个系统重新做优先级预算；但至少 CANopen 这一侧已经有清晰边界，而不是隐藏在应用主线程里。</p><h3 id="3-3-为什么不在-CAN-ISR-直接跑-CANopenNode-API"><a href="#3-3-为什么不在-CAN-ISR-直接跑-CANopenNode-API" class="headerlink" title="3.3 为什么不在 CAN ISR 直接跑 CANopenNode API"></a>3.3 为什么不在 CAN ISR 直接跑 CANopenNode API</h3><p><code>CO_driver_target.h</code> 中已经明确说明，本 RT-Thread port 运行在 RT-Thread 线程上下文：<code>co_rx</code> 分发接收 callback，<code>co_rt</code> 处理实时路径，<code>co_main</code> 处理异步路径。可能获取 port 锁的 CANopenNode API 不应直接从 ISR 调用，需要把工作延后到线程。</p><p>这条约束很重要。</p><p>很多嵌入式协议栈第一次能跑起来，靠的是“中断一来直接处理”；等项目复杂后就开始遇到锁、优先级反转、不可重入 API、长 ISR、文件系统调用等问题。现在把线程所有权写清楚，后续应用层调用 SDO Client、Gateway、OD extension 时就有明确依据。</p><hr><h2 id="4-这个软件包真正解决的是“RT-Thread-工程化集成”"><a href="#4-这个软件包真正解决的是“RT-Thread-工程化集成”" class="headerlink" title="4. 这个软件包真正解决的是“RT-Thread 工程化集成”"></a>4. 这个软件包真正解决的是“RT-Thread 工程化集成”</h2><p>如果只看 CANopenNode 上游源码，很多功能本来就有，所以有人可能会问：既然上游已经有 NMT、SDO、PDO、LSS，那么这个 RT-Thread 软件包到底增加了什么？</p><p>我认为主要价值可以概括为五层：设备接入、运行时、配置裁剪、持久化，以及可验证性。</p><h3 id="4-1-RT-Thread-CAN-设备绑定"><a href="#4-1-RT-Thread-CAN-设备绑定" class="headerlink" title="4.1 RT-Thread CAN 设备绑定"></a>4.1 RT-Thread CAN 设备绑定</h3><p>软件包通过 RT-Thread CAN 设备框架绑定实际 CAN 控制器。默认配置项是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">PKG_CANOPENNODE_CAN_DEV_NAME=&quot;can1&quot;</span><br></pre></td></tr></table></figure><p>实际项目里可以改成 <code>can0</code>、<code>can1</code> 或 BSP 导出的其他 CAN 设备名。初始化阶段会通过 RT-Thread 设备框架查找、打开并配置 CAN 设备，而不是在协议层直接依赖 STM32 HAL、FDCAN HAL 或某一个具体芯片寄存器。</p><p>这带来的好处是：只要 BSP 的 CAN 驱动已经按 RT-Thread 设备模型工作，协议栈移植层通常不需要跟着 MCU 型号重写。</p><h3 id="4-2-CAN-接收与硬件过滤器"><a href="#4-2-CAN-接收与硬件过滤器" class="headerlink" title="4.2 CAN 接收与硬件过滤器"></a>4.2 CAN 接收与硬件过滤器</h3><p>CANopenNode 本身需要多个 RX buffer 来匹配不同 COB-ID。<code>canopennode-rtt</code> 可以选择使用 RT-Thread CAN HDR 硬件过滤器：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">PKG_CANOPENNODE_USING_RTT_CAN_FILTER</span><br></pre></td></tr></table></figure><p>如果 BSP 支持硬件过滤器并且 filter bank 足够，软件包会尽量使用硬件过滤；如果资源不足或配置失败，则可以回退到软件 RX 分发。</p><p>这点对 STM32 之类硬件过滤 bank 数量有限的平台很实用，因为“开了硬件过滤器”不应该变成“一旦资源不够整个协议栈启动失败”的单点风险。</p><h3 id="4-3-Kconfig-与-SCons-不只是提供几个开关"><a href="#4-3-Kconfig-与-SCons-不只是提供几个开关" class="headerlink" title="4.3 Kconfig 与 SCons 不只是提供几个开关"></a>4.3 Kconfig 与 SCons 不只是提供几个开关</h3><p>软件包不是把整个 CANopenNode 目录无脑加入编译，而是根据 Kconfig 选择真正需要的源文件。</p><p>例如：</p><ul><li>开启 SDO Server，构建 <code>CO_SDOserver.c</code>；</li><li>开启 SDO Client，构建 <code>CO_SDOclient.c</code>；</li><li>开启 TIME，加入 <code>CO_TIME.c</code>；</li><li>开启 LSS Master&#x2F;Slave，加入对应 LSS 源文件；</li><li>开启 Gateway，加入 CiA 309-3 相关实现；</li><li>开启 SRDO&#x2F;GFC，加入 CiA 304 相关实现；</li><li>开启 Demo NMT Master Test，才把对应测试夹具编进固件。</li></ul><p>这对于 MCU 很关键。CANopenNode 是一个可裁剪协议栈，如果 RT-Thread 软件包最后把所有功能都硬编进去，就失去了原本的设计优势。</p><h3 id="4-4-CO-CONFIG-与-RT-Thread-配置统一"><a href="#4-4-CO-CONFIG-与-RT-Thread-配置统一" class="headerlink" title="4.4 CO_CONFIG_* 与 RT-Thread 配置统一"></a>4.4 <code>CO_CONFIG_*</code> 与 RT-Thread 配置统一</h3><p>CANopenNode V4 的很多能力通过 <code>CO_CONFIG_*</code> 宏决定，而 RT-Thread 用户通常希望所有组件都能在 <code>menuconfig</code> 中统一配置。</p><p><code>port/rtthread/CO_config_rtt.h</code> 就承担这层映射：上层选 <code>PKG_CANOPENNODE_USING_SDO_CLIENT</code>，最终转换为上游 CANopenNode 所需的 SDO Client 配置；选择 Gateway、FIFO、CRC、LSS 等功能时，也按照依赖关系组合对应宏。</p><p>因此使用者不需要一边改 <code>rtconfig.h</code>，一边再手工维护一份完全独立的 <code>CO_config.h</code>。</p><h3 id="4-5-Communication-Reset-生命周期"><a href="#4-5-Communication-Reset-生命周期" class="headerlink" title="4.5 Communication Reset 生命周期"></a>4.5 Communication Reset 生命周期</h3><p>CANopen 的 Communication Reset 不是简单把一个状态变量清零。协议通信对象可能需要重新初始化，callback、OD binding、Heartbeat Consumer、Storage、CAN mode 等相关运行状态都要重新建立。</p><p><code>canopennode-rtt</code> 把这些动作放进统一的 <code>CANopenNodeRTT</code> 生命周期中，让应用层不必自己拼一个“reset 后重新造 CO_t”的私有流程。</p><p>这也是为什么仓库后续做主站能力、Gateway、Demo fixture 时，可以在同一个 wrapper 生命周期里重绑定，而不需要每个功能自己维护一套对象所有权。</p><hr><h2 id="5-从站怎么用：默认路径已经足够适合多数-CANopen-设备"><a href="#5-从站怎么用：默认路径已经足够适合多数-CANopen-设备" class="headerlink" title="5. 从站怎么用：默认路径已经足够适合多数 CANopen 设备"></a>5. 从站怎么用：默认路径已经足够适合多数 CANopen 设备</h2><p>先看大家最熟悉的设备&#x2F;从站场景。</p><p>一个典型 CANopen MCU 设备至少会有：</p><ul><li>自己的 Node-ID；</li><li>NMT Slave；</li><li>Heartbeat Producer；</li><li>Object Dictionary；</li><li>SDO Server；</li><li>根据业务需要配置 RPDO&#x2F;TPDO；</li><li>可能还会有 EMCY、SYNC、TIME、LSS Slave 和参数持久化。</li></ul><p><code>canopennode-rtt</code> 默认配置就偏向这类 bring-up 场景：SDO Server、PDO、SYNC、LSS Slave 等基础能力可以直接通过 Kconfig 启用。</p><h3 id="5-1-最短-bring-up-链路"><a href="#5-1-最短-bring-up-链路" class="headerlink" title="5.1 最短 bring-up 链路"></a>5.1 最短 bring-up 链路</h3><p>从工程角度，第一次把节点跑起来建议只追求一条最短链路：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    BSP[&quot;RT-Thread BSP CAN 驱动可用&quot;] --&gt; PKG[&quot;启用 canopennode-rtt&quot;]</span><br><span class="line">    PKG --&gt; CFG[&quot;配置 can1 / Node-ID / bitrate&quot;]</span><br><span class="line">    CFG --&gt; OD2[&quot;启用 demo OD&quot;]</span><br><span class="line">    OD2 --&gt; BUILD[&quot;SCons 构建&quot;]</span><br><span class="line">    BUILD --&gt; BOOT[&quot;发送 Boot-up&quot;]</span><br><span class="line">    BOOT --&gt; SDO[&quot;Host 通过 SDO 访问 OD&quot;]</span><br><span class="line">    SDO --&gt; PDO3[&quot;继续验证 PDO / EMCY / Storage&quot;]</span><br></pre></td></tr></table></figure><p>第一次联调不要一上来就把 LSS、SRDO、Gateway、Storage、几十个 PDO 全部打开。先确认 CAN 物理层和基础 CiA 301 链路正常，然后再加能力，定位问题会简单很多。</p><h3 id="5-2-最基本的配置"><a href="#5-2-最基本的配置" class="headerlink" title="5.2 最基本的配置"></a>5.2 最基本的配置</h3><p>在 RT-Thread 工程里至少需要：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">RT_USING_HEAP</span><br><span class="line">RT_USING_DEVICE</span><br><span class="line">RT_USING_CAN</span><br><span class="line">RT_USING_MUTEX</span><br><span class="line">RT_USING_SEMAPHORE</span><br><span class="line">PKG_USING_CANOPENNODE=y</span><br></pre></td></tr></table></figure><p>首次 demo 可以继续保留：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">PKG_CANOPENNODE_APP_AUTO_INIT=y</span><br><span class="line">PKG_CANOPENNODE_USING_DEMO_OD=y</span><br><span class="line">PKG_CANOPENNODE_USING_SDO_SERVER=y</span><br><span class="line">PKG_CANOPENNODE_USING_PDO=y</span><br></pre></td></tr></table></figure><p>然后确认：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">PKG_CANOPENNODE_CAN_DEV_NAME=&quot;can1&quot;</span><br><span class="line">PKG_CANOPENNODE_AUTO_INIT_NODE_ID=1</span><br><span class="line">PKG_CANOPENNODE_AUTO_INIT_BITRATE=1000</span><br></pre></td></tr></table></figure><p>这里 bitrate 单位是 kbit&#x2F;s，因此 <code>1000</code> 对应 1 Mbit&#x2F;s。</p><h3 id="5-3-自动初始化和手动初始化"><a href="#5-3-自动初始化和手动初始化" class="headerlink" title="5.3 自动初始化和手动初始化"></a>5.3 自动初始化和手动初始化</h3><p>单节点 Demo 最方便的是 <code>PKG_CANOPENNODE_APP_AUTO_INIT=y</code>，软件包会在 RT-Thread 应用初始化阶段创建默认实例。</p><p>如果产品希望自己控制对象生命周期，可以关闭 auto init，然后显式初始化：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;CO_app_RTT.h&quot;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">static</span> CANopenNodeRTT canopen_app;</span><br><span class="line"></span><br><span class="line"><span class="type">static</span> <span class="type">int</span> <span class="title function_">app_canopen_init</span><span class="params">(<span class="type">void</span>)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">return</span> (<span class="type">int</span>)canopen_app_rtt_init(&amp;canopen_app, <span class="string">&quot;can1&quot;</span>, <span class="number">1</span>, <span class="number">1000</span>);</span><br><span class="line">&#125;</span><br><span class="line">INIT_APP_EXPORT(app_canopen_init);</span><br></pre></td></tr></table></figure><p>有两个细节不要忽略：</p><p>第一，<code>CANopenNodeRTT</code> 首次使用前要零初始化。上面使用静态对象天然满足这一点。</p><p>第二，传入的 CAN 设备名需要在实例生命周期内保持有效，因为 wrapper 保存的是字符串指针，而不是复制一份名字。</p><h3 id="5-4-怎样确认从站真的起来了"><a href="#5-4-怎样确认从站真的起来了" class="headerlink" title="5.4 怎样确认从站真的起来了"></a>5.4 怎样确认从站真的起来了</h3><p>如果 Node-ID 是 1，节点完成启动后应该能看到 CANopen Boot-up：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">COB-ID: 0x701</span><br><span class="line">Data:   00</span><br></pre></td></tr></table></figure><p>接下来再使用主站工具或者测试程序访问 SDO Server。</p><p>只要 Boot-up 和 SDO 这两步正常，说明至少下面这条链已经成立：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">RT-Thread CAN driver</span><br><span class="line">    -&gt; canopennode-rtt target driver</span><br><span class="line">    -&gt; CANopenNode core</span><br><span class="line">    -&gt; Object Dictionary</span><br><span class="line">    -&gt; CAN bus</span><br></pre></td></tr></table></figure><p>再往后才是产品自己的 PDO mapping、EMCY、参数持久化和应用状态机。</p><hr><h2 id="6-Object-Dictionary：Demo-能让你跑起来，但产品一定要换成自己的-OD"><a href="#6-Object-Dictionary：Demo-能让你跑起来，但产品一定要换成自己的-OD" class="headerlink" title="6. Object Dictionary：Demo 能让你跑起来，但产品一定要换成自己的 OD"></a>6. Object Dictionary：Demo 能让你跑起来，但产品一定要换成自己的 OD</h2><p>CANopen 设备开发里，Object Dictionary 不是“一个用于调试的表”，而是设备通信模型的中心。</p><p>设备的通信参数、应用变量、PDO mapping、Identity、Heartbeat、Storage 参数，以及后续设备 profile 对象，都围绕 OD 组织。</p><p>仓库提供：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">examples/demo_device/</span><br><span class="line">├── project.xdd</span><br><span class="line">├── OD.c</span><br><span class="line">├── OD.h</span><br><span class="line">├── project.eds</span><br><span class="line">└── project.md</span><br></pre></td></tr></table></figure><p>这套 Demo 的意义是帮助首次 bring-up 和自动测试，不应该原样变成产品 OD。</p><h3 id="6-1-推荐的产品工作流"><a href="#6-1-推荐的产品工作流" class="headerlink" title="6.1 推荐的产品工作流"></a>6.1 推荐的产品工作流</h3><p>比较推荐的做法是把 XDD&#x2F;EDS 作为设备描述的 source of truth，然后用 CANopenEditor 生成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">XDD / 项目定义</span><br><span class="line">      |</span><br><span class="line">      +--&gt; OD.c</span><br><span class="line">      +--&gt; OD.h</span><br><span class="line">      +--&gt; EDS / XDD</span><br><span class="line">      +--&gt; Markdown 文档</span><br></pre></td></tr></table></figure><p>应用只去绑定生成后的对象，不在运行时凭空发明一套“隐藏 OD”。</p><p>这种方式尤其适合后续做 CiA 402、复杂 PDO、多轴对象或者产品参数持久化，因为 EDS&#x2F;XDD 本身就是主站工具、调试工具和设备集成的重要交付物。</p><h3 id="6-2-Demo-OD-里为什么有-0x2300～0x2303"><a href="#6-2-Demo-OD-里为什么有-0x2300～0x2303" class="headerlink" title="6.2 Demo OD 里为什么有 0x2300～0x2303"></a>6.2 Demo OD 里为什么有 0x2300～0x2303</h3><p>当前仓库为了协议自动验证，在 Demo OD 里增加了一些 test-only 诊断对象，例如：</p><ul><li><code>0x2300</code>：TIME consumer 诊断；</li><li><code>0x2301</code>：远端 EMCY consumer 诊断；</li><li><code>0x2302</code>：GFC 测试记录；</li><li><code>0x2303</code>：MCU SDO Client 自动测试状态。</li></ul><p>这些对象的定位是“测试可观察性”，而不是新的 CANopen 标准对象，更不是产品 API。</p><p>这一点在写产品 EDS&#x2F;XDD 时要分清。Demo 测试对象可以帮助开发阶段自动收集证据，但产品对象字典应该按自己的设备模型重新定义。</p><hr><h2 id="7-主站怎么做：不是换一套协议栈，而是在同一实例上打开控制器能力"><a href="#7-主站怎么做：不是换一套协议栈，而是在同一实例上打开控制器能力" class="headerlink" title="7. 主站怎么做：不是换一套协议栈，而是在同一实例上打开控制器能力"></a>7. 主站怎么做：不是换一套协议栈，而是在同一实例上打开控制器能力</h2><p>现在进入这篇文章最想强调的部分。</p><p>如果你要让 RT-Thread MCU 控制其他 CANopen 节点，不需要把 <code>canopennode-rtt</code> 换成另一套“Master Stack”。CANopenNode 本身已经有需要的基础对象，当前 RT-Thread wrapper 也已经把相应编译与运行入口接出来。</p><p>一个最小的 CANopen 控制器通常至少需要：</p><ol><li>NMT Master：让远端节点进入 Operational、Stopped、Pre-operational，或者做 reset；</li><li>Heartbeat Consumer：不能只发命令，还要确认远端节点真的在线、状态真的变了；</li><li>SDO Client：读取 Identity、配置 PDO、写设备参数；</li><li>PDO：运行阶段收发过程数据；</li><li>如果使用同步运动或者同步采样，还需要 SYNC Producer；</li><li>如果要在现场分配 Node-ID&#x2F;bitrate，可以加 LSS Master；</li><li>如果希望通过串口&#x2F;Console 暴露标准控制接口，可以加 CiA 309-3 Gateway。</li></ol><h3 id="7-1-一个典型-MCU-主站结构"><a href="#7-1-一个典型-MCU-主站结构" class="headerlink" title="7.1 一个典型 MCU 主站结构"></a>7.1 一个典型 MCU 主站结构</h3><p>下面这张图不是说所有项目必须照这个层次实现，而是展示 <code>canopennode-rtt</code> 已经能提供哪些“积木”。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line">flowchart TB</span><br><span class="line">    CTRL[&quot;RT-Thread 控制器应用&quot;] --&gt; NMTM[&quot;NMT Master&quot;]</span><br><span class="line">    CTRL --&gt; SDOC[&quot;SDO Client&quot;]</span><br><span class="line">    CTRL --&gt; HBC[&quot;Heartbeat Consumer&quot;]</span><br><span class="line">    CTRL --&gt; SYNCP[&quot;SYNC Producer&quot;]</span><br><span class="line">    CTRL --&gt; PDOAPP[&quot;RPDO / TPDO Process Image&quot;]</span><br><span class="line">    CTRL --&gt; LSSM[&quot;LSS Master 可选&quot;]</span><br><span class="line"></span><br><span class="line">    NMTM --&gt; BUS2[&quot;CAN Bus&quot;]</span><br><span class="line">    SDOC --&gt; BUS2</span><br><span class="line">    HBC --&gt; BUS2</span><br><span class="line">    SYNCP --&gt; BUS2</span><br><span class="line">    PDOAPP --&gt; BUS2</span><br><span class="line">    LSSM --&gt; BUS2</span><br><span class="line"></span><br><span class="line">    BUS2 --&gt; D2[&quot;Node 2 Drive / I/O&quot;]</span><br><span class="line">    BUS2 --&gt; D3[&quot;Node 3 Drive / I/O&quot;]</span><br><span class="line">    BUS2 --&gt; D4[&quot;Node 4 Sensor / Device&quot;]</span><br></pre></td></tr></table></figure><p>这里真正需要你在产品层补充的是“编排策略”：什么时候发现设备、什么时候配置、哪些 SDO 失败允许重试、启动前检查哪些 Identity、心跳掉线后怎样处理、PDO 映射是谁负责、设备恢复后是否允许自动重新进入 Operational。</p><p>基础协议对象不需要从零写。</p><h3 id="7-2-NMT-Master-已经不是纸面能力"><a href="#7-2-NMT-Master-已经不是纸面能力" class="headerlink" title="7.2 NMT Master 已经不是纸面能力"></a>7.2 NMT Master 已经不是纸面能力</h3><p>仓库当前已经有专门的 NMT Master 自动测试模块：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">port/rtthread/demo/CO_demo_nmt_master.c</span><br></pre></td></tr></table></figure><p>对应配置：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">PKG_CANOPENNODE_DEMO_NMT_MASTER_TEST=y</span><br></pre></td></tr></table></figure><p>它会自动选择：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">PKG_CANOPENNODE_NMT_MASTER</span><br><span class="line">PKG_CANOPENNODE_USING_HB_CONS</span><br><span class="line">PKG_CANOPENNODE_HB_CONS_QUERY_FUNCT</span><br><span class="line">PKG_CANOPENNODE_GLOBAL_OD_DYNAMIC</span><br></pre></td></tr></table></figure><p>测试中，RT-Thread MCU 自己仍然保留本节点的 NMT Slave、Heartbeat、SDO、PDO 等功能，同时调用：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">CO_NMT_sendCommand(...)</span><br></pre></td></tr></table></figure><p>去控制远端节点。</p><p>这正好证明了前面那句话：<strong>本地“从站侧能力”与对远端的 NMT Master 能力并不互斥。</strong></p><h3 id="7-3-NMT-Master-自动序列"><a href="#7-3-NMT-Master-自动序列" class="headerlink" title="7.3 NMT Master 自动序列"></a>7.3 NMT Master 自动序列</h3><p>当前测试不是“发一帧看总线上有没有数据”，而是把 Heartbeat Consumer 也纳入状态确认：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant MCU as RT-Thread MCU Node 1</span><br><span class="line">    participant CAN as CAN Bus</span><br><span class="line">    participant PEER as Remote Node 2</span><br><span class="line"></span><br><span class="line">    MCU-&gt;&gt;CAN: Wait Heartbeat Consumer ACTIVE</span><br><span class="line">    PEER-&gt;&gt;CAN: Heartbeat</span><br><span class="line">    CAN-&gt;&gt;MCU: Remote NMT state valid</span><br><span class="line">    MCU-&gt;&gt;CAN: NMT START</span><br><span class="line">    CAN-&gt;&gt;PEER: Enter Operational</span><br><span class="line">    PEER-&gt;&gt;CAN: Heartbeat Operational</span><br><span class="line">    CAN-&gt;&gt;MCU: Confirm Operational</span><br><span class="line">    MCU-&gt;&gt;CAN: NMT STOP</span><br><span class="line">    CAN-&gt;&gt;PEER: Enter Stopped</span><br><span class="line">    PEER-&gt;&gt;CAN: Heartbeat Stopped</span><br><span class="line">    CAN-&gt;&gt;MCU: Confirm Stopped</span><br><span class="line">    MCU-&gt;&gt;CAN: NMT PRE-OP / RESET commands</span><br><span class="line">    CAN-&gt;&gt;PEER: State transition / reset</span><br><span class="line">    PEER-&gt;&gt;CAN: Boot-up and Heartbeat</span><br><span class="line">    CAN-&gt;&gt;MCU: Confirm reset and new state</span><br></pre></td></tr></table></figure><p>正式测试序列包含 START、STOP、PREOP、RESET_COMM、RESET_NODE、再次 START，并对远端状态迁移进行确认。</p><p>所以这里的价值并不只是“CANopenNode 有一个 <code>CO_NMT_sendCommand()</code> 函数”，而是 RT-Thread wrapper 已经开始围绕这个能力建立 MCU 侧验证路径。</p><h3 id="7-4-但它不等于完整-Network-Manager"><a href="#7-4-但它不等于完整-Network-Manager" class="headerlink" title="7.4 但它不等于完整 Network Manager"></a>7.4 但它不等于完整 Network Manager</h3><p>当前 NMT Master 测试文档自己也明确说明：它验证的是 simple NMT Master command producer 与远端节点控制，不等价于完整 NMT network manager。</p><p>这是一个很重要的工程边界。</p><p>一个真正的产品级主站往往还需要：</p><ul><li>设备清单和拓扑配置；</li><li>Expected Node-ID；</li><li>Identity 校验；</li><li>启动参数下载；</li><li>PDO mapping 下发；</li><li>设备 Ready 条件；</li><li>故障恢复策略；</li><li>网络状态机；</li><li>产品自己的诊断与日志；</li><li>对不同设备 profile 的控制器逻辑。</li></ul><p>这些逻辑没有必要塞进通用 RT-Thread port，它应该属于更上层的 controller application 或 profile module。</p><hr><h2 id="8-SDO-Client：主站真正做参数配置时最常用的能力之一"><a href="#8-SDO-Client：主站真正做参数配置时最常用的能力之一" class="headerlink" title="8. SDO Client：主站真正做参数配置时最常用的能力之一"></a>8. SDO Client：主站真正做参数配置时最常用的能力之一</h2><p>NMT 只能改变节点网络管理状态，真正做设备配置通常离不开 SDO Client。</p><p>从上游 CANopenNode 的定义看，SDO Server 用来暴露“本节点”的 Object Dictionary，SDO Client 则用来访问“其他节点”的 Object Dictionary。对于主站&#x2F;控制器来说，后者通常负责：</p><ul><li>读取 <code>0x1000</code> Device Type；</li><li>读取 <code>0x1018</code> Identity Object；</li><li>修改 Heartbeat 参数；</li><li>配置远端 PDO Communication Parameter；</li><li>配置远端 PDO Mapping Parameter；</li><li>写入厂商自定义参数；</li><li>读取错误记录或诊断对象；</li><li>在设备 profile 中下发模式、限制值或初始化参数。</li></ul><p>当前软件包直接提供：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">PKG_CANOPENNODE_USING_SDO_CLIENT</span><br><span class="line">PKG_CANOPENNODE_SDO_CLI_SEGMENTED</span><br><span class="line">PKG_CANOPENNODE_SDO_CLI_BLOCK</span><br><span class="line">PKG_CANOPENNODE_SDO_CLI_BUFFER_SIZE</span><br></pre></td></tr></table></figure><p>配置指南对 <code>PKG_CANOPENNODE_USING_SDO_CLIENT</code> 的描述也很直接：用于 master、gateway 或本地工具访问远端节点。</p><h3 id="8-1-为什么-SDO-Client-不能简单写成一个阻塞函数"><a href="#8-1-为什么-SDO-Client-不能简单写成一个阻塞函数" class="headerlink" title="8.1 为什么 SDO Client 不能简单写成一个阻塞函数"></a>8.1 为什么 SDO Client 不能简单写成一个阻塞函数</h3><p>在 PC 上写测试工具时，我们很容易写出这种伪代码：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">write_sdo(node, index, subindex, value);</span><br><span class="line">wait_response();</span><br></pre></td></tr></table></figure><p>但 MCU&#x2F;RTOS 环境里，如果一个 SDO transfer 因远端节点掉线卡住几百毫秒，主线程不能因此把整个 CANopen runtime 阻塞掉。</p><p>CANopenNode 的 SDO Client 本身是非阻塞状态机，这意味着更适合在 <code>co_main</code> 中周期推进。产品层只需要在它外面维护自己的 request&#x2F;result 生命周期，而不是把线程 sleep 当协议状态机。</p><p>当前 Demo 已经有 <code>PKG_CANOPENNODE_DEMO_SDO_CLIENT_TEST</code>，并通过 test-only OD <code>0x2303</code> 发布 request、active、completion sequence 和原生 SDO 结果，用来验证 MCU 侧非阻塞 SDO Client。</p><p>这类测试设计很有意义，因为它把“API 能编译”提升到了“运行时状态机能够被 Host 驱动和观察”的层次。</p><h3 id="8-2-主站应用层应该怎样组织-SDO"><a href="#8-2-主站应用层应该怎样组织-SDO" class="headerlink" title="8.2 主站应用层应该怎样组织 SDO"></a>8.2 主站应用层应该怎样组织 SDO</h3><p>推荐把 SDO Client 当成一个异步资源，而不是到处直接调用。</p><p>例如一个控制器可以设计成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">Device Manager</span><br><span class="line">    |</span><br><span class="line">    +-- Node 2 commissioning sequence</span><br><span class="line">    |      +-- read 0x1018</span><br><span class="line">    |      +-- configure heartbeat</span><br><span class="line">    |      +-- configure RPDO/TPDO</span><br><span class="line">    |      +-- write application parameters</span><br><span class="line">    |</span><br><span class="line">    +-- Node 3 commissioning sequence</span><br><span class="line">    |</span><br><span class="line">    +-- SDO request scheduler</span><br><span class="line">           +-- start transfer</span><br><span class="line">           +-- process non-blocking state</span><br><span class="line">           +-- timeout / abort handling</span><br><span class="line">           +-- result callback</span><br></pre></td></tr></table></figure><p>是否允许并发多个 Client、是否给不同远端节点配置多个 SDO Client channel，取决于 Object Dictionary 与资源设计，但总原则是不要把网络管理策略塞进底层 driver。</p><hr><h2 id="9-Heartbeat-Consumer：主站不能只会发命令，还必须知道对方到底活没活"><a href="#9-Heartbeat-Consumer：主站不能只会发命令，还必须知道对方到底活没活" class="headerlink" title="9. Heartbeat Consumer：主站不能只会发命令，还必须知道对方到底活没活"></a>9. Heartbeat Consumer：主站不能只会发命令，还必须知道对方到底活没活</h2><p>如果一个所谓“主站”只有 NMT 发送和 SDO 写入，却不知道远端节点是不是在线，那它只能算一个 CAN 发包器。</p><p>Heartbeat Consumer 是控制器应用非常重要的基础能力。</p><p>当前软件包默认就支持：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">PKG_CANOPENNODE_USING_HB_CONS=y</span><br></pre></td></tr></table></figure><p>并且 NMT Master 自动测试把 Heartbeat Consumer 作为远端状态确认的核心证据。</p><p>这个设计很值得借鉴：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">发出 NMT START</span><br><span class="line">    ↓</span><br><span class="line">不是“发送成功”就结束</span><br><span class="line">    ↓</span><br><span class="line">等待目标节点 Heartbeat</span><br><span class="line">    ↓</span><br><span class="line">确认远端 NMT state = OPERATIONAL</span><br><span class="line">    ↓</span><br><span class="line">再进入下一步</span><br></pre></td></tr></table></figure><p>对于产品主站也是一样。CAN 控制器返回“帧发送成功”只能证明本地驱动接受了发送请求，不能证明远端节点执行了命令。</p><h3 id="9-1-Heartbeat-Consumer-还能承担什么"><a href="#9-1-Heartbeat-Consumer-还能承担什么" class="headerlink" title="9.1 Heartbeat Consumer 还能承担什么"></a>9.1 Heartbeat Consumer 还能承担什么</h3><p>在真实项目中，它通常还能作为：</p><ul><li>节点上线发现；</li><li>节点掉线检测；</li><li>远端 NMT 状态观测；</li><li>reset 后重新上线判定；</li><li>自动重配置触发条件；</li><li>系统故障降级条件；</li><li>HMI&#x2F;上位机节点健康状态来源。</li></ul><p>如果后续做 CiA 402 Controller，Heartbeat Consumer 仍然会是“这个驱动节点是否存在、当前是否允许继续控制”的基础网络证据之一。</p><hr><h2 id="10-SYNC-Producer-PDO：做实时控制时，主站能力就不只是-SDO-配参数"><a href="#10-SYNC-Producer-PDO：做实时控制时，主站能力就不只是-SDO-配参数" class="headerlink" title="10. SYNC Producer + PDO：做实时控制时，主站能力就不只是 SDO 配参数"></a>10. SYNC Producer + PDO：做实时控制时，主站能力就不只是 SDO 配参数</h2><p>CANopen 运行阶段真正高频的数据交换主要依靠 PDO，而不是 SDO。</p><p>在一个控制器场景中，可能存在：</p><ul><li>主站向多个驱动发送目标位置、目标速度、Controlword；</li><li>各驱动反馈 Statusword、实际位置、实际速度；</li><li>所有驱动按照同一个 SYNC 周期更新同步 PDO；</li><li>控制器在每个 SYNC 周期形成新的 process image。</li></ul><p>当前配置中：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">PKG_CANOPENNODE_USING_SYNC=y</span><br><span class="line">PKG_CANOPENNODE_SYNC_PRODUCER=y</span><br><span class="line">PKG_CANOPENNODE_USING_PDO=y</span><br><span class="line">PKG_CANOPENNODE_RPDO=y</span><br><span class="line">PKG_CANOPENNODE_TPDO=y</span><br><span class="line">PKG_CANOPENNODE_PDO_SYNC=y</span><br></pre></td></tr></table></figure><p>这些并不是“只为从站准备”的能力。</p><p>对于 controller application 来说，SYNC Producer 尤其关键。它可以让 MCU 成为同步网络的节拍来源，而 RPDO&#x2F;TPDO 则承担周期过程数据交换。</p><h3 id="10-1-realtime-线程为什么要单独存在"><a href="#10-1-realtime-线程为什么要单独存在" class="headerlink" title="10.1 realtime 线程为什么要单独存在"></a>10.1 realtime 线程为什么要单独存在</h3><p>前面说到 <code>co_rt</code> 专门处理：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">SYNC -&gt; RPDO -&gt; TPDO -&gt; SRDO</span><br></pre></td></tr></table></figure><p>这就是为了让周期过程数据路径与 SDO、Storage、Gateway 等异步逻辑分开。</p><p>对于普通 I&#x2F;O 节点，这能降低同步 PDO 抖动；对于未来运动控制 profile，这个架构边界更重要。</p><p>当前 CiA 402 规划 Issue 中已经明确提出：同步模式未来需要在 <code>CO_process_RPDO()</code> 与 <code>CO_process_TPDO()</code> 之间插入有界、非阻塞的 profile fast path，而不去修改上游 CANopenNode core。</p><p>这说明现有 realtime 架构不是为了 Demo 临时搭出来的，它已经为后续同步控制场景留出了合理插入点。</p><hr><h2 id="11-LSS-Master-和-CiA-309-3-Gateway：让-MCU-不只是“设备”，还可以做现场配置入口"><a href="#11-LSS-Master-和-CiA-309-3-Gateway：让-MCU-不只是“设备”，还可以做现场配置入口" class="headerlink" title="11. LSS Master 和 CiA 309-3 Gateway：让 MCU 不只是“设备”，还可以做现场配置入口"></a>11. LSS Master 和 CiA 309-3 Gateway：让 MCU 不只是“设备”，还可以做现场配置入口</h2><p>CANopen 项目在批量设备、现场调试和售后阶段，经常会遇到 Node-ID、bitrate 和远端参数配置问题。</p><p>这时 LSS 和 Gateway 的价值就体现出来了。</p><h3 id="11-1-LSS-Slave-与-LSS-Master-都可以选"><a href="#11-1-LSS-Slave-与-LSS-Master-都可以选" class="headerlink" title="11.1 LSS Slave 与 LSS Master 都可以选"></a>11.1 LSS Slave 与 LSS Master 都可以选</h3><p>当前软件包默认有 LSS Slave：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">PKG_CANOPENNODE_USING_LSS_SLAVE=y</span><br></pre></td></tr></table></figure><p>用于让外部 LSS Master 配置本节点。</p><p>同时也提供：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">PKG_CANOPENNODE_USING_LSS_MASTER</span><br></pre></td></tr></table></figure><p>用于本 MCU 去配置其他节点。</p><p>这意味着同一套软件包既能做“被配置设备”，也能做 commissioning controller。</p><h3 id="11-2-CiA-309-3-ASCII-Gateway"><a href="#11-2-CiA-309-3-ASCII-Gateway" class="headerlink" title="11.2 CiA 309-3 ASCII Gateway"></a>11.2 CiA 309-3 ASCII Gateway</h3><p>CANopenNode 上游 Gateway ASCII 将 NMT Master、SDO Client 和 LSS Master 映射成 CiA 309-3 ASCII 命令接口。</p><p><code>canopennode-rtt</code> 当前也提供：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">PKG_CANOPENNODE_USING_GATEWAY_ASCII</span><br></pre></td></tr></table></figure><p>以及 RT-Thread 侧 <code>CO_gateway_RTT.c/.h</code> helper。</p><p>这里的边界是：软件包负责编译和运行 gateway helper，实际命令从哪里来由应用决定。你可以把它接到：</p><ul><li>RT-Thread FinSH&#x2F;MSH；</li><li>UART；</li><li>USB CDC；</li><li>TCP&#x2F;串口网关；</li><li>产品自己的 CLI。</li></ul><p>于是 MCU 本身就可以成为一个现场 CANopen 工具入口。</p><p>例如：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    PC[&quot;PC / Service Tool&quot;] --&gt; UART[&quot;UART / USB / TCP&quot;]</span><br><span class="line">    UART --&gt; CLI[&quot;RT-Thread Console&quot;]</span><br><span class="line">    CLI --&gt; GTW2[&quot;CiA 309-3 Gateway&quot;]</span><br><span class="line">    GTW2 --&gt; NMT2[&quot;NMT Master&quot;]</span><br><span class="line">    GTW2 --&gt; SDO2[&quot;SDO Client&quot;]</span><br><span class="line">    GTW2 --&gt; LSS2[&quot;LSS Master&quot;]</span><br><span class="line">    NMT2 --&gt; BUS3[&quot;CAN Bus&quot;]</span><br><span class="line">    SDO2 --&gt; BUS3</span><br><span class="line">    LSS2 --&gt; BUS3</span><br><span class="line">    BUS3 --&gt; DEVX[&quot;Remote CANopen Devices&quot;]</span><br></pre></td></tr></table></figure><p>对于没有 Linux 主机、只有 MCU + CAN + 一个维护串口的工业设备，这种模式非常实用。</p><hr><h2 id="12-event-driven-mainline：不必永久用固定-1-ms-轮询烧-CPU"><a href="#12-event-driven-mainline：不必永久用固定-1-ms-轮询烧-CPU" class="headerlink" title="12. event-driven mainline：不必永久用固定 1 ms 轮询烧 CPU"></a>12. event-driven mainline：不必永久用固定 1 ms 轮询烧 CPU</h2><p>当前 <code>master</code> 已经加入 event-driven mainline scheduling。</p><p>配置项：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">PKG_CANOPENNODE_GLOBAL_TIMERNEXT</span><br></pre></td></tr></table></figure><p>开启后，wrapper 会利用 CANopenNode 的 <code>timerNext_us</code> 和 callback-pre 机制，通过 RT-Thread event 唤醒 mainline，而不是无条件固定 1 ms 轮询所有异步对象。</p><p>如果关闭，则仍保留原有固定轮询路径。</p><h3 id="12-1-这个功能为什么有价值"><a href="#12-1-这个功能为什么有价值" class="headerlink" title="12.1 这个功能为什么有价值"></a>12.1 这个功能为什么有价值</h3><p>在一个资源不算紧张的 MCU 上，1 ms 轮询听起来没什么。但当系统同时有：</p><ul><li>多个通讯协议；</li><li>文件系统；</li><li>显示；</li><li>低功耗需求；</li><li>电机控制；</li><li>大量中断；</li><li>多个 CANopen 实例；</li></ul><p>固定高频唤醒就会逐渐成为不必要的 CPU 占用来源。</p><p><code>timerNext_us</code> 的思路是让协议对象告诉调度器“下一次最早什么时候需要再处理”，再配合 callback-pre 在新事件到达时提前唤醒。</p><p>这类改动看起来不像“多支持了一个协议”，但它对一个 RTOS 软件包的成熟度很关键：协议栈开始从“能跑”向“能与其他实时任务长期共存”演进。</p><h3 id="12-2-realtime-路径仍保持独立"><a href="#12-2-realtime-路径仍保持独立" class="headerlink" title="12.2 realtime 路径仍保持独立"></a>12.2 realtime 路径仍保持独立</h3><p>event-driven mainline 并没有把 realtime path 也改成事件聚合。</p><p>当前 realtime 仍然由周期 timer -&gt; semaphore -&gt; <code>co_rt</code> 驱动，用于 SYNC&#x2F;RPDO&#x2F;TPDO&#x2F;SRDO。</p><p>也就是说，异步路径可以尽量少唤醒，而同步实时路径继续保持明确周期，两者边界没有混在一起。</p><hr><h2 id="13-高分辨率时间：CANopenNode-的微秒时间基准终于不必只靠-RTOS-Tick-猜"><a href="#13-高分辨率时间：CANopenNode-的微秒时间基准终于不必只靠-RTOS-Tick-猜" class="headerlink" title="13. 高分辨率时间：CANopenNode 的微秒时间基准终于不必只靠 RTOS Tick 猜"></a>13. 高分辨率时间：CANopenNode 的微秒时间基准终于不必只靠 RTOS Tick 猜</h2><p>CANopenNode V4 的处理函数普遍使用微秒时间差参数。RT-Thread 系统 tick 如果是 1 ms 甚至 10 ms，直接把 tick 换算成微秒在很多场景下只能得到比较粗的时间基准。</p><p>当前仓库已经加入高分辨率时间抽象：</p><ul><li>默认可以使用 RT-Thread tick fallback；</li><li>也可以配置专用 1 MHz hardware timer backend；</li><li>mainline 和 realtime 都使用测得的微秒 delta；</li><li>Communication Reset 时会重新建立时间基线。</li></ul><p>这对于 SYNC、周期调度、SDO timeout、Heartbeat 等所有依赖 <code>timeDifference_us</code> 的对象都有意义。</p><p>当然，硬件 timer backend 仍然有明确约束，例如当前文档要求关注 1 MHz、32-bit 和单实例所有权等问题，具体使用前应按 BSP 的 timer 实现确认。</p><p>这个功能再次说明软件包的方向不是“只做一个协议 Demo”，而是在补齐 RT-Thread 上长期运行需要的时间、调度和生命周期基础设施。</p><hr><h2 id="14-Storage：把-0x1010-0x1011-接到-RT-Thread-实际存储资源"><a href="#14-Storage：把-0x1010-0x1011-接到-RT-Thread-实际存储资源" class="headerlink" title="14. Storage：把 0x1010&#x2F;0x1011 接到 RT-Thread 实际存储资源"></a>14. Storage：把 0x1010&#x2F;0x1011 接到 RT-Thread 实际存储资源</h2><p>CANopen 产品经常需要保存：</p><ul><li>通信参数；</li><li>应用参数；</li><li>厂商参数；</li><li>LSS 配置；</li><li>PDO mapping；</li><li>校准值。</li></ul><p>CANopenNode 提供 storage 基础对象，但最终落到什么非易失介质，仍然需要 target integration。</p><p><code>canopennode-rtt</code> 当前支持三类 backend：</p><table><thead><tr><th>Backend</th><th>场景</th></tr></thead><tbody><tr><td>DFS</td><td>有文件系统的设备，可以落文件</td></tr><tr><td>AT24CXX EEPROM</td><td>外挂 EEPROM 场景</td></tr><tr><td>User backend</td><td>产品自己接 NOR&#x2F;NAND&#x2F;FRAM&#x2F;参数系统</td></tr></tbody></table><p>对应总开关：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">PKG_CANOPENNODE_USING_STORAGE</span><br></pre></td></tr></table></figure><p>并且可以选择持久化：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">PKG_CANOPENNODE_STORAGE_PERSIST_COMM</span><br><span class="line">PKG_CANOPENNODE_STORAGE_PERSIST_APP</span><br><span class="line">PKG_CANOPENNODE_STORAGE_PERSIST_MANU</span><br></pre></td></tr></table></figure><h3 id="14-1-为什么我更看重“User-backend”"><a href="#14-1-为什么我更看重“User-backend”" class="headerlink" title="14.1 为什么我更看重“User backend”"></a>14.1 为什么我更看重“User backend”</h3><p>DFS 和 AT24CXX 能让 Demo 很快跑起来，但真正产品里往往已经有自己的参数存储模块，例如：</p><ul><li>双备份参数区；</li><li>CRC + version；</li><li>wear leveling；</li><li>参数迁移；</li><li>掉电保护；</li><li>Flash 分区；</li><li>安全写入事务。</li></ul><p>因此通用软件包最重要的不是强迫所有人使用一种存储，而是允许产品把 CANopen storage 接到已有参数系统。</p><p><code>PKG_CANOPENNODE_USING_STORAGE_USER</code> 就是为这种场景保留的边界。</p><hr><h2 id="15-测试不是另起炉灶：MCU-侧与-Linux-Host-分工很清晰"><a href="#15-测试不是另起炉灶：MCU-侧与-Linux-Host-分工很清晰" class="headerlink" title="15. 测试不是另起炉灶：MCU 侧与 Linux Host 分工很清晰"></a>15. 测试不是另起炉灶：MCU 侧与 Linux Host 分工很清晰</h2><p>这个仓库还有一个我比较喜欢的设计：没有试图把所有测试都塞进 MCU。</p><p>当前配套仓库：</p><p><a href="https://github.com/wdfk-prog/canopen-slave-tester">https://github.com/wdfk-prog/canopen-slave-tester</a></p><p>它基于 Linux&#x2F;Lely，主要承担 Host&#x2F;主站自动协议测试；<code>canopennode-rtt</code> 则负责 MCU&#x2F;RT-Thread 侧的协议运行、测试夹具和诊断对象。</p><p>两边通过标准 CANopen 对象和 Demo&#x2F;Test OD 配合，不共享一套私有进程内接口。</p><h3 id="15-1-普通从站测试拓扑"><a href="#15-1-普通从站测试拓扑" class="headerlink" title="15.1 普通从站测试拓扑"></a>15.1 普通从站测试拓扑</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">Linux / Lely Master or Tester</span><br><span class="line">        |</span><br><span class="line">        | CAN</span><br><span class="line">        v</span><br><span class="line">RT-Thread MCU / canopennode-rtt</span><br></pre></td></tr></table></figure><p>Host 可以验证：</p><ul><li>Boot-up；</li><li>SDO Server；</li><li>PDO；</li><li>TIME；</li><li>EMCY；</li><li>GFC&#x2F;SRDO 等按配置启用的能力。</li></ul><h3 id="15-2-验证-MCU-NMT-Master-时角色反过来"><a href="#15-2-验证-MCU-NMT-Master-时角色反过来" class="headerlink" title="15.2 验证 MCU NMT Master 时角色反过来"></a>15.2 验证 MCU NMT Master 时角色反过来</h3><p>NMT Master 验证时，Linux 侧可以切成 Lely <code>BasicSlave</code> Node 2：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">RT-Thread MCU / CANopenNode Node 1</span><br><span class="line">        |</span><br><span class="line">        | NMT Master + Heartbeat Consumer</span><br><span class="line">        v</span><br><span class="line">Linux / Lely BasicSlave Node 2</span><br></pre></td></tr></table></figure><p>MCU 负责发送 NMT command，并用 Heartbeat Consumer 验证远端真实状态；Linux 侧则验证自己收到的命令 callback 与 reset 行为。</p><p>两边证据互补：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">MCU：远端在线 + NMT state + reset Boot-up</span><br><span class="line">Linux：收到的命令 + Lely reset callback sequence</span><br></pre></td></tr></table></figure><p>这是一个很好的测试思想：不要让同一个实现既当被测对象，又自己宣布自己通过。</p><hr><h2 id="16-一块-MCU-同时“做从站”和“做主站”到底是什么样子"><a href="#16-一块-MCU-同时“做从站”和“做主站”到底是什么样子" class="headerlink" title="16. 一块 MCU 同时“做从站”和“做主站”到底是什么样子"></a>16. 一块 MCU 同时“做从站”和“做主站”到底是什么样子</h2><p>前面把能力拆开讲了，这里把它们重新拼起来。</p><p>假设我们有一个设备：</p><ul><li>MCU 自己是 CANopen Node 1；</li><li>对上位机暴露自己的状态和参数；</li><li>同时管理两个远端执行器 Node 2、Node 3；</li><li>上电后检查两个执行器；</li><li>下发必要参数；</li><li>运行阶段同步发送控制目标；</li><li>任何一个远端掉线时进入系统降级状态。</li></ul><p>这时候 Node 1 可以同时打开：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">本地设备侧：</span><br><span class="line">NMT Slave</span><br><span class="line">Heartbeat Producer</span><br><span class="line">SDO Server</span><br><span class="line">RPDO / TPDO</span><br><span class="line">EMCY Producer</span><br><span class="line"></span><br><span class="line">控制器侧：</span><br><span class="line">NMT Master</span><br><span class="line">Heartbeat Consumer</span><br><span class="line">SDO Client</span><br><span class="line">SYNC Producer</span><br><span class="line">EMCY Consumer</span><br></pre></td></tr></table></figure><p>系统关系可以画成：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">flowchart TB</span><br><span class="line">    HOST[&quot;上位机 / Service Tool&quot;] &lt;--&gt; N1[&quot;Node 1: RT-Thread MCU&quot;]</span><br><span class="line">    N1 --&gt; LOCAL[&quot;本机应用 / I/O / 状态&quot;]</span><br><span class="line">    N1 --&gt; N2[&quot;Node 2: Remote Drive&quot;]</span><br><span class="line">    N1 --&gt; N3[&quot;Node 3: Remote I/O&quot;]</span><br><span class="line"></span><br><span class="line">    HOST -. &quot;SDO / PDO / NMT to Node 1&quot; .-&gt; N1</span><br><span class="line">    N1 -. &quot;NMT / SDO Client / SYNC / PDO&quot; .-&gt; N2</span><br><span class="line">    N1 -. &quot;NMT / SDO Client / PDO&quot; .-&gt; N3</span><br></pre></td></tr></table></figure><p>这时说 Node 1 是“从站”不完整，说它是“纯主站”也不完整。</p><p>它更像一个<strong>有自己 Object Dictionary 的 CANopen 控制器节点</strong>。</p><p>这正是 <code>canopennode-rtt</code> 保持 role-neutral 的意义。底层 wrapper 不需要根据“主站模式&#x2F;从站模式”切换一套完全不同的 runtime；角色由协议对象和应用组合得到。</p><h3 id="16-1-这对产品架构有什么好处"><a href="#16-1-这对产品架构有什么好处" class="headerlink" title="16.1 这对产品架构有什么好处"></a>16.1 这对产品架构有什么好处</h3><p>最大的好处是减少重复基础设施。</p><p>如果一个团队分别找一套 CANopen Slave Stack 和一套 CANopen Master Stack，常见结果是：</p><ul><li>两套 CAN driver adapter；</li><li>两套 timer；</li><li>两套 mutex&#x2F;critical section 规则；</li><li>两套日志；</li><li>两套配置系统；</li><li>两套对象字典概念；</li><li>两套升级维护路径；</li><li>两套协议行为差异。</li></ul><p>而 CANopenNode 本身已经允许这些角色组合，同一个 RT-Thread wrapper 继续复用，会让系统边界更统一。</p><hr><h2 id="17-三种推荐配置思路：设备节点、控制器节点、现场网关"><a href="#17-三种推荐配置思路：设备节点、控制器节点、现场网关" class="headerlink" title="17. 三种推荐配置思路：设备节点、控制器节点、现场网关"></a>17. 三种推荐配置思路：设备节点、控制器节点、现场网关</h2><p>为了更直观，下面给三个常见配置组合。这里不是完整 <code>.config</code>，而是帮助理解“哪些能力通常一起出现”。最终依赖关系仍以仓库 <code>Kconfig</code> 为准。</p><h3 id="17-1-方案-A：普通-CANopen-设备-从站"><a href="#17-1-方案-A：普通-CANopen-设备-从站" class="headerlink" title="17.1 方案 A：普通 CANopen 设备 &#x2F; 从站"></a>17.1 方案 A：普通 CANopen 设备 &#x2F; 从站</h3><p>适合：传感器、执行器、I&#x2F;O 模块、自研驱动器、通用 MCU 设备。</p><p>建议重点开启：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">PKG_USING_CANOPENNODE=y</span><br><span class="line">PKG_CANOPENNODE_USING_SDO_SERVER=y</span><br><span class="line">PKG_CANOPENNODE_USING_HB_CONS=y          # 若本节点也需要监控其他节点可保留</span><br><span class="line">PKG_CANOPENNODE_USING_SYNC=y</span><br><span class="line">PKG_CANOPENNODE_USING_PDO=y</span><br><span class="line">PKG_CANOPENNODE_RPDO=y</span><br><span class="line">PKG_CANOPENNODE_TPDO=y</span><br><span class="line">PKG_CANOPENNODE_USING_LSS_SLAVE=y</span><br></pre></td></tr></table></figure><p>产品 OD 根据设备实际对象模型生成。</p><p>如果设备只被上位机控制，不需要主动访问其他节点，则 SDO Client、NMT Master、LSS Master 可以不启用。</p><h3 id="17-2-方案-B：CANopen-控制器-简单主站"><a href="#17-2-方案-B：CANopen-控制器-简单主站" class="headerlink" title="17.2 方案 B：CANopen 控制器 &#x2F; 简单主站"></a>17.2 方案 B：CANopen 控制器 &#x2F; 简单主站</h3><p>适合：小型运动控制器、设备协调控制器、测试工装、机器人子系统控制器。</p><p>在设备侧基础能力之外重点增加：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">PKG_CANOPENNODE_NMT_MASTER=y</span><br><span class="line">PKG_CANOPENNODE_USING_SDO_CLIENT=y</span><br><span class="line">PKG_CANOPENNODE_USING_HB_CONS=y</span><br><span class="line">PKG_CANOPENNODE_SYNC_PRODUCER=y</span><br><span class="line">PKG_CANOPENNODE_EM_CONSUMER=y            # 需要远端故障监控时</span><br></pre></td></tr></table></figure><p>应用层再实现：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">Node discovery / expected node list</span><br><span class="line">        ↓</span><br><span class="line">Identity check</span><br><span class="line">        ↓</span><br><span class="line">SDO commissioning</span><br><span class="line">        ↓</span><br><span class="line">PDO mapping / parameter setup</span><br><span class="line">        ↓</span><br><span class="line">NMT START</span><br><span class="line">        ↓</span><br><span class="line">Heartbeat supervision</span><br><span class="line">        ↓</span><br><span class="line">Runtime PDO / SYNC</span><br><span class="line">        ↓</span><br><span class="line">Fault recovery</span><br></pre></td></tr></table></figure><p>这套结构已经是真正的主站&#x2F;控制器框架基础。</p><h3 id="17-3-方案-C：Commissioning-Gateway-工具节点"><a href="#17-3-方案-C：Commissioning-Gateway-工具节点" class="headerlink" title="17.3 方案 C：Commissioning &#x2F; Gateway 工具节点"></a>17.3 方案 C：Commissioning &#x2F; Gateway 工具节点</h3><p>适合：产线工装、售后工具、现场配置器、网关设备。</p><p>重点增加：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">PKG_CANOPENNODE_USING_SDO_CLIENT=y</span><br><span class="line">PKG_CANOPENNODE_NMT_MASTER=y</span><br><span class="line">PKG_CANOPENNODE_USING_LSS_MASTER=y</span><br><span class="line">PKG_CANOPENNODE_USING_GATEWAY_ASCII=y</span><br></pre></td></tr></table></figure><p>上层把 Gateway 接到 Console、UART、USB 或网络接口，就可以做一个 MCU 侧 CANopen 维护入口。</p><p>这种方式特别适合现场设备本身没有 Linux 主机，但又希望保留标准化诊断&#x2F;配置通道的产品。</p><hr><h2 id="18-哪些项目比较适合直接用-canopennode-rtt"><a href="#18-哪些项目比较适合直接用-canopennode-rtt" class="headerlink" title="18. 哪些项目比较适合直接用 canopennode-rtt"></a>18. 哪些项目比较适合直接用 canopennode-rtt</h2><p>我认为下面几类项目尤其适合。</p><h3 id="18-1-已经使用-RT-Thread，并且-BSP-CAN-驱动成熟"><a href="#18-1-已经使用-RT-Thread，并且-BSP-CAN-驱动成熟" class="headerlink" title="18.1 已经使用 RT-Thread，并且 BSP CAN 驱动成熟"></a>18.1 已经使用 RT-Thread，并且 BSP CAN 驱动成熟</h3><p>这是最直接的场景。</p><p>如果你的 CAN 已经通过 RT-Thread device framework 导出为 <code>can0</code>&#x2F;<code>can1</code>，那这个软件包可以最大程度复用已有 BSP，而不需要给每一个 STM32、NXP、GD32 或其他 MCU 单独写一套 CANopen driver。</p><h3 id="18-2-想用成熟-CANopenNode，但不想自己维护-RTOS-胶水层"><a href="#18-2-想用成熟-CANopenNode，但不想自己维护-RTOS-胶水层" class="headerlink" title="18.2 想用成熟 CANopenNode，但不想自己维护 RTOS 胶水层"></a>18.2 想用成熟 CANopenNode，但不想自己维护 RTOS 胶水层</h3><p>CANopenNode 上游值得用，但 target integration 的生命周期、线程、锁、配置和 Storage 仍然需要做。</p><p>如果项目团队的真正业务是运动控制、机器人、工业控制，而不是“研究怎样把 CANopenNode 移到 RT-Thread”，那么直接复用一个持续维护的 wrapper 更划算。</p><h3 id="18-3-设备未来可能从单纯从站升级成控制器"><a href="#18-3-设备未来可能从单纯从站升级成控制器" class="headerlink" title="18.3 设备未来可能从单纯从站升级成控制器"></a>18.3 设备未来可能从单纯从站升级成控制器</h3><p>很多项目第一版只要求：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">上位机 &lt;-&gt; MCU CANopen Device</span><br></pre></td></tr></table></figure><p>第二版就变成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">上位机 &lt;-&gt; 主控 MCU &lt;-&gt; 多个 CANopen 子节点</span><br></pre></td></tr></table></figure><p>如果一开始就使用 role-neutral 的 CANopenNode 集成层，未来加 SDO Client、NMT Master、Heartbeat Consumer 时不用整体替换协议栈。</p><h3 id="18-4-需要可裁剪，而不是“所有功能一次性打包”"><a href="#18-4-需要可裁剪，而不是“所有功能一次性打包”" class="headerlink" title="18.4 需要可裁剪，而不是“所有功能一次性打包”"></a>18.4 需要可裁剪，而不是“所有功能一次性打包”</h3><p>Kconfig + SCons 组合比较适合资源受限 MCU。</p><p>你可以根据产品需要只开：</p><ul><li>CiA 301 基础；</li><li>SDO Server；</li><li>PDO；</li><li>Storage；</li></ul><p>也可以扩展到：</p><ul><li>SDO Client；</li><li>NMT Master；</li><li>LSS；</li><li>Gateway；</li><li>GFC&#x2F;SRDO；</li><li>Event-driven mainline。</li></ul><p>资源和功能之间的关系比较透明。</p><h3 id="18-5-希望-Host-自动测试与-MCU-代码分开"><a href="#18-5-希望-Host-自动测试与-MCU-代码分开" class="headerlink" title="18.5 希望 Host 自动测试与 MCU 代码分开"></a>18.5 希望 Host 自动测试与 MCU 代码分开</h3><p>有 Linux + SocketCAN&#x2F;Lely 测试条件的团队，很适合用 <code>canopen-slave-tester</code> 做 Host 自动协议验证，再让 <code>canopennode-rtt</code> 只承担 MCU 侧被测实现。</p><p>这种结构比把所有测试命令都写成 MSH 手工操作更容易持续集成。</p><hr><h2 id="19-和“自己移植-CANopenNode”相比，实际省掉的工作不只是一个-CAN-driver"><a href="#19-和“自己移植-CANopenNode”相比，实际省掉的工作不只是一个-CAN-driver" class="headerlink" title="19. 和“自己移植 CANopenNode”相比，实际省掉的工作不只是一个 CAN driver"></a>19. 和“自己移植 CANopenNode”相比，实际省掉的工作不只是一个 CAN driver</h2><p>如果从零做一套 RT-Thread port，第一阶段可能只需要几天：写 driver，起线程，看到 Boot-up。</p><p>真正耗时间的是第二阶段以后。</p><p>例如：</p><h3 id="19-1-第一次-Communication-Reset"><a href="#19-1-第一次-Communication-Reset" class="headerlink" title="19.1 第一次 Communication Reset"></a>19.1 第一次 Communication Reset</h3><p>你会发现：</p><ul><li>旧 <code>CO_t</code> 怎么释放；</li><li>CAN normal mode 怎么退出；</li><li>RX 线程是不是还在读旧对象；</li><li>callback 需要不要重绑；</li><li>Storage 与 LSS 参数在哪个时机加载；</li><li>realtime timer 是否需要停；</li><li>应用持有的指针是不是已经失效。</li></ul><h3 id="19-2-第一次加-SDO-Client"><a href="#19-2-第一次加-SDO-Client" class="headerlink" title="19.2 第一次加 SDO Client"></a>19.2 第一次加 SDO Client</h3><p>你会发现：</p><ul><li>FIFO&#x2F;segment&#x2F;block 配置要一起跟上；</li><li>mainline 里怎样 non-blocking process；</li><li>同节点 local transfer 是否打开；</li><li>timeout 与 application state 怎么连接；</li><li>回调线程上下文怎么约束。</li></ul><h3 id="19-3-第一次加-Gateway"><a href="#19-3-第一次加-Gateway" class="headerlink" title="19.3 第一次加 Gateway"></a>19.3 第一次加 Gateway</h3><p>你会发现：</p><ul><li>命令输入与响应输出怎样接；</li><li>Console 是否与 Gateway 抢 UART；</li><li>callback 能不能直接打印；</li><li>Gateway 与 mainline 谁驱动谁；</li><li>多线程怎样避免并发访问状态机。</li></ul><h3 id="19-4-第一次做参数持久化"><a href="#19-4-第一次做参数持久化" class="headerlink" title="19.4 第一次做参数持久化"></a>19.4 第一次做参数持久化</h3><p>你会发现：</p><ul><li>0x1010&#x2F;0x1011 与产品自己的参数系统怎样对接；</li><li>什么时候写 Flash；</li><li>reset 后怎样加载；</li><li>LSS Node-ID&#x2F;bitrate 什么时候生效；</li><li>掉电中断怎么办。</li></ul><p>这些才是一个协议栈 wrapper 真正长期维护的成本。</p><p><code>canopennode-rtt</code> 当前已经把其中相当一部分问题显式化到代码、Kconfig 和文档里，所以它的价值不能只用“有没有 CANopen 协议代码”来衡量。</p><hr><h2 id="20-和-RTT-CanFestival-相比怎么选：两者都能跑-CANopen，但技术路线和适用阶段已经明显不同"><a href="#20-和-RTT-CanFestival-相比怎么选：两者都能跑-CANopen，但技术路线和适用阶段已经明显不同" class="headerlink" title="20. 和 RTT-CanFestival 相比怎么选：两者都能跑 CANopen，但技术路线和适用阶段已经明显不同"></a>20. 和 RTT-CanFestival 相比怎么选：两者都能跑 CANopen，但技术路线和适用阶段已经明显不同</h2><p>提到 RT-Thread 上的 CANopen，很多老用户第一时间想到的其实不是 <code>canopennode-rtt</code>，而是 RT-Thread 软件包生态里已经存在多年的 CanFestival 移植。</p><p>这里的对比对象特指：</p><ul><li>RT-Thread 软件包 <code>CanFestival</code>；</li><li>对应仓库：<a href="https://github.com/gbcwbz/canfestival-rtt">https://github.com/gbcwbz/canfestival-rtt</a>；</li><li>该仓库 README 明确说明，它是从 CanFestival-3 项目移植到 RT-Thread 的实现。</li></ul><p>所以在推荐 <code>canopennode-rtt</code> 时，有一个问题很难绕开：<strong>RT-Thread 已经有 CanFestival 了，为什么还要再做一个 CANopenNode 的 RT-Thread 集成？</strong></p><p>我的结论是：两者并不是“同一个协议栈换了一个名字”，也不应该简单理解成“新的就一定比旧的好”。它们代表的是两套不同的 CANopen 协议栈、不同的 RT-Thread 集成方式，以及不同的工程取舍。</p><p>如果是一个已经稳定量产、基于 CanFestival 跑了很多年的项目，没有必要为了追新而强制迁移；但如果现在从零开始做新的 RT-Thread CANopen 产品，尤其希望后续继续扩展主站、Gateway、Storage、自动测试、现代 Object Dictionary 工作流或者持续跟进上游协议栈，那么我会更倾向从 <code>canopennode-rtt</code> 开始。</p><h3 id="20-1-最根本的区别：底层不是同一套-CANopen-协议栈"><a href="#20-1-最根本的区别：底层不是同一套-CANopen-协议栈" class="headerlink" title="20.1 最根本的区别：底层不是同一套 CANopen 协议栈"></a>20.1 最根本的区别：底层不是同一套 CANopen 协议栈</h3><p><code>canopennode-rtt</code> 的协议核心来自：</p><p><a href="https://github.com/CANopenNode/CANopenNode">https://github.com/CANopenNode/CANopenNode</a></p><p>仓库本身主要维护 RT-Thread integration&#x2F;runtime，协议核心通过 git submodule 固定到明确 revision。也就是说，RT-Thread wrapper 与 CANopenNode upstream 是明显分层的。</p><p>RTT-CanFestival 则来自另一条技术路线。<code>gbcwbz/canfestival-rtt</code> README 明确写着：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Forked from the CanFestival-3 project</span><br></pre></td></tr></table></figure><p>它把 CanFestival 的协议实现和 RT-Thread 适配一起保存在自己的 <code>src/</code>、<code>inc/</code> 中。当前源码目录可以直接看到 <code>nmtMaster.c</code>、<code>nmtSlave.c</code>、<code>sdo.c</code>、<code>pdo.c</code>、<code>emcy.c</code>、<code>sync.c</code>、<code>lifegrd.c</code>、<code>lss.c</code>、<code>can_rtthread.c</code>、<code>timer_rtthread.c</code> 等文件。</p><p>因此两者最本质的关系其实是：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    subgraph CNRTT[&quot;canopennode-rtt&quot;]</span><br><span class="line">        A1[&quot;产品应用&quot;] --&gt; A2[&quot;RT-Thread Wrapper / Runtime&quot;]</span><br><span class="line">        A2 --&gt; A3[&quot;CANopenNode V4 git submodule&quot;]</span><br><span class="line">        A2 --&gt; A4[&quot;RT-Thread CAN / Storage / Timer&quot;]</span><br><span class="line">    end</span><br><span class="line"></span><br><span class="line">    subgraph CFRTT[&quot;RTT-CanFestival&quot;]</span><br><span class="line">        B1[&quot;产品应用 / Master402 示例&quot;] --&gt; B2[&quot;CanFestival 协议源码&quot;]</span><br><span class="line">        B2 --&gt; B3[&quot;can_rtthread.c&quot;]</span><br><span class="line">        B2 --&gt; B4[&quot;timer_rtthread.c&quot;]</span><br><span class="line">        B3 --&gt; B5[&quot;RT-Thread CAN&quot;]</span><br><span class="line">        B4 --&gt; B6[&quot;RT-Thread hwtimer&quot;]</span><br><span class="line">    end</span><br></pre></td></tr></table></figure><p><code>canopennode-rtt</code> 更像“现代 CANopenNode 上游 + RT-Thread 适配层”；RTT-CanFestival 更像“把一套 CanFestival 协议栈整体移植并固化到 RT-Thread 软件包中”。这决定了后面很多差异。</p><h3 id="20-2-先用一张表把主要差异摆出来"><a href="#20-2-先用一张表把主要差异摆出来" class="headerlink" title="20.2 先用一张表把主要差异摆出来"></a>20.2 先用一张表把主要差异摆出来</h3><table><thead><tr><th>对比维度</th><th><code>canopennode-rtt</code></th><th>RTT-CanFestival &#x2F; <code>canfestival-rtt</code></th></tr></thead><tbody><tr><td>CANopen 核心</td><td>CANopenNode V4，上游通过 git submodule 固定 revision</td><td>CanFestival-3 派生代码直接位于仓库 <code>src/</code> &#x2F; <code>inc/</code></td></tr><tr><td>RT-Thread 定位</td><td>integration&#x2F;runtime wrapper，尽量不修改 upstream core</td><td>协议栈与 RT-Thread port 一体化维护</td></tr><tr><td>从站能力</td><td>NMT Slave、SDO Server、PDO、Heartbeat、LSS Slave 等按 Kconfig 裁剪</td><td>有 NMT Slave、SDO、PDO、Heartbeat&#x2F;Node Guarding 等传统 CanFestival 能力</td></tr><tr><td>主站能力</td><td>NMT Master、SDO Client、Heartbeat Consumer、SYNC Producer、LSS Master、Gateway 等可组合</td><td>源码包含 NMT Master&#x2F;SDO 等主站基础，并提供可运行的 <code>Master402</code> 示例</td></tr><tr><td>CiA 402</td><td>Device&#x2F;Controller 正在规划和分阶段实现，当前不能宣称完整支持</td><td>当前仓库已经自带 CiA 402 Master 示例，可控制 Node-ID 2 伺服的 Profile Position 场景</td></tr><tr><td>定时模型</td><td>realtime 使用 RT-Thread software timer 唤醒；时间测量默认 tick fallback，可选专用 1 MHz High-Res Timer</td><td><code>timer_rtthread.c</code> 直接依赖 RT-Thread hwtimer，配置 1 MHz one-shot，并由 <code>cf_timer</code> 线程执行 <code>TimeDispatch()</code></td></tr><tr><td>CAN RX</td><td>独立 <code>co_rx</code> helper，支持可选 RT-Thread CAN HDR hardware filter，失败可回退软件分发</td><td><code>cf_recv</code> 线程 + semaphore，当前 port 中收到帧后在全局 mutex 下调用 <code>canDispatch()</code></td></tr><tr><td>Mainline &#x2F; realtime 分工</td><td><code>co_main</code> 与 <code>co_rt</code> 明确分离，SYNC&#x2F;RPDO&#x2F;TPDO&#x2F;SRDO 走 realtime path</td><td>主要由 CAN dispatch 与 CanFestival timer dispatch 驱动，没有 CANopenNode 式 mainline&#x2F;realtime wrapper 分层</td></tr><tr><td>多实例设计</td><td><code>CANopenNodeRTT</code> 为显式实例；CAN binding count 可配置；tick 时间源支持多实例，高精度 Timer backend 当前限制单实例</td><td>当前 port 使用全局 <code>candev</code>、<code>OD_Data</code>、<code>canfstvl_mutex</code> 等状态，结构上更接近单实例实现</td></tr><tr><td>构建裁剪</td><td>Kconfig 映射 <code>CO_CONFIG_*</code>，SCons 按功能选择 CANopenNode 源文件</td><td>当前 <code>src/SConscript</code> 在 CAN + HWTIMER 条件下编译固定核心文件集合，示例通过单独开关加入</td></tr><tr><td>Object Dictionary</td><td>明确使用 XDD&#x2F;生成 OD 工作流，Demo OD 与产品 OD 边界清晰</td><td><code>Master402</code> 自带 <code>.od</code>、生成后的 <code>_od.c/.h</code>，更偏 CanFestival 原有 OD 工作流和固定示例</td></tr><tr><td>Storage</td><td>已提供 DFS、AT24CXX EEPROM、User backend，并接 0x1010&#x2F;0x1011</td><td>当前 RT-Thread port 没有同层级的通用 RT-Thread Storage backend abstraction，需要项目自行扩展</td></tr><tr><td>扩展能力</td><td>Kconfig 已覆盖 LSS、CiA 309-3 Gateway、GFC、SRDO、TIME 等多个能力组</td><td>仓库保留传统 CanFestival 功能；当前构建脚本和 RT-Thread wrapper 暴露的高级扩展相对少</td></tr><tr><td>测试体系</td><td>MCU demo&#x2F;test OD + 独立 Linux&#x2F;Lely Host 测试仓库 + CI&#x2F;文档</td><td>优势主要是已有长期使用历史和 <code>Master402</code> 示例，当前仓库自动化验证体系相对简单</td></tr><tr><td>维护状态（2026-09-01 基线）</td><td>本仓库当天仍有提交，上游 CANopenNode 2026 年仍在更新</td><td>GitHub repository metadata 显示最近一次 push 为 2022-11-02，但历史用户量和社区积累更大</td></tr><tr><td>协议核心许可证</td><td>CANopenNode upstream 为 Apache-2.0；wrapper 自身仍应按仓库实际许可文件确认</td><td>仓库明确声明 LGPL-2.1</td></tr></tbody></table><p>这张表里最容易误解的一项是“主站能力”：<strong>RTT-CanFestival 不是只能做从站，<code>canopennode-rtt</code> 也不是因为有 NMT Master 就天然比它更像主站。</strong> 两边都有主站基础能力，真正的差异在于主站能力怎样被组织、维护和继续扩展。</p><h3 id="20-3-RTT-CanFestival-的优势：老、直接，而且现在就有一个能参考的-CiA-402-主站例子"><a href="#20-3-RTT-CanFestival-的优势：老、直接，而且现在就有一个能参考的-CiA-402-主站例子" class="headerlink" title="20.3 RTT-CanFestival 的优势：老、直接，而且现在就有一个能参考的 CiA 402 主站例子"></a>20.3 RTT-CanFestival 的优势：老、直接，而且现在就有一个能参考的 CiA 402 主站例子</h3><p>先说 RTT-CanFestival 的优势，因为这些优势是真实存在的。</p><p>第一，它进入 RT-Thread 生态更早，历史使用时间明显更长。<code>gbcwbz/canfestival-rtt</code> 仓库创建于 2018 年，长期以来就是很多 RT-Thread 用户搜索 CANopen 时最容易遇到的现成方案。对于已经基于它量产的项目，这种“代码已经在自己的硬件上跑了很多年”的工程价值，远比 GitHub 上哪个仓库更新更重要。</p><p>第二，它的接入方式很直接。README 给出的依赖就是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">RT-Thread 3.0+</span><br><span class="line">CAN driver</span><br><span class="line">hwtimer driver</span><br></pre></td></tr></table></figure><p>配置 CAN device name、timer device name 和两个线程优先级，就可以进入 CanFestival 的运行模型。对于功能单一、硬件固定、团队已经熟悉 CanFestival API 的产品，这种简单直接反而是一种优势。</p><p>第三，也是当前它相对 <code>canopennode-rtt</code> 最明显的现实优势：<strong>它现在就带有 <code>examples/master402/</code>。</strong></p><p>该示例 README 明确说明：</p><ul><li>它是一个 CiA 402 主站示例；</li><li>可以控制伺服驱动器；</li><li>默认伺服 Node-ID 为 2；</li><li>默认使用 Profile Position 模式；</li><li>可以通过 FinSH 测试 servo on 和相对移动。</li></ul><p>因此，如果你的目标非常具体：</p><blockquote><p>“我今天就是想在 RT-Thread MCU 上通过 CANopen 控制一台 CiA 402 伺服，先把 Profile Position 跑起来。”</p></blockquote><p>那么 RTT-CanFestival 的 <code>Master402</code> 确实比当前 <code>canopennode-rtt</code> 更接近一个现成起点。<code>canopennode-rtt</code> 现在已有 NMT Master、SDO Client、SYNC、PDO 等 Controller 所需通信能力，但 CiA 402 Controller profile 本身仍在 Roadmap 中，这一点不能回避。</p><p>第四，RTT-CanFestival 没有 git submodule 这一层管理成本。协议栈源码就在包里，对于一些封闭、长期冻结、不希望处理 upstream revision 的项目，这种“拿到什么就编什么”的方式也更容易管理。</p><h3 id="20-4-canopennode-rtt-的优势：更适合把-CANopen-当成长期维护的产品基础设施"><a href="#20-4-canopennode-rtt-的优势：更适合把-CANopen-当成长期维护的产品基础设施" class="headerlink" title="20.4 canopennode-rtt 的优势：更适合把 CANopen 当成长期维护的产品基础设施"></a>20.4 <code>canopennode-rtt</code> 的优势：更适合把 CANopen 当成长期维护的产品基础设施</h3><p>如果项目不是“一次把某个 Demo 跑起来”就结束，而是希望 CANopen 在未来几年持续增加功能，我认为 <code>canopennode-rtt</code> 的优势会更明显。</p><p>首先是上游关系更清晰。CANopenNode core 保持为独立 git submodule，RT-Thread port 不需要把整个协议核心 fork 进来修改。升级协议栈时，可以明确看到 submodule revision 从哪里变到哪里，也更容易把 wrapper 的兼容性问题和 upstream 行为变化分开定位。</p><p>其次是配置粒度更细。<code>canopennode-rtt</code> 并不是简单地在 <code>RT_USING_CAN</code> 打开后把所有协议文件一起编进去，而是把 NMT、SDO Server、SDO Client、Heartbeat Consumer、TIME、SYNC、PDO、Storage、LSS、Gateway、GFC、SRDO 等能力拆成 Kconfig 选项，再由 SCons 根据实际配置决定源文件集合。</p><p>对于资源受限 MCU，这种方式更容易回答两个问题：</p><ol><li>我到底编进去了什么？</li><li>我为了某个主站功能又引入了哪些依赖？</li></ol><p>第三是运行时边界更适合复杂 RTOS 项目。CAN RX、realtime、mainline 分开之后，哪个上下文可以调用什么 API、SYNC&#x2F;RPDO&#x2F;TPDO 在哪里执行、Communication Reset 如何重建对象、Gateway&#x2F;Storage callback 在哪里重新绑定，都会比“协议栈函数能跑起来”更加显式。</p><p>第四是多实例方向。当前 RTT-CanFestival port 的 <code>can_rtthread.c</code> 使用：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">static rt_device_t candev;</span><br><span class="line">static CO_Data *OD_Data;</span><br><span class="line">static rt_mutex_t canfstvl_mutex;</span><br></pre></td></tr></table></figure><p>这意味着当前 RT-Thread port 本身是明显围绕单个全局 CANopen 上下文组织的。<code>canopennode-rtt</code> 则把运行时放在 <code>CANopenNodeRTT</code> 实例中，并提供 CAN binding count。需要注意：开启专用 High-Res Timer 时目前仍有 package-wide 单 Timer、单实例限制；但关闭 High-Res、使用 tick fallback 时，wrapper 本身保留多实例设计空间。</p><p>第五是 RT-Thread 周边集成更完整。Storage、ULOG、PIN LED、CAN HDR filter、event-driven mainline、High-Resolution Time、Host 测试仓库等都已经被当成软件包的一部分来处理，而不是留给每个产品再次造胶水层。</p><h3 id="20-5-定时架构的差异尤其值得看：两边都需要“时间”，但用法完全不同"><a href="#20-5-定时架构的差异尤其值得看：两边都需要“时间”，但用法完全不同" class="headerlink" title="20.5 定时架构的差异尤其值得看：两边都需要“时间”，但用法完全不同"></a>20.5 定时架构的差异尤其值得看：两边都需要“时间”，但用法完全不同</h3><p>RTT-CanFestival 对 hwtimer 是直接依赖。当前 <code>src/SConscript</code> 的 <code>DefineGroup()</code> 同时依赖：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">RT_USING_CAN</span><br><span class="line">RT_USING_HWTIMER</span><br></pre></td></tr></table></figure><p><code>timer_rtthread.c</code> 中会：</p><ol><li>找到配置的 RT-Thread hwtimer；</li><li>把频率设置为 1 MHz；</li><li>设置为 one-shot；</li><li>timer timeout 后释放 semaphore；</li><li><code>cf_timer</code> 线程被唤醒；</li><li>在 CanFestival mutex 下调用 <code>TimeDispatch()</code>。</li></ol><p>这是 CanFestival 原有 timer queue 与 RT-Thread hwtimer 的直接适配，逻辑比较清楚，但代价是<strong>目标 BSP 必须提供可用 hwtimer，而且这个硬件 Timer 是协议栈运行的基础依赖</strong>。</p><p><code>canopennode-rtt</code> 则把“realtime worker 如何被唤醒”和“CANopenNode 每次 process 实际经过多少微秒”拆成两个问题：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">RT-Thread software timer</span><br><span class="line">        -&gt; rtSem</span><br><span class="line">        -&gt; co_rt</span><br><span class="line"></span><br><span class="line">时间测量</span><br><span class="line">        -&gt; RT-Thread tick fallback</span><br><span class="line">        或</span><br><span class="line">        -&gt; 专用 1 MHz High-Res hardware timer</span><br></pre></td></tr></table></figure><p>也就是说，不开 High-Res Timer 时，CANopenNode 仍然可以运行，只是 <code>timeDifference_us</code> 的精度受 RT-Thread tick 限制；真正需要更精确微秒时间时，再额外占用一个符合要求的硬件 Timer。</p><p>这两种设计不存在绝对对错。</p><ul><li>RTT-CanFestival 的模型更直接，CanFestival timer 本身就由 hwtimer 驱动；</li><li><code>canopennode-rtt</code> 的模型更强调“基本可运行能力”和“高精度时间能力”解耦，对 BSP Timer 资源紧张的项目更灵活。</li></ul><h3 id="20-6-CiA-402-是目前两者最容易被拿来比较、也最容易说过头的地方"><a href="#20-6-CiA-402-是目前两者最容易被拿来比较、也最容易说过头的地方" class="headerlink" title="20.6 CiA 402 是目前两者最容易被拿来比较、也最容易说过头的地方"></a>20.6 CiA 402 是目前两者最容易被拿来比较、也最容易说过头的地方</h3><p>如果只看目录，RTT-CanFestival 有 <code>Master402</code>，而 <code>canopennode-rtt</code> 的 CiA 402 还在开发，很容易直接得出：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">CanFestival 支持 402</span><br><span class="line">CANopenNode RT-Thread 不支持 402</span><br></pre></td></tr></table></figure><p>这个判断太粗。</p><p>更准确的说法是：</p><p><strong>RTT-CanFestival 当前已经交付了一个具体的 CiA 402 Master 应用示例；<code>canopennode-rtt</code> 当前已经具备构建 Controller 所需的大量 CANopen 通信能力，但尚未交付规划中的通用 CiA 402 Device&#x2F;Controller profile。</strong></p><p>CanFestival 的 <code>Master402</code> 目前公开说明的目标很具体：Node-ID 2、Profile Position、servo on、相对移动。它很有参考价值，但不能仅凭这个示例就把它扩大解释成“已经提供完整、通用、经过一致性验证的 CiA 402 Controller framework”。</p><p>反过来，<code>canopennode-rtt</code> 也不能因为 NMT Master、SDO Client、PDO、SYNC 都已经具备，就提前宣称 CiA 402 Controller 已经完成。通信能力是 profile 的地基，不等于 profile 状态机本身。</p><p>所以当前选型可以非常实际：</p><ul><li><strong>现在马上要做一个简单 402 主站控制伺服</strong>：RTT-CanFestival 的 <code>Master402</code> 参考价值更直接；</li><li><strong>现在主要做 CANopen Device，后续准备演进多轴 402 Device&#x2F;Controller</strong>：<code>canopennode-rtt</code> 的长期架构更值得关注；</li><li><strong>要求严格 CiA 402 conformance、复杂模式、多厂商互操作</strong>：两边都不能只看 Demo 名字，仍需要按目标规范版本建立对象、状态机和互操作验证矩阵。</li></ul><h3 id="20-7-RTT-CanFestival-的几个现实短板"><a href="#20-7-RTT-CanFestival-的几个现实短板" class="headerlink" title="20.7 RTT-CanFestival 的几个现实短板"></a>20.7 RTT-CanFestival 的几个现实短板</h3><p>第一，维护活跃度需要纳入新项目选型。以本文 2026-09-01 的 GitHub 元数据为基线，<code>gbcwbz/canfestival-rtt</code> 最近一次代码 push 是 2022-11-02。仓库并没有被 archived，而且已有用户和 issue 仍然具有参考价值，但对一个预计维护五年甚至更久的新产品来说，协议栈和 port 后续由谁维护必须提前考虑。</p><p>第二，当前 port 的全局状态比较重。<code>candev</code>、<code>OD_Data</code>、mutex 都是 package-level static，全局资源模型在单 CANopen 实例上简单，但当产品需要多个 CAN 控制器、多个逻辑实例或者更复杂生命周期管理时，扩展成本会明显增加。</p><p>第三，构建粒度较粗。当前 <code>src/SConscript</code> 在 CAN + HWTIMER 条件满足后固定加入 NMT Master、NMT Slave、SDO、PDO、EMCY、SYNC、Timer 等核心文件；虽然代码中还有 <code>lss.c</code>、<code>dcf.c</code> 等文件，但它们并不在当前这组固定 source list 中。相比之下，<code>canopennode-rtt</code> 把更多能力直接做成 Kconfig + SCons 的显式依赖关系。</p><p>第四，RT-Thread 平台能力集成相对少。它解决了 CAN 与 hwtimer 这两个最核心的 port 点，但没有像 <code>canopennode-rtt</code> 一样继续向 Storage backend、CAN hardware filter fallback、event-driven mainline、独立 Host tester 等方向扩展。</p><p>这不代表旧实现“不能做”这些功能，而是说需要产品自己继续维护更多适配代码。</p><h3 id="20-8-canopennode-rtt-也有自己的短板，不应该把推荐文章写成单方面碾压"><a href="#20-8-canopennode-rtt-也有自己的短板，不应该把推荐文章写成单方面碾压" class="headerlink" title="20.8 canopennode-rtt 也有自己的短板，不应该把推荐文章写成单方面碾压"></a>20.8 <code>canopennode-rtt</code> 也有自己的短板，不应该把推荐文章写成单方面碾压</h3><p><code>canopennode-rtt</code> 最大的短板首先就是它很新。仓库 2026 年才建立，虽然目前开发活跃、文档和测试补得比较快，但现场项目数量、长期运行历史、第三方使用反馈显然不可能和一个 2018 年就进入 RT-Thread 社区的方案相比。</p><p>第二，它现在没有 RTT-CanFestival 那样可以直接拿来控制伺服的成熟 <code>Master402</code> 示例。CiA 402 Device&#x2F;Controller 仍然是 Roadmap，当前阶段如果项目唯一目标就是快速控制已有 402 驱动器，这会直接影响选型。</p><p>第三，git submodule 虽然有利于锁定上游 revision，也会增加仓库拉取、离线镜像、版本升级和 CI 缓存管理的复杂度。团队如果对子模块使用不熟，第一次构建最常见的问题就是只 clone 了 wrapper，没有拉 <code>CANopenNode/</code>。</p><p>第四，High-Res Timer 当前仍有单实例限制，而且依赖用户&#x2F;BSP 自己确认物理 Timer 真的是 32-bit、1 MHz、up-counting 并且未被其他模块占用。这个能力比“强制要求 hwtimer”更灵活，但也意味着高精度配置的责任被显式交给了产品集成者。</p><p>第五，许可证信息也需要产品团队认真处理。CANopenNode upstream 当前是 Apache-2.0；RTT-CanFestival 仓库则明确声明 LGPL-2.1。<code>canopennode-rtt</code> wrapper 自身的仓库级许可证应以实际仓库文件为准，商业产品在发布前不应该仅根据 upstream CANopenNode 的 Apache-2.0 就推断整个 wrapper 的许可义务。</p><h3 id="20-9-两套-API-和-OD-模型不兼容，迁移不是“替换一个目录”"><a href="#20-9-两套-API-和-OD-模型不兼容，迁移不是“替换一个目录”" class="headerlink" title="20.9 两套 API 和 OD 模型不兼容，迁移不是“替换一个目录”"></a>20.9 两套 API 和 OD 模型不兼容，迁移不是“替换一个目录”</h3><p>如果你现在已经在用 RTT-CanFestival，又考虑迁移到 <code>canopennode-rtt</code>，需要先接受一个事实：这不是 HAL 层兼容替换，而是协议栈迁移。</p><p>CanFestival 常见对象和接口围绕：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">CO_Data</span><br><span class="line">canDispatch()</span><br><span class="line">setState()</span><br><span class="line">writeNetworkDict()</span><br><span class="line">readNetworkDict()</span><br></pre></td></tr></table></figure><p>CANopenNode 则围绕：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">CO_t</span><br><span class="line">OD_t / OD_entry_t</span><br><span class="line">CO_process()</span><br><span class="line">CO_NMT_sendCommand()</span><br><span class="line">CO_SDOclient*()</span><br><span class="line">CO_process_RPDO()</span><br><span class="line">CO_process_TPDO()</span><br></pre></td></tr></table></figure><p>同时还会涉及：</p><ul><li>Object Dictionary 生成方式变化；</li><li>应用 callback 重新绑定；</li><li>SDO Client 状态机重写；</li><li>NMT Master API 重写；</li><li>PDO mapping 与 OD access 重新检查；</li><li>timer&#x2F;lifecycle 模型变化；</li><li>reset 和 storage 行为重新验证。</li></ul><p>所以对于已经稳定的 CanFestival 产品，我不建议因为“CANopenNode 更新”就直接重写。只有当现有项目真正受到维护、扩展、协议能力、资源组织或者测试体系限制时，迁移才有足够收益。</p><h3 id="20-10-最后给一个实际选型建议"><a href="#20-10-最后给一个实际选型建议" class="headerlink" title="20.10 最后给一个实际选型建议"></a>20.10 最后给一个实际选型建议</h3><p>如果现在让我按项目类型选择，我会这样判断：</p><table><thead><tr><th>项目情况</th><th>更建议的起点</th><th>原因</th></tr></thead><tbody><tr><td>新建 RT-Thread CANopen Device &#x2F; I&#x2F;O &#x2F; Sensor &#x2F; Actuator</td><td><code>canopennode-rtt</code></td><td>上游活跃、配置裁剪清晰、OD&#x2F;Storage&#x2F;测试链路更适合继续产品化</td></tr><tr><td>新建 MCU CANopen Controller，但不依赖现成 402 profile</td><td><code>canopennode-rtt</code></td><td>NMT Master、SDO Client、Heartbeat Consumer、SYNC Producer、LSS Master 等基础已经齐全</td></tr><tr><td>今天就要快速参考一个 RT-Thread CiA 402 PP 主站</td><td>RTT-CanFestival</td><td><code>Master402</code> 已经存在，目标场景明确，能直接参考 servo on&#x2F;relative move</td></tr><tr><td>已经量产并稳定运行的 CanFestival 项目</td><td>优先继续 CanFestival</td><td>不为技术栈“新旧”制造无业务收益的迁移风险</td></tr><tr><td>未来需要 Gateway、Storage、多协议能力裁剪、自动 Host 测试</td><td><code>canopennode-rtt</code></td><td>wrapper 已经把这些能力当成长期基础设施设计</td></tr><tr><td>需要多个 CANopen 实例</td><td>优先评估 <code>canopennode-rtt</code></td><td>显式实例模型更合适；但开启 High-Res Timer 时仍需注意当前单实例限制</td></tr><tr><td>对 LGPL 有明确合规流程、且已有 CanFestival 资产</td><td>CanFestival 仍可合理使用</td><td>许可证义务已知，既有资产与验证证据可能比重新选栈更有价值</td></tr></tbody></table><p>所以我推荐 <code>canopennode-rtt</code>，并不是因为 RTT-CanFestival “不能用”。恰恰相反，CanFestival 的 RT-Thread port 对很多早期项目起到了非常重要的作用，而且今天仍然保留一个对 CiA 402 主站很有价值的例子。</p><p>真正的区别在于：<strong>如果把 CANopen 看成一个未来还会继续扩展、维护、测试和升级的产品基础设施，CANopenNode + role-neutral RT-Thread wrapper 这条路线更符合我对新项目的长期预期；如果面对的是一个已经稳定的 CanFestival 存量项目，或者当前唯一目标就是快速参考现成的 402 主站控制流程，那么 RTT-CanFestival 仍然有它非常明确的优势。</strong></p><hr><h2 id="21-关于-CiA-402-DS402：这是后续重点方向，但当前文章不把规划写成已完成功能"><a href="#21-关于-CiA-402-DS402：这是后续重点方向，但当前文章不把规划写成已完成功能" class="headerlink" title="21. 关于 CiA 402 &#x2F; DS402：这是后续重点方向，但当前文章不把规划写成已完成功能"></a>21. 关于 CiA 402 &#x2F; DS402：这是后续重点方向，但当前文章不把规划写成已完成功能</h2><p>如果这个仓库未来面向伺服、步进驱动、机器人关节或者运动控制，CiA 402 几乎一定是大家最关注的扩展之一。</p><p>仓库目前已经有公开规划 Issue：</p><p><a href="https://github.com/wdfk-prog/canopennode-rtt/issues/9">https://github.com/wdfk-prog/canopennode-rtt/issues/9</a></p><p>Issue 标题为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">[Plan] CiA402 Multi-axis Device Implementation</span><br></pre></td></tr></table></figure><p>这里需要特别强调状态：</p><p><strong>截至本文基线，CiA 402&#x2F;DS402 属于正在规划和分阶段实施的额外 profile 能力，不应该把它宣传成当前 <code>master</code> 已经提供完整 CiA 402 Device&#x2F;Controller。</strong></p><h3 id="21-1-当前规划最重要的架构决策"><a href="#21-1-当前规划最重要的架构决策" class="headerlink" title="21.1 当前规划最重要的架构决策"></a>21.1 当前规划最重要的架构决策</h3><p>Issue 中已经把长期角色边界写得比较清楚：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">CANopenNode / RT-Thread communication layer</span><br><span class="line">                |</span><br><span class="line">                +-- role-neutral</span><br><span class="line">                |</span><br><span class="line">                +-- CiA402 common</span><br><span class="line">                +-- CiA402 device</span><br><span class="line">                +-- CiA402 controller (future)</span><br></pre></td></tr></table></figure><p>也就是说，后续不会把 <code>canopennode-rtt</code> 强行定义成“只做 CiA 402 从站”。</p><p>规划方向是：</p><ul><li>communication layer 保持角色中立；</li><li>CiA 402 拆成 <code>common + device + controller</code>；</li><li>当前阶段优先实现 <code>common + device</code>；</li><li>Controller 后续独立实施；</li><li>不修改 CANopenNode upstream&#x2F;core；</li><li>Device 与 Controller 不共用一个混乱的 runtime 状态机。</li></ul><p>这和本文前面强调的软件包定位是一致的。</p><h3 id="21-2-Device-与-Controller-是两个不同问题"><a href="#21-2-Device-与-Controller-是两个不同问题" class="headerlink" title="21.2 Device 与 Controller 是两个不同问题"></a>21.2 Device 与 Controller 是两个不同问题</h3><p>CiA 402 Device 的含义是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">远端 Controller</span><br><span class="line">      |</span><br><span class="line">Controlword / Target</span><br><span class="line">      |</span><br><span class="line">本机 Object Dictionary</span><br><span class="line">      |</span><br><span class="line">PDS FSA / Operation Mode</span><br><span class="line">      |</span><br><span class="line">本机 Motor / Drive</span><br><span class="line">      |</span><br><span class="line">Statusword / Actual Value</span><br></pre></td></tr></table></figure><p>也就是本 MCU 自己就是驱动器。</p><p>而 CiA 402 Controller 是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">远端 Drive Statusword</span><br><span class="line">      |</span><br><span class="line">本机 Controller Logic</span><br><span class="line">      |</span><br><span class="line">生成 Controlword / SDO / PDO</span><br><span class="line">      |</span><br><span class="line">控制远端 Drive</span><br></pre></td></tr></table></figure><p>也就是本 MCU 控制其他 CiA 402 驱动器。</p><p>这两者可以共享 state enum、mode enum、Controlword&#x2F;Statusword 定义等无角色公共逻辑，但不能把本地电机 runtime 与远端 drive control runtime 混成一个对象。</p><h3 id="21-3-多轴-Device-规划"><a href="#21-3-多轴-Device-规划" class="headerlink" title="21.3 多轴 Device 规划"></a>21.3 多轴 Device 规划</h3><p>当前 Issue 还规划了单 Node-ID 多轴 Device：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">CAN Bus</span><br><span class="line">   |</span><br><span class="line">Node-ID = 1</span><br><span class="line">   |</span><br><span class="line">CANopenNode</span><br><span class="line">   |</span><br><span class="line">CiA402 Device</span><br><span class="line">   +-- Axis 0 -&gt; Motor 0</span><br><span class="line">   +-- Axis 1 -&gt; Motor 1</span><br><span class="line">   +-- Axis 2 -&gt; Motor 2</span><br></pre></td></tr></table></figure><p>也就是说，一个 MCU&#x2F;一个 CANopen Node-ID 可以管理多个逻辑轴，而不是为了每个电机都创建一个 <code>CO_t</code> 和一个 Node-ID。</p><p>多轴对象通过 XDD 显式定义并生成 OD，不计划运行时偷偷创建隐藏对象。</p><h3 id="21-4-realtime-架构已经为同步模式留出了位置"><a href="#21-4-realtime-架构已经为同步模式留出了位置" class="headerlink" title="21.4 realtime 架构已经为同步模式留出了位置"></a>21.4 realtime 架构已经为同步模式留出了位置</h3><p>未来 CSP&#x2F;CSV&#x2F;CST 等同步模式对 SYNC 周期与 PDO 时序要求更高。</p><p>当前规划希望复用已有 realtime path：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">CO_process_SYNC()</span><br><span class="line">        ↓</span><br><span class="line">CO_process_RPDO()</span><br><span class="line">        ↓</span><br><span class="line">CiA402 synchronous fast path</span><br><span class="line">        ↓</span><br><span class="line">CO_process_TPDO()</span><br></pre></td></tr></table></figure><p>也就是说，先让目标值通过 RPDO 进入本地 OD，再执行 profile 同步逻辑，然后由 TPDO 发布新的状态&#x2F;实际值。</p><p>这条路径不需要为了 CiA 402 去给 CANopenNode core 打一个私有 callback hook。</p><h3 id="21-5-当前不要宣称-CiA-402-Conformance"><a href="#21-5-当前不要宣称-CiA-402-Conformance" class="headerlink" title="21.5 当前不要宣称 CiA 402 Conformance"></a>21.5 当前不要宣称 CiA 402 Conformance</h3><p>公开规划也明确要求：在规范矩阵、Host、目标板和互操作验证形成完整证据以前，不宣称 CiA 402 conformance。</p><p>这是正确的做法。</p><p>CiA 402 不是“定义几个 0x6040&#x2F;0x6041 对象就算支持”。真正实现还涉及：</p><ul><li>PDS FSA；</li><li>Controlword &#x2F; Statusword transition；</li><li>operation mode；</li><li>PP&#x2F;PV&#x2F;HM；</li><li>CSP&#x2F;CSV&#x2F;CST；</li><li>Quick Stop；</li><li>Fault Reaction；</li><li>Following Error；</li><li>PDO 时序；</li><li>SYNC；</li><li>多轴对象分区；</li><li>EMCY 与诊断；</li><li>与 NMT 状态的交互。</li></ul><p>所以这部分现在最合适的表述就是：<strong>已经有明确架构与实施计划，正在逐步补齐，但仍属于 Roadmap。</strong></p><h3 id="21-6-Roadmap-关系图"><a href="#21-6-Roadmap-关系图" class="headerlink" title="21.6 Roadmap 关系图"></a>21.6 Roadmap 关系图</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    BASE[&quot;当前基础: CANopenNode + RT-Thread runtime&quot;] --&gt; DEV301[&quot;CiA 301 Device / Controller building blocks&quot;]</span><br><span class="line">    DEV301 --&gt; C402[&quot;CiA 402 common definitions&quot;]</span><br><span class="line">    C402 --&gt; D402[&quot;CiA 402 Device / Multi-axis&quot;]</span><br><span class="line">    C402 -. &quot;后续阶段&quot; .-&gt; M402[&quot;CiA 402 Controller&quot;]</span><br><span class="line">    D402 --&gt; MODES[&quot;PP / PV / HM / CSP / CSV / CST&quot;]</span><br><span class="line">    M402 -. &quot;后续&quot; .-&gt; REMOTE[&quot;Remote Drive orchestration&quot;]</span><br></pre></td></tr></table></figure><p>图中的虚线表示规划中的后续 Controller 路径，不代表当前 <code>master</code> 已交付。</p><hr><h2 id="22-GFC、SRDO、TIME-等能力怎么看：有实现入口不等于你的产品天然满足对应系统要求"><a href="#22-GFC、SRDO、TIME-等能力怎么看：有实现入口不等于你的产品天然满足对应系统要求" class="headerlink" title="22. GFC、SRDO、TIME 等能力怎么看：有实现入口不等于你的产品天然满足对应系统要求"></a>22. GFC、SRDO、TIME 等能力怎么看：有实现入口不等于你的产品天然满足对应系统要求</h2><p>当前软件包还能裁剪 CiA 304 的 GFC、SRDO，以及 TIME、EMCY Consumer 等能力。</p><p>这里同样要区分“协议对象可编译运行”和“产品系统已经满足应用要求”。</p><p>例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">PKG_CANOPENNODE_USING_GFC</span><br><span class="line">PKG_CANOPENNODE_USING_SRDO</span><br></pre></td></tr></table></figure><p>可以启用对应协议能力，但这不代表设备因此自动满足功能安全认证、SIL&#x2F;PL、双通道硬件、故障覆盖率或者系统级安全生命周期要求。</p><p>仓库文档也明确说明，Demo GFC 诊断用于协议验证，不驱动真实执行器，也不代表功能安全认证。</p><p>这种边界应该保留。通用协议栈负责报文和状态对象，产品安全设计仍然需要独立的 hazard analysis、hardware architecture、diagnostic coverage 和验证证据。</p><hr><h2 id="23-当前已知限制：推荐使用不等于回避边界"><a href="#23-当前已知限制：推荐使用不等于回避边界" class="headerlink" title="23. 当前已知限制：推荐使用不等于回避边界"></a>23. 当前已知限制：推荐使用不等于回避边界</h2><p>任何软件包推荐如果只写优点、不写限制，实际参考价值会很低。</p><p>当前仓库 README 已经列出几项已知限制。</p><h3 id="23-1-Trace-recorder-当前不可用"><a href="#23-1-Trace-recorder-当前不可用" class="headerlink" title="23.1 Trace recorder 当前不可用"></a>23.1 Trace recorder 当前不可用</h3><p>CANopenNode 的 trace 模块还没有适配本包当前使用的 SDO Server 和 Object Dictionary API，因此该选项刻意保持不可用。</p><p>如果你的项目强依赖 CANopenNode trace，需要先补适配，而不是在 Kconfig 里强行打开。</p><h3 id="23-2-内置-AT24CXX-EEPROM-backend-当前限制单实例"><a href="#23-2-内置-AT24CXX-EEPROM-backend-当前限制单实例" class="headerlink" title="23.2 内置 AT24CXX EEPROM backend 当前限制单实例"></a>23.2 内置 AT24CXX EEPROM backend 当前限制单实例</h3><p>如果一个系统需要多个 CANopenNode 实例共享或分别使用 EEPROM，当前内置 adapter 不是直接开箱即用的多实例方案。</p><p>可以考虑产品自定义 User Storage backend。</p><h3 id="23-3-硬件-CAN-HDR-filter-是优化路径，不是强依赖"><a href="#23-3-硬件-CAN-HDR-filter-是优化路径，不是强依赖" class="headerlink" title="23.3 硬件 CAN HDR filter 是优化路径，不是强依赖"></a>23.3 硬件 CAN HDR filter 是优化路径，不是强依赖</h3><p>过滤 bank 不够或者配置失败时会回退软件 RX 分发。</p><p>如果系统总线负载很高，应实际测量 CPU 占用和 RX latency，而不是默认认为“有硬件过滤选项就一定全走硬件”。</p><h3 id="23-4-Demo-OD-只用于-bring-up"><a href="#23-4-Demo-OD-只用于-bring-up" class="headerlink" title="23.4 Demo OD 只用于 bring-up"></a>23.4 Demo OD 只用于 bring-up</h3><p>产品必须替换自己的 OD，并重新检查：</p><ul><li>PDO mapping；</li><li>Identity；</li><li>Storage group；</li><li>LSS；</li><li>参数权限；</li><li>profile 对象；</li><li>EDS&#x2F;XDD 交付。</li></ul><h3 id="23-5-“可做主站”不等于当前提供完整产品级主站应用框架"><a href="#23-5-“可做主站”不等于当前提供完整产品级主站应用框架" class="headerlink" title="23.5 “可做主站”不等于当前提供完整产品级主站应用框架"></a>23.5 “可做主站”不等于当前提供完整产品级主站应用框架</h3><p>这是本文反复强调的边界。</p><p>协议能力已经具备并且部分能力有自动测试，但设备发现、网络编排、远端 profile controller、故障恢复等产品策略还需要应用层实现。</p><p>把这个边界说清楚，反而更容易正确选型。</p><hr><h2 id="24-我为什么推荐这个仓库"><a href="#24-我为什么推荐这个仓库" class="headerlink" title="24. 我为什么推荐这个仓库"></a>24. 我为什么推荐这个仓库</h2><p>总结下来，我推荐它不是因为“又多了一个 CANopen 实现”，而是因为它选择了一个更合理的维护边界。</p><h3 id="24-1-不重复实现-CANopen-核心"><a href="#24-1-不重复实现-CANopen-核心" class="headerlink" title="24.1 不重复实现 CANopen 核心"></a>24.1 不重复实现 CANopen 核心</h3><p>协议核心直接来自 CANopenNode，上游继续负责 CiA 301 等核心协议对象。</p><p>RT-Thread 软件包只维护 target integration。</p><p>这比复制一份 CANopenNode 源码后长期私有修改更健康。</p><h3 id="24-2-RT-Thread-习惯比较统一"><a href="#24-2-RT-Thread-习惯比较统一" class="headerlink" title="24.2 RT-Thread 习惯比较统一"></a>24.2 RT-Thread 习惯比较统一</h3><p>Kconfig、SCons、<code>rt_device</code>、thread、semaphore、mutex、ULOG、DFS、PIN 等都走 RT-Thread 自己的基础设施。</p><p>对 RT-Thread 开发者来说学习成本更低。</p><h3 id="24-3-能力是可裁剪的"><a href="#24-3-能力是可裁剪的" class="headerlink" title="24.3 能力是可裁剪的"></a>24.3 能力是可裁剪的</h3><p>从最小设备节点到 SDO Client&#x2F;NMT Master&#x2F;Gateway，都可以按项目需要组合，不必一次把全部协议对象塞进 MCU。</p><h3 id="24-4-主站与从站不是两套互斥产品"><a href="#24-4-主站与从站不是两套互斥产品" class="headerlink" title="24.4 主站与从站不是两套互斥产品"></a>24.4 主站与从站不是两套互斥产品</h3><p>这一点是我最看重的。</p><p>设备今天只做 SDO Server&#x2F;PDO，明天可以加 SDO Client&#x2F;NMT Master；一个 Node-ID 可以同时有本地设备职责和远端控制职责。</p><p>这让产品架构有更大的演进空间。</p><h3 id="24-5-测试方向明确"><a href="#24-5-测试方向明确" class="headerlink" title="24.5 测试方向明确"></a>24.5 测试方向明确</h3><p>MCU 侧提供可观察性和 fixture，Linux&#x2F;Lely Host 提供独立测试角色；NMT Master 验证还能反转角色，让 Linux 充当远端 Slave。</p><p>这种测试边界比只靠串口日志“看起来正常”可靠得多。</p><h3 id="24-6-文档已经开始覆盖真正的集成问题"><a href="#24-6-文档已经开始覆盖真正的集成问题" class="headerlink" title="24.6 文档已经开始覆盖真正的集成问题"></a>24.6 文档已经开始覆盖真正的集成问题</h3><p>当前文档不仅有 README，还拆出了：</p><ul><li>Quick Start；</li><li>RT-Thread Integration；</li><li>Configuration；</li><li>High-Resolution Time；</li><li>LSS Persistence；</li><li>Testing；</li><li>NMT Master Test；</li><li>Object Dictionary；</li><li>Submodule Update；</li><li>Troubleshooting。</li></ul><p>对于开源嵌入式软件包来说，这些文档的重要性不低于代码本身。</p><hr><h2 id="25-一个建议的首次上手顺序"><a href="#25-一个建议的首次上手顺序" class="headerlink" title="25. 一个建议的首次上手顺序"></a>25. 一个建议的首次上手顺序</h2><p>如果你第一次使用这个仓库，我建议不要从所有高级功能开始。</p><h3 id="第一步：确认-RT-Thread-CAN-驱动"><a href="#第一步：确认-RT-Thread-CAN-驱动" class="headerlink" title="第一步：确认 RT-Thread CAN 驱动"></a>第一步：确认 RT-Thread CAN 驱动</h3><p>先在 BSP 层确认：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">rt_device_find(&quot;can1&quot;)</span><br></pre></td></tr></table></figure><p>对应设备存在，CAN 收发器、电气连接、120 Ω 终端、电源、bitrate 都正确。</p><h3 id="第二步：完整拉取子模块"><a href="#第二步：完整拉取子模块" class="headerlink" title="第二步：完整拉取子模块"></a>第二步：完整拉取子模块</h3><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git <span class="built_in">clone</span> --recursive https://github.com/wdfk-prog/canopennode-rtt.git</span><br></pre></td></tr></table></figure><p>或者：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git submodule update --init --recursive</span><br></pre></td></tr></table></figure><h3 id="第三步：只开基础设备能力"><a href="#第三步：只开基础设备能力" class="headerlink" title="第三步：只开基础设备能力"></a>第三步：只开基础设备能力</h3><p>先保持：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">PKG_USING_CANOPENNODE=y</span><br><span class="line">PKG_CANOPENNODE_APP_AUTO_INIT=y</span><br><span class="line">PKG_CANOPENNODE_USING_DEMO_OD=y</span><br><span class="line">PKG_CANOPENNODE_USING_SDO_SERVER=y</span><br><span class="line">PKG_CANOPENNODE_USING_PDO=y</span><br></pre></td></tr></table></figure><p>确认 CAN 设备名、Node-ID 和 bitrate。</p><h3 id="第四步：看-Boot-up"><a href="#第四步：看-Boot-up" class="headerlink" title="第四步：看 Boot-up"></a>第四步：看 Boot-up</h3><p>Node-ID 1 应看到：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">701#00</span><br></pre></td></tr></table></figure><p>如果没有，先排查：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">CAN device open</span><br><span class="line">bitrate</span><br><span class="line">transceiver</span><br><span class="line">termination</span><br><span class="line">CAN normal mode</span><br><span class="line">Node-ID</span><br></pre></td></tr></table></figure><p>不要先怀疑 PDO。</p><h3 id="第五步：验证-SDO"><a href="#第五步：验证-SDO" class="headerlink" title="第五步：验证 SDO"></a>第五步：验证 SDO</h3><p>用 CANopen Master、Lely、CANopen 工具或者配套测试仓库访问 Demo OD。</p><p>确认 SDO Server 之后，再继续 PDO。</p><h3 id="第六步：替换产品-OD"><a href="#第六步：替换产品-OD" class="headerlink" title="第六步：替换产品 OD"></a>第六步：替换产品 OD</h3><p>将 Demo XDD&#x2F;OD 换成产品自己的对象模型。</p><p>这一步之后才真正开始“设备产品化”。</p><h3 id="第七步：需要主站时再打开-Client-Master-能力"><a href="#第七步：需要主站时再打开-Client-Master-能力" class="headerlink" title="第七步：需要主站时再打开 Client&#x2F;Master 能力"></a>第七步：需要主站时再打开 Client&#x2F;Master 能力</h3><p>例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">PKG_CANOPENNODE_NMT_MASTER=y</span><br><span class="line">PKG_CANOPENNODE_USING_SDO_CLIENT=y</span><br><span class="line">PKG_CANOPENNODE_USING_HB_CONS=y</span><br></pre></td></tr></table></figure><p>先用一个可控远端节点验证：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">remote online</span><br><span class="line">-&gt; NMT state</span><br><span class="line">-&gt; SDO read</span><br><span class="line">-&gt; SDO write</span><br><span class="line">-&gt; PDO</span><br></pre></td></tr></table></figure><p>然后再实现产品自己的 network manager。</p><h3 id="第八步：同步控制再评估-SYNC-Producer-与-realtime-周期"><a href="#第八步：同步控制再评估-SYNC-Producer-与-realtime-周期" class="headerlink" title="第八步：同步控制再评估 SYNC Producer 与 realtime 周期"></a>第八步：同步控制再评估 SYNC Producer 与 realtime 周期</h3><p>涉及多轴控制或者同步 I&#x2F;O 时，再根据系统周期规划：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">PKG_CANOPENNODE_TIMER_PERIOD_US</span><br><span class="line">PKG_CANOPENNODE_SYNC_PRODUCER</span><br><span class="line">PKG_CANOPENNODE_PDO_SYNC</span><br></pre></td></tr></table></figure><p>同时检查整个 RT-Thread 的线程优先级和 CPU budget。</p><hr><h2 id="26-结语：把它当成-RT-Thread-上的-CANopen“基础设施层”，会比把它叫从站包更准确"><a href="#26-结语：把它当成-RT-Thread-上的-CANopen“基础设施层”，会比把它叫从站包更准确" class="headerlink" title="26. 结语：把它当成 RT-Thread 上的 CANopen“基础设施层”，会比把它叫从站包更准确"></a>26. 结语：把它当成 RT-Thread 上的 CANopen“基础设施层”，会比把它叫从站包更准确</h2><p>如果只想做一个最简单的 CANopen 从站，<code>canopennode-rtt</code> 可以完成这个任务：RT-Thread CAN 设备绑定、Boot-up、NMT Slave、Heartbeat、SDO Server、PDO、LSS Slave、Object Dictionary、Storage 都有明确接入路径。</p><p>但如果只把它看成“从站包”，其实低估了这套集成的上限。</p><p>CANopenNode 上游已经提供 simple NMT Master、SDO Client、LSS Master、SYNC Producer、Gateway 等能力；<code>canopennode-rtt</code> 当前的 Kconfig、SCons 和 runtime 也已经把这些能力接入 RT-Thread，并且 NMT Master、SDO Client 等方向已经有对应 MCU 侧自动验证支持。</p><p>因此更准确的定位应该是：</p><blockquote><p><strong>canopennode-rtt 是 CANopenNode 在 RT-Thread 上的集成与运行时基础层。它既可以承载普通 CANopen Device，也可以作为 MCU CANopen Controller&#x2F;Master 的基础，并允许同一个节点同时保留本地设备能力和远端控制能力。</strong></p></blockquote><p>对现在只做单节点设备的项目，这意味着可以少维护一套 target port；对未来要扩展多节点控制的项目，这意味着不需要因为“要做主站”而整体换栈；对计划进入伺服和运动控制的项目，现有 realtime、PDO、SYNC、OD&#x2F;XDD 和 role-neutral 架构也为后续 CiA 402 留出了比较合理的演进空间。</p><p>CiA 402&#x2F;DS402 等 profile 目前仍处于规划与分阶段实施状态，尤其 Controller 端不能提前当成已交付能力宣传；但从公开 Roadmap 看，方向已经明确：通信层保持角色中立，Device 与 Controller 分开，优先把基础能力做扎实，再逐步补 profile。</p><p>如果你正在使用 RT-Thread，又刚好需要 CANopen，我认为这个仓库值得直接拉下来阅读代码和文档，而不是只看 README 里“CANopenNode integration package”这一句话。</p><p>先把一个标准 Node 跑起来，再根据项目需要打开 NMT Master、SDO Client、Heartbeat Consumer、SYNC Producer、LSS Master 和 Gateway。你会发现，这套软件包真正提供的不是“主站版”和“从站版”两个产品，而是一组可以在 RT-Thread MCU 上按需求组合的 CANopen 基础能力。</p><hr><h2 id="相关链接"><a href="#相关链接" class="headerlink" title="相关链接"></a>相关链接</h2><ul><li>canopennode-rtt：<a href="https://github.com/wdfk-prog/canopennode-rtt">https://github.com/wdfk-prog/canopennode-rtt</a></li><li>中文 README：<a href="https://github.com/wdfk-prog/canopennode-rtt/blob/master/README.zh-CN.md">https://github.com/wdfk-prog/canopennode-rtt/blob/master/README.zh-CN.md</a></li><li>在线文档：<a href="https://wdfk-prog.space/canopennode-rtt/">https://wdfk-prog.space/canopennode-rtt/</a></li><li>CANopenNode 上游：<a href="https://github.com/CANopenNode/CANopenNode">https://github.com/CANopenNode/CANopenNode</a></li><li>CANopenNode 官方文档：<a href="https://canopennode.github.io/CANopenNode/">https://canopennode.github.io/CANopenNode/</a></li><li>配套 Linux&#x2F;Lely 测试仓库：<a href="https://github.com/wdfk-prog/canopen-slave-tester">https://github.com/wdfk-prog/canopen-slave-tester</a></li><li>CiA 402 多轴 Device&#x2F;Controller 规划：<a href="https://github.com/wdfk-prog/canopennode-rtt/issues/9">https://github.com/wdfk-prog/canopennode-rtt/issues/9</a></li><li>RTT-CanFestival &#x2F; canfestival-rtt：<a href="https://github.com/gbcwbz/canfestival-rtt">https://github.com/gbcwbz/canfestival-rtt</a></li><li>RT-Thread CanFestival 软件包页面：<a href="https://packages.rt-thread.org/en/detail.html?package=CanFestival">https://packages.rt-thread.org/en/detail.html?package=CanFestival</a></li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;meta name=&quot;referrer&quot; content=&quot;no-referrer&quot; /&gt;

&lt;h1 id=&quot;canopennode-rtt推荐，不只可以做从站，也可以承担主站角色&quot;&gt;&lt;a href=&quot;#canopennode-rtt推荐，不只可以做从站，也可以承担主站角色&quot; </summary>
      
    
    
    
    <category term="rt-thread" scheme="https://wdfk-prog.space/categories/rt-thread/"/>
    
    
    <category term="rt-thread" scheme="https://wdfk-prog.space/tags/rt-thread/"/>
    
  </entry>
  
  <entry>
    <title>RT-Thread 如何用 MPU 做线程栈保护</title>
    <link href="https://wdfk-prog.space/posts/15bcf925/"/>
    <id>https://wdfk-prog.space/posts/15bcf925/</id>
    <published>2026-08-28T08:56:23.000Z</published>
    <updated>2026-08-28T08:57:04.226Z</updated>
    
    <content type="html"><![CDATA[<meta name="referrer" content="no-referrer" /><h1 id="RT-Thread-如何用-MPU-做线程栈保护"><a href="#RT-Thread-如何用-MPU-做线程栈保护" class="headerlink" title="RT-Thread 如何用 MPU 做线程栈保护"></a>RT-Thread 如何用 MPU 做线程栈保护</h1><p><img src="https://i-blog.csdnimg.cn/direct/69c48f7f5a4b49748a9ebaffef40263c.png" alt="在这里插入图片描述"></p><blockquote><p>摘要：结合 RT-Thread 当前 Cortex-M4 实现，拆解 MPU 内存保护、线程双 Guard 区、上下文切换重装载以及 MemManage Fault 的完整运行链路。</p></blockquote><p>在 Cortex-M 系列 MCU 上，线程栈溢出一直是最难排查的一类故障之一。它往往不是在“栈刚好用完”的那一刻表现为明确错误，而是先覆盖相邻内存，再以随机变量异常、链表损坏、HardFault、跑飞甚至数分钟后的非确定性故障出现。</p><p>RT-Thread 传统的 <code>RT_USING_OVERFLOW_CHECK</code> 可以对线程栈做软件检查，但软件检查本质上依赖检查时机：如果栈已经越界，在下一次检查发生之前，相邻内存仍可能先被破坏。</p><p>另一条路线是利用 Cortex-M 自带的 MPU（Memory Protection Unit）。RT-Thread 的 <code>RT_USING_MEM_PROTECTION</code> 提供了 MPU 抽象层，而 <code>RT_USING_HW_STACK_GUARD</code> 则进一步利用 MPU，在每个线程栈的上下边界放置不可访问区域。一旦 CPU 真正访问到 Guard 区，就会触发 MemManage Fault。</p><p>截至 2026-08-27，RT-Thread 主线已经加入 Cortex-M4 对这套机制的支持。相关提交为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">e2e6245aace07b545b8dbcef8fbcdb4469f55d9f</span><br><span class="line">[arm][cortex-m4] Add hardware stack guard support</span><br></pre></td></tr></table></figure><p>这使得 STM32F407 这类 Cortex-M4F MCU 可以直接使用 RT-Thread 的 MPU 抽象层和硬件线程栈保护。下面从硬件原理、RT-Thread 实现和实际运行过程三个层面把整条链路串起来。</p><hr><h2 id="1-两个配置项分别解决什么问题"><a href="#1-两个配置项分别解决什么问题" class="headerlink" title="1. 两个配置项分别解决什么问题"></a>1. 两个配置项分别解决什么问题</h2><p>先看 Kconfig 里的依赖关系：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">RT_USING_HW_STACK_GUARD</span><br><span class="line">        |</span><br><span class="line">        v</span><br><span class="line">RT_USING_MEM_PROTECTION</span><br><span class="line">        |</span><br><span class="line">        v</span><br><span class="line">RT_USING_HEAP</span><br></pre></td></tr></table></figure><p>当前 <code>components/mprotect/Kconfig</code> 中：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">config RT_USING_MEM_PROTECTION</span><br><span class="line">    bool &quot;Enable memory protection&quot;</span><br><span class="line">    default n</span><br><span class="line">    select RT_USING_HEAP</span><br><span class="line"></span><br><span class="line">config RT_USING_HW_STACK_GUARD</span><br><span class="line">    bool &quot;Enable hardware stack guard&quot;</span><br><span class="line">    default n</span><br><span class="line">    select RT_USING_MEM_PROTECTION</span><br></pre></td></tr></table></figure><p>因此，<code>RT_USING_HW_STACK_GUARD</code> 不是一个独立实现，它是 <code>RT_USING_MEM_PROTECTION</code> 之上的一种具体应用。</p><h3 id="1-1-RT-USING-MEM-PROTECTION"><a href="#1-1-RT-USING-MEM-PROTECTION" class="headerlink" title="1.1 RT_USING_MEM_PROTECTION"></a>1.1 <code>RT_USING_MEM_PROTECTION</code></h3><p>它提供的是一层通用的内存保护抽象。</p><p>在 ARMv7-M Cortex-M4 上，底层对应硬件 MPU。RT-Thread 用统一的 <code>rt_mem_region_t</code> 描述一个受保护区域：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="type">void</span> *start;</span><br><span class="line">    <span class="type">rt_size_t</span> size;</span><br><span class="line">    <span class="type">rt_mem_attr_t</span> attr;</span><br><span class="line">&#125; <span class="type">rt_mem_region_t</span>;</span><br></pre></td></tr></table></figure><p>应用层可以通过：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">rt_mprotect_add_region();</span><br><span class="line">rt_mprotect_delete_region();</span><br><span class="line">rt_mprotect_update_region();</span><br></pre></td></tr></table></figure><p>给某个线程增加、删除或修改 MPU region。</p><p>也可以通过：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">rt_mprotect_add_exclusive_region();</span><br><span class="line">rt_mprotect_delete_exclusive_region();</span><br></pre></td></tr></table></figure><p>建立“只有 owner 线程能够访问”的区域。</p><p>它可以用来完成：</p><ul><li>Flash 只读、可执行；</li><li>RAM 可读写、不可执行；</li><li>指定线程只能访问指定数据；</li><li>某段内存仅由一个线程拥有；</li><li>线程栈边界保护。</li></ul><p>但要注意，普通 RT-Thread Cortex-M4 系统中的线程和内核仍然共享同一套物理地址空间。这里的 MPU 更准确地说是在共享地址空间上增加访问约束，而不是像 MMU + 虚拟内存那样建立进程级地址空间隔离。</p><h3 id="1-2-RT-USING-HW-STACK-GUARD"><a href="#1-2-RT-USING-HW-STACK-GUARD" class="headerlink" title="1.2 RT_USING_HW_STACK_GUARD"></a>1.2 <code>RT_USING_HW_STACK_GUARD</code></h3><p>这个选项专门解决线程栈越界检测。</p><p>它不会改变 C 编译器生成函数栈帧的方式，也不是 GCC 的 <code>-fstack-protector</code>。它的工作方式更接近硬件“防撞墙”：RT-Thread 在每个线程的栈两端各放置一个 MPU No-Access 区域，一旦 CPU 对这块区域发起访问，就由 MPU 拦截。</p><p>所以它检测的是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">CPU 是否真正访问了线程栈边界之外的受保护区域</span><br></pre></td></tr></table></figure><p>而不是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">某个软件标记是否已经被改写</span><br></pre></td></tr></table></figure><hr><h2 id="2-Cortex-M4-MPU-为什么能实现这种保护"><a href="#2-Cortex-M4-MPU-为什么能实现这种保护" class="headerlink" title="2. Cortex-M4 MPU 为什么能实现这种保护"></a>2. Cortex-M4 MPU 为什么能实现这种保护</h2><p>Cortex-M4 属于 ARMv7E-M 架构，MPU region 具有几个直接影响 RT-Thread 实现的约束：</p><ul><li>region 数量有限；</li><li>最小 region 大小为 32 Byte；</li><li>region size 必须是 2 的幂；</li><li>region base 必须按 region size 自然对齐；</li><li>可以配置读、写、执行权限；</li><li>访问违反权限的地址可以产生 MemManage Fault。</li></ul><p>RT-Thread Cortex-M4 实现中的 <code>rt_hw_mpu_region_valid()</code> 也直接检查这三个条件：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (region-&gt;size &lt; MPU_MIN_REGION_SIZE)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">return</span> RT_FALSE;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> ((region-&gt;size &amp; (region-&gt;size - <span class="number">1U</span>)) != <span class="number">0U</span>)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">return</span> RT_FALSE;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (((<span class="type">rt_uint32_t</span>)region-&gt;start &amp; (region-&gt;size - <span class="number">1U</span>)) != <span class="number">0U</span>)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">return</span> RT_FALSE;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>其中：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> MPU_MIN_REGION_SIZE 32U</span></span><br></pre></td></tr></table></figure><p>对于 STM32F407，MPU 提供 8 个 region。因此当前使用这套功能时，典型配置为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">NUM_MEM_REGIONS=8</span><br></pre></td></tr></table></figure><p>RT-Thread 不会只相信配置值。<code>rt_hw_mpu_init()</code> 启动时还会读取：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">MPU-&gt;TYPE</span><br></pre></td></tr></table></figure><p>获取硬件真实支持的 region 数量，然后与 <code>NUM_MEM_REGIONS</code> 比较。配置和硬件不一致时初始化会失败。</p><p>这个设计很重要，因为 <code>NUM_MEM_REGIONS</code> 不是“想要几个就配置几个”，而是对目标处理器硬件能力的描述。</p><hr><h2 id="3-一个线程的栈实际上被改造成什么样"><a href="#3-一个线程的栈实际上被改造成什么样" class="headerlink" title="3. 一个线程的栈实际上被改造成什么样"></a>3. 一个线程的栈实际上被改造成什么样</h2><p>在 Cortex-M4 上，RT-Thread 线程栈向低地址方向增长。</p><p>打开硬件栈保护以后，原始 stack buffer 不再全部作为可用线程栈，而会被切成三个主要部分：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line">低地址</span><br><span class="line"></span><br><span class="line">原始 stack buffer</span><br><span class="line">+--------------------------------------------------+</span><br><span class="line">| 对齐填充                                         |</span><br><span class="line">+--------------------------------------------------+</span><br><span class="line">| Bottom Guard：32 B，MPU No Access                |</span><br><span class="line">+--------------------------------------------------+</span><br><span class="line">|                                                  |</span><br><span class="line">|             可用线程栈                           |</span><br><span class="line">|                                                  |</span><br><span class="line">|           stack 向低地址增长                     |</span><br><span class="line">|                    ↓                             |</span><br><span class="line">+--------------------------------------------------+</span><br><span class="line">| Top Guard：32 B，MPU No Access                   |</span><br><span class="line">+--------------------------------------------------+</span><br><span class="line">| 对齐填充                                         |</span><br><span class="line">+--------------------------------------------------+</span><br><span class="line"></span><br><span class="line">高地址</span><br></pre></td></tr></table></figure><p>真正用于捕捉典型线程栈溢出的主要是低地址侧 Bottom Guard，因为 Cortex-M4 的线程栈通常从高地址向低地址增长。</p><p>高地址侧 Top Guard 仍然有意义，它可以覆盖错误 SP、越界访问或者其他破坏栈上边界的情况。</p><p>对应的 Cortex-M4 代码位于：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">libcpu/arm/cortex-m4/cpuport.c</span><br></pre></td></tr></table></figure><p>核心函数是：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">rt_hw_stack_guard_init</span><span class="params">(<span class="type">rt_thread_t</span> thread)</span></span><br></pre></td></tr></table></figure><p>其主要逻辑可以概括为：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">stack_bottom = thread-&gt;stack_addr;</span><br><span class="line">stack_top = thread-&gt;stack_addr + thread-&gt;stack_size;</span><br><span class="line"></span><br><span class="line">bottom_guard_start = align_up(stack_bottom, <span class="number">32</span>);</span><br><span class="line">top_guard_start = align_down(stack_top - <span class="number">32</span>, <span class="number">32</span>);</span><br><span class="line"></span><br><span class="line">bottom_guard = [bottom_guard_start, bottom_guard_start + <span class="number">32</span>);</span><br><span class="line">top_guard = [top_guard_start, top_guard_start + <span class="number">32</span>);</span><br><span class="line"></span><br><span class="line">rt_mprotect_add_region(thread, &amp;bottom_guard);</span><br><span class="line">rt_mprotect_add_region(thread, &amp;top_guard);</span><br></pre></td></tr></table></figure><p>两个 region 的属性都是：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">RT_MEM_REGION_P_NA_U_NA</span><br></pre></td></tr></table></figure><p>也就是 Privileged No Access &#x2F; Unprivileged No Access。</p><p>随后 RT-Thread 会修改线程控制块：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">thread-&gt;stack_buf = thread-&gt;stack_addr;</span><br><span class="line">thread-&gt;stack_addr = (<span class="type">void</span> *)(bottom_guard_start + <span class="number">32</span>);</span><br><span class="line">thread-&gt;stack_size = top_guard_start - bottom_guard_start - <span class="number">32</span>;</span><br></pre></td></tr></table></figure><p>这里的语义变化非常重要：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">stack_buf</span><br><span class="line">    -&gt; 原始分配出来的完整 stack buffer</span><br><span class="line"></span><br><span class="line">stack_addr / stack_size</span><br><span class="line">    -&gt; 排除两个 Guard 后真正允许线程使用的栈</span><br></pre></td></tr></table></figure><p>所以开启 Guard 以后，<code>thread-&gt;stack_size</code> 已经不是最初传给 <code>rt_thread_create()</code> 或 <code>rt_thread_init()</code> 的原始大小。</p><hr><h2 id="4-为什么线程创建时还要分配一张-MPU-region-表"><a href="#4-为什么线程创建时还要分配一张-MPU-region-表" class="headerlink" title="4. 为什么线程创建时还要分配一张 MPU region 表"></a>4. 为什么线程创建时还要分配一张 MPU region 表</h2><p>很多人第一次看到 MPU 栈保护时，会自然认为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">每个线程直接占用两个硬件 MPU region</span><br></pre></td></tr></table></figure><p>实际上不是。</p><p>Cortex-M4 的硬件 MPU 只有固定数量的 region。例如 STM32F407 只有 8 个，不可能 20 个线程就需要 40 个硬件 region。</p><p>RT-Thread 使用的是“软件表 + 上下文切换时重装载”的方式。</p><p>每个线程的 <code>rt_thread</code> 中都有：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">ifdef</span> RT_USING_MEM_PROTECTION</span></span><br><span class="line">    <span class="type">void</span> *mem_regions;</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br></pre></td></tr></table></figure><p>开启硬件栈保护时：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> NUM_DYNAMIC_REGIONS (2 + NUM_CONFIGURABLE_REGIONS)</span></span><br></pre></td></tr></table></figure><p>这里额外的 <code>2</code> 就是 Bottom Guard 和 Top Guard。</p><p>线程第一次执行 <code>rt_mprotect_add_region()</code> 时，RT-Thread 会为该线程分配自己的 region 表：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">thread-&gt;mem_regions = RT_KERNEL_MALLOC(</span><br><span class="line">    NUM_DYNAMIC_REGIONS * <span class="keyword">sizeof</span>(<span class="type">rt_mem_region_t</span>));</span><br></pre></td></tr></table></figure><p>因此整个关系实际上是：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    TCB[&quot;rt_thread / TCB&quot;] --&gt; Table[&quot;线程软件 mem_regions 表&quot;]</span><br><span class="line">    Table --&gt; Bottom[&quot;Bottom Guard 配置&quot;]</span><br><span class="line">    Table --&gt; Top[&quot;Top Guard 配置&quot;]</span><br><span class="line">    Table --&gt; Custom[&quot;可选 configurable regions&quot;]</span><br><span class="line">    Table --&gt; Switch[&quot;PendSV 上下文切换&quot;]</span><br><span class="line">    Switch --&gt; MPU[&quot;Cortex-M4 硬件 MPU region slots&quot;]</span><br></pre></td></tr></table></figure><p>图里最重要的是最后两步：线程自己的 region 配置长期保存在 RAM 中，真正的硬件 MPU slot 在调度时被当前线程反复复用。</p><p>这也解释了两个现象。</p><p>第一，线程数量增加不会线性消耗硬件 MPU region 数量，但会增加 RAM 占用。</p><p>第二，只要启用 per-thread MPU region，线程切换就需要重新编程 MPU，因此上下文切换路径会增加额外工作。</p><p>还有一个容易忽略的工程语义：即使线程本身通过 <code>rt_thread_init()</code> 使用静态 TCB 和静态 stack，当前硬件 Guard 的 <code>mem_regions</code> 仍由 <code>RT_KERNEL_MALLOC()</code> 分配。因此开启该功能以后，“静态线程”不再意味着整个线程相关数据都完全不使用 heap。</p><p>这也正是 <code>RT_USING_MEM_PROTECTION</code> 在 Kconfig 中主动 <code>select RT_USING_HEAP</code> 的原因之一。</p><hr><h2 id="5-从-rt-thread-create-到-Guard-建立的完整调用链"><a href="#5-从-rt-thread-create-到-Guard-建立的完整调用链" class="headerlink" title="5. 从 rt_thread_create() 到 Guard 建立的完整调用链"></a>5. 从 <code>rt_thread_create()</code> 到 Guard 建立的完整调用链</h2><p>线程初始化核心逻辑位于：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">src/thread.c</span><br></pre></td></tr></table></figure><p>简化后的调用关系如下：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line">rt_thread_create()</span><br><span class="line">    |</span><br><span class="line">    +-- 分配 rt_thread 对象</span><br><span class="line">    |</span><br><span class="line">    +-- RT_KERNEL_MALLOC(stack_size)</span><br><span class="line">    |</span><br><span class="line">    +-- _thread_init()</span><br><span class="line">            |</span><br><span class="line">            +-- thread-&gt;stack_addr = stack_start</span><br><span class="line">            +-- thread-&gt;stack_size = stack_size</span><br><span class="line">            |</span><br><span class="line">            +-- memset(stack, &#x27;#&#x27;, stack_size)</span><br><span class="line">            |</span><br><span class="line">            +-- rt_hw_stack_guard_init()</span><br><span class="line">            |       |</span><br><span class="line">            |       +-- 计算上下两个 32 B Guard</span><br><span class="line">            |       +-- rt_mprotect_add_region(bottom)</span><br><span class="line">            |       +-- rt_mprotect_add_region(top)</span><br><span class="line">            |       +-- 保存 stack_buf</span><br><span class="line">            |       +-- 重写 stack_addr / stack_size</span><br><span class="line">            |</span><br><span class="line">            +-- rt_hw_stack_init()</span><br><span class="line">                    |</span><br><span class="line">                    +-- 在“可用栈”顶部构造初始异常栈帧</span><br></pre></td></tr></table></figure><p>注意顺序：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">先调整 Guard 和有效 stack 边界</span><br><span class="line">再建立线程初始上下文</span><br></pre></td></tr></table></figure><p>这保证 <code>rt_hw_stack_init()</code> 不会把初始寄存器栈帧放进 Top Guard。</p><p>整个运行过程可以用下面的时序表示：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant APP as &quot;线程创建者&quot;</span><br><span class="line">    participant THR as &quot;RT-Thread thread.c&quot;</span><br><span class="line">    participant GUARD as &quot;Cortex-M4 stack guard&quot;</span><br><span class="line">    participant MPRO as &quot;mprotect&quot;</span><br><span class="line">    participant RAM as &quot;线程 mem_regions 表&quot;</span><br><span class="line"></span><br><span class="line">    APP-&gt;&gt;THR: rt_thread_create(stack_size)</span><br><span class="line">    THR-&gt;&gt;THR: 分配 TCB 和原始 stack buffer</span><br><span class="line">    THR-&gt;&gt;GUARD: rt_hw_stack_guard_init(thread)</span><br><span class="line">    GUARD-&gt;&gt;MPRO: add bottom No-Access region</span><br><span class="line">    MPRO-&gt;&gt;RAM: 首次需要时分配并保存 region</span><br><span class="line">    GUARD-&gt;&gt;MPRO: add top No-Access region</span><br><span class="line">    MPRO-&gt;&gt;RAM: 保存 region</span><br><span class="line">    GUARD-&gt;&gt;THR: 更新 stack_buf / stack_addr / stack_size</span><br><span class="line">    THR-&gt;&gt;THR: rt_hw_stack_init()</span><br><span class="line">    THR--&gt;&gt;APP: 返回 thread handle</span><br></pre></td></tr></table></figure><hr><h2 id="6-MPU-本身什么时候启用"><a href="#6-MPU-本身什么时候启用" class="headerlink" title="6. MPU 本身什么时候启用"></a>6. MPU 本身什么时候启用</h2><p>通用 MPU 组件位于：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">components/mprotect/</span><br></pre></td></tr></table></figure><p>在 STM32 BSP 的启动路径中，这个初始化发生得比普通线程创建更早。当前调用关系是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">rtthread_startup()</span><br><span class="line">    -&gt; rt_hw_board_init()</span><br><span class="line">        -&gt; rt_system_heap_init()</span><br><span class="line">        -&gt; rt_components_board_init()</span><br><span class="line">            -&gt; INIT_BOARD_EXPORT(rt_mprotect_init)</span><br><span class="line">                -&gt; rt_hw_mpu_init()</span><br><span class="line">    -&gt; rt_system_scheduler_init()</span><br><span class="line">    -&gt; rt_application_init()</span><br><span class="line">        -&gt; rt_thread_create(&quot;main&quot;, ...)</span><br></pre></td></tr></table></figure><p>这条顺序很关键：<code>RT_USING_MEM_PROTECTION</code> 会使用 heap 保存 per-thread region 表，因此 STM32 BSP 先完成 heap 初始化，再执行 Board Export 中的 MPU 初始化；随后 <code>rt_application_init()</code> 才创建 main 线程并建立它的 Guard。</p><p>初始化入口：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">rt_mprotect_init</span><span class="params">(<span class="type">void</span>)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">return</span> (<span class="type">int</span>)rt_hw_mpu_init();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">INIT_BOARD_EXPORT(rt_mprotect_init);</span><br></pre></td></tr></table></figure><p>在 Cortex-M4 上，最终进入：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">libcpu/arm/cortex-m4/mpu.c</span><br></pre></td></tr></table></figure><p>的：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">rt_hw_mpu_init()</span><br></pre></td></tr></table></figure><p>这个函数主要做四件事。</p><p>第一，读取 MPU 硬件能力：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">MPU-&gt;TYPE.DREGION</span><br></pre></td></tr></table></figure><p>确认处理器确实有 MPU，并校验它与 <code>NUM_MEM_REGIONS</code> 一致。</p><p>第二，检查 region 预算：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">NUM_STATIC_REGIONS</span><br><span class="line">+ NUM_CONFIGURABLE_REGIONS</span><br><span class="line">+ NUM_EXCLUSIVE_REGIONS</span><br><span class="line">+ 2 个 Stack Guard</span><br><span class="line">&lt;= NUM_MEM_REGIONS</span><br></pre></td></tr></table></figure><p>第三，关闭 MPU 并写入 BSP 定义的 static regions。</p><p>第四，重新启用 MPU：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ARM_MPU_Enable(MPU_CTRL_PRIVDEFENA_Msk);</span><br></pre></td></tr></table></figure><p><code>PRIVDEFENA</code> 表示 privileged code 对未被 MPU region 显式覆盖的地址仍采用默认内存映射。这样 BSP 不需要一开始就把整个 4 GB 地址空间全部拆成 MPU region。</p><p>CMSIS 的 <code>ARM_MPU_Enable()</code> 同时会打开 MemManage Fault：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">SCB-&gt;SHCSR |= SCB_SHCSR_MEMFAULTENA_Msk;</span><br></pre></td></tr></table></figure><p>因此 MPU 权限违规能够进入 <code>MemManage_Handler()</code>，而不是所有问题都只表现为 HardFault。</p><hr><h2 id="7-BSP-为什么还需要-static-regions"><a href="#7-BSP-为什么还需要-static-regions" class="headerlink" title="7. BSP 为什么还需要 static_regions[]"></a>7. BSP 为什么还需要 <code>static_regions[]</code></h2><p><code>NUM_MEM_REGIONS</code> 是处理器总共有多少 MPU slot，而 <code>NUM_STATIC_REGIONS</code> 则由 BSP 决定要永久占用多少个 MPU region。</p><p>当前 STM32F407 参考 BSP：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">bsp/stm32/stm32f407-fk407m2-zgt6</span><br></pre></td></tr></table></figure><p>在 <code>board.h</code> 中定义：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">ifdef</span> RT_USING_MEM_PROTECTION</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> NUM_STATIC_REGIONS 1</span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br></pre></td></tr></table></figure><p>在 <code>board.c</code> 中提供：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">rt_mem_region_t</span> static_regions[NUM_STATIC_REGIONS] = &#123;</span><br><span class="line">    &#123;</span><br><span class="line">        .start = (<span class="type">void</span> *)STM32_FLASH_START_ADRESS,</span><br><span class="line">        .size = (<span class="type">rt_size_t</span>)STM32_FLASH_SIZE,</span><br><span class="line">        .attr = RT_MEM_REGION_P_RX_U_RX,</span><br><span class="line">    &#125;,</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>含义是把整个 Flash 配置成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Read + Execute</span><br><span class="line">No Write</span><br></pre></td></tr></table></figure><p>对于 STM32F407，如果只做线程 Guard，可以用这样的 region 预算：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">硬件 MPU region 总数：        8</span><br><span class="line">static Flash region：          1</span><br><span class="line">per-thread configurable：      0</span><br><span class="line">exclusive region：             0</span><br><span class="line">stack guard：                  2</span><br><span class="line">---------------------------------</span><br><span class="line">当前线程运行时使用：           3 / 8</span><br></pre></td></tr></table></figure><p>关键是“当前线程运行时”。</p><p>两个 Guard 并不是每个线程永久占用硬件 slot，而是每个线程保留自己的配置，调度时装进相同的硬件动态 slot。</p><hr><h2 id="8-PendSV-为什么必须参与-MPU-保护"><a href="#8-PendSV-为什么必须参与-MPU-保护" class="headerlink" title="8. PendSV 为什么必须参与 MPU 保护"></a>8. PendSV 为什么必须参与 MPU 保护</h2><p>如果只在线程创建时写一次 MPU，会出现一个根本问题：不同线程需要不同的栈 Guard 地址。</p><p>线程 A 的栈可能位于：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">0x20001000 ~ 0x20001400</span><br></pre></td></tr></table></figure><p>线程 B 的栈可能位于：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">0x20002000 ~ 0x20002800</span><br></pre></td></tr></table></figure><p>CPU 从 A 切到 B 以后，如果 MPU 仍然保留 A 的 Guard，那么 B 的栈根本没有受到正确保护。</p><p>因此 MPU region 必须成为“线程上下文”的一部分。</p><p>Cortex-M4 当前 GCC 上下文切换代码在 PendSV 切换路径中加入：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">rt_thread_self()</span><br><span class="line">    -&gt; rt_hw_mpu_table_switch(current_thread)</span><br></pre></td></tr></table></figure><p><code>rt_hw_mpu_table_switch()</code> 会：</p><ol><li>读取当前线程的 <code>mem_regions</code>；</li><li>把 dynamic regions 写入 MPU；</li><li>根据 exclusive region owner 关系写入需要隔离的区域；</li><li>清空没有使用的 MPU slot。</li></ol><p>运行链路如下：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant PEND as &quot;PendSV&quot;</span><br><span class="line">    participant SCHED as &quot;RT-Thread 调度上下文&quot;</span><br><span class="line">    participant TCB as &quot;新线程 TCB&quot;</span><br><span class="line">    participant SW as &quot;rt_hw_mpu_table_switch&quot;</span><br><span class="line">    participant MPU as &quot;Cortex-M4 MPU&quot;</span><br><span class="line"></span><br><span class="line">    PEND-&gt;&gt;SCHED: 完成线程上下文选择</span><br><span class="line">    SCHED-&gt;&gt;TCB: current_thread 指向新线程</span><br><span class="line">    PEND-&gt;&gt;SW: rt_hw_mpu_table_switch(current_thread)</span><br><span class="line">    SW-&gt;&gt;TCB: 读取 mem_regions</span><br><span class="line">    SW-&gt;&gt;MPU: 写入当前线程 dynamic regions</span><br><span class="line">    SW-&gt;&gt;MPU: 写入适用的 exclusive regions</span><br><span class="line">    SW-&gt;&gt;MPU: 清除剩余动态 slot</span><br><span class="line">    SW--&gt;&gt;PEND: MPU 已匹配新线程</span><br><span class="line">    PEND-&gt;&gt;PEND: 恢复新线程寄存器并返回</span><br></pre></td></tr></table></figure><p>这一步决定了硬件 Guard 能否随着线程真正“移动”。</p><p>也正因为如此，启用内存保护以后，线程切换路径会增加 MPU register programming 开销。这个开销能从源码确认存在，但具体增加多少 cycle 不能只靠代码静态推导；它取决于 CPU 主频、编译优化、动态 region 数量、Flash wait state、FPU 上下文等因素，需要在具体目标板上测量。</p><hr><h2 id="9-栈真正越界时，CPU-和-RT-Thread-会发生什么"><a href="#9-栈真正越界时，CPU-和-RT-Thread-会发生什么" class="headerlink" title="9. 栈真正越界时，CPU 和 RT-Thread 会发生什么"></a>9. 栈真正越界时，CPU 和 RT-Thread 会发生什么</h2><p>假设一个线程可用栈已经接近 Bottom Guard：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">高地址</span><br><span class="line"></span><br><span class="line">Top Guard</span><br><span class="line">+----------------------+</span><br><span class="line">|                      |</span><br><span class="line">|      可用 stack      |</span><br><span class="line">|                      |</span><br><span class="line">|        ↓ SP          |</span><br><span class="line">|        ↓             |</span><br><span class="line">|        ↓             |</span><br><span class="line">+----------------------+</span><br><span class="line">Bottom Guard  No Access</span><br><span class="line"></span><br><span class="line">低地址</span><br></pre></td></tr></table></figure><p>随着函数调用、局部变量分配或者寄存器压栈继续消耗 stack，SP 最终进入 Bottom Guard。</p><p>接下来 CPU 对 Guard 地址发起真实读写访问时：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br></pre></td><td class="code"><pre><span class="line">线程执行</span><br><span class="line">   |</span><br><span class="line">   v</span><br><span class="line">SP / 内存访问进入 Guard</span><br><span class="line">   |</span><br><span class="line">   v</span><br><span class="line">Cortex-M4 MPU 权限检查</span><br><span class="line">   |</span><br><span class="line">   +-- 允许 --&gt; 正常访问</span><br><span class="line">   |</span><br><span class="line">   +-- No Access</span><br><span class="line">           |</span><br><span class="line">           v</span><br><span class="line">      MemManage Fault</span><br><span class="line">           |</span><br><span class="line">           v</span><br><span class="line">   MemManage_Handler()</span><br><span class="line">           |</span><br><span class="line">           v</span><br><span class="line">   构造 rt_mem_exception_info_t</span><br><span class="line">           |</span><br><span class="line">           +-- thread</span><br><span class="line">           +-- addr</span><br><span class="line">           +-- region</span><br><span class="line">           +-- mmfsr</span><br><span class="line">           |</span><br><span class="line">           v</span><br><span class="line">   用户注册的 exception hook</span><br><span class="line">           |</span><br><span class="line">           v</span><br><span class="line">       while (1)</span><br></pre></td></tr></table></figure><p>当前 Cortex-M4 <code>MemManage_Handler()</code> 会组织：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="type">rt_thread_t</span> thread;</span><br><span class="line">    <span class="type">void</span> *addr;</span><br><span class="line">    <span class="type">rt_mem_region_t</span> region;</span><br><span class="line">    <span class="type">rt_uint8_t</span> mmfsr;</span><br><span class="line">&#125; <span class="type">rt_mem_exception_info_t</span>;</span><br></pre></td></tr></table></figure><p>应用可以通过：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">rt_hw_mpu_exception_set_hook(...);</span><br></pre></td></tr></table></figure><p>注册诊断 hook。</p><p>当前实现中，hook 返回后 handler 最终进入：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">while</span> (<span class="number">1</span>);</span><br></pre></td></tr></table></figure><p>所以这里的 hook 应理解为故障诊断入口，而不是通用 fault recovery 接口。</p><p>也就是说，当前主线不能简单理解成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">某个线程越界</span><br><span class="line">-&gt; MPU 捕获</span><br><span class="line">-&gt; 删除线程</span><br><span class="line">-&gt; 从异常返回</span><br><span class="line">-&gt; 系统继续运行</span><br></pre></td></tr></table></figure><p>如果产品希望做这种“单线程故障恢复”，还要进一步处理 faulting instruction、异常堆栈、PSP&#x2F;MSP、调度器状态和 MPU 状态，不能仅依赖当前 hook。</p><hr><h2 id="10-为什么真实栈溢出不一定总有有效的-MMFAR"><a href="#10-为什么真实栈溢出不一定总有有效的-MMFAR" class="headerlink" title="10. 为什么真实栈溢出不一定总有有效的 MMFAR"></a>10. 为什么真实栈溢出不一定总有有效的 MMFAR</h2><p>如果线程通过普通 C 语句访问 Guard，例如写入一个位于 Guard 内的地址，MemManage Status Register 常见表现是普通数据访问违规，同时 MMFAR 可能提供有效地址。</p><p>但真正的栈溢出可能发生在更多场景：</p><ul><li>函数 prologue 压栈；</li><li>异常进入时硬件自动 stacking；</li><li>异常返回时 unstacking；</li><li>FPU 上下文保存；</li><li>嵌套异常。</li></ul><p>如果故障发生在异常压栈或出栈过程，MMFSR 可能出现：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">MSTKERR</span><br><span class="line">MUNSTKERR</span><br></pre></td></tr></table></figure><p>此时不能无条件使用 MMFAR。</p><p>正确的 Fault 分析规则仍然是：只有当 CFSR 中对应的 <code>MMARVALID</code> 置位时，MMFAR 才能作为有效 fault address 使用。</p><p>这属于 ARMv7-M Fault 语义，而不是 RT-Thread 自己定义的规则。</p><hr><h2 id="11-开启以后，原来的线程栈会少多少"><a href="#11-开启以后，原来的线程栈会少多少" class="headerlink" title="11. 开启以后，原来的线程栈会少多少"></a>11. 开启以后，原来的线程栈会少多少</h2><p>两个固定 Guard 一共占：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">32 B + 32 B = 64 B</span><br></pre></td></tr></table></figure><p>但当前实现还会把上下边界按 32 Byte 对齐。</p><p>因此实际损失不仅是固定 64 Byte，还要考虑原始 stack buffer 地址和大小带来的 alignment padding。</p><p>按当前算法分析，最坏情况下有效 stack 损失可以接近：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">64 ~ 126 Byte</span><br></pre></td></tr></table></figure><p>这里的范围来自源码中的 32 Byte Guard 与上下边界对齐关系，是实现层面的推导，不是官方规定“所有线程固定损失 126 Byte”。</p><p>如果是静态线程，比较理想的做法是让 stack 本身 32 Byte 对齐，并把总大小设计成 32 Byte 的整数倍。例如：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">rt_align(<span class="number">32</span>)</span><br><span class="line"><span class="type">static</span> <span class="type">rt_uint8_t</span> worker_stack[<span class="number">1024</span> + <span class="number">64</span>];</span><br></pre></td></tr></table></figure><p>在边界完全对齐时，两个 32 Byte Guard 消耗 64 Byte，可以给线程保留约 1024 Byte 有效 stack。</p><p>对于 <code>rt_thread_create()</code> 动态 stack，因为实际 heap 返回地址未必正好 32 Byte 对齐，如果希望原来 1024 Byte 的有效空间大致保持不变，工程上可以先保守额外预留约 128 Byte，再结合实际 stack watermark 调整。</p><p>这个“增加约 128 Byte”是基于当前实现的工程估算，而不是 RT-Thread API 的硬性要求。</p><hr><h2 id="12-RAM-开销不仅来自两个-Guard"><a href="#12-RAM-开销不仅来自两个-Guard" class="headerlink" title="12. RAM 开销不仅来自两个 Guard"></a>12. RAM 开销不仅来自两个 Guard</h2><p>Guard region 本身只是权限描述，不会额外分配两块独立 32 Byte RAM；它们实际上占用了原始 stack buffer 的两段空间。</p><p>额外 RAM 开销主要来自每线程 MPU region table 和 TCB 新字段。</p><p>只启用硬件 Guard、且：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">NUM_CONFIGURABLE_REGIONS = 0</span><br></pre></td></tr></table></figure><p>时：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">NUM_DYNAMIC_REGIONS = 2</span><br></pre></td></tr></table></figure><p>在 32-bit Cortex-M4 上，<code>rt_mem_region_t</code> 通常包含：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">start pointer    4 B</span><br><span class="line">size             4 B</span><br><span class="line">attr.rasr        4 B</span><br><span class="line">--------------------</span><br><span class="line">约              12 B</span><br></pre></td></tr></table></figure><p>两个 region 的纯结构体数据大约 24 Byte&#x2F;线程。</p><p>TCB 还会额外保存：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">mem_regions</span><br><span class="line">stack_buf</span><br></pre></td></tr></table></figure><p>两个指针通常再增加约 8 Byte。</p><p>最终实际 heap 增量还要叠加 allocator metadata 和 alignment，所以应以目标工程的 <code>sizeof()</code>、map 文件和 heap 统计为准，而不能把 32 Byte 左右当成绝对值。</p><hr><h2 id="13-RT-USING-OVERFLOW-CHECK-和硬件-Guard-是替代关系吗"><a href="#13-RT-USING-OVERFLOW-CHECK-和硬件-Guard-是替代关系吗" class="headerlink" title="13. RT_USING_OVERFLOW_CHECK 和硬件 Guard 是替代关系吗"></a>13. <code>RT_USING_OVERFLOW_CHECK</code> 和硬件 Guard 是替代关系吗</h2><p>不是完全替代。</p><p>软件 overflow check 和 MPU Guard 解决的是同一个大问题，但检测机制不同。</p><table><thead><tr><th>维度</th><th><code>RT_USING_OVERFLOW_CHECK</code></th><th><code>RT_USING_HW_STACK_GUARD</code></th></tr></thead><tbody><tr><td>检测方式</td><td>软件检查栈边界&#x2F;填充值</td><td>MPU 访问权限</td></tr><tr><td>触发时机</td><td>到达软件检查点时</td><td>CPU 第一次访问 Guard 时</td></tr><tr><td>是否占 MPU region</td><td>否</td><td>是，每线程配置中占 2 个动态 region</td></tr><tr><td>是否减少有效 stack</td><td>通常没有 Guard 固定损失</td><td>有，至少两个 32 B Guard</td></tr><tr><td>上下文切换额外工作</td><td>很小或无 MPU 重装载</td><td>需要切换 MPU dynamic regions</td></tr><tr><td>故障表现</td><td>软件检测逻辑</td><td>MemManage Fault</td></tr><tr><td>适合用途</td><td>watermark、常规溢出检测</td><td>快速阻断越界继续扩散</td></tr></tbody></table><p>所以在调试版本或高可靠 MCU 系统中，两者可以同时开启：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">RT_USING_OVERFLOW_CHECK=y</span><br><span class="line">RT_USING_HW_STACK_GUARD=y</span><br></pre></td></tr></table></figure><p>软件检查继续提供栈使用情况和传统检测能力，MPU Guard 则提供硬件访问边界。</p><hr><h2 id="14-STM32F407-上一个最小而清晰的配置"><a href="#14-STM32F407-上一个最小而清晰的配置" class="headerlink" title="14. STM32F407 上一个最小而清晰的配置"></a>14. STM32F407 上一个最小而清晰的配置</h2><p>如果目标只是先把硬件线程栈保护跑起来，而不做复杂的 per-thread 内存隔离，可以把动态 MPU 功能压到最小：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">CONFIG_RT_USING_MEM_PROTECTION=y</span><br><span class="line">CONFIG_RT_USING_HW_STACK_GUARD=y</span><br><span class="line">CONFIG_USE_MEM_PROTECTION_EXAMPLES=n</span><br><span class="line"></span><br><span class="line">CONFIG_NUM_MEM_REGIONS=8</span><br><span class="line">CONFIG_NUM_EXCLUSIVE_REGIONS=0</span><br><span class="line">CONFIG_NUM_CONFIGURABLE_REGIONS=0</span><br></pre></td></tr></table></figure><p>对应的 <code>rtconfig.h</code> 概念上类似：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> RT_USING_MEM_PROTECTION</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> RT_USING_HW_STACK_GUARD</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> NUM_MEM_REGIONS 8</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> NUM_EXCLUSIVE_REGIONS 0</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> NUM_CONFIGURABLE_REGIONS 0</span></span><br></pre></td></tr></table></figure><p>BSP 再提供：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">ifdef</span> RT_USING_MEM_PROTECTION</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> NUM_STATIC_REGIONS 1</span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br></pre></td></tr></table></figure><p>以及一条 Flash RX static region。</p><p>这个配置下，一个正在运行的线程所需 MPU slot 为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">1 个 static Flash region</span><br><span class="line">2 个当前线程 Stack Guard region</span><br><span class="line">= 3 个 MPU region</span><br></pre></td></tr></table></figure><p>对于 STM32F407 的 8-region MPU 来说空间充足，也让第一次引入这项功能时的变量尽可能少。</p><hr><h2 id="15-当前-Cortex-M4-实现还有哪些明确边界"><a href="#15-当前-Cortex-M4-实现还有哪些明确边界" class="headerlink" title="15. 当前 Cortex-M4 实现还有哪些明确边界"></a>15. 当前 Cortex-M4 实现还有哪些明确边界</h2><p>这套支持已经进入主线，但它仍是非常新的 Cortex-M4 port 功能。理解下面几个边界，有助于避免把“功能已存在”误解成“所有工程配置都已成熟覆盖”。</p><h3 id="15-1-当前构建脚本限制-GCC-llvm-arm"><a href="#15-1-当前构建脚本限制-GCC-llvm-arm" class="headerlink" title="15.1 当前构建脚本限制 GCC &#x2F; llvm-arm"></a>15.1 当前构建脚本限制 GCC &#x2F; llvm-arm</h3><p><code>libcpu/arm/cortex-m4/SConscript</code> 当前逻辑为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">using_mpu = RT_USING_MEM_PROTECTION || RT_USING_HW_STACK_GUARD</span><br></pre></td></tr></table></figure><p>启用后只允许：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">gcc</span><br><span class="line">llvm-arm</span><br></pre></td></tr></table></figure><p>其他工具链会直接抛出错误。</p><p>因此使用 Keil ARMCC、ArmClang 或 IAR 的工程不能只打开 Kconfig 就认为当前实现可以直接工作。</p><h3 id="15-2-rt-hw-stack-guard-init-当前不向上处理-region-添加失败"><a href="#15-2-rt-hw-stack-guard-init-当前不向上处理-region-添加失败" class="headerlink" title="15.2 rt_hw_stack_guard_init() 当前不向上处理 region 添加失败"></a>15.2 <code>rt_hw_stack_guard_init()</code> 当前不向上处理 region 添加失败</h3><p>当前函数会连续调用两个：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">rt_mprotect_add_region()</span><br></pre></td></tr></table></figure><p>然后修改 <code>stack_addr/stack_size</code>。</p><p>从当前源码看，它没有把 <code>rt_mprotect_add_region()</code> 的错误状态返回给 <code>_thread_init()</code>。</p><p>因此如果内部 <code>mem_regions</code> heap 分配失败，或者添加 region 失败，错误传播链仍值得产品工程进一步审查。</p><p>这是根据当前源码得到的实现事实，不表示已确认存在某个线上故障。</p><h3 id="15-3-通用-MPU-配置-peripheral-时要继续核对-memory-type"><a href="#15-3-通用-MPU-配置-peripheral-时要继续核对-memory-type" class="headerlink" title="15.3 通用 MPU 配置 peripheral 时要继续核对 memory type"></a>15.3 通用 MPU 配置 peripheral 时要继续核对 memory type</h3><p>当前 Cortex-M4 <code>rt_hw_mpu_region_default_attr()</code> 对大部分低于 <code>0xE0000000</code> 的地址使用 <code>NORMAL_NON_CACHEABLE_SHAREABLE</code> 作为默认 memory type。</p><p>对于线程栈所在 SRAM，这条路径没有明显冲突。</p><p>但如果未来把通用 <code>rt_mprotect_add_region()</code> 用到 <code>0x40000000</code> 一类 peripheral&#x2F;MMIO 地址，只看 AP&#x2F;XN 权限还不够，还需要结合具体 MCU memory map 复核 Device&#x2F;Normal memory type 是否正确。</p><p>该函数被声明为 <code>rt_weak</code>，因此 BSP 可以根据芯片地址空间覆盖默认策略。</p><p>这也是使用 MPU 时一个很重要的原则：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">访问权限正确，不等于 memory type 一定正确。</span><br></pre></td></tr></table></figure><hr><h2 id="16-从启动到-Fault-的整条运行链路"><a href="#16-从启动到-Fault-的整条运行链路" class="headerlink" title="16. 从启动到 Fault 的整条运行链路"></a>16. 从启动到 Fault 的整条运行链路</h2><p>把前面的实现合并起来，RT-Thread Cortex-M4 硬件栈保护可以理解成四个阶段。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    Boot[&quot;系统启动&quot;] --&gt; MPUInit[&quot;rt_mprotect_init -&gt; rt_hw_mpu_init&quot;]</span><br><span class="line">    MPUInit --&gt; Static[&quot;配置 static MPU regions 并启用 MPU&quot;]</span><br><span class="line">    Static --&gt; ThreadCreate[&quot;线程初始化&quot;]</span><br><span class="line">    ThreadCreate --&gt; GuardInit[&quot;rt_hw_stack_guard_init&quot;]</span><br><span class="line">    GuardInit --&gt; RegionTable[&quot;保存上下两个 Guard 到线程 mem_regions&quot;]</span><br><span class="line">    RegionTable --&gt; Run[&quot;线程进入调度&quot;]</span><br><span class="line">    Run --&gt; PendSV[&quot;PendSV 线程切换&quot;]</span><br><span class="line">    PendSV --&gt; TableSwitch[&quot;rt_hw_mpu_table_switch&quot;]</span><br><span class="line">    TableSwitch --&gt; ProgramMPU[&quot;把当前线程 Guard 写入硬件 MPU&quot;]</span><br><span class="line">    ProgramMPU --&gt; Execute[&quot;线程运行&quot;]</span><br><span class="line">    Execute --&gt;|&quot;访问正常 stack&quot;| Execute</span><br><span class="line">    Execute --&gt;|&quot;访问 Guard&quot;| MemFault[&quot;MemManage Fault&quot;]</span><br><span class="line">    MemFault --&gt; Handler[&quot;MemManage_Handler&quot;]</span><br><span class="line">    Handler --&gt; Hook[&quot;用户 exception hook&quot;]</span><br><span class="line">    Hook --&gt; Stop[&quot;当前实现最终停在 while(1)&quot;]</span><br></pre></td></tr></table></figure><p>这张图可以概括成一句话：</p><blockquote><p>Guard 是在线程创建时计算和保存的，在上下文切换时装入硬件，在真正越界访问时由 MPU 触发 Fault。</p></blockquote><p>理解这三个时间点，就基本理解了 RT-Thread 这套设计。</p><hr><h2 id="17-为什么这种设计适合-MCU-RTOS"><a href="#17-为什么这种设计适合-MCU-RTOS" class="headerlink" title="17. 为什么这种设计适合 MCU RTOS"></a>17. 为什么这种设计适合 MCU RTOS</h2><p>在没有 MMU 的 Cortex-M4 上，RTOS 很难像 Linux 那样给每个线程建立独立虚拟地址空间，但 MPU 仍然能提供一种非常实用的“有限硬件边界”。</p><p>RT-Thread 当前设计没有试图让每个线程永久独占一套硬件 region，而是把：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">线程自己的保护策略</span><br></pre></td></tr></table></figure><p>保存在软件结构中，再把：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">当前正在运行线程的保护策略</span><br></pre></td></tr></table></figure><p>映射进有限的 MPU hardware slots。</p><p>这是典型的 MCU 资源复用思路：用少量硬件 region 换取任意数量线程的时分复用保护。</p><p>代价也很明确：</p><ul><li>TCB 和 heap 增加少量 RAM；</li><li>线程有效 stack 减少；</li><li>PendSV 增加 MPU register programming；</li><li>Fault 默认是终止性诊断路径，而不是自动恢复机制。</li></ul><p>因此它更适合被理解成“提高故障发现确定性和系统内存边界强度”的机制，而不是零成本的安全隔离方案。</p><p>对于 STM32F407 这类具有 MPU、但没有 MMU 的 Cortex-M4 MCU，这种取舍是合理且实用的。</p><hr><h2 id="18-结语"><a href="#18-结语" class="headerlink" title="18. 结语"></a>18. 结语</h2><p><code>RT_USING_MEM_PROTECTION</code> 和 <code>RT_USING_HW_STACK_GUARD</code> 的核心并不复杂：前者建立 RT-Thread 的 MPU 抽象层，后者利用这层能力，为每个线程栈生成两个 No-Access region。</p><p>真正值得理解的是 RT-Thread 如何把有限的 Cortex-M4 MPU region 和多线程调度结合起来：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">线程创建</span><br><span class="line">    -&gt; 建立 Guard 描述</span><br><span class="line">    -&gt; 保存到 per-thread mem_regions</span><br><span class="line"></span><br><span class="line">线程切换</span><br><span class="line">    -&gt; 根据 current_thread 重装 MPU</span><br><span class="line"></span><br><span class="line">线程运行</span><br><span class="line">    -&gt; MPU 实时检查访问权限</span><br><span class="line"></span><br><span class="line">栈越界</span><br><span class="line">    -&gt; MemManage Fault</span><br><span class="line">    -&gt; 收集线程、地址、region 和 MMFSR</span><br></pre></td></tr></table></figure><p>它不是软件 canary，也不是完整的进程内存隔离，而是一套与 RTOS 上下文切换绑定的硬件访问边界机制。</p><p>如果目标只是在线程栈失控时尽可能早地阻止内存继续被覆盖，那么 Cortex-M4 MPU Guard 相比单纯依赖软件检查提供了更直接的硬件故障边界；同时，软件 overflow check 仍可以继续承担 watermark 和传统检测职责，两者并不冲突。</p><p>对于使用 STM32F407、GCC 和当前 RT-Thread 主线的项目，先从 <code>static Flash RX + 双 Stack Guard</code> 这一最小组合开始，是目前最容易理解和控制的使用方式。等这一层稳定以后，再扩展到 per-thread RAM、exclusive region 或 MMIO 权限，会更容易区分权限设计、内存属性和调度开销各自带来的影响。</p><hr><h2 id="参考源码与资料"><a href="#参考源码与资料" class="headerlink" title="参考源码与资料"></a>参考源码与资料</h2><p>RT-Thread 主线源码：</p><ul><li><code>components/mprotect/Kconfig</code></li><li><code>components/mprotect/mprotect.c</code></li><li><code>components/mprotect/mprotect.h</code></li><li><code>components/mprotect/README.md</code></li><li><code>libcpu/arm/cortex-m4/mpu.c</code></li><li><code>libcpu/arm/cortex-m4/mpu.h</code></li><li><code>libcpu/arm/cortex-m4/mputype.h</code></li><li><code>libcpu/arm/cortex-m4/cpuport.c</code></li><li><code>libcpu/arm/cortex-m4/context_gcc.S</code></li><li><code>libcpu/arm/cortex-m4/SConscript</code></li><li><code>src/thread.c</code></li><li><code>bsp/stm32/stm32f407-fk407m2-zgt6/board/board.h</code></li><li><code>bsp/stm32/stm32f407-fk407m2-zgt6/board/board.c</code></li></ul><p>相关提交：</p><ul><li>RT-Thread commit <a href="https://github.com/RT-Thread/rt-thread/commit/e2e6245aace07b545b8dbcef8fbcdb4469f55d9f"><code>e2e6245aace07b545b8dbcef8fbcdb4469f55d9f</code></a>：<code>[arm][cortex-m4] Add hardware stack guard support</code></li><li>RT-Thread <a href="https://github.com/RT-Thread/rt-thread/tree/master/components/mprotect"><code>components/mprotect</code></a></li><li>RT-Thread <a href="https://github.com/RT-Thread/rt-thread/tree/master/libcpu/arm/cortex-m4"><code>libcpu/arm/cortex-m4</code></a></li><li>STM32 公共 BSP 初始化路径 <a href="https://github.com/RT-Thread/rt-thread/blob/master/bsp/stm32/libraries/HAL_Drivers/drv_common.c"><code>drv_common.c</code></a></li></ul><p>硬件资料：</p><ul><li>ST <a href="https://www.st.com/resource/en/application_note/an4838-introduction-to-memory-protection-unit-management-on-stm32-mcus-stmicroelectronics.pdf">AN4838：Introduction to memory protection unit management on STM32 MCUs</a></li><li>ST <a href="https://www.st.com/resource/en/programming_manual/dm00046982-stm32-cortexm4-mcus-and-mpus-programming-manual-stmicroelectronics.pdf">PM0214：STM32 Cortex-M4 MCUs and MPUs programming manual</a></li><li>Arm Cortex-M4 Processor Technical Reference &#x2F; Architecture documentation</li></ul><blockquote><p>版本说明：文章依据截至 2026-08-27 的 RT-Thread <code>master</code> 实现整理。Cortex-M4 MPU&#x2F;Stack Guard 支持属于近期新增功能，旧版 RT-Thread 即使存在同名 Kconfig，也应检查 <code>libcpu/arm/cortex-m4/</code> 是否已经包含完整 MPU port、PendSV MPU table switch 和 <code>rt_hw_stack_guard_init()</code>。</p></blockquote>]]></content>
    
    
      
      
    <summary type="html">&lt;meta name=&quot;referrer&quot; content=&quot;no-referrer&quot; /&gt;

&lt;h1 id=&quot;RT-Thread-如何用-MPU-做线程栈保护&quot;&gt;&lt;a href=&quot;#RT-Thread-如何用-MPU-做线程栈保护&quot; class=&quot;headerlink&quot; tit</summary>
      
    
    
    
    <category term="rt-thread" scheme="https://wdfk-prog.space/categories/rt-thread/"/>
    
    
    <category term="rt-thread" scheme="https://wdfk-prog.space/tags/rt-thread/"/>
    
  </entry>
  
  <entry>
    <title>GitHub push 失败：如何扫描并清理 Git 历史中的大文件</title>
    <link href="https://wdfk-prog.space/posts/c38fc09/"/>
    <id>https://wdfk-prog.space/posts/c38fc09/</id>
    <published>2026-08-28T01:45:16.000Z</published>
    <updated>2026-08-28T01:51:26.839Z</updated>
    
    <content type="html"><![CDATA[<meta name="referrer" content="no-referrer" /><h1 id="GitHub-push-失败：如何扫描并清理-Git-历史中的大文件"><a href="#GitHub-push-失败：如何扫描并清理-Git-历史中的大文件" class="headerlink" title="GitHub push 失败：如何扫描并清理 Git 历史中的大文件"></a>GitHub push 失败：如何扫描并清理 Git 历史中的大文件</h1><p>@[toc]<br><img src="https://i-blog.csdnimg.cn/direct/d3f37ed1c409438ca25977d9033197c5.png" alt="在这里插入图片描述"></p><blockquote><p>摘要：从 GitHub 100 MiB 大文件拒绝问题出发，说明为何删除文件后仍无法推送，并给出 PowerShell 扫描、历史清理、Git LFS 与防复发方案。</p></blockquote><p>在把一个已有 Git 项目首次推送到 GitHub 时，可能会遇到如下错误：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">remote: error: File vendor/example/.git.zip is 151.91 MB;</span><br><span class="line">remote: error: this exceeds GitHub&#x27;s file size limit of 100.00 MB</span><br><span class="line">remote: error: GH001: Large files detected.</span><br><span class="line">! [remote rejected] master -&gt; master (pre-receive hook declined)</span><br></pre></td></tr></table></figure><p>这里最容易产生的误解是：既然问题文件已经不需要了，那么再新增一个 commit 把它删除，然后重新 <code>git push</code> 不就可以了吗？</p><p>答案是否定的。</p><p>GitHub 检查的不只是当前工作区最终长什么样，还会接收并检查这次 push 所需要传输的 Git 对象。只要超大文件仍存在于待上传的历史提交中，即使后面的 commit 已经把文件删除，那个大文件对应的 Git blob 仍然属于历史的一部分，push 依然可能被拒绝。</p><p>这类问题真正需要处理的是“Git 历史中的大对象”，而不仅仅是“当前目录里的大文件”。</p><h2 id="1-GitHub-为什么会拒绝大文件"><a href="#1-GitHub-为什么会拒绝大文件" class="headerlink" title="1. GitHub 为什么会拒绝大文件"></a>1. GitHub 为什么会拒绝大文件</h2><p>截至本文整理时，GitHub 官方文档对普通 Git 仓库的文件大小约束为：</p><ul><li>通过浏览器上传时，单文件不能超过 25 MiB；</li><li>普通 Git 操作添加或更新超过 50 MiB 的文件时会收到警告；</li><li>普通 Git 仓库会阻止超过 100 MiB 的文件；</li><li>如果确实需要版本化管理更大的文件，应考虑 Git Large File Storage（Git LFS）。</li></ul><p>这里需要区分“仓库当前版本”和“Git 历史”。Git 的 commit 并不是简单地保存一份文件列表，而是通过 tree、blob 等对象构成历史快照。一个 150 MiB 的压缩包一旦进入某个 commit，即使后续删除，它对应的 blob 仍可能继续保存在历史中。</p><p>例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">A ---- B ---- C</span><br><span class="line">       |      |</span><br><span class="line">       |      +-- 删除 big-file.zip</span><br><span class="line">       |</span><br><span class="line">       +--------- 提交 big-file.zip（150 MiB）</span><br></pre></td></tr></table></figure><p>当前 HEAD 位于 C，工作目录中已经没有 <code>big-file.zip</code>。</p><p>但是第一次向远端推送 A、B、C 时，B 所引用的 150 MiB blob 仍然必须被传输。GitHub 因此仍会拒绝这次 push。</p><p>所以：</p><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">git <span class="built_in">rm</span> <span class="literal">--cached</span> <span class="string">&quot;path/to/big-file.zip&quot;</span></span><br><span class="line">git commit <span class="literal">-m</span> <span class="string">&quot;chore: remove large file&quot;</span></span><br></pre></td></tr></table></figure><p>只能表示“从这个新 commit 开始不再跟踪该文件”，不能删除旧 commit 中已经存在的 blob。</p><p><code>.gitignore</code> 也是同样的道理。它只能阻止后续未跟踪文件被再次加入 Git，不会自动修改历史。</p><h2 id="2-先判断：大文件是在当前目录，还是已经进入-Git-历史"><a href="#2-先判断：大文件是在当前目录，还是已经进入-Git-历史" class="headerlink" title="2. 先判断：大文件是在当前目录，还是已经进入 Git 历史"></a>2. 先判断：大文件是在当前目录，还是已经进入 Git 历史</h2><p>处理之前建议分别扫描两遍。</p><p>第一遍检查当前工作区，可以知道哪些超大文件现在还真实存在于项目目录中；第二遍检查 Git 对象历史，可以找出已经删除、移动或改名，但仍残留在历史中的大文件。</p><p>以下命令均按 Windows PowerShell 编写。</p><h3 id="2-1-扫描当前项目中的-100-MiB-以上文件"><a href="#2-1-扫描当前项目中的-100-MiB-以上文件" class="headerlink" title="2.1 扫描当前项目中的 100 MiB 以上文件"></a>2.1 扫描当前项目中的 100 MiB 以上文件</h3><p>在 Git 仓库根目录执行：</p><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="variable">$Limit</span> = <span class="number">100</span>MB</span><br><span class="line"><span class="variable">$Root</span> = (<span class="built_in">Get-Location</span>).Path</span><br><span class="line"></span><br><span class="line"><span class="variable">$LargeFiles</span> = <span class="built_in">Get-ChildItem</span> <span class="operator">-File</span> <span class="literal">-Recurse</span> <span class="literal">-Force</span> |</span><br><span class="line">    <span class="built_in">Where-Object</span> &#123;</span><br><span class="line">        <span class="variable">$_</span>.FullName <span class="operator">-notmatch</span> <span class="string">&#x27;[\\/]\.git[\\/]&#x27;</span> <span class="operator">-and</span></span><br><span class="line">        <span class="variable">$_</span>.Length <span class="operator">-ge</span> <span class="variable">$Limit</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"><span class="variable">$LargeFiles</span> |</span><br><span class="line">    <span class="built_in">Sort-Object</span> Length <span class="literal">-Descending</span> |</span><br><span class="line">    <span class="built_in">ForEach-Object</span> &#123;</span><br><span class="line">        <span class="variable">$RelativePath</span> = <span class="variable">$_</span>.FullName.Substring(<span class="variable">$Root</span>.Length + <span class="number">1</span>)</span><br><span class="line">        <span class="string">&quot;&#123;0:N2&#125; MiB`t&#123;1&#125;&quot;</span> <span class="operator">-f</span> (<span class="variable">$_</span>.Length / <span class="number">1</span>MB), <span class="variable">$RelativePath</span></span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure><p>示例输出：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">151.91 MiB    vendor/example/.git.zip</span><br><span class="line">126.35 MiB    output/archive.bin</span><br></pre></td></tr></table></figure><p>这里显式排除了仓库自己的 <code>.git</code> 目录，否则 Git pack 文件本身也可能被扫描出来，造成干扰。</p><h3 id="2-2-扫描整个-Git-历史中的-100-MiB-以上-blob"><a href="#2-2-扫描整个-Git-历史中的-100-MiB-以上-blob" class="headerlink" title="2.2 扫描整个 Git 历史中的 100 MiB 以上 blob"></a>2.2 扫描整个 Git 历史中的 100 MiB 以上 blob</h3><p>仅扫描工作目录是不够的。真正决定 GitHub 是否会因为旧提交而拒绝 push 的，是历史对象。</p><p>执行：</p><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="variable">$Limit</span> = <span class="number">100</span>MB</span><br><span class="line"></span><br><span class="line">git rev<span class="literal">-list</span> <span class="literal">--objects</span> <span class="literal">--all</span> |</span><br><span class="line">    git <span class="built_in">cat</span><span class="operator">-file</span> <span class="literal">--batch-check</span>=<span class="string">&quot;%(objecttype) %(objectname) %(objectsize) %(rest)&quot;</span> |</span><br><span class="line">    <span class="built_in">ForEach-Object</span> &#123;</span><br><span class="line">        <span class="variable">$Fields</span> = <span class="variable">$_</span> <span class="operator">-split</span> <span class="string">&#x27; &#x27;</span>, <span class="number">4</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (</span><br><span class="line">            <span class="variable">$Fields</span>.Count <span class="operator">-ge</span> <span class="number">4</span> <span class="operator">-and</span></span><br><span class="line">            <span class="variable">$Fields</span>[<span class="number">0</span>] <span class="operator">-eq</span> <span class="string">&quot;blob&quot;</span> <span class="operator">-and</span></span><br><span class="line">            [<span class="built_in">int</span><span class="type">64</span>]<span class="variable">$Fields</span>[<span class="number">2</span>] <span class="operator">-ge</span> <span class="variable">$Limit</span></span><br><span class="line">        ) &#123;</span><br><span class="line">            <span class="string">&quot;&#123;0:N2&#125; MiB`t&#123;1&#125;&quot;</span> <span class="operator">-f</span> ([<span class="built_in">int</span><span class="type">64</span>]<span class="variable">$Fields</span>[<span class="number">2</span>] / <span class="number">1</span>MB), <span class="variable">$Fields</span>[<span class="number">3</span>]</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure><p>这组命令的逻辑是：</p><ol><li><code>git rev-list --objects --all</code> 枚举所有可达 Git 对象及路径；</li><li><code>git cat-file --batch-check</code> 查询对象类型和实际大小；</li><li>只保留 <code>blob</code>；</li><li>再筛选大小达到 100 MiB 的对象。</li></ol><p>如果当前目录已经没有某个文件，但这里仍然能看到它，说明问题就在 Git 历史中。</p><h2 id="3-先决定大文件到底应该怎么管理"><a href="#3-先决定大文件到底应该怎么管理" class="headerlink" title="3. 先决定大文件到底应该怎么管理"></a>3. 先决定大文件到底应该怎么管理</h2><p>发现大文件后，不要马上批量删除。先判断它属于哪一种情况。</p><table><thead><tr><th>文件类型或用途</th><th>推荐处理</th></tr></thead><tbody><tr><td>临时压缩包、构建产物、缓存、日志、备份</td><td>从 Git 历史删除，并加入 <code>.gitignore</code></td></tr><tr><td><code>.git.zip</code>、嵌套仓库备份等 Git 元数据</td><td>通常直接排除，不建议放进 Git LFS</td></tr><tr><td>必须随项目版本化的大型模型、资源、媒体或二进制文件</td><td>使用 Git LFS</td></tr><tr><td>发布给用户下载的固件包、安装包、大型归档</td><td>更适合 GitHub Release 或独立制品存储</td></tr></tbody></table><p>尤其是类似：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">vendor/example/.git.zip</span><br></pre></td></tr></table></figure><p>这种文件通常只是另一个 Git 仓库元数据的压缩备份。把它继续放进主仓库，无论普通 Git 还是 Git LFS，通常都没有必要，而且容易让仓库体积不断膨胀。</p><p>本文后面的批量清理方案适用于“这些大文件本来就不应该进入普通 Git 历史”的情况。</p><p>如果扫描结果中存在必须保留版本的大文件，应先跳到“Git LFS”一节，不要直接用 <code>--strip-blobs-bigger-than</code> 一刀切。</p><h2 id="4-批量清理所有不需要的-100-MiB-以上历史文件"><a href="#4-批量清理所有不需要的-100-MiB-以上历史文件" class="headerlink" title="4. 批量清理所有不需要的 100 MiB 以上历史文件"></a>4. 批量清理所有不需要的 100 MiB 以上历史文件</h2><p>历史重写属于破坏性操作。对于个人仓库、首次推送且 GitHub 已经整体拒绝的仓库，风险通常较低；对于已经多人协作、远端已有分支和 tag 的仓库，则必须先协调，因为历史重写会改变相关 commit 的 SHA。</p><h3 id="4-1-先确认仓库状态"><a href="#4-1-先确认仓库状态" class="headerlink" title="4.1 先确认仓库状态"></a>4.1 先确认仓库状态</h3><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">git status</span><br><span class="line">git branch <span class="literal">--show-current</span></span><br><span class="line">git remote <span class="literal">-v</span></span><br></pre></td></tr></table></figure><p>如果当前还有未提交修改，建议先处理干净再进行历史重写。</p><p>还可以额外创建一个仓库级备份：</p><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git bundle create <span class="string">&quot;../repository-before-large-file-cleanup.bundle&quot;</span> <span class="literal">--all</span></span><br></pre></td></tr></table></figure><p>这个 bundle 位于当前仓库目录之外，可以在误操作时作为恢复来源。</p><h3 id="4-2-备份当前仍存在的大文件"><a href="#4-2-备份当前仍存在的大文件" class="headerlink" title="4.2 备份当前仍存在的大文件"></a>4.2 备份当前仍存在的大文件</h3><p>历史清理可能导致这些文件从当前工作树消失。如果目标是“文件继续留在电脑上，只是不进入 Git”，可以先复制到仓库外的临时目录。</p><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="variable">$Limit</span> = <span class="number">100</span>MB</span><br><span class="line"><span class="variable">$Root</span> = (<span class="built_in">Get-Location</span>).Path</span><br><span class="line"><span class="variable">$Backup</span> = <span class="built_in">Join-Path</span> <span class="variable">$env:TEMP</span> <span class="string">&quot;git-large-files-backup&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="variable">$LargeFiles</span> = <span class="built_in">Get-ChildItem</span> <span class="operator">-File</span> <span class="literal">-Recurse</span> <span class="literal">-Force</span> |</span><br><span class="line">    <span class="built_in">Where-Object</span> &#123;</span><br><span class="line">        <span class="variable">$_</span>.FullName <span class="operator">-notmatch</span> <span class="string">&#x27;[\\/]\.git[\\/]&#x27;</span> <span class="operator">-and</span></span><br><span class="line">        <span class="variable">$_</span>.Length <span class="operator">-ge</span> <span class="variable">$Limit</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"><span class="built_in">Remove-Item</span> <span class="variable">$Backup</span> <span class="literal">-Recurse</span> <span class="literal">-Force</span> <span class="literal">-ErrorAction</span> SilentlyContinue</span><br><span class="line"><span class="built_in">New-Item</span> <span class="literal">-ItemType</span> Directory <span class="literal">-Path</span> <span class="variable">$Backup</span> <span class="literal">-Force</span> | <span class="built_in">Out-Null</span></span><br><span class="line"></span><br><span class="line"><span class="variable">$Paths</span> = <span class="selector-tag">@</span>()</span><br><span class="line"></span><br><span class="line"><span class="keyword">foreach</span> (<span class="variable">$File</span> <span class="keyword">in</span> <span class="variable">$LargeFiles</span>) &#123;</span><br><span class="line">    <span class="variable">$Path</span> = <span class="variable">$File</span>.FullName.Substring(<span class="variable">$Root</span>.Length + <span class="number">1</span>)</span><br><span class="line">    <span class="variable">$Paths</span> += <span class="variable">$Path</span></span><br><span class="line"></span><br><span class="line">    <span class="variable">$Destination</span> = <span class="built_in">Join-Path</span> <span class="variable">$Backup</span> <span class="variable">$Path</span></span><br><span class="line">    <span class="built_in">New-Item</span> <span class="literal">-ItemType</span> Directory <span class="literal">-Path</span> (<span class="built_in">Split-Path</span> <span class="variable">$Destination</span>) <span class="literal">-Force</span> | <span class="built_in">Out-Null</span></span><br><span class="line">    <span class="built_in">Copy-Item</span> <span class="literal">-LiteralPath</span> <span class="variable">$File</span>.FullName <span class="literal">-Destination</span> <span class="variable">$Destination</span> <span class="literal">-Force</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里记录的是当前工作区中的大文件。历史中已经不存在于工作区的旧文件不需要恢复到本地。</p><h3 id="4-3-安装-git-filter-repo"><a href="#4-3-安装-git-filter-repo" class="headerlink" title="4.3 安装 git-filter-repo"></a>4.3 安装 <code>git-filter-repo</code></h3><p>GitHub 官方文档在处理需要重写历史的数据时也使用 <code>git-filter-repo</code>。Windows + Python 环境下可以通过 pip 安装：</p><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">python <span class="literal">-m</span> pip install git<span class="literal">-filter-repo</span></span><br></pre></td></tr></table></figure><p>安装后确认：</p><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git <span class="keyword">filter</span><span class="literal">-repo</span> <span class="literal">--version</span></span><br></pre></td></tr></table></figure><p>如果 PowerShell 找不到该命令，需要检查 Python 的 Scripts 目录是否已经加入 <code>PATH</code>。</p><h3 id="4-4-从整个-Git-历史删除超过-100-MiB-的-blob"><a href="#4-4-从整个-Git-历史删除超过-100-MiB-的-blob" class="headerlink" title="4.4 从整个 Git 历史删除超过 100 MiB 的 blob"></a>4.4 从整个 Git 历史删除超过 100 MiB 的 blob</h3><p>确认扫描结果中的所有超大 blob 都可以删除后，执行：</p><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git <span class="keyword">filter</span><span class="literal">-repo</span> <span class="literal">--strip-blobs-bigger-than</span> <span class="number">100</span>M <span class="literal">--force</span></span><br></pre></td></tr></table></figure><p>这条命令的关键不是删除“当前文件”，而是重写 Git 历史，把所有超过阈值的 blob 从可达历史中剔除。</p><p>因此它有两个重要影响：</p><ul><li>相关历史 commit 的 SHA 会变化；</li><li>如果远端已经存在这些旧 commit，后续必须以历史重写的方式更新远端。</li></ul><p>这也是为什么它不能被简单理解为“新增一个删除 commit”。</p><h2 id="5-恢复本地文件，并用一个新-commit-防止再次提交"><a href="#5-恢复本地文件，并用一个新-commit-防止再次提交" class="headerlink" title="5. 恢复本地文件，并用一个新 commit 防止再次提交"></a>5. 恢复本地文件，并用一个新 commit 防止再次提交</h2><p>历史清理完成以后，如果这些大文件仍需要留在电脑上，可以从临时目录恢复，并把对应路径加入 <code>.gitignore</code>。</p><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (!(<span class="built_in">Test-Path</span> <span class="string">&quot;.gitignore&quot;</span>)) &#123;</span><br><span class="line">    <span class="built_in">New-Item</span> <span class="literal">-ItemType</span> File <span class="literal">-Path</span> <span class="string">&quot;.gitignore&quot;</span> | <span class="built_in">Out-Null</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">foreach</span> (<span class="variable">$Path</span> <span class="keyword">in</span> <span class="variable">$Paths</span>) &#123;</span><br><span class="line">    <span class="variable">$Source</span> = <span class="built_in">Join-Path</span> <span class="variable">$Backup</span> <span class="variable">$Path</span></span><br><span class="line">    <span class="variable">$Directory</span> = <span class="built_in">Split-Path</span> <span class="variable">$Path</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (<span class="variable">$Directory</span>) &#123;</span><br><span class="line">        <span class="built_in">New-Item</span> <span class="literal">-ItemType</span> Directory <span class="literal">-Path</span> <span class="variable">$Directory</span> <span class="literal">-Force</span> | <span class="built_in">Out-Null</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">Copy-Item</span> <span class="literal">-LiteralPath</span> <span class="variable">$Source</span> <span class="literal">-Destination</span> <span class="variable">$Path</span> <span class="literal">-Force</span></span><br><span class="line"></span><br><span class="line">    <span class="variable">$IgnorePath</span> = <span class="string">&quot;/&quot;</span> + <span class="variable">$Path</span>.Replace(<span class="string">&quot;\&quot;</span>, <span class="string">&quot;/&quot;</span>)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> ((<span class="built_in">Get-Content</span> <span class="string">&quot;.gitignore&quot;</span>) <span class="operator">-notcontains</span> <span class="variable">$IgnorePath</span>) &#123;</span><br><span class="line">        <span class="built_in">Add-Content</span> <span class="literal">-Path</span> <span class="string">&quot;.gitignore&quot;</span> <span class="literal">-Value</span> <span class="variable">$IgnorePath</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>然后只提交 <code>.gitignore</code>：</p><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">git add <span class="literal">--</span> .gitignore</span><br><span class="line">git commit <span class="literal">-m</span> <span class="string">&quot;chore: exclude oversized files&quot;</span></span><br></pre></td></tr></table></figure><p>从使用者视角看，这样可以把“以后不要再提交这些本地大文件”的策略集中放在一个新的 commit 中。</p><p>但需要准确理解：</p><blockquote><p><code>.gitignore</code> 可以集中成一个新 commit；历史中的大 blob 则是通过 <code>git filter-repo</code> 被移除的，历史重写会改变旧 commit 的对象关系和 SHA。</p></blockquote><p>不存在一种“完全不改旧历史、只新增一个 commit，同时又让 GitHub 看不到旧大文件”的普通 Git 操作。</p><h2 id="6-清理后再次扫描，而不是直接-push"><a href="#6-清理后再次扫描，而不是直接-push" class="headerlink" title="6. 清理后再次扫描，而不是直接 push"></a>6. 清理后再次扫描，而不是直接 push</h2><p>先重新执行历史扫描：</p><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="variable">$Limit</span> = <span class="number">100</span>MB</span><br><span class="line"></span><br><span class="line">git rev<span class="literal">-list</span> <span class="literal">--objects</span> <span class="literal">--all</span> |</span><br><span class="line">    git <span class="built_in">cat</span><span class="operator">-file</span> <span class="literal">--batch-check</span>=<span class="string">&quot;%(objecttype) %(objectname) %(objectsize) %(rest)&quot;</span> |</span><br><span class="line">    <span class="built_in">ForEach-Object</span> &#123;</span><br><span class="line">        <span class="variable">$Fields</span> = <span class="variable">$_</span> <span class="operator">-split</span> <span class="string">&#x27; &#x27;</span>, <span class="number">4</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (</span><br><span class="line">            <span class="variable">$Fields</span>.Count <span class="operator">-ge</span> <span class="number">4</span> <span class="operator">-and</span></span><br><span class="line">            <span class="variable">$Fields</span>[<span class="number">0</span>] <span class="operator">-eq</span> <span class="string">&quot;blob&quot;</span> <span class="operator">-and</span></span><br><span class="line">            [<span class="built_in">int</span><span class="type">64</span>]<span class="variable">$Fields</span>[<span class="number">2</span>] <span class="operator">-ge</span> <span class="variable">$Limit</span></span><br><span class="line">        ) &#123;</span><br><span class="line">            <span class="string">&quot;&#123;0:N2&#125; MiB`t&#123;1&#125;&quot;</span> <span class="operator">-f</span> ([<span class="built_in">int</span><span class="type">64</span>]<span class="variable">$Fields</span>[<span class="number">2</span>] / <span class="number">1</span>MB), <span class="variable">$Fields</span>[<span class="number">3</span>]</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure><p>如果没有任何输出，说明当前所有可达 Git 历史中已经没有达到 100 MiB 的普通 blob。</p><p>再检查本地文件是否确实被忽略：</p><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git status <span class="literal">--ignored</span></span><br></pre></td></tr></table></figure><p>以及确认最终提交：</p><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git log <span class="literal">--oneline</span> <span class="literal">-5</span></span><br></pre></td></tr></table></figure><h2 id="7-最后如何重新推送-GitHub"><a href="#7-最后如何重新推送-GitHub" class="headerlink" title="7. 最后如何重新推送 GitHub"></a>7. 最后如何重新推送 GitHub</h2><p>先检查远端：</p><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git remote <span class="literal">-v</span></span><br></pre></td></tr></table></figure><p>历史清理工具可能会调整远端配置，因此不要直接假设原来的远端名称还存在。</p><p>如果这是第一次向 GitHub 推送，而且之前的 push 已经整体被拒绝、远端目标分支实际上还没有建立，那么重新添加或确认远端后，通常直接正常 push 即可：</p><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git push <span class="literal">-u</span> origin master</span><br></pre></td></tr></table></figure><p>如果你的远端名称不是 <code>origin</code>，例如叫 <code>github</code>：</p><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git push <span class="literal">-u</span> github master</span><br></pre></td></tr></table></figure><p>如果远端已经存在旧历史，并且你明确要用清理后的本地历史替换它，应先与其他协作者确认，再使用：</p><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git push <span class="literal">-u</span> origin master <span class="literal">--force-with-lease</span></span><br></pre></td></tr></table></figure><p>相比无条件的 <code>--force</code>，<code>--force-with-lease</code> 会额外检查远端引用是否仍符合本地预期，可以降低覆盖他人新提交的风险。</p><p>多人共享仓库中，历史重写之后其他开发者已有的 clone 也需要同步处理，不能把它当成一次普通的 fast-forward push。</p><h2 id="8-如果大文件必须保留：使用-Git-LFS，而不是删除"><a href="#8-如果大文件必须保留：使用-Git-LFS，而不是删除" class="headerlink" title="8. 如果大文件必须保留：使用 Git LFS，而不是删除"></a>8. 如果大文件必须保留：使用 Git LFS，而不是删除</h2><p>Git LFS 的机制不是把超大二进制直接作为普通 Git blob 存入仓库，而是在 Git 历史里保存较小的 pointer，由 LFS 存储实际对象。</p><p>对于以后新增的大文件，可以先配置跟踪规则：</p><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">git lfs install</span><br><span class="line">git lfs track <span class="string">&quot;*.bin&quot;</span></span><br><span class="line">git lfs track <span class="string">&quot;*.zip&quot;</span></span><br><span class="line"></span><br><span class="line">git add <span class="literal">--</span> .gitattributes</span><br><span class="line">git commit <span class="literal">-m</span> <span class="string">&quot;chore: track large binaries with Git LFS&quot;</span></span><br></pre></td></tr></table></figure><p>之后再正常 <code>git add</code>、<code>git commit</code>。</p><p>但有一个非常重要的限制：</p><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git lfs track <span class="string">&quot;*.zip&quot;</span></span><br></pre></td></tr></table></figure><p>不会把旧 commit 中已经存在的普通 Git blob 自动转换成 LFS 对象。</p><p>如果大文件已经进入历史，需要使用 <code>git lfs migrate</code>。例如先查看整个历史的情况：</p><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git lfs migrate info <span class="literal">--everything</span></span><br></pre></td></tr></table></figure><p>再针对确实应该进入 LFS 的类型执行迁移，例如：</p><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git lfs migrate import <span class="literal">--everything</span> <span class="literal">--include</span>=<span class="string">&quot;*.bin,*.zip&quot;</span></span><br></pre></td></tr></table></figure><p>Git LFS 官方文档明确说明，这类历史迁移同样会重写历史并改变 Git 对象 ID，所以如果远端已有对应历史，仍然需要协调并进行强制更新。</p><p>因此，Git LFS 解决的是“这个大文件应该继续版本化，但不适合普通 Git blob”的问题；<code>git filter-repo</code> 删除历史解决的是“这个大文件根本不应该进入仓库”的问题。两者不能混为一谈。</p><h2 id="9-一套适合首次推送失败场景的处理顺序"><a href="#9-一套适合首次推送失败场景的处理顺序" class="headerlink" title="9. 一套适合首次推送失败场景的处理顺序"></a>9. 一套适合首次推送失败场景的处理顺序</h2><p>对于下面这种典型情况：</p><ul><li>本地已有较长 Git 历史；</li><li>第一次推送 GitHub；</li><li>GitHub 因历史中存在 100 MiB 以上文件而整体拒绝；</li><li>大文件属于备份、缓存、构建产物或嵌套 <code>.git</code> 压缩包；</li><li>希望文件继续留在本机，但不进入远端仓库；</li></ul><p>推荐流程可以归纳为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line">扫描当前工作区大文件</span><br><span class="line">        ↓</span><br><span class="line">扫描完整 Git 历史中的大 blob</span><br><span class="line">        ↓</span><br><span class="line">人工确认哪些文件不应进入 Git</span><br><span class="line">        ↓</span><br><span class="line">仓库外备份当前仍存在的大文件</span><br><span class="line">        ↓</span><br><span class="line">git filter-repo 重写历史并删除大 blob</span><br><span class="line">        ↓</span><br><span class="line">恢复本地文件</span><br><span class="line">        ↓</span><br><span class="line">.gitignore 排除这些文件</span><br><span class="line">        ↓</span><br><span class="line">新增一个防复发 commit</span><br><span class="line">        ↓</span><br><span class="line">重新扫描 Git 历史</span><br><span class="line">        ↓</span><br><span class="line">确认无超限对象后再 push</span><br></pre></td></tr></table></figure><p>这比反复执行“删除文件 → 新增 commit → 再 push”更可靠，因为它直接处理了 GitHub 真正拒绝的对象：历史中的超大 blob。</p><h2 id="10-防止下一次再踩同样的问题"><a href="#10-防止下一次再踩同样的问题" class="headerlink" title="10. 防止下一次再踩同样的问题"></a>10. 防止下一次再踩同样的问题</h2><p>对于源码仓库，最有效的防复发措施不是等 GitHub 拒绝后再清理，而是在提交之前区分“源码”和“制品”。</p><p>通常应该优先排除：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"># Build outputs</span><br><span class="line">/build/</span><br><span class="line">/out/</span><br><span class="line">/dist/</span><br><span class="line"></span><br><span class="line"># Temporary archives</span><br><span class="line">*.tmp</span><br><span class="line">*.bak</span><br><span class="line"></span><br><span class="line"># Nested Git metadata backups</span><br><span class="line">**/.git.zip</span><br></pre></td></tr></table></figure><p>实际项目不能机械复制这些规则，应根据构建目录和交付方式调整。例如某些项目确实需要版本化少量 ZIP 测试资源，就不能直接使用全局 <code>*.zip</code> 忽略规则。</p><p>提交前还可以快速检查暂存区：</p><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git <span class="built_in">diff</span> <span class="literal">--cached</span> <span class="literal">--name-only</span></span><br></pre></td></tr></table></figure><p>如果项目经常产生大型固件、模型、日志或压缩制品，则应该在仓库层面提前约定：</p><ul><li>哪些文件属于源码；</li><li>哪些文件用 Git LFS；</li><li>哪些文件发布到 GitHub Release；</li><li>哪些文件只存放于制品服务器或对象存储；</li><li>哪些目录必须进入 <code>.gitignore</code>。</li></ul><p>这样比事后清洗 Git 历史的成本低得多。</p><h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>GitHub 的大文件报错表面上是一个“文件超过 100 MiB”的问题，本质上却经常是对 Git 历史模型理解不完整导致的：当前目录中看不到文件，并不意味着 Git 历史中不存在它；新增一个删除 commit，也不等于删除旧 blob。</p><p>处理这类问题时，最值得记住的判断顺序只有三步：</p><ol><li>先扫描当前工作区，再扫描完整 Git 历史；</li><li>判断大文件应该“删除历史”还是“迁移到 Git LFS”；</li><li>只在确认历史中没有不允许的超大 blob 后再推送。</li></ol><p>如果大文件本来就不应该进入仓库，<code>git filter-repo</code> + <code>.gitignore</code> 是直接的处理路线；如果大文件本来就需要版本化，则应该设计 Git LFS 或独立制品管理方案，而不是单纯删除。</p><h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ul><li>GitHub Docs — About large files on GitHub<br><a href="https://docs.github.com/en/repositories/working-with-files/managing-large-files/about-large-files-on-github">https://docs.github.com/en/repositories/working-with-files/managing-large-files/about-large-files-on-github</a></li><li>GitHub Docs — Adding a file to a repository<br><a href="https://docs.github.com/en/repositories/working-with-files/managing-files/adding-a-file-to-a-repository">https://docs.github.com/en/repositories/working-with-files/managing-files/adding-a-file-to-a-repository</a></li><li>GitHub Docs — Removing sensitive data from a repository（包含 <code>git-filter-repo</code> 历史重写说明）<br><a href="https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/removing-sensitive-data-from-a-repository">https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/removing-sensitive-data-from-a-repository</a></li><li><code>git-filter-repo</code> — 官方 GitHub 仓库与文档<br><a href="https://github.com/newren/git-filter-repo">https://github.com/newren/git-filter-repo</a></li><li>Git LFS — <code>git lfs migrate</code> 文档<br><a href="https://github.com/git-lfs/git-lfs/blob/main/docs/man/git-lfs-migrate.adoc">https://github.com/git-lfs/git-lfs/blob/main/docs/man/git-lfs-migrate.adoc</a></li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;meta name=&quot;referrer&quot; content=&quot;no-referrer&quot; /&gt;

&lt;h1 id=&quot;GitHub-push-失败：如何扫描并清理-Git-历史中的大文件&quot;&gt;&lt;a href=&quot;#GitHub-push-失败：如何扫描并清理-Git-历史中的大文件&quot; cl</summary>
      
    
    
    
    <category term="git" scheme="https://wdfk-prog.space/categories/git/"/>
    
    
    <category term="git" scheme="https://wdfk-prog.space/tags/git/"/>
    
  </entry>
  
  <entry>
    <title>Lely CANopen 启动与从机联机后的运行机制</title>
    <link href="https://wdfk-prog.space/posts/dea69d33/"/>
    <id>https://wdfk-prog.space/posts/dea69d33/</id>
    <published>2026-08-03T05:30:43.000Z</published>
    <updated>2026-08-03T05:32:27.435Z</updated>
    
    <content type="html"><![CDATA[<meta name="referrer" content="no-referrer" /><h1 id="Lely-CANopen-启动与从机联机后的运行机制：Executor、Loop、Timer、CAN-与-Future-Promise"><a href="#Lely-CANopen-启动与从机联机后的运行机制：Executor、Loop、Timer、CAN-与-Future-Promise" class="headerlink" title="Lely CANopen 启动与从机联机后的运行机制：Executor、Loop、Timer、CAN 与 Future&#x2F;Promise"></a>Lely CANopen 启动与从机联机后的运行机制：Executor、Loop、Timer、CAN 与 Future&#x2F;Promise</h1><blockquote><p>摘要：梳理 Lely CANopen 主站启动、从机 boot 和稳定运行阶段实际存在的任务、事件循环、逻辑定时器、CAN 报文及 Future&#x2F;Promise 完成链路。<br><img src="https://i-blog.csdnimg.cn/direct/46210d8ed9ba4318ae85e8227e4b0a86.png" alt="在这里插入图片描述"></p></blockquote><p>@[toc]</p><h2 id="结论"><a href="#结论" class="headerlink" title="结论"></a>结论</h2><p>以 Linux、SocketCAN、<code>liblely-io2</code>、<code>liblely-ev</code>、<code>liblely-coapp</code> 和 <code>canopen::AsyncMaster</code> 的典型主站为基线，可以先得到六个结论：</p><ol><li><strong>通常只有一个主 <code>ev::Loop</code>，不是每个从机一个 Loop。</strong> 多个从机的 NMT boot、SDO、Heartbeat 和 PDO 状态机可由同一个事件循环驱动。</li><li><strong>通常只有一个 Loop executor。</strong> <code>AsyncMaster</code> 将用户侧 <code>OnBoot()</code>、<code>OnConfig()</code>、<code>OnState()</code>、<code>OnHeartbeat()</code>、<code>OnEmcy()</code>、<code>OnRpdoWrite()</code> 等回调包装为 <code>ev_task</code>，再投递到相应 Driver 的 executor。</li><li><strong>不会为每个从机创建 boot 线程。</strong> 每个正在 boot 的从机有自己的 NMT boot 状态上下文、CAN receiver、逻辑 timer 和 SDO 状态，但由 CAN 接收事件、定时器到期事件和 SDO confirmation 推进。</li><li><strong>一个 CANopen Node 通常只占用一个底层系统 Timer。</strong> NMT、Heartbeat、SDO、PDO、SYNC、TIME 等大量 <code>can_timer_t</code> 通过 <code>can_net</code> 和 <code>io_tqueue</code> 的两级最小堆，最终复用同一个 Linux <code>timerfd</code>。</li><li><strong>CANopen 没有 TCP 式“连接建立”。</strong> 工程上通常把“从机 boot 成功、配置完成、错误控制已建立，并观测到从机进入 Operational”视为联机完成。</li><li><strong>Future&#x2F;Promise 是一次异步请求的结果通道，不是连接对象。</strong> 每次 <code>AsyncRead()</code>、<code>AsyncWrite()</code>、异步等待或异步反配置通常产生一个共享完成状态；CAN 响应、SDO abort、超时或取消最终完成 Promise，使 Future ready，并将后续 task 投递到 executor。</li></ol><h2 id="分析边界"><a href="#分析边界" class="headerlink" title="分析边界"></a>分析边界</h2><p>本文按下列运行模型分析：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">io::Context ctx;</span><br><span class="line"><span class="function">io::Poll <span class="title">poll</span><span class="params">(ctx)</span></span>;</span><br><span class="line"><span class="function">ev::Loop <span class="title">event_loop</span><span class="params">(poll.get_poll())</span></span>;</span><br><span class="line"><span class="keyword">auto</span> exec = event_loop.<span class="built_in">get_executor</span>();</span><br><span class="line"><span class="function">io::Timer <span class="title">timer</span><span class="params">(poll, exec, CLOCK_MONOTONIC)</span></span>;</span><br><span class="line"><span class="function">io::CanController <span class="title">controller</span><span class="params">(<span class="string">&quot;can1&quot;</span>)</span></span>;</span><br><span class="line"><span class="function">io::CanChannel <span class="title">channel</span><span class="params">(poll, exec)</span></span>;</span><br><span class="line">channel.<span class="built_in">open</span>(controller);</span><br><span class="line"><span class="function">canopen::AsyncMaster <span class="title">master</span><span class="params">(timer, channel, <span class="string">&quot;master.dcf&quot;</span>, <span class="string">&quot;master.bin&quot;</span>, master_id)</span></span>;</span><br><span class="line"><span class="function">Driver <span class="title">driver</span><span class="params">(exec, master, slave_id)</span></span>;</span><br><span class="line">master.<span class="built_in">Reset</span>();</span><br><span class="line">event_loop.<span class="built_in">run</span>();</span><br></pre></td></tr></table></figure><p>具体对象数量会受以下因素影响：</p><ul><li>使用 <code>BasicMaster</code> 还是 <code>AsyncMaster</code>；</li><li>Driver 使用 <code>BasicDriver</code>、<code>FiberDriver</code>、<code>LoopDriver</code> 还是自定义 executor；</li><li>主站 DCF 中 <code>0x1F80</code>、<code>0x1F81</code>、Heartbeat、SYNC、PDO、配置文件等参数；</li><li>编译时是否关闭 NMT boot、node guarding、block SDO、LSS 等功能；</li><li>同一进程中创建了多少个 CANopen <code>Node</code>；</li><li>Linux SocketCAN、Windows IXXAT 或用户自定义 I&#x2F;O 后端。</li></ul><p>官方当前开发文档显示版本为 2.4.0；最新稳定发布页列出的稳定版本为 v2.3.5。两者的总体调度架构一致，但具体字段、条件编译和实现细节应以你实际编译版本为准。</p><h2 id="一、运行时分层：哪些对象属于哪一层"><a href="#一、运行时分层：哪些对象属于哪一层" class="headerlink" title="一、运行时分层：哪些对象属于哪一层"></a>一、运行时分层：哪些对象属于哪一层</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    App[&quot;应用层 main 和 Driver&quot;] --&gt; Master[&quot;coapp::AsyncMaster&quot;]</span><br><span class="line">    App --&gt; DriverExec[&quot;Driver executor&quot;]</span><br><span class="line">    Master --&gt; Node[&quot;coapp::Node&quot;]</span><br><span class="line">    Node --&gt; CanNet[&quot;io::CanNet / io_can_net&quot;]</span><br><span class="line">    Node --&gt; Nmt[&quot;co_nmt_t 和 CANopen 服务&quot;]</span><br><span class="line"></span><br><span class="line">    EvLoop[&quot;ev::Loop&quot;] --&gt; LoopExec[&quot;ev_loop executor&quot;]</span><br><span class="line">    LoopExec --&gt; TaskQ[&quot;ev_task 全局队列&quot;]</span><br><span class="line">    Poll[&quot;io::Poll / epoll&quot;] --&gt; EvLoop</span><br><span class="line"></span><br><span class="line">    CanChan[&quot;io::CanChannel / SocketCAN&quot;] --&gt; Poll</span><br><span class="line">    SysTimer[&quot;io::Timer / timerfd&quot;] --&gt; Poll</span><br><span class="line">    LoopExec --&gt; CanNet</span><br><span class="line">    CanNet --&gt; CanChan</span><br><span class="line">    CanNet --&gt; SysTimer</span><br><span class="line"></span><br><span class="line">    Nmt --&gt; Boot[&quot;每从机 co_nmt_boot_t&quot;]</span><br><span class="line">    Nmt --&gt; Sdo[&quot;CSDO / SSDO&quot;]</span><br><span class="line">    Nmt --&gt; Pdo[&quot;RPDO / TPDO&quot;]</span><br><span class="line">    Nmt --&gt; Hb[&quot;Heartbeat / guarding&quot;]</span><br><span class="line">    Nmt --&gt; SyncTime[&quot;SYNC / TIME&quot;]</span><br><span class="line"></span><br><span class="line">    Sdo --&gt; LogicTimer[&quot;can_timer_t&quot;]</span><br><span class="line">    Pdo --&gt; LogicTimer</span><br><span class="line">    Hb --&gt; LogicTimer</span><br><span class="line">    SyncTime --&gt; LogicTimer</span><br><span class="line">    Boot --&gt; LogicTimer</span><br><span class="line">    LogicTimer --&gt; CanNet</span><br><span class="line"></span><br><span class="line">    Master --&gt; DriverExec</span><br><span class="line">    DriverExec --&gt; TaskQ</span><br></pre></td></tr></table></figure><p>需要避免三个概念混淆：</p><table><thead><tr><th>概念</th><th>实际含义</th><th>不代表什么</th></tr></thead><tbody><tr><td><code>ev_task</code></td><td>一个待执行函数及其目标 executor 的侵入式任务对象</td><td>不等于线程，不一定动态分配</td></tr><tr><td>executor</td><td>接受、排队、运行或取消 task 的抽象接口</td><td>不一定拥有独立线程</td></tr><tr><td><code>ev::Loop</code></td><td>保存任务队列、统计 outstanding work、在无任务时调用 Poll 的运行器</td><td>不理解 NMT、SDO、PDO 协议语义</td></tr></tbody></table><p>CANopen 协议状态机通常在某个 CAN 接收任务或定时器任务的调用栈中同步推进若干状态；只有遇到“等待下一帧、等待 SDO confirmation、等待超时或等待应用配置完成”时才退出当前调用栈。不能把每个协议状态都理解成一个独立 <code>ev_task</code>。</p><h2 id="二、程序创建后、调用-Reset-前存在什么"><a href="#二、程序创建后、调用-Reset-前存在什么" class="headerlink" title="二、程序创建后、调用 Reset() 前存在什么"></a>二、程序创建后、调用 <code>Reset()</code> 前存在什么</h2><h3 id="1-io-Context"><a href="#1-io-Context" class="headerlink" title="1. io::Context"></a>1. <code>io::Context</code></h3><p><code>io::Context</code> 用于统一管理 I&#x2F;O 服务生命周期和 shutdown。它不是 event loop，也不执行 CANopen 状态机。</p><h3 id="2-io-Poll"><a href="#2-io-Poll" class="headerlink" title="2. io::Poll"></a>2. <code>io::Poll</code></h3><p>Linux 下通常对应 <code>epoll</code> 后端，用于监听：</p><ul><li>SocketCAN channel 的可读、可写或错误事件；</li><li><code>timerfd</code> 到期；</li><li>SignalSet 对应的信号事件；</li><li>其他已注册 I&#x2F;O 服务。</li></ul><h3 id="3-ev-Loop-和主-executor"><a href="#3-ev-Loop-和主-executor" class="headerlink" title="3. ev::Loop 和主 executor"></a>3. <code>ev::Loop</code> 和主 executor</h3><p><code>ev::Loop</code> 内部拥有任务队列和一个标准 executor。循环的核心行为是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">队列中有真实 task</span><br><span class="line">  -&gt; 取出一个 task</span><br><span class="line">  -&gt; ev_exec_run(task-&gt;exec, task)</span><br><span class="line">  -&gt; task 返回后继续</span><br><span class="line"></span><br><span class="line">队列为空但还有 outstanding work</span><br><span class="line">  -&gt; 调用 Poll 阻塞等待 I/O</span><br><span class="line">  -&gt; I/O 完成处理把 task post 到 executor</span><br><span class="line">  -&gt; Loop 被唤醒并执行 task</span><br><span class="line"></span><br><span class="line">队列为空且没有 outstanding work</span><br><span class="line">  -&gt; Loop 停止并返回</span><br></pre></td></tr></table></figure><p>因此，如果没有 CAN read、timer wait、signal wait 或 Future waiter 等 outstanding work，<code>event_loop.run()</code> 会立即返回。</p><h3 id="4-io-Timer"><a href="#4-io-Timer" class="headerlink" title="4. io::Timer"></a>4. <code>io::Timer</code></h3><p>构造 <code>io::Timer</code> 只会创建并注册底层计时资源。Linux 路径通常包括：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">timerfd_create()</span><br><span class="line">  -&gt; 注册到 io::Poll</span><br><span class="line">  -&gt; 等待未来 settime 和 submit_wait</span><br></pre></td></tr></table></figure><p>创建 Timer 不等于已经开始任何 CANopen 周期定时。</p><h3 id="5-io-CanChannel"><a href="#5-io-CanChannel" class="headerlink" title="5. io::CanChannel"></a>5. <code>io::CanChannel</code></h3><p>打开 CAN channel 后，Linux 后端接入 SocketCAN。<code>Node</code> 构造过程中，<code>io::CanNet::start()</code> 开始处理 CAN 帧，通常形成一个持续重提交的异步读取链：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">提交一次 CAN read</span><br><span class="line">  -&gt; Poll 等待 SocketCAN 可读</span><br><span class="line">  -&gt; 完成 read task</span><br><span class="line">  -&gt; 将 CAN 帧交给 can_net</span><br><span class="line">  -&gt; 匹配所有相关 can_recv_t</span><br><span class="line">  -&gt; 重新提交下一次 CAN read</span><br></pre></td></tr></table></figure><p>它不是不断占用 CPU 的轮询线程；没有帧时，执行线程阻塞在 Poll。</p><h3 id="6-AsyncMaster-和-Driver"><a href="#6-AsyncMaster-和-Driver" class="headerlink" title="6. AsyncMaster 和 Driver"></a>6. <code>AsyncMaster</code> 和 Driver</h3><p><code>AsyncMaster</code> 构造阶段完成对象装配、DCF&#x2F;对象字典加载、NMT 回调注册和 Driver 注册表准备。官方接口明确说明：构造完成后仍处于 NMT Initialisation 状态，尚未启动完整 NMT boot-up 流程；需要调用 <code>Reset()</code>。</p><p>注册一个 Driver 本身通常不会创建线程：</p><ul><li><code>BasicDriver</code>：不自动创建线程；</li><li><code>FiberDriver</code>：创建或使用 fiber executor，fiber 仍由底层 event loop 线程运行；</li><li><code>LoopDriver</code>：是例外，会为 Driver 建立专用 event loop，并使用独立线程运行；</li><li>自定义 Driver executor：线程和并行语义由应用决定。</li></ul><h2 id="三、调用-master-Reset-后产生什么"><a href="#三、调用-master-Reset-后产生什么" class="headerlink" title="三、调用 master.Reset() 后产生什么"></a>三、调用 <code>master.Reset()</code> 后产生什么</h2><p><code>Reset()</code> 的语义相当于本节点收到 NMT Reset Node。其主要作用不是“启动线程”，而是让本地主站 NMT 状态机进入 Reset Application、Reset Communication，再创建或启动当前对象字典允许的 CANopen 服务。</p><p>典型时序如下：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant App as &quot;应用 main&quot;</span><br><span class="line">    participant Master as &quot;AsyncMaster&quot;</span><br><span class="line">    participant Nmt as &quot;co_nmt_t&quot;</span><br><span class="line">    participant CanNet as &quot;io_can_net&quot;</span><br><span class="line">    participant Chan as &quot;SocketCAN channel&quot;</span><br><span class="line">    participant Ev as &quot;ev::Loop&quot;</span><br><span class="line"></span><br><span class="line">    App-&gt;&gt;Master: master.Reset()</span><br><span class="line">    Master-&gt;&gt;Nmt: 注入 RESET_NODE</span><br><span class="line">    Nmt-&gt;&gt;Nmt: Reset Application</span><br><span class="line">    Nmt-&gt;&gt;Nmt: Reset Communication</span><br><span class="line">    Nmt-&gt;&gt;CanNet: 启动接收器和逻辑定时器</span><br><span class="line">    Nmt-&gt;&gt;CanNet: 发送本地主站 boot-up</span><br><span class="line">    CanNet-&gt;&gt;Chan: 提交异步 CAN write</span><br><span class="line">    Master--&gt;&gt;App: Reset() 返回</span><br><span class="line">    App-&gt;&gt;Ev: event_loop.run()</span><br><span class="line">    Ev-&gt;&gt;Ev: 执行已投递 task 或阻塞在 Poll</span><br></pre></td></tr></table></figure><h3 id="Reset-后可能激活的协议对象"><a href="#Reset-后可能激活的协议对象" class="headerlink" title="Reset 后可能激活的协议对象"></a>Reset 后可能激活的协议对象</h3><table><thead><tr><th>协议对象</th><th>何时存在或激活</th><th>主要事件来源</th></tr></thead><tbody><tr><td><code>co_nmt_t</code></td><td>Node 创建后存在，<code>Reset()</code> 后进入运行状态</td><td>NMT command、boot-up、Heartbeat、timer</td></tr><tr><td>NMT command receiver</td><td>Reset Communication 后</td><td>CAN-ID <code>0x000</code></td></tr><tr><td>EMCY service</td><td>对象字典和编译配置允许时</td><td>错误上报、远端 EMCY</td></tr><tr><td>Server-SDO</td><td>本地 OD 中启用时</td><td>SDO request CAN 帧、transfer timeout</td></tr><tr><td>Client-SDO</td><td>主站为远端节点创建或请求时</td><td>SDO response、abort、timeout</td></tr><tr><td>RPDO&#x2F;TPDO</td><td>对象字典中存在并有效时</td><td>CAN 帧、SYNC、event timer</td></tr><tr><td>SYNC service</td><td><code>0x1005/0x1006/0x1007</code> 配置有效时</td><td>SYNC CAN、周期 timer</td></tr><tr><td>TIME service</td><td><code>0x1012</code> 配置及 StartTime 调用允许时</td><td>TIME CAN、producer timer</td></tr><tr><td>Heartbeat producer&#x2F;consumer</td><td><code>0x1017/0x1016</code> 非零时</td><td>周期 timer、Heartbeat CAN</td></tr><tr><td>NMT boot slave</td><td><code>0x1F81</code> 指定节点需要 boot 时</td><td>boot-up、SDO confirmation、timer</td></tr></tbody></table><h3 id="Reset-后常见的基础-task"><a href="#Reset-后常见的基础-task" class="headerlink" title="Reset 后常见的基础 task"></a>Reset 后常见的基础 task</h3><p>以下不是固定线程，而是 event loop 中可能出现的 task 类型：</p><ol><li><strong>CAN read completion task</strong>：读取一帧或一批 CAN 帧，交给 <code>can_net</code> 分发，再重提下一次 read。</li><li><strong>CAN write completion task</strong>：处理发送成功、错误或确认超时，并继续发送队列中的下一帧。</li><li><strong>Timer 内部 wait task</strong>：<code>timerfd</code> 到期后读取到期计数，将到期的 <code>io_tqueue_wait</code> 投递给 executor。</li><li><strong><code>io_can_net.wait_next</code> task</strong>：把当前单调时间回灌到 <code>can_net_set_time()</code>，触发所有到期的 <code>can_timer_t</code>。</li><li><strong>Driver callback task</strong>：<code>AsyncMaster</code> 将 CANopen 事件包装后投递到 Driver executor。</li><li><strong>Future continuation task</strong>：某个 Promise 完成后，Future 中登记的 task 被投递到指定 executor。</li><li><strong>Signal wait task</strong>：SIGINT&#x2F;SIGTERM 到来时执行清理逻辑。</li></ol><h2 id="四、主站开始-boot-一个从机时产生什么"><a href="#四、主站开始-boot-一个从机时产生什么" class="headerlink" title="四、主站开始 boot 一个从机时产生什么"></a>四、主站开始 boot 一个从机时产生什么</h2><h3 id="“建立链接”在-Lely-中的准确含义"><a href="#“建立链接”在-Lely-中的准确含义" class="headerlink" title="“建立链接”在 Lely 中的准确含义"></a>“建立链接”在 Lely 中的准确含义</h3><p>CANopen 是广播总线协议，没有 TCP connect，也没有一个长期保持的“连接句柄”。Lely 的 NMT master 通过 boot slave 流程建立的是以下逻辑事实：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">目标 Node-ID 已被发现</span><br><span class="line">  + 设备类型和身份符合主站 DCF 期望</span><br><span class="line">  + 必要配置已下载</span><br><span class="line">  + 错误控制已建立</span><br><span class="line">  + 主站已执行启动策略</span><br></pre></td></tr></table></figure><p>工程上还应额外确认从机 NMT state 已被观测为 Operational。因为 NMT Start Remote Node 是无确认命令，<code>OnBoot()</code> 成功不必然意味着从机已经被后续 Heartbeat&#x2F;PDO 观测为 Operational。</p><h3 id="每个-booting-从机的状态对象"><a href="#每个-booting-从机的状态对象" class="headerlink" title="每个 booting 从机的状态对象"></a>每个 booting 从机的状态对象</h3><p>当 <code>Boot(id)</code> 或自动 startup 策略启动某个从机时，Lely 不创建线程，而是使用该节点对应的状态上下文：</p><table><thead><tr><th>对象</th><th>作用</th></tr></thead><tbody><tr><td><code>co_nmt_boot_t</code></td><td>保存当前 boot state、Node-ID、错误状态、超时和回调上下文</td></tr><tr><td>boot CAN receiver</td><td>接收目标节点 boot-up、Heartbeat 或 guarding response</td></tr><tr><td>boot logical timer</td><td>Reset 等待、重试、状态检查和阶段超时</td></tr><tr><td>默认 Client-SDO</td><td>读取 0x1000、0x1018，下载配置或程序</td></tr><tr><td>configuration request context</td><td>执行 concise DCF 或进入用户 <code>OnConfig()</code> 扩展阶段</td></tr><tr><td>Master ready&#x2F;configuring 标志</td><td>提供 <code>IsReady()</code>、<code>IsConfig()</code> 等观察状态</td></tr></tbody></table><p>boot 状态机可能在一次事件中连续穿过多个同步状态，但遇到下列事件边界时返回 event loop：</p><ul><li>等待从机 boot-up 或 Heartbeat；</li><li>等待某个 boot timer 到期；</li><li>等待 Client-SDO upload&#x2F;download confirmation；</li><li>等待配置子流程完成；</li><li>等待用户调用 <code>OnConfig()</code> 提供的完成函数。</li></ul><h3 id="典型-boot-CAN-序列"><a href="#典型-boot-CAN-序列" class="headerlink" title="典型 boot CAN 序列"></a>典型 boot CAN 序列</h3><p>具体报文由 <code>0x1F80</code>、<code>0x1F81</code>、设备期望对象和配置文件决定。常见序列为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">主站 -&gt; 0x000 : Reset Communication，目标为单节点或全部节点</span><br><span class="line">从机 -&gt; 0x700 + Node-ID : 00 boot-up</span><br><span class="line">主站 -&gt; 0x600 + Node-ID : SDO upload 0x1000 Device Type</span><br><span class="line">从机 -&gt; 0x580 + Node-ID : SDO response</span><br><span class="line">主站 -&gt; 0x600 + Node-ID : SDO upload 0x1018 Identity</span><br><span class="line">从机 -&gt; 0x580 + Node-ID : SDO response</span><br><span class="line">主站 &lt;-&gt; 从机 : 可选配置 SDO、程序下载、PDO 参数下载</span><br><span class="line">主站 -&gt; 0x000 : Start Remote Node</span><br><span class="line">从机 -&gt; 0x700 + Node-ID : 后续 Heartbeat，状态通常为 0x05</span><br></pre></td></tr></table></figure><p>按你当前默认从机测试计划中的 Node-ID 1，常用 CAN-ID 为：</p><table><thead><tr><th>功能</th><th align="right">CAN-ID</th></tr></thead><tbody><tr><td>NMT command</td><td align="right"><code>0x000</code></td></tr><tr><td>EMCY</td><td align="right"><code>0x081</code></td></tr><tr><td>SYNC</td><td align="right"><code>0x080</code></td></tr><tr><td>TIME</td><td align="right"><code>0x100</code></td></tr><tr><td>TPDO1</td><td align="right"><code>0x181</code></td></tr><tr><td>RPDO1</td><td align="right"><code>0x201</code></td></tr><tr><td>SDO request</td><td align="right"><code>0x601</code></td></tr><tr><td>SDO response</td><td align="right"><code>0x581</code></td></tr><tr><td>boot-up &#x2F; Heartbeat</td><td align="right"><code>0x701</code></td></tr></tbody></table><h2 id="五、boot-期间会产生哪些-executor-task"><a href="#五、boot-期间会产生哪些-executor-task" class="headerlink" title="五、boot 期间会产生哪些 executor task"></a>五、boot 期间会产生哪些 executor task</h2><p>boot state machine 本身主要由 C 层 callback 推进，并不保证“每个状态一个 task”。典型执行链为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">SocketCAN 收到从机帧</span><br><span class="line">  -&gt; Poll 返回</span><br><span class="line">  -&gt; CAN read completion task 进入 loop</span><br><span class="line">  -&gt; can_net_recv()</span><br><span class="line">  -&gt; 匹配 boot receiver 或 CSDO receiver</span><br><span class="line">  -&gt; boot/CSDO state handler 同步推进</span><br><span class="line">  -&gt; 若产生 Master/Driver 事件</span><br><span class="line">       AsyncMaster 将 OnState/OnConfig/OnBoot 包装为 task</span><br><span class="line">       post 到 Driver executor</span><br><span class="line">  -&gt; 当前 read task 返回</span><br></pre></td></tr></table></figure><p>主要用户侧 task 包括：</p><table><thead><tr><th>task</th><th>触发条件</th><th>执行位置</th></tr></thead><tbody><tr><td><code>OnState(BOOTUP/PREOP/OPERATIONAL/STOPPED)</code></td><td>收到远端状态变化</td><td>Driver executor</td></tr><tr><td><code>OnConfig(done)</code></td><td>boot 进入用户扩展配置窗口</td><td>Driver executor</td></tr><tr><td><code>OnBoot(st, es, what)</code></td><td>boot 流程成功或失败结束</td><td>Driver executor</td></tr><tr><td><code>OnHeartbeat(...)</code></td><td>收到 Heartbeat</td><td>Driver executor</td></tr><tr><td><code>OnEmcy(...)</code></td><td>收到远端 EMCY</td><td>Driver executor</td></tr><tr><td><code>OnRpdoWrite(...)</code></td><td>远端 TPDO 被主站 RPDO 接收并写入本地映射对象</td><td>Driver executor</td></tr></tbody></table><p>对 <code>BasicMaster</code>，部分回调可能在协议事件调用栈中直接执行；对 <code>AsyncMaster</code>，Driver 回调被投递为 task，使协议栈回调与用户业务代码解耦。</p><h3 id="OnConfig-done-不是普通通知"><a href="#OnConfig-done-不是普通通知" class="headerlink" title="OnConfig(done) 不是普通通知"></a><code>OnConfig(done)</code> 不是普通通知</h3><p><code>OnConfig()</code> 接收的 <code>done</code>&#x2F;<code>res</code> 是 boot 状态机继续运行所必需的一次性完成回调：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">NMT boot 将默认 Client-SDO 临时交给 Driver</span><br><span class="line">  -&gt; AsyncMaster post OnConfig task</span><br><span class="line">  -&gt; Driver 发起一个或多个异步 SDO</span><br><span class="line">  -&gt; 所有配置完成后调用 done(error_code)</span><br><span class="line">  -&gt; Master 将结果交还 NMT boot</span><br><span class="line">  -&gt; boot 状态机继续</span><br></pre></td></tr></table></figure><p>遗漏 <code>done()</code> 会使节点长期停留在 configuring 状态。boot 期间默认 Client-SDO 通常由 NMT master 独占；应用通常只能在 <code>OnConfig()</code> 窗口内，或 <code>OnBoot()</code> 后稳定使用它。</p><h2 id="六、与从机联机后还剩下哪些长期任务"><a href="#六、与从机联机后还剩下哪些长期任务" class="headerlink" title="六、与从机联机后还剩下哪些长期任务"></a>六、与从机联机后还剩下哪些长期任务</h2><p>boot 完成后，不会留下一个“连接线程”。稳定运行阶段通常保持以下异步工作：</p><h3 id="1-永久-CAN-read-链"><a href="#1-永久-CAN-read-链" class="headerlink" title="1. 永久 CAN read 链"></a>1. 永久 CAN read 链</h3><p>只要 Node&#x2F;CanNet 仍在运行，CAN channel 会持续保持下一次异步 read。它是 event loop 不退出的重要 outstanding work 来源之一。</p><h3 id="2-CAN-write-队列"><a href="#2-CAN-write-队列" class="headerlink" title="2. CAN write 队列"></a>2. CAN write 队列</h3><p>NMT、SDO、PDO、SYNC、TIME、EMCY 和 Heartbeat 的发送最终汇入 CAN network&#x2F;channel 的异步写路径。发送通常串行排队，写完成或失败后继续队列。</p><h3 id="3-周期或超时逻辑定时器"><a href="#3-周期或超时逻辑定时器" class="headerlink" title="3. 周期或超时逻辑定时器"></a>3. 周期或超时逻辑定时器</h3><p>只有配置有效或请求正在进行时，对应 logical timer 才处于 active 状态。</p><table><thead><tr><th>类别</th><th>典型逻辑 timer</th><th>是否长期 active</th></tr></thead><tbody><tr><td>主站 Heartbeat producer</td><td>NMT error-control timer</td><td><code>0x1017 != 0</code> 时周期 active</td></tr><tr><td>从机 Heartbeat consumer</td><td>每个监控节点一个 timeout timer</td><td><code>0x1016</code> 配置有效时 active，并在每帧 Heartbeat 后重装</td></tr><tr><td>node guarding</td><td>每从机 guarding&#x2F;lifetime timer</td><td>仅启用 legacy guarding 时 active</td></tr><tr><td>SYNC producer</td><td>SYNC cycle timer</td><td><code>0x1006 != 0</code> 且本节点为 producer 时周期 active</td></tr><tr><td>TIME producer</td><td>TIME interval timer</td><td>调用 StartTime 且 interval 非零时 active</td></tr><tr><td>TPDO</td><td>event timer、同步窗口 timer</td><td>对应 PDO 参数有效时 active</td></tr><tr><td>RPDO</td><td>deadline&#x2F;event monitoring、同步窗口 timer</td><td>对应参数有效时 active</td></tr><tr><td>CSDO</td><td>transfer timeout timer</td><td>仅 SDO 请求 pending 时 active</td></tr><tr><td>SSDO</td><td>segmented&#x2F;block transfer timeout</td><td>仅一次服务端传输进行中时 active</td></tr><tr><td>NMT boot</td><td>reset&#x2F;check&#x2F;retry timer</td><td>仅节点 booting 时 active，结束后停止</td></tr><tr><td>LSS</td><td>response&#x2F;inhibit&#x2F;scan timer</td><td>仅 LSS 操作进行时 active</td></tr><tr><td>用户等待</td><td><code>SubmitWait()</code>&#x2F;<code>AsyncWait()</code> deadline</td><td>仅等待 pending 时 active</td></tr></tbody></table><h3 id="4-Driver-业务-task"><a href="#4-Driver-业务-task" class="headerlink" title="4. Driver 业务 task"></a>4. Driver 业务 task</h3><p>稳定阶段每次收到 Heartbeat、EMCY、TPDO、状态变化或用户 Future 完成，仍会产生短生命周期 task。它们执行完就从队列消失，不是长期驻留线程。</p><h2 id="七、为什么大量协议定时器最终只有一个-timerfd"><a href="#七、为什么大量协议定时器最终只有一个-timerfd" class="headerlink" title="七、为什么大量协议定时器最终只有一个 timerfd"></a>七、为什么大量协议定时器最终只有一个 <code>timerfd</code></h2><p>Lely 的定时链分两级复用：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">NMT / SDO / PDO / SYNC / TIME 的 can_timer_t</span><br><span class="line">  -&gt; can_net.timer_heap，选出最早协议截止时间</span><br><span class="line">  -&gt; io_can_net.wait_next</span><br><span class="line">  -&gt; io_tqueue，和发送确认、用户等待一起再次选最早截止时间</span><br><span class="line">  -&gt; 一个 io::Timer</span><br><span class="line">  -&gt; 一个 Linux timerfd</span><br></pre></td></tr></table></figure><p><code>io_can_net</code> 至少直接维护两个 <code>io_tqueue_wait</code>：</p><table><thead><tr><th>等待对象</th><th>作用</th></tr></thead><tbody><tr><td><code>wait_next</code></td><td>等待 <code>can_net</code> 中最早的协议 logical timer 到期</td></tr><tr><td><code>wait_confirm</code></td><td>CAN write confirmation 超时</td></tr></tbody></table><p>Node 上提交的用户等待也使用同一 timer queue。底层 Timer 到期后，调用链大致为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">timerfd 到期</span><br><span class="line">  -&gt; epoll 返回</span><br><span class="line">  -&gt; Timer 内部 wait_task 被 post</span><br><span class="line">  -&gt; 读取 timerfd 到期计数</span><br><span class="line">  -&gt; io_tqueue 弹出所有已到期 wait</span><br><span class="line">  -&gt; io_can_net.wait_next task 执行</span><br><span class="line">  -&gt; can_net_set_time(now)</span><br><span class="line">  -&gt; 弹出所有已到期 can_timer_t</span><br><span class="line">  -&gt; 调用具体 NMT/SDO/PDO/SYNC/TIME timer callback</span><br></pre></td></tr></table></figure><p>所以“一个主站有多少定时器”应分两层回答：</p><ul><li><strong>系统 Timer 数</strong>：典型单 Node 主站为 1 个 <code>io::Timer</code>，Linux 下对应 1 个 <code>timerfd</code>；</li><li><strong>协议 logical timer 数</strong>：可能有很多，数量与远端节点数、启用服务和当前 pending 请求相关。</li></ul><h2 id="八、Future-Promise-承诺机制如何参与-SDO-和配置"><a href="#八、Future-Promise-承诺机制如何参与-SDO-和配置" class="headerlink" title="八、Future&#x2F;Promise 承诺机制如何参与 SDO 和配置"></a>八、Future&#x2F;Promise 承诺机制如何参与 SDO 和配置</h2><h3 id="1-Promise-和-Future-的职责"><a href="#1-Promise-和-Future-的职责" class="headerlink" title="1. Promise 和 Future 的职责"></a>1. Promise 和 Future 的职责</h3><table><thead><tr><th>对象</th><th>职责</th></tr></thead><tbody><tr><td>Promise</td><td>异步操作生产者持有，发布成功值或错误，且只能完成一次</td></tr><tr><td>Future</td><td>调用方持有，观察 ready 状态、读取结果、登记 continuation task</td></tr><tr><td>task</td><td>Future ready 后需要执行的动作，明确绑定 executor</td></tr><tr><td>executor</td><td>真正安排 task 的执行时机和执行线程</td></tr></tbody></table><p>Promise 和 Future 共享同一控制块、同一结果区和引用计数，不是两个独立通信对象。</p><h3 id="2-AsyncWrite-的典型链路"><a href="#2-AsyncWrite-的典型链路" class="headerlink" title="2. AsyncWrite() 的典型链路"></a>2. <code>AsyncWrite()</code> 的典型链路</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant Driver as &quot;Driver task&quot;</span><br><span class="line">    participant Req as &quot;SDO request object&quot;</span><br><span class="line">    participant Fut as &quot;Future shared state&quot;</span><br><span class="line">    participant CSDO as &quot;co_csdo_t&quot;</span><br><span class="line">    participant Bus as &quot;CAN bus&quot;</span><br><span class="line">    participant Exec as &quot;executor&quot;</span><br><span class="line"></span><br><span class="line">    Driver-&gt;&gt;Req: AsyncWrite(index, subindex, value)</span><br><span class="line">    Req-&gt;&gt;Fut: 创建 Promise/Future</span><br><span class="line">    Req-&gt;&gt;CSDO: 提交 SDO download</span><br><span class="line">    CSDO-&gt;&gt;Bus: 发送 SDO request</span><br><span class="line">    Req--&gt;&gt;Driver: 返回 Future</span><br><span class="line"></span><br><span class="line">    alt 收到成功响应</span><br><span class="line">        Bus--&gt;&gt;CSDO: SDO response</span><br><span class="line">        CSDO-&gt;&gt;Fut: Promise set success</span><br><span class="line">    else 收到 SDO abort</span><br><span class="line">        Bus--&gt;&gt;CSDO: abort response</span><br><span class="line">        CSDO-&gt;&gt;Fut: Promise set SdoError</span><br><span class="line">    else transfer timeout</span><br><span class="line">        CSDO-&gt;&gt;Fut: Promise set timeout error</span><br><span class="line">    end</span><br><span class="line"></span><br><span class="line">    Fut-&gt;&gt;Exec: post 已登记的 continuation task</span><br><span class="line">    Exec-&gt;&gt;Driver: 恢复 Wait 或执行 submit callback</span><br></pre></td></tr></table></figure><p>每个 pending <code>AsyncRead()</code> 或 <code>AsyncWrite()</code> 通常至少对应：</p><ul><li>一个 SDO 请求状态对象；</li><li>一个 Promise&#x2F;Future 共享状态；</li><li>一个 CSDO timeout logical timer；</li><li>零个或多个在 Future 上登记的 completion task。</li></ul><h3 id="3-Future-本身不驱动-event-loop"><a href="#3-Future-本身不驱动-event-loop" class="headerlink" title="3. Future 本身不驱动 event loop"></a>3. Future 本身不驱动 event loop</h3><p><code>Future::get()</code> 只允许读取已经 ready 的结果，不会负责处理 SocketCAN 或定时器。必须有线程持续运行 <code>event_loop.run()</code>、<code>wait()</code> 或相应 executor，Future 才能因为 CAN response 或 timeout 而完成。</p><h3 id="4-Wait-为什么看起来像同步"><a href="#4-Wait-为什么看起来像同步" class="headerlink" title="4. Wait() 为什么看起来像同步"></a>4. <code>Wait()</code> 为什么看起来像同步</h3><ul><li><code>FiberDriver::Wait(future)</code>：暂停当前 fiber，底层 event loop 继续处理其他 CAN 和 timer task；Future ready 后恢复该 fiber。</li><li><code>LoopDriver::Wait(future)</code>：运行 Driver 自己的专用或嵌套 event loop 等待 Future，容易引入重入、优先级反转或死锁风险。</li><li>普通 <code>BasicDriver</code>：不应阻塞 event loop，通常使用 Future continuation 或 callback。</li></ul><h3 id="5-Future-task-与-outstanding-work"><a href="#5-Future-task-与-outstanding-work" class="headerlink" title="5. Future task 与 outstanding work"></a>5. Future task 与 outstanding work</h3><p>Future 尚未 ready 时，等待 task 还没有进入 event loop 的真实执行队列。Lely 会通过 <code>on_task_init()</code> 增加 outstanding work，防止 loop 因暂时没有可执行 task 而退出。Promise 完成并把 task post 到 executor 后，再通过 <code>on_task_fini()</code> 释放这份保活计数。</p><h2 id="九、Loop-和线程数量的准确计算"><a href="#九、Loop-和线程数量的准确计算" class="headerlink" title="九、Loop 和线程数量的准确计算"></a>九、Loop 和线程数量的准确计算</h2><h3 id="典型-AsyncMaster-BasicDriver"><a href="#典型-AsyncMaster-BasicDriver" class="headerlink" title="典型 AsyncMaster + BasicDriver"></a>典型 <code>AsyncMaster + BasicDriver</code></h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">主 ev::Loop：1</span><br><span class="line">主 Loop executor：1</span><br><span class="line">额外 Driver Loop：0</span><br><span class="line">额外 Driver 线程：0</span><br><span class="line">每从机 boot 线程：0</span><br></pre></td></tr></table></figure><h3 id="典型-AsyncMaster-FiberDriver"><a href="#典型-AsyncMaster-FiberDriver" class="headerlink" title="典型 AsyncMaster + FiberDriver"></a>典型 <code>AsyncMaster + FiberDriver</code></h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">主 ev::Loop：1</span><br><span class="line">主 Loop executor：1</span><br><span class="line">Fiber executor：每 Driver 可有包装/上下文，但底层仍由主 Loop 驱动</span><br><span class="line">额外 OS 线程：通常 0</span><br><span class="line">每个并发 Driver 回调：作为 fiber task 运行</span><br></pre></td></tr></table></figure><h3 id="AsyncMaster-LoopDriver"><a href="#AsyncMaster-LoopDriver" class="headerlink" title="AsyncMaster + LoopDriver"></a><code>AsyncMaster + LoopDriver</code></h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">主 ev::Loop：1</span><br><span class="line">每个 LoopDriver：通常增加 1 个专用 Loop 和 1 个线程</span><br><span class="line">N 个 LoopDriver：可能为 1 + N 个 Loop、N 个额外线程</span><br></pre></td></tr></table></figure><h3 id="多-event-loop-run-worker"><a href="#多-event-loop-run-worker" class="headerlink" title="多 event_loop.run() worker"></a>多 <code>event_loop.run()</code> worker</h3><p>同一个 <code>ev::Loop</code> 可以由多个线程调用 <code>run()</code>。这会提高并行性，但也意味着不同 task 可能并发执行。是否单线程、任务顺序是否串行、不同 Driver 是否互斥，取决于 executor、strand&#x2F;fiber&#x2F;loop 选择和应用锁设计，不能只凭 <code>AsyncMaster</code> 名称判断。</p><h2 id="十、从启动到稳定运行的完整清单"><a href="#十、从启动到稳定运行的完整清单" class="headerlink" title="十、从启动到稳定运行的完整清单"></a>十、从启动到稳定运行的完整清单</h2><table><thead><tr><th>阶段</th><th>新增或激活的对象&#x2F;工作</th><th>主要停止条件</th></tr></thead><tbody><tr><td>创建 Poll&#x2F;Loop</td><td>Poll backend、主 Loop、主 executor</td><td>对象析构</td></tr><tr><td>创建 Timer</td><td>一个底层 timerfd 和内部 wait task</td><td>Timer shutdown&#x2F;析构</td></tr><tr><td>打开 CanChannel</td><td>SocketCAN channel、异步读写基础设施</td><td>channel shutdown&#x2F;析构</td></tr><tr><td>创建 AsyncMaster</td><td>CanNet、对象字典、NMT 对象、回调注册</td><td>Master 析构</td></tr><tr><td>注册 Driver</td><td>node-ID 到 Driver&#x2F;executor 的映射</td><td>Driver 解除注册&#x2F;析构</td></tr><tr><td><code>Reset()</code></td><td>NMT 服务、CAN receivers、有效 PDO&#x2F;SDO&#x2F;SYNC&#x2F;TIME&#x2F;EMCY 服务</td><td>Reset、stop 或析构</td></tr><tr><td>自动 boot 从机</td><td>每节点 boot state、receiver、timer、默认 CSDO 所有权</td><td><code>OnBoot()</code> 成功&#x2F;失败</td></tr><tr><td><code>OnConfig()</code></td><td>Driver callback task、配置 SDO Future、完成回调</td><td><code>done(ec)</code></td></tr><tr><td>boot 完成</td><td><code>OnBoot()</code> task、ready 状态</td><td>新 boot-up、reset、重新 boot</td></tr><tr><td>稳定运行</td><td>CAN read、周期 Heartbeat&#x2F;SYNC&#x2F;PDO timer、短期业务 task</td><td>shutdown&#x2F;reset&#x2F;参数关闭</td></tr><tr><td>每次 Async SDO</td><td>Promise&#x2F;Future、SDO timer、completion task</td><td>响应、abort、timeout、cancel</td></tr><tr><td>shutdown</td><td>cancel read&#x2F;write&#x2F;timer wait，执行剩余 completion task</td><td>outstanding work 清零，Loop 返回</td></tr></tbody></table><h2 id="十一、针对你当前-Node-ID-1-从机的运行判断"><a href="#十一、针对你当前-Node-ID-1-从机的运行判断" class="headerlink" title="十一、针对你当前 Node-ID 1 从机的运行判断"></a>十一、针对你当前 Node-ID 1 从机的运行判断</h2><p>按已提供的默认从机测试计划，联机后观察到以下组合时，才能较可靠地判断主站已进入稳定运行：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">0x701#00                         从机 boot-up</span><br><span class="line">0x601 / 0x581                   boot 身份检查或配置 SDO</span><br><span class="line">0x000#0101                      主站发 Start Remote Node</span><br><span class="line">0x701#05                        从机 Heartbeat 已观测为 Operational</span><br><span class="line">0x181 周期或事件发送            TPDO1 路径工作</span><br><span class="line">0x201 写入后 OD 变化            RPDO1 路径工作</span><br></pre></td></tr></table></figure><p>其中：</p><ul><li>只看到 <code>OnBoot(es == 0)</code>：表示 Lely boot&#x2F;config&#x2F;error-control 流程成功；</li><li>再看到 <code>0x701#05</code>：表示主站已从总线观测到从机 Operational；</li><li>再看到预期 PDO：才说明 PDO 通道和应用映射已进入有效运行状态。</li></ul><p>建议业务层把“设备可用”定义为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">boot_success</span><br><span class="line">&amp;&amp; nmt_state == Operational</span><br><span class="line">&amp;&amp; 必要的应用状态/PDO 首帧已满足</span><br></pre></td></tr></table></figure><p>而不是只依据 <code>OnBoot()</code>。</p><h2 id="十二、最终心智模型"><a href="#十二、最终心智模型" class="headerlink" title="十二、最终心智模型"></a>十二、最终心智模型</h2><p>Lely CANopen 主站不是“主线程创建多个从机任务并等待连接”，而是一个分层异步系统：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">一个或少量 event loop 线程</span><br><span class="line">  -&gt; executor 运行短生命周期 ev_task</span><br><span class="line">  -&gt; Poll 等待 SocketCAN、timerfd 和信号</span><br><span class="line">  -&gt; CanNet 把 CAN 帧和时间交给被动协议栈</span><br><span class="line">  -&gt; NMT/SDO/PDO/Heartbeat 等状态机由事件推进</span><br><span class="line">  -&gt; AsyncMaster 把用户事件投递到 Driver executor</span><br><span class="line">  -&gt; Future/Promise连接异步请求与后续 task</span><br></pre></td></tr></table></figure><p>对典型单主站、多从机应用，最准确的资源描述是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">1 个主 ev::Loop</span><br><span class="line">1 个主 executor</span><br><span class="line">1 个 CANopen Node 对应 1 个专用 CanChannel</span><br><span class="line">1 个 CANopen Node 对应 1 个专用 io::Timer/timerfd</span><br><span class="line">N 个从机对应 N 份远端状态和按需激活的 boot/heartbeat/SDO 上下文</span><br><span class="line">0 个固定的“每从机连接线程”</span><br><span class="line">大量短生命周期 task、receiver 和 logical timer</span><br><span class="line">每个异步请求对应一份一次性 Future/Promise 完成状态</span><br></pre></td></tr></table></figure><h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ol><li><a href="https://opensource.lely.com/canopen/docs/overview/">Lely CANopen Library overview</a></li><li><a href="https://opensource.lely.com/canopen/docs/cpp-tutorial/">Lely CANopen C++ tutorial</a></li><li><a href="https://lely_industries.gitlab.io/lely-core/doxygen/classlely_1_1canopen_1_1Node.html">Lely core <code>Node</code> API</a></li><li><a href="https://lely_industries.gitlab.io/lely-core/doxygen/classlely_1_1canopen_1_1BasicMaster.html">Lely core <code>BasicMaster</code> API</a></li><li><a href="https://lely_industries.gitlab.io/lely-core/doxygen/loop_8h.html">Lely core polling event loop API</a></li><li><a href="https://lely_industries.gitlab.io/lely-core/doxygen/net_8c.html">Lely core CAN timer API</a></li><li><a href="https://gitlab.com/lely_industries/lely-core/-/issues/76">Lely issue #76: default Client-SDO ownership during boot</a></li><li><a href="https://gitlab.com/lely_industries/lely-core/-/issues/92">Lely issue #92: OnBoot and earliest reliable PDO usage</a></li><li>用户提供的 <code>canopen_mcu_slave_test_plan(2).md</code></li><li>用户提供的 Lely CANopen 系列源码学习笔记压缩包 <code>canopen(1).zip</code></li></ol>]]></content>
    
    
      
      
    <summary type="html">&lt;meta name=&quot;referrer&quot; content=&quot;no-referrer&quot; /&gt;

&lt;h1 id=&quot;Lely-CANopen-启动与从机联机后的运行机制：Executor、Loop、Timer、CAN-与-Future-Promise&quot;&gt;&lt;a href=&quot;#Lely-</summary>
      
    
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/categories/canopen/"/>
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/tags/canopen/"/>
    
  </entry>
  
  <entry>
    <title>Lely CANopen coapp Driver</title>
    <link href="https://wdfk-prog.space/posts/ba2a86a3/"/>
    <id>https://wdfk-prog.space/posts/ba2a86a3/</id>
    <published>2026-08-03T03:33:16.000Z</published>
    <updated>2026-08-03T03:33:51.486Z</updated>
    
    <content type="html"><![CDATA[<meta name="referrer" content="no-referrer" /><h1 id="Lely-CANopen-coapp-Driver：BasicMaster-与-BasicDriver-的职责边界、注册表和事件流程"><a href="#Lely-CANopen-coapp-Driver：BasicMaster-与-BasicDriver-的职责边界、注册表和事件流程" class="headerlink" title="Lely CANopen coapp Driver：BasicMaster 与 BasicDriver 的职责边界、注册表和事件流程"></a>Lely CANopen coapp Driver：BasicMaster 与 BasicDriver 的职责边界、注册表和事件流程</h1><blockquote><p>摘要：从 BasicMaster 的远程节点操作能力出发，解析 Driver 注册表、非拥有关系、事件分发，以及各类 Driver 的协作机制。</p></blockquote><p><img src="https://i-blog.csdnimg.cn/direct/eaa5824809244f1483d1cfbfe045b239.png" alt="在这里插入图片描述"><br>@[toc]<br>Lely CANopen 的 <code>coapp</code> 层中，<code>Driver</code> 很容易被误解成 CAN 硬件驱动、协议状态机，或者由 Master 自动创建的“远程节点对象”。源码表达的关系并不是这样：<strong><code>BasicMaster</code> 本身可以直接操作远程节点；<code>BasicDriver</code> 是绑定固定 Node-ID 和 Executor 的节点级应用适配器，同时也是 Master 分发节点事件的接收对象。</strong></p><h2 id="1-先回答两个核心问题"><a href="#1-先回答两个核心问题" class="headerlink" title="1. 先回答两个核心问题"></a>1. 先回答两个核心问题</h2><h3 id="1-1-BasicMaster-能否直接操作远程节点"><a href="#1-1-BasicMaster-能否直接操作远程节点" class="headerlink" title="1.1 BasicMaster 能否直接操作远程节点"></a>1.1 BasicMaster 能否直接操作远程节点</h3><p>可以。<code>BasicMaster</code> 的公开接口已经包含 Node-ID 参数，可以直接对指定远程节点执行操作，例如：</p><ul><li><code>Command(cs, id)</code>：发送 NMT 命令；</li><li><code>Boot(id)</code>、<code>IsReady(id)</code>：启动并查询 NMT boot-slave 流程；</li><li><code>SubmitRead()</code>、<code>SubmitWrite()</code>：提交 SDO 上传或下载；</li><li><code>AsyncRead()</code>、<code>AsyncWrite()</code>：以 Future 形式执行 SDO；</li><li><code>RpdoMapped(id)</code>、<code>TpdoMapped(id)</code>：取得指定节点的 PDO 映射访问器；</li><li><code>ConfigHeartbeat(id, ...)</code>：配置 Master 本地对象 <code>0x1016</code> 中对该节点的心跳消费参数。</li></ul><p>因此，下面两条路径都成立：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">直接调用：Application -&gt; BasicMaster(node_id, ...) -&gt; CANopen services</span><br><span class="line">Driver 调用：Application -&gt; BasicDriver(...) -&gt; BasicMaster(node_id, ...) -&gt; CANopen services</span><br></pre></td></tr></table></figure><p><code>BasicDriver</code> 不是访问远程节点的必经层，也不存在“先通过 Driver 取出远程节点对象，再进行通信”的步骤。</p><h3 id="1-2-BasicMaster-是否会构造一组-DriverBase"><a href="#1-2-BasicMaster-是否会构造一组-DriverBase" class="headerlink" title="1.2 BasicMaster 是否会构造一组 DriverBase"></a>1.2 BasicMaster 是否会构造一组 DriverBase</h3><p>不会。下面的继承声明只表示 <code>BasicMaster</code> 内部带有一个 Driver 注册表：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">BasicMaster</span></span><br><span class="line">    : <span class="keyword">public</span> Node,</span><br><span class="line">      <span class="keyword">protected</span> std::map&lt;<span class="type">uint8_t</span>, DriverBase*&gt; &#123;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>这个 <code>std::map</code> 在 <code>BasicMaster</code> 构造时会被默认构造成空容器，但其中的 <code>DriverBase</code> 对象由应用创建。Master 只保存裸指针并按 Node-ID 查找，不负责 <code>new</code>、<code>delete</code> 或决定 Driver 的实际类型。</p><p>最准确的概括是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">BasicMaster：拥有 CANopen 主站协议能力，并维护 Driver 注册表</span><br><span class="line">BasicDriver：由应用创建，绑定一个远程 Node-ID，并自动注册到 Master</span><br><span class="line">DriverBase*：Master 用于事件分发的非拥有指针</span><br></pre></td></tr></table></figure><h2 id="2-BasicMaster-同时承担协议入口和事件路由"><a href="#2-BasicMaster-同时承担协议入口和事件路由" class="headerlink" title="2. BasicMaster 同时承担协议入口和事件路由"></a>2. BasicMaster 同时承担协议入口和事件路由</h2><p><code>BasicMaster</code> 的继承关系包含两个不同维度：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">BasicMaster</span></span><br><span class="line">    : <span class="keyword">public</span> Node,</span><br><span class="line">      <span class="keyword">protected</span> std::map&lt;<span class="type">uint8_t</span>, DriverBase*&gt;;</span><br></pre></td></tr></table></figure><p><code>public Node</code> 提供主站节点本身的 CANopen 能力；<code>protected std::map</code> 则提供远程节点 Driver 的内部索引。二者不能混为同一个“远程节点集合”。</p><h3 id="2-1-Node-一侧：实际执行-CANopen-操作"><a href="#2-1-Node-一侧：实际执行-CANopen-操作" class="headerlink" title="2.1 Node 一侧：实际执行 CANopen 操作"></a>2.1 Node 一侧：实际执行 CANopen 操作</h3><p><code>BasicMaster</code> 通过 <code>Node</code>、NMT 服务、Client-SDO、PDO 映射和内部实现对象执行协议操作。直接 SDO 调用的关键参数是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Executor + Node-ID + Index + Sub-index + completion/future</span><br></pre></td></tr></table></figure><p>概念上可写为：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">const</span> <span class="type">uint8_t</span> node_id = <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">auto</span> future = master.<span class="built_in">AsyncRead</span>&lt;<span class="type">uint32_t</span>&gt;(</span><br><span class="line">    exec, node_id, <span class="number">0x1000</span>, <span class="number">0x00</span>);</span><br><span class="line"></span><br><span class="line">master.<span class="built_in">Command</span>(lely::canopen::NmtCommand::START, node_id);</span><br></pre></td></tr></table></figure><p><code>SubmitRead()</code> 和 <code>AsyncRead()</code> 的实现会根据 Node-ID 获取 Client-SDO 服务。这个过程不通过 Driver 注册表；如果没有可用 Client-SDO，则返回或报告 <code>SdoErrc::NO_SDO</code>。因此：</p><blockquote><p>Driver 不是 SDO 资源本身。是否有可用 SDO 连接，由 Master 的 SDO 服务配置和运行状态决定，而不是由 <code>std::map&lt;uint8_t, DriverBase*&gt;</code> 决定。</p></blockquote><h3 id="2-2-map-一侧：把事件交给节点级应用对象"><a href="#2-2-map-一侧：把事件交给节点级应用对象" class="headerlink" title="2.2 map 一侧：把事件交给节点级应用对象"></a>2.2 map 一侧：把事件交给节点级应用对象</h3><p>Driver 注册表使用：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">std::map&lt;<span class="type">uint8_t</span>, DriverBase*&gt;</span><br></pre></td></tr></table></figure><p>其键和值分别是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">key   = 远程节点 Node-ID</span><br><span class="line">value = 该节点对应的 DriverBase 非拥有指针</span><br></pre></td></tr></table></figure><p>它主要用于两类工作：</p><ol><li>节点级事件分发，例如 <code>OnState(id, st)</code>、<code>OnHeartbeat(id, occurred)</code>、<code>OnEmcy(id, ...)</code>；</li><li>Driver 参与的配置和反配置流程，例如调用 <code>DriverBase::OnConfig()</code>、<code>OnDeconfig()</code>。</li></ol><p>网络级事件，如 CAN 状态、CAN 错误、SYNC 和 TIME，会遍历全部已注册 Driver；节点级事件则使用 <code>find(id)</code> 只通知对应 Driver。</p><h3 id="2-3-没有注册-Driver-时，哪些能力仍然存在"><a href="#2-3-没有注册-Driver-时，哪些能力仍然存在" class="headerlink" title="2.3 没有注册 Driver 时，哪些能力仍然存在"></a>2.3 没有注册 Driver 时，哪些能力仍然存在</h3><table><thead><tr><th>能力</th><th>无 Driver 时是否可用</th><th>条件或差异</th></tr></thead><tbody><tr><td>发送 NMT 命令</td><td>可以</td><td>直接调用 <code>BasicMaster::Command()</code></td></tr><tr><td>发起 boot-slave</td><td>可以</td><td>直接调用 <code>Boot(id)</code>；仍受 NMT、DCF 和 SDO 配置约束</td></tr><tr><td>SDO 读写</td><td>可以</td><td>必须存在可用 Client-SDO；调用时显式传 Executor 和 Node-ID</td></tr><tr><td>查询 ready 状态</td><td>可以</td><td>直接调用 <code>IsReady(id)</code></td></tr><tr><td>访问 PDO 映射视图</td><td>可以</td><td>直接调用 <code>RpdoMapped(id)</code>、<code>TpdoMapped(id)</code></td></tr><tr><td><code>DriverBase::OnState()</code> 等节点回调</td><td>不会触发</td><td>注册表中没有接收对象</td></tr><tr><td>自定义 Driver <code>OnConfig()</code></td><td>不会执行</td><td>Master 找不到 Driver 时，将该自定义配置步骤视为完成</td></tr><tr><td><code>AsyncDeconfig(id)</code> 调用 Driver 反配置</td><td>无实际 Driver 可调用</td><td>返回空 Future 或不执行节点应用反配置</td></tr></tbody></table><p>这里需要特别注意 boot 流程：<code>BasicMaster::OnConfig(id)</code> 查不到对应 Driver 时，会直接以成功结果结束“update configuration”步骤。这不表示设备已经执行了应用自定义配置，只表示 <strong>Master 没有注册可调用的远程节点接口，因此没有额外配置工作需要等待</strong>。</p><h2 id="3-protected-map-是注册表，不是所有权容器"><a href="#3-protected-map-是注册表，不是所有权容器" class="headerlink" title="3. protected map 是注册表，不是所有权容器"></a>3. protected map 是注册表，不是所有权容器</h2><h3 id="3-1-为什么使用-protected-继承"><a href="#3-1-为什么使用-protected-继承" class="headerlink" title="3.1 为什么使用 protected 继承"></a>3.1 为什么使用 protected 继承</h3><p>从效果上，可以把源码理解为下面这种组合关系：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">BasicMaster</span> : <span class="keyword">public</span> Node &#123;</span><br><span class="line"> <span class="keyword">protected</span>:</span><br><span class="line">  std::map&lt;<span class="type">uint8_t</span>, DriverBase*&gt; drivers;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>源码选择继承 <code>std::map</code>，使 <code>BasicMaster</code> 内部可以直接调用 <code>find()</code>、<code>begin()</code>、<code>end()</code>、<code>erase()</code> 和遍历 <code>*this</code>。由于继承方式是 <code>protected</code>：</p><ul><li><code>BasicMaster</code> 自己及其派生类可以访问 map 接口；</li><li>普通应用代码不能把 <code>BasicMaster</code> 当成公开的 <code>std::map</code> 使用；</li><li>对外暴露的是 <code>Insert()</code>、<code>Erase()</code> 和 Master 自身的 CANopen API，而不是任意修改注册表。</li></ul><p>这是一种实现方式，不代表“Master 是一个远程节点容器”的领域建模结论。</p><h3 id="3-2-Driver-的创建、注册和注销"><a href="#3-2-Driver-的创建、注册和注销" class="headerlink" title="3.2 Driver 的创建、注册和注销"></a>3.2 Driver 的创建、注册和注销</h3><p>典型应用代码由外部构造 Master 和 Driver：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">lely::<span class="function">canopen::AsyncMaster <span class="title">master</span><span class="params">(...)</span></span>;</span><br><span class="line"><span class="function">MotorDriver <span class="title">motor_1</span><span class="params">(exec, master, <span class="number">1</span>)</span></span>;</span><br><span class="line"><span class="function">MotorDriver <span class="title">motor_2</span><span class="params">(exec, master, <span class="number">2</span>)</span></span>;</span><br></pre></td></tr></table></figure><p><code>BasicDriver</code> 构造时执行：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">master.<span class="built_in">Insert</span>(*<span class="keyword">this</span>);</span><br></pre></td></tr></table></figure><p>析构时执行：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">master.<span class="built_in">Erase</span>(*<span class="keyword">this</span>);</span><br></pre></td></tr></table></figure><p><code>BasicMaster::Insert()</code> 会检查：</p><ul><li>Node-ID 是否位于 <code>1..127</code>；</li><li>是否错误地使用了 Master 自身的 Node-ID；</li><li>同一个 Node-ID 是否已经注册。</li></ul><p>通过检查后，才执行等价于下面的登记：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">drivers[driver.<span class="built_in">id</span>()] = &amp;driver;</span><br></pre></td></tr></table></figure><p><code>Erase()</code> 只有在 Node-ID 和指针都匹配时才删除登记，并取消该节点未完成或待处理的 SDO 请求。</p><h3 id="3-3-裸指针意味着什么"><a href="#3-3-裸指针意味着什么" class="headerlink" title="3.3 裸指针意味着什么"></a>3.3 裸指针意味着什么</h3><p>注册表保存的是：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">DriverBase*</span><br></pre></td></tr></table></figure><p>而不是：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">std::unique_ptr&lt;DriverBase&gt;</span><br><span class="line">std::shared_ptr&lt;DriverBase&gt;</span><br><span class="line">DriverBase</span><br></pre></td></tr></table></figure><p>因此它表达的是“可查找关系”，不是对象所有权：</p><ul><li>Driver 内存由应用管理；</li><li>Master 不负责销毁 Driver；</li><li>Driver 析构时主动注销；</li><li>Master 必须比注册在其中的 Driver 活得更久；</li><li>不能在仍有可能投递到 Driver 的任务存在时，提前破坏 Driver 生命周期。</li></ul><p>推荐的局部对象顺序是先构造 Master，再构造 Driver。C++ 逆序析构时，Driver 会先注销，随后 Master 才销毁。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant App as Application</span><br><span class="line">    participant Mst as BasicMaster</span><br><span class="line">    participant Drv as BasicDriver</span><br><span class="line"></span><br><span class="line">    App-&gt;&gt;Mst: construct master</span><br><span class="line">    App-&gt;&gt;Drv: construct driver with exec, master and node_id</span><br><span class="line">    Drv-&gt;&gt;Mst: Insert this Driver</span><br><span class="line">    Mst-&gt;&gt;Mst: store node_id to Driver pointer</span><br><span class="line"></span><br><span class="line">    Note over Mst,Drv: Master only stores a non-owning pointer</span><br><span class="line"></span><br><span class="line">    App-&gt;&gt;Drv: destroy driver</span><br><span class="line">    Drv-&gt;&gt;Mst: Erase this Driver</span><br><span class="line">    Mst-&gt;&gt;Mst: cancel SDO requests and erase entry</span><br><span class="line">    App-&gt;&gt;Mst: destroy master</span><br></pre></td></tr></table></figure><h2 id="4-类关系：协议主站、物理节点-Driver-与逻辑设备"><a href="#4-类关系：协议主站、物理节点-Driver-与逻辑设备" class="headerlink" title="4. 类关系：协议主站、物理节点 Driver 与逻辑设备"></a>4. 类关系：协议主站、物理节点 Driver 与逻辑设备</h2><p>观察下图时，应区分三条关系：</p><ul><li><code>BasicMaster</code> 是协议操作入口，并按 Node-ID 非拥有地注册物理 Driver；</li><li><code>BasicDriver</code>、<code>FiberDriver</code>、<code>LoopDriver</code> 表示一个物理远程节点采用何种执行模型；</li><li><code>BasicLogicalDriver</code> 用于一个物理 Node-ID 内包含多个逻辑设备的情况。</li></ul><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br></pre></td><td class="code"><pre><span class="line">classDiagram</span><br><span class="line">    class Node</span><br><span class="line"></span><br><span class="line">    class BasicMaster &#123;</span><br><span class="line">        +Command(command, node_id)</span><br><span class="line">        +Boot(node_id)</span><br><span class="line">        +SubmitRead(exec, node_id, ...)</span><br><span class="line">        +AsyncRead(exec, node_id, ...)</span><br><span class="line">        +RpdoMapped(node_id)</span><br><span class="line">        +TpdoMapped(node_id)</span><br><span class="line">        +Insert(DriverBase)</span><br><span class="line">        +Erase(DriverBase)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    class DriverBase &#123;</span><br><span class="line">        &lt;&lt;abstract&gt;&gt;</span><br><span class="line">        +GetExecutor()</span><br><span class="line">        +netid()</span><br><span class="line">        +id()</span><br><span class="line">        +OnState()</span><br><span class="line">        +OnBoot()</span><br><span class="line">        +OnConfig()</span><br><span class="line">        +OnDeconfig()</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    class BasicDriver &#123;</span><br><span class="line">        +master</span><br><span class="line">        +rpdo_mapped</span><br><span class="line">        +tpdo_mapped</span><br><span class="line">        +SubmitRead()</span><br><span class="line">        +AsyncRead()</span><br><span class="line">        +Post()</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    class FiberDriver &#123;</span><br><span class="line">        +Wait()</span><br><span class="line">        +USleep()</span><br><span class="line">        +Defer()</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    class LoopDriver &#123;</span><br><span class="line">        +GetLoop()</span><br><span class="line">        +Join()</span><br><span class="line">        +Wait()</span><br><span class="line">        +RunRead()</span><br><span class="line">        +RunWrite()</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    class LogicalDriverBase &#123;</span><br><span class="line">        &lt;&lt;abstract&gt;&gt;</span><br><span class="line">        +Number()</span><br><span class="line">        +AsyncConfig()</span><br><span class="line">        +AsyncDeconfig()</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    class BasicLogicalDriver &#123;</span><br><span class="line">        +DeviceType()</span><br><span class="line">        +Profile()</span><br><span class="line">        +ObjectIndex()</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    Node &lt;|-- BasicMaster</span><br><span class="line">    DriverBase &lt;|-- BasicDriver</span><br><span class="line">    BasicDriver &lt;|-- FiberDriver</span><br><span class="line">    BasicDriver &lt;|-- LoopDriver</span><br><span class="line">    DriverBase &lt;|-- LogicalDriverBase</span><br><span class="line">    LogicalDriverBase &lt;|-- BasicLogicalDriver</span><br><span class="line">    BasicMaster &quot;1&quot; --&gt; &quot;0..*&quot; DriverBase : non-owning registry by Node-ID</span><br><span class="line">    BasicDriver &quot;1&quot; --&gt; &quot;0..8&quot; LogicalDriverBase : non-owning logical registry</span><br></pre></td></tr></table></figure><p>源码中 <code>BasicDriver</code> 对 <code>DriverBase</code> 使用非公开继承，而 <code>FiberDriver</code> 和 <code>LoopDriver</code> 再公开继承 <code>BasicDriver</code>。应用通常直接派生 <code>BasicDriver</code> 或执行模型变体，并覆写受保护回调；<code>DriverBase</code> 主要是 Master 内部统一事件分发所需的抽象契约。</p><blockquote><p>分析说明：源码没有用注释解释非公开继承的设计动机。将其理解为“隐藏底层 DriverBase 接口，同时向具体 Driver 派生类提供节点级 API 和受保护回调”，属于基于访问控制和使用方式的实现分析。</p></blockquote><h2 id="5-driver-hpp：DriverBase-定义节点应用事件契约"><a href="#5-driver-hpp：DriverBase-定义节点应用事件契约" class="headerlink" title="5. driver.hpp：DriverBase 定义节点应用事件契约"></a>5. driver.hpp：DriverBase 定义节点应用事件契约</h2><p><code>DriverBase</code> 是远程节点 Driver 的抽象接口。它不持有 SDO 状态机，也不代表远程节点对象字典副本，主要定义身份、执行域和回调契约。</p><h3 id="5-1-节点身份与执行域"><a href="#5-1-节点身份与执行域" class="headerlink" title="5.1 节点身份与执行域"></a>5.1 节点身份与执行域</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">virtual</span> ev::Executor <span class="title">GetExecutor</span><span class="params">()</span> <span class="type">const</span> <span class="keyword">noexcept</span> </span>= <span class="number">0</span>;</span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">uint8_t</span> <span class="title">netid</span><span class="params">()</span> <span class="type">const</span> <span class="keyword">noexcept</span> </span>= <span class="number">0</span>;</span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">uint8_t</span> <span class="title">id</span><span class="params">()</span> <span class="type">const</span> <span class="keyword">noexcept</span> </span>= <span class="number">0</span>;</span><br></pre></td></tr></table></figure><p><code>GetExecutor()</code> 不只是普通工具参数。它至少承担两类任务：</p><ul><li><code>BasicDriver::SubmitRead()</code>、<code>SubmitWrite()</code>、<code>AsyncRead()</code>、<code>AsyncWrite()</code> 等接口把它传给 Master，作为 SDO completion 或 Future 完成任务的执行器；</li><li>在 <code>AsyncMaster</code> 模型中，远程节点事件会先投递到这个 Executor，再调用对应的 Driver 回调。</li></ul><p>因此，同一 Driver 内部状态应按该 Executor 的并发语义设计；使用 <code>Strand</code> 时，可以把同一节点的回调串行化；<code>FiberDriver</code> 和 <code>LoopDriver</code> 的核心差异，也在于如何建立和运行这个执行域。</p><h3 id="5-2-BasicMaster-与-AsyncMaster-的回调执行差异"><a href="#5-2-BasicMaster-与-AsyncMaster-的回调执行差异" class="headerlink" title="5.2 BasicMaster 与 AsyncMaster 的回调执行差异"></a>5.2 BasicMaster 与 AsyncMaster 的回调执行差异</h3><p>这里必须区分基类默认实现和异步派生实现：</p><table><thead><tr><th>Master 类型</th><th>Driver 事件调用方式</th><th>需要注意的语义</th></tr></thead><tbody><tr><td><code>BasicMaster</code></td><td>默认事件处理函数查找 Driver 后直接调用相应回调</td><td>不应仅凭 <code>GetExecutor()</code> 就假定事件一定经过异步队列</td></tr><tr><td><code>AsyncMaster</code></td><td>重写相关事件处理函数，并把 Driver 回调投递到 <code>driver-&gt;GetExecutor()</code></td><td>不同 Driver 可以使用不同 Executor、Strand、fiber 或专用 Loop</td></tr></tbody></table><p>SDO completion 则由调用 <code>SubmitRead()</code>、<code>AsyncRead()</code> 等接口时显式传入的 Executor 决定。<code>BasicDriver</code> 只是自动传入自身的 <code>GetExecutor()</code>。</p><p>这一区分解释了为什么 <code>FiberDriver</code> 和 <code>LoopDriver</code> 的构造函数要求 <code>AsyncMaster</code>：它们不仅要为 SDO completion 提供执行域，也要求节点事件真正被投递到各自的 fiber 或专用事件循环。</p><h3 id="5-3-CANopen-事件回调"><a href="#5-3-CANopen-事件回调" class="headerlink" title="5.3 CANopen 事件回调"></a>5.3 CANopen 事件回调</h3><table><thead><tr><th>回调</th><th>触发含义</th><th>作用域与关键语义</th></tr></thead><tbody><tr><td><code>OnCanState()</code></td><td>CAN 总线状态变化</td><td>网络级，广播给已注册 Driver</td></tr><tr><td><code>OnCanError()</code></td><td>CAN 总线错误</td><td>网络级</td></tr><tr><td><code>OnCommand()</code></td><td>Master 自身发生 NMT 状态切换</td><td>网络级，不等同于远程节点状态</td></tr><tr><td><code>OnSync()</code></td><td>Master 发送或接收 SYNC</td><td>PDO 处理完成后调用</td></tr><tr><td><code>OnSyncError()</code></td><td>SYNC 长度不匹配</td><td>网络级协议异常</td></tr><tr><td><code>OnTime()</code></td><td>Master 收到 TIME</td><td>网络级时间事件</td></tr><tr><td><code>OnState()</code></td><td>心跳协议检测到远程节点状态或 boot-up</td><td>节点级</td></tr><tr><td><code>OnHeartbeat()</code></td><td>心跳超时发生或恢复</td><td>节点级，不是每帧心跳通知</td></tr><tr><td><code>OnNodeGuarding()</code></td><td>node guarding 超时发生或恢复</td><td>节点级</td></tr><tr><td><code>OnEmcy()</code></td><td>收到该远程节点的 EMCY</td><td>节点级</td></tr><tr><td><code>OnBoot()</code></td><td>NMT boot-slave 流程结束</td><td>节点级，携带状态、阶段错误码和说明</td></tr><tr><td><code>OnRpdoWrite()</code></td><td>本地主站 OD 中远程 RPDO 映射值被写入</td><td>参数采用远程对象索引语义</td></tr></tbody></table><p>这里最容易混淆的是：</p><ul><li><code>OnCommand()</code> 描述 Master 自身的 NMT 命令和状态迁移；</li><li><code>OnState()</code> 描述通过心跳协议观察到的远程节点状态；</li><li><code>OnHeartbeat(bool occurred)</code> 描述超时事件发生或解除，而不是心跳报文接收通知。</li></ul><h3 id="5-4-配置完成契约"><a href="#5-4-配置完成契约" class="headerlink" title="5.4 配置完成契约"></a>5.4 配置完成契约</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">OnConfig</span><span class="params">(</span></span></span><br><span class="line"><span class="params"><span class="function">    std::function&lt;<span class="type">void</span>(std::error_code)&gt; res)</span> <span class="keyword">noexcept</span> </span>= <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">OnDeconfig</span><span class="params">(</span></span></span><br><span class="line"><span class="params"><span class="function">    std::function&lt;<span class="type">void</span>(std::error_code)&gt; res)</span> <span class="keyword">noexcept</span> </span>= <span class="number">0</span>;</span><br></pre></td></tr></table></figure><p>这两个接口有四个关键约束：</p><ol><li>回调自身必须非阻塞；</li><li>配置工作应异步执行，或转移到其他线程或 fiber；</li><li>Master 会等待 <code>res()</code> 返回后继续流程；</li><li>实现必须最终调用一次 <code>res()</code>，否则 boot 或 deconfig 流程无法闭环。</li></ol><p>函数声明为 <code>noexcept</code>，派生类不能让异常越过边界；应在内部转换为 <code>std::error_code</code>。</p><h2 id="6-BasicDriver：固定-Node-ID、Executor-和-Master-引用"><a href="#6-BasicDriver：固定-Node-ID、Executor-和-Master-引用" class="headerlink" title="6. BasicDriver：固定 Node-ID、Executor 和 Master 引用"></a>6. BasicDriver：固定 Node-ID、Executor 和 Master 引用</h2><p><code>BasicDriver</code> 在 <code>DriverBase</code> 之上增加面向单个物理节点的应用 API。核心成员可分为两组。</p><p>固定节点上下文：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">master -&gt; 当前 CANopen Master</span><br><span class="line">exec_  -&gt; 本 Driver 的事件和 completion 执行器</span><br><span class="line">id_    -&gt; 远程节点 Node-ID</span><br></pre></td></tr></table></figure><p>PDO 映射访问能力：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">rpdo_mapped      -&gt; 远程 RPDO 映射对象的只读访问器</span><br><span class="line">tpdo_mapped      -&gt; 远程 TPDO 映射对象的读写访问器</span><br><span class="line">tpdo_event_mutex -&gt; 与 Master 共享的 TPDO 事件同步对象</span><br></pre></td></tr></table></figure><p>构造函数的核心逻辑是：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">BasicDriver::<span class="built_in">BasicDriver</span>(exec, master, id)</span><br><span class="line">    : <span class="built_in">master</span>(master),</span><br><span class="line">      <span class="built_in">rpdo_mapped</span>(master.<span class="built_in">RpdoMapped</span>(id)),</span><br><span class="line">      <span class="built_in">tpdo_mapped</span>(master.<span class="built_in">TpdoMapped</span>(id)),</span><br><span class="line">      <span class="built_in">tpdo_event_mutex</span>(master.tpdo_event_mutex),</span><br><span class="line">      <span class="built_in">exec_</span>(exec ? exec : master.<span class="built_in">GetExecutor</span>()),</span><br><span class="line">      <span class="built_in">id_</span>(id) &#123;</span><br><span class="line">  master.<span class="built_in">Insert</span>(*<span class="keyword">this</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>析构函数调用 <code>master.Erase(*this)</code>。因此，Driver 的职责不是创建通信服务，而是：</p><ul><li>固定远程 Node-ID；</li><li>固定回调执行域；</li><li>为该节点提供简化 API；</li><li>注册为节点事件接收对象；</li><li>为应用封装节点业务状态和行为。</li></ul><h3 id="6-1-为什么构造阶段就确定-Executor"><a href="#6-1-为什么构造阶段就确定-Executor" class="headerlink" title="6.1 为什么构造阶段就确定 Executor"></a>6.1 为什么构造阶段就确定 Executor</h3><p>Master 完成 <code>Insert()</code> 后，后续节点事件就可能被分发到该 Driver。因此注册前必须完成：</p><ul><li>Executor 选择；</li><li>PDO 映射访问器建立；</li><li>Node-ID 保存。</li></ul><p>这也解释了 <code>FiberDriverBase</code> 和 <code>LoopDriverBase</code> 为什么出现在继承列表前部：C++ 先构造基类，使 Fiber Executor、Loop 或 Strand 在 <code>BasicDriver</code> 注册到 Master 之前准备完成。</p><h2 id="7-直接使用-BasicMaster，还是通过-BasicDriver"><a href="#7-直接使用-BasicMaster，还是通过-BasicDriver" class="headerlink" title="7. 直接使用 BasicMaster，还是通过 BasicDriver"></a>7. 直接使用 BasicMaster，还是通过 BasicDriver</h2><p>二者不是互斥方案。生产应用通常同时使用：</p><ul><li>Master 处理网络级操作和跨节点协调；</li><li>Driver 封装每个节点的业务行为和回调状态。</li></ul><h3 id="7-1-同一个-SDO-操作的两种入口"><a href="#7-1-同一个-SDO-操作的两种入口" class="headerlink" title="7.1 同一个 SDO 操作的两种入口"></a>7.1 同一个 SDO 操作的两种入口</h3><p>直接调用 Master 时，应用显式传入 Executor 和 Node-ID：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">auto</span> future = master.<span class="built_in">AsyncRead</span>&lt;<span class="type">uint32_t</span>&gt;(</span><br><span class="line">    exec, node_id, <span class="number">0x1000</span>, <span class="number">0x00</span>);</span><br></pre></td></tr></table></figure><p>通过 Driver 时，Node-ID 和 Executor 已经固定：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">auto</span> future = driver.<span class="built_in">AsyncRead</span>&lt;<span class="type">uint32_t</span>&gt;(<span class="number">0x1000</span>, <span class="number">0x00</span>);</span><br></pre></td></tr></table></figure><p><code>BasicDriver</code> 的内联实现本质上执行：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">return</span> master.<span class="built_in">AsyncRead</span>&lt;<span class="type">uint32_t</span>&gt;(</span><br><span class="line">    <span class="built_in">GetExecutor</span>(), <span class="built_in">id</span>(), <span class="number">0x1000</span>, <span class="number">0x00</span>);</span><br></pre></td></tr></table></figure><p>因此 Driver 是节点级门面，不是另一套 SDO 实现。</p><h3 id="7-2-两条操作路径和一条事件路径"><a href="#7-2-两条操作路径和一条事件路径" class="headerlink" title="7.2 两条操作路径和一条事件路径"></a>7.2 两条操作路径和一条事件路径</h3><table><thead><tr><th>路径</th><th>是否经过 Driver 注册表</th><th>实际作用</th></tr></thead><tbody><tr><td>应用直接调用 <code>BasicMaster</code></td><td>否</td><td>显式给出 Executor 和 Node-ID，直接提交 NMT、SDO 或 PDO 相关操作</td></tr><tr><td>应用调用 <code>BasicDriver</code> 门面</td><td>否</td><td>Driver 补入已绑定的 Executor 和 Node-ID，随后仍调用 <code>BasicMaster</code></td></tr><tr><td>Master 分发协议事件</td><td>是</td><td>网络级事件遍历注册表；节点级事件按 Node-ID 查找 Driver</td></tr></tbody></table><p>因此，<strong>操作路径是否经过 <code>BasicDriver</code> 由应用选择；事件路径能否进入节点业务对象，则由 Driver 注册表决定。</strong> 注册一个 Driver 不会创建额外 SDO 服务；不注册 Driver 也不会阻断 Master 的直接通信接口。</p><h3 id="7-3-选用原则"><a href="#7-3-选用原则" class="headerlink" title="7.3 选用原则"></a>7.3 选用原则</h3><table><thead><tr><th>场景</th><th>推荐入口</th><th>原因</th></tr></thead><tbody><tr><td>临时测试工具、一次性 SDO 读写</td><td>直接 <code>BasicMaster</code></td><td>代码短，不需要建立节点业务类</td></tr><tr><td>网络级启动、停止和跨节点协调</td><td><code>BasicMaster</code></td><td>Node-ID 是运行时参数，便于统一调度</td></tr><tr><td>长期运行的单节点业务封装</td><td><code>BasicDriver</code></td><td>固定 Node-ID&#x2F;Executor，并承接节点回调</td></tr><tr><td>多种设备类型、多节点并行运行</td><td>每节点一个 Driver，共享一个 Master</td><td>隔离业务状态，避免重复传 Node-ID</td></tr><tr><td>一个物理节点包含多个逻辑设备</td><td><code>BasicDriver</code> + <code>LogicalDriver</code></td><td>需要 profile 索引换算和逻辑设备事件分发</td></tr></tbody></table><h2 id="8-PDO-映射访问：它不是一次-SDO-读写"><a href="#8-PDO-映射访问：它不是一次-SDO-读写" class="headerlink" title="8. PDO 映射访问：它不是一次 SDO 读写"></a>8. PDO 映射访问：它不是一次 SDO 读写</h2><p><code>rpdo_mapped</code> 和 <code>tpdo_mapped</code> 是 Master 对远程节点 PDO 映射数据建立的对象访问门面：</p><table><thead><tr><th>成员</th><th>权限</th><th>典型用途</th></tr></thead><tbody><tr><td><code>rpdo_mapped</code></td><td>只读</td><td>读取由远程节点 RPDO 语义对应的映射值</td></tr><tr><td><code>tpdo_mapped</code></td><td>读写</td><td>更新将通过 Master TPDO 路径发送给远程节点的映射值</td></tr></tbody></table><p>这里的命名应从 Master 的角度理解。它们访问的是 Master 管理的映射对象视图，不应等同于“立即向远程 OD 发起一次 SDO”。实际网络发送时机仍受 PDO 通信参数、传输类型、SYNC、事件触发和 Master 的 PDO 实现控制。</p><p><code>tpdo_event_mutex</code> 暴露了与 Master 共享的 TPDO 事件互斥量。它说明 TPDO 映射值更新与事件触发之间存在共享同步要求。应用在组合多个映射值并触发 PDO 时，应遵循上层 API 的锁定语义，避免其他执行上下文观察到不完整的一组数据。</p><h2 id="9-driver-cpp：默认事件分发不是业务处理"><a href="#9-driver-cpp：默认事件分发不是业务处理" class="headerlink" title="9. driver.cpp：默认事件分发不是业务处理"></a>9. driver.cpp：默认事件分发不是业务处理</h2><p><code>BasicDriver</code> 为所有 <code>DriverBase</code> 回调提供默认实现，但这些默认实现基本不处理设备业务。它们的主要职责是把事件转发给已注册的 <code>LogicalDriver</code>。</p><p>例如，CAN 状态、CAN 错误、NMT 命令、心跳超时、远程节点状态、SYNC、TIME、EMCY、node guarding 和 boot 结果，默认都会遍历内部逻辑设备表并广播。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    Mst[&quot;BasicMaster detects event&quot;] --&gt; Phy[&quot;BasicDriver callback&quot;]</span><br><span class="line">    Phy --&gt; HasLogical&#123;&quot;Logical drivers registered?&quot;&#125;</span><br><span class="line">    HasLogical -- &quot;No&quot; --&gt; Done[&quot;Default callback returns&quot;]</span><br><span class="line">    HasLogical -- &quot;Yes&quot; --&gt; Route&#123;&quot;Profile-index-specific event?&quot;&#125;</span><br><span class="line">    Route -- &quot;No&quot; --&gt; Broadcast[&quot;Broadcast to all logical drivers&quot;]</span><br><span class="line">    Route -- &quot;Yes&quot; --&gt; Select[&quot;Select logical device and rebase index&quot;]</span><br></pre></td></tr></table></figure><p>这带来一个直接结论：</p><ul><li>如果应用直接派生 <code>BasicDriver</code>、<code>FiberDriver</code> 或 <code>LoopDriver</code>，应覆写自己关心的回调；</li><li>如果没有覆写，且没有注册任何 <code>LogicalDriver</code>，大多数默认事件处理函数只是返回，不会自动完成设备业务。</li></ul><h3 id="9-1-OnRpdoWrite-的特殊路由"><a href="#9-1-OnRpdoWrite-的特殊路由" class="headerlink" title="9.1 OnRpdoWrite 的特殊路由"></a>9.1 OnRpdoWrite 的特殊路由</h3><p><code>OnRpdoWrite()</code> 是默认分发中唯一需要按对象索引选择逻辑设备的事件。</p><p>当索引位于 <code>0x6000..0x9FFF</code> 时，源码按每个逻辑设备 <code>0x800</code> 个索引计算逻辑设备号：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">logical_number = (index - 0x6000) / 0x800 + 1</span><br></pre></td></tr></table></figure><p>然后把索引还原到第一个逻辑设备的标准 profile 区域：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">logical_index = index - (logical_number - 1) * 0x800</span><br></pre></td></tr></table></figure><p>例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">物理索引 0x6810</span><br><span class="line">逻辑设备号 = (0x6810 - 0x6000) / 0x800 + 1 = 2</span><br><span class="line">传给逻辑 Driver 的索引 = 0x6810 - 0x800 = 0x6010</span><br></pre></td></tr></table></figure><p>如果索引不在 profile 区域，则事件广播给全部逻辑设备，因为源码没有足够信息把通信参数区或制造商区对象唯一归属到某个逻辑设备。</p><h2 id="10-OnConfig-与-OnDeconfig：配置流程如何闭环"><a href="#10-OnConfig-与-OnDeconfig：配置流程如何闭环" class="headerlink" title="10. OnConfig 与 OnDeconfig：配置流程如何闭环"></a>10. OnConfig 与 OnDeconfig：配置流程如何闭环</h2><p><code>BasicDriver::OnConfig()</code> 的默认行为并不是“替当前物理节点下载一组 DCF”。它只负责聚合已注册 <code>LogicalDriver</code> 的配置流程。</p><p>其执行逻辑如下：</p><ol><li>没有逻辑设备：立即 <code>res(success)</code>；</li><li>指定某个逻辑设备号：只运行对应 Driver 的 <code>AsyncConfig()</code>；</li><li>只有一个逻辑设备：直接返回该 Future；</li><li>有多个逻辑设备：分别启动 <code>AsyncConfig()</code>，使用 <code>ev::when_all()</code> 等待全部完成；</li><li>Future 完成后提取可识别的 <code>std::system_error</code>，转换为 <code>std::error_code</code>，再调用 <code>res()</code>。</li></ol><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant Mst as BasicMaster boot process</span><br><span class="line">    participant Drv as BasicDriver</span><br><span class="line">    participant Log as Logical drivers</span><br><span class="line">    participant Exec as Driver Executor</span><br><span class="line"></span><br><span class="line">    Mst-&gt;&gt;Drv: OnConfig(result_callback)</span><br><span class="line">    alt no logical driver</span><br><span class="line">        Drv--&gt;&gt;Mst: result_callback(success)</span><br><span class="line">    else one or more logical drivers</span><br><span class="line">        Drv-&gt;&gt;Log: AsyncConfig() for each target</span><br><span class="line">        Log--&gt;&gt;Exec: SdoFuture results</span><br><span class="line">        Exec-&gt;&gt;Drv: when_all completion</span><br><span class="line">        Drv--&gt;&gt;Mst: result_callback(error_code)</span><br><span class="line">    end</span><br></pre></td></tr></table></figure><p><code>OnDeconfig()</code> 使用同样的聚合模式。</p><h3 id="10-1-直接派生-BasicDriver-时应如何理解"><a href="#10-1-直接派生-BasicDriver-时应如何理解" class="headerlink" title="10.1 直接派生 BasicDriver 时应如何理解"></a>10.1 直接派生 BasicDriver 时应如何理解</h3><p>如果某设备只有一个普通物理功能，且应用直接派生 <code>FiberDriver</code>，通常会覆写该物理 Driver 的 <code>OnConfig()</code>。此时，派生类自己的实现取代 <code>BasicDriver</code> 默认实现，配置内容由应用决定。</p><p>如果物理节点包含多个逻辑设备，则可以不覆写物理 Driver 的 <code>OnConfig()</code>，让 <code>BasicDriver</code> 默认聚合各个 <code>LogicalDriver::OnConfig()</code>。</p><h3 id="10-2-错误传播边界"><a href="#10-2-错误传播边界" class="headerlink" title="10.2 错误传播边界"></a>10.2 错误传播边界</h3><p>源码在配置聚合完成后只把 <code>std::system_error</code> 中的 <code>error_code</code> 提取出来；其他异常类型会被忽略，然后以默认构造的成功码调用 <code>res()</code>。这意味着逻辑设备配置失败应通过 <code>SdoFuture</code> 中可转换为 <code>std::system_error</code> 的错误语义传播，而不应依赖任意异常类型。</p><p>这是实现层面的能力边界，不应把它理解为“所有异常都会自动转成配置失败”。应用自己的 <code>noexcept OnConfig()</code> 也应捕获全部异常，并为未知异常选择明确的本地错误码。</p><h2 id="11-LogicalDriver：一个-Node-ID-内拆分最多八个设备"><a href="#11-LogicalDriver：一个-Node-ID-内拆分最多八个设备" class="headerlink" title="11. LogicalDriver：一个 Node-ID 内拆分最多八个设备"></a>11. LogicalDriver：一个 Node-ID 内拆分最多八个设备</h2><p><code>BasicLogicalDriver</code> 不是新的 CANopen 节点，也没有独立 Node-ID。它共享物理 <code>BasicDriver</code> 的：</p><ul><li>Master；</li><li>Node-ID；</li><li>Executor；</li><li>PDO 映射视图；</li><li>TPDO 事件互斥量。</li></ul><p>它增加的是逻辑设备号、设备类型和对象索引换算。</p><h3 id="11-1-注册与生命周期"><a href="#11-1-注册与生命周期" class="headerlink" title="11.1 注册与生命周期"></a>11.1 注册与生命周期</h3><p>构造时调用 <code>driver.Insert(*this)</code>，析构时调用 <code>driver.Erase(*this)</code>。物理 Driver 内部使用 <code>std::map&lt;uint8_t, LogicalDriverBase*&gt;</code> 保存逻辑设备，接口把逻辑设备号限制在 <code>1..8</code>。</p><p>这里需要保留一个源码级未决点：当前 <code>master</code> 的 <code>BasicDriver::Insert()</code> 使用 <code>find(driver.id())</code> 检查已有项，但实际插入键是 <code>driver.Number()</code>。由于 <code>id()</code> 返回物理节点 Node-ID，而 <code>Number()</code> 返回逻辑设备号，这两个键的语义并不一致。仅根据当前实现，不能断言重复逻辑设备号一定会在插入前被正确拒绝；这更像是需要向上游确认的实现缺口。本文其余流程按公开接口“逻辑设备号唯一”的设计意图解释，但不把该意图写成已经验证的运行保证。</p><p>逻辑 Driver 持有的是裸指针注册关系，因此生命周期顺序由应用保证：逻辑 Driver 销毁前，不能再有可能访问它的待执行任务。<code>LoopDriver</code> 的文档因此特别要求，在销毁逻辑 Driver 前调用 <code>Join()</code>，让专用事件循环停止并清理剩余任务。</p><h3 id="11-2-对象索引换算"><a href="#11-2-对象索引换算" class="headerlink" title="11.2 对象索引换算"></a>11.2 对象索引换算</h3><p>逻辑 Driver 让业务代码始终使用第一个逻辑设备的标准 profile 区域 <code>0x6000..0x67FF</code>。对于逻辑设备 <code>n</code>，实际远程对象索引为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">actual_index = logical_index + (n - 1) * 0x800</span><br></pre></td></tr></table></figure><p>例如逻辑设备 3 访问 <code>0x6040</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">actual_index = 0x6040 + 2 * 0x800 = 0x7040</span><br></pre></td></tr></table></figure><p>该换算会应用到 SDO 读写以及 <code>rpdo_mapped</code>、<code>tpdo_mapped</code> 访问器；非 profile 区域索引保持不变。</p><h3 id="11-3-配置前识别设备类型"><a href="#11-3-配置前识别设备类型" class="headerlink" title="11.3 配置前识别设备类型"></a>11.3 配置前识别设备类型</h3><p>逻辑 Driver 的 <code>AsyncConfig()</code> 会先更新 <code>DeviceType()</code>：</p><ul><li>对第一个逻辑设备，优先读取 Master 本地对象 <code>0x1F84:Node-ID</code> 中的期望设备类型；</li><li>如果本地没有该值，则通过 SDO 读取远程 <code>0x1000:00</code>；</li><li>如果设备类型高 16 位为 <code>0xFFFF</code>，表示需要从逻辑设备类型对象读取具体类型；</li><li>对第二至第八个逻辑设备，读取经过索引换算的 <code>0x67FF:00</code>；</li><li>类型解析成功后，才调用该逻辑 Driver 的 <code>OnConfig()</code>。</li></ul><p>因此，<code>LogicalDriver</code> 不只是事件分组器，也承担复合设备 profile 区域换算和逻辑设备类型装配。</p><h2 id="12-FiberDriver：共享线程上的可读顺序流程"><a href="#12-FiberDriver：共享线程上的可读顺序流程" class="headerlink" title="12. FiberDriver：共享线程上的可读顺序流程"></a>12. FiberDriver：共享线程上的可读顺序流程</h2><p><code>FiberDriver</code> 在 <code>BasicDriver</code> 前增加：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">FiberThread</span><br><span class="line">FiberExecutor</span><br><span class="line">Strand</span><br></pre></td></tr></table></figure><p>其任务和回调运行在 fiber 中。<code>Wait(SdoFuture&lt;T&gt;)</code> 调用 <code>fiber_await()</code> 挂起当前 fiber，待 Future 完成后继续执行。底层承载线程仍可运行其他任务，因此它提供的是“同步写法”，不是阻塞整个事件循环线程。</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">OnConfig fiber</span><br><span class="line">  -&gt; AsyncWrite()</span><br><span class="line">  -&gt; Wait(): suspend current fiber</span><br><span class="line">  -&gt; event loop continues processing CAN/SDO</span><br><span class="line">  -&gt; future ready</span><br><span class="line">  -&gt; resume current fiber</span><br></pre></td></tr></table></figure><p><code>FiberDriver</code> 不创建每节点一个专用 OS 线程。它使用传入的 inner executor；未提供时使用 Master Executor。<code>Strand</code> 用于保证该 Driver 投递的任务按串行语义执行。</p><p>使用限制也很明确：</p><ul><li>Driver 必须在其任务实际运行的线程上实例化；</li><li><code>Wait()</code> 只能从提交到该 Driver Executor 的任务中调用；</li><li>不能从任意外部线程直接调用 <code>Wait()</code>。</li></ul><h3 id="12-1-一个配置示例"><a href="#12-1-一个配置示例" class="headerlink" title="12.1 一个配置示例"></a>12.1 一个配置示例</h3><p>下面示例体现 <code>FiberDriver</code> 的典型用途：在 <code>OnConfig()</code> 中保持顺序式代码，同时通过 fiber 挂起等待异步 SDO。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;lely/coapp/fiber_driver.hpp&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;cstdint&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;functional&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;system_error&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">DeviceDriver</span> <span class="keyword">final</span> : <span class="keyword">public</span> lely::canopen::FiberDriver &#123;</span><br><span class="line"> <span class="keyword">public</span>:</span><br><span class="line">  <span class="built_in">DeviceDriver</span>(<span class="type">ev_exec_t</span>* exec,</span><br><span class="line">               lely::canopen::AsyncMaster&amp; master,</span><br><span class="line">               <span class="type">uint8_t</span> id)</span><br><span class="line">      : <span class="built_in">FiberDriver</span>(exec, master, id) &#123;&#125;</span><br><span class="line"></span><br><span class="line"> <span class="keyword">protected</span>:</span><br><span class="line">  <span class="function"><span class="type">void</span> <span class="title">OnConfig</span><span class="params">(</span></span></span><br><span class="line"><span class="params"><span class="function">      std::function&lt;<span class="type">void</span>(std::error_code)&gt; res)</span> <span class="keyword">noexcept</span> <span class="keyword">override</span> </span>&#123;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">      <span class="comment">// Wait() suspends this fiber; it does not block the underlying event loop.</span></span><br><span class="line">      <span class="built_in">Wait</span>(<span class="built_in">AsyncWrite</span>(<span class="number">0x1017</span>, <span class="number">0</span>, <span class="type">uint16_t</span>&#123;<span class="number">1000</span>&#125;));</span><br><span class="line"></span><br><span class="line">      <span class="type">const</span> <span class="keyword">auto</span> device_type = <span class="built_in">Wait</span>(<span class="built_in">AsyncRead</span>&lt;<span class="type">uint32_t</span>&gt;(<span class="number">0x1000</span>, <span class="number">0</span>));</span><br><span class="line">      (<span class="type">void</span>)device_type;</span><br><span class="line"></span><br><span class="line">      <span class="built_in">res</span>(&#123;&#125;);</span><br><span class="line">    &#125; <span class="built_in">catch</span> (<span class="type">const</span> std::system_error&amp; e) &#123;</span><br><span class="line">      <span class="built_in">res</span>(e.<span class="built_in">code</span>());</span><br><span class="line">    &#125; <span class="built_in">catch</span> (...) &#123;</span><br><span class="line">      <span class="built_in">res</span>(std::<span class="built_in">make_error_code</span>(std::errc::io_error));</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>这个例子只说明 Driver 执行模型，不代表所有设备都应在启动阶段修改 <code>0x1017</code>。具体对象、数据类型和配置顺序必须以从站 EDS&#x2F;DCF 和产品要求为准。</p><h2 id="13-LoopDriver：每个-Driver-独立事件循环和线程"><a href="#13-LoopDriver：每个-Driver-独立事件循环和线程" class="headerlink" title="13. LoopDriver：每个 Driver 独立事件循环和线程"></a>13. LoopDriver：每个 Driver 独立事件循环和线程</h2><p><code>LoopDriver</code> 也继承 <code>BasicDriver</code>，但它的执行资源完全不同：</p><ul><li>自有 <code>ev::Loop</code>；</li><li>与 Loop 绑定的 <code>Strand</code>；</li><li>独立线程运行 <code>loop.run()</code>；</li><li><code>Wait()</code> 通过运行该 Loop 的待处理任务，直到 Future 就绪；</li><li><code>RunRead()</code>、<code>RunWrite()</code> 在 <code>AsyncRead()</code>、<code>AsyncWrite()</code> 外提供同步式包装。</li></ul><p>其构造顺序是：</p><ol><li><code>LoopDriverBase</code> 先创建 Loop 和 Strand；</li><li><code>BasicDriver</code> 使用 Strand 的 inner executor 注册到 Master；</li><li><code>Impl_</code> 创建线程并运行专用 Loop；</li><li><code>Impl_</code> 同时注册到 Master 的 I&#x2F;O Context，以便 Context shutdown 时触发 Driver shutdown。</li></ol><p>停止流程比普通 <code>BasicDriver</code> 更复杂：</p><ol><li>从 Master 注销 Driver，阻止新事件投递，并取消未完成的 SDO；</li><li>停止阻塞运行的事件循环；</li><li>重启 Loop 并以非阻塞方式处理剩余任务；</li><li>设置 stopped promise；</li><li><code>Join()</code> 等待线程退出。</li></ol><p><code>Join()</code> 通过原子标志保证多次调用时只进行一次实际等待。</p><h3 id="13-1-适用代价"><a href="#13-1-适用代价" class="headerlink" title="13.1 适用代价"></a>13.1 适用代价</h3><p><code>LoopDriver</code> 提供最直接的线程隔离，但每个 Driver 都拥有线程和事件循环。节点数量较多时，需要评估：</p><ul><li>线程栈内存；</li><li>调度和上下文切换；</li><li>Driver 销毁顺序；</li><li>逻辑 Driver 待处理任务；</li><li>应用共享资源的跨线程同步。</li></ul><blockquote><p>分析说明：<code>LoopDriver::Wait()</code> 会通过专用 Loop 继续处理待执行任务，因此同步外观不等于“期间没有其他回调运行”。若业务状态跨多个等待点，仍需考虑回调重入和状态一致性。这是根据 <code>loop.wait()</code> 的运行方式得出的并发分析。</p></blockquote><h2 id="14-三种执行模型如何选择"><a href="#14-三种执行模型如何选择" class="headerlink" title="14. 三种执行模型如何选择"></a>14. 三种执行模型如何选择</h2><table><thead><tr><th>方案</th><th>OS 线程模型</th><th>SDO 编程方式</th><th>隔离性</th><th>主要代价</th><th>适合场景</th></tr></thead><tbody><tr><td><code>BasicDriver</code></td><td>使用指定 Executor，不自行创建线程</td><td>Submit 回调或 Async Future</td><td>取决于外部 Executor</td><td>需要显式组织异步状态</td><td>已有成熟事件驱动框架，追求最小封装</td></tr><tr><td><code>FiberDriver</code></td><td>通常共享事件循环线程，不按节点新增线程</td><td><code>Wait(Async...)</code> 顺序式写法</td><td>Strand 可提供节点内串行化</td><td>受 fiber 调用上下文约束</td><td>多节点共享主循环，希望配置代码保持可读顺序</td></tr><tr><td><code>LoopDriver</code></td><td>每节点一个专用线程和 Loop</td><td><code>Wait()</code>、<code>RunRead()</code>、<code>RunWrite()</code></td><td>节点间线程隔离较强</td><td>线程、栈和销毁同步开销</td><td>节点较少，希望设备逻辑与主循环隔离</td></tr></tbody></table><p><code>LogicalDriver</code> 不属于第四种执行模型。它可以建立在 <code>BasicDriver</code>、<code>FiberDriver</code> 或 <code>LoopDriver</code> 之上，用于一个物理 Node-ID 内存在多个逻辑设备的情况。</p><p>对普通单逻辑设备从站，通常不需要 <code>LogicalDriver</code>。对多轴驱动器、复合 I&#x2F;O 或一个 CANopen 节点暴露多个 profile 区域的设备，才需要使用逻辑设备拆分。</p><h2 id="15-三条运行路径如何汇合"><a href="#15-三条运行路径如何汇合" class="headerlink" title="15. 三条运行路径如何汇合"></a>15. 三条运行路径如何汇合</h2><p>理解这套设计，关键不是只记住一条调用链，而是区分“直接操作”“Driver 门面操作”和“事件回调”三条路径。</p><h3 id="15-1-直接通过-BasicMaster-操作远程节点"><a href="#15-1-直接通过-BasicMaster-操作远程节点" class="headerlink" title="15.1 直接通过 BasicMaster 操作远程节点"></a>15.1 直接通过 BasicMaster 操作远程节点</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant App as Application</span><br><span class="line">    participant Mst as BasicMaster</span><br><span class="line">    participant Sdo as Client SDO service</span><br><span class="line">    participant Bus as CAN bus</span><br><span class="line">    participant Exec as Completion executor</span><br><span class="line"></span><br><span class="line">    App-&gt;&gt;Mst: AsyncRead(exec, node_id, index, subindex)</span><br><span class="line">    Mst-&gt;&gt;Sdo: GetSdo(node_id) and submit upload</span><br><span class="line">    Sdo-&gt;&gt;Bus: SDO request</span><br><span class="line">    Bus--&gt;&gt;Sdo: SDO response or timeout</span><br><span class="line">    Sdo-&gt;&gt;Exec: complete future</span><br><span class="line">    Exec--&gt;&gt;App: value or error</span><br></pre></td></tr></table></figure><p>这条路径不查询 Driver 注册表。它只要求 Master 能根据 Node-ID 获得所需协议资源，例如可用的 Client-SDO。</p><h3 id="15-2-通过-BasicDriver-使用节点级门面"><a href="#15-2-通过-BasicDriver-使用节点级门面" class="headerlink" title="15.2 通过 BasicDriver 使用节点级门面"></a>15.2 通过 BasicDriver 使用节点级门面</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant App as Application</span><br><span class="line">    participant Drv as BasicDriver</span><br><span class="line">    participant Mst as BasicMaster</span><br><span class="line">    participant Core as CANopen services</span><br><span class="line"></span><br><span class="line">    App-&gt;&gt;Drv: AsyncRead(index, subindex)</span><br><span class="line">    Drv-&gt;&gt;Mst: AsyncRead(GetExecutor(), id(), index, subindex)</span><br><span class="line">    Mst-&gt;&gt;Core: submit operation</span><br><span class="line">    Core--&gt;&gt;Drv: complete on Driver executor</span><br><span class="line">    Drv--&gt;&gt;App: future continuation</span><br></pre></td></tr></table></figure><p>Driver 只补齐已绑定的 Executor 和 Node-ID。真正的 SDO 请求管理、超时、传输和取消仍由 Master 及底层服务执行。</p><h3 id="15-3-协议事件回到节点业务"><a href="#15-3-协议事件回到节点业务" class="headerlink" title="15.3 协议事件回到节点业务"></a>15.3 协议事件回到节点业务</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    Bus[&quot;CAN frame or timer event&quot;] --&gt; Core[&quot;CANopen protocol services&quot;]</span><br><span class="line">    Core --&gt; Mst[&quot;Master identifies event scope and Node-ID&quot;]</span><br><span class="line">    Mst --&gt; Scope&#123;&quot;Network-wide or node-specific?&quot;&#125;</span><br><span class="line">    Scope --&gt;|&quot;Network-wide&quot;| All[&quot;Iterate all registered DriverBase pointers&quot;]</span><br><span class="line">    Scope --&gt;|&quot;Node-specific&quot;| Find&#123;&quot;find node_id&quot;&#125;</span><br><span class="line">    Find --&gt;|&quot;Not found&quot;| Skip[&quot;No DriverBase callback&quot;]</span><br><span class="line">    Find --&gt;|&quot;Found&quot;| Mode&#123;&quot;Master implementation&quot;&#125;</span><br><span class="line">    All --&gt; Mode</span><br><span class="line">    Mode --&gt;|&quot;BasicMaster&quot;| Direct[&quot;Call Driver callback directly&quot;]</span><br><span class="line">    Mode --&gt;|&quot;AsyncMaster&quot;| Post[&quot;Post callback to Driver Executor&quot;]</span><br><span class="line">    Direct --&gt; Drv[&quot;BasicDriver or derived callback&quot;]</span><br><span class="line">    Post --&gt; Drv</span><br><span class="line">    Drv --&gt; Override&#123;&quot;Physical Driver overrides callback?&quot;&#125;</span><br><span class="line">    Override --&gt;|&quot;Yes&quot;| App[&quot;Run node-specific application logic&quot;]</span><br><span class="line">    Override --&gt;|&quot;No&quot;| Logical&#123;&quot;Logical drivers registered?&quot;&#125;</span><br><span class="line">    Logical --&gt;|&quot;No&quot;| Return[&quot;Default implementation returns&quot;]</span><br><span class="line">    Logical --&gt;|&quot;Yes&quot;| Dispatch[&quot;Broadcast or route by profile index&quot;]</span><br></pre></td></tr></table></figure><p><code>BasicMaster</code> 与 <code>AsyncMaster</code> 共享同一 Driver 注册关系，但回调调度方式不同：前者的默认实现直接通知 Driver，后者把回调投递到 Driver 的 Executor。无论哪种实现，找不到对应 Node-ID 时都不会产生节点级 Driver 回调。</p><p>三条路径合起来，才是完整心智模型：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">BasicMaster 负责协议操作和网络调度</span><br><span class="line">BasicDriver 负责节点上下文封装和节点事件接入</span><br><span class="line">Driver 注册表只服务于查找和回调，不是协议资源或远程对象所有权</span><br></pre></td></tr></table></figure><h2 id="16-关键设计结论"><a href="#16-关键设计结论" class="headerlink" title="16. 关键设计结论"></a>16. 关键设计结论</h2><ol><li><strong><code>BasicMaster</code> 可以直接操作远程节点。</strong> NMT、boot-slave、SDO 和 PDO 映射接口都显式接收 Node-ID，Driver 不是通信必经层。</li><li><strong><code>protected std::map&lt;uint8_t, DriverBase*&gt;</code> 是非拥有注册表。</strong> Master 构造的是空 map，不会构造、持有或删除一组 Driver 对象。</li><li><strong>Driver 对象由应用创建。</strong> <code>BasicDriver</code> 构造时 <code>Insert()</code>，析构时 <code>Erase()</code>；Master 必须比 Driver 生命周期更长。</li><li><strong>协议资源与 Driver 注册表相互独立。</strong> SDO 调用通过 Node-ID 获取 Client-SDO；没有 Driver 不等于没有 SDO，没有 SDO 也不能通过创建 Driver 自动补足。</li><li><strong><code>BasicDriver</code> 是固定节点上下文的门面。</strong> 它把 Executor 和 Node-ID 注入 <code>BasicMaster</code> API，并为应用集中保存节点业务状态。</li><li><strong>Driver 注册表决定节点级应用回调能否到达。</strong> 没有注册 Driver 时，Master 仍可通信，但不会调用该节点的 <code>DriverBase::OnState()</code>、<code>OnEmcy()</code> 等回调。</li><li><strong>事件回调是否异步投递取决于 Master 实现。</strong> <code>BasicMaster</code> 默认直接通知 Driver；<code>AsyncMaster</code> 才把相关回调投递到 <code>driver-&gt;GetExecutor()</code>。SDO completion 则由提交操作时传入的 Executor 决定。</li><li><strong>自定义配置流程依赖 Driver。</strong> Master 找不到 Driver 时会把额外的 update-configuration 步骤视为完成，不会替应用执行自定义 <code>OnConfig()</code>。</li><li><strong><code>driver.cpp</code> 的默认回调主要服务于 LogicalDriver 聚合。</strong> 普通物理 Driver 未覆写回调且未注册逻辑 Driver 时，默认实现通常不会产生设备业务行为。</li><li><strong>FiberDriver 和 LoopDriver 只改变执行与等待模型。</strong> Fiber 在共享执行线程上挂起 fiber；Loop 为每个 Driver 建立独立线程和事件循环，并且二者依赖 <code>AsyncMaster</code> 完成 Driver 事件投递。</li><li><strong>LogicalDriver 是同一物理 Node-ID 内的 profile 分区。</strong> 它共享 Master、Node-ID 和通信资源，只增加逻辑编号、类型识别、索引换算和二次事件分发。</li></ol><h2 id="参考源码"><a href="#参考源码" class="headerlink" title="参考源码"></a>参考源码</h2><ol><li><a href="https://gitlab.com/lely_industries/lely-core/-/blob/master/include/lely/coapp/master.hpp">include&#x2F;lely&#x2F;coapp&#x2F;master.hpp</a></li><li><a href="https://gitlab.com/lely_industries/lely-core/-/blob/master/src/coapp/master.cpp">src&#x2F;coapp&#x2F;master.cpp</a></li><li><a href="https://gitlab.com/lely_industries/lely-core/-/blob/master/include/lely/coapp/driver.hpp">include&#x2F;lely&#x2F;coapp&#x2F;driver.hpp</a></li><li><a href="https://gitlab.com/lely_industries/lely-core/-/blob/master/src/coapp/driver.cpp">src&#x2F;coapp&#x2F;driver.cpp</a></li><li><a href="https://gitlab.com/lely_industries/lely-core/-/blob/master/include/lely/coapp/fiber_driver.hpp">include&#x2F;lely&#x2F;coapp&#x2F;fiber_driver.hpp</a></li><li><a href="https://gitlab.com/lely_industries/lely-core/-/blob/master/src/coapp/fiber_driver.cpp">src&#x2F;coapp&#x2F;fiber_driver.cpp</a></li><li><a href="https://gitlab.com/lely_industries/lely-core/-/blob/master/include/lely/coapp/loop_driver.hpp">include&#x2F;lely&#x2F;coapp&#x2F;loop_driver.hpp</a></li><li><a href="https://gitlab.com/lely_industries/lely-core/-/blob/master/src/coapp/loop_driver.cpp">src&#x2F;coapp&#x2F;loop_driver.cpp</a></li><li><a href="https://gitlab.com/lely_industries/lely-core/-/blob/master/include/lely/coapp/logical_driver.hpp">include&#x2F;lely&#x2F;coapp&#x2F;logical_driver.hpp</a></li><li><a href="https://gitlab.com/lely_industries/lely-core/-/blob/master/src/coapp/logical_driver.cpp">src&#x2F;coapp&#x2F;logical_driver.cpp</a></li><li><a href="https://lely_industries.gitlab.io/lely-core/doxygen/classlely_1_1canopen_1_1BasicMaster.html">BasicMaster 2.4.0 API 与源码页</a></li></ol>]]></content>
    
    
      
      
    <summary type="html">&lt;meta name=&quot;referrer&quot; content=&quot;no-referrer&quot; /&gt;

&lt;h1 id=&quot;Lely-CANopen-coapp-Driver：BasicMaster-与-BasicDriver-的职责边界、注册表和事件流程&quot;&gt;&lt;a href=&quot;#Lely-C</summary>
      
    
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/categories/canopen/"/>
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/tags/canopen/"/>
    
  </entry>
  
  <entry>
    <title>Lely CANopen Master</title>
    <link href="https://wdfk-prog.space/posts/2e6671a0/"/>
    <id>https://wdfk-prog.space/posts/2e6671a0/</id>
    <published>2026-08-03T02:11:42.000Z</published>
    <updated>2026-08-03T02:12:13.067Z</updated>
    
    <content type="html"><![CDATA[<meta name="referrer" content="no-referrer" /><h1 id="Lely-CANopen-coapp-Master：NMT-总控、SDO-仲裁与-Driver-事件分发机制"><a href="#Lely-CANopen-coapp-Master：NMT-总控、SDO-仲裁与-Driver-事件分发机制" class="headerlink" title="Lely CANopen coapp::Master：NMT 总控、SDO 仲裁与 Driver 事件分发机制"></a>Lely CANopen <code>coapp::Master</code>：NMT 总控、SDO 仲裁与 Driver 事件分发机制</h1><blockquote><p>摘要：从 master.hpp 与 master.cpp 追踪 BasicMaster&#x2F;AsyncMaster 如何组织 NMT 启动、SDO 所有权、配置握手及 Driver 事件分发。</p></blockquote><p><img src="https://i-blog.csdnimg.cn/direct/c08a32e555f64f67ba98001e54020cc6.png" alt="在这里插入图片描述"></p><p>@[toc]<br><code>lely::canopen::BasicMaster</code> 和 <code>lely::canopen::AsyncMaster</code> 是 Lely CANopen C++ 应用层中负责主站编排的核心对象。它们并不是简单地把 NMT、SDO、PDO API 包装成 C++ 成员函数，而是在 <code>Node</code> 的本地 CANopen 节点能力之上，进一步维护远端节点 Driver、节点启动状态、配置阶段状态以及 Client-SDO 使用权。</p><p>理解这两个类的关键，不是逐个记住 <code>SubmitRead()</code>、<code>Command()</code>、<code>OnBoot()</code> 等接口，而是建立下面这条主线：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">本地主站 Node 启动</span><br><span class="line">    -&gt; NMT master 发起或接管远端节点 boot slave 流程</span><br><span class="line">    -&gt; boot 过程中独占或临时移交 Client-SDO</span><br><span class="line">    -&gt; Driver 完成设备相关配置</span><br><span class="line">    -&gt; Master 汇总状态并分发 NMT/PDO/EMCY/SYNC/TIME 事件</span><br></pre></td></tr></table></figure><hr><h2 id="1-先建立整体心智模型"><a href="#1-先建立整体心智模型" class="headerlink" title="1. 先建立整体心智模型"></a>1. 先建立整体心智模型</h2><p><code>BasicMaster</code> 同时承担四类职责：</p><ol><li><strong>本地 CANopen 节点</strong>：继承 <code>Node</code>，拥有本地对象字典、NMT、PDO、SYNC、TIME、EMCY、CAN channel、timer 和 executor 等能力。</li><li><strong>远端节点编排器</strong>：通过 NMT master 的 boot slave 流程识别、检查、配置并启动从站。</li><li><strong>Driver 注册表</strong>：按 node-ID 保存一个 <code>DriverBase*</code>，把属于某个远端节点的事件交给对应 Driver。</li><li><strong>Client-SDO 仲裁器</strong>：决定应用层 SDO 当前是否可用，以及由普通请求、NMT boot 还是配置阶段占用。</li></ol><p><code>AsyncMaster</code> 没有重新实现一套协议状态机。它继承 <code>BasicMaster</code>，主要改变“事件如何进入用户 Driver”：</p><ul><li><code>BasicMaster</code> 在协议事件处理路径中直接调用 Driver 回调，但调用前暂时释放 Master 锁；</li><li><code>AsyncMaster</code> 把回调封装为 task，投递到该 Driver 的 executor，当前协议回调随后返回。</li></ul><p>官方教程也用这个差异解释 <code>AsyncMaster</code>：用户定义的 CANopen 事件回调会作为事件循环任务执行，而不是直接嵌在协议栈事件处理调用栈中。[S3]</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    App[&quot;应用程序&quot;] --&gt; Master[&quot;BasicMaster / AsyncMaster&quot;]</span><br><span class="line"></span><br><span class="line">    Master --&gt; Node[&quot;Node：本地主站节点&quot;]</span><br><span class="line">    Node --&gt; Dev[&quot;Device：本地对象字典&quot;]</span><br><span class="line">    Node --&gt; Nmt[&quot;NMT / PDO / EMCY / SYNC / TIME&quot;]</span><br><span class="line">    Node --&gt; Io[&quot;CAN channel / timer / executor&quot;]</span><br><span class="line"></span><br><span class="line">    Master --&gt; Registry[&quot;Driver 注册表 node-ID -&gt; DriverBase*&quot;]</span><br><span class="line">    Master --&gt; State[&quot;ready[] / config[]&quot;]</span><br><span class="line">    Master --&gt; Sdos[&quot;sdos：按节点维护 Client-SDO&quot;]</span><br><span class="line"></span><br><span class="line">    Nmt --&gt; Master</span><br><span class="line">    Master --&gt; DriverA[&quot;Driver：node 2&quot;]</span><br><span class="line">    Master --&gt; DriverB[&quot;Driver：node 3&quot;]</span><br></pre></td></tr></table></figure><p>这张图中最重要的关系是：<strong>Master 本身仍然是一个 CANopen Node；Driver 才是远端设备的应用层接口。</strong></p><hr><h2 id="2-master-hpp-定义了什么抽象"><a href="#2-master-hpp-定义了什么抽象" class="headerlink" title="2. master.hpp 定义了什么抽象"></a>2. <code>master.hpp</code> 定义了什么抽象</h2><h3 id="2-1-继承关系不是装饰，而是职责组合"><a href="#2-1-继承关系不是装饰，而是职责组合" class="headerlink" title="2.1 继承关系不是装饰，而是职责组合"></a>2.1 继承关系不是装饰，而是职责组合</h3><p><code>master.hpp</code> 中的核心声明可以概括为：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">BasicMaster</span> : <span class="keyword">public</span> Node,</span><br><span class="line">                    <span class="keyword">protected</span> std::map&lt;<span class="type">uint8_t</span>, DriverBase*&gt;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">AsyncMaster</span> : <span class="keyword">public</span> BasicMaster;</span><br></pre></td></tr></table></figure><p>这意味着：</p><ul><li>从 <code>Node</code> 继承本地主站所需的协议服务和对象字典访问能力；</li><li>从受保护的 <code>std::map</code> 继承 Driver 容器行为；</li><li>派生类可以按关联容器方式遍历 Driver，但外部应用不能随意破坏注册表；</li><li><code>AsyncMaster</code> 只需覆盖事件入口，即可改变整个 Driver 回调的执行上下文。[S1][S4]</li></ul><h3 id="2-2-BasicMaster-Impl-保存真正的主站协调状态"><a href="#2-2-BasicMaster-Impl-保存真正的主站协调状态" class="headerlink" title="2.2 BasicMaster::Impl_ 保存真正的主站协调状态"></a>2.2 <code>BasicMaster::Impl_</code> 保存真正的主站协调状态</h3><p><code>master.cpp:45-68</code> 中的私有实现包含以下数据：</p><table><thead><tr><th>成员</th><th>作用</th></tr></thead><tbody><tr><td><code>self</code></td><td>回指所属 <code>BasicMaster</code></td></tr><tr><td><code>on_node_guarding</code></td><td>可注册的 node guarding 通知函数</td></tr><tr><td><code>on_boot</code></td><td>可注册的 boot 完成通知函数</td></tr><tr><td><code>ready[CO_NUM_NODES]</code></td><td>每个 node-ID 的 boot-ready 标志</td></tr><tr><td><code>config[CO_NUM_NODES]</code></td><td>每个 node-ID 是否处于 update configuration 阶段</td></tr><tr><td><code>sdos</code></td><td><code>node-ID -&gt; Sdo</code>，保存默认或配置阶段 Client-SDO 队列</td></tr></tbody></table><p>这里没有保存一份“远端完整对象字典”。远端设备类型、业务行为和回调由 Driver 表达；Master 只保存完成编排所需的最小状态。</p><h3 id="2-3-三组访问接口对应三种不同的数据路径"><a href="#2-3-三组访问接口对应三种不同的数据路径" class="headerlink" title="2.3 三组访问接口对应三种不同的数据路径"></a>2.3 三组访问接口对应三种不同的数据路径</h3><p><code>BasicMaster</code> 对外暴露的对象访问接口容易混淆，实际上分别对应：</p><table><thead><tr><th>接口形态</th><th>操作对象</th><th align="right">是否产生远端 SDO 报文</th></tr></thead><tbody><tr><td><code>master[idx][subidx]</code></td><td>主站本地对象字典</td><td align="right">否</td></tr><tr><td><code>master.RpdoMapped(id)[idx][subidx]</code></td><td>主站 RPDO 代理中缓存的远端数据</td><td align="right">否</td></tr><tr><td><code>master.TpdoMapped(id)[idx][subidx]</code></td><td>主站 TPDO 代理中准备发送给远端的数据</td><td align="right">否</td></tr><tr><td><code>SubmitRead/AsyncRead</code></td><td>远端节点对象字典</td><td align="right">是</td></tr><tr><td><code>SubmitWrite/AsyncWrite</code></td><td>远端节点对象字典</td><td align="right">是</td></tr></tbody></table><p>从总线方向理解更直观：</p><ul><li><code>RpdoMapped(id)</code> 是只读入口，读取主站已经通过 RPDO 接收的数据；这些数据来源于远端节点的 TPDO。</li><li><code>TpdoMapped(id)</code> 是读写入口，修改主站本地 TPDO 代理；随后数据可通过主站 TPDO 发往远端节点的 RPDO。</li><li>只有 SDO API 才会按 index&#x2F;sub-index 对远端对象字典发起请求。</li></ul><p>因此，PDO 映射访问是“本地代理对象访问”，SDO 访问才是“远端对象字典事务”。[S1][S3]</p><hr><h2 id="3-从构造到-Reset-：Master-何时真正开始工作"><a href="#3-从构造到-Reset-：Master-何时真正开始工作" class="headerlink" title="3. 从构造到 Reset()：Master 何时真正开始工作"></a>3. 从构造到 <code>Reset()</code>：Master 何时真正开始工作</h2><h3 id="3-1-构造函数只完成装配"><a href="#3-1-构造函数只完成装配" class="headerlink" title="3.1 构造函数只完成装配"></a>3.1 构造函数只完成装配</h3><p><code>BasicMaster</code> 支持三种设备描述来源：</p><ol><li>已创建的 <code>co_dev_t*</code>；</li><li>文本 EDS&#x2F;DCF 与可选 concise DCF；</li><li>静态设备描述 <code>co_sdev*</code>。</li></ol><p>三个构造函数都遵循同一结构：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">构造 Node</span><br><span class="line">    -&gt; 构造 BasicMaster::TpdoEventMutex</span><br><span class="line">    -&gt; 创建 Impl_</span><br><span class="line">    -&gt; 将 Node 内部的 co_nmt_t* 交给 Impl_</span><br><span class="line">    -&gt; 注册 NMT boot/config/node-guarding indication</span><br></pre></td></tr></table></figure><p>官方 API 明确说明：构造完成后 Master 仍处于 NMT Initialisation 状态，不会立即创建全部服务或进行通信；调用 <code>Reset()</code> 后才启动本地 NMT boot-up。[S2][S4]</p><h3 id="3-2-dcf-txt-与-dcf-bin-的角色"><a href="#3-2-dcf-txt-与-dcf-bin-的角色" class="headerlink" title="3.2 dcf_txt 与 dcf_bin 的角色"></a>3.2 <code>dcf_txt</code> 与 <code>dcf_bin</code> 的角色</h3><p>构造参数中的：</p><ul><li><code>dcf_txt</code> 描述主站本地对象字典和 NMT 配置；</li><li><code>dcf_bin</code> 是加载到主站本地对象字典的 concise DCF；</li><li>对远端从站的自动配置通常由主站 DCF 中的网络管理对象引用相应配置内容，再由 NMT boot slave 流程执行。</li></ul><p>官方 C++ 教程使用 <code>dcfgen</code> 生成 <code>master.dcf</code>，并说明某些配置还会生成 <code>master.bin</code>；Master 构造后调用 <code>Reset()</code>，由本地 NMT 服务开始运行。[S3]</p><p>下面是用于说明装配关系的最小化代码，省略 I&#x2F;O 初始化细节：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">lely::<span class="function">canopen::AsyncMaster <span class="title">master</span><span class="params">(</span></span></span><br><span class="line"><span class="params"><span class="function">    executor, timer, channel, <span class="string">&quot;master.dcf&quot;</span>, <span class="string">&quot;master.bin&quot;</span>, <span class="number">1</span>)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="function">MyDriver <span class="title">node2_driver</span><span class="params">(driver_executor, master, <span class="number">2</span>)</span></span>;</span><br><span class="line"><span class="function">MyDriver <span class="title">node3_driver</span><span class="params">(driver_executor, master, <span class="number">3</span>)</span></span>;</span><br><span class="line"></span><br><span class="line">master.<span class="built_in">Reset</span>();</span><br></pre></td></tr></table></figure><p>这里的顺序有实际含义：</p><ul><li>Master 必须先存在，Driver 才能注册到它；</li><li>Driver 应在远端节点事件到来前完成注册；</li><li><code>Reset()</code> 只是启动本地主站 NMT，具体是否自动 boot 某个从站还受主站 DCF 中 CiA 302 网络管理配置影响。</li></ul><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant App as &quot;Application&quot;</span><br><span class="line">    participant M as &quot;AsyncMaster&quot;</span><br><span class="line">    participant N as &quot;Node / co_nmt_t&quot;</span><br><span class="line">    participant D as &quot;Driver&quot;</span><br><span class="line">    participant E as &quot;event loop&quot;</span><br><span class="line"></span><br><span class="line">    App-&gt;&gt;M: &quot;construct(timer, channel, DCF, node-ID)&quot;</span><br><span class="line">    M-&gt;&gt;N: &quot;construct local CANopen node&quot;</span><br><span class="line">    M-&gt;&gt;N: &quot;register boot/config indications&quot;</span><br><span class="line">    App-&gt;&gt;D: &quot;construct and register node-ID&quot;</span><br><span class="line">    App-&gt;&gt;M: &quot;Reset()&quot;</span><br><span class="line">    M-&gt;&gt;N: &quot;enter reset application / communication&quot;</span><br><span class="line">    N-&gt;&gt;E: &quot;schedule CANopen and I/O work&quot;</span><br></pre></td></tr></table></figure><hr><h2 id="4-Driver-注册表如何把多从站拆成独立接口"><a href="#4-Driver-注册表如何把多从站拆成独立接口" class="headerlink" title="4. Driver 注册表如何把多从站拆成独立接口"></a>4. Driver 注册表如何把多从站拆成独立接口</h2><h3 id="4-1-一个-node-ID-只能注册一个-Driver"><a href="#4-1-一个-node-ID-只能注册一个-Driver" class="headerlink" title="4.1 一个 node-ID 只能注册一个 Driver"></a>4.1 一个 node-ID 只能注册一个 Driver</h3><p><code>BasicMaster::Insert()</code> 在加锁后检查：</p><ul><li>node-ID 必须位于 1…127；</li><li>不能等于 Master 自己的 node-ID；</li><li>同一 node-ID 不能重复注册。</li></ul><p>通过检查后，注册表保存：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">node-ID -&gt; DriverBase*</span><br></pre></td></tr></table></figure><p><code>Erase()</code> 只删除与当前指针完全匹配的 Driver，并先取消该节点的 SDO 队列。这使该节点的排队或进行中 SDO 与 Driver 注册生命周期一并结束。[S2]</p><h3 id="4-2-事件分为“广播型”和“单节点型”"><a href="#4-2-事件分为“广播型”和“单节点型”" class="headerlink" title="4.2 事件分为“广播型”和“单节点型”"></a>4.2 事件分为“广播型”和“单节点型”</h3><table><thead><tr><th>事件</th><th>分发范围</th><th>典型 Driver 回调</th></tr></thead><tbody><tr><td>CAN state 变化</td><td>所有 Driver</td><td><code>OnCanState()</code></td></tr><tr><td>CAN error</td><td>所有 Driver</td><td><code>OnCanError()</code></td></tr><tr><td>Master 本地 NMT command</td><td>所有 Driver</td><td><code>OnCommand()</code></td></tr><tr><td>SYNC</td><td>所有 Driver</td><td><code>OnSync()</code></td></tr><tr><td>SYNC 长度错误</td><td>所有 Driver</td><td><code>OnSyncError()</code></td></tr><tr><td>TIME</td><td>所有 Driver</td><td><code>OnTime()</code></td></tr><tr><td>某节点 PDO 数据写入</td><td>指定 node-ID</td><td><code>OnRpdoWrite()</code></td></tr><tr><td>heartbeat 发生&#x2F;恢复</td><td>指定 node-ID</td><td><code>OnHeartbeat()</code></td></tr><tr><td>远端 NMT state 变化</td><td>指定 node-ID</td><td><code>OnState()</code></td></tr><tr><td>EMCY</td><td>指定 node-ID</td><td><code>OnEmcy()</code></td></tr><tr><td>node guarding 超时&#x2F;恢复</td><td>指定 node-ID</td><td><code>OnNodeGuarding()</code></td></tr><tr><td>boot slave 完成</td><td>指定 node-ID</td><td><code>OnBoot()</code></td></tr><tr><td>update configuration</td><td>指定 node-ID</td><td><code>OnConfig()</code></td></tr></tbody></table><p>这种划分使主站可以同时管理不同类型的从站：通用总线事件广播给所有 Driver，设备相关事件只进入对应 node-ID 的 Driver。</p><hr><h2 id="5-Boot-id-：单节点启动流程的编排入口"><a href="#5-Boot-id-：单节点启动流程的编排入口" class="headerlink" title="5. Boot(id)：单节点启动流程的编排入口"></a>5. <code>Boot(id)</code>：单节点启动流程的编排入口</h2><h3 id="5-1-Boot-不是发送一帧-NMT-命令"><a href="#5-1-Boot-不是发送一帧-NMT-命令" class="headerlink" title="5.1 Boot() 不是发送一帧 NMT 命令"></a>5.1 <code>Boot()</code> 不是发送一帧 NMT 命令</h3><p><code>BasicMaster::Boot(id)</code> 请求的是完整 NMT boot slave 过程，而不是简单调用一次 <code>Command(START, id)</code>。其等价逻辑如下：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">校验 node-ID</span><br><span class="line">    -&gt; 获取 Master 锁</span><br><span class="line">    -&gt; 若该节点已经 booting，返回 false</span><br><span class="line">    -&gt; 取消该节点正在执行或排队的应用 SDO</span><br><span class="line">    -&gt; 暂时清除 ready 标志</span><br><span class="line">    -&gt; co_nmt_boot_req(...)</span><br><span class="line">    -&gt; 请求提交失败时恢复原 ready 状态并报错</span><br></pre></td></tr></table></figure><p>为什么先取消 SDO？源码注释直接给出原因：NMT master 在 boot slave 过程中可能需要使用同一个默认 Client-SDO 服务，例如读取设备类型、身份对象或下发配置。[S2]</p><h3 id="5-2-boot-slave-的核心阶段"><a href="#5-2-boot-slave-的核心阶段" class="headerlink" title="5.2 boot slave 的核心阶段"></a>5.2 boot slave 的核心阶段</h3><p><code>master.cpp</code> 并未重新实现 CiA 302 boot 状态机；它调用 C 层 NMT 服务，并通过 indication 接收关键节点：</p><ul><li><code>co_nmt_set_boot_ind()</code>：boot slave 完成；</li><li><code>co_nmt_set_cfg_ind()</code>：进入 update configuration 阶段；</li><li><code>co_nmt_set_ng_ind()</code>：node guarding 事件。</li></ul><p>因此 <code>BasicMaster</code> 的作用更接近“C 层 NMT 状态机的 C++ 编排和应用桥接层”。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant App as &quot;Application&quot;</span><br><span class="line">    participant M as &quot;BasicMaster&quot;</span><br><span class="line">    participant N as &quot;C NMT master&quot;</span><br><span class="line">    participant S as &quot;Remote slave&quot;</span><br><span class="line">    participant D as &quot;Driver&quot;</span><br><span class="line"></span><br><span class="line">    App-&gt;&gt;M: &quot;Boot(id)&quot;</span><br><span class="line">    M-&gt;&gt;M: &quot;CancelSdo(id), ready=false&quot;</span><br><span class="line">    M-&gt;&gt;N: &quot;co_nmt_boot_req(id, timeout)&quot;</span><br><span class="line">    N-&gt;&gt;S: &quot;NMT / SDO checks&quot;</span><br><span class="line"></span><br><span class="line">    opt &quot;需要应用层更新配置&quot;</span><br><span class="line">        N-&gt;&gt;M: &quot;configuration indication with co_csdo_t*&quot;</span><br><span class="line">        M-&gt;&gt;D: &quot;OnConfig(done)&quot;</span><br><span class="line">        D--&gt;&gt;M: &quot;done(error_code)&quot;</span><br><span class="line">        M-&gt;&gt;N: &quot;co_nmt_cfg_res(id, abort-code)&quot;</span><br><span class="line">    end</span><br><span class="line"></span><br><span class="line">    N-&gt;&gt;M: &quot;boot indication(id, state, error-status)&quot;</span><br><span class="line">    M-&gt;&gt;M: &quot;update ready flag&quot;</span><br><span class="line">    M-&gt;&gt;D: &quot;OnBoot(state, status, description)&quot;</span><br></pre></td></tr></table></figure><h3 id="5-3-ready-的准确语义"><a href="#5-3-ready-的准确语义" class="headerlink" title="5.3 ready 的准确语义"></a>5.3 <code>ready</code> 的准确语义</h3><p>官方 API 对 <code>IsReady(id)</code> 的定义是：</p><ul><li>该节点的 boot slave 流程已经成功完成；</li><li>此后没有再次收到该节点的 boot-up 事件；</li><li>调用 <code>AsyncDeconfig()</code> 也会将其标记为 not ready。[S4]</li></ul><p><code>Impl_::OnBootInd()</code> 在无错误，或错误状态字符为 <code>L</code> 时，把 <code>ready[id - 1]</code> 置为 <code>true</code>。<code>L</code> 表示从站最初已处于 Operational，NMT master 可以继续处理其他节点。[S2][S4]</p><p>需要特别区分：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">IsReady(id) == true</span><br></pre></td></tr></table></figure><p>不等价于：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">远端设备此刻已经完成应用内部初始化，并稳定处于 Operational</span><br></pre></td></tr></table></figure><p>维护者在 issue #92 中解释过：NMT start 命令没有确认机制，<code>OnBoot()</code> 可能在从站真正进入 Operational 前先被调用；判断 PDO 是否已经可靠可用，应同时观察 Master 和从站的 NMT state。[S6]</p><hr><h2 id="6-Client-SDO-为什么必须由-Master-仲裁"><a href="#6-Client-SDO-为什么必须由-Master-仲裁" class="headerlink" title="6. Client-SDO 为什么必须由 Master 仲裁"></a>6. Client-SDO 为什么必须由 Master 仲裁</h2><p>这是 <code>master.cpp</code> 中最关键、也最容易在实际项目中触发异常的设计。</p><h3 id="6-1-默认-SDO-不是永久可用资源"><a href="#6-1-默认-SDO-不是永久可用资源" class="headerlink" title="6.1 默认 SDO 不是永久可用资源"></a>6.1 默认 SDO 不是永久可用资源</h3><p><code>GetSdo(id)</code> 按以下顺序决定是否返回 Client-SDO：</p><table><thead><tr><th>条件</th><th>结果</th><th>原因</th></tr></thead><tbody><tr><td>node-ID 非法</td><td>抛出范围异常</td><td>请求本身无效</td></tr><tr><td>Master 不在 Pre-operational 或 Operational</td><td>返回空</td><td>CSDO 服务在当前 NMT 状态不可用</td></tr><tr><td><code>sdos[id]</code> 已存在</td><td>返回现有队列</td><td>可能是普通默认 SDO，也可能是配置阶段临时包装</td></tr><tr><td>该节点正在 boot slave</td><td>返回空</td><td>NMT master 正在占用默认 Client-SDO</td></tr><tr><td>其余情况</td><td>延迟创建并返回默认 <code>Sdo</code></td><td>应用可提交普通远端 SDO 请求</td></tr></tbody></table><p>这里的判断顺序很重要：<strong>配置阶段的 <code>sdos[id]</code> 检查位于 booting 检查之前。</strong> 这使得节点虽然仍处于 boot slave 流程，Driver 在 <code>OnConfig()</code> 内却可以合法使用 NMT 服务临时移交的 Client-SDO。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    Start[&quot;GetSdo(id)&quot;] --&gt; IdOk&#123;&quot;node-ID 有效?&quot;&#125;</span><br><span class="line">    IdOk -- &quot;否&quot; --&gt; Throw[&quot;抛出 out_of_range&quot;]</span><br><span class="line">    IdOk -- &quot;是&quot; --&gt; StateOk&#123;&quot;Master 为 PREOP 或 START?&quot;&#125;</span><br><span class="line">    StateOk -- &quot;否&quot; --&gt; NoneA[&quot;返回 nullptr / NO_SDO&quot;]</span><br><span class="line">    StateOk -- &quot;是&quot; --&gt; Existing&#123;&quot;sdos[id] 已存在?&quot;&#125;</span><br><span class="line">    Existing -- &quot;是&quot; --&gt; ReturnExisting[&quot;返回现有 Sdo&quot;]</span><br><span class="line">    Existing -- &quot;否&quot; --&gt; Booting&#123;&quot;节点正在 booting?&quot;&#125;</span><br><span class="line">    Booting -- &quot;是&quot; --&gt; NoneB[&quot;返回 nullptr / NO_SDO&quot;]</span><br><span class="line">    Booting -- &quot;否&quot; --&gt; Create[&quot;创建默认 Client-SDO 队列&quot;]</span><br><span class="line">    Create --&gt; ReturnNew[&quot;返回新 Sdo&quot;]</span><br></pre></td></tr></table></figure><h3 id="6-2-所有-SDO-API-最终都经过同一闸门"><a href="#6-2-所有-SDO-API-最终都经过同一闸门" class="headerlink" title="6.2 所有 SDO API 最终都经过同一闸门"></a>6.2 所有 SDO API 最终都经过同一闸门</h3><p><code>SubmitRead()</code>、<code>SubmitWrite()</code>、block 传输、future 版本以及 DCF 下载接口，虽然模板重载很多，主干基本一致：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">获取 Master 锁</span><br><span class="line">    -&gt; GetSdo(id)</span><br><span class="line">    -&gt; 更新 CAN network time</span><br><span class="line">    -&gt; 向 Sdo 队列提交请求</span><br><span class="line">    -&gt; 不可用时返回或抛出 SdoErrc::NO_SDO</span><br></pre></td></tr></table></figure><p>因此，看到 <code>060A0023 Resource not available: SDO connection</code> 时，不能直接判断为从站 SDO server 故障。它可能只是：</p><ul><li>Master 本地 NMT 状态不允许创建 Client-SDO；</li><li>NMT boot slave 正在使用默认 Client-SDO；</li><li>boot-up 事件触发后旧队列已被主动取消；</li><li>NMT command 使 Master 离开 Pre-operational&#x2F;Operational，全部应用 SDO 被清理。</li></ul><p>Lely 维护者在 issue #76 中明确说明：默认 SDO client 只有在 NMT master 不使用它时才可用；通常应用应在 <code>OnBoot()</code> 后使用，或在 <code>OnConfig()</code> 的配置窗口内使用。[S5]</p><h3 id="6-3-CancelSdo-是生命周期切换的一部分"><a href="#6-3-CancelSdo-是生命周期切换的一部分" class="headerlink" title="6.3 CancelSdo() 是生命周期切换的一部分"></a>6.3 <code>CancelSdo()</code> 是生命周期切换的一部分</h3><p>源码在以下位置主动清理 SDO：</p><ul><li><code>Boot(id)</code> 开始前；</li><li>Driver 从注册表移除时；</li><li>Master 收到使本地状态离开 Pre-operational&#x2F;Operational 的 NMT command 时；</li><li>远端节点出现新的 boot-up，且当前不在配置阶段时；</li><li>配置阶段结束并把 Client-SDO 重新交还 NMT boot 流程时。</li></ul><p>这说明 <code>Sdo</code> 队列的生命周期服从 NMT 生命周期，而不是应用对象的生命周期。应用不能保存一个 <code>Sdo*</code>，然后假设它在 reset、boot-up 或重新配置后仍然有效。</p><hr><h2 id="7-OnConfig-：NMT-与-Driver-之间的配置握手"><a href="#7-OnConfig-：NMT-与-Driver-之间的配置握手" class="headerlink" title="7. OnConfig()：NMT 与 Driver 之间的配置握手"></a>7. <code>OnConfig()</code>：NMT 与 Driver 之间的配置握手</h2><h3 id="7-1-C-层把正在使用的-Client-SDO-临时交给-C-层"><a href="#7-1-C-层把正在使用的-Client-SDO-临时交给-C-层" class="headerlink" title="7.1 C 层把正在使用的 Client-SDO 临时交给 C++ 层"></a>7.1 C 层把正在使用的 Client-SDO 临时交给 C++ 层</h3><p><code>Impl_</code> 构造时通过 <code>co_nmt_set_cfg_ind()</code> 注册配置 indication。当 NMT boot slave 进入 update configuration 阶段时，C 层回调提供一个 <code>co_csdo_t*</code>。</p><p><code>Impl_::OnCfgInd()</code> 执行三件事：</p><ol><li>用该 <code>co_csdo_t*</code> 构造一个 C++ <code>Sdo</code> 包装对象并放入 <code>sdos[id]</code>；</li><li>将 <code>config[id - 1]</code> 设为 <code>true</code>；</li><li>调用 <code>self-&gt;OnConfig(id)</code>。</li></ol><p>此时 <code>GetSdo(id)</code> 会优先返回这个已存在的队列，所以 Driver 可以在 boot slave 尚未结束时提交配置 SDO。</p><h3 id="7-2-无-Driver-与有-Driver-的处理不同"><a href="#7-2-无-Driver-与有-Driver-的处理不同" class="headerlink" title="7.2 无 Driver 与有 Driver 的处理不同"></a>7.2 无 Driver 与有 Driver 的处理不同</h3><p><code>BasicMaster::OnConfig(id)</code>：</p><ul><li>没有为该 node-ID 注册 Driver：直接向 NMT 报告配置成功；</li><li>有 Driver：释放 Master 锁，调用 Driver 的 <code>OnConfig(done)</code>；</li><li>Driver 完成后调用 <code>done(ec)</code>；</li><li>完成函数重新获取 Master 锁并进入 <code>ConfigResult()</code>。</li></ul><p><code>AsyncMaster::OnConfig(id)</code> 的业务逻辑相同，但它先把 Driver 配置任务投递到 <code>driver-&gt;GetExecutor()</code>，避免在 NMT indication 调用栈内执行用户代码。[S2]</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant N as &quot;C NMT master&quot;</span><br><span class="line">    participant I as &quot;BasicMaster::Impl_&quot;</span><br><span class="line">    participant M as &quot;BasicMaster / AsyncMaster&quot;</span><br><span class="line">    participant X as &quot;Driver executor&quot;</span><br><span class="line">    participant D as &quot;Driver&quot;</span><br><span class="line"></span><br><span class="line">    N-&gt;&gt;I: &quot;OnCfgInd(id, co_csdo_t*)&quot;</span><br><span class="line">    I-&gt;&gt;I: &quot;sdos[id] = Sdo(co_csdo_t*)&quot;</span><br><span class="line">    I-&gt;&gt;I: &quot;config[id] = true&quot;</span><br><span class="line">    I-&gt;&gt;M: &quot;OnConfig(id)&quot;</span><br><span class="line"></span><br><span class="line">    alt &quot;BasicMaster&quot;</span><br><span class="line">        M-&gt;&gt;D: &quot;OnConfig(done), Master 锁已释放&quot;</span><br><span class="line">    else &quot;AsyncMaster&quot;</span><br><span class="line">        M-&gt;&gt;X: &quot;post configuration task&quot;</span><br><span class="line">        X-&gt;&gt;D: &quot;OnConfig(done)&quot;</span><br><span class="line">    end</span><br><span class="line"></span><br><span class="line">    D--&gt;&gt;M: &quot;done(error_code)&quot;</span><br><span class="line">    M-&gt;&gt;M: &quot;lock and ConfigResult(id, ec)&quot;</span><br><span class="line">    M-&gt;&gt;M: &quot;config[id] = false&quot;</span><br><span class="line">    M-&gt;&gt;M: &quot;erase temporary Sdo when booting&quot;</span><br><span class="line">    M-&gt;&gt;N: &quot;co_nmt_cfg_res(id, SDO abort code)&quot;</span><br></pre></td></tr></table></figure><h3 id="7-3-done-是继续-NMT-状态机的必要条件"><a href="#7-3-done-是继续-NMT-状态机的必要条件" class="headerlink" title="7.3 done 是继续 NMT 状态机的必要条件"></a>7.3 <code>done</code> 是继续 NMT 状态机的必要条件</h3><p>官方 API 说明，NMT boot slave 会停在 update configuration 阶段，直到应用通过 <code>ConfigResult()</code> 报告结果；Driver 层对应的就是 <code>OnConfig(done)</code> 中的完成函数。[S4]</p><p>因此 <code>OnConfig()</code> 的正确语义是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">执行设备相关配置</span><br><span class="line">    -&gt; 将所有异步结果归并为一个 error_code</span><br><span class="line">    -&gt; 调用 done(error_code)</span><br><span class="line">    -&gt; 允许 NMT boot slave 继续</span><br></pre></td></tr></table></figure><p>它不是普通通知回调。若遗漏完成函数，boot slave 会保持在配置阶段，<code>IsConfig(id)</code> 持续为真，最终也不会进入正常的 boot 完成路径。</p><h3 id="7-4-ConfigResult-完成所有权回收"><a href="#7-4-ConfigResult-完成所有权回收" class="headerlink" title="7.4 ConfigResult() 完成所有权回收"></a>7.4 <code>ConfigResult()</code> 完成所有权回收</h3><p><code>ConfigResult()</code>：</p><ul><li>清除 <code>config</code> 标志；</li><li>若节点仍处于 booting，删除 <code>sdos[id]</code> 包装对象，因为 CSDO 将重新由 NMT master 接管；</li><li>把 <code>std::error_code</code> 转换为 SDO abort code；</li><li>调用 <code>co_nmt_cfg_res()</code> 让 C 层 NMT 状态机继续运行。</li></ul><p>所以这不是“配置线程结束”这么简单，而是一次明确的资源交还：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">NMT master 持有 CSDO</span><br><span class="line">    -&gt; OnCfgInd 临时交给 Driver</span><br><span class="line">    -&gt; Driver 调用 done</span><br><span class="line">    -&gt; ConfigResult 归还 NMT master</span><br></pre></td></tr></table></figure><hr><h2 id="8-BasicMaster-与-AsyncMaster-的执行时序差异"><a href="#8-BasicMaster-与-AsyncMaster-的执行时序差异" class="headerlink" title="8. BasicMaster 与 AsyncMaster 的执行时序差异"></a>8. <code>BasicMaster</code> 与 <code>AsyncMaster</code> 的执行时序差异</h2><h3 id="8-1-BasicMaster：同步调用，但先释放锁"><a href="#8-1-BasicMaster：同步调用，但先释放锁" class="headerlink" title="8.1 BasicMaster：同步调用，但先释放锁"></a>8.1 <code>BasicMaster</code>：同步调用，但先释放锁</h3><p>以 <code>OnCanState()</code> 为例，<code>BasicMaster</code> 遍历 Driver，并用 <code>UnlockGuard</code> 暂时释放 Master 锁后调用 <code>Driver::OnCanState()</code>。单节点事件则先查注册表，再以相同方式调用对应 Driver。</p><p>这解决两个问题：</p><ul><li>Driver 回调可以再次调用需要 Master 锁的 API，不会直接自锁；</li><li>用户代码的执行时间不会把 Master 锁长期占住。</li></ul><p>但回调仍发生在当前协议事件处理调用链中。若 Driver 执行阻塞操作，当前事件循环仍可能被拖延。</p><h3 id="8-2-AsyncMaster：只负责投递，Driver-稍后执行"><a href="#8-2-AsyncMaster：只负责投递，Driver-稍后执行" class="headerlink" title="8.2 AsyncMaster：只负责投递，Driver 稍后执行"></a>8.2 <code>AsyncMaster</code>：只负责投递，Driver 稍后执行</h3><p><code>AsyncMaster</code> 的覆盖函数通常执行：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">找到 Driver</span><br><span class="line">    -&gt; 复制回调参数</span><br><span class="line">    -&gt; driver-&gt;GetExecutor().post(task)</span><br><span class="line">    -&gt; 当前 Master 事件入口返回</span><br></pre></td></tr></table></figure><p>例如：</p><ul><li>CAN state&#x2F;error、NMT command、SYNC&#x2F;TIME 会给每个 Driver 投递 task；</li><li>heartbeat、state、EMCY、boot 等只投递给对应 node-ID；</li><li><code>OnConfig()</code> 也在 Driver executor 上运行。</li></ul><p><code>OnEmcy()</code> 还有一个值得注意的细节：源码先把 5 字节 manufacturer-specific error field 复制到 <code>std::array</code>，再捕获进异步 task。原因是原始 <code>uint8_t msef[5]</code> 指针的生命周期只覆盖当前回调，不能直接跨越异步投递边界。[S2]</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant Stack as &quot;CANopen stack&quot;</span><br><span class="line">    participant M as &quot;AsyncMaster::OnEmcy&quot;</span><br><span class="line">    participant E as &quot;Driver executor&quot;</span><br><span class="line">    participant D as &quot;Driver::OnEmcy&quot;</span><br><span class="line"></span><br><span class="line">    Stack-&gt;&gt;M: &quot;OnEmcy(id, eec, er, msef*)&quot;</span><br><span class="line">    M-&gt;&gt;M: &quot;copy msef into owned array&quot;</span><br><span class="line">    M-&gt;&gt;E: &quot;post(lambda with copied values)&quot;</span><br><span class="line">    M--&gt;&gt;Stack: &quot;return&quot;</span><br><span class="line">    E-&gt;&gt;D: &quot;invoke OnEmcy later&quot;</span><br></pre></td></tr></table></figure><h3 id="8-3-异步不等于并行，也不自动保证全局顺序"><a href="#8-3-异步不等于并行，也不自动保证全局顺序" class="headerlink" title="8.3 异步不等于并行，也不自动保证全局顺序"></a>8.3 异步不等于并行，也不自动保证全局顺序</h3><p>从这两个文件能确认的是“任务被投递到各 Driver 的 executor”。以下行为取决于应用选择的 executor、strand、fiber 或独立线程模型：</p><ul><li>同一 Driver 内多个任务是否串行；</li><li>不同 Driver 回调是否并行；</li><li>回调相对其他应用 task 的排队顺序；</li><li>长耗时回调是否阻塞共享 event loop。</li></ul><p>因此，<code>AsyncMaster</code> 保证的是调用栈解耦，不应被直接解释为“每个节点都有独立线程”。官方教程进一步提供 <code>FiberDriver</code> 和 <code>LoopDriver</code> 等不同执行模型；它们的阻塞语义和死锁风险并不相同。[S3]</p><hr><h2 id="9-NMT-command、远端状态与-SDO-清理如何联动"><a href="#9-NMT-command、远端状态与-SDO-清理如何联动" class="headerlink" title="9. NMT command、远端状态与 SDO 清理如何联动"></a>9. NMT command、远端状态与 SDO 清理如何联动</h2><h3 id="9-1-Command-只是发起-NMT-command"><a href="#9-1-Command-只是发起-NMT-command" class="headerlink" title="9.1 Command() 只是发起 NMT command"></a>9.1 <code>Command()</code> 只是发起 NMT command</h3><p><code>BasicMaster::Command(cs, id)</code> 获取锁后调用 C 层 <code>co_nmt_cs_req()</code>。node-ID 为 0 时可表示广播，具体帧和状态行为由 NMT 服务执行。[S2]</p><h3 id="9-2-OnCommand-处理的是-Master-自身状态迁移"><a href="#9-2-OnCommand-处理的是-Master-自身状态迁移" class="headerlink" title="9.2 OnCommand() 处理的是 Master 自身状态迁移"></a>9.2 <code>OnCommand()</code> 处理的是 Master 自身状态迁移</h3><p>当 Master 本地 NMT 收到 command 时，<code>OnCommand(cs)</code> 会检查即将进入的状态：</p><ul><li>若不是 Pre-operational 或 Operational，取消全部应用 SDO；</li><li>再将 command 事件通知各 Driver。</li></ul><p>这与 <code>GetSdo()</code> 的可用条件一致：离开可提供 Client-SDO 的 NMT 状态后，旧请求队列必须失效。</p><h3 id="9-3-新-boot-up-事件会使旧-ready-与-SDO-状态失效"><a href="#9-3-新-boot-up-事件会使旧-ready-与-SDO-状态失效" class="headerlink" title="9.3 新 boot-up 事件会使旧 ready 与 SDO 状态失效"></a>9.3 新 boot-up 事件会使旧 ready 与 SDO 状态失效</h3><p>远端节点发送 boot-up 后，<code>OnState(id, BOOTUP)</code> 在非配置阶段执行：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">ready[id] = false</span><br><span class="line">CancelSdo(id)</span><br></pre></td></tr></table></figure><p>因为新的 boot-up 表示远端通信状态已重置，之前的“已完成 boot”结论以及应用 SDO 会话都不能继续沿用；NMT master 也可能马上重新进入 boot slave 流程并接管 Client-SDO。[S2]</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">stateDiagram-v2</span><br><span class="line">    [*] --&gt; NotReady</span><br><span class="line">    NotReady --&gt; Booting: &quot;Boot(id) 或检测到 boot-up&quot;</span><br><span class="line">    Booting --&gt; Configuring: &quot;update configuration indication&quot;</span><br><span class="line">    Configuring --&gt; Booting: &quot;done(ec) / ConfigResult&quot;</span><br><span class="line">    Booting --&gt; Ready: &quot;boot indication success 或 L&quot;</span><br><span class="line">    Booting --&gt; NotReady: &quot;boot failure&quot;</span><br><span class="line">    Ready --&gt; NotReady: &quot;新的 boot-up&quot;</span><br><span class="line">    Ready --&gt; NotReady: &quot;AsyncDeconfig&quot;</span><br></pre></td></tr></table></figure><p>这张图描述的是 <code>BasicMaster</code> 暴露的应用状态，不是 CiA 302 内部 boot state machine 的完整替代。</p><hr><h2 id="10-AsyncDeconfig-：与-boot-相反的-Driver-生命周期入口"><a href="#10-AsyncDeconfig-：与-boot-相反的-Driver-生命周期入口" class="headerlink" title="10. AsyncDeconfig()：与 boot 相反的 Driver 生命周期入口"></a>10. <code>AsyncDeconfig()</code>：与 boot 相反的 Driver 生命周期入口</h2><p><code>AsyncDeconfig(id)</code> 查找对应 Driver，随后由 <code>Impl_::AsyncDeconfig()</code>：</p><ol><li>先把该节点标记为 not ready；</li><li>创建 promise&#x2F;future；</li><li>向 Driver executor 投递 <code>OnDeconfig(done)</code>；</li><li>Driver 完成后设置 promise；</li><li>调用者通过 future 获得完成或错误结果。</li></ol><p>无参数版本会为全部 Driver 创建 future，再通过 <code>when_all</code> 聚合。[S2]</p><p>这一接口主要表达应用层设备资源的解除过程。它不会自动等同于 NMT stop、reset communication 或物理掉线；具体设备资源、业务状态和 I&#x2F;O 停止动作由 Driver 的 <code>OnDeconfig()</code> 实现。</p><hr><h2 id="11-tpdo-event-mutex-为什么在-Master-中重新包装"><a href="#11-tpdo-event-mutex-为什么在-Master-中重新包装" class="headerlink" title="11. tpdo_event_mutex 为什么在 Master 中重新包装"></a>11. <code>tpdo_event_mutex</code> 为什么在 Master 中重新包装</h2><p><code>Node::TpdoEventMutex</code> 用于延迟事件驱动或异步 TPDO 的发送，使应用可以批量修改多个映射值后再统一触发。</p><p><code>BasicMaster::TpdoEventMutex::lock()</code> 和 <code>unlock()</code> 在调用基类实现前，先获取 Master 锁。这样能维持两层同步关系：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Master 状态与对象访问同步</span><br><span class="line">    +</span><br><span class="line">Node 的 TPDO event 延迟机制</span><br></pre></td></tr></table></figure><p>典型意图是把多个 <code>TpdoMapped()</code> 写入放入同一个临界区，避免更新到一半时触发 TPDO：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    <span class="function">std::lock_guard&lt;lely::canopen::BasicMaster::TpdoEventMutex&gt; <span class="title">guard</span><span class="params">(</span></span></span><br><span class="line"><span class="params"><span class="function">        master.tpdo_event_mutex)</span></span>;</span><br><span class="line"></span><br><span class="line">    master.<span class="built_in">TpdoMapped</span>(<span class="number">2</span>)[<span class="number">0x6040</span>][<span class="number">0</span>] = <span class="type">uint16_t</span>&#123;<span class="number">0x0006</span>&#125;;</span><br><span class="line">    master.<span class="built_in">TpdoMapped</span>(<span class="number">2</span>)[<span class="number">0x607A</span>][<span class="number">0</span>] = <span class="type">int32_t</span>&#123;<span class="number">100000</span>&#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>是否立即产生总线帧仍取决于 TPDO transmission type、event timer、SYNC 和 mapping 配置；mutex 只负责推迟事件触发，不改变 PDO 通信参数。[S1][S2]</p><hr><h2 id="12-把完整流程串起来"><a href="#12-把完整流程串起来" class="headerlink" title="12. 把完整流程串起来"></a>12. 把完整流程串起来</h2><p>下面以一个 <code>AsyncMaster + Driver</code> 管理 node 2 的典型启动过程为例：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant App as &quot;Application&quot;</span><br><span class="line">    participant Loop as &quot;event loop&quot;</span><br><span class="line">    participant M as &quot;AsyncMaster&quot;</span><br><span class="line">    participant N as &quot;C NMT master&quot;</span><br><span class="line">    participant S as &quot;Slave node 2&quot;</span><br><span class="line">    participant E as &quot;Driver executor&quot;</span><br><span class="line">    participant D as &quot;Driver node 2&quot;</span><br><span class="line"></span><br><span class="line">    App-&gt;&gt;M: &quot;construct with master DCF&quot;</span><br><span class="line">    App-&gt;&gt;D: &quot;register Driver for node 2&quot;</span><br><span class="line">    App-&gt;&gt;M: &quot;Reset()&quot;</span><br><span class="line">    M-&gt;&gt;N: &quot;start local NMT service&quot;</span><br><span class="line">    N-&gt;&gt;S: &quot;reset communication / boot checks&quot;</span><br><span class="line">    S--&gt;&gt;N: &quot;boot-up and NMT responses&quot;</span><br><span class="line"></span><br><span class="line">    N-&gt;&gt;M: &quot;OnState(node 2, BOOTUP)&quot;</span><br><span class="line">    M-&gt;&gt;M: &quot;ready=false, CancelSdo(2)&quot;</span><br><span class="line">    M-&gt;&gt;E: &quot;post Driver::OnState&quot;</span><br><span class="line"></span><br><span class="line">    N-&gt;&gt;M: &quot;OnCfgInd(node 2, CSDO)&quot;</span><br><span class="line">    M-&gt;&gt;M: &quot;wrap CSDO, config=true&quot;</span><br><span class="line">    M-&gt;&gt;E: &quot;post Driver::OnConfig&quot;</span><br><span class="line">    E-&gt;&gt;D: &quot;OnConfig(done)&quot;</span><br><span class="line">    D-&gt;&gt;M: &quot;AsyncWrite / AsyncRead&quot;</span><br><span class="line">    M-&gt;&gt;M: &quot;GetSdo returns config CSDO&quot;</span><br><span class="line">    M-&gt;&gt;N: &quot;submit SDO through handed-off queue&quot;</span><br><span class="line">    N-&gt;&gt;S: &quot;SDO requests&quot;</span><br><span class="line">    S--&gt;&gt;N: &quot;SDO responses&quot;</span><br><span class="line">    D--&gt;&gt;M: &quot;done(0)&quot;</span><br><span class="line">    M-&gt;&gt;N: &quot;co_nmt_cfg_res(success)&quot;</span><br><span class="line"></span><br><span class="line">    N-&gt;&gt;S: &quot;remaining boot steps / NMT start&quot;</span><br><span class="line">    N-&gt;&gt;M: &quot;OnBoot(node 2, state, status)&quot;</span><br><span class="line">    M-&gt;&gt;M: &quot;ready=true on accepted result&quot;</span><br><span class="line">    M-&gt;&gt;E: &quot;post Driver::OnBoot&quot;</span><br><span class="line">    E-&gt;&gt;D: &quot;OnBoot&quot;</span><br><span class="line"></span><br><span class="line">    S--&gt;&gt;M: &quot;TPDO / heartbeat / EMCY&quot;</span><br><span class="line">    M-&gt;&gt;E: &quot;post per-node event&quot;</span><br><span class="line">    E-&gt;&gt;D: &quot;OnRpdoWrite / OnHeartbeat / OnEmcy&quot;</span><br></pre></td></tr></table></figure><p>从这个流程可以得到三个工程结论：</p><ol><li><code>OnState(BOOTUP)</code> 不是执行普通应用 SDO 的稳定窗口，因为 NMT boot 可能已经准备接管默认 Client-SDO。</li><li><code>OnConfig()</code> 是 boot 期间由 NMT 明确授权给 Driver 的 SDO 窗口，必须以完成回调结束。</li><li><code>OnBoot()</code> 表示 boot slave 流程完成，但对依赖 PDO 的业务逻辑，仍应结合远端 NMT state 判断设备是否真正进入可运行阶段。</li></ol><hr><h2 id="13-条件编译决定哪些路径真实存在"><a href="#13-条件编译决定哪些路径真实存在" class="headerlink" title="13. 条件编译决定哪些路径真实存在"></a>13. 条件编译决定哪些路径真实存在</h2><p><code>master.hpp/master.cpp</code> 中的重要能力受编译配置控制：</p><table><thead><tr><th>条件</th><th>影响</th></tr></thead><tbody><tr><td><code>LELY_NO_COAPP_MASTER</code></td><td>整个 C++ Master 实现不编译</td></tr><tr><td><code>LELY_NO_CO_DCF</code></td><td>文本 DCF 构造路径不可用</td></tr><tr><td><code>LELY_NO_CO_SDEV</code></td><td>静态设备描述构造路径不可用</td></tr><tr><td><code>LELY_NO_CO_NMT_BOOT</code></td><td><code>Boot()</code> 与 boot indication 相关路径不可用</td></tr><tr><td><code>LELY_NO_CO_NMT_CFG</code></td><td>update configuration、<code>config[]</code> 和配置 CSDO 交接不可用</td></tr><tr><td><code>LELY_NO_CO_NG</code></td><td>node guarding indication 不注册</td></tr></tbody></table><p>因此，对具体构建产物分析时，不能只看头文件里是否存在某个 API，还要确认目标库的 feature macros。本文描述的是这些功能启用时的主流程。[S1][S2]</p><hr><h2 id="14-如何理解-BasicMaster、AsyncMaster-与-Driver-的边界"><a href="#14-如何理解-BasicMaster、AsyncMaster-与-Driver-的边界" class="headerlink" title="14. 如何理解 BasicMaster、AsyncMaster 与 Driver 的边界"></a>14. 如何理解 <code>BasicMaster</code>、<code>AsyncMaster</code> 与 Driver 的边界</h2><p>可以把三者压缩成下面的职责模型：</p><table><thead><tr><th>对象</th><th>负责什么</th><th>不负责什么</th></tr></thead><tbody><tr><td><code>Node</code></td><td>本地 CANopen 节点、对象字典与协议服务</td><td>不表达多个远端设备的业务差异</td></tr><tr><td><code>BasicMaster</code></td><td>NMT 主站编排、Driver 注册、ready&#x2F;config、SDO 仲裁、同步事件分发</td><td>不为每种从站实现设备业务逻辑</td></tr><tr><td><code>AsyncMaster</code></td><td>在 <code>BasicMaster</code> 基础上把 Driver 事件投递到 executor</td><td>不自动创建线程，也不改变 CANopen 协议语义</td></tr><tr><td><code>DriverBase</code>&#x2F;派生 Driver</td><td>单个远端节点的配置、状态处理和业务行为</td><td>不管理全网 NMT 主状态机</td></tr><tr><td><code>Sdo</code></td><td>一条 Client-SDO 队列及传输请求</td><td>不决定自己何时可与 NMT boot 并发使用</td></tr></tbody></table><p>最终可以用一句话概括 <code>coapp::Master</code>：</p><blockquote><p>它是本地 CANopen Node 与远端设备 Driver 之间的协调层，以 NMT 生命周期为主轴，对 Client-SDO 所有权、节点 ready&#x2F;config 状态和协议事件执行上下文进行统一管理。</p></blockquote><hr><h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ul><li>[S1] <a href="https://lely_industries.gitlab.io/lely-core/doxygen/master_8hpp_source.html">Lely core development Doxygen: <code>master.hpp</code> source</a></li><li>[S2] <a href="https://lely_industries.gitlab.io/lely-core/doxygen/master_8cpp_source.html">Lely core development Doxygen: <code>master.cpp</code> source</a></li><li>[S3] <a href="https://opensource.lely.com/canopen/docs/cpp-tutorial/">Lely CANopen C++ tutorial</a></li><li>[S4] <a href="https://lely_industries.gitlab.io/lely-core/doxygen/classlely_1_1canopen_1_1BasicMaster.html">Lely Doxygen: <code>lely::canopen::BasicMaster</code> class reference</a></li><li>[S5] <a href="https://gitlab.com/lely_industries/lely-core/-/issues/76">Lely issue #76: SDO availability during early initialization and boot</a></li><li>[S6] <a href="https://gitlab.com/lely_industries/lely-core/-/issues/92">Lely issue #92: relationship between <code>OnBoot()</code>, Operational state and PDO events</a></li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;meta name=&quot;referrer&quot; content=&quot;no-referrer&quot; /&gt;

&lt;h1 id=&quot;Lely-CANopen-coapp-Master：NMT-总控、SDO-仲裁与-Driver-事件分发机制&quot;&gt;&lt;a href=&quot;#Lely-CANopen-coapp</summary>
      
    
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/categories/canopen/"/>
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/tags/canopen/"/>
    
  </entry>
  
  <entry>
    <title>CANopen MPDO：动态对象寻址</title>
    <link href="https://wdfk-prog.space/posts/6ca4effc/"/>
    <id>https://wdfk-prog.space/posts/6ca4effc/</id>
    <published>2026-08-03T00:55:33.000Z</published>
    <updated>2026-08-03T05:32:26.334Z</updated>
    
    <content type="html"><![CDATA[<meta name="referrer" content="no-referrer" /><h1 id="CANopen-MPDO：动态对象寻址"><a href="#CANopen-MPDO：动态对象寻址" class="headerlink" title="CANopen MPDO：动态对象寻址"></a>CANopen MPDO：动态对象寻址</h1><blockquote><p>摘要：解析 MPDO 的 DAM&#x2F;SAM 寻址、对象列表与报文流程，并结合 Lely、CANopenNode 和 CanFestival 源码说明实现差异及替代方案。</p></blockquote><p><img src="https://i-blog.csdnimg.cn/direct/5cb22a46d181462f911fdc438c717bf0.png" alt="在这里插入图片描述"></p><p>@[toc]</p><h2 id="1-MPDO-解决了普通-PDO-的什么问题"><a href="#1-MPDO-解决了普通-PDO-的什么问题" class="headerlink" title="1. MPDO 解决了普通 PDO 的什么问题"></a>1. MPDO 解决了普通 PDO 的什么问题</h2><p>普通 PDO 的优势是协议开销小、实时性高，但它的对象映射通常在通信前确定：一个 PDO COB-ID 对应一组固定的对象字典条目。收到数据帧后，消费者按照 <code>0x1600～0x17FF</code> 中的映射，把第 1 段、第 2 段数据写到预先配置的本地对象。</p><p>当设备存在大量低频、稀疏变化的 8&#x2F;16&#x2F;32 位对象时，固定映射会遇到三个问题：</p><ol><li>为每组对象分配 PDO，会增加通信参数、映射对象和 COB-ID 管理量。</li><li>反复动态重映射 PDO，需要先禁用 PDO、清空映射、修改条目、恢复映射，再重新启用，不能把它当成逐帧选择机制。</li><li>改用 SDO 虽然可以逐次携带 Index&#x2F;Sub-index，但每次访问都有请求、响应和协议状态，语义更可靠，实时性和总线效率却不同于 PDO。</li></ol><p>MPDO（Multiplexed Process Data Object）在 PDO 数据区中携带 Node-ID、Index 和 Sub-index，使同一个 PDO COB-ID 能够按帧选择不同对象。它可以近似理解为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">PDO 的单帧、无确认、事件驱动传输</span><br><span class="line">+</span><br><span class="line">SDO 风格的对象字典地址</span><br></pre></td></tr></table></figure><p>但这只是帮助理解的类比。MPDO 仍属于 PDO 服务：它使用 PDO 通信参数、遵循生产者&#x2F;消费者模型，并通过 Scanner List 或 Dispatching List 管理对象路由；它没有 SDO response，也不会返回 SDO abort 帧。</p><h2 id="2-一帧-MPDO-如何携带对象地址"><a href="#2-一帧-MPDO-如何携带对象地址" class="headerlink" title="2. 一帧 MPDO 如何携带对象地址"></a>2. 一帧 MPDO 如何携带对象地址</h2><p>经典 CANopen MPDO 使用 8 字节数据帧，最多携带 4 字节过程数据。</p><table><thead><tr><th>字节</th><th>字段</th><th>含义</th></tr></thead><tbody><tr><td>Byte 0</td><td><code>f + addr</code></td><td>bit7 为寻址模式；bit0～6 为 Node-ID</td></tr><tr><td>Byte 1</td><td>Index 低字节</td><td>对象字典 Index 的低 8 位</td></tr><tr><td>Byte 2</td><td>Index 高字节</td><td>对象字典 Index 的高 8 位</td></tr><tr><td>Byte 3</td><td>Sub-index</td><td>对象字典子索引</td></tr><tr><td>Byte 4～7</td><td>Data</td><td>最多 32 bit，按 CANopen 小端编码</td></tr></tbody></table><p>Byte 0 的定义为：</p><ul><li><code>f = 0</code>：SAM，<code>addr</code> 表示生产者 Node-ID；</li><li><code>f = 1</code>：DAM，<code>addr</code> 表示消费者 Node-ID；</li><li>DAM 中 <code>addr = 0</code> 表示面向所有配置为该 DAM-MPDO 消费者的节点；</li><li>SAM 中 <code>addr = 0</code> 保留；</li><li><code>addr = 1～127</code> 表示具体 CANopen Node-ID。</li></ul><p>例如，DAM-MPDO 向节点 1 的 <code>0x2200:00</code> 写入 <code>0x12345678</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">81 00 22 00 78 56 34 12</span><br><span class="line">│  │─────│  │──────────│</span><br><span class="line">│  │      │       └─ 数据 0x12345678</span><br><span class="line">│  │      └───────── Sub-index 0x00</span><br><span class="line">│  └──────────────── Index 0x2200，小端</span><br><span class="line">└─────────────────── DAM 标志 0x80 + Node-ID 1</span><br></pre></td></tr></table></figure><p>SAM-MPDO 由节点 5 发布其 <code>0x2100:00 = 100</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">05 00 21 00 64 00 00 00</span><br></pre></td></tr></table></figure><p>此时 <code>0x05</code> 不是目标地址，而是“这份数据来自 Node-ID 5”。消费者随后使用 Object Dispatching List 判断该远端对象应写入哪个本地对象。</p><p>MPDO 的代价也在报文中直接体现：8 字节中有 4 字节用于动态寻址，真正的数据只剩 4 字节。因此它适合短、稀疏、无需逐帧确认的数据，不适合大于 32 位的参数或文件数据。</p><h2 id="3-DAM-与-SAM：一个指定目的，一个声明来源"><a href="#3-DAM-与-SAM：一个指定目的，一个声明来源" class="headerlink" title="3. DAM 与 SAM：一个指定目的，一个声明来源"></a>3. DAM 与 SAM：一个指定目的，一个声明来源</h2><h3 id="3-1-DAM：帧中直接指定接收方对象"><a href="#3-1-DAM：帧中直接指定接收方对象" class="headerlink" title="3.1 DAM：帧中直接指定接收方对象"></a>3.1 DAM：帧中直接指定接收方对象</h3><p>DAM（Destination Address Mode）中的 Node-ID、Index 和 Sub-index描述接收方对象字典。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    appA[&quot;生产者应用&quot;] --&gt; damBuild[&quot;组装 DAM-MPDO&lt;br/&gt;目标 Node-ID + 本地 Index/Sub-index + Value&quot;]</span><br><span class="line">    damBuild --&gt; canBus[&quot;同一个 MPDO COB-ID&quot;]</span><br><span class="line">    canBus --&gt; node1[&quot;节点 1：地址匹配&quot;]</span><br><span class="line">    canBus --&gt; node2[&quot;节点 2：地址不匹配&quot;]</span><br><span class="line">    node1 --&gt; localOd[&quot;检查本地对象、长度和写权限&quot;]</span><br><span class="line">    localOd --&gt; writeOk[&quot;写入目标对象&quot;]</span><br><span class="line">    localOd --&gt; writeErr[&quot;目标不可用：错误处理/EMCY&quot;]</span><br><span class="line">    node2 --&gt; discard[&quot;忽略该帧&quot;]</span><br><span class="line"></span><br><span class="line">    appB[&quot;SAM 生产者对象事件&quot;] --&gt; scanner[&quot;Object Scanner List&lt;br/&gt;0x1FA0～0x1FCF&quot;]</span><br><span class="line">    scanner --&gt; samBuild[&quot;组装 SAM-MPDO&lt;br/&gt;来源 Node-ID + 来源 Index/Sub-index + Value&quot;]</span><br><span class="line">    samBuild --&gt; canBus2[&quot;同一个 MPDO COB-ID&quot;]</span><br><span class="line">    canBus2 --&gt; dispatch[&quot;消费者 Object Dispatching List&lt;br/&gt;0x1FD0～0x1FFF&quot;]</span><br><span class="line">    dispatch --&gt; localMap[&quot;来源对象映射到本地 Index/Sub-index&quot;]</span><br><span class="line">    localMap --&gt; localOd2[&quot;写入本地对象&quot;]</span><br></pre></td></tr></table></figure><p>DAM 的语义接近一次“无应答的对象字典下载”：</p><ul><li>生产者决定目标 Node-ID 和目标对象；</li><li>消费者不需要预先为每个远端对象建立普通 PDO 映射；</li><li>没有成功响应帧；</li><li>目标对象不存在、不可写或长度不兼容时，不能像 SDO 那样返回 abort response；</li><li>按用户提供的 CiA 301 材料，目标对象不可用对应 EMCY error code <code>0x8230</code>。</li></ul><p><code>addr = 0</code> 是 DAM 广播语义。它不是“任意分组号”：哪些节点真正处理该帧，仍取决于这些节点是否配置为相应 COB-ID 的 DAM-MPDO 消费者。</p><h3 id="3-2-SAM：帧中声明发送方对象"><a href="#3-2-SAM：帧中声明发送方对象" class="headerlink" title="3.2 SAM：帧中声明发送方对象"></a>3.2 SAM：帧中声明发送方对象</h3><p>SAM（Source Address Mode）中的 Node-ID、Index 和 Sub-index描述生产者对象字典。按用户提供的 CiA 301 材料，每个 CANopen 设备只允许一个这种 SAM-MPDO 生产者。消费者不是把数据写到帧内指定的本地 Index，而是查询 Dispatching List：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">远端 Node-ID + 远端 Index + 远端 Sub-index</span><br><span class="line">                         ↓</span><br><span class="line">             Object Dispatching List</span><br><span class="line">                         ↓</span><br><span class="line">本地 Index + 本地 Sub-index</span><br></pre></td></tr></table></figure><p>SAM 需要两类对象字典列表：</p><table><thead><tr><th>角色</th><th align="right">对象范围</th><th align="right">数据类型</th><th>作用</th></tr></thead><tbody><tr><td>SAM 生产者</td><td align="right"><code>0x1FA0～0x1FCF</code></td><td align="right">ARRAY of UNSIGNED32</td><td>Object Scanner List，决定哪些本地对象可被扫描并发送</td></tr><tr><td>SAM 消费者</td><td align="right"><code>0x1FD0～0x1FFF</code></td><td align="right">ARRAY of UNSIGNED64</td><td>Object Dispatching List，把远端来源对象映射到本地目标对象</td></tr></tbody></table><p>Scanner List 条目包含块大小、Index 和 Sub-index，可描述连续子索引。Dispatching List 条目同时包含远端 Node-ID、远端 Index&#x2F;Sub-index、本地 Index&#x2F;Sub-index 和块大小，用于建立跨节点对象引用。</p><p>这与普通 PDO mapping 不同：普通 PDO mapping 描述“数据帧中第几位对应哪个本地对象”；SAM 的两个列表描述“哪个来源对象允许发送，以及它在消费者上应该落到哪里”。</p><h3 id="3-3-MPDO-通过-PDO-mapping-的特殊值启用"><a href="#3-3-MPDO-通过-PDO-mapping-的特殊值启用" class="headerlink" title="3.3 MPDO 通过 PDO mapping 的特殊值启用"></a>3.3 MPDO 通过 PDO mapping 的特殊值启用</h3><p>在 RPDO&#x2F;TPDO mapping parameter 的子索引 0 中，普通 PDO 使用 <code>0x00～0x40</code> 表示有效映射条目数。MPDO 使用特殊值：</p><table><thead><tr><th align="right">子索引 0</th><th>含义</th></tr></thead><tbody><tr><td align="right"><code>0x00</code></td><td>映射禁用</td></tr><tr><td align="right"><code>0x01～0x40</code></td><td>普通 PDO，有效固定映射条目数</td></tr><tr><td align="right"><code>0xFE</code></td><td>SAM-MPDO</td></tr><tr><td align="right"><code>0xFF</code></td><td>DAM-MPDO</td></tr></tbody></table><p>因此，“在 EDS 中加入 <code>0x1FA0～0x1FFF</code>”并不足以实现 MPDO。协议栈还必须在 RPDO&#x2F;TPDO 收发路径中识别 <code>0xFE/0xFF</code>，解析 8 字节 MPDO 头，执行 Scanner&#x2F;Dispatching List 查找，并接入对象字典读写和错误处理。</p><h2 id="4-MPDO、普通-PDO-与-SDO-的边界"><a href="#4-MPDO、普通-PDO-与-SDO-的边界" class="headerlink" title="4. MPDO、普通 PDO 与 SDO 的边界"></a>4. MPDO、普通 PDO 与 SDO 的边界</h2><table><thead><tr><th>维度</th><th>普通 PDO</th><th>MPDO</th><th>SDO</th></tr></thead><tbody><tr><td>对象选择</td><td>通信前固定映射</td><td>每帧携带对象地址</td><td>每次请求携带对象地址</td></tr><tr><td>单帧有效数据</td><td>经典 CAN 最多 8 字节</td><td>最多 4 字节</td><td>expedited 最多 4 字节；可分段&#x2F;块传输任意长度</td></tr><tr><td>成功确认</td><td>无</td><td>无</td><td>有响应和 abort code</td></tr><tr><td>触发方式</td><td>同步、事件、定时器、部分实现支持 RTR</td><td>CiA 301 材料限定为事件驱动</td><td>客户端请求驱动</td></tr><tr><td>配置复杂度</td><td>映射和 COB-ID 管理</td><td>Scanner&#x2F;Dispatching List 与动态路由</td><td>SDO client&#x2F;server 参数和状态机</td></tr><tr><td>典型用途</td><td>高频固定过程数据</td><td>大量稀疏变化的短对象</td><td>参数配置、诊断、大数据和可靠访问</td></tr><tr><td>失败反馈</td><td>通常通过 EMCY&#x2F;应用诊断</td><td>无逐帧响应，必要时 EMCY</td><td>SDO abort response</td></tr></tbody></table><p>工程上可以使用以下判断：</p><ol><li>字段固定、频率高、对时延敏感：普通 PDO。</li><li>数据超过 32 位，或调用方必须知道成功&#x2F;失败：SDO。</li><li>对象很多、变化稀疏、每项不超过 32 位、允许无确认，并且双方协议栈均支持标准 MPDO：MPDO。</li></ol><p>MPDO 不是“更多 PDO”的通用替代品。如果对象数量不多，扩展普通 PDO 往往更简单；如果数据必须可靠落盘，MPDO 又不如 SDO 合适。</p><h2 id="5-Lely-如何表示和配置-MPDO"><a href="#5-Lely-如何表示和配置-MPDO" class="headerlink" title="5. Lely 如何表示和配置 MPDO"></a>5. Lely 如何表示和配置 MPDO</h2><p>Lely 官方的 standards support 页面把 MPDO、<code>0x1FA0～0x1FCF</code> 和 <code>0x1FD0～0x1FFF</code> 列为已实现能力；构建时可以通过 <code>--disable-mpdo</code> 或 <code>LELY_NO_CO_MPDO</code> 去除相关代码。</p><p>Lely 没有另建一套完全独立于 PDO 的服务对象。MPDO 仍由：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">co_tpdo_t：生产路径</span><br><span class="line">co_rpdo_t：消费路径</span><br><span class="line">co_dev_t：对象字典和列表查询</span><br><span class="line">co_nmt_t：对象事件到 TPDO 服务的桥接</span><br></pre></td></tr></table></figure><p>协同完成。关键公开符号包括：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">CO_PDO_MAP_SAM_MPDO</span><br><span class="line">CO_PDO_MAP_DAM_MPDO</span><br><span class="line">co_dam_mpdo_event()</span><br><span class="line">co_sam_mpdo_event()</span><br><span class="line">co_dev_sam_mpdo_event()</span><br><span class="line">co_dev_chk_sam_mpdo()</span><br><span class="line">co_dev_map_sam_mpdo()</span><br><span class="line">co_sam_mpdo_up()</span><br></pre></td></tr></table></figure><p>Lely 的 <code>pdo.c</code> 还揭示了一个重要设计：普通 PDO 和 MPDO 最终都通过“本地 SDO request”访问对象字典。</p><ul><li><code>co_pdo_dn()</code>：通过本地 SDO download request 把普通 RPDO 数据写入对象字典；</li><li><code>co_pdo_up()</code>：通过本地 SDO upload request 从对象字典构造普通 TPDO；</li><li><code>co_sam_mpdo_up()</code>：通过本地 SDO upload request 读取一个 SAM-MPDO 对象，输出固定 4 字节缓冲区。</li></ul><p>这里的“本地 SDO request”是内部对象访问抽象，不会在 CAN 总线上产生 SDO 报文。它的价值是复用对象存在性、访问权限、数据类型和自定义 indication 逻辑。</p><h3 id="5-1-dcfgen-的配置边界"><a href="#5-1-dcfgen-的配置边界" class="headerlink" title="5.1 dcfgen 的配置边界"></a>5.1 <code>dcfgen</code> 的配置边界</h3><p>Lely v2.3.0 发布说明明确写明：当时 <code>dcfgen</code> 尚不能自动创建 Scanner List 和 Dispatching List。当前公开的 dcf-tools 文档列出了普通 <code>rpdo</code>、<code>tpdo</code> 和 <code>sdo</code> 配置，但没有给出 MPDO 列表的 YAML 生成语法。</p><p>据此可以确认的是：</p><ul><li>v2.3.0 时需要手工提供这两类对象；</li><li>截至本文查阅的公开 dcf-tools 文档，没有找到“已新增 MPDO 列表自动生成”的明确说明。</li></ul><p>这不等价于证明所有当前工具版本都绝对不支持。项目落地时应检查最终生成的 DCF 是否真实包含 <code>0x1FA0～0x1FFF</code>，而不是仅依据 YAML 配置名称推断。</p><h2 id="6-Lely-的-MPDO-发送运行流程"><a href="#6-Lely-的-MPDO-发送运行流程" class="headerlink" title="6. Lely 的 MPDO 发送运行流程"></a>6. Lely 的 MPDO 发送运行流程</h2><h3 id="6-1-DAM-MPDO：应用显式给出目标对象"><a href="#6-1-DAM-MPDO：应用显式给出目标对象" class="headerlink" title="6.1 DAM-MPDO：应用显式给出目标对象"></a>6.1 DAM-MPDO：应用显式给出目标对象</h3><p>DAM 没有 Scanner List 路由，因为目标地址由本次调用直接提供。应用通过 <code>co_dam_mpdo_event()</code>，或 C++ coapp 层的 <code>DamMpdoEvent()</code>，传入目标 Node-ID、Index、Sub-index 和数据。</p><p>核心流程可整理为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">application</span><br><span class="line">    ↓ co_dam_mpdo_event()</span><br><span class="line">检查 TPDO 通信参数和 mapping mode = 0xFF</span><br><span class="line">    ↓</span><br><span class="line">检查事件触发条件和 inhibit time</span><br><span class="line">    ↓</span><br><span class="line">Byte0 = 0x80 | destination Node-ID</span><br><span class="line">Byte1..3 = destination Index/Sub-index</span><br><span class="line">Byte4..7 = data</span><br><span class="line">    ↓</span><br><span class="line">co_tpdo_send_frame() / can_net_send()</span><br></pre></td></tr></table></figure><p>DAM API 返回值能够说明本地参数检查、组帧或发送调用是否成功，但不能证明远端对象已经写入。协议没有对应确认帧；需要可靠闭环时，应用仍应使用 SDO 回读、远端状态 PDO 或应用级确认机制。</p><h3 id="6-2-SAM-MPDO：对象事件驱动-Scanner-List"><a href="#6-2-SAM-MPDO：对象事件驱动-Scanner-List" class="headerlink" title="6.2 SAM-MPDO：对象事件驱动 Scanner List"></a>6.2 SAM-MPDO：对象事件驱动 Scanner List</h3><p>SAM 的入口不是由应用每次手工指定目标节点。应用先报告“本地某个对象发生事件”：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">co_dev_sam_mpdo_event(dev, idx, subidx)</span><br></pre></td></tr></table></figure><p>Lely v2.3.0 发布说明说明，C++ <code>SetEvent()</code> 会同时触发普通 TPDO event tracking 和 SAM-MPDO event tracking，因此上层不需要预先判断对象究竟映射到哪一种 PDO。</p><p>SAM 的主要调用关系如下：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">应用修改本地对象</span><br><span class="line">    ↓</span><br><span class="line">SetEvent() / co_dev_sam_mpdo_event()</span><br><span class="line">    ↓ 对象字典 SAM event indication</span><br><span class="line">co_nmt_on_sam_mpdo_event()</span><br><span class="line">    ↓ 将对象事件转交给相关 TPDO 服务</span><br><span class="line">co_sam_mpdo_event()</span><br><span class="line">    ↓</span><br><span class="line">co_dev_chk_sam_mpdo()</span><br><span class="line">    ↓ 查询 0x1FA0～0x1FCF</span><br><span class="line">co_sam_mpdo_up()</span><br><span class="line">    ↓ 本地 SDO upload 读取对象，最多 4 字节</span><br><span class="line">组装来源 Node-ID + 来源 Index/Sub-index + Value</span><br><span class="line">    ↓</span><br><span class="line">CAN 发送</span><br></pre></td></tr></table></figure><p>这条路径的核心不是“周期扫描整个对象字典”，而是“对象事件先给出候选 Index&#x2F;Sub-index，再由 Scanner List 判断该对象是否属于某个 SAM-MPDO”。这样避免了每次事件都遍历所有应用对象。</p><h2 id="7-Lely-的-MPDO-接收运行流程"><a href="#7-Lely-的-MPDO-接收运行流程" class="headerlink" title="7. Lely 的 MPDO 接收运行流程"></a>7. Lely 的 MPDO 接收运行流程</h2><p>下图把发送和接收路径放在同一张图中。应重点观察：DAM 直接使用帧内目标对象；SAM 必须经过 Dispatching List 才得到本地目标对象。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    damApi[&quot;DAM API&lt;br/&gt;co_dam_mpdo_event&quot;] --&gt; damCheck[&quot;检查 TPDO 有效、事件模式、0xFF mapping&quot;]</span><br><span class="line">    damCheck --&gt; damFrame[&quot;编码目标 Node-ID&lt;br/&gt;目标 Index/Sub-index + Value&quot;]</span><br><span class="line">    damFrame --&gt; tx[&quot;TPDO 发送到 CAN network&quot;]</span><br><span class="line"></span><br><span class="line">    objEvent[&quot;本地对象事件&quot;] --&gt; devSam[&quot;co_dev_sam_mpdo_event&quot;]</span><br><span class="line">    devSam --&gt; nmtSam[&quot;co_nmt_on_sam_mpdo_event&quot;]</span><br><span class="line">    nmtSam --&gt; samEvent[&quot;co_sam_mpdo_event&quot;]</span><br><span class="line">    samEvent --&gt; scanner[&quot;co_dev_chk_sam_mpdo&lt;br/&gt;查询 Scanner List&quot;]</span><br><span class="line">    scanner --&gt; samUp[&quot;co_sam_mpdo_up&lt;br/&gt;本地 SDO upload&quot;]</span><br><span class="line">    samUp --&gt; samFrame[&quot;编码来源 Node-ID&lt;br/&gt;来源 Index/Sub-index + Value&quot;]</span><br><span class="line">    samFrame --&gt; tx</span><br><span class="line"></span><br><span class="line">    rx[&quot;CAN frame 到达 RPDO receiver&quot;] --&gt; recv[&quot;co_rpdo_recv&quot;]</span><br><span class="line">    recv --&gt; read[&quot;co_rpdo_read_frame&quot;]</span><br><span class="line">    read --&gt; mode&#123;&quot;mapping mode&quot;&#125;</span><br><span class="line">    mode --&gt;|&quot;0xFE SAM&quot;| samParse[&quot;解析来源 Node-ID/Index/Sub-index&quot;]</span><br><span class="line">    samParse --&gt; dispatch[&quot;co_dev_map_sam_mpdo&lt;br/&gt;查询 Dispatching List&quot;]</span><br><span class="line">    dispatch --&gt; localSam[&quot;得到本地 Index/Sub-index&quot;]</span><br><span class="line">    localSam --&gt; odWrite[&quot;通过本地 SDO download 写 OD&quot;]</span><br><span class="line"></span><br><span class="line">    mode --&gt;|&quot;0xFF DAM&quot;| damParse[&quot;校验目标 Node-ID&lt;br/&gt;使用帧内目标 Index/Sub-index&quot;]</span><br><span class="line">    damParse --&gt; odWrite</span><br><span class="line">    odWrite --&gt; callback[&quot;对象 write indication / 应用回调&quot;]</span><br><span class="line">    odWrite --&gt; error[&quot;失败：SDO abort code&lt;br/&gt;RPDO error/EMCY 路径&quot;]</span><br></pre></td></tr></table></figure><h3 id="7-1-共同入口：co-rpdo-recv-与-co-rpdo-read-frame"><a href="#7-1-共同入口：co-rpdo-recv-与-co-rpdo-read-frame" class="headerlink" title="7.1 共同入口：co_rpdo_recv() 与 co_rpdo_read_frame()"></a>7.1 共同入口：<code>co_rpdo_recv()</code> 与 <code>co_rpdo_read_frame()</code></h3><p><code>co_rpdo_recv()</code> 是 RPDO 服务注册到 CAN network 的接收回调。对于需要立即处理的事件型数据，它进入 <code>co_rpdo_read_frame()</code>；同步普通 PDO 还可能先缓存到下一个 SYNC，但 MPDO 按协议是事件驱动，不应依赖同步触发。</p><p><code>co_rpdo_read_frame()</code> 负责解析帧并更新对象字典，返回 0 或内部 SDO abort code。该返回值属于 Lely 内部错误传播，不是发送到总线的 SDO response。</p><h3 id="7-2-接收-SAM-MPDO"><a href="#7-2-接收-SAM-MPDO" class="headerlink" title="7.2 接收 SAM-MPDO"></a>7.2 接收 SAM-MPDO</h3><p>SAM 接收流程为：</p><ol><li>从 Byte 0 获取来源 Node-ID，解析来源 Index&#x2F;Sub-index。</li><li><code>co_dev_map_sam_mpdo()</code> 查询 <code>0x1FD0～0x1FFF</code>。</li><li>找到匹配项后，返回本地 Index&#x2F;Sub-index。</li><li>通过本地对象字典 download 路径写入数据。</li><li>上层 <code>RpdoRead()</code>、<code>RpdoGet()</code> 和 <code>OnRpdoWrite()</code> 可像普通 RPDO 一样观察更新；Lely v2.3.0 发布说明明确说明了这一点。</li></ol><p>如果 Dispatching List 没有匹配项，不能凭帧内 Index 直接写入同名本地对象，因为 SAM 的帧地址描述来源，不描述目的。</p><h3 id="7-3-接收-DAM-MPDO"><a href="#7-3-接收-DAM-MPDO" class="headerlink" title="7.3 接收 DAM-MPDO"></a>7.3 接收 DAM-MPDO</h3><p>DAM 接收流程为：</p><ol><li>解析并校验目标 Node-ID；非本节点且不是广播地址时忽略。</li><li>使用帧内 Index&#x2F;Sub-index 直接定位本地对象。</li><li>检查对象存在性、写权限和数据长度。</li><li>通过本地对象字典 download 路径写入。</li></ol><p>Lely 的 C++ 应用层对 DAM 有一个重要限制：<code>OnWrite()</code> 能看到本地对象被写入，但无法仅凭该回调区分来源是 DAM-MPDO 还是 SDO download。原因是两条路径最终复用了同一本地对象 download indication。</p><h2 id="8-为什么-CANopenNode-没有实现-MPDO"><a href="#8-为什么-CANopenNode-没有实现-MPDO" class="headerlink" title="8. 为什么 CANopenNode 没有实现 MPDO"></a>8. 为什么 CANopenNode 没有实现 MPDO</h2><h3 id="8-1-已确认的实现状态"><a href="#8-1-已确认的实现状态" class="headerlink" title="8.1 已确认的实现状态"></a>8.1 已确认的实现状态</h3><p>在本文检查的 CANopenNode 基线中，PDO 公开对象和 API 仍围绕普通 RPDO&#x2F;TPDO：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">CO_PDO_common_t</span><br><span class="line">CO_RPDO_t</span><br><span class="line">CO_TPDO_t</span><br><span class="line">CO_RPDO_init() / CO_RPDO_process()</span><br><span class="line">CO_TPDO_init() / CO_TPDO_process()</span><br></pre></td></tr></table></figure><p>没有发现对应的 <code>CO_MPDO_t</code>、DAM&#x2F;SAM 初始化入口、Scanner&#x2F;Dispatching List 运行时查找函数，或类似 <code>CO_CONFIG_MPDO</code> 的独立配置能力。仓库中保留 <code>CO_EMC_DAM_MPDO</code> 一类标准错误码，也不能证明存在 MPDO 收发路径；错误码表和协议服务实现是两个不同层次。</p><p>CANopenNode Issue #588 在本文查阅时仍为 open，且没有 assignee 或 milestone。维护者的回复可以归纳为三点：</p><ul><li>自己没有实际使用 MPDO 的经验；</li><li>功能可以加入 <code>CO_PDO</code>；</li><li>工作并非简单改动，而现有 PDO 代码已经较复杂。</li></ul><p>Issue #72 中，维护者也明确说明当时未实现 MPDO，并建议通过增加普通 PDO、自定义 COB-ID，或使用 SDO Domain 传输大数据。</p><h3 id="8-2-“为什么没实现”的工程解释"><a href="#8-2-“为什么没实现”的工程解释" class="headerlink" title="8.2 “为什么没实现”的工程解释"></a>8.2 “为什么没实现”的工程解释</h3><p>以下是基于协议和现有结构的工程分析，不是维护者原话。</p><p>MPDO 不是在普通 PDO 前面加 4 字节头即可完成。一个可互操作的实现至少需要：</p><ol><li>识别 mapping 子索引 0 的 <code>0xFE/0xFF</code> 特殊模式。</li><li>实现 DAM 和 SAM 两套不同的地址语义。</li><li>支持 <code>0x1FA0～0x1FCF</code> Scanner List。</li><li>支持 <code>0x1FD0～0x1FFF</code> Dispatching List。</li><li>处理目标 Node-ID、DAM 广播和非目标帧过滤。</li><li>对每帧执行动态 OD 查找、长度检查和访问权限检查。</li><li>把对象不可用等异常连接到 <code>0x8230</code> EMCY 与内部错误状态。</li><li>处理通信复位、动态 OD 修改、条件编译和并发访问。</li><li>为普通 PDO、SAM、DAM、不同数据宽度和错误路径增加一致性测试。</li></ol><p>CANopenNode 面向资源受限 MCU，并强调可配置裁剪。加入 MPDO 会增加 ROM、RAM、条件分支和测试矩阵，而实际使用面远小于普通 PDO 和 SDO。结合维护者缺少实际使用经验，可以判断其优先级和长期维护收益目前不足；但不能把原因简单写成“MCU 性能不够”。</p><h2 id="9-CANopenNode-中如何替代-MPDO"><a href="#9-CANopenNode-中如何替代-MPDO" class="headerlink" title="9. CANopenNode 中如何替代 MPDO"></a>9. CANopenNode 中如何替代 MPDO</h2><p>替代方案应按需求选择，而不是机械地用某一种服务覆盖所有场景。</p><table><thead><tr><th>实际需求</th><th>推荐方案</th><th>主要代价与限制</th></tr></thead><tbody><tr><td>固定过程数据超过默认 4 个 PDO</td><td>增加 TPDO&#x2F;RPDO 数量，分配唯一 COB-ID</td><td>需要管理 COB-ID 冲突、总线负载和更多映射对象</td></tr><tr><td>少量对象高频实时更新</td><td>普通 PDO</td><td>映射固定，但实时性最好</td></tr><tr><td>低频参数读写且需要确认</td><td>expedited&#x2F;segmented SDO</td><td>请求响应开销更高</td></tr><tr><td>大块缓冲区、文件或记录</td><td>SDO Domain，必要时 block transfer</td><td>不适合硬实时过程闭环</td></tr><tr><td>稀疏事件、只在自有系统内使用</td><td>自定义应用层 CAN 帧</td><td>不是标准 MPDO，第三方工具无法自动解释</td></tr><tr><td>设备 profile 明确要求 MPDO</td><td>更换支持 MPDO 的协议栈，或为 CANopenNode 完整扩展</td><td>实现和一致性测试成本最高</td></tr></tbody></table><p>CANopenNode 并不存在“只能有 4 个 TPDO”的硬性协议限制。4 个是预定义连接集中的默认数量；项目可以增加 PDO 对象并使用其他无冲突 COB-ID。需要注意的是，同一 CAN-ID 被两个生产者使用时，只有数据完全相同的位级仲裁才不会立即暴露冲突；正常设计必须保证生产 COB-ID 唯一。</p><p>对于大于 32 位、必须确认成功的数据，SDO 不是退而求其次，而是语义正确的选择。对于固定且高频的数据，增加普通 PDO 也通常比移植 MPDO 更容易验证。</p><h2 id="10-CanFestival-是否实现了-MPDO"><a href="#10-CanFestival-是否实现了-MPDO" class="headerlink" title="10. CanFestival 是否实现了 MPDO"></a>10. CanFestival 是否实现了 MPDO</h2><p>本文检查了 <code>ljessendk/CanFestival</code> 的 <code>include/pdo.h</code> 和 <code>src/pdo.c</code>，以及 RT-Thread 分支 <code>wdfk-prog/canfestival-rtt</code> 的对应 PDO 路径。</p><p>公开头文件提供的主要接口为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">buildPDO()</span><br><span class="line">proceedPDO()</span><br><span class="line">sendPDOrequest()</span><br><span class="line">sendPDOevent()</span><br><span class="line">sendOnePDOevent()</span><br><span class="line">PDOEnable() / PDODisable()</span><br><span class="line">PDOInit() / PDOStop()</span><br></pre></td></tr></table></figure><p><code>buildPDO()</code> 的运行模型是：读取固定 TPDO mapping count，逐条读取 mapping parameter，再通过 <code>getODentry()</code> 把本地对象复制到 payload。</p><p><code>proceedPDO()</code> 的运行模型是：按 COB-ID 查找 RPDO，读取固定 mapping parameter，再通过 <code>setODentry()</code> 写入对应本地对象。</p><p>在所检查的版本中，没有发现以下 MPDO 必需路径：</p><ul><li>mapping count 为 <code>0xFE/0xFF</code> 时的专用分支；</li><li>Byte 0 Node-ID 和 DAM&#x2F;SAM 标志解析；</li><li><code>0x1FA0～0x1FCF</code> Scanner List 查询；</li><li><code>0x1FD0～0x1FFF</code> Dispatching List 查询；</li><li>DAM&#x2F;SAM 专用 API 或服务状态；</li><li><code>0x8230</code> DAM target error 的运行时连接。</li></ul><p>因此可得出的限定结论是：</p><blockquote><p>在 <code>ljessendk/CanFestival@bebf332d</code> 和 <code>wdfk-prog/canfestival-rtt@0e73022b</code> 的检查范围内，没有发现完整的 CiA 301 SAM-MPDO 或 DAM-MPDO 实现；其 PDO 代码是固定映射的普通 RPDO&#x2F;TPDO 路径。</p></blockquote><p>这不是对所有历史私有分支或厂商派生版本的断言。某个产品若声称基于 CanFestival 支持 MPDO，仍需要检查其私有补丁，而不能根据“CanFestival”名称直接判断。</p><h2 id="11-当前-Lely-主站与-CANopenNode-MCU-从机应如何选择"><a href="#11-当前-Lely-主站与-CANopenNode-MCU-从机应如何选择" class="headerlink" title="11. 当前 Lely 主站与 CANopenNode MCU 从机应如何选择"></a>11. 当前 Lely 主站与 CANopenNode MCU 从机应如何选择</h2><p>当前项目组合是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Linux 主站：Lely CANopen，支持 SAM/DAM MPDO</span><br><span class="line">MCU 从站：CANopenNode，当前检查基线未实现 MPDO</span><br></pre></td></tr></table></figure><p>CANopen 是双端协议能力的组合。主站支持 MPDO，并不会让普通 PDO 对端自动获得 MPDO 能力。若 Lely 发出 DAM-MPDO：</p><ul><li>CANopenNode 可能在 CAN filter 层接收到相同 COB-ID；</li><li>但它不会按 MPDO 头解析 Node-ID、Index 和 Sub-index；</li><li>手工在 EDS 中增加 Scanner&#x2F;Dispatching List，也不会自动创建协议处理代码；</li><li>把 MPDO payload 当成普通 RPDO 映射，还会把前 4 字节寻址头误当成过程数据。</li></ul><p>因此项目应采用以下边界：</p><ol><li>CANopenNode 默认和扩展功能测试不把 MPDO 列为通过项。</li><li>固定过程量继续使用普通 TPDO&#x2F;RPDO。</li><li>配置、诊断和需要结果确认的访问使用 SDO。</li><li>对象数量增加时，优先扩展普通 PDO 数量并规划唯一 COB-ID。</li><li>只有设备 profile 或第三方互操作规范明确依赖 MPDO 时，才评估协议栈替换或完整实现 MPDO。</li></ol><p>这也解释了一个容易混淆的事实：Lely 的 RPDO&#x2F;TPDO 文档和源码中能看到 MPDO 错误路径，不代表 CANopenNode 或 CanFestival 对端能够互操作。功能验收必须以双方都实现同一通信服务为前提。</p><h2 id="12-结论"><a href="#12-结论" class="headerlink" title="12. 结论"></a>12. 结论</h2><p>MPDO 的核心价值不是提高单帧数据量，而是用 4 字节寻址头换取动态对象选择：</p><ul><li>DAM 由生产者直接指定目标节点和目标对象；</li><li>SAM 由生产者声明来源对象，消费者通过 Dispatching List 路由到本地对象；</li><li>两者都只剩最多 4 字节数据，且没有逐帧成功确认；</li><li>普通 PDO、MPDO 和 SDO 的选择，应由数据是否固定、是否需要确认、数据宽度和实时性共同决定。</li></ul><p>Lely 已把 MPDO 集成进 <code>co_tpdo_t</code>、<code>co_rpdo_t</code>、<code>co_dev_t</code> 和 NMT 事件桥接，支持 DAM&#x2F;SAM 的生产与消费。CANopenNode 当前没有完整实现，维护者也未给出明确版本计划；CanFestival 的所查版本同样只发现普通固定映射 PDO 路径。</p><p>对当前 Lely 主站 + CANopenNode MCU 从站项目，最小风险方案仍是普通 PDO + SDO。只有外部 profile 明确要求 MPDO 时，才值得承担协议栈替换或扩展实现的成本。</p><h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ol><li>用户提供：《CiA 301 V4.2.0（中文注释版）》，第 7.2.3、7.5.2.36、7.5.2.39、7.5.2.40 节及 EMCY error code 表。</li><li><a href="https://opensource.lely.com/canopen/docs/overview/">Lely CANopen - Library overview</a>，查阅日期 2026-08-01。</li><li><a href="https://opensource.lely.com/canopen/docs/standards/">Lely CANopen - Standards support</a>，查阅日期 2026-08-01。</li><li><a href="https://opensource.lely.com/canopen/release/v2.3.0/">Lely CANopen - New release v2.3.0</a>，MPDO API、C++ 回调和 dcfgen 边界。</li><li><a href="https://opensource.lely.com/canopen/docs/configuration/">Lely CANopen - Build configuration</a>，<code>LELY_NO_CO_MPDO</code>。</li><li><a href="https://lely_industries.gitlab.io/lely-core/doxygen/pdo_8c.html">Lely Doxygen 2.4.0 - pdo.c</a>，Scanner&#x2F;Dispatching List 与本地 SDO 访问函数。</li><li><a href="https://lely_industries.gitlab.io/lely-core/doxygen/rpdo_8c.html">Lely Doxygen 2.4.0 - rpdo.c</a>，RPDO 接收和帧解析入口。</li><li><a href="https://gitlab.com/lely_industries/lely-core/-/commit/88848aa28599ea5d6d7e766a9ffa2054446821b5">Lely GitLab master commit 88848aa2</a>。</li><li><a href="https://github.com/CANopenNode/CANopenNode/issues/588">CANopenNode Issue #588 - Future Support for MPDOs?</a>。</li><li><a href="https://github.com/CANopenNode/CANopenNode/issues/72">CANopenNode Issue #72 - Multiple(xed) PDOs</a>。</li><li><a href="https://github.com/CANopenNode/CANopenNode/commit/9b8beed8367241e96ac03f916cd5a500bcb2cf23">CANopenNode commit 9b8beed8</a>。</li><li><a href="https://github.com/ljessendk/CanFestival/commit/bebf332dd87c1cdfab3e097f0aaf1eb68edcf369">CanFestival mirror commit bebf332d</a>。</li><li><a href="https://github.com/wdfk-prog/canfestival-rtt/commit/0e73022bc192bf56a085cf56b8c4d3fda6da9a55">CanFestival RT-Thread branch commit 0e73022b</a>。</li></ol>]]></content>
    
    
      
      
    <summary type="html">&lt;meta name=&quot;referrer&quot; content=&quot;no-referrer&quot; /&gt;

&lt;h1 id=&quot;CANopen-MPDO：动态对象寻址&quot;&gt;&lt;a href=&quot;#CANopen-MPDO：动态对象寻址&quot; class=&quot;headerlink&quot; title=&quot;CANope</summary>
      
    
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/categories/canopen/"/>
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/tags/canopen/"/>
    
  </entry>
  
  <entry>
    <title>Lely CANopen Node</title>
    <link href="https://wdfk-prog.space/posts/635fb6ac/"/>
    <id>https://wdfk-prog.space/posts/635fb6ac/</id>
    <published>2026-08-01T07:38:51.000Z</published>
    <updated>2026-08-01T07:39:47.905Z</updated>
    
    <content type="html"><![CDATA[<meta name="referrer" content="no-referrer" /><h1 id="Lely-CANopen-coapp-Node：CAN-网络、NMT-服务与-C-事件回调的装配机制"><a href="#Lely-CANopen-coapp-Node：CAN-网络、NMT-服务与-C-事件回调的装配机制" class="headerlink" title="Lely CANopen coapp::Node：CAN 网络、NMT 服务与 C++ 事件回调的装配机制"></a>Lely CANopen <code>coapp::Node</code>：CAN 网络、NMT 服务与 C++ 事件回调的装配机制</h1><blockquote><p>摘要：从源码追踪 coapp::Node 如何组合 CAN I&#x2F;O、对象字典、NMT 服务、定时队列与 C++ 回调，并解释构造、复位和事件分发的完整流程。</p></blockquote><p>@[toc]<br><img src="https://i-blog.csdnimg.cn/direct/9762e6dae52a4616bcf7886781ec0aa6.png" alt="在这里插入图片描述"></p><h2 id="1-Node-不是协议重写，而是装配中心"><a href="#1-Node-不是协议重写，而是装配中心" class="headerlink" title="1. Node 不是协议重写，而是装配中心"></a>1. <code>Node</code> 不是协议重写，而是装配中心</h2><p>阅读 <code>lely-core/include/lely/coapp/node.hpp</code> 和 <code>lely-core/src/coapp/node.cpp</code> 时，最重要的判断不是“<code>Node</code> 实现了多少 CANopen 服务”，而是：</p><blockquote><p><code>lely::canopen::Node</code> 把 CAN I&#x2F;O、时间源、本地对象字典、NMT 总控和 C++ 应用回调装配成一个可运行节点；具体 PDO、SYNC、TIME、EMCY、LSS 等协议状态机仍由 C 层服务对象实现。</p></blockquote><p>这一判断可以直接从继承关系和内部对象得到：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Node</span> : <span class="keyword">public</span> io::CanNet, <span class="keyword">public</span> Device &#123;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">    <span class="keyword">struct</span> <span class="title class_">Impl_</span>;</span><br><span class="line">    std::unique_ptr&lt;Impl_&gt; impl_;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p><code>Node</code> 同时拥有三种能力：</p><table><thead><tr><th>层次</th><th>主要对象</th><th>在 <code>Node</code> 中的职责</th></tr></thead><tbody><tr><td>I&#x2F;O 与时间</td><td><code>io::CanNet</code></td><td>接收和发送 CAN 帧，连接 Timer Queue、Executor 与系统时钟</td></tr><tr><td>设备描述</td><td><code>Device</code> &#x2F; <code>co_dev_t</code></td><td>保存对象字典、Node-ID、通信参数及本地 OD 访问能力</td></tr><tr><td>CANopen 总控</td><td><code>co_nmt_t</code></td><td>管理 NMT 状态，并按状态创建、销毁或访问其他协议服务</td></tr><tr><td>C++ 应用接口</td><td>虚函数与 <code>std::function</code></td><td>把 C 层 indication 转换成可继承、可注册的 C++ 事件接口</td></tr></tbody></table><p>Lely 的底层 <code>liblely-co</code> 是被动协议栈：它自身不创建线程、不直接访问总线，也不主动读取系统时钟。CAN 帧和时间必须由外部网络对象注入。<code>io::CanNet</code> 正好承担这一适配，而 <code>co_nmt_t</code> 再在其上管理 CANopen 服务。</p><p>下面的关系图只画出本文需要的边界，不展开每个协议状态机内部实现。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    EXEC[&quot;Executor&quot;] --&gt; CNET[&quot;io::CanNet&lt;br/&gt;CAN I/O 与时间接入&quot;]</span><br><span class="line">    TIMER[&quot;TimerBase / Timer Queue&quot;] --&gt; CNET</span><br><span class="line">    CHAN[&quot;CanChannelBase&quot;] --&gt; CNET</span><br><span class="line"></span><br><span class="line">    DCF[&quot;EDS / DCF&quot;] --&gt; DEV[&quot;Device / co_dev_t&lt;br/&gt;对象字典与 Node-ID&quot;]</span><br><span class="line">    SDEV[&quot;co_sdev 静态设备描述&quot;] --&gt; DEV</span><br><span class="line"></span><br><span class="line">    NODE[&quot;coapp::Node&lt;br/&gt;装配与适配中心&quot;] --&gt; CNET</span><br><span class="line">    NODE --&gt; DEV</span><br><span class="line">    NODE --&gt; IMPL[&quot;Node::Impl_&quot;]</span><br><span class="line">    IMPL --&gt; NMT[&quot;co_nmt_t&lt;br/&gt;NMT 总控&quot;]</span><br><span class="line">    NMT --&gt; SVC[&quot;RPDO / TPDO / SYNC / TIME&lt;br/&gt;EMCY / LSS / SDO 等服务&quot;]</span><br><span class="line"></span><br><span class="line">    SVC --&gt; BRIDGE[&quot;C indication 回调&quot;]</span><br><span class="line">    BRIDGE --&gt; APP[&quot;虚函数与 std::function&lt;br/&gt;C++ 应用逻辑&quot;]</span><br></pre></td></tr></table></figure><p>图中的 <code>Node → CanNet</code> 和 <code>Node → Device</code> 表示继承关系；<code>Node → Impl_ → co_nmt_t</code> 表示内部所有权关系。<code>Node</code> 并不把这些层合并成一套新协议实现，而是在生命周期、锁和回调层面把它们连起来。</p><h2 id="2-类结构、所有权与构造输入"><a href="#2-类结构、所有权与构造输入" class="headerlink" title="2. 类结构、所有权与构造输入"></a>2. 类结构、所有权与构造输入</h2><h3 id="2-1-三种设备描述入口"><a href="#2-1-三种设备描述入口" class="headerlink" title="2.1 三种设备描述入口"></a>2.1 三种设备描述入口</h3><p><code>Node</code> 的公开构造函数支持三类设备描述来源：</p><ol><li>已存在的 <code>co_dev_t*</code>；</li><li>EDS&#x2F;DCF 文本及可选 concise DCF 二进制；</li><li>编译进程序的 <code>co_sdev</code> 静态设备描述。</li></ol><p>对应重载是否存在受条件编译控制：</p><ul><li><code>LELY_NO_CO_DCF</code> 会移除 DCF&#x2F;EDS 构造路径；</li><li><code>LELY_NO_CO_SDEV</code> 会移除静态设备描述构造路径。</li></ul><p>三条路径最终都得到一个 <code>Device</code>，并把同一份内部 <code>co_dev_t</code> 交给 <code>co_nmt_create()</code>。因此，设备描述来源不同，但进入 NMT 总控后的运行模型相同。</p><h3 id="2-2-构造顺序透露了对象依赖"><a href="#2-2-构造顺序透露了对象依赖" class="headerlink" title="2.2 构造顺序透露了对象依赖"></a>2.2 构造顺序透露了对象依赖</h3><p>以接收 <code>co_dev_t*</code> 的构造函数为例，初始化顺序可以压缩为：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">Node::<span class="built_in">Node</span>(<span class="type">ev_exec_t</span>* exec, io::TimerBase&amp; timer,</span><br><span class="line">           io::CanChannelBase&amp; chan, <span class="type">co_dev_t</span>* dev, <span class="type">uint8_t</span> id)</span><br><span class="line">    : io::<span class="built_in">CanNet</span>(exec, timer, chan, <span class="number">0</span>, <span class="number">0</span>),</span><br><span class="line">      <span class="built_in">Device</span>(dev, id, <span class="keyword">this</span>),</span><br><span class="line">      <span class="built_in">tpdo_event_mutex</span>(*<span class="keyword">this</span>),</span><br><span class="line">      <span class="built_in">impl_</span>(<span class="keyword">new</span> <span class="built_in">Impl_</span>(<span class="keyword">this</span>, <span class="built_in">net</span>(), Device::<span class="built_in">dev</span>())) &#123;</span><br><span class="line">    <span class="built_in">start</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里有四个关键关系。</p><p>第一，<code>CanNet</code> 必须先建立，因为 <code>Impl_</code> 创建 <code>co_nmt_t</code> 时需要 <code>can_net_t*</code>。</p><p>第二，<code>Device(dev, id, this)</code> 将 <code>Node</code> 自身作为 <code>Device</code> 使用的 <code>BasicLockable</code>。这表示对象字典访问和 CAN 网络事件共享 <code>Node/CanNet</code> 的互斥边界，而不是各自维护互不相关的锁。</p><p>第三，<code>Impl_</code> 同时取得：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Node*       -&gt; 回调到 C++ 对象</span><br><span class="line">can_net_t*  -&gt; 连接协议服务与 CAN 网络</span><br><span class="line">co_dev_t*   -&gt; 提供对象字典和通信参数</span><br></pre></td></tr></table></figure><p>第四，构造函数末尾调用的是 <code>CanNet::start()</code>，注释也明确写为“Start processing CAN frames”。它只表示开始处理 CAN I&#x2F;O，不等价于 CANopen 节点已经完成 boot-up。</p><h3 id="2-3-co-nmt-t-的-RAII-管理"><a href="#2-3-co-nmt-t-的-RAII-管理" class="headerlink" title="2.3 co_nmt_t 的 RAII 管理"></a>2.3 <code>co_nmt_t</code> 的 RAII 管理</h3><p><code>Impl_</code> 用自定义删除器持有 NMT 对象：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">NmtDeleter</span> &#123;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">operator</span><span class="params">()</span><span class="params">(<span class="type">co_nmt_t</span>* nmt)</span> <span class="type">const</span> <span class="keyword">noexcept</span> </span>&#123;</span><br><span class="line">        <span class="built_in">co_nmt_destroy</span>(nmt);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">std::unique_ptr&lt;<span class="type">co_nmt_t</span>, NmtDeleter&gt; nmt;</span><br></pre></td></tr></table></figure><p>这层封装解决了 C&#x2F;C++ 所有权边界：</p><ul><li>C API 负责 <code>co_nmt_create()</code> &#x2F; <code>co_nmt_destroy()</code>；</li><li>C++ <code>unique_ptr</code> 负责异常路径和析构路径上的自动释放；</li><li><code>Node</code> 禁止复制，避免多个实例共同拥有同一 NMT 服务及网络资源。</li></ul><p>官方接口还要求传入的 <code>TimerBase</code> 与 <code>CanChannelBase</code> 不再被其他用途复用。这不是普通的“引用生命周期”提醒，而是因为 <code>CanNet</code> 会在其上安装异步读写、定时和状态处理，复用可能破坏事件所有权及执行顺序。</p><h2 id="3-构造完成后为什么仍然必须调用-Reset"><a href="#3-构造完成后为什么仍然必须调用-Reset" class="headerlink" title="3. 构造完成后为什么仍然必须调用 Reset()"></a>3. 构造完成后为什么仍然必须调用 <code>Reset()</code></h2><h3 id="3-1-两个“启动”属于不同层"><a href="#3-1-两个“启动”属于不同层" class="headerlink" title="3.1 两个“启动”属于不同层"></a>3.1 两个“启动”属于不同层</h3><p><code>Node</code> 构造结束时发生了：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">CanNet::start()</span><br></pre></td></tr></table></figure><p>它使 CAN 通道开始向 <code>CanNet</code> 提交帧和状态事件。但新建的 <code>co_nmt_t</code> 仍处于 NMT Initialisation 状态。按照 Lely 的 NMT 设计，此时不会创建完整协议服务，也不会执行正常 CANopen 通信。</p><p>应用需要显式调用：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">node.<span class="built_in">Reset</span>();</span><br></pre></td></tr></table></figure><p><code>Reset()</code> 的语义不是重新启动事件循环，而是模拟收到 NMT <code>reset node</code> 命令：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">Node::Reset</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="function">std::lock_guard&lt;util::BasicLockable&gt; <span class="title">lock</span><span class="params">(*<span class="keyword">this</span>)</span></span>;</span><br><span class="line">    <span class="built_in">SetTime</span>();</span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">co_nmt_cs_ind</span>(<span class="built_in">nmt</span>(), CO_NMT_CS_RESET_NODE) == <span class="number">-1</span>)</span><br><span class="line">        util::<span class="built_in">throw_errc</span>(<span class="string">&quot;Reset&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>所以两者应严格区分：</p><table><thead><tr><th>动作</th><th>所属层</th><th>直接效果</th></tr></thead><tbody><tr><td>构造函数中的 <code>start()</code></td><td>CAN I&#x2F;O 层</td><td>开始处理 CAN 帧、时间和 CAN 状态事件</td></tr><tr><td><code>Node::Reset()</code></td><td>CANopen NMT 层</td><td>注入 <code>RESET_NODE</code>，启动 NMT 复位和 boot-up 流程</td></tr></tbody></table><h3 id="3-2-为什么复位前先更新时间"><a href="#3-2-为什么复位前先更新时间" class="headerlink" title="3.2 为什么复位前先更新时间"></a>3.2 为什么复位前先更新时间</h3><p><code>Reset()</code> 在 <code>co_nmt_cs_ind()</code> 之前调用 <code>SetTime()</code>，而 <code>SetTime()</code> 只是转调 <code>CanNet::set_time()</code>。</p><p>源码注释给出的目的很具体：如果该节点作为主站运行，先更新 CAN 网络时间可以避免 SDO timeout 从过旧时间基准开始计算，导致超时过早发生。</p><p>这里的“网络时间”是 <code>can_net_t</code> 的单调定时基准，不是 CANopen TIME 对象 <code>0x1012/0x1013</code> 表示的绝对时间。两者名称相似，但用途完全不同：</p><ul><li><code>CanNet::set_time()</code>：推进协议定时器；</li><li>TIME service：收发 CANopen 时间戳报文。</li></ul><h3 id="3-3-完整构造与复位时序"><a href="#3-3-完整构造与复位时序" class="headerlink" title="3.3 完整构造与复位时序"></a>3.3 完整构造与复位时序</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant APP as Application</span><br><span class="line">    participant NODE as coapp::Node</span><br><span class="line">    participant CNET as io::CanNet</span><br><span class="line">    participant DEV as Device</span><br><span class="line">    participant IMPL as Node::Impl_</span><br><span class="line">    participant NMT as co_nmt_t</span><br><span class="line"></span><br><span class="line">    APP-&gt;&gt;NODE: 构造 Node</span><br><span class="line">    NODE-&gt;&gt;CNET: 构造 CAN 网络适配</span><br><span class="line">    NODE-&gt;&gt;DEV: 构造对象字典封装&lt;br/&gt;mutex = this</span><br><span class="line">    NODE-&gt;&gt;IMPL: new Impl_(this, net, dev)</span><br><span class="line">    IMPL-&gt;&gt;NMT: co_nmt_create(net, dev)</span><br><span class="line">    IMPL-&gt;&gt;NMT: 注册 NMT/HB/State/SYNC 总控回调</span><br><span class="line">    NODE-&gt;&gt;CNET: start()</span><br><span class="line">    Note over CNET,NMT: CAN 帧处理已启动&lt;br/&gt;NMT 仍为 Initialisation</span><br><span class="line"></span><br><span class="line">    APP-&gt;&gt;NODE: Reset()</span><br><span class="line">    NODE-&gt;&gt;NODE: 加锁</span><br><span class="line">    NODE-&gt;&gt;CNET: SetTime()</span><br><span class="line">    NODE-&gt;&gt;NMT: co_nmt_cs_ind(RESET_NODE)</span><br><span class="line">    NMT--&gt;&gt;IMPL: OnCsInd(状态阶段命令)</span><br><span class="line">    IMPL-&gt;&gt;NMT: 获取已创建服务并安装细分回调</span><br><span class="line">    IMPL-&gt;&gt;DEV: 更新 RPDO/TPDO 反向映射</span><br><span class="line">    IMPL-&gt;&gt;NODE: 虚函数 OnCommand()</span><br><span class="line">    IMPL--&gt;&gt;APP: 解锁后调用注册函数</span><br></pre></td></tr></table></figure><p><code>Reset()</code> 会进一步触发 <code>OnCommand()</code>。官方 API 明确要求不得从 <code>OnCommand()</code> 中再次调用 <code>Reset()</code>，否则会在同一命令处理链中递归注入 reset node，破坏状态机和锁语义。</p><h2 id="4-Impl-如何把-C-NMT-对象接入-C"><a href="#4-Impl-如何把-C-NMT-对象接入-C" class="headerlink" title="4. Impl_ 如何把 C NMT 对象接入 C++"></a>4. <code>Impl_</code> 如何把 C NMT 对象接入 C++</h2><h3 id="4-1-构造时只注册“总控级”回调"><a href="#4-1-构造时只注册“总控级”回调" class="headerlink" title="4.1 构造时只注册“总控级”回调"></a>4.1 构造时只注册“总控级”回调</h3><p><code>Impl_</code> 构造函数创建 <code>co_nmt_t</code> 后，立即注册四类入口：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">co_nmt_set_cs_ind()    -&gt; NMT command indication</span><br><span class="line">co_nmt_set_hb_ind()    -&gt; heartbeat error-control indication</span><br><span class="line">co_nmt_set_st_ind()    -&gt; remote NMT state indication</span><br><span class="line">co_nmt_set_sync_ind()  -&gt; SYNC indication（功能启用时）</span><br></pre></td></tr></table></figure><p>它们有一致的 C 回调桥接形态：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">co_nmt_set_cs_ind</span>(</span><br><span class="line">    nmt.<span class="built_in">get</span>(),</span><br><span class="line">    [](<span class="type">co_nmt_t</span>* nmt, <span class="type">uint8_t</span> cs, <span class="type">void</span>* data) <span class="keyword">noexcept</span> &#123;</span><br><span class="line">        <span class="built_in">static_cast</span>&lt;Impl_*&gt;(data)-&gt;<span class="built_in">OnCsInd</span>(nmt, cs);</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="keyword">this</span>);</span><br></pre></td></tr></table></figure><p>C API 只认识函数指针和 <code>void* data</code>。静态 lambda 不捕获对象，能够转换为 C 函数指针；<code>data</code> 保存 <code>Impl_*</code>，再把事件转回成员函数。</p><h3 id="4-2-为什么不能在构造时注册全部服务回调"><a href="#4-2-为什么不能在构造时注册全部服务回调" class="headerlink" title="4.2 为什么不能在构造时注册全部服务回调"></a>4.2 为什么不能在构造时注册全部服务回调</h3><p><code>co_nmt_t</code> 不是简单持有一组永久存在的服务对象。它会依据 NMT 状态和对象字典配置创建、配置或销毁 RPDO、TPDO、SYNC、TIME、EMCY、LSS 等服务。</p><p>因此，在 <code>Impl_</code> 构造时：</p><ul><li><code>co_nmt_t</code> 已经存在；</li><li>具体服务未必存在；</li><li>即使某服务未来会出现，此刻也可能无法通过 <code>co_nmt_get_*()</code> 获得。</li></ul><p>这解释了源码为什么把细分服务的回调安装集中放到 <code>Impl_::OnCsInd()</code>：只有状态迁移已经推进，NMT 总控完成相应服务装配后，<code>Node</code> 才能取得服务指针并设置 indication。</p><h2 id="5-NMT-状态迁移中的动态服务装配"><a href="#5-NMT-状态迁移中的动态服务装配" class="headerlink" title="5. NMT 状态迁移中的动态服务装配"></a>5. NMT 状态迁移中的动态服务装配</h2><p><code>OnCsInd()</code> 是理解 <code>Node</code> 的核心函数。它并不实现 NMT 状态机，而是在 NMT 状态机发出 command indication 后，对新出现的服务安装 C++ 桥接回调。</p><h3 id="5-1-RESET-COMM：接入-LSS-配置动作"><a href="#5-1-RESET-COMM：接入-LSS-配置动作" class="headerlink" title="5.1 RESET_COMM：接入 LSS 配置动作"></a>5.1 <code>RESET_COMM</code>：接入 LSS 配置动作</h3><p>当命令为 <code>CO_NMT_CS_RESET_COMM</code>，并且未定义 <code>LELY_NO_CO_LSS</code> 时：</p><ol><li>通过 <code>co_nmt_get_lss()</code> 获取 LSS 服务；</li><li>通过 <code>co_lss_set_rate_ind()</code> 注册码率激活回调；</li><li>通过 <code>co_lss_set_store_ind()</code> 注册 Node-ID&#x2F;码率存储回调。</li></ol><p>LSS 回调中的码率单位会从 C 层的 kbit&#x2F;s 转为 C++ 接口使用的 bit&#x2F;s：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">self-&gt;<span class="built_in">OnSwitchBitrate</span>(rate * <span class="number">1000</span>, std::chrono::<span class="built_in">milliseconds</span>(delay));</span><br></pre></td></tr></table></figure><p>存储回调 <code>OnStoreInd()</code> 捕获所有异常并返回 <code>-1</code>，因为 C 回调边界不能传播 C++ 异常。</p><h3 id="5-2-START-或-ENTER-PREOP：接入-SYNC-错误处理"><a href="#5-2-START-或-ENTER-PREOP：接入-SYNC-错误处理" class="headerlink" title="5.2 START 或 ENTER_PREOP：接入 SYNC 错误处理"></a>5.2 <code>START</code> 或 <code>ENTER_PREOP</code>：接入 SYNC 错误处理</h3><p>当节点进入 Operational 或 Pre-operational 后，源码尝试取得 <code>co_sync_t</code>，并设置 <code>co_sync_set_err()</code>。</p><p>SYNC indication 本身在 <code>Impl_</code> 构造时通过 <code>co_nmt_set_sync_ind()</code> 接入；SYNC service 的长度错误等细分错误回调，则要等服务创建后再注册。这体现了两级回调：</p><ul><li>NMT 总控可直接转发的通用事件；</li><li>具体 service 对象才能提供的细分事件。</li></ul><h3 id="5-3-TIME-与-EMCY：服务存在即接入"><a href="#5-3-TIME-与-EMCY：服务存在即接入" class="headerlink" title="5.3 TIME 与 EMCY：服务存在即接入"></a>5.3 TIME 与 EMCY：服务存在即接入</h3><p><code>OnCsInd()</code> 每次运行时都会尝试：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">co_nmt_get_time() -&gt; co_time_set_ind()</span><br><span class="line">co_nmt_get_emcy() -&gt; co_emcy_set_ind()</span><br></pre></td></tr></table></figure><p>代码仍先检查返回指针。对象字典配置、构建选项或当前 NMT 阶段都可能导致服务不存在，<code>Node</code> 不把“编译了该模块”等同于“当前节点一定创建了该服务”。</p><h3 id="5-4-START：遍历运行中的-PDO"><a href="#5-4-START：遍历运行中的-PDO" class="headerlink" title="5.4 START：遍历运行中的 PDO"></a>5.4 <code>START</code>：遍历运行中的 PDO</h3><p>进入 <code>CO_NMT_CS_START</code> 时，源码遍历编号 <code>1..512</code>：</p><ul><li>对每个存在的 RPDO 设置完成 indication 和 error indication；</li><li>对每个存在的 TPDO 设置发送完成 indication。</li></ul><p>这段遍历不是创建 512 个 PDO。<code>co_nmt_get_rpdo()</code> &#x2F; <code>co_nmt_get_tpdo()</code> 只返回对象字典配置后实际存在的服务，空槽位会被跳过。</p><h3 id="5-5-非-reset-阶段：刷新-PDO-反向映射"><a href="#5-5-非-reset-阶段：刷新-PDO-反向映射" class="headerlink" title="5.5 非 reset 阶段：刷新 PDO 反向映射"></a>5.5 非 reset 阶段：刷新 PDO 反向映射</h3><p>当命令既不是 <code>RESET_NODE</code> 也不是 <code>RESET_COMM</code> 时，<code>Node</code> 调用：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">self-&gt;<span class="built_in">UpdateRpdoMapping</span>();</span><br><span class="line">self-&gt;<span class="built_in">UpdateTpdoMapping</span>();</span><br></pre></td></tr></table></figure><p>这两个函数来自 <code>Device</code>，用于更新本地代理对象与远程 PDO 地址之间的映射关系。它们不是重建 C 层 PDO 状态机，而是让 <code>Device::RpdoRead()</code>、<code>Device::TpdoWrite()</code> 等高层接口看到当前通信配置。</p><h3 id="5-6-动态装配表"><a href="#5-6-动态装配表" class="headerlink" title="5.6 动态装配表"></a>5.6 动态装配表</h3><table><thead><tr><th>NMT command indication</th><th><code>Node</code> 层主要动作</th><th>前提</th></tr></thead><tbody><tr><td><code>RESET_COMM</code></td><td>接入 LSS bitrate&#x2F;store 回调</td><td>LSS 编译启用且服务存在</td></tr><tr><td><code>START</code> &#x2F; <code>ENTER_PREOP</code></td><td>接入 SYNC error 回调</td><td>SYNC 编译启用且服务存在</td></tr><tr><td>多个阶段</td><td>接入 TIME、EMCY indication</td><td>对应服务存在</td></tr><tr><td><code>START</code></td><td>遍历并接入 RPDO&#x2F;TPDO 回调</td><td>PDO 服务已经由 NMT 创建</td></tr><tr><td>非 reset command</td><td>更新 RPDO&#x2F;TPDO 反向映射</td><td>对应模块编译启用</td></tr><tr><td>所有 command</td><td>调用虚函数和注册的 <code>OnCommand</code></td><td>注册函数可为空</td></tr></tbody></table><p>由此可以得到一个稳定的源码阅读原则：</p><blockquote><p>在 <code>Node</code> 中看到 <code>co_nmt_get_*()</code>，通常不是在“使用一个永久成员”，而是在当前 NMT 阶段查询该服务是否已经由总控创建。</p></blockquote><h2 id="6-从-C-回调到-C-应用：固定的四段式分发"><a href="#6-从-C-回调到-C-应用：固定的四段式分发" class="headerlink" title="6. 从 C 回调到 C++ 应用：固定的四段式分发"></a>6. 从 C 回调到 C++ 应用：固定的四段式分发</h2><p><code>Node</code> 对大多数事件采用相同分发框架：</p><ol><li>必要时先执行 C 层默认行为；</li><li>调用可被派生类覆盖的虚函数；</li><li>复制当前注册的 <code>std::function</code>；</li><li>用 <code>UnlockGuard</code> 临时释放 <code>Node</code> 锁，再执行外部函数对象。</li></ol><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant CAN as CAN / Timer Event</span><br><span class="line">    participant CSVC as C Service Callback</span><br><span class="line">    participant IMPL as Node::Impl_</span><br><span class="line">    participant DEF as C Default Handler</span><br><span class="line">    participant VIRT as Node Virtual Hook</span><br><span class="line">    participant GUARD as UnlockGuard</span><br><span class="line">    participant FUNC as Registered std::function</span><br><span class="line"></span><br><span class="line">    CAN-&gt;&gt;CSVC: 帧、超时或服务完成事件</span><br><span class="line">    CSVC-&gt;&gt;IMPL: static lambda + void* data</span><br><span class="line">    opt 该事件需要 C 层默认行为</span><br><span class="line">        IMPL-&gt;&gt;DEF: co_nmt_on_hb / co_nmt_on_st</span><br><span class="line">    end</span><br><span class="line">    IMPL-&gt;&gt;VIRT: OnXxx()，此时 Node 锁仍持有</span><br><span class="line">    IMPL-&gt;&gt;IMPL: 复制函数对象</span><br><span class="line">    IMPL-&gt;&gt;GUARD: 临时 unlock(*self)</span><br><span class="line">    GUARD-&gt;&gt;FUNC: 调用应用注册函数</span><br><span class="line">    FUNC--&gt;&gt;GUARD: 返回</span><br><span class="line">    GUARD-&gt;&gt;IMPL: 作用域结束后重新加锁</span><br></pre></td></tr></table></figure><h3 id="6-1-为什么先复制函数对象"><a href="#6-1-为什么先复制函数对象" class="headerlink" title="6.1 为什么先复制函数对象"></a>6.1 为什么先复制函数对象</h3><p>典型代码如下：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (on_emcy) &#123;</span><br><span class="line">    <span class="keyword">auto</span> f = on_emcy;</span><br><span class="line">    <span class="function">util::UnlockGuard&lt;util::BasicLockable&gt; <span class="title">unlock</span><span class="params">(*self)</span></span>;</span><br><span class="line">    <span class="built_in">f</span>(id, ec, er, msef);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>复制发生在解锁前，因此读取 <code>on_emcy</code> 受当前锁保护。解锁后调用副本，即使应用回调内部重新注册另一个回调，也不会使正在执行的 callable 引用失效。</p><h3 id="6-2-虚函数与注册函数的锁语义不同"><a href="#6-2-虚函数与注册函数的锁语义不同" class="headerlink" title="6.2 虚函数与注册函数的锁语义不同"></a>6.2 虚函数与注册函数的锁语义不同</h3><p>官方类说明指出：</p><ul><li>调用公开成员函数时，调用方必须保证 <code>Node</code> 互斥量当前未被自己持有；</li><li>进入虚函数回调时，<code>Node</code> 锁处于持有状态；</li><li>动态注册的 <code>std::function</code> 在虚函数完成后执行，并由实现临时解锁。</li></ul><p>这形成了两种扩展方式：</p><table><thead><tr><th>扩展方式</th><th>执行时锁状态</th><th>更适合的工作</th></tr></thead><tbody><tr><td>派生类重写 <code>virtual OnXxx()</code></td><td>锁定</td><td>快速更新派生类内部状态，调用要求锁已持有的受保护操作</td></tr><tr><td><code>Node::OnXxx(std::function)</code></td><td>解锁</td><td>调用公开 API、跨对象通知、提交新异步任务</td></tr></tbody></table><p>如果在虚函数回调中直接调用会再次锁定 <code>Node</code> 的公开成员，存在自锁风险。源码没有用递归线程锁来掩盖这个约束，而是通过接口文档和两阶段回调明确区分使用场景。</p><h3 id="6-3-Heartbeat：默认处理、超时筛选和应用通知"><a href="#6-3-Heartbeat：默认处理、超时筛选和应用通知" class="headerlink" title="6.3 Heartbeat：默认处理、超时筛选和应用通知"></a>6.3 Heartbeat：默认处理、超时筛选和应用通知</h3><p><code>OnHbInd()</code> 首先执行：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">co_nmt_on_hb</span>(nmt, id, state, reason);</span><br></pre></td></tr></table></figure><p>这是 NMT 默认 heartbeat error-control 行为。随后源码只处理 <code>CO_NMT_EC_TIMEOUT</code>：</p><ul><li><code>state == CO_NMT_EC_OCCURRED</code> 转换为 <code>occurred = true</code>；</li><li>超时恢复转换为 <code>occurred = false</code>；</li><li>其他 heartbeat 状态变化由 <code>OnStInd()</code> 路径负责。</li></ul><p>所以 <code>OnHeartbeat(id, occurred)</code> 表达的是“heartbeat timeout 故障发生或解除”，不是每次心跳帧到达通知。</p><h3 id="6-4-NMT-state：忽略本节点，只报告远端状态"><a href="#6-4-NMT-state：忽略本节点，只报告远端状态" class="headerlink" title="6.4 NMT state：忽略本节点，只报告远端状态"></a>6.4 NMT state：忽略本节点，只报告远端状态</h3><p><code>OnStInd()</code> 同样先调用 <code>co_nmt_on_st()</code>，再比较事件 Node-ID 与当前设备 Node-ID：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (id == <span class="built_in">co_dev_get_id</span>(<span class="built_in">co_nmt_get_dev</span>(nmt)))</span><br><span class="line">    <span class="keyword">return</span>;</span><br></pre></td></tr></table></figure><p>本节点状态变化不通过 <code>OnState()</code> 继续通知；该接口针对 heartbeat 协议检测到的远端节点 boot-up 或状态变化。当前节点自身的命令阶段通过 <code>OnCommand()</code> 路径表达。</p><h2 id="7-各协议事件如何汇入统一应用模型"><a href="#7-各协议事件如何汇入统一应用模型" class="headerlink" title="7. 各协议事件如何汇入统一应用模型"></a>7. 各协议事件如何汇入统一应用模型</h2><p><code>Node</code> 没有强行把所有协议事件压成同一种参数，而是保留各服务最有价值的信息，同时统一回调生命周期和锁策略。</p><h3 id="7-1-RPDO-与-TPDO"><a href="#7-1-RPDO-与-TPDO" class="headerlink" title="7.1 RPDO 与 TPDO"></a>7.1 RPDO 与 TPDO</h3><p>RPDO 完成回调取得：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">PDO 编号 + SDO abort/status code + 原始映射数据指针 + 长度</span><br></pre></td></tr></table></figure><p>源码通过 <code>co_rpdo_get_num()</code> 获取编号，并把 <code>uint32_t ac</code> 转为 <code>SdoErrc/std::error_code</code> 语义。对于启用 MPDO 的构建，<code>OnRpdoInd()</code> 还会识别 SAM-MPDO，并调用 <code>RpdoWrite()</code> 更新本地远程 PDO 代理映射。</p><p>RPDO 错误回调传递 emergency error code 和 error register。默认虚函数实现调用：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">Error</span>(eec, er);</span><br></pre></td></tr></table></figure><p>TPDO 完成回调结构相似，通过 <code>co_tpdo_get_num()</code> 获得编号。它表示“TPDO 已发送或处理发生错误”，不是“对象字典任意值被写入”的通用通知。</p><h3 id="7-2-SYNC"><a href="#7-2-SYNC" class="headerlink" title="7.2 SYNC"></a>7.2 SYNC</h3><p><code>OnSyncInd()</code> 在事件到达时读取：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">auto</span> t = self-&gt;<span class="built_in">GetClock</span>().<span class="built_in">gettime</span>();</span><br></pre></td></tr></table></figure><p>随后把 SYNC counter 和这一单调时钟时间点传给应用。因此参数中的 <code>time_point</code> 是本地收到或发送 SYNC 时的网络时钟观测点，不是 CANopen TIME 报文中的绝对时间。</p><p>SYNC 错误默认调用 <code>OnSyncError()</code>，其基类实现再进入 <code>Error(eec, er)</code>，即交给 NMT 的统一通信错误处理。</p><h3 id="7-3-TIME"><a href="#7-3-TIME" class="headerlink" title="7.3 TIME"></a>7.3 TIME</h3><p>TIME service 给出 <code>timespec</code> 后，<code>Node</code> 转换为：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">std::chrono::system_clock::time_point</span><br></pre></td></tr></table></figure><p>TIME 使用系统绝对时间语义，而 SYNC 使用 <code>TimerBase::time_point</code>。这种类型区分避免把周期同步时刻和日历时间混为一谈。</p><h3 id="7-4-EMCY"><a href="#7-4-EMCY" class="headerlink" title="7.4 EMCY"></a>7.4 EMCY</h3><p>EMCY indication 直接保留：</p><ul><li>远端 Node-ID；</li><li>emergency error code；</li><li>error register；</li><li>5 字节 manufacturer-specific error field。</li></ul><p><code>Node</code> 不在这一层解释厂商字节，也不自动改写远端错误；具体含义仍由设备规范和 EDS&#x2F;DCF 决定。</p><h3 id="7-5-LSS"><a href="#7-5-LSS" class="headerlink" title="7.5 LSS"></a>7.5 LSS</h3><p>LSS 码率切换 indication 提供新码率和延时。<code>Node</code> 负责单位转换和 C++ 回调分发，但不会自动选择具体 CAN controller。应用可以在 <code>OnSwitchBitrate()</code> 中调用 <code>AsyncSwitchBitrate(ctrl, bitrate, delay)</code> 完成本地控制器切换。</p><h3 id="7-6-CAN-控制器状态如何进入-EMCY-NMT-错误处理"><a href="#7-6-CAN-控制器状态如何进入-EMCY-NMT-错误处理" class="headerlink" title="7.6 CAN 控制器状态如何进入 EMCY&#x2F;NMT 错误处理"></a>7.6 CAN 控制器状态如何进入 EMCY&#x2F;NMT 错误处理</h3><p><code>Node::OnCanState()</code> 对两个状态变化执行默认处理：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">进入 Error Passive       -&gt; Error(0x8120, 0x10)</span><br><span class="line">从 Bus-off 状态恢复      -&gt; Error(0x8140, 0x10)</span><br></pre></td></tr></table></figure><p><code>Error()</code> 继续调用 <code>co_nmt_on_err()</code>，由 NMT 默认错误处理生成 EMCY，并按照对象 <code>0x1029:01</code> 等 Error behavior 配置执行状态行为。</p><p>需要注意，源码中仍有 TODO：进入 error active 后清除相关 EMCY 尚未在该函数中实现。文章只能确认当前代码的上报路径，不能把 TODO 描述成已完成的自动清除能力。</p><h2 id="8-Timer-Queue-与-Future：为什么-Node-也提供异步等待"><a href="#8-Timer-Queue-与-Future：为什么-Node-也提供异步等待" class="headerlink" title="8. Timer Queue 与 Future：为什么 Node 也提供异步等待"></a>8. Timer Queue 与 Future：为什么 <code>Node</code> 也提供异步等待</h2><p><code>Node</code> 继承的 <code>CanNet</code> 本身就是 <code>io_tqueue_t</code> 的使用者，因此它把同一计时基础暴露为：</p><ul><li><code>SubmitWait()</code>；</li><li><code>AsyncWait()</code>；</li><li><code>CancelWait()</code>；</li><li><code>AbortWait()</code>。</li></ul><h3 id="8-1-相对时间最终转换为绝对时间"><a href="#8-1-相对时间最终转换为绝对时间" class="headerlink" title="8.1 相对时间最终转换为绝对时间"></a>8.1 相对时间最终转换为绝对时间</h3><p>相对等待不会直接把 duration 传给底层，而是：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">AsyncWait</span>(exec, <span class="built_in">GetClock</span>().<span class="built_in">gettime</span>() + d, pwait);</span><br></pre></td></tr></table></figure><p>这样所有等待都在同一个 <code>TimerBase</code> 时间域中排序，避免各异步调用分别维护相对倒计时。</p><h3 id="8-2-默认-Executor"><a href="#8-2-默认-Executor" class="headerlink" title="8.2 默认 Executor"></a>8.2 默认 Executor</h3><p>当 <code>exec == nullptr</code> 时：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">exec = <span class="built_in">GetExecutor</span>();</span><br></pre></td></tr></table></figure><p>这使等待完成任务默认回到 <code>Node</code> 所在的执行器。调用方仍可显式提供其他 executor，从而改变 continuation 的执行上下文，但底层过期时间仍来自同一 Timer Queue。</p><h3 id="8-3-错误从整数转换为异常通道"><a href="#8-3-错误从整数转换为异常通道" class="headerlink" title="8.3 错误从整数转换为异常通道"></a>8.3 错误从整数转换为异常通道</h3><p><code>io_tqueue_async_wait()</code> 返回 <code>Future&lt;void, int&gt;</code>。<code>Node</code> 再追加 continuation，把非零 <code>errc</code> 转成异常，使公开返回值成为：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ev::Future&lt;<span class="type">void</span>, std::exception_ptr&gt;</span><br></pre></td></tr></table></figure><p>因此，上层 Future 链可以统一用 <code>get().value()</code> 传播失败，而不必每层手工判断 C 整数错误码。</p><h3 id="8-4-CancelWait-与-AbortWait-不是同义词"><a href="#8-4-CancelWait-与-AbortWait-不是同义词" class="headerlink" title="8.4 CancelWait() 与 AbortWait() 不是同义词"></a>8.4 <code>CancelWait()</code> 与 <code>AbortWait()</code> 不是同义词</h3><table><thead><tr><th>操作</th><th>pending wait 的处理</th><th>completion task</th></tr></thead><tbody><tr><td><code>CancelWait()</code></td><td>取消等待</td><td>仍提交，以 <code>operation_canceled</code> 完成</td></tr><tr><td><code>AbortWait()</code></td><td>中止等待</td><td>不再提交完成任务</td></tr></tbody></table><p>前者适合让 Future 链感知取消，后者适合彻底丢弃尚未执行的完成路径。选择错误会直接影响资源释放、continuation 是否运行及上层状态机是否收到结束信号。</p><h2 id="9-AsyncSwitchBitrate-："><a href="#9-AsyncSwitchBitrate-：" class="headerlink" title="9. AsyncSwitchBitrate()："></a>9. <code>AsyncSwitchBitrate()</code>：</h2><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">stop</span><br><span class="line">  -&gt; AsyncWait(delay)</span><br><span class="line">  -&gt; continuation 返回 AsyncWait(delay)</span><br><span class="line">  -&gt; set_bitrate</span><br><span class="line">  -&gt; continuation 返回 AsyncWait(delay)</span><br><span class="line">  -&gt; restart</span><br></pre></td></tr></table></figure><p>对应时序如下。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant APP as OnSwitchBitrate / Application</span><br><span class="line">    participant NODE as Node</span><br><span class="line">    participant CTRL as CanControllerBase</span><br><span class="line">    participant TQ as Timer Queue</span><br><span class="line">    participant FUT as Future Continuations</span><br><span class="line"></span><br><span class="line">    APP-&gt;&gt;NODE: AsyncSwitchBitrate(ctrl, bitrate, delay)</span><br><span class="line">    NODE-&gt;&gt;CTRL: stop()</span><br><span class="line">    NODE-&gt;&gt;TQ: AsyncWait(delay) #1</span><br><span class="line">    TQ--&gt;&gt;FUT: wait #1 ready</span><br><span class="line">    FUT-&gt;&gt;TQ: AsyncWait(delay) #2</span><br><span class="line">    TQ--&gt;&gt;FUT: wait #2 ready</span><br><span class="line">    FUT-&gt;&gt;CTRL: set_bitrate(bitrate)</span><br><span class="line">    FUT-&gt;&gt;TQ: AsyncWait(delay) #3</span><br><span class="line">    TQ--&gt;&gt;FUT: wait #3 ready</span><br><span class="line">    FUT-&gt;&gt;CTRL: restart()</span><br><span class="line">    FUT--&gt;&gt;APP: Future ready or exception</span><br></pre></td></tr></table></figure><h2 id="10-TpdoEventMutex-不是普通线程互斥锁"><a href="#10-TpdoEventMutex-不是普通线程互斥锁" class="headerlink" title="10. TpdoEventMutex 不是普通线程互斥锁"></a>10. <code>TpdoEventMutex</code> 不是普通线程互斥锁</h2><p><code>Node</code> 的受保护成员：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">TpdoEventMutex tpdo_event_mutex;</span><br></pre></td></tr></table></figure><p>实现只有两个关键调用：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">TpdoEventMutex::lock</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="built_in">co_nmt_on_tpdo_event_lock</span>(node-&gt;<span class="built_in">nmt</span>());</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">TpdoEventMutex::unlock</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="built_in">co_nmt_on_tpdo_event_unlock</span>(node-&gt;<span class="built_in">nmt</span>());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>它实现 <code>BasicLockable</code> 接口，但锁住的不是 C++ 临界区，而是 NMT 的异步 TPDO event 触发路径。持锁期间：</p><ul><li>由 <code>co_nmt_on_tpdo_event()</code> 触发的 acyclic&#x2F;event-driven TPDO 被推迟；</li><li>嵌套锁按递归计数匹配；</li><li>最外层 <code>unlock()</code> 后，NMT 才可能发送已积累的 TPDO 事件。</li></ul><p>它适合在派生类中成组更新多个会共同影响同一 TPDO 的对象。例如：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    <span class="function">std::lock_guard&lt;TpdoEventMutex&gt; <span class="title">hold</span><span class="params">(tpdo_event_mutex)</span></span>;</span><br><span class="line">    <span class="comment">// 更新多个相关 OD 值</span></span><br><span class="line">    <span class="comment">// 期间触发的 event-driven TPDO 被延后</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 最外层解锁后再处理 TPDO event</span></span><br></pre></td></tr></table></figure><p>这与 <code>Device(dev, id, this)</code> 使用的 <code>Node/CanNet</code> 线程互斥量必须分开理解：</p><table><thead><tr><th>锁</th><th>保护对象</th><th>目的</th></tr></thead><tbody><tr><td><code>Node</code> &#x2F; <code>CanNet</code> 的 <code>BasicLockable</code></td><td>对象字典、网络事件和回调共享状态</td><td>防止并发数据竞争，规定回调重入边界</td></tr><tr><td><code>TpdoEventMutex</code></td><td>NMT 的 TPDO event 触发计数</td><td>暂缓异步 TPDO，避免中间数据组合被发送</td></tr></tbody></table><p><code>TpdoEvent()</code> 则是显式向 NMT 报告某个 TPDO 发生事件：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">co_nmt_on_tpdo_event</span>(<span class="built_in">nmt</span>(), num);</span><br></pre></td></tr></table></figure><p><code>num == 0</code> 的具体广播或选择语义由底层 NMT API 负责，<code>Node</code> 不重新实现事件调度。</p><h2 id="11-条件编译如何改变能力，而不改变总体结构"><a href="#11-条件编译如何改变能力，而不改变总体结构" class="headerlink" title="11. 条件编译如何改变能力，而不改变总体结构"></a>11. 条件编译如何改变能力，而不改变总体结构</h2><p><code>node.cpp</code> 对 RPDO、TPDO、MPDO、SYNC、TIME、EMCY、LSS 等模块使用 <code>LELY_NO_CO_*</code> 宏。</p><p>这种条件编译主要改变三处：</p><ol><li>是否包含对应 C service 头文件；</li><li><code>Impl_</code> 是否保存对应 <code>std::function</code> 和 handler；</li><li>注册接口是否真正保存回调，或退化为 <code>(void)callback</code> 的空操作。</li></ol><p>例如，当 <code>LELY_NO_CO_TIME</code> 为真时，<code>Node::OnTime(std::function&lt;...&gt;)</code> 这一 C++ 成员仍可出现在公共头文件中，但实现不会保存回调；<code>StartTime()</code>&#x2F;<code>StopTime()</code> 的实际可用性也受对应编译分支约束。</p><p>这意味着仅凭应用源码“成功调用了注册函数”不能证明该服务已启用。能力判断至少要同时看：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">构建宏是否启用</span><br><span class="line">+ 对象字典是否配置</span><br><span class="line">+ 当前 NMT 阶段是否创建服务</span><br></pre></td></tr></table></figure><p>三个条件缺一，都可能得到“接口存在但没有运行事件”的结果。</p><h2 id="12-最终运行模型"><a href="#12-最终运行模型" class="headerlink" title="12. 最终运行模型"></a>12. 最终运行模型</h2><p>把前面的源码关系压缩后，<code>coapp::Node</code> 可以用四条路径理解。</p><h3 id="12-1-输入路径"><a href="#12-1-输入路径" class="headerlink" title="12.1 输入路径"></a>12.1 输入路径</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">CanChannelBase</span><br><span class="line">  -&gt; io::CanNet</span><br><span class="line">  -&gt; can_net_t</span><br><span class="line">  -&gt; co_nmt_t / 具体 CANopen service</span><br></pre></td></tr></table></figure><p><code>CanNet</code> 把真实 CAN I&#x2F;O 和系统时间送入被动的 C 协议栈。</p><h3 id="12-2-状态路径"><a href="#12-2-状态路径" class="headerlink" title="12.2 状态路径"></a>12.2 状态路径</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">Node::Reset() 或 NMT command</span><br><span class="line">  -&gt; co_nmt_cs_ind()</span><br><span class="line">  -&gt; NMT 状态迁移</span><br><span class="line">  -&gt; 创建/销毁协议服务</span><br><span class="line">  -&gt; Impl_::OnCsInd()</span><br><span class="line">  -&gt; 安装当前阶段可用的 service callback</span><br></pre></td></tr></table></figure><p>服务生命周期归 <code>co_nmt_t</code> 管理，<code>Node</code> 只在合适阶段接入 C++ 回调。</p><h3 id="12-3-应用路径"><a href="#12-3-应用路径" class="headerlink" title="12.3 应用路径"></a>12.3 应用路径</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">C service indication</span><br><span class="line">  -&gt; static lambda + void* data</span><br><span class="line">  -&gt; Node::Impl_::OnXxx()</span><br><span class="line">  -&gt; C 默认处理（按需）</span><br><span class="line">  -&gt; virtual Node::OnXxx()（锁定）</span><br><span class="line">  -&gt; std::function（临时解锁）</span><br></pre></td></tr></table></figure><p>这条路径同时解决 C ABI、C++ 多态、动态回调注册和重入控制。</p><h3 id="12-4-时间路径"><a href="#12-4-时间路径" class="headerlink" title="12.4 时间路径"></a>12.4 时间路径</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">TimerBase / io_tqueue</span><br><span class="line">  -&gt; CANopen timeout</span><br><span class="line">  -&gt; Node::AsyncWait()</span><br><span class="line">  -&gt; Future continuation</span><br><span class="line">  -&gt; LSS bitrate switch 等异步流程</span><br></pre></td></tr></table></figure><p>Timer Queue 既支撑 CANopen 内部定时，也为应用层串联无阻塞动作提供统一时间域。</p><p>因此，<code>Node</code> 最准确的定位不是“CANopen 协议全集”，而是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">Node = CanNet 的 I/O/时间能力</span><br><span class="line">     + Device 的对象字典能力</span><br><span class="line">     + co_nmt_t 的服务生命周期</span><br><span class="line">     + C 到 C++ 的事件适配</span><br><span class="line">     + 明确的锁与异步执行语义</span><br></pre></td></tr></table></figure><p>掌握这一模型后，再阅读 <code>Master</code>、<code>Slave</code>、<code>Driver</code> 或某个 PDO&#x2F;SDO service 时，就能先判断代码位于哪一层：是协议状态机、网络适配、对象字典访问，还是应用回调装配，而不会把所有行为都归到 <code>Node</code> 本身。</p><h2 id="参考资料与版本说明"><a href="#参考资料与版本说明" class="headerlink" title="参考资料与版本说明"></a>参考资料与版本说明</h2><ul><li>Lely core GitLab：<a href="https://gitlab.com/lely_industries/lely-core/-/tree/master?ref_type=heads">https://gitlab.com/lely_industries/lely-core/-/tree/master?ref_type=heads</a></li><li><code>node.hpp</code> 源码：<a href="https://lely_industries.gitlab.io/lely-core/doxygen/node_8hpp_source.html">https://lely_industries.gitlab.io/lely-core/doxygen/node_8hpp_source.html</a></li><li><code>node.cpp</code> 源码：<a href="https://lely_industries.gitlab.io/lely-core/doxygen/node_8cpp_source.html">https://lely_industries.gitlab.io/lely-core/doxygen/node_8cpp_source.html</a></li><li><code>lely::canopen::Node</code> API：<a href="https://lely_industries.gitlab.io/lely-core/doxygen/classlely_1_1canopen_1_1Node.html">https://lely_industries.gitlab.io/lely-core/doxygen/classlely_1_1canopen_1_1Node.html</a></li><li>Lely CANopen Library overview：<a href="https://opensource.lely.com/canopen/docs/overview/">https://opensource.lely.com/canopen/docs/overview/</a></li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;meta name=&quot;referrer&quot; content=&quot;no-referrer&quot; /&gt;

&lt;h1 id=&quot;Lely-CANopen-coapp-Node：CAN-网络、NMT-服务与-C-事件回调的装配机制&quot;&gt;&lt;a href=&quot;#Lely-CANopen-coapp-Nod</summary>
      
    
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/categories/canopen/"/>
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/tags/canopen/"/>
    
  </entry>
  
  <entry>
    <title>Lely CANopen `co_nmt_t`：主从 NMT 总控机制、状态机与运行流程</title>
    <link href="https://wdfk-prog.space/posts/c0f1c80e/"/>
    <id>https://wdfk-prog.space/posts/c0f1c80e/</id>
    <published>2026-08-01T03:07:29.000Z</published>
    <updated>2026-08-01T03:08:12.790Z</updated>
    
    <content type="html"><![CDATA[<meta name="referrer" content="no-referrer" /><h1 id="Lely-CANopen-co-nmt-t：主从-NMT-总控机制、状态机与运行流程"><a href="#Lely-CANopen-co-nmt-t：主从-NMT-总控机制、状态机与运行流程" class="headerlink" title="Lely CANopen co_nmt_t：主从 NMT 总控机制、状态机与运行流程"></a>Lely CANopen <code>co_nmt_t</code>：主从 NMT 总控机制、状态机与运行流程</h1><p><img src="https://i-blog.csdnimg.cn/direct/19d2214ebcd2495c81655ed330c5c7c7.png" alt="在这里插入图片描述"></p><p>@[toc]</p><p><code>co_nmt_t</code> 不能只理解成“接收 CAN-ID <code>0x000</code> 后切换 NMT 状态”的对象。Lely 官方库概览直接把它定位为 CANopen 设备的核心服务管理器：它维护本节点 NMT 状态，并按状态创建、启动、停止或销毁 SDO、PDO、SYNC、TIME、EMCY、LSS 等服务。新建对象初始停留在 Initialisation，不立即通信，应用可以先安装回调，再通过 <code>Reset Node</code> 启动完整 boot-up 链路。[1]</p><p>结合 <code>src/co/nmt.c</code>、<code>include/lely/co/nmt.h</code> 与 <code>include/lely/co/nmt.hpp</code>，可以把它概括为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">co_nmt_t</span><br><span class="line">= 本节点 NMT 有限状态机</span><br><span class="line">+ 本地 CANopen 服务生命周期管理</span><br><span class="line">+ NMT 命令收发与发送抑制</span><br><span class="line">+ Heartbeat / Node Guarding / Life Guarding 总控</span><br><span class="line">+ 主站从节点状态表与启动策略</span><br><span class="line">+ 对象字典动态配置入口</span><br><span class="line">+ Boot Slave / Configuration Request 编排入口</span><br><span class="line">+ SYNC、EMCY、TPDO 事件的统一转接点</span><br></pre></td></tr></table></figure><hr><h2 id="1-co-nmt-t-在-Lely-全栈中的位置"><a href="#1-co-nmt-t-在-Lely-全栈中的位置" class="headerlink" title="1. co_nmt_t 在 Lely 全栈中的位置"></a>1. <code>co_nmt_t</code> 在 Lely 全栈中的位置</h2><p>下面这张图重点观察两类关系：谁向 <code>co_nmt_t</code> 输入事件，以及 <code>co_nmt_t</code> 具体管理哪些对象。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line">flowchart TB</span><br><span class="line">    App[&quot;应用层或 coapp Node&quot;] --&gt; Nmt[&quot;co_nmt_t&lt;br/&gt;NMT 总控对象&quot;]</span><br><span class="line">    Dev[&quot;co_dev_t&lt;br/&gt;对象字典与 DCF&quot;] --&gt; Nmt</span><br><span class="line">    Net[&quot;can_net_t&lt;br/&gt;CAN receiver 与逻辑 timer&quot;] --&gt; Nmt</span><br><span class="line"></span><br><span class="line">    Nmt --&gt; Fsm[&quot;NMT 状态机&quot;]</span><br><span class="line">    Nmt --&gt; Srv[&quot;co_nmt_srv&lt;br/&gt;本地服务管理器&quot;]</span><br><span class="line">    Nmt --&gt; Ec[&quot;Heartbeat 与 Guarding&quot;]</span><br><span class="line">    Nmt --&gt; Slaves[&quot;slaves 1..127&lt;br/&gt;主站远端节点状态&quot;]</span><br><span class="line">    Nmt --&gt; Boot[&quot;co_nmt_boot_t&lt;br/&gt;单从站启动编排&quot;]</span><br><span class="line">    Nmt --&gt; Cfg[&quot;co_nmt_cfg_t&lt;br/&gt;远端配置编排&quot;]</span><br><span class="line"></span><br><span class="line">    Srv --&gt; Ssdo[&quot;Server-SDO&quot;]</span><br><span class="line">    Srv --&gt; Csdo[&quot;Client-SDO&quot;]</span><br><span class="line">    Srv --&gt; Pdo[&quot;RPDO 与 TPDO&quot;]</span><br><span class="line">    Srv --&gt; Aux[&quot;SYNC TIME EMCY LSS&quot;]</span><br><span class="line"></span><br><span class="line">    Net --&gt; Rx0[&quot;CAN-ID 0x000 NMT command&quot;]</span><br><span class="line">    Net --&gt; Rx7[&quot;CAN-ID 0x700 + Node-ID&quot;]</span><br><span class="line">    Net --&gt; Timers[&quot;CAN logical timers&quot;]</span><br><span class="line">    Rx0 --&gt; Nmt</span><br><span class="line">    Rx7 --&gt; Nmt</span><br><span class="line">    Timers --&gt; Nmt</span><br></pre></td></tr></table></figure><p>这体现了 Lely CANopen 的被动架构：<code>co_nmt_t</code> 不创建线程、不读 SocketCAN、不直接读取操作系统时钟。外部 I&#x2F;O 桥接层把 CAN 帧和时间推进交给 <code>can_net_t</code>，<code>can_net_t</code> 再调用 NMT 注册的 receiver&#x2F;timer 回调。</p><p><code>co_nmt_t</code> 同时依赖 <code>co_dev_t</code>。对象字典不是只在初始化时读取一次：<code>0x100C</code>、<code>0x100D</code>、<code>0x1016</code>、<code>0x1017</code>、<code>0x1F25</code>、<code>0x1F80</code>、<code>0x1F82</code> 的写操作会进入 NMT 安装的 download indication，从而实时改变内部 receiver、timer、角色或流程。</p><hr><h2 id="2-struct-co-nmt-如何划分职责"><a href="#2-struct-co-nmt-如何划分职责" class="headerlink" title="2. struct __co_nmt 如何划分职责"></a>2. <code>struct __co_nmt</code> 如何划分职责</h2><p><code>struct __co_nmt</code> 字段很多，但可以按职责分成五组。</p><h3 id="2-1-核心状态与配置快照"><a href="#2-1-核心状态与配置快照" class="headerlink" title="2.1 核心状态与配置快照"></a>2.1 核心状态与配置快照</h3><table><thead><tr><th>字段</th><th>作用</th></tr></thead><tbody><tr><td><code>net</code></td><td>CAN 逻辑网络，提供 receiver、timer 和发送入口</td></tr><tr><td><code>dev</code></td><td>本地设备对象字典</td></tr><tr><td><code>id</code></td><td>待在 Reset Communication 阶段应用的 Node-ID</td></tr><tr><td><code>dcf_node</code></td><td>应用参数 <code>0x2000..0x9FFF</code> 的 concise DCF 快照</td></tr><tr><td><code>dcf_comm</code></td><td>通信参数 <code>0x1000..0x1FFF</code> 的 concise DCF 快照</td></tr><tr><td><code>state</code></td><td>当前内部状态对象指针</td></tr><tr><td><code>startup</code></td><td>对象 <code>0x1F80 NMT startup</code> 缓存</td></tr><tr><td><code>master</code></td><td>当前是否以 NMT master 运行</td></tr><tr><td><code>reset</code></td><td>当前启动流程是否由 Reset Communication 触发</td></tr></tbody></table><p><code>dcf_node</code> 和 <code>dcf_comm</code> 是复位语义的关键。Lely 在 NMT 对象初始化时先把对象字典当前值写成两份内存 concise DCF；后续 Reset Node&#x2F;Reset Communication 再回放相应范围，而不是重新解析外部 EDS&#x2F;DCF 文件。</p><h3 id="2-2-本地服务生命周期"><a href="#2-2-本地服务生命周期" class="headerlink" title="2.2 本地服务生命周期"></a>2.2 本地服务生命周期</h3><p><code>struct co_nmt_srv srv</code> 持有或管理本地 CANopen 服务，例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">SSDO / CSDO</span><br><span class="line">RPDO / TPDO</span><br><span class="line">SYNC / TIME</span><br><span class="line">EMCY / LSS</span><br></pre></td></tr></table></figure><p>NMT 状态进入函数只决定目标服务掩码，具体对象的创建、启动、停止和销毁委托给 <code>co_nmt_srv_set()</code>。</p><p>源码定义的服务集合是：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> CO_NMT_STOP_SRV  CO_NMT_SRV_LSS</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> CO_NMT_PREOP_SRV \</span></span><br><span class="line"><span class="meta">    (CO_NMT_STOP_SRV | CO_NMT_SRV_SDO | CO_NMT_SRV_SYNC \</span></span><br><span class="line"><span class="meta">     | CO_NMT_SRV_TIME | CO_NMT_SRV_EMCY)</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> CO_NMT_START_SRV \</span></span><br><span class="line"><span class="meta">    (CO_NMT_PREOP_SRV | CO_NMT_SRV_PDO)</span></span><br></pre></td></tr></table></figure><p>这比“Operational 才能通信”更精确：</p><table><thead><tr><th>NMT 状态</th><th>本地服务集合</th></tr></thead><tbody><tr><td>Reset Application &#x2F; Reset Communication</td><td>全部关闭；Reset Communication 后临时只启用 LSS</td></tr><tr><td>Pre-operational</td><td>LSS、SDO、SYNC、TIME、EMCY；不启用 PDO</td></tr><tr><td>Operational</td><td>Pre-operational 全部服务，再加 PDO</td></tr><tr><td>Stopped</td><td>只保留 LSS</td></tr></tbody></table><h3 id="2-3-NMT-command-与错误控制"><a href="#2-3-NMT-command-与错误控制" class="headerlink" title="2.3 NMT command 与错误控制"></a>2.3 NMT command 与错误控制</h3><table><thead><tr><th>字段</th><th>作用</th></tr></thead><tbody><tr><td><code>recv_000</code></td><td>接收 CAN-ID <code>0x000</code> 的 NMT command</td></tr><tr><td><code>recv_700</code></td><td>本节点侧 Node Guarding RTR receiver；master 的远端 boot-up&#x2F;guarding 使用每个 <code>slave-&gt;recv</code>，两者共用 <code>co_nmt_recv_700()</code> 回调</td></tr><tr><td><code>ec_timer</code></td><td>Heartbeat producer 或 Life Guarding 定时器</td></tr><tr><td><code>st</code></td><td>当前 NMT 状态，最高位还可携带 toggle bit</td></tr><tr><td><code>gt</code> &#x2F; <code>ltf</code></td><td>Guard time 与 Life time factor</td></tr><tr><td><code>ms</code></td><td>Producer heartbeat time</td></tr><tr><td><code>hbs</code> &#x2F; <code>nhb</code></td><td>每个 <code>0x1016</code> 子项对应的 Heartbeat consumer</td></tr></tbody></table><p>Heartbeat producer 和 Life Guarding 共用 <code>ec_timer</code>。当 <code>0x1017</code> 非零时，Heartbeat producer 优先，Life Guarding 被抑制。</p><h3 id="2-4-主站侧远端节点管理"><a href="#2-4-主站侧远端节点管理" class="headerlink" title="2.4 主站侧远端节点管理"></a>2.4 主站侧远端节点管理</h3><p>启用 master 功能时，<code>co_nmt_t</code> 内含 <code>slaves[CO_NUM_NODES]</code>。每个元素记录：</p><ul><li><code>0x1F81</code> assignment；</li><li>期望状态 <code>est</code> 与接收状态 <code>rst</code>；</li><li>boot-up 是否收到；</li><li>Boot Slave 是否进行&#x2F;完成及错误字母；</li><li>Configuration Request 是否进行；</li><li>Node Guarding 周期、生命因子和超时计数；</li><li>本节点对应的 receiver、timer、boot&#x2F;cfg 子对象。</li></ul><p>这说明 <code>co_nmt_t</code> 既是本节点状态机，也是主站网络级状态表。</p><h3 id="2-5-回调与事件桥接"><a href="#2-5-回调与事件桥接" class="headerlink" title="2.5 回调与事件桥接"></a>2.5 回调与事件桥接</h3><p>NMT 对象保存 command、heartbeat、guarding、state、boot、configuration、SDO progress、SYNC 等回调。其默认回调通常再调用 <code>co_nmt_on_*()</code>，形成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">底层协议事件</span><br><span class="line">    -&gt; indication callback</span><br><span class="line">    -&gt; 默认 handler</span><br><span class="line">    -&gt; co_nmt_on_* 策略函数</span><br><span class="line">    -&gt; EMCY / reset node / 状态迁移 / 用户回调</span><br></pre></td></tr></table></figure><p>此外，<code>co_nmt_t</code> 把 <code>co_dev_t</code> 的 TPDO event indication 接到 <code>co_tpdo_event()</code>；锁定期间用 bitmask 合并重复事件，解锁时再逐个触发，避免对象批量更新期间立即发送多个不完整 TPDO。</p><hr><h2 id="3-状态机不是-switch-大循环，而是状态对象表"><a href="#3-状态机不是-switch-大循环，而是状态对象表" class="headerlink" title="3. 状态机不是 switch 大循环，而是状态对象表"></a>3. 状态机不是 <code>switch</code> 大循环，而是状态对象表</h2><p><code>nmt.c</code> 用不透明的 <code>co_nmt_state_t</code> 表示状态，每个状态对象包含：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> __<span class="title">co_nmt_state</span> &#123;</span></span><br><span class="line">    <span class="type">co_nmt_state_t</span> *(*on_enter)(<span class="type">co_nmt_t</span> *nmt);</span><br><span class="line">    <span class="type">co_nmt_state_t</span> *(*on_cs)(<span class="type">co_nmt_t</span> *nmt, <span class="type">co_unsigned8_t</span> cs);</span><br><span class="line">    <span class="type">co_nmt_state_t</span> *(*on_boot)(<span class="type">co_nmt_t</span> *nmt,</span><br><span class="line">            <span class="type">co_unsigned8_t</span> id, <span class="type">co_unsigned8_t</span> st, <span class="type">char</span> es);</span><br><span class="line">    <span class="type">void</span> (*on_leave)(<span class="type">co_nmt_t</span> *nmt);</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>不同构建配置下，<code>on_boot</code> 可被条件编译移除。状态对象包括：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">initializing</span><br><span class="line">reset application</span><br><span class="line">reset communication</span><br><span class="line">boot-up</span><br><span class="line">pre-operational</span><br><span class="line">operational</span><br><span class="line">stopped</span><br></pre></td></tr></table></figure><h3 id="3-1-co-nmt-enter-支持连续自动迁移"><a href="#3-1-co-nmt-enter-支持连续自动迁移" class="headerlink" title="3.1 co_nmt_enter() 支持连续自动迁移"></a>3.1 <code>co_nmt_enter()</code> 支持连续自动迁移</h3><p>核心代码等价于：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">while</span> (next) &#123;</span><br><span class="line">    prev = nmt-&gt;state;</span><br><span class="line">    nmt-&gt;state = next;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (prev &amp;&amp; prev-&gt;on_leave)</span><br><span class="line">        prev-&gt;on_leave(nmt);</span><br><span class="line"></span><br><span class="line">    next = next-&gt;on_enter ? next-&gt;on_enter(nmt) : <span class="literal">NULL</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>因此，一个 entry handler 可以直接返回下一个状态。外部只触发一次 <code>Reset Node</code>，内部可连续执行：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">Reset Application</span><br><span class="line">    -&gt; Reset Communication</span><br><span class="line">    -&gt; Boot-up</span><br><span class="line">    -&gt; Pre-operational</span><br><span class="line">    -&gt; 根据 0x1F80 决定是否自动 Operational</span><br></pre></td></tr></table></figure><p>这不是线程里的轮询循环，而是在同一调用栈内按 entry handler 返回值连续推进。只有遇到必须等待外部异步事件的步骤，例如 LSS 完成或 mandatory slave boot 完成，entry handler 才返回 <code>NULL</code> 停住；后续由 <code>co_nmt_lss_con()</code> 或 <code>co_nmt_boot_con()</code> 重新推进。</p><h3 id="3-2-状态关系"><a href="#3-2-状态关系" class="headerlink" title="3.2 状态关系"></a>3.2 状态关系</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line">stateDiagram-v2</span><br><span class="line">    [*] --&gt; Initializing</span><br><span class="line">    Initializing --&gt; ResetApplication: Reset Node</span><br><span class="line">    ResetApplication --&gt; ResetCommunication: entry 自动返回</span><br><span class="line">    ResetCommunication --&gt; Bootup: 无需等待 LSS</span><br><span class="line">    ResetCommunication --&gt; ResetCommunication: Reset Communication</span><br><span class="line">    ResetCommunication --&gt; ResetApplication: Reset Node</span><br><span class="line">    Bootup --&gt; PreOperational: Node-ID 有效并发送 boot-up</span><br><span class="line">    PreOperational --&gt; Operational: Start 或 startup 自动启动</span><br><span class="line">    PreOperational --&gt; Stopped: Stop</span><br><span class="line">    Operational --&gt; PreOperational: Enter Pre-operational</span><br><span class="line">    Operational --&gt; Stopped: Stop</span><br><span class="line">    Stopped --&gt; PreOperational: Enter Pre-operational</span><br><span class="line">    Stopped --&gt; Operational: Start</span><br><span class="line">    PreOperational --&gt; ResetApplication: Reset Node</span><br><span class="line">    Operational --&gt; ResetApplication: Reset Node</span><br><span class="line">    Stopped --&gt; ResetApplication: Reset Node</span><br><span class="line">    PreOperational --&gt; ResetCommunication: Reset Communication</span><br><span class="line">    Operational --&gt; ResetCommunication: Reset Communication</span><br><span class="line">    Stopped --&gt; ResetCommunication: Reset Communication</span><br></pre></td></tr></table></figure><p>内部的 <code>Reset Application</code> 和 <code>Reset Communication</code> 状态值分别是 <code>0x06</code>、<code>0x07</code>。它们是状态机内部子状态，不是 Heartbeat 正常周期广播的运行状态。</p><hr><h2 id="4-Reset-Node-到-Operational-的完整运行链"><a href="#4-Reset-Node-到-Operational-的完整运行链" class="headerlink" title="4. Reset Node 到 Operational 的完整运行链"></a>4. <code>Reset Node</code> 到 Operational 的完整运行链</h2><h3 id="4-1-NMT-对象创建后为什么不立即启动"><a href="#4-1-NMT-对象创建后为什么不立即启动" class="headerlink" title="4.1 NMT 对象创建后为什么不立即启动"></a>4.1 NMT 对象创建后为什么不立即启动</h3><p><code>__co_nmt_init()</code> 会完成资源装配：</p><ol><li>保存 <code>net</code> 和 <code>dev</code>；</li><li>记录待应用 Node-ID；</li><li>生成应用参数和通信参数 concise DCF 快照；</li><li>初始化 <code>co_nmt_srv</code>；</li><li>创建 <code>recv_000</code>、<code>recv_700</code>、<code>ec_timer</code>；</li><li>master 构建下创建 NMT command buffer、inhibit timer 和每个远端节点的 receiver&#x2F;timer；</li><li>安装对象字典 download indication；</li><li>安装 TPDO&#x2F;SAM-MPDO event indication；</li><li>进入 <code>initializing</code>。</li></ol><p>此时不会自动创建 SDO&#x2F;PDO 等服务，也不会发送 boot-up。只有收到或本地提交 <code>Reset Node</code>，<code>co_nmt_init_on_cs()</code> 才返回 <code>reset application</code>。</p><p>这样设计使应用可以在网络通信开始前先安装所有回调。</p><h3 id="4-2-Reset-Application"><a href="#4-2-Reset-Application" class="headerlink" title="4.2 Reset Application"></a>4.2 Reset Application</h3><p>进入 <code>co_nmt_reset_node_on_enter()</code> 后执行：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">停止主站从节点管理</span><br><span class="line">关闭所有本地 CANopen 服务</span><br><span class="line">销毁 Heartbeat consumer</span><br><span class="line">关闭错误控制</span><br><span class="line">停止接收 NMT command</span><br><span class="line">回放 dcf_node，恢复 0x2000..0x9FFF 应用参数</span><br><span class="line">更新内部状态并通知回调</span><br><span class="line">自动返回 Reset Communication</span><br></pre></td></tr></table></figure><p>Reset Node 不只是 MCU 复位的抽象命令。在当前 Lely 对象内，它明确恢复初始化时保存的应用参数快照，然后继续做通信复位。</p><h3 id="4-3-Reset-Communication"><a href="#4-3-Reset-Communication" class="headerlink" title="4.3 Reset Communication"></a>4.3 Reset Communication</h3><p>进入 <code>co_nmt_reset_comm_on_enter()</code> 后执行：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">再次确保 slave management、本地服务、Heartbeat、错误控制均关闭</span><br><span class="line">停止 NMT command receiver</span><br><span class="line">回放 dcf_comm，恢复 0x1000..0x1FFF 通信参数</span><br><span class="line">若 pending Node-ID 改变：更新 co_dev_t 并重建通信 DCF 快照</span><br><span class="line">读取 0x1F80</span><br><span class="line">由 bit0 决定 master/slave 角色</span><br><span class="line">设置 reset 标志</span><br><span class="line">slave 角色重新监听 CAN-ID 0x000</span><br><span class="line">临时只启用 LSS</span><br><span class="line">若 master 配置了 LSS callback：等待 LSS completion</span><br><span class="line">否则进入 Boot-up</span><br></pre></td></tr></table></figure><p>Node-ID 修改没有立即改写运行中的所有服务，而是在 Reset Communication 阶段集中应用。这样可避免 receiver、COB-ID、对象字典相对 Node-ID 值和服务实例处于半更新状态。</p><h3 id="4-4-Boot-up"><a href="#4-4-Boot-up" class="headerlink" title="4.4 Boot-up"></a>4.4 Boot-up</h3><p><code>co_nmt_bootup_on_enter()</code> 首先检查 Node-ID：</p><ul><li>Node-ID 为 <code>0xFF</code>：认为尚未配置，停留在当前阶段，不进入 Pre-operational；</li><li>Node-ID 有效：初始化错误控制和 Heartbeat consumer，发送一帧 boot-up，再返回 Pre-operational。</li></ul><p>Boot-up 帧：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">CAN-ID = 0x700 + Node-ID</span><br><span class="line">DLC    = 1</span><br><span class="line">Data0  = 0x00</span><br></pre></td></tr></table></figure><h3 id="4-5-Pre-operational-与-startup"><a href="#4-5-Pre-operational-与-startup" class="headerlink" title="4.5 Pre-operational 与 startup"></a>4.5 Pre-operational 与 startup</h3><p>进入 Pre-operational 后：</p><ol><li>启用除 PDO 外的服务；</li><li>更新本节点状态为 <code>0x7F</code>；</li><li>触发 state&#x2F;command indication；</li><li>进入 <code>co_nmt_startup()</code>。</li></ol><p>若当前为 slave，startup 只根据 <code>reset</code> 与 <code>0x1F80 bit2</code> 判断是否自动进入 Operational。</p><p>若当前为 master，startup 还会初始化远端从节点表、发送 Reset Communication、启动 Boot Slave 流程，并等待 mandatory slave 完成。</p><h3 id="4-6-完整时序"><a href="#4-6-完整时序" class="headerlink" title="4.6 完整时序"></a>4.6 完整时序</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant App as Application</span><br><span class="line">    participant Nmt as co_nmt_t</span><br><span class="line">    participant Dev as co_dev_t</span><br><span class="line">    participant Srv as co_nmt_srv</span><br><span class="line">    participant Net as can_net_t</span><br><span class="line">    participant Sub as Remote slaves</span><br><span class="line"></span><br><span class="line">    App-&gt;&gt;Nmt: co_nmt_cs_ind Reset Node</span><br><span class="line">    Nmt-&gt;&gt;Srv: disable all services</span><br><span class="line">    Nmt-&gt;&gt;Dev: replay application DCF</span><br><span class="line">    Nmt-&gt;&gt;Dev: replay communication DCF</span><br><span class="line">    Nmt-&gt;&gt;Dev: read 0x1F80 and role</span><br><span class="line">    Nmt-&gt;&gt;Srv: enable LSS only</span><br><span class="line">    Nmt-&gt;&gt;Net: start error control and heartbeat consumers</span><br><span class="line">    Nmt-&gt;&gt;Net: send boot-up 0x700 plus Node-ID</span><br><span class="line">    Nmt-&gt;&gt;Srv: enable Pre-operational services</span><br><span class="line"></span><br><span class="line">    alt NMT master</span><br><span class="line">        Nmt-&gt;&gt;Sub: Reset Communication</span><br><span class="line">        Nmt-&gt;&gt;Sub: start Boot Slave workflows</span><br><span class="line">        Sub--&gt;&gt;Nmt: boot completion callbacks</span><br><span class="line">        Nmt-&gt;&gt;Srv: enable PDO after startup policy passes</span><br><span class="line">    else NMT slave</span><br><span class="line">        Nmt-&gt;&gt;Nmt: evaluate 0x1F80 auto-start policy</span><br><span class="line">        Nmt-&gt;&gt;Srv: enable PDO when Operational</span><br><span class="line">    end</span><br></pre></td></tr></table></figure><hr><h2 id="5-NMT-command-的接收与发送不是对称实现"><a href="#5-NMT-command-的接收与发送不是对称实现" class="headerlink" title="5. NMT command 的接收与发送不是对称实现"></a>5. NMT command 的接收与发送不是对称实现</h2><h3 id="5-1-从站接收：co-nmt-recv-000"><a href="#5-1-从站接收：co-nmt-recv-000" class="headerlink" title="5.1 从站接收：co_nmt_recv_000()"></a>5.1 从站接收：<code>co_nmt_recv_000()</code></h3><p>Reset Communication 阶段，只有 slave 角色启动 <code>recv_000</code>，监听 CAN-ID <code>0x000</code>。收到报文后会检查：</p><ul><li>帧是否为预期格式；</li><li>DLC 是否至少为 2；</li><li><code>data[1]</code> 是否为 <code>0</code> 或本节点 Node-ID；</li><li>command specifier 是否受支持。</li></ul><p>有效命令进入 <code>co_nmt_cs_ind()</code>，再由当前状态的 <code>on_cs()</code> 决定下一状态。</p><p>处理顺序为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">CAN frame</span><br><span class="line">  -&gt; co_nmt_recv_000</span><br><span class="line">  -&gt; co_nmt_cs_ind</span><br><span class="line">  -&gt; co_nmt_emit_cs</span><br><span class="line">  -&gt; current_state.on_cs</span><br><span class="line">  -&gt; co_nmt_enter</span><br><span class="line">  -&gt; state entry side effects</span><br><span class="line">  -&gt; cs_ind callback</span><br></pre></td></tr></table></figure><p><code>nmt.h</code> 明确说明，command indication 在状态切换之后调用，而且回调仍处于 NMT 状态迁移内部；不能在该回调中再次对本节点调用 <code>co_nmt_cs_ind()</code> 或 <code>co_nmt_cs_req()</code>，否则会形成重入状态迁移。</p><h3 id="5-2-主站发送：co-nmt-cs-req"><a href="#5-2-主站发送：co-nmt-cs-req" class="headerlink" title="5.2 主站发送：co_nmt_cs_req()"></a>5.2 主站发送：<code>co_nmt_cs_req()</code></h3><p>只有 <code>master</code> 为真时才能调用：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">co_nmt_cs_req(nmt, command, target_id);</span><br></pre></td></tr></table></figure><p>处理分为两条路径：</p><ul><li>目标是本节点：不发 CAN 帧，直接调用 <code>co_nmt_cs_ind()</code>；</li><li>目标是远端或广播：生成 <code>0x000</code>、2 字节 NMT 帧，写入 <code>can_buf</code>，再由 <code>cs_timer</code> 发送。</li></ul><p>主站发送不是每次调用都立即 <code>can_net_send()</code>。对象 <code>0x102A NMT inhibit time</code> 用于限制相邻 NMT command 的最小间隔，<code>co_nmt_cs_timer()</code> 会：</p><ol><li>查看队首帧；</li><li>若尚未到 <code>inhibit</code> 截止时间，则重新启动定时器；</li><li>到期后发送；</li><li>更新目标从站的期望状态 <code>est</code>；</li><li>计算下一次允许发送的时间；</li><li>继续处理缓冲区。</li></ol><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    Req[&quot;co_nmt_cs_req&quot;] --&gt; Local&#123;&quot;目标是本节点&quot;&#125;</span><br><span class="line">    Local -- &quot;是&quot; --&gt; Ind[&quot;co_nmt_cs_ind&lt;br/&gt;直接状态迁移&quot;]</span><br><span class="line">    Local -- &quot;否&quot; --&gt; Frame[&quot;构造 CAN-ID 0x000 帧&quot;]</span><br><span class="line">    Frame --&gt; Buf[&quot;写入 can_buf&quot;]</span><br><span class="line">    Buf --&gt; Timer[&quot;co_nmt_cs_timer&quot;]</span><br><span class="line">    Timer --&gt; Check&#123;&quot;0x102A inhibit 已到期&quot;&#125;</span><br><span class="line">    Check -- &quot;否&quot; --&gt; Wait[&quot;重新设置 CAN timer&quot;]</span><br><span class="line">    Check -- &quot;是&quot; --&gt; Send[&quot;can_net_send&quot;]</span><br><span class="line">    Send --&gt; Est[&quot;更新 slave expected state&quot;]</span><br></pre></td></tr></table></figure><p>这也解释了为什么连续调用多个 NMT request 时，函数返回成功不等于所有帧已经物理发送完成；它只表示请求已被接受并进入内部发送链路。</p><hr><h2 id="6-Heartbeat、Node-Guarding-与-Life-Guarding-如何汇入-NMT"><a href="#6-Heartbeat、Node-Guarding-与-Life-Guarding-如何汇入-NMT" class="headerlink" title="6. Heartbeat、Node Guarding 与 Life Guarding 如何汇入 NMT"></a>6. Heartbeat、Node Guarding 与 Life Guarding 如何汇入 NMT</h2><h3 id="6-1-Producer-和-Life-Guarding-共用-ec-timer"><a href="#6-1-Producer-和-Life-Guarding-共用-ec-timer" class="headerlink" title="6.1 Producer 和 Life Guarding 共用 ec_timer"></a>6.1 Producer 和 Life Guarding 共用 <code>ec_timer</code></h3><p><code>co_nmt_ec_update()</code> 根据三个配置值决定模式：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">0x1017 != 0</span><br><span class="line">    -&gt; Heartbeat producer</span><br><span class="line"></span><br><span class="line">0x1017 == 0 且 0x100C * 0x100D != 0</span><br><span class="line">    -&gt; Life Guarding</span><br><span class="line"></span><br><span class="line">两者都为 0</span><br><span class="line">    -&gt; 停止 ec_timer</span><br></pre></td></tr></table></figure><p>Heartbeat production 优先于 Life Guarding。master 角色还会禁止自身 Life Guarding。</p><p><code>co_nmt_ec_timer()</code> 到期时：</p><ul><li>Heartbeat 模式：发送当前状态，去掉 toggle bit；</li><li>Life Guarding 模式：报告 life guarding error。</li></ul><h3 id="6-2-Heartbeat-consumer-是一项配置一个对象"><a href="#6-2-Heartbeat-consumer-是一项配置一个对象" class="headerlink" title="6.2 Heartbeat consumer 是一项配置一个对象"></a>6.2 Heartbeat consumer 是一项配置一个对象</h3><p><code>co_nmt_hb_init()</code> 读取 <code>0x1016:00</code> 的子项数量，为每个子项创建 <code>co_nmt_hb_t</code>，再解析：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">bits 16..23 = producer Node-ID</span><br><span class="line">bits 0..15  = consumer timeout ms</span><br></pre></td></tr></table></figure><p>每个 <code>co_nmt_hb_t</code> 独立注册 receiver 和 timer。它检测到 timeout 或 state change 后回调 <code>co_nmt_hb_ind()</code>；NMT 再调用用户 heartbeat indication，并在状态变化时继续发出统一 state indication。</p><p>动态写 <code>0x1016</code> 时，<code>co_1016_dn_ind()</code> 还会：</p><ul><li>禁止写 sub-index 0；</li><li>检查 sub-index 范围；</li><li>禁止两个启用的 consumer 使用相同 Node-ID；</li><li>boot&#x2F;config 期间临时把实际监控时间设为 0；</li><li>更新对象字典后立即重配对应 <code>co_nmt_hb_t</code>。</li></ul><h3 id="6-3-主站的每节点-receiver-共用-co-nmt-recv-700-处理-boot-up-和-guarding"><a href="#6-3-主站的每节点-receiver-共用-co-nmt-recv-700-处理-boot-up-和-guarding" class="headerlink" title="6.3 主站的每节点 receiver 共用 co_nmt_recv_700() 处理 boot-up 和 guarding"></a>6.3 主站的每节点 receiver 共用 <code>co_nmt_recv_700()</code> 处理 boot-up 和 guarding</h3><p>主站接收 <code>0x700 + id</code> 后：</p><ul><li><code>data[0] == 0x00</code>：记录 boot-up，预期下一状态为 Pre-operational，并通知 state callback；</li><li>boot&#x2F;config 进行中：忽略普通 guarding 状态处理；</li><li>Node Guarding 启用：校验 toggle bit、清零未响应 RTR 次数、检测 timeout 恢复与异常状态变化。</li></ul><p>默认 heartbeat&#x2F;node guarding handler 会进一步调用 <code>co_nmt_node_err_ind()</code>。对 mandatory slave，<code>0x1F80</code> 的策略位可触发全网 Stop、全网 Reset Node，或只复位故障节点。</p><hr><h2 id="7-对象字典不是静态配置文件，而是运行期控制面"><a href="#7-对象字典不是静态配置文件，而是运行期控制面" class="headerlink" title="7. 对象字典不是静态配置文件，而是运行期控制面"></a>7. 对象字典不是静态配置文件，而是运行期控制面</h2><p>NMT 初始化时会把 download indication 安装到以下对象：</p><table><thead><tr><th align="right">对象</th><th>运行期作用</th></tr></thead><tbody><tr><td align="right"><code>0x100C</code></td><td>更新 Guard time，并重算错误控制模式</td></tr><tr><td align="right"><code>0x100D</code></td><td>更新 Life time factor，并重算错误控制模式</td></tr><tr><td align="right"><code>0x1016</code></td><td>重配某个 Heartbeat consumer</td></tr><tr><td align="right"><code>0x1017</code></td><td>更新 Producer heartbeat time</td></tr><tr><td align="right"><code>0x1F25</code></td><td>发起 Configuration Request</td></tr><tr><td align="right"><code>0x1F80</code></td><td>更新 NMT startup 策略缓存</td></tr><tr><td align="right"><code>0x1F82</code></td><td>将对象写操作转换为 NMT command request</td></tr></tbody></table><p>这些 callback 的共同模式是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">解析 co_sdo_req</span><br><span class="line">  -&gt; 校验类型、sub-index、值域和当前角色</span><br><span class="line">  -&gt; 比较新旧值</span><br><span class="line">  -&gt; 先更新内部缓存或流程状态</span><br><span class="line">  -&gt; co_sub_dn 写入对象字典</span><br><span class="line">  -&gt; 重配 receiver/timer/service 或发起异步流程</span><br></pre></td></tr></table></figure><p>因此，对象字典写入并非简单内存赋值。通过普通 SDO 下载这些对象时，会立即影响 NMT 运行状态；通过绕过 indication 的直接 setter 修改值，则可能只改变 OD 存储，不会同步重配 NMT 内部资源。</p><h3 id="7-1-0x1F82-Request-NMT"><a href="#7-1-0x1F82-Request-NMT" class="headerlink" title="7.1 0x1F82 Request NMT"></a>7.1 <code>0x1F82 Request NMT</code></h3><p>master 侧写 <code>0x1F82:&lt;node-id&gt;</code> 可以转换成相应 NMT request。对象字典因此成为 NMT 控制面的一个标准化入口，而不是额外复制一套状态。</p><h3 id="7-2-0x1F25-Configuration-request"><a href="#7-2-0x1F25-Configuration-request" class="headerlink" title="7.2 0x1F25 Configuration request"></a>7.2 <code>0x1F25 Configuration request</code></h3><p>向对应子项写入特定控制值后，NMT 校验节点是否在 <code>0x1F81</code> 网络列表中，并检查 <code>0x1F20/0x1F22</code> 是否提供配置来源，再启动 <code>co_nmt_cfg_req()</code>。</p><p>在配置期间，对应 Heartbeat consumer 被临时禁用；流程完成后再按 <code>0x1016</code> 原值恢复，以避免从站在 reset、下载 DCF 或重启过程中被误判 timeout。</p><hr><h2 id="8-主站-startup-是网络级编排，不只是本节点进-Operational"><a href="#8-主站-startup-是网络级编排，不只是本节点进-Operational" class="headerlink" title="8. 主站 startup 是网络级编排，不只是本节点进 Operational"></a>8. 主站 startup 是网络级编排，不只是本节点进 Operational</h2><p><code>0x1F80 NMT startup</code> 决定主站角色和自动启动策略，<code>0x1F81</code> 为每个从站提供 assignment。进入 Pre-operational 后，master 执行：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">co_nmt_slaves_init</span><br><span class="line">  -&gt; 读取每个 0x1F81 assignment</span><br><span class="line">  -&gt; 为远端节点启动接收和状态管理</span><br><span class="line">  -&gt; 按 keep-alive 策略发送 Reset Communication</span><br><span class="line">  -&gt; co_nmt_slaves_boot</span><br><span class="line">  -&gt; 每个节点创建 co_nmt_boot_t</span><br><span class="line">  -&gt; 等待 mandatory slaves</span><br><span class="line">  -&gt; 成功后按 0x1F80 策略启动从站和本节点</span><br></pre></td></tr></table></figure><p><code>co_nmt_boot_t</code> 负责单节点身份检查、SDO 访问、配置、程序下载和错误控制启动；<code>co_nmt_t</code> 负责：</p><ul><li>何时创建它；</li><li>哪些节点必须等待；</li><li>是否因 mandatory slave 失败而暂停 startup；</li><li>boot 完成后是否发送 Start；</li><li>是否进入本节点 Operational；</li><li>如何向应用报告结果。</li></ul><p><code>co_nmt_preop_on_boot()</code> 只在所有 mandatory slave 不再 boot 且没有 halt 时继续 <code>co_nmt_startup_slave()</code>。这意味着 optional slave 未完成不一定阻塞主站进入下一阶段，最终行为由 assignment 和 startup bit 共同决定。</p><hr><h2 id="9-SYNC、EMCY-与-PDO-为什么也由-NMT-暴露入口"><a href="#9-SYNC、EMCY-与-PDO-为什么也由-NMT-暴露入口" class="headerlink" title="9. SYNC、EMCY 与 PDO 为什么也由 NMT 暴露入口"></a>9. SYNC、EMCY 与 PDO 为什么也由 NMT 暴露入口</h2><h3 id="9-1-SYNC：先-TPDO，后-RPDO"><a href="#9-1-SYNC：先-TPDO，后-RPDO" class="headerlink" title="9.1 SYNC：先 TPDO，后 RPDO"></a>9.1 SYNC：先 TPDO，后 RPDO</h3><p><code>co_nmt_on_sync()</code> 的顺序是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">遍历所有 TPDO，调用 co_tpdo_sync</span><br><span class="line">遍历所有 RPDO，调用 co_rpdo_sync</span><br><span class="line">最后调用用户 sync_ind</span><br></pre></td></tr></table></figure><p>源码注释给出的原因是：如果同一对象同时映射到同步 TPDO 和 RPDO，应先发送上一同步窗口的值，再用当前收到的 RPDO 更新对象，避免时序竞争。</p><p>所以应用层收到 NMT 的 SYNC callback 时，同步 PDO 已经处理完成。</p><h3 id="9-2-EMCY：NMT-负责把通信错误连接到状态策略"><a href="#9-2-EMCY：NMT-负责把通信错误连接到状态策略" class="headerlink" title="9.2 EMCY：NMT 负责把通信错误连接到状态策略"></a>9.2 EMCY：NMT 负责把通信错误连接到状态策略</h3><p><code>co_nmt_on_err()</code> 先把错误压入 EMCY 服务；若错误码属于 <code>0x81xx</code> 通信错误，再调用 <code>co_nmt_comm_err_ind()</code>，读取 <code>0x1029:01</code> 决定：</p><ul><li>保持当前状态；</li><li>Operational 降到 Pre-operational；</li><li>进入 Stopped。</li></ul><p>这说明 EMCY 上报与 NMT 状态策略是两层：EMCY 记录&#x2F;发送错误，NMT 根据 Error behavior 对象决定通信状态变化。</p><h3 id="9-3-co-nmt-on-tpdo-event-lock-unlock-的实际调用链"><a href="#9-3-co-nmt-on-tpdo-event-lock-unlock-的实际调用链" class="headerlink" title="9.3 co_nmt_on_tpdo_event_lock() &#x2F; unlock() 的实际调用链"></a>9.3 <code>co_nmt_on_tpdo_event_lock()</code> &#x2F; <code>unlock()</code> 的实际调用链</h3><p>官方在 v2.1.0 的 TPDO event tracking 说明中明确指出：应用可以在批量修改对象字典时暂缓 TPDO event；这个“锁”允许嵌套，只有最后一次解锁才真正触发累计的 TPDO。[9]</p><p>这套机制不是从 <code>co_nmt_on_tpdo_event_lock()</code> 单独开始的。完整链路包含“事件桥注册、应用加锁、对象事件进入 NMT、最后一次解锁统一释放”四个阶段。</p><h4 id="9-3-1-NMT-初始化时先接管对象字典的-TPDO-event"><a href="#9-3-1-NMT-初始化时先接管对象字典的-TPDO-event" class="headerlink" title="9.3.1 NMT 初始化时先接管对象字典的 TPDO event"></a>9.3.1 NMT 初始化时先接管对象字典的 TPDO event</h4><p><code>__co_nmt_init()</code> 会把 NMT 的静态回调安装到 <code>co_dev_t</code>：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">co_dev_set_tpdo_event_ind(nmt-&gt;dev, &amp;co_nmt_tpdo_event_ind, nmt);</span><br></pre></td></tr></table></figure><p>之后应用调用 <code>co_dev_tpdo_event(dev, idx, subidx)</code>，或 C++ <code>Device::TpdoWriteEvent()</code> &#x2F; <code>WriteEvent()</code> 时，<code>co_dev_t</code> 会检查该子对象的 TPDO 映射，并对命中的 PDO 编号调用已注册的 indication：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">对象字典发生 TPDO event</span><br><span class="line">  -&gt; co_dev_tpdo_event(dev, idx, subidx)</span><br><span class="line">  -&gt; 查找映射了该对象的 TPDO</span><br><span class="line">  -&gt; dev-&gt;tpdo_event_ind(pdo_number, data)</span><br><span class="line">  -&gt; co_nmt_tpdo_event_ind(pdo_number, nmt)</span><br><span class="line">  -&gt; co_nmt_on_tpdo_event(nmt, pdo_number)</span><br></pre></td></tr></table></figure><p>因此，<code>co_nmt_on_tpdo_event()</code> 是对象字典事件进入 NMT&#x2F;TPDO 服务的统一入口，而 lock&#x2F;unlock 控制的是这个入口是否立即向下调用 <code>co_tpdo_event()</code>。</p><h4 id="9-3-2-谁会调用-lock-unlock"><a href="#9-3-2-谁会调用-lock-unlock" class="headerlink" title="9.3.2 谁会调用 lock&#x2F;unlock"></a>9.3.2 谁会调用 lock&#x2F;unlock</h4><p>C 接口可以由应用直接成对调用：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">co_nmt_on_tpdo_event_lock(nmt);</span><br><span class="line"><span class="comment">/* 连续更新多个会触发 TPDO event 的对象 */</span></span><br><span class="line">co_nmt_on_tpdo_event_unlock(nmt);</span><br></pre></td></tr></table></figure><p><code>nmt.hpp</code> 的 <code>CONMT</code> 只是直接转发：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">CONMT::onTPDOEventLock()</span><br><span class="line">  -&gt; co_nmt_on_tpdo_event_lock(this)</span><br><span class="line"></span><br><span class="line">CONMT::onTPDOEventUnlock()</span><br><span class="line">  -&gt; co_nmt_on_tpdo_event_unlock(this)</span><br></pre></td></tr></table></figure><p>在更高层的 coapp 中，<code>Node::TpdoEventMutex</code> 把这两个 C 接口包装成 <code>BasicLockable</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">Node::TpdoEventMutex::lock()</span><br><span class="line">  -&gt; co_nmt_on_tpdo_event_lock(node-&gt;nmt())</span><br><span class="line"></span><br><span class="line">Node::TpdoEventMutex::unlock()</span><br><span class="line">  -&gt; co_nmt_on_tpdo_event_unlock(node-&gt;nmt())</span><br></pre></td></tr></table></figure><p><code>Node</code> 暴露受保护成员 <code>tpdo_event_mutex</code>，供派生节点、主站或驱动在批量调用 <code>TpdoWriteEvent()</code> &#x2F; <code>WriteEvent()</code> 时使用。<code>BasicMaster::TpdoEventMutex</code> 还会先取得节点本身的 <code>BasicLockable</code>，再进入 <code>Node::TpdoEventMutex</code>，把 coapp 的线程串行化与 NMT 的 TPDO event 延期机制组合起来。[10][11][12]</p><p>这里必须区分两层“锁”：</p><ul><li><code>Node</code> &#x2F; <code>BasicMaster</code> 的 <code>BasicLockable</code> 负责访问串行化；</li><li><code>co_nmt_on_tpdo_event_lock()</code> 只增加延期计数，不是线程互斥原语。</li></ul><h4 id="9-3-3-加锁期间如何积累事件"><a href="#9-3-3-加锁期间如何积累事件" class="headerlink" title="9.3.3 加锁期间如何积累事件"></a>9.3.3 加锁期间如何积累事件</h4><p><code>co_nmt_on_tpdo_event_lock()</code> 的实现只有一个核心动作：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">nmt-&gt;tpdo_event_wait++;</span><br></pre></td></tr></table></figure><p>当 <code>co_nmt_on_tpdo_event()</code> 收到 PDO 编号时：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">tpdo_event_wait == 0</span><br><span class="line">  -&gt; 立即调用 co_tpdo_event(pdo)</span><br><span class="line"></span><br><span class="line">tpdo_event_wait &gt; 0</span><br><span class="line">  -&gt; 在 tpdo_event_mask 中置位</span><br><span class="line">  -&gt; 暂不调用 co_tpdo_event(pdo)</span><br></pre></td></tr></table></figure><p><code>tpdo_event_mask</code> 每个 TPDO 只占一个 bit。因此，同一 TPDO 在锁定期间被重复触发多次，会合并成一个待处理事件；不同 TPDO 则分别保留自己的 bit。参数 <code>n == 0</code> 表示对当前全部 TPDO 执行同样处理。</p><h4 id="9-3-4-最后一次-unlock-才释放延期事件"><a href="#9-3-4-最后一次-unlock-才释放延期事件" class="headerlink" title="9.3.4 最后一次 unlock 才释放延期事件"></a>9.3.4 最后一次 unlock 才释放延期事件</h4><p><code>co_nmt_on_tpdo_event_unlock()</code> 先递减计数：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (--nmt-&gt;tpdo_event_wait)</span><br><span class="line">    <span class="keyword">return</span>;</span><br></pre></td></tr></table></figure><p>只要仍有外层锁，函数立即返回。计数降到 <code>0</code> 后，它才遍历 <code>tpdo_event_mask</code>，对每个置位且仍然存在的 TPDO 调用一次 <code>co_tpdo_event()</code>，随后清除对应 bit。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    A[&quot;应用进入批量更新&quot;] --&gt; B[&quot;lock: tpdo_event_wait 加一&quot;]</span><br><span class="line">    B --&gt; C[&quot;WriteEvent 或 TpdoWriteEvent&quot;]</span><br><span class="line">    C --&gt; D[&quot;co_dev_tpdo_event&quot;]</span><br><span class="line">    D --&gt; E[&quot;co_nmt_on_tpdo_event&quot;]</span><br><span class="line">    E --&gt; F&#123;&quot;tpdo_event_wait 是否大于零&quot;&#125;</span><br><span class="line">    F -- &quot;是&quot; --&gt; G[&quot;在 tpdo_event_mask 中置位&quot;]</span><br><span class="line">    F -- &quot;否&quot; --&gt; H[&quot;立即调用 co_tpdo_event&quot;]</span><br><span class="line">    G --&gt; I[&quot;unlock: tpdo_event_wait 减一&quot;]</span><br><span class="line">    I --&gt; J&#123;&quot;计数是否归零&quot;&#125;</span><br><span class="line">    J -- &quot;否&quot; --&gt; K[&quot;继续等待外层 unlock&quot;]</span><br><span class="line">    J -- &quot;是&quot; --&gt; L[&quot;遍历并清除 bitmask&quot;]</span><br><span class="line">    L --&gt; M[&quot;每个延期 TPDO 调用一次 co_tpdo_event&quot;]</span><br></pre></td></tr></table></figure><p>完整调用关系可压缩为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">应用或 coapp RAII lock</span><br><span class="line">  -&gt; co_nmt_on_tpdo_event_lock</span><br><span class="line">  -&gt; tpdo_event_wait++</span><br><span class="line"></span><br><span class="line">对象写事件</span><br><span class="line">  -&gt; co_dev_tpdo_event</span><br><span class="line">  -&gt; co_nmt_tpdo_event_ind</span><br><span class="line">  -&gt; co_nmt_on_tpdo_event</span><br><span class="line">  -&gt; tpdo_event_mask 置位</span><br><span class="line"></span><br><span class="line">应用或 coapp RAII unlock</span><br><span class="line">  -&gt; co_nmt_on_tpdo_event_unlock</span><br><span class="line">  -&gt; 最外层计数归零</span><br><span class="line">  -&gt; 扫描 tpdo_event_mask</span><br><span class="line">  -&gt; co_tpdo_event</span><br><span class="line">  -&gt; TPDO inhibit time / event transmission 逻辑继续执行</span><br></pre></td></tr></table></figure><p>它解决的核心问题不是“防止多个线程同时写对象字典”，而是避免一次逻辑事务中多个对象逐项更新时过早发送 TPDO，使同一个或多个事件驱动 TPDO 在批量更新结束后再统一进入发送流程。</p><hr><h2 id="10-C-CONMT-的接口设计与生命周期边界"><a href="#10-C-CONMT-的接口设计与生命周期边界" class="headerlink" title="10. C++ CONMT 的接口设计与生命周期边界"></a>10. C++ <code>CONMT</code> 的接口设计与生命周期边界</h2><p><code>CONMT</code> 继承 <code>incomplete_c_type&lt;__co_nmt&gt;</code>，构造函数接收 <code>CANNet*</code> 和 <code>CODev*</code>。其 C++ 方法基本是一对一转发：</p><table><thead><tr><th>C++ 方法</th><th>C API</th></tr></thead><tbody><tr><td><code>getNet()</code></td><td><code>co_nmt_get_net()</code></td></tr><tr><td><code>getSt()</code></td><td><code>co_nmt_get_st()</code></td></tr><tr><td><code>csReq()</code></td><td><code>co_nmt_cs_req()</code></td></tr><tr><td><code>bootReq()</code></td><td><code>co_nmt_boot_req()</code></td></tr><tr><td><code>cfgReq()</code></td><td><code>co_nmt_cfg_req()</code></td></tr><tr><td><code>ngReq()</code></td><td><code>co_nmt_ng_req()</code></td></tr><tr><td><code>onSync()</code></td><td><code>co_nmt_on_sync()</code></td></tr><tr><td><code>onErr()</code></td><td><code>co_nmt_on_err()</code></td></tr><tr><td><code>onTPDOEvent()</code></td><td><code>co_nmt_on_tpdo_event()</code></td></tr><tr><td><code>onTPDOEventLock()</code></td><td><code>co_nmt_on_tpdo_event_lock()</code></td></tr><tr><td><code>onTPDOEventUnlock()</code></td><td><code>co_nmt_on_tpdo_event_unlock()</code></td></tr></tbody></table><p>每类 callback 都提供三种注册方式：</p><ol><li>原始 C 函数指针和 <code>void*</code>；</li><li>callable 对象指针；</li><li>指定成员函数的对象指针。</li></ol><p>这种包装不复制 callable，也不拥有用户对象。传入的对象地址必须在 callback 可能发生期间保持有效。</p><p>另外，<code>CONMT</code> 持有 NMT C 对象，但 <code>CANNet</code> 与 <code>CODev</code> 只以指针传入。它们的生命周期必须覆盖 <code>CONMT</code>，否则 <code>co_nmt_t</code> 内部保存的 <code>net/dev</code> 会悬空。</p><hr><h2 id="11-三条典型调用链"><a href="#11-三条典型调用链" class="headerlink" title="11. 三条典型调用链"></a>11. 三条典型调用链</h2><h3 id="11-1-从站收到-Start-Remote-Node"><a href="#11-1-从站收到-Start-Remote-Node" class="headerlink" title="11.1 从站收到 Start Remote Node"></a>11.1 从站收到 Start Remote Node</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">CAN-ID 0x000, data = 01 &lt;local-id&gt;</span><br><span class="line">  -&gt; co_nmt_recv_000</span><br><span class="line">  -&gt; co_nmt_cs_ind(CO_NMT_CS_START)</span><br><span class="line">  -&gt; current state on_cs</span><br><span class="line">  -&gt; co_nmt_start_state</span><br><span class="line">  -&gt; co_nmt_start_on_enter</span><br><span class="line">  -&gt; co_nmt_srv_set(CO_NMT_START_SRV)</span><br><span class="line">  -&gt; create/start PDO services</span><br><span class="line">  -&gt; update local state to 0x05</span><br><span class="line">  -&gt; state indication</span><br><span class="line">  -&gt; command indication</span><br></pre></td></tr></table></figure><h3 id="11-2-应用修改-Producer-heartbeat-time"><a href="#11-2-应用修改-Producer-heartbeat-time" class="headerlink" title="11.2 应用修改 Producer heartbeat time"></a>11.2 应用修改 Producer heartbeat time</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">SDO download 0x1017:00</span><br><span class="line">  -&gt; co_1017_dn_ind</span><br><span class="line">  -&gt; parse and compare value</span><br><span class="line">  -&gt; update nmt-&gt;ms</span><br><span class="line">  -&gt; write OD value</span><br><span class="line">  -&gt; co_nmt_ec_update</span><br><span class="line">  -&gt; stop/restart ec_timer</span><br><span class="line">  -&gt; subsequent timer expiry sends heartbeat</span><br></pre></td></tr></table></figure><h3 id="11-3-主站检测-mandatory-slave-Heartbeat-timeout"><a href="#11-3-主站检测-mandatory-slave-Heartbeat-timeout" class="headerlink" title="11.3 主站检测 mandatory slave Heartbeat timeout"></a>11.3 主站检测 mandatory slave Heartbeat timeout</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">co_nmt_hb_t timeout</span><br><span class="line">  -&gt; co_nmt_hb_ind</span><br><span class="line">  -&gt; user/default heartbeat handler</span><br><span class="line">  -&gt; co_nmt_on_hb</span><br><span class="line">  -&gt; co_nmt_node_err_ind</span><br><span class="line">  -&gt; read 0x1F81 assignment and 0x1F80 policy</span><br><span class="line">  -&gt; stop all / reset all / reset individual slave</span><br><span class="line">  -&gt; co_nmt_cs_req buffers NMT command</span><br><span class="line">  -&gt; inhibit timer sends CAN frame</span><br></pre></td></tr></table></figure><p>这三条链分别展示了 <code>co_nmt_t</code> 的三种角色：本节点状态机、对象字典控制面、主站网络策略执行器。</p><hr><h2 id="12-最终心智模型"><a href="#12-最终心智模型" class="headerlink" title="12. 最终心智模型"></a>12. 最终心智模型</h2><p>阅读 <code>nmt.c</code> 时，应始终把以下边界保持清楚：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line">can_net_t</span><br><span class="line">= 提供 CAN frame 与逻辑时间事件</span><br><span class="line"></span><br><span class="line">co_dev_t</span><br><span class="line">= 保存对象字典、DCF 和配置值</span><br><span class="line"></span><br><span class="line">co_nmt_t</span><br><span class="line">= 依据事件与配置推进 NMT 状态和网络策略</span><br><span class="line"></span><br><span class="line">co_nmt_srv</span><br><span class="line">= 按目标服务掩码装配本地协议服务</span><br><span class="line"></span><br><span class="line">co_nmt_hb</span><br><span class="line">= 单个远端节点的 Heartbeat consumer</span><br><span class="line"></span><br><span class="line">co_nmt_boot / co_nmt_cfg</span><br><span class="line">= 单个远端节点的异步 boot/config 子流程</span><br><span class="line"></span><br><span class="line">nmt.hpp</span><br><span class="line">= C++ 所有权和 callback 适配，不是另一套协议实现</span><br></pre></td></tr></table></figure><p>最关键的一点是：</p><blockquote><p>Lely 的 <code>co_nmt_t</code> 是 CANopen 节点运行期的总控状态机。NMT command 只是触发源之一；DCF 恢复、对象字典动态写入、Heartbeat&#x2F;Guarding、通信错误、SYNC&#x2F;PDO 事件和主站从节点启动结果，都会进入这一总控对象，再由它决定状态、服务集合和网络动作。</p></blockquote><hr><h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ol><li>Lely CANopen, <em>Library overview</em>: <a href="https://opensource.lely.com/canopen/docs/overview/">https://opensource.lely.com/canopen/docs/overview/</a></li><li>Lely CANopen official site, latest release information: <a href="https://opensource.lely.com/canopen/">https://opensource.lely.com/canopen/</a></li><li>Lely CANopen v2.3.5 release note: <a href="https://opensource.lely.com/canopen/release/v2.3.5/">https://opensource.lely.com/canopen/release/v2.3.5/</a></li><li>Lely official GitLab repository: <a href="https://gitlab.com/lely_industries/lely-core">https://gitlab.com/lely_industries/lely-core</a></li><li><code>src/co/nmt.c</code>, commit <code>620d1858eb8520dbc3dc5e1a7314565becd54199</code>: <a href="https://github.com/lely-industries/lely-core/blob/620d1858eb8520dbc3dc5e1a7314565becd54199/src/co/nmt.c">https://github.com/lely-industries/lely-core/blob/620d1858eb8520dbc3dc5e1a7314565becd54199/src/co/nmt.c</a></li><li><code>include/lely/co/nmt.h</code>, same commit: <a href="https://github.com/lely-industries/lely-core/blob/620d1858eb8520dbc3dc5e1a7314565becd54199/include/lely/co/nmt.h">https://github.com/lely-industries/lely-core/blob/620d1858eb8520dbc3dc5e1a7314565becd54199/include/lely/co/nmt.h</a></li><li><code>include/lely/co/nmt.hpp</code>, same commit: <a href="https://github.com/lely-industries/lely-core/blob/620d1858eb8520dbc3dc5e1a7314565becd54199/include/lely/co/nmt.hpp">https://github.com/lely-industries/lely-core/blob/620d1858eb8520dbc3dc5e1a7314565becd54199/include/lely/co/nmt.hpp</a></li><li>CiA 301 V4.2.0, NMT and error control sections.</li><li>Lely CANopen v2.1.0 release note, TPDO event tracking: <a href="https://opensource.lely.com/canopen/release/v2.1.0/">https://opensource.lely.com/canopen/release/v2.1.0/</a></li><li><code>src/coapp/node.cpp</code>, same commit: <a href="https://github.com/lely-industries/lely-core/blob/620d1858eb8520dbc3dc5e1a7314565becd54199/src/coapp/node.cpp">https://github.com/lely-industries/lely-core/blob/620d1858eb8520dbc3dc5e1a7314565becd54199/src/coapp/node.cpp</a></li><li><code>include/lely/coapp/node.hpp</code>, same commit: <a href="https://github.com/lely-industries/lely-core/blob/620d1858eb8520dbc3dc5e1a7314565becd54199/include/lely/coapp/node.hpp">https://github.com/lely-industries/lely-core/blob/620d1858eb8520dbc3dc5e1a7314565becd54199/include/lely/coapp/node.hpp</a></li><li><code>src/coapp/master.cpp</code>, same commit: <a href="https://github.com/lely-industries/lely-core/blob/620d1858eb8520dbc3dc5e1a7314565becd54199/src/coapp/master.cpp">https://github.com/lely-industries/lely-core/blob/620d1858eb8520dbc3dc5e1a7314565becd54199/src/coapp/master.cpp</a></li></ol>]]></content>
    
    
      
      
    <summary type="html">&lt;meta name=&quot;referrer&quot; content=&quot;no-referrer&quot; /&gt;

&lt;h1 id=&quot;Lely-CANopen-co-nmt-t：主从-NMT-总控机制、状态机与运行流程&quot;&gt;&lt;a href=&quot;#Lely-CANopen-co-nmt-t：主从-NMT-总</summary>
      
    
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/categories/canopen/"/>
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/tags/canopen/"/>
    
  </entry>
  
  <entry>
    <title>Lely CANopen LSS：主从状态机、Fastscan 与配置流程</title>
    <link href="https://wdfk-prog.space/posts/c644ba5a/"/>
    <id>https://wdfk-prog.space/posts/c644ba5a/</id>
    <published>2026-07-31T07:59:17.000Z</published>
    <updated>2026-07-31T08:03:26.809Z</updated>
    
    <content type="html"><![CDATA[<meta name="referrer" content="no-referrer" /><h1 id="Lely-CANopen-LSS：主从状态机、Fastscan-与配置流程"><a href="#Lely-CANopen-LSS：主从状态机、Fastscan-与配置流程" class="headerlink" title="Lely CANopen LSS：主从状态机、Fastscan 与配置流程"></a>Lely CANopen LSS：主从状态机、Fastscan 与配置流程</h1><p><img src="https://i-blog.csdnimg.cn/direct/65092effad404731919093eafe9d6623.png" alt="在这里插入图片描述"></p><p>@[toc]</p><p>LSS（Layer Setting Services）用于解决一个基础但特殊的问题：当设备还没有有效 Node-ID，或者整个网络需要切换 CAN 位率时，主站不能依赖普通 SDO 通道完成配置，因为 SDO 本身通常已经依赖有效 Node-ID。LSS 因此使用固定的 CAN-ID，并用对象字典 <code>0x1018</code> 的四个 32 位身份字段识别设备。</p><p>在 Lely CANopen 中，<code>co_lss_t</code> 同时承载 LSS master 和 LSS slave 两种角色。它不是一组“发送命令后阻塞等待结果”的同步函数，而是一个依附于 <code>co_nmt_t</code>、<code>can_net_t</code> 和 <code>co_dev_t</code> 的事件驱动有限状态机：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">应用提交 LSS 请求</span><br><span class="line">    ↓</span><br><span class="line">co_lss_*_req() 构造首帧或进入内部状态</span><br><span class="line">    ↓</span><br><span class="line">can_net_send() 发送 0x7E5</span><br><span class="line">    ↓</span><br><span class="line">can_recv_t 等待 0x7E4，can_timer_t 负责超时与帧间隔</span><br><span class="line">    ↓</span><br><span class="line">当前状态的 on_recv / on_time 推进状态机</span><br><span class="line">    ↓</span><br><span class="line">回到 idle 状态后调用完成回调</span><br></pre></td></tr></table></figure><p>从站侧也不是“接收回调里直接改硬件”。<code>lss.c</code> 只负责协议状态、pending Node-ID、pending 位率和应答；真正切换 CAN 控制器位率、保存到非易失存储，必须由应用注册的回调完成。</p><h2 id="源码基线与证据边界"><a href="#源码基线与证据边界" class="headerlink" title="源码基线与证据边界"></a>源码基线与证据边界</h2><p>本文围绕以下三个文件展开：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">include/lely/co/lss.h</span><br><span class="line">include/lely/co/lss.hpp</span><br><span class="line">src/co/lss.c</span><br></pre></td></tr></table></figure><p>源码取自 Lely Industries 官方 GitHub 镜像的 <code>master</code> 分支，查阅时间为 2026-07-31。该镜像由项目维护者提供，并与官方 GitLab 仓库同步。为避免把“当前网页版本”静默等同于某个发布版本，本文记录三个文件的 blob SHA：</p><table><thead><tr><th>文件</th><th>blob SHA</th></tr></thead><tbody><tr><td><code>include/lely/co/lss.h</code></td><td><code>9ed835cae9f6eba48ea6a3d903dbb2a2306faaa8</code></td></tr><tr><td><code>include/lely/co/lss.hpp</code></td><td><code>8f634addb1d7aa04fe70fce5fc4b3f6f4e67ce91</code></td></tr><tr><td><code>src/co/lss.c</code></td><td><code>6fdc8c1de73b7d51f5277ec2e00ccf22b0e34b83</code></td></tr></tbody></table><p>协议语义以 CiA 305 v3.0.0 的公开说明为边界；Lely 官方 standards support 页面确认其 LSS 实现覆盖全局&#x2F;选择性状态切换、Node-ID 与位时序配置、身份与 Node-ID 查询、设备识别和 Fastscan，而“激活位时序”和“保存配置”明确属于 application-specific 部分。</p><p>本文没有声称完成目标板构建、CAN 抓包或真实位率切换验证。函数、字段、分支和调用链来自源码；对返回值一致性、接口使用风险和应用边界的说明属于基于源码的工程分析。</p><h2 id="三个文件分别负责什么"><a href="#三个文件分别负责什么" class="headerlink" title="三个文件分别负责什么"></a>三个文件分别负责什么</h2><p>先建立文件级边界，可以避免把 C++ 包装、C API 和协议状态机混在一起。</p><table><thead><tr><th>文件</th><th>层次</th><th>主要职责</th><th>不负责什么</th></tr></thead><tbody><tr><td><code>lss.h</code></td><td>公共 C API</td><td>定义回调类型、生命周期接口、master 请求接口、默认超时和固定 CAN-ID</td><td>不保存运行状态，不执行状态迁移</td></tr><tr><td><code>lss.hpp</code></td><td>C++ 包装层</td><td>用 <code>incomplete_c_type</code> 管理 C 对象，并把 C 回调适配为 callable 或成员函数</td><td>不重新实现 LSS，不创建线程，不增加 Future 状态机</td></tr><tr><td><code>lss.c</code></td><td>C 协议实现</td><td>保存主从运行状态，注册 CAN receiver&#x2F;timer，处理请求、响应、超时、Slowscan 和 Fastscan</td><td>不直接操作 SocketCAN，不直接切换硬件位率，不负责持久化介质</td></tr></tbody></table><p>整体关系如下。图中需要观察的是：LSS 服务只消费 <code>can_net_t</code> 提供的 CAN 帧与逻辑时间，操作系统 I&#x2F;O 仍位于更低层。</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">应用代码</span><br><span class="line">├─ 直接调用 co_lss_* C API</span><br><span class="line">└─ 通过 lely::COLSS C++ 包装</span><br><span class="line">        ↓</span><br><span class="line">co_lss_t：事件驱动状态机</span><br><span class="line">├─ co_nmt_t：角色、NMT 状态、pending Node-ID</span><br><span class="line">├─ co_dev_t：0x1018、active Node-ID、位率能力</span><br><span class="line">├─ can_net_t：CAN 分发与逻辑定时</span><br><span class="line">├─ can_recv_t：监听 0x7E5 或 0x7E4</span><br><span class="line">├─ can_timer_t：响应超时与 inhibit 间隔</span><br><span class="line">├─ rate_ind：应用负责切换 CAN 位率</span><br><span class="line">└─ store_ind：应用负责持久化参数</span><br><span class="line">        ↓</span><br><span class="line">io_can_net / 平台桥接</span><br><span class="line">        ↓</span><br><span class="line">SocketCAN 或目标平台 CAN 驱动</span><br></pre></td></tr></table></figure><p><code>co_lss_t</code> 并不拥有独立线程。只要外部持续推进 <code>can_net_t</code> 的接收和时间，LSS 状态机就能工作；如果 <code>can_net_t</code> 没有收到帧或没有推进时间，master 的响应回调和超时也不会凭空发生。</p><h2 id="LSS-的固定通信通道与-128-位地址"><a href="#LSS-的固定通信通道与-128-位地址" class="headerlink" title="LSS 的固定通信通道与 128 位地址"></a>LSS 的固定通信通道与 128 位地址</h2><p>CiA 305 将设备分为 LSS manager 和 LSS server。Lely 的 API 仍使用 <code>master/slave</code> 命名，但协议方向一致：</p><table><thead><tr><th>方向</th><th align="right">CAN-ID</th><th>Lely 宏展开</th></tr></thead><tbody><tr><td>LSS master → LSS slave</td><td align="right"><code>0x7E5</code></td><td><code>CO_LSS_CANID(1)</code></td></tr><tr><td>LSS slave → LSS master</td><td align="right"><code>0x7E4</code></td><td><code>CO_LSS_CANID(0)</code></td></tr></tbody></table><p><code>lss.h</code> 中的宏非常直接：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> CO_LSS_CANID(master) (0x7e4 + !!(master))</span></span><br></pre></td></tr></table></figure><p>这两条固定通道不依赖普通 Node-ID，因此即使节点尚未配置、active Node-ID 为 <code>0xFF</code>，仍然可以参与 LSS 识别和配置。</p><p>LSS address 由对象字典 <code>0x1018 Identity object</code> 的四个 32 位数构成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">0x1018:01  Vendor-ID</span><br><span class="line">0x1018:02  Product code</span><br><span class="line">0x1018:03  Revision number</span><br><span class="line">0x1018:04  Serial number</span><br></pre></td></tr></table></figure><p>总长度为 128 bit。选择性切换、设备识别、Slowscan 和 Fastscan 最终都围绕这四个值工作。<code>lss.c</code> 并没有另建一份独立身份数据库，而是通过 <code>co_dev_find_obj(dev, 0x1018)</code> 和 <code>co_obj_get_val_u32()</code> 读取本地对象字典。</p><h2 id="lss-h：公共接口按角色分为三组"><a href="#lss-h：公共接口按角色分为三组" class="headerlink" title="lss.h：公共接口按角色分为三组"></a><code>lss.h</code>：公共接口按角色分为三组</h2><h3 id="生命周期和公共属性"><a href="#生命周期和公共属性" class="headerlink" title="生命周期和公共属性"></a>生命周期和公共属性</h3><p><code>co_lss_create()</code> 接收一个 <code>co_nmt_t *</code>。LSS 不要求应用再次传入 <code>can_net_t</code> 或 <code>co_dev_t</code>，因为初始化阶段会从 NMT 对象中取得它们：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">co_lss_create(nmt)</span><br><span class="line">    ↓</span><br><span class="line">__co_lss_init(lss, nmt)</span><br><span class="line">    ├─ co_nmt_get_net(nmt)</span><br><span class="line">    ├─ co_nmt_get_dev(nmt)</span><br><span class="line">    ├─ can_recv_create()</span><br><span class="line">    ├─ master build: can_timer_create()</span><br><span class="line">    └─ co_lss_start()</span><br></pre></td></tr></table></figure><p>公开生命周期接口包括：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">co_lss_t</span> *<span class="title function_">co_lss_create</span><span class="params">(<span class="type">co_nmt_t</span> *nmt)</span>;</span><br><span class="line"><span class="type">void</span> <span class="title function_">co_lss_destroy</span><span class="params">(<span class="type">co_lss_t</span> *lss)</span>;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">co_lss_start</span><span class="params">(<span class="type">co_lss_t</span> *lss)</span>;</span><br><span class="line"><span class="type">void</span> <span class="title function_">co_lss_stop</span><span class="params">(<span class="type">co_lss_t</span> *lss)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">co_lss_is_stopped</span><span class="params">(<span class="type">const</span> <span class="type">co_lss_t</span> *lss)</span>;</span><br></pre></td></tr></table></figure><p>创建成功后服务已经启动，不需要再额外调用一次 <code>co_lss_start()</code>。重复调用 <code>start()</code> 会直接返回成功，<code>stop()</code> 也具有幂等检查。</p><h3 id="从站需要应用接管的两个动作"><a href="#从站需要应用接管的两个动作" class="headerlink" title="从站需要应用接管的两个动作"></a>从站需要应用接管的两个动作</h3><p>Lely 把两项与平台强相关的动作留给应用：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="type">void</span> <span class="title function_">co_lss_rate_ind_t</span><span class="params">(</span></span><br><span class="line"><span class="params">        <span class="type">co_lss_t</span> *lss, <span class="type">co_unsigned16_t</span> rate, <span class="type">int</span> delay, <span class="type">void</span> *data)</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="type">int</span> <span class="title function_">co_lss_store_ind_t</span><span class="params">(</span></span><br><span class="line"><span class="params">        <span class="type">co_lss_t</span> *lss, <span class="type">co_unsigned8_t</span> id,</span></span><br><span class="line"><span class="params">        <span class="type">co_unsigned16_t</span> rate, <span class="type">void</span> *data)</span>;</span><br></pre></td></tr></table></figure><p>二者的职责不同：</p><table><thead><tr><th>回调</th><th>触发命令</th><th>应用职责</th></tr></thead><tbody><tr><td><code>rate_ind</code></td><td>Activate bit timing parameters，CS <code>0x15</code></td><td>停止发送、等待切换延时、重配 CAN 控制器、再等待静默时间</td></tr><tr><td><code>store_ind</code></td><td>Store configuration，CS <code>0x17</code></td><td>把 pending Node-ID 和位率写入 Flash、EEPROM 或其他持久介质</td></tr></tbody></table><p>协议栈只把请求和参数交给应用，不知道 Linux <code>ip link</code>、MCU CAN 寄存器、Bootloader 参数区或 Flash 擦写策略。</p><h3 id="Master-的异步请求和完成回调"><a href="#Master-的异步请求和完成回调" class="headerlink" title="Master 的异步请求和完成回调"></a>Master 的异步请求和完成回调</h3><p>Master 请求接口可以再分成五类：</p><table><thead><tr><th>类别</th><th>代表接口</th><th>完成方式</th></tr></thead><tbody><tr><td>状态切换</td><td><code>co_lss_switch_req()</code>、<code>co_lss_switch_sel_req()</code></td><td>全局切换无响应；选择性切换回调返回 CS 或超时</td></tr><tr><td>参数配置</td><td><code>co_lss_set_id_req()</code>、<code>co_lss_set_rate_req()</code>、<code>co_lss_switch_rate_req()</code>、<code>co_lss_store_req()</code></td><td>配置&#x2F;保存返回错误响应；激活位率无普通确认</td></tr><tr><td>参数查询</td><td><code>co_lss_get_vendor_id_req()</code> 等、<code>co_lss_get_id_req()</code></td><td>回调返回身份值或 Node-ID</td></tr><tr><td>设备识别</td><td><code>co_lss_id_slave_req()</code>、<code>co_lss_id_non_cfg_slave_req()</code></td><td>回调返回匹配命令或超时</td></tr><tr><td>聚合扫描</td><td><code>co_lss_slowscan_req()</code>、<code>co_lss_fastscan_req()</code></td><td>内部多轮状态机结束后回调返回完整 LSS address</td></tr></tbody></table><p>这些函数的返回值只表示“请求是否成功提交”，不表示远端操作已经完成。远端成功、协议错误和超时由回调异步报告。</p><p>Master 还暴露两个运行状态：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">co_lss_is_master</span><span class="params">(<span class="type">const</span> <span class="type">co_lss_t</span> *lss)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">co_lss_is_idle</span><span class="params">(<span class="type">const</span> <span class="type">co_lss_t</span> *lss)</span>;</span><br></pre></td></tr></table></figure><p>所有需要响应的 master 请求都先检查：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">当前对象必须是 LSS master</span><br><span class="line">并且</span><br><span class="line">当前状态必须是 idle</span><br></pre></td></tr></table></figure><p>否则设置 <code>ERRNUM_PERM</code> 并返回 <code>-1</code>。这意味着同一个 <code>co_lss_t</code> 同时只能执行一个 master 请求；如果上层需要连续执行“扫描 → 改 ID → 保存”，应在前一个完成回调中提交下一步，而不是并发调用。</p><h2 id="lss-hpp：薄封装，而不是另一套-LSS-实现"><a href="#lss-hpp：薄封装，而不是另一套-LSS-实现" class="headerlink" title="lss.hpp：薄封装，而不是另一套 LSS 实现"></a><code>lss.hpp</code>：薄封装，而不是另一套 LSS 实现</h2><p><code>lss.hpp</code> 中的核心类型是：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">COLSS</span> : <span class="keyword">public</span> incomplete_c_type&lt;__co_lss&gt;;</span><br></pre></td></tr></table></figure><p>它通过 <code>c_type_traits&lt;__co_lss&gt;</code> 把 C 生命周期函数接入通用不完整类型包装：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">alloc() → __co_lss_alloc()</span><br><span class="line">init()  → __co_lss_init()</span><br><span class="line">fini()  → __co_lss_fini()</span><br><span class="line">free()  → __co_lss_free()</span><br></pre></td></tr></table></figure><p><code>COLSS</code> 的成员函数基本都是一行转发：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">start</span><span class="params">()</span> <span class="keyword">noexcept</span> </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="built_in">co_lss_start</span>(<span class="keyword">this</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">setIdReq</span><span class="params">(<span class="type">co_unsigned8_t</span> id,</span></span></span><br><span class="line"><span class="params"><span class="function">             <span class="type">co_lss_err_ind_t</span>* ind,</span></span></span><br><span class="line"><span class="params"><span class="function">             <span class="type">void</span>* data)</span> <span class="keyword">noexcept</span> </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="built_in">co_lss_set_id_req</span>(<span class="keyword">this</span>, id, ind, data);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>模板重载使用 <code>c_obj_call</code> 和 <code>c_mem_call</code>，允许把普通 callable 对象或成员函数适配为 C 回调。它解决的是调用形式与对象生命周期问题，不改变以下底层事实：</p><ul><li>仍然只有一个 <code>co_lss_t</code> 状态机；</li><li>仍然通过 C 回调完成请求；</li><li>仍然需要外部驱动 <code>can_net_t</code>；</li><li>没有额外的线程、Future 或队列；</li><li>C++ 方法返回 <code>int</code> 时，语义仍是 C API 的“是否成功提交”。</li></ul><p>因此，读 <code>lss.hpp</code> 时不必重复追一遍协议。先理解 <code>lss.c</code> 的状态和回调时机，再回来看模板适配即可。</p><h2 id="co-lss-t-保存了哪些运行状态"><a href="#co-lss-t-保存了哪些运行状态" class="headerlink" title="co_lss_t 保存了哪些运行状态"></a><code>co_lss_t</code> 保存了哪些运行状态</h2><p><code>src/co/lss.c</code> 中的 <code>struct __co_lss</code> 可以按四组理解。</p><h3 id="依赖对象"><a href="#依赖对象" class="headerlink" title="依赖对象"></a>依赖对象</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">nmt   → co_nmt_t</span><br><span class="line">net   → can_net_t</span><br><span class="line">dev   → co_dev_t</span><br><span class="line">state → 当前 co_lss_state_t</span><br></pre></td></tr></table></figure><p>NMT 决定本节点角色和 pending Node-ID；device 提供 active Node-ID、<code>0x1018</code> 和位率能力；network 提供发送、接收分发和逻辑时间。</p><h3 id="收发和定时资源"><a href="#收发和定时资源" class="headerlink" title="收发和定时资源"></a>收发和定时资源</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">recv  → 一个 can_recv_t</span><br><span class="line">timer → master build 下的一个 can_timer_t</span><br></pre></td></tr></table></figure><p>同一个 receiver 会根据角色和请求阶段切换监听方向：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">slave waiting/configuration：监听 0x7E5</span><br><span class="line">master waiting response：监听 0x7E4</span><br></pre></td></tr></table></figure><p>Master 的 timer 同时承担两类时间：</p><ol><li>多帧请求之间的 inhibit 间隔；</li><li>等待从站响应的 timeout。</li></ol><p>默认值来自 <code>lss.h</code>：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> LELY_CO_LSS_INHIBIT 10   <span class="comment">// 10 × 100 us = 1 ms</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> LELY_CO_LSS_TIMEOUT 100  <span class="comment">// 100 ms</span></span></span><br></pre></td></tr></table></figure><p>运行时可以通过 <code>co_lss_set_inhibit()</code> 和 <code>co_lss_set_timeout()</code> 修改。timeout 设为 <code>0</code> 表示关闭响应超时。</p><h3 id="扫描与响应上下文"><a href="#扫描与响应上下文" class="headerlink" title="扫描与响应上下文"></a>扫描与响应上下文</h3><p>Master build 还保存：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">next       多帧请求的下一帧序号</span><br><span class="line">lo / hi    Slowscan 地址范围</span><br><span class="line">id         当前识别到或正在构造的 LSS address</span><br><span class="line">mask       Fastscan 已知位掩码</span><br><span class="line">bitchk     当前检查的 bit 位置</span><br><span class="line">lsssub     当前检查第几个 32-bit 身份字段</span><br><span class="line">cs         期望收到的 command specifier</span><br><span class="line">err/spec   配置响应错误码</span><br><span class="line">lssid/nid  查询结果</span><br></pre></td></tr></table></figure><p>这些字段表明 Slowscan 和 Fastscan 不是外部循环反复调用单帧接口，而是 <code>co_lss_t</code> 内部持久化上下文的聚合异步操作。</p><h3 id="完成回调"><a href="#完成回调" class="headerlink" title="完成回调"></a>完成回调</h3><p>每一类响应都有独立回调和 <code>void *data</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">cs_ind      命令响应</span><br><span class="line">err_ind     配置错误响应</span><br><span class="line">lssid_ind   身份字段查询</span><br><span class="line">nid_ind     Node-ID 查询</span><br><span class="line">scan_ind    Slowscan/Fastscan 结果</span><br></pre></td></tr></table></figure><p>状态离开时先停止 receiver&#x2F;timer，再调用相应回调。由于 <code>co_lss_enter()</code> 会先把当前状态切到目标状态，再调用旧状态的 <code>on_leave()</code>，回调执行时 master 通常已经回到 <code>co_lss_wait_state</code>，上层可以在回调内安全地提交下一项串行请求。</p><h2 id="状态机为什么不用枚举加大-switch"><a href="#状态机为什么不用枚举加大-switch" class="headerlink" title="状态机为什么不用枚举加大 switch"></a>状态机为什么不用枚举加大 switch</h2><p>Lely 为每个状态定义一个函数指针集合：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> __<span class="title">co_lss_state</span> &#123;</span></span><br><span class="line">  <span class="type">co_lss_state_t</span> *(*on_enter)(<span class="type">co_lss_t</span> *lss);</span><br><span class="line">  <span class="type">co_lss_state_t</span> *(*on_recv)(<span class="type">co_lss_t</span> *lss,</span><br><span class="line">                             <span class="type">const</span> <span class="keyword">struct</span> can_msg *msg);</span><br><span class="line">  <span class="type">co_lss_state_t</span> *(*on_time)(<span class="type">co_lss_t</span> *lss,</span><br><span class="line">                             <span class="type">const</span> <span class="keyword">struct</span> timespec *tp);</span><br><span class="line">  <span class="type">void</span> (*on_leave)(<span class="type">co_lss_t</span> *lss);</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>事件只有当前状态关心的函数会被调用。<code>co_lss_enter()</code> 支持“瞬时状态”：某个 <code>on_enter()</code> 可以直接返回下一个状态，不需要等待新的 CAN 帧或 timer 事件。</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">外部事件</span><br><span class="line">  ├─ CAN frame → co_lss_emit_recv()</span><br><span class="line">  └─ timeout   → co_lss_emit_time()</span><br><span class="line">                    ↓</span><br><span class="line">             当前 state handler</span><br><span class="line">                    ↓</span><br><span class="line">              返回 next state</span><br><span class="line">                    ↓</span><br><span class="line">               co_lss_enter()</span><br><span class="line">                    ├─ previous.on_leave()</span><br><span class="line">                    ├─ next.on_enter()</span><br><span class="line">                    └─ 若 on_enter 又返回 next，继续迁移</span><br></pre></td></tr></table></figure><p>这种结构特别适合选择性切换和扫描：它们有大量“发送一帧 → 等间隔 → 再发一帧”“收到响应 → 先回 idle → 再执行用户回调”的过渡步骤。如果全部放进一个大 switch，状态与事件组合会迅速膨胀。</p><h2 id="创建后如何确定-master-或-slave-角色"><a href="#创建后如何确定-master-或-slave-角色" class="headerlink" title="创建后如何确定 master 或 slave 角色"></a>创建后如何确定 master 或 slave 角色</h2><p><code>__co_lss_init()</code> 并不把角色永久固定在构造参数中。初始化完成后调用 <code>co_lss_start()</code>，进入 <code>co_lss_wait_state</code>；该状态的 entry 函数执行：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">lss-&gt;master = co_nmt_is_master(lss-&gt;nmt)</span><br></pre></td></tr></table></figure><p>只有 NMT master 才能成为 LSS master：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">co_lss_wait_state</span><br><span class="line">    ├─ NMT 是 master → 留在 master idle 状态</span><br><span class="line">    └─ NMT 不是 master → 进入 co_lss_wait_slave_state</span><br><span class="line">                              └─ receiver 监听 0x7E5</span><br></pre></td></tr></table></figure><p>因此，<code>co_lss_t</code> 的角色来自它依附的 NMT 对象，而不是由 <code>co_lss_create()</code> 的单独参数指定。每次重新进入 wait 状态都会重新判断角色。</p><h2 id="Slave：waiting-与-configuration-两个主状态"><a href="#Slave：waiting-与-configuration-两个主状态" class="headerlink" title="Slave：waiting 与 configuration 两个主状态"></a>Slave：waiting 与 configuration 两个主状态</h2><p>LSS slave 主要在两个协议状态之间切换：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line">co_lss_start</span><br><span class="line">    ↓</span><br><span class="line">进入 co_lss_wait_state，检查 NMT 角色</span><br><span class="line">    ↓</span><br><span class="line">LSS waiting：监听 0x7E5</span><br><span class="line">    ├─ Switch global mode=1 ───────────────┐</span><br><span class="line">    ├─ Selective 四段身份完全匹配 ────────┤</span><br><span class="line">    └─ Fastscan 完成最后一段 ──────────────┤</span><br><span class="line">                                           ↓</span><br><span class="line">                                  LSS configuration</span><br><span class="line">                                  ├─ 配置 pending Node-ID</span><br><span class="line">                                  ├─ 配置 pending 位率</span><br><span class="line">                                  ├─ 查询身份或 Node-ID</span><br><span class="line">                                  ├─ 应用持久化配置</span><br><span class="line">                                  └─ 应用激活新位率</span><br><span class="line">                                           │</span><br><span class="line">                         Switch global mode=0</span><br><span class="line">                                           ↓</span><br><span class="line">                                      LSS waiting</span><br></pre></td></tr></table></figure><p>Waiting 状态只接受用于发现或进入 configuration 的命令；Node-ID、位率、保存和查询等配置命令只在 configuration 状态处理。这个状态隔离是网络安全性的关键：主站必须保证最终只有目标设备进入 configuration，否则广播式配置命令可能同时作用于多个节点。</p><h3 id="全局切换"><a href="#全局切换" class="headerlink" title="全局切换"></a>全局切换</h3><p>CS <code>0x04</code> 携带 mode：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">mode = 0 → waiting</span><br><span class="line">mode = 1 → configuration</span><br></pre></td></tr></table></figure><p>全局切换会影响所有能接收该帧的 LSS slave。它适用于已确认网络中只有一个待配置节点，或者确实需要所有节点同步改变 LSS 状态的场景；它不能替代设备唯一选择。</p><h3 id="选择性切换"><a href="#选择性切换" class="headerlink" title="选择性切换"></a>选择性切换</h3><p>选择性切换由四帧组成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">0x40 Vendor-ID</span><br><span class="line">0x41 Product code</span><br><span class="line">0x42 Revision number</span><br><span class="line">0x43 Serial number</span><br></pre></td></tr></table></figure><p>从站用 <code>lss-&gt;cs</code> 记录下一步期望值。任一字段或顺序不匹配就清零匹配进度；只有四个字段全部匹配，才发送 CS <code>0x44</code> 响应并进入 configuration。</p><p>下面的时序图同时展示了选择性切换与后续 Node-ID 配置。图中“请求完成”不等于新 Node-ID 已成为 active ID。</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">应用                 LSS master              CAN 总线              LSS slave / NMT</span><br><span class="line"> │                       │                       │                         │</span><br><span class="line"> │ switchSelReq(address) │                       │                         │</span><br><span class="line"> ├──────────────────────&gt;│                       │                         │</span><br><span class="line"> │                       ├─ 0x7E5 0x40 Vendor ─&gt;│────────────────────────&gt;│</span><br><span class="line"> │                       ├─ 0x7E5 0x41 Product -&gt;│───────────────────────&gt;│</span><br><span class="line"> │                       ├─ 0x7E5 0x42 Revision &gt;│────────────────────────&gt;│</span><br><span class="line"> │                       ├─ 0x7E5 0x43 Serial ─&gt;│────────────────────────&gt;│</span><br><span class="line"> │                       │                       │&lt;─ 0x7E4 0x44 ───────────┤</span><br><span class="line"> │&lt;─ cs_ind(0x44) ───────┤&lt;──────────────────────│                         │</span><br><span class="line"> │                       │                       │                         │</span><br><span class="line"> │ setIdReq(new_id)      │                       │                         │</span><br><span class="line"> ├──────────────────────&gt;│─ 0x7E5 0x11 new_id ─&gt;│────────────────────────&gt;│</span><br><span class="line"> │                       │                       │                         ├─ co_nmt_set_id()</span><br><span class="line"> │                       │                       │&lt;─ 0x7E4 0x11 err/spec ──┤</span><br><span class="line"> │&lt;─ err_ind(...) ───────┤&lt;──────────────────────│                         │</span><br></pre></td></tr></table></figure><p>Master 在发送四段选择性请求时，会用 <code>inhibit</code> 控制相邻帧间隔。每发完一段非末帧，源码停止接收器，并把 timer 设置为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">当前 can_net 时间 + 100 us × inhibit</span><br></pre></td></tr></table></figure><p>默认 <code>inhibit=10</code>，即约 1 ms。发送最后一帧后，才切换 receiver 到 <code>0x7E4</code> 并启动响应 timeout。</p><h2 id="Slave-如何配置-pending-Node-ID"><a href="#Slave-如何配置-pending-Node-ID" class="headerlink" title="Slave 如何配置 pending Node-ID"></a>Slave 如何配置 pending Node-ID</h2><p>Configuration 状态收到 CS <code>0x11</code> 后调用：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">co_nmt_set_id(lss-&gt;nmt, msg-&gt;data[<span class="number">1</span>]);</span><br></pre></td></tr></table></figure><p>若 Node-ID 非法，源码保留原错误上下文，并在响应 byte1 返回错误码 <code>1</code>。成功时响应错误码为 <code>0</code>。</p><p>这里修改的是 NMT 管理的 pending Node-ID，不是立即重写 <code>co_dev_t</code> 的 active Node-ID。源码在处理“查询 Node-ID”时明确区分二者：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">NMT 处于 BOOTUP / RESET_NODE / RESET_COMM</span><br><span class="line">    → 返回 co_nmt_get_id()，即 pending Node-ID</span><br><span class="line"></span><br><span class="line">其他 NMT 状态</span><br><span class="line">    → 返回 co_dev_get_id()，即 active Node-ID</span><br></pre></td></tr></table></figure><p>Lely 官方命令行教程也展示了相同行为：执行 <code>lss_set_node</code> 后，新 ID 仍处于 pending；退出 LSS configuration 并执行 NMT reset communication 后，节点才以新 Node-ID 发送 boot-up。</p><p>这意味着完整链路至少有三步：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">LSS configure node-ID</span><br><span class="line">    ↓ pending</span><br><span class="line">可选：LSS store configuration</span><br><span class="line">    ↓ 持久化</span><br><span class="line">NMT reset communication / reset node</span><br><span class="line">    ↓</span><br><span class="line">新 Node-ID 成为运行期 active ID</span><br></pre></td></tr></table></figure><h2 id="Slave-如何配置和激活位率"><a href="#Slave-如何配置和激活位率" class="headerlink" title="Slave 如何配置和激活位率"></a>Slave 如何配置和激活位率</h2><p>CS <code>0x13</code> 只配置 pending 位率。Master API 接受 kbit&#x2F;s，并映射为 CiA 305 的 table index：</p><table><thead><tr><th align="right"><code>rate</code> 参数</th><th align="right">table index</th></tr></thead><tbody><tr><td align="right">1000</td><td align="right">0</td></tr><tr><td align="right">800</td><td align="right">1</td></tr><tr><td align="right">500</td><td align="right">2</td></tr><tr><td align="right">250</td><td align="right">3</td></tr><tr><td align="right">125</td><td align="right">4</td></tr><tr><td align="right">50</td><td align="right">6</td></tr><tr><td align="right">20</td><td align="right">7</td></tr><tr><td align="right">10</td><td align="right">8</td></tr><tr><td align="right">0，自动检测</td><td align="right">9</td></tr></tbody></table><p>从站收到请求后先检查：</p><ol><li>selector byte 必须为 <code>0</code>；</li><li>必须已经注册 <code>rate_ind</code>；</li><li><code>co_dev_get_baud()</code> 对应能力位必须支持目标速率。</li></ol><p>通过后只调用：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">co_dev_set_rate(dev, rate)</span><br></pre></td></tr></table></figure><p>这仍然没有操作 CAN 控制器。</p><p>CS <code>0x15</code> 才是 Activate bit timing parameters。它携带一个 16 位 delay，源码直接调用：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">rate_ind(lss, co_dev_get_rate(dev), delay, rate_data);</span><br></pre></td></tr></table></figure><p>应用必须自行实现两个静默区间和硬件重配置。协议栈无法知道：</p><ul><li>当前平台怎样停止 CAN TX；</li><li>是否需要先等待发送邮箱清空；</li><li>SocketCAN 使用 <code>ip link</code>、netlink 还是外部管理进程；</li><li>MCU 是否需要进入 init mode；</li><li>时钟树、采样点、SJW 和预分频器怎样配置；</li><li>自动位率检测由哪一层执行。</li></ul><p>此外，激活位率请求没有普通的 LSS 确认响应。主站和所有目标节点必须按同一延时计划切换，否则主站可能在新旧位率边界失去通信。</p><h2 id="保存配置为什么必须由应用实现"><a href="#保存配置为什么必须由应用实现" class="headerlink" title="保存配置为什么必须由应用实现"></a>保存配置为什么必须由应用实现</h2><p>CS <code>0x17</code> 触发 <code>store_ind</code>。Lely 把当前 pending Node-ID 和 pending rate 作为参数传给应用：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">store_ind(lss,</span><br><span class="line">          co_nmt_get_id(nmt),</span><br><span class="line">          co_dev_get_rate(dev),</span><br><span class="line">          store_data);</span><br></pre></td></tr></table></figure><p>响应语义由源码固定：</p><table><thead><tr><th>条件</th><th align="right">LSS error code</th></tr></thead><tbody><tr><td>回调成功</td><td align="right"><code>0</code></td></tr><tr><td>未注册 <code>store_ind</code>，不支持保存</td><td align="right"><code>1</code></td></tr><tr><td>回调返回 <code>-1</code>，存储失败</td><td align="right"><code>2</code></td></tr></tbody></table><p>协议栈不会为应用选择 Flash 分区，也不会定义掉电原子性。实际产品应由持久化层决定：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">参数格式和版本</span><br><span class="line">CRC 或校验策略</span><br><span class="line">双备份或 journal</span><br><span class="line">擦写寿命</span><br><span class="line">掉电恢复</span><br><span class="line">何时把 pending 参数提交为 active 配置</span><br></pre></td></tr></table></figure><p>这些属于设备实现，不是 CiA 305 帧级状态机的一部分。</p><h2 id="Master-请求的统一执行模型"><a href="#Master-请求的统一执行模型" class="headerlink" title="Master 请求的统一执行模型"></a>Master 请求的统一执行模型</h2><p>除全局状态切换和激活位率外，大多数 master 请求采用相同模式：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">检查 is_master &amp;&amp; is_idle</span><br><span class="line">    ↓</span><br><span class="line">构造 8-byte LSS 请求帧，CAN-ID=0x7E5</span><br><span class="line">    ↓</span><br><span class="line">can_net_send()</span><br><span class="line">    ↓</span><br><span class="line">co_lss_init_ind(expected_cs)</span><br><span class="line">    ├─ receiver 监听 0x7E4</span><br><span class="line">    └─ timer 启动 timeout</span><br><span class="line">    ↓</span><br><span class="line">收到 expected_cs 或超时</span><br><span class="line">    ↓</span><br><span class="line">停止 receiver/timer</span><br><span class="line">    ↓</span><br><span class="line">回到 wait/idle</span><br><span class="line">    ↓</span><br><span class="line">调用用户回调</span><br></pre></td></tr></table></figure><p><code>co_lss_init_req()</code> 总是把帧初始化为 8 字节，并把 byte0 写成 command specifier。未使用字节保持初始化值。</p><p>超时的表达方式不是单独的错误枚举，而是把回调中的 <code>cs</code> 置为 <code>0</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">cs != 0 → 收到匹配响应</span><br><span class="line">cs == 0 → timeout 或未找到节点</span><br></pre></td></tr></table></figure><p>因此回调必须先检查 <code>cs</code>，再解释 <code>err</code>、<code>id</code> 或扫描结果指针。</p><p><code>co_lss_abort_req()</code> 的函数体只执行一次状态迁移：<code>co_lss_enter(lss, co_lss_wait_state)</code>。它没有在函数内统一调用 <code>can_timer_stop()</code> 或 <code>can_recv_stop()</code>；资源清理和回调是否发生，取决于被中止状态是否定义了 <code>on_leave()</code>。例如错误响应、身份查询和 Node-ID 查询状态的 leave handler 会停止 receiver&#x2F;timer 并调用回调，而部分多帧发送状态没有统一的 leave handler。调用方不能把 abort 理解成“所有请求都以同一种方式静默取消”，应按目标源码版本逐项确认正在使用的请求状态。若需要彻底停用服务，<code>co_lss_stop()</code> 才会显式停止 timer 和 receiver。</p><h2 id="设备识别与-Slowscan"><a href="#设备识别与-Slowscan" class="headerlink" title="设备识别与 Slowscan"></a>设备识别与 Slowscan</h2><h3 id="Identify-remote-slave"><a href="#Identify-remote-slave" class="headerlink" title="Identify remote slave"></a>Identify remote slave</h3><p><code>co_lss_id_slave_req()</code> 接受上下界：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">Vendor-ID      必须精确相等</span><br><span class="line">Product code   必须精确相等</span><br><span class="line">Revision       位于 [lo, hi]</span><br><span class="line">Serial number  位于 [lo, hi]</span><br></pre></td></tr></table></figure><p>Master 依次发送 CS <code>0x46..0x4B</code>。从站逐段检查，全部匹配后发送 <code>0x4F</code>。这项服务回答的是“指定范围内是否存在匹配节点”，并不直接返回完整身份值。</p><h3 id="Slowscan"><a href="#Slowscan" class="headerlink" title="Slowscan"></a>Slowscan</h3><p><code>co_lss_slowscan_req()</code> 是建立在 identify remote slave 和 switch state selective 之上的聚合流程：</p><ol><li>先确认给定范围内至少存在一个匹配节点；</li><li>在 revision 和 serial number 范围内执行二分搜索；</li><li>每轮根据“是否有响应”收缩上下界；</li><li>找到唯一地址后再执行选择性切换；</li><li>成功时把目标节点留在 LSS configuration 状态。</li></ol><p>源码计算中点时显式避免整数溢出：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">mid = low + (high - low) / 2</span><br></pre></td></tr></table></figure><p>收到第一条响应后并不立刻结束本轮，而是进入 wait 状态，忽略后续响应直到 timeout。这样可以给所有匹配节点完整响应窗口，再统一更新搜索边界。</p><p>Lely 官方控制工具把 <code>_lss_slowscan</code> 标为 Lely-specific 聚合命令。它不是新的 CAN 帧协议，而是对标准 identify 和 selective switch 的本地编排。</p><h2 id="Fastscan-如何逐位恢复-128-位身份"><a href="#Fastscan-如何逐位恢复-128-位身份" class="headerlink" title="Fastscan 如何逐位恢复 128 位身份"></a>Fastscan 如何逐位恢复 128 位身份</h2><p>Fastscan 的核心不是“读取 0x1018”，而是反复询问：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">网络中是否有设备的当前身份前缀与这个候选值匹配？</span><br></pre></td></tr></table></figure><p>Master 不需要预先知道 Node-ID。它维护四个 32 位字段、一个 mask、当前字段序号 <code>lsssub</code> 和当前 bit <code>bitchk</code>。</p><h3 id="id-和-mask-的语义"><a href="#id-和-mask-的语义" class="headerlink" title="id 和 mask 的语义"></a><code>id</code> 和 <code>mask</code> 的语义</h3><p><code>co_lss_fastscan_req()</code> 接收可选的 <code>id</code> 和 <code>mask</code>：</p><ul><li><code>id</code> 保存已知位的值；</li><li><code>mask</code> 中 bit 为 <code>1</code> 表示该位已经知道，扫描时跳过；</li><li>mask 为 <code>0</code> 的 bit 需要逐位探测。</li></ul><p>源码会先把未知位清零：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">id.field &amp;= mask.field</span><br></pre></td></tr></table></figure><p>随后从 Vendor-ID 的 bit31 开始，依次处理 Product code、Revision 和 Serial number，直到四个字段全部确定。</p><h3 id="响应与超时怎样表示一个-bit"><a href="#响应与超时怎样表示一个-bit" class="headerlink" title="响应与超时怎样表示一个 bit"></a>响应与超时怎样表示一个 bit</h3><p>Lely 的扫描状态机采用以下判断：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">发送当前候选前缀</span><br><span class="line">    ├─ 收到 0x4F 响应 → 当前候选前缀存在，当前 bit 保持 0</span><br><span class="line">    └─ timeout         → 当前 bit 设为 1</span><br></pre></td></tr></table></figure><p>每次确定一个 bit 后，都把 mask 对应位置设为 <code>1</code>，再继续下一个未知 bit。流程如下：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line">发送 Fastscan reset（bitchk = 0x80）</span><br><span class="line">    ↓</span><br><span class="line">是否收到 0x4F？</span><br><span class="line">    ├─ 否：未找到可扫描节点，scan_ind(cs=0)</span><br><span class="line">    └─ 是：从 Vendor-ID bit31 开始选择最高未知 bit</span><br><span class="line">              ↓</span><br><span class="line">          发送候选前缀（0x7E5，CS 0x51）</span><br><span class="line">              ↓</span><br><span class="line">          timeout 前是否收到 0x4F？</span><br><span class="line">              ├─ 是：当前 bit 保持 0</span><br><span class="line">              └─ 否：当前 bit 设置为 1</span><br><span class="line">              ↓</span><br><span class="line">          mask 对应 bit 置 1</span><br><span class="line">              ↓</span><br><span class="line">          当前 32-bit 字段是否完成？</span><br><span class="line">              ├─ 否：继续下一个未知 bit</span><br><span class="line">              └─ 是：切换到下一个身份字段</span><br><span class="line">                         ↓</span><br><span class="line">                     四个字段是否全部完成？</span><br><span class="line">                         ├─ 否：bitchk 重新从 31 开始</span><br><span class="line">                         └─ 是：目标从站进入 configuration，返回完整 LSS address</span><br></pre></td></tr></table></figure><p>从站侧 <code>co_lss_fastscan()</code> 会用对象 <code>0x1018</code> 的真实值检查候选前缀。完成最后一个字段后，<code>lssnext</code> 回绕，从站进入 configuration 状态，并仍发送 <code>0x4F</code> 表示匹配。</p><p>当所有 128 bit 都未知且 timeout 使用默认 100 ms 时，扫描的主要时延由逐位响应窗口决定。Lely 官方教程的全零 mask 示例会检查 128 bit，典型用时略多于 13 秒；已知位越多，mask 跳过的 bit 越多，扫描越快。</p><h3 id="多个节点响应时的处理"><a href="#多个节点响应时的处理" class="headerlink" title="多个节点响应时的处理"></a>多个节点响应时的处理</h3><p>Fastscan 和 Slowscan 都可能在一轮中收到多个从站响应。源码在收到第一条匹配响应后进入 wait 状态，并忽略后续响应，直到本轮 timeout 到期再更新结果。这不是为了丢弃错误，而是把“至少有一个匹配节点”压缩成布尔结果。</p><p>最终能否唯一定位设备，取决于网络中 <code>0x1018:01..04</code> 是否唯一。若多个节点拥有完全相同的 128 位 LSS address，Fastscan 无法凭相同身份把它们区分为两个设备；它们可能同时进入 configuration。协议栈不能替代制造阶段的序列号唯一性管理。</p><h2 id="非配置节点识别的条件比-active-ID-无效更严格"><a href="#非配置节点识别的条件比-active-ID-无效更严格" class="headerlink" title="非配置节点识别的条件比 active ID 无效更严格"></a>非配置节点识别的条件比 active ID 无效更严格</h2><p>收到 CS <code>0x4C</code> 时，从站只有同时满足以下条件才返回 <code>0x50</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">co_dev_get_id(dev) == 0xFF</span><br><span class="line">并且</span><br><span class="line">co_nmt_get_id(nmt) == 0xFF</span><br><span class="line">并且</span><br><span class="line">NMT 位于 BOOTUP / RESET_NODE / RESET_COMM</span><br></pre></td></tr></table></figure><p>也就是说，源码同时检查 active Node-ID 与 pending Node-ID，还限制在 NMT 初始化阶段。仅把其中一个值设为 <code>0xFF</code>，或者节点已经进入正常运行状态，并不足以让它响应“identify non-configured slave”。</p><h2 id="把完整运行链压缩成一张图"><a href="#把完整运行链压缩成一张图" class="headerlink" title="把完整运行链压缩成一张图"></a>把完整运行链压缩成一张图</h2><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line">NMT 决定本节点是 LSS master 还是 slave</span><br><span class="line">    ↓</span><br><span class="line">co_lss_start() 进入 wait</span><br><span class="line">    ├─ master：等待应用提交一个异步请求</span><br><span class="line">    │     ↓</span><br><span class="line">    │   发送 0x7E5</span><br><span class="line">    │     ↓</span><br><span class="line">    │   receiver 监听 0x7E4 + timer 计时</span><br><span class="line">    │     ↓</span><br><span class="line">    │   收到响应或 timeout</span><br><span class="line">    │     ↓</span><br><span class="line">    │   回到 idle，再调用用户回调</span><br><span class="line">    │</span><br><span class="line">    └─ slave：receiver 长期监听 0x7E5</span><br><span class="line">          ↓</span><br><span class="line">        waiting 状态只处理发现和状态选择</span><br><span class="line">          ↓</span><br><span class="line">        唯一目标进入 configuration</span><br><span class="line">          ↓</span><br><span class="line">        修改 pending Node-ID / pending rate</span><br><span class="line">          ↓</span><br><span class="line">        rate_ind 负责真实位率切换</span><br><span class="line">        store_ind 负责真实持久化</span><br><span class="line">          ↓</span><br><span class="line">        NMT reset communication 使 pending Node-ID 生效</span><br></pre></td></tr></table></figure><p>理解 Lely LSS 的关键不是记住所有 command specifier，而是抓住四个边界：</p><ul><li><code>lss.h</code> 定义异步契约，返回值只表示请求是否提交；</li><li><code>lss.hpp</code> 只包装 C API，不改变协议和调度模型；</li><li><code>lss.c</code> 用 receiver、timer 和函数指针状态机实现主从流程；</li><li>位率激活、持久化和最终 NMT 复位属于应用与系统集成责任。</li></ul><h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ol><li>Lely Core 官方 GitLab 仓库：<a href="https://gitlab.com/lely_industries/lely-core">https://gitlab.com/lely_industries/lely-core</a></li><li>Lely Core 官方 GitHub 镜像：<a href="https://github.com/lely-industries/lely-core">https://github.com/lely-industries/lely-core</a></li><li><code>include/lely/co/lss.h</code>：<a href="https://github.com/lely-industries/lely-core/blob/master/include/lely/co/lss.h">https://github.com/lely-industries/lely-core/blob/master/include/lely/co/lss.h</a></li><li><code>include/lely/co/lss.hpp</code>：<a href="https://github.com/lely-industries/lely-core/blob/master/include/lely/co/lss.hpp">https://github.com/lely-industries/lely-core/blob/master/include/lely/co/lss.hpp</a></li><li><code>src/co/lss.c</code>：<a href="https://github.com/lely-industries/lely-core/blob/master/src/co/lss.c">https://github.com/lely-industries/lely-core/blob/master/src/co/lss.c</a></li><li>CiA 305 Layer Setting Services：<a href="https://www.can-cia.org/can-knowledge/cia-305-layer-setting-services-lss">https://www.can-cia.org/can-knowledge/cia-305-layer-setting-services-lss</a></li><li>Lely CANopen standards support：<a href="https://opensource.lely.com/canopen/docs/standards/">https://opensource.lely.com/canopen/docs/standards/</a></li><li>Lely CANopen build configuration：<a href="https://opensource.lely.com/canopen/docs/configuration/">https://opensource.lely.com/canopen/docs/configuration/</a></li><li>Lely CANopen finite-state machines：<a href="https://opensource.lely.com/canopen/docs/fsm/">https://opensource.lely.com/canopen/docs/fsm/</a></li><li>Lely CANopen command-line tutorial：<a href="https://opensource.lely.com/canopen/docs/cmd-tutorial/">https://opensource.lely.com/canopen/docs/cmd-tutorial/</a></li><li>Lely CANopen control tool：<a href="https://opensource.lely.com/canopen/docs/coctl/">https://opensource.lely.com/canopen/docs/coctl/</a></li></ol>]]></content>
    
    
      
      
    <summary type="html">&lt;meta name=&quot;referrer&quot; content=&quot;no-referrer&quot; /&gt;

&lt;h1 id=&quot;Lely-CANopen-LSS：主从状态机、Fastscan-与配置流程&quot;&gt;&lt;a href=&quot;#Lely-CANopen-LSS：主从状态机、Fastscan-与配置</summary>
      
    
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/categories/canopen/"/>
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/tags/canopen/"/>
    
  </entry>
  
  <entry>
    <title>Lely CANopen SYNC、TIME 与 EMCY学习</title>
    <link href="https://wdfk-prog.space/posts/7f0529c9/"/>
    <id>https://wdfk-prog.space/posts/7f0529c9/</id>
    <published>2026-07-31T07:28:29.000Z</published>
    <updated>2026-07-31T07:28:56.171Z</updated>
    
    <content type="html"><![CDATA[<meta name="referrer" content="no-referrer" /><h1 id="Lely-CANopen-SYNC、TIME-与-EMCY学习"><a href="#Lely-CANopen-SYNC、TIME-与-EMCY学习" class="headerlink" title="Lely CANopen SYNC、TIME 与 EMCY学习"></a>Lely CANopen SYNC、TIME 与 EMCY学习</h1><p><img src="https://i-blog.csdnimg.cn/direct/1702fefc376b44b7b55eb7a372cac2bf.png" alt="在这里插入图片描述"></p><p>@[toc]</p><p>SYNC、TIME 和 EMCY 都属于 CiA 301 通信对象，但在 Lely CANopen 中，它们并不是三个“收到 CAN 帧后直接调用用户回调”的简单模块。三者都被实现成依附于 <code>can_net_t</code> 和 <code>co_dev_t</code> 的被动服务对象：对象字典决定角色与 CAN-ID，<code>can_recv_t</code> 接收匹配报文，<code>can_timer_t</code> 驱动周期或抑制时间，最终通过回调或 <code>can_net_send()</code> 把事件交给上层。</p><p>三者的触发模型并不相同：</p><ul><li><code>co_sync_t</code> 的 producer 周期直接由对象 <code>0x1006</code> 驱动，修改 <code>0x1005/0x1006/0x1019</code> 会立即重配 receiver、timer 和 counter；</li><li><code>co_time_t</code> 的 <code>0x1012</code> 只决定 producer&#x2F;consumer 角色和 COB-ID，实际发送起点与周期仍要由 <code>co_time_start_prod()</code> 显式提交；</li><li><code>co_emcy_t</code> 不做周期发送，它把错误变化压入活动错误栈和 CAN 帧缓冲，再按 <code>0x1015</code> inhibit time 串行发送；</li><li><code>sync.hpp</code>、<code>time.hpp</code>、<code>emcy.hpp</code> 没有重新实现协议，只使用 <code>incomplete_c_type</code> 管理 C 对象并适配 C++ callable。</li></ul><p>这篇文章以用户提供的本地 <code>lely-core</code> 源码包为主要证据，结合 CiA 301 V4.2.0 和 Lely 官方文档校对协议边界。源码包未携带 <code>.git</code> 元数据，因此本文不把它静默等同于某个 Git commit；文中涉及行号时，均指本次本地归档文件。</p><hr><h2 id="1-九个文件在整条链路中的位置"><a href="#1-九个文件在整条链路中的位置" class="headerlink" title="1. 九个文件在整条链路中的位置"></a>1. 九个文件在整条链路中的位置</h2><p>本文重点分析以下文件：</p><table><thead><tr><th>文件</th><th>主要内容</th><th align="right">是否保存运行状态</th></tr></thead><tbody><tr><td><code>include/lely/co/sync.h</code></td><td>SYNC C API、COB-ID 位定义、indication&#x2F;error callback</td><td align="right">否</td></tr><tr><td><code>include/lely/co/sync.hpp</code></td><td><code>COSync</code> C++ 不完整类型包装与 callable 适配</td><td align="right">否</td></tr><tr><td><code>src/co/sync.c</code></td><td>SYNC receiver、periodic timer、计数器、OD 动态重配置</td><td align="right">是</td></tr><tr><td><code>include/lely/co/time.h</code></td><td>TIME C API、TIME_OF_DAY 转换、producer 调度接口</td><td align="right">否</td></tr><tr><td><code>include/lely/co/time.hpp</code></td><td><code>COTime</code> C++ 包装</td><td align="right">否</td></tr><tr><td><code>src/co/time.c</code></td><td>TIME receiver、timer、时间编码与 <code>0x1013</code> 更新</td><td align="right">是</td></tr><tr><td><code>include/lely/co/emcy.h</code></td><td>EMCY producer&#x2F;consumer API、错误栈接口和 indication</td><td align="right">否</td></tr><tr><td><code>include/lely/co/emcy.hpp</code></td><td><code>COEmcy</code> C++ 包装</td><td align="right">否</td></tr><tr><td><code>src/co/emcy.c</code></td><td>活动错误栈、<code>0x1003</code>、发送缓冲、inhibit timer、远端 EMCY consumer</td><td align="right">是</td></tr></tbody></table><p>Lely 官方概览把 CANopen 核心描述为完全被动的实现，并在 standards support 页面列出 CiA 301 的 SYNC、TIME、EMCY 及相关 OD 对象支持范围：[^lely-standards]</p><p>协议库不创建线程，不自行执行 CAN I&#x2F;O，也不直接读取系统时钟；应用需要把 CAN 帧和当前时间送入 <code>can_net_t</code>。SYNC、TIME 和 EMCY 都是这种架构中的 service object。[^lely-overview]</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line">flowchart TB</span><br><span class="line">    App[&quot;应用或 co_nmt_t&quot;] --&gt; Dev[&quot;co_dev_t&lt;br/&gt;对象字典与当前参数&quot;]</span><br><span class="line">    App --&gt; Net[&quot;can_net_t&lt;br/&gt;CAN 接收分发与逻辑定时&quot;]</span><br><span class="line"></span><br><span class="line">    Dev --&gt; Sync[&quot;co_sync_t&quot;]</span><br><span class="line">    Dev --&gt; TimeSvc[&quot;co_time_t&quot;]</span><br><span class="line">    Dev --&gt; Emcy[&quot;co_emcy_t&quot;]</span><br><span class="line"></span><br><span class="line">    Net --&gt; Sync</span><br><span class="line">    Net --&gt; TimeSvc</span><br><span class="line">    Net --&gt; Emcy</span><br><span class="line"></span><br><span class="line">    Sync --&gt; SyncRx[&quot;can_recv_t&lt;br/&gt;接收 SYNC&quot;]</span><br><span class="line">    Sync --&gt; SyncTimer[&quot;can_timer_t&lt;br/&gt;周期发送 SYNC&quot;]</span><br><span class="line"></span><br><span class="line">    TimeSvc --&gt; TimeRx[&quot;can_recv_t&lt;br/&gt;接收 TIME&quot;]</span><br><span class="line">    TimeSvc --&gt; TimeTimer[&quot;can_timer_t&lt;br/&gt;按 API 调度 TIME&quot;]</span><br><span class="line"></span><br><span class="line">    Emcy --&gt; EmcyRx[&quot;最多 127 个 can_recv_t&lt;br/&gt;远端 EMCY consumer&quot;]</span><br><span class="line">    Emcy --&gt; EmcyBuf[&quot;活动错误栈 + CAN 帧缓冲&quot;]</span><br><span class="line">    Emcy --&gt; EmcyTimer[&quot;can_timer_t&lt;br/&gt;inhibit deadline&quot;]</span><br><span class="line"></span><br><span class="line">    SyncRx --&gt; Bus[&quot;CAN 帧入口/出口&quot;]</span><br><span class="line">    SyncTimer --&gt; Bus</span><br><span class="line">    TimeRx --&gt; Bus</span><br><span class="line">    TimeTimer --&gt; Bus</span><br><span class="line">    EmcyRx --&gt; Bus</span><br><span class="line">    EmcyTimer --&gt; Bus</span><br></pre></td></tr></table></figure><p>这里要特别区分两层“时间”：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">can_timer_t</span><br><span class="line">= 协议核心内部的逻辑定时器</span><br><span class="line"></span><br><span class="line">Linux timerfd / io_timer_t / io_tqueue</span><br><span class="line">= io2 层把逻辑截止时间映射到操作系统的机制</span><br></pre></td></tr></table></figure><p><code>sync.c</code>、<code>time.c</code>、<code>emcy.c</code> 只操作 <code>can_timer_t</code>。它们不知道 Linux <code>timerfd</code>、SocketCAN、<code>epoll</code> 或线程，实际系统 I&#x2F;O 仍由 <code>io_can_net</code> 等桥接层完成。</p><hr><h2 id="2-三个服务共享的生命周期模型"><a href="#2-三个服务共享的生命周期模型" class="headerlink" title="2. 三个服务共享的生命周期模型"></a>2. 三个服务共享的生命周期模型</h2><p>三组源码都采用相同的对象生命周期：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">__co_xxx_alloc()</span><br><span class="line">    -&gt; __co_xxx_init(net, dev)</span><br><span class="line">        -&gt; 创建 receiver/timer/buffer</span><br><span class="line">        -&gt; 读取必要对象是否存在</span><br><span class="line">        -&gt; co_xxx_start()</span><br><span class="line"></span><br><span class="line">co_xxx_stop()</span><br><span class="line">    -&gt; 停止 receiver/timer</span><br><span class="line">    -&gt; 移除对象字典 download indication</span><br><span class="line"></span><br><span class="line">__co_xxx_fini()</span><br><span class="line">    -&gt; co_xxx_stop()</span><br><span class="line">    -&gt; 销毁内部对象</span><br><span class="line"></span><br><span class="line">__co_xxx_free()</span><br></pre></td></tr></table></figure><h3 id="2-1-创建时已经启动"><a href="#2-1-创建时已经启动" class="headerlink" title="2.1 创建时已经启动"></a>2.1 创建时已经启动</h3><p><code>co_sync_create()</code>、<code>co_time_create()</code> 和 <code>co_emcy_create()</code> 都通过内部 <code>__co_xxx_init()</code> 完成初始化，并在成功路径中启动服务。对应头文件也明确说明“service is started as if by <code>co_xxx_start()</code>”。因此：</p><ul><li><code>create()</code> 不是单纯分配内存；</li><li>创建后对象字典回调和 CAN receiver&#x2F;timer 可能已经生效；</li><li><code>stop()</code> 必须解除这些外部关联，否则仅释放外层指针会留下悬空回调。</li></ul><h3 id="2-2-对象字典写入直接驱动运行期重配置"><a href="#2-2-对象字典写入直接驱动运行期重配置" class="headerlink" title="2.2 对象字典写入直接驱动运行期重配置"></a>2.2 对象字典写入直接驱动运行期重配置</h3><p>三个服务启动时都会为关键 OD 对象安装 download indication：</p><table><thead><tr><th>服务</th><th>被拦截对象</th><th>重配置效果</th></tr></thead><tbody><tr><td>SYNC</td><td><code>0x1005</code>、<code>0x1006</code>、<code>0x1019</code></td><td>重注册 receiver、重设周期 timer、复位 counter</td></tr><tr><td>TIME</td><td><code>0x1012</code></td><td>重注册 consumer receiver；必要时停止 producer timer</td></tr><tr><td>EMCY</td><td><code>0x1003</code>、<code>0x1014</code>、<code>0x1028</code></td><td>清错误历史、改变 producer COB-ID、重配指定远端 consumer</td></tr></tbody></table><p>这类写入不是“先修改 OD，等下次复位再生效”。实际链路是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">SDO download / 本地 SDO 写入</span><br><span class="line">    -&gt; co_obj_set_dn_ind() 安装的 handler</span><br><span class="line">    -&gt; 校验活动状态、CAN-ID 和取值范围</span><br><span class="line">    -&gt; co_sub_dn() 保存新值</span><br><span class="line">    -&gt; 立即更新 receiver/timer/service state</span><br></pre></td></tr></table></figure><p>因此运行期写通信参数时，访问规则本身也是状态机的一部分。例如 SYNC producer 活动时不允许直接把旧 CAN-ID 改成另一个仍然有效的 producer CAN-ID；正确过程通常是先禁用，再修改标识符，最后重新启用。</p><h3 id="2-3-条件编译直接改变接口和-ABI"><a href="#2-3-条件编译直接改变接口和-ABI" class="headerlink" title="2.3 条件编译直接改变接口和 ABI"></a>2.3 条件编译直接改变接口和 ABI</h3><p>源码分别受以下宏控制：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">if</span> !LELY_NO_CO_SYNC</span></span><br><span class="line"><span class="meta">#<span class="keyword">if</span> !LELY_NO_CO_TIME</span></span><br><span class="line"><span class="meta">#<span class="keyword">if</span> !LELY_NO_CO_EMCY</span></span><br></pre></td></tr></table></figure><p>Autotools 对应选项为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">--disable-sync  -&gt; LELY_NO_CO_SYNC</span><br><span class="line">--disable-time  -&gt; LELY_NO_CO_TIME</span><br><span class="line">--disable-emcy  -&gt; LELY_NO_CO_EMCY</span><br></pre></td></tr></table></figure><p>Lely 官方构建文档列出了这些裁剪选项，并说明部分结构体相关特性宏必须在库与应用侧保持一致。对于嵌入式静态库，不能只在应用代码中包含头文件后假定服务一定存在；库、头文件和应用的条件编译配置必须匹配。[^lely-config]</p><hr><h2 id="3-SYNC：对象字典直接驱动-producer-周期"><a href="#3-SYNC：对象字典直接驱动-producer-周期" class="headerlink" title="3. SYNC：对象字典直接驱动 producer 周期"></a>3. SYNC：对象字典直接驱动 producer 周期</h2><h3 id="3-1-struct-co-sync-保存了什么"><a href="#3-1-struct-co-sync-保存了什么" class="headerlink" title="3.1 struct __co_sync 保存了什么"></a>3.1 <code>struct __co_sync</code> 保存了什么</h3><p><code>src/co/sync.c:40-67</code> 的对象非常紧凑：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">co_sync_t</span><br><span class="line">├─ net / dev</span><br><span class="line">├─ stopped</span><br><span class="line">├─ cobid       &lt;- 0x1005</span><br><span class="line">├─ us          &lt;- 0x1006，单位 µs</span><br><span class="line">├─ max_cnt     &lt;- 0x1019</span><br><span class="line">├─ recv</span><br><span class="line">├─ timer</span><br><span class="line">├─ cnt</span><br><span class="line">├─ indication callback</span><br><span class="line">└─ error callback</span><br></pre></td></tr></table></figure><p>它没有独立线程，没有报文队列，也不直接维护 PDO。SYNC 服务只负责：</p><ol><li>接收或产生 SYNC 帧；</li><li>维护可选计数器；</li><li>把同步事件交给上层；</li><li>在帧长度不符合配置时报告通信错误。</li></ol><h3 id="3-2-三个对象决定-SYNC-本体，0x1007-不在这里处理"><a href="#3-2-三个对象决定-SYNC-本体，0x1007-不在这里处理" class="headerlink" title="3.2 三个对象决定 SYNC 本体，0x1007 不在这里处理"></a>3.2 三个对象决定 SYNC 本体，<code>0x1007</code> 不在这里处理</h3><table><thead><tr><th align="right">OD</th><th>作用</th><th><code>sync.c</code> 中的行为</th></tr></thead><tbody><tr><td align="right"><code>0x1005</code></td><td>SYNC COB-ID、producer 位、标准&#x2F;扩展帧</td><td>决定 receiver 或 producer 角色及 CAN-ID</td></tr><tr><td align="right"><code>0x1006</code></td><td>communication cycle period，单位 µs</td><td>producer 周期；为 0 时不启动周期发送</td></tr><tr><td align="right"><code>0x1019</code></td><td>synchronous counter overflow</td><td>0 表示无 payload；2..240 表示一字节计数器上限</td></tr><tr><td align="right"><code>0x1007</code></td><td>synchronous window length</td><td><strong>不由 <code>sync.c</code> 处理</strong>，由 RPDO&#x2F;TPDO 服务用于同步窗口判定</td></tr></tbody></table><p>这一区分很重要。<code>co_sync_t</code> 只产生“同步边沿”；同步窗口内是否允许发送 TPDO、是否接收并执行 RPDO，是 PDO 服务的职责。把 <code>0x1007</code> 写入 <code>sync.c</code> 的心智模型，会导致调用关系完全错位。</p><h3 id="3-3-co-sync-update-的角色矩阵"><a href="#3-3-co-sync-update-的角色矩阵" class="headerlink" title="3.3 co_sync_update() 的角色矩阵"></a>3.3 <code>co_sync_update()</code> 的角色矩阵</h3><p><code>src/co/sync.c:380-420</code> 是配置落地的中心：</p><table><thead><tr><th align="right">producer 位</th><th align="right"><code>0x1006</code></th><th>receiver</th><th>timer</th></tr></thead><tbody><tr><td align="right">0</td><td align="right">任意</td><td>按 <code>0x1005</code> 注册，作为 consumer</td><td>停止</td></tr><tr><td align="right">1</td><td align="right">0</td><td>停止，避免接收自己的 SYNC</td><td>停止</td></tr><tr><td align="right">1</td><td align="right">非 0</td><td>停止</td><td>周期启动</td></tr></tbody></table><p>producer 周期不是从“当前时刻 + 周期”任意起算。源码先取得 <code>can_net_t</code> 当前时间，再对周期取模，把首次发送时间对齐到下一个周期整数倍：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">now</span><br><span class="line">  -&gt; now % period</span><br><span class="line">  -&gt; 向下对齐到最近的 period 边界</span><br><span class="line">  -&gt; 加一个 period</span><br><span class="line">  -&gt; 作为首次 deadline</span><br></pre></td></tr></table></figure><p>这使多个使用同一时间基准、同一周期的节点更容易落在一致的时间栅格上。</p><h3 id="3-4-consumer-接收流程"><a href="#3-4-consumer-接收流程" class="headerlink" title="3.4 consumer 接收流程"></a>3.4 consumer 接收流程</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant CanBus as CAN 总线</span><br><span class="line">    participant Net as can_net_t</span><br><span class="line">    participant SyncSvc as co_sync_t</span><br><span class="line">    participant Nmt as co_nmt_t</span><br><span class="line">    participant Tpdo as TPDO services</span><br><span class="line">    participant Rpdo as RPDO services</span><br><span class="line">    participant UserCb as 用户回调</span><br><span class="line"></span><br><span class="line">    CanBus-&gt;&gt;Net: SYNC frame</span><br><span class="line">    Net-&gt;&gt;SyncSvc: co_sync_recv(msg)</span><br><span class="line">    SyncSvc-&gt;&gt;SyncSvc: 期望长度 = max_cnt ? 1 : 0</span><br><span class="line">    alt DLC 不匹配</span><br><span class="line">        SyncSvc-&gt;&gt;Nmt: error callback(0x8240, 0x10)</span><br><span class="line">    end</span><br><span class="line">    SyncSvc-&gt;&gt;Nmt: indication(cnt 或 0)</span><br><span class="line">    Nmt-&gt;&gt;Tpdo: co_tpdo_sync(cnt)</span><br><span class="line">    Nmt-&gt;&gt;Rpdo: co_rpdo_sync(cnt)</span><br><span class="line">    Nmt-&gt;&gt;UserCb: sync indication</span><br></pre></td></tr></table></figure><p><code>co_sync_recv()</code> 的细节有两点：</p><ol><li>若 <code>0x1019 == 0</code>，期望 DLC 为 0；否则期望 DLC 为 1；</li><li>即使 DLC 错误，源码仍会调用 indication，只是传入 <code>cnt = 0</code>，同时通过 error callback 报告 <code>0x8240 / 0x10</code>。</li></ol><p>在 NMT 集成路径中，SYNC indication 会进入 <code>co_nmt_on_sync()</code>。源码明确先处理 TPDO，再处理 RPDO，以确保同一对象同时映射到同步 TPDO 和 RPDO 时，发送的是“上一个同步窗口”的值，然后才用本次收到的 RPDO 更新对象字典（<code>src/co/nmt.c:1621-1643</code>）。这不是普通的调用顺序偏好，而是同步数据一致性的语义要求。</p><h3 id="3-5-producer-发送流程"><a href="#3-5-producer-发送流程" class="headerlink" title="3.5 producer 发送流程"></a>3.5 producer 发送流程</h3><p>周期到期后 <code>co_sync_timer()</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">can_timer_t 到期</span><br><span class="line">    -&gt; 根据 0x1005 生成标准或扩展 CAN-ID</span><br><span class="line">    -&gt; max_cnt == 0：DLC = 0</span><br><span class="line">    -&gt; max_cnt != 0：DLC = 1，发送 cnt</span><br><span class="line">    -&gt; cnt 在 1..max_cnt 之间循环</span><br><span class="line">    -&gt; can_net_send()</span><br><span class="line">    -&gt; 调用同一个 SYNC indication</span><br></pre></td></tr></table></figure><p>因此本节点作为 producer 时，也会通过 indication 驱动本地同步 PDO，不必等待 CAN 控制器把自己发送的帧再回环接收。<code>co_sync_update()</code> 同时会停掉 receiver，正是为了避免“本地 timer indication + 自收回环”造成重复同步事件。</p><h3 id="3-6-0x1019-的运行期限制"><a href="#3-6-0x1019-的运行期限制" class="headerlink" title="3.6 0x1019 的运行期限制"></a>3.6 <code>0x1019</code> 的运行期限制</h3><p>CiA 301 规定 <code>0x1019</code>：[^cia301]</p><ul><li><code>0</code>：SYNC 无数据；</li><li><code>1</code>：保留；</li><li><code>2..240</code>：一字节 counter；</li><li><code>241..255</code>：保留。</li></ul><p>源码进一步禁止在 <code>0x1006 != 0</code> 时修改 <code>0x1019</code>，返回 <code>CO_SDO_AC_DATA_DEV</code>。也就是说，要改变 counter 形式，应先把通信周期清零，再修改 overflow，最后恢复周期。</p><hr><h2 id="4-TIME：角色由-0x1012-决定，调度由-API-决定"><a href="#4-TIME：角色由-0x1012-决定，调度由-API-决定" class="headerlink" title="4. TIME：角色由 0x1012 决定，调度由 API 决定"></a>4. TIME：角色由 <code>0x1012</code> 决定，调度由 API 决定</h2><h3 id="4-1-TIME-与-SYNC-的根本差异"><a href="#4-1-TIME-与-SYNC-的根本差异" class="headerlink" title="4.1 TIME 与 SYNC 的根本差异"></a>4.1 TIME 与 SYNC 的根本差异</h3><p>SYNC 解决“同步动作发生在什么时候”，TIME 解决“网络当前时间是什么”。两者虽然都使用 producer&#x2F;consumer 模型，但 Lely 的调度方式不同：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">SYNC producer</span><br><span class="line">    0x1006 非零后自动周期运行</span><br><span class="line"></span><br><span class="line">TIME producer</span><br><span class="line">    0x1012 只允许 producer</span><br><span class="line">    仍需 co_time_start_prod() 提交起点与周期</span><br></pre></td></tr></table></figure><p>这一点由 <code>co_time_update()</code> 直接体现：</p><ul><li>consumer 位有效时注册 receiver；</li><li>consumer 位无效时停止 receiver；</li><li>producer 位无效时停止 timer；</li><li>producer 位有效时，函数本身并不启动 timer。</li></ul><h3 id="4-2-相关对象"><a href="#4-2-相关对象" class="headerlink" title="4.2 相关对象"></a>4.2 相关对象</h3><table><thead><tr><th align="right">OD</th><th>作用</th><th>本源码中的使用方式</th></tr></thead><tbody><tr><td align="right"><code>0x1012</code></td><td>TIME COB-ID、consumer 位、producer 位、帧类型</td><td>决定角色和 CAN-ID</td></tr><tr><td align="right"><code>0x1013</code></td><td>High resolution time stamp，单位 µs</td><td>producer 每次发送前写入“服务创建后经过的微秒数”</td></tr></tbody></table><p><code>0x1013</code> 不是 TIME 帧的第七或第八个字节。它是本地对象字典中的高分辨率时间戳；标准 TIME 帧仍固定为 6 字节。</p><h3 id="4-3-TIME-OF-DAY-编码与-epoch-转换"><a href="#4-3-TIME-OF-DAY-编码与-epoch-转换" class="headerlink" title="4.3 TIME_OF_DAY 编码与 epoch 转换"></a>4.3 TIME_OF_DAY 编码与 epoch 转换</h3><p>TIME payload：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Byte 0..3：当天经过的毫秒数，低 28 bit 有效</span><br><span class="line">Byte 4..5：自 1984-01-01 起的天数</span><br></pre></td></tr></table></figure><p>Lely 对外使用 <code>struct timespec</code>。转换函数完成 Unix epoch 与 CANopen epoch 的偏移：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">1970-01-01 -&gt; 1984-01-01</span><br><span class="line">= 14 年 + 3 个闰日</span><br></pre></td></tr></table></figure><p><code>co_time_of_day_get()</code> 把 CANopen TIME_OF_DAY 转为 Unix <code>timespec</code>；<code>co_time_of_day_set()</code> 执行反向转换。Lely v2.3.4 的发布说明曾专门修复 <code>co_time_of_day_set()</code> “只计算但没有真正写回结果”的问题，说明这部分转换应纳入版本核对范围。[^lely-v234]</p><h3 id="4-4-consumer-接收流程"><a href="#4-4-consumer-接收流程" class="headerlink" title="4.4 consumer 接收流程"></a>4.4 consumer 接收流程</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line">sequenceDiagram</span><br><span class="line">    participant CanBus as CAN 总线</span><br><span class="line">    participant Net as can_net_t</span><br><span class="line">    participant TimeSvc as co_time_t</span><br><span class="line">    participant Converter as TIME_OF_DAY 转换</span><br><span class="line">    participant UserCb as indication callback</span><br><span class="line"></span><br><span class="line">    CanBus-&gt;&gt;Net: TIME frame</span><br><span class="line">    Net-&gt;&gt;TimeSvc: co_time_recv(msg)</span><br><span class="line">    alt RTR 或 CAN FD 帧</span><br><span class="line">        TimeSvc--&gt;&gt;Net: return 0</span><br><span class="line">    else DLC 小于 6</span><br><span class="line">        TimeSvc--&gt;&gt;Net: return 0</span><br><span class="line">    else 至少 6 字节</span><br><span class="line">        TimeSvc-&gt;&gt;TimeSvc: 提取 28-bit ms 与 16-bit days</span><br><span class="line">        TimeSvc-&gt;&gt;Converter: 转换为 struct timespec</span><br><span class="line">        Converter--&gt;&gt;TimeSvc: Unix epoch time</span><br><span class="line">        TimeSvc-&gt;&gt;UserCb: ind(time, &amp;timespec)</span><br><span class="line">        TimeSvc--&gt;&gt;Net: return 1</span><br><span class="line">    end</span><br></pre></td></tr></table></figure><p>源码只要求 <code>msg-&gt;len &gt;= 6</code>，不会因经典 CAN 帧带有额外字节而拒绝；实际只解析前 6 字节。它会忽略 RTR 和 CAN FD 格式帧。</p><h3 id="4-5-producer-调度与发送"><a href="#4-5-producer-调度与发送" class="headerlink" title="4.5 producer 调度与发送"></a>4.5 producer 调度与发送</h3><p><code>co_time_start_prod(time, start, interval)</code> 直接把参数交给 <code>can_timer_start()</code>：</p><table><thead><tr><th><code>start</code></th><th><code>interval</code></th><th>语义</th></tr></thead><tbody><tr><td>非空</td><td>非空</td><td>从绝对时间 <code>start</code> 开始周期发送</td></tr><tr><td>非空</td><td>空</td><td>在绝对时间发送一次</td></tr><tr><td>空</td><td>非空</td><td>相对当前时间等待一个 interval 后周期发送</td></tr><tr><td>空</td><td>空</td><td>停止 producer timer；实际工程直接调用 <code>co_time_stop_prod()</code> 表意更清楚</td></tr></tbody></table><p>发送回调 <code>co_time_timer(tp)</code> 使用的 <code>tp</code> 是本次 timer deadline：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">更新 0x1013 = tp - service creation time（µs，截断为 u32）</span><br><span class="line">    -&gt; 把 tp 转为 CANopen TIME_OF_DAY</span><br><span class="line">    -&gt; 构造 6 字节 TIME 帧</span><br><span class="line">    -&gt; can_net_send()</span><br></pre></td></tr></table></figure><p>这里存在两个值得注意的时基边界：</p><ol><li>TIME payload 表达的是绝对日期时间，通常需要与真实 UTC&#x2F;日历时钟一致；</li><li><code>can_net_t</code> 的超时调度在工程上往往接入单调时钟。若直接用单调时钟值编码 TIME payload，得到的不会是有意义的日历时间。</li></ol><p>因此 TIME producer 的 <code>can_net_t</code> 时间源、应用提供的 <code>start</code> 和系统实时时钟之间必须有明确的映射策略。源码不会替应用自动完成时钟同步、时区处理或 UTC 校准。</p><hr><h2 id="5-EMCY：活动错误栈、历史对象和发送抑制是三套不同状态"><a href="#5-EMCY：活动错误栈、历史对象和发送抑制是三套不同状态" class="headerlink" title="5. EMCY：活动错误栈、历史对象和发送抑制是三套不同状态"></a>5. EMCY：活动错误栈、历史对象和发送抑制是三套不同状态</h2><h3 id="5-1-co-emcy-t-内部不是一个错误码变量"><a href="#5-1-co-emcy-t-内部不是一个错误码变量" class="headerlink" title="5.1 co_emcy_t 内部不是一个错误码变量"></a>5.1 <code>co_emcy_t</code> 内部不是一个错误码变量</h3><p><code>src/co/emcy.c:85-123</code> 的核心状态包括：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">co_emcy_t</span><br><span class="line">├─ 0x1001 指针</span><br><span class="line">├─ 可选 0x1003 对象</span><br><span class="line">├─ 活动错误栈 msgs[]</span><br><span class="line">├─ CAN 帧缓冲 can_buf</span><br><span class="line">├─ inhibit timer 与下一允许发送时间</span><br><span class="line">├─ 127 个远端节点 consumer 槽位</span><br><span class="line">└─ received EMCY indication</span><br></pre></td></tr></table></figure><p>必须区分：</p><ul><li><strong>活动错误栈</strong>：当前仍未解除的错误条件，用于计算组合 error register；</li><li><strong><code>0x1003</code></strong>：向对象字典暴露的预定义错误域；</li><li><strong>CAN 帧缓冲</strong>：已经形成、但受 inhibit time 限制尚未发送的 EMCY 帧；</li><li><strong>consumer receiver</strong>：接收别的节点的 EMCY，与本节点 producer 错误栈无直接所有权关系。</li></ul><h3 id="5-2-相关对象"><a href="#5-2-相关对象" class="headerlink" title="5.2 相关对象"></a>5.2 相关对象</h3><table><thead><tr><th align="right">OD</th><th>作用</th><th>Lely 实现</th></tr></thead><tbody><tr><td align="right"><code>0x1001</code></td><td>Error register</td><td>每次 <code>co_emcy_send()</code> 前更新为当前组合值</td></tr><tr><td align="right"><code>0x1003</code></td><td>Pre-defined error field</td><td>把活动错误栈中的 EEC 写入子项，最新错误位于最前</td></tr><tr><td align="right"><code>0x1014</code></td><td>EMCY producer COB-ID</td><td>决定 producer 是否有效及 CAN-ID</td></tr><tr><td align="right"><code>0x1015</code></td><td>EMCY inhibit time，单位 100 µs</td><td>决定相邻发送帧最小间隔</td></tr><tr><td align="right"><code>0x1028</code></td><td>Emergency consumer object</td><td>每个子项把远端 Node-ID 关联到一个 EMCY COB-ID</td></tr></tbody></table><p>CiA 301 对 <code>0x1003</code> 的每个错误项定义为 32 位：[^cia301]</p><p>低 16 位是 error code，高 16 位是制造商附加信息。本源码 <code>co_emcy_set_1003()</code> 只写 <code>emcy-&gt;msgs[i].eec</code>，因此高 16 位保持 0。这是合法的“未提供附加信息”实现，但要注意：<code>co_emcy_push()</code> 传入的 5 字节 MSEF 不会进入 <code>0x1003</code>。</p><h3 id="5-3-push-如何建立新的错误状态"><a href="#5-3-push-如何建立新的错误状态" class="headerlink" title="5.3 push() 如何建立新的错误状态"></a>5.3 <code>push()</code> 如何建立新的错误状态</h3><p><code>co_emcy_push()</code> 的执行顺序：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">检查 eec != 0</span><br><span class="line">    -&gt; 强制置 Error Register bit0（generic error）</span><br><span class="line">    -&gt; 为活动错误栈扩容或检查静态容量</span><br><span class="line">    -&gt; 旧错误向后移动，新错误放到 index 0</span><br><span class="line">    -&gt; 更新 0x1003</span><br><span class="line">    -&gt; 对全部活动错误的 er 做按位 OR</span><br><span class="line">    -&gt; 形成 EMCY 帧并进入发送缓冲</span><br></pre></td></tr></table></figure><p>组合 error register 不是只取最新错误：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">for</span> (<span class="type">size_t</span> i = <span class="number">0</span>; i &lt; emcy-&gt;nmsg; i++)</span><br><span class="line">    er |= emcy-&gt;msgs[i].er;</span><br></pre></td></tr></table></figure><p>因此多个错误属于不同类别时，<code>0x1001</code> 可以同时反映多个类别。Lely v2.3.4 发布说明明确提到“error register 改为按需计算”，以支持按任意顺序解除错误。[^lely-v234]</p><h3 id="5-4-remove-、pop-与-clear"><a href="#5-4-remove-、pop-与-clear" class="headerlink" title="5.4 remove()、pop() 与 clear()"></a>5.4 <code>remove()</code>、<code>pop()</code> 与 <code>clear()</code></h3><table><thead><tr><th>API</th><th>行为</th></tr></thead><tbody><tr><td><code>co_emcy_pop()</code></td><td>读取并移除 index 0，即最近错误</td></tr><tr><td><code>co_emcy_remove(n)</code></td><td>移除任意栈位置，用于错误恢复顺序与发生顺序不同的情况</td></tr><tr><td><code>co_emcy_find(eec)</code></td><td>从新到旧查找指定 EEC</td></tr><tr><td><code>co_emcy_clear()</code></td><td>清空所有活动错误并发送 no-error EMCY</td></tr></tbody></table><p>移除一个错误后，源码发送 EEC <code>0x0000</code> 的 reset 报文：</p><ul><li>若仍有活动错误，error register 是剩余错误的 OR；MSEF 前两字节放当前最新错误 EEC；</li><li>若已无活动错误，发送 EEC <code>0x0000</code>、ER <code>0x00</code>。</li></ul><p>这解释了为什么“解除其中一个错误”不等于 <code>0x1001</code> 必然清零。</p><h3 id="5-5-inhibit-time-与发送缓冲"><a href="#5-5-inhibit-time-与发送缓冲" class="headerlink" title="5.5 inhibit time 与发送缓冲"></a>5.5 inhibit time 与发送缓冲</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">flowchart TD</span><br><span class="line">    ErrorEvent[&quot;push/remove/clear 产生 EMCY 状态变化&quot;] --&gt; Build[&quot;构造 8 字节 EMCY 帧&quot;]</span><br><span class="line">    Build --&gt; Queue[&quot;写入 can_buf&quot;]</span><br><span class="line">    Queue --&gt; Check&#123;&quot;当前时间 &gt;= inhibit deadline?&quot;&#125;</span><br><span class="line">    Check -- &quot;否&quot; --&gt; Arm[&quot;can_timer_start(deadline, one-shot)&quot;]</span><br><span class="line">    Arm --&gt; TimerCb[&quot;co_emcy_timer()&quot;]</span><br><span class="line">    TimerCb --&gt; Check</span><br><span class="line">    Check -- &quot;是&quot; --&gt; Update[&quot;deadline = now + 0x1015 * 100 us&quot;]</span><br><span class="line">    Update --&gt; Send[&quot;can_net_send() 发送一帧&quot;]</span><br><span class="line">    Send --&gt; More&#123;&quot;缓冲区还有帧?&quot;&#125;</span><br><span class="line">    More -- &quot;是&quot; --&gt; Check</span><br><span class="line">    More -- &quot;否&quot; --&gt; Done[&quot;结束&quot;]</span><br></pre></td></tr></table></figure><p><code>0x1015 == 0</code> 时，deadline 每次仍等于 <code>now</code>，循环可以在一次 <code>co_emcy_flush()</code> 中把全部缓存帧提交给 <code>can_net_send()</code>。当 inhibit 非零时，函数每次最多发送当前允许的一帧，然后为下一帧建立 one-shot deadline。</p><p>注意 <code>can_buf</code> 只是 Lely CAN 协议层的帧缓冲。<code>can_net_send()</code> 之后，io2 CAN channel 仍可能有自己的异步写队列；两者解决的是不同层的背压问题。</p><h3 id="5-6-远端-EMCY-consumer"><a href="#5-6-远端-EMCY-consumer" class="headerlink" title="5.6 远端 EMCY consumer"></a>5.6 远端 EMCY consumer</h3><p><code>0x1028</code> 的每个有效子项对应一个远端节点。初始化阶段，源码为 1..127 的节点槽位准备 receiver；启动或写入 <code>0x1028</code> 时，再按配置的 COB-ID 注册指定 receiver。</p><p>收到帧后：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">忽略 RTR</span><br><span class="line">    -&gt; 忽略 CAN FD 格式</span><br><span class="line">    -&gt; len &gt;= 2 时读取 EEC</span><br><span class="line">    -&gt; len &gt;= 3 时读取 ER</span><br><span class="line">    -&gt; len &gt;= 4 时读取 MSEF</span><br><span class="line">    -&gt; indication(emcy, node_id, eec, er, msef)</span><br></pre></td></tr></table></figure><p>本地归档的 MSEF 复制代码为：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">memcpy</span>(msef, msg-&gt;data + <span class="number">3</span>,</span><br><span class="line">        MAX((<span class="type">uint_least8_t</span>)(msg-&gt;len - <span class="number">3</span>), <span class="number">5</span>));</span><br></pre></td></tr></table></figure><p>从协议语义看，这里更合理的长度应是 <code>MIN(msg-&gt;len - 3, 5)</code>。当前 <code>MAX</code> 会在 DLC 为 4..7 时复制 5 字节，把有效 payload 之外的数据数组内容也交给用户回调。对经典 CAN 的固定 <code>data[8]</code> 布局而言，这通常不会越过结构体数组边界，但会把未由该帧声明的尾部字节当成 MSEF，属于<strong>已确认的有效长度处理异常</strong>。同样，本文未声称已完成运行时复现或确认上游当前分支状态。</p><h3 id="5-7-无动态内存时的资源边界"><a href="#5-7-无动态内存时的资源边界" class="headerlink" title="5.7 无动态内存时的资源边界"></a>5.7 无动态内存时的资源边界</h3><p>定义 <code>LELY_NO_MALLOC</code> 后，EMCY 改用静态存储：</p><table><thead><tr><th>宏</th><th align="right">默认值</th><th>影响</th></tr></thead><tbody><tr><td><code>CO_EMCY_MAX_NMSG</code></td><td align="right">8</td><td>同时保留的活动错误数量上限</td></tr><tr><td><code>CO_EMCY_CAN_BUF_SIZE</code></td><td align="right">16 帧</td><td>等待 inhibit time 发送的帧缓冲容量</td></tr></tbody></table><p><code>co_emcy_push()</code> 在活动错误达到上限后返回 <code>ERRNUM_NOMEM</code>；CAN 帧缓冲不能扩容时，<code>co_emcy_send()</code> 也会失败。嵌入式目标应根据最坏情况下的并发错误数和错误突发速率配置这两个宏，而不是只看单帧 EMCY 的 8 字节 payload。</p><hr><h2 id="6-SYNC-错误如何进入-EMCY"><a href="#6-SYNC-错误如何进入-EMCY" class="headerlink" title="6. SYNC 错误如何进入 EMCY"></a>6. SYNC 错误如何进入 EMCY</h2><p>SYNC 与 EMCY 并非完全独立。Lely 的 NMT service manager 在创建 SYNC 时安装两个回调：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">SYNC indication -&gt; co_nmt_on_sync()</span><br><span class="line">SYNC error      -&gt; co_nmt_on_err()</span><br></pre></td></tr></table></figure><p>当 SYNC DLC 与 <code>0x1019</code> 配置不匹配时：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">co_sync_recv()</span><br><span class="line">    -&gt; sync error callback(0x8240, 0x10)</span><br><span class="line">    -&gt; co_nmt_srv_sync_err()</span><br><span class="line">    -&gt; co_nmt_on_err()</span><br><span class="line">    -&gt; co_emcy_push()</span><br><span class="line">    -&gt; 更新 0x1001/0x1003</span><br><span class="line">    -&gt; 受 0x1015 约束发送 EMCY</span><br></pre></td></tr></table></figure><p>如果 EEC 属于 <code>0x81xx</code> communication error，<code>co_nmt_on_err()</code> 还会调用由 <code>0x1029:01</code> 指定的 communication error behavior。<code>0x8240</code> 不属于 <code>0x81xx</code>，因此它进入 EMCY，但不会走这条 communication error behavior 分支。</p><p>TIME 在当前 <code>nmt_srv.c</code> 中只被创建和销毁，没有像 SYNC 那样自动安装 indication&#x2F;error bridge。应用若需要在收到 TIME 后校准本地时钟，必须自行取得 TIME service 并设置 indication，或在更高层完成对应接入。</p><hr><h2 id="7-三个模块的运行模型对照"><a href="#7-三个模块的运行模型对照" class="headerlink" title="7. 三个模块的运行模型对照"></a>7. 三个模块的运行模型对照</h2><table><thead><tr><th>维度</th><th>SYNC</th><th>TIME</th><th>EMCY</th></tr></thead><tbody><tr><td>通信模型</td><td>producer&#x2F;consumer</td><td>producer&#x2F;consumer</td><td>producer&#x2F;consumer</td></tr><tr><td>典型默认 CAN-ID</td><td><code>0x080</code></td><td><code>0x100</code></td><td><code>0x080 + Node-ID</code></td></tr><tr><td>payload</td><td>0 或 1 字节</td><td>固定 6 字节</td><td>固定 8 字节</td></tr><tr><td>producer 触发</td><td><code>0x1006</code> 周期</td><td><code>co_time_start_prod()</code></td><td>错误状态变化</td></tr><tr><td>consumer 注册</td><td>非 producer 时自动注册</td><td><code>0x1012</code> consumer 位</td><td><code>0x1028</code> 每个远端节点</td></tr><tr><td>使用 timer</td><td>周期 timer</td><td>应用提交的 timer</td><td>inhibit deadline timer</td></tr><tr><td>内部队列</td><td>无</td><td>无</td><td>活动错误栈 + CAN 帧缓冲</td></tr><tr><td>与 NMT 集成</td><td>自动驱动 TPDO&#x2F;RPDO，错误可进入 EMCY</td><td>当前只创建&#x2F;销毁</td><td>作为 NMT 错误出口</td></tr><tr><td>动态配置对象</td><td><code>1005/1006/1019</code></td><td><code>1012</code></td><td><code>1003/1014/1028</code>，发送时读取 <code>1015</code></td></tr></tbody></table><p>三者共同的设计思想可以概括为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">对象字典定义通信参数</span><br><span class="line">    + can_net_t 提供 CAN 和时间输入</span><br><span class="line">    + service object 保存最小协议状态</span><br><span class="line">    + callback 把协议事件交给 NMT 或应用</span><br></pre></td></tr></table></figure><hr><h2 id="8-C-头文件：所有权和回调适配，不是第二套实现"><a href="#8-C-头文件：所有权和回调适配，不是第二套实现" class="headerlink" title="8. C++ 头文件：所有权和回调适配，不是第二套实现"></a>8. C++ 头文件：所有权和回调适配，不是第二套实现</h2><p>三个 <code>.hpp</code> 的结构基本一致：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">c_type_traits&lt;__co_xxx&gt;</span><br><span class="line">    alloc/free/init/fini</span><br><span class="line">        ↓</span><br><span class="line">incomplete_c_type&lt;__co_xxx&gt;</span><br><span class="line">        ↓</span><br><span class="line">COSync / COTime / COEmcy</span><br></pre></td></tr></table></figure><p>例如：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">COSync</span> : <span class="keyword">public</span> incomplete_c_type&lt;__co_sync&gt; &#123;</span><br><span class="line"> <span class="keyword">public</span>:</span><br><span class="line">  <span class="built_in">COSync</span>(CANNet* net, CODev* dev) : <span class="built_in">c_base</span>(net, dev) &#123;&#125;</span><br><span class="line">  <span class="function"><span class="type">int</span> <span class="title">start</span><span class="params">()</span> <span class="keyword">noexcept</span> </span>&#123; <span class="keyword">return</span> <span class="built_in">co_sync_start</span>(<span class="keyword">this</span>); &#125;</span><br><span class="line">  <span class="function"><span class="type">void</span> <span class="title">stop</span><span class="params">()</span> <span class="keyword">noexcept</span> </span>&#123; <span class="built_in">co_sync_stop</span>(<span class="keyword">this</span>); &#125;</span><br><span class="line">  <span class="comment">// setInd()/setErr() 仅把 callable 适配为 C callback。</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>关键点：</p><ol><li><code>alloc/init/fini/free</code> 最终仍调用 C 层 <code>__co_xxx_*()</code>；</li><li><code>start()</code>、<code>stop()</code>、<code>push()</code>、<code>startProd()</code> 等均是薄转发；</li><li>模板版 <code>setInd()</code>&#x2F;<code>setErr()</code> 使用 <code>c_obj_call</code> 或 <code>c_mem_call</code> 把函数对象、成员函数适配为 C callback；</li><li>三个类的析构函数都是 <code>protected</code>，说明它们不是按普通值类型随意栈上创建和销毁的接口；</li><li>协议行为、条件编译和 ABI 最终仍由 C 实现决定。</li></ol><p>Lely 官方 Doxygen 当前标识为 2.4.0，并将这些头文件描述为对应 C service object 的 C++ interface。[^lely-doxygen] 在新应用中，若已经使用 <code>liblely-coapp</code>，通常应优先通过 <code>Node/BasicMaster/BasicSlave</code> 等上层对象取得服务能力，而不是绕过 NMT service manager 再创建一套重复的 SYNC&#x2F;TIME&#x2F;EMCY 对象。</p><hr><h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><p>[^lely-overview]: <a href="https://opensource.lely.com/canopen/docs/overview/">Lely CANopen - Library overview</a>，说明协议栈的被动、异步架构以及 <code>can_net_t</code>、SYNC、TIME、EMCY service object。<br>[^lely-config]: <a href="https://opensource.lely.com/canopen/docs/configuration/">Lely CANopen - Build configuration</a>，说明 <code>--disable-sync</code>、<code>--disable-time</code>、<code>--disable-emcy</code>、<code>LELY_NO_MALLOC</code> 以及静态 EMCY 容量宏。<br>[^lely-v234]: <a href="https://opensource.lely.com/canopen/release/v2.3.4/">Lely CANopen - Release v2.3.4</a>，记录 <code>co_time_of_day_set()</code>、EMCY 任意错误移除、组合 error register 和 SYNC stop 行为修复。<br>[^lely-doxygen]: <a href="https://lely_industries.gitlab.io/lely-core/doxygen/">Lely core libraries 2.4.0 Doxygen</a>，用于核对公开 API、C&#x2F;C++ 包装和版本标识。<br>[^lely-standards]: <a href="https://opensource.lely.com/canopen/docs/standards/">Lely CANopen - Standards support</a>，说明 CiA 301 中 SYNC、TIME、EMCY 及相关对象的支持范围。<br>[^cia301]: 《CiA 301 CANopen 应用层和通信协议 V4.2.0（中文注释版）》，重点参考 7.2.5 SYNC、7.2.6 TIME、7.2.7 EMCY，以及 7.5.2 中 <code>0x1003/0x1005/0x1006/0x1007/0x1012/0x1013/0x1014/0x1015/0x1019/0x1028</code> 对象定义。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;meta name=&quot;referrer&quot; content=&quot;no-referrer&quot; /&gt;

&lt;h1 id=&quot;Lely-CANopen-SYNC、TIME-与-EMCY学习&quot;&gt;&lt;a href=&quot;#Lely-CANopen-SYNC、TIME-与-EMCY学习&quot; class=&quot;h</summary>
      
    
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/categories/canopen/"/>
    
    
    <category term="canopen" scheme="https://wdfk-prog.space/tags/canopen/"/>
    
  </entry>
  
  <entry>
    <title>linux学习笔记系列</title>
    <link href="https://wdfk-prog.space/posts/29909618/"/>
    <id>https://wdfk-prog.space/posts/29909618/</id>
    <published>2026-07-31T07:12:35.000Z</published>
    <updated>2026-08-01T08:03:04.885Z</updated>
    
    <content type="html"><![CDATA[<h1 id="linux学习笔记系列"><a href="#linux学习笔记系列" class="headerlink" title="linux学习笔记系列"></a>linux学习笔记系列</h1><ul><li><ol><li><a href="./arch/">arch</a></li></ol><ul><li>1.1. <a href="./arch/arm/">arm</a><ul><li><a href="./arch/arm/assembly.md">1.1.1. assembly</a> (<a href="https://wdfk-prog.space/posts/1a09ef0b/">个人博客链接</a>)</li><li><a href="./arch/arm/boot.md">1.1.2. boot</a> (<a href="https://wdfk-prog.space/posts/46edaec4/">个人博客链接</a>)</li><li><a href="./arch/arm/debug.md">1.1.3. debug</a> (<a href="https://wdfk-prog.space/posts/6ca547a7/">个人博客链接</a>)</li><li><a href="./arch/arm/include.md">1.1.4. include</a> (<a href="https://wdfk-prog.space/posts/70ad6c8b/">个人博客链接</a>)</li><li><a href="./arch/arm/kernel.md">1.1.5. kernel</a> (<a href="https://wdfk-prog.space/posts/5dd29aab/">个人博客链接</a>)</li><li><a href="./arch/arm/lds.md">1.1.6. lds</a> (<a href="https://wdfk-prog.space/posts/76116573/">个人博客链接</a>)</li><li><a href="./arch/arm/lib.md">1.1.7. lib</a> (<a href="https://wdfk-prog.space/posts/a90f3bcc/">个人博客链接</a>)</li><li><a href="./arch/arm/mm.md">1.1.8. mm</a> (<a href="https://wdfk-prog.space/posts/a2891af0/">个人博客链接</a>)</li><li><a href="./arch/arm/process.md">1.1.9. process</a> (<a href="https://wdfk-prog.space/posts/861d1896/">个人博客链接</a>)</li><li><a href="./arch/arm/syscall_table%E7%94%9F%E6%88%90%E6%B5%81%E7%A8%8B.md">1.1.10. sys_call_table</a> (<a href="https://wdfk-prog.space/posts/b50f985b/">个人博客链接</a>)</li></ul></li></ul></li><li><ol start="2"><li><a href="./block/">block</a></li></ol><ul><li><a href="./block/bfq-iosched.md">2.1. bfq-iosched</a> (<a href="https://wdfk-prog.space/posts/5e919c43/">个人博客链接</a>)</li><li><a href="./block/bio.md">2.2. bio</a> (<a href="https://wdfk-prog.space/posts/dd206a7b/">个人博客链接</a>)</li><li><a href="./block/blk-core.md">2.3. blk-core</a> (<a href="https://wdfk-prog.space/posts/aa511041/">个人博客链接</a>)</li><li><a href="./block/blk-ioc.md">2.4. blk-ioc</a> (<a href="https://wdfk-prog.space/posts/3311cb09/">个人博客链接</a>)</li><li><a href="./block/blk-mq.md">2.5. blk-mq</a> (<a href="https://wdfk-prog.space/posts/83df644f/">个人博客链接</a>)</li><li><a href="./block/block.md">2.6. block</a> (<a href="https://wdfk-prog.space/posts/831b9722/">个人博客链接</a>)</li><li><a href="./block/brd.md">2.7. brd</a> (<a href="https://wdfk-prog.space/posts/e3c47869/">个人博客链接</a>)</li><li><a href="./block/elevator.md">2.8. elevator</a> (<a href="https://wdfk-prog.space/posts/1398eb33/">个人博客链接</a>)</li><li><a href="./block/fops.md">2.9. fops</a> (<a href="https://wdfk-prog.space/posts/9ab1a2ae/">个人博客链接</a>)</li><li><a href="./block/genhd.md">2.10. genhd</a> (<a href="https://wdfk-prog.space/posts/447af9b5/">个人博客链接</a>)</li><li><a href="./block/kyber-iosched.md">2.11. kyber-iosched</a> (<a href="https://wdfk-prog.space/posts/b8013273/">个人博客链接</a>)</li><li><a href="./block/mq-deadline.md">2.12. mq-deadline</a> (<a href="https://wdfk-prog.space/posts/48974a1a/">个人博客链接</a>)</li></ul></li><li><ol start="3"><li><a href="./drivers/">drivers</a></li></ol><ul><li>3.1. <a href="./drivers/base/">base</a><ul><li><a href="./drivers/base/base.md">3.1.1. base</a> (<a href="https://wdfk-prog.space/posts/c0b4fe61/">个人博客链接</a>)</li><li><a href="./drivers/base/cacheinfo.md">3.1.2. cacheinfo</a> (<a href="https://wdfk-prog.space/posts/7a40c4d3/">个人博客链接</a>)</li><li><a href="./drivers/base/class.md">3.1.3. class</a> (<a href="https://wdfk-prog.space/posts/ed4b199f/">个人博客链接</a>)</li><li><a href="./drivers/base/container.md">3.1.4. container</a> (<a href="https://wdfk-prog.space/posts/c7a2ec1b/">个人博客链接</a>)</li><li><a href="./drivers/base/core.md">3.1.5. core</a> (<a href="https://wdfk-prog.space/posts/6b8d854f/">个人博客链接</a>)</li><li><a href="./drivers/base/cpu.md">3.1.6. cpu</a> (<a href="https://wdfk-prog.space/posts/ba80502e/">个人博客链接</a>)</li><li><a href="./drivers/base/dd.md">3.1.7. dd</a> (<a href="https://wdfk-prog.space/posts/a97191d/">个人博客链接</a>)</li><li><a href="./drivers/base/devres.md">3.1.8. devres</a> (<a href="https://wdfk-prog.space/posts/bbd2e8c8/">个人博客链接</a>)</li><li><a href="./drivers/base/devtmpfs.md">3.1.9. devtmpfs</a> (<a href="https://wdfk-prog.space/posts/5e321190/">个人博客链接</a>)</li><li><a href="./drivers/base/faux.md">3.1.10. faux</a> (<a href="https://wdfk-prog.space/posts/7a8aa269/">个人博客链接</a>)</li><li><a href="./drivers/base/firmware.md">3.1.11. firmware</a> (<a href="https://wdfk-prog.space/posts/d5ecd7c4/">个人博客链接</a>)</li><li><a href="./drivers/base/map.md">3.1.12. map</a> (<a href="https://wdfk-prog.space/posts/93adaabb/">个人博客链接</a>)</li><li><a href="./drivers/base/platform.md">3.1.13. platform</a> (<a href="https://wdfk-prog.space/posts/3952d0cb/">个人博客链接</a>)</li><li><a href="./drivers/base/regmap.md">3.1.14. regmap</a> (<a href="https://wdfk-prog.space/posts/6ce74947/">个人博客链接</a>)</li><li><a href="./drivers/base/syscore.md">3.1.15. syscore</a> (<a href="https://wdfk-prog.space/posts/987a898d/">个人博客链接</a>)</li><li><a href="./drivers/base/topology.md">3.1.16. topology</a> (<a href="https://wdfk-prog.space/posts/1198610c/">个人博客链接</a>)</li></ul></li><li>3.2. <a href="./drivers/bus/">bus</a><ul><li><a href="./drivers/bus/bus.md">3.2.1. bus</a> (<a href="https://wdfk-prog.space/posts/2f566b69/">个人博客链接</a>)</li><li><a href="./drivers/bus/simple-pm-bus.md">3.2.2. simple-pm-bus</a> (<a href="https://wdfk-prog.space/posts/b441a160/">个人博客链接</a>)</li></ul></li><li>3.3. <a href="./drivers/clk/">clk</a><ul><li><a href="./drivers/clk/clk-bulk.md">3.3.1. clk-bulk</a> (<a href="https://wdfk-prog.space/posts/fc0f43c4/">个人博客链接</a>)</li><li><a href="./drivers/clk/clk.md">3.3.2. clk</a> (<a href="https://wdfk-prog.space/posts/a6f83010/">个人博客链接</a>)</li></ul></li><li>3.4. <a href="./drivers/dma/">dma</a><ul><li><a href="./drivers/dma/coherent.md">3.4.1. coherent</a> (<a href="https://wdfk-prog.space/posts/5a79feca/">个人博客链接</a>)</li><li><a href="./drivers/dma/dma-buf.md">3.4.2. dma-buf</a> (<a href="https://wdfk-prog.space/posts/1d3fd482/">个人博客链接</a>)</li><li><a href="./drivers/dma/dma.md">3.4.3. dma</a> (<a href="https://wdfk-prog.space/posts/5a79feca/">个人博客链接</a>)</li><li><a href="./drivers/dma/dmaengine.md">3.4.4. dmaengine</a> (<a href="https://wdfk-prog.space/posts/d8c38d13/">个人博客链接</a>)</li><li><a href="./drivers/dma/dmapool.md">3.4.5. dmapool</a> (<a href="https://wdfk-prog.space/posts/d8c38d13/">个人博客链接</a>)</li><li><a href="./drivers/dma/stm32-dma.md">3.4.6. stm32-dma</a> (<a href="https://wdfk-prog.space/posts/4333972f/">个人博客链接</a>)</li></ul></li><li>3.5. <a href="./drivers/gpio/">gpio</a><ul><li><a href="./drivers/gpio/gpio.md">3.5.1. gpio</a> (<a href="https://wdfk-prog.space/posts/ba76d4d1/">个人博客链接</a>)</li><li><a href="./drivers/gpio/gpiolib.md">3.5.2. gpiolib</a> (<a href="https://wdfk-prog.space/posts/7976b063/">个人博客链接</a>)</li></ul></li><li>3.6. <a href="./drivers/hid/">hid</a><ul><li><a href="./drivers/hid/hid-core.md">3.6.1. hid-core</a> (<a href="https://wdfk-prog.space/posts/98ff91fb/">个人博客链接</a>)</li></ul></li><li>3.7. <a href="./drivers/I2C/">I2C</a><ul><li><a href="./drivers/I2C/busses.md">3.7.1. busses</a> (<a href="https://wdfk-prog.space/posts/3b770652/">个人博客链接</a>)</li><li><a href="./drivers/I2C/i2c-core.md">3.7.2. i2c</a> (<a href="https://wdfk-prog.space/posts/818c4d2e/">个人博客链接</a>)</li><li><a href="./drivers/I2C/i2c-dev.md">3.7.3. i2c-dev</a> (<a href="https://wdfk-prog.space/posts/a8082287/">个人博客链接</a>)</li><li><a href="./drivers/I2C/i2c-smbus.md">3.7.4. i2c-smbus</a> (<a href="https://wdfk-prog.space/posts/7764307b/">个人博客链接</a>)</li></ul></li><li>3.8. <a href="./drivers/input/">input</a><ul><li><a href="./drivers/input/input.md">3.8.1. input</a> (<a href="https://wdfk-prog.space/posts/d82832d7/">个人博客链接</a>)</li><li><a href="./drivers/input/serio.md">3.8.2. serio</a> (<a href="https://wdfk-prog.space/posts/4aef7a2a/">个人博客链接</a>)</li></ul></li><li>3.9. <a href="./drivers/mmc/">mmc</a><ul><li><a href="./drivers/mmc/mmc_core.md">3.9.1. mmc</a> (<a href="https://wdfk-prog.space/posts/bba6a469/">个人博客链接</a>)</li><li><a href="./drivers/mmc/mmc_sd.md">3.9.2. mmc_sd</a> (<a href="https://wdfk-prog.space/posts/64b4b0cf/">个人博客链接</a>)</li><li><a href="./drivers/mmc/mmc_sdio.md">3.9.3. mmc_sdio</a> (<a href="https://wdfk-prog.space/posts/afe8a261/">个人博客链接</a>)</li><li><a href="./drivers/mmc/mmci.md">3.9.4. mmci</a> (<a href="https://wdfk-prog.space/posts/d2b94929/">个人博客链接</a>)</li></ul></li><li>3.10. <a href="./drivers/tty/">tty</a><ul><li><a href="./drivers/tty/serial.md">3.10.1. serial</a> (<a href="https://wdfk-prog.space/posts/d374c9dc/">个人博客链接</a>)</li><li><a href="./drivers/tty/serial_core.md">3.10.2. serial_core</a> (<a href="https://wdfk-prog.space/posts/880590d0/">个人博客链接</a>)</li><li><a href="./drivers/tty/stm32-usart.md">3.10.3. stm32-usart</a> (<a href="https://wdfk-prog.space/posts/9a895f89/">个人博客链接</a>)</li><li><a href="./drivers/tty/sysrq.md">3.10.4. sysrq</a> (<a href="https://wdfk-prog.space/posts/2db0ec3/">个人博客链接</a>)</li><li><a href="./drivers/tty/tty.md">3.10.5. tty</a> (<a href="https://wdfk-prog.space/posts/ce336cf4/">个人博客链接</a>)</li></ul></li><li><a href="./drivers/amba.md">3.11. amba</a> (<a href="https://wdfk-prog.space/posts/8faf4fe2/">个人博客链接</a>)</li><li><a href="./drivers/char.md">3.12. char</a> (<a href="https://wdfk-prog.space/posts/8cfe579f/">个人博客链接</a>)</li><li><a href="./drivers/clocksource.md">3.13. clocksource</a> (<a href="https://wdfk-prog.space/posts/d32d8ac9/">个人博客链接</a>)</li><li><a href="./drivers/dma-buf.md">3.14. dma-buf</a> (<a href="https://wdfk-prog.space/posts/1d3fd482/">个人博客链接</a>)</li><li><a href="./drivers/fdt.md">3.15. fdt</a> (<a href="https://wdfk-prog.space/posts/e5e27506/">个人博客链接</a>)</li><li><a href="./drivers/iio.md">3.16. iio</a> (<a href="https://wdfk-prog.space/posts/d175859a/">个人博客链接</a>)</li><li><a href="./drivers/leds.md">3.17. leds</a> (<a href="https://wdfk-prog.space/posts/d6351049/">个人博客链接</a>)</li><li><a href="./drivers/mfd.md">3.18. mfd</a> (<a href="https://wdfk-prog.space/posts/c636e801/">个人博客链接</a>)</li><li><a href="./drivers/nvmem.md">3.19. nvmem</a> (<a href="https://wdfk-prog.space/posts/c5f21b05/">个人博客链接</a>)</li><li><a href="./drivers/of.md">3.20. OF</a> (<a href="https://wdfk-prog.space/posts/a987a590/">个人博客链接</a>)</li><li><a href="./drivers/pinctrl.md">3.21. pinctrl</a> (<a href="https://wdfk-prog.space/posts/5b9b368d/">个人博客链接</a>)</li><li><a href="./drivers/regmap.md">3.22. regmap</a> (<a href="https://wdfk-prog.space/posts/6ce74947/">个人博客链接</a>)</li><li><a href="./drivers/regulator.md">3.23. regulator</a> (<a href="https://wdfk-prog.space/posts/656712e5/">个人博客链接</a>)</li><li><a href="./drivers/reset.md">3.24. reset</a> (<a href="https://wdfk-prog.space/posts/509dbf4d/">个人博客链接</a>)</li><li><a href="./drivers/rtc.md">3.25. rtc</a> (<a href="https://wdfk-prog.space/posts/37dce93c/">个人博客链接</a>)</li><li><a href="./drivers/watchdog.md">3.26. watchdog</a> (<a href="https://wdfk-prog.space/posts/20a87d78/">个人博客链接</a>)</li></ul></li><li><ol start="4"><li><a href="./fs/">fs</a></li></ol><ul><li><a href="./fs/anon_inodes.md">4.1. anon_inodes</a> (<a href="https://wdfk-prog.space/posts/52310506/">个人博客链接</a>)</li><li><a href="./fs/binfmt_script.md">4.2. binfmt_script</a> (<a href="https://wdfk-prog.space/posts/f793f059/">个人博客链接</a>)</li><li><a href="./fs/buffer.md">4.3. buffer</a> (<a href="https://wdfk-prog.space/posts/315af0f2/">个人博客链接</a>)</li><li><a href="./fs/dcache.md">4.4. dcache</a> (<a href="https://wdfk-prog.space/posts/a5c27015/">个人博客链接</a>)</li><li><a href="./fs/drop_caches.md">4.5. drop_caches</a> (<a href="https://wdfk-prog.space/posts/17c21950/">个人博客链接</a>)</li><li><a href="./fs/exec.md">4.6. exec</a> (<a href="https://wdfk-prog.space/posts/bb6f4ec5/">个人博客链接</a>)</li><li><a href="./fs/ext4.md">4.7. ext4</a> (<a href="https://wdfk-prog.space/posts/1db9a982/">个人博客链接</a>)</li><li><a href="./fs/fcntl.md">4.8. fcntl</a> (<a href="https://wdfk-prog.space/posts/b4dd5ab6/">个人博客链接</a>)</li><li><a href="./fs/file.md">4.9. file</a> (<a href="https://wdfk-prog.space/posts/8c9f3610/">个人博客链接</a>)</li><li><a href="./fs/file_table.md">4.10. file_table</a> (<a href="https://wdfk-prog.space/posts/5caa3f71/">个人博客链接</a>)</li><li><a href="./fs/filesystems.md">4.11. filesystems</a> (<a href="https://wdfk-prog.space/posts/2089198e/">个人博客链接</a>)</li><li><a href="./fs/fs-writeback.md">4.12. fs-writeback</a> (<a href="https://wdfk-prog.space/posts/88ab2b13/">个人博客链接</a>)</li><li><a href="./fs/fs_context.md">4.13. fs_context</a> (<a href="https://wdfk-prog.space/posts/56a583b/">个人博客链接</a>)</li><li><a href="./fs/fs_parser.md">4.14. fs_parser</a> (<a href="https://wdfk-prog.space/posts/a042817d/">个人博客链接</a>)</li><li><a href="./fs/fs_struct.md">4.15. fs_struct</a> (<a href="https://wdfk-prog.space/posts/35629520/">个人博客链接</a>)</li><li><a href="./fs/initramfs.md">4.16. initramfs</a> (<a href="https://wdfk-prog.space/posts/e7194024/">个人博客链接</a>)</li><li><a href="./fs/inode.md">4.17. inode</a> (<a href="https://wdfk-prog.space/posts/f63bd578/">个人博客链接</a>)</li><li><a href="./fs/iomap.md">4.18. iomap</a> (<a href="https://wdfk-prog.space/posts/5da976dd/">个人博客链接</a>)</li><li><a href="./fs/kernfs.md">4.19. kernfs</a> (<a href="https://wdfk-prog.space/posts/fbf7c49d/">个人博客链接</a>)</li><li><a href="./fs/libfs.md">4.20. libfs</a> (<a href="https://wdfk-prog.space/posts/6d362631/">个人博客链接</a>)</li><li><a href="./fs/locks.md">4.21. locks</a> (<a href="https://wdfk-prog.space/posts/fc316d97/">个人博客链接</a>)</li><li><a href="./fs/mbcache.md">4.22. mbcache</a> (<a href="https://wdfk-prog.space/posts/b554c0f0/">个人博客链接</a>)</li><li><a href="./fs/mnt_idmapping.md">4.23. mnt_idmapping</a> (<a href="https://wdfk-prog.space/posts/f1c7d54a/">个人博客链接</a>)</li><li><a href="./fs/namei.md">4.24. namei</a> (<a href="https://wdfk-prog.space/posts/f51b03a/">个人博客链接</a>)</li><li><a href="./fs/namespace.md">4.25. namespace</a> (<a href="https://wdfk-prog.space/posts/33e16b56/">个人博客链接</a>)</li><li><a href="./fs/nsfs.md">4.26. nsfs</a> (<a href="https://wdfk-prog.space/posts/56a16582/">个人博客链接</a>)</li><li><a href="./fs/open.md">4.27. open</a> (<a href="https://wdfk-prog.space/posts/a47083a4/">个人博客链接</a>)</li><li><a href="./fs/pidfs.md">4.28. pidfs</a> (<a href="https://wdfk-prog.space/posts/ccab2000/">个人博客链接</a>)</li><li><a href="./fs/pipe.md">4.29. pipe</a> (<a href="https://wdfk-prog.space/posts/1f9a630e/">个人博客链接</a>)</li><li><a href="./fs/proc.md">4.30. proc</a> (<a href="https://wdfk-prog.space/posts/2bd08434/">个人博客链接</a>)</li><li><a href="./fs/ramfs.md">4.31. ramfs</a> (<a href="https://wdfk-prog.space/posts/7c0e6000/">个人博客链接</a>)</li><li><a href="./fs/read_write.md">4.32. read_write</a> (<a href="https://wdfk-prog.space/posts/22eb068b/">个人博客链接</a>)</li><li><a href="./fs/seq_file.md">4.33. seq_file</a> (<a href="https://wdfk-prog.space/posts/4995452a/">个人博客链接</a>)</li><li><a href="./fs/stat.md">4.34. stat</a> (<a href="https://wdfk-prog.space/posts/20b8ff21/">个人博客链接</a>)</li><li><a href="./fs/super.md">4.35. super</a> (<a href="https://wdfk-prog.space/posts/d6c1da0e/">个人博客链接</a>)</li><li><a href="./fs/sync.md">4.36. sync</a> (<a href="https://wdfk-prog.space/posts/2c2bf57a/">个人博客链接</a>)</li><li><a href="./fs/sysfs.md">4.37. sysfs</a> (<a href="https://wdfk-prog.space/posts/c27bb8ba/">个人博客链接</a>)</li></ul></li><li><ol start="5"><li><a href="./include/">include</a></li></ol><ul><li><a href="./include/atomic.md">5.1. atomic</a> (<a href="https://wdfk-prog.space/posts/3df66479/">个人博客链接</a>)</li><li><a href="./include/bits.md">5.2. bits</a> (<a href="https://wdfk-prog.space/posts/75328c4f/">个人博客链接</a>)</li><li><a href="./include/compiler.md">5.3. compiler</a> (<a href="https://wdfk-prog.space/posts/aa62bd49/">个人博客链接</a>)</li><li><a href="./include/hashtable.md">5.4. hashtable</a> (<a href="https://wdfk-prog.space/posts/14dfa8ed/">个人博客链接</a>)</li><li><a href="./include/%E4%BB%A3%E7%A0%81%E8%A1%A5%E4%B8%81.md">5.5. 代码补丁</a> (<a href="https://wdfk-prog.space/posts/1557581/">个人博客链接</a>)</li><li><a href="./include/%E5%86%85%E5%AD%98%E7%AE%A1%E7%90%86%E4%B8%8E%E8%AE%BF%E9%97%AE.md">5.6. 内存管理与访问</a> (<a href="https://wdfk-prog.space/posts/85edd85d/">个人博客链接</a>)</li><li><a href="./include/%E5%86%85%E6%A0%B8%E6%94%AF%E6%8C%81%E4%B8%8E%E6%95%B0%E6%8D%AE.md">5.7. 内核支持与数据</a> (<a href="https://wdfk-prog.space/posts/826ad06a/">个人博客链接</a>)</li><li><a href="./include/%E5%BA%95%E5%B1%82CPU%E4%B8%8E%E4%BD%93%E7%B3%BB%E7%BB%93%E6%9E%84%E5%AE%8F.md">5.8. 底层CPU与体系结构宏</a> (<a href="https://wdfk-prog.space/posts/febaf417/">个人博客链接</a>)</li><li><a href="./include/%E9%80%9A%E7%94%A8%E5%B7%A5%E5%85%B7%E4%B8%8E%E9%94%99%E8%AF%AF%E5%A4%84%E7%90%86%E5%AE%8F.md">5.9. 通用工具与错误处理宏</a> (<a href="https://wdfk-prog.space/posts/41f8aaaf/">个人博客链接</a>)</li><li><a href="./include/%E9%80%9A%E7%94%A8%E6%95%B0%E5%AD%A6%E4%B8%8E%E4%BD%8D%E6%93%8D%E4%BD%9C%E5%AE%8F.md">5.10. 通用数学与位操作宏</a> (<a href="https://wdfk-prog.space/posts/1c76181d/">个人博客链接</a>)</li></ul></li><li><ol start="6"><li><a href="./kernel/">kernel</a></li></ol><ul><li>6.1. <a href="./kernel/irq/">irq</a><ul><li><a href="./kernel/irq/chip.md">6.1.1. chained_irq</a> (<a href="https://wdfk-prog.space/posts/e55ce67f/">个人博客链接</a>)</li><li><a href="./kernel/irq/drivers.md">6.1.2. drivers</a> (<a href="https://wdfk-prog.space/posts/e410c307/">个人博客链接</a>)</li><li><a href="./kernel/irq/irq.md">6.1.3. interrupt</a> (<a href="https://wdfk-prog.space/posts/fbff46d9/">个人博客链接</a>)</li><li><a href="./kernel/irq/irq_work.md">6.1.4. irq_work</a> (<a href="https://wdfk-prog.space/posts/d9469419/">个人博客链接</a>)</li><li><a href="./kernel/irq/manage.md">6.1.5. manage</a> (<a href="https://wdfk-prog.space/posts/2472aa4a/">个人博客链接</a>)</li><li><a href="./kernel/irq/softirq.md">6.1.6. softirq</a> (<a href="https://wdfk-prog.space/posts/366cb710/">个人博客链接</a>)</li></ul></li><li>6.2. <a href="./kernel/lock/">lock</a><ul><li><a href="./kernel/lock/debug_lock.md">6.2.1. debug_locks</a> (<a href="https://wdfk-prog.space/posts/a3bbff77/">个人博客链接</a>)</li><li><a href="./kernel/lock/local_lock.md">6.2.2. local_lock</a> (<a href="https://wdfk-prog.space/posts/bb90f1dd/">个人博客链接</a>)</li><li><a href="./kernel/lock/mutex.md">6.2.3. mutex</a> (<a href="https://wdfk-prog.space/posts/eecdb22f/">个人博客链接</a>)</li><li><a href="./kernel/lock/rwlock.md">6.2.4. rwlock</a> (<a href="https://wdfk-prog.space/posts/1a1175a4/">个人博客链接</a>)</li><li><a href="./kernel/lock/rwsem.md">6.2.5. rwsem</a> (<a href="https://wdfk-prog.space/posts/cee68899/">个人博客链接</a>)</li><li><a href="./kernel/lock/seqlock.md">6.2.6. seqlock</a> (<a href="https://wdfk-prog.space/posts/7aac064c/">个人博客链接</a>)</li><li><a href="./kernel/lock/spinlock.md">6.2.7. spinlock</a> (<a href="https://wdfk-prog.space/posts/4a74a9d/">个人博客链接</a>)</li></ul></li><li>6.3. <a href="./kernel/rcu/">rcu</a><ul><li><a href="./kernel/rcu/RCU.md">6.3.1. rcu</a> (<a href="https://wdfk-prog.space/posts/c68bd8fb/">个人博客链接</a>)</li><li><a href="./kernel/rcu/SRCU.md">6.3.2. SRCU</a> (<a href="https://wdfk-prog.space/posts/57913c33/">个人博客链接</a>)</li><li><a href="./kernel/rcu/tree.md">6.3.3. tree</a> (<a href="https://wdfk-prog.space/posts/b73e5edc/">个人博客链接</a>)</li></ul></li><li>6.4. <a href="./kernel/sched/">sched</a><ul><li><a href="./kernel/sched/deadline.md">6.4.1. deadline</a> (<a href="https://wdfk-prog.space/posts/b74774f2/">个人博客链接</a>)</li><li><a href="./kernel/sched/fair.md">6.4.2. fair</a> (<a href="https://wdfk-prog.space/posts/7c28162a/">个人博客链接</a>)</li><li><a href="./kernel/sched/idle.md">6.4.3. idle</a> (<a href="https://wdfk-prog.space/posts/dc24b515/">个人博客链接</a>)</li><li><a href="./kernel/sched/preempt.md">6.4.4. preempt</a> (<a href="https://wdfk-prog.space/posts/529cc833/">个人博客链接</a>)</li><li><a href="./kernel/sched/rt.md">6.4.5. rt</a> (<a href="https://wdfk-prog.space/posts/bb8bcae/">个人博客链接</a>)</li><li><a href="./kernel/sched/sched.md">6.4.6. sched</a> (<a href="https://wdfk-prog.space/posts/455295d4/">个人博客链接</a>)</li><li><a href="./kernel/sched/syscalls.md">6.4.7. syscalls</a> (<a href="https://wdfk-prog.space/posts/af3a081f/">个人博客链接</a>)</li></ul></li><li>6.5. <a href="./kernel/time/">time</a><ul><li><a href="./kernel/time/alarmtimer.md">6.5.1. alarmtimer</a> (<a href="https://wdfk-prog.space/posts/18d6212c/">个人博客链接</a>)</li><li><a href="./kernel/time/clockevents.md">6.5.2. clockevents</a> (<a href="https://wdfk-prog.space/posts/df20a2f0/">个人博客链接</a>)</li><li><a href="./kernel/time/clocksource.md">6.5.3. clocksource</a> (<a href="https://wdfk-prog.space/posts/d32d8ac9/">个人博客链接</a>)</li><li><a href="./kernel/time/hrtimer.md">6.5.4. hrtimer</a> (<a href="https://wdfk-prog.space/posts/cf2d5831/">个人博客链接</a>)</li><li><a href="./kernel/time/jiffies.md">6.5.5. jiffies</a> (<a href="https://wdfk-prog.space/posts/3eef7be/">个人博客链接</a>)</li><li><a href="./kernel/time/sched_clock.md">6.5.6. sched_clock</a> (<a href="https://wdfk-prog.space/posts/b35f2c55/">个人博客链接</a>)</li><li><a href="./kernel/time/tick.md">6.5.7. tick</a> (<a href="https://wdfk-prog.space/posts/16af98cc/">个人博客链接</a>)</li><li><a href="./kernel/time/time.md">6.5.8. time</a> (<a href="https://wdfk-prog.space/posts/6f949845/">个人博客链接</a>)</li><li><a href="./kernel/time/timekeeping.md">6.5.9. timekeeping</a> (<a href="https://wdfk-prog.space/posts/b962eb21/">个人博客链接</a>)</li><li><a href="./kernel/time/timer.md">6.5.10. timer</a> (<a href="https://wdfk-prog.space/posts/6ad0de1a/">个人博客链接</a>)</li></ul></li><li><a href="./kernel/async.md">6.6. async</a> (<a href="https://wdfk-prog.space/posts/6f1f8386/">个人博客链接</a>)</li><li><a href="./kernel/bounds.md">6.7. bounds</a> (<a href="https://wdfk-prog.space/posts/39a9d7cf/">个人博客链接</a>)</li><li><a href="./kernel/completion.md">6.8. completion</a> (<a href="https://wdfk-prog.space/posts/38d6377e/">个人博客链接</a>)</li><li><a href="./kernel/cpu.md">6.9. cpu</a> (<a href="https://wdfk-prog.space/posts/ba80502e/">个人博客链接</a>)</li><li><a href="./kernel/cred.md">6.10. cred</a> (<a href="https://wdfk-prog.space/posts/df7016c/">个人博客链接</a>)</li><li><a href="./kernel/exit.md">6.11. exit</a> (<a href="https://wdfk-prog.space/posts/9409840e/">个人博客链接</a>)</li><li><a href="./kernel/fork.md">6.12. fork</a> (<a href="https://wdfk-prog.space/posts/bbeb587a/">个人博客链接</a>)</li><li><a href="./kernel/iomem.md">6.13. iomem</a> (<a href="https://wdfk-prog.space/posts/5ac3df00/">个人博客链接</a>)</li><li><a href="./kernel/kallsyms.md">6.14. kallsyms</a> (<a href="https://wdfk-prog.space/posts/dd631879/">个人博客链接</a>)</li><li><a href="./kernel/ksysfs.md">6.15. ksysfs</a> (<a href="https://wdfk-prog.space/posts/d7a8119d/">个人博客链接</a>)</li><li><a href="./kernel/kthread.md">6.16. kthread</a> (<a href="https://wdfk-prog.space/posts/943f2a41/">个人博客链接</a>)</li><li><a href="./kernel/memory_barrier.md">6.17. memory_barrier</a> (<a href="https://wdfk-prog.space/posts/d2090477/">个人博客链接</a>)</li><li><a href="./kernel/notifier.md">6.18. notifier</a> (<a href="https://wdfk-prog.space/posts/26e6dc85/">个人博客链接</a>)</li><li><a href="./kernel/nsproxy.md">6.19. nsproxy</a> (<a href="https://wdfk-prog.space/posts/7e03a90a/">个人博客链接</a>)</li><li><a href="./kernel/panic.md">6.20. panic</a> (<a href="https://wdfk-prog.space/posts/9ea78192/">个人博客链接</a>)</li><li><a href="./kernel/params.md">6.21. params</a> (<a href="https://wdfk-prog.space/posts/8fce0ef3/">个人博客链接</a>)</li><li><a href="./kernel/pid.md">6.22. pid</a> (<a href="https://wdfk-prog.space/posts/5550c4ed/">个人博客链接</a>)</li><li><a href="./kernel/power.md">6.23. power</a> (<a href="https://wdfk-prog.space/posts/ab8a01a0/">个人博客链接</a>)</li><li><a href="./kernel/printk.md">6.24. printk</a> (<a href="https://wdfk-prog.space/posts/aca6454c/">个人博客链接</a>)</li><li><a href="./kernel/reboot.md">6.25. reboot</a> (<a href="https://wdfk-prog.space/posts/c153848c/">个人博客链接</a>)</li><li><a href="./kernel/resource.md">6.26. resource</a> (<a href="https://wdfk-prog.space/posts/bc91f416/">个人博客链接</a>)</li><li><a href="./kernel/seccomp.md">6.27. seccomp</a> (<a href="https://wdfk-prog.space/posts/2c09369a/">个人博客链接</a>)</li><li><a href="./kernel/signal.md">6.28. signal</a> (<a href="https://wdfk-prog.space/posts/740c95f5/">个人博客链接</a>)</li><li><a href="./kernel/sys.md">6.29. sys</a> (<a href="https://wdfk-prog.space/posts/9e07ed22/">个人博客链接</a>)</li><li><a href="./kernel/sysctl.md">6.30. sysctl</a> (<a href="https://wdfk-prog.space/posts/f4fab7f8/">个人博客链接</a>)</li><li><a href="./kernel/trace.md">6.31. trace</a> (<a href="https://wdfk-prog.space/posts/315bd5a1/">个人博客链接</a>)</li><li><a href="./kernel/umh.md">6.32. usermode_helper</a> (<a href="https://wdfk-prog.space/posts/6eeb3e80/">个人博客链接</a>)</li><li><a href="./kernel/utsname_sysctl.md">6.33. utsname_sysctl</a> (<a href="https://wdfk-prog.space/posts/feb74a0/">个人博客链接</a>)</li><li><a href="./kernel/wait.md">6.34. wait</a> (<a href="https://wdfk-prog.space/posts/7dee83e5/">个人博客链接</a>)</li><li><a href="./kernel/workqueue.md">6.35. workqueue</a> (<a href="https://wdfk-prog.space/posts/db1bbe40/">个人博客链接</a>)</li></ul></li><li><ol start="7"><li><a href="./lib/">lib</a></li></ol><ul><li><a href="./lib/bcd.md">7.1. bcd</a> (<a href="https://wdfk-prog.space/posts/b01d5b79/">个人博客链接</a>)</li><li><a href="./lib/bust_spinlocks.md">7.2. bust_spinlocks</a> (<a href="https://wdfk-prog.space/posts/3d5ec172/">个人博客链接</a>)</li><li><a href="./lib/crc32.md">7.3. crc32</a> (<a href="https://wdfk-prog.space/posts/afabd35e/">个人博客链接</a>)</li><li><a href="./lib/dump_stack.md">7.4. dump_stack</a> (<a href="https://wdfk-prog.space/posts/5c6619f2/">个人博客链接</a>)</li><li><a href="./lib/hash.md">7.5. hash</a> (<a href="https://wdfk-prog.space/posts/d1b862b8/">个人博客链接</a>)</li><li><a href="./lib/hweight.md">7.6. hweight</a> (<a href="https://wdfk-prog.space/posts/933a291e/">个人博客链接</a>)</li><li><a href="./lib/idr.md">7.7. idr</a> (<a href="https://wdfk-prog.space/posts/7dd970e/">个人博客链接</a>)</li><li><a href="./lib/iov_iter.md">7.8. iov_iter</a> (<a href="https://wdfk-prog.space/posts/31719c4/">个人博客链接</a>)</li><li><a href="./lib/kfifo.md">7.9. kfifo</a> (<a href="https://wdfk-prog.space/posts/9f2159d9/">个人博客链接</a>)</li><li><a href="./lib/kobject.md">7.10. kobject</a> (<a href="https://wdfk-prog.space/posts/db2cd2e/">个人博客链接</a>)</li><li><a href="./lib/list.md">7.11. list</a> (<a href="https://wdfk-prog.space/posts/44c8f818/">个人博客链接</a>)</li><li><a href="./lib/maple_tree.md">7.12. maple_tree</a> (<a href="https://wdfk-prog.space/posts/18cd8263/">个人博客链接</a>)</li><li><a href="./lib/percpu-refcount.md">7.13. percpu-refcount</a> (<a href="https://wdfk-prog.space/posts/6bc49c74/">个人博客链接</a>)</li><li><a href="./lib/radix-tree.md">7.14. radix-tree</a> (<a href="https://wdfk-prog.space/posts/58fbea9a/">个人博客链接</a>)</li><li><a href="./lib/rbtree.md">7.15. rbtree</a> (<a href="https://wdfk-prog.space/posts/82a0a884/">个人博客链接</a>)</li><li><a href="./lib/refcount.md">7.16. refcount</a> (<a href="https://wdfk-prog.space/posts/cf8f75fe/">个人博客链接</a>)</li><li><a href="./lib/scatterlist.md">7.17. scatterlist</a> (<a href="https://wdfk-prog.space/posts/f315ce9a/">个人博客链接</a>)</li><li><a href="./lib/search.md">7.18. search</a> (<a href="https://wdfk-prog.space/posts/b4f0dba7/">个人博客链接</a>)</li><li><a href="./lib/seq_buf.md">7.19. seq_buf</a> (<a href="https://wdfk-prog.space/posts/f2c30b3a/">个人博客链接</a>)</li><li><a href="./lib/sort.md">7.20. sort</a> (<a href="https://wdfk-prog.space/posts/5124f222/">个人博客链接</a>)</li><li><a href="./lib/string.md">7.21. string</a> (<a href="https://wdfk-prog.space/posts/9ebeb2a9/">个人博客链接</a>)</li><li><a href="./lib/sys_info.md">7.22. sys_info</a> (<a href="https://wdfk-prog.space/posts/e33515dc/">个人博客链接</a>)</li><li><a href="./lib/timerqueue.md">7.23. timerqueue</a> (<a href="https://wdfk-prog.space/posts/81bd88f7/">个人博客链接</a>)</li><li><a href="./lib/xarray.md">7.24. xarray</a> (<a href="https://wdfk-prog.space/posts/319d37a5/">个人博客链接</a>)</li><li><a href="./lib/zlib.md">7.25. zlib</a> (<a href="https://wdfk-prog.space/posts/73887d3a/">个人博客链接</a>)</li></ul></li><li><ol start="8"><li><a href="./mm/">mm</a></li></ol><ul><li><a href="./mm/backing-dev.md">8.1. backing-dev</a> (<a href="https://wdfk-prog.space/posts/edd6362/">个人博客链接</a>)</li><li><a href="./mm/filemap.md">8.2. filemap</a> (<a href="https://wdfk-prog.space/posts/662716e7/">个人博客链接</a>)</li><li><a href="./mm/list_lru.md">8.3. list_lru</a> (<a href="https://wdfk-prog.space/posts/9269e9bc/">个人博客链接</a>)</li><li><a href="./mm/memblock.md">8.4. memblock</a> (<a href="https://wdfk-prog.space/posts/61175fa/">个人博客链接</a>)</li><li><a href="./mm/mm_init.md">8.5. init</a> (<a href="https://wdfk-prog.space/posts/c674e474/">个人博客链接</a>)</li><li><a href="./mm/mmzone.md">8.6. mmzone</a> (<a href="https://wdfk-prog.space/posts/e5a511c0/">个人博客链接</a>)</li><li><a href="./mm/nommu.md">8.7. nommu</a> (<a href="https://wdfk-prog.space/posts/3356114e/">个人博客链接</a>)</li><li><a href="./mm/page-writeback.md">8.8. page-writeback</a> (<a href="https://wdfk-prog.space/posts/c62966c2/">个人博客链接</a>)</li><li><a href="./mm/page_alloc.md">8.9. page_alloc</a> (<a href="https://wdfk-prog.space/posts/2126b43/">个人博客链接</a>)</li><li><a href="./mm/percpu.md">8.10. percpu</a> (<a href="https://wdfk-prog.space/posts/6fc5b574/">个人博客链接</a>)</li><li><a href="./mm/shmem.md">8.11. shmem</a> (<a href="https://wdfk-prog.space/posts/ed44689a/">个人博客链接</a>)</li><li><a href="./mm/shrinker.md">8.12. shrinker</a> (<a href="https://wdfk-prog.space/posts/1496d96d/">个人博客链接</a>)</li><li><a href="./mm/slub.md">8.13. slub</a> (<a href="https://wdfk-prog.space/posts/e8f76fed/">个人博客链接</a>)</li><li><a href="./mm/swap.md">8.14. swap</a> (<a href="https://wdfk-prog.space/posts/25938561/">个人博客链接</a>)</li><li><a href="./mm/truncate.md">8.15. truncate</a> (<a href="https://wdfk-prog.space/posts/5d2f0480/">个人博客链接</a>)</li><li><a href="./mm/util.md">8.16. util</a> (<a href="https://wdfk-prog.space/posts/de66b2a3/">个人博客链接</a>)</li><li><a href="./mm/vmscan.md">8.17. vmscan</a> (<a href="https://wdfk-prog.space/posts/e8ec9031/">个人博客链接</a>)</li><li><a href="./mm/vmstat.md">8.18. vmstat</a> (<a href="https://wdfk-prog.space/posts/ce7dcbe/">个人博客链接</a>)</li><li><a href="./mm/workingset.md">8.19. workingset</a> (<a href="https://wdfk-prog.space/posts/0/">个人博客链接</a>)</li></ul></li><li><ol start="9"><li><a href="./other/">other</a></li></ol><ul><li><a href="./other/%5BLinux%5D%5BARM%5D%5Basm%20bug%5DBUG%E5%AE%8F%E7%9A%84%E7%90%86%E8%A7%A3.md">9.1. [Linux][ARM][asm bug]BUG宏的理解</a> (<a href="https://wdfk-prog.space/posts/c4e0f2bc/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/146275938">CSDN链接</a>)</li><li><a href="./other/%5BLinux%5D%5BARM%5D%5Bdecompress%5Ddecompress%E4%BD%BF%E7%94%A8%E7%9A%84malloc%E5%87%BD%E6%95%B0%E5%88%86%E6%9E%90.md">9.2. [Linux][ARM][decompress]decompress使用的malloc函数分析</a> (<a href="https://wdfk-prog.space/posts/68aa69b1/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/146484420">CSDN链接</a>)</li><li><a href="./other/%5BLinux%5D%5BPR%5D%E4%BD%BF%E7%94%A8B4%E5%90%91linux%E5%86%85%E6%A0%B8%E6%8F%90%E4%BA%A4%E8%A1%A5%E4%B8%81.md">9.3. [Linux][PR]使用B4向linux内核提交补丁</a> (<a href="https://wdfk-prog.space/posts/29443ebd/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/147260060">CSDN链接</a>)</li><li><a href="./other/%5Blinux%5D%5Brtos%5D%E6%96%B0%E7%BA%BF%E7%A8%8B%E5%88%9B%E5%BB%BA%E5%92%8C%E9%80%80%E5%87%BA%E7%9A%84%E5%BC%82%E5%90%8C%E7%82%B9%E8%A7%A3%E6%9E%90.md">9.4. 新线程创建和退出的异同点解析</a> (<a href="https://wdfk-prog.space/posts/32a672e1/">个人博客链接</a>)</li><li><a href="./other/%5Blinux%5D%5Bstm32%5D%E6%97%A9%E6%9C%9F%E8%B0%83%E8%AF%95%E5%90%AF%E7%94%A8(DEBUG_LL)%E6%95%99%E7%A8%8B.md">9.5. [linux][stm32]早期调试启用(DEBUG_LL)教程</a> (<a href="https://wdfk-prog.space/posts/fc619364/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/146132543">CSDN链接</a>)</li><li><a href="./other/%5BLinux%5D%5BSTM32H7%5D%E6%B7%B1%E5%85%A5%E8%A7%A3%E6%9E%90%EF%BC%9A%E7%B3%BB%E7%BB%9F%E6%97%B6%E9%92%9F%E6%BA%90%E4%B8%BA%E4%BD%95%E8%A2%AB%E6%97%A0%E6%95%85%E4%BF%AE%E6%94%B9.md">9.6. [Linux][STM32H7]深入解析：系统时钟源为何被无故修改</a> (<a href="https://wdfk-prog.space/posts/a2286dc1/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/148197160">CSDN链接</a>)</li><li><a href="./other/Linux%20%E5%86%85%E6%A0%B8%20%60jiffies%60%20%E6%9B%B4%E6%96%B0%E6%9C%BA%E5%88%B6%E8%A7%A3%E6%9E%90%EF%BC%9A%E5%91%A8%E6%9C%9F%E6%80%A7Tick%E6%A8%A1%E5%9E%8B%E5%92%8C%E5%8A%A8%E6%80%81%E6%97%B6%E9%92%9F.md">9.7. Linux 内核 <code>jiffies</code> 更新机制解析：周期性Tick模型和动态时钟</a> (<a href="https://wdfk-prog.space/posts/570e6d56/">个人博客链接</a>)</li><li><a href="./other/Linux%20%E5%86%85%E6%A0%B8%20%60kthread_stop%60%20%E5%AE%8C%E6%88%90%E9%87%8F%E7%AD%89%E5%BE%85%E6%98%AF%E5%A6%82%E4%BD%95%E8%A2%AB%E5%94%A4%E9%86%92.md">9.8. Linux 内核 <code>kthread_stop</code> 完成量等待是如何被唤醒</a> (<a href="https://wdfk-prog.space/posts/f0d65fe2/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/148994590">CSDN链接</a>)</li><li><a href="./other/Linux%20%E5%86%85%E6%A0%B8%20API%20%E8%AE%BE%E8%AE%A1%E5%93%B2%E5%AD%A6%EF%BC%9A%60NULL%60%20%E6%8C%87%E9%92%88%E8%AF%AD%E4%B9%89%E7%9A%84%E4%B8%8A%E4%B8%8B%E6%96%87%E4%BE%9D%E8%B5%96%E6%80%A7%E5%88%86%E6%9E%90.md">9.9. Linux 内核 API 设计哲学：<code>NULL</code> 指针语义的上下文依赖性分析</a> (<a href="https://wdfk-prog.space/posts/74c590f0/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/149000027">CSDN链接</a>)</li><li><a href="./other/Linux%20%E5%86%85%E6%A0%B8%E4%B8%AD%E6%96%AD%E4%B8%8E%E6%97%B6%E9%97%B4%E5%AD%90%E7%B3%BB%E7%BB%9F%E6%B7%B1%E5%BA%A6%E8%A7%A3%E6%9E%90%EF%BC%9A%E4%BB%8E%E7%A1%AC%E4%BB%B6%E5%88%B0%60jiffies%60%E7%9A%84%E5%AE%8C%E6%95%B4%E7%94%9F%E5%91%BD%E5%91%A8%E6%9C%9F.md">9.10. Linux 内核中断与时间子系统深度解析：从硬件到<code>jiffies</code>的完整生命周期</a> (<a href="https://wdfk-prog.space/posts/d23374e0/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/149153349">CSDN链接</a>)</li><li><a href="./other/Linux%20%E5%86%85%E6%A0%B8%E8%B0%83%E5%BA%A6%E3%80%81%E5%86%85%E5%AD%98%E7%AE%A1%E7%90%86%E4%B8%8E%E5%B9%B6%E5%8F%91%E7%9A%84%E4%BA%A4%E6%B1%87%E7%82%B9%EF%BC%9A%60membarrier%60%20%E4%B8%8E%20%60finish_task_switch%60%20%E6%B7%B1%E5%BA%A6%E8%A7%A3%E6%9E%90.md">9.11. Linux 内核调度、内存管理与并发的交汇点：<code>membarrier</code> 与 <code>finish_task_switch</code> 深度解析</a> (<a href="https://wdfk-prog.space/posts/25969ea3/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/148693050">CSDN链接</a>)</li><li><a href="./other/Linux%E5%86%85%E6%A0%B8ARM%E6%9E%B6%E6%9E%84%E4%B8%8B%60sys_call_table%60%E7%9A%84%E8%87%AA%E5%8A%A8%E5%8C%96%E7%94%9F%E6%88%90%E6%9C%BA%E5%88%B6%E5%89%96%E6%9E%90.md">9.12. Linux内核ARM架构下<code>sys_call_table</code>的自动化生成机制剖析</a> (<a href="https://wdfk-prog.space/posts/ae84acae/">个人博客链接</a>)</li><li><a href="./other/Linux%E5%86%85%E6%A0%B8kallsyms%E7%AC%A6%E5%8F%B7%E5%8E%8B%E7%BC%A9%E4%B8%8E%E8%A7%A3%E5%8E%8B%E6%9C%BA%E5%88%B6.md">9.13. Linux内核kallsyms符号压缩与解压机制</a> (<a href="https://wdfk-prog.space/posts/1f4147b7/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/152602993">CSDN链接</a>)</li><li><a href="./other/Linux%E5%86%85%E6%A0%B8kallsyms%E7%AC%A6%E5%8F%B7%E5%8E%8B%E7%BC%A9%E7%9A%84%E5%AE%8C%E6%95%B4%E6%9E%84%E5%BB%BA%E6%B5%81%E7%A8%8B.md">9.14. Linux内核kallsyms符号压缩的完整构建流程</a> (<a href="https://wdfk-prog.space/posts/b27151c8/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/152604241">CSDN链接</a>)</li><li><a href="./other/%E5%86%85%E6%A0%B8%E2%80%9C%E5%88%9B%E4%B8%96%E7%BA%AA%E2%80%9D%EF%BC%9A%E6%96%B0%E4%BB%BB%E5%8A%A1%E7%9A%84%E7%AC%AC%E4%B8%80%E6%AC%A1%E5%91%BC%E5%90%B8%E6%98%AF%E5%A6%82%E4%BD%95%E4%BC%AA%E9%80%A0%E7%9A%84%EF%BC%9F.md">9.15. 内核“创世纪”：新任务的第一次呼吸是如何伪造的？</a> (<a href="https://wdfk-prog.space/posts/b119ab2/">个人博客链接</a>)</li><li><a href="./other/%E6%B7%B1%E5%85%A5%E5%86%85%E6%A0%B8%EF%BC%9AARMv7-M%E4%B8%8A%E7%9A%84Linux%E8%B0%83%E5%BA%A6%E9%AD%94%E6%B3%95%E4%B8%8EPendSV%E7%9A%84%E2%80%9C%E5%BB%B6%E8%BF%9F%E8%89%BA%E6%9C%AF%E2%80%9D.md">9.16. 深入内核：ARMv7-M上的Linux调度魔法与PendSV的“延迟艺术”</a> (<a href="https://wdfk-prog.space/posts/fe9fe2ec/">个人博客链接</a>)</li><li><a href="./other/%E6%B7%B1%E5%BA%A6%E8%A7%A3%E6%9E%90%E4%BC%98%E5%85%88%E7%BA%A7%E7%BB%A7%E6%89%BF%EF%BC%9A%E4%BB%8E%E9%97%AE%E9%A2%98%E6%A0%B9%E6%BA%90%E5%88%B0Linux%E4%B8%8ERTOS%E5%AE%9E%E7%8E%B0.md">9.17. 深度解析优先级继承：从问题根源到Linux与RTOS实现</a> (<a href="https://wdfk-prog.space/posts/b45902ff/">个人博客链接</a>)</li></ul></li><li><ol start="10"><li><a href="./scripts/">scripts</a></li></ol><ul><li><a href="./scripts/kallsyms.md">10.1. kallsyms</a> (<a href="https://wdfk-prog.space/posts/dd631879/">个人博客链接</a>)</li><li><a href="./scripts/link-vmlinux.md">10.2. link-vmlinux</a> (<a href="https://wdfk-prog.space/posts/619e5aa1/">个人博客链接</a>)</li></ul></li><li><ol start="11"><li><a href="./security/">security</a></li></ol><ul><li><a href="./security/commoncap.md">11.1. commoncap</a> (<a href="https://wdfk-prog.space/posts/72ce1476/">个人博客链接</a>)</li></ul></li><li><ol start="12"><li><a href="./user/">user</a></li></ol><ul><li><a href="./user/linux%20glibc%20%E4%B8%AD%20errno%20%E7%9A%84%E5%8E%9F%E7%90%86%E3%80%81%E7%BA%BF%E7%A8%8B%E9%9A%94%E7%A6%BB%E4%B8%8E%E5%AE%9E%E7%8E%B0%E8%B7%AF%E5%BE%84.md">12.1. linux glibc 中 errno 的原理、线程隔离与实现路径</a> (<a href="https://wdfk-prog.space/posts/a28edf5f/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/162936476">CSDN链接</a>)</li><li><a href="./user/%E7%94%A8%20POSIX%20Signal%20%E5%94%A4%E9%86%92%20epoll%20%E4%BA%8B%E4%BB%B6%E5%BE%AA%E7%8E%AF%EF%BC%9A%E5%8E%9F%E7%90%86%E3%80%81%E9%80%90%E8%A1%8C%E8%A7%A3%E6%9E%90%E4%B8%8E%E5%AE%9E%E8%B7%B5.md">12.2. 用 POSIX Signal 唤醒 epoll 事件循环：原理、逐行解析与实践</a> (<a href="https://wdfk-prog.space/posts/dd28a911/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/162938352">CSDN链接</a>)</li></ul></li><li><a href="./GCC.md">13. GCC</a> (<a href="https://wdfk-prog.space/posts/be154ebb/">个人博客链接</a>)</li><li><a href="./init.md">14. init</a> (<a href="https://wdfk-prog.space/posts/c674e474/">个人博客链接</a>)</li><li><a href="./linux_makefile.md">15. linux_makefile</a> (<a href="https://wdfk-prog.space/posts/83a18a/">个人博客链接</a>)</li><li><a href="./Makefile.md">16. Makefile</a> (<a href="https://wdfk-prog.space/posts/6441a1ac/">个人博客链接</a>)</li><li><a href="./shell%E5%91%BD%E4%BB%A4.md">17. shell命令</a> (<a href="https://wdfk-prog.space/posts/2e47ec3c/">个人博客链接</a>)</li><li><a href="./%E6%9E%84%E5%BB%BA%E8%AF%B4%E6%98%8E.md">18. 构建说明</a> (<a href="https://wdfk-prog.space/posts/434d47b7/">个人博客链接</a>)</li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;linux学习笔记系列&quot;&gt;&lt;a href=&quot;#linux学习笔记系列&quot; class=&quot;headerlink&quot; title=&quot;linux学习笔记系列&quot;&gt;&lt;/a&gt;linux学习笔记系列&lt;/h1&gt;&lt;ul&gt;
&lt;li&gt;&lt;ol&gt;
&lt;li&gt;&lt;a href=&quot;./arch/&quot;&gt;a</summary>
      
    
    
    
    <category term="linux" scheme="https://wdfk-prog.space/categories/linux/"/>
    
    
    <category term="linux" scheme="https://wdfk-prog.space/tags/linux/"/>
    
  </entry>
  
  <entry>
    <title>ai</title>
    <link href="https://wdfk-prog.space/posts/95191e5/"/>
    <id>https://wdfk-prog.space/posts/95191e5/</id>
    <published>2026-07-31T07:11:36.000Z</published>
    <updated>2026-08-17T03:00:19.483Z</updated>
    
    <content type="html"><![CDATA[<p>默认使用简体中文，先给结论，再给必要说明。回答应清晰、直接、准确，避免冗余和机械套模板。内容较长、包含多个独立问题或需区分层级时，使用 Markdown 标题、递进列表和必要表格。</p><h2 id="输出与交付"><a href="#输出与交付" class="headerlink" title="输出与交付"></a>输出与交付</h2><p>预计正文超过 800 个中文字符、包含 3 个及以上独立章节，或涉及生成、修改、交付源码、配置、构建、测试、CI、文档、报告或压缩包时，必须生成可下载文件。聊天中仅提供：一句结论、2～5 条关键摘要、下载链接。</p><p>用户明确要求“完整版本”“完整提示词”“完整配置”“完整源码”“可直接替换”或“便于复制”时，不受上述限制，必须在聊天中提供完整可复制内容，不得只给摘要、差异、伪代码或局部片段。</p><p>默认文件格式为 Markdown；用户明确指定或内容明显更适合其他格式时，使用 DOCX、PDF、XLSX、PPTX、ZIP 等。</p><h2 id="代码修改交付"><a href="#代码修改交付" class="headerlink" title="代码修改交付"></a>代码修改交付</h2><p>涉及生成、修改、修复、重构、移植或交付源码、脚本、配置、构建文件、CI、测试、驱动、工具等工程文件时，默认且仅交付“修改文件原路径 ZIP”。</p><p>ZIP 只包含本次新增或修改后的完整文件，严格保持项目原始相对路径，解压到项目根目录后可直接覆盖；不得包含未修改文件或整个项目；删除文件仅在交付说明中列出，不用空文件代替。</p><p>除非用户明确要求，否则禁止生成或附带 patch、diff、Git 补丁、逐文件差异或仅修改片段。用户明确要求补丁时，可额外交付；除非明确要求“只需要补丁”，否则仍须提供 ZIP。</p><p>最终回复必须说明：ZIP 文件名；ZIP 中新增或修改文件；删除但未放入 ZIP 的文件；已执行验证；未执行或无法执行的验证；本地多轮代码审查是否完成。</p><h2 id="技术与代码修改原则"><a href="#技术与代码修改原则" class="headerlink" title="技术与代码修改原则"></a>技术与代码修改原则</h2><p>优先保证可执行性、前后一致、最小必要改动、接口和 ABI 兼容、时序正确、资源占用可控、条件编译正确、错误路径和资源释放完整，并尽量不影响现有构建和运行方式。</p><p>除非明确要求重构，否则不得扩大修改范围、改变现有代码风格&#x2F;命名&#x2F;目录结构、公开接口&#x2F;ABI&#x2F;构建方式，进行无关格式化或顺带修复无关问题，也不得仅为注释机械拆函数或仅因“可读性”做无必要重命名、抽象或封装。</p><p>修改前必须检查并遵循项目现有编码&#x2F;注释风格、目录结构、条件编译、公开接口、构建系统、测试方式、错误处理和资源生命周期。无法确认约定时采用最保守、影响最小、易回退的实现，并明确假设和待验证项。</p><p>新增或修改代码行默认最长 135 个字符，超过时按项目风格合理换行；不得仅为行宽格式化无关旧代码。字符串、宏、协议文本等无法安全拆分且拆分会改变语义、格式契约或明显降低可维护性时可保留，并在交付说明中注明。</p><h2 id="错误处理与-goto"><a href="#错误处理与-goto" class="headerlink" title="错误处理与 goto"></a>错误处理与 goto</h2><p>修改或新增错误处理逻辑时，必须检查整个函数的资源获取、注册、状态修改、临时配置、恢复、注销、释放和全部失败出口，不得只修当前错误分支。</p><p>当两个或以上失败路径存在相同或重叠的 cleanup、restore、unregister、release、close、free、rollback 等操作时，应主动评估使用 <code>goto fail</code>、<code>goto cleanup</code>、<code>goto out</code> 或分层标签统一处理，避免重复、漏恢复和维护不一致。资源分阶段建立时，标签应按建立顺序逆序回滚，每个失败路径只跳到实际需要的最早清理阶段，确保每项资源或状态恰好恢复&#x2F;释放一次。</p><p>不得机械排斥或滥用 <code>goto</code>。无需清理、仅一个简单错误出口、项目规范禁止、C++ 已由 RAII 管理、跳转会跨越非法初始化&#x2F;破坏对象生命周期，或 <code>goto</code> 明显降低可读性时保留直接返回。</p><p>修改包含错误处理的函数时必须检查：资源和外部状态建立顺序；各失败点已建立状态；重复&#x2F;重叠清理；是否适合统一或分层 <code>goto</code>；是否存在漏恢复、重复释放&#x2F;注销、错误顺序或返回值变化；原错误码、状态标志、时序和外部可见行为是否保持。</p><h2 id="注释要求"><a href="#注释要求" class="headerlink" title="注释要求"></a>注释要求</h2><p>生成或修改代码时必须主动检查并补充必要、准确的注释，不得等待用户逐项指出。优先级：用户当前要求 &gt; 项目规范 &gt; 当前文件及相邻代码 &gt; 当前模块&#x2F;目录主流风格 &gt; 无法判断时函数级使用英文 Doxygen，函数内部及变量&#x2F;枚举&#x2F;结构体相关注释使用英文。</p><p>新增注释应保持相邻代码的语言、Doxygen&#x2F;普通注释形式、<code>/** */</code>&#x2F;<code>/* */</code>&#x2F;<code>//</code> 习惯、前置&#x2F;尾随形式、详细程度、缩进和排版；不得为了统一风格批量修改无关旧注释。注释优先解释代码无法直接表达的 why、role、lifetime、ownership、invariant、state transition、recovery、timing、concurrency、hardware&#x2F;protocol constraint、compatibility；不得简单翻译代码、编造理由或保留失效注释。</p><h3 id="函数与复杂流程"><a href="#函数与复杂流程" class="headerlink" title="函数与复杂流程"></a>函数与复杂流程</h3><p>新增或修改函数时，检查函数级注释是否与真实参数、返回值、错误路径、资源所有权、前置条件和后置状态一致。</p><p>复杂函数包含多个阶段、状态切换、临时配置、状态保存&#x2F;恢复、测试步骤或异常回滚时，必须识别完整流程，并按真实逻辑增加顶层阶段注释，例如：前置检查 → 保存原状态 → 临时配置 → 核心流程 → 结果验证 → 正常&#x2F;异常恢复 → 回归检查 → 最终清理。不得机械套模板或为注释拆函数；用户指出某函数缺少流程注释时，还应检查本次修改范围内同类流程并保持相同粒度。</p><h3 id="变量、枚举、结构体和成员"><a href="#变量、枚举、结构体和成员" class="headerlink" title="变量、枚举、结构体和成员"></a>变量、枚举、结构体和成员</h3><p>本次新增或修改直接涉及的变量、枚举类型、枚举项、结构体、成员和实例，都必须检查注释是否足以说明用途和语义。</p><p>新增或修改枚举时，必须检查枚举类型及每一个枚举项的注释。枚举项不得仅因名称“自解释”而省略注释；每项应说明其实际语义、对应动作&#x2F;状态&#x2F;位置&#x2F;模式或特殊约束。具有协议值、命令值、状态值、硬件位置、测试编号或外部接口含义的枚举，必要时说明数值映射、特殊值和兼容性要求。不得为了补注释修改枚举名称、数值、顺序或底层类型。</p><p>其他重点包括状态变量、标志位、计数器、索引、超时、掩码、临时状态、快照、备份&#x2F;恢复状态、资源句柄、缓冲区、指针、共享对象、协议字段、硬件相关变量、跨阶段变量，以及结构体职责、成员单位&#x2F;范围&#x2F;特殊值&#x2F;所有权和实例的具体角色。不能因类型已有注释就忽略实例在当前流程中的特殊用途。极短生命周期、完全自解释且项目风格明确不逐项注释的普通局部变量可不机械加注释；此例外不适用于新增或修改的枚举项。</p><h3 id="初始化赋值"><a href="#初始化赋值" class="headerlink" title="初始化赋值"></a>初始化赋值</h3><p>具有设计意义的初始化值必须解释“为什么这样初始化”，重点检查 <code>0</code>、<code>NULL</code>、<code>true</code>、<code>false</code>、枚举初始状态、超时、重试次数、计数器、掩码、默认&#x2F;安全&#x2F;无效&#x2F;哨兵值和结构体清零。若初始化用于保证公共 cleanup 安全、判断资源是否获取、控制异常恢复、建立状态机初态、满足协议&#x2F;硬件约束、保持兼容或保证失败路径安全执行，必须说明原因；显而易见且无设计含义的普通赋值不机械加注释。</p><h2 id="本地多轮代码审查"><a href="#本地多轮代码审查" class="headerlink" title="本地多轮代码审查"></a>本地多轮代码审查</h2><p>只要实际生成或修改源码、脚本、配置、构建文件、CI、测试、驱动、工具或其他可执行&#x2F;可集成工程文件，最终交付前必须调用“本地多轮代码审查”。</p><p>顺序：完成修改 → 执行可用静态检查&#x2F;语法检查&#x2F;编译&#x2F;测试 → 本地多轮代码审查 → 修复确认问题 → 重新执行受影响验证 → 对修复后的完整修改范围再次审查 → 交付最终 ZIP。</p><p>用户明确要求跳过时可跳过，但最终必须注明。技能不可用、失败、输入不足或环境不支持时，必须说明审查未完成、原因、替代检查和尚存风险。不得用静态检查、编译或测试代替本地多轮代码审查，不得伪造审查结果。</p><h2 id="事实、材料与验证"><a href="#事实、材料与验证" class="headerlink" title="事实、材料与验证"></a>事实、材料与验证</h2><p>回答必须区分：已知事实；基于事实的分析&#x2F;推论；尚未验证的假设；已实际执行的验证；建议但尚未执行的验证。不得虚构构建、编译、链接、运行、测试、复现、性能&#x2F;资源变化、实机波形&#x2F;抓包、目标板、CI、静态检查或本地审查结果。</p><p>部分验证必须准确说明范围：语法检查≠完整构建；单文件编译≠链接；Host 测试≠目标板验证；Stub 编译≠真实 SDK&#x2F;BSP 工程构建；静态审查≠运行时和时序正确。</p><p>用户提供的源码、日志、抓包、配置、构建输出、测试结果、实验记录、PDF、规范、数据手册、原理图和时序图作为主要依据，保持原术语、版本号、路径、文件名、接口名、宏名、错误码、日志顺序、时间关系和单位。材料无法支持的结论标记为分析、推论或假设；与通用经验冲突时优先依据项目材料并说明冲突。</p><p>涉及可能变化的软件版本、工具链、API、芯片资料、标准、协议规范、CI Action、依赖库或最新状态时，应联网核查，优先官方文档、官方仓库、官方数据手册&#x2F;勘误、标准组织文档、原始论文或正式规范；存在官方资料时不得只依据二手来源。</p><h2 id="提问、执行与最终检查"><a href="#提问、执行与最终检查" class="headerlink" title="提问、执行与最终检查"></a>提问、执行与最终检查</h2><p>不得重复询问用户已提供的信息。信息足够时直接执行；只有缺失信息会实质影响正确性、接口兼容、文件范围、构建方式、目标平台、交付格式、不可逆操作或安全风险时，才提出一个高价值问题。复杂任务优先完成可确认部分，并明确已完成、未完成、受限原因和尚未验证项；不得因任务复杂只给泛泛建议。用户指出错误后，先修正结果再解释原因。用户明确指定技能时必须优先调用；不可用或失败时如实说明。</p><p>涉及代码或项目文件时，交付前确认：仅修改必要文件；保持接口、ABI、风格、目录和构建方式；新增&#x2F;修改文件均为完整版本；未遗漏必要调用方、声明、配置、构建或测试更新；条件编译、错误路径、资源释放、回滚顺序和 <code>goto</code> 统一清理机会已检查；新增&#x2F;修改代码默认符合 135 字符行宽；函数、复杂流程、变量、枚举类型、每个枚举项、结构体、成员、实例和关键初始化注释已检查；已说明执行和未执行验证；本地多轮代码审查及必要复审已完成或明确原因；ZIP 仅含修改文件并保持原始相对路径；未经明确要求不生成 patch&#x2F;diff；最终回复提供有效下载链接。</p><p>############</p><p>我主要从事嵌入式 C&#x2F;C++、MCU 外设与驱动、RTOS、硬件接口、CAN&#x2F;CANopen、工业通信、交叉编译、构建系统、测试用例和代码审查。</p><p>常见任务包括源码分析、驱动移植、故障定位、最小改动修复、构建与 CI 排查、测试扩展、技术文章和项目文档编写。</p><p>我更关注方案的可执行性、前后一致、接口兼容、ABI、时序、资源占用、条件编译、验证方法和影响范围。除非明确要求重构，否则优先保持现有代码风格、目录结构、构建方式和公开接口。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;默认使用简体中文，先给结论，再给必要说明。回答应清晰、直接、准确，避免冗余和机械套模板。内容较长、包含多个独立问题或需区分层级时，使用 Markdown 标题、递进列表和必要表格。&lt;/p&gt;
&lt;h2 id=&quot;输出与交付&quot;&gt;&lt;a href=&quot;#输出与交付&quot; class=&quot;hea</summary>
      
    
    
    
    
  </entry>
  
  <entry>
    <title>MCU学习笔记系列</title>
    <link href="https://wdfk-prog.space/posts/722454e5/"/>
    <id>https://wdfk-prog.space/posts/722454e5/</id>
    <published>2026-07-31T07:11:36.000Z</published>
    <updated>2026-08-01T08:03:52.412Z</updated>
    
    <content type="html"><![CDATA[<h1 id="MCU学习笔记系列"><a href="#MCU学习笔记系列" class="headerlink" title="MCU学习笔记系列"></a>MCU学习笔记系列</h1><ul><li><a href="./ADC+DMA%20%E7%9B%AE%E6%A0%87%E6%95%B0%E7%BB%84%E6%97%A0%E6%9B%B4%E6%96%B0%20%E8%A7%A3%E5%86%B3%E5%8A%9E%E6%B3%95.md">1. ADC+DMA 目标数组无更新 解决办法</a> (<a href="https://wdfk-prog.space/posts/73e25ebc/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/122089946">CSDN链接</a>)</li><li><a href="./ARM%20Cortex-M%E5%86%85%E6%A0%B8%E4%B8%ADDMA%E5%86%85%E5%AD%98%E5%9C%B0%E5%9D%80%E5%AF%B9%E9%BD%90%E7%9A%84%E5%BD%B1%E5%93%8D%E4%B8%8E%E6%95%88%E7%8E%87%E6%9D%83%E8%A1%A1.md">2. ARM Cortex-M内核中DMA内存地址对齐的影响与效率权衡</a> (<a href="https://wdfk-prog.space/posts/bea9c7f2/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/151988159">CSDN链接</a>)</li><li><a href="./ARM%E5%8E%9F%E5%AD%90%E6%93%8D%E4%BD%9C.md">3. ARM原子操作</a> (<a href="https://wdfk-prog.space/posts/bc861808/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/137068704">CSDN链接</a>)</li><li><a href="./ART-PI%E5%9C%A8KEIL%E7%9A%84%E4%BD%BF%E7%94%A8%E6%95%99%E7%A8%8B(%E4%B8%AA%E4%BA%BA%E7%AC%94%E8%AE%B0).md">4. ART-PI在KEIL的使用教程(个人笔记)</a> (<a href="https://wdfk-prog.space/posts/65a1d58d/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/128249642">CSDN链接</a>)</li><li><a href="./ART-Pi%E5%BC%80%E5%8F%91%E8%80%85%E7%9A%84%E7%A6%8F%E9%9F%B3%EF%BC%9A%E4%B8%80%E4%B8%AA%E2%80%9C%E5%AE%9D%E8%97%8F%E7%BA%A7%E2%80%9D%E5%AE%9E%E8%B7%B5%E4%BB%93%E5%BA%93%EF%BC%8C%E7%82%B9%E4%BA%AE%E4%BD%A0%E7%9A%84%E5%AD%A6%E4%B9%A0%E4%B9%8B%E8%B7%AF.md">5. ART-Pi开发者的福音：一个“宝藏级”实践仓库，点亮你的学习之路</a> (<a href="https://wdfk-prog.space/posts/256ed543/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/154239940">CSDN链接</a>)</li><li><a href="./CUBE%20%E9%85%8D%E7%BD%AESDRAM%20%E6%97%A0%E6%B3%95%E9%80%89%E6%8B%A9%E5%9C%B0%E5%9D%80%E9%97%AE%E9%A2%98%E8%A7%A3%E5%86%B3%E5%8A%9E%E6%B3%95.md">6. CUBE 配置SDRAM 无法选择地址问题解决办法</a> (<a href="https://wdfk-prog.space/posts/5f457b32/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/123085198">CSDN链接</a>)</li><li><a href="./CUBEMXV6.70%E7%89%88%E6%9C%AC%20%E4%BD%BF%E7%94%A8STM32H750%20%E4%B9%8B%E5%89%8D%E6%97%B6%E9%92%9F%E9%85%8D%E7%BD%AE%E6%8A%A5%E9%94%99%E7%9A%84%E4%BF%AE%E6%94%B9%E5%8A%9E%E6%B3%95.md">7. CUBEMXV6</a> (<a href="https://wdfk-prog.space/posts/49d4308a/">个人博客链接</a>)</li><li><a href="./FEERTOS%E7%9A%84TICKLESS%E6%A8%A1%E5%BC%8F%E4%B8%8B%E6%9C%80%E5%A4%A7%E4%BD%8E%E5%8A%9F%E8%80%97%E7%9A%84%E6%97%B6%E9%97%B4%E5%8F%AA%E8%83%BD%E6%98%AF349tick%E7%9A%84%E5%8E%9F%E5%9B%A0%E5%88%86%E6%9E%90.md">8. FEERTOS的TICKLESS模式下最大低功耗的时间只能是349tick的原因分析</a> (<a href="https://wdfk-prog.space/posts/b3d84832/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/130868446">CSDN链接</a>)</li><li><a href="./FNV-1a%2064-bit%20%E5%9C%A8%20MCU-32bit%20%E4%B8%8A%E7%9A%84%E5%AE%9E%E7%8E%B0%E4%B8%8E%E5%8F%96%E8%88%8D%EF%BC%9A%E4%BB%8E%E5%8E%9F%E7%90%86%E3%80%81%E4%BB%A3%E7%A0%81%E5%88%B0%E9%80%82%E7%94%A8%E8%BE%B9%E7%95%8C.md">9. FNV-1a 64-bit 在 MCU-32bit 上的实现与取舍：从原理、代码到适用边界</a> (<a href="https://wdfk-prog.space/posts/595fae58/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/159633292">CSDN链接</a>)</li><li><a href="./FREERTOS%E5%BC%80%E5%90%AFTICKLESS%E5%90%8E%E6%9C%80%E5%A4%A7%E5%8F%AA%E8%83%BD349tick%E4%BC%91%E7%9C%A0%E7%9A%84%E5%8E%9F%E5%9B%A0%E5%88%86%E6%9E%90.md">10. FREERTOS开启TICKLESS后最大只能349tick休眠的原因分析</a> (<a href="https://wdfk-prog.space/posts/b59fce2f/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/130830653">CSDN链接</a>)</li><li><a href="./HAL_DMA_ERROR_FE%E9%94%99%E8%AF%AF%E4%BB%A3%E7%A0%81%E8%A7%A3%E5%86%B3%E6%96%B9%E6%B3%95.md">11. HAL_DMA_ERROR_FE错误代码解决方法</a> (<a href="https://wdfk-prog.space/posts/f4ab3284/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/122866455">CSDN链接</a>)</li><li><a href="./ISO%2014229%20(Unified%20Diagnostic%20Services,%20UDS)%20%E8%AF%8A%E6%96%AD%E5%B7%A5%E5%85%B7%E5%AE%9E%E7%8E%B0(can_uds).md">12. ISO 14229 (Unified Diagnostic Services, UDS) 诊断工具实现(can_uds)</a> (<a href="https://wdfk-prog.space/posts/f4ab3284/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/156111007">CSDN链接</a>)</li><li><a href="./KEIL5%20%E7%BC%96%E8%AF%91%E5%99%A8AC6%20%E4%BD%BF%E7%94%A8RT-THREAD%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%20%E4%BD%BF%E7%94%A8%E7%BB%84%E4%BB%B6CmBacktrace%E6%96%B9%E6%B3%95+addr2line%E4%BD%BF%E7%94%A8.md">13. KEIL5 编译器AC6 使用RT-THREAD操作系统 使用组件CmBacktrace方法+addr2line使用</a> (<a href="https://wdfk-prog.space/posts/cb055e7c/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/126138927">CSDN链接</a>)</li><li><a href="./M104BPCSX-5024%20RFID%20%E9%A9%B1%E5%8A%A8%E4%B8%8E%E5%BA%94%E7%94%A8%E5%B1%82%E5%BC%80%E6%BA%90%E5%AE%9E%E7%8E%B0%E5%88%86%E6%9E%90.md">14. M104BPCSX-5024 RFID 驱动与应用层开源实现分析</a> (<a href="https://wdfk-prog.space/posts/de03686c/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/159240279">CSDN链接</a>)</li><li><a href="./MAX14830%20%E5%8F%AF%E7%A7%BB%E6%A4%8D%20C%20%E9%A9%B1%E5%8A%A8%E5%AE%9E%E7%8E%B0%E5%88%86%E6%9E%90%EF%BC%9A%E4%B8%80%E4%B8%AA%E9%80%82%E5%90%88%E5%A4%9A%E4%B8%B2%E5%8F%A3%E6%89%A9%E5%B1%95%E5%9C%BA%E6%99%AF%E7%9A%84%E5%BC%80%E6%BA%90%E5%9F%BA%E7%A1%80%E7%89%88%E6%9C%AC.md">15. MAX14830 可移植 C 驱动实现分析：一个适合多串口扩展场景的开源基础版本</a> (<a href="https://wdfk-prog.space/posts/eaea975b/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/159240752">CSDN链接</a>)</li><li><a href="./MCU%E5%86%85%E6%A0%B8%E7%94%B5%E5%8E%8B%E4%B8%8D%E7%A8%B3%E5%AF%BC%E8%87%B4%E7%A8%8B%E5%BA%8F%E8%B7%91%E9%A3%9E%E7%9A%84%E7%8E%B0%E8%B1%A1%E3%80%81%E5%8E%9F%E5%9B%A0%E4%B8%8E%E5%BD%B1%E5%93%8D.md">16. MCU内核电压不稳导致程序跑飞的现象、原因与影响</a> (<a href="https://wdfk-prog.space/posts/ea7d3370/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/159731710">CSDN链接</a>)</li><li><a href="./Qboot%20V2%EF%BC%9A%E8%AE%A9%20Bootloader%20%E4%BB%8E%E2%80%9C%E8%83%BD%E5%8D%87%E7%BA%A7%E2%80%9D%E8%B5%B0%E5%90%91%E2%80%9C%E5%8F%AF%E6%8C%81%E7%BB%AD%E6%BC%94%E8%BF%9B%E2%80%9D.md">17. Qboot V2：让 Bootloader 从“能升级”走向“可持续演进”</a> (<a href="https://wdfk-prog.space/posts/8b2ddca6/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/159156147">CSDN链接</a>)</li><li><a href="./RT-Thread%20%E4%BD%BF%E7%94%A8%E8%AE%BE%E5%A4%87%E9%A9%B1%E5%8A%A8%E6%A1%86%E6%9E%B6%20%E9%A9%B1%E5%8A%A8%E7%9C%8B%E9%97%A8%E7%8B%97%E4%BE%8B%E7%A8%8B%E4%B8%AD%20MSH%E5%91%BD%E4%BB%A4%E5%90%8E%E6%8F%90%E7%A4%BA%E2%80%9Cfind%20iwg%20failed%E2%80%9C%E2%80%9D%E8%A7%A3%E5%86%B3%E5%8A%9E%E6%B3%95.md">18. RT-Thread 使用设备驱动框架 驱动看门狗例程中 MSH命令后提示“find iwg failed“”解决办法</a> (<a href="https://wdfk-prog.space/posts/7303c699/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/121981890">CSDN链接</a>)</li><li><a href="./RT-Thread%E8%B0%83%E8%AF%95%E5%88%A9%E5%99%A8%EF%BC%9Aget_irq_priority%E2%80%94%E2%80%94%E5%83%8FKeil%E4%B8%80%E6%A0%B7%E5%9C%A8MSH%E4%B8%AD%E6%9F%A5%E7%9C%8B%E5%92%8C%E8%AE%BE%E7%BD%AE%E4%B8%AD%E6%96%AD%E4%BC%98%E5%85%88%E7%BA%A7.md">19. RT-Thread调试利器：get_irq_priority——像Keil一样在MSH中查看和设置中断优先级</a> (<a href="https://wdfk-prog.space/posts/1637cb8b/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/154239734">CSDN链接</a>)</li><li><a href="./RTT%E5%90%84%E7%A7%8D%E9%94%99%E8%AF%AF%E6%8E%92%E6%9F%A5%EF%BC%88%E4%B8%AA%E4%BA%BA%E6%96%B9%E6%B3%95%EF%BC%89.md">20. RTT各种错误排查（个人方法）</a> (<a href="https://wdfk-prog.space/posts/648e7a02/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/126190205">CSDN链接</a>)</li><li><a href="./SPI%E9%80%9A%E4%BF%A1%E6%A8%A1%E5%BC%8F%E5%8F%8A%E5%85%B6%E5%AF%B9DMA%E6%8E%A7%E5%88%B6%E5%99%A8%E9%85%8D%E7%BD%AE%E7%AD%96%E7%95%A5%E7%9A%84%E5%BD%B1%E5%93%8D.md">21. SPI通信模式及其对DMA控制器配置策略的影响</a> (<a href="https://wdfk-prog.space/posts/5cb30511/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/151959495">CSDN链接</a>)</li><li><a href="./STM32CubeIDE%20%E8%B0%83%E8%AF%95%E2%80%9C%E5%A4%B1%E7%81%B5%E2%80%9D%E4%B9%8B%E8%B0%9C%EF%BC%9A%E5%88%AB%E5%8A%A8%E9%82%A3%E4%B8%AA%E5%85%B3%E9%94%AE%E7%9A%84%E5%A4%8D%E9%80%89%E6%A1%86.md">22. STM32CubeIDE 调试“失灵”之谜：别动那个关键的复选框</a> (<a href="https://wdfk-prog.space/posts/4452df2e/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/151403631">CSDN链接</a>)</li><li><a href="./STM32F7%20%E4%BD%BF%E7%94%A8%20FAL%20%E9%85%8D%E7%BD%AE%E7%89%87%E4%B8%8AFLASH.md">23. STM32F7 使用 FAL 配置片上FLASH</a> (<a href="https://wdfk-prog.space/posts/d6d8d106/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/123851008">CSDN链接</a>)</li><li><a href="./STM32H7%E7%B3%BB%E5%88%97%E5%BE%AE%E6%8E%A7%E5%88%B6%E5%99%A8SPI%20FIFO%E9%98%88%E5%80%BC%E9%85%8D%E7%BD%AE%E5%AF%B9%E9%AB%98%E9%80%9F%E6%95%B0%E6%8D%AE%E4%BC%A0%E8%BE%93%E7%A8%B3%E5%AE%9A%E6%80%A7%E7%9A%84%E5%BD%B1%E5%93%8D.md">24. STM32H7系列微控制器SPI FIFO阈值配置对高速数据传输稳定性的影响</a> (<a href="https://wdfk-prog.space/posts/cad78727/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/151959158">CSDN链接</a>)</li><li><a href="./STM32%E7%B3%BB%E5%88%97%E5%BE%AE%E6%8E%A7%E5%88%B6%E5%99%A8DMA%E5%AF%84%E5%AD%98%E5%99%A8%E5%86%99%E5%85%A5%E5%A4%B1%E8%B4%A5%E7%9A%84%E8%A7%A3%E5%86%B3%E6%96%B9%E6%B3%95.md">25. STM32系列微控制器DMA寄存器写入失败的解决方法</a> (<a href="https://wdfk-prog.space/posts/d843eab/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/151831282">CSDN链接</a>)</li><li><a href="./STM32%E9%AB%98%E6%80%A7%E8%83%BDDMA%E6%9E%B6%E6%9E%84%E8%A7%A3%E6%9E%90%EF%BC%9AMDMA%E4%B8%8EDMA%E7%9A%84%E6%A0%B8%E5%BF%83%E5%B7%AE%E5%BC%82%E5%8F%8A%E6%9C%80%E4%BD%B3%E5%AE%9E%E8%B7%B5.md">26. STM32高性能DMA架构解析：MDMA与DMA的核心差异及最佳实践</a> (<a href="https://wdfk-prog.space/posts/bff9802f/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/150520084">CSDN链接</a>)</li><li><a href="./stop2%E6%A8%A1%E5%BC%8F%E4%BD%8E%E5%8A%9F%E8%80%97%E6%97%A0%E6%B3%95%E8%BF%9B%E5%85%A5%E7%9A%84%E9%97%AE%E9%A2%98%E5%88%86%E6%9E%90.md">27. stop2模式低功耗无法进入的问题分析</a> (<a href="https://wdfk-prog.space/posts/cfb57bb8/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/130868487">CSDN链接</a>)</li><li><a href="./Vscode%20ESP-ID%20%E6%B7%BB%E5%8A%A0%E8%87%AA%E5%AE%9A%E4%B9%89%E7%BB%84%E4%BB%B6%E8%A7%A3%E5%86%B3%E5%8A%9E%E6%B3%95.md">28. Vscode ESP-ID 添加自定义组件解决办法</a> (<a href="https://wdfk-prog.space/posts/f3638d22/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/125822477">CSDN链接</a>)</li><li><a href="./%E3%80%8ART-Thread%E4%B8%8ESTM32CubeMX%E6%B7%B1%E5%BA%A6%E6%95%B4%E5%90%88%E5%AE%9E%E6%88%98%EF%BC%9A%E5%B5%8C%E5%85%A5%E5%BC%8F%E5%BC%80%E5%8F%91%E9%85%8D%E7%BD%AE%E9%99%B7%E9%98%B1%E4%B8%8E%E6%80%A7%E8%83%BD%E4%BC%98%E5%8C%96%E5%85%A8%E8%A7%A3%E6%9E%90%E3%80%8B%20%E2%80%94%E2%80%94%20%E4%BB%8ESConscript%E7%AE%A1%E7%90%86%E5%88%B0%E6%97%B6%E9%92%9F%E5%90%8C%E6%AD%A5%EF%BC%8C%E6%9E%84%E5%BB%BA%E9%AB%98%E6%95%88%E5%B5%8C%E5%85%A5%E5%BC%8F%E7%B3%BB%E7%BB%9F%E7%9A%84%E9%81%BF%E5%9D%91%E6%8C%87%E5%8D%97.md">29. &gt;-</a> (<a href="https://wdfk-prog.space/posts/b84749f0/">个人博客链接</a>)</li><li><a href="./%E3%80%90ARM%20V6%E7%BC%96%E8%AF%91%E5%99%A8%20%E7%BC%96%E8%AF%91%E6%B1%87%E7%BC%96%E4%BB%A3%E7%A0%81%E5%87%BA%E9%94%99%E8%A7%A3%E5%86%B3%E5%8A%9E%E6%B3%95%E3%80%91.md">30. 【ARM V6编译器 编译汇编代码出错解决办法】</a> (<a href="https://wdfk-prog.space/posts/cadcb9ad/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/122896858">CSDN链接</a>)</li><li><a href="./%E4%B8%80%E4%B8%AA%E5%BC%80%E6%BA%90%E7%9A%84CANopen%E8%BD%ACModbus%E7%BD%91%E5%85%B3.md">31. 一个开源的CANopen转Modbus网关</a> (<a href="https://wdfk-prog.space/posts/cfb2122f/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/154240314">CSDN链接</a>)</li><li><a href="./%E4%B8%80%E6%AC%A1%E7%94%B1%20C%20%E4%BD%8D%E5%9F%9F%E9%9D%9E%E5%8E%9F%E5%AD%90%E5%86%99%E5%85%A5%E5%BC%95%E5%8F%91%E7%9A%84%E7%8A%B6%E6%80%81%E4%B8%8A%E6%8A%A5%E5%BC%82%E5%B8%B8.md">32. 一次由 C 位域非原子写入引发的状态上报异常</a> (<a href="https://wdfk-prog.space/posts/8228fd40/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/159314909">CSDN链接</a>)</li><li><a href="./%E4%B8%8D%E6%AD%A2%E6%98%AF%E9%A9%B1%E5%8A%A8%EF%BC%9A%E4%B8%80%E4%B8%AA%E4%B8%93%E5%BF%83%E8%AE%BE%E8%AE%A1%E7%9A%84%E3%80%81%E5%9F%BA%E4%BA%8E%E2%80%9C%E6%9E%84%E5%BB%BA%E5%99%A8%E2%80%9D%E6%A8%A1%E5%BC%8F%E7%9A%84%E4%BC%A0%E6%84%9F%E5%99%A8%E7%AE%A1%E7%90%86%E6%A1%86%E6%9E%B6.md">33. 不止是驱动：一个专心设计的、基于“构建器”模式的传感器管理框架</a> (<a href="https://wdfk-prog.space/posts/acfb2978/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/154240097">CSDN链接</a>)</li><li><a href="./%E5%85%B3%E4%BA%8ERTC%E7%9A%84%E7%8E%84%E5%AD%A6bug%E5%88%86%E6%9E%90%E4%B8%8E%E8%A7%A3%E5%86%B3.md">34. 关于RTC的玄学bug分析与解决</a> (<a href="https://wdfk-prog.space/posts/fffbf793/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/131601663">CSDN链接</a>)</li><li><a href="./%E6%95%B0%E6%8D%AE%E5%BC%82%E5%B8%B8%E5%80%BC%E6%A3%80%E6%B5%8B%E5%88%A9%E5%99%A8%EF%BC%9AGrubbs%E2%80%98%E6%A3%80%E9%AA%8C%E7%9A%84C%E8%AF%AD%E8%A8%80%E5%AE%9E%E7%8E%B0.md">35. 数据异常值检测利器：Grubbs‘检验的C语言实现</a> (<a href="https://wdfk-prog.space/posts/4c10e7de/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/154240201">CSDN链接</a>)</li><li><a href="./%E7%94%B1%E4%BA%8E%20Cortex-M%20%E6%A0%88%E6%9C%AA%E6%BB%A1%E8%B6%B3%208%20%E5%AD%97%E8%8A%82%E5%AF%B9%E9%BD%90%E5%AF%BC%E8%87%B4%E7%9A%84%E9%97%AE%E9%A2%98%E4%B8%8E%E5%8E%9F%E5%9B%A0%E5%88%86%E6%9E%90.md">36. 由于 Cortex-M 栈未满足 8 字节对齐导致的问题与原因分析</a> (<a href="https://wdfk-prog.space/posts/e168822b/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/162413368">CSDN链接</a>)</li><li><a href="./%E7%BB%93%E5%90%88QBoot%E4%B8%8EHPatchLite%E5%AE%9E%E7%8E%B0%E9%AB%98%E6%95%88%E5%B7%AE%E5%88%86%E5%8D%87%E7%BA%A7%EF%BC%88FOTA%EF%BC%89.md">37. 结合QBoot与HPatchLite实现高效差分升级（FOTA）</a> (<a href="https://wdfk-prog.space/posts/39c3100c/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/154239086">CSDN链接</a>)</li><li>[38. 面向 MCU 与 RTOS 的 Newlib、Newlib-nano、&#96;–specs&#x3D;nano](&lt;.&#x2F;%E9%9D%A2%E5%90%91%20MCU%20%E4%B8%8E%20RTOS%20%E7%9A%84%20Newlib%E3%80%81Newlib-nano%E3%80%81%60–specs%3Dnano.specs%60%20%E4%B8%8E%20%60_REENT_SMALL%60%20%E8%AF%B4%E6%98%8E.md&gt;) (<a href="https://wdfk-prog.space/posts/40596f29/">个人博客链接</a>)</li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;MCU学习笔记系列&quot;&gt;&lt;a href=&quot;#MCU学习笔记系列&quot; class=&quot;headerlink&quot; title=&quot;MCU学习笔记系列&quot;&gt;&lt;/a&gt;MCU学习笔记系列&lt;/h1&gt;&lt;ul&gt;
&lt;li&gt;&lt;a href=&quot;./ADC+DMA%20%E7%9B%AE%E6%A0</summary>
      
    
    
    
    <category term="MCU" scheme="https://wdfk-prog.space/categories/MCU/"/>
    
    
    <category term="MCU" scheme="https://wdfk-prog.space/tags/MCU/"/>
    
  </entry>
  
  <entry>
    <title>git学习笔记系列</title>
    <link href="https://wdfk-prog.space/posts/98557f4f/"/>
    <id>https://wdfk-prog.space/posts/98557f4f/</id>
    <published>2026-07-31T07:11:36.000Z</published>
    <updated>2026-08-01T08:04:33.416Z</updated>
    
    <content type="html"><![CDATA[<h1 id="git学习笔记系列"><a href="#git学习笔记系列" class="headerlink" title="git学习笔记系列"></a>git学习笔记系列</h1><ul><li><a href="./GitHub%20%E5%BC%80%E6%BA%90%E9%A1%B9%E7%9B%AE%E5%8F%AF%E4%BB%A5%E7%94%A8%E5%93%AA%E4%BA%9B%E5%85%8D%E8%B4%B9%E7%9A%84%20AI%20%E4%BB%A3%E7%A0%81%E5%AE%A1%E6%A0%B8%E5%B7%A5%E5%85%B7%EF%BC%9FCodeRabbit%E3%80%81Qodo%20OSS%E3%80%81LlamaPReview%20%E6%80%8E%E4%B9%88%E9%80%89.md">1. GitHub 开源项目可以用哪些免费的 AI 代码审核工具？CodeRabbit、Qodo OSS、LlamaPReview 怎么选</a> (<a href="https://wdfk-prog.space/posts/50285399/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/160851250">CSDN链接</a>)</li><li><a href="./GitHub%E4%BE%A6%E6%8E%A2%E6%8A%80%E5%B7%A7%EF%BC%9A%E5%A6%82%E4%BD%95%E7%B2%BE%E7%A1%AE%E6%9F%A5%E6%89%BE%E7%89%B9%E5%AE%9A%E9%82%AE%E7%AE%B1%E5%9C%B0%E5%9D%80%E7%9A%84%E6%89%80%E6%9C%89%E4%BB%A3%E7%A0%81%E6%8F%90%E4%BA%A4(Commit).md">2. GitHub侦探技巧：如何精确查找特定邮箱地址的所有代码提交(Commit)</a> (<a href="https://wdfk-prog.space/posts/3458f8bd/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/154241896">CSDN链接</a>)</li><li><a href="./Git%E5%90%91%E6%9C%AC%E5%9C%B0%E9%9D%9E%E8%A3%B8%E4%BB%93%E5%BA%93%E6%8E%A8%E9%80%81%E7%9A%84%E6%9C%BA%E5%88%B6%E4%B8%8E%E9%94%99%E8%AF%AF%E5%A4%84%E7%90%86.md">3. Git向本地非裸仓库推送的机制与错误处理</a> (<a href="https://wdfk-prog.space/posts/286f3b19/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/152038574">CSDN链接</a>)</li><li><a href="./Git%E5%AD%90%E6%A8%A1%E5%9D%97%EF%BC%88Submodule%EF%BC%89%E5%90%88%E5%B9%B6%E5%86%B2%E7%AA%81%E7%9A%84%E5%8E%9F%E7%90%86%E4%B8%8E%E8%A7%A3%E5%86%B3%E6%96%B9%E6%A1%88.md">4. Git子模块（Submodule）合并冲突的原理与解决方案</a> (<a href="https://wdfk-prog.space/posts/7fc95dfb/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/151557492">CSDN链接</a>)</li><li><a href="./Git%E5%AE%9E%E6%88%98%E6%8C%87%E5%8D%97%EF%BC%9A%E5%A6%82%E4%BD%95%E4%BB%8E%E5%8F%A6%E4%B8%80%E4%B8%AA%E5%88%86%E6%94%AF%E5%90%8C%E6%AD%A5%E6%96%87%E4%BB%B6%E6%97%B6%E5%AE%8C%E6%95%B4%E4%BF%9D%E7%95%99Commit%E6%8F%90%E4%BA%A4%E5%8E%86%E5%8F%B2.md">5. Git实战指南：如何从另一个分支同步文件时完整保留Commit提交历史</a> (<a href="https://wdfk-prog.space/posts/cbdad328/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/156383713">CSDN链接</a>)</li><li><a href="./Git%E6%96%87%E4%BB%B6%E7%8A%B6%E6%80%81%E6%98%BE%E7%A4%BA%E5%BC%82%E5%B8%B8%E7%9A%84%E8%A7%A3%E5%86%B3%E6%96%B9%E6%A1%88.md">6. Git文件状态显示异常的解决方案</a> (<a href="https://wdfk-prog.space/posts/6cf8ce53/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/157329957">CSDN链接</a>)</li><li><a href="./Git%E7%B4%A7%E6%80%A5%E6%95%91%E6%8F%B4%EF%BC%9A%E5%A6%82%E4%BD%95%E7%94%A8%E6%9C%80%E5%BF%AB%E6%96%B9%E6%B3%95%E5%9B%9E%E9%80%80%E4%B8%8A%E4%B8%87%E4%B8%AA%E6%96%87%E4%BB%B6%E4%BF%AE%E6%94%B9%EF%BC%9F.md">7. 修复 Git 仓库损坏全记录</a> (<a href="https://wdfk-prog.space/posts/cbdad328/">个人博客链接</a>)</li><li><a href="./%E4%BF%AE%E5%A4%8D%20Git%20%E4%BB%93%E5%BA%93%E6%8D%9F%E5%9D%8F%E5%85%A8%E8%AE%B0%E5%BD%95.md">8. 修复 Git 仓库损坏全记录</a> (<a href="https://wdfk-prog.space/posts/cbdad328/">个人博客链接</a>)</li><li><a href="./%E5%A6%82%E4%BD%95%E5%B0%86Git%E4%BB%93%E5%BA%93%E4%B8%AD%E7%9A%84%E7%89%B9%E5%AE%9A%E6%96%87%E4%BB%B6%E5%A4%B9%E5%8F%8A%E5%85%B6%E5%8E%86%E5%8F%B2%E5%AE%8C%E6%95%B4%E8%BF%81%E7%A7%BB%E5%88%B0%E5%8F%A6%E4%B8%80%E4%B8%AA%E4%BB%93%E5%BA%93.md">9. 如何将Git仓库中的特定文件夹及其历史完整迁移到另一个仓库</a> (<a href="https://wdfk-prog.space/posts/390f04ae/">个人博客链接</a>)</li><li><a href="./%E5%B0%86%20Git%20%E4%BB%93%E5%BA%93%E6%89%80%E6%9C%89%E6%96%87%E4%BB%B6%E7%A7%BB%E5%85%A5%E5%AD%90%E7%9B%AE%E5%BD%95%E7%9A%84%E6%AD%A3%E7%A1%AE%E5%A7%BF%E5%8A%BF.md">10. 将 Git 仓库所有文件移入子目录的正确姿势</a> (<a href="https://wdfk-prog.space/posts/46fb3bcf/">个人博客链接</a>)</li><li><a href="./%E8%A7%A3%E5%86%B3%20%60git%20cherry-pick%60%20%E5%BC%95%E5%85%A5%E5%A4%A7%E9%87%8F%E6%96%B0%E6%96%87%E4%BB%B6%E7%9A%84%E9%97%AE%E9%A2%98.md">11. 解决 <code>git cherry-pick</code> 引入大量新文件的问题</a> (<a href="https://wdfk-prog.space/posts/c1a906f6/">个人博客链接</a>) (<a href="https://wdfk-prog.blog.csdn.net/article/details/157615583">CSDN链接</a>)</li><li><a href="./%E8%AE%A9%E6%96%87%E4%BB%B6%E2%80%9C%E8%84%B1%E7%A6%BB%E2%80%9D%E7%89%88%E6%9C%AC%E6%8E%A7%E5%88%B6.md">12. 让文件“脱离”版本控制</a> (<a href="https://wdfk-prog.space/posts/d6480f7b/">个人博客链接</a>)</li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;git学习笔记系列&quot;&gt;&lt;a href=&quot;#git学习笔记系列&quot; class=&quot;headerlink&quot; title=&quot;git学习笔记系列&quot;&gt;&lt;/a&gt;git学习笔记系列&lt;/h1&gt;&lt;ul&gt;
&lt;li&gt;&lt;a href=&quot;./GitHub%20%E5%BC%80%E6%BA%</summary>
      
    
    
    
    <category term="git" scheme="https://wdfk-prog.space/categories/git/"/>
    
    
    <category term="git" scheme="https://wdfk-prog.space/tags/git/"/>
    
  </entry>
  
</feed>
